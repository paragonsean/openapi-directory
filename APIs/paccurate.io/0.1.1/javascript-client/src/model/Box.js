/**
 * paccurate.io
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BoxProperties from './BoxProperties';
import Item from './Item';
import Point from './Point';
import RateTable from './RateTable';

/**
 * The Box model module.
 * @module model/Box
 * @version 0.1.1
 */
class Box {
    /**
     * Constructs a new <code>Box</code>.
     * A completed, packed box.
     * @alias module:model/Box
     * @implements module:model/BoxProperties
     * @param dimensions {module:model/Point} the [height,length,width] of the box.
     * @param weightMax {Number} maximum allowable gross weight for the box, i.e., all packed item weights plus the weightTare.
     */
    constructor(dimensions, weightMax) { 
        BoxProperties.initialize(this, dimensions, weightMax);
        Box.initialize(this, dimensions, weightMax);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dimensions, weightMax) { 
        obj['dimensions'] = dimensions;
        obj['weightMax'] = weightMax;
    }

    /**
     * Constructs a <code>Box</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Box} obj Optional instance to populate.
     * @return {module:model/Box} The populated <code>Box</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Box();
            BoxProperties.constructFromObject(data, obj);

            if (data.hasOwnProperty('dimensions')) {
                obj['dimensions'] = ApiClient.convertToType(data['dimensions'], Point);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('rateTable')) {
                obj['rateTable'] = ApiClient.convertToType(data['rateTable'], RateTable);
            }
            if (data.hasOwnProperty('weightMax')) {
                obj['weightMax'] = ApiClient.convertToType(data['weightMax'], 'Number');
            }
            if (data.hasOwnProperty('weightTare')) {
                obj['weightTare'] = ApiClient.convertToType(data['weightTare'], 'Number');
            }
            if (data.hasOwnProperty('dimensionalWeight')) {
                obj['dimensionalWeight'] = ApiClient.convertToType(data['dimensionalWeight'], 'Number');
            }
            if (data.hasOwnProperty('dimensionalWeightUsed')) {
                obj['dimensionalWeightUsed'] = ApiClient.convertToType(data['dimensionalWeightUsed'], 'Boolean');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [Item]);
            }
            if (data.hasOwnProperty('svg')) {
                obj['svg'] = ApiClient.convertToType(data['svg'], 'String');
            }
            if (data.hasOwnProperty('volumeMax')) {
                obj['volumeMax'] = ApiClient.convertToType(data['volumeMax'], 'Number');
            }
            if (data.hasOwnProperty('volumeRemaining')) {
                obj['volumeRemaining'] = ApiClient.convertToType(data['volumeRemaining'], 'Number');
            }
            if (data.hasOwnProperty('volumeUsed')) {
                obj['volumeUsed'] = ApiClient.convertToType(data['volumeUsed'], 'Number');
            }
            if (data.hasOwnProperty('volumeUtilization')) {
                obj['volumeUtilization'] = ApiClient.convertToType(data['volumeUtilization'], 'Number');
            }
            if (data.hasOwnProperty('weightNet')) {
                obj['weightNet'] = ApiClient.convertToType(data['weightNet'], 'Number');
            }
            if (data.hasOwnProperty('weightRemaining')) {
                obj['weightRemaining'] = ApiClient.convertToType(data['weightRemaining'], 'Number');
            }
            if (data.hasOwnProperty('weightUsed')) {
                obj['weightUsed'] = ApiClient.convertToType(data['weightUsed'], 'Number');
            }
            if (data.hasOwnProperty('weightUtilization')) {
                obj['weightUtilization'] = ApiClient.convertToType(data['weightUtilization'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Box</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Box</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Box.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `dimensions`
        if (data['dimensions']) { // data not null
          Point.validateJSON(data['dimensions']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `rateTable`
        if (data['rateTable']) { // data not null
          RateTable.validateJSON(data['rateTable']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                Item.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['svg'] && !(typeof data['svg'] === 'string' || data['svg'] instanceof String)) {
            throw new Error("Expected the field `svg` to be a primitive type in the JSON string but got " + data['svg']);
        }

        return true;
    }


}

Box.RequiredProperties = ["dimensions", "weightMax"];

/**
 * the [height,length,width] of the box.
 * @member {module:model/Point} dimensions
 */
Box.prototype['dimensions'] = undefined;

/**
 * name for the type of box.
 * @member {String} name
 */
Box.prototype['name'] = undefined;

/**
 * Fixed price of the container, in whole units of currency, default USD cents. This can represent the cost of a flat rate carton, the cost of the actual carton materials, or it can include any other flat fees that may need to be added on a <i>per-carton</i> basis, such as handling, accessorial surchages, oversize fees, etc. This value is <i>added</i> to any rate table rates defined for the carton.
 * @member {Number} price
 */
Box.prototype['price'] = undefined;

/**
 * An optional rate table definition for improved carton selection and pricing optimization. Defaults are included using retail rates for FedEx and UPS if carrier and service is provided, but optimization can be improved with more data passed in a carton's specific rate table. Methods are <ol><li>Provide carrier, service, and zone.</li><li>Provide all acceptable weights and prices to use for the carton, similar to actual carrier rate tables.</li><li>Provide the coefficients required for a simple linear weight-dependent pricing model.</li></ol>
 * @member {module:model/RateTable} rateTable
 */
Box.prototype['rateTable'] = undefined;

/**
 * maximum allowable gross weight for the box, i.e., all packed item weights plus the weightTare.
 * @member {Number} weightMax
 */
Box.prototype['weightMax'] = undefined;

/**
 * weight of the container when empty or otherwise unladen, i.e., of the box itself.
 * @member {Number} weightTare
 */
Box.prototype['weightTare'] = undefined;

/**
 * the calculated dimensional weight of this box, if applicable.
 * @member {Number} dimensionalWeight
 */
Box.prototype['dimensionalWeight'] = undefined;

/**
 * whether or not dimensional weight was used for this box.
 * @member {Boolean} dimensionalWeightUsed
 */
Box.prototype['dimensionalWeightUsed'] = undefined;

/**
 * @member {Number} id
 */
Box.prototype['id'] = undefined;

/**
 * @member {Array.<module:model/Item>} items
 */
Box.prototype['items'] = undefined;

/**
 * raw svg of visualization.
 * @member {String} svg
 */
Box.prototype['svg'] = undefined;

/**
 * total volume of the box.
 * @member {Number} volumeMax
 */
Box.prototype['volumeMax'] = undefined;

/**
 * remaining volume of the box.
 * @member {Number} volumeRemaining
 */
Box.prototype['volumeRemaining'] = undefined;

/**
 * utilized volume of the box.
 * @member {Number} volumeUsed
 */
Box.prototype['volumeUsed'] = undefined;

/**
 * percentage of volume utilized by packed items.
 * @member {Number} volumeUtilization
 */
Box.prototype['volumeUtilization'] = undefined;

/**
 * total weight of box's contents, not including the box's empty (tare) weight.
 * @member {Number} weightNet
 */
Box.prototype['weightNet'] = undefined;

/**
 * remaining weight of the box.
 * @member {Number} weightRemaining
 */
Box.prototype['weightRemaining'] = undefined;

/**
 * utilized weight of the box.
 * @member {Number} weightUsed
 */
Box.prototype['weightUsed'] = undefined;

/**
 * percentage of weight utilized by packed items.
 * @member {Number} weightUtilization
 */
Box.prototype['weightUtilization'] = undefined;


// Implement BoxProperties interface:
/**
 * the [height,length,width] of the box.
 * @member {module:model/Point} dimensions
 */
BoxProperties.prototype['dimensions'] = undefined;
/**
 * name for the type of box.
 * @member {String} name
 */
BoxProperties.prototype['name'] = undefined;
/**
 * Fixed price of the container, in whole units of currency, default USD cents. This can represent the cost of a flat rate carton, the cost of the actual carton materials, or it can include any other flat fees that may need to be added on a <i>per-carton</i> basis, such as handling, accessorial surchages, oversize fees, etc. This value is <i>added</i> to any rate table rates defined for the carton.
 * @member {Number} price
 */
BoxProperties.prototype['price'] = undefined;
/**
 * An optional rate table definition for improved carton selection and pricing optimization. Defaults are included using retail rates for FedEx and UPS if carrier and service is provided, but optimization can be improved with more data passed in a carton's specific rate table. Methods are <ol><li>Provide carrier, service, and zone.</li><li>Provide all acceptable weights and prices to use for the carton, similar to actual carrier rate tables.</li><li>Provide the coefficients required for a simple linear weight-dependent pricing model.</li></ol>
 * @member {module:model/RateTable} rateTable
 */
BoxProperties.prototype['rateTable'] = undefined;
/**
 * maximum allowable gross weight for the box, i.e., all packed item weights plus the weightTare.
 * @member {Number} weightMax
 */
BoxProperties.prototype['weightMax'] = undefined;
/**
 * weight of the container when empty or otherwise unladen, i.e., of the box itself.
 * @member {Number} weightTare
 */
BoxProperties.prototype['weightTare'] = undefined;




export default Box;

