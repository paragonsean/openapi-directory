/**
 * FireBrowse Beta API
 * A simple and elegant way to explore cancer data
 *
 * The version of the OpenAPI document: 1.1.38 (2018-02-26 11:01:29 484103261f6ef681a05cf163)
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";

/**
* Analyses service.
* @module api/AnalysesApi
* @version 1.1.38 (2018-02-26 11:01:29 484103261f6ef681a05cf163)
*/
export default class AnalysesApi {

    /**
    * Constructs a new AnalysesApi. 
    * @alias module:api/AnalysesApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the all operation.
     * @callback module:api/AnalysesApi~allCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve all data by genes Gistic2 results.
     * This service provides access to the Gistic2 all_data_by_genes.txt output data. This data is a gene-level table of copy number values for all samples. The returned copy number values are in units (copy number - 2) so that no amplification or deletion is 0, genes with amplifications have positive values, and genes with deletions are negative values. The data are converted from marker level to gene level using the extreme method: a gene is assigned the greatest amplification or the least deletion value among the markers it covers. Results may be filtered by cohort, gene, or barcode, but at least one gene or barcode must be supplied.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<String>} [gene] Comma separated list of gene name(s).
     * @param {Array.<String>} [tcgaParticipantBarcode] Comma separated list of TCGA participant barcodes (e.g. TCGA-GF-A4EO).
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'cohort')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~allCallback} callback The callback function, accepting three arguments: error, data, response
     */
    all(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'gene': this.apiClient.buildCollectionParam(opts['gene'], 'csv'),
        'tcga_participant_barcode': this.apiClient.buildCollectionParam(opts['tcgaParticipantBarcode'], 'csv'),
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/CopyNumber/Genes/All', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the amplified operation.
     * @callback module:api/AnalysesApi~amplifiedCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve Gistic2 significantly amplified genes results.
     * This service provides access to the Gistic2 amp_genes.conf_99.txt output data.  At least 1 gene or cohort must be supplied.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<String>} [gene] Comma separated list of gene name(s).
     * @param {Number} [q] Only return results with Q-value <= given threshold.
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'cohort')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~amplifiedCallback} callback The callback function, accepting three arguments: error, data, response
     */
    amplified(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'gene': this.apiClient.buildCollectionParam(opts['gene'], 'csv'),
        'q': opts['q'],
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/CopyNumber/Genes/Amplified', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the deleted operation.
     * @callback module:api/AnalysesApi~deletedCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve Gistic2 significantly deleted genes results.
     * This service provides access to the Gistic2 del_genes.conf_99.txt output data.  At least 1 gene or cohort must be supplied.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<String>} [gene] Comma separated list of gene name(s).
     * @param {Number} [q] Only return results with Q-value <= given threshold.
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'cohort')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~deletedCallback} callback The callback function, accepting three arguments: error, data, response
     */
    deleted(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'gene': this.apiClient.buildCollectionParam(opts['gene'], 'csv'),
        'q': opts['q'],
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/CopyNumber/Genes/Deleted', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the featureTable operation.
     * @callback module:api/AnalysesApi~featureTableCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve aggregated analysis features table.
     * This service returns part or all of the so-called <strong>feature table</strong>; which aggregates the most important findings across ALL pipelines in the GDAC Firehose analysis workflow into a single table for simple access.  One feature table is created per disease cohort.  Results may be filtered by date or cohort, but at least 1 cohort must be specified here. For more details please visit the <a href=https://confluence.broadinstitute.org/display/GDAC/Documentation\\#Documentation-FeatureTable>online documentation</a>.  Please note that the service is still undergoing experimental evaluation and does not return JSON format.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'tsv')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<module:model/Date>} [date] Select one or more date stamps.
     * @param {Array.<String>} [column] Comma separated list of which data columns/fields to return.
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:api/AnalysesApi~featureTableCallback} callback The callback function, accepting three arguments: error, data, response
     */
    featureTable(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'date': this.apiClient.buildCollectionParam(opts['date'], 'csv'),
        'column': this.apiClient.buildCollectionParam(opts['column'], 'csv'),
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv')
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/FeatureTable', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the focal operation.
     * @callback module:api/AnalysesApi~focalCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve focal data by genes Gistic2 results.
     * This service provides access to the Gistic2 focal_data_by_genes.txt output data. This output is similar to the all_data_by_genes.txt output, but using only focal events with lengths greater than the  focal length cutoff. This data is a gene-level table of copy number values for all samples. The returned copy number values are in units (copy number - 2) so that no amplification or deletion is 0, genes with amplifications have positive values, and genes with deletions are negative values. The data are converted from marker level to gene level using the extreme method: a gene is assigned the greatest amplification or the least deletion value among the markers it covers. Results may be filtered by cohort, gene, and/or barcode, but at least one gene or barcode must be supplied.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<String>} [gene] Comma separated list of gene name(s).
     * @param {Array.<String>} [tcgaParticipantBarcode] Comma separated list of TCGA participant barcodes (e.g. TCGA-GF-A4EO).
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'cohort')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~focalCallback} callback The callback function, accepting three arguments: error, data, response
     */
    focal(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'gene': this.apiClient.buildCollectionParam(opts['gene'], 'csv'),
        'tcga_participant_barcode': this.apiClient.buildCollectionParam(opts['tcgaParticipantBarcode'], 'csv'),
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/CopyNumber/Genes/Focal', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the mAF operation.
     * @callback module:api/AnalysesApi~mAFCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve MutSig final analysis MAF.
     * This service returns columns from the MAF generated by MutSig. Results may be filtered by gene, cohort, tool, or barcode, but at least one gene OR barcode OR cohort must be given.  By default a subset consisting of the most commonly used columns will be returned, but that can be modified with the column parameter. Specifying 'all' in this parameter is a convenient way to return every column of the respective MAF, and has precedence over any any other column selection expression.  The complete list of column names that may be specified is <a href='http://firebrowse.org/api-docs/#!/Metadata/MAFColNames'>given here</a>.  For more information on the mutation data, and how it is processed by Firehose, please consult the <a href=\"https://confluence.broadinstitute.org/display/GDAC/Documentation#Documentation-MutationPipelines\">pipeline documentation</a>.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<module:model/String>} [tool] Narrow search to include only data/results produced by the selected Firehose tool.
     * @param {Array.<String>} [gene] Comma separated list of gene name(s).
     * @param {Array.<String>} [tcgaParticipantBarcode] Comma separated list of TCGA participant barcodes (e.g. TCGA-GF-A4EO).
     * @param {Array.<String>} [column] Comma separated list of which data columns/fields to return.
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'cohort')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~mAFCallback} callback The callback function, accepting three arguments: error, data, response
     */
    mAF(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'tool': this.apiClient.buildCollectionParam(opts['tool'], 'csv'),
        'gene': this.apiClient.buildCollectionParam(opts['gene'], 'csv'),
        'tcga_participant_barcode': this.apiClient.buildCollectionParam(opts['tcgaParticipantBarcode'], 'csv'),
        'column': this.apiClient.buildCollectionParam(opts['column'], 'csv'),
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/Mutation/MAF', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the mRNASeqQuartiles operation.
     * @callback module:api/AnalysesApi~mRNASeqQuartilesCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Returns RNASeq expression quartiles, e.g. suitable for drawing a boxplot.
     * For a given gene compute quartiles and extrema, suitable e.g. for drawing a boxplot (Tukey 1977).  Results may be filtered by cohort, sample type, patient barcode  or characterization protocol, and are returned sorted by cohort.  Note that samples for which no expression value was recorded (e.g. <a href=\"http://firebrowse.org/api/v1/Samples/mRNASeq?&gene=egfr&cohort=SKCM&tcga_participant_barcode=TCGA-GN-A262\">the melanoma sample TCGA-GN-262</a>) are removed prior to calculation.
     * @param {String} gene Enter a single gene name.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<module:model/String>} [protocol] Narrow search to one or more sample characterization protocols from the scrollable list.
     * @param {Array.<module:model/String>} [sampleType] For which type of sample(s) should quartiles be computed?
     * @param {Array.<String>} [exclude] Comma separated list of TCGA participants, identified by barcodes such as TCGA-GF-A4EO, denoting samples to exclude from computation.
     * @param {module:api/AnalysesApi~mRNASeqQuartilesCallback} callback The callback function, accepting three arguments: error, data, response
     */
    mRNASeqQuartiles(gene, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'gene' is set
      if (gene === undefined || gene === null) {
        throw new Error("Missing the required parameter 'gene' when calling mRNASeqQuartiles");
      }

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'gene': gene,
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'protocol': this.apiClient.buildCollectionParam(opts['protocol'], 'csv'),
        'sample_type': this.apiClient.buildCollectionParam(opts['sampleType'], 'csv'),
        'Exclude': this.apiClient.buildCollectionParam(opts['exclude'], 'csv')
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/mRNASeq/Quartiles', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the reports operation.
     * @callback module:api/AnalysesApi~reportsCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve links to summary reports from Firehose analysis runs.
     * This service returns URLs to the analysis result reports for runs of the Broad Institute GDAC Firehose analysis pipeline. At least one year of run reports are maintained in the database, but the reports from the latest run will be returned by default. The set of <a href=\"https://confluence.broadinstitute.org/display/GDAC/Nozzle\">Nozzle</a> reports returned may be filtered by disease cohort, report type and report name.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/Date>} [date] Select one or more date stamps.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<module:model/String>} [name] Narrow search to one or more report names.
     * @param {Array.<module:model/String>} [type] Narrow search to one or more report types.
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'date')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~reportsCallback} callback The callback function, accepting three arguments: error, data, response
     */
    reports(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'date': this.apiClient.buildCollectionParam(opts['date'], 'csv'),
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'name': this.apiClient.buildCollectionParam(opts['name'], 'csv'),
        'type': this.apiClient.buildCollectionParam(opts['type'], 'csv'),
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/Reports', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the sMG operation.
     * @callback module:api/AnalysesApi~sMGCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve Significantly Mutated Genes (SMG).
     * This service provides a list of significantly mutated genes, as scored by MutSig.  It may be filtered by cohort, rank, gene, tool and/or Q-value threshold, but at least one cohort or gene must be supplied.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<module:model/String>} [tool] Narrow search to include only data/results produced by the selected Firehose tool.
     * @param {Number} [rank] Number of significant genes to return.
     * @param {Array.<String>} [gene] Comma separated list of gene name(s).
     * @param {Number} [q] Only return results with Q-value <= given threshold.
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'rank')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~sMGCallback} callback The callback function, accepting three arguments: error, data, response
     */
    sMG(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'tool': this.apiClient.buildCollectionParam(opts['tool'], 'csv'),
        'rank': opts['rank'],
        'gene': this.apiClient.buildCollectionParam(opts['gene'], 'csv'),
        'q': opts['q'],
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/Mutation/SMG', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the thresholded operation.
     * @callback module:api/AnalysesApi~thresholdedCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Retrieve all thresholded by genes Gistic2 results.
     * This service provides access to the Gistic2 all_thresholded_by_genes.txt output data. A gene-level table of discrete amplification and deletion indicators for all samples. A table value of 0 means no amplification or deletion above the threshold. Amplifications are positive numbers: 1 means amplification above the amplification threshold; 2 means amplifications larger to the arm level amplifications observed for the sample. Deletions are represented by negative table values: -1 represents deletion beyond the threshold; -2 means deletions greater than the minimum arm-level deletion observed for the sample. Results maybe filtered by cohort, gene or barcode, but at least one gene or barcode must be supplied.
     * @param {Object} opts Optional parameters
     * @param {module:model/String} [format = 'json')] Format of result.
     * @param {Array.<module:model/String>} [cohort] Narrow search to one or more TCGA disease cohorts from the scrollable list.
     * @param {Array.<String>} [gene] Comma separated list of gene name(s).
     * @param {Array.<String>} [tcgaParticipantBarcode] Comma separated list of TCGA participant barcodes (e.g. TCGA-GF-A4EO).
     * @param {Array.<Number>} [page] Which page (slice) of entire results set should be returned. 
     * @param {Array.<Number>} [pageSize] Number of records per page of results.  Max is 2000.
     * @param {module:model/String} [sortBy = 'cohort')] Which column in the results should be used for sorting paginated results?
     * @param {module:api/AnalysesApi~thresholdedCallback} callback The callback function, accepting three arguments: error, data, response
     */
    thresholded(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'format': opts['format'],
        'cohort': this.apiClient.buildCollectionParam(opts['cohort'], 'csv'),
        'gene': this.apiClient.buildCollectionParam(opts['gene'], 'csv'),
        'tcga_participant_barcode': this.apiClient.buildCollectionParam(opts['tcgaParticipantBarcode'], 'csv'),
        'page': this.apiClient.buildCollectionParam(opts['page'], 'csv'),
        'page_size': this.apiClient.buildCollectionParam(opts['pageSize'], 'csv'),
        'sort_by': opts['sortBy']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = [];
      let returnType = null;
      return this.apiClient.callApi(
        '/Analyses/CopyNumber/Genes/Thresholded', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
