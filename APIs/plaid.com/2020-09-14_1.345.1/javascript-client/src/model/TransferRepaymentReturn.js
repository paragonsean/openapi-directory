/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TransferRepaymentReturn model module.
 * @module model/TransferRepaymentReturn
 * @version 2020-09-14_1.345.1
 */
class TransferRepaymentReturn {
    /**
     * Constructs a new <code>TransferRepaymentReturn</code>.
     * Represents a return on a Guaranteed ACH transfer that is included in the specified repayment.
     * @alias module:model/TransferRepaymentReturn
     * @extends Object
     * @param amount {String} The value of the returned transfer.
     * @param eventId {Number} The unique identifier of the corresponding `returned` transfer event.
     * @param isoCurrencyCode {String} The currency of the repayment, e.g. \"USD\".
     * @param transferId {String} The unique identifier of the guaranteed transfer that was returned.
     */
    constructor(amount, eventId, isoCurrencyCode, transferId) { 
        
        TransferRepaymentReturn.initialize(this, amount, eventId, isoCurrencyCode, transferId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, eventId, isoCurrencyCode, transferId) { 
        obj['amount'] = amount;
        obj['event_id'] = eventId;
        obj['iso_currency_code'] = isoCurrencyCode;
        obj['transfer_id'] = transferId;
    }

    /**
     * Constructs a <code>TransferRepaymentReturn</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransferRepaymentReturn} obj Optional instance to populate.
     * @return {module:model/TransferRepaymentReturn} The populated <code>TransferRepaymentReturn</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransferRepaymentReturn();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('event_id')) {
                obj['event_id'] = ApiClient.convertToType(data['event_id'], 'Number');
            }
            if (data.hasOwnProperty('iso_currency_code')) {
                obj['iso_currency_code'] = ApiClient.convertToType(data['iso_currency_code'], 'String');
            }
            if (data.hasOwnProperty('transfer_id')) {
                obj['transfer_id'] = ApiClient.convertToType(data['transfer_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransferRepaymentReturn</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransferRepaymentReturn</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransferRepaymentReturn.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // ensure the json data is a string
        if (data['iso_currency_code'] && !(typeof data['iso_currency_code'] === 'string' || data['iso_currency_code'] instanceof String)) {
            throw new Error("Expected the field `iso_currency_code` to be a primitive type in the JSON string but got " + data['iso_currency_code']);
        }
        // ensure the json data is a string
        if (data['transfer_id'] && !(typeof data['transfer_id'] === 'string' || data['transfer_id'] instanceof String)) {
            throw new Error("Expected the field `transfer_id` to be a primitive type in the JSON string but got " + data['transfer_id']);
        }

        return true;
    }


}

TransferRepaymentReturn.RequiredProperties = ["amount", "event_id", "iso_currency_code", "transfer_id"];

/**
 * The value of the returned transfer.
 * @member {String} amount
 */
TransferRepaymentReturn.prototype['amount'] = undefined;

/**
 * The unique identifier of the corresponding `returned` transfer event.
 * @member {Number} event_id
 */
TransferRepaymentReturn.prototype['event_id'] = undefined;

/**
 * The currency of the repayment, e.g. \"USD\".
 * @member {String} iso_currency_code
 */
TransferRepaymentReturn.prototype['iso_currency_code'] = undefined;

/**
 * The unique identifier of the guaranteed transfer that was returned.
 * @member {String} transfer_id
 */
TransferRepaymentReturn.prototype['transfer_id'] = undefined;






export default TransferRepaymentReturn;

