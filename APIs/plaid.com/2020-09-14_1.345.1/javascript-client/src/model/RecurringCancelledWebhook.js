/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WebhookEnvironmentValues from './WebhookEnvironmentValues';

/**
 * The RecurringCancelledWebhook model module.
 * @module model/RecurringCancelledWebhook
 * @version 2020-09-14_1.345.1
 */
class RecurringCancelledWebhook {
    /**
     * Constructs a new <code>RecurringCancelledWebhook</code>.
     * Fired when a recurring transfer is cancelled by Plaid.
     * @alias module:model/RecurringCancelledWebhook
     * @extends Object
     * @param environment {module:model/WebhookEnvironmentValues} 
     * @param recurringTransferId {String} Plaid’s unique identifier for a recurring transfer.
     * @param webhookCode {String} `RECURRING_CANCELLED`
     * @param webhookType {String} `TRANSFER`
     */
    constructor(environment, recurringTransferId, webhookCode, webhookType) { 
        
        RecurringCancelledWebhook.initialize(this, environment, recurringTransferId, webhookCode, webhookType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, environment, recurringTransferId, webhookCode, webhookType) { 
        obj['environment'] = environment;
        obj['recurring_transfer_id'] = recurringTransferId;
        obj['webhook_code'] = webhookCode;
        obj['webhook_type'] = webhookType;
    }

    /**
     * Constructs a <code>RecurringCancelledWebhook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecurringCancelledWebhook} obj Optional instance to populate.
     * @return {module:model/RecurringCancelledWebhook} The populated <code>RecurringCancelledWebhook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecurringCancelledWebhook();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('environment')) {
                obj['environment'] = WebhookEnvironmentValues.constructFromObject(data['environment']);
            }
            if (data.hasOwnProperty('recurring_transfer_id')) {
                obj['recurring_transfer_id'] = ApiClient.convertToType(data['recurring_transfer_id'], 'String');
            }
            if (data.hasOwnProperty('webhook_code')) {
                obj['webhook_code'] = ApiClient.convertToType(data['webhook_code'], 'String');
            }
            if (data.hasOwnProperty('webhook_type')) {
                obj['webhook_type'] = ApiClient.convertToType(data['webhook_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecurringCancelledWebhook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecurringCancelledWebhook</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecurringCancelledWebhook.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['recurring_transfer_id'] && !(typeof data['recurring_transfer_id'] === 'string' || data['recurring_transfer_id'] instanceof String)) {
            throw new Error("Expected the field `recurring_transfer_id` to be a primitive type in the JSON string but got " + data['recurring_transfer_id']);
        }
        // ensure the json data is a string
        if (data['webhook_code'] && !(typeof data['webhook_code'] === 'string' || data['webhook_code'] instanceof String)) {
            throw new Error("Expected the field `webhook_code` to be a primitive type in the JSON string but got " + data['webhook_code']);
        }
        // ensure the json data is a string
        if (data['webhook_type'] && !(typeof data['webhook_type'] === 'string' || data['webhook_type'] instanceof String)) {
            throw new Error("Expected the field `webhook_type` to be a primitive type in the JSON string but got " + data['webhook_type']);
        }

        return true;
    }


}

RecurringCancelledWebhook.RequiredProperties = ["environment", "recurring_transfer_id", "webhook_code", "webhook_type"];

/**
 * @member {module:model/WebhookEnvironmentValues} environment
 */
RecurringCancelledWebhook.prototype['environment'] = undefined;

/**
 * Plaid’s unique identifier for a recurring transfer.
 * @member {String} recurring_transfer_id
 */
RecurringCancelledWebhook.prototype['recurring_transfer_id'] = undefined;

/**
 * `RECURRING_CANCELLED`
 * @member {String} webhook_code
 */
RecurringCancelledWebhook.prototype['webhook_code'] = undefined;

/**
 * `TRANSFER`
 * @member {String} webhook_type
 */
RecurringCancelledWebhook.prototype['webhook_type'] = undefined;






export default RecurringCancelledWebhook;

