/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TransferRepayment model module.
 * @module model/TransferRepayment
 * @version 2020-09-14_1.345.1
 */
class TransferRepayment {
    /**
     * Constructs a new <code>TransferRepayment</code>.
     * A repayment is created automatically after one or more guaranteed transactions receive a return. If there are multiple eligible returns in a day, they are batched together into a single repayment.  Repayments are sent over ACH, with funds typically available on the next banking day.
     * @alias module:model/TransferRepayment
     * @extends Object
     * @param amount {String} Decimal amount of the repayment as it appears on your account ledger.
     * @param created {Date} The datetime when the repayment occurred, in RFC 3339 format.
     * @param isoCurrencyCode {String} The currency of the repayment, e.g. \"USD\".
     * @param repaymentId {String} Identifier of the repayment.
     */
    constructor(amount, created, isoCurrencyCode, repaymentId) { 
        
        TransferRepayment.initialize(this, amount, created, isoCurrencyCode, repaymentId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, created, isoCurrencyCode, repaymentId) { 
        obj['amount'] = amount;
        obj['created'] = created;
        obj['iso_currency_code'] = isoCurrencyCode;
        obj['repayment_id'] = repaymentId;
    }

    /**
     * Constructs a <code>TransferRepayment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransferRepayment} obj Optional instance to populate.
     * @return {module:model/TransferRepayment} The populated <code>TransferRepayment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransferRepayment();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Date');
            }
            if (data.hasOwnProperty('iso_currency_code')) {
                obj['iso_currency_code'] = ApiClient.convertToType(data['iso_currency_code'], 'String');
            }
            if (data.hasOwnProperty('repayment_id')) {
                obj['repayment_id'] = ApiClient.convertToType(data['repayment_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransferRepayment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransferRepayment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransferRepayment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // ensure the json data is a string
        if (data['iso_currency_code'] && !(typeof data['iso_currency_code'] === 'string' || data['iso_currency_code'] instanceof String)) {
            throw new Error("Expected the field `iso_currency_code` to be a primitive type in the JSON string but got " + data['iso_currency_code']);
        }
        // ensure the json data is a string
        if (data['repayment_id'] && !(typeof data['repayment_id'] === 'string' || data['repayment_id'] instanceof String)) {
            throw new Error("Expected the field `repayment_id` to be a primitive type in the JSON string but got " + data['repayment_id']);
        }

        return true;
    }


}

TransferRepayment.RequiredProperties = ["amount", "created", "iso_currency_code", "repayment_id"];

/**
 * Decimal amount of the repayment as it appears on your account ledger.
 * @member {String} amount
 */
TransferRepayment.prototype['amount'] = undefined;

/**
 * The datetime when the repayment occurred, in RFC 3339 format.
 * @member {Date} created
 */
TransferRepayment.prototype['created'] = undefined;

/**
 * The currency of the repayment, e.g. \"USD\".
 * @member {String} iso_currency_code
 */
TransferRepayment.prototype['iso_currency_code'] = undefined;

/**
 * Identifier of the repayment.
 * @member {String} repayment_id
 */
TransferRepayment.prototype['repayment_id'] = undefined;






export default TransferRepayment;

