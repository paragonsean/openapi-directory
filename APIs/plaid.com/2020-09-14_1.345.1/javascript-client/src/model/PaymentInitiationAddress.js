/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaymentInitiationAddress model module.
 * @module model/PaymentInitiationAddress
 * @version 2020-09-14_1.345.1
 */
class PaymentInitiationAddress {
    /**
     * Constructs a new <code>PaymentInitiationAddress</code>.
     * The optional address of the payment recipient. Required by most institutions outside of the UK.
     * @alias module:model/PaymentInitiationAddress
     * @extends Object
     * @param city {String} The city where the recipient is located. Maximum of 35 characters.
     * @param country {String} The ISO 3166-1 alpha-2 country code where the recipient is located.
     * @param postalCode {String} The postal code where the recipient is located. Maximum of 16 characters.
     * @param street {Array.<String>} An array of length 1-2 representing the street address where the recipient is located. Maximum of 70 characters.
     */
    constructor(city, country, postalCode, street) { 
        
        PaymentInitiationAddress.initialize(this, city, country, postalCode, street);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, city, country, postalCode, street) { 
        obj['city'] = city;
        obj['country'] = country;
        obj['postal_code'] = postalCode;
        obj['street'] = street;
    }

    /**
     * Constructs a <code>PaymentInitiationAddress</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentInitiationAddress} obj Optional instance to populate.
     * @return {module:model/PaymentInitiationAddress} The populated <code>PaymentInitiationAddress</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentInitiationAddress();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('city')) {
                obj['city'] = ApiClient.convertToType(data['city'], 'String');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
            if (data.hasOwnProperty('postal_code')) {
                obj['postal_code'] = ApiClient.convertToType(data['postal_code'], 'String');
            }
            if (data.hasOwnProperty('street')) {
                obj['street'] = ApiClient.convertToType(data['street'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentInitiationAddress</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentInitiationAddress</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentInitiationAddress.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['city'] && !(typeof data['city'] === 'string' || data['city'] instanceof String)) {
            throw new Error("Expected the field `city` to be a primitive type in the JSON string but got " + data['city']);
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }
        // ensure the json data is a string
        if (data['postal_code'] && !(typeof data['postal_code'] === 'string' || data['postal_code'] instanceof String)) {
            throw new Error("Expected the field `postal_code` to be a primitive type in the JSON string but got " + data['postal_code']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['street'])) {
            throw new Error("Expected the field `street` to be an array in the JSON data but got " + data['street']);
        }

        return true;
    }


}

PaymentInitiationAddress.RequiredProperties = ["city", "country", "postal_code", "street"];

/**
 * The city where the recipient is located. Maximum of 35 characters.
 * @member {String} city
 */
PaymentInitiationAddress.prototype['city'] = undefined;

/**
 * The ISO 3166-1 alpha-2 country code where the recipient is located.
 * @member {String} country
 */
PaymentInitiationAddress.prototype['country'] = undefined;

/**
 * The postal code where the recipient is located. Maximum of 16 characters.
 * @member {String} postal_code
 */
PaymentInitiationAddress.prototype['postal_code'] = undefined;

/**
 * An array of length 1-2 representing the street address where the recipient is located. Maximum of 70 characters.
 * @member {Array.<String>} street
 */
PaymentInitiationAddress.prototype['street'] = undefined;






export default PaymentInitiationAddress;

