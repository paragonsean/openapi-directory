/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Location model module.
 * @module model/Location
 * @version 2020-09-14_1.345.1
 */
class Location {
    /**
     * Constructs a new <code>Location</code>.
     * A representation of where a transaction took place
     * @alias module:model/Location
     * @extends Object
     * @param address {String} The street address where the transaction occurred.
     * @param city {String} The city where the transaction occurred.
     * @param country {String} The ISO 3166-1 alpha-2 country code where the transaction occurred.
     * @param lat {Number} The latitude where the transaction occurred.
     * @param lon {Number} The longitude where the transaction occurred.
     * @param postalCode {String} The postal code where the transaction occurred. In API versions 2018-05-22 and earlier, this field is called `zip`.
     * @param region {String} The region or state where the transaction occurred. In API versions 2018-05-22 and earlier, this field is called `state`.
     * @param storeNumber {String} The merchant defined store number where the transaction occurred.
     */
    constructor(address, city, country, lat, lon, postalCode, region, storeNumber) { 
        
        Location.initialize(this, address, city, country, lat, lon, postalCode, region, storeNumber);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, address, city, country, lat, lon, postalCode, region, storeNumber) { 
        obj['address'] = address;
        obj['city'] = city;
        obj['country'] = country;
        obj['lat'] = lat;
        obj['lon'] = lon;
        obj['postal_code'] = postalCode;
        obj['region'] = region;
        obj['store_number'] = storeNumber;
    }

    /**
     * Constructs a <code>Location</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Location} obj Optional instance to populate.
     * @return {module:model/Location} The populated <code>Location</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Location();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('city')) {
                obj['city'] = ApiClient.convertToType(data['city'], 'String');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
            if (data.hasOwnProperty('lat')) {
                obj['lat'] = ApiClient.convertToType(data['lat'], 'Number');
            }
            if (data.hasOwnProperty('lon')) {
                obj['lon'] = ApiClient.convertToType(data['lon'], 'Number');
            }
            if (data.hasOwnProperty('postal_code')) {
                obj['postal_code'] = ApiClient.convertToType(data['postal_code'], 'String');
            }
            if (data.hasOwnProperty('region')) {
                obj['region'] = ApiClient.convertToType(data['region'], 'String');
            }
            if (data.hasOwnProperty('store_number')) {
                obj['store_number'] = ApiClient.convertToType(data['store_number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Location</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Location</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Location.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['city'] && !(typeof data['city'] === 'string' || data['city'] instanceof String)) {
            throw new Error("Expected the field `city` to be a primitive type in the JSON string but got " + data['city']);
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }
        // ensure the json data is a string
        if (data['postal_code'] && !(typeof data['postal_code'] === 'string' || data['postal_code'] instanceof String)) {
            throw new Error("Expected the field `postal_code` to be a primitive type in the JSON string but got " + data['postal_code']);
        }
        // ensure the json data is a string
        if (data['region'] && !(typeof data['region'] === 'string' || data['region'] instanceof String)) {
            throw new Error("Expected the field `region` to be a primitive type in the JSON string but got " + data['region']);
        }
        // ensure the json data is a string
        if (data['store_number'] && !(typeof data['store_number'] === 'string' || data['store_number'] instanceof String)) {
            throw new Error("Expected the field `store_number` to be a primitive type in the JSON string but got " + data['store_number']);
        }

        return true;
    }


}

Location.RequiredProperties = ["address", "city", "country", "lat", "lon", "postal_code", "region", "store_number"];

/**
 * The street address where the transaction occurred.
 * @member {String} address
 */
Location.prototype['address'] = undefined;

/**
 * The city where the transaction occurred.
 * @member {String} city
 */
Location.prototype['city'] = undefined;

/**
 * The ISO 3166-1 alpha-2 country code where the transaction occurred.
 * @member {String} country
 */
Location.prototype['country'] = undefined;

/**
 * The latitude where the transaction occurred.
 * @member {Number} lat
 */
Location.prototype['lat'] = undefined;

/**
 * The longitude where the transaction occurred.
 * @member {Number} lon
 */
Location.prototype['lon'] = undefined;

/**
 * The postal code where the transaction occurred. In API versions 2018-05-22 and earlier, this field is called `zip`.
 * @member {String} postal_code
 */
Location.prototype['postal_code'] = undefined;

/**
 * The region or state where the transaction occurred. In API versions 2018-05-22 and earlier, this field is called `state`.
 * @member {String} region
 */
Location.prototype['region'] = undefined;

/**
 * The merchant defined store number where the transaction occurred.
 * @member {String} store_number
 */
Location.prototype['store_number'] = undefined;






export default Location;

