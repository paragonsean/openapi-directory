/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnrichTransactionDirection from './EnrichTransactionDirection';
import Enrichments from './Enrichments';

/**
 * The ClientProvidedEnrichedTransaction model module.
 * @module model/ClientProvidedEnrichedTransaction
 * @version 2020-09-14_1.345.1
 */
class ClientProvidedEnrichedTransaction {
    /**
     * Constructs a new <code>ClientProvidedEnrichedTransaction</code>.
     * A client-provided transaction that Plaid has enriched.
     * @alias module:model/ClientProvidedEnrichedTransaction
     * @extends Object
     * @param amount {Number} The absolute value of the transaction (>= 0)
     * @param description {String} The raw description of the transaction.
     * @param enrichments {module:model/Enrichments} 
     * @param id {String} The unique ID for the transaction as provided by you in the request.
     * @param isoCurrencyCode {String} The ISO-4217 currency code of the transaction e.g. USD.
     */
    constructor(amount, description, enrichments, id, isoCurrencyCode) { 
        
        ClientProvidedEnrichedTransaction.initialize(this, amount, description, enrichments, id, isoCurrencyCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, description, enrichments, id, isoCurrencyCode) { 
        obj['amount'] = amount;
        obj['description'] = description;
        obj['enrichments'] = enrichments;
        obj['id'] = id;
        obj['iso_currency_code'] = isoCurrencyCode;
    }

    /**
     * Constructs a <code>ClientProvidedEnrichedTransaction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ClientProvidedEnrichedTransaction} obj Optional instance to populate.
     * @return {module:model/ClientProvidedEnrichedTransaction} The populated <code>ClientProvidedEnrichedTransaction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ClientProvidedEnrichedTransaction();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('direction')) {
                obj['direction'] = EnrichTransactionDirection.constructFromObject(data['direction']);
            }
            if (data.hasOwnProperty('enrichments')) {
                obj['enrichments'] = Enrichments.constructFromObject(data['enrichments']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('iso_currency_code')) {
                obj['iso_currency_code'] = ApiClient.convertToType(data['iso_currency_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ClientProvidedEnrichedTransaction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ClientProvidedEnrichedTransaction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ClientProvidedEnrichedTransaction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['iso_currency_code'] && !(typeof data['iso_currency_code'] === 'string' || data['iso_currency_code'] instanceof String)) {
            throw new Error("Expected the field `iso_currency_code` to be a primitive type in the JSON string but got " + data['iso_currency_code']);
        }

        return true;
    }


}

ClientProvidedEnrichedTransaction.RequiredProperties = ["amount", "description", "enrichments", "id", "iso_currency_code"];

/**
 * The absolute value of the transaction (>= 0)
 * @member {Number} amount
 */
ClientProvidedEnrichedTransaction.prototype['amount'] = undefined;

/**
 * The raw description of the transaction.
 * @member {String} description
 */
ClientProvidedEnrichedTransaction.prototype['description'] = undefined;

/**
 * @member {module:model/EnrichTransactionDirection} direction
 */
ClientProvidedEnrichedTransaction.prototype['direction'] = undefined;

/**
 * @member {module:model/Enrichments} enrichments
 */
ClientProvidedEnrichedTransaction.prototype['enrichments'] = undefined;

/**
 * The unique ID for the transaction as provided by you in the request.
 * @member {String} id
 */
ClientProvidedEnrichedTransaction.prototype['id'] = undefined;

/**
 * The ISO-4217 currency code of the transaction e.g. USD.
 * @member {String} iso_currency_code
 */
ClientProvidedEnrichedTransaction.prototype['iso_currency_code'] = undefined;






export default ClientProvidedEnrichedTransaction;

