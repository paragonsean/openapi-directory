/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WebhookEnvironmentValues from './WebhookEnvironmentValues';

/**
 * The PendingExpirationWebhook model module.
 * @module model/PendingExpirationWebhook
 * @version 2020-09-14_1.345.1
 */
class PendingExpirationWebhook {
    /**
     * Constructs a new <code>PendingExpirationWebhook</code>.
     * Fired when an Item’s access consent is expiring in 7 days. Some Items have explicit expiration times and we try to relay this when possible to reduce service disruption. This can be resolved by having the user go through Link’s update mode.
     * @alias module:model/PendingExpirationWebhook
     * @extends Object
     * @param consentExpirationTime {Date} The date and time at which the Item's access consent will expire, in [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format
     * @param environment {module:model/WebhookEnvironmentValues} 
     * @param itemId {String} The `item_id` of the Item associated with this webhook, warning, or error
     * @param webhookCode {String} `PENDING_EXPIRATION`
     * @param webhookType {String} `ITEM`
     */
    constructor(consentExpirationTime, environment, itemId, webhookCode, webhookType) { 
        
        PendingExpirationWebhook.initialize(this, consentExpirationTime, environment, itemId, webhookCode, webhookType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, consentExpirationTime, environment, itemId, webhookCode, webhookType) { 
        obj['consent_expiration_time'] = consentExpirationTime;
        obj['environment'] = environment;
        obj['item_id'] = itemId;
        obj['webhook_code'] = webhookCode;
        obj['webhook_type'] = webhookType;
    }

    /**
     * Constructs a <code>PendingExpirationWebhook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PendingExpirationWebhook} obj Optional instance to populate.
     * @return {module:model/PendingExpirationWebhook} The populated <code>PendingExpirationWebhook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PendingExpirationWebhook();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('consent_expiration_time')) {
                obj['consent_expiration_time'] = ApiClient.convertToType(data['consent_expiration_time'], 'Date');
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = WebhookEnvironmentValues.constructFromObject(data['environment']);
            }
            if (data.hasOwnProperty('item_id')) {
                obj['item_id'] = ApiClient.convertToType(data['item_id'], 'String');
            }
            if (data.hasOwnProperty('webhook_code')) {
                obj['webhook_code'] = ApiClient.convertToType(data['webhook_code'], 'String');
            }
            if (data.hasOwnProperty('webhook_type')) {
                obj['webhook_type'] = ApiClient.convertToType(data['webhook_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PendingExpirationWebhook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PendingExpirationWebhook</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PendingExpirationWebhook.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['item_id'] && !(typeof data['item_id'] === 'string' || data['item_id'] instanceof String)) {
            throw new Error("Expected the field `item_id` to be a primitive type in the JSON string but got " + data['item_id']);
        }
        // ensure the json data is a string
        if (data['webhook_code'] && !(typeof data['webhook_code'] === 'string' || data['webhook_code'] instanceof String)) {
            throw new Error("Expected the field `webhook_code` to be a primitive type in the JSON string but got " + data['webhook_code']);
        }
        // ensure the json data is a string
        if (data['webhook_type'] && !(typeof data['webhook_type'] === 'string' || data['webhook_type'] instanceof String)) {
            throw new Error("Expected the field `webhook_type` to be a primitive type in the JSON string but got " + data['webhook_type']);
        }

        return true;
    }


}

PendingExpirationWebhook.RequiredProperties = ["consent_expiration_time", "environment", "item_id", "webhook_code", "webhook_type"];

/**
 * The date and time at which the Item's access consent will expire, in [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format
 * @member {Date} consent_expiration_time
 */
PendingExpirationWebhook.prototype['consent_expiration_time'] = undefined;

/**
 * @member {module:model/WebhookEnvironmentValues} environment
 */
PendingExpirationWebhook.prototype['environment'] = undefined;

/**
 * The `item_id` of the Item associated with this webhook, warning, or error
 * @member {String} item_id
 */
PendingExpirationWebhook.prototype['item_id'] = undefined;

/**
 * `PENDING_EXPIRATION`
 * @member {String} webhook_code
 */
PendingExpirationWebhook.prototype['webhook_code'] = undefined;

/**
 * `ITEM`
 * @member {String} webhook_type
 */
PendingExpirationWebhook.prototype['webhook_type'] = undefined;






export default PendingExpirationWebhook;

