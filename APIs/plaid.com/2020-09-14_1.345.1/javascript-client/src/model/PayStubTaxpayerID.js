/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PayStubTaxpayerID model module.
 * @module model/PayStubTaxpayerID
 * @version 2020-09-14_1.345.1
 */
class PayStubTaxpayerID {
    /**
     * Constructs a new <code>PayStubTaxpayerID</code>.
     * Taxpayer ID of the individual receiving the paystub.
     * @alias module:model/PayStubTaxpayerID
     * @extends Object
     * @param idMask {String} ID mask; i.e. last 4 digits of the taxpayer ID.
     * @param idType {String} Type of ID, e.g. 'SSN'.
     */
    constructor(idMask, idType) { 
        
        PayStubTaxpayerID.initialize(this, idMask, idType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, idMask, idType) { 
        obj['id_mask'] = idMask;
        obj['id_type'] = idType;
    }

    /**
     * Constructs a <code>PayStubTaxpayerID</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayStubTaxpayerID} obj Optional instance to populate.
     * @return {module:model/PayStubTaxpayerID} The populated <code>PayStubTaxpayerID</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayStubTaxpayerID();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('id_mask')) {
                obj['id_mask'] = ApiClient.convertToType(data['id_mask'], 'String');
            }
            if (data.hasOwnProperty('id_type')) {
                obj['id_type'] = ApiClient.convertToType(data['id_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayStubTaxpayerID</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayStubTaxpayerID</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayStubTaxpayerID.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id_mask'] && !(typeof data['id_mask'] === 'string' || data['id_mask'] instanceof String)) {
            throw new Error("Expected the field `id_mask` to be a primitive type in the JSON string but got " + data['id_mask']);
        }
        // ensure the json data is a string
        if (data['id_type'] && !(typeof data['id_type'] === 'string' || data['id_type'] instanceof String)) {
            throw new Error("Expected the field `id_type` to be a primitive type in the JSON string but got " + data['id_type']);
        }

        return true;
    }


}

PayStubTaxpayerID.RequiredProperties = ["id_mask", "id_type"];

/**
 * ID mask; i.e. last 4 digits of the taxpayer ID.
 * @member {String} id_mask
 */
PayStubTaxpayerID.prototype['id_mask'] = undefined;

/**
 * Type of ID, e.g. 'SSN'.
 * @member {String} id_type
 */
PayStubTaxpayerID.prototype['id_type'] = undefined;






export default PayStubTaxpayerID;

