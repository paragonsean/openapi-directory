/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WatchlistScreeningHit from './WatchlistScreeningHit';

/**
 * The WatchlistScreeningIndividualHitListResponse model module.
 * @module model/WatchlistScreeningIndividualHitListResponse
 * @version 2020-09-14_1.345.1
 */
class WatchlistScreeningIndividualHitListResponse {
    /**
     * Constructs a new <code>WatchlistScreeningIndividualHitListResponse</code>.
     * Paginated list of individual watchlist screening hits
     * @alias module:model/WatchlistScreeningIndividualHitListResponse
     * @extends Object
     * @param nextCursor {String} An identifier that determines which page of results you receive.
     * @param requestId {String} A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive.
     * @param watchlistScreeningHits {Array.<module:model/WatchlistScreeningHit>} List of individual watchlist screening hits
     */
    constructor(nextCursor, requestId, watchlistScreeningHits) { 
        
        WatchlistScreeningIndividualHitListResponse.initialize(this, nextCursor, requestId, watchlistScreeningHits);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, nextCursor, requestId, watchlistScreeningHits) { 
        obj['next_cursor'] = nextCursor;
        obj['request_id'] = requestId;
        obj['watchlist_screening_hits'] = watchlistScreeningHits;
    }

    /**
     * Constructs a <code>WatchlistScreeningIndividualHitListResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WatchlistScreeningIndividualHitListResponse} obj Optional instance to populate.
     * @return {module:model/WatchlistScreeningIndividualHitListResponse} The populated <code>WatchlistScreeningIndividualHitListResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WatchlistScreeningIndividualHitListResponse();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('next_cursor')) {
                obj['next_cursor'] = ApiClient.convertToType(data['next_cursor'], 'String');
            }
            if (data.hasOwnProperty('request_id')) {
                obj['request_id'] = ApiClient.convertToType(data['request_id'], 'String');
            }
            if (data.hasOwnProperty('watchlist_screening_hits')) {
                obj['watchlist_screening_hits'] = ApiClient.convertToType(data['watchlist_screening_hits'], [WatchlistScreeningHit]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WatchlistScreeningIndividualHitListResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WatchlistScreeningIndividualHitListResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WatchlistScreeningIndividualHitListResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['next_cursor'] && !(typeof data['next_cursor'] === 'string' || data['next_cursor'] instanceof String)) {
            throw new Error("Expected the field `next_cursor` to be a primitive type in the JSON string but got " + data['next_cursor']);
        }
        // ensure the json data is a string
        if (data['request_id'] && !(typeof data['request_id'] === 'string' || data['request_id'] instanceof String)) {
            throw new Error("Expected the field `request_id` to be a primitive type in the JSON string but got " + data['request_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['watchlist_screening_hits'])) {
            throw new Error("Expected the field `watchlist_screening_hits` to be an array in the JSON data but got " + data['watchlist_screening_hits']);
        }

        return true;
    }


}

WatchlistScreeningIndividualHitListResponse.RequiredProperties = ["next_cursor", "request_id", "watchlist_screening_hits"];

/**
 * An identifier that determines which page of results you receive.
 * @member {String} next_cursor
 */
WatchlistScreeningIndividualHitListResponse.prototype['next_cursor'] = undefined;

/**
 * A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive.
 * @member {String} request_id
 */
WatchlistScreeningIndividualHitListResponse.prototype['request_id'] = undefined;

/**
 * List of individual watchlist screening hits
 * @member {Array.<module:model/WatchlistScreeningHit>} watchlist_screening_hits
 */
WatchlistScreeningIndividualHitListResponse.prototype['watchlist_screening_hits'] = undefined;






export default WatchlistScreeningIndividualHitListResponse;

