/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PhoneNumber model module.
 * @module model/PhoneNumber
 * @version 2020-09-14_1.345.1
 */
class PhoneNumber {
    /**
     * Constructs a new <code>PhoneNumber</code>.
     * A phone number
     * @alias module:model/PhoneNumber
     * @extends Object
     * @param data {String} The phone number.
     * @param primary {Boolean} When `true`, identifies the phone number as the primary number on an account.
     * @param type {module:model/PhoneNumber.TypeEnum} The type of phone number.
     */
    constructor(data, primary, type) { 
        
        PhoneNumber.initialize(this, data, primary, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, primary, type) { 
        obj['data'] = data;
        obj['primary'] = primary;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>PhoneNumber</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PhoneNumber} obj Optional instance to populate.
     * @return {module:model/PhoneNumber} The populated <code>PhoneNumber</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PhoneNumber();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], 'String');
            }
            if (data.hasOwnProperty('primary')) {
                obj['primary'] = ApiClient.convertToType(data['primary'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PhoneNumber</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PhoneNumber</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PhoneNumber.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['data'] && !(typeof data['data'] === 'string' || data['data'] instanceof String)) {
            throw new Error("Expected the field `data` to be a primitive type in the JSON string but got " + data['data']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

PhoneNumber.RequiredProperties = ["data", "primary", "type"];

/**
 * The phone number.
 * @member {String} data
 */
PhoneNumber.prototype['data'] = undefined;

/**
 * When `true`, identifies the phone number as the primary number on an account.
 * @member {Boolean} primary
 */
PhoneNumber.prototype['primary'] = undefined;

/**
 * The type of phone number.
 * @member {module:model/PhoneNumber.TypeEnum} type
 */
PhoneNumber.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
PhoneNumber['TypeEnum'] = {

    /**
     * value: "home"
     * @const
     */
    "home": "home",

    /**
     * value: "work"
     * @const
     */
    "work": "work",

    /**
     * value: "office"
     * @const
     */
    "office": "office",

    /**
     * value: "mobile"
     * @const
     */
    "mobile": "mobile",

    /**
     * value: "mobile1"
     * @const
     */
    "mobile1": "mobile1",

    /**
     * value: "other"
     * @const
     */
    "other": "other"
};



export default PhoneNumber;

