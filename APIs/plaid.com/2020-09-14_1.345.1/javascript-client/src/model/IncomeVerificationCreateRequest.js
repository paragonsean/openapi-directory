/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IncomeVerificationCreateRequestOptions from './IncomeVerificationCreateRequestOptions';

/**
 * The IncomeVerificationCreateRequest model module.
 * @module model/IncomeVerificationCreateRequest
 * @version 2020-09-14_1.345.1
 */
class IncomeVerificationCreateRequest {
    /**
     * Constructs a new <code>IncomeVerificationCreateRequest</code>.
     * IncomeVerificationCreateRequest defines the request schema for &#x60;/income/verification/create&#x60;
     * @alias module:model/IncomeVerificationCreateRequest
     * @param webhook {String} The URL endpoint to which Plaid should send webhooks related to the progress of the income verification process.
     */
    constructor(webhook) { 
        
        IncomeVerificationCreateRequest.initialize(this, webhook);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, webhook) { 
        obj['webhook'] = webhook;
    }

    /**
     * Constructs a <code>IncomeVerificationCreateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IncomeVerificationCreateRequest} obj Optional instance to populate.
     * @return {module:model/IncomeVerificationCreateRequest} The populated <code>IncomeVerificationCreateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IncomeVerificationCreateRequest();

            if (data.hasOwnProperty('client_id')) {
                obj['client_id'] = ApiClient.convertToType(data['client_id'], 'String');
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = IncomeVerificationCreateRequestOptions.constructFromObject(data['options']);
            }
            if (data.hasOwnProperty('precheck_id')) {
                obj['precheck_id'] = ApiClient.convertToType(data['precheck_id'], 'String');
            }
            if (data.hasOwnProperty('secret')) {
                obj['secret'] = ApiClient.convertToType(data['secret'], 'String');
            }
            if (data.hasOwnProperty('webhook')) {
                obj['webhook'] = ApiClient.convertToType(data['webhook'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IncomeVerificationCreateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IncomeVerificationCreateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IncomeVerificationCreateRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['client_id'] && !(typeof data['client_id'] === 'string' || data['client_id'] instanceof String)) {
            throw new Error("Expected the field `client_id` to be a primitive type in the JSON string but got " + data['client_id']);
        }
        // validate the optional field `options`
        if (data['options']) { // data not null
          IncomeVerificationCreateRequestOptions.validateJSON(data['options']);
        }
        // ensure the json data is a string
        if (data['precheck_id'] && !(typeof data['precheck_id'] === 'string' || data['precheck_id'] instanceof String)) {
            throw new Error("Expected the field `precheck_id` to be a primitive type in the JSON string but got " + data['precheck_id']);
        }
        // ensure the json data is a string
        if (data['secret'] && !(typeof data['secret'] === 'string' || data['secret'] instanceof String)) {
            throw new Error("Expected the field `secret` to be a primitive type in the JSON string but got " + data['secret']);
        }
        // ensure the json data is a string
        if (data['webhook'] && !(typeof data['webhook'] === 'string' || data['webhook'] instanceof String)) {
            throw new Error("Expected the field `webhook` to be a primitive type in the JSON string but got " + data['webhook']);
        }

        return true;
    }


}

IncomeVerificationCreateRequest.RequiredProperties = ["webhook"];

/**
 * Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body.
 * @member {String} client_id
 */
IncomeVerificationCreateRequest.prototype['client_id'] = undefined;

/**
 * @member {module:model/IncomeVerificationCreateRequestOptions} options
 */
IncomeVerificationCreateRequest.prototype['options'] = undefined;

/**
 * The ID of a precheck created with `/income/verification/precheck`. Will be used to improve conversion of the income verification flow.
 * @member {String} precheck_id
 */
IncomeVerificationCreateRequest.prototype['precheck_id'] = undefined;

/**
 * Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body.
 * @member {String} secret
 */
IncomeVerificationCreateRequest.prototype['secret'] = undefined;

/**
 * The URL endpoint to which Plaid should send webhooks related to the progress of the income verification process.
 * @member {String} webhook
 */
IncomeVerificationCreateRequest.prototype['webhook'] = undefined;






export default IncomeVerificationCreateRequest;

