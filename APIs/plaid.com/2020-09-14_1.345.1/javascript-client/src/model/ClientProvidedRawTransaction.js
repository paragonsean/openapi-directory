/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ClientProvidedRawTransaction model module.
 * @module model/ClientProvidedRawTransaction
 * @version 2020-09-14_1.345.1
 */
class ClientProvidedRawTransaction {
    /**
     * Constructs a new <code>ClientProvidedRawTransaction</code>.
     * A client-provided transaction for Plaid to enhance.
     * @alias module:model/ClientProvidedRawTransaction
     * @extends Object
     * @param amount {Number} The value of the transaction with direction. (NOTE: this will affect enrichment results, so directions are important):.   Negative (-) for credits (e.g., incoming transfers, refunds)   Positive (+) for debits (e.g., purchases, fees, outgoing transfers)
     * @param description {String} The raw description of the transaction.
     * @param id {String} A unique ID for the transaction used to help you tie data back to your systems.
     * @param isoCurrencyCode {String} The ISO-4217 currency code of the transaction e.g. USD.
     */
    constructor(amount, description, id, isoCurrencyCode) { 
        
        ClientProvidedRawTransaction.initialize(this, amount, description, id, isoCurrencyCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, description, id, isoCurrencyCode) { 
        obj['amount'] = amount;
        obj['description'] = description;
        obj['id'] = id;
        obj['iso_currency_code'] = isoCurrencyCode;
    }

    /**
     * Constructs a <code>ClientProvidedRawTransaction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ClientProvidedRawTransaction} obj Optional instance to populate.
     * @return {module:model/ClientProvidedRawTransaction} The populated <code>ClientProvidedRawTransaction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ClientProvidedRawTransaction();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('iso_currency_code')) {
                obj['iso_currency_code'] = ApiClient.convertToType(data['iso_currency_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ClientProvidedRawTransaction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ClientProvidedRawTransaction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ClientProvidedRawTransaction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['iso_currency_code'] && !(typeof data['iso_currency_code'] === 'string' || data['iso_currency_code'] instanceof String)) {
            throw new Error("Expected the field `iso_currency_code` to be a primitive type in the JSON string but got " + data['iso_currency_code']);
        }

        return true;
    }


}

ClientProvidedRawTransaction.RequiredProperties = ["amount", "description", "id", "iso_currency_code"];

/**
 * The value of the transaction with direction. (NOTE: this will affect enrichment results, so directions are important):.   Negative (-) for credits (e.g., incoming transfers, refunds)   Positive (+) for debits (e.g., purchases, fees, outgoing transfers)
 * @member {Number} amount
 */
ClientProvidedRawTransaction.prototype['amount'] = undefined;

/**
 * The raw description of the transaction.
 * @member {String} description
 */
ClientProvidedRawTransaction.prototype['description'] = undefined;

/**
 * A unique ID for the transaction used to help you tie data back to your systems.
 * @member {String} id
 */
ClientProvidedRawTransaction.prototype['id'] = undefined;

/**
 * The ISO-4217 currency code of the transaction e.g. USD.
 * @member {String} iso_currency_code
 */
ClientProvidedRawTransaction.prototype['iso_currency_code'] = undefined;






export default ClientProvidedRawTransaction;

