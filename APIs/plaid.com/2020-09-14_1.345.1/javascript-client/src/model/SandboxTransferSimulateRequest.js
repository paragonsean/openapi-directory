/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransferFailure from './TransferFailure';

/**
 * The SandboxTransferSimulateRequest model module.
 * @module model/SandboxTransferSimulateRequest
 * @version 2020-09-14_1.345.1
 */
class SandboxTransferSimulateRequest {
    /**
     * Constructs a new <code>SandboxTransferSimulateRequest</code>.
     * Defines the request schema for &#x60;/sandbox/transfer/simulate&#x60;
     * @alias module:model/SandboxTransferSimulateRequest
     * @param eventType {String} The asynchronous event to be simulated. May be: `posted`, `settled`, `failed`, or `returned`.  An error will be returned if the event type is incompatible with the current transfer status. Compatible status --> event type transitions include:  `pending` --> `failed`  `pending` --> `posted`  `posted` --> `returned`  `posted` --> `settled` 
     * @param transferId {String} Plaid’s unique identifier for a transfer.
     */
    constructor(eventType, transferId) { 
        
        SandboxTransferSimulateRequest.initialize(this, eventType, transferId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, eventType, transferId) { 
        obj['event_type'] = eventType;
        obj['transfer_id'] = transferId;
    }

    /**
     * Constructs a <code>SandboxTransferSimulateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SandboxTransferSimulateRequest} obj Optional instance to populate.
     * @return {module:model/SandboxTransferSimulateRequest} The populated <code>SandboxTransferSimulateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SandboxTransferSimulateRequest();

            if (data.hasOwnProperty('client_id')) {
                obj['client_id'] = ApiClient.convertToType(data['client_id'], 'String');
            }
            if (data.hasOwnProperty('event_type')) {
                obj['event_type'] = ApiClient.convertToType(data['event_type'], 'String');
            }
            if (data.hasOwnProperty('failure_reason')) {
                obj['failure_reason'] = TransferFailure.constructFromObject(data['failure_reason']);
            }
            if (data.hasOwnProperty('secret')) {
                obj['secret'] = ApiClient.convertToType(data['secret'], 'String');
            }
            if (data.hasOwnProperty('transfer_id')) {
                obj['transfer_id'] = ApiClient.convertToType(data['transfer_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SandboxTransferSimulateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SandboxTransferSimulateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SandboxTransferSimulateRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['client_id'] && !(typeof data['client_id'] === 'string' || data['client_id'] instanceof String)) {
            throw new Error("Expected the field `client_id` to be a primitive type in the JSON string but got " + data['client_id']);
        }
        // ensure the json data is a string
        if (data['event_type'] && !(typeof data['event_type'] === 'string' || data['event_type'] instanceof String)) {
            throw new Error("Expected the field `event_type` to be a primitive type in the JSON string but got " + data['event_type']);
        }
        // ensure the json data is a string
        if (data['secret'] && !(typeof data['secret'] === 'string' || data['secret'] instanceof String)) {
            throw new Error("Expected the field `secret` to be a primitive type in the JSON string but got " + data['secret']);
        }
        // ensure the json data is a string
        if (data['transfer_id'] && !(typeof data['transfer_id'] === 'string' || data['transfer_id'] instanceof String)) {
            throw new Error("Expected the field `transfer_id` to be a primitive type in the JSON string but got " + data['transfer_id']);
        }

        return true;
    }


}

SandboxTransferSimulateRequest.RequiredProperties = ["event_type", "transfer_id"];

/**
 * Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body.
 * @member {String} client_id
 */
SandboxTransferSimulateRequest.prototype['client_id'] = undefined;

/**
 * The asynchronous event to be simulated. May be: `posted`, `settled`, `failed`, or `returned`.  An error will be returned if the event type is incompatible with the current transfer status. Compatible status --> event type transitions include:  `pending` --> `failed`  `pending` --> `posted`  `posted` --> `returned`  `posted` --> `settled` 
 * @member {String} event_type
 */
SandboxTransferSimulateRequest.prototype['event_type'] = undefined;

/**
 * @member {module:model/TransferFailure} failure_reason
 */
SandboxTransferSimulateRequest.prototype['failure_reason'] = undefined;

/**
 * Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body.
 * @member {String} secret
 */
SandboxTransferSimulateRequest.prototype['secret'] = undefined;

/**
 * Plaid’s unique identifier for a transfer.
 * @member {String} transfer_id
 */
SandboxTransferSimulateRequest.prototype['transfer_id'] = undefined;






export default SandboxTransferSimulateRequest;

