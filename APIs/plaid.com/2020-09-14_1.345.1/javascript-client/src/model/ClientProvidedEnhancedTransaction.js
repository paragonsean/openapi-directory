/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Enhancements from './Enhancements';

/**
 * The ClientProvidedEnhancedTransaction model module.
 * @module model/ClientProvidedEnhancedTransaction
 * @version 2020-09-14_1.345.1
 */
class ClientProvidedEnhancedTransaction {
    /**
     * Constructs a new <code>ClientProvidedEnhancedTransaction</code>.
     * A client-provided transaction that Plaid has enhanced.
     * @alias module:model/ClientProvidedEnhancedTransaction
     * @extends Object
     * @param amount {Number} The value of the transaction, denominated in the account's currency, as stated in `iso_currency_code`. Positive values when money moves out of the account; negative values when money moves in. For example, debit card purchases are positive; credit card payments, direct deposits, and refunds are negative.
     * @param description {String} The raw description of the transaction.
     * @param enhancements {module:model/Enhancements} 
     * @param id {String} Unique transaction identifier to tie transactions back to clients' systems.
     * @param isoCurrencyCode {String} The ISO-4217 currency code of the transaction.
     */
    constructor(amount, description, enhancements, id, isoCurrencyCode) { 
        
        ClientProvidedEnhancedTransaction.initialize(this, amount, description, enhancements, id, isoCurrencyCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, description, enhancements, id, isoCurrencyCode) { 
        obj['amount'] = amount;
        obj['description'] = description;
        obj['enhancements'] = enhancements;
        obj['id'] = id;
        obj['iso_currency_code'] = isoCurrencyCode;
    }

    /**
     * Constructs a <code>ClientProvidedEnhancedTransaction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ClientProvidedEnhancedTransaction} obj Optional instance to populate.
     * @return {module:model/ClientProvidedEnhancedTransaction} The populated <code>ClientProvidedEnhancedTransaction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ClientProvidedEnhancedTransaction();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('enhancements')) {
                obj['enhancements'] = Enhancements.constructFromObject(data['enhancements']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('iso_currency_code')) {
                obj['iso_currency_code'] = ApiClient.convertToType(data['iso_currency_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ClientProvidedEnhancedTransaction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ClientProvidedEnhancedTransaction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ClientProvidedEnhancedTransaction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['iso_currency_code'] && !(typeof data['iso_currency_code'] === 'string' || data['iso_currency_code'] instanceof String)) {
            throw new Error("Expected the field `iso_currency_code` to be a primitive type in the JSON string but got " + data['iso_currency_code']);
        }

        return true;
    }


}

ClientProvidedEnhancedTransaction.RequiredProperties = ["amount", "description", "enhancements", "id", "iso_currency_code"];

/**
 * The value of the transaction, denominated in the account's currency, as stated in `iso_currency_code`. Positive values when money moves out of the account; negative values when money moves in. For example, debit card purchases are positive; credit card payments, direct deposits, and refunds are negative.
 * @member {Number} amount
 */
ClientProvidedEnhancedTransaction.prototype['amount'] = undefined;

/**
 * The raw description of the transaction.
 * @member {String} description
 */
ClientProvidedEnhancedTransaction.prototype['description'] = undefined;

/**
 * @member {module:model/Enhancements} enhancements
 */
ClientProvidedEnhancedTransaction.prototype['enhancements'] = undefined;

/**
 * Unique transaction identifier to tie transactions back to clients' systems.
 * @member {String} id
 */
ClientProvidedEnhancedTransaction.prototype['id'] = undefined;

/**
 * The ISO-4217 currency code of the transaction.
 * @member {String} iso_currency_code
 */
ClientProvidedEnhancedTransaction.prototype['iso_currency_code'] = undefined;






export default ClientProvidedEnhancedTransaction;

