/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BankTransferSweep model module.
 * @module model/BankTransferSweep
 * @version 2020-09-14_1.345.1
 */
class BankTransferSweep {
    /**
     * Constructs a new <code>BankTransferSweep</code>.
     * BankTransferSweep describes a sweep transfer.
     * @alias module:model/BankTransferSweep
     * @extends Object
     * @param amount {String} The amount of the sweep.
     * @param createdAt {Date} The datetime when the sweep occurred, in RFC 3339 format.
     * @param id {String} Identifier of the sweep.
     * @param isoCurrencyCode {String} The currency of the sweep, e.g. \"USD\".
     */
    constructor(amount, createdAt, id, isoCurrencyCode) { 
        
        BankTransferSweep.initialize(this, amount, createdAt, id, isoCurrencyCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, createdAt, id, isoCurrencyCode) { 
        obj['amount'] = amount;
        obj['created_at'] = createdAt;
        obj['id'] = id;
        obj['iso_currency_code'] = isoCurrencyCode;
    }

    /**
     * Constructs a <code>BankTransferSweep</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BankTransferSweep} obj Optional instance to populate.
     * @return {module:model/BankTransferSweep} The populated <code>BankTransferSweep</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BankTransferSweep();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('iso_currency_code')) {
                obj['iso_currency_code'] = ApiClient.convertToType(data['iso_currency_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BankTransferSweep</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BankTransferSweep</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BankTransferSweep.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['iso_currency_code'] && !(typeof data['iso_currency_code'] === 'string' || data['iso_currency_code'] instanceof String)) {
            throw new Error("Expected the field `iso_currency_code` to be a primitive type in the JSON string but got " + data['iso_currency_code']);
        }

        return true;
    }


}

BankTransferSweep.RequiredProperties = ["amount", "created_at", "id", "iso_currency_code"];

/**
 * The amount of the sweep.
 * @member {String} amount
 */
BankTransferSweep.prototype['amount'] = undefined;

/**
 * The datetime when the sweep occurred, in RFC 3339 format.
 * @member {Date} created_at
 */
BankTransferSweep.prototype['created_at'] = undefined;

/**
 * Identifier of the sweep.
 * @member {String} id
 */
BankTransferSweep.prototype['id'] = undefined;

/**
 * The currency of the sweep, e.g. \"USD\".
 * @member {String} iso_currency_code
 */
BankTransferSweep.prototype['iso_currency_code'] = undefined;






export default BankTransferSweep;

