/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WatchlistScreeningIndividualReviewCreateRequest model module.
 * @module model/WatchlistScreeningIndividualReviewCreateRequest
 * @version 2020-09-14_1.345.1
 */
class WatchlistScreeningIndividualReviewCreateRequest {
    /**
     * Constructs a new <code>WatchlistScreeningIndividualReviewCreateRequest</code>.
     * Request input for creating a screening review
     * @alias module:model/WatchlistScreeningIndividualReviewCreateRequest
     * @param confirmedHits {Array.<String>} Hits to mark as a true positive after thorough manual review. These hits will never recur or be updated once dismissed. In most cases, confirmed hits indicate that the customer should be rejected.
     * @param dismissedHits {Array.<String>} Hits to mark as a false positive after thorough manual review. These hits will never recur or be updated once dismissed.
     * @param watchlistScreeningId {String} ID of the associated screening.
     */
    constructor(confirmedHits, dismissedHits, watchlistScreeningId) { 
        
        WatchlistScreeningIndividualReviewCreateRequest.initialize(this, confirmedHits, dismissedHits, watchlistScreeningId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, confirmedHits, dismissedHits, watchlistScreeningId) { 
        obj['confirmed_hits'] = confirmedHits;
        obj['dismissed_hits'] = dismissedHits;
        obj['watchlist_screening_id'] = watchlistScreeningId;
    }

    /**
     * Constructs a <code>WatchlistScreeningIndividualReviewCreateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WatchlistScreeningIndividualReviewCreateRequest} obj Optional instance to populate.
     * @return {module:model/WatchlistScreeningIndividualReviewCreateRequest} The populated <code>WatchlistScreeningIndividualReviewCreateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WatchlistScreeningIndividualReviewCreateRequest();

            if (data.hasOwnProperty('client_id')) {
                obj['client_id'] = ApiClient.convertToType(data['client_id'], 'String');
            }
            if (data.hasOwnProperty('comment')) {
                obj['comment'] = ApiClient.convertToType(data['comment'], 'String');
            }
            if (data.hasOwnProperty('confirmed_hits')) {
                obj['confirmed_hits'] = ApiClient.convertToType(data['confirmed_hits'], ['String']);
            }
            if (data.hasOwnProperty('dismissed_hits')) {
                obj['dismissed_hits'] = ApiClient.convertToType(data['dismissed_hits'], ['String']);
            }
            if (data.hasOwnProperty('secret')) {
                obj['secret'] = ApiClient.convertToType(data['secret'], 'String');
            }
            if (data.hasOwnProperty('watchlist_screening_id')) {
                obj['watchlist_screening_id'] = ApiClient.convertToType(data['watchlist_screening_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WatchlistScreeningIndividualReviewCreateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WatchlistScreeningIndividualReviewCreateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WatchlistScreeningIndividualReviewCreateRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['client_id'] && !(typeof data['client_id'] === 'string' || data['client_id'] instanceof String)) {
            throw new Error("Expected the field `client_id` to be a primitive type in the JSON string but got " + data['client_id']);
        }
        // ensure the json data is a string
        if (data['comment'] && !(typeof data['comment'] === 'string' || data['comment'] instanceof String)) {
            throw new Error("Expected the field `comment` to be a primitive type in the JSON string but got " + data['comment']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['confirmed_hits'])) {
            throw new Error("Expected the field `confirmed_hits` to be an array in the JSON data but got " + data['confirmed_hits']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['dismissed_hits'])) {
            throw new Error("Expected the field `dismissed_hits` to be an array in the JSON data but got " + data['dismissed_hits']);
        }
        // ensure the json data is a string
        if (data['secret'] && !(typeof data['secret'] === 'string' || data['secret'] instanceof String)) {
            throw new Error("Expected the field `secret` to be a primitive type in the JSON string but got " + data['secret']);
        }
        // ensure the json data is a string
        if (data['watchlist_screening_id'] && !(typeof data['watchlist_screening_id'] === 'string' || data['watchlist_screening_id'] instanceof String)) {
            throw new Error("Expected the field `watchlist_screening_id` to be a primitive type in the JSON string but got " + data['watchlist_screening_id']);
        }

        return true;
    }


}

WatchlistScreeningIndividualReviewCreateRequest.RequiredProperties = ["confirmed_hits", "dismissed_hits", "watchlist_screening_id"];

/**
 * Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body.
 * @member {String} client_id
 */
WatchlistScreeningIndividualReviewCreateRequest.prototype['client_id'] = undefined;

/**
 * A comment submitted by a team member as part of reviewing a watchlist screening.
 * @member {String} comment
 */
WatchlistScreeningIndividualReviewCreateRequest.prototype['comment'] = undefined;

/**
 * Hits to mark as a true positive after thorough manual review. These hits will never recur or be updated once dismissed. In most cases, confirmed hits indicate that the customer should be rejected.
 * @member {Array.<String>} confirmed_hits
 */
WatchlistScreeningIndividualReviewCreateRequest.prototype['confirmed_hits'] = undefined;

/**
 * Hits to mark as a false positive after thorough manual review. These hits will never recur or be updated once dismissed.
 * @member {Array.<String>} dismissed_hits
 */
WatchlistScreeningIndividualReviewCreateRequest.prototype['dismissed_hits'] = undefined;

/**
 * Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body.
 * @member {String} secret
 */
WatchlistScreeningIndividualReviewCreateRequest.prototype['secret'] = undefined;

/**
 * ID of the associated screening.
 * @member {String} watchlist_screening_id
 */
WatchlistScreeningIndividualReviewCreateRequest.prototype['watchlist_screening_id'] = undefined;






export default WatchlistScreeningIndividualReviewCreateRequest;

