/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.345.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WebhookEnvironmentValues from './WebhookEnvironmentValues';

/**
 * The VerificationExpiredWebhook model module.
 * @module model/VerificationExpiredWebhook
 * @version 2020-09-14_1.345.1
 */
class VerificationExpiredWebhook {
    /**
     * Constructs a new <code>VerificationExpiredWebhook</code>.
     * Fired when an Item was not verified via automated micro-deposits after seven days since the automated micro-deposit was made.
     * @alias module:model/VerificationExpiredWebhook
     * @extends Object
     * @param accountId {String} The `account_id` of the account associated with the webhook
     * @param environment {module:model/WebhookEnvironmentValues} 
     * @param itemId {String} The `item_id` of the Item associated with this webhook, warning, or error
     * @param webhookCode {String} `VERIFICATION_EXPIRED`
     * @param webhookType {String} `AUTH`
     */
    constructor(accountId, environment, itemId, webhookCode, webhookType) { 
        
        VerificationExpiredWebhook.initialize(this, accountId, environment, itemId, webhookCode, webhookType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountId, environment, itemId, webhookCode, webhookType) { 
        obj['account_id'] = accountId;
        obj['environment'] = environment;
        obj['item_id'] = itemId;
        obj['webhook_code'] = webhookCode;
        obj['webhook_type'] = webhookType;
    }

    /**
     * Constructs a <code>VerificationExpiredWebhook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VerificationExpiredWebhook} obj Optional instance to populate.
     * @return {module:model/VerificationExpiredWebhook} The populated <code>VerificationExpiredWebhook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VerificationExpiredWebhook();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = WebhookEnvironmentValues.constructFromObject(data['environment']);
            }
            if (data.hasOwnProperty('item_id')) {
                obj['item_id'] = ApiClient.convertToType(data['item_id'], 'String');
            }
            if (data.hasOwnProperty('webhook_code')) {
                obj['webhook_code'] = ApiClient.convertToType(data['webhook_code'], 'String');
            }
            if (data.hasOwnProperty('webhook_type')) {
                obj['webhook_type'] = ApiClient.convertToType(data['webhook_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VerificationExpiredWebhook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VerificationExpiredWebhook</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VerificationExpiredWebhook.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['item_id'] && !(typeof data['item_id'] === 'string' || data['item_id'] instanceof String)) {
            throw new Error("Expected the field `item_id` to be a primitive type in the JSON string but got " + data['item_id']);
        }
        // ensure the json data is a string
        if (data['webhook_code'] && !(typeof data['webhook_code'] === 'string' || data['webhook_code'] instanceof String)) {
            throw new Error("Expected the field `webhook_code` to be a primitive type in the JSON string but got " + data['webhook_code']);
        }
        // ensure the json data is a string
        if (data['webhook_type'] && !(typeof data['webhook_type'] === 'string' || data['webhook_type'] instanceof String)) {
            throw new Error("Expected the field `webhook_type` to be a primitive type in the JSON string but got " + data['webhook_type']);
        }

        return true;
    }


}

VerificationExpiredWebhook.RequiredProperties = ["account_id", "environment", "item_id", "webhook_code", "webhook_type"];

/**
 * The `account_id` of the account associated with the webhook
 * @member {String} account_id
 */
VerificationExpiredWebhook.prototype['account_id'] = undefined;

/**
 * @member {module:model/WebhookEnvironmentValues} environment
 */
VerificationExpiredWebhook.prototype['environment'] = undefined;

/**
 * The `item_id` of the Item associated with this webhook, warning, or error
 * @member {String} item_id
 */
VerificationExpiredWebhook.prototype['item_id'] = undefined;

/**
 * `VERIFICATION_EXPIRED`
 * @member {String} webhook_code
 */
VerificationExpiredWebhook.prototype['webhook_code'] = undefined;

/**
 * `AUTH`
 * @member {String} webhook_type
 */
VerificationExpiredWebhook.prototype['webhook_type'] = undefined;






export default VerificationExpiredWebhook;

