# item_api

ItemApi - JavaScript client for item_api
Please make sure you use the correct version of the APIs for your use case. To find out the appropriate version, go to the API Docs  drop down on the menu.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 3.0.1
- Package version: 3.0.1
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install item_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your item_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var ItemApi = require('item_api');


var api = new ItemApi.Version2Api()
var WM_CONSUMER_CHANNEL_TYPE = "'SWAGGER_CHANNEL_TYPE'"; // {String} Channel Type
var WM_CONSUMER_ID = "WM_CONSUMER_ID_example"; // {String} Your Consumer ID
var WM_SEC_TIMESTAMP = "'Auto populated'"; // {String} Epoch timestamp
var WM_SEC_AUTH_SIGNATURE = "'Auto populated'"; // {String} Authentication signature
var WM_SVC_NAME = "'Walmart Marketplace'"; // {String} The Service name
var WM_QOS_CORRELATION_ID = "'123456abcdef'"; // {String} A Transaction ID
var file = "/path/to/file"; // {File} Feed File to upload
var opts = {
  'feedType': "'item'" // {String} Feed Type
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.v2doPostMultiPart(WM_CONSUMER_CHANNEL_TYPE, WM_CONSUMER_ID, WM_SEC_TIMESTAMP, WM_SEC_AUTH_SIGNATURE, WM_SVC_NAME, WM_QOS_CORRELATION_ID, file, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://developer.walmart.com/proxy/item-api-doc-app/rest*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*ItemApi.Version2Api* | [**v2doPostMultiPart**](docs/Version2Api.md#v2doPostMultiPart) | **POST** /v2/feeds | Upload an item feed
*ItemApi.Version2Api* | [**v2getAllItemsStatus**](docs/Version2Api.md#v2getAllItemsStatus) | **GET** /v2/feeds/{feedId} | Get status of an item within a feed
*ItemApi.Version2Api* | [**v2getFeedItemStatus**](docs/Version2Api.md#v2getFeedItemStatus) | **GET** /v2/feeds | Get status of an item feed
*ItemApi.Version3Api* | [**v3doPostMultiPart**](docs/Version3Api.md#v3doPostMultiPart) | **POST** /v3/feeds | Upload an item feed
*ItemApi.Version3Api* | [**v3getAllItemsStatus**](docs/Version3Api.md#v3getAllItemsStatus) | **GET** /v3/feeds/{feedId} | Get status of an item within a feed
*ItemApi.Version3Api* | [**v3getFeedItemStatus**](docs/Version3Api.md#v3getFeedItemStatus) | **GET** /v3/feeds | Get status of an item feed


## Documentation for Models



## Documentation for Authorization

Endpoints do not require authorization.

