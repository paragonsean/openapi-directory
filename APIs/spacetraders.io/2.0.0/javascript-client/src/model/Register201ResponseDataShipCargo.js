/**
 * SpaceTraders API
 * SpaceTraders is an open-universe game and learning platform that offers a set of HTTP endpoints to control a fleet of ships and explore a multiplayer universe.  The API is documented using [OpenAPI](https://github.com/SpaceTradersAPI/api-docs). You can send your first request right here in your browser to check the status of the game server.  ```json http {   \"method\": \"GET\",   \"url\": \"https://api.spacetraders.io/v2\", } ```  Unlike a traditional game, SpaceTraders does not have a first-party client or app to play the game. Instead, you can use the API to build your own client, write a script to automate your ships, or try an app built by the community.  We have a [Discord channel](https://discord.com/invite/jh6zurdWk5) where you can share your projects, ask questions, and get help from other players.   
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: joel@spacetraders.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Register201ResponseDataShipCargoInventoryInner from './Register201ResponseDataShipCargoInventoryInner';

/**
 * The Register201ResponseDataShipCargo model module.
 * @module model/Register201ResponseDataShipCargo
 * @version 2.0.0
 */
class Register201ResponseDataShipCargo {
    /**
     * Constructs a new <code>Register201ResponseDataShipCargo</code>.
     * @alias module:model/Register201ResponseDataShipCargo
     * @param capacity {Number} The max number of items that can be stored in the cargo hold.
     * @param inventory {Array.<module:model/Register201ResponseDataShipCargoInventoryInner>} The items currently in the cargo hold.
     * @param units {Number} The number of items currently stored in the cargo hold.
     */
    constructor(capacity, inventory, units) { 
        
        Register201ResponseDataShipCargo.initialize(this, capacity, inventory, units);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, capacity, inventory, units) { 
        obj['capacity'] = capacity;
        obj['inventory'] = inventory;
        obj['units'] = units;
    }

    /**
     * Constructs a <code>Register201ResponseDataShipCargo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Register201ResponseDataShipCargo} obj Optional instance to populate.
     * @return {module:model/Register201ResponseDataShipCargo} The populated <code>Register201ResponseDataShipCargo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Register201ResponseDataShipCargo();

            if (data.hasOwnProperty('capacity')) {
                obj['capacity'] = ApiClient.convertToType(data['capacity'], 'Number');
            }
            if (data.hasOwnProperty('inventory')) {
                obj['inventory'] = ApiClient.convertToType(data['inventory'], [Register201ResponseDataShipCargoInventoryInner]);
            }
            if (data.hasOwnProperty('units')) {
                obj['units'] = ApiClient.convertToType(data['units'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Register201ResponseDataShipCargo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Register201ResponseDataShipCargo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Register201ResponseDataShipCargo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['inventory']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['inventory'])) {
                throw new Error("Expected the field `inventory` to be an array in the JSON data but got " + data['inventory']);
            }
            // validate the optional field `inventory` (array)
            for (const item of data['inventory']) {
                Register201ResponseDataShipCargoInventoryInner.validateJSON(item);
            };
        }

        return true;
    }


}

Register201ResponseDataShipCargo.RequiredProperties = ["capacity", "inventory", "units"];

/**
 * The max number of items that can be stored in the cargo hold.
 * @member {Number} capacity
 */
Register201ResponseDataShipCargo.prototype['capacity'] = undefined;

/**
 * The items currently in the cargo hold.
 * @member {Array.<module:model/Register201ResponseDataShipCargoInventoryInner>} inventory
 */
Register201ResponseDataShipCargo.prototype['inventory'] = undefined;

/**
 * The number of items currently stored in the cargo hold.
 * @member {Number} units
 */
Register201ResponseDataShipCargo.prototype['units'] = undefined;






export default Register201ResponseDataShipCargo;

