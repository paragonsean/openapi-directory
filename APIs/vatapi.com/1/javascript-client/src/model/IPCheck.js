/**
 * VAT API
 * A developer friendly API to help your business achieve VAT compliance
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CountryRates from './CountryRates';

/**
 * The IPCheck model module.
 * @module model/IPCheck
 * @version 1
 */
class IPCheck {
    /**
     * Constructs a new <code>IPCheck</code>.
     * @alias module:model/IPCheck
     * @param country {String} The country name
     * @param countryCode {String} The 2 digit country code
     * @param rates {module:model/CountryRates} 
     * @param status {Number} Response code
     * @param vatApplies {String} TRUE or FALSE, depending if country is within the EU.
     */
    constructor(country, countryCode, rates, status, vatApplies) { 
        
        IPCheck.initialize(this, country, countryCode, rates, status, vatApplies);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, country, countryCode, rates, status, vatApplies) { 
        obj['country'] = country;
        obj['country_code'] = countryCode;
        obj['rates'] = rates;
        obj['status'] = status;
        obj['vat_applies'] = vatApplies;
    }

    /**
     * Constructs a <code>IPCheck</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IPCheck} obj Optional instance to populate.
     * @return {module:model/IPCheck} The populated <code>IPCheck</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IPCheck();

            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
            if (data.hasOwnProperty('country_code')) {
                obj['country_code'] = ApiClient.convertToType(data['country_code'], 'String');
            }
            if (data.hasOwnProperty('rates')) {
                obj['rates'] = CountryRates.constructFromObject(data['rates']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'Number');
            }
            if (data.hasOwnProperty('vat_applies')) {
                obj['vat_applies'] = ApiClient.convertToType(data['vat_applies'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IPCheck</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IPCheck</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IPCheck.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }
        // ensure the json data is a string
        if (data['country_code'] && !(typeof data['country_code'] === 'string' || data['country_code'] instanceof String)) {
            throw new Error("Expected the field `country_code` to be a primitive type in the JSON string but got " + data['country_code']);
        }
        // validate the optional field `rates`
        if (data['rates']) { // data not null
          CountryRates.validateJSON(data['rates']);
        }
        // ensure the json data is a string
        if (data['vat_applies'] && !(typeof data['vat_applies'] === 'string' || data['vat_applies'] instanceof String)) {
            throw new Error("Expected the field `vat_applies` to be a primitive type in the JSON string but got " + data['vat_applies']);
        }

        return true;
    }


}

IPCheck.RequiredProperties = ["country", "country_code", "rates", "status", "vat_applies"];

/**
 * The country name
 * @member {String} country
 */
IPCheck.prototype['country'] = undefined;

/**
 * The 2 digit country code
 * @member {String} country_code
 */
IPCheck.prototype['country_code'] = undefined;

/**
 * @member {module:model/CountryRates} rates
 */
IPCheck.prototype['rates'] = undefined;

/**
 * Response code
 * @member {Number} status
 */
IPCheck.prototype['status'] = undefined;

/**
 * TRUE or FALSE, depending if country is within the EU.
 * @member {String} vat_applies
 */
IPCheck.prototype['vat_applies'] = undefined;






export default IPCheck;

