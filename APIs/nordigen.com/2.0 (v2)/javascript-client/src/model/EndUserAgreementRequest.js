/**
 * Nordigen Account Information Services API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.0 (v2)
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EndUserAgreementRequest model module.
 * @module model/EndUserAgreementRequest
 * @version 2.0 (v2)
 */
class EndUserAgreementRequest {
    /**
     * Constructs a new <code>EndUserAgreementRequest</code>.
     * Represents an end-user agreement.
     * @alias module:model/EndUserAgreementRequest
     * @param institutionId {String} an Institution ID for this EUA
     */
    constructor(institutionId) { 
        
        EndUserAgreementRequest.initialize(this, institutionId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, institutionId) { 
        obj['access_valid_for_days'] = 90;
        obj['institution_id'] = institutionId;
        obj['max_historical_days'] = 90;
    }

    /**
     * Constructs a <code>EndUserAgreementRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EndUserAgreementRequest} obj Optional instance to populate.
     * @return {module:model/EndUserAgreementRequest} The populated <code>EndUserAgreementRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EndUserAgreementRequest();

            if (data.hasOwnProperty('access_scope')) {
                obj['access_scope'] = ApiClient.convertToType(data['access_scope'], [[Object]]);
            }
            if (data.hasOwnProperty('access_valid_for_days')) {
                obj['access_valid_for_days'] = ApiClient.convertToType(data['access_valid_for_days'], 'Number');
            }
            if (data.hasOwnProperty('institution_id')) {
                obj['institution_id'] = ApiClient.convertToType(data['institution_id'], 'String');
            }
            if (data.hasOwnProperty('max_historical_days')) {
                obj['max_historical_days'] = ApiClient.convertToType(data['max_historical_days'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EndUserAgreementRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EndUserAgreementRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EndUserAgreementRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['access_scope'])) {
            throw new Error("Expected the field `access_scope` to be an array in the JSON data but got " + data['access_scope']);
        }
        // ensure the json data is a string
        if (data['institution_id'] && !(typeof data['institution_id'] === 'string' || data['institution_id'] instanceof String)) {
            throw new Error("Expected the field `institution_id` to be a primitive type in the JSON string but got " + data['institution_id']);
        }

        return true;
    }


}

EndUserAgreementRequest.RequiredProperties = ["institution_id"];

/**
 * Array containing one or several values of ['balances', 'details', 'transactions']
 * @member {Array.<Array.<Object>>} access_scope
 */
EndUserAgreementRequest.prototype['access_scope'] = undefined;

/**
 * Number of days from acceptance that the access can be used.
 * @member {Number} access_valid_for_days
 * @default 90
 */
EndUserAgreementRequest.prototype['access_valid_for_days'] = 90;

/**
 * an Institution ID for this EUA
 * @member {String} institution_id
 */
EndUserAgreementRequest.prototype['institution_id'] = undefined;

/**
 * Maximum number of days of transaction data to retrieve.
 * @member {Number} max_historical_days
 * @default 90
 */
EndUserAgreementRequest.prototype['max_historical_days'] = 90;






export default EndUserAgreementRequest;

