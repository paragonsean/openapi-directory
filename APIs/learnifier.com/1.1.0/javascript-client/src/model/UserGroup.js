/**
 * Learnifier
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserGroup model module.
 * @module model/UserGroup
 * @version 1.1.0
 */
class UserGroup {
    /**
     * Constructs a new <code>UserGroup</code>.
     * @alias module:model/UserGroup
     * @param name {String} The name of the User Group.
     */
    constructor(name) { 
        
        UserGroup.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>UserGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserGroup} obj Optional instance to populate.
     * @return {module:model/UserGroup} The populated <code>UserGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserGroup();

            if (data.hasOwnProperty('children')) {
                obj['children'] = ApiClient.convertToType(data['children'], [UserGroup]);
            }
            if (data.hasOwnProperty('globalId')) {
                obj['globalId'] = ApiClient.convertToType(data['globalId'], 'Number');
            }
            if (data.hasOwnProperty('groupId')) {
                obj['groupId'] = ApiClient.convertToType(data['groupId'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('parent')) {
                obj['parent'] = ApiClient.convertToType(data['parent'], 'Number');
            }
            if (data.hasOwnProperty('userGroup')) {
                obj['userGroup'] = ApiClient.convertToType(data['userGroup'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['children']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['children'])) {
                throw new Error("Expected the field `children` to be an array in the JSON data but got " + data['children']);
            }
            // validate the optional field `children` (array)
            for (const item of data['children']) {
                UserGroup.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

UserGroup.RequiredProperties = ["name"];

/**
 * Children User Groups.
 * @member {Array.<module:model/UserGroup>} children
 */
UserGroup.prototype['children'] = undefined;

/**
 * Global group ID if this group is based on a global group, otherwise null.
 * @member {Number} globalId
 */
UserGroup.prototype['globalId'] = undefined;

/**
 * Unique identifier representing a User Group. Id numbers are never reused.
 * @member {Number} groupId
 */
UserGroup.prototype['groupId'] = undefined;

/**
 * The name of the User Group.
 * @member {String} name
 */
UserGroup.prototype['name'] = undefined;

/**
 * Unique identifier of the parent or *null* if there is no parent.
 * @member {Number} parent
 */
UserGroup.prototype['parent'] = undefined;

/**
 * If of internal user group object that holds the members of this group.
 * @member {Number} userGroup
 */
UserGroup.prototype['userGroup'] = undefined;






export default UserGroup;

