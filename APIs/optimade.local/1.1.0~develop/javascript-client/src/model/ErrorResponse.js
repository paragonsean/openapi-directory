/**
 * OPTIMADE API
 * The [Open Databases Integration for Materials Design (OPTIMADE) consortium](https://www.optimade.org/) aims to make materials databases interoperational by developing a common REST API.  This specification is generated using [`optimade-python-tools`](https://github.com/Materials-Consortia/optimade-python-tools/tree/v0.16.0) v0.16.0.
 *
 * The version of the OpenAPI document: 1.1.0~develop
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Data from './Data';
import JsonApi from './JsonApi';
import OptimadeError from './OptimadeError';
import Resource from './Resource';
import ResponseMeta from './ResponseMeta';
import ToplevelLinks from './ToplevelLinks';

/**
 * The ErrorResponse model module.
 * @module model/ErrorResponse
 * @version 1.1.0~develop
 */
class ErrorResponse {
    /**
     * Constructs a new <code>ErrorResponse</code>.
     * errors MUST be present and data MUST be skipped
     * @alias module:model/ErrorResponse
     * @param errors {Array.<module:model/OptimadeError>} A list of OPTIMADE-specific JSON API error objects, where the field detail MUST be present.
     * @param meta {module:model/ResponseMeta} A meta object containing non-standard information
     */
    constructor(errors, meta) { 
        
        ErrorResponse.initialize(this, errors, meta);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, errors, meta) { 
        obj['errors'] = errors;
        obj['meta'] = meta;
    }

    /**
     * Constructs a <code>ErrorResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ErrorResponse} obj Optional instance to populate.
     * @return {module:model/ErrorResponse} The populated <code>ErrorResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ErrorResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = Data.constructFromObject(data['data']);
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [OptimadeError]);
            }
            if (data.hasOwnProperty('included')) {
                obj['included'] = ApiClient.convertToType(data['included'], [Resource]);
            }
            if (data.hasOwnProperty('jsonapi')) {
                obj['jsonapi'] = ApiClient.convertToType(data['jsonapi'], JsonApi);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = ApiClient.convertToType(data['links'], ToplevelLinks);
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = ApiClient.convertToType(data['meta'], ResponseMeta);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ErrorResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ErrorResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ErrorResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `data`
        if (data['data']) { // data not null
          Data.validateJSON(data['data']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                OptimadeError.validateJSON(item);
            };
        }
        if (data['included']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['included'])) {
                throw new Error("Expected the field `included` to be an array in the JSON data but got " + data['included']);
            }
            // validate the optional field `included` (array)
            for (const item of data['included']) {
                Resource.validateJSON(item);
            };
        }
        // validate the optional field `jsonapi`
        if (data['jsonapi']) { // data not null
          JsonApi.validateJSON(data['jsonapi']);
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          ToplevelLinks.validateJSON(data['links']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          ResponseMeta.validateJSON(data['meta']);
        }

        return true;
    }


}

ErrorResponse.RequiredProperties = ["errors", "meta"];

/**
 * @member {module:model/Data} data
 */
ErrorResponse.prototype['data'] = undefined;

/**
 * A list of OPTIMADE-specific JSON API error objects, where the field detail MUST be present.
 * @member {Array.<module:model/OptimadeError>} errors
 */
ErrorResponse.prototype['errors'] = undefined;

/**
 * A list of unique included resources
 * @member {Array.<module:model/Resource>} included
 */
ErrorResponse.prototype['included'] = undefined;

/**
 * Information about the JSON API used
 * @member {module:model/JsonApi} jsonapi
 */
ErrorResponse.prototype['jsonapi'] = undefined;

/**
 * Links associated with the primary data or errors
 * @member {module:model/ToplevelLinks} links
 */
ErrorResponse.prototype['links'] = undefined;

/**
 * A meta object containing non-standard information
 * @member {module:model/ResponseMeta} meta
 */
ErrorResponse.prototype['meta'] = undefined;






export default ErrorResponse;

