/**
 * Magento B2B
 * Magento Commerce is the leading provider of open omnichannel innovation.
 *
 * The version of the OpenAPI document: 2.2.10
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIGuestCartsCartIdGiftMessageItemIdApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIGuestCartsCartIdGiftMessageItemIdApi::OAIGuestCartsCartIdGiftMessageItemIdApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIGuestCartsCartIdGiftMessageItemIdApi::~OAIGuestCartsCartIdGiftMessageItemIdApi() {
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://example.com/rest/default"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("giftMessageGuestItemRepositoryV1GetGet", defaultConf);
    _serverIndices.insert("giftMessageGuestItemRepositoryV1GetGet", 0);
    _serverConfigs.insert("giftMessageGuestItemRepositoryV1SavePost", defaultConf);
    _serverIndices.insert("giftMessageGuestItemRepositoryV1SavePost", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIGuestCartsCartIdGiftMessageItemIdApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIGuestCartsCartIdGiftMessageItemIdApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::setUsername(const QString &username) {
    _username = username;
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::setPassword(const QString &password) {
    _password = password;
}


void OAIGuestCartsCartIdGiftMessageItemIdApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIGuestCartsCartIdGiftMessageItemIdApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIGuestCartsCartIdGiftMessageItemIdApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIGuestCartsCartIdGiftMessageItemIdApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIGuestCartsCartIdGiftMessageItemIdApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIGuestCartsCartIdGiftMessageItemIdApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIGuestCartsCartIdGiftMessageItemIdApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::giftMessageGuestItemRepositoryV1GetGet(const QString &cart_id, const qint32 &item_id) {
    QString fullPath = QString(_serverConfigs["giftMessageGuestItemRepositoryV1GetGet"][_serverIndices.value("giftMessageGuestItemRepositoryV1GetGet")].URL()+"/V1/guest-carts/{cartId}/gift-message/{itemId}");
    
    
    {
        QString cart_idPathParam("{");
        cart_idPathParam.append("cartId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "cartId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"cartId"+pathSuffix : pathPrefix;
        fullPath.replace(cart_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(cart_id)));
    }
    
    {
        QString item_idPathParam("{");
        item_idPathParam.append("itemId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "itemId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"itemId"+pathSuffix : pathPrefix;
        fullPath.replace(item_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(item_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIGuestCartsCartIdGiftMessageItemIdApi::giftMessageGuestItemRepositoryV1GetGetCallback);
    connect(this, &OAIGuestCartsCartIdGiftMessageItemIdApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::giftMessageGuestItemRepositoryV1GetGetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIGift_message_data_message_interface output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT giftMessageGuestItemRepositoryV1GetGetSignal(output);
        Q_EMIT giftMessageGuestItemRepositoryV1GetGetSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT giftMessageGuestItemRepositoryV1GetGetSignalE(output, error_type, error_str);
        Q_EMIT giftMessageGuestItemRepositoryV1GetGetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT giftMessageGuestItemRepositoryV1GetGetSignalError(output, error_type, error_str);
        Q_EMIT giftMessageGuestItemRepositoryV1GetGetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::giftMessageGuestItemRepositoryV1SavePost(const QString &cart_id, const qint32 &item_id, const ::OpenAPI::OptionalParam<OAIGiftMessageCartRepositoryV1SavePost_request> &oai_gift_message_cart_repository_v1_save_post_request) {
    QString fullPath = QString(_serverConfigs["giftMessageGuestItemRepositoryV1SavePost"][_serverIndices.value("giftMessageGuestItemRepositoryV1SavePost")].URL()+"/V1/guest-carts/{cartId}/gift-message/{itemId}");
    
    
    {
        QString cart_idPathParam("{");
        cart_idPathParam.append("cartId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "cartId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"cartId"+pathSuffix : pathPrefix;
        fullPath.replace(cart_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(cart_id)));
    }
    
    {
        QString item_idPathParam("{");
        item_idPathParam.append("itemId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "itemId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"itemId"+pathSuffix : pathPrefix;
        fullPath.replace(item_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(item_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (oai_gift_message_cart_repository_v1_save_post_request.hasValue()){

        
        QByteArray output = oai_gift_message_cart_repository_v1_save_post_request.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIGuestCartsCartIdGiftMessageItemIdApi::giftMessageGuestItemRepositoryV1SavePostCallback);
    connect(this, &OAIGuestCartsCartIdGiftMessageItemIdApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::giftMessageGuestItemRepositoryV1SavePostCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    bool output;
    ::OpenAPI::fromStringValue(QString(worker->response), output);
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT giftMessageGuestItemRepositoryV1SavePostSignal(output);
        Q_EMIT giftMessageGuestItemRepositoryV1SavePostSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT giftMessageGuestItemRepositoryV1SavePostSignalE(output, error_type, error_str);
        Q_EMIT giftMessageGuestItemRepositoryV1SavePostSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT giftMessageGuestItemRepositoryV1SavePostSignalError(output, error_type, error_str);
        Q_EMIT giftMessageGuestItemRepositoryV1SavePostSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIGuestCartsCartIdGiftMessageItemIdApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
