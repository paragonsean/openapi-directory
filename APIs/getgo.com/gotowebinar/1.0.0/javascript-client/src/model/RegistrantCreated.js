/**
 * GoToWebinar
 * The GoToWebinar API provides seamless integration of webinar registrant and attendee data into your existing infrastructure or third-party applications. The ability to register participants, as well as pull lists of registrants and attendees for a webinar, allows organizers to manage the flow of information between their primary applications without manual intervention.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: developer-support@logmein.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RegistrantCreated model module.
 * @module model/RegistrantCreated
 * @version 1.0.0
 */
class RegistrantCreated {
    /**
     * Constructs a new <code>RegistrantCreated</code>.
     * Describes a newly created webinar registrant.
     * @alias module:model/RegistrantCreated
     * @param joinUrl {String} The URL the registrant will use to join the webinar.
     * @param registrantKey {Number} The registrant's key
     */
    constructor(joinUrl, registrantKey) { 
        
        RegistrantCreated.initialize(this, joinUrl, registrantKey);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, joinUrl, registrantKey) { 
        obj['joinUrl'] = joinUrl;
        obj['registrantKey'] = registrantKey;
    }

    /**
     * Constructs a <code>RegistrantCreated</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RegistrantCreated} obj Optional instance to populate.
     * @return {module:model/RegistrantCreated} The populated <code>RegistrantCreated</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RegistrantCreated();

            if (data.hasOwnProperty('joinUrl')) {
                obj['joinUrl'] = ApiClient.convertToType(data['joinUrl'], 'String');
            }
            if (data.hasOwnProperty('registrantKey')) {
                obj['registrantKey'] = ApiClient.convertToType(data['registrantKey'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RegistrantCreated</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RegistrantCreated</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RegistrantCreated.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['joinUrl'] && !(typeof data['joinUrl'] === 'string' || data['joinUrl'] instanceof String)) {
            throw new Error("Expected the field `joinUrl` to be a primitive type in the JSON string but got " + data['joinUrl']);
        }

        return true;
    }


}

RegistrantCreated.RequiredProperties = ["joinUrl", "registrantKey"];

/**
 * The URL the registrant will use to join the webinar.
 * @member {String} joinUrl
 */
RegistrantCreated.prototype['joinUrl'] = undefined;

/**
 * The registrant's key
 * @member {Number} registrantKey
 */
RegistrantCreated.prototype['registrantKey'] = undefined;






export default RegistrantCreated;

