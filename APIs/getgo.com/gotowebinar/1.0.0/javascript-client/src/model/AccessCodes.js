/**
 * GoToWebinar
 * The GoToWebinar API provides seamless integration of webinar registrant and attendee data into your existing infrastructure or third-party applications. The ability to register participants, as well as pull lists of registrants and attendees for a webinar, allows organizers to manage the flow of information between their primary applications without manual intervention.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: developer-support@logmein.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccessCodes model module.
 * @module model/AccessCodes
 * @version 1.0.0
 */
class AccessCodes {
    /**
     * Constructs a new <code>AccessCodes</code>.
     * Describes the access codes for organizer, panelists and attendees
     * @alias module:model/AccessCodes
     * @param attendee {String} Access code for attendees
     * @param organizer {String} Access code for the organizer
     * @param panelist {String} Access code for panelists
     */
    constructor(attendee, organizer, panelist) { 
        
        AccessCodes.initialize(this, attendee, organizer, panelist);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, attendee, organizer, panelist) { 
        obj['attendee'] = attendee;
        obj['organizer'] = organizer;
        obj['panelist'] = panelist;
    }

    /**
     * Constructs a <code>AccessCodes</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccessCodes} obj Optional instance to populate.
     * @return {module:model/AccessCodes} The populated <code>AccessCodes</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccessCodes();

            if (data.hasOwnProperty('attendee')) {
                obj['attendee'] = ApiClient.convertToType(data['attendee'], 'String');
            }
            if (data.hasOwnProperty('organizer')) {
                obj['organizer'] = ApiClient.convertToType(data['organizer'], 'String');
            }
            if (data.hasOwnProperty('panelist')) {
                obj['panelist'] = ApiClient.convertToType(data['panelist'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccessCodes</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccessCodes</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AccessCodes.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['attendee'] && !(typeof data['attendee'] === 'string' || data['attendee'] instanceof String)) {
            throw new Error("Expected the field `attendee` to be a primitive type in the JSON string but got " + data['attendee']);
        }
        // ensure the json data is a string
        if (data['organizer'] && !(typeof data['organizer'] === 'string' || data['organizer'] instanceof String)) {
            throw new Error("Expected the field `organizer` to be a primitive type in the JSON string but got " + data['organizer']);
        }
        // ensure the json data is a string
        if (data['panelist'] && !(typeof data['panelist'] === 'string' || data['panelist'] instanceof String)) {
            throw new Error("Expected the field `panelist` to be a primitive type in the JSON string but got " + data['panelist']);
        }

        return true;
    }


}

AccessCodes.RequiredProperties = ["attendee", "organizer", "panelist"];

/**
 * Access code for attendees
 * @member {String} attendee
 */
AccessCodes.prototype['attendee'] = undefined;

/**
 * Access code for the organizer
 * @member {String} organizer
 */
AccessCodes.prototype['organizer'] = undefined;

/**
 * Access code for panelists
 * @member {String} panelist
 */
AccessCodes.prototype['panelist'] = undefined;






export default AccessCodes;

