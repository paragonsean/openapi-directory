/**
 * GoToWebinar
 * The GoToWebinar API provides seamless integration of webinar registrant and attendee data into your existing infrastructure or third-party applications. The ability to register participants, as well as pull lists of registrants and attendees for a webinar, allows organizers to manage the flow of information between their primary applications without manual intervention.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: developer-support@logmein.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIAttendeesApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIAttendeesApi::OAIAttendeesApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIAttendeesApi::~OAIAttendeesApi() {
}

void OAIAttendeesApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.getgo.com/G2W/rest"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("getAttendee", defaultConf);
    _serverIndices.insert("getAttendee", 0);
    _serverConfigs.insert("getAttendeePollAnswers", defaultConf);
    _serverIndices.insert("getAttendeePollAnswers", 0);
    _serverConfigs.insert("getAttendeeQuestions", defaultConf);
    _serverIndices.insert("getAttendeeQuestions", 0);
    _serverConfigs.insert("getAttendeeSurveyAnswers", defaultConf);
    _serverIndices.insert("getAttendeeSurveyAnswers", 0);
    _serverConfigs.insert("getAttendees", defaultConf);
    _serverIndices.insert("getAttendees", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIAttendeesApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIAttendeesApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIAttendeesApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIAttendeesApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIAttendeesApi::setUsername(const QString &username) {
    _username = username;
}

void OAIAttendeesApi::setPassword(const QString &password) {
    _password = password;
}


void OAIAttendeesApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIAttendeesApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIAttendeesApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIAttendeesApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIAttendeesApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIAttendeesApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIAttendeesApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIAttendeesApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIAttendeesApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIAttendeesApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIAttendeesApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIAttendeesApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIAttendeesApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIAttendeesApi::getAttendee(const QString &authorization, const qint64 &organizer_key, const qint64 &webinar_key, const qint64 &session_key, const qint64 &registrant_key) {
    QString fullPath = QString(_serverConfigs["getAttendee"][_serverIndices.value("getAttendee")].URL()+"/organizers/{organizerKey}/webinars/{webinarKey}/sessions/{sessionKey}/attendees/{registrantKey}");
    
    
    {
        QString organizer_keyPathParam("{");
        organizer_keyPathParam.append("organizerKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "organizerKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"organizerKey"+pathSuffix : pathPrefix;
        fullPath.replace(organizer_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(organizer_key)));
    }
    
    {
        QString webinar_keyPathParam("{");
        webinar_keyPathParam.append("webinarKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webinarKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webinarKey"+pathSuffix : pathPrefix;
        fullPath.replace(webinar_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(webinar_key)));
    }
    
    {
        QString session_keyPathParam("{");
        session_keyPathParam.append("sessionKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "sessionKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"sessionKey"+pathSuffix : pathPrefix;
        fullPath.replace(session_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(session_key)));
    }
    
    {
        QString registrant_keyPathParam("{");
        registrant_keyPathParam.append("registrantKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "registrantKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"registrantKey"+pathSuffix : pathPrefix;
        fullPath.replace(registrant_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(registrant_key)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(authorization).isEmpty()) {
            input.headers.insert("Authorization", ::OpenAPI::toStringValue(authorization));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIAttendeesApi::getAttendeeCallback);
    connect(this, &OAIAttendeesApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIAttendeesApi::getAttendeeCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIRegistrant output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getAttendeeSignal(output);
        Q_EMIT getAttendeeSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getAttendeeSignalE(output, error_type, error_str);
        Q_EMIT getAttendeeSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getAttendeeSignalError(output, error_type, error_str);
        Q_EMIT getAttendeeSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIAttendeesApi::getAttendeePollAnswers(const QString &authorization, const qint64 &organizer_key, const qint64 &webinar_key, const qint64 &session_key, const qint64 &registrant_key) {
    QString fullPath = QString(_serverConfigs["getAttendeePollAnswers"][_serverIndices.value("getAttendeePollAnswers")].URL()+"/organizers/{organizerKey}/webinars/{webinarKey}/sessions/{sessionKey}/attendees/{registrantKey}/polls");
    
    
    {
        QString organizer_keyPathParam("{");
        organizer_keyPathParam.append("organizerKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "organizerKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"organizerKey"+pathSuffix : pathPrefix;
        fullPath.replace(organizer_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(organizer_key)));
    }
    
    {
        QString webinar_keyPathParam("{");
        webinar_keyPathParam.append("webinarKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webinarKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webinarKey"+pathSuffix : pathPrefix;
        fullPath.replace(webinar_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(webinar_key)));
    }
    
    {
        QString session_keyPathParam("{");
        session_keyPathParam.append("sessionKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "sessionKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"sessionKey"+pathSuffix : pathPrefix;
        fullPath.replace(session_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(session_key)));
    }
    
    {
        QString registrant_keyPathParam("{");
        registrant_keyPathParam.append("registrantKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "registrantKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"registrantKey"+pathSuffix : pathPrefix;
        fullPath.replace(registrant_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(registrant_key)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(authorization).isEmpty()) {
            input.headers.insert("Authorization", ::OpenAPI::toStringValue(authorization));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIAttendeesApi::getAttendeePollAnswersCallback);
    connect(this, &OAIAttendeesApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIAttendeesApi::getAttendeePollAnswersCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QList<OAIPollAnswer> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAIPollAnswer val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getAttendeePollAnswersSignal(output);
        Q_EMIT getAttendeePollAnswersSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getAttendeePollAnswersSignalE(output, error_type, error_str);
        Q_EMIT getAttendeePollAnswersSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getAttendeePollAnswersSignalError(output, error_type, error_str);
        Q_EMIT getAttendeePollAnswersSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIAttendeesApi::getAttendeeQuestions(const QString &authorization, const qint64 &organizer_key, const qint64 &webinar_key, const qint64 &session_key, const qint64 &registrant_key) {
    QString fullPath = QString(_serverConfigs["getAttendeeQuestions"][_serverIndices.value("getAttendeeQuestions")].URL()+"/organizers/{organizerKey}/webinars/{webinarKey}/sessions/{sessionKey}/attendees/{registrantKey}/questions");
    
    
    {
        QString organizer_keyPathParam("{");
        organizer_keyPathParam.append("organizerKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "organizerKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"organizerKey"+pathSuffix : pathPrefix;
        fullPath.replace(organizer_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(organizer_key)));
    }
    
    {
        QString webinar_keyPathParam("{");
        webinar_keyPathParam.append("webinarKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webinarKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webinarKey"+pathSuffix : pathPrefix;
        fullPath.replace(webinar_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(webinar_key)));
    }
    
    {
        QString session_keyPathParam("{");
        session_keyPathParam.append("sessionKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "sessionKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"sessionKey"+pathSuffix : pathPrefix;
        fullPath.replace(session_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(session_key)));
    }
    
    {
        QString registrant_keyPathParam("{");
        registrant_keyPathParam.append("registrantKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "registrantKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"registrantKey"+pathSuffix : pathPrefix;
        fullPath.replace(registrant_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(registrant_key)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(authorization).isEmpty()) {
            input.headers.insert("Authorization", ::OpenAPI::toStringValue(authorization));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIAttendeesApi::getAttendeeQuestionsCallback);
    connect(this, &OAIAttendeesApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIAttendeesApi::getAttendeeQuestionsCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QList<OAIAttendeeQuestion> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAIAttendeeQuestion val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getAttendeeQuestionsSignal(output);
        Q_EMIT getAttendeeQuestionsSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getAttendeeQuestionsSignalE(output, error_type, error_str);
        Q_EMIT getAttendeeQuestionsSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getAttendeeQuestionsSignalError(output, error_type, error_str);
        Q_EMIT getAttendeeQuestionsSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIAttendeesApi::getAttendeeSurveyAnswers(const QString &authorization, const qint64 &organizer_key, const qint64 &webinar_key, const qint64 &session_key, const qint64 &registrant_key) {
    QString fullPath = QString(_serverConfigs["getAttendeeSurveyAnswers"][_serverIndices.value("getAttendeeSurveyAnswers")].URL()+"/organizers/{organizerKey}/webinars/{webinarKey}/sessions/{sessionKey}/attendees/{registrantKey}/surveys");
    
    
    {
        QString organizer_keyPathParam("{");
        organizer_keyPathParam.append("organizerKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "organizerKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"organizerKey"+pathSuffix : pathPrefix;
        fullPath.replace(organizer_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(organizer_key)));
    }
    
    {
        QString webinar_keyPathParam("{");
        webinar_keyPathParam.append("webinarKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webinarKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webinarKey"+pathSuffix : pathPrefix;
        fullPath.replace(webinar_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(webinar_key)));
    }
    
    {
        QString session_keyPathParam("{");
        session_keyPathParam.append("sessionKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "sessionKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"sessionKey"+pathSuffix : pathPrefix;
        fullPath.replace(session_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(session_key)));
    }
    
    {
        QString registrant_keyPathParam("{");
        registrant_keyPathParam.append("registrantKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "registrantKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"registrantKey"+pathSuffix : pathPrefix;
        fullPath.replace(registrant_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(registrant_key)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(authorization).isEmpty()) {
            input.headers.insert("Authorization", ::OpenAPI::toStringValue(authorization));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIAttendeesApi::getAttendeeSurveyAnswersCallback);
    connect(this, &OAIAttendeesApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIAttendeesApi::getAttendeeSurveyAnswersCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QList<OAIPollAnswer> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAIPollAnswer val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getAttendeeSurveyAnswersSignal(output);
        Q_EMIT getAttendeeSurveyAnswersSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getAttendeeSurveyAnswersSignalE(output, error_type, error_str);
        Q_EMIT getAttendeeSurveyAnswersSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getAttendeeSurveyAnswersSignalError(output, error_type, error_str);
        Q_EMIT getAttendeeSurveyAnswersSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIAttendeesApi::getAttendees(const QString &authorization, const qint64 &organizer_key, const qint64 &webinar_key, const qint64 &session_key) {
    QString fullPath = QString(_serverConfigs["getAttendees"][_serverIndices.value("getAttendees")].URL()+"/organizers/{organizerKey}/webinars/{webinarKey}/sessions/{sessionKey}/attendees");
    
    
    {
        QString organizer_keyPathParam("{");
        organizer_keyPathParam.append("organizerKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "organizerKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"organizerKey"+pathSuffix : pathPrefix;
        fullPath.replace(organizer_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(organizer_key)));
    }
    
    {
        QString webinar_keyPathParam("{");
        webinar_keyPathParam.append("webinarKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webinarKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webinarKey"+pathSuffix : pathPrefix;
        fullPath.replace(webinar_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(webinar_key)));
    }
    
    {
        QString session_keyPathParam("{");
        session_keyPathParam.append("sessionKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "sessionKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"sessionKey"+pathSuffix : pathPrefix;
        fullPath.replace(session_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(session_key)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(authorization).isEmpty()) {
            input.headers.insert("Authorization", ::OpenAPI::toStringValue(authorization));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIAttendeesApi::getAttendeesCallback);
    connect(this, &OAIAttendeesApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIAttendeesApi::getAttendeesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QList<OAIAttendee> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAIAttendee val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getAttendeesSignal(output);
        Q_EMIT getAttendeesSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getAttendeesSignalE(output, error_type, error_str);
        Q_EMIT getAttendeesSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getAttendeesSignalError(output, error_type, error_str);
        Q_EMIT getAttendeesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIAttendeesApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
