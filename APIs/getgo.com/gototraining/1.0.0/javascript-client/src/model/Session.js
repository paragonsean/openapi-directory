/**
 * GoToTraining
 * The GoToTraining API enables developers to use the stable and robust GoToTraining functionality as the basis for online trainings in a proprietary learning management system. The GoToTraining APIs provide the ability to access the scheduling, registration, management, and reporting functions of GoToTraining from external applications. With the ability to tightly integrate GoToTraining into your learning infrastructure, you can offer your learners a seamless user experience and provide them with a market leading virtual classroom environment.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: developer-support@logmein.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Organizer from './Organizer';

/**
 * The Session model module.
 * @module model/Session
 * @version 1.0.0
 */
class Session {
    /**
     * Constructs a new <code>Session</code>.
     * Describes a completed training session.
     * @alias module:model/Session
     * @param attendanceCount {Number} The number of attendees at the training session
     * @param duration {Number} The duration of the training session in minutes
     * @param organizers {Array.<module:model/Organizer>} The organizers of the training session.
     * @param sessionEndTime {Date} The time the training session finished
     * @param sessionKey {String} The key of the training session
     * @param sessionStartTime {Date} The time the training session started
     * @param trainingName {String} The subject of the training
     */
    constructor(attendanceCount, duration, organizers, sessionEndTime, sessionKey, sessionStartTime, trainingName) { 
        
        Session.initialize(this, attendanceCount, duration, organizers, sessionEndTime, sessionKey, sessionStartTime, trainingName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, attendanceCount, duration, organizers, sessionEndTime, sessionKey, sessionStartTime, trainingName) { 
        obj['attendanceCount'] = attendanceCount;
        obj['duration'] = duration;
        obj['organizers'] = organizers;
        obj['sessionEndTime'] = sessionEndTime;
        obj['sessionKey'] = sessionKey;
        obj['sessionStartTime'] = sessionStartTime;
        obj['trainingName'] = trainingName;
    }

    /**
     * Constructs a <code>Session</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Session} obj Optional instance to populate.
     * @return {module:model/Session} The populated <code>Session</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Session();

            if (data.hasOwnProperty('attendanceCount')) {
                obj['attendanceCount'] = ApiClient.convertToType(data['attendanceCount'], 'Number');
            }
            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'Number');
            }
            if (data.hasOwnProperty('organizers')) {
                obj['organizers'] = ApiClient.convertToType(data['organizers'], [Organizer]);
            }
            if (data.hasOwnProperty('sessionEndTime')) {
                obj['sessionEndTime'] = ApiClient.convertToType(data['sessionEndTime'], 'Date');
            }
            if (data.hasOwnProperty('sessionKey')) {
                obj['sessionKey'] = ApiClient.convertToType(data['sessionKey'], 'String');
            }
            if (data.hasOwnProperty('sessionStartTime')) {
                obj['sessionStartTime'] = ApiClient.convertToType(data['sessionStartTime'], 'Date');
            }
            if (data.hasOwnProperty('trainingName')) {
                obj['trainingName'] = ApiClient.convertToType(data['trainingName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Session</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Session</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Session.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['organizers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['organizers'])) {
                throw new Error("Expected the field `organizers` to be an array in the JSON data but got " + data['organizers']);
            }
            // validate the optional field `organizers` (array)
            for (const item of data['organizers']) {
                Organizer.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['sessionKey'] && !(typeof data['sessionKey'] === 'string' || data['sessionKey'] instanceof String)) {
            throw new Error("Expected the field `sessionKey` to be a primitive type in the JSON string but got " + data['sessionKey']);
        }
        // ensure the json data is a string
        if (data['trainingName'] && !(typeof data['trainingName'] === 'string' || data['trainingName'] instanceof String)) {
            throw new Error("Expected the field `trainingName` to be a primitive type in the JSON string but got " + data['trainingName']);
        }

        return true;
    }


}

Session.RequiredProperties = ["attendanceCount", "duration", "organizers", "sessionEndTime", "sessionKey", "sessionStartTime", "trainingName"];

/**
 * The number of attendees at the training session
 * @member {Number} attendanceCount
 */
Session.prototype['attendanceCount'] = undefined;

/**
 * The duration of the training session in minutes
 * @member {Number} duration
 */
Session.prototype['duration'] = undefined;

/**
 * The organizers of the training session.
 * @member {Array.<module:model/Organizer>} organizers
 */
Session.prototype['organizers'] = undefined;

/**
 * The time the training session finished
 * @member {Date} sessionEndTime
 */
Session.prototype['sessionEndTime'] = undefined;

/**
 * The key of the training session
 * @member {String} sessionKey
 */
Session.prototype['sessionKey'] = undefined;

/**
 * The time the training session started
 * @member {Date} sessionStartTime
 */
Session.prototype['sessionStartTime'] = undefined;

/**
 * The subject of the training
 * @member {String} trainingName
 */
Session.prototype['trainingName'] = undefined;






export default Session;

