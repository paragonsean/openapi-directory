/**
 * GoToTraining
 * The GoToTraining API enables developers to use the stable and robust GoToTraining functionality as the basis for online trainings in a proprietary learning management system. The GoToTraining APIs provide the ability to access the scheduling, registration, management, and reporting functions of GoToTraining from external applications. With the ability to tightly integrate GoToTraining into your learning infrastructure, you can offer your learners a seamless user experience and provide them with a market leading virtual classroom environment.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: developer-support@logmein.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Attendance from './Attendance';

/**
 * The Attendee model module.
 * @module model/Attendee
 * @version 1.0.0
 */
class Attendee {
    /**
     * Constructs a new <code>Attendee</code>.
     * Describes the attendee of a training session.
     * @alias module:model/Attendee
     * @param email {String} The attendee's email address
     * @param givenName {String} The attendee's first name
     * @param inSessionTimes {Array.<module:model/Attendance>} List of the parts of the training session the attendee joined
     * @param surname {String} The attendee's surname
     * @param timeInSession {Number} The total time in minutes spent at all the parts of the training session the attendee joined
     */
    constructor(email, givenName, inSessionTimes, surname, timeInSession) { 
        
        Attendee.initialize(this, email, givenName, inSessionTimes, surname, timeInSession);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, givenName, inSessionTimes, surname, timeInSession) { 
        obj['email'] = email;
        obj['givenName'] = givenName;
        obj['inSessionTimes'] = inSessionTimes;
        obj['surname'] = surname;
        obj['timeInSession'] = timeInSession;
    }

    /**
     * Constructs a <code>Attendee</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Attendee} obj Optional instance to populate.
     * @return {module:model/Attendee} The populated <code>Attendee</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Attendee();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('givenName')) {
                obj['givenName'] = ApiClient.convertToType(data['givenName'], 'String');
            }
            if (data.hasOwnProperty('inSessionTimes')) {
                obj['inSessionTimes'] = ApiClient.convertToType(data['inSessionTimes'], [Attendance]);
            }
            if (data.hasOwnProperty('surname')) {
                obj['surname'] = ApiClient.convertToType(data['surname'], 'String');
            }
            if (data.hasOwnProperty('timeInSession')) {
                obj['timeInSession'] = ApiClient.convertToType(data['timeInSession'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Attendee</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Attendee</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Attendee.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['givenName'] && !(typeof data['givenName'] === 'string' || data['givenName'] instanceof String)) {
            throw new Error("Expected the field `givenName` to be a primitive type in the JSON string but got " + data['givenName']);
        }
        if (data['inSessionTimes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['inSessionTimes'])) {
                throw new Error("Expected the field `inSessionTimes` to be an array in the JSON data but got " + data['inSessionTimes']);
            }
            // validate the optional field `inSessionTimes` (array)
            for (const item of data['inSessionTimes']) {
                Attendance.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['surname'] && !(typeof data['surname'] === 'string' || data['surname'] instanceof String)) {
            throw new Error("Expected the field `surname` to be a primitive type in the JSON string but got " + data['surname']);
        }

        return true;
    }


}

Attendee.RequiredProperties = ["email", "givenName", "inSessionTimes", "surname", "timeInSession"];

/**
 * The attendee's email address
 * @member {String} email
 */
Attendee.prototype['email'] = undefined;

/**
 * The attendee's first name
 * @member {String} givenName
 */
Attendee.prototype['givenName'] = undefined;

/**
 * List of the parts of the training session the attendee joined
 * @member {Array.<module:model/Attendance>} inSessionTimes
 */
Attendee.prototype['inSessionTimes'] = undefined;

/**
 * The attendee's surname
 * @member {String} surname
 */
Attendee.prototype['surname'] = undefined;

/**
 * The total time in minutes spent at all the parts of the training session the attendee joined
 * @member {Number} timeInSession
 */
Attendee.prototype['timeInSession'] = undefined;






export default Attendee;

