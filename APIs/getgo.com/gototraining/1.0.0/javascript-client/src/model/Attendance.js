/**
 * GoToTraining
 * The GoToTraining API enables developers to use the stable and robust GoToTraining functionality as the basis for online trainings in a proprietary learning management system. The GoToTraining APIs provide the ability to access the scheduling, registration, management, and reporting functions of GoToTraining from external applications. With the ability to tightly integrate GoToTraining into your learning infrastructure, you can offer your learners a seamless user experience and provide them with a market leading virtual classroom environment.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: developer-support@logmein.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Attendance model module.
 * @module model/Attendance
 * @version 1.0.0
 */
class Attendance {
    /**
     * Constructs a new <code>Attendance</code>.
     * Describes the times the attendee joined and left a part of a training session.
     * @alias module:model/Attendance
     * @param joinTime {Date} The time the attendee joined a part of a training session
     * @param leaveTime {Date} The time the attendee left a part of a training session
     * @param timeInPartOfSession {Number} The time in minutes the attendee spent at a part of a training session
     */
    constructor(joinTime, leaveTime, timeInPartOfSession) { 
        
        Attendance.initialize(this, joinTime, leaveTime, timeInPartOfSession);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, joinTime, leaveTime, timeInPartOfSession) { 
        obj['joinTime'] = joinTime;
        obj['leaveTime'] = leaveTime;
        obj['timeInPartOfSession'] = timeInPartOfSession;
    }

    /**
     * Constructs a <code>Attendance</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Attendance} obj Optional instance to populate.
     * @return {module:model/Attendance} The populated <code>Attendance</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Attendance();

            if (data.hasOwnProperty('joinTime')) {
                obj['joinTime'] = ApiClient.convertToType(data['joinTime'], 'Date');
            }
            if (data.hasOwnProperty('leaveTime')) {
                obj['leaveTime'] = ApiClient.convertToType(data['leaveTime'], 'Date');
            }
            if (data.hasOwnProperty('timeInPartOfSession')) {
                obj['timeInPartOfSession'] = ApiClient.convertToType(data['timeInPartOfSession'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Attendance</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Attendance</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Attendance.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

Attendance.RequiredProperties = ["joinTime", "leaveTime", "timeInPartOfSession"];

/**
 * The time the attendee joined a part of a training session
 * @member {Date} joinTime
 */
Attendance.prototype['joinTime'] = undefined;

/**
 * The time the attendee left a part of a training session
 * @member {Date} leaveTime
 */
Attendance.prototype['leaveTime'] = undefined;

/**
 * The time in minutes the attendee spent at a part of a training session
 * @member {Number} timeInPartOfSession
 */
Attendance.prototype['timeInPartOfSession'] = undefined;






export default Attendance;

