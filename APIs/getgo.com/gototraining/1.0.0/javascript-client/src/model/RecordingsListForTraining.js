/**
 * GoToTraining
 * The GoToTraining API enables developers to use the stable and robust GoToTraining functionality as the basis for online trainings in a proprietary learning management system. The GoToTraining APIs provide the ability to access the scheduling, registration, management, and reporting functions of GoToTraining from external applications. With the ability to tightly integrate GoToTraining into your learning infrastructure, you can offer your learners a seamless user experience and provide them with a market leading virtual classroom environment.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: developer-support@logmein.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Recording from './Recording';

/**
 * The RecordingsListForTraining model module.
 * @module model/RecordingsListForTraining
 * @version 1.0.0
 */
class RecordingsListForTraining {
    /**
     * Constructs a new <code>RecordingsListForTraining</code>.
     * Contains a list of recordings for the training.
     * @alias module:model/RecordingsListForTraining
     * @param recordingList {Array.<module:model/Recording>} The list of online recordings for the training
     * @param trainingKey {Number} The training's unique key
     */
    constructor(recordingList, trainingKey) { 
        
        RecordingsListForTraining.initialize(this, recordingList, trainingKey);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, recordingList, trainingKey) { 
        obj['recordingList'] = recordingList;
        obj['trainingKey'] = trainingKey;
    }

    /**
     * Constructs a <code>RecordingsListForTraining</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecordingsListForTraining} obj Optional instance to populate.
     * @return {module:model/RecordingsListForTraining} The populated <code>RecordingsListForTraining</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecordingsListForTraining();

            if (data.hasOwnProperty('recordingList')) {
                obj['recordingList'] = ApiClient.convertToType(data['recordingList'], [Recording]);
            }
            if (data.hasOwnProperty('trainingKey')) {
                obj['trainingKey'] = ApiClient.convertToType(data['trainingKey'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecordingsListForTraining</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecordingsListForTraining</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecordingsListForTraining.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['recordingList']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['recordingList'])) {
                throw new Error("Expected the field `recordingList` to be an array in the JSON data but got " + data['recordingList']);
            }
            // validate the optional field `recordingList` (array)
            for (const item of data['recordingList']) {
                Recording.validateJSON(item);
            };
        }

        return true;
    }


}

RecordingsListForTraining.RequiredProperties = ["recordingList", "trainingKey"];

/**
 * The list of online recordings for the training
 * @member {Array.<module:model/Recording>} recordingList
 */
RecordingsListForTraining.prototype['recordingList'] = undefined;

/**
 * The training's unique key
 * @member {Number} trainingKey
 */
RecordingsListForTraining.prototype['trainingKey'] = undefined;






export default RecordingsListForTraining;

