/**
 * Miataru
 * The Miataru API is very simple and straight forward. Generally you're posting (HTTP POST) a JSON formatted request to a service method locations and you get back a JSON formatted answer. Please take into consideration that this has the request-for-comment status and that it can change while there's work done on client and server applications. Versioning therefore is done by prepending the version number - /v1/ for version 1 - to the method call.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: info@miataru.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ACK model module.
 * @module model/ACK
 * @version 1.0.0
 */
class ACK {
    /**
     * Constructs a new <code>ACK</code>.
     * @alias module:model/ACK
     * @param miataruResponse {String} 
     * @param miataruVerboseResponse {String} 
     */
    constructor(miataruResponse, miataruVerboseResponse) { 
        
        ACK.initialize(this, miataruResponse, miataruVerboseResponse);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, miataruResponse, miataruVerboseResponse) { 
        obj['MiataruResponse'] = miataruResponse || 'ACK';
        obj['MiataruVerboseResponse'] = miataruVerboseResponse;
    }

    /**
     * Constructs a <code>ACK</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ACK} obj Optional instance to populate.
     * @return {module:model/ACK} The populated <code>ACK</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ACK();

            if (data.hasOwnProperty('MiataruResponse')) {
                obj['MiataruResponse'] = ApiClient.convertToType(data['MiataruResponse'], 'String');
            }
            if (data.hasOwnProperty('MiataruVerboseResponse')) {
                obj['MiataruVerboseResponse'] = ApiClient.convertToType(data['MiataruVerboseResponse'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ACK</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ACK</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ACK.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['MiataruResponse'] && !(typeof data['MiataruResponse'] === 'string' || data['MiataruResponse'] instanceof String)) {
            throw new Error("Expected the field `MiataruResponse` to be a primitive type in the JSON string but got " + data['MiataruResponse']);
        }
        // ensure the json data is a string
        if (data['MiataruVerboseResponse'] && !(typeof data['MiataruVerboseResponse'] === 'string' || data['MiataruVerboseResponse'] instanceof String)) {
            throw new Error("Expected the field `MiataruVerboseResponse` to be a primitive type in the JSON string but got " + data['MiataruVerboseResponse']);
        }

        return true;
    }


}

ACK.RequiredProperties = ["MiataruResponse", "MiataruVerboseResponse"];

/**
 * @member {String} MiataruResponse
 * @default 'ACK'
 */
ACK.prototype['MiataruResponse'] = 'ACK';

/**
 * @member {String} MiataruVerboseResponse
 */
ACK.prototype['MiataruVerboseResponse'] = undefined;






export default ACK;

