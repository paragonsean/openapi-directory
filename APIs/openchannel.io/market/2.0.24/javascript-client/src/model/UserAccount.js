/**
 * OpenChannel Market API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.0.24
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserAccount model module.
 * @module model/UserAccount
 * @version 2.0.24
 */
class UserAccount {
    /**
     * Constructs a new <code>UserAccount</code>.
     * A User Account
     * @alias module:model/UserAccount
     * @param userAccountId {String} The id of this user account
     * @param userId {String} The id of this user that this account belongs to
     */
    constructor(userAccountId, userId) { 
        
        UserAccount.initialize(this, userAccountId, userId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, userAccountId, userId) { 
        obj['userAccountId'] = userAccountId;
        obj['userId'] = userId;
    }

    /**
     * Constructs a <code>UserAccount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserAccount} obj Optional instance to populate.
     * @return {module:model/UserAccount} The populated <code>UserAccount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserAccount();

            if (data.hasOwnProperty('customData')) {
                obj['customData'] = ApiClient.convertToType(data['customData'], Object);
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('userAccountId')) {
                obj['userAccountId'] = ApiClient.convertToType(data['userAccountId'], 'String');
            }
            if (data.hasOwnProperty('userId')) {
                obj['userId'] = ApiClient.convertToType(data['userId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserAccount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserAccount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserAccount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['userAccountId'] && !(typeof data['userAccountId'] === 'string' || data['userAccountId'] instanceof String)) {
            throw new Error("Expected the field `userAccountId` to be a primitive type in the JSON string but got " + data['userAccountId']);
        }
        // ensure the json data is a string
        if (data['userId'] && !(typeof data['userId'] === 'string' || data['userId'] instanceof String)) {
            throw new Error("Expected the field `userId` to be a primitive type in the JSON string but got " + data['userId']);
        }

        return true;
    }


}

UserAccount.RequiredProperties = ["userAccountId", "userId"];

/**
 * A custom JSON object that you can create and attach to this record
 * @member {Object} customData
 */
UserAccount.prototype['customData'] = undefined;

/**
 * The contact email address
 * @member {String} email
 */
UserAccount.prototype['email'] = undefined;

/**
 * The name for the account
 * @member {String} name
 */
UserAccount.prototype['name'] = undefined;

/**
 * The id of this user account
 * @member {String} userAccountId
 */
UserAccount.prototype['userAccountId'] = undefined;

/**
 * The id of this user that this account belongs to
 * @member {String} userId
 */
UserAccount.prototype['userId'] = undefined;






export default UserAccount;

