/**
 * Botify API
 * Botify Saas API
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CrawlDatamodelField from './CrawlDatamodelField';
import CrawlDatamodelGroup from './CrawlDatamodelGroup';

/**
 * The CrawlDatamodel model module.
 * @module model/CrawlDatamodel
 * @version 1.0.0
 */
class CrawlDatamodel {
    /**
     * Constructs a new <code>CrawlDatamodel</code>.
     * @alias module:model/CrawlDatamodel
     * @param fields {Array.<module:model/CrawlDatamodelField>} 
     * @param groups {Array.<module:model/CrawlDatamodelGroup>} 
     */
    constructor(fields, groups) { 
        
        CrawlDatamodel.initialize(this, fields, groups);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fields, groups) { 
        obj['fields'] = fields;
        obj['groups'] = groups;
    }

    /**
     * Constructs a <code>CrawlDatamodel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CrawlDatamodel} obj Optional instance to populate.
     * @return {module:model/CrawlDatamodel} The populated <code>CrawlDatamodel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CrawlDatamodel();

            if (data.hasOwnProperty('fields')) {
                obj['fields'] = ApiClient.convertToType(data['fields'], [CrawlDatamodelField]);
            }
            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], [CrawlDatamodelGroup]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CrawlDatamodel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CrawlDatamodel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CrawlDatamodel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['fields']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['fields'])) {
                throw new Error("Expected the field `fields` to be an array in the JSON data but got " + data['fields']);
            }
            // validate the optional field `fields` (array)
            for (const item of data['fields']) {
                CrawlDatamodelField.validateJSON(item);
            };
        }
        if (data['groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groups'])) {
                throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
            }
            // validate the optional field `groups` (array)
            for (const item of data['groups']) {
                CrawlDatamodelGroup.validateJSON(item);
            };
        }

        return true;
    }


}

CrawlDatamodel.RequiredProperties = ["fields", "groups"];

/**
 * 
 * @member {Array.<module:model/CrawlDatamodelField>} fields
 */
CrawlDatamodel.prototype['fields'] = undefined;

/**
 * 
 * @member {Array.<module:model/CrawlDatamodelGroup>} groups
 */
CrawlDatamodel.prototype['groups'] = undefined;






export default CrawlDatamodel;

