/**
 * climateKuul live
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIEcommerceDeliveryApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIEcommerceDeliveryApi::OAIEcommerceDeliveryApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIEcommerceDeliveryApi::~OAIEcommerceDeliveryApi() {
}

void OAIEcommerceDeliveryApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("http://api.climatekuul.com:8000/footprint"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    serverConf.append(OAIServerConfiguration(
    QUrl("http://api.climatekuul.com:8000/footprint"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("confirmCarbonOffset1", serverConf);
    _serverIndices.insert("confirmCarbonOffset1", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("http://api.climatekuul.com:8000/footprint"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("confirmPayment1", serverConf);
    _serverIndices.insert("confirmPayment1", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("http://api.climatekuul.com:8000/footprint"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("confirmPaymentOfTransaction1", serverConf);
    _serverIndices.insert("confirmPaymentOfTransaction1", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("http://api.climatekuul.com:8000/footprint"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("confirmsPlanting2", serverConf);
    _serverIndices.insert("confirmsPlanting2", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("http://api.climatekuul.com:8000/footprint"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("ecommerceDelivery", serverConf);
    _serverIndices.insert("ecommerceDelivery", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIEcommerceDeliveryApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIEcommerceDeliveryApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIEcommerceDeliveryApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIEcommerceDeliveryApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIEcommerceDeliveryApi::setUsername(const QString &username) {
    _username = username;
}

void OAIEcommerceDeliveryApi::setPassword(const QString &password) {
    _password = password;
}


void OAIEcommerceDeliveryApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIEcommerceDeliveryApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIEcommerceDeliveryApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIEcommerceDeliveryApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIEcommerceDeliveryApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIEcommerceDeliveryApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIEcommerceDeliveryApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIEcommerceDeliveryApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIEcommerceDeliveryApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIEcommerceDeliveryApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIEcommerceDeliveryApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIEcommerceDeliveryApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIEcommerceDeliveryApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIEcommerceDeliveryApi::confirmCarbonOffset1(const QString &carbon_offset, const QString &transaction_id, const ::OpenAPI::OptionalParam<QString> &contact_email, const ::OpenAPI::OptionalParam<QString> &contact_first_name, const ::OpenAPI::OptionalParam<QString> &contact_last_name) {
    QString fullPath = QString(_serverConfigs["confirmCarbonOffset1"][_serverIndices.value("confirmCarbonOffset1")].URL()+"/ecommerceDelivery/confirmCarbonOffset");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PATCH");

    
    {
        input.add_var("carbonOffset", ::OpenAPI::toStringValue(carbon_offset));
    }
    if (contact_email.hasValue())
    {
        input.add_var("contactEmail", ::OpenAPI::toStringValue(contact_email.value()));
    }
    if (contact_first_name.hasValue())
    {
        input.add_var("contactFirstName", ::OpenAPI::toStringValue(contact_first_name.value()));
    }
    if (contact_last_name.hasValue())
    {
        input.add_var("contactLastName", ::OpenAPI::toStringValue(contact_last_name.value()));
    }
    
    {
        input.add_var("transaction_id", ::OpenAPI::toStringValue(transaction_id));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIEcommerceDeliveryApi::confirmCarbonOffset1Callback);
    connect(this, &OAIEcommerceDeliveryApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIEcommerceDeliveryApi::confirmCarbonOffset1Callback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT confirmCarbonOffset1Signal();
        Q_EMIT confirmCarbonOffset1SignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT confirmCarbonOffset1SignalE(error_type, error_str);
        Q_EMIT confirmCarbonOffset1SignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT confirmCarbonOffset1SignalError(error_type, error_str);
        Q_EMIT confirmCarbonOffset1SignalErrorFull(worker, error_type, error_str);
    }
}

void OAIEcommerceDeliveryApi::confirmPayment1(const QString &api_key_l1, const QString &api_key_l2, const QString &confirm_payment, const qint32 &payment_id, const QString &transaction_id) {
    QString fullPath = QString(_serverConfigs["confirmPayment1"][_serverIndices.value("confirmPayment1")].URL()+"/ecommerceDelivery/confirmPayment");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PATCH");

    
    {
        input.add_var("apiKey_l1", ::OpenAPI::toStringValue(api_key_l1));
    }
    
    {
        input.add_var("apiKey_l2", ::OpenAPI::toStringValue(api_key_l2));
    }
    
    {
        input.add_var("confirmPayment", ::OpenAPI::toStringValue(confirm_payment));
    }
    
    {
        input.add_var("paymentID", ::OpenAPI::toStringValue(payment_id));
    }
    
    {
        input.add_var("transaction_id", ::OpenAPI::toStringValue(transaction_id));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIEcommerceDeliveryApi::confirmPayment1Callback);
    connect(this, &OAIEcommerceDeliveryApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIEcommerceDeliveryApi::confirmPayment1Callback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT confirmPayment1Signal();
        Q_EMIT confirmPayment1SignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT confirmPayment1SignalE(error_type, error_str);
        Q_EMIT confirmPayment1SignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT confirmPayment1SignalError(error_type, error_str);
        Q_EMIT confirmPayment1SignalErrorFull(worker, error_type, error_str);
    }
}

void OAIEcommerceDeliveryApi::confirmPaymentOfTransaction1(const QString &confirm_transaction, const QString &transaction_id) {
    QString fullPath = QString(_serverConfigs["confirmPaymentOfTransaction1"][_serverIndices.value("confirmPaymentOfTransaction1")].URL()+"/ecommerceDelivery/confirmTransaction");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PATCH");

    
    {
        input.add_var("confirmTransaction", ::OpenAPI::toStringValue(confirm_transaction));
    }
    
    {
        input.add_var("transaction_id", ::OpenAPI::toStringValue(transaction_id));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIEcommerceDeliveryApi::confirmPaymentOfTransaction1Callback);
    connect(this, &OAIEcommerceDeliveryApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIEcommerceDeliveryApi::confirmPaymentOfTransaction1Callback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT confirmPaymentOfTransaction1Signal();
        Q_EMIT confirmPaymentOfTransaction1SignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT confirmPaymentOfTransaction1SignalE(error_type, error_str);
        Q_EMIT confirmPaymentOfTransaction1SignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT confirmPaymentOfTransaction1SignalError(error_type, error_str);
        Q_EMIT confirmPaymentOfTransaction1SignalErrorFull(worker, error_type, error_str);
    }
}

void OAIEcommerceDeliveryApi::confirmsPlanting2(const QString &api_key_l1, const QString &api_key_l2, const QString &confirm_planting, const QString &transaction_id) {
    QString fullPath = QString(_serverConfigs["confirmsPlanting2"][_serverIndices.value("confirmsPlanting2")].URL()+"/ecommerceDelivery/confirmPlanting");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PATCH");

    
    {
        input.add_var("apiKey_l1", ::OpenAPI::toStringValue(api_key_l1));
    }
    
    {
        input.add_var("apiKey_l2", ::OpenAPI::toStringValue(api_key_l2));
    }
    
    {
        input.add_var("confirmPlanting", ::OpenAPI::toStringValue(confirm_planting));
    }
    
    {
        input.add_var("transaction_id", ::OpenAPI::toStringValue(transaction_id));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIEcommerceDeliveryApi::confirmsPlanting2Callback);
    connect(this, &OAIEcommerceDeliveryApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIEcommerceDeliveryApi::confirmsPlanting2Callback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT confirmsPlanting2Signal();
        Q_EMIT confirmsPlanting2SignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT confirmsPlanting2SignalE(error_type, error_str);
        Q_EMIT confirmsPlanting2SignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT confirmsPlanting2SignalError(error_type, error_str);
        Q_EMIT confirmsPlanting2SignalErrorFull(worker, error_type, error_str);
    }
}

void OAIEcommerceDeliveryApi::ecommerceDelivery(const QString &content_type, const QString &api_key_l1, const QString &api_key_l2, const double &destination_latitude, const double &destination_longitude, const double &origin_latitude, const double &origin_longitude, const double &volumetric_weight, const QString &waybill_type, const ::OpenAPI::OptionalParam<QString> &destination_airport_code, const ::OpenAPI::OptionalParam<QString> &origin_airport_code) {
    QString fullPath = QString(_serverConfigs["ecommerceDelivery"][_serverIndices.value("ecommerceDelivery")].URL()+"/ecommerceDelivery");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    
    {
        input.add_var("apiKey_l1", ::OpenAPI::toStringValue(api_key_l1));
    }
    
    {
        input.add_var("apiKey_l2", ::OpenAPI::toStringValue(api_key_l2));
    }
    if (destination_airport_code.hasValue())
    {
        input.add_var("destination_airport_code", ::OpenAPI::toStringValue(destination_airport_code.value()));
    }
    
    {
        input.add_var("destination_latitude", ::OpenAPI::toStringValue(destination_latitude));
    }
    
    {
        input.add_var("destination_longitude", ::OpenAPI::toStringValue(destination_longitude));
    }
    if (origin_airport_code.hasValue())
    {
        input.add_var("origin_airport_code", ::OpenAPI::toStringValue(origin_airport_code.value()));
    }
    
    {
        input.add_var("origin_latitude", ::OpenAPI::toStringValue(origin_latitude));
    }
    
    {
        input.add_var("origin_longitude", ::OpenAPI::toStringValue(origin_longitude));
    }
    
    {
        input.add_var("volumetric_weight", ::OpenAPI::toStringValue(volumetric_weight));
    }
    
    {
        input.add_var("waybill_type", ::OpenAPI::toStringValue(waybill_type));
    }

    
    {
        if (!::OpenAPI::toStringValue(content_type).isEmpty()) {
            input.headers.insert("Content-Type", ::OpenAPI::toStringValue(content_type));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIEcommerceDeliveryApi::ecommerceDeliveryCallback);
    connect(this, &OAIEcommerceDeliveryApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIEcommerceDeliveryApi::ecommerceDeliveryCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT ecommerceDeliverySignal();
        Q_EMIT ecommerceDeliverySignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT ecommerceDeliverySignalE(error_type, error_str);
        Q_EMIT ecommerceDeliverySignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT ecommerceDeliverySignalError(error_type, error_str);
        Q_EMIT ecommerceDeliverySignalErrorFull(worker, error_type, error_str);
    }
}

void OAIEcommerceDeliveryApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
