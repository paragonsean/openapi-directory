# exo_api

ExoApi - JavaScript client for exo_api
Universal API for developers - Geocoding, HTML rendering, barcode generation & more
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://exoapi.dev](https://exoapi.dev)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install exo_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your exo_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var ExoApi = require('exo_api');

var defaultClient = ExoApi.ApiClient.instance;
// Configure Bearer access token for authorization: bearerAuth
var bearerAuth = defaultClient.authentications['bearerAuth'];
bearerAuth.accessToken = "YOUR ACCESS TOKEN"

var api = new ExoApi.BarcodeGeneratorApi()
var barcodeGeneratorPostRequest = new ExoApi.BarcodeGeneratorPostRequest(); // {BarcodeGeneratorPostRequest} 
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.barcodeGeneratorPost(barcodeGeneratorPostRequest, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.exoapi.dev*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*ExoApi.BarcodeGeneratorApi* | [**barcodeGeneratorPost**](docs/BarcodeGeneratorApi.md#barcodeGeneratorPost) | **POST** /barcode-generator | 
*ExoApi.HTMLRendererApi* | [**htmlRendererPost**](docs/HTMLRendererApi.md#htmlRendererPost) | **POST** /html-renderer | 
*ExoApi.ReverseGeocodingApi* | [**reverseGeocodingGet**](docs/ReverseGeocodingApi.md#reverseGeocodingGet) | **GET** /reverse-geocoding | 
*ExoApi.UnitConverterApi* | [**unitConverterGet**](docs/UnitConverterApi.md#unitConverterGet) | **GET** /unit-converter | 


## Documentation for Models

 - [ExoApi.BarcodeGeneratorPost400Response](docs/BarcodeGeneratorPost400Response.md)
 - [ExoApi.BarcodeGeneratorPostRequest](docs/BarcodeGeneratorPostRequest.md)
 - [ExoApi.HtmlRendererPostRequest](docs/HtmlRendererPostRequest.md)
 - [ExoApi.ReverseGeocodingGet200Response](docs/ReverseGeocodingGet200Response.md)
 - [ExoApi.UnitConverterGet200Response](docs/UnitConverterGet200Response.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### bearerAuth

- **Type**: Bearer authentication

