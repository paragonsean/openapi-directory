/**
 * 
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExternalAccountBinding model module.
 * @module model/ExternalAccountBinding
 * @version 1.0.0
 */
class ExternalAccountBinding {
    /**
     * Constructs a new <code>ExternalAccountBinding</code>.
     * @alias module:model/ExternalAccountBinding
     * @param directoryUrl {String} ACME directory resource URL.
     * @param hmacKey {String} EAB HMAC key for the ACME account
     * @param keyId {String} EAB key identifier for the ACME account.
     */
    constructor(directoryUrl, hmacKey, keyId) { 
        
        ExternalAccountBinding.initialize(this, directoryUrl, hmacKey, keyId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, directoryUrl, hmacKey, keyId) { 
        obj['directoryUrl'] = directoryUrl;
        obj['hmacKey'] = hmacKey;
        obj['keyId'] = keyId;
    }

    /**
     * Constructs a <code>ExternalAccountBinding</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExternalAccountBinding} obj Optional instance to populate.
     * @return {module:model/ExternalAccountBinding} The populated <code>ExternalAccountBinding</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExternalAccountBinding();

            if (data.hasOwnProperty('directoryUrl')) {
                obj['directoryUrl'] = ApiClient.convertToType(data['directoryUrl'], 'String');
            }
            if (data.hasOwnProperty('hmacKey')) {
                obj['hmacKey'] = ApiClient.convertToType(data['hmacKey'], 'String');
            }
            if (data.hasOwnProperty('keyId')) {
                obj['keyId'] = ApiClient.convertToType(data['keyId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExternalAccountBinding</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExternalAccountBinding</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExternalAccountBinding.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['directoryUrl'] && !(typeof data['directoryUrl'] === 'string' || data['directoryUrl'] instanceof String)) {
            throw new Error("Expected the field `directoryUrl` to be a primitive type in the JSON string but got " + data['directoryUrl']);
        }
        // ensure the json data is a string
        if (data['hmacKey'] && !(typeof data['hmacKey'] === 'string' || data['hmacKey'] instanceof String)) {
            throw new Error("Expected the field `hmacKey` to be a primitive type in the JSON string but got " + data['hmacKey']);
        }
        // ensure the json data is a string
        if (data['keyId'] && !(typeof data['keyId'] === 'string' || data['keyId'] instanceof String)) {
            throw new Error("Expected the field `keyId` to be a primitive type in the JSON string but got " + data['keyId']);
        }

        return true;
    }


}

ExternalAccountBinding.RequiredProperties = ["directoryUrl", "hmacKey", "keyId"];

/**
 * ACME directory resource URL.
 * @member {String} directoryUrl
 */
ExternalAccountBinding.prototype['directoryUrl'] = undefined;

/**
 * EAB HMAC key for the ACME account
 * @member {String} hmacKey
 */
ExternalAccountBinding.prototype['hmacKey'] = undefined;

/**
 * EAB key identifier for the ACME account.
 * @member {String} keyId
 */
ExternalAccountBinding.prototype['keyId'] = undefined;






export default ExternalAccountBinding;

