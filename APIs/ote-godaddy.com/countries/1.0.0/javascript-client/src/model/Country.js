/**
 * 
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import State from './State';

/**
 * The Country model module.
 * @module model/Country
 * @version 1.0.0
 */
class Country {
    /**
     * Constructs a new <code>Country</code>.
     * @alias module:model/Country
     */
    constructor() { 
        
        Country.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Country</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Country} obj Optional instance to populate.
     * @return {module:model/Country} The populated <code>Country</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Country();

            if (data.hasOwnProperty('callingCode')) {
                obj['callingCode'] = ApiClient.convertToType(data['callingCode'], 'String');
            }
            if (data.hasOwnProperty('countryKey')) {
                obj['countryKey'] = ApiClient.convertToType(data['countryKey'], 'String');
            }
            if (data.hasOwnProperty('label')) {
                obj['label'] = ApiClient.convertToType(data['label'], 'String');
            }
            if (data.hasOwnProperty('states')) {
                obj['states'] = ApiClient.convertToType(data['states'], [State]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Country</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Country</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['callingCode'] && !(typeof data['callingCode'] === 'string' || data['callingCode'] instanceof String)) {
            throw new Error("Expected the field `callingCode` to be a primitive type in the JSON string but got " + data['callingCode']);
        }
        // ensure the json data is a string
        if (data['countryKey'] && !(typeof data['countryKey'] === 'string' || data['countryKey'] instanceof String)) {
            throw new Error("Expected the field `countryKey` to be a primitive type in the JSON string but got " + data['countryKey']);
        }
        // ensure the json data is a string
        if (data['label'] && !(typeof data['label'] === 'string' || data['label'] instanceof String)) {
            throw new Error("Expected the field `label` to be a primitive type in the JSON string but got " + data['label']);
        }
        if (data['states']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['states'])) {
                throw new Error("Expected the field `states` to be an array in the JSON data but got " + data['states']);
            }
            // validate the optional field `states` (array)
            for (const item of data['states']) {
                State.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The calling code prefix used for phone numbers in this country
 * @member {String} callingCode
 */
Country.prototype['callingCode'] = undefined;

/**
 * The ISO country-code
 * @member {String} countryKey
 */
Country.prototype['countryKey'] = undefined;

/**
 * The localized name of the country
 * @member {String} label
 */
Country.prototype['label'] = undefined;

/**
 * List of states/provinces in this country
 * @member {Array.<module:model/State>} states
 */
Country.prototype['states'] = undefined;






export default Country;

