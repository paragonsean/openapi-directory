/**
 * 
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ErrorField from './ErrorField';

/**
 * The ErrorLimit model module.
 * @module model/ErrorLimit
 * @version 1.0.0
 */
class ErrorLimit {
    /**
     * Constructs a new <code>ErrorLimit</code>.
     * @alias module:model/ErrorLimit
     * @param code {String} Short identifier for the error, suitable for indicating the specific error within client code
     * @param retryAfterSec {Number} Number of seconds to wait before attempting a similar request
     */
    constructor(code, retryAfterSec) { 
        
        ErrorLimit.initialize(this, code, retryAfterSec);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, code, retryAfterSec) { 
        obj['code'] = code;
        obj['retryAfterSec'] = retryAfterSec;
    }

    /**
     * Constructs a <code>ErrorLimit</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ErrorLimit} obj Optional instance to populate.
     * @return {module:model/ErrorLimit} The populated <code>ErrorLimit</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ErrorLimit();

            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('fields')) {
                obj['fields'] = ApiClient.convertToType(data['fields'], [ErrorField]);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('retryAfterSec')) {
                obj['retryAfterSec'] = ApiClient.convertToType(data['retryAfterSec'], 'Number');
            }
            if (data.hasOwnProperty('stack')) {
                obj['stack'] = ApiClient.convertToType(data['stack'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ErrorLimit</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ErrorLimit</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ErrorLimit.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        if (data['fields']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['fields'])) {
                throw new Error("Expected the field `fields` to be an array in the JSON data but got " + data['fields']);
            }
            // validate the optional field `fields` (array)
            for (const item of data['fields']) {
                ErrorField.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['stack'])) {
            throw new Error("Expected the field `stack` to be an array in the JSON data but got " + data['stack']);
        }

        return true;
    }


}

ErrorLimit.RequiredProperties = ["code", "retryAfterSec"];

/**
 * Short identifier for the error, suitable for indicating the specific error within client code
 * @member {String} code
 */
ErrorLimit.prototype['code'] = undefined;

/**
 * List of the specific fields, and the errors found with their contents
 * @member {Array.<module:model/ErrorField>} fields
 */
ErrorLimit.prototype['fields'] = undefined;

/**
 * Human-readable, English description of the error
 * @member {String} message
 */
ErrorLimit.prototype['message'] = undefined;

/**
 * Number of seconds to wait before attempting a similar request
 * @member {Number} retryAfterSec
 */
ErrorLimit.prototype['retryAfterSec'] = undefined;

/**
 * Stack trace indicating where the error occurred.<br/> NOTE: This attribute <strong>MAY</strong> be included for Development and Test environments. However, it <strong>MUST NOT</strong> be exposed from OTE nor Production systems
 * @member {Array.<String>} stack
 */
ErrorLimit.prototype['stack'] = undefined;






export default ErrorLimit;

