# pdf_blocks_api

PdfBlocksApi - JavaScript client for pdf_blocks_api
PDF Blocks is a secure, reliable, and fast API to work with PDF documents. Actions include: Merge PDF documents, add or remove passwords, add watermarks, remove, extract, reverse, and rotate pages, and more.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.5.0
- Package version: 1.5.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://www.pdfblocks.com/support](https://www.pdfblocks.com/support)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install pdf_blocks_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your pdf_blocks_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var PdfBlocksApi = require('pdf_blocks_api');

var defaultClient = PdfBlocksApi.ApiClient.instance;
// Configure API key authorization: apiKey
var apiKey = defaultClient.authentications['apiKey'];
apiKey.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//apiKey.apiKeyPrefix['X-Api-Key'] = "Token"

var api = new PdfBlocksApi.DefaultApi()
var file = "/path/to/file"; // {File} The input PDF document
var image = "/path/to/file"; // {File} The image to add as a watermark. The format of the image can be either PNG or JPEG.
var opts = {
  'margin': 1, // {Number} The distance in inches from the border of the page to the image watermark.
  'transparency': 50 // {Number} The transparency level for the image watermark from 0 (opaque) to 100 (transparent).
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.addImageWatermarkV1(file, image, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.pdfblocks.com*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*PdfBlocksApi.DefaultApi* | [**addImageWatermarkV1**](docs/DefaultApi.md#addImageWatermarkV1) | **POST** /v1/add_watermark/image | Add an image watermark to a PDF
*PdfBlocksApi.DefaultApi* | [**addPasswordV1**](docs/DefaultApi.md#addPasswordV1) | **POST** /v1/add_password | Add a password to a PDF
*PdfBlocksApi.DefaultApi* | [**addRestrictionsV1**](docs/DefaultApi.md#addRestrictionsV1) | **POST** /v1/add_restrictions | Add restrictions to a PDF
*PdfBlocksApi.DefaultApi* | [**addTextWatermarkV1**](docs/DefaultApi.md#addTextWatermarkV1) | **POST** /v1/add_watermark/text | Add a text watermark to a PDF
*PdfBlocksApi.DefaultApi* | [**extractPagesV1**](docs/DefaultApi.md#extractPagesV1) | **POST** /v1/extract_pages | Extract pages from a PDF
*PdfBlocksApi.DefaultApi* | [**mergeDocumentsV1**](docs/DefaultApi.md#mergeDocumentsV1) | **POST** /v1/merge_documents | Merge PDF documents
*PdfBlocksApi.DefaultApi* | [**removePagesV1**](docs/DefaultApi.md#removePagesV1) | **POST** /v1/remove_pages | Remove pages from a PDF
*PdfBlocksApi.DefaultApi* | [**removePasswordV1**](docs/DefaultApi.md#removePasswordV1) | **POST** /v1/remove_password | Remove the password from a PDF
*PdfBlocksApi.DefaultApi* | [**removeRestrictionsV1**](docs/DefaultApi.md#removeRestrictionsV1) | **POST** /v1/remove_restrictions | Remove the restrictions from a PDF
*PdfBlocksApi.DefaultApi* | [**removeSignaturesV1**](docs/DefaultApi.md#removeSignaturesV1) | **POST** /v1/remove_signatures | Remove the signatures from a PDF
*PdfBlocksApi.DefaultApi* | [**reversePagesV1**](docs/DefaultApi.md#reversePagesV1) | **POST** /v1/reverse_pages | Reverse the pages of a PDF
*PdfBlocksApi.DefaultApi* | [**rotatePagesV1**](docs/DefaultApi.md#rotatePagesV1) | **POST** /v1/rotate_pages | Rotate pages in a PDF


## Documentation for Models

 - [PdfBlocksApi.AddPasswordV14XXResponse](docs/AddPasswordV14XXResponse.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### apiKey


- **Type**: API key
- **API key parameter name**: X-Api-Key
- **Location**: HTTP header

