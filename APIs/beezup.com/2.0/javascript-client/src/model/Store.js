/**
 * BeezUP Merchant API 
 * # The REST API of BeezUP system ## Overview The REST APIs provide programmatic access to read and write BeezUP data.  Basically, with this API you will be able to do everything like you were with your browser on https://go.beezup.com !  The main features are: - Register and manage your account - Create and manage and share your stores with your friends/co-workers. - Import your product catalog and schedule the auto importation - Search the channels your want to use - Configure your channels for your catalogs to export your product information:     - cost and general settings     - category and columns mappings     - your will be able to create and manage your custom column     - put in place exlusion filters based on simple conditions on your product data     - override product values     - get product vision for a channel catalog scope - Analyze and optimize your performance of your catalogs on all yours channels with different type of reportings by day, channel, category and by product. - Automatize your optimisation by using rules! - And of course... Manage your orders harvested from all your marketplaces:     - Synchronize your orders in an uniformized way     - Get the available actions and update the order status - ...and more!  ## Authentication credentials The public API with the base path **_/v2/public** have been put in place to give you an entry point to our system for the user registration, login and lost password. The public API does not require any credentials. We give you the some public list of values and public channels for our public commercial web site [www.beezup.com](http://www.beezup.com).  The user API with the base path **_/v2/user** requires a token which is available on this page: https://go.beezup.com/Account/MyAccount  ## Things to keep in mind ### API Rate Limits - The BeezUP REST API is limited to 100 calls/minute.  ### Media type The default media type for requests and responses is application/json. Where noted, some operations support other content types. If no additional content type is mentioned for a specific operation, then the media type is application/json.  ### Required content type The required and default encoding for the request and responses is UTF8.  ### Required date time format All our date time are formatted in ISO 8601 format: 2014-06-24T16:25:00Z.  ### Base URL The Base URL of the BeezUP API Order Management REST API conforms to the following template.  https://api.beezup.com  All URLs returned by the BeezUP API are relative to this base URL, and all requests to the REST API must use this base URL template.  You can test our API on https://api-docs.beezup.com/swagger-ui\\\\ You can contact us on [gitter, #BeezUP/API](https://gitter.im/BeezUP/API)
 *
 * The version of the OpenAPI document: 2.0
 * Contact: help@beezup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import StoreLinks from './StoreLinks';
import StoreStatus from './StoreStatus';
import StoreUserRole from './StoreUserRole';

/**
 * The Store model module.
 * @module model/Store
 * @version 2.0
 */
class Store {
    /**
     * Constructs a new <code>Store</code>.
     * @alias module:model/Store
     * @param countryIsoCodeAlpha3 {String} The country iso code alpha 3 based on the list of values /user/lov/StoreCountry
     * @param creationUtcDate {Date} The creation date of the store
     * @param currencyCode {String} The currency code <a href=\"https://en.wikipedia.org/wiki/ISO_4217\">(ISO 4217)</a> 
     * @param goVersion {Number} The version of GO to use
     * @param isTest {Boolean} Is the store a test or a production store
     * @param links {module:model/StoreLinks} 
     * @param name {String} The store name. Must be unique.
     * @param offerId {Number} The offer id based on /offers. Not a free offer of course.
     * @param offerName {String} The offer Name
     * @param ownerUserId {String} The user id of the owner of the store
     * @param sectors {Array.<String>} The store's sectors based on the list of values /user/lov/ParamSector
     * @param shareCount {Number} The share count related to this store
     * @param status {module:model/StoreStatus} 
     * @param storeId {String} The store identifier
     * @param url {String} The url of your store
     * @param userRole {module:model/StoreUserRole} 
     */
    constructor(countryIsoCodeAlpha3, creationUtcDate, currencyCode, goVersion, isTest, links, name, offerId, offerName, ownerUserId, sectors, shareCount, status, storeId, url, userRole) { 
        
        Store.initialize(this, countryIsoCodeAlpha3, creationUtcDate, currencyCode, goVersion, isTest, links, name, offerId, offerName, ownerUserId, sectors, shareCount, status, storeId, url, userRole);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, countryIsoCodeAlpha3, creationUtcDate, currencyCode, goVersion, isTest, links, name, offerId, offerName, ownerUserId, sectors, shareCount, status, storeId, url, userRole) { 
        obj['countryIsoCodeAlpha3'] = countryIsoCodeAlpha3;
        obj['creationUtcDate'] = creationUtcDate;
        obj['currencyCode'] = currencyCode;
        obj['goVersion'] = goVersion;
        obj['isTest'] = isTest;
        obj['links'] = links;
        obj['name'] = name;
        obj['offerId'] = offerId;
        obj['offerName'] = offerName;
        obj['ownerUserId'] = ownerUserId;
        obj['sectors'] = sectors;
        obj['shareCount'] = shareCount;
        obj['status'] = status;
        obj['storeId'] = storeId;
        obj['url'] = url;
        obj['userRole'] = userRole;
    }

    /**
     * Constructs a <code>Store</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Store} obj Optional instance to populate.
     * @return {module:model/Store} The populated <code>Store</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Store();

            if (data.hasOwnProperty('countryIsoCodeAlpha3')) {
                obj['countryIsoCodeAlpha3'] = ApiClient.convertToType(data['countryIsoCodeAlpha3'], 'String');
            }
            if (data.hasOwnProperty('creationUtcDate')) {
                obj['creationUtcDate'] = ApiClient.convertToType(data['creationUtcDate'], 'Date');
            }
            if (data.hasOwnProperty('currencyCode')) {
                obj['currencyCode'] = ApiClient.convertToType(data['currencyCode'], 'String');
            }
            if (data.hasOwnProperty('goVersion')) {
                obj['goVersion'] = ApiClient.convertToType(data['goVersion'], 'Number');
            }
            if (data.hasOwnProperty('isTest')) {
                obj['isTest'] = ApiClient.convertToType(data['isTest'], 'Boolean');
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = StoreLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('offerId')) {
                obj['offerId'] = ApiClient.convertToType(data['offerId'], 'Number');
            }
            if (data.hasOwnProperty('offerName')) {
                obj['offerName'] = ApiClient.convertToType(data['offerName'], 'String');
            }
            if (data.hasOwnProperty('ownerUserId')) {
                obj['ownerUserId'] = ApiClient.convertToType(data['ownerUserId'], 'String');
            }
            if (data.hasOwnProperty('sectors')) {
                obj['sectors'] = ApiClient.convertToType(data['sectors'], ['String']);
            }
            if (data.hasOwnProperty('shareCount')) {
                obj['shareCount'] = ApiClient.convertToType(data['shareCount'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = StoreStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('storeId')) {
                obj['storeId'] = ApiClient.convertToType(data['storeId'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('userRole')) {
                obj['userRole'] = StoreUserRole.constructFromObject(data['userRole']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Store</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Store</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Store.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['countryIsoCodeAlpha3'] && !(typeof data['countryIsoCodeAlpha3'] === 'string' || data['countryIsoCodeAlpha3'] instanceof String)) {
            throw new Error("Expected the field `countryIsoCodeAlpha3` to be a primitive type in the JSON string but got " + data['countryIsoCodeAlpha3']);
        }
        // ensure the json data is a string
        if (data['currencyCode'] && !(typeof data['currencyCode'] === 'string' || data['currencyCode'] instanceof String)) {
            throw new Error("Expected the field `currencyCode` to be a primitive type in the JSON string but got " + data['currencyCode']);
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          StoreLinks.validateJSON(data['links']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['offerName'] && !(typeof data['offerName'] === 'string' || data['offerName'] instanceof String)) {
            throw new Error("Expected the field `offerName` to be a primitive type in the JSON string but got " + data['offerName']);
        }
        // ensure the json data is a string
        if (data['ownerUserId'] && !(typeof data['ownerUserId'] === 'string' || data['ownerUserId'] instanceof String)) {
            throw new Error("Expected the field `ownerUserId` to be a primitive type in the JSON string but got " + data['ownerUserId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['sectors'])) {
            throw new Error("Expected the field `sectors` to be an array in the JSON data but got " + data['sectors']);
        }
        // ensure the json data is a string
        if (data['storeId'] && !(typeof data['storeId'] === 'string' || data['storeId'] instanceof String)) {
            throw new Error("Expected the field `storeId` to be a primitive type in the JSON string but got " + data['storeId']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

Store.RequiredProperties = ["countryIsoCodeAlpha3", "creationUtcDate", "currencyCode", "goVersion", "isTest", "links", "name", "offerId", "offerName", "ownerUserId", "sectors", "shareCount", "status", "storeId", "url", "userRole"];

/**
 * The country iso code alpha 3 based on the list of values /user/lov/StoreCountry
 * @member {String} countryIsoCodeAlpha3
 */
Store.prototype['countryIsoCodeAlpha3'] = undefined;

/**
 * The creation date of the store
 * @member {Date} creationUtcDate
 */
Store.prototype['creationUtcDate'] = undefined;

/**
 * The currency code <a href=\"https://en.wikipedia.org/wiki/ISO_4217\">(ISO 4217)</a> 
 * @member {String} currencyCode
 */
Store.prototype['currencyCode'] = undefined;

/**
 * The version of GO to use
 * @member {Number} goVersion
 */
Store.prototype['goVersion'] = undefined;

/**
 * Is the store a test or a production store
 * @member {Boolean} isTest
 */
Store.prototype['isTest'] = undefined;

/**
 * @member {module:model/StoreLinks} links
 */
Store.prototype['links'] = undefined;

/**
 * The store name. Must be unique.
 * @member {String} name
 */
Store.prototype['name'] = undefined;

/**
 * The offer id based on /offers. Not a free offer of course.
 * @member {Number} offerId
 */
Store.prototype['offerId'] = undefined;

/**
 * The offer Name
 * @member {String} offerName
 */
Store.prototype['offerName'] = undefined;

/**
 * The user id of the owner of the store
 * @member {String} ownerUserId
 */
Store.prototype['ownerUserId'] = undefined;

/**
 * The store's sectors based on the list of values /user/lov/ParamSector
 * @member {Array.<String>} sectors
 */
Store.prototype['sectors'] = undefined;

/**
 * The share count related to this store
 * @member {Number} shareCount
 */
Store.prototype['shareCount'] = undefined;

/**
 * @member {module:model/StoreStatus} status
 */
Store.prototype['status'] = undefined;

/**
 * The store identifier
 * @member {String} storeId
 */
Store.prototype['storeId'] = undefined;

/**
 * The url of your store
 * @member {String} url
 */
Store.prototype['url'] = undefined;

/**
 * @member {module:model/StoreUserRole} userRole
 */
Store.prototype['userRole'] = undefined;






export default Store;

