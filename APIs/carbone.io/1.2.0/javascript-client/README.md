# carbone_api

CarboneApi - JavaScript client for carbone_api
Carbone Cloud/On-premise Open API reference.

For requesting:
- Carbone Cloud API: find your API key on your [Carbone account](https://account.carbone.io). Home page > Copy the `production` or `testing` API key.
- Carbone On-premise: Update the `Server URL` on the Open API specification.

Useful links:
- [API Flow](https://carbone.io/api-reference.html#quickstart-api-flow)
- [Integration / SDKs](https://carbone.io/api-reference.html#api-integration)
- [Generated document storage](https://carbone.io/api-reference.html#report-storage)
- [Request timeout](https://carbone.io/api-reference.html#api-timeout)
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.2.0
- Package version: 1.2.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://help.carbone.io](https://help.carbone.io)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install carbone_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your carbone_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var CarboneApi = require('carbone_api');


var api = new CarboneApi.RenderApi()
var renderId = "renderId_example"; // {String} Unique identifier of the report
var carboneVersion = 4; // {Number} Carbone version
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.renderRenderIdGet(renderId, carboneVersion, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.carbone.io*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*CarboneApi.RenderApi* | [**renderRenderIdGet**](docs/RenderApi.md#renderRenderIdGet) | **GET** /render/{renderId} | Retreive a generated document from a render ID.
*CarboneApi.RenderApi* | [**renderTemplateIdPost**](docs/RenderApi.md#renderTemplateIdPost) | **POST** /render/{templateId} | Generate a document from a template ID, and a JSON data-set
*CarboneApi.StatusApi* | [**statusGet**](docs/StatusApi.md#statusGet) | **GET** /status | Fetch the API status and version
*CarboneApi.TemplateApi* | [**templatePost**](docs/TemplateApi.md#templatePost) | **POST** /template | Upload a template.
*CarboneApi.TemplateApi* | [**templateTemplateIdDelete**](docs/TemplateApi.md#templateTemplateIdDelete) | **DELETE** /template/{templateId} | Delete a template from a template ID
*CarboneApi.TemplateApi* | [**templateTemplateIdGet**](docs/TemplateApi.md#templateTemplateIdGet) | **GET** /template/{templateId} | Download a template from a template ID


## Documentation for Models

 - [CarboneApi.RenderTemplateIdPost200Response](docs/RenderTemplateIdPost200Response.md)
 - [CarboneApi.RenderTemplateIdPost200ResponseData](docs/RenderTemplateIdPost200ResponseData.md)
 - [CarboneApi.RenderTemplateIdPostRequest](docs/RenderTemplateIdPostRequest.md)
 - [CarboneApi.StatusGet200Response](docs/StatusGet200Response.md)
 - [CarboneApi.TemplatePost200Response](docs/TemplatePost200Response.md)
 - [CarboneApi.TemplatePost200ResponseData](docs/TemplatePost200ResponseData.md)
 - [CarboneApi.TemplatePostRequest](docs/TemplatePostRequest.md)
 - [CarboneApi.TemplateTemplateIdDelete200Response](docs/TemplateTemplateIdDelete200Response.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### bearerAuth

- **Type**: Bearer authentication (eyJhbGci...)

