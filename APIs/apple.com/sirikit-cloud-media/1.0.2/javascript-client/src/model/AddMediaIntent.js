/**
 * SiriKit Cloud Media
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Intent from './Intent';
import MediaDestination from './MediaDestination';
import MediaItem from './MediaItem';
import MediaSearch from './MediaSearch';

/**
 * The AddMediaIntent model module.
 * @module model/AddMediaIntent
 * @version 1.0.2
 */
class AddMediaIntent {
    /**
     * Constructs a new <code>AddMediaIntent</code>.
     * @alias module:model/AddMediaIntent
     * @extends module:model/Intent
     * @implements module:model/Intent
     * @param _class {module:model/AddMediaIntent.ClassEnum} 
     * @param identifier {String} 
     */
    constructor(_class, identifier) { 
        Intent.initialize(this, _class, identifier);
        AddMediaIntent.initialize(this, _class, identifier);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, _class, identifier) { 
        obj['class'] = _class;
    }

    /**
     * Constructs a <code>AddMediaIntent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddMediaIntent} obj Optional instance to populate.
     * @return {module:model/AddMediaIntent} The populated <code>AddMediaIntent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddMediaIntent();
            Intent.constructFromObject(data, obj);
            Intent.constructFromObject(data, obj);

            if (data.hasOwnProperty('class')) {
                obj['class'] = ApiClient.convertToType(data['class'], 'String');
            }
            if (data.hasOwnProperty('mediaDestination')) {
                obj['mediaDestination'] = MediaDestination.constructFromObject(data['mediaDestination']);
            }
            if (data.hasOwnProperty('mediaItems')) {
                obj['mediaItems'] = ApiClient.convertToType(data['mediaItems'], [MediaItem]);
            }
            if (data.hasOwnProperty('mediaSearch')) {
                obj['mediaSearch'] = MediaSearch.constructFromObject(data['mediaSearch']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddMediaIntent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddMediaIntent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddMediaIntent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['class'] && !(typeof data['class'] === 'string' || data['class'] instanceof String)) {
            throw new Error("Expected the field `class` to be a primitive type in the JSON string but got " + data['class']);
        }
        // validate the optional field `mediaDestination`
        if (data['mediaDestination']) { // data not null
          MediaDestination.validateJSON(data['mediaDestination']);
        }
        if (data['mediaItems']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['mediaItems'])) {
                throw new Error("Expected the field `mediaItems` to be an array in the JSON data but got " + data['mediaItems']);
            }
            // validate the optional field `mediaItems` (array)
            for (const item of data['mediaItems']) {
                MediaItem.validateJSON(item);
            };
        }
        // validate the optional field `mediaSearch`
        if (data['mediaSearch']) { // data not null
          MediaSearch.validateJSON(data['mediaSearch']);
        }

        return true;
    }


}

AddMediaIntent.RequiredProperties = ["class", "identifier"];

/**
 * @member {module:model/AddMediaIntent.ClassEnum} class
 */
AddMediaIntent.prototype['class'] = undefined;

/**
 * @member {module:model/MediaDestination} mediaDestination
 */
AddMediaIntent.prototype['mediaDestination'] = undefined;

/**
 * @member {Array.<module:model/MediaItem>} mediaItems
 */
AddMediaIntent.prototype['mediaItems'] = undefined;

/**
 * @member {module:model/MediaSearch} mediaSearch
 */
AddMediaIntent.prototype['mediaSearch'] = undefined;


// Implement Intent interface:
/**
 * @member {String} class
 */
Intent.prototype['class'] = undefined;
/**
 * @member {String} identifier
 */
Intent.prototype['identifier'] = undefined;



/**
 * Allowed values for the <code>class</code> property.
 * @enum {String}
 * @readonly
 */
AddMediaIntent['ClassEnum'] = {

    /**
     * value: "AddMediaIntent"
     * @const
     */
    "AddMediaIntent": "AddMediaIntent"
};



export default AddMediaIntent;

