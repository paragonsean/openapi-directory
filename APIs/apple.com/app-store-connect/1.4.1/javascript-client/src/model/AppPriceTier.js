/**
 * App Store Connect API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AppPriceTierRelationships from './AppPriceTierRelationships';
import ResourceLinks from './ResourceLinks';

/**
 * The AppPriceTier model module.
 * @module model/AppPriceTier
 * @version 1.4.1
 */
class AppPriceTier {
    /**
     * Constructs a new <code>AppPriceTier</code>.
     * @alias module:model/AppPriceTier
     * @param id {String} 
     * @param links {module:model/ResourceLinks} 
     * @param type {module:model/AppPriceTier.TypeEnum} 
     */
    constructor(id, links, type) { 
        
        AppPriceTier.initialize(this, id, links, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, links, type) { 
        obj['id'] = id;
        obj['links'] = links;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>AppPriceTier</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AppPriceTier} obj Optional instance to populate.
     * @return {module:model/AppPriceTier} The populated <code>AppPriceTier</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AppPriceTier();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = ResourceLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('relationships')) {
                obj['relationships'] = AppPriceTierRelationships.constructFromObject(data['relationships']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AppPriceTier</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AppPriceTier</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AppPriceTier.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          ResourceLinks.validateJSON(data['links']);
        }
        // validate the optional field `relationships`
        if (data['relationships']) { // data not null
          AppPriceTierRelationships.validateJSON(data['relationships']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

AppPriceTier.RequiredProperties = ["id", "links", "type"];

/**
 * @member {String} id
 */
AppPriceTier.prototype['id'] = undefined;

/**
 * @member {module:model/ResourceLinks} links
 */
AppPriceTier.prototype['links'] = undefined;

/**
 * @member {module:model/AppPriceTierRelationships} relationships
 */
AppPriceTier.prototype['relationships'] = undefined;

/**
 * @member {module:model/AppPriceTier.TypeEnum} type
 */
AppPriceTier.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
AppPriceTier['TypeEnum'] = {

    /**
     * value: "appPriceTiers"
     * @const
     */
    "appPriceTiers": "appPriceTiers"
};



export default AppPriceTier;

