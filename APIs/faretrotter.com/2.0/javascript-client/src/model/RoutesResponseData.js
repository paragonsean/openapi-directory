/**
 * Faretrotter Travel API
 * Multimodal travel API to return modes of transportation between cities and points of interest.
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PlaceResponse from './PlaceResponse';
import RoutesResponseDataOperatorsInner from './RoutesResponseDataOperatorsInner';
import RoutesResponseDataRoutesInner from './RoutesResponseDataRoutesInner';

/**
 * The RoutesResponseData model module.
 * @module model/RoutesResponseData
 * @version 2.0
 */
class RoutesResponseData {
    /**
     * Constructs a new <code>RoutesResponseData</code>.
     * @alias module:model/RoutesResponseData
     * @param operators {Array.<module:model/RoutesResponseDataOperatorsInner>} 
     * @param places {Array.<module:model/PlaceResponse>} 
     * @param routes {Array.<module:model/RoutesResponseDataRoutesInner>} 
     */
    constructor(operators, places, routes) { 
        
        RoutesResponseData.initialize(this, operators, places, routes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, operators, places, routes) { 
        obj['operators'] = operators;
        obj['places'] = places;
        obj['routes'] = routes;
    }

    /**
     * Constructs a <code>RoutesResponseData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoutesResponseData} obj Optional instance to populate.
     * @return {module:model/RoutesResponseData} The populated <code>RoutesResponseData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoutesResponseData();

            if (data.hasOwnProperty('operators')) {
                obj['operators'] = ApiClient.convertToType(data['operators'], [RoutesResponseDataOperatorsInner]);
            }
            if (data.hasOwnProperty('places')) {
                obj['places'] = ApiClient.convertToType(data['places'], [PlaceResponse]);
            }
            if (data.hasOwnProperty('routes')) {
                obj['routes'] = ApiClient.convertToType(data['routes'], [RoutesResponseDataRoutesInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoutesResponseData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoutesResponseData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoutesResponseData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['operators']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['operators'])) {
                throw new Error("Expected the field `operators` to be an array in the JSON data but got " + data['operators']);
            }
            // validate the optional field `operators` (array)
            for (const item of data['operators']) {
                RoutesResponseDataOperatorsInner.validateJSON(item);
            };
        }
        if (data['places']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['places'])) {
                throw new Error("Expected the field `places` to be an array in the JSON data but got " + data['places']);
            }
            // validate the optional field `places` (array)
            for (const item of data['places']) {
                PlaceResponse.validateJSON(item);
            };
        }
        if (data['routes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['routes'])) {
                throw new Error("Expected the field `routes` to be an array in the JSON data but got " + data['routes']);
            }
            // validate the optional field `routes` (array)
            for (const item of data['routes']) {
                RoutesResponseDataRoutesInner.validateJSON(item);
            };
        }

        return true;
    }


}

RoutesResponseData.RequiredProperties = ["operators", "places", "routes"];

/**
 * @member {Array.<module:model/RoutesResponseDataOperatorsInner>} operators
 */
RoutesResponseData.prototype['operators'] = undefined;

/**
 * @member {Array.<module:model/PlaceResponse>} places
 */
RoutesResponseData.prototype['places'] = undefined;

/**
 * @member {Array.<module:model/RoutesResponseDataRoutesInner>} routes
 */
RoutesResponseData.prototype['routes'] = undefined;






export default RoutesResponseData;

