/**
 * Midjourney REST API by useapi.net
 * Simple, reliable and affordable way to use Midjourney via standard REST API
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ButtonResponseErrorUpscaled model module.
 * @module model/ButtonResponseErrorUpscaled
 * @version 1.0
 */
class ButtonResponseErrorUpscaled {
    /**
     * Constructs a new <code>ButtonResponseErrorUpscaled</code>.
     * @alias module:model/ButtonResponseErrorUpscaled
     * @param button {module:model/ButtonResponseErrorUpscaled.ButtonEnum} 
     * @param code {module:model/ButtonResponseErrorUpscaled.CodeEnum} 
     * @param error {String} 
     * @param jobid {String} 
     */
    constructor(button, code, error, jobid) { 
        
        ButtonResponseErrorUpscaled.initialize(this, button, code, error, jobid);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, button, code, error, jobid) { 
        obj['button'] = button;
        obj['code'] = code;
        obj['error'] = error;
        obj['jobid'] = jobid;
    }

    /**
     * Constructs a <code>ButtonResponseErrorUpscaled</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ButtonResponseErrorUpscaled} obj Optional instance to populate.
     * @return {module:model/ButtonResponseErrorUpscaled} The populated <code>ButtonResponseErrorUpscaled</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ButtonResponseErrorUpscaled();

            if (data.hasOwnProperty('button')) {
                obj['button'] = ApiClient.convertToType(data['button'], 'String');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'Number');
            }
            if (data.hasOwnProperty('error')) {
                obj['error'] = ApiClient.convertToType(data['error'], 'String');
            }
            if (data.hasOwnProperty('jobid')) {
                obj['jobid'] = ApiClient.convertToType(data['jobid'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ButtonResponseErrorUpscaled</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ButtonResponseErrorUpscaled</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ButtonResponseErrorUpscaled.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['button'] && !(typeof data['button'] === 'string' || data['button'] instanceof String)) {
            throw new Error("Expected the field `button` to be a primitive type in the JSON string but got " + data['button']);
        }
        // ensure the json data is a string
        if (data['error'] && !(typeof data['error'] === 'string' || data['error'] instanceof String)) {
            throw new Error("Expected the field `error` to be a primitive type in the JSON string but got " + data['error']);
        }
        // ensure the json data is a string
        if (data['jobid'] && !(typeof data['jobid'] === 'string' || data['jobid'] instanceof String)) {
            throw new Error("Expected the field `jobid` to be a primitive type in the JSON string but got " + data['jobid']);
        }

        return true;
    }


}

ButtonResponseErrorUpscaled.RequiredProperties = ["button", "code", "error", "jobid"];

/**
 * @member {module:model/ButtonResponseErrorUpscaled.ButtonEnum} button
 */
ButtonResponseErrorUpscaled.prototype['button'] = undefined;

/**
 * @member {module:model/ButtonResponseErrorUpscaled.CodeEnum} code
 */
ButtonResponseErrorUpscaled.prototype['code'] = undefined;

/**
 * @member {String} error
 */
ButtonResponseErrorUpscaled.prototype['error'] = undefined;

/**
 * @member {String} jobid
 */
ButtonResponseErrorUpscaled.prototype['jobid'] = undefined;





/**
 * Allowed values for the <code>button</code> property.
 * @enum {String}
 * @readonly
 */
ButtonResponseErrorUpscaled['ButtonEnum'] = {

    /**
     * value: "U1"
     * @const
     */
    "U1": "U1",

    /**
     * value: "U2"
     * @const
     */
    "U2": "U2",

    /**
     * value: "U3"
     * @const
     */
    "U3": "U3",

    /**
     * value: "U4"
     * @const
     */
    "U4": "U4"
};


/**
 * Allowed values for the <code>code</code> property.
 * @enum {Number}
 * @readonly
 */
ButtonResponseErrorUpscaled['CodeEnum'] = {

    /**
     * value: 409
     * @const
     */
    "409": 409
};



export default ButtonResponseErrorUpscaled;

