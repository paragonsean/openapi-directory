/**
 * Files.com API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * Contact: support@files.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FormFieldEntity from './FormFieldEntity';

/**
 * The FormFieldSetEntity model module.
 * @module model/FormFieldSetEntity
 * @version 0.0.1
 */
class FormFieldSetEntity {
    /**
     * Constructs a new <code>FormFieldSetEntity</code>.
     * List Form Field Sets
     * @alias module:model/FormFieldSetEntity
     */
    constructor() { 
        
        FormFieldSetEntity.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FormFieldSetEntity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FormFieldSetEntity} obj Optional instance to populate.
     * @return {module:model/FormFieldSetEntity} The populated <code>FormFieldSetEntity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FormFieldSetEntity();

            if (data.hasOwnProperty('form_fields')) {
                obj['form_fields'] = ApiClient.convertToType(data['form_fields'], [FormFieldEntity]);
            }
            if (data.hasOwnProperty('form_layout')) {
                obj['form_layout'] = ApiClient.convertToType(data['form_layout'], ['Number']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('skip_company')) {
                obj['skip_company'] = ApiClient.convertToType(data['skip_company'], 'Boolean');
            }
            if (data.hasOwnProperty('skip_email')) {
                obj['skip_email'] = ApiClient.convertToType(data['skip_email'], 'Boolean');
            }
            if (data.hasOwnProperty('skip_name')) {
                obj['skip_name'] = ApiClient.convertToType(data['skip_name'], 'Boolean');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FormFieldSetEntity</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FormFieldSetEntity</code>.
     */
    static validateJSON(data) {
        if (data['form_fields']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['form_fields'])) {
                throw new Error("Expected the field `form_fields` to be an array in the JSON data but got " + data['form_fields']);
            }
            // validate the optional field `form_fields` (array)
            for (const item of data['form_fields']) {
                FormFieldEntity.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['form_layout'])) {
            throw new Error("Expected the field `form_layout` to be an array in the JSON data but got " + data['form_layout']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}



/**
 * Associated form fields
 * @member {Array.<module:model/FormFieldEntity>} form_fields
 */
FormFieldSetEntity.prototype['form_fields'] = undefined;

/**
 * Layout of the form
 * @member {Array.<Number>} form_layout
 */
FormFieldSetEntity.prototype['form_layout'] = undefined;

/**
 * Form field set id
 * @member {Number} id
 */
FormFieldSetEntity.prototype['id'] = undefined;

/**
 * Any associated InboxRegistrations or BundleRegistrations can be saved without providing company
 * @member {Boolean} skip_company
 */
FormFieldSetEntity.prototype['skip_company'] = undefined;

/**
 * Any associated InboxRegistrations or BundleRegistrations can be saved without providing email
 * @member {Boolean} skip_email
 */
FormFieldSetEntity.prototype['skip_email'] = undefined;

/**
 * Any associated InboxRegistrations or BundleRegistrations can be saved without providing name
 * @member {Boolean} skip_name
 */
FormFieldSetEntity.prototype['skip_name'] = undefined;

/**
 * Title to be displayed
 * @member {String} title
 */
FormFieldSetEntity.prototype['title'] = undefined;






export default FormFieldSetEntity;

