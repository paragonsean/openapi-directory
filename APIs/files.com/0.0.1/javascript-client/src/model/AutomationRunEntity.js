/**
 * Files.com API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * Contact: support@files.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AutomationRunEntity model module.
 * @module model/AutomationRunEntity
 * @version 0.0.1
 */
class AutomationRunEntity {
    /**
     * Constructs a new <code>AutomationRunEntity</code>.
     * List Automation Runs
     * @alias module:model/AutomationRunEntity
     */
    constructor() { 
        
        AutomationRunEntity.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AutomationRunEntity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AutomationRunEntity} obj Optional instance to populate.
     * @return {module:model/AutomationRunEntity} The populated <code>AutomationRunEntity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AutomationRunEntity();

            if (data.hasOwnProperty('automation_id')) {
                obj['automation_id'] = ApiClient.convertToType(data['automation_id'], 'Number');
            }
            if (data.hasOwnProperty('completed_at')) {
                obj['completed_at'] = ApiClient.convertToType(data['completed_at'], 'Date');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('status_messages_url')) {
                obj['status_messages_url'] = ApiClient.convertToType(data['status_messages_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AutomationRunEntity</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AutomationRunEntity</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['status_messages_url'] && !(typeof data['status_messages_url'] === 'string' || data['status_messages_url'] instanceof String)) {
            throw new Error("Expected the field `status_messages_url` to be a primitive type in the JSON string but got " + data['status_messages_url']);
        }

        return true;
    }


}



/**
 * ID of the associated Automation.
 * @member {Number} automation_id
 */
AutomationRunEntity.prototype['automation_id'] = undefined;

/**
 * Automation run completion/failure date/time.
 * @member {Date} completed_at
 */
AutomationRunEntity.prototype['completed_at'] = undefined;

/**
 * Automation run start date/time.
 * @member {Date} created_at
 */
AutomationRunEntity.prototype['created_at'] = undefined;

/**
 * ID.
 * @member {Number} id
 */
AutomationRunEntity.prototype['id'] = undefined;

/**
 * The success status of the AutomationRun. One of `running`, `success`, `partial_failure`, or `failure`.
 * @member {module:model/AutomationRunEntity.StatusEnum} status
 */
AutomationRunEntity.prototype['status'] = undefined;

/**
 * Link to status messages log file.
 * @member {String} status_messages_url
 */
AutomationRunEntity.prototype['status_messages_url'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
AutomationRunEntity['StatusEnum'] = {

    /**
     * value: "running"
     * @const
     */
    "running": "running",

    /**
     * value: "success"
     * @const
     */
    "success": "success",

    /**
     * value: "partial_failure"
     * @const
     */
    "partial_failure": "partial_failure",

    /**
     * value: "failure"
     * @const
     */
    "failure": "failure",

    /**
     * value: "skipped"
     * @const
     */
    "skipped": "skipped"
};



export default AutomationRunEntity;

