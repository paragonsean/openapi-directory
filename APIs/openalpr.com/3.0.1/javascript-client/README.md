# open_alpr_car_check_api

OpenAlprCarCheckApi - JavaScript client for open_alpr_car_check_api
The OpenALPR CarCheck API allows you to send images to the cloud for processing. 
The image will be analyzed for license plates and vehicle make/models.  The results 
are returned in JSON format

This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 3.0.1
- Package version: 3.0.1
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install open_alpr_car_check_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your open_alpr_car_check_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var OpenAlprCarCheckApi = require('open_alpr_car_check_api');


var api = new OpenAlprCarCheckApi.DefaultApi()
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.getConfig(callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.openalpr.com/v3*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*OpenAlprCarCheckApi.DefaultApi* | [**getConfig**](docs/DefaultApi.md#getConfig) | **GET** /config | 
*OpenAlprCarCheckApi.DefaultApi* | [**recognizeBytes**](docs/DefaultApi.md#recognizeBytes) | **POST** /recognize_bytes | 
*OpenAlprCarCheckApi.DefaultApi* | [**recognizeFile**](docs/DefaultApi.md#recognizeFile) | **POST** /recognize | 
*OpenAlprCarCheckApi.DefaultApi* | [**recognizeUrl**](docs/DefaultApi.md#recognizeUrl) | **POST** /recognize_url | 


## Documentation for Models

 - [OpenAlprCarCheckApi.Coordinate](docs/Coordinate.md)
 - [OpenAlprCarCheckApi.GetConfig200Response](docs/GetConfig200Response.md)
 - [OpenAlprCarCheckApi.GetConfig200ResponseCountriesInner](docs/GetConfig200ResponseCountriesInner.md)
 - [OpenAlprCarCheckApi.GetConfig200ResponseVehicleLabels](docs/GetConfig200ResponseVehicleLabels.md)
 - [OpenAlprCarCheckApi.PlateCandidate](docs/PlateCandidate.md)
 - [OpenAlprCarCheckApi.PlateDetails](docs/PlateDetails.md)
 - [OpenAlprCarCheckApi.RecognizeFile200Response](docs/RecognizeFile200Response.md)
 - [OpenAlprCarCheckApi.RecognizeFile200ResponseProcessingTime](docs/RecognizeFile200ResponseProcessingTime.md)
 - [OpenAlprCarCheckApi.RecognizeFile400Response](docs/RecognizeFile400Response.md)
 - [OpenAlprCarCheckApi.RegionOfInterest](docs/RegionOfInterest.md)
 - [OpenAlprCarCheckApi.VehicleCandidate](docs/VehicleCandidate.md)
 - [OpenAlprCarCheckApi.VehicleDetails](docs/VehicleDetails.md)
 - [OpenAlprCarCheckApi.Vehicles](docs/Vehicles.md)


## Documentation for Authorization

Endpoints do not require authorization.

