/**
 * Twitter API v2
 * Twitter API v2 available endpoints
 *
 * The version of the OpenAPI document: 2.62
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateDmEventResponseData model module.
 * @module model/CreateDmEventResponseData
 * @version 2.62
 */
class CreateDmEventResponseData {
    /**
     * Constructs a new <code>CreateDmEventResponseData</code>.
     * @alias module:model/CreateDmEventResponseData
     * @param dmConversationId {String} Unique identifier of a DM conversation. This can either be a numeric string, or a pair of numeric strings separated by a '-' character in the case of one-on-one DM Conversations.
     * @param dmEventId {String} Unique identifier of a DM Event.
     */
    constructor(dmConversationId, dmEventId) { 
        
        CreateDmEventResponseData.initialize(this, dmConversationId, dmEventId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dmConversationId, dmEventId) { 
        obj['dm_conversation_id'] = dmConversationId;
        obj['dm_event_id'] = dmEventId;
    }

    /**
     * Constructs a <code>CreateDmEventResponseData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateDmEventResponseData} obj Optional instance to populate.
     * @return {module:model/CreateDmEventResponseData} The populated <code>CreateDmEventResponseData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateDmEventResponseData();

            if (data.hasOwnProperty('dm_conversation_id')) {
                obj['dm_conversation_id'] = ApiClient.convertToType(data['dm_conversation_id'], 'String');
            }
            if (data.hasOwnProperty('dm_event_id')) {
                obj['dm_event_id'] = ApiClient.convertToType(data['dm_event_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateDmEventResponseData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateDmEventResponseData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateDmEventResponseData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['dm_conversation_id'] && !(typeof data['dm_conversation_id'] === 'string' || data['dm_conversation_id'] instanceof String)) {
            throw new Error("Expected the field `dm_conversation_id` to be a primitive type in the JSON string but got " + data['dm_conversation_id']);
        }
        // ensure the json data is a string
        if (data['dm_event_id'] && !(typeof data['dm_event_id'] === 'string' || data['dm_event_id'] instanceof String)) {
            throw new Error("Expected the field `dm_event_id` to be a primitive type in the JSON string but got " + data['dm_event_id']);
        }

        return true;
    }


}

CreateDmEventResponseData.RequiredProperties = ["dm_conversation_id", "dm_event_id"];

/**
 * Unique identifier of a DM conversation. This can either be a numeric string, or a pair of numeric strings separated by a '-' character in the case of one-on-one DM Conversations.
 * @member {String} dm_conversation_id
 */
CreateDmEventResponseData.prototype['dm_conversation_id'] = undefined;

/**
 * Unique identifier of a DM Event.
 * @member {String} dm_event_id
 */
CreateDmEventResponseData.prototype['dm_event_id'] = undefined;






export default CreateDmEventResponseData;

