/**
 * Twitter API v2
 * Twitter API v2 available endpoints
 *
 * The version of the OpenAPI document: 2.62
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CashtagFields from './CashtagFields';
import EntityIndicesInclusiveExclusive from './EntityIndicesInclusiveExclusive';

/**
 * The CashtagEntity model module.
 * @module model/CashtagEntity
 * @version 2.62
 */
class CashtagEntity {
    /**
     * Constructs a new <code>CashtagEntity</code>.
     * @alias module:model/CashtagEntity
     * @implements module:model/EntityIndicesInclusiveExclusive
     * @implements module:model/CashtagFields
     * @param end {Number} Index (zero-based) at which position this entity ends.  The index is exclusive.
     * @param start {Number} Index (zero-based) at which position this entity starts.  The index is inclusive.
     * @param tag {String} 
     */
    constructor(end, start, tag) { 
        EntityIndicesInclusiveExclusive.initialize(this, end, start);CashtagFields.initialize(this, tag);
        CashtagEntity.initialize(this, end, start, tag);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, end, start, tag) { 
        obj['end'] = end;
        obj['start'] = start;
        obj['tag'] = tag;
    }

    /**
     * Constructs a <code>CashtagEntity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CashtagEntity} obj Optional instance to populate.
     * @return {module:model/CashtagEntity} The populated <code>CashtagEntity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CashtagEntity();
            EntityIndicesInclusiveExclusive.constructFromObject(data, obj);
            CashtagFields.constructFromObject(data, obj);

            if (data.hasOwnProperty('end')) {
                obj['end'] = ApiClient.convertToType(data['end'], 'Number');
            }
            if (data.hasOwnProperty('start')) {
                obj['start'] = ApiClient.convertToType(data['start'], 'Number');
            }
            if (data.hasOwnProperty('tag')) {
                obj['tag'] = ApiClient.convertToType(data['tag'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CashtagEntity</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CashtagEntity</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CashtagEntity.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['tag'] && !(typeof data['tag'] === 'string' || data['tag'] instanceof String)) {
            throw new Error("Expected the field `tag` to be a primitive type in the JSON string but got " + data['tag']);
        }

        return true;
    }


}

CashtagEntity.RequiredProperties = ["end", "start", "tag"];

/**
 * Index (zero-based) at which position this entity ends.  The index is exclusive.
 * @member {Number} end
 */
CashtagEntity.prototype['end'] = undefined;

/**
 * Index (zero-based) at which position this entity starts.  The index is inclusive.
 * @member {Number} start
 */
CashtagEntity.prototype['start'] = undefined;

/**
 * @member {String} tag
 */
CashtagEntity.prototype['tag'] = undefined;


// Implement EntityIndicesInclusiveExclusive interface:
/**
 * Index (zero-based) at which position this entity ends.  The index is exclusive.
 * @member {Number} end
 */
EntityIndicesInclusiveExclusive.prototype['end'] = undefined;
/**
 * Index (zero-based) at which position this entity starts.  The index is inclusive.
 * @member {Number} start
 */
EntityIndicesInclusiveExclusive.prototype['start'] = undefined;
// Implement CashtagFields interface:
/**
 * @member {String} tag
 */
CashtagFields.prototype['tag'] = undefined;




export default CashtagEntity;

