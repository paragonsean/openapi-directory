/**
 * Twitter API v2
 * Twitter API v2 available endpoints
 *
 * The version of the OpenAPI document: 2.62
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EntityIndicesInclusiveInclusive from './EntityIndicesInclusiveInclusive';

/**
 * The FullTextEntitiesAnnotationsInner model module.
 * @module model/FullTextEntitiesAnnotationsInner
 * @version 2.62
 */
class FullTextEntitiesAnnotationsInner {
    /**
     * Constructs a new <code>FullTextEntitiesAnnotationsInner</code>.
     * Annotation for entities based on the Tweet text.
     * @alias module:model/FullTextEntitiesAnnotationsInner
     * @implements module:model/EntityIndicesInclusiveInclusive
     * @param end {Number} Index (zero-based) at which position this entity ends.  The index is inclusive.
     * @param start {Number} Index (zero-based) at which position this entity starts.  The index is inclusive.
     */
    constructor(end, start) { 
        EntityIndicesInclusiveInclusive.initialize(this, end, start);
        FullTextEntitiesAnnotationsInner.initialize(this, end, start);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, end, start) { 
        obj['end'] = end;
        obj['start'] = start;
    }

    /**
     * Constructs a <code>FullTextEntitiesAnnotationsInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FullTextEntitiesAnnotationsInner} obj Optional instance to populate.
     * @return {module:model/FullTextEntitiesAnnotationsInner} The populated <code>FullTextEntitiesAnnotationsInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FullTextEntitiesAnnotationsInner();
            EntityIndicesInclusiveInclusive.constructFromObject(data, obj);

            if (data.hasOwnProperty('end')) {
                obj['end'] = ApiClient.convertToType(data['end'], 'Number');
            }
            if (data.hasOwnProperty('start')) {
                obj['start'] = ApiClient.convertToType(data['start'], 'Number');
            }
            if (data.hasOwnProperty('normalized_text')) {
                obj['normalized_text'] = ApiClient.convertToType(data['normalized_text'], 'String');
            }
            if (data.hasOwnProperty('probability')) {
                obj['probability'] = ApiClient.convertToType(data['probability'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FullTextEntitiesAnnotationsInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FullTextEntitiesAnnotationsInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FullTextEntitiesAnnotationsInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['normalized_text'] && !(typeof data['normalized_text'] === 'string' || data['normalized_text'] instanceof String)) {
            throw new Error("Expected the field `normalized_text` to be a primitive type in the JSON string but got " + data['normalized_text']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

FullTextEntitiesAnnotationsInner.RequiredProperties = ["end", "start"];

/**
 * Index (zero-based) at which position this entity ends.  The index is inclusive.
 * @member {Number} end
 */
FullTextEntitiesAnnotationsInner.prototype['end'] = undefined;

/**
 * Index (zero-based) at which position this entity starts.  The index is inclusive.
 * @member {Number} start
 */
FullTextEntitiesAnnotationsInner.prototype['start'] = undefined;

/**
 * Text used to determine annotation.
 * @member {String} normalized_text
 */
FullTextEntitiesAnnotationsInner.prototype['normalized_text'] = undefined;

/**
 * Confidence factor for annotation type.
 * @member {Number} probability
 */
FullTextEntitiesAnnotationsInner.prototype['probability'] = undefined;

/**
 * Annotation type.
 * @member {String} type
 */
FullTextEntitiesAnnotationsInner.prototype['type'] = undefined;


// Implement EntityIndicesInclusiveInclusive interface:
/**
 * Index (zero-based) at which position this entity ends.  The index is inclusive.
 * @member {Number} end
 */
EntityIndicesInclusiveInclusive.prototype['end'] = undefined;
/**
 * Index (zero-based) at which position this entity starts.  The index is inclusive.
 * @member {Number} start
 */
EntityIndicesInclusiveInclusive.prototype['start'] = undefined;




export default FullTextEntitiesAnnotationsInner;

