/**
 * Twitter API v2
 * Twitter API v2 available endpoints
 *
 * The version of the OpenAPI document: 2.62
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Problem from './Problem';

/**
 * The UsageCapExceededProblem model module.
 * @module model/UsageCapExceededProblem
 * @version 2.62
 */
class UsageCapExceededProblem {
    /**
     * Constructs a new <code>UsageCapExceededProblem</code>.
     * A problem that indicates that a usage cap has been exceeded.
     * @alias module:model/UsageCapExceededProblem
     * @extends module:model/Problem
     * @implements module:model/Problem
     * @param title {String} 
     * @param type {String} 
     */
    constructor(title, type) { 
        Problem.initialize(this, title, type);
        UsageCapExceededProblem.initialize(this, title, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, title, type) { 
    }

    /**
     * Constructs a <code>UsageCapExceededProblem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UsageCapExceededProblem} obj Optional instance to populate.
     * @return {module:model/UsageCapExceededProblem} The populated <code>UsageCapExceededProblem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UsageCapExceededProblem();
            Problem.constructFromObject(data, obj);
            Problem.constructFromObject(data, obj);

            if (data.hasOwnProperty('period')) {
                obj['period'] = ApiClient.convertToType(data['period'], 'String');
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UsageCapExceededProblem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UsageCapExceededProblem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UsageCapExceededProblem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['period'] && !(typeof data['period'] === 'string' || data['period'] instanceof String)) {
            throw new Error("Expected the field `period` to be a primitive type in the JSON string but got " + data['period']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }

        return true;
    }


}

UsageCapExceededProblem.RequiredProperties = ["title", "type"];

/**
 * @member {module:model/UsageCapExceededProblem.PeriodEnum} period
 */
UsageCapExceededProblem.prototype['period'] = undefined;

/**
 * @member {module:model/UsageCapExceededProblem.ScopeEnum} scope
 */
UsageCapExceededProblem.prototype['scope'] = undefined;


// Implement Problem interface:
/**
 * @member {String} detail
 */
Problem.prototype['detail'] = undefined;
/**
 * @member {Number} status
 */
Problem.prototype['status'] = undefined;
/**
 * @member {String} title
 */
Problem.prototype['title'] = undefined;
/**
 * @member {String} type
 */
Problem.prototype['type'] = undefined;



/**
 * Allowed values for the <code>period</code> property.
 * @enum {String}
 * @readonly
 */
UsageCapExceededProblem['PeriodEnum'] = {

    /**
     * value: "Daily"
     * @const
     */
    "Daily": "Daily",

    /**
     * value: "Monthly"
     * @const
     */
    "Monthly": "Monthly"
};


/**
 * Allowed values for the <code>scope</code> property.
 * @enum {String}
 * @readonly
 */
UsageCapExceededProblem['ScopeEnum'] = {

    /**
     * value: "Account"
     * @const
     */
    "Account": "Account",

    /**
     * value: "Product"
     * @const
     */
    "Product": "Product"
};



export default UsageCapExceededProblem;

