/**
 * Twitter API v2
 * Twitter API v2 available endpoints
 *
 * The version of the OpenAPI document: 2.62
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserWithheld model module.
 * @module model/UserWithheld
 * @version 2.62
 */
class UserWithheld {
    /**
     * Constructs a new <code>UserWithheld</code>.
     * Indicates withholding details for [withheld content](https://help.twitter.com/en/rules-and-policies/tweet-withheld-by-country).
     * @alias module:model/UserWithheld
     * @param countryCodes {Array.<String>} Provides a list of countries where this content is not available.
     */
    constructor(countryCodes) { 
        
        UserWithheld.initialize(this, countryCodes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, countryCodes) { 
        obj['country_codes'] = countryCodes;
    }

    /**
     * Constructs a <code>UserWithheld</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserWithheld} obj Optional instance to populate.
     * @return {module:model/UserWithheld} The populated <code>UserWithheld</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserWithheld();

            if (data.hasOwnProperty('country_codes')) {
                obj['country_codes'] = ApiClient.convertToType(data['country_codes'], ['String']);
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserWithheld</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserWithheld</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserWithheld.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['country_codes'])) {
            throw new Error("Expected the field `country_codes` to be an array in the JSON data but got " + data['country_codes']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }

        return true;
    }


}

UserWithheld.RequiredProperties = ["country_codes"];

/**
 * Provides a list of countries where this content is not available.
 * @member {Array.<String>} country_codes
 */
UserWithheld.prototype['country_codes'] = undefined;

/**
 * Indicates that the content being withheld is a `user`.
 * @member {module:model/UserWithheld.ScopeEnum} scope
 */
UserWithheld.prototype['scope'] = undefined;





/**
 * Allowed values for the <code>scope</code> property.
 * @enum {String}
 * @readonly
 */
UserWithheld['ScopeEnum'] = {

    /**
     * value: "user"
     * @const
     */
    "user": "user"
};



export default UserWithheld;

