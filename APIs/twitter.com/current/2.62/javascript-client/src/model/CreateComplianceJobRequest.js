/**
 * Twitter API v2
 * Twitter API v2 available endpoints
 *
 * The version of the OpenAPI document: 2.62
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateComplianceJobRequest model module.
 * @module model/CreateComplianceJobRequest
 * @version 2.62
 */
class CreateComplianceJobRequest {
    /**
     * Constructs a new <code>CreateComplianceJobRequest</code>.
     * A request to create a new batch compliance job.
     * @alias module:model/CreateComplianceJobRequest
     * @param type {module:model/CreateComplianceJobRequest.TypeEnum} Type of compliance job to list.
     */
    constructor(type) { 
        
        CreateComplianceJobRequest.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['type'] = type;
    }

    /**
     * Constructs a <code>CreateComplianceJobRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateComplianceJobRequest} obj Optional instance to populate.
     * @return {module:model/CreateComplianceJobRequest} The populated <code>CreateComplianceJobRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateComplianceJobRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('resumable')) {
                obj['resumable'] = ApiClient.convertToType(data['resumable'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateComplianceJobRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateComplianceJobRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateComplianceJobRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

CreateComplianceJobRequest.RequiredProperties = ["type"];

/**
 * User-provided name for a compliance job.
 * @member {String} name
 */
CreateComplianceJobRequest.prototype['name'] = undefined;

/**
 * If true, this endpoint will return a pre-signed URL with resumable uploads enabled.
 * @member {Boolean} resumable
 */
CreateComplianceJobRequest.prototype['resumable'] = undefined;

/**
 * Type of compliance job to list.
 * @member {module:model/CreateComplianceJobRequest.TypeEnum} type
 */
CreateComplianceJobRequest.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
CreateComplianceJobRequest['TypeEnum'] = {

    /**
     * value: "tweets"
     * @const
     */
    "tweets": "tweets",

    /**
     * value: "users"
     * @const
     */
    "users": "users"
};



export default CreateComplianceJobRequest;

