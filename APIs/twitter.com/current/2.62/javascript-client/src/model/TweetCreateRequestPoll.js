/**
 * Twitter API v2
 * Twitter API v2 available endpoints
 *
 * The version of the OpenAPI document: 2.62
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TweetCreateRequestPoll model module.
 * @module model/TweetCreateRequestPoll
 * @version 2.62
 */
class TweetCreateRequestPoll {
    /**
     * Constructs a new <code>TweetCreateRequestPoll</code>.
     * Poll options for a Tweet with a poll. This is mutually exclusive from Media, Quote Tweet Id, and Card URI.
     * @alias module:model/TweetCreateRequestPoll
     * @param durationMinutes {Number} Duration of the poll in minutes.
     * @param options {Array.<String>} 
     */
    constructor(durationMinutes, options) { 
        
        TweetCreateRequestPoll.initialize(this, durationMinutes, options);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, durationMinutes, options) { 
        obj['duration_minutes'] = durationMinutes;
        obj['options'] = options;
    }

    /**
     * Constructs a <code>TweetCreateRequestPoll</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TweetCreateRequestPoll} obj Optional instance to populate.
     * @return {module:model/TweetCreateRequestPoll} The populated <code>TweetCreateRequestPoll</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TweetCreateRequestPoll();

            if (data.hasOwnProperty('duration_minutes')) {
                obj['duration_minutes'] = ApiClient.convertToType(data['duration_minutes'], 'Number');
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], ['String']);
            }
            if (data.hasOwnProperty('reply_settings')) {
                obj['reply_settings'] = ApiClient.convertToType(data['reply_settings'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TweetCreateRequestPoll</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TweetCreateRequestPoll</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TweetCreateRequestPoll.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['options'])) {
            throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
        }
        // ensure the json data is a string
        if (data['reply_settings'] && !(typeof data['reply_settings'] === 'string' || data['reply_settings'] instanceof String)) {
            throw new Error("Expected the field `reply_settings` to be a primitive type in the JSON string but got " + data['reply_settings']);
        }

        return true;
    }


}

TweetCreateRequestPoll.RequiredProperties = ["duration_minutes", "options"];

/**
 * Duration of the poll in minutes.
 * @member {Number} duration_minutes
 */
TweetCreateRequestPoll.prototype['duration_minutes'] = undefined;

/**
 * @member {Array.<String>} options
 */
TweetCreateRequestPoll.prototype['options'] = undefined;

/**
 * Settings to indicate who can reply to the Tweet.
 * @member {module:model/TweetCreateRequestPoll.ReplySettingsEnum} reply_settings
 */
TweetCreateRequestPoll.prototype['reply_settings'] = undefined;





/**
 * Allowed values for the <code>reply_settings</code> property.
 * @enum {String}
 * @readonly
 */
TweetCreateRequestPoll['ReplySettingsEnum'] = {

    /**
     * value: "following"
     * @const
     */
    "following": "following",

    /**
     * value: "mentionedUsers"
     * @const
     */
    "mentionedUsers": "mentionedUsers"
};



export default TweetCreateRequestPoll;

