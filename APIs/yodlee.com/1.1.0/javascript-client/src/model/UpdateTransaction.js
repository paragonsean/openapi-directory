/**
 * Yodlee Core APIs
 * This file describes the Yodlee Platform APIs using the swagger notation. You can use this swagger file to generate client side SDKs to the Yodlee Platform APIs for many different programming languages. Yodlee supports the Java SDK and it is available <a href=\"https://developer.yodlee.com/java-sdk-overview \">here</a>. You can generate a client SDK for Python, Java, JavaScript, PHP or other languages according to your development needs. For more details about the APIs, refer to <a href=\"https://developer.yodlee.com/docs/api/1.1/Overview\">Yodlee API v1.1 - Overview</a>.
 *
 * The version of the OpenAPI document: 1.1.0
 * Contact: developer@yodlee.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Description from './Description';

/**
 * The UpdateTransaction model module.
 * @module model/UpdateTransaction
 * @version 1.1.0
 */
class UpdateTransaction {
    /**
     * Constructs a new <code>UpdateTransaction</code>.
     * @alias module:model/UpdateTransaction
     * @param categoryId {Number} 
     * @param categorySource {module:model/UpdateTransaction.CategorySourceEnum} 
     * @param container {module:model/UpdateTransaction.ContainerEnum} 
     */
    constructor(categoryId, categorySource, container) { 
        
        UpdateTransaction.initialize(this, categoryId, categorySource, container);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categoryId, categorySource, container) { 
        obj['categoryId'] = categoryId;
        obj['categorySource'] = categorySource;
        obj['container'] = container;
    }

    /**
     * Constructs a <code>UpdateTransaction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateTransaction} obj Optional instance to populate.
     * @return {module:model/UpdateTransaction} The populated <code>UpdateTransaction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateTransaction();

            if (data.hasOwnProperty('categoryId')) {
                obj['categoryId'] = ApiClient.convertToType(data['categoryId'], 'Number');
            }
            if (data.hasOwnProperty('categorySource')) {
                obj['categorySource'] = ApiClient.convertToType(data['categorySource'], 'String');
            }
            if (data.hasOwnProperty('container')) {
                obj['container'] = ApiClient.convertToType(data['container'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = Description.constructFromObject(data['description']);
            }
            if (data.hasOwnProperty('memo')) {
                obj['memo'] = ApiClient.convertToType(data['memo'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateTransaction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateTransaction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateTransaction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['categorySource'] && !(typeof data['categorySource'] === 'string' || data['categorySource'] instanceof String)) {
            throw new Error("Expected the field `categorySource` to be a primitive type in the JSON string but got " + data['categorySource']);
        }
        // ensure the json data is a string
        if (data['container'] && !(typeof data['container'] === 'string' || data['container'] instanceof String)) {
            throw new Error("Expected the field `container` to be a primitive type in the JSON string but got " + data['container']);
        }
        // validate the optional field `description`
        if (data['description']) { // data not null
          Description.validateJSON(data['description']);
        }
        // ensure the json data is a string
        if (data['memo'] && !(typeof data['memo'] === 'string' || data['memo'] instanceof String)) {
            throw new Error("Expected the field `memo` to be a primitive type in the JSON string but got " + data['memo']);
        }

        return true;
    }


}

UpdateTransaction.RequiredProperties = ["categoryId", "categorySource", "container"];

/**
 * @member {Number} categoryId
 */
UpdateTransaction.prototype['categoryId'] = undefined;

/**
 * @member {module:model/UpdateTransaction.CategorySourceEnum} categorySource
 */
UpdateTransaction.prototype['categorySource'] = undefined;

/**
 * @member {module:model/UpdateTransaction.ContainerEnum} container
 */
UpdateTransaction.prototype['container'] = undefined;

/**
 * @member {module:model/Description} description
 */
UpdateTransaction.prototype['description'] = undefined;

/**
 * @member {String} memo
 */
UpdateTransaction.prototype['memo'] = undefined;





/**
 * Allowed values for the <code>categorySource</code> property.
 * @enum {String}
 * @readonly
 */
UpdateTransaction['CategorySourceEnum'] = {

    /**
     * value: "SYSTEM"
     * @const
     */
    "SYSTEM": "SYSTEM",

    /**
     * value: "USER"
     * @const
     */
    "USER": "USER"
};


/**
 * Allowed values for the <code>container</code> property.
 * @enum {String}
 * @readonly
 */
UpdateTransaction['ContainerEnum'] = {

    /**
     * value: "bank"
     * @const
     */
    "bank": "bank",

    /**
     * value: "creditCard"
     * @const
     */
    "creditCard": "creditCard",

    /**
     * value: "investment"
     * @const
     */
    "investment": "investment",

    /**
     * value: "insurance"
     * @const
     */
    "insurance": "insurance",

    /**
     * value: "loan"
     * @const
     */
    "loan": "loan",

    /**
     * value: "reward"
     * @const
     */
    "reward": "reward",

    /**
     * value: "realEstate"
     * @const
     */
    "realEstate": "realEstate",

    /**
     * value: "otherAssets"
     * @const
     */
    "otherAssets": "otherAssets",

    /**
     * value: "otherLiabilities"
     * @const
     */
    "otherLiabilities": "otherLiabilities"
};



export default UpdateTransaction;

