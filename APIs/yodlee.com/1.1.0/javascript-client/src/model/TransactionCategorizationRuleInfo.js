/**
 * Yodlee Core APIs
 * This file describes the Yodlee Platform APIs using the swagger notation. You can use this swagger file to generate client side SDKs to the Yodlee Platform APIs for many different programming languages. Yodlee supports the Java SDK and it is available <a href=\"https://developer.yodlee.com/java-sdk-overview \">here</a>. You can generate a client SDK for Python, Java, JavaScript, PHP or other languages according to your development needs. For more details about the APIs, refer to <a href=\"https://developer.yodlee.com/docs/api/1.1/Overview\">Yodlee API v1.1 - Overview</a>.
 *
 * The version of the OpenAPI document: 1.1.0
 * Contact: developer@yodlee.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FieldOperation from './FieldOperation';

/**
 * The TransactionCategorizationRuleInfo model module.
 * @module model/TransactionCategorizationRuleInfo
 * @version 1.1.0
 */
class TransactionCategorizationRuleInfo {
    /**
     * Constructs a new <code>TransactionCategorizationRuleInfo</code>.
     * @alias module:model/TransactionCategorizationRuleInfo
     * @param categoryId {Number} 
     * @param ruleClause {Array.<module:model/FieldOperation>} 
     */
    constructor(categoryId, ruleClause) { 
        
        TransactionCategorizationRuleInfo.initialize(this, categoryId, ruleClause);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categoryId, ruleClause) { 
        obj['categoryId'] = categoryId;
        obj['ruleClause'] = ruleClause;
    }

    /**
     * Constructs a <code>TransactionCategorizationRuleInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransactionCategorizationRuleInfo} obj Optional instance to populate.
     * @return {module:model/TransactionCategorizationRuleInfo} The populated <code>TransactionCategorizationRuleInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransactionCategorizationRuleInfo();

            if (data.hasOwnProperty('categoryId')) {
                obj['categoryId'] = ApiClient.convertToType(data['categoryId'], 'Number');
            }
            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'Number');
            }
            if (data.hasOwnProperty('ruleClause')) {
                obj['ruleClause'] = ApiClient.convertToType(data['ruleClause'], [FieldOperation]);
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransactionCategorizationRuleInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransactionCategorizationRuleInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransactionCategorizationRuleInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['ruleClause']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ruleClause'])) {
                throw new Error("Expected the field `ruleClause` to be an array in the JSON data but got " + data['ruleClause']);
            }
            // validate the optional field `ruleClause` (array)
            for (const item of data['ruleClause']) {
                FieldOperation.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

TransactionCategorizationRuleInfo.RequiredProperties = ["categoryId", "ruleClause"];

/**
 * @member {Number} categoryId
 */
TransactionCategorizationRuleInfo.prototype['categoryId'] = undefined;

/**
 * @member {Number} priority
 */
TransactionCategorizationRuleInfo.prototype['priority'] = undefined;

/**
 * @member {Array.<module:model/FieldOperation>} ruleClause
 */
TransactionCategorizationRuleInfo.prototype['ruleClause'] = undefined;

/**
 * @member {module:model/TransactionCategorizationRuleInfo.SourceEnum} source
 */
TransactionCategorizationRuleInfo.prototype['source'] = undefined;





/**
 * Allowed values for the <code>source</code> property.
 * @enum {String}
 * @readonly
 */
TransactionCategorizationRuleInfo['SourceEnum'] = {

    /**
     * value: "SYSTEM"
     * @const
     */
    "SYSTEM": "SYSTEM",

    /**
     * value: "USER"
     * @const
     */
    "USER": "USER"
};



export default TransactionCategorizationRuleInfo;

