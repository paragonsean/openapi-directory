/**
 * obono RKSV API
 * Provides a RESTful API for interacting with virtual cash registers and creating receipts that are conform with the Registrierkassensicherheitsverordnung (RKSV).  You may find our [automatically generated clients](./clients) for various programming languages and environments helpful... 
 *
 * The version of the OpenAPI document: 1.4.0.0
 * Contact: info@obono.at
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Zahlung model module.
 * @module model/Zahlung
 * @version 1.4.0.0
 */
class Zahlung {
    /**
     * Constructs a new <code>Zahlung</code>.
     * @alias module:model/Zahlung
     * @param betrag {Number} The amount in cents
     * @param bezeichnung {String} 
     */
    constructor(betrag, bezeichnung) { 
        
        Zahlung.initialize(this, betrag, bezeichnung);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, betrag, bezeichnung) { 
        obj['Betrag'] = betrag;
        obj['Bezeichnung'] = bezeichnung;
    }

    /**
     * Constructs a <code>Zahlung</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Zahlung} obj Optional instance to populate.
     * @return {module:model/Zahlung} The populated <code>Zahlung</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Zahlung();

            if (data.hasOwnProperty('Betrag')) {
                obj['Betrag'] = ApiClient.convertToType(data['Betrag'], 'Number');
            }
            if (data.hasOwnProperty('Bezeichnung')) {
                obj['Bezeichnung'] = ApiClient.convertToType(data['Bezeichnung'], 'String');
            }
            if (data.hasOwnProperty('Referenz')) {
                obj['Referenz'] = ApiClient.convertToType(data['Referenz'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Zahlung</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Zahlung</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Zahlung.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Bezeichnung'] && !(typeof data['Bezeichnung'] === 'string' || data['Bezeichnung'] instanceof String)) {
            throw new Error("Expected the field `Bezeichnung` to be a primitive type in the JSON string but got " + data['Bezeichnung']);
        }
        // ensure the json data is a string
        if (data['Referenz'] && !(typeof data['Referenz'] === 'string' || data['Referenz'] instanceof String)) {
            throw new Error("Expected the field `Referenz` to be a primitive type in the JSON string but got " + data['Referenz']);
        }

        return true;
    }


}

Zahlung.RequiredProperties = ["Betrag", "Bezeichnung"];

/**
 * The amount in cents
 * @member {Number} Betrag
 */
Zahlung.prototype['Betrag'] = undefined;

/**
 * @member {String} Bezeichnung
 */
Zahlung.prototype['Bezeichnung'] = undefined;

/**
 * @member {String} Referenz
 */
Zahlung.prototype['Referenz'] = undefined;






export default Zahlung;

