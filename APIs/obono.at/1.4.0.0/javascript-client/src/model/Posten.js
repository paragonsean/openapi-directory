/**
 * obono RKSV API
 * Provides a RESTful API for interacting with virtual cash registers and creating receipts that are conform with the Registrierkassensicherheitsverordnung (RKSV).  You may find our [automatically generated clients](./clients) for various programming languages and environments helpful... 
 *
 * The version of the OpenAPI document: 1.4.0.0
 * Contact: info@obono.at
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Posten model module.
 * @module model/Posten
 * @version 1.4.0.0
 */
class Posten {
    /**
     * Constructs a new <code>Posten</code>.
     * @alias module:model/Posten
     * @param bezeichnung {String} 
     * @param bruttoBetrag {Number} The amount in cents
     * @param menge {Number} 
     * @param nettoBetrag {Number} The amount in cents
     * @param satz {module:model/Posten.SatzEnum} 
     */
    constructor(bezeichnung, bruttoBetrag, menge, nettoBetrag, satz) { 
        
        Posten.initialize(this, bezeichnung, bruttoBetrag, menge, nettoBetrag, satz);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bezeichnung, bruttoBetrag, menge, nettoBetrag, satz) { 
        obj['Bezeichnung'] = bezeichnung;
        obj['BruttoBetrag'] = bruttoBetrag;
        obj['Menge'] = menge;
        obj['NettoBetrag'] = nettoBetrag;
        obj['Satz'] = satz;
    }

    /**
     * Constructs a <code>Posten</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Posten} obj Optional instance to populate.
     * @return {module:model/Posten} The populated <code>Posten</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Posten();

            if (data.hasOwnProperty('Bezeichnung')) {
                obj['Bezeichnung'] = ApiClient.convertToType(data['Bezeichnung'], 'String');
            }
            if (data.hasOwnProperty('BruttoBetrag')) {
                obj['BruttoBetrag'] = ApiClient.convertToType(data['BruttoBetrag'], 'Number');
            }
            if (data.hasOwnProperty('Externer-Beleg-Belegkreis')) {
                obj['Externer-Beleg-Belegkreis'] = ApiClient.convertToType(data['Externer-Beleg-Belegkreis'], 'String');
            }
            if (data.hasOwnProperty('Externer-Beleg-Bezeichnung')) {
                obj['Externer-Beleg-Bezeichnung'] = ApiClient.convertToType(data['Externer-Beleg-Bezeichnung'], 'String');
            }
            if (data.hasOwnProperty('Externer-Beleg-Referenz')) {
                obj['Externer-Beleg-Referenz'] = ApiClient.convertToType(data['Externer-Beleg-Referenz'], 'String');
            }
            if (data.hasOwnProperty('Menge')) {
                obj['Menge'] = ApiClient.convertToType(data['Menge'], 'Number');
            }
            if (data.hasOwnProperty('NettoBetrag')) {
                obj['NettoBetrag'] = ApiClient.convertToType(data['NettoBetrag'], 'Number');
            }
            if (data.hasOwnProperty('Satz')) {
                obj['Satz'] = ApiClient.convertToType(data['Satz'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Posten</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Posten</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Posten.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Bezeichnung'] && !(typeof data['Bezeichnung'] === 'string' || data['Bezeichnung'] instanceof String)) {
            throw new Error("Expected the field `Bezeichnung` to be a primitive type in the JSON string but got " + data['Bezeichnung']);
        }
        // ensure the json data is a string
        if (data['Externer-Beleg-Belegkreis'] && !(typeof data['Externer-Beleg-Belegkreis'] === 'string' || data['Externer-Beleg-Belegkreis'] instanceof String)) {
            throw new Error("Expected the field `Externer-Beleg-Belegkreis` to be a primitive type in the JSON string but got " + data['Externer-Beleg-Belegkreis']);
        }
        // ensure the json data is a string
        if (data['Externer-Beleg-Bezeichnung'] && !(typeof data['Externer-Beleg-Bezeichnung'] === 'string' || data['Externer-Beleg-Bezeichnung'] instanceof String)) {
            throw new Error("Expected the field `Externer-Beleg-Bezeichnung` to be a primitive type in the JSON string but got " + data['Externer-Beleg-Bezeichnung']);
        }
        // ensure the json data is a string
        if (data['Externer-Beleg-Referenz'] && !(typeof data['Externer-Beleg-Referenz'] === 'string' || data['Externer-Beleg-Referenz'] instanceof String)) {
            throw new Error("Expected the field `Externer-Beleg-Referenz` to be a primitive type in the JSON string but got " + data['Externer-Beleg-Referenz']);
        }
        // ensure the json data is a string
        if (data['Satz'] && !(typeof data['Satz'] === 'string' || data['Satz'] instanceof String)) {
            throw new Error("Expected the field `Satz` to be a primitive type in the JSON string but got " + data['Satz']);
        }

        return true;
    }


}

Posten.RequiredProperties = ["Bezeichnung", "BruttoBetrag", "Menge", "NettoBetrag", "Satz"];

/**
 * @member {String} Bezeichnung
 */
Posten.prototype['Bezeichnung'] = undefined;

/**
 * The amount in cents
 * @member {Number} BruttoBetrag
 */
Posten.prototype['BruttoBetrag'] = undefined;

/**
 * @member {String} Externer-Beleg-Belegkreis
 */
Posten.prototype['Externer-Beleg-Belegkreis'] = undefined;

/**
 * @member {String} Externer-Beleg-Bezeichnung
 */
Posten.prototype['Externer-Beleg-Bezeichnung'] = undefined;

/**
 * @member {String} Externer-Beleg-Referenz
 */
Posten.prototype['Externer-Beleg-Referenz'] = undefined;

/**
 * @member {Number} Menge
 */
Posten.prototype['Menge'] = undefined;

/**
 * The amount in cents
 * @member {Number} NettoBetrag
 */
Posten.prototype['NettoBetrag'] = undefined;

/**
 * @member {module:model/Posten.SatzEnum} Satz
 */
Posten.prototype['Satz'] = undefined;





/**
 * Allowed values for the <code>Satz</code> property.
 * @enum {String}
 * @readonly
 */
Posten['SatzEnum'] = {

    /**
     * value: "NORMAL"
     * @const
     */
    "NORMAL": "NORMAL",

    /**
     * value: "ERMAESSIGT1"
     * @const
     */
    "ERMAESSIGT1": "ERMAESSIGT1",

    /**
     * value: "ERMAESSIGT2"
     * @const
     */
    "ERMAESSIGT2": "ERMAESSIGT2",

    /**
     * value: "BESONDERS"
     * @const
     */
    "BESONDERS": "BESONDERS",

    /**
     * value: "NULL"
     * @const
     */
    "NULL": "NULL"
};



export default Posten;

