/**
 * Postmark Account-level API
 * Postmark makes sending and receiving email incredibly easy. The Account-level API allows users to configure all Servers, Domains, and Sender Signatures associated with an Account. 
 *
 * The version of the OpenAPI document: 0.9.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAISenderSignaturesAPIApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAISenderSignaturesAPIApi::OAISenderSignaturesAPIApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAISenderSignaturesAPIApi::~OAISenderSignaturesAPIApi() {
}

void OAISenderSignaturesAPIApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("//api.postmarkapp.com/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("createSenderSignature", defaultConf);
    _serverIndices.insert("createSenderSignature", 0);
    _serverConfigs.insert("deleteSenderSignature", defaultConf);
    _serverIndices.insert("deleteSenderSignature", 0);
    _serverConfigs.insert("editSenderSignature", defaultConf);
    _serverIndices.insert("editSenderSignature", 0);
    _serverConfigs.insert("getSenderSignature", defaultConf);
    _serverIndices.insert("getSenderSignature", 0);
    _serverConfigs.insert("listSenderSignatures", defaultConf);
    _serverIndices.insert("listSenderSignatures", 0);
    _serverConfigs.insert("requestNewDKIMKeyForSenderSignature", defaultConf);
    _serverIndices.insert("requestNewDKIMKeyForSenderSignature", 0);
    _serverConfigs.insert("requestSPFVerificationForSenderSignature", defaultConf);
    _serverIndices.insert("requestSPFVerificationForSenderSignature", 0);
    _serverConfigs.insert("resendSenderSignatureConfirmationEmail", defaultConf);
    _serverIndices.insert("resendSenderSignatureConfirmationEmail", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAISenderSignaturesAPIApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAISenderSignaturesAPIApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAISenderSignaturesAPIApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAISenderSignaturesAPIApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAISenderSignaturesAPIApi::setUsername(const QString &username) {
    _username = username;
}

void OAISenderSignaturesAPIApi::setPassword(const QString &password) {
    _password = password;
}


void OAISenderSignaturesAPIApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAISenderSignaturesAPIApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAISenderSignaturesAPIApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAISenderSignaturesAPIApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAISenderSignaturesAPIApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAISenderSignaturesAPIApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAISenderSignaturesAPIApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAISenderSignaturesAPIApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAISenderSignaturesAPIApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAISenderSignaturesAPIApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAISenderSignaturesAPIApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAISenderSignaturesAPIApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAISenderSignaturesAPIApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAISenderSignaturesAPIApi::createSenderSignature(const QString &x_postmark_account_token, const ::OpenAPI::OptionalParam<OAISenderSignatureCreationModel> &body) {
    QString fullPath = QString(_serverConfigs["createSenderSignature"][_serverIndices.value("createSenderSignature")].URL()+"/senders");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (body.hasValue()){

        
        QByteArray output = body.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::createSenderSignatureCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::createSenderSignatureCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISenderSignatureExtendedInformation output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT createSenderSignatureSignal(output);
        Q_EMIT createSenderSignatureSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT createSenderSignatureSignalE(output, error_type, error_str);
        Q_EMIT createSenderSignatureSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT createSenderSignatureSignalError(output, error_type, error_str);
        Q_EMIT createSenderSignatureSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::deleteSenderSignature(const QString &x_postmark_account_token, const qint32 &signatureid) {
    QString fullPath = QString(_serverConfigs["deleteSenderSignature"][_serverIndices.value("deleteSenderSignature")].URL()+"/senders/{signatureid}");
    
    
    {
        QString signatureidPathParam("{");
        signatureidPathParam.append("signatureid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "signatureid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"signatureid"+pathSuffix : pathPrefix;
        fullPath.replace(signatureidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(signatureid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::deleteSenderSignatureCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::deleteSenderSignatureCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIStandardPostmarkResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT deleteSenderSignatureSignal(output);
        Q_EMIT deleteSenderSignatureSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT deleteSenderSignatureSignalE(output, error_type, error_str);
        Q_EMIT deleteSenderSignatureSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT deleteSenderSignatureSignalError(output, error_type, error_str);
        Q_EMIT deleteSenderSignatureSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::editSenderSignature(const QString &x_postmark_account_token, const qint32 &signatureid, const ::OpenAPI::OptionalParam<OAISenderSignatureEditingModel> &body) {
    QString fullPath = QString(_serverConfigs["editSenderSignature"][_serverIndices.value("editSenderSignature")].URL()+"/senders/{signatureid}");
    
    
    {
        QString signatureidPathParam("{");
        signatureidPathParam.append("signatureid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "signatureid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"signatureid"+pathSuffix : pathPrefix;
        fullPath.replace(signatureidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(signatureid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PUT");

    if (body.hasValue()){

        
        QByteArray output = body.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::editSenderSignatureCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::editSenderSignatureCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISenderSignatureExtendedInformation output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT editSenderSignatureSignal(output);
        Q_EMIT editSenderSignatureSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT editSenderSignatureSignalE(output, error_type, error_str);
        Q_EMIT editSenderSignatureSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT editSenderSignatureSignalError(output, error_type, error_str);
        Q_EMIT editSenderSignatureSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::getSenderSignature(const QString &x_postmark_account_token, const qint32 &signatureid) {
    QString fullPath = QString(_serverConfigs["getSenderSignature"][_serverIndices.value("getSenderSignature")].URL()+"/senders/{signatureid}");
    
    
    {
        QString signatureidPathParam("{");
        signatureidPathParam.append("signatureid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "signatureid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"signatureid"+pathSuffix : pathPrefix;
        fullPath.replace(signatureidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(signatureid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::getSenderSignatureCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::getSenderSignatureCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISenderSignatureExtendedInformation output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getSenderSignatureSignal(output);
        Q_EMIT getSenderSignatureSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getSenderSignatureSignalE(output, error_type, error_str);
        Q_EMIT getSenderSignatureSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getSenderSignatureSignalError(output, error_type, error_str);
        Q_EMIT getSenderSignatureSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::listSenderSignatures(const QString &x_postmark_account_token, const qint32 &count, const qint32 &offset) {
    QString fullPath = QString(_serverConfigs["listSenderSignatures"][_serverIndices.value("listSenderSignatures")].URL()+"/senders");
    
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "count", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("count")).append(querySuffix).append(QUrl::toPercentEncoding(count));
    }
    
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "offset", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("offset")).append(querySuffix).append(QUrl::toPercentEncoding(offset));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::listSenderSignaturesCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::listSenderSignaturesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISenderListingResults output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT listSenderSignaturesSignal(output);
        Q_EMIT listSenderSignaturesSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT listSenderSignaturesSignalE(output, error_type, error_str);
        Q_EMIT listSenderSignaturesSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT listSenderSignaturesSignalError(output, error_type, error_str);
        Q_EMIT listSenderSignaturesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::requestNewDKIMKeyForSenderSignature(const QString &x_postmark_account_token, const qint32 &signatureid) {
    QString fullPath = QString(_serverConfigs["requestNewDKIMKeyForSenderSignature"][_serverIndices.value("requestNewDKIMKeyForSenderSignature")].URL()+"/senders/{signatureid}/requestnewdkim");
    
    
    {
        QString signatureidPathParam("{");
        signatureidPathParam.append("signatureid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "signatureid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"signatureid"+pathSuffix : pathPrefix;
        fullPath.replace(signatureidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(signatureid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");


    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::requestNewDKIMKeyForSenderSignatureCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::requestNewDKIMKeyForSenderSignatureCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIStandardPostmarkResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT requestNewDKIMKeyForSenderSignatureSignal(output);
        Q_EMIT requestNewDKIMKeyForSenderSignatureSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT requestNewDKIMKeyForSenderSignatureSignalE(output, error_type, error_str);
        Q_EMIT requestNewDKIMKeyForSenderSignatureSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT requestNewDKIMKeyForSenderSignatureSignalError(output, error_type, error_str);
        Q_EMIT requestNewDKIMKeyForSenderSignatureSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::requestSPFVerificationForSenderSignature(const QString &x_postmark_account_token, const qint32 &signatureid) {
    QString fullPath = QString(_serverConfigs["requestSPFVerificationForSenderSignature"][_serverIndices.value("requestSPFVerificationForSenderSignature")].URL()+"/senders/{signatureid}/verifyspf");
    
    
    {
        QString signatureidPathParam("{");
        signatureidPathParam.append("signatureid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "signatureid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"signatureid"+pathSuffix : pathPrefix;
        fullPath.replace(signatureidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(signatureid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");


    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::requestSPFVerificationForSenderSignatureCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::requestSPFVerificationForSenderSignatureCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISenderSignatureExtendedInformation output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT requestSPFVerificationForSenderSignatureSignal(output);
        Q_EMIT requestSPFVerificationForSenderSignatureSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT requestSPFVerificationForSenderSignatureSignalE(output, error_type, error_str);
        Q_EMIT requestSPFVerificationForSenderSignatureSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT requestSPFVerificationForSenderSignatureSignalError(output, error_type, error_str);
        Q_EMIT requestSPFVerificationForSenderSignatureSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::resendSenderSignatureConfirmationEmail(const QString &x_postmark_account_token, const qint32 &signatureid) {
    QString fullPath = QString(_serverConfigs["resendSenderSignatureConfirmationEmail"][_serverIndices.value("resendSenderSignatureConfirmationEmail")].URL()+"/senders/{signatureid}/resend");
    
    
    {
        QString signatureidPathParam("{");
        signatureidPathParam.append("signatureid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "signatureid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"signatureid"+pathSuffix : pathPrefix;
        fullPath.replace(signatureidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(signatureid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");


    
    {
        if (!::OpenAPI::toStringValue(x_postmark_account_token).isEmpty()) {
            input.headers.insert("X-Postmark-Account-Token", ::OpenAPI::toStringValue(x_postmark_account_token));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISenderSignaturesAPIApi::resendSenderSignatureConfirmationEmailCallback);
    connect(this, &OAISenderSignaturesAPIApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISenderSignaturesAPIApi::resendSenderSignatureConfirmationEmailCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIStandardPostmarkResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT resendSenderSignatureConfirmationEmailSignal(output);
        Q_EMIT resendSenderSignatureConfirmationEmailSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT resendSenderSignatureConfirmationEmailSignalE(output, error_type, error_str);
        Q_EMIT resendSenderSignatureConfirmationEmailSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT resendSenderSignatureConfirmationEmailSignalError(output, error_type, error_str);
        Q_EMIT resendSenderSignatureConfirmationEmailSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISenderSignaturesAPIApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
