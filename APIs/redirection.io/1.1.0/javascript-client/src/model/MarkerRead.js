/**
 * redirection.io
 * API documentation for redirection.io
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MarkerRead model module.
 * @module model/MarkerRead
 * @version 1.1.0
 */
class MarkerRead {
    /**
     * Constructs a new <code>MarkerRead</code>.
     * 
     * @alias module:model/MarkerRead
     * @param name {String} 
     * @param type {String} 
     */
    constructor(name, type) { 
        
        MarkerRead.initialize(this, name, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, type) { 
        obj['name'] = name;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>MarkerRead</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MarkerRead} obj Optional instance to populate.
     * @return {module:model/MarkerRead} The populated <code>MarkerRead</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MarkerRead();

            if (data.hasOwnProperty('maximumOccurrence')) {
                obj['maximumOccurrence'] = ApiClient.convertToType(data['maximumOccurrence'], 'Number');
            }
            if (data.hasOwnProperty('minimumOccurrence')) {
                obj['minimumOccurrence'] = ApiClient.convertToType(data['minimumOccurrence'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], ['String']);
            }
            if (data.hasOwnProperty('transformers')) {
                obj['transformers'] = ApiClient.convertToType(data['transformers'], ['String']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MarkerRead</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MarkerRead</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MarkerRead.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['options'])) {
            throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['transformers'])) {
            throw new Error("Expected the field `transformers` to be an array in the JSON data but got " + data['transformers']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

MarkerRead.RequiredProperties = ["name", "type"];

/**
 * @member {Number} maximumOccurrence
 */
MarkerRead.prototype['maximumOccurrence'] = undefined;

/**
 * @member {Number} minimumOccurrence
 */
MarkerRead.prototype['minimumOccurrence'] = undefined;

/**
 * @member {String} name
 */
MarkerRead.prototype['name'] = undefined;

/**
 * @member {Array.<String>} options
 */
MarkerRead.prototype['options'] = undefined;

/**
 * @member {Array.<String>} transformers
 */
MarkerRead.prototype['transformers'] = undefined;

/**
 * @member {String} type
 */
MarkerRead.prototype['type'] = undefined;






export default MarkerRead;

