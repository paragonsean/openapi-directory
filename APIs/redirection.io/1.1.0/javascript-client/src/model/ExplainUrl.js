/**
 * redirection.io
 * API documentation for redirection.io
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExplainUrl model module.
 * @module model/ExplainUrl
 * @version 1.1.0
 */
class ExplainUrl {
    /**
     * Constructs a new <code>ExplainUrl</code>.
     * 
     * @alias module:model/ExplainUrl
     * @param project {String} 
     * @param url {String} 
     */
    constructor(project, url) { 
        
        ExplainUrl.initialize(this, project, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, project, url) { 
        obj['project'] = project;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>ExplainUrl</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExplainUrl} obj Optional instance to populate.
     * @return {module:model/ExplainUrl} The populated <code>ExplainUrl</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExplainUrl();

            if (data.hasOwnProperty('explain')) {
                obj['explain'] = ApiClient.convertToType(data['explain'], Object);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('project')) {
                obj['project'] = ApiClient.convertToType(data['project'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExplainUrl</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExplainUrl</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExplainUrl.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['project'] && !(typeof data['project'] === 'string' || data['project'] instanceof String)) {
            throw new Error("Expected the field `project` to be a primitive type in the JSON string but got " + data['project']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

ExplainUrl.RequiredProperties = ["project", "url"];

/**
 * @member {Object} explain
 */
ExplainUrl.prototype['explain'] = undefined;

/**
 * @member {String} id
 */
ExplainUrl.prototype['id'] = undefined;

/**
 * @member {String} project
 */
ExplainUrl.prototype['project'] = undefined;

/**
 * @member {String} url
 */
ExplainUrl.prototype['url'] = undefined;






export default ExplainUrl;

