/**
 * redirection.io
 * API documentation for redirection.io
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ProjectWrite model module.
 * @module model/ProjectWrite
 * @version 1.1.0
 */
class ProjectWrite {
    /**
     * Constructs a new <code>ProjectWrite</code>.
     * 
     * @alias module:model/ProjectWrite
     * @param name {String} 
     */
    constructor(name) { 
        
        ProjectWrite.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>ProjectWrite</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProjectWrite} obj Optional instance to populate.
     * @return {module:model/ProjectWrite} The populated <code>ProjectWrite</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProjectWrite();

            if (data.hasOwnProperty('ignoreProjectTypes')) {
                obj['ignoreProjectTypes'] = ApiClient.convertToType(data['ignoreProjectTypes'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('onboardingCompletedDemos')) {
                obj['onboardingCompletedDemos'] = ApiClient.convertToType(data['onboardingCompletedDemos'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProjectWrite</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProjectWrite</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProjectWrite.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['ignoreProjectTypes'])) {
            throw new Error("Expected the field `ignoreProjectTypes` to be an array in the JSON data but got " + data['ignoreProjectTypes']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['onboardingCompletedDemos'])) {
            throw new Error("Expected the field `onboardingCompletedDemos` to be an array in the JSON data but got " + data['onboardingCompletedDemos']);
        }

        return true;
    }


}

ProjectWrite.RequiredProperties = ["name"];

/**
 * @member {Array.<String>} ignoreProjectTypes
 */
ProjectWrite.prototype['ignoreProjectTypes'] = undefined;

/**
 * @member {String} name
 */
ProjectWrite.prototype['name'] = undefined;

/**
 * @member {Array.<String>} onboardingCompletedDemos
 */
ProjectWrite.prototype['onboardingCompletedDemos'] = undefined;






export default ProjectWrite;

