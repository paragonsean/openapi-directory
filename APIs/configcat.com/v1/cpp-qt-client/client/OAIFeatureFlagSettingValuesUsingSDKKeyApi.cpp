/**
 * ConfigCat Public Management API
 * **Base API URL**: https://api.configcat.com  If you prefer the swagger documentation, you can find it here: [Swagger UI](https://api.configcat.com/swagger).  The purpose of this API is to access the ConfigCat platform programmatically.  You can **Create**, **Read**, **Update** and **Delete** any entities like **Feature Flags, Configs, Environments** or **Products** within ConfigCat.   The API is based on HTTP REST, uses resource-oriented URLs, status codes and supports JSON  and JSON+HAL format. Do not use this API for accessing and evaluating feature flag values. Use the [SDKs instead](https://configcat.com/docs/sdk-reference/overview).   # OpenAPI Specification  The complete specification is publicly available here: [swagger.json](v1/swagger.json).  You can use it to generate client libraries in various languages with [OpenAPI Generator](https://github.com/OpenAPITools/openapi-generator) or [Swagger Codegen](https://swagger.io/tools/swagger-codegen/) to interact with this API.  # Authentication This API uses the [Basic HTTP Authentication Scheme](https://en.wikipedia.org/wiki/Basic_access_authentication).   <!-- ReDoc-Inject: <security-definitions> -->  # Throttling and rate limits All the rate limited API calls are returning information about the current rate limit period in the following HTTP headers:  | Header | Description | | :- | :- | | X-Rate-Limit-Remaining | The maximum number of requests remaining in the current rate limit period. | | X-Rate-Limit-Reset     | The time when the current rate limit period resets.        |  When the rate limit is exceeded by a request, the API returns with a `HTTP 429 - Too many requests` status along with a `Retry-After` HTTP header. 
 *
 * The version of the OpenAPI document: v1
 * Contact: support@configcat.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIFeatureFlagSettingValuesUsingSDKKeyApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIFeatureFlagSettingValuesUsingSDKKeyApi::OAIFeatureFlagSettingValuesUsingSDKKeyApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIFeatureFlagSettingValuesUsingSDKKeyApi::~OAIFeatureFlagSettingValuesUsingSDKKeyApi() {
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.configcat.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("getSettingValueBySdkkey", defaultConf);
    _serverIndices.insert("getSettingValueBySdkkey", 0);
    _serverConfigs.insert("replaceSettingValueBySdkkey", defaultConf);
    _serverIndices.insert("replaceSettingValueBySdkkey", 0);
    _serverConfigs.insert("updateSettingValueBySdkkey", defaultConf);
    _serverIndices.insert("updateSettingValueBySdkkey", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIFeatureFlagSettingValuesUsingSDKKeyApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setUsername(const QString &username) {
    _username = username;
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setPassword(const QString &password) {
    _password = password;
}


void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIFeatureFlagSettingValuesUsingSDKKeyApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIFeatureFlagSettingValuesUsingSDKKeyApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIFeatureFlagSettingValuesUsingSDKKeyApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIFeatureFlagSettingValuesUsingSDKKeyApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIFeatureFlagSettingValuesUsingSDKKeyApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::getSettingValueBySdkkey(const QString &setting_key_or_id, const ::OpenAPI::OptionalParam<QString> &x_configcat_sdkkey) {
    QString fullPath = QString(_serverConfigs["getSettingValueBySdkkey"][_serverIndices.value("getSettingValueBySdkkey")].URL()+"/v1/settings/{settingKeyOrId}/value");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString setting_key_or_idPathParam("{");
        setting_key_or_idPathParam.append("settingKeyOrId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "settingKeyOrId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"settingKeyOrId"+pathSuffix : pathPrefix;
        fullPath.replace(setting_key_or_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(setting_key_or_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    if (x_configcat_sdkkey.hasValue())
    {
        if (!::OpenAPI::toStringValue(x_configcat_sdkkey.value()).isEmpty()) {
            input.headers.insert("X-CONFIGCAT-SDKKEY", ::OpenAPI::toStringValue(x_configcat_sdkkey.value()));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFeatureFlagSettingValuesUsingSDKKeyApi::getSettingValueBySdkkeyCallback);
    connect(this, &OAIFeatureFlagSettingValuesUsingSDKKeyApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::getSettingValueBySdkkeyCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISettingValueModel_haljson output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getSettingValueBySdkkeySignal(output);
        Q_EMIT getSettingValueBySdkkeySignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getSettingValueBySdkkeySignalE(output, error_type, error_str);
        Q_EMIT getSettingValueBySdkkeySignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getSettingValueBySdkkeySignalError(output, error_type, error_str);
        Q_EMIT getSettingValueBySdkkeySignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::replaceSettingValueBySdkkey(const QString &setting_key_or_id, const OAIUpdateSettingValueModel &oai_update_setting_value_model, const ::OpenAPI::OptionalParam<QString> &reason, const ::OpenAPI::OptionalParam<QString> &x_configcat_sdkkey) {
    QString fullPath = QString(_serverConfigs["replaceSettingValueBySdkkey"][_serverIndices.value("replaceSettingValueBySdkkey")].URL()+"/v1/settings/{settingKeyOrId}/value");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString setting_key_or_idPathParam("{");
        setting_key_or_idPathParam.append("settingKeyOrId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "settingKeyOrId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"settingKeyOrId"+pathSuffix : pathPrefix;
        fullPath.replace(setting_key_or_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(setting_key_or_id)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (reason.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "reason", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("reason")).append(querySuffix).append(QUrl::toPercentEncoding(reason.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PUT");

    {

        
        QByteArray output = oai_update_setting_value_model.asJson().toUtf8();
        input.request_body.append(output);
    }
    if (x_configcat_sdkkey.hasValue())
    {
        if (!::OpenAPI::toStringValue(x_configcat_sdkkey.value()).isEmpty()) {
            input.headers.insert("X-CONFIGCAT-SDKKEY", ::OpenAPI::toStringValue(x_configcat_sdkkey.value()));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFeatureFlagSettingValuesUsingSDKKeyApi::replaceSettingValueBySdkkeyCallback);
    connect(this, &OAIFeatureFlagSettingValuesUsingSDKKeyApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::replaceSettingValueBySdkkeyCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISettingValueModel_haljson output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT replaceSettingValueBySdkkeySignal(output);
        Q_EMIT replaceSettingValueBySdkkeySignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT replaceSettingValueBySdkkeySignalE(output, error_type, error_str);
        Q_EMIT replaceSettingValueBySdkkeySignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT replaceSettingValueBySdkkeySignalError(output, error_type, error_str);
        Q_EMIT replaceSettingValueBySdkkeySignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::updateSettingValueBySdkkey(const QString &setting_key_or_id, const OAIJsonPatch &oai_json_patch, const ::OpenAPI::OptionalParam<QString> &reason, const ::OpenAPI::OptionalParam<QString> &x_configcat_sdkkey) {
    QString fullPath = QString(_serverConfigs["updateSettingValueBySdkkey"][_serverIndices.value("updateSettingValueBySdkkey")].URL()+"/v1/settings/{settingKeyOrId}/value");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString setting_key_or_idPathParam("{");
        setting_key_or_idPathParam.append("settingKeyOrId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "settingKeyOrId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"settingKeyOrId"+pathSuffix : pathPrefix;
        fullPath.replace(setting_key_or_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(setting_key_or_id)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (reason.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "reason", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("reason")).append(querySuffix).append(QUrl::toPercentEncoding(reason.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PATCH");

    {

        
        QByteArray output = oai_json_patch.asJson().toUtf8();
        input.request_body.append(output);
    }
    if (x_configcat_sdkkey.hasValue())
    {
        if (!::OpenAPI::toStringValue(x_configcat_sdkkey.value()).isEmpty()) {
            input.headers.insert("X-CONFIGCAT-SDKKEY", ::OpenAPI::toStringValue(x_configcat_sdkkey.value()));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFeatureFlagSettingValuesUsingSDKKeyApi::updateSettingValueBySdkkeyCallback);
    connect(this, &OAIFeatureFlagSettingValuesUsingSDKKeyApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::updateSettingValueBySdkkeyCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISettingValueModel_haljson output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT updateSettingValueBySdkkeySignal(output);
        Q_EMIT updateSettingValueBySdkkeySignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT updateSettingValueBySdkkeySignalE(output, error_type, error_str);
        Q_EMIT updateSettingValueBySdkkeySignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT updateSettingValueBySdkkeySignalError(output, error_type, error_str);
        Q_EMIT updateSettingValueBySdkkeySignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFeatureFlagSettingValuesUsingSDKKeyApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
