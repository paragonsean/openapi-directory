/**
 * ConfigCat Public Management API
 * **Base API URL**: https://api.configcat.com  If you prefer the swagger documentation, you can find it here: [Swagger UI](https://api.configcat.com/swagger).  The purpose of this API is to access the ConfigCat platform programmatically.  You can **Create**, **Read**, **Update** and **Delete** any entities like **Feature Flags, Configs, Environments** or **Products** within ConfigCat.   The API is based on HTTP REST, uses resource-oriented URLs, status codes and supports JSON  and JSON+HAL format. Do not use this API for accessing and evaluating feature flag values. Use the [SDKs instead](https://configcat.com/docs/sdk-reference/overview).   # OpenAPI Specification  The complete specification is publicly available here: [swagger.json](v1/swagger.json).  You can use it to generate client libraries in various languages with [OpenAPI Generator](https://github.com/OpenAPITools/openapi-generator) or [Swagger Codegen](https://swagger.io/tools/swagger-codegen/) to interact with this API.  # Authentication This API uses the [Basic HTTP Authentication Scheme](https://en.wikipedia.org/wiki/Basic_access_authentication).   <!-- ReDoc-Inject: <security-definitions> -->  # Throttling and rate limits All the rate limited API calls are returning information about the current rate limit period in the following HTTP headers:  | Header | Description | | :- | :- | | X-Rate-Limit-Remaining | The maximum number of requests remaining in the current rate limit period. | | X-Rate-Limit-Reset     | The time when the current rate limit period resets.        |  When the rate limit is exceeded by a request, the API returns with a `HTTP 429 - Too many requests` status along with a `Retry-After` HTTP header. 
 *
 * The version of the OpenAPI document: v1
 * Contact: support@configcat.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReferenceLine from './ReferenceLine';

/**
 * The ReferenceLines model module.
 * @module model/ReferenceLines
 * @version v1
 */
class ReferenceLines {
    /**
     * Constructs a new <code>ReferenceLines</code>.
     * @alias module:model/ReferenceLines
     * @param file {String} The file's name in where the code reference has been found. (Appears on the ConfigCat Dashboard)
     * @param referenceLine {module:model/ReferenceLine} 
     */
    constructor(file, referenceLine) { 
        
        ReferenceLines.initialize(this, file, referenceLine);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, file, referenceLine) { 
        obj['file'] = file;
        obj['referenceLine'] = referenceLine;
    }

    /**
     * Constructs a <code>ReferenceLines</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReferenceLines} obj Optional instance to populate.
     * @return {module:model/ReferenceLines} The populated <code>ReferenceLines</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReferenceLines();

            if (data.hasOwnProperty('file')) {
                obj['file'] = ApiClient.convertToType(data['file'], 'String');
            }
            if (data.hasOwnProperty('fileUrl')) {
                obj['fileUrl'] = ApiClient.convertToType(data['fileUrl'], 'String');
            }
            if (data.hasOwnProperty('postLines')) {
                obj['postLines'] = ApiClient.convertToType(data['postLines'], [ReferenceLine]);
            }
            if (data.hasOwnProperty('preLines')) {
                obj['preLines'] = ApiClient.convertToType(data['preLines'], [ReferenceLine]);
            }
            if (data.hasOwnProperty('referenceLine')) {
                obj['referenceLine'] = ReferenceLine.constructFromObject(data['referenceLine']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReferenceLines</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReferenceLines</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReferenceLines.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['file'] && !(typeof data['file'] === 'string' || data['file'] instanceof String)) {
            throw new Error("Expected the field `file` to be a primitive type in the JSON string but got " + data['file']);
        }
        // ensure the json data is a string
        if (data['fileUrl'] && !(typeof data['fileUrl'] === 'string' || data['fileUrl'] instanceof String)) {
            throw new Error("Expected the field `fileUrl` to be a primitive type in the JSON string but got " + data['fileUrl']);
        }
        if (data['postLines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['postLines'])) {
                throw new Error("Expected the field `postLines` to be an array in the JSON data but got " + data['postLines']);
            }
            // validate the optional field `postLines` (array)
            for (const item of data['postLines']) {
                ReferenceLine.validateJSON(item);
            };
        }
        if (data['preLines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['preLines'])) {
                throw new Error("Expected the field `preLines` to be an array in the JSON data but got " + data['preLines']);
            }
            // validate the optional field `preLines` (array)
            for (const item of data['preLines']) {
                ReferenceLine.validateJSON(item);
            };
        }
        // validate the optional field `referenceLine`
        if (data['referenceLine']) { // data not null
          ReferenceLine.validateJSON(data['referenceLine']);
        }

        return true;
    }


}

ReferenceLines.RequiredProperties = ["file", "referenceLine"];

/**
 * The file's name in where the code reference has been found. (Appears on the ConfigCat Dashboard)
 * @member {String} file
 */
ReferenceLines.prototype['file'] = undefined;

/**
 * The file's url. (Used to point to the file on the repository's website)
 * @member {String} fileUrl
 */
ReferenceLines.prototype['fileUrl'] = undefined;

/**
 * The lines after the actual reference line.
 * @member {Array.<module:model/ReferenceLine>} postLines
 */
ReferenceLines.prototype['postLines'] = undefined;

/**
 * The lines before the actual reference line.
 * @member {Array.<module:model/ReferenceLine>} preLines
 */
ReferenceLines.prototype['preLines'] = undefined;

/**
 * @member {module:model/ReferenceLine} referenceLine
 */
ReferenceLines.prototype['referenceLine'] = undefined;






export default ReferenceLines;

