# agent_os_api_v3_maintenance_call_group

AgentOsApiV3MaintenanceCallGroup - JavaScript client for agent_os_api_v3_maintenance_call_group
No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: v3-maintenance
- Package version: v3-maintenance
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install agent_os_api_v3_maintenance_call_group --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your agent_os_api_v3_maintenance_call_group from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var AgentOsApiV3MaintenanceCallGroup = require('agent_os_api_v3_maintenance_call_group');


var api = new AgentOsApiV3MaintenanceCallGroup.MaintenanceControllerApi()
var shortName = "shortName_example"; // {String} The unique client short-name
var branchID = "branchID_example"; // {String} The unique ID of the Branch
var maintenanceIssueModel = new AgentOsApiV3MaintenanceCallGroup.MaintenanceIssueModel(); // {MaintenanceIssueModel} A JSON object containing details of the maintenance job
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.maintenanceControllerCreateMaintenanceJob(shortName, branchID, maintenanceIssueModel, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://live-api.letmc.com*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*AgentOsApiV3MaintenanceCallGroup.MaintenanceControllerApi* | [**maintenanceControllerCreateMaintenanceJob**](docs/MaintenanceControllerApi.md#maintenanceControllerCreateMaintenanceJob) | **POST** /v3/maintenance/{shortName}/maintenance/{branchID}/createmaintenancejob | Create a maintenance job for a specific branch


## Documentation for Models

 - [AgentOsApiV3MaintenanceCallGroup.MaintenanceDocumentModel](docs/MaintenanceDocumentModel.md)
 - [AgentOsApiV3MaintenanceCallGroup.MaintenanceIssueModel](docs/MaintenanceIssueModel.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### apiKey


- **Type**: API key
- **API key parameter name**: ApiKey
- **Location**: HTTP header

### basic

- **Type**: HTTP basic authentication

