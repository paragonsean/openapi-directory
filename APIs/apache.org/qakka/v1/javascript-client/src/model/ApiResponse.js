/**
 * Qakka
 * API for Qakka Queue System
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Queue from './Queue';
import QueueMessage from './QueueMessage';

/**
 * The ApiResponse model module.
 * @module model/ApiResponse
 * @version v1
 */
class ApiResponse {
    /**
     * Constructs a new <code>ApiResponse</code>.
     * Response returned by most Queue API calls.
     * @alias module:model/ApiResponse
     */
    constructor() { 
        
        ApiResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ApiResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiResponse} obj Optional instance to populate.
     * @return {module:model/ApiResponse} The populated <code>ApiResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiResponse();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('queueMessages')) {
                obj['queueMessages'] = ApiClient.convertToType(data['queueMessages'], [QueueMessage]);
            }
            if (data.hasOwnProperty('queues')) {
                obj['queues'] = ApiClient.convertToType(data['queues'], [Queue]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        if (data['queueMessages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['queueMessages'])) {
                throw new Error("Expected the field `queueMessages` to be an array in the JSON data but got " + data['queueMessages']);
            }
            // validate the optional field `queueMessages` (array)
            for (const item of data['queueMessages']) {
                QueueMessage.validateJSON(item);
            };
        }
        if (data['queues']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['queues'])) {
                throw new Error("Expected the field `queues` to be an array in the JSON data but got " + data['queues']);
            }
            // validate the optional field `queues` (array)
            for (const item of data['queues']) {
                Queue.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Count of Queues or QueueMessages returned by the call.
 * @member {Number} count
 */
ApiResponse.prototype['count'] = undefined;

/**
 * Informative message intended for client.
 * @member {String} message
 */
ApiResponse.prototype['message'] = undefined;

/**
 * Queues Messages returned by the call, or empty if not applicable.
 * @member {Array.<module:model/QueueMessage>} queueMessages
 */
ApiResponse.prototype['queueMessages'] = undefined;

/**
 * Queues returned but the call, or empty if not applicable.
 * @member {Array.<module:model/Queue>} queues
 */
ApiResponse.prototype['queues'] = undefined;






export default ApiResponse;

