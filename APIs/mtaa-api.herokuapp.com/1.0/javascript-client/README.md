# mtaa_api_documentation

MtaaApiDocumentation - JavaScript client for mtaa_api_documentation
Mtaa A simple REST API to access Tanzania's location information,With mtaa API you can easily query and integrate all the location in tanzania from region level to streets from your programming language of your your choice
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0
- Package version: 1.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://github.com/HackEAC/mtaaAPI](https://github.com/HackEAC/mtaaAPI)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install mtaa_api_documentation --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your mtaa_api_documentation from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var MtaaApiDocumentation = require('mtaa_api_documentation');


var api = new MtaaApiDocumentation.DistrictsInRegionApi()
var country = "country_example"; // {String} Country name in lowercase eg( tanzania)
var region = "region_example"; // {String} Name of the region eg (Mbeya)
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.districtsInARegion(country, region, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://mtaa-api.herokuapp.com/api*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*MtaaApiDocumentation.DistrictsInRegionApi* | [**districtsInARegion**](docs/DistrictsInRegionApi.md#districtsInARegion) | **GET** /{country}/{region} | Returns all districts in region
*MtaaApiDocumentation.NeighborhoodInAStreetApi* | [**neighborhoodInAStreet**](docs/NeighborhoodInAStreetApi.md#neighborhoodInAStreet) | **GET** /{country}/{region}/{district}/{ward}/{street} | Returns all neighborhood in a street
*MtaaApiDocumentation.StreetsInAWardApi* | [**streetsInAWard**](docs/StreetsInAWardApi.md#streetsInAWard) | **GET** /{country}/{region}/{district}/{ward} | Returns all streets in a ward
*MtaaApiDocumentation.TanzaniaRegionsApi* | [**tanzaniaRegions**](docs/TanzaniaRegionsApi.md#tanzaniaRegions) | **GET** /{country} | Returns all regions present in Tanzania
*MtaaApiDocumentation.WardsInADistrictApi* | [**wardsInADistrict**](docs/WardsInADistrictApi.md#wardsInADistrict) | **GET** /{country}/{region}/{district} | Returns all wards in a district


## Documentation for Models



## Documentation for Authorization

Endpoints do not require authorization.

