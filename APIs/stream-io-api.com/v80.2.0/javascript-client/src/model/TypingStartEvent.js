/**
 * Stream Chat API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v80.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UserObject from './UserObject';

/**
 * The TypingStartEvent model module.
 * @module model/TypingStartEvent
 * @version v80.2.0
 */
class TypingStartEvent {
    /**
     * Constructs a new <code>TypingStartEvent</code>.
     * @alias module:model/TypingStartEvent
     * @param channelId {String} 
     * @param channelType {String} 
     * @param cid {String} 
     * @param createdAt {Date} 
     * @param type {String} 
     */
    constructor(channelId, channelType, cid, createdAt, type) { 
        
        TypingStartEvent.initialize(this, channelId, channelType, cid, createdAt, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channelId, channelType, cid, createdAt, type) { 
        obj['channel_id'] = channelId;
        obj['channel_type'] = channelType;
        obj['cid'] = cid;
        obj['created_at'] = createdAt;
        obj['type'] = type || 'typing.start';
    }

    /**
     * Constructs a <code>TypingStartEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TypingStartEvent} obj Optional instance to populate.
     * @return {module:model/TypingStartEvent} The populated <code>TypingStartEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TypingStartEvent();

            if (data.hasOwnProperty('channel_id')) {
                obj['channel_id'] = ApiClient.convertToType(data['channel_id'], 'String');
            }
            if (data.hasOwnProperty('channel_type')) {
                obj['channel_type'] = ApiClient.convertToType(data['channel_type'], 'String');
            }
            if (data.hasOwnProperty('cid')) {
                obj['cid'] = ApiClient.convertToType(data['cid'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('parent_id')) {
                obj['parent_id'] = ApiClient.convertToType(data['parent_id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = UserObject.constructFromObject(data['user']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TypingStartEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TypingStartEvent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TypingStartEvent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['channel_id'] && !(typeof data['channel_id'] === 'string' || data['channel_id'] instanceof String)) {
            throw new Error("Expected the field `channel_id` to be a primitive type in the JSON string but got " + data['channel_id']);
        }
        // ensure the json data is a string
        if (data['channel_type'] && !(typeof data['channel_type'] === 'string' || data['channel_type'] instanceof String)) {
            throw new Error("Expected the field `channel_type` to be a primitive type in the JSON string but got " + data['channel_type']);
        }
        // ensure the json data is a string
        if (data['cid'] && !(typeof data['cid'] === 'string' || data['cid'] instanceof String)) {
            throw new Error("Expected the field `cid` to be a primitive type in the JSON string but got " + data['cid']);
        }
        // ensure the json data is a string
        if (data['parent_id'] && !(typeof data['parent_id'] === 'string' || data['parent_id'] instanceof String)) {
            throw new Error("Expected the field `parent_id` to be a primitive type in the JSON string but got " + data['parent_id']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

TypingStartEvent.RequiredProperties = ["channel_id", "channel_type", "cid", "created_at", "type"];

/**
 * @member {String} channel_id
 */
TypingStartEvent.prototype['channel_id'] = undefined;

/**
 * @member {String} channel_type
 */
TypingStartEvent.prototype['channel_type'] = undefined;

/**
 * @member {String} cid
 */
TypingStartEvent.prototype['cid'] = undefined;

/**
 * @member {Date} created_at
 */
TypingStartEvent.prototype['created_at'] = undefined;

/**
 * @member {String} parent_id
 */
TypingStartEvent.prototype['parent_id'] = undefined;

/**
 * @member {String} type
 * @default 'typing.start'
 */
TypingStartEvent.prototype['type'] = 'typing.start';

/**
 * @member {module:model/UserObject} user
 */
TypingStartEvent.prototype['user'] = undefined;






export default TypingStartEvent;

