/**
 * Stream Chat API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v80.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChannelStateResponseFields from './ChannelStateResponseFields';

/**
 * The ChannelsResponse model module.
 * @module model/ChannelsResponse
 * @version v80.2.0
 */
class ChannelsResponse {
    /**
     * Constructs a new <code>ChannelsResponse</code>.
     * @alias module:model/ChannelsResponse
     * @param channels {Array.<module:model/ChannelStateResponseFields>} List of channels
     * @param duration {String} 
     */
    constructor(channels, duration) { 
        
        ChannelsResponse.initialize(this, channels, duration);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channels, duration) { 
        obj['channels'] = channels;
        obj['duration'] = duration;
    }

    /**
     * Constructs a <code>ChannelsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChannelsResponse} obj Optional instance to populate.
     * @return {module:model/ChannelsResponse} The populated <code>ChannelsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChannelsResponse();

            if (data.hasOwnProperty('channels')) {
                obj['channels'] = ApiClient.convertToType(data['channels'], [ChannelStateResponseFields]);
            }
            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChannelsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChannelsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChannelsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['channels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['channels'])) {
                throw new Error("Expected the field `channels` to be an array in the JSON data but got " + data['channels']);
            }
            // validate the optional field `channels` (array)
            for (const item of data['channels']) {
                ChannelStateResponseFields.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['duration'] && !(typeof data['duration'] === 'string' || data['duration'] instanceof String)) {
            throw new Error("Expected the field `duration` to be a primitive type in the JSON string but got " + data['duration']);
        }

        return true;
    }


}

ChannelsResponse.RequiredProperties = ["channels", "duration"];

/**
 * List of channels
 * @member {Array.<module:model/ChannelStateResponseFields>} channels
 */
ChannelsResponse.prototype['channels'] = undefined;

/**
 * @member {String} duration
 */
ChannelsResponse.prototype['duration'] = undefined;






export default ChannelsResponse;

