/**
 * Stream Chat API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v80.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ImageData from './ImageData';

/**
 * The Images model module.
 * @module model/Images
 * @version v80.2.0
 */
class Images {
    /**
     * Constructs a new <code>Images</code>.
     * @alias module:model/Images
     * @param fixedHeight {module:model/ImageData} 
     * @param fixedHeightDownsampled {module:model/ImageData} 
     * @param fixedHeightStill {module:model/ImageData} 
     * @param fixedWidth {module:model/ImageData} 
     * @param fixedWidthDownsampled {module:model/ImageData} 
     * @param fixedWidthStill {module:model/ImageData} 
     * @param original {module:model/ImageData} 
     */
    constructor(fixedHeight, fixedHeightDownsampled, fixedHeightStill, fixedWidth, fixedWidthDownsampled, fixedWidthStill, original) { 
        
        Images.initialize(this, fixedHeight, fixedHeightDownsampled, fixedHeightStill, fixedWidth, fixedWidthDownsampled, fixedWidthStill, original);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fixedHeight, fixedHeightDownsampled, fixedHeightStill, fixedWidth, fixedWidthDownsampled, fixedWidthStill, original) { 
        obj['fixed_height'] = fixedHeight;
        obj['fixed_height_downsampled'] = fixedHeightDownsampled;
        obj['fixed_height_still'] = fixedHeightStill;
        obj['fixed_width'] = fixedWidth;
        obj['fixed_width_downsampled'] = fixedWidthDownsampled;
        obj['fixed_width_still'] = fixedWidthStill;
        obj['original'] = original;
    }

    /**
     * Constructs a <code>Images</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Images} obj Optional instance to populate.
     * @return {module:model/Images} The populated <code>Images</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Images();

            if (data.hasOwnProperty('fixed_height')) {
                obj['fixed_height'] = ImageData.constructFromObject(data['fixed_height']);
            }
            if (data.hasOwnProperty('fixed_height_downsampled')) {
                obj['fixed_height_downsampled'] = ImageData.constructFromObject(data['fixed_height_downsampled']);
            }
            if (data.hasOwnProperty('fixed_height_still')) {
                obj['fixed_height_still'] = ImageData.constructFromObject(data['fixed_height_still']);
            }
            if (data.hasOwnProperty('fixed_width')) {
                obj['fixed_width'] = ImageData.constructFromObject(data['fixed_width']);
            }
            if (data.hasOwnProperty('fixed_width_downsampled')) {
                obj['fixed_width_downsampled'] = ImageData.constructFromObject(data['fixed_width_downsampled']);
            }
            if (data.hasOwnProperty('fixed_width_still')) {
                obj['fixed_width_still'] = ImageData.constructFromObject(data['fixed_width_still']);
            }
            if (data.hasOwnProperty('original')) {
                obj['original'] = ImageData.constructFromObject(data['original']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Images</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Images</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Images.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `fixed_height`
        if (data['fixed_height']) { // data not null
          ImageData.validateJSON(data['fixed_height']);
        }
        // validate the optional field `fixed_height_downsampled`
        if (data['fixed_height_downsampled']) { // data not null
          ImageData.validateJSON(data['fixed_height_downsampled']);
        }
        // validate the optional field `fixed_height_still`
        if (data['fixed_height_still']) { // data not null
          ImageData.validateJSON(data['fixed_height_still']);
        }
        // validate the optional field `fixed_width`
        if (data['fixed_width']) { // data not null
          ImageData.validateJSON(data['fixed_width']);
        }
        // validate the optional field `fixed_width_downsampled`
        if (data['fixed_width_downsampled']) { // data not null
          ImageData.validateJSON(data['fixed_width_downsampled']);
        }
        // validate the optional field `fixed_width_still`
        if (data['fixed_width_still']) { // data not null
          ImageData.validateJSON(data['fixed_width_still']);
        }
        // validate the optional field `original`
        if (data['original']) { // data not null
          ImageData.validateJSON(data['original']);
        }

        return true;
    }


}

Images.RequiredProperties = ["fixed_height", "fixed_height_downsampled", "fixed_height_still", "fixed_width", "fixed_width_downsampled", "fixed_width_still", "original"];

/**
 * @member {module:model/ImageData} fixed_height
 */
Images.prototype['fixed_height'] = undefined;

/**
 * @member {module:model/ImageData} fixed_height_downsampled
 */
Images.prototype['fixed_height_downsampled'] = undefined;

/**
 * @member {module:model/ImageData} fixed_height_still
 */
Images.prototype['fixed_height_still'] = undefined;

/**
 * @member {module:model/ImageData} fixed_width
 */
Images.prototype['fixed_width'] = undefined;

/**
 * @member {module:model/ImageData} fixed_width_downsampled
 */
Images.prototype['fixed_width_downsampled'] = undefined;

/**
 * @member {module:model/ImageData} fixed_width_still
 */
Images.prototype['fixed_width_still'] = undefined;

/**
 * @member {module:model/ImageData} original
 */
Images.prototype['original'] = undefined;






export default Images;

