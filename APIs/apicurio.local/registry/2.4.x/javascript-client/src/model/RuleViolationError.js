/**
 * Apicurio Registry API [v2]
 * Apicurio Registry is a datastore for standard event schemas and API designs. Apicurio Registry enables developers to manage and share the structure of their data using a REST interface. For example, client applications can dynamically push or pull the latest updates to or from the registry without needing to redeploy. Apicurio Registry also enables developers to create rules that govern how registry content can evolve over time. For example, this includes rules for content validation and version compatibility.  The Apicurio Registry REST API enables client applications to manage the artifacts in the registry. This API provides create, read, update, and delete operations for schema and API artifacts, rules, versions, and metadata.   The supported artifact types include: - Apache Avro schema - AsyncAPI specification - Google protocol buffers - GraphQL schema - JSON Schema - Kafka Connect schema - OpenAPI specification - Web Services Description Language - XML Schema Definition   **Important**: The Apicurio Registry REST API is available from `https://MY-REGISTRY-URL/apis/registry/v2` by default. Therefore you must prefix all API operation paths with `../apis/registry/v2` in this case. For example: `../apis/registry/v2/ids/globalIds/{globalId}`. 
 *
 * The version of the OpenAPI document: 2.4.x
 * Contact: apicurio@lists.jboss.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Error from './Error';
import RuleViolationCause from './RuleViolationCause';

/**
 * The RuleViolationError model module.
 * @module model/RuleViolationError
 * @version 2.4.x
 */
class RuleViolationError {
    /**
     * Constructs a new <code>RuleViolationError</code>.
     * All error responses, whether &#x60;4xx&#x60; or &#x60;5xx&#x60; will include one of these as the response body.
     * @alias module:model/RuleViolationError
     * @implements module:model/Error
     * @param causes {Array.<module:model/RuleViolationCause>} List of rule violation causes.
     */
    constructor(causes) { 
        Error.initialize(this);
        RuleViolationError.initialize(this, causes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, causes) { 
        obj['causes'] = causes;
    }

    /**
     * Constructs a <code>RuleViolationError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RuleViolationError} obj Optional instance to populate.
     * @return {module:model/RuleViolationError} The populated <code>RuleViolationError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RuleViolationError();
            Error.constructFromObject(data, obj);

            if (data.hasOwnProperty('detail')) {
                obj['detail'] = ApiClient.convertToType(data['detail'], 'String');
            }
            if (data.hasOwnProperty('error_code')) {
                obj['error_code'] = ApiClient.convertToType(data['error_code'], 'Number');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('causes')) {
                obj['causes'] = ApiClient.convertToType(data['causes'], [RuleViolationCause]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RuleViolationError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RuleViolationError</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RuleViolationError.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['detail'] && !(typeof data['detail'] === 'string' || data['detail'] instanceof String)) {
            throw new Error("Expected the field `detail` to be a primitive type in the JSON string but got " + data['detail']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['causes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['causes'])) {
                throw new Error("Expected the field `causes` to be an array in the JSON data but got " + data['causes']);
            }
            // validate the optional field `causes` (array)
            for (const item of data['causes']) {
                RuleViolationCause.validateJSON(item);
            };
        }

        return true;
    }


}

RuleViolationError.RequiredProperties = ["causes"];

/**
 * Full details about the error.  This might contain a server stack trace, for example.
 * @member {String} detail
 */
RuleViolationError.prototype['detail'] = undefined;

/**
 * The server-side error code.
 * @member {Number} error_code
 */
RuleViolationError.prototype['error_code'] = undefined;

/**
 * The short error message.
 * @member {String} message
 */
RuleViolationError.prototype['message'] = undefined;

/**
 * The error name - typically the classname of the exception thrown by the server.
 * @member {String} name
 */
RuleViolationError.prototype['name'] = undefined;

/**
 * List of rule violation causes.
 * @member {Array.<module:model/RuleViolationCause>} causes
 */
RuleViolationError.prototype['causes'] = undefined;


// Implement Error interface:
/**
 * Full details about the error.  This might contain a server stack trace, for example.
 * @member {String} detail
 */
Error.prototype['detail'] = undefined;
/**
 * The server-side error code.
 * @member {Number} error_code
 */
Error.prototype['error_code'] = undefined;
/**
 * The short error message.
 * @member {String} message
 */
Error.prototype['message'] = undefined;
/**
 * The error name - typically the classname of the exception thrown by the server.
 * @member {String} name
 */
Error.prototype['name'] = undefined;




export default RuleViolationError;

