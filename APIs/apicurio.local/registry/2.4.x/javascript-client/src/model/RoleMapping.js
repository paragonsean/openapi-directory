/**
 * Apicurio Registry API [v2]
 * Apicurio Registry is a datastore for standard event schemas and API designs. Apicurio Registry enables developers to manage and share the structure of their data using a REST interface. For example, client applications can dynamically push or pull the latest updates to or from the registry without needing to redeploy. Apicurio Registry also enables developers to create rules that govern how registry content can evolve over time. For example, this includes rules for content validation and version compatibility.  The Apicurio Registry REST API enables client applications to manage the artifacts in the registry. This API provides create, read, update, and delete operations for schema and API artifacts, rules, versions, and metadata.   The supported artifact types include: - Apache Avro schema - AsyncAPI specification - Google protocol buffers - GraphQL schema - JSON Schema - Kafka Connect schema - OpenAPI specification - Web Services Description Language - XML Schema Definition   **Important**: The Apicurio Registry REST API is available from `https://MY-REGISTRY-URL/apis/registry/v2` by default. Therefore you must prefix all API operation paths with `../apis/registry/v2` in this case. For example: `../apis/registry/v2/ids/globalIds/{globalId}`. 
 *
 * The version of the OpenAPI document: 2.4.x
 * Contact: apicurio@lists.jboss.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RoleType from './RoleType';

/**
 * The RoleMapping model module.
 * @module model/RoleMapping
 * @version 2.4.x
 */
class RoleMapping {
    /**
     * Constructs a new <code>RoleMapping</code>.
     * The mapping between a user/principal and their role.
     * @alias module:model/RoleMapping
     * @param principalId {String} 
     * @param role {module:model/RoleType} 
     */
    constructor(principalId, role) { 
        
        RoleMapping.initialize(this, principalId, role);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, principalId, role) { 
        obj['principalId'] = principalId;
        obj['role'] = role;
    }

    /**
     * Constructs a <code>RoleMapping</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoleMapping} obj Optional instance to populate.
     * @return {module:model/RoleMapping} The populated <code>RoleMapping</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoleMapping();

            if (data.hasOwnProperty('principalId')) {
                obj['principalId'] = ApiClient.convertToType(data['principalId'], 'String');
            }
            if (data.hasOwnProperty('principalName')) {
                obj['principalName'] = ApiClient.convertToType(data['principalName'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = RoleType.constructFromObject(data['role']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoleMapping</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoleMapping</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoleMapping.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['principalId'] && !(typeof data['principalId'] === 'string' || data['principalId'] instanceof String)) {
            throw new Error("Expected the field `principalId` to be a primitive type in the JSON string but got " + data['principalId']);
        }
        // ensure the json data is a string
        if (data['principalName'] && !(typeof data['principalName'] === 'string' || data['principalName'] instanceof String)) {
            throw new Error("Expected the field `principalName` to be a primitive type in the JSON string but got " + data['principalName']);
        }

        return true;
    }


}

RoleMapping.RequiredProperties = ["principalId", "role"];

/**
 * 
 * @member {String} principalId
 */
RoleMapping.prototype['principalId'] = undefined;

/**
 * A friendly name for the principal.
 * @member {String} principalName
 */
RoleMapping.prototype['principalName'] = undefined;

/**
 * @member {module:model/RoleType} role
 */
RoleMapping.prototype['role'] = undefined;






export default RoleMapping;

