/**
 * Severa Public Rest API Documentation
 * The API uses OAuth2 client creadentials flow. To get the Bearer token for the resources you have to request the token by using route '/token', found from Authentication controller.    You need to provide a Client_Id, client_secret and scope needed. The client_id and client_secret can be obtained from Severa UI Rest Api settings section.    After authentication, calls need to use the Bearer token as authorization header (Bearer {accessToken}). The calls also need to have Client_Id header.    The access token can be refreshed from '/refreshtoken' route using the refresh token which was obtained from the authentication.    
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MoneyOutputModel from './MoneyOutputModel';
import Workweek from './Workweek';

/**
 * The UserWorkContractSubModel model module.
 * @module model/UserWorkContractSubModel
 * @version 1.0
 */
class UserWorkContractSubModel {
    /**
     * Constructs a new <code>UserWorkContractSubModel</code>.
     * @alias module:model/UserWorkContractSubModel
     * @param startDate {Date} 
     * @param title {String} 
     */
    constructor(startDate, title) { 
        
        UserWorkContractSubModel.initialize(this, startDate, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, startDate, title) { 
        obj['isFlextimeActive'] = true;
        obj['startDate'] = startDate;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>UserWorkContractSubModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserWorkContractSubModel} obj Optional instance to populate.
     * @return {module:model/UserWorkContractSubModel} The populated <code>UserWorkContractSubModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserWorkContractSubModel();

            if (data.hasOwnProperty('dailyHours')) {
                obj['dailyHours'] = ApiClient.convertToType(data['dailyHours'], 'Number');
            }
            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            if (data.hasOwnProperty('flextimeLimitPerDay')) {
                obj['flextimeLimitPerDay'] = ApiClient.convertToType(data['flextimeLimitPerDay'], 'Number');
            }
            if (data.hasOwnProperty('guid')) {
                obj['guid'] = ApiClient.convertToType(data['guid'], 'String');
            }
            if (data.hasOwnProperty('hourCost')) {
                obj['hourCost'] = MoneyOutputModel.constructFromObject(data['hourCost']);
            }
            if (data.hasOwnProperty('isFlextimeActive')) {
                obj['isFlextimeActive'] = ApiClient.convertToType(data['isFlextimeActive'], 'Boolean');
            }
            if (data.hasOwnProperty('isOvertimeAllowed')) {
                obj['isOvertimeAllowed'] = ApiClient.convertToType(data['isOvertimeAllowed'], 'Boolean');
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('workWeek')) {
                obj['workWeek'] = ApiClient.convertToType(data['workWeek'], [Workweek]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserWorkContractSubModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserWorkContractSubModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserWorkContractSubModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['guid'] && !(typeof data['guid'] === 'string' || data['guid'] instanceof String)) {
            throw new Error("Expected the field `guid` to be a primitive type in the JSON string but got " + data['guid']);
        }
        // validate the optional field `hourCost`
        if (data['hourCost']) { // data not null
          MoneyOutputModel.validateJSON(data['hourCost']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['workWeek'])) {
            throw new Error("Expected the field `workWeek` to be an array in the JSON data but got " + data['workWeek']);
        }

        return true;
    }


}

UserWorkContractSubModel.RequiredProperties = ["startDate", "title"];

/**
 * @member {Number} dailyHours
 */
UserWorkContractSubModel.prototype['dailyHours'] = undefined;

/**
 * @member {Date} endDate
 */
UserWorkContractSubModel.prototype['endDate'] = undefined;

/**
 * @member {Number} flextimeLimitPerDay
 */
UserWorkContractSubModel.prototype['flextimeLimitPerDay'] = undefined;

/**
 * @member {String} guid
 */
UserWorkContractSubModel.prototype['guid'] = undefined;

/**
 * @member {module:model/MoneyOutputModel} hourCost
 */
UserWorkContractSubModel.prototype['hourCost'] = undefined;

/**
 * @member {Boolean} isFlextimeActive
 * @default true
 */
UserWorkContractSubModel.prototype['isFlextimeActive'] = true;

/**
 * @member {Boolean} isOvertimeAllowed
 */
UserWorkContractSubModel.prototype['isOvertimeAllowed'] = undefined;

/**
 * @member {Date} startDate
 */
UserWorkContractSubModel.prototype['startDate'] = undefined;

/**
 * @member {String} title
 */
UserWorkContractSubModel.prototype['title'] = undefined;

/**
 * @member {Array.<module:model/Workweek>} workWeek
 */
UserWorkContractSubModel.prototype['workWeek'] = undefined;






export default UserWorkContractSubModel;

