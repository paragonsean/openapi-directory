/**
 * Payments Gateway API
 * >ℹ️ Onboarding guide  >  > Check the new [Payments onboarding guide](https://developers.vtex.com/docs/guides/payments-overview). We created this guide to improve the onboarding experience for developers at VTEX. It assembles all documentation on our Developer Portal about Payments and is organized by focusing on the developer's journey.    The Payments Gateway API allows you to get payment data and process your store's transactions.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Action from './Action';
import Request from './Request';

/**
 * The TransactionSettlementDetails model module.
 * @module model/TransactionSettlementDetails
 * @version 1.0
 */
class TransactionSettlementDetails {
    /**
     * Constructs a new <code>TransactionSettlementDetails</code>.
     * @alias module:model/TransactionSettlementDetails
     * @param actions {Array.<module:model/Action>} 
     * @param requests {Array.<module:model/Request>} 
     */
    constructor(actions, requests) { 
        
        TransactionSettlementDetails.initialize(this, actions, requests);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, actions, requests) { 
        obj['actions'] = actions;
        obj['requests'] = requests;
    }

    /**
     * Constructs a <code>TransactionSettlementDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransactionSettlementDetails} obj Optional instance to populate.
     * @return {module:model/TransactionSettlementDetails} The populated <code>TransactionSettlementDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransactionSettlementDetails();

            if (data.hasOwnProperty('actions')) {
                obj['actions'] = ApiClient.convertToType(data['actions'], [Action]);
            }
            if (data.hasOwnProperty('requests')) {
                obj['requests'] = ApiClient.convertToType(data['requests'], [Request]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransactionSettlementDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransactionSettlementDetails</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransactionSettlementDetails.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['actions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['actions'])) {
                throw new Error("Expected the field `actions` to be an array in the JSON data but got " + data['actions']);
            }
            // validate the optional field `actions` (array)
            for (const item of data['actions']) {
                Action.validateJSON(item);
            };
        }
        if (data['requests']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['requests'])) {
                throw new Error("Expected the field `requests` to be an array in the JSON data but got " + data['requests']);
            }
            // validate the optional field `requests` (array)
            for (const item of data['requests']) {
                Request.validateJSON(item);
            };
        }

        return true;
    }


}

TransactionSettlementDetails.RequiredProperties = ["actions", "requests"];

/**
 * 
 * @member {Array.<module:model/Action>} actions
 */
TransactionSettlementDetails.prototype['actions'] = undefined;

/**
 * 
 * @member {Array.<module:model/Request>} requests
 */
TransactionSettlementDetails.prototype['requests'] = undefined;






export default TransactionSettlementDetails;

