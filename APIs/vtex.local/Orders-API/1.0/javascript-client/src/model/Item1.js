/**
 * Orders API
 *     >Check the new [Orders onboarding guide](https://developers.vtex.com/docs/guides/orders-overview). We created this guide to improve the onboarding experience for developers at VTEX. It assembles all documentation on our Developer Portal about Orders and is organized by focusing on the developer's journey.    Receive, process, and manage every order in your store through our Order Management API.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Item1 model module.
 * @module model/Item1
 * @version 1.0
 */
class Item1 {
    /**
     * Constructs a new <code>Item1</code>.
     * @alias module:model/Item1
     * @param id {String} ID of the SKU being invoiced.
     * @param price {Number} Total price of the SKU being invoiced in cents. Do not use any decimal separator. For instance, `$24.99` should be represented as `2499`.
     * @param quantity {Number} Quantity currently in inventory of the SKU being invoiced.
     */
    constructor(id, price, quantity) { 
        
        Item1.initialize(this, id, price, quantity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, price, quantity) { 
        obj['description'] = '335';
        obj['id'] = id;
        obj['price'] = price || 2499;
        obj['quantity'] = quantity || 2;
    }

    /**
     * Constructs a <code>Item1</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Item1} obj Optional instance to populate.
     * @return {module:model/Item1} The populated <code>Item1</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Item1();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Item1</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Item1</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Item1.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }

        return true;
    }


}

Item1.RequiredProperties = ["id", "price", "quantity"];

/**
 * You can use this field for additional information about the SKU being invoiced.
 * @member {String} description
 * @default '335'
 */
Item1.prototype['description'] = '335';

/**
 * ID of the SKU being invoiced.
 * @member {String} id
 */
Item1.prototype['id'] = undefined;

/**
 * Total price of the SKU being invoiced in cents. Do not use any decimal separator. For instance, `$24.99` should be represented as `2499`.
 * @member {Number} price
 * @default 2499
 */
Item1.prototype['price'] = 2499;

/**
 * Quantity currently in inventory of the SKU being invoiced.
 * @member {Number} quantity
 * @default 2
 */
Item1.prototype['quantity'] = 2;






export default Item1;

