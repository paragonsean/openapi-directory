/**
 * Orders API
 *     >Check the new [Orders onboarding guide](https://developers.vtex.com/docs/guides/orders-overview). We created this guide to improve the onboarding experience for developers at VTEX. It assembles all documentation on our Developer Portal about Orders and is organized by focusing on the developer's journey.    Receive, process, and manage every order in your store through our Order Management API.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Payment1 from './Payment1';

/**
 * The GetPaymenttransaction model module.
 * @module model/GetPaymenttransaction
 * @version 1.0
 */
class GetPaymenttransaction {
    /**
     * Constructs a new <code>GetPaymenttransaction</code>.
     * @alias module:model/GetPaymenttransaction
     * @param isActive {Boolean} If it is an active transaction (`true`) or not (`false`).
     * @param merchantName {String} Transaction merchant's name.
     * @param payments {Array.<module:model/Payment1>} Payments' details object.
     * @param status {String} Transaction status.
     * @param transactionId {String} Transaction ID.
     */
    constructor(isActive, merchantName, payments, status, transactionId) { 
        
        GetPaymenttransaction.initialize(this, isActive, merchantName, payments, status, transactionId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, isActive, merchantName, payments, status, transactionId) { 
        obj['isActive'] = isActive;
        obj['merchantName'] = merchantName;
        obj['payments'] = payments;
        obj['status'] = status;
        obj['transactionId'] = transactionId;
    }

    /**
     * Constructs a <code>GetPaymenttransaction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetPaymenttransaction} obj Optional instance to populate.
     * @return {module:model/GetPaymenttransaction} The populated <code>GetPaymenttransaction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetPaymenttransaction();

            if (data.hasOwnProperty('isActive')) {
                obj['isActive'] = ApiClient.convertToType(data['isActive'], 'Boolean');
            }
            if (data.hasOwnProperty('merchantName')) {
                obj['merchantName'] = ApiClient.convertToType(data['merchantName'], 'String');
            }
            if (data.hasOwnProperty('payments')) {
                obj['payments'] = ApiClient.convertToType(data['payments'], [Payment1]);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('transactionId')) {
                obj['transactionId'] = ApiClient.convertToType(data['transactionId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetPaymenttransaction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetPaymenttransaction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetPaymenttransaction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['merchantName'] && !(typeof data['merchantName'] === 'string' || data['merchantName'] instanceof String)) {
            throw new Error("Expected the field `merchantName` to be a primitive type in the JSON string but got " + data['merchantName']);
        }
        if (data['payments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['payments'])) {
                throw new Error("Expected the field `payments` to be an array in the JSON data but got " + data['payments']);
            }
            // validate the optional field `payments` (array)
            for (const item of data['payments']) {
                Payment1.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['transactionId'] && !(typeof data['transactionId'] === 'string' || data['transactionId'] instanceof String)) {
            throw new Error("Expected the field `transactionId` to be a primitive type in the JSON string but got " + data['transactionId']);
        }

        return true;
    }


}

GetPaymenttransaction.RequiredProperties = ["isActive", "merchantName", "payments", "status", "transactionId"];

/**
 * If it is an active transaction (`true`) or not (`false`).
 * @member {Boolean} isActive
 */
GetPaymenttransaction.prototype['isActive'] = undefined;

/**
 * Transaction merchant's name.
 * @member {String} merchantName
 */
GetPaymenttransaction.prototype['merchantName'] = undefined;

/**
 * Payments' details object.
 * @member {Array.<module:model/Payment1>} payments
 */
GetPaymenttransaction.prototype['payments'] = undefined;

/**
 * Transaction status.
 * @member {String} status
 */
GetPaymenttransaction.prototype['status'] = undefined;

/**
 * Transaction ID.
 * @member {String} transactionId
 */
GetPaymenttransaction.prototype['transactionId'] = undefined;






export default GetPaymenttransaction;

