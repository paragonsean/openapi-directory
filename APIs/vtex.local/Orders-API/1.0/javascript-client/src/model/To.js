/**
 * Orders API
 *     >Check the new [Orders onboarding guide](https://developers.vtex.com/docs/guides/orders-overview). We created this guide to improve the onboarding experience for developers at VTEX. It assembles all documentation on our Developer Portal about Orders and is organized by focusing on the developer's journey.    Receive, process, and manage every order in your store through our Order Management API.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The To model module.
 * @module model/To
 * @version 1.0
 */
class To {
    /**
     * Constructs a new <code>To</code>.
     * Conversation receiver.
     * @alias module:model/To
     * @param aliasMaskType {Number} Conversation tracker mask type.
     * @param conversationRelatedTo {String} Related order ID.
     * @param conversationSubject {String} Conversation subject.
     * @param email {String} Receiver's email.
     * @param emailAlias {String} Sender transactional tracker email.
     * @param name {String} Receiver's name.
     * @param role {String} If it is a customer or null, for transactional emails.
     */
    constructor(aliasMaskType, conversationRelatedTo, conversationSubject, email, emailAlias, name, role) { 
        
        To.initialize(this, aliasMaskType, conversationRelatedTo, conversationSubject, email, emailAlias, name, role);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, aliasMaskType, conversationRelatedTo, conversationSubject, email, emailAlias, name, role) { 
        obj['aliasMaskType'] = aliasMaskType;
        obj['conversationRelatedTo'] = conversationRelatedTo;
        obj['conversationSubject'] = conversationSubject;
        obj['email'] = email;
        obj['emailAlias'] = emailAlias;
        obj['name'] = name;
        obj['role'] = role;
    }

    /**
     * Constructs a <code>To</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/To} obj Optional instance to populate.
     * @return {module:model/To} The populated <code>To</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new To();

            if (data.hasOwnProperty('aliasMaskType')) {
                obj['aliasMaskType'] = ApiClient.convertToType(data['aliasMaskType'], 'Number');
            }
            if (data.hasOwnProperty('conversationRelatedTo')) {
                obj['conversationRelatedTo'] = ApiClient.convertToType(data['conversationRelatedTo'], 'String');
            }
            if (data.hasOwnProperty('conversationSubject')) {
                obj['conversationSubject'] = ApiClient.convertToType(data['conversationSubject'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('emailAlias')) {
                obj['emailAlias'] = ApiClient.convertToType(data['emailAlias'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>To</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>To</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of To.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['conversationRelatedTo'] && !(typeof data['conversationRelatedTo'] === 'string' || data['conversationRelatedTo'] instanceof String)) {
            throw new Error("Expected the field `conversationRelatedTo` to be a primitive type in the JSON string but got " + data['conversationRelatedTo']);
        }
        // ensure the json data is a string
        if (data['conversationSubject'] && !(typeof data['conversationSubject'] === 'string' || data['conversationSubject'] instanceof String)) {
            throw new Error("Expected the field `conversationSubject` to be a primitive type in the JSON string but got " + data['conversationSubject']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['emailAlias'] && !(typeof data['emailAlias'] === 'string' || data['emailAlias'] instanceof String)) {
            throw new Error("Expected the field `emailAlias` to be a primitive type in the JSON string but got " + data['emailAlias']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }

        return true;
    }


}

To.RequiredProperties = ["aliasMaskType", "conversationRelatedTo", "conversationSubject", "email", "emailAlias", "name", "role"];

/**
 * Conversation tracker mask type.
 * @member {Number} aliasMaskType
 */
To.prototype['aliasMaskType'] = undefined;

/**
 * Related order ID.
 * @member {String} conversationRelatedTo
 */
To.prototype['conversationRelatedTo'] = undefined;

/**
 * Conversation subject.
 * @member {String} conversationSubject
 */
To.prototype['conversationSubject'] = undefined;

/**
 * Receiver's email.
 * @member {String} email
 */
To.prototype['email'] = undefined;

/**
 * Sender transactional tracker email.
 * @member {String} emailAlias
 */
To.prototype['emailAlias'] = undefined;

/**
 * Receiver's name.
 * @member {String} name
 */
To.prototype['name'] = undefined;

/**
 * If it is a customer or null, for transactional emails.
 * @member {String} role
 */
To.prototype['role'] = undefined;






export default To;

