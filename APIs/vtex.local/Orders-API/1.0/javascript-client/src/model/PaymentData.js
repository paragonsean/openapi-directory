/**
 * Orders API
 *     >Check the new [Orders onboarding guide](https://developers.vtex.com/docs/guides/orders-overview). We created this guide to improve the onboarding experience for developers at VTEX. It assembles all documentation on our Developer Portal about Orders and is organized by focusing on the developer's journey.    Receive, process, and manage every order in your store through our Order Management API.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Transaction from './Transaction';

/**
 * The PaymentData model module.
 * @module model/PaymentData
 * @version 1.0
 */
class PaymentData {
    /**
     * Constructs a new <code>PaymentData</code>.
     * Object with information about the payment.
     * @alias module:model/PaymentData
     * @param transactions {Array.<module:model/Transaction>} Information about financial transactions.
     */
    constructor(transactions) { 
        
        PaymentData.initialize(this, transactions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, transactions) { 
        obj['transactions'] = transactions;
    }

    /**
     * Constructs a <code>PaymentData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentData} obj Optional instance to populate.
     * @return {module:model/PaymentData} The populated <code>PaymentData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentData();

            if (data.hasOwnProperty('giftCards')) {
                obj['giftCards'] = ApiClient.convertToType(data['giftCards'], [Object]);
            }
            if (data.hasOwnProperty('transactions')) {
                obj['transactions'] = ApiClient.convertToType(data['transactions'], [Transaction]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['giftCards'])) {
            throw new Error("Expected the field `giftCards` to be an array in the JSON data but got " + data['giftCards']);
        }
        if (data['transactions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['transactions'])) {
                throw new Error("Expected the field `transactions` to be an array in the JSON data but got " + data['transactions']);
            }
            // validate the optional field `transactions` (array)
            for (const item of data['transactions']) {
                Transaction.validateJSON(item);
            };
        }

        return true;
    }


}

PaymentData.RequiredProperties = ["transactions"];

/**
 * Array with information about Gift Cards.
 * @member {Array.<Object>} giftCards
 */
PaymentData.prototype['giftCards'] = undefined;

/**
 * Information about financial transactions.
 * @member {Array.<module:model/Transaction>} transactions
 */
PaymentData.prototype['transactions'] = undefined;






export default PaymentData;

