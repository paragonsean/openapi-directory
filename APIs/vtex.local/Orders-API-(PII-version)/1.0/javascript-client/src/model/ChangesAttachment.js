/**
 * Orders API (PII version)
 * Endpoints that deal with order management. New version of the orders API.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChangesDatum from './ChangesDatum';

/**
 * The ChangesAttachment model module.
 * @module model/ChangesAttachment
 * @version 1.0
 */
class ChangesAttachment {
    /**
     * Constructs a new <code>ChangesAttachment</code>.
     * @alias module:model/ChangesAttachment
     * @param changesData {Array.<module:model/ChangesDatum>} Array with data on order changes.
     * @param id {String} Change ID.
     */
    constructor(changesData, id) { 
        
        ChangesAttachment.initialize(this, changesData, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, changesData, id) { 
        obj['changesData'] = changesData;
        obj['id'] = id;
    }

    /**
     * Constructs a <code>ChangesAttachment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChangesAttachment} obj Optional instance to populate.
     * @return {module:model/ChangesAttachment} The populated <code>ChangesAttachment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChangesAttachment();

            if (data.hasOwnProperty('changesData')) {
                obj['changesData'] = ApiClient.convertToType(data['changesData'], [ChangesDatum]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChangesAttachment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChangesAttachment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChangesAttachment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['changesData']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['changesData'])) {
                throw new Error("Expected the field `changesData` to be an array in the JSON data but got " + data['changesData']);
            }
            // validate the optional field `changesData` (array)
            for (const item of data['changesData']) {
                ChangesDatum.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }

        return true;
    }


}

ChangesAttachment.RequiredProperties = ["changesData", "id"];

/**
 * Array with data on order changes.
 * @member {Array.<module:model/ChangesDatum>} changesData
 */
ChangesAttachment.prototype['changesData'] = undefined;

/**
 * Change ID.
 * @member {String} id
 */
ChangesAttachment.prototype['id'] = undefined;






export default ChangesAttachment;

