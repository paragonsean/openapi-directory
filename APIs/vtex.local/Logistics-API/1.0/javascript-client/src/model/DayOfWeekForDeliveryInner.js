/**
 * Logistics API
 *     >Check the [Fulfillment onboarding guide](https://developers.vtex.com/docs/guides/fulfillment). We created this guide to improve the onboarding experience for developers at VTEX. It assembles all documentation on our Developer Portal about fulfillment and is organized by focusing on the developer's journey.    Logistics or fulfillment is the module responsible for shipping calculation and inventory management.     The variable `{{environment}}` can be filled with `vtexcommercestable` or `vtexcommercebeta`, depending on the environment you want to test.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DayOfWeekForDeliveryInnerDeliveryRangesInner from './DayOfWeekForDeliveryInnerDeliveryRangesInner';

/**
 * The DayOfWeekForDeliveryInner model module.
 * @module model/DayOfWeekForDeliveryInner
 * @version 1.0
 */
class DayOfWeekForDeliveryInner {
    /**
     * Constructs a new <code>DayOfWeekForDeliveryInner</code>.
     * @alias module:model/DayOfWeekForDeliveryInner
     * @param dayOfWeek {Number} Day of the week, from `0` (sunday) to `6` (saturday).
     * @param deliveryRanges {Array.<module:model/DayOfWeekForDeliveryInnerDeliveryRangesInner>} Reservation windows for scheduled delivery.
     */
    constructor(dayOfWeek, deliveryRanges) { 
        
        DayOfWeekForDeliveryInner.initialize(this, dayOfWeek, deliveryRanges);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dayOfWeek, deliveryRanges) { 
        obj['dayOfWeek'] = dayOfWeek;
        obj['deliveryRanges'] = deliveryRanges;
    }

    /**
     * Constructs a <code>DayOfWeekForDeliveryInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DayOfWeekForDeliveryInner} obj Optional instance to populate.
     * @return {module:model/DayOfWeekForDeliveryInner} The populated <code>DayOfWeekForDeliveryInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DayOfWeekForDeliveryInner();

            if (data.hasOwnProperty('dayOfWeek')) {
                obj['dayOfWeek'] = ApiClient.convertToType(data['dayOfWeek'], 'Number');
            }
            if (data.hasOwnProperty('deliveryRanges')) {
                obj['deliveryRanges'] = ApiClient.convertToType(data['deliveryRanges'], [DayOfWeekForDeliveryInnerDeliveryRangesInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DayOfWeekForDeliveryInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DayOfWeekForDeliveryInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DayOfWeekForDeliveryInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['deliveryRanges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['deliveryRanges'])) {
                throw new Error("Expected the field `deliveryRanges` to be an array in the JSON data but got " + data['deliveryRanges']);
            }
            // validate the optional field `deliveryRanges` (array)
            for (const item of data['deliveryRanges']) {
                DayOfWeekForDeliveryInnerDeliveryRangesInner.validateJSON(item);
            };
        }

        return true;
    }


}

DayOfWeekForDeliveryInner.RequiredProperties = ["dayOfWeek", "deliveryRanges"];

/**
 * Day of the week, from `0` (sunday) to `6` (saturday).
 * @member {Number} dayOfWeek
 */
DayOfWeekForDeliveryInner.prototype['dayOfWeek'] = undefined;

/**
 * Reservation windows for scheduled delivery.
 * @member {Array.<module:model/DayOfWeekForDeliveryInnerDeliveryRangesInner>} deliveryRanges
 */
DayOfWeekForDeliveryInner.prototype['deliveryRanges'] = undefined;






export default DayOfWeekForDeliveryInner;

