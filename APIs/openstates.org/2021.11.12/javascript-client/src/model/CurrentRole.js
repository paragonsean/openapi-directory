/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import District from './District';
import OrgClassification from './OrgClassification';

/**
 * The CurrentRole model module.
 * @module model/CurrentRole
 * @version 2021.11.12
 */
class CurrentRole {
    /**
     * Constructs a new <code>CurrentRole</code>.
     * @alias module:model/CurrentRole
     * @param orgClassification {module:model/OrgClassification} 
     * @param title {String} 
     */
    constructor(orgClassification, title) { 
        
        CurrentRole.initialize(this, orgClassification, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orgClassification, title) { 
        obj['division_id'] = '';
        obj['org_classification'] = orgClassification;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>CurrentRole</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurrentRole} obj Optional instance to populate.
     * @return {module:model/CurrentRole} The populated <code>CurrentRole</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurrentRole();

            if (data.hasOwnProperty('district')) {
                obj['district'] = District.constructFromObject(data['district']);
            }
            if (data.hasOwnProperty('division_id')) {
                obj['division_id'] = ApiClient.convertToType(data['division_id'], 'String');
            }
            if (data.hasOwnProperty('org_classification')) {
                obj['org_classification'] = ApiClient.convertToType(data['org_classification'], OrgClassification);
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurrentRole</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurrentRole</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CurrentRole.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `district`
        if (data['district']) { // data not null
          District.validateJSON(data['district']);
        }
        // ensure the json data is a string
        if (data['division_id'] && !(typeof data['division_id'] === 'string' || data['division_id'] instanceof String)) {
            throw new Error("Expected the field `division_id` to be a primitive type in the JSON string but got " + data['division_id']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

CurrentRole.RequiredProperties = ["org_classification", "title"];

/**
 * @member {module:model/District} district
 */
CurrentRole.prototype['district'] = undefined;

/**
 * @member {String} division_id
 * @default ''
 */
CurrentRole.prototype['division_id'] = '';

/**
 * @member {module:model/OrgClassification} org_classification
 */
CurrentRole.prototype['org_classification'] = undefined;

/**
 * @member {String} title
 */
CurrentRole.prototype['title'] = undefined;






export default CurrentRole;

