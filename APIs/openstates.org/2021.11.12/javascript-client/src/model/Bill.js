/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BillAbstract from './BillAbstract';
import BillAction from './BillAction';
import BillDocumentOrVersion from './BillDocumentOrVersion';
import BillIdentifier from './BillIdentifier';
import BillSponsorship from './BillSponsorship';
import BillTitle from './BillTitle';
import CompactJurisdiction from './CompactJurisdiction';
import Link from './Link';
import Organization from './Organization';
import RelatedBill from './RelatedBill';
import VoteEvent from './VoteEvent';

/**
 * The Bill model module.
 * @module model/Bill
 * @version 2021.11.12
 */
class Bill {
    /**
     * Constructs a new <code>Bill</code>.
     * @alias module:model/Bill
     * @param createdAt {Date} 
     * @param fromOrganization {module:model/Organization} 
     * @param id {String} 
     * @param identifier {String} 
     * @param jurisdiction {module:model/CompactJurisdiction} 
     * @param openstatesUrl {String} 
     * @param session {String} 
     * @param title {String} 
     * @param updatedAt {Date} 
     */
    constructor(createdAt, fromOrganization, id, identifier, jurisdiction, openstatesUrl, session, title, updatedAt) { 
        
        Bill.initialize(this, createdAt, fromOrganization, id, identifier, jurisdiction, openstatesUrl, session, title, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, createdAt, fromOrganization, id, identifier, jurisdiction, openstatesUrl, session, title, updatedAt) { 
        obj['created_at'] = createdAt;
        obj['first_action_date'] = '';
        obj['from_organization'] = fromOrganization;
        obj['id'] = id;
        obj['identifier'] = identifier;
        obj['jurisdiction'] = jurisdiction;
        obj['latest_action_date'] = '';
        obj['latest_action_description'] = '';
        obj['latest_passage_date'] = '';
        obj['openstates_url'] = openstatesUrl;
        obj['session'] = session;
        obj['title'] = title;
        obj['updated_at'] = updatedAt;
    }

    /**
     * Constructs a <code>Bill</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Bill} obj Optional instance to populate.
     * @return {module:model/Bill} The populated <code>Bill</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Bill();

            if (data.hasOwnProperty('abstracts')) {
                obj['abstracts'] = ApiClient.convertToType(data['abstracts'], [BillAbstract]);
            }
            if (data.hasOwnProperty('actions')) {
                obj['actions'] = ApiClient.convertToType(data['actions'], [BillAction]);
            }
            if (data.hasOwnProperty('classification')) {
                obj['classification'] = ApiClient.convertToType(data['classification'], ['String']);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('documents')) {
                obj['documents'] = ApiClient.convertToType(data['documents'], [BillDocumentOrVersion]);
            }
            if (data.hasOwnProperty('extras')) {
                obj['extras'] = ApiClient.convertToType(data['extras'], Object);
            }
            if (data.hasOwnProperty('first_action_date')) {
                obj['first_action_date'] = ApiClient.convertToType(data['first_action_date'], 'String');
            }
            if (data.hasOwnProperty('from_organization')) {
                obj['from_organization'] = Organization.constructFromObject(data['from_organization']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('identifier')) {
                obj['identifier'] = ApiClient.convertToType(data['identifier'], 'String');
            }
            if (data.hasOwnProperty('jurisdiction')) {
                obj['jurisdiction'] = CompactJurisdiction.constructFromObject(data['jurisdiction']);
            }
            if (data.hasOwnProperty('latest_action_date')) {
                obj['latest_action_date'] = ApiClient.convertToType(data['latest_action_date'], 'String');
            }
            if (data.hasOwnProperty('latest_action_description')) {
                obj['latest_action_description'] = ApiClient.convertToType(data['latest_action_description'], 'String');
            }
            if (data.hasOwnProperty('latest_passage_date')) {
                obj['latest_passage_date'] = ApiClient.convertToType(data['latest_passage_date'], 'String');
            }
            if (data.hasOwnProperty('openstates_url')) {
                obj['openstates_url'] = ApiClient.convertToType(data['openstates_url'], 'String');
            }
            if (data.hasOwnProperty('other_identifiers')) {
                obj['other_identifiers'] = ApiClient.convertToType(data['other_identifiers'], [BillIdentifier]);
            }
            if (data.hasOwnProperty('other_titles')) {
                obj['other_titles'] = ApiClient.convertToType(data['other_titles'], [BillTitle]);
            }
            if (data.hasOwnProperty('related_bills')) {
                obj['related_bills'] = ApiClient.convertToType(data['related_bills'], [RelatedBill]);
            }
            if (data.hasOwnProperty('session')) {
                obj['session'] = ApiClient.convertToType(data['session'], 'String');
            }
            if (data.hasOwnProperty('sources')) {
                obj['sources'] = ApiClient.convertToType(data['sources'], [Link]);
            }
            if (data.hasOwnProperty('sponsorships')) {
                obj['sponsorships'] = ApiClient.convertToType(data['sponsorships'], [BillSponsorship]);
            }
            if (data.hasOwnProperty('subject')) {
                obj['subject'] = ApiClient.convertToType(data['subject'], ['String']);
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('versions')) {
                obj['versions'] = ApiClient.convertToType(data['versions'], [BillDocumentOrVersion]);
            }
            if (data.hasOwnProperty('votes')) {
                obj['votes'] = ApiClient.convertToType(data['votes'], [VoteEvent]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Bill</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Bill</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Bill.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['abstracts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['abstracts'])) {
                throw new Error("Expected the field `abstracts` to be an array in the JSON data but got " + data['abstracts']);
            }
            // validate the optional field `abstracts` (array)
            for (const item of data['abstracts']) {
                BillAbstract.validateJSON(item);
            };
        }
        if (data['actions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['actions'])) {
                throw new Error("Expected the field `actions` to be an array in the JSON data but got " + data['actions']);
            }
            // validate the optional field `actions` (array)
            for (const item of data['actions']) {
                BillAction.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['classification'])) {
            throw new Error("Expected the field `classification` to be an array in the JSON data but got " + data['classification']);
        }
        if (data['documents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['documents'])) {
                throw new Error("Expected the field `documents` to be an array in the JSON data but got " + data['documents']);
            }
            // validate the optional field `documents` (array)
            for (const item of data['documents']) {
                BillDocumentOrVersion.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['first_action_date'] && !(typeof data['first_action_date'] === 'string' || data['first_action_date'] instanceof String)) {
            throw new Error("Expected the field `first_action_date` to be a primitive type in the JSON string but got " + data['first_action_date']);
        }
        // validate the optional field `from_organization`
        if (data['from_organization']) { // data not null
          Organization.validateJSON(data['from_organization']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['identifier'] && !(typeof data['identifier'] === 'string' || data['identifier'] instanceof String)) {
            throw new Error("Expected the field `identifier` to be a primitive type in the JSON string but got " + data['identifier']);
        }
        // validate the optional field `jurisdiction`
        if (data['jurisdiction']) { // data not null
          CompactJurisdiction.validateJSON(data['jurisdiction']);
        }
        // ensure the json data is a string
        if (data['latest_action_date'] && !(typeof data['latest_action_date'] === 'string' || data['latest_action_date'] instanceof String)) {
            throw new Error("Expected the field `latest_action_date` to be a primitive type in the JSON string but got " + data['latest_action_date']);
        }
        // ensure the json data is a string
        if (data['latest_action_description'] && !(typeof data['latest_action_description'] === 'string' || data['latest_action_description'] instanceof String)) {
            throw new Error("Expected the field `latest_action_description` to be a primitive type in the JSON string but got " + data['latest_action_description']);
        }
        // ensure the json data is a string
        if (data['latest_passage_date'] && !(typeof data['latest_passage_date'] === 'string' || data['latest_passage_date'] instanceof String)) {
            throw new Error("Expected the field `latest_passage_date` to be a primitive type in the JSON string but got " + data['latest_passage_date']);
        }
        // ensure the json data is a string
        if (data['openstates_url'] && !(typeof data['openstates_url'] === 'string' || data['openstates_url'] instanceof String)) {
            throw new Error("Expected the field `openstates_url` to be a primitive type in the JSON string but got " + data['openstates_url']);
        }
        if (data['other_identifiers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['other_identifiers'])) {
                throw new Error("Expected the field `other_identifiers` to be an array in the JSON data but got " + data['other_identifiers']);
            }
            // validate the optional field `other_identifiers` (array)
            for (const item of data['other_identifiers']) {
                BillIdentifier.validateJSON(item);
            };
        }
        if (data['other_titles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['other_titles'])) {
                throw new Error("Expected the field `other_titles` to be an array in the JSON data but got " + data['other_titles']);
            }
            // validate the optional field `other_titles` (array)
            for (const item of data['other_titles']) {
                BillTitle.validateJSON(item);
            };
        }
        if (data['related_bills']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['related_bills'])) {
                throw new Error("Expected the field `related_bills` to be an array in the JSON data but got " + data['related_bills']);
            }
            // validate the optional field `related_bills` (array)
            for (const item of data['related_bills']) {
                RelatedBill.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['session'] && !(typeof data['session'] === 'string' || data['session'] instanceof String)) {
            throw new Error("Expected the field `session` to be a primitive type in the JSON string but got " + data['session']);
        }
        if (data['sources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sources'])) {
                throw new Error("Expected the field `sources` to be an array in the JSON data but got " + data['sources']);
            }
            // validate the optional field `sources` (array)
            for (const item of data['sources']) {
                Link.validateJSON(item);
            };
        }
        if (data['sponsorships']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sponsorships'])) {
                throw new Error("Expected the field `sponsorships` to be an array in the JSON data but got " + data['sponsorships']);
            }
            // validate the optional field `sponsorships` (array)
            for (const item of data['sponsorships']) {
                BillSponsorship.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['subject'])) {
            throw new Error("Expected the field `subject` to be an array in the JSON data but got " + data['subject']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        if (data['versions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['versions'])) {
                throw new Error("Expected the field `versions` to be an array in the JSON data but got " + data['versions']);
            }
            // validate the optional field `versions` (array)
            for (const item of data['versions']) {
                BillDocumentOrVersion.validateJSON(item);
            };
        }
        if (data['votes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['votes'])) {
                throw new Error("Expected the field `votes` to be an array in the JSON data but got " + data['votes']);
            }
            // validate the optional field `votes` (array)
            for (const item of data['votes']) {
                VoteEvent.validateJSON(item);
            };
        }

        return true;
    }


}

Bill.RequiredProperties = ["created_at", "from_organization", "id", "identifier", "jurisdiction", "openstates_url", "session", "title", "updated_at"];

/**
 * @member {Array.<module:model/BillAbstract>} abstracts
 */
Bill.prototype['abstracts'] = undefined;

/**
 * @member {Array.<module:model/BillAction>} actions
 */
Bill.prototype['actions'] = undefined;

/**
 * @member {Array.<String>} classification
 */
Bill.prototype['classification'] = undefined;

/**
 * @member {Date} created_at
 */
Bill.prototype['created_at'] = undefined;

/**
 * @member {Array.<module:model/BillDocumentOrVersion>} documents
 */
Bill.prototype['documents'] = undefined;

/**
 * @member {Object} extras
 */
Bill.prototype['extras'] = undefined;

/**
 * @member {String} first_action_date
 * @default ''
 */
Bill.prototype['first_action_date'] = '';

/**
 * @member {module:model/Organization} from_organization
 */
Bill.prototype['from_organization'] = undefined;

/**
 * @member {String} id
 */
Bill.prototype['id'] = undefined;

/**
 * @member {String} identifier
 */
Bill.prototype['identifier'] = undefined;

/**
 * @member {module:model/CompactJurisdiction} jurisdiction
 */
Bill.prototype['jurisdiction'] = undefined;

/**
 * @member {String} latest_action_date
 * @default ''
 */
Bill.prototype['latest_action_date'] = '';

/**
 * @member {String} latest_action_description
 * @default ''
 */
Bill.prototype['latest_action_description'] = '';

/**
 * @member {String} latest_passage_date
 * @default ''
 */
Bill.prototype['latest_passage_date'] = '';

/**
 * @member {String} openstates_url
 */
Bill.prototype['openstates_url'] = undefined;

/**
 * @member {Array.<module:model/BillIdentifier>} other_identifiers
 */
Bill.prototype['other_identifiers'] = undefined;

/**
 * @member {Array.<module:model/BillTitle>} other_titles
 */
Bill.prototype['other_titles'] = undefined;

/**
 * @member {Array.<module:model/RelatedBill>} related_bills
 */
Bill.prototype['related_bills'] = undefined;

/**
 * @member {String} session
 */
Bill.prototype['session'] = undefined;

/**
 * @member {Array.<module:model/Link>} sources
 */
Bill.prototype['sources'] = undefined;

/**
 * @member {Array.<module:model/BillSponsorship>} sponsorships
 */
Bill.prototype['sponsorships'] = undefined;

/**
 * @member {Array.<String>} subject
 */
Bill.prototype['subject'] = undefined;

/**
 * @member {String} title
 */
Bill.prototype['title'] = undefined;

/**
 * @member {Date} updated_at
 */
Bill.prototype['updated_at'] = undefined;

/**
 * @member {Array.<module:model/BillDocumentOrVersion>} versions
 */
Bill.prototype['versions'] = undefined;

/**
 * @member {Array.<module:model/VoteEvent>} votes
 */
Bill.prototype['votes'] = undefined;






export default Bill;

