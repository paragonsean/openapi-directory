/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Chamber from './Chamber';
import JurisdictionClassification from './JurisdictionClassification';
import LegislativeSession from './LegislativeSession';
import RunPlan from './RunPlan';

/**
 * The Jurisdiction model module.
 * @module model/Jurisdiction
 * @version 2021.11.12
 */
class Jurisdiction {
    /**
     * Constructs a new <code>Jurisdiction</code>.
     * @alias module:model/Jurisdiction
     * @param classification {module:model/JurisdictionClassification} 
     * @param id {String} 
     * @param latestBillUpdate {Date} 
     * @param latestPeopleUpdate {Date} 
     * @param name {String} 
     * @param url {String} 
     */
    constructor(classification, id, latestBillUpdate, latestPeopleUpdate, name, url) { 
        
        Jurisdiction.initialize(this, classification, id, latestBillUpdate, latestPeopleUpdate, name, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, classification, id, latestBillUpdate, latestPeopleUpdate, name, url) { 
        obj['classification'] = classification;
        obj['division_id'] = '';
        obj['id'] = id;
        obj['latest_bill_update'] = latestBillUpdate;
        obj['latest_people_update'] = latestPeopleUpdate;
        obj['name'] = name;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>Jurisdiction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Jurisdiction} obj Optional instance to populate.
     * @return {module:model/Jurisdiction} The populated <code>Jurisdiction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Jurisdiction();

            if (data.hasOwnProperty('classification')) {
                obj['classification'] = ApiClient.convertToType(data['classification'], JurisdictionClassification);
            }
            if (data.hasOwnProperty('division_id')) {
                obj['division_id'] = ApiClient.convertToType(data['division_id'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('latest_bill_update')) {
                obj['latest_bill_update'] = ApiClient.convertToType(data['latest_bill_update'], 'Date');
            }
            if (data.hasOwnProperty('latest_people_update')) {
                obj['latest_people_update'] = ApiClient.convertToType(data['latest_people_update'], 'Date');
            }
            if (data.hasOwnProperty('latest_runs')) {
                obj['latest_runs'] = ApiClient.convertToType(data['latest_runs'], [RunPlan]);
            }
            if (data.hasOwnProperty('legislative_sessions')) {
                obj['legislative_sessions'] = ApiClient.convertToType(data['legislative_sessions'], [LegislativeSession]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('organizations')) {
                obj['organizations'] = ApiClient.convertToType(data['organizations'], [Chamber]);
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Jurisdiction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Jurisdiction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Jurisdiction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['division_id'] && !(typeof data['division_id'] === 'string' || data['division_id'] instanceof String)) {
            throw new Error("Expected the field `division_id` to be a primitive type in the JSON string but got " + data['division_id']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['latest_runs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['latest_runs'])) {
                throw new Error("Expected the field `latest_runs` to be an array in the JSON data but got " + data['latest_runs']);
            }
            // validate the optional field `latest_runs` (array)
            for (const item of data['latest_runs']) {
                RunPlan.validateJSON(item);
            };
        }
        if (data['legislative_sessions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['legislative_sessions'])) {
                throw new Error("Expected the field `legislative_sessions` to be an array in the JSON data but got " + data['legislative_sessions']);
            }
            // validate the optional field `legislative_sessions` (array)
            for (const item of data['legislative_sessions']) {
                LegislativeSession.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['organizations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['organizations'])) {
                throw new Error("Expected the field `organizations` to be an array in the JSON data but got " + data['organizations']);
            }
            // validate the optional field `organizations` (array)
            for (const item of data['organizations']) {
                Chamber.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

Jurisdiction.RequiredProperties = ["classification", "id", "latest_bill_update", "latest_people_update", "name", "url"];

/**
 * @member {module:model/JurisdictionClassification} classification
 */
Jurisdiction.prototype['classification'] = undefined;

/**
 * @member {String} division_id
 * @default ''
 */
Jurisdiction.prototype['division_id'] = '';

/**
 * @member {String} id
 */
Jurisdiction.prototype['id'] = undefined;

/**
 * @member {Date} latest_bill_update
 */
Jurisdiction.prototype['latest_bill_update'] = undefined;

/**
 * @member {Date} latest_people_update
 */
Jurisdiction.prototype['latest_people_update'] = undefined;

/**
 * @member {Array.<module:model/RunPlan>} latest_runs
 */
Jurisdiction.prototype['latest_runs'] = undefined;

/**
 * @member {Array.<module:model/LegislativeSession>} legislative_sessions
 */
Jurisdiction.prototype['legislative_sessions'] = undefined;

/**
 * @member {String} name
 */
Jurisdiction.prototype['name'] = undefined;

/**
 * @member {Array.<module:model/Chamber>} organizations
 */
Jurisdiction.prototype['organizations'] = undefined;

/**
 * @member {String} url
 */
Jurisdiction.prototype['url'] = undefined;






export default Jurisdiction;

