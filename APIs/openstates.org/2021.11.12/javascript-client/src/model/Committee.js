/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AltName from './AltName';
import CommitteeClassification from './CommitteeClassification';
import CommitteeMembership from './CommitteeMembership';
import Link from './Link';

/**
 * The Committee model module.
 * @module model/Committee
 * @version 2021.11.12
 */
class Committee {
    /**
     * Constructs a new <code>Committee</code>.
     * @alias module:model/Committee
     * @param classification {module:model/CommitteeClassification} 
     * @param extras {Object} 
     * @param id {String} 
     * @param name {String} 
     * @param parentId {String} 
     */
    constructor(classification, extras, id, name, parentId) { 
        
        Committee.initialize(this, classification, extras, id, name, parentId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, classification, extras, id, name, parentId) { 
        obj['classification'] = classification;
        obj['extras'] = extras;
        obj['id'] = id;
        obj['name'] = name;
        obj['parent_id'] = parentId;
    }

    /**
     * Constructs a <code>Committee</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Committee} obj Optional instance to populate.
     * @return {module:model/Committee} The populated <code>Committee</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Committee();

            if (data.hasOwnProperty('classification')) {
                obj['classification'] = CommitteeClassification.constructFromObject(data['classification']);
            }
            if (data.hasOwnProperty('extras')) {
                obj['extras'] = ApiClient.convertToType(data['extras'], Object);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = ApiClient.convertToType(data['links'], [Link]);
            }
            if (data.hasOwnProperty('memberships')) {
                obj['memberships'] = ApiClient.convertToType(data['memberships'], [CommitteeMembership]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('other_names')) {
                obj['other_names'] = ApiClient.convertToType(data['other_names'], [AltName]);
            }
            if (data.hasOwnProperty('parent_id')) {
                obj['parent_id'] = ApiClient.convertToType(data['parent_id'], 'String');
            }
            if (data.hasOwnProperty('sources')) {
                obj['sources'] = ApiClient.convertToType(data['sources'], [Link]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Committee</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Committee</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Committee.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['links']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['links'])) {
                throw new Error("Expected the field `links` to be an array in the JSON data but got " + data['links']);
            }
            // validate the optional field `links` (array)
            for (const item of data['links']) {
                Link.validateJSON(item);
            };
        }
        if (data['memberships']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['memberships'])) {
                throw new Error("Expected the field `memberships` to be an array in the JSON data but got " + data['memberships']);
            }
            // validate the optional field `memberships` (array)
            for (const item of data['memberships']) {
                CommitteeMembership.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['other_names']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['other_names'])) {
                throw new Error("Expected the field `other_names` to be an array in the JSON data but got " + data['other_names']);
            }
            // validate the optional field `other_names` (array)
            for (const item of data['other_names']) {
                AltName.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['parent_id'] && !(typeof data['parent_id'] === 'string' || data['parent_id'] instanceof String)) {
            throw new Error("Expected the field `parent_id` to be a primitive type in the JSON string but got " + data['parent_id']);
        }
        if (data['sources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sources'])) {
                throw new Error("Expected the field `sources` to be an array in the JSON data but got " + data['sources']);
            }
            // validate the optional field `sources` (array)
            for (const item of data['sources']) {
                Link.validateJSON(item);
            };
        }

        return true;
    }


}

Committee.RequiredProperties = ["classification", "extras", "id", "name", "parent_id"];

/**
 * @member {module:model/CommitteeClassification} classification
 */
Committee.prototype['classification'] = undefined;

/**
 * @member {Object} extras
 */
Committee.prototype['extras'] = undefined;

/**
 * @member {String} id
 */
Committee.prototype['id'] = undefined;

/**
 * @member {Array.<module:model/Link>} links
 */
Committee.prototype['links'] = undefined;

/**
 * @member {Array.<module:model/CommitteeMembership>} memberships
 */
Committee.prototype['memberships'] = undefined;

/**
 * @member {String} name
 */
Committee.prototype['name'] = undefined;

/**
 * @member {Array.<module:model/AltName>} other_names
 */
Committee.prototype['other_names'] = undefined;

/**
 * @member {String} parent_id
 */
Committee.prototype['parent_id'] = undefined;

/**
 * @member {Array.<module:model/Link>} sources
 */
Committee.prototype['sources'] = undefined;






export default Committee;

