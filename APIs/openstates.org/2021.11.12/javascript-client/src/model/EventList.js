/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Event from './Event';
import PaginationMeta from './PaginationMeta';

/**
 * The EventList model module.
 * @module model/EventList
 * @version 2021.11.12
 */
class EventList {
    /**
     * Constructs a new <code>EventList</code>.
     * @alias module:model/EventList
     * @param pagination {module:model/PaginationMeta} 
     * @param results {Array.<module:model/Event>} 
     */
    constructor(pagination, results) { 
        
        EventList.initialize(this, pagination, results);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, pagination, results) { 
        obj['pagination'] = pagination;
        obj['results'] = results;
    }

    /**
     * Constructs a <code>EventList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventList} obj Optional instance to populate.
     * @return {module:model/EventList} The populated <code>EventList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventList();

            if (data.hasOwnProperty('pagination')) {
                obj['pagination'] = PaginationMeta.constructFromObject(data['pagination']);
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [Event]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EventList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `pagination`
        if (data['pagination']) { // data not null
          PaginationMeta.validateJSON(data['pagination']);
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                Event.validateJSON(item);
            };
        }

        return true;
    }


}

EventList.RequiredProperties = ["pagination", "results"];

/**
 * @member {module:model/PaginationMeta} pagination
 */
EventList.prototype['pagination'] = undefined;

/**
 * @member {Array.<module:model/Event>} results
 */
EventList.prototype['results'] = undefined;






export default EventList;

