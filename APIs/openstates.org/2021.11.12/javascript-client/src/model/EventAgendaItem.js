/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EventMedia from './EventMedia';
import EventRelatedEntity from './EventRelatedEntity';

/**
 * The EventAgendaItem model module.
 * @module model/EventAgendaItem
 * @version 2021.11.12
 */
class EventAgendaItem {
    /**
     * Constructs a new <code>EventAgendaItem</code>.
     * @alias module:model/EventAgendaItem
     * @param classification {Array.<String>} 
     * @param description {String} 
     * @param extras {Object} 
     * @param media {Array.<module:model/EventMedia>} 
     * @param notes {Array.<String>} 
     * @param order {Number} 
     * @param relatedEntities {Array.<module:model/EventRelatedEntity>} 
     * @param subjects {Array.<String>} 
     */
    constructor(classification, description, extras, media, notes, order, relatedEntities, subjects) { 
        
        EventAgendaItem.initialize(this, classification, description, extras, media, notes, order, relatedEntities, subjects);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, classification, description, extras, media, notes, order, relatedEntities, subjects) { 
        obj['classification'] = classification;
        obj['description'] = description;
        obj['extras'] = extras;
        obj['media'] = media;
        obj['notes'] = notes;
        obj['order'] = order;
        obj['related_entities'] = relatedEntities;
        obj['subjects'] = subjects;
    }

    /**
     * Constructs a <code>EventAgendaItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventAgendaItem} obj Optional instance to populate.
     * @return {module:model/EventAgendaItem} The populated <code>EventAgendaItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventAgendaItem();

            if (data.hasOwnProperty('classification')) {
                obj['classification'] = ApiClient.convertToType(data['classification'], ['String']);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('extras')) {
                obj['extras'] = ApiClient.convertToType(data['extras'], Object);
            }
            if (data.hasOwnProperty('media')) {
                obj['media'] = ApiClient.convertToType(data['media'], [EventMedia]);
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], ['String']);
            }
            if (data.hasOwnProperty('order')) {
                obj['order'] = ApiClient.convertToType(data['order'], 'Number');
            }
            if (data.hasOwnProperty('related_entities')) {
                obj['related_entities'] = ApiClient.convertToType(data['related_entities'], [EventRelatedEntity]);
            }
            if (data.hasOwnProperty('subjects')) {
                obj['subjects'] = ApiClient.convertToType(data['subjects'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventAgendaItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventAgendaItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EventAgendaItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['classification'])) {
            throw new Error("Expected the field `classification` to be an array in the JSON data but got " + data['classification']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['media']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['media'])) {
                throw new Error("Expected the field `media` to be an array in the JSON data but got " + data['media']);
            }
            // validate the optional field `media` (array)
            for (const item of data['media']) {
                EventMedia.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['notes'])) {
            throw new Error("Expected the field `notes` to be an array in the JSON data but got " + data['notes']);
        }
        if (data['related_entities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['related_entities'])) {
                throw new Error("Expected the field `related_entities` to be an array in the JSON data but got " + data['related_entities']);
            }
            // validate the optional field `related_entities` (array)
            for (const item of data['related_entities']) {
                EventRelatedEntity.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['subjects'])) {
            throw new Error("Expected the field `subjects` to be an array in the JSON data but got " + data['subjects']);
        }

        return true;
    }


}

EventAgendaItem.RequiredProperties = ["classification", "description", "extras", "media", "notes", "order", "related_entities", "subjects"];

/**
 * @member {Array.<String>} classification
 */
EventAgendaItem.prototype['classification'] = undefined;

/**
 * @member {String} description
 */
EventAgendaItem.prototype['description'] = undefined;

/**
 * @member {Object} extras
 */
EventAgendaItem.prototype['extras'] = undefined;

/**
 * @member {Array.<module:model/EventMedia>} media
 */
EventAgendaItem.prototype['media'] = undefined;

/**
 * @member {Array.<String>} notes
 */
EventAgendaItem.prototype['notes'] = undefined;

/**
 * @member {Number} order
 */
EventAgendaItem.prototype['order'] = undefined;

/**
 * @member {Array.<module:model/EventRelatedEntity>} related_entities
 */
EventAgendaItem.prototype['related_entities'] = undefined;

/**
 * @member {Array.<String>} subjects
 */
EventAgendaItem.prototype['subjects'] = undefined;






export default EventAgendaItem;

