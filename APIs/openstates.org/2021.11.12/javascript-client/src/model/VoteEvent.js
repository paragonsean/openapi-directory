/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Link from './Link';
import Organization from './Organization';
import PersonVote from './PersonVote';
import VoteCount from './VoteCount';

/**
 * The VoteEvent model module.
 * @module model/VoteEvent
 * @version 2021.11.12
 */
class VoteEvent {
    /**
     * Constructs a new <code>VoteEvent</code>.
     * @alias module:model/VoteEvent
     * @param counts {Array.<module:model/VoteCount>} 
     * @param extras {Object} 
     * @param id {String} 
     * @param identifier {String} 
     * @param motionText {String} 
     * @param organization {module:model/Organization} 
     * @param result {String} 
     * @param sources {Array.<module:model/Link>} 
     * @param startDate {String} 
     * @param votes {Array.<module:model/PersonVote>} 
     */
    constructor(counts, extras, id, identifier, motionText, organization, result, sources, startDate, votes) { 
        
        VoteEvent.initialize(this, counts, extras, id, identifier, motionText, organization, result, sources, startDate, votes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, counts, extras, id, identifier, motionText, organization, result, sources, startDate, votes) { 
        obj['counts'] = counts;
        obj['extras'] = extras;
        obj['id'] = id;
        obj['identifier'] = identifier;
        obj['motion_text'] = motionText;
        obj['organization'] = organization;
        obj['result'] = result;
        obj['sources'] = sources;
        obj['start_date'] = startDate;
        obj['votes'] = votes;
    }

    /**
     * Constructs a <code>VoteEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VoteEvent} obj Optional instance to populate.
     * @return {module:model/VoteEvent} The populated <code>VoteEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VoteEvent();

            if (data.hasOwnProperty('counts')) {
                obj['counts'] = ApiClient.convertToType(data['counts'], [VoteCount]);
            }
            if (data.hasOwnProperty('extras')) {
                obj['extras'] = ApiClient.convertToType(data['extras'], Object);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('identifier')) {
                obj['identifier'] = ApiClient.convertToType(data['identifier'], 'String');
            }
            if (data.hasOwnProperty('motion_classification')) {
                obj['motion_classification'] = ApiClient.convertToType(data['motion_classification'], ['String']);
            }
            if (data.hasOwnProperty('motion_text')) {
                obj['motion_text'] = ApiClient.convertToType(data['motion_text'], 'String');
            }
            if (data.hasOwnProperty('organization')) {
                obj['organization'] = Organization.constructFromObject(data['organization']);
            }
            if (data.hasOwnProperty('result')) {
                obj['result'] = ApiClient.convertToType(data['result'], 'String');
            }
            if (data.hasOwnProperty('sources')) {
                obj['sources'] = ApiClient.convertToType(data['sources'], [Link]);
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'String');
            }
            if (data.hasOwnProperty('votes')) {
                obj['votes'] = ApiClient.convertToType(data['votes'], [PersonVote]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VoteEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VoteEvent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VoteEvent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['counts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['counts'])) {
                throw new Error("Expected the field `counts` to be an array in the JSON data but got " + data['counts']);
            }
            // validate the optional field `counts` (array)
            for (const item of data['counts']) {
                VoteCount.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['identifier'] && !(typeof data['identifier'] === 'string' || data['identifier'] instanceof String)) {
            throw new Error("Expected the field `identifier` to be a primitive type in the JSON string but got " + data['identifier']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['motion_classification'])) {
            throw new Error("Expected the field `motion_classification` to be an array in the JSON data but got " + data['motion_classification']);
        }
        // ensure the json data is a string
        if (data['motion_text'] && !(typeof data['motion_text'] === 'string' || data['motion_text'] instanceof String)) {
            throw new Error("Expected the field `motion_text` to be a primitive type in the JSON string but got " + data['motion_text']);
        }
        // validate the optional field `organization`
        if (data['organization']) { // data not null
          Organization.validateJSON(data['organization']);
        }
        // ensure the json data is a string
        if (data['result'] && !(typeof data['result'] === 'string' || data['result'] instanceof String)) {
            throw new Error("Expected the field `result` to be a primitive type in the JSON string but got " + data['result']);
        }
        if (data['sources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sources'])) {
                throw new Error("Expected the field `sources` to be an array in the JSON data but got " + data['sources']);
            }
            // validate the optional field `sources` (array)
            for (const item of data['sources']) {
                Link.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['start_date'] && !(typeof data['start_date'] === 'string' || data['start_date'] instanceof String)) {
            throw new Error("Expected the field `start_date` to be a primitive type in the JSON string but got " + data['start_date']);
        }
        if (data['votes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['votes'])) {
                throw new Error("Expected the field `votes` to be an array in the JSON data but got " + data['votes']);
            }
            // validate the optional field `votes` (array)
            for (const item of data['votes']) {
                PersonVote.validateJSON(item);
            };
        }

        return true;
    }


}

VoteEvent.RequiredProperties = ["counts", "extras", "id", "identifier", "motion_text", "organization", "result", "sources", "start_date", "votes"];

/**
 * @member {Array.<module:model/VoteCount>} counts
 */
VoteEvent.prototype['counts'] = undefined;

/**
 * @member {Object} extras
 */
VoteEvent.prototype['extras'] = undefined;

/**
 * @member {String} id
 */
VoteEvent.prototype['id'] = undefined;

/**
 * @member {String} identifier
 */
VoteEvent.prototype['identifier'] = undefined;

/**
 * @member {Array.<String>} motion_classification
 */
VoteEvent.prototype['motion_classification'] = undefined;

/**
 * @member {String} motion_text
 */
VoteEvent.prototype['motion_text'] = undefined;

/**
 * @member {module:model/Organization} organization
 */
VoteEvent.prototype['organization'] = undefined;

/**
 * @member {String} result
 */
VoteEvent.prototype['result'] = undefined;

/**
 * @member {Array.<module:model/Link>} sources
 */
VoteEvent.prototype['sources'] = undefined;

/**
 * @member {String} start_date
 */
VoteEvent.prototype['start_date'] = undefined;

/**
 * @member {Array.<module:model/PersonVote>} votes
 */
VoteEvent.prototype['votes'] = undefined;






export default VoteEvent;

