/**
 * Open States API v3
 *  * [More documentation](https://docs.openstates.org/en/latest/api/v3/index.html) * [Register for an account](https://openstates.org/accounts/signup/)   **We are currently working to restore experimental support for committees & events.**  During this period please note that data is not yet available for all states and the exact format of the new endpoints may change slightly depending on user feedback.  If you have any issues or questions use our [GitHub Issues](https://github.com/openstates/issues/issues) to give feedback. 
 *
 * The version of the OpenAPI document: 2021.11.12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DataExport from './DataExport';

/**
 * The LegislativeSession model module.
 * @module model/LegislativeSession
 * @version 2021.11.12
 */
class LegislativeSession {
    /**
     * Constructs a new <code>LegislativeSession</code>.
     * @alias module:model/LegislativeSession
     * @param classification {String} 
     * @param endDate {String} 
     * @param identifier {String} 
     * @param name {String} 
     * @param startDate {String} 
     */
    constructor(classification, endDate, identifier, name, startDate) { 
        
        LegislativeSession.initialize(this, classification, endDate, identifier, name, startDate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, classification, endDate, identifier, name, startDate) { 
        obj['classification'] = classification;
        obj['end_date'] = endDate;
        obj['identifier'] = identifier;
        obj['name'] = name;
        obj['start_date'] = startDate;
    }

    /**
     * Constructs a <code>LegislativeSession</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LegislativeSession} obj Optional instance to populate.
     * @return {module:model/LegislativeSession} The populated <code>LegislativeSession</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LegislativeSession();

            if (data.hasOwnProperty('classification')) {
                obj['classification'] = ApiClient.convertToType(data['classification'], 'String');
            }
            if (data.hasOwnProperty('downloads')) {
                obj['downloads'] = ApiClient.convertToType(data['downloads'], [DataExport]);
            }
            if (data.hasOwnProperty('end_date')) {
                obj['end_date'] = ApiClient.convertToType(data['end_date'], 'String');
            }
            if (data.hasOwnProperty('identifier')) {
                obj['identifier'] = ApiClient.convertToType(data['identifier'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LegislativeSession</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LegislativeSession</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LegislativeSession.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['classification'] && !(typeof data['classification'] === 'string' || data['classification'] instanceof String)) {
            throw new Error("Expected the field `classification` to be a primitive type in the JSON string but got " + data['classification']);
        }
        if (data['downloads']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['downloads'])) {
                throw new Error("Expected the field `downloads` to be an array in the JSON data but got " + data['downloads']);
            }
            // validate the optional field `downloads` (array)
            for (const item of data['downloads']) {
                DataExport.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['end_date'] && !(typeof data['end_date'] === 'string' || data['end_date'] instanceof String)) {
            throw new Error("Expected the field `end_date` to be a primitive type in the JSON string but got " + data['end_date']);
        }
        // ensure the json data is a string
        if (data['identifier'] && !(typeof data['identifier'] === 'string' || data['identifier'] instanceof String)) {
            throw new Error("Expected the field `identifier` to be a primitive type in the JSON string but got " + data['identifier']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['start_date'] && !(typeof data['start_date'] === 'string' || data['start_date'] instanceof String)) {
            throw new Error("Expected the field `start_date` to be a primitive type in the JSON string but got " + data['start_date']);
        }

        return true;
    }


}

LegislativeSession.RequiredProperties = ["classification", "end_date", "identifier", "name", "start_date"];

/**
 * @member {String} classification
 */
LegislativeSession.prototype['classification'] = undefined;

/**
 * @member {Array.<module:model/DataExport>} downloads
 */
LegislativeSession.prototype['downloads'] = undefined;

/**
 * @member {String} end_date
 */
LegislativeSession.prototype['end_date'] = undefined;

/**
 * @member {String} identifier
 */
LegislativeSession.prototype['identifier'] = undefined;

/**
 * @member {String} name
 */
LegislativeSession.prototype['name'] = undefined;

/**
 * @member {String} start_date
 */
LegislativeSession.prototype['start_date'] = undefined;






export default LegislativeSession;

