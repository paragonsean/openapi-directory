/**
 * API v1
 * DocSpring is a service that helps you fill out and sign PDF templates.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CombinedSubmissionSourcePdfsInnerOneOf model module.
 * @module model/CombinedSubmissionSourcePdfsInnerOneOf
 * @version v1
 */
class CombinedSubmissionSourcePdfsInnerOneOf {
    /**
     * Constructs a new <code>CombinedSubmissionSourcePdfsInnerOneOf</code>.
     * @alias module:model/CombinedSubmissionSourcePdfsInnerOneOf
     * @param id {String} 
     * @param type {module:model/CombinedSubmissionSourcePdfsInnerOneOf.TypeEnum} 
     */
    constructor(id, type) { 
        
        CombinedSubmissionSourcePdfsInnerOneOf.initialize(this, id, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, type) { 
        obj['id'] = id;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>CombinedSubmissionSourcePdfsInnerOneOf</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CombinedSubmissionSourcePdfsInnerOneOf} obj Optional instance to populate.
     * @return {module:model/CombinedSubmissionSourcePdfsInnerOneOf} The populated <code>CombinedSubmissionSourcePdfsInnerOneOf</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CombinedSubmissionSourcePdfsInnerOneOf();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CombinedSubmissionSourcePdfsInnerOneOf</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CombinedSubmissionSourcePdfsInnerOneOf</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CombinedSubmissionSourcePdfsInnerOneOf.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

CombinedSubmissionSourcePdfsInnerOneOf.RequiredProperties = ["id", "type"];

/**
 * @member {String} id
 */
CombinedSubmissionSourcePdfsInnerOneOf.prototype['id'] = undefined;

/**
 * @member {module:model/CombinedSubmissionSourcePdfsInnerOneOf.TypeEnum} type
 */
CombinedSubmissionSourcePdfsInnerOneOf.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
CombinedSubmissionSourcePdfsInnerOneOf['TypeEnum'] = {

    /**
     * value: "submission"
     * @const
     */
    "submission": "submission",

    /**
     * value: "combined_submission"
     * @const
     */
    "combined_submission": "combined_submission",

    /**
     * value: "template"
     * @const
     */
    "template": "template",

    /**
     * value: "custom_file"
     * @const
     */
    "custom_file": "custom_file"
};



export default CombinedSubmissionSourcePdfsInnerOneOf;

