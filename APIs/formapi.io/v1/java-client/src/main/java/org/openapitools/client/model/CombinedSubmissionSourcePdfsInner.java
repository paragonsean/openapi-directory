/*
 * API v1
 * DocSpring is a service that helps you fill out and sign PDF templates.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.net.URI;
import java.util.Arrays;
import org.openapitools.client.model.CombinedSubmissionSourcePdfsInnerOneOf;
import org.openapitools.client.model.CombinedSubmissionSourcePdfsInnerOneOf1;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T12:42:11.973433-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class CombinedSubmissionSourcePdfsInner extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CombinedSubmissionSourcePdfsInner.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CombinedSubmissionSourcePdfsInner.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CombinedSubmissionSourcePdfsInner' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<CombinedSubmissionSourcePdfsInnerOneOf> adapterCombinedSubmissionSourcePdfsInnerOneOf = gson.getDelegateAdapter(this, TypeToken.get(CombinedSubmissionSourcePdfsInnerOneOf.class));
            final TypeAdapter<CombinedSubmissionSourcePdfsInnerOneOf1> adapterCombinedSubmissionSourcePdfsInnerOneOf1 = gson.getDelegateAdapter(this, TypeToken.get(CombinedSubmissionSourcePdfsInnerOneOf1.class));

            return (TypeAdapter<T>) new TypeAdapter<CombinedSubmissionSourcePdfsInner>() {
                @Override
                public void write(JsonWriter out, CombinedSubmissionSourcePdfsInner value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `CombinedSubmissionSourcePdfsInnerOneOf`
                    if (value.getActualInstance() instanceof CombinedSubmissionSourcePdfsInnerOneOf) {
                        JsonElement element = adapterCombinedSubmissionSourcePdfsInnerOneOf.toJsonTree((CombinedSubmissionSourcePdfsInnerOneOf)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `CombinedSubmissionSourcePdfsInnerOneOf1`
                    if (value.getActualInstance() instanceof CombinedSubmissionSourcePdfsInnerOneOf1) {
                        JsonElement element = adapterCombinedSubmissionSourcePdfsInnerOneOf1.toJsonTree((CombinedSubmissionSourcePdfsInnerOneOf1)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: CombinedSubmissionSourcePdfsInnerOneOf, CombinedSubmissionSourcePdfsInnerOneOf1");
                }

                @Override
                public CombinedSubmissionSourcePdfsInner read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize CombinedSubmissionSourcePdfsInnerOneOf
                    try {
                        // validate the JSON object to see if any exception is thrown
                        CombinedSubmissionSourcePdfsInnerOneOf.validateJsonElement(jsonElement);
                        actualAdapter = adapterCombinedSubmissionSourcePdfsInnerOneOf;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'CombinedSubmissionSourcePdfsInnerOneOf'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for CombinedSubmissionSourcePdfsInnerOneOf failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'CombinedSubmissionSourcePdfsInnerOneOf'", e);
                    }
                    // deserialize CombinedSubmissionSourcePdfsInnerOneOf1
                    try {
                        // validate the JSON object to see if any exception is thrown
                        CombinedSubmissionSourcePdfsInnerOneOf1.validateJsonElement(jsonElement);
                        actualAdapter = adapterCombinedSubmissionSourcePdfsInnerOneOf1;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'CombinedSubmissionSourcePdfsInnerOneOf1'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for CombinedSubmissionSourcePdfsInnerOneOf1 failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'CombinedSubmissionSourcePdfsInnerOneOf1'", e);
                    }

                    if (match == 1) {
                        CombinedSubmissionSourcePdfsInner ret = new CombinedSubmissionSourcePdfsInner();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CombinedSubmissionSourcePdfsInner: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CombinedSubmissionSourcePdfsInner() {
        super("oneOf", Boolean.FALSE);
    }

    public CombinedSubmissionSourcePdfsInner(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("CombinedSubmissionSourcePdfsInnerOneOf", CombinedSubmissionSourcePdfsInnerOneOf.class);
        schemas.put("CombinedSubmissionSourcePdfsInnerOneOf1", CombinedSubmissionSourcePdfsInnerOneOf1.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CombinedSubmissionSourcePdfsInner.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * CombinedSubmissionSourcePdfsInnerOneOf, CombinedSubmissionSourcePdfsInnerOneOf1
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof CombinedSubmissionSourcePdfsInnerOneOf) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof CombinedSubmissionSourcePdfsInnerOneOf1) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be CombinedSubmissionSourcePdfsInnerOneOf, CombinedSubmissionSourcePdfsInnerOneOf1");
    }

    /**
     * Get the actual instance, which can be the following:
     * CombinedSubmissionSourcePdfsInnerOneOf, CombinedSubmissionSourcePdfsInnerOneOf1
     *
     * @return The actual instance (CombinedSubmissionSourcePdfsInnerOneOf, CombinedSubmissionSourcePdfsInnerOneOf1)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `CombinedSubmissionSourcePdfsInnerOneOf`. If the actual instance is not `CombinedSubmissionSourcePdfsInnerOneOf`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `CombinedSubmissionSourcePdfsInnerOneOf`
     * @throws ClassCastException if the instance is not `CombinedSubmissionSourcePdfsInnerOneOf`
     */
    public CombinedSubmissionSourcePdfsInnerOneOf getCombinedSubmissionSourcePdfsInnerOneOf() throws ClassCastException {
        return (CombinedSubmissionSourcePdfsInnerOneOf)super.getActualInstance();
    }
    /**
     * Get the actual instance of `CombinedSubmissionSourcePdfsInnerOneOf1`. If the actual instance is not `CombinedSubmissionSourcePdfsInnerOneOf1`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `CombinedSubmissionSourcePdfsInnerOneOf1`
     * @throws ClassCastException if the instance is not `CombinedSubmissionSourcePdfsInnerOneOf1`
     */
    public CombinedSubmissionSourcePdfsInnerOneOf1 getCombinedSubmissionSourcePdfsInnerOneOf1() throws ClassCastException {
        return (CombinedSubmissionSourcePdfsInnerOneOf1)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CombinedSubmissionSourcePdfsInner
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with CombinedSubmissionSourcePdfsInnerOneOf
        try {
            CombinedSubmissionSourcePdfsInnerOneOf.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for CombinedSubmissionSourcePdfsInnerOneOf failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with CombinedSubmissionSourcePdfsInnerOneOf1
        try {
            CombinedSubmissionSourcePdfsInnerOneOf1.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for CombinedSubmissionSourcePdfsInnerOneOf1 failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for CombinedSubmissionSourcePdfsInner with oneOf schemas: CombinedSubmissionSourcePdfsInnerOneOf, CombinedSubmissionSourcePdfsInnerOneOf1. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of CombinedSubmissionSourcePdfsInner given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CombinedSubmissionSourcePdfsInner
     * @throws IOException if the JSON string is invalid with respect to CombinedSubmissionSourcePdfsInner
     */
    public static CombinedSubmissionSourcePdfsInner fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CombinedSubmissionSourcePdfsInner.class);
    }

    /**
     * Convert an instance of CombinedSubmissionSourcePdfsInner to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

