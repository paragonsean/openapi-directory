/**
 * Debian Code Search
 * OpenAPI for https://codesearch.debian.net/
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: stapelberg@debian.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SearchResult from './SearchResult';

/**
 * The PackageSearchResult model module.
 * @module model/PackageSearchResult
 * @version 1.4.0
 */
class PackageSearchResult {
    /**
     * Constructs a new <code>PackageSearchResult</code>.
     * @alias module:model/PackageSearchResult
     * @param _package {String} The Debian source package for which up to 2 search results have been aggregated in `results`.
     * @param results {Array.<module:model/SearchResult>} 
     */
    constructor(_package, results) { 
        
        PackageSearchResult.initialize(this, _package, results);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, _package, results) { 
        obj['package'] = _package;
        obj['results'] = results;
    }

    /**
     * Constructs a <code>PackageSearchResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PackageSearchResult} obj Optional instance to populate.
     * @return {module:model/PackageSearchResult} The populated <code>PackageSearchResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PackageSearchResult();

            if (data.hasOwnProperty('package')) {
                obj['package'] = ApiClient.convertToType(data['package'], 'String');
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [SearchResult]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PackageSearchResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PackageSearchResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PackageSearchResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['package'] && !(typeof data['package'] === 'string' || data['package'] instanceof String)) {
            throw new Error("Expected the field `package` to be a primitive type in the JSON string but got " + data['package']);
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                SearchResult.validateJSON(item);
            };
        }

        return true;
    }


}

PackageSearchResult.RequiredProperties = ["package", "results"];

/**
 * The Debian source package for which up to 2 search results have been aggregated in `results`.
 * @member {String} package
 */
PackageSearchResult.prototype['package'] = undefined;

/**
 * @member {Array.<module:model/SearchResult>} results
 */
PackageSearchResult.prototype['results'] = undefined;






export default PackageSearchResult;

