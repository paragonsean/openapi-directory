/**
 * ticketmaster publish api
 * Publish API
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Source from './Source';

/**
 * The Entitlement model module.
 * @module model/Entitlement
 * @version v2
 */
class Entitlement {
    /**
     * Constructs a new <code>Entitlement</code>.
     * This class defines an entitlement data on the Publish API
     * @alias module:model/Entitlement
     * @param data {Object} The actual entitlements information to add to the entity
     * @param relatedEntityType {module:model/Entitlement.RelatedEntityTypeEnum} The type of the entity to add this entitlement to
     * @param source {module:model/Entitlement.SourceEnum} Source of the extension, where it came from
     */
    constructor(data, relatedEntityType, source) { 
        
        Entitlement.initialize(this, data, relatedEntityType, source);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, relatedEntityType, source) { 
        obj['data'] = data;
        obj['relatedEntityType'] = relatedEntityType;
        obj['source'] = source;
    }

    /**
     * Constructs a <code>Entitlement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Entitlement} obj Optional instance to populate.
     * @return {module:model/Entitlement} The populated <code>Entitlement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Entitlement();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], Object);
            }
            if (data.hasOwnProperty('relatedEntityId')) {
                obj['relatedEntityId'] = ApiClient.convertToType(data['relatedEntityId'], 'String');
            }
            if (data.hasOwnProperty('relatedEntitySource')) {
                obj['relatedEntitySource'] = Source.constructFromObject(data['relatedEntitySource']);
            }
            if (data.hasOwnProperty('relatedEntityType')) {
                obj['relatedEntityType'] = ApiClient.convertToType(data['relatedEntityType'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('versionNumber')) {
                obj['versionNumber'] = ApiClient.convertToType(data['versionNumber'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Entitlement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Entitlement</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Entitlement.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['relatedEntityId'] && !(typeof data['relatedEntityId'] === 'string' || data['relatedEntityId'] instanceof String)) {
            throw new Error("Expected the field `relatedEntityId` to be a primitive type in the JSON string but got " + data['relatedEntityId']);
        }
        // validate the optional field `relatedEntitySource`
        if (data['relatedEntitySource']) { // data not null
          Source.validateJSON(data['relatedEntitySource']);
        }
        // ensure the json data is a string
        if (data['relatedEntityType'] && !(typeof data['relatedEntityType'] === 'string' || data['relatedEntityType'] instanceof String)) {
            throw new Error("Expected the field `relatedEntityType` to be a primitive type in the JSON string but got " + data['relatedEntityType']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

Entitlement.RequiredProperties = ["data", "relatedEntityType", "source"];

/**
 * The actual entitlements information to add to the entity
 * @member {Object} data
 */
Entitlement.prototype['data'] = undefined;

/**
 * Id of the entity to add this extionsion to. If the relatedEntityId is Null, a relatedEntitySource MUST be provided
 * @member {String} relatedEntityId
 */
Entitlement.prototype['relatedEntityId'] = undefined;

/**
 * @member {module:model/Source} relatedEntitySource
 */
Entitlement.prototype['relatedEntitySource'] = undefined;

/**
 * The type of the entity to add this entitlement to
 * @member {module:model/Entitlement.RelatedEntityTypeEnum} relatedEntityType
 */
Entitlement.prototype['relatedEntityType'] = undefined;

/**
 * Source of the extension, where it came from
 * @member {module:model/Entitlement.SourceEnum} source
 */
Entitlement.prototype['source'] = undefined;

/**
 * Version of the entitlements. Version is to prevent to override an entitlements with an older one
 * @member {Number} versionNumber
 */
Entitlement.prototype['versionNumber'] = undefined;





/**
 * Allowed values for the <code>relatedEntityType</code> property.
 * @enum {String}
 * @readonly
 */
Entitlement['RelatedEntityTypeEnum'] = {

    /**
     * value: "event"
     * @const
     */
    "event": "event",

    /**
     * value: "venue"
     * @const
     */
    "venue": "venue",

    /**
     * value: "attraction"
     * @const
     */
    "attraction": "attraction"
};


/**
 * Allowed values for the <code>source</code> property.
 * @enum {String}
 * @readonly
 */
Entitlement['SourceEnum'] = {

    /**
     * value: "ticketmaster"
     * @const
     */
    "ticketmaster": "ticketmaster"
};



export default Entitlement;

