/**
 * SearchLy API v1
 * # Introduction The SearchLy API provides similarity searching based on song lyrics.  # Operations The API allows for the `/similarity/by_song` operation, which allows clients to search the similarity for an existing song in the database. Also, the API has an additional `/similarity/by_content` endpoint which allows clients to search similarity given a free String input through a JSON request body. Additional `/song/search` operation is available for searching songs given a query String.  # Endpoint The API endpoint for the SearchLy API v1 is as follows: ``` https://searchly.asuarez.dev/api/v1 ```  # Motivation This project was built in order to create an API for searching similarities based on song lyrics. There are a lot of songs in the industry and most of them are talking about the same topic. What I wanted to prove with SearchLy was to estimate how similar are two songs between them based on the meaning of their lyrics.  SearchLy is using a database of 100k songs from AZLyrics, using [this scraper](https://github.com/AlbertSuarez/azlyrics-scraper), which is being updated periodically. Then, using word2vec and NMSLIB, it was possible to create an index where you can search similarities using the k-nearest neighbors (KNN) algorithm.  > **Note**: I am currently using a micro-instance from DigitalOcean where the API is deployed, so you should expect a bad performance. However, if this API becomes popular I will deploy it in a bigger instance. 
 *
 * The version of the OpenAPI document: 1.0
 * Contact: hi@asuarez.dev
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The APIResponseSimilarityResponseSimilarityListInner model module.
 * @module model/APIResponseSimilarityResponseSimilarityListInner
 * @version 1.0
 */
class APIResponseSimilarityResponseSimilarityListInner {
    /**
     * Constructs a new <code>APIResponseSimilarityResponseSimilarityListInner</code>.
     * Song similarity item.
     * @alias module:model/APIResponseSimilarityResponseSimilarityListInner
     */
    constructor() { 
        
        APIResponseSimilarityResponseSimilarityListInner.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>APIResponseSimilarityResponseSimilarityListInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/APIResponseSimilarityResponseSimilarityListInner} obj Optional instance to populate.
     * @return {module:model/APIResponseSimilarityResponseSimilarityListInner} The populated <code>APIResponseSimilarityResponseSimilarityListInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new APIResponseSimilarityResponseSimilarityListInner();

            if (data.hasOwnProperty('artist_name')) {
                obj['artist_name'] = ApiClient.convertToType(data['artist_name'], 'String');
            }
            if (data.hasOwnProperty('artist_url')) {
                obj['artist_url'] = ApiClient.convertToType(data['artist_url'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('index_id')) {
                obj['index_id'] = ApiClient.convertToType(data['index_id'], 'Number');
            }
            if (data.hasOwnProperty('lyrics')) {
                obj['lyrics'] = ApiClient.convertToType(data['lyrics'], 'String');
            }
            if (data.hasOwnProperty('percentage')) {
                obj['percentage'] = ApiClient.convertToType(data['percentage'], 'Number');
            }
            if (data.hasOwnProperty('song_name')) {
                obj['song_name'] = ApiClient.convertToType(data['song_name'], 'String');
            }
            if (data.hasOwnProperty('song_url')) {
                obj['song_url'] = ApiClient.convertToType(data['song_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>APIResponseSimilarityResponseSimilarityListInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>APIResponseSimilarityResponseSimilarityListInner</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['artist_name'] && !(typeof data['artist_name'] === 'string' || data['artist_name'] instanceof String)) {
            throw new Error("Expected the field `artist_name` to be a primitive type in the JSON string but got " + data['artist_name']);
        }
        // ensure the json data is a string
        if (data['artist_url'] && !(typeof data['artist_url'] === 'string' || data['artist_url'] instanceof String)) {
            throw new Error("Expected the field `artist_url` to be a primitive type in the JSON string but got " + data['artist_url']);
        }
        // ensure the json data is a string
        if (data['lyrics'] && !(typeof data['lyrics'] === 'string' || data['lyrics'] instanceof String)) {
            throw new Error("Expected the field `lyrics` to be a primitive type in the JSON string but got " + data['lyrics']);
        }
        // ensure the json data is a string
        if (data['song_name'] && !(typeof data['song_name'] === 'string' || data['song_name'] instanceof String)) {
            throw new Error("Expected the field `song_name` to be a primitive type in the JSON string but got " + data['song_name']);
        }
        // ensure the json data is a string
        if (data['song_url'] && !(typeof data['song_url'] === 'string' || data['song_url'] instanceof String)) {
            throw new Error("Expected the field `song_url` to be a primitive type in the JSON string but got " + data['song_url']);
        }

        return true;
    }


}



/**
 * Artist's song name.
 * @member {String} artist_name
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['artist_name'] = undefined;

/**
 * Artist URL.
 * @member {String} artist_url
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['artist_url'] = undefined;

/**
 * Song internal identifier.
 * @member {Number} id
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['id'] = undefined;

/**
 * Index internal identifier.
 * @member {Number} index_id
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['index_id'] = undefined;

/**
 * Song lyrics.
 * @member {String} lyrics
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['lyrics'] = undefined;

/**
 * Similarity percentage.
 * @member {Number} percentage
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['percentage'] = undefined;

/**
 * Song name.
 * @member {String} song_name
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['song_name'] = undefined;

/**
 * Song URL.
 * @member {String} song_url
 */
APIResponseSimilarityResponseSimilarityListInner.prototype['song_url'] = undefined;






export default APIResponseSimilarityResponseSimilarityListInner;

