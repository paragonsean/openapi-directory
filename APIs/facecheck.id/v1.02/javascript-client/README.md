# facial_recognition_reverse_image_face_search_api

FacialRecognitionReverseImageFaceSearchApi - JavaScript client for facial_recognition_reverse_image_face_search_api
Let your users search the Internet by face! Integrate FaceCheck facial search seamlessly with your app, website or software platform. 
FaceCheck's REST API is hassle-free and easy to use. For code examples visit <a href='https://facecheck.id/Face-Search/API'>https://facecheck.id/Face-Search/API</a>
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: v1.02
- Package version: v1.02
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://FaceCheck.ID](https://FaceCheck.ID)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install facial_recognition_reverse_image_face_search_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your facial_recognition_reverse_image_face_search_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var FacialRecognitionReverseImageFaceSearchApi = require('facial_recognition_reverse_image_face_search_api');

var defaultClient = FacialRecognitionReverseImageFaceSearchApi.ApiClient.instance;
// Configure API key authorization: Bearer
var Bearer = defaultClient.authentications['Bearer'];
Bearer.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//Bearer.apiKeyPrefix['Authorization'] = "Token"

var api = new FacialRecognitionReverseImageFaceSearchApi.FaceCheckAPIApi()
var opts = {
  'idSearch': "idSearch_example", // {String} 
  'idPic': "idPic_example" // {String} 
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.apiDeletePicPost(opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *http://localhost*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*FacialRecognitionReverseImageFaceSearchApi.FaceCheckAPIApi* | [**apiDeletePicPost**](docs/FaceCheckAPIApi.md#apiDeletePicPost) | **POST** /api/delete_pic | Remove an image from a search request
*FacialRecognitionReverseImageFaceSearchApi.FaceCheckAPIApi* | [**apiInfoPost**](docs/FaceCheckAPIApi.md#apiInfoPost) | **POST** /api/info | Returns remaining search credits, search engine online status, and number of indexed faces
*FacialRecognitionReverseImageFaceSearchApi.FaceCheckAPIApi* | [**apiSearchPost**](docs/FaceCheckAPIApi.md#apiSearchPost) | **POST** /api/search | 
*FacialRecognitionReverseImageFaceSearchApi.FaceCheckAPIApi* | [**apiUploadPicPost**](docs/FaceCheckAPIApi.md#apiUploadPicPost) | **POST** /api/upload_pic | 


## Documentation for Models

 - [FacialRecognitionReverseImageFaceSearchApi.BrowserJsonResponse](docs/BrowserJsonResponse.md)
 - [FacialRecognitionReverseImageFaceSearchApi.InfoResponse](docs/InfoResponse.md)
 - [FacialRecognitionReverseImageFaceSearchApi.InputImage](docs/InputImage.md)
 - [FacialRecognitionReverseImageFaceSearchApi.SearchData](docs/SearchData.md)
 - [FacialRecognitionReverseImageFaceSearchApi.SearchItem](docs/SearchItem.md)
 - [FacialRecognitionReverseImageFaceSearchApi.SearchResults](docs/SearchResults.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### Bearer


- **Type**: API key
- **API key parameter name**: Authorization
- **Location**: HTTP header

