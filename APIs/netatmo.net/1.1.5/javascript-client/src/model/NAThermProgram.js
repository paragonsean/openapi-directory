/**
 * Netatmo
 * <h3>Welcome to the Netatmo swagger on-line documentation !</h3> This site is a complement to the official <a href=\"https://dev.netatmo.com/\">Netatmo developper documentation</a> using swagger to bring interactivity and easy testing of requests with the \"try it\" button (authenticate with the authorization code OAuth2 flow by clicking the authenticate button in the methods). You can find the source code for this site can be found in the project <a href=\"https://github.com/cbornet/netatmo-swagger-ui\">netatmo-swagger-ui</a>. You can also use the online <a href=\"./swagger.json\">swagger declaration</a> file to generate code or static documentation (see <a href=\"https://github.com/cbornet/netatmo-swagger-api\">netatmo-swagger-api</a>). 
 *
 * The version of the OpenAPI document: 1.1.5
 * Contact: contact-api@netatmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NATimeTableItem from './NATimeTableItem';
import NAZone from './NAZone';

/**
 * The NAThermProgram model module.
 * @module model/NAThermProgram
 * @version 1.1.5
 */
class NAThermProgram {
    /**
     * Constructs a new <code>NAThermProgram</code>.
     * @alias module:model/NAThermProgram
     */
    constructor() { 
        
        NAThermProgram.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NAThermProgram</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NAThermProgram} obj Optional instance to populate.
     * @return {module:model/NAThermProgram} The populated <code>NAThermProgram</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NAThermProgram();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('program_id')) {
                obj['program_id'] = ApiClient.convertToType(data['program_id'], 'String');
            }
            if (data.hasOwnProperty('selected')) {
                obj['selected'] = ApiClient.convertToType(data['selected'], 'Boolean');
            }
            if (data.hasOwnProperty('timetable')) {
                obj['timetable'] = ApiClient.convertToType(data['timetable'], [NATimeTableItem]);
            }
            if (data.hasOwnProperty('zones')) {
                obj['zones'] = ApiClient.convertToType(data['zones'], [NAZone]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NAThermProgram</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NAThermProgram</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['program_id'] && !(typeof data['program_id'] === 'string' || data['program_id'] instanceof String)) {
            throw new Error("Expected the field `program_id` to be a primitive type in the JSON string but got " + data['program_id']);
        }
        if (data['timetable']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['timetable'])) {
                throw new Error("Expected the field `timetable` to be an array in the JSON data but got " + data['timetable']);
            }
            // validate the optional field `timetable` (array)
            for (const item of data['timetable']) {
                NATimeTableItem.validateJSON(item);
            };
        }
        if (data['zones']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['zones'])) {
                throw new Error("Expected the field `zones` to be an array in the JSON data but got " + data['zones']);
            }
            // validate the optional field `zones` (array)
            for (const item of data['zones']) {
                NAZone.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {String} name
 */
NAThermProgram.prototype['name'] = undefined;

/**
 * @member {String} program_id
 */
NAThermProgram.prototype['program_id'] = undefined;

/**
 * @member {Boolean} selected
 */
NAThermProgram.prototype['selected'] = undefined;

/**
 * @member {Array.<module:model/NATimeTableItem>} timetable
 */
NAThermProgram.prototype['timetable'] = undefined;

/**
 * @member {Array.<module:model/NAZone>} zones
 */
NAThermProgram.prototype['zones'] = undefined;






export default NAThermProgram;

