/**
 * Netatmo
 * <h3>Welcome to the Netatmo swagger on-line documentation !</h3> This site is a complement to the official <a href=\"https://dev.netatmo.com/\">Netatmo developper documentation</a> using swagger to bring interactivity and easy testing of requests with the \"try it\" button (authenticate with the authorization code OAuth2 flow by clicking the authenticate button in the methods). You can find the source code for this site can be found in the project <a href=\"https://github.com/cbornet/netatmo-swagger-ui\">netatmo-swagger-ui</a>. You can also use the online <a href=\"./swagger.json\">swagger declaration</a> file to generate code or static documentation (see <a href=\"https://github.com/cbornet/netatmo-swagger-api\">netatmo-swagger-api</a>). 
 *
 * The version of the OpenAPI document: 1.1.5
 * Contact: contact-api@netatmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NAWelcomeCamera model module.
 * @module model/NAWelcomeCamera
 * @version 1.1.5
 */
class NAWelcomeCamera {
    /**
     * Constructs a new <code>NAWelcomeCamera</code>.
     * @alias module:model/NAWelcomeCamera
     */
    constructor() { 
        
        NAWelcomeCamera.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NAWelcomeCamera</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NAWelcomeCamera} obj Optional instance to populate.
     * @return {module:model/NAWelcomeCamera} The populated <code>NAWelcomeCamera</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NAWelcomeCamera();

            if (data.hasOwnProperty('alim_status')) {
                obj['alim_status'] = ApiClient.convertToType(data['alim_status'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('is_local')) {
                obj['is_local'] = ApiClient.convertToType(data['is_local'], 'Boolean');
            }
            if (data.hasOwnProperty('light_mode_status')) {
                obj['light_mode_status'] = ApiClient.convertToType(data['light_mode_status'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('sd_status')) {
                obj['sd_status'] = ApiClient.convertToType(data['sd_status'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('vpn_url')) {
                obj['vpn_url'] = ApiClient.convertToType(data['vpn_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NAWelcomeCamera</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NAWelcomeCamera</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['alim_status'] && !(typeof data['alim_status'] === 'string' || data['alim_status'] instanceof String)) {
            throw new Error("Expected the field `alim_status` to be a primitive type in the JSON string but got " + data['alim_status']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['light_mode_status'] && !(typeof data['light_mode_status'] === 'string' || data['light_mode_status'] instanceof String)) {
            throw new Error("Expected the field `light_mode_status` to be a primitive type in the JSON string but got " + data['light_mode_status']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['sd_status'] && !(typeof data['sd_status'] === 'string' || data['sd_status'] instanceof String)) {
            throw new Error("Expected the field `sd_status` to be a primitive type in the JSON string but got " + data['sd_status']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['vpn_url'] && !(typeof data['vpn_url'] === 'string' || data['vpn_url'] instanceof String)) {
            throw new Error("Expected the field `vpn_url` to be a primitive type in the JSON string but got " + data['vpn_url']);
        }

        return true;
    }


}



/**
 * If power supply is ok (on/off)
 * @member {String} alim_status
 */
NAWelcomeCamera.prototype['alim_status'] = undefined;

/**
 * Id of the camera
 * @member {String} id
 */
NAWelcomeCamera.prototype['id'] = undefined;

/**
 * Only for scope access_camera. If Camera and application requesting the information are on the same IP (true/false)
 * @member {Boolean} is_local
 */
NAWelcomeCamera.prototype['is_local'] = undefined;

/**
 * State of (flood-)light
 * @member {module:model/NAWelcomeCamera.LightModeStatusEnum} light_mode_status
 */
NAWelcomeCamera.prototype['light_mode_status'] = undefined;

/**
 * Name of the camera
 * @member {String} name
 */
NAWelcomeCamera.prototype['name'] = undefined;

/**
 * If SD card status is ok (on/off)
 * @member {String} sd_status
 */
NAWelcomeCamera.prototype['sd_status'] = undefined;

/**
 * If camera is monitoring (on/off)
 * @member {String} status
 */
NAWelcomeCamera.prototype['status'] = undefined;

/**
 * Type of the camera
 * @member {String} type
 */
NAWelcomeCamera.prototype['type'] = undefined;

/**
 * Only for scope access_camera. Address of the camera
 * @member {String} vpn_url
 */
NAWelcomeCamera.prototype['vpn_url'] = undefined;





/**
 * Allowed values for the <code>light_mode_status</code> property.
 * @enum {String}
 * @readonly
 */
NAWelcomeCamera['LightModeStatusEnum'] = {

    /**
     * value: "true"
     * @const
     */
    "true": "true",

    /**
     * value: "false"
     * @const
     */
    "false": "false",

    /**
     * value: "auto"
     * @const
     */
    "auto": "auto"
};



export default NAWelcomeCamera;

