/**
 * KYC API Documentation
 * API Interface to retrieve company data and products from business registers
 *
 * The version of the OpenAPI document: 2021.12
 * Contact: support@kompany.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIV1VatVerificationApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIV1VatVerificationApi::OAIV1VatVerificationApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIV1VatVerificationApi::~OAIV1VatVerificationApi() {
}

void OAIV1VatVerificationApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.kompany.com/"),
    "Main endpoint",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("vatBasic", defaultConf);
    _serverIndices.insert("vatBasic", 0);
    _serverConfigs.insert("vatComprehensive", defaultConf);
    _serverIndices.insert("vatComprehensive", 0);
    _serverConfigs.insert("vatLevelTwo", defaultConf);
    _serverIndices.insert("vatLevelTwo", 0);
    _serverConfigs.insert("vatLookup", defaultConf);
    _serverIndices.insert("vatLookup", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIV1VatVerificationApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIV1VatVerificationApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIV1VatVerificationApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIV1VatVerificationApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIV1VatVerificationApi::setUsername(const QString &username) {
    _username = username;
}

void OAIV1VatVerificationApi::setPassword(const QString &password) {
    _password = password;
}


void OAIV1VatVerificationApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIV1VatVerificationApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIV1VatVerificationApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIV1VatVerificationApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIV1VatVerificationApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIV1VatVerificationApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIV1VatVerificationApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIV1VatVerificationApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIV1VatVerificationApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIV1VatVerificationApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIV1VatVerificationApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIV1VatVerificationApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIV1VatVerificationApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIV1VatVerificationApi::vatBasic(const QString &country, const QString &vat_number, const ::OpenAPI::OptionalParam<QString> &company_address, const ::OpenAPI::OptionalParam<QString> &company_name, const ::OpenAPI::OptionalParam<QString> &company_number) {
    QString fullPath = QString(_serverConfigs["vatBasic"][_serverIndices.value("vatBasic")].URL()+"/api/v1/vat-verification/basic-check/{country}");
    
    if (_apiKeys.contains("user_key")) {
        addHeaders("user_key",_apiKeys.find("user_key").value());
    }
    
    
    {
        QString countryPathParam("{");
        countryPathParam.append("country").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "country", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"country"+pathSuffix : pathPrefix;
        fullPath.replace(countryPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(country)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (company_address.hasValue())
    {
        input.add_var("companyAddress", ::OpenAPI::toStringValue(company_address.value()));
    }
    if (company_name.hasValue())
    {
        input.add_var("companyName", ::OpenAPI::toStringValue(company_name.value()));
    }
    if (company_number.hasValue())
    {
        input.add_var("companyNumber", ::OpenAPI::toStringValue(company_number.value()));
    }
    
    {
        input.add_var("vatNumber", ::OpenAPI::toStringValue(vat_number));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIV1VatVerificationApi::vatBasicCallback);
    connect(this, &OAIV1VatVerificationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIV1VatVerificationApi::vatBasicCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIVatBasic_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT vatBasicSignal(output);
        Q_EMIT vatBasicSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT vatBasicSignalE(output, error_type, error_str);
        Q_EMIT vatBasicSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT vatBasicSignalError(output, error_type, error_str);
        Q_EMIT vatBasicSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIV1VatVerificationApi::vatComprehensive(const QString &country, const QString &vat_number, const ::OpenAPI::OptionalParam<QString> &company_address, const ::OpenAPI::OptionalParam<QString> &company_name, const ::OpenAPI::OptionalParam<QString> &company_number) {
    QString fullPath = QString(_serverConfigs["vatComprehensive"][_serverIndices.value("vatComprehensive")].URL()+"/api/v1/vat-verification/comprehensive-check/{country}");
    
    if (_apiKeys.contains("user_key")) {
        addHeaders("user_key",_apiKeys.find("user_key").value());
    }
    
    
    {
        QString countryPathParam("{");
        countryPathParam.append("country").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "country", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"country"+pathSuffix : pathPrefix;
        fullPath.replace(countryPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(country)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (company_address.hasValue())
    {
        input.add_var("companyAddress", ::OpenAPI::toStringValue(company_address.value()));
    }
    if (company_name.hasValue())
    {
        input.add_var("companyName", ::OpenAPI::toStringValue(company_name.value()));
    }
    if (company_number.hasValue())
    {
        input.add_var("companyNumber", ::OpenAPI::toStringValue(company_number.value()));
    }
    
    {
        input.add_var("vatNumber", ::OpenAPI::toStringValue(vat_number));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIV1VatVerificationApi::vatComprehensiveCallback);
    connect(this, &OAIV1VatVerificationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIV1VatVerificationApi::vatComprehensiveCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT vatComprehensiveSignal();
        Q_EMIT vatComprehensiveSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT vatComprehensiveSignalE(error_type, error_str);
        Q_EMIT vatComprehensiveSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT vatComprehensiveSignalError(error_type, error_str);
        Q_EMIT vatComprehensiveSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIV1VatVerificationApi::vatLevelTwo(const QString &country, const QString &vat_number, const ::OpenAPI::OptionalParam<bool> &confirmation) {
    QString fullPath = QString(_serverConfigs["vatLevelTwo"][_serverIndices.value("vatLevelTwo")].URL()+"/api/v1/vat-verification/leveltwo-check/{country}");
    
    if (_apiKeys.contains("user_key")) {
        addHeaders("user_key",_apiKeys.find("user_key").value());
    }
    
    
    {
        QString countryPathParam("{");
        countryPathParam.append("country").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "country", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"country"+pathSuffix : pathPrefix;
        fullPath.replace(countryPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(country)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (confirmation.hasValue())
    {
        input.add_var("confirmation", ::OpenAPI::toStringValue(confirmation.value()));
    }
    
    {
        input.add_var("vatNumber", ::OpenAPI::toStringValue(vat_number));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIV1VatVerificationApi::vatLevelTwoCallback);
    connect(this, &OAIV1VatVerificationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIV1VatVerificationApi::vatLevelTwoCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIVatLevelTwo_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT vatLevelTwoSignal(output);
        Q_EMIT vatLevelTwoSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT vatLevelTwoSignalE(output, error_type, error_str);
        Q_EMIT vatLevelTwoSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT vatLevelTwoSignalError(output, error_type, error_str);
        Q_EMIT vatLevelTwoSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIV1VatVerificationApi::vatLookup(const QString &country, const QString &name, const ::OpenAPI::OptionalParam<QString> &address) {
    QString fullPath = QString(_serverConfigs["vatLookup"][_serverIndices.value("vatLookup")].URL()+"/api/v1/vat-verification/lookup/{country}");
    
    if (_apiKeys.contains("user_key")) {
        addHeaders("user_key",_apiKeys.find("user_key").value());
    }
    
    
    {
        QString countryPathParam("{");
        countryPathParam.append("country").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "country", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"country"+pathSuffix : pathPrefix;
        fullPath.replace(countryPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(country)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (address.hasValue())
    {
        input.add_var("address", ::OpenAPI::toStringValue(address.value()));
    }
    
    {
        input.add_var("name", ::OpenAPI::toStringValue(name));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIV1VatVerificationApi::vatLookupCallback);
    connect(this, &OAIV1VatVerificationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIV1VatVerificationApi::vatLookupCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIVatLookup_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT vatLookupSignal(output);
        Q_EMIT vatLookupSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT vatLookupSignalE(output, error_type, error_str);
        Q_EMIT vatLookupSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT vatLookupSignalError(output, error_type, error_str);
        Q_EMIT vatLookupSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIV1VatVerificationApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
