/**
 * vRealize Network Insight API Reference
 * vRealize Network Insight API Reference
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BaseEntity from './BaseEntity';
import EntityType from './EntityType';
import Reference from './Reference';

/**
 * The SecurityTag model module.
 * @module model/SecurityTag
 * @version 1.0.0
 */
class SecurityTag {
    /**
     * Constructs a new <code>SecurityTag</code>.
     * @alias module:model/SecurityTag
     * @extends module:model/BaseEntity
     * @implements module:model/BaseEntity
     */
    constructor() { 
        BaseEntity.initialize(this);
        SecurityTag.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SecurityTag</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SecurityTag} obj Optional instance to populate.
     * @return {module:model/SecurityTag} The populated <code>SecurityTag</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SecurityTag();
            BaseEntity.constructFromObject(data, obj);
            BaseEntity.constructFromObject(data, obj);

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('direct_security_groups')) {
                obj['direct_security_groups'] = ApiClient.convertToType(data['direct_security_groups'], [Reference]);
            }
            if (data.hasOwnProperty('nsx_manager')) {
                obj['nsx_manager'] = Reference.constructFromObject(data['nsx_manager']);
            }
            if (data.hasOwnProperty('security_groups')) {
                obj['security_groups'] = ApiClient.convertToType(data['security_groups'], [Reference]);
            }
            if (data.hasOwnProperty('vendor_id')) {
                obj['vendor_id'] = ApiClient.convertToType(data['vendor_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SecurityTag</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SecurityTag</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['direct_security_groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['direct_security_groups'])) {
                throw new Error("Expected the field `direct_security_groups` to be an array in the JSON data but got " + data['direct_security_groups']);
            }
            // validate the optional field `direct_security_groups` (array)
            for (const item of data['direct_security_groups']) {
                Reference.validateJSON(item);
            };
        }
        // validate the optional field `nsx_manager`
        if (data['nsx_manager']) { // data not null
          Reference.validateJSON(data['nsx_manager']);
        }
        if (data['security_groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['security_groups'])) {
                throw new Error("Expected the field `security_groups` to be an array in the JSON data but got " + data['security_groups']);
            }
            // validate the optional field `security_groups` (array)
            for (const item of data['security_groups']) {
                Reference.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['vendor_id'] && !(typeof data['vendor_id'] === 'string' || data['vendor_id'] instanceof String)) {
            throw new Error("Expected the field `vendor_id` to be a primitive type in the JSON string but got " + data['vendor_id']);
        }

        return true;
    }


}



/**
 * @member {String} description
 */
SecurityTag.prototype['description'] = undefined;

/**
 * @member {Array.<module:model/Reference>} direct_security_groups
 */
SecurityTag.prototype['direct_security_groups'] = undefined;

/**
 * @member {module:model/Reference} nsx_manager
 */
SecurityTag.prototype['nsx_manager'] = undefined;

/**
 * @member {Array.<module:model/Reference>} security_groups
 */
SecurityTag.prototype['security_groups'] = undefined;

/**
 * @member {String} vendor_id
 */
SecurityTag.prototype['vendor_id'] = undefined;


// Implement BaseEntity interface:
/**
 * @member {String} entity_id
 */
BaseEntity.prototype['entity_id'] = undefined;
/**
 * @member {module:model/EntityType} entity_type
 */
BaseEntity.prototype['entity_type'] = undefined;
/**
 * @member {String} name
 */
BaseEntity.prototype['name'] = undefined;




export default SecurityTag;

