/**
 * vRealize Network Insight API Reference
 * vRealize Network Insight API Reference
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BaseEntity from './BaseEntity';
import EntityType from './EntityType';
import Reference from './Reference';
import RuleSet from './RuleSet';

/**
 * The BaseFirewall model module.
 * @module model/BaseFirewall
 * @version 1.0.0
 */
class BaseFirewall {
    /**
     * Constructs a new <code>BaseFirewall</code>.
     * @alias module:model/BaseFirewall
     * @extends module:model/BaseEntity
     * @implements module:model/BaseEntity
     */
    constructor() { 
        BaseEntity.initialize(this);
        BaseFirewall.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BaseFirewall</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BaseFirewall} obj Optional instance to populate.
     * @return {module:model/BaseFirewall} The populated <code>BaseFirewall</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BaseFirewall();
            BaseEntity.constructFromObject(data, obj);
            BaseEntity.constructFromObject(data, obj);

            if (data.hasOwnProperty('exclusions')) {
                obj['exclusions'] = ApiClient.convertToType(data['exclusions'], [Reference]);
            }
            if (data.hasOwnProperty('firewall_rules')) {
                obj['firewall_rules'] = ApiClient.convertToType(data['firewall_rules'], [RuleSet]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BaseFirewall</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BaseFirewall</code>.
     */
    static validateJSON(data) {
        if (data['exclusions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['exclusions'])) {
                throw new Error("Expected the field `exclusions` to be an array in the JSON data but got " + data['exclusions']);
            }
            // validate the optional field `exclusions` (array)
            for (const item of data['exclusions']) {
                Reference.validateJSON(item);
            };
        }
        if (data['firewall_rules']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['firewall_rules'])) {
                throw new Error("Expected the field `firewall_rules` to be an array in the JSON data but got " + data['firewall_rules']);
            }
            // validate the optional field `firewall_rules` (array)
            for (const item of data['firewall_rules']) {
                RuleSet.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/Reference>} exclusions
 */
BaseFirewall.prototype['exclusions'] = undefined;

/**
 * @member {Array.<module:model/RuleSet>} firewall_rules
 */
BaseFirewall.prototype['firewall_rules'] = undefined;


// Implement BaseEntity interface:
/**
 * @member {String} entity_id
 */
BaseEntity.prototype['entity_id'] = undefined;
/**
 * @member {module:model/EntityType} entity_type
 */
BaseEntity.prototype['entity_type'] = undefined;
/**
 * @member {String} name
 */
BaseEntity.prototype['name'] = undefined;




export default BaseFirewall;

