/**
 * Wealth Reader API
 * Las APIs regulatorias basadas en PSD2 proporcionan acceso a cierta información financiera como saldos de cuentas bancarias y transacciones. Sin embargo, hay otras fuentes de información patrimonial que no son accesibles por estas APIs. La API de Wealth Reader amplía la información ofrecida por las APIs regulatorias proporcionando acceso en tiempo real a las fuentes patrimoniales adicionales en cualquier entidad del mundo. Existen otros dos documentos relacionados que te ayudarán a integrar la API de Wealth Reader. Uno es la guía de integración del widget Javascript: https://docs-es.wealthreader.com/, y el  otro una colección Postman basada en esta documentación.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: info@wealthreader.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Statistics model module.
 * @module model/Statistics
 * @version 1.0.0
 */
class Statistics {
    /**
     * Constructs a new <code>Statistics</code>.
     * @alias module:model/Statistics
     * @param SESSION {String} Guarda siempre este valor, tanto si la conexión es correcta como si no. Es necesario para obtener soporte
     * @param executionTime {Number} Duración de la ejecución
     */
    constructor(SESSION, executionTime) { 
        
        Statistics.initialize(this, SESSION, executionTime);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, SESSION, executionTime) { 
        obj['SESSION'] = SESSION;
        obj['execution_time'] = executionTime;
    }

    /**
     * Constructs a <code>Statistics</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Statistics} obj Optional instance to populate.
     * @return {module:model/Statistics} The populated <code>Statistics</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Statistics();

            if (data.hasOwnProperty('SESSION')) {
                obj['SESSION'] = ApiClient.convertToType(data['SESSION'], 'String');
            }
            if (data.hasOwnProperty('execution_time')) {
                obj['execution_time'] = ApiClient.convertToType(data['execution_time'], 'Number');
            }
            if (data.hasOwnProperty('operation_id')) {
                obj['operation_id'] = ApiClient.convertToType(data['operation_id'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Statistics</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Statistics</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Statistics.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['SESSION'] && !(typeof data['SESSION'] === 'string' || data['SESSION'] instanceof String)) {
            throw new Error("Expected the field `SESSION` to be a primitive type in the JSON string but got " + data['SESSION']);
        }
        // ensure the json data is a string
        if (data['operation_id'] && !(typeof data['operation_id'] === 'string' || data['operation_id'] instanceof String)) {
            throw new Error("Expected the field `operation_id` to be a primitive type in the JSON string but got " + data['operation_id']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }

        return true;
    }


}

Statistics.RequiredProperties = ["SESSION", "execution_time"];

/**
 * Guarda siempre este valor, tanto si la conexión es correcta como si no. Es necesario para obtener soporte
 * @member {String} SESSION
 */
Statistics.prototype['SESSION'] = undefined;

/**
 * Duración de la ejecución
 * @member {Number} execution_time
 */
Statistics.prototype['execution_time'] = undefined;

/**
 * El valor vendrá indicado cuando se haya invocado a la API a través del Widget que proporciona Wealth Reader. Para saber más sobre este tipo de integración, visita: https://docs-es.wealthreader.com/
 * @member {String} operation_id
 */
Statistics.prototype['operation_id'] = undefined;

/**
 * El valor vendrá indicado cuando se ha pedido la custodia de credenciales con la opción tokenize=true
 * @member {String} token
 */
Statistics.prototype['token'] = undefined;






export default Statistics;

