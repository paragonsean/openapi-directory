/**
 * Marketcheck APIs
 * One API serving data spanned across multiple verticals
 *
 * The version of the OpenAPI document: 2.01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import Error from '../model/Error';
import Listing from '../model/Listing';
import ListingExtraAttributes from '../model/ListingExtraAttributes';
import ListingMedia from '../model/ListingMedia';
import SearchAutoCompleteResponse from '../model/SearchAutoCompleteResponse';
import SearchResponse from '../model/SearchResponse';
import UKSearchResponse from '../model/UKSearchResponse';

/**
* CarSearch service.
* @module api/CarSearchApi
* @version 2.01
*/
export default class CarSearchApi {

    /**
    * Constructs a new CarSearchApi. 
    * @alias module:api/CarSearchApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the autoComplete operation.
     * @callback module:api/CarSearchApi~autoCompleteCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchAutoCompleteResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * API for auto-completion of inputs
     * Auto-complete the inputs of your end users
     * @param {module:model/String} field Field name for which you want auto-completion
     * @param {String} input Input entered so far
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {String} [year] To filter listing on their year
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [bodySubtype] Body subtype to filter the listings on. Valid filter values are those that our Search facets API returns for unique body subtypes. You can pass in multiple body subtype values comma separated
     * @param {String} [vehicleType] To filter listing on their vehicle type
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineBlock] Engine Block to match. Valid filter values are those that our Search facets API returns for unique Engine Block. You can pass in multiple Engine Block values comma separated
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [source] To filter listing on their source only for widget requests
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {module:model/String} [country = 'US')] To filter listing on Country in which they are listed
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used
     * @param {module:model/String} [includeNonVinListings = 'false')] Flag to indicate whether to include non vin listing terms in results or not. Default is false to avoid un-normalised terms from non vin listings out of results
     * @param {Boolean} [ignoreCase = true)] Boolean variable to indicate ignore case of current input
     * @param {Boolean} [termCounts = false)] Boolean variable to indicate wheather to include term counts as well in response
     * @param {module:model/String} [sortBy = 'index')] Sort the response, either by index or count(default)
     * @param {String} [sellerType] seller type for autocomplete
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {String} [inventoryCountRange] Inventory count range to filter listings with count of total listings in dealers inventory. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [excludeDealerIds] A list of dealer ids to exclude from result
     * @param {String} [excludeSources] A list of sources to exclude from result
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {Number} [facetMinCount = 1)] Provide minimum count value for facets
     * @param {module:api/CarSearchApi~autoCompleteCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchAutoCompleteResponse}
     */
    autoComplete(field, input, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'field' is set
      if (field === undefined || field === null) {
        throw new Error("Missing the required parameter 'field' when calling autoComplete");
      }
      // verify the required parameter 'input' is set
      if (input === undefined || input === null) {
        throw new Error("Missing the required parameter 'input' when calling autoComplete");
      }

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'field': field,
        'input': input,
        'year': opts['year'],
        'make': opts['make'],
        'model': opts['model'],
        'trim': opts['trim'],
        'body_type': opts['bodyType'],
        'body_subtype': opts['bodySubtype'],
        'vehicle_type': opts['vehicleType'],
        'transmission': opts['transmission'],
        'drivetrain': opts['drivetrain'],
        'fuel_type': opts['fuelType'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'engine': opts['engine'],
        'engine_size': opts['engineSize'],
        'engine_block': opts['engineBlock'],
        'state': opts['state'],
        'city': opts['city'],
        'source': opts['source'],
        'dealer_id': opts['dealerId'],
        'country': opts['country'],
        'car_type': opts['carType'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'ignore_case': opts['ignoreCase'],
        'term_counts': opts['termCounts'],
        'sort_by': opts['sortBy'],
        'seller_type': opts['sellerType'],
        'radius': opts['radius'],
        'zip': opts['zip'],
        'inventory_count_range': opts['inventoryCountRange'],
        'exclude_dealer_ids': opts['excludeDealerIds'],
        'exclude_sources': opts['excludeSources'],
        'in_transit': opts['inTransit'],
        'facet_min_count': opts['facetMinCount']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = SearchAutoCompleteResponse;
      return this.apiClient.callApi(
        '/search/car/auto-complete', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the carDealerInventoryActiveGet operation.
     * @callback module:api/CarSearchApi~carDealerInventoryActiveGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get dealers active inventory
     * Get dealers active inventory
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Number} [latitude] Latitude component of location
     * @param {Number} [longitude] Longitude component of location
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {Boolean} [includeLease] Boolean param to search for listings that include leasing options in them
     * @param {Boolean} [includeFinance] Boolean param to search for listings that include finance options in them
     * @param {String} [leaseTerm] Search listings with exact lease term, or inside a range with min and max seperated by a dash like lease_term=30-60
     * @param {String} [leaseDownPayment] Search listings with exact down payment in lease offers, or inside a range with min and max seperated by a dash like lease_down_payment=30-60
     * @param {String} [leaseEmp] Search listings with lease offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like lease_emp=30-60
     * @param {String} [financeLoanTerm] Search listings with exact finance loan term, or inside a range with min and max seperated by a dash like finance_loan_term=30-60
     * @param {String} [financeLoanApr] Search listings with finance offers exactly matching loans Annual Percentage Rate, or inside a range with min and max seperated by a dash like finance_loan_apr=30-60
     * @param {String} [financeEmp] Search listings with finance offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like finance_emp=30-60
     * @param {String} [financeDownPayment] Search listings with exact down payment in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment=30-60
     * @param {String} [financeDownPaymentPer] Search listings with exact down payment percentage in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment_per=30-60
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used / certified
     * @param {module:model/String} [carfax1Owner] Indicates whether car has had only one owner or not
     * @param {module:model/String} [carfaxCleanTitle] Indicates whether car has clean ownership records
     * @param {String} [yearRange] Year range to filter listings with the year in the range given. Range to be given in the format - min-max e.g. 2019-2021
     * @param {String} [year] To filter listing on their year
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {String} [vin] To filter listing on their VIN
     * @param {String} [source] To filter listing on their source
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [bodySubtype] Body subtype to filter the listings on. Valid filter values are those that our Search facets API returns for unique body subtypes. You can pass in multiple body subtype values comma separated
     * @param {String} [vehicleType] To filter listing on their vehicle type
     * @param {String} [vins] Comma separated list of 17 digit vins to search the matching cars for. Only 10 VINs allowed per request. If the request contains more than 10 VINs the first 10 VINs will be considered. Could be used as a More Like This or Similar Vehicles search for the given VINs. Ths vins parameter is an alternative to taxonomy_vins or ymmt parameters available with the search API. vins and taxonomy_vins parameters could be used to filter our cars with the exact build represented by the vins or taxonomy_vins whereas ymmt is a top level filter that does not filter cars by the build attributes like doors, drivetrain, cylinders, body type, body subtype, vehicle type etc
     * @param {String} [taxonomyVins] Comma separated list of 10 letters excert from the 17 letter VIN. The 10 letters to be picked up from the 17 letter VIN are - first 8 letters and the 10th and 11th letter. E.g. For a VIN - 1FTFW1EF3EKE57182 the taxonomy vin would be - 1FTFW1EFEK  A taxonomy VIN identified a build of a car and could be used to filter our cars of a particular build. This is an alternative to the vin or ymmt parameters to the search API.
     * @param {String} [mm] Make-Model concatenated string. To help passing the results of auto-complete API on mm field, use this parameter and pass in the selected value as is
     * @param {String} [ymm] Year-Make-Model concatenated string. To help passing the results of auto-complete API on ymm field, use this parameter and pass in the selected value as is
     * @param {String} [ymmt] Comma separated list of Year, Make, Model, Trim combinations. Each combination needs to have the year,make,model, trim values separated by a pipe '|' character in the form year|make|model|trim. e.g. 2010|Audi|A5,2014|Nissan|Sentra|S 6MT,|Honda|City|   You could just provide strings of the form - 'year|make||' or 'year|make|model' or '|make|model|' combinations. Individual year / make / model filters provied with the API calls will take precedence over the Year, Make, Model, Trim combinations. The Make, Model, Trim values must be valid values as per the Marketcheck Vin Decoder. If you are using a separate vin decoder then look at using the 'vins' or 'taxonomy_vins' parameter to the search api instead the year|make|model|trim combinations.
     * @param {String} [match] Comma separated list of Year, Make, Model, Trim fields. For example - year,make,model,trim fields for which user wants to do an exact match
     * @param {String} [cylinders] To filter listing on their cylinders
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [doors] Doors to filter the cars on. Valid filter values are those that our Search facets API returns for unique doors. You can pass in multiple doors values comma separated
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [baseExteriorColor] Base exterior color to match. Valid filter values are those that our Search facets API returns for unique base exterior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [baseInteriorColor] Base interior color to match. Valid filter values are those that our Search facets API returns for unique base interior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineAspiration] Engine Aspiration to match. Valid filter values are those that our Search facets API returns for unique Engine Aspirations. You can pass in multiple Engine aspirations values comma separated
     * @param {String} [engineBlock] Engine Block to match. Valid filter values are those that our Search facets API returns for unique Engine Block. You can pass in multiple Engine Block values comma separated
     * @param {String} [highwayMpgRange] Highway mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [cityMpgRange] City mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [milesRange] Miles range to filter listings with miles in the given range. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [priceRange] Price range to filter listings with the price in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [msrpRange] MSRP range to filter listings with the msrp in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [domRange] Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [sortBy] Sort by field. Default sort field is distance from the given point
     * @param {module:model/String} [sortOrder] Sort order - asc or desc. Default sort order is asc
     * @param {Number} [rows = 10)] Number of results to return. Default is 10. Max is 50
     * @param {Number} [start = 0)] Page number to fetch the results for the given criteria. Default is 0. Pagination is allowed only till first 10000 results for the search and sort criteria. The page value can be only between 1 to 10000/rows
     * @param {Boolean} [includeNonVinListings = false)] To include non vin listings. Default is false
     * @param {String} [msaCode] To filter listing on msa code in which they are listed
     * @param {String} [facets] The comma separated list of fields for which facets are requested. Facets could be requested in addition to the listings for the search. Please note - The API calls with lots of facet fields may take longer to respond.
     * @param {String} [rangeFacets] The comma separated list of numeric fields for which range facets are requested. Range facets could be requested in addition to the listings for the search. Please note - The API calls with lots of range facet fields may take longer to respond.
     * @param {module:model/String} [facetSort = 'count')] Control sort order of facets with this parameter with default sort being on count, Other available sort is alphabetical sort, which can be obtained by using index as value for this param
     * @param {String} [stats] The list of fields for which stats need to be generated based on the matching listings for the search criteria. The stats consists of mean, max, average and count of listings based on which the stats are calculated for the field. Stats could be requested in addition to the listings for the search. Please note - The API calls with the stats fields may take longer to respond.
     * @param {module:model/String} [country = 'US')] To filter listing on Country in which they are listed
     * @param {Boolean} [plot] If plot has value true results in around 25k coordinates with limited fields to plot respective graph
     * @param {Boolean} [nodedup] If nodedup is set to true then API will give results without is_searchable i.e multiple listings for single vin
     * @param {Boolean} [dedup] If dedup is set to true then will give results with is_searchable irrespecive of dealer_id or source
     * @param {Boolean} [owned] Used in combination with dealer_id or source, when true returns the listings actually owned by dealer himself
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [dealerName] Filter listings on dealer_name
     * @param {String} [dealershipGroupName] Name of the dealership group to search for
     * @param {String} [trimO] Filter listings on web scraped trim
     * @param {String} [trimR] Filter trim on custom possible matches
     * @param {String} [domActiveRange] Active Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dom180Range] Last 180 Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {Boolean} [excludeCertified] Boolean param to exclude certified cars from search results
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {module:model/String} [dealerType] Filter based on dealer type independant or franchise
     * @param {Boolean} [photoLinks] A boolean indicating whether to include only those listings that have photo_links in search results, And discard those that don't have them
     * @param {Boolean} [photoLinksCached] A boolean indicating whether to include only those listings that have photo_links_cached in search results, And discard those that don't have them
     * @param {String} [stockNo] To filter listing on their stock number on lot
     * @param {String} [lastSeenRange] Last seen date range to filter listings with the last seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenRange] First seen date range to filter listings with the first seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtSourceRange] First seen at source date range to filter listings with the first seen at source in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtMcRange] First seen at MC date range to filter listings with the first seen at MC in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [lastSeenDays] Last seen days range to filter listings with the last seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenDays] First seen days range to filter listings with the first seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtSourceDays] First seen at source days range to filter listings with the first seen at source in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtMcDays] First seen at MC days range to filter listings with the first seen at MC in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {String} [inventoryCountRange] Inventory count range to filter listings with count of total listings in dealers inventory. Range to be given in the format - min-max e.g. 10-50
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {String} [seatingCapacity] To filter on vehicle seating capacity
     * @param {String} [engineSizeRange] Engine size range to filter listings with engine size in the given range. Range to be given in the format - min-max e.g. 1.0-2
     * @param {String} [powertrainType] To filter on powertrain_type
     * @param {String} [minPhotoLinks] Filter listings based by number of photo links within given range
     * @param {String} [minPhotoLinksCached] Filter listings based by number of cached photo links within given range
     * @param {module:api/CarSearchApi~carDealerInventoryActiveGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchResponse}
     */
    carDealerInventoryActiveGet(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'radius': opts['radius'],
        'zip': opts['zip'],
        'include_lease': opts['includeLease'],
        'include_finance': opts['includeFinance'],
        'lease_term': opts['leaseTerm'],
        'lease_down_payment': opts['leaseDownPayment'],
        'lease_emp': opts['leaseEmp'],
        'finance_loan_term': opts['financeLoanTerm'],
        'finance_loan_apr': opts['financeLoanApr'],
        'finance_emp': opts['financeEmp'],
        'finance_down_payment': opts['financeDownPayment'],
        'finance_down_payment_per': opts['financeDownPaymentPer'],
        'car_type': opts['carType'],
        'carfax_1_owner': opts['carfax1Owner'],
        'carfax_clean_title': opts['carfaxCleanTitle'],
        'year_range': opts['yearRange'],
        'year': opts['year'],
        'make': opts['make'],
        'model': opts['model'],
        'trim': opts['trim'],
        'dealer_id': opts['dealerId'],
        'vin': opts['vin'],
        'source': opts['source'],
        'body_type': opts['bodyType'],
        'body_subtype': opts['bodySubtype'],
        'vehicle_type': opts['vehicleType'],
        'vins': opts['vins'],
        'taxonomy_vins': opts['taxonomyVins'],
        'mm': opts['mm'],
        'ymm': opts['ymm'],
        'ymmt': opts['ymmt'],
        'match': opts['match'],
        'cylinders': opts['cylinders'],
        'transmission': opts['transmission'],
        'doors': opts['doors'],
        'drivetrain': opts['drivetrain'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'base_exterior_color': opts['baseExteriorColor'],
        'base_interior_color': opts['baseInteriorColor'],
        'engine': opts['engine'],
        'engine_size': opts['engineSize'],
        'engine_aspiration': opts['engineAspiration'],
        'engine_block': opts['engineBlock'],
        'highway_mpg_range': opts['highwayMpgRange'],
        'city_mpg_range': opts['cityMpgRange'],
        'miles_range': opts['milesRange'],
        'price_range': opts['priceRange'],
        'msrp_range': opts['msrpRange'],
        'dom_range': opts['domRange'],
        'sort_by': opts['sortBy'],
        'sort_order': opts['sortOrder'],
        'rows': opts['rows'],
        'start': opts['start'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'msa_code': opts['msaCode'],
        'facets': opts['facets'],
        'range_facets': opts['rangeFacets'],
        'facet_sort': opts['facetSort'],
        'stats': opts['stats'],
        'country': opts['country'],
        'plot': opts['plot'],
        'nodedup': opts['nodedup'],
        'dedup': opts['dedup'],
        'owned': opts['owned'],
        'state': opts['state'],
        'city': opts['city'],
        'dealer_name': opts['dealerName'],
        'dealership_group_name': opts['dealershipGroupName'],
        'trim_o': opts['trimO'],
        'trim_r': opts['trimR'],
        'dom_active_range': opts['domActiveRange'],
        'dom_180_range': opts['dom180Range'],
        'exclude_certified': opts['excludeCertified'],
        'fuel_type': opts['fuelType'],
        'dealer_type': opts['dealerType'],
        'photo_links': opts['photoLinks'],
        'photo_links_cached': opts['photoLinksCached'],
        'stock_no': opts['stockNo'],
        'last_seen_range': opts['lastSeenRange'],
        'first_seen_range': opts['firstSeenRange'],
        'first_seen_at_source_range': opts['firstSeenAtSourceRange'],
        'first_seen_at_mc_range': opts['firstSeenAtMcRange'],
        'last_seen_days': opts['lastSeenDays'],
        'first_seen_days': opts['firstSeenDays'],
        'first_seen_at_source_days': opts['firstSeenAtSourceDays'],
        'first_seen_at_mc_days': opts['firstSeenAtMcDays'],
        'include_relevant_links': opts['includeRelevantLinks'],
        'inventory_count_range': opts['inventoryCountRange'],
        'in_transit': opts['inTransit'],
        'seating_capacity': opts['seatingCapacity'],
        'engine_size_range': opts['engineSizeRange'],
        'powertrain_type': opts['powertrainType'],
        'min_photo_links': opts['minPhotoLinks'],
        'min_photo_links_cached': opts['minPhotoLinksCached']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = SearchResponse;
      return this.apiClient.callApi(
        '/car/dealer/inventory/active', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getListing operation.
     * @callback module:api/CarSearchApi~getListingCallback
     * @param {String} error Error message, if any.
     * @param {module:model/Listing} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing by id
     * Get a particular dealer listing by its id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {module:api/CarSearchApi~getListingCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/Listing}
     */
    getListing(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getListing");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'include_relevant_links': opts['includeRelevantLinks']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = Listing;
      return this.apiClient.callApi(
        '/listing/car/{id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarAuctionIdExtraGet operation.
     * @callback module:api/CarSearchApi~listingCarAuctionIdExtraGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingExtraAttributes} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Long text Listings attributes for Listing with the given id
     * Get listing options, features, seller comments
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {module:api/CarSearchApi~listingCarAuctionIdExtraGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingExtraAttributes}
     */
    listingCarAuctionIdExtraGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarAuctionIdExtraGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingExtraAttributes;
      return this.apiClient.callApi(
        '/listing/car/auction/{id}/extra', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarAuctionIdGet operation.
     * @callback module:api/CarSearchApi~listingCarAuctionIdGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/Listing} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing by id
     * Get a particular auction listing by its id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {module:api/CarSearchApi~listingCarAuctionIdGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/Listing}
     */
    listingCarAuctionIdGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarAuctionIdGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'include_relevant_links': opts['includeRelevantLinks']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = Listing;
      return this.apiClient.callApi(
        '/listing/car/auction/{id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarAuctionIdMediaGet operation.
     * @callback module:api/CarSearchApi~listingCarAuctionIdMediaGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingMedia} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing media by id
     * Get listing media (photo, photos) by id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {module:api/CarSearchApi~listingCarAuctionIdMediaGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingMedia}
     */
    listingCarAuctionIdMediaGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarAuctionIdMediaGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingMedia;
      return this.apiClient.callApi(
        '/listing/car/auction/{id}/media', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarFsboIdExtraGet operation.
     * @callback module:api/CarSearchApi~listingCarFsboIdExtraGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingExtraAttributes} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Long text Listings attributes for Listing with the given id
     * Get listing options, features, seller comments
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {module:api/CarSearchApi~listingCarFsboIdExtraGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingExtraAttributes}
     */
    listingCarFsboIdExtraGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarFsboIdExtraGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingExtraAttributes;
      return this.apiClient.callApi(
        '/listing/car/fsbo/{id}/extra', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarFsboIdGet operation.
     * @callback module:api/CarSearchApi~listingCarFsboIdGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/Listing} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing by id
     * Get a particular private party listing by its id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {module:api/CarSearchApi~listingCarFsboIdGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/Listing}
     */
    listingCarFsboIdGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarFsboIdGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'include_relevant_links': opts['includeRelevantLinks']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = Listing;
      return this.apiClient.callApi(
        '/listing/car/fsbo/{id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarFsboIdMediaGet operation.
     * @callback module:api/CarSearchApi~listingCarFsboIdMediaGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingMedia} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing media by id
     * Get listing media (photo, photos) by id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {module:api/CarSearchApi~listingCarFsboIdMediaGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingMedia}
     */
    listingCarFsboIdMediaGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarFsboIdMediaGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingMedia;
      return this.apiClient.callApi(
        '/listing/car/fsbo/{id}/media', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarIdExtraGet operation.
     * @callback module:api/CarSearchApi~listingCarIdExtraGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingExtraAttributes} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Long text Listings attributes for Listing with the given id
     * Get listing options, features, seller comments
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {module:api/CarSearchApi~listingCarIdExtraGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingExtraAttributes}
     */
    listingCarIdExtraGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarIdExtraGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingExtraAttributes;
      return this.apiClient.callApi(
        '/listing/car/{id}/extra', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarIdMediaGet operation.
     * @callback module:api/CarSearchApi~listingCarIdMediaGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingMedia} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing media by id
     * Get listing media (photo, photos) by id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {module:api/CarSearchApi~listingCarIdMediaGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingMedia}
     */
    listingCarIdMediaGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarIdMediaGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingMedia;
      return this.apiClient.callApi(
        '/listing/car/{id}/media', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarUkIdExtraGet operation.
     * @callback module:api/CarSearchApi~listingCarUkIdExtraGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingExtraAttributes} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Long text Listings attributes for Listing with the given id
     * Get listing options, features, seller comments
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {module:api/CarSearchApi~listingCarUkIdExtraGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingExtraAttributes}
     */
    listingCarUkIdExtraGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarUkIdExtraGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingExtraAttributes;
      return this.apiClient.callApi(
        '/listing/car/uk/{id}/extra', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarUkIdGet operation.
     * @callback module:api/CarSearchApi~listingCarUkIdGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/Listing} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing by id
     * Get a particular dealer listing by its id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {module:api/CarSearchApi~listingCarUkIdGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/Listing}
     */
    listingCarUkIdGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarUkIdGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = Listing;
      return this.apiClient.callApi(
        '/listing/car/uk/{id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the listingCarUkIdMediaGet operation.
     * @callback module:api/CarSearchApi~listingCarUkIdMediaGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ListingMedia} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Listing media by id
     * Get listing media (photo, photos) by id
     * @param {String} id Listing id to get all the listing attributes
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {module:api/CarSearchApi~listingCarUkIdMediaGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/ListingMedia}
     */
    listingCarUkIdMediaGet(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling listingCarUkIdMediaGet");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ListingMedia;
      return this.apiClient.callApi(
        '/listing/car/uk/{id}/media', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the search operation.
     * @callback module:api/CarSearchApi~searchCallback
     * @param {String} error Error message, if any.
     * @param {module:model/UKSearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gets active car listings in UK for the given search criteria
     * Search cars for sale in UK
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = false)] Flag on whether to include api_key in response API urls (if any)
     * @param {Number} [latitude] Latitude component of location
     * @param {Number} [longitude] Longitude component of location
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [postalCode] To filter listing on postal code around which they are listed
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used
     * @param {String} [year] To filter listing on their year
     * @param {String} [yearRange] Year range to filter listings with the year in the range given. Range to be given in the format - min-max e.g. 2019-2021
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [variant] To filter listing on their variant
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [ymmt] Comma separated list of Year, Make, Model, Trim combinations. Each combination needs to have the year,make,model, trim values separated by a pipe '|' character in the form year|make|model|trim. e.g. 2010|Audi|A5,2014|Nissan|Sentra|S 6MT,|Honda|City|   You could just provide strings of the form - 'year|make||' or 'year|make|model' or '|make|model|' combinations. Individual year / make / model filters provied with the API calls will take precedence over the Year, Make, Model, Trim combinations. The Make, Model, Trim values must be valid values as per the Marketcheck Vin Decoder. If you are using a separate vin decoder then look at using the 'vins' or 'taxonomy_vins' parameter to the search api instead the year|make|model|trim combinations.
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [doors] Doors to filter the cars on. Valid filter values are those that our Search facets API returns for unique doors. You can pass in multiple doors values comma separated
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [milesRange] Miles range to filter listings with miles in the given range. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [priceRange] Price range to filter listings with the price in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [msrpRange] MSRP range to filter listings with the msrp in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [sortBy] Sort by field. Default sort field is distance from the given point
     * @param {module:model/String} [sortOrder] Sort order - asc or desc. Default sort order is asc
     * @param {Number} [rows = 10)] Number of results to return. Default is 10. Max is 50
     * @param {Number} [start = 0)] Page number to fetch the results for the given criteria. Default is 0. Pagination is allowed only till first 10000 results for the search and sort criteria. The page value can be only between 1 to 10000/rows
     * @param {String} [msaCode] To filter listing on msa code in which they are listed
     * @param {String} [facets] The comma separated list of fields for which facets are requested. Facets could be requested in addition to the listings for the search. Please note - The API calls with lots of facet fields may take longer to respond.
     * @param {String} [rangeFacets] The comma separated list of numeric fields for which range facets are requested. Range facets could be requested in addition to the listings for the search. Please note - The API calls with lots of range facet fields may take longer to respond.
     * @param {module:model/String} [facetSort = 'count')] Control sort order of facets with this parameter with default sort being on count, Other available sort is alphabetical sort, which can be obtained by using index as value for this param
     * @param {String} [stats] The list of fields for which stats need to be generated based on the matching listings for the search criteria. The stats consists of mean, max, average and count of listings based on which the stats are calculated for the field. Stats could be requested in addition to the listings for the search. Please note - The API calls with the stats fields may take longer to respond.
     * @param {module:model/String} [country = 'uk')] To filter listing on Country in which they are listed
     * @param {Boolean} [plot] If plot has value true results in around 25k coordinates with limited fields to plot respective graph
     * @param {Boolean} [nodedup] If nodedup is set to true then API will give results without is_searchable i.e multiple listings for single vin
     * @param {Boolean} [dedup] If dedup is set to true then will give results with is_searchable irrespecive of dealer_id or source
     * @param {String} [county] To filter listing on county in which they are listed
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {String} [stockNo] To filter listing on their stock number on lot
     * @param {String} [domRange] Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [domActiveRange] Active Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dom180Range] Last 180 Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [lastSeenRange] Last seen date range to filter listings with the last seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenRange] First seen date range to filter listings with the first seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtSourceRange] First seen at source date range to filter listings with the first seen at source in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtMcRange] First seen at MC date range to filter listings with the first seen at MC in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [lastSeenDays] Last seen days range to filter listings with the last seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenDays] First seen days range to filter listings with the first seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtSourceDays] First seen at source days range to filter listings with the first seen at source in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtMcDays] First seen at MC days range to filter listings with the first seen at MC in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [co2Emissions] CO2 emissions
     * @param {String} [insuranceGroup] Insurance Group
     * @param {String} [vehicleRegistrationMark] Vehicle Registration Mark
     * @param {String} [vehicleRegistrationDateRange] Vehicle registration date range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [numOwners] Number of owners. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [inventoryCountRange] Inventory count range to filter listings with count of total listings in dealers inventory. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [source] To filter listing on their source only for widget requests
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {String} [excludeSources] A list of sources to exclude from result
     * @param {String} [excludeDealerIds] A list of dealer ids to exclude from result
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {Boolean} [includeNonVinListings = false)] To include non vin listings. Default is false
     * @param {String} [cylinders] To filter listing on their cylinders
     * @param {Boolean} [photoLinks] A boolean indicating whether to include only those listings that have photo_links in search results, And discard those that don't have them
     * @param {Boolean} [photoLinksCached] A boolean indicating whether to include only those listings that have photo_links_cached in search results, And discard those that don't have them
     * @param {String} [baseExteriorColor] Base exterior color to match. Valid filter values are those that our Search facets API returns for unique base exterior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [baseInteriorColor] Base interior color to match. Valid filter values are those that our Search facets API returns for unique base interior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [writeOffCategory] write off category
     * @param {String} [excludeWriteOffCategory] To exclude write off category
     * @param {String} [fcaStatus] To filter on fca status
     * @param {String} [seatingCapacity] To filter on vehicle seating capacity
     * @param {String} [vrm] To filter on vrm
     * @param {String} [powertrainType] To filter on powertrain_type
     * @param {Boolean} [clientFilters = true)] Flag to add explicit filters set on client level in solr
     * @param {Boolean} [boost = true)] Flag to sort listings based on client filter score in solr
     * @param {String} [carLocationSellerName] Filter cars on seller name
     * @param {String} [carLocationStreet] Filter cars on street name
     * @param {String} [carLocationCity] Filter cars on city
     * @param {String} [carLocationCounty] Filter cars on county
     * @param {String} [carLocationZip] To filter listing on car ZIP around which they are listed
     * @param {Number} [carLocationLatitude] Latitude component of car location
     * @param {Number} [carLocationLongitude] Longitude component of car location
     * @param {module:model/String} [priceChange] Query to filter listings based on their positive and negative price change
     * @param {String} [priceChangeRange] Price change range to filter listings with price change within given price_change_range. Range to be given in the format - min-max e.g. 10-500
     * @param {String} [activeInventoryDateRange] date range to filter listings that were active within given date range. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineSizeRange] Engine size range to filter listings with engine size in the given range. Range to be given in the format - min-max e.g. 1.0-2
     * @param {String} [uvcId] To filter on uvc id
     * @param {String} [highwayMpgRange] Highway mileage range for UK to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [cityMpgRange] City mileage range for UK to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [combinedMpgRange] Combined mileage range for UK to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {Boolean} [owned] Used in combination with dealer_id or source, when true returns the listings actually owned by dealer himself
     * @param {String} [minPhotoLinks] Filter listings based by number of photo links within given range
     * @param {String} [minPhotoLinksCached] Filter listings based by number of cached photo links within given range
     * @param {String} [match] Comma separated list of Year, Make, Model, Trim fields. For example - year,make,model,trim fields for which user wants to do an exact match
     * @param {Boolean} [ulezCompliant] Filter listings based on drive into ultra low emmition zone
     * @param {module:api/CarSearchApi~searchCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/UKSearchResponse}
     */
    search(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'radius': opts['radius'],
        'postal_code': opts['postalCode'],
        'zip': opts['zip'],
        'car_type': opts['carType'],
        'year': opts['year'],
        'year_range': opts['yearRange'],
        'make': opts['make'],
        'model': opts['model'],
        'variant': opts['variant'],
        'trim': opts['trim'],
        'body_type': opts['bodyType'],
        'ymmt': opts['ymmt'],
        'transmission': opts['transmission'],
        'doors': opts['doors'],
        'drivetrain': opts['drivetrain'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'engine': opts['engine'],
        'miles_range': opts['milesRange'],
        'price_range': opts['priceRange'],
        'msrp_range': opts['msrpRange'],
        'sort_by': opts['sortBy'],
        'sort_order': opts['sortOrder'],
        'rows': opts['rows'],
        'start': opts['start'],
        'msa_code': opts['msaCode'],
        'facets': opts['facets'],
        'range_facets': opts['rangeFacets'],
        'facet_sort': opts['facetSort'],
        'stats': opts['stats'],
        'country': opts['country'],
        'plot': opts['plot'],
        'nodedup': opts['nodedup'],
        'dedup': opts['dedup'],
        'county': opts['county'],
        'state': opts['state'],
        'city': opts['city'],
        'fuel_type': opts['fuelType'],
        'stock_no': opts['stockNo'],
        'dom_range': opts['domRange'],
        'dom_active_range': opts['domActiveRange'],
        'dom_180_range': opts['dom180Range'],
        'last_seen_range': opts['lastSeenRange'],
        'first_seen_range': opts['firstSeenRange'],
        'first_seen_at_source_range': opts['firstSeenAtSourceRange'],
        'first_seen_at_mc_range': opts['firstSeenAtMcRange'],
        'last_seen_days': opts['lastSeenDays'],
        'first_seen_days': opts['firstSeenDays'],
        'first_seen_at_source_days': opts['firstSeenAtSourceDays'],
        'first_seen_at_mc_days': opts['firstSeenAtMcDays'],
        'co2_emissions': opts['co2Emissions'],
        'insurance_group': opts['insuranceGroup'],
        'vehicle_registration_mark': opts['vehicleRegistrationMark'],
        'vehicle_registration_date_range': opts['vehicleRegistrationDateRange'],
        'num_owners': opts['numOwners'],
        'inventory_count_range': opts['inventoryCountRange'],
        'source': opts['source'],
        'dealer_id': opts['dealerId'],
        'exclude_sources': opts['excludeSources'],
        'exclude_dealer_ids': opts['excludeDealerIds'],
        'in_transit': opts['inTransit'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'cylinders': opts['cylinders'],
        'photo_links': opts['photoLinks'],
        'photo_links_cached': opts['photoLinksCached'],
        'base_exterior_color': opts['baseExteriorColor'],
        'base_interior_color': opts['baseInteriorColor'],
        'write_off_category': opts['writeOffCategory'],
        'exclude_write_off_category': opts['excludeWriteOffCategory'],
        'fca_status': opts['fcaStatus'],
        'seating_capacity': opts['seatingCapacity'],
        'vrm': opts['vrm'],
        'powertrain_type': opts['powertrainType'],
        'client_filters': opts['clientFilters'],
        'boost': opts['boost'],
        'car_location_seller_name': opts['carLocationSellerName'],
        'car_location_street': opts['carLocationStreet'],
        'car_location_city': opts['carLocationCity'],
        'car_location_county': opts['carLocationCounty'],
        'car_location_zip': opts['carLocationZip'],
        'car_location_latitude': opts['carLocationLatitude'],
        'car_location_longitude': opts['carLocationLongitude'],
        'price_change': opts['priceChange'],
        'price_change_range': opts['priceChangeRange'],
        'active_inventory_date_range': opts['activeInventoryDateRange'],
        'engine_size': opts['engineSize'],
        'engine_size_range': opts['engineSizeRange'],
        'uvc_id': opts['uvcId'],
        'highway_mpg_range': opts['highwayMpgRange'],
        'city_mpg_range': opts['cityMpgRange'],
        'combined_mpg_range': opts['combinedMpgRange'],
        'owned': opts['owned'],
        'min_photo_links': opts['minPhotoLinks'],
        'min_photo_links_cached': opts['minPhotoLinksCached'],
        'match': opts['match'],
        'ulez_compliant': opts['ulezCompliant']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = UKSearchResponse;
      return this.apiClient.callApi(
        '/search/car/uk/active', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchCarActiveGet operation.
     * @callback module:api/CarSearchApi~searchCarActiveGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gets active car listings for the given search criteria
     * This endpoint is the meat of the API and serves many purposes. This API produces a list of currently active cars from the market for the given search criteria. The API results are limited to allow pagination upto 10000 rows.   The search API facilitates the following use cases -  1. Search Cars around a given geo-point within a given radius  2. Search cars for a specific year / make / model or combination of these  3. Search cars matching multiple year, make, model combinatins in the same search request 4. Filter results by most car specification attributes 5. Search for similar cars by VIN or Taxonomy VIN  6. Filter cars within a given price / miles / days on market (dom) range 7. Specify a sort order for the results on price / miles / dom / listed date  8. Search cars for a given City / State combination  9. Get Facets to build the search drill downs  10. Get Market averages for price/miles/dom for your search
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Number} [latitude] Latitude component of location
     * @param {Number} [longitude] Longitude component of location
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {Boolean} [includeLease] Boolean param to search for listings that include leasing options in them
     * @param {Boolean} [includeFinance] Boolean param to search for listings that include finance options in them
     * @param {String} [leaseTerm] Search listings with exact lease term, or inside a range with min and max seperated by a dash like lease_term=30-60
     * @param {String} [leaseDownPayment] Search listings with exact down payment in lease offers, or inside a range with min and max seperated by a dash like lease_down_payment=30-60
     * @param {String} [leaseEmp] Search listings with lease offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like lease_emp=30-60
     * @param {String} [financeLoanTerm] Search listings with exact finance loan term, or inside a range with min and max seperated by a dash like finance_loan_term=30-60
     * @param {String} [financeLoanApr] Search listings with finance offers exactly matching loans Annual Percentage Rate, or inside a range with min and max seperated by a dash like finance_loan_apr=30-60
     * @param {String} [financeEmp] Search listings with finance offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like finance_emp=30-60
     * @param {String} [financeDownPayment] Search listings with exact down payment in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment=30-60
     * @param {String} [financeDownPaymentPer] Search listings with exact down payment percentage in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment_per=30-60
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used / certified
     * @param {module:model/String} [carfax1Owner] Indicates whether car has had only one owner or not
     * @param {module:model/String} [carfaxCleanTitle] Indicates whether car has clean ownership records
     * @param {String} [yearRange] Year range to filter listings with the year in the range given. Range to be given in the format - min-max e.g. 2019-2021
     * @param {String} [year] To filter listing on their year
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [vin] To filter listing on their VIN
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [bodySubtype] Body subtype to filter the listings on. Valid filter values are those that our Search facets API returns for unique body subtypes. You can pass in multiple body subtype values comma separated
     * @param {String} [vehicleType] To filter listing on their vehicle type
     * @param {String} [vins] Comma separated list of 17 digit vins to search the matching cars for. Only 10 VINs allowed per request. If the request contains more than 10 VINs the first 10 VINs will be considered. Could be used as a More Like This or Similar Vehicles search for the given VINs. Ths vins parameter is an alternative to taxonomy_vins or ymmt parameters available with the search API. vins and taxonomy_vins parameters could be used to filter our cars with the exact build represented by the vins or taxonomy_vins whereas ymmt is a top level filter that does not filter cars by the build attributes like doors, drivetrain, cylinders, body type, body subtype, vehicle type etc
     * @param {String} [taxonomyVins] Comma separated list of 10 letters excert from the 17 letter VIN. The 10 letters to be picked up from the 17 letter VIN are - first 8 letters and the 10th and 11th letter. E.g. For a VIN - 1FTFW1EF3EKE57182 the taxonomy vin would be - 1FTFW1EFEK  A taxonomy VIN identified a build of a car and could be used to filter our cars of a particular build. This is an alternative to the vin or ymmt parameters to the search API.
     * @param {String} [mm] Make-Model concatenated string. To help passing the results of auto-complete API on mm field, use this parameter and pass in the selected value as is
     * @param {String} [ymm] Year-Make-Model concatenated string. To help passing the results of auto-complete API on ymm field, use this parameter and pass in the selected value as is
     * @param {String} [ymmt] Comma separated list of Year, Make, Model, Trim combinations. Each combination needs to have the year,make,model, trim values separated by a pipe '|' character in the form year|make|model|trim. e.g. 2010|Audi|A5,2014|Nissan|Sentra|S 6MT,|Honda|City|   You could just provide strings of the form - 'year|make||' or 'year|make|model' or '|make|model|' combinations. Individual year / make / model filters provied with the API calls will take precedence over the Year, Make, Model, Trim combinations. The Make, Model, Trim values must be valid values as per the Marketcheck Vin Decoder. If you are using a separate vin decoder then look at using the 'vins' or 'taxonomy_vins' parameter to the search api instead the year|make|model|trim combinations.
     * @param {String} [match] Comma separated list of Year, Make, Model, Trim fields. For example - year,make,model,trim fields for which user wants to do an exact match
     * @param {String} [cylinders] To filter listing on their cylinders
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [doors] Doors to filter the cars on. Valid filter values are those that our Search facets API returns for unique doors. You can pass in multiple doors values comma separated
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [baseExteriorColor] Base exterior color to match. Valid filter values are those that our Search facets API returns for unique base exterior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [baseInteriorColor] Base interior color to match. Valid filter values are those that our Search facets API returns for unique base interior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineAspiration] Engine Aspiration to match. Valid filter values are those that our Search facets API returns for unique Engine Aspirations. You can pass in multiple Engine aspirations values comma separated
     * @param {String} [engineBlock] Engine Block to match. Valid filter values are those that our Search facets API returns for unique Engine Block. You can pass in multiple Engine Block values comma separated
     * @param {String} [highwayMpgRange] Highway mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [cityMpgRange] City mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [milesRange] Miles range to filter listings with miles in the given range. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [priceRange] Price range to filter listings with the price in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [msrpRange] MSRP range to filter listings with the msrp in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [domRange] Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [sortBy] Sort by field. Default sort field is distance from the given point
     * @param {module:model/String} [sortOrder] Sort order - asc or desc. Default sort order is asc
     * @param {Number} [rows = 10)] Number of results to return. Default is 10. Max is 50
     * @param {Number} [start = 0)] Page number to fetch the results for the given criteria. Default is 0. Pagination is allowed only till first 10000 results for the search and sort criteria. The page value can be only between 1 to 10000/rows
     * @param {Boolean} [includeNonVinListings = false)] To include non vin listings. Default is false
     * @param {String} [msaCode] To filter listing on msa code in which they are listed
     * @param {String} [facets] The comma separated list of fields for which facets are requested. Facets could be requested in addition to the listings for the search. Please note - The API calls with lots of facet fields may take longer to respond.
     * @param {String} [rangeFacets] The comma separated list of numeric fields for which range facets are requested. Range facets could be requested in addition to the listings for the search. Please note - The API calls with lots of range facet fields may take longer to respond.
     * @param {module:model/String} [facetSort = 'count')] Control sort order of facets with this parameter with default sort being on count, Other available sort is alphabetical sort, which can be obtained by using index as value for this param
     * @param {String} [stats] The list of fields for which stats need to be generated based on the matching listings for the search criteria. The stats consists of mean, max, average and count of listings based on which the stats are calculated for the field. Stats could be requested in addition to the listings for the search. Please note - The API calls with the stats fields may take longer to respond.
     * @param {module:model/String} [country = 'US')] To filter listing on Country in which they are listed
     * @param {Boolean} [plot] If plot has value true results in around 25k coordinates with limited fields to plot respective graph
     * @param {Boolean} [nodedup] If nodedup is set to true then API will give results without is_searchable i.e multiple listings for single vin
     * @param {Boolean} [dedup] If dedup is set to true then will give results with is_searchable irrespecive of dealer_id or source
     * @param {Boolean} [owned] Used in combination with dealer_id or source, when true returns the listings actually owned by dealer himself
     * @param {String} [source] To filter listing on their source only for widget requests
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [trimO] Filter listings on web scraped trim
     * @param {String} [trimR] Filter trim on custom possible matches
     * @param {String} [domActiveRange] Active Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dom180Range] Last 180 Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {Boolean} [excludeCertified] Boolean param to exclude certified cars from search results
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {module:model/String} [dealerType] Filter based on dealer type independant or franchise
     * @param {Boolean} [photoLinks] A boolean indicating whether to include only those listings that have photo_links in search results, And discard those that don't have them
     * @param {Boolean} [photoLinksCached] A boolean indicating whether to include only those listings that have photo_links_cached in search results, And discard those that don't have them
     * @param {String} [stockNo] To filter listing on their stock number on lot
     * @param {String} [lastSeenRange] Last seen date range to filter listings with the last seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenRange] First seen date range to filter listings with the first seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtSourceRange] First seen at source date range to filter listings with the first seen at source in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtMcRange] First seen at MC date range to filter listings with the first seen at MC in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [lastSeenDays] Last seen days range to filter listings with the last seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenDays] First seen days range to filter listings with the first seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtSourceDays] First seen at source days range to filter listings with the first seen at source in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtMcDays] First seen at MC days range to filter listings with the first seen at MC in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {String} [inventoryCountRange] Inventory count range to filter listings with count of total listings in dealers inventory. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {String} [excludeDealerIds] A list of dealer ids to exclude from result
     * @param {String} [excludeSources] A list of sources to exclude from result
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {String} [seatingCapacity] To filter on vehicle seating capacity
     * @param {String} [powertrainType] To filter on powertrain_type
     * @param {module:model/String} [priceChange] Query to filter listings based on their positive and negative price change
     * @param {String} [priceChangeRange] Price change range to filter listings with price change within given price_change_range. Range to be given in the format - min-max e.g. 10-500
     * @param {String} [activeInventoryDateRange] date range to filter listings that were active within given date range. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [engineSizeRange] Engine size range to filter listings with engine size in the given range. Range to be given in the format - min-max e.g. 1.0-2
     * @param {String} [highValueFeatures] To filter listings on their high_value_features. Results will be intersection of provided HVFs
     * @param {String} [minPhotoLinks] Filter listings based by number of photo links within given range
     * @param {String} [minPhotoLinksCached] Filter listings based by number of cached photo links within given range
     * @param {Boolean} [clientFilters = true)] Flag to add explicit filters set on client level in solr
     * @param {module:api/CarSearchApi~searchCarActiveGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchResponse}
     */
    searchCarActiveGet(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'radius': opts['radius'],
        'zip': opts['zip'],
        'include_lease': opts['includeLease'],
        'include_finance': opts['includeFinance'],
        'lease_term': opts['leaseTerm'],
        'lease_down_payment': opts['leaseDownPayment'],
        'lease_emp': opts['leaseEmp'],
        'finance_loan_term': opts['financeLoanTerm'],
        'finance_loan_apr': opts['financeLoanApr'],
        'finance_emp': opts['financeEmp'],
        'finance_down_payment': opts['financeDownPayment'],
        'finance_down_payment_per': opts['financeDownPaymentPer'],
        'car_type': opts['carType'],
        'carfax_1_owner': opts['carfax1Owner'],
        'carfax_clean_title': opts['carfaxCleanTitle'],
        'year_range': opts['yearRange'],
        'year': opts['year'],
        'make': opts['make'],
        'model': opts['model'],
        'trim': opts['trim'],
        'vin': opts['vin'],
        'body_type': opts['bodyType'],
        'body_subtype': opts['bodySubtype'],
        'vehicle_type': opts['vehicleType'],
        'vins': opts['vins'],
        'taxonomy_vins': opts['taxonomyVins'],
        'mm': opts['mm'],
        'ymm': opts['ymm'],
        'ymmt': opts['ymmt'],
        'match': opts['match'],
        'cylinders': opts['cylinders'],
        'transmission': opts['transmission'],
        'doors': opts['doors'],
        'drivetrain': opts['drivetrain'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'base_exterior_color': opts['baseExteriorColor'],
        'base_interior_color': opts['baseInteriorColor'],
        'engine': opts['engine'],
        'engine_size': opts['engineSize'],
        'engine_aspiration': opts['engineAspiration'],
        'engine_block': opts['engineBlock'],
        'highway_mpg_range': opts['highwayMpgRange'],
        'city_mpg_range': opts['cityMpgRange'],
        'miles_range': opts['milesRange'],
        'price_range': opts['priceRange'],
        'msrp_range': opts['msrpRange'],
        'dom_range': opts['domRange'],
        'sort_by': opts['sortBy'],
        'sort_order': opts['sortOrder'],
        'rows': opts['rows'],
        'start': opts['start'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'msa_code': opts['msaCode'],
        'facets': opts['facets'],
        'range_facets': opts['rangeFacets'],
        'facet_sort': opts['facetSort'],
        'stats': opts['stats'],
        'country': opts['country'],
        'plot': opts['plot'],
        'nodedup': opts['nodedup'],
        'dedup': opts['dedup'],
        'owned': opts['owned'],
        'source': opts['source'],
        'state': opts['state'],
        'city': opts['city'],
        'trim_o': opts['trimO'],
        'trim_r': opts['trimR'],
        'dom_active_range': opts['domActiveRange'],
        'dom_180_range': opts['dom180Range'],
        'exclude_certified': opts['excludeCertified'],
        'fuel_type': opts['fuelType'],
        'dealer_type': opts['dealerType'],
        'photo_links': opts['photoLinks'],
        'photo_links_cached': opts['photoLinksCached'],
        'stock_no': opts['stockNo'],
        'last_seen_range': opts['lastSeenRange'],
        'first_seen_range': opts['firstSeenRange'],
        'first_seen_at_source_range': opts['firstSeenAtSourceRange'],
        'first_seen_at_mc_range': opts['firstSeenAtMcRange'],
        'last_seen_days': opts['lastSeenDays'],
        'first_seen_days': opts['firstSeenDays'],
        'first_seen_at_source_days': opts['firstSeenAtSourceDays'],
        'first_seen_at_mc_days': opts['firstSeenAtMcDays'],
        'include_relevant_links': opts['includeRelevantLinks'],
        'inventory_count_range': opts['inventoryCountRange'],
        'dealer_id': opts['dealerId'],
        'exclude_dealer_ids': opts['excludeDealerIds'],
        'exclude_sources': opts['excludeSources'],
        'in_transit': opts['inTransit'],
        'seating_capacity': opts['seatingCapacity'],
        'powertrain_type': opts['powertrainType'],
        'price_change': opts['priceChange'],
        'price_change_range': opts['priceChangeRange'],
        'active_inventory_date_range': opts['activeInventoryDateRange'],
        'engine_size_range': opts['engineSizeRange'],
        'high_value_features': opts['highValueFeatures'],
        'min_photo_links': opts['minPhotoLinks'],
        'min_photo_links_cached': opts['minPhotoLinksCached'],
        'client_filters': opts['clientFilters']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = SearchResponse;
      return this.apiClient.callApi(
        '/search/car/active', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchCarAuctionActiveGet operation.
     * @callback module:api/CarSearchApi~searchCarAuctionActiveGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gets active auction car listings for the given search criteria
     * This API produces a list of currently active auction cars from the market for the given search criteria. The API results are limited to allow pagination upto 5000 rows.   The search API facilitates the following use cases -  1. Search Cars around a given geo-point within a given radius  2. Search cars for a specific year / make / model or combination of these  3. Search cars matching multiple year, make, model combinatins in the same search request 4. Filter results by most car specification attributes 5. Search for similar cars by VIN or Taxonomy VIN  6. Filter cars within a given price / miles / days on market (dom) range 7. Specify a sort order for the results on price / miles / dom / listed date  8. Search cars for a given City / State combination  9. Get Facets to build the search drill downs  10. Get Market averages for price/miles/dom for your search
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Number} [latitude] Latitude component of location
     * @param {Number} [longitude] Longitude component of location
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {Boolean} [includeLease] Boolean param to search for listings that include leasing options in them
     * @param {Boolean} [includeFinance] Boolean param to search for listings that include finance options in them
     * @param {String} [leaseTerm] Search listings with exact lease term, or inside a range with min and max seperated by a dash like lease_term=30-60
     * @param {String} [leaseDownPayment] Search listings with exact down payment in lease offers, or inside a range with min and max seperated by a dash like lease_down_payment=30-60
     * @param {String} [leaseEmp] Search listings with lease offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like lease_emp=30-60
     * @param {String} [financeLoanTerm] Search listings with exact finance loan term, or inside a range with min and max seperated by a dash like finance_loan_term=30-60
     * @param {String} [financeLoanApr] Search listings with finance offers exactly matching loans Annual Percentage Rate, or inside a range with min and max seperated by a dash like finance_loan_apr=30-60
     * @param {String} [financeEmp] Search listings with finance offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like finance_emp=30-60
     * @param {String} [financeDownPayment] Search listings with exact down payment in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment=30-60
     * @param {String} [financeDownPaymentPer] Search listings with exact down payment percentage in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment_per=30-60
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used / certified
     * @param {module:model/String} [carfax1Owner] Indicates whether car has had only one owner or not
     * @param {module:model/String} [carfaxCleanTitle] Indicates whether car has clean ownership records
     * @param {String} [yearRange] Year range to filter listings with the year in the range given. Range to be given in the format - min-max e.g. 2019-2021
     * @param {String} [year] To filter listing on their year
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [vin] To filter listing on their VIN
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [bodySubtype] Body subtype to filter the listings on. Valid filter values are those that our Search facets API returns for unique body subtypes. You can pass in multiple body subtype values comma separated
     * @param {String} [vehicleType] To filter listing on their vehicle type
     * @param {String} [vins] Comma separated list of 17 digit vins to search the matching cars for. Only 10 VINs allowed per request. If the request contains more than 10 VINs the first 10 VINs will be considered. Could be used as a More Like This or Similar Vehicles search for the given VINs. Ths vins parameter is an alternative to taxonomy_vins or ymmt parameters available with the search API. vins and taxonomy_vins parameters could be used to filter our cars with the exact build represented by the vins or taxonomy_vins whereas ymmt is a top level filter that does not filter cars by the build attributes like doors, drivetrain, cylinders, body type, body subtype, vehicle type etc
     * @param {String} [taxonomyVins] Comma separated list of 10 letters excert from the 17 letter VIN. The 10 letters to be picked up from the 17 letter VIN are - first 8 letters and the 10th and 11th letter. E.g. For a VIN - 1FTFW1EF3EKE57182 the taxonomy vin would be - 1FTFW1EFEK  A taxonomy VIN identified a build of a car and could be used to filter our cars of a particular build. This is an alternative to the vin or ymmt parameters to the search API.
     * @param {String} [mm] Make-Model concatenated string. To help passing the results of auto-complete API on mm field, use this parameter and pass in the selected value as is
     * @param {String} [ymm] Year-Make-Model concatenated string. To help passing the results of auto-complete API on ymm field, use this parameter and pass in the selected value as is
     * @param {String} [ymmt] Comma separated list of Year, Make, Model, Trim combinations. Each combination needs to have the year,make,model, trim values separated by a pipe '|' character in the form year|make|model|trim. e.g. 2010|Audi|A5,2014|Nissan|Sentra|S 6MT,|Honda|City|   You could just provide strings of the form - 'year|make||' or 'year|make|model' or '|make|model|' combinations. Individual year / make / model filters provied with the API calls will take precedence over the Year, Make, Model, Trim combinations. The Make, Model, Trim values must be valid values as per the Marketcheck Vin Decoder. If you are using a separate vin decoder then look at using the 'vins' or 'taxonomy_vins' parameter to the search api instead the year|make|model|trim combinations.
     * @param {String} [match] Comma separated list of Year, Make, Model, Trim fields. For example - year,make,model,trim fields for which user wants to do an exact match
     * @param {String} [cylinders] To filter listing on their cylinders
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [doors] Doors to filter the cars on. Valid filter values are those that our Search facets API returns for unique doors. You can pass in multiple doors values comma separated
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [baseExteriorColor] Base exterior color to match. Valid filter values are those that our Search facets API returns for unique base exterior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [baseInteriorColor] Base interior color to match. Valid filter values are those that our Search facets API returns for unique base interior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineAspiration] Engine Aspiration to match. Valid filter values are those that our Search facets API returns for unique Engine Aspirations. You can pass in multiple Engine aspirations values comma separated
     * @param {String} [engineBlock] Engine Block to match. Valid filter values are those that our Search facets API returns for unique Engine Block. You can pass in multiple Engine Block values comma separated
     * @param {String} [highwayMpgRange] Highway mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [cityMpgRange] City mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [milesRange] Miles range to filter listings with miles in the given range. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [priceRange] Price range to filter listings with the price in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [msrpRange] MSRP range to filter listings with the msrp in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [domRange] Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [sortBy] Sort by field. Default sort field is distance from the given point
     * @param {module:model/String} [sortOrder] Sort order - asc or desc. Default sort order is asc
     * @param {Number} [rows = 10)] Number of results to return. Default is 10. Max is 50
     * @param {Number} [start = 0)] Page number to fetch the results for the given criteria. Default is 0. Pagination is allowed only till first 10000 results for the search and sort criteria. The page value can be only between 1 to 10000/rows
     * @param {Boolean} [includeNonVinListings = false)] To include non vin listings. Default is false
     * @param {String} [msaCode] To filter listing on msa code in which they are listed
     * @param {String} [facets] The comma separated list of fields for which facets are requested. Facets could be requested in addition to the listings for the search. Please note - The API calls with lots of facet fields may take longer to respond.
     * @param {String} [rangeFacets] The comma separated list of numeric fields for which range facets are requested. Range facets could be requested in addition to the listings for the search. Please note - The API calls with lots of range facet fields may take longer to respond.
     * @param {module:model/String} [facetSort = 'count')] Control sort order of facets with this parameter with default sort being on count, Other available sort is alphabetical sort, which can be obtained by using index as value for this param
     * @param {String} [stats] The list of fields for which stats need to be generated based on the matching listings for the search criteria. The stats consists of mean, max, average and count of listings based on which the stats are calculated for the field. Stats could be requested in addition to the listings for the search. Please note - The API calls with the stats fields may take longer to respond.
     * @param {module:model/String} [country = 'US')] To filter listing on Country in which they are listed
     * @param {Boolean} [plot] If plot has value true results in around 25k coordinates with limited fields to plot respective graph
     * @param {Boolean} [nodedup] If nodedup is set to true then API will give results without is_searchable i.e multiple listings for single vin
     * @param {Boolean} [dedup] If dedup is set to true then will give results with is_searchable irrespecive of dealer_id or source
     * @param {Boolean} [owned] Used in combination with dealer_id or source, when true returns the listings actually owned by dealer himself
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [source] To filter listing on their source only for widget requests
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {String} [trimO] Filter listings on web scraped trim
     * @param {String} [trimR] Filter trim on custom possible matches
     * @param {String} [domActiveRange] Active Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dom180Range] Last 180 Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {Boolean} [excludeCertified] Boolean param to exclude certified cars from search results
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {module:model/String} [dealerType] Filter based on dealer type independant or franchise
     * @param {Boolean} [photoLinks] A boolean indicating whether to include only those listings that have photo_links in search results, And discard those that don't have them
     * @param {Boolean} [photoLinksCached] A boolean indicating whether to include only those listings that have photo_links_cached in search results, And discard those that don't have them
     * @param {String} [stockNo] To filter listing on their stock number on lot
     * @param {String} [lastSeenRange] Last seen date range to filter listings with the last seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenRange] First seen date range to filter listings with the first seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtSourceRange] First seen at source date range to filter listings with the first seen at source in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtMcRange] First seen at MC date range to filter listings with the first seen at MC in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [lastSeenDays] Last seen days range to filter listings with the last seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenDays] First seen days range to filter listings with the first seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtSourceDays] First seen at source days range to filter listings with the first seen at source in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtMcDays] First seen at MC days range to filter listings with the first seen at MC in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {String} [inventoryCountRange] Inventory count range to filter listings with count of total listings in dealers inventory. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [excludeDealerIds] A list of dealer ids to exclude from result
     * @param {String} [excludeSources] A list of sources to exclude from result
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {module:model/String} [titleType] To filter on title type
     * @param {String} [seatingCapacity] To filter on vehicle seating capacity
     * @param {String} [engineSizeRange] Engine size range to filter listings with engine size in the given range. Range to be given in the format - min-max e.g. 1.0-2
     * @param {String} [minPhotoLinks] Filter listings based by number of photo links within given range
     * @param {String} [minPhotoLinksCached] Filter listings based by number of cached photo links within given range
     * @param {module:api/CarSearchApi~searchCarAuctionActiveGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchResponse}
     */
    searchCarAuctionActiveGet(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'radius': opts['radius'],
        'zip': opts['zip'],
        'include_lease': opts['includeLease'],
        'include_finance': opts['includeFinance'],
        'lease_term': opts['leaseTerm'],
        'lease_down_payment': opts['leaseDownPayment'],
        'lease_emp': opts['leaseEmp'],
        'finance_loan_term': opts['financeLoanTerm'],
        'finance_loan_apr': opts['financeLoanApr'],
        'finance_emp': opts['financeEmp'],
        'finance_down_payment': opts['financeDownPayment'],
        'finance_down_payment_per': opts['financeDownPaymentPer'],
        'car_type': opts['carType'],
        'carfax_1_owner': opts['carfax1Owner'],
        'carfax_clean_title': opts['carfaxCleanTitle'],
        'year_range': opts['yearRange'],
        'year': opts['year'],
        'make': opts['make'],
        'model': opts['model'],
        'trim': opts['trim'],
        'vin': opts['vin'],
        'body_type': opts['bodyType'],
        'body_subtype': opts['bodySubtype'],
        'vehicle_type': opts['vehicleType'],
        'vins': opts['vins'],
        'taxonomy_vins': opts['taxonomyVins'],
        'mm': opts['mm'],
        'ymm': opts['ymm'],
        'ymmt': opts['ymmt'],
        'match': opts['match'],
        'cylinders': opts['cylinders'],
        'transmission': opts['transmission'],
        'doors': opts['doors'],
        'drivetrain': opts['drivetrain'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'base_exterior_color': opts['baseExteriorColor'],
        'base_interior_color': opts['baseInteriorColor'],
        'engine': opts['engine'],
        'engine_size': opts['engineSize'],
        'engine_aspiration': opts['engineAspiration'],
        'engine_block': opts['engineBlock'],
        'highway_mpg_range': opts['highwayMpgRange'],
        'city_mpg_range': opts['cityMpgRange'],
        'miles_range': opts['milesRange'],
        'price_range': opts['priceRange'],
        'msrp_range': opts['msrpRange'],
        'dom_range': opts['domRange'],
        'sort_by': opts['sortBy'],
        'sort_order': opts['sortOrder'],
        'rows': opts['rows'],
        'start': opts['start'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'msa_code': opts['msaCode'],
        'facets': opts['facets'],
        'range_facets': opts['rangeFacets'],
        'facet_sort': opts['facetSort'],
        'stats': opts['stats'],
        'country': opts['country'],
        'plot': opts['plot'],
        'nodedup': opts['nodedup'],
        'dedup': opts['dedup'],
        'owned': opts['owned'],
        'state': opts['state'],
        'city': opts['city'],
        'source': opts['source'],
        'dealer_id': opts['dealerId'],
        'trim_o': opts['trimO'],
        'trim_r': opts['trimR'],
        'dom_active_range': opts['domActiveRange'],
        'dom_180_range': opts['dom180Range'],
        'exclude_certified': opts['excludeCertified'],
        'fuel_type': opts['fuelType'],
        'dealer_type': opts['dealerType'],
        'photo_links': opts['photoLinks'],
        'photo_links_cached': opts['photoLinksCached'],
        'stock_no': opts['stockNo'],
        'last_seen_range': opts['lastSeenRange'],
        'first_seen_range': opts['firstSeenRange'],
        'first_seen_at_source_range': opts['firstSeenAtSourceRange'],
        'first_seen_at_mc_range': opts['firstSeenAtMcRange'],
        'last_seen_days': opts['lastSeenDays'],
        'first_seen_days': opts['firstSeenDays'],
        'first_seen_at_source_days': opts['firstSeenAtSourceDays'],
        'first_seen_at_mc_days': opts['firstSeenAtMcDays'],
        'include_relevant_links': opts['includeRelevantLinks'],
        'inventory_count_range': opts['inventoryCountRange'],
        'exclude_dealer_ids': opts['excludeDealerIds'],
        'exclude_sources': opts['excludeSources'],
        'in_transit': opts['inTransit'],
        'title_type': opts['titleType'],
        'seating_capacity': opts['seatingCapacity'],
        'engine_size_range': opts['engineSizeRange'],
        'min_photo_links': opts['minPhotoLinks'],
        'min_photo_links_cached': opts['minPhotoLinksCached']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = SearchResponse;
      return this.apiClient.callApi(
        '/search/car/auction/active', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchCarFsboActiveGet operation.
     * @callback module:api/CarSearchApi~searchCarFsboActiveGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gets active private party car listings for the given search criteria
     * This API produces a list of currently active FSBO cars from the market for the given search criteria. The API results are limited to allow pagination upto 5000 rows.   The search API facilitates the following use cases -  1. Search Cars around a given geo-point within a given radius  2. Search cars for a specific year / make / model or combination of these  3. Search cars matching multiple year, make, model combinatins in the same search request 4. Filter results by most car specification attributes 5. Search for similar cars by VIN or Taxonomy VIN  6. Filter cars within a given price / miles / days on market (dom) range 7. Specify a sort order for the results on price / miles / dom / listed date  8. Search cars for a given City / State combination  9. Get Facets to build the search drill downs  10. Get Market averages for price/miles/dom for your search
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Number} [latitude] Latitude component of location
     * @param {Number} [longitude] Longitude component of location
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {Boolean} [includeLease] Boolean param to search for listings that include leasing options in them
     * @param {Boolean} [includeFinance] Boolean param to search for listings that include finance options in them
     * @param {String} [leaseTerm] Search listings with exact lease term, or inside a range with min and max seperated by a dash like lease_term=30-60
     * @param {String} [leaseDownPayment] Search listings with exact down payment in lease offers, or inside a range with min and max seperated by a dash like lease_down_payment=30-60
     * @param {String} [leaseEmp] Search listings with lease offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like lease_emp=30-60
     * @param {String} [financeLoanTerm] Search listings with exact finance loan term, or inside a range with min and max seperated by a dash like finance_loan_term=30-60
     * @param {String} [financeLoanApr] Search listings with finance offers exactly matching loans Annual Percentage Rate, or inside a range with min and max seperated by a dash like finance_loan_apr=30-60
     * @param {String} [financeEmp] Search listings with finance offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like finance_emp=30-60
     * @param {String} [financeDownPayment] Search listings with exact down payment in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment=30-60
     * @param {String} [financeDownPaymentPer] Search listings with exact down payment percentage in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment_per=30-60
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used / certified
     * @param {module:model/String} [carfax1Owner] Indicates whether car has had only one owner or not
     * @param {module:model/String} [carfaxCleanTitle] Indicates whether car has clean ownership records
     * @param {String} [yearRange] Year range to filter listings with the year in the range given. Range to be given in the format - min-max e.g. 2019-2021
     * @param {String} [year] To filter listing on their year
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [vin] To filter listing on their VIN
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [bodySubtype] Body subtype to filter the listings on. Valid filter values are those that our Search facets API returns for unique body subtypes. You can pass in multiple body subtype values comma separated
     * @param {String} [vehicleType] To filter listing on their vehicle type
     * @param {String} [vins] Comma separated list of 17 digit vins to search the matching cars for. Only 10 VINs allowed per request. If the request contains more than 10 VINs the first 10 VINs will be considered. Could be used as a More Like This or Similar Vehicles search for the given VINs. Ths vins parameter is an alternative to taxonomy_vins or ymmt parameters available with the search API. vins and taxonomy_vins parameters could be used to filter our cars with the exact build represented by the vins or taxonomy_vins whereas ymmt is a top level filter that does not filter cars by the build attributes like doors, drivetrain, cylinders, body type, body subtype, vehicle type etc
     * @param {String} [taxonomyVins] Comma separated list of 10 letters excert from the 17 letter VIN. The 10 letters to be picked up from the 17 letter VIN are - first 8 letters and the 10th and 11th letter. E.g. For a VIN - 1FTFW1EF3EKE57182 the taxonomy vin would be - 1FTFW1EFEK  A taxonomy VIN identified a build of a car and could be used to filter our cars of a particular build. This is an alternative to the vin or ymmt parameters to the search API.
     * @param {String} [mm] Make-Model concatenated string. To help passing the results of auto-complete API on mm field, use this parameter and pass in the selected value as is
     * @param {String} [ymm] Year-Make-Model concatenated string. To help passing the results of auto-complete API on ymm field, use this parameter and pass in the selected value as is
     * @param {String} [ymmt] Comma separated list of Year, Make, Model, Trim combinations. Each combination needs to have the year,make,model, trim values separated by a pipe '|' character in the form year|make|model|trim. e.g. 2010|Audi|A5,2014|Nissan|Sentra|S 6MT,|Honda|City|   You could just provide strings of the form - 'year|make||' or 'year|make|model' or '|make|model|' combinations. Individual year / make / model filters provied with the API calls will take precedence over the Year, Make, Model, Trim combinations. The Make, Model, Trim values must be valid values as per the Marketcheck Vin Decoder. If you are using a separate vin decoder then look at using the 'vins' or 'taxonomy_vins' parameter to the search api instead the year|make|model|trim combinations.
     * @param {String} [match] Comma separated list of Year, Make, Model, Trim fields. For example - year,make,model,trim fields for which user wants to do an exact match
     * @param {String} [cylinders] To filter listing on their cylinders
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [doors] Doors to filter the cars on. Valid filter values are those that our Search facets API returns for unique doors. You can pass in multiple doors values comma separated
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [baseExteriorColor] Base exterior color to match. Valid filter values are those that our Search facets API returns for unique base exterior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [baseInteriorColor] Base interior color to match. Valid filter values are those that our Search facets API returns for unique base interior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineAspiration] Engine Aspiration to match. Valid filter values are those that our Search facets API returns for unique Engine Aspirations. You can pass in multiple Engine aspirations values comma separated
     * @param {String} [engineBlock] Engine Block to match. Valid filter values are those that our Search facets API returns for unique Engine Block. You can pass in multiple Engine Block values comma separated
     * @param {String} [highwayMpgRange] Highway mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [cityMpgRange] City mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [milesRange] Miles range to filter listings with miles in the given range. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [priceRange] Price range to filter listings with the price in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [msrpRange] MSRP range to filter listings with the msrp in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [domRange] Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [sortBy] Sort by field. Default sort field is distance from the given point
     * @param {module:model/String} [sortOrder] Sort order - asc or desc. Default sort order is asc
     * @param {Number} [rows = 10)] Number of results to return. Default is 10. Max is 50
     * @param {Number} [start = 0)] Page number to fetch the results for the given criteria. Default is 0. Pagination is allowed only till first 10000 results for the search and sort criteria. The page value can be only between 1 to 10000/rows
     * @param {Boolean} [includeNonVinListings = false)] To include non vin listings. Default is false
     * @param {String} [msaCode] To filter listing on msa code in which they are listed
     * @param {String} [facets] The comma separated list of fields for which facets are requested. Facets could be requested in addition to the listings for the search. Please note - The API calls with lots of facet fields may take longer to respond.
     * @param {String} [rangeFacets] The comma separated list of numeric fields for which range facets are requested. Range facets could be requested in addition to the listings for the search. Please note - The API calls with lots of range facet fields may take longer to respond.
     * @param {module:model/String} [facetSort = 'count')] Control sort order of facets with this parameter with default sort being on count, Other available sort is alphabetical sort, which can be obtained by using index as value for this param
     * @param {String} [stats] The list of fields for which stats need to be generated based on the matching listings for the search criteria. The stats consists of mean, max, average and count of listings based on which the stats are calculated for the field. Stats could be requested in addition to the listings for the search. Please note - The API calls with the stats fields may take longer to respond.
     * @param {module:model/String} [country = 'US')] To filter listing on Country in which they are listed
     * @param {Boolean} [plot] If plot has value true results in around 25k coordinates with limited fields to plot respective graph
     * @param {Boolean} [nodedup] If nodedup is set to true then API will give results without is_searchable i.e multiple listings for single vin
     * @param {Boolean} [dedup] If dedup is set to true then will give results with is_searchable irrespecive of dealer_id or source
     * @param {Boolean} [owned] Used in combination with dealer_id or source, when true returns the listings actually owned by dealer himself
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [source] To filter listing on their source only for widget requests
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {String} [trimO] Filter listings on web scraped trim
     * @param {String} [trimR] Filter trim on custom possible matches
     * @param {String} [domActiveRange] Active Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dom180Range] Last 180 Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {Boolean} [excludeCertified] Boolean param to exclude certified cars from search results
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {module:model/String} [dealerType] Filter based on dealer type independant or franchise
     * @param {Boolean} [photoLinks] A boolean indicating whether to include only those listings that have photo_links in search results, And discard those that don't have them
     * @param {Boolean} [photoLinksCached] A boolean indicating whether to include only those listings that have photo_links_cached in search results, And discard those that don't have them
     * @param {String} [stockNo] To filter listing on their stock number on lot
     * @param {String} [lastSeenRange] Last seen date range to filter listings with the last seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenRange] First seen date range to filter listings with the first seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtSourceRange] First seen at source date range to filter listings with the first seen at source in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtMcRange] First seen at MC date range to filter listings with the first seen at MC in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [lastSeenDays] Last seen days range to filter listings with the last seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenDays] First seen days range to filter listings with the first seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtSourceDays] First seen at source days range to filter listings with the first seen at source in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtMcDays] First seen at MC days range to filter listings with the first seen at MC in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {String} [inventoryCountRange] Inventory count range to filter listings with count of total listings in dealers inventory. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [excludeDealerIds] A list of dealer ids to exclude from result
     * @param {String} [excludeSources] A list of sources to exclude from result
     * @param {Boolean} [excludeDealerListings] A list of fsbo listings to exclude from result
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {String} [seatingCapacity] To filter on vehicle seating capacity
     * @param {String} [engineSizeRange] Engine size range to filter listings with engine size in the given range. Range to be given in the format - min-max e.g. 1.0-2
     * @param {String} [minPhotoLinks] Filter listings based by number of photo links within given range
     * @param {String} [minPhotoLinksCached] Filter listings based by number of cached photo links within given range
     * @param {module:api/CarSearchApi~searchCarFsboActiveGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchResponse}
     */
    searchCarFsboActiveGet(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'radius': opts['radius'],
        'zip': opts['zip'],
        'include_lease': opts['includeLease'],
        'include_finance': opts['includeFinance'],
        'lease_term': opts['leaseTerm'],
        'lease_down_payment': opts['leaseDownPayment'],
        'lease_emp': opts['leaseEmp'],
        'finance_loan_term': opts['financeLoanTerm'],
        'finance_loan_apr': opts['financeLoanApr'],
        'finance_emp': opts['financeEmp'],
        'finance_down_payment': opts['financeDownPayment'],
        'finance_down_payment_per': opts['financeDownPaymentPer'],
        'car_type': opts['carType'],
        'carfax_1_owner': opts['carfax1Owner'],
        'carfax_clean_title': opts['carfaxCleanTitle'],
        'year_range': opts['yearRange'],
        'year': opts['year'],
        'make': opts['make'],
        'model': opts['model'],
        'trim': opts['trim'],
        'vin': opts['vin'],
        'body_type': opts['bodyType'],
        'body_subtype': opts['bodySubtype'],
        'vehicle_type': opts['vehicleType'],
        'vins': opts['vins'],
        'taxonomy_vins': opts['taxonomyVins'],
        'mm': opts['mm'],
        'ymm': opts['ymm'],
        'ymmt': opts['ymmt'],
        'match': opts['match'],
        'cylinders': opts['cylinders'],
        'transmission': opts['transmission'],
        'doors': opts['doors'],
        'drivetrain': opts['drivetrain'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'base_exterior_color': opts['baseExteriorColor'],
        'base_interior_color': opts['baseInteriorColor'],
        'engine': opts['engine'],
        'engine_size': opts['engineSize'],
        'engine_aspiration': opts['engineAspiration'],
        'engine_block': opts['engineBlock'],
        'highway_mpg_range': opts['highwayMpgRange'],
        'city_mpg_range': opts['cityMpgRange'],
        'miles_range': opts['milesRange'],
        'price_range': opts['priceRange'],
        'msrp_range': opts['msrpRange'],
        'dom_range': opts['domRange'],
        'sort_by': opts['sortBy'],
        'sort_order': opts['sortOrder'],
        'rows': opts['rows'],
        'start': opts['start'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'msa_code': opts['msaCode'],
        'facets': opts['facets'],
        'range_facets': opts['rangeFacets'],
        'facet_sort': opts['facetSort'],
        'stats': opts['stats'],
        'country': opts['country'],
        'plot': opts['plot'],
        'nodedup': opts['nodedup'],
        'dedup': opts['dedup'],
        'owned': opts['owned'],
        'state': opts['state'],
        'city': opts['city'],
        'source': opts['source'],
        'dealer_id': opts['dealerId'],
        'trim_o': opts['trimO'],
        'trim_r': opts['trimR'],
        'dom_active_range': opts['domActiveRange'],
        'dom_180_range': opts['dom180Range'],
        'exclude_certified': opts['excludeCertified'],
        'fuel_type': opts['fuelType'],
        'dealer_type': opts['dealerType'],
        'photo_links': opts['photoLinks'],
        'photo_links_cached': opts['photoLinksCached'],
        'stock_no': opts['stockNo'],
        'last_seen_range': opts['lastSeenRange'],
        'first_seen_range': opts['firstSeenRange'],
        'first_seen_at_source_range': opts['firstSeenAtSourceRange'],
        'first_seen_at_mc_range': opts['firstSeenAtMcRange'],
        'last_seen_days': opts['lastSeenDays'],
        'first_seen_days': opts['firstSeenDays'],
        'first_seen_at_source_days': opts['firstSeenAtSourceDays'],
        'first_seen_at_mc_days': opts['firstSeenAtMcDays'],
        'include_relevant_links': opts['includeRelevantLinks'],
        'inventory_count_range': opts['inventoryCountRange'],
        'exclude_dealer_ids': opts['excludeDealerIds'],
        'exclude_sources': opts['excludeSources'],
        'exclude_dealer_listings': opts['excludeDealerListings'],
        'in_transit': opts['inTransit'],
        'seating_capacity': opts['seatingCapacity'],
        'engine_size_range': opts['engineSizeRange'],
        'min_photo_links': opts['minPhotoLinks'],
        'min_photo_links_cached': opts['minPhotoLinksCached']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = SearchResponse;
      return this.apiClient.callApi(
        '/search/car/fsbo/active', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchCarRecentsGet operation.
     * @callback module:api/CarSearchApi~searchCarRecentsGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gets Recent car listings for the given search criteria
     * This API produces a list of recently active (past 90 days) cars from the market for the given search criteria
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Number} [latitude] Latitude component of location
     * @param {Number} [longitude] Longitude component of location
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {Boolean} [includeLease] Boolean param to search for listings that include leasing options in them
     * @param {Boolean} [includeFinance] Boolean param to search for listings that include finance options in them
     * @param {String} [leaseTerm] Search listings with exact lease term, or inside a range with min and max seperated by a dash like lease_term=30-60
     * @param {String} [leaseDownPayment] Search listings with exact down payment in lease offers, or inside a range with min and max seperated by a dash like lease_down_payment=30-60
     * @param {String} [leaseEmp] Search listings with lease offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like lease_emp=30-60
     * @param {String} [financeLoanTerm] Search listings with exact finance loan term, or inside a range with min and max seperated by a dash like finance_loan_term=30-60
     * @param {String} [financeLoanApr] Search listings with finance offers exactly matching loans Annual Percentage Rate, or inside a range with min and max seperated by a dash like finance_loan_apr=30-60
     * @param {String} [financeEmp] Search listings with finance offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like finance_emp=30-60
     * @param {String} [financeDownPayment] Search listings with exact down payment in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment=30-60
     * @param {String} [financeDownPaymentPer] Search listings with exact down payment percentage in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment_per=30-60
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used / certified
     * @param {module:model/String} [carfax1Owner] Indicates whether car has had only one owner or not
     * @param {module:model/String} [carfaxCleanTitle] Indicates whether car has clean ownership records
     * @param {String} [yearRange] Year range to filter listings with the year in the range given. Range to be given in the format - min-max e.g. 2019-2021
     * @param {String} [year] To filter listing on their year
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {String} [vin] To filter listing on their VIN
     * @param {String} [source] To filter listing on their source
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [bodySubtype] Body subtype to filter the listings on. Valid filter values are those that our Search facets API returns for unique body subtypes. You can pass in multiple body subtype values comma separated
     * @param {String} [vehicleType] To filter listing on their vehicle type
     * @param {String} [vins] Comma separated list of 17 digit vins to search the matching cars for. Only 10 VINs allowed per request. If the request contains more than 10 VINs the first 10 VINs will be considered. Could be used as a More Like This or Similar Vehicles search for the given VINs. Ths vins parameter is an alternative to taxonomy_vins or ymmt parameters available with the search API. vins and taxonomy_vins parameters could be used to filter our cars with the exact build represented by the vins or taxonomy_vins whereas ymmt is a top level filter that does not filter cars by the build attributes like doors, drivetrain, cylinders, body type, body subtype, vehicle type etc
     * @param {String} [taxonomyVins] Comma separated list of 10 letters excert from the 17 letter VIN. The 10 letters to be picked up from the 17 letter VIN are - first 8 letters and the 10th and 11th letter. E.g. For a VIN - 1FTFW1EF3EKE57182 the taxonomy vin would be - 1FTFW1EFEK  A taxonomy VIN identified a build of a car and could be used to filter our cars of a particular build. This is an alternative to the vin or ymmt parameters to the search API.
     * @param {String} [ymmt] Comma separated list of Year, Make, Model, Trim combinations. Each combination needs to have the year,make,model, trim values separated by a pipe '|' character in the form year|make|model|trim. e.g. 2010|Audi|A5,2014|Nissan|Sentra|S 6MT,|Honda|City|   You could just provide strings of the form - 'year|make||' or 'year|make|model' or '|make|model|' combinations. Individual year / make / model filters provied with the API calls will take precedence over the Year, Make, Model, Trim combinations. The Make, Model, Trim values must be valid values as per the Marketcheck Vin Decoder. If you are using a separate vin decoder then look at using the 'vins' or 'taxonomy_vins' parameter to the search api instead the year|make|model|trim combinations.
     * @param {String} [match] Comma separated list of Year, Make, Model, Trim fields. For example - year,make,model,trim fields for which user wants to do an exact match
     * @param {String} [cylinders] To filter listing on their cylinders
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [doors] Doors to filter the cars on. Valid filter values are those that our Search facets API returns for unique doors. You can pass in multiple doors values comma separated
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [baseExteriorColor] Base exterior color to match. Valid filter values are those that our Search facets API returns for unique base exterior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [baseInteriorColor] Base interior color to match. Valid filter values are those that our Search facets API returns for unique base interior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineAspiration] Engine Aspiration to match. Valid filter values are those that our Search facets API returns for unique Engine Aspirations. You can pass in multiple Engine aspirations values comma separated
     * @param {String} [engineBlock] Engine Block to match. Valid filter values are those that our Search facets API returns for unique Engine Block. You can pass in multiple Engine Block values comma separated
     * @param {String} [highwayMpgRange] Highway mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [cityMpgRange] City mileage range to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [milesRange] Miles range to filter listings with miles in the given range. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [priceRange] Price range to filter listings with the price in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [msrpRange] MSRP range to filter listings with the msrp in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [domRange] Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [lastSeenRange] Last seen date range to filter listings with the last seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenRange] First seen date range to filter listings with the first seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtSourceRange] First seen at source date range to filter listings with the first seen at source in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtMcRange] First seen at MC date range to filter listings with the first seen at MC in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [lastSeenDays] Last seen days range to filter listings with the last seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenDays] First seen days range to filter listings with the first seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtSourceDays] First seen at source days range to filter listings with the first seen at source in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtMcDays] First seen at MC days range to filter listings with the first seen at MC in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [sortBy] Sort by field. Default sort field is distance from the given point
     * @param {module:model/String} [sortOrder] Sort order - asc or desc. Default sort order is asc
     * @param {Number} [rows = 10)] Number of results to return. Default is 10. Max is 50
     * @param {Number} [start = 0)] Page number to fetch the results for the given criteria. Default is 0. Pagination is allowed only till first 10000 results for the search and sort criteria. The page value can be only between 1 to 10000/rows
     * @param {Boolean} [includeNonVinListings = false)] To include non vin listings. Default is false
     * @param {String} [facets] The comma separated list of fields for which facets are requested. Facets could be requested in addition to the listings for the search. Please note - The API calls with lots of facet fields may take longer to respond.
     * @param {String} [rangeFacets] The comma separated list of numeric fields for which range facets are requested. Range facets could be requested in addition to the listings for the search. Please note - The API calls with lots of range facet fields may take longer to respond.
     * @param {module:model/String} [facetSort = 'count')] Control sort order of facets with this parameter with default sort being on count, Other available sort is alphabetical sort, which can be obtained by using index as value for this param
     * @param {String} [stats] The list of fields for which stats need to be generated based on the matching listings for the search criteria. The stats consists of mean, max, average and count of listings based on which the stats are calculated for the field. Stats could be requested in addition to the listings for the search. Please note - The API calls with the stats fields may take longer to respond.
     * @param {module:model/String} [country = 'US')] To filter listing on Country in which they are listed
     * @param {Boolean} [plot] If plot has value true results in around 25k coordinates with limited fields to plot respective graph
     * @param {Boolean} [nodedup] If nodedup is set to true then API will give results without is_searchable i.e multiple listings for single vin
     * @param {Boolean} [dedup] If dedup is set to true then will give results with is_searchable irrespecive of dealer_id or source
     * @param {Boolean} [owned] Used in combination with dealer_id or source, when true returns the listings actually owned by dealer himself
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [msaCode] To filter listing on msa code in which they are listed
     * @param {String} [dealerName] Filter listings on dealer_name
     * @param {String} [dealershipGroupName] Name of the dealership group to search for
     * @param {String} [trimO] Filter listings on web scraped trim
     * @param {String} [trimR] Filter trim on custom possible matches
     * @param {String} [domActiveRange] Active Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dom180Range] Last 180 Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {Boolean} [excludeCertified] Boolean param to exclude certified cars from search results
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {module:model/String} [dealerType] Filter based on dealer type independant or franchise
     * @param {Boolean} [photoLinks] A boolean indicating whether to include only those listings that have photo_links in search results, And discard those that don't have them
     * @param {Boolean} [photoLinksCached] A boolean indicating whether to include only those listings that have photo_links_cached in search results, And discard those that don't have them
     * @param {String} [stockNo] To filter listing on their stock number on lot
     * @param {Boolean} [sold] sold parameter to fetch only sold listings
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {module:model/String} [expired] Boolean falg to either fetch only the expired listings or active ones
     * @param {String} [excludeDealerIds] A list of dealer ids to exclude from result
     * @param {String} [excludeSources] A list of sources to exclude from result
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {String} [seatingCapacity] To filter on vehicle seating capacity
     * @param {String} [activeInventoryDateRange] date range to filter listings that were active within given date range. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [engineSizeRange] Engine size range to filter listings with engine size in the given range. Range to be given in the format - min-max e.g. 1.0-2
     * @param {String} [priceChangeRange] Price change range to filter listings with price change within given price_change_range. Range to be given in the format - min-max e.g. 10-500
     * @param {module:api/CarSearchApi~searchCarRecentsGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchResponse}
     */
    searchCarRecentsGet(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'radius': opts['radius'],
        'zip': opts['zip'],
        'include_lease': opts['includeLease'],
        'include_finance': opts['includeFinance'],
        'lease_term': opts['leaseTerm'],
        'lease_down_payment': opts['leaseDownPayment'],
        'lease_emp': opts['leaseEmp'],
        'finance_loan_term': opts['financeLoanTerm'],
        'finance_loan_apr': opts['financeLoanApr'],
        'finance_emp': opts['financeEmp'],
        'finance_down_payment': opts['financeDownPayment'],
        'finance_down_payment_per': opts['financeDownPaymentPer'],
        'car_type': opts['carType'],
        'carfax_1_owner': opts['carfax1Owner'],
        'carfax_clean_title': opts['carfaxCleanTitle'],
        'year_range': opts['yearRange'],
        'year': opts['year'],
        'make': opts['make'],
        'model': opts['model'],
        'trim': opts['trim'],
        'dealer_id': opts['dealerId'],
        'vin': opts['vin'],
        'source': opts['source'],
        'body_type': opts['bodyType'],
        'body_subtype': opts['bodySubtype'],
        'vehicle_type': opts['vehicleType'],
        'vins': opts['vins'],
        'taxonomy_vins': opts['taxonomyVins'],
        'ymmt': opts['ymmt'],
        'match': opts['match'],
        'cylinders': opts['cylinders'],
        'transmission': opts['transmission'],
        'doors': opts['doors'],
        'drivetrain': opts['drivetrain'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'base_exterior_color': opts['baseExteriorColor'],
        'base_interior_color': opts['baseInteriorColor'],
        'engine': opts['engine'],
        'engine_size': opts['engineSize'],
        'engine_aspiration': opts['engineAspiration'],
        'engine_block': opts['engineBlock'],
        'highway_mpg_range': opts['highwayMpgRange'],
        'city_mpg_range': opts['cityMpgRange'],
        'miles_range': opts['milesRange'],
        'price_range': opts['priceRange'],
        'msrp_range': opts['msrpRange'],
        'dom_range': opts['domRange'],
        'last_seen_range': opts['lastSeenRange'],
        'first_seen_range': opts['firstSeenRange'],
        'first_seen_at_source_range': opts['firstSeenAtSourceRange'],
        'first_seen_at_mc_range': opts['firstSeenAtMcRange'],
        'last_seen_days': opts['lastSeenDays'],
        'first_seen_days': opts['firstSeenDays'],
        'first_seen_at_source_days': opts['firstSeenAtSourceDays'],
        'first_seen_at_mc_days': opts['firstSeenAtMcDays'],
        'sort_by': opts['sortBy'],
        'sort_order': opts['sortOrder'],
        'rows': opts['rows'],
        'start': opts['start'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'facets': opts['facets'],
        'range_facets': opts['rangeFacets'],
        'facet_sort': opts['facetSort'],
        'stats': opts['stats'],
        'country': opts['country'],
        'plot': opts['plot'],
        'nodedup': opts['nodedup'],
        'dedup': opts['dedup'],
        'owned': opts['owned'],
        'state': opts['state'],
        'city': opts['city'],
        'msa_code': opts['msaCode'],
        'dealer_name': opts['dealerName'],
        'dealership_group_name': opts['dealershipGroupName'],
        'trim_o': opts['trimO'],
        'trim_r': opts['trimR'],
        'dom_active_range': opts['domActiveRange'],
        'dom_180_range': opts['dom180Range'],
        'exclude_certified': opts['excludeCertified'],
        'fuel_type': opts['fuelType'],
        'dealer_type': opts['dealerType'],
        'photo_links': opts['photoLinks'],
        'photo_links_cached': opts['photoLinksCached'],
        'stock_no': opts['stockNo'],
        'sold': opts['sold'],
        'include_relevant_links': opts['includeRelevantLinks'],
        'expired': opts['expired'],
        'exclude_dealer_ids': opts['excludeDealerIds'],
        'exclude_sources': opts['excludeSources'],
        'in_transit': opts['inTransit'],
        'seating_capacity': opts['seatingCapacity'],
        'active_inventory_date_range': opts['activeInventoryDateRange'],
        'engine_size_range': opts['engineSizeRange'],
        'price_change_range': opts['priceChangeRange']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = SearchResponse;
      return this.apiClient.callApi(
        '/search/car/recents', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the searchCarUkRecentsGet operation.
     * @callback module:api/CarSearchApi~searchCarUkRecentsGetCallback
     * @param {String} error Error message, if any.
     * @param {module:model/UKSearchResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Gets Recent UK car listings for the given search criteria
     * This API produces a list of recently active (past 90 days) cars from the market for the given search criteria
     * @param {Object} opts Optional parameters
     * @param {String} [apiKey] The API Authentication Key. Mandatory with all API calls.
     * @param {Boolean} [appendApiKey = true)] Flag on whether to include api_key in response API urls (if any)
     * @param {Number} [latitude] Latitude component of location
     * @param {Number} [longitude] Longitude component of location
     * @param {Number} [radius] Radius around the search location (Unit - Miles)
     * @param {String} [zip] To filter listing on ZIP around which they are listed
     * @param {Boolean} [includeLease] Boolean param to search for listings that include leasing options in them
     * @param {Boolean} [includeFinance] Boolean param to search for listings that include finance options in them
     * @param {String} [leaseTerm] Search listings with exact lease term, or inside a range with min and max seperated by a dash like lease_term=30-60
     * @param {String} [leaseDownPayment] Search listings with exact down payment in lease offers, or inside a range with min and max seperated by a dash like lease_down_payment=30-60
     * @param {String} [leaseEmp] Search listings with lease offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like lease_emp=30-60
     * @param {String} [financeLoanTerm] Search listings with exact finance loan term, or inside a range with min and max seperated by a dash like finance_loan_term=30-60
     * @param {String} [financeLoanApr] Search listings with finance offers exactly matching loans Annual Percentage Rate, or inside a range with min and max seperated by a dash like finance_loan_apr=30-60
     * @param {String} [financeEmp] Search listings with finance offers exactly matching Estimated Monthly Payment(EMI), or inside a range with min and max seperated by a dash like finance_emp=30-60
     * @param {String} [financeDownPayment] Search listings with exact down payment in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment=30-60
     * @param {String} [financeDownPaymentPer] Search listings with exact down payment percentage in finance offers, or inside a range with min and max seperated by a dash like finance_down_payment_per=30-60
     * @param {module:model/String} [carType] Car type. Allowed values are - new / used / certified
     * @param {module:model/String} [carfax1Owner] Indicates whether car has had only one owner or not
     * @param {module:model/String} [carfaxCleanTitle] Indicates whether car has clean ownership records
     * @param {String} [yearRange] Year range to filter listings with the year in the range given. Range to be given in the format - min-max e.g. 2019-2021
     * @param {String} [year] To filter listing on their year
     * @param {String} [make] To filter listings on their make
     * @param {String} [model] To filter listings on their model
     * @param {String} [trim] To filter listing on their trim
     * @param {String} [dealerId] Dealer id to filter the listings.
     * @param {String} [source] To filter listing on their source
     * @param {String} [bodyType] To filter listing on their body type
     * @param {String} [bodySubtype] Body subtype to filter the listings on. Valid filter values are those that our Search facets API returns for unique body subtypes. You can pass in multiple body subtype values comma separated
     * @param {String} [vehicleType] To filter listing on their vehicle type
     * @param {String} [ymmt] Comma separated list of Year, Make, Model, Trim combinations. Each combination needs to have the year,make,model, trim values separated by a pipe '|' character in the form year|make|model|trim. e.g. 2010|Audi|A5,2014|Nissan|Sentra|S 6MT,|Honda|City|   You could just provide strings of the form - 'year|make||' or 'year|make|model' or '|make|model|' combinations. Individual year / make / model filters provied with the API calls will take precedence over the Year, Make, Model, Trim combinations. The Make, Model, Trim values must be valid values as per the Marketcheck Vin Decoder. If you are using a separate vin decoder then look at using the 'vins' or 'taxonomy_vins' parameter to the search api instead the year|make|model|trim combinations.
     * @param {String} [match] Comma separated list of Year, Make, Model, Trim fields. For example - year,make,model,trim fields for which user wants to do an exact match
     * @param {String} [cylinders] To filter listing on their cylinders
     * @param {String} [transmission] To filter listing on their transmission
     * @param {String} [doors] Doors to filter the cars on. Valid filter values are those that our Search facets API returns for unique doors. You can pass in multiple doors values comma separated
     * @param {String} [drivetrain] To filter listing on their drivetrain
     * @param {String} [exteriorColor] Exterior color to match. Valid filter values are those that our Search facets API returns for unique exterior colors. You can pass in multiple exterior color values comma separated
     * @param {String} [interiorColor] Interior color to match. Valid filter values are those that our Search facets API returns for unique interior colors. You can pass in multiple interior color values comma separated
     * @param {String} [baseExteriorColor] Base exterior color to match. Valid filter values are those that our Search facets API returns for unique base exterior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [baseInteriorColor] Base interior color to match. Valid filter values are those that our Search facets API returns for unique base interior colors. You can pass in multiple base interior color values comma separated
     * @param {String} [engine] To filter listing on their engine
     * @param {String} [engineSize] Engine Size to match. Valid filter values are those that our Search facets API returns for unique engine size. You can pass in multiple engine size values comma separated
     * @param {String} [engineAspiration] Engine Aspiration to match. Valid filter values are those that our Search facets API returns for unique Engine Aspirations. You can pass in multiple Engine aspirations values comma separated
     * @param {String} [engineBlock] Engine Block to match. Valid filter values are those that our Search facets API returns for unique Engine Block. You can pass in multiple Engine Block values comma separated
     * @param {String} [highwayMpgRange] Highway mileage range for UK to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [cityMpgRange] City mileage range for UK to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [combinedMpgRange] Combined mileage range for UK to filter listings with the mileage in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [milesRange] Miles range to filter listings with miles in the given range. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [priceRange] Price range to filter listings with the price in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [msrpRange] MSRP range to filter listings with the msrp in the range given. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [domRange] Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [lastSeenRange] Last seen date range to filter listings with the last seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenRange] First seen date range to filter listings with the first seen in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtSourceRange] First seen at source date range to filter listings with the first seen at source in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [firstSeenAtMcRange] First seen at MC date range to filter listings with the first seen at MC in the range given. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [lastSeenDays] Last seen days range to filter listings with the last seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenDays] First seen days range to filter listings with the first seen in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtSourceDays] First seen at source days range to filter listings with the first seen at source in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [firstSeenAtMcDays] First seen at MC days range to filter listings with the first seen at MC in the range given. Range to be given in the format - min-max e.g. 25-12
     * @param {String} [sortBy] Sort by field. Default sort field is distance from the given point
     * @param {module:model/String} [sortOrder] Sort order - asc or desc. Default sort order is asc
     * @param {Number} [rows = 10)] Number of results to return. Default is 10. Max is 50
     * @param {Number} [start = 0)] Page number to fetch the results for the given criteria. Default is 0. Pagination is allowed only till first 10000 results for the search and sort criteria. The page value can be only between 1 to 10000/rows
     * @param {Boolean} [includeNonVinListings = false)] To include non vin listings. Default is false
     * @param {String} [facets] The comma separated list of fields for which facets are requested. Facets could be requested in addition to the listings for the search. Please note - The API calls with lots of facet fields may take longer to respond.
     * @param {String} [rangeFacets] The comma separated list of numeric fields for which range facets are requested. Range facets could be requested in addition to the listings for the search. Please note - The API calls with lots of range facet fields may take longer to respond.
     * @param {module:model/String} [facetSort = 'count')] Control sort order of facets with this parameter with default sort being on count, Other available sort is alphabetical sort, which can be obtained by using index as value for this param
     * @param {String} [stats] The list of fields for which stats need to be generated based on the matching listings for the search criteria. The stats consists of mean, max, average and count of listings based on which the stats are calculated for the field. Stats could be requested in addition to the listings for the search. Please note - The API calls with the stats fields may take longer to respond.
     * @param {module:model/String} [country = 'uk')] To filter listing on Country in which they are listed
     * @param {Boolean} [plot] If plot has value true results in around 25k coordinates with limited fields to plot respective graph
     * @param {Boolean} [nodedup] If nodedup is set to true then API will give results without is_searchable i.e multiple listings for single vin
     * @param {Boolean} [dedup] If dedup is set to true then will give results with is_searchable irrespecive of dealer_id or source
     * @param {Boolean} [owned] Used in combination with dealer_id or source, when true returns the listings actually owned by dealer himself
     * @param {String} [state] To filter listing on State in which they are listed
     * @param {String} [city] To filter listing on City in which they are listed
     * @param {String} [msaCode] To filter listing on msa code in which they are listed
     * @param {String} [dealerName] Filter listings on dealer_name
     * @param {String} [dealershipGroupName] Name of the dealership group to search for
     * @param {String} [trimO] Filter listings on web scraped trim
     * @param {String} [trimR] Filter trim on custom possible matches
     * @param {String} [domActiveRange] Active Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {String} [dom180Range] Last 180 Days on Market range to filter cars with the DOM within the given range. Range to be given in the format - min-max e.g. 10-50
     * @param {Boolean} [excludeCertified] Boolean param to exclude certified cars from search results
     * @param {String} [fuelType] To filter listing on their fuel type
     * @param {module:model/String} [dealerType] Filter based on dealer type independant or franchise
     * @param {Boolean} [photoLinks] A boolean indicating whether to include only those listings that have photo_links in search results, And discard those that don't have them
     * @param {Boolean} [photoLinksCached] A boolean indicating whether to include only those listings that have photo_links_cached in search results, And discard those that don't have them
     * @param {String} [stockNo] To filter listing on their stock number on lot
     * @param {Boolean} [sold] sold parameter to fetch only sold listings
     * @param {Boolean} [includeRelevantLinks = false)] To include_relevant_links. Default is true
     * @param {module:model/String} [expired] Boolean falg to either fetch only the expired listings or active ones
     * @param {String} [excludeDealerIds] A list of dealer ids to exclude from result
     * @param {String} [excludeSources] A list of sources to exclude from result
     * @param {module:model/String} [inTransit] A boolean to filter in transit vehicles
     * @param {String} [seatingCapacity] To filter on vehicle seating capacity
     * @param {String} [insuranceGroup] Insurance Group
     * @param {String} [vrm] To filter on vrm
     * @param {String} [numOwners] Number of owners. Range to be given in the format - min-max e.g. 1000-5000
     * @param {String} [variant] To filter listing on their variant
     * @param {String} [postalCode] To filter listing on postal code around which they are listed
     * @param {String} [writeOffCategory] write off category
     * @param {String} [fcaStatus] To filter on fca status
     * @param {String} [activeInventoryDateRange] date range to filter listings that were active within given date range. Range to be given in the format [YYYYMMDD] - min-max e.g. 20190523-20190623
     * @param {String} [engineSizeRange] Engine size range to filter listings with engine size in the given range. Range to be given in the format - min-max e.g. 1.0-2
     * @param {String} [priceChangeRange] Price change range to filter listings with price change within given price_change_range. Range to be given in the format - min-max e.g. 10-500
     * @param {module:api/CarSearchApi~searchCarUkRecentsGetCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/UKSearchResponse}
     */
    searchCarUkRecentsGet(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'api_key': opts['apiKey'],
        'append_api_key': opts['appendApiKey'],
        'latitude': opts['latitude'],
        'longitude': opts['longitude'],
        'radius': opts['radius'],
        'zip': opts['zip'],
        'include_lease': opts['includeLease'],
        'include_finance': opts['includeFinance'],
        'lease_term': opts['leaseTerm'],
        'lease_down_payment': opts['leaseDownPayment'],
        'lease_emp': opts['leaseEmp'],
        'finance_loan_term': opts['financeLoanTerm'],
        'finance_loan_apr': opts['financeLoanApr'],
        'finance_emp': opts['financeEmp'],
        'finance_down_payment': opts['financeDownPayment'],
        'finance_down_payment_per': opts['financeDownPaymentPer'],
        'car_type': opts['carType'],
        'carfax_1_owner': opts['carfax1Owner'],
        'carfax_clean_title': opts['carfaxCleanTitle'],
        'year_range': opts['yearRange'],
        'year': opts['year'],
        'make': opts['make'],
        'model': opts['model'],
        'trim': opts['trim'],
        'dealer_id': opts['dealerId'],
        'source': opts['source'],
        'body_type': opts['bodyType'],
        'body_subtype': opts['bodySubtype'],
        'vehicle_type': opts['vehicleType'],
        'ymmt': opts['ymmt'],
        'match': opts['match'],
        'cylinders': opts['cylinders'],
        'transmission': opts['transmission'],
        'doors': opts['doors'],
        'drivetrain': opts['drivetrain'],
        'exterior_color': opts['exteriorColor'],
        'interior_color': opts['interiorColor'],
        'base_exterior_color': opts['baseExteriorColor'],
        'base_interior_color': opts['baseInteriorColor'],
        'engine': opts['engine'],
        'engine_size': opts['engineSize'],
        'engine_aspiration': opts['engineAspiration'],
        'engine_block': opts['engineBlock'],
        'highway_mpg_range': opts['highwayMpgRange'],
        'city_mpg_range': opts['cityMpgRange'],
        'combined_mpg_range': opts['combinedMpgRange'],
        'miles_range': opts['milesRange'],
        'price_range': opts['priceRange'],
        'msrp_range': opts['msrpRange'],
        'dom_range': opts['domRange'],
        'last_seen_range': opts['lastSeenRange'],
        'first_seen_range': opts['firstSeenRange'],
        'first_seen_at_source_range': opts['firstSeenAtSourceRange'],
        'first_seen_at_mc_range': opts['firstSeenAtMcRange'],
        'last_seen_days': opts['lastSeenDays'],
        'first_seen_days': opts['firstSeenDays'],
        'first_seen_at_source_days': opts['firstSeenAtSourceDays'],
        'first_seen_at_mc_days': opts['firstSeenAtMcDays'],
        'sort_by': opts['sortBy'],
        'sort_order': opts['sortOrder'],
        'rows': opts['rows'],
        'start': opts['start'],
        'include_non_vin_listings': opts['includeNonVinListings'],
        'facets': opts['facets'],
        'range_facets': opts['rangeFacets'],
        'facet_sort': opts['facetSort'],
        'stats': opts['stats'],
        'country': opts['country'],
        'plot': opts['plot'],
        'nodedup': opts['nodedup'],
        'dedup': opts['dedup'],
        'owned': opts['owned'],
        'state': opts['state'],
        'city': opts['city'],
        'msa_code': opts['msaCode'],
        'dealer_name': opts['dealerName'],
        'dealership_group_name': opts['dealershipGroupName'],
        'trim_o': opts['trimO'],
        'trim_r': opts['trimR'],
        'dom_active_range': opts['domActiveRange'],
        'dom_180_range': opts['dom180Range'],
        'exclude_certified': opts['excludeCertified'],
        'fuel_type': opts['fuelType'],
        'dealer_type': opts['dealerType'],
        'photo_links': opts['photoLinks'],
        'photo_links_cached': opts['photoLinksCached'],
        'stock_no': opts['stockNo'],
        'sold': opts['sold'],
        'include_relevant_links': opts['includeRelevantLinks'],
        'expired': opts['expired'],
        'exclude_dealer_ids': opts['excludeDealerIds'],
        'exclude_sources': opts['excludeSources'],
        'in_transit': opts['inTransit'],
        'seating_capacity': opts['seatingCapacity'],
        'insurance_group': opts['insuranceGroup'],
        'vrm': opts['vrm'],
        'num_owners': opts['numOwners'],
        'variant': opts['variant'],
        'postal_code': opts['postalCode'],
        'write_off_category': opts['writeOffCategory'],
        'fca_status': opts['fcaStatus'],
        'active_inventory_date_range': opts['activeInventoryDateRange'],
        'engine_size_range': opts['engineSizeRange'],
        'price_change_range': opts['priceChangeRange']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['authorizeEndpoint'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = UKSearchResponse;
      return this.apiClient.callApi(
        '/search/car/uk/recents', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
