# endpoints

Endpoints - JavaScript client for endpoints
No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install endpoints --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your endpoints from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var Endpoints = require('endpoints');


var api = new Endpoints.DefaultApi()
var opts = {
  'tokens': "3375, 3306", // {String} 
  'limit': "1000" // {String} 
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.correlation(opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.tokenmetrics.com*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*Endpoints.DefaultApi* | [**correlation**](docs/DefaultApi.md#correlation) | **GET** /v1/correlation | Correlation
*Endpoints.DefaultApi* | [**indices**](docs/DefaultApi.md#indices) | **GET** /v1/indices | Indices
*Endpoints.DefaultApi* | [**investorGrades**](docs/DefaultApi.md#investorGrades) | **GET** /v1/investor-grades | Investor Grades
*Endpoints.DefaultApi* | [**marketIndicator**](docs/DefaultApi.md#marketIndicator) | **GET** /v1/market-indicator | Market Indicator
*Endpoints.DefaultApi* | [**price**](docs/DefaultApi.md#price) | **GET** /v1/price | Price
*Endpoints.DefaultApi* | [**pricePrediction**](docs/DefaultApi.md#pricePrediction) | **GET** /v1/price-prediction | Price Prediction
*Endpoints.DefaultApi* | [**quantmetricsTier1**](docs/DefaultApi.md#quantmetricsTier1) | **GET** /v1/quantmetrics-tier-1 | Quantmetrics Tier 1
*Endpoints.DefaultApi* | [**quantmetricsTier2**](docs/DefaultApi.md#quantmetricsTier2) | **GET** /v1/quantmetrics-tier-2 | Quantmetrics Tier 2
*Endpoints.DefaultApi* | [**resistanceSupport**](docs/DefaultApi.md#resistanceSupport) | **GET** /v1/resistance-support | Resistance &amp; Support
*Endpoints.DefaultApi* | [**scenarioAnalysis**](docs/DefaultApi.md#scenarioAnalysis) | **GET** /v1/scenario-analysis | Scenario Analysis
*Endpoints.DefaultApi* | [**sentiments**](docs/DefaultApi.md#sentiments) | **GET** /v1/sentiments | Sentiments
*Endpoints.DefaultApi* | [**tokens**](docs/DefaultApi.md#tokens) | **GET** /v1/tokens | Tokens
*Endpoints.DefaultApi* | [**traderGrades**](docs/DefaultApi.md#traderGrades) | **GET** /v1/trader-grades | Trader Grades
*Endpoints.DefaultApi* | [**tradingIndicator**](docs/DefaultApi.md#tradingIndicator) | **GET** /v1/trading-indicator | Trading Indicator


## Documentation for Models



## Documentation for Authorization

Endpoints do not require authorization.

