/**
 * Shutterstock API Explorer
 * The Shutterstock API provides access to Shutterstock's library of media, as well as information about customers' accounts and the contributors that provide the media.
 *
 * The version of the OpenAPI document: 1.1.32
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Image from './Image';
import Insights from './Insights';

/**
 * The ImageSearchResults model module.
 * @module model/ImageSearchResults
 * @version 1.1.32
 */
class ImageSearchResults {
    /**
     * Constructs a new <code>ImageSearchResults</code>.
     * Image search results
     * @alias module:model/ImageSearchResults
     * @param data {Array.<module:model/Image>} List of images
     * @param searchId {String} Unique identifier for the search request
     * @param totalCount {Number} Total count of all results across all pages
     */
    constructor(data, searchId, totalCount) { 
        
        ImageSearchResults.initialize(this, data, searchId, totalCount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, searchId, totalCount) { 
        obj['data'] = data;
        obj['search_id'] = searchId;
        obj['total_count'] = totalCount;
    }

    /**
     * Constructs a <code>ImageSearchResults</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImageSearchResults} obj Optional instance to populate.
     * @return {module:model/ImageSearchResults} The populated <code>ImageSearchResults</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImageSearchResults();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [Image]);
            }
            if (data.hasOwnProperty('insights')) {
                obj['insights'] = Insights.constructFromObject(data['insights']);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('page')) {
                obj['page'] = ApiClient.convertToType(data['page'], 'Number');
            }
            if (data.hasOwnProperty('per_page')) {
                obj['per_page'] = ApiClient.convertToType(data['per_page'], 'Number');
            }
            if (data.hasOwnProperty('search_id')) {
                obj['search_id'] = ApiClient.convertToType(data['search_id'], 'String');
            }
            if (data.hasOwnProperty('spellcheck_info')) {
                obj['spellcheck_info'] = ApiClient.convertToType(data['spellcheck_info'], Object);
            }
            if (data.hasOwnProperty('total_count')) {
                obj['total_count'] = ApiClient.convertToType(data['total_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImageSearchResults</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImageSearchResults</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ImageSearchResults.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                Image.validateJSON(item);
            };
        }
        // validate the optional field `insights`
        if (data['insights']) { // data not null
          Insights.validateJSON(data['insights']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['search_id'] && !(typeof data['search_id'] === 'string' || data['search_id'] instanceof String)) {
            throw new Error("Expected the field `search_id` to be a primitive type in the JSON string but got " + data['search_id']);
        }

        return true;
    }


}

ImageSearchResults.RequiredProperties = ["data", "search_id", "total_count"];

/**
 * List of images
 * @member {Array.<module:model/Image>} data
 */
ImageSearchResults.prototype['data'] = undefined;

/**
 * @member {module:model/Insights} insights
 */
ImageSearchResults.prototype['insights'] = undefined;

/**
 * Server-generated message, if any
 * @member {String} message
 */
ImageSearchResults.prototype['message'] = undefined;

/**
 * Current page that is returned
 * @member {Number} page
 */
ImageSearchResults.prototype['page'] = undefined;

/**
 * Number of results per page
 * @member {Number} per_page
 */
ImageSearchResults.prototype['per_page'] = undefined;

/**
 * Unique identifier for the search request
 * @member {String} search_id
 */
ImageSearchResults.prototype['search_id'] = undefined;

/**
 * Returns information if search phrase has potentially been mistyped or another query would lead to better search results
 * @member {Object} spellcheck_info
 */
ImageSearchResults.prototype['spellcheck_info'] = undefined;

/**
 * Total count of all results across all pages
 * @member {Number} total_count
 */
ImageSearchResults.prototype['total_count'] = undefined;






export default ImageSearchResults;

