/**
 * Shutterstock API Explorer
 * The Shutterstock API provides access to Shutterstock's library of media, as well as information about customers' accounts and the contributors that provide the media.
 *
 * The version of the OpenAPI document: 1.1.32
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AudioRenderTimelineSpanInstrumentGroupStatus from './AudioRenderTimelineSpanInstrumentGroupStatus';

/**
 * The AudioRenderTimelineSpanInstrumentGroup model module.
 * @module model/AudioRenderTimelineSpanInstrumentGroup
 * @version 1.1.32
 */
class AudioRenderTimelineSpanInstrumentGroup {
    /**
     * Constructs a new <code>AudioRenderTimelineSpanInstrumentGroup</code>.
     * An instrument and the status objects that specify when that instrument plays
     * @alias module:model/AudioRenderTimelineSpanInstrumentGroup
     * @param instrumentGroup {String} The instrument ID
     */
    constructor(instrumentGroup) { 
        
        AudioRenderTimelineSpanInstrumentGroup.initialize(this, instrumentGroup);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, instrumentGroup) { 
        obj['instrument_group'] = instrumentGroup;
    }

    /**
     * Constructs a <code>AudioRenderTimelineSpanInstrumentGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AudioRenderTimelineSpanInstrumentGroup} obj Optional instance to populate.
     * @return {module:model/AudioRenderTimelineSpanInstrumentGroup} The populated <code>AudioRenderTimelineSpanInstrumentGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AudioRenderTimelineSpanInstrumentGroup();

            if (data.hasOwnProperty('instrument_group')) {
                obj['instrument_group'] = ApiClient.convertToType(data['instrument_group'], 'String');
            }
            if (data.hasOwnProperty('statuses')) {
                obj['statuses'] = ApiClient.convertToType(data['statuses'], [AudioRenderTimelineSpanInstrumentGroupStatus]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AudioRenderTimelineSpanInstrumentGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AudioRenderTimelineSpanInstrumentGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AudioRenderTimelineSpanInstrumentGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['instrument_group'] && !(typeof data['instrument_group'] === 'string' || data['instrument_group'] instanceof String)) {
            throw new Error("Expected the field `instrument_group` to be a primitive type in the JSON string but got " + data['instrument_group']);
        }
        if (data['statuses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['statuses'])) {
                throw new Error("Expected the field `statuses` to be an array in the JSON data but got " + data['statuses']);
            }
            // validate the optional field `statuses` (array)
            for (const item of data['statuses']) {
                AudioRenderTimelineSpanInstrumentGroupStatus.validateJSON(item);
            };
        }

        return true;
    }


}

AudioRenderTimelineSpanInstrumentGroup.RequiredProperties = ["instrument_group"];

/**
 * The instrument ID
 * @member {String} instrument_group
 */
AudioRenderTimelineSpanInstrumentGroup.prototype['instrument_group'] = undefined;

/**
 * An array of status objects
 * @member {Array.<module:model/AudioRenderTimelineSpanInstrumentGroupStatus>} statuses
 */
AudioRenderTimelineSpanInstrumentGroup.prototype['statuses'] = undefined;






export default AudioRenderTimelineSpanInstrumentGroup;

