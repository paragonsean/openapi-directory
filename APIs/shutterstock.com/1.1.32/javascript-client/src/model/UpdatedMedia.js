/**
 * Shutterstock API Explorer
 * The Shutterstock API provides access to Shutterstock's library of media, as well as information about customers' accounts and the contributors that provide the media.
 *
 * The version of the OpenAPI document: 1.1.32
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdatedMedia model module.
 * @module model/UpdatedMedia
 * @version 1.1.32
 */
class UpdatedMedia {
    /**
     * Constructs a new <code>UpdatedMedia</code>.
     * Information about a piece of updated media
     * @alias module:model/UpdatedMedia
     * @param id {String} ID of the media
     * @param updatedTime {Date} Date that the media was updated
     * @param updates {Array.<String>} Types of updates that were made to the piece of media
     */
    constructor(id, updatedTime, updates) { 
        
        UpdatedMedia.initialize(this, id, updatedTime, updates);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, updatedTime, updates) { 
        obj['id'] = id;
        obj['updated_time'] = updatedTime;
        obj['updates'] = updates;
    }

    /**
     * Constructs a <code>UpdatedMedia</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdatedMedia} obj Optional instance to populate.
     * @return {module:model/UpdatedMedia} The populated <code>UpdatedMedia</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdatedMedia();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('updated_time')) {
                obj['updated_time'] = ApiClient.convertToType(data['updated_time'], 'Date');
            }
            if (data.hasOwnProperty('updates')) {
                obj['updates'] = ApiClient.convertToType(data['updates'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdatedMedia</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdatedMedia</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdatedMedia.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['updates'])) {
            throw new Error("Expected the field `updates` to be an array in the JSON data but got " + data['updates']);
        }

        return true;
    }


}

UpdatedMedia.RequiredProperties = ["id", "updated_time", "updates"];

/**
 * ID of the media
 * @member {String} id
 */
UpdatedMedia.prototype['id'] = undefined;

/**
 * Date that the media was updated
 * @member {Date} updated_time
 */
UpdatedMedia.prototype['updated_time'] = undefined;

/**
 * Types of updates that were made to the piece of media
 * @member {Array.<String>} updates
 */
UpdatedMedia.prototype['updates'] = undefined;






export default UpdatedMedia;

