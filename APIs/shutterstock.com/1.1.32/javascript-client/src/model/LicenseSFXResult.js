/**
 * Shutterstock API Explorer
 * The Shutterstock API provides access to Shutterstock's library of media, as well as information about customers' accounts and the contributors that provide the media.
 *
 * The version of the OpenAPI document: 1.1.32
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Url from './Url';

/**
 * The LicenseSFXResult model module.
 * @module model/LicenseSFXResult
 * @version 1.1.32
 */
class LicenseSFXResult {
    /**
     * Constructs a new <code>LicenseSFXResult</code>.
     * The response to a licensing request for an sound effects
     * @alias module:model/LicenseSFXResult
     * @param sfxId {String} Sound effects ID that was licensed
     */
    constructor(sfxId) { 
        
        LicenseSFXResult.initialize(this, sfxId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, sfxId) { 
        obj['sfx_id'] = sfxId;
    }

    /**
     * Constructs a <code>LicenseSFXResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LicenseSFXResult} obj Optional instance to populate.
     * @return {module:model/LicenseSFXResult} The populated <code>LicenseSFXResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LicenseSFXResult();

            if (data.hasOwnProperty('allotment_charge')) {
                obj['allotment_charge'] = ApiClient.convertToType(data['allotment_charge'], 'Number');
            }
            if (data.hasOwnProperty('download')) {
                obj['download'] = Url.constructFromObject(data['download']);
            }
            if (data.hasOwnProperty('error')) {
                obj['error'] = ApiClient.convertToType(data['error'], 'String');
            }
            if (data.hasOwnProperty('license_id')) {
                obj['license_id'] = ApiClient.convertToType(data['license_id'], 'String');
            }
            if (data.hasOwnProperty('sfx_id')) {
                obj['sfx_id'] = ApiClient.convertToType(data['sfx_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LicenseSFXResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LicenseSFXResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LicenseSFXResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `download`
        if (data['download']) { // data not null
          Url.validateJSON(data['download']);
        }
        // ensure the json data is a string
        if (data['error'] && !(typeof data['error'] === 'string' || data['error'] instanceof String)) {
            throw new Error("Expected the field `error` to be a primitive type in the JSON string but got " + data['error']);
        }
        // ensure the json data is a string
        if (data['license_id'] && !(typeof data['license_id'] === 'string' || data['license_id'] instanceof String)) {
            throw new Error("Expected the field `license_id` to be a primitive type in the JSON string but got " + data['license_id']);
        }
        // ensure the json data is a string
        if (data['sfx_id'] && !(typeof data['sfx_id'] === 'string' || data['sfx_id'] instanceof String)) {
            throw new Error("Expected the field `sfx_id` to be a primitive type in the JSON string but got " + data['sfx_id']);
        }

        return true;
    }


}

LicenseSFXResult.RequiredProperties = ["sfx_id"];

/**
 * Number of credits that this licensing event used
 * @member {Number} allotment_charge
 */
LicenseSFXResult.prototype['allotment_charge'] = undefined;

/**
 * @member {module:model/Url} download
 */
LicenseSFXResult.prototype['download'] = undefined;

/**
 * Error message, appears only if there was an error
 * @member {String} error
 */
LicenseSFXResult.prototype['error'] = undefined;

/**
 * ID of the license event
 * @member {String} license_id
 */
LicenseSFXResult.prototype['license_id'] = undefined;

/**
 * Sound effects ID that was licensed
 * @member {String} sfx_id
 */
LicenseSFXResult.prototype['sfx_id'] = undefined;






export default LicenseSFXResult;

