/**
 * Shutterstock API Explorer
 * The Shutterstock API provides access to Shutterstock's library of media, as well as information about customers' accounts and the contributors that provide the media.
 *
 * The version of the OpenAPI document: 1.1.32
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Price from './Price';
import Url from './Url';

/**
 * The LicenseImageResult model module.
 * @module model/LicenseImageResult
 * @version 1.1.32
 */
class LicenseImageResult {
    /**
     * Constructs a new <code>LicenseImageResult</code>.
     * The response to a licensing request for an image
     * @alias module:model/LicenseImageResult
     * @param imageId {String} Image ID that was licensed
     */
    constructor(imageId) { 
        
        LicenseImageResult.initialize(this, imageId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, imageId) { 
        obj['image_id'] = imageId;
    }

    /**
     * Constructs a <code>LicenseImageResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LicenseImageResult} obj Optional instance to populate.
     * @return {module:model/LicenseImageResult} The populated <code>LicenseImageResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LicenseImageResult();

            if (data.hasOwnProperty('allotment_charge')) {
                obj['allotment_charge'] = ApiClient.convertToType(data['allotment_charge'], 'Number');
            }
            if (data.hasOwnProperty('download')) {
                obj['download'] = Url.constructFromObject(data['download']);
            }
            if (data.hasOwnProperty('error')) {
                obj['error'] = ApiClient.convertToType(data['error'], 'String');
            }
            if (data.hasOwnProperty('image_id')) {
                obj['image_id'] = ApiClient.convertToType(data['image_id'], 'String');
            }
            if (data.hasOwnProperty('license_id')) {
                obj['license_id'] = ApiClient.convertToType(data['license_id'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = Price.constructFromObject(data['price']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LicenseImageResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LicenseImageResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LicenseImageResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `download`
        if (data['download']) { // data not null
          Url.validateJSON(data['download']);
        }
        // ensure the json data is a string
        if (data['error'] && !(typeof data['error'] === 'string' || data['error'] instanceof String)) {
            throw new Error("Expected the field `error` to be a primitive type in the JSON string but got " + data['error']);
        }
        // ensure the json data is a string
        if (data['image_id'] && !(typeof data['image_id'] === 'string' || data['image_id'] instanceof String)) {
            throw new Error("Expected the field `image_id` to be a primitive type in the JSON string but got " + data['image_id']);
        }
        // ensure the json data is a string
        if (data['license_id'] && !(typeof data['license_id'] === 'string' || data['license_id'] instanceof String)) {
            throw new Error("Expected the field `license_id` to be a primitive type in the JSON string but got " + data['license_id']);
        }
        // validate the optional field `price`
        if (data['price']) { // data not null
          Price.validateJSON(data['price']);
        }

        return true;
    }


}

LicenseImageResult.RequiredProperties = ["image_id"];

/**
 * Number of credits that this licensing event used
 * @member {Number} allotment_charge
 */
LicenseImageResult.prototype['allotment_charge'] = undefined;

/**
 * @member {module:model/Url} download
 */
LicenseImageResult.prototype['download'] = undefined;

/**
 * Error message, appears only if there was an error
 * @member {String} error
 */
LicenseImageResult.prototype['error'] = undefined;

/**
 * Image ID that was licensed
 * @member {String} image_id
 */
LicenseImageResult.prototype['image_id'] = undefined;

/**
 * ID of the license event
 * @member {String} license_id
 */
LicenseImageResult.prototype['license_id'] = undefined;

/**
 * @member {module:model/Price} price
 */
LicenseImageResult.prototype['price'] = undefined;






export default LicenseImageResult;

