/**
 * Noosh API application
 * Full description of Noosh API
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RfeItemSimpleVO from './RfeItemSimpleVO';
import RfeSuEstimateSimpleVO from './RfeSuEstimateSimpleVO';

/**
 * The RfeSimpleVO model module.
 * @module model/RfeSimpleVO
 * @version 1.0
 */
class RfeSimpleVO {
    /**
     * Constructs a new <code>RfeSimpleVO</code>.
     * Java type: com.noosh.nooshapi.vo.RfeSimpleVO
     * @alias module:model/RfeSimpleVO
     */
    constructor() { 
        
        RfeSimpleVO.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RfeSimpleVO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RfeSimpleVO} obj Optional instance to populate.
     * @return {module:model/RfeSimpleVO} The populated <code>RfeSimpleVO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RfeSimpleVO();

            if (data.hasOwnProperty('estimate_due_date')) {
                obj['estimate_due_date'] = ApiClient.convertToType(data['estimate_due_date'], 'Date');
            }
            if (data.hasOwnProperty('rfe_id')) {
                obj['rfe_id'] = ApiClient.convertToType(data['rfe_id'], 'Number');
            }
            if (data.hasOwnProperty('rfe_items')) {
                obj['rfe_items'] = ApiClient.convertToType(data['rfe_items'], [RfeItemSimpleVO]);
            }
            if (data.hasOwnProperty('rfe_title')) {
                obj['rfe_title'] = ApiClient.convertToType(data['rfe_title'], 'String');
            }
            if (data.hasOwnProperty('supplier_estimates')) {
                obj['supplier_estimates'] = ApiClient.convertToType(data['supplier_estimates'], [RfeSuEstimateSimpleVO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RfeSimpleVO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RfeSimpleVO</code>.
     */
    static validateJSON(data) {
        if (data['rfe_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rfe_items'])) {
                throw new Error("Expected the field `rfe_items` to be an array in the JSON data but got " + data['rfe_items']);
            }
            // validate the optional field `rfe_items` (array)
            for (const item of data['rfe_items']) {
                RfeItemSimpleVO.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['rfe_title'] && !(typeof data['rfe_title'] === 'string' || data['rfe_title'] instanceof String)) {
            throw new Error("Expected the field `rfe_title` to be a primitive type in the JSON string but got " + data['rfe_title']);
        }
        if (data['supplier_estimates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['supplier_estimates'])) {
                throw new Error("Expected the field `supplier_estimates` to be an array in the JSON data but got " + data['supplier_estimates']);
            }
            // validate the optional field `supplier_estimates` (array)
            for (const item of data['supplier_estimates']) {
                RfeSuEstimateSimpleVO.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * 
 * @member {Date} estimate_due_date
 */
RfeSimpleVO.prototype['estimate_due_date'] = undefined;

/**
 * 
 * @member {Number} rfe_id
 */
RfeSimpleVO.prototype['rfe_id'] = undefined;

/**
 * 
 * @member {Array.<module:model/RfeItemSimpleVO>} rfe_items
 */
RfeSimpleVO.prototype['rfe_items'] = undefined;

/**
 * 
 * @member {String} rfe_title
 */
RfeSimpleVO.prototype['rfe_title'] = undefined;

/**
 * 
 * @member {Array.<module:model/RfeSuEstimateSimpleVO>} supplier_estimates
 */
RfeSimpleVO.prototype['supplier_estimates'] = undefined;






export default RfeSimpleVO;

