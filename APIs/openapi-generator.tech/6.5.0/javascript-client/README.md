# open_api_generator_online

OpenApiGeneratorOnline - JavaScript client for open_api_generator_online
This is an online openapi generator server.  You can find out more at https://github.com/OpenAPITools/openapi-generator.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 6.5.0
- Package version: 6.5.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install open_api_generator_online --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your open_api_generator_online from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var OpenApiGeneratorOnline = require('open_api_generator_online');


var api = new OpenApiGeneratorOnline.ClientsApi()
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.clientOptions(callback);

```

## Documentation for API Endpoints

All URIs are relative to *http://api.openapi-generator.tech*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*OpenApiGeneratorOnline.ClientsApi* | [**clientOptions**](docs/ClientsApi.md#clientOptions) | **GET** /api/gen/clients | Gets languages supported by the client generator
*OpenApiGeneratorOnline.ClientsApi* | [**downloadFile**](docs/ClientsApi.md#downloadFile) | **GET** /api/gen/download/{fileId} | Downloads a pre-generated file
*OpenApiGeneratorOnline.ClientsApi* | [**generateClient**](docs/ClientsApi.md#generateClient) | **POST** /api/gen/clients/{language} | Generates a client library
*OpenApiGeneratorOnline.ClientsApi* | [**getClientOptions**](docs/ClientsApi.md#getClientOptions) | **GET** /api/gen/clients/{language} | Returns options for a client library
*OpenApiGeneratorOnline.ServersApi* | [**downloadFile_0**](docs/ServersApi.md#downloadFile_0) | **GET** /api/gen/download/{fileId} | Downloads a pre-generated file
*OpenApiGeneratorOnline.ServersApi* | [**generateServerForLanguage**](docs/ServersApi.md#generateServerForLanguage) | **POST** /api/gen/servers/{framework} | Generates a server library
*OpenApiGeneratorOnline.ServersApi* | [**getServerOptions**](docs/ServersApi.md#getServerOptions) | **GET** /api/gen/servers/{framework} | Returns options for a server framework
*OpenApiGeneratorOnline.ServersApi* | [**serverOptions**](docs/ServersApi.md#serverOptions) | **GET** /api/gen/servers | Gets languages supported by the server generator


## Documentation for Models

 - [OpenApiGeneratorOnline.AuthorizationValue](docs/AuthorizationValue.md)
 - [OpenApiGeneratorOnline.CliOption](docs/CliOption.md)
 - [OpenApiGeneratorOnline.GeneratorInput](docs/GeneratorInput.md)
 - [OpenApiGeneratorOnline.ResponseCode](docs/ResponseCode.md)


## Documentation for Authorization

Endpoints do not require authorization.

