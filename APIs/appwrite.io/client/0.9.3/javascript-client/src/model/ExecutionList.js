/**
 * Appwrite
 * Appwrite backend as a service cuts up to 70% of the time and costs required for building a modern application. We abstract and simplify common development tasks behind a REST APIs, to help you develop your app in a fast and secure way. For full API documentation and tutorials go to [https://appwrite.io/docs](https://appwrite.io/docs)
 *
 * The version of the OpenAPI document: 0.9.3
 * Contact: team@appwrite.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Execution from './Execution';

/**
 * The ExecutionList model module.
 * @module model/ExecutionList
 * @version 0.9.3
 */
class ExecutionList {
    /**
     * Constructs a new <code>ExecutionList</code>.
     * Executions List
     * @alias module:model/ExecutionList
     * @param executions {Array.<module:model/Execution>} List of executions.
     * @param sum {Number} Total sum of items in the list.
     */
    constructor(executions, sum) { 
        
        ExecutionList.initialize(this, executions, sum);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, executions, sum) { 
        obj['executions'] = executions;
        obj['sum'] = sum;
    }

    /**
     * Constructs a <code>ExecutionList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExecutionList} obj Optional instance to populate.
     * @return {module:model/ExecutionList} The populated <code>ExecutionList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExecutionList();

            if (data.hasOwnProperty('executions')) {
                obj['executions'] = ApiClient.convertToType(data['executions'], [Execution]);
            }
            if (data.hasOwnProperty('sum')) {
                obj['sum'] = ApiClient.convertToType(data['sum'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExecutionList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExecutionList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExecutionList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['executions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['executions'])) {
                throw new Error("Expected the field `executions` to be an array in the JSON data but got " + data['executions']);
            }
            // validate the optional field `executions` (array)
            for (const item of data['executions']) {
                Execution.validateJSON(item);
            };
        }

        return true;
    }


}

ExecutionList.RequiredProperties = ["executions", "sum"];

/**
 * List of executions.
 * @member {Array.<module:model/Execution>} executions
 */
ExecutionList.prototype['executions'] = undefined;

/**
 * Total sum of items in the list.
 * @member {Number} sum
 */
ExecutionList.prototype['sum'] = undefined;






export default ExecutionList;

