/**
 * Appwrite
 * Appwrite backend as a service cuts up to 70% of the time and costs required for building a modern application. We abstract and simplify common development tasks behind a REST APIs, to help you develop your app in a fast and secure way. For full API documentation and tutorials go to [https://appwrite.io/docs](https://appwrite.io/docs)
 *
 * The version of the OpenAPI document: 0.9.3
 * Contact: team@appwrite.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Session from './Session';

/**
 * The SessionList model module.
 * @module model/SessionList
 * @version 0.9.3
 */
class SessionList {
    /**
     * Constructs a new <code>SessionList</code>.
     * Sessions List
     * @alias module:model/SessionList
     * @param sessions {Array.<module:model/Session>} List of sessions.
     * @param sum {Number} Total sum of items in the list.
     */
    constructor(sessions, sum) { 
        
        SessionList.initialize(this, sessions, sum);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, sessions, sum) { 
        obj['sessions'] = sessions;
        obj['sum'] = sum;
    }

    /**
     * Constructs a <code>SessionList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SessionList} obj Optional instance to populate.
     * @return {module:model/SessionList} The populated <code>SessionList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SessionList();

            if (data.hasOwnProperty('sessions')) {
                obj['sessions'] = ApiClient.convertToType(data['sessions'], [Session]);
            }
            if (data.hasOwnProperty('sum')) {
                obj['sum'] = ApiClient.convertToType(data['sum'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SessionList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SessionList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SessionList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['sessions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sessions'])) {
                throw new Error("Expected the field `sessions` to be an array in the JSON data but got " + data['sessions']);
            }
            // validate the optional field `sessions` (array)
            for (const item of data['sessions']) {
                Session.validateJSON(item);
            };
        }

        return true;
    }


}

SessionList.RequiredProperties = ["sessions", "sum"];

/**
 * List of sessions.
 * @member {Array.<module:model/Session>} sessions
 */
SessionList.prototype['sessions'] = undefined;

/**
 * Total sum of items in the list.
 * @member {Number} sum
 */
SessionList.prototype['sum'] = undefined;






export default SessionList;

