/**
 * Appwrite
 * Appwrite backend as a service cuts up to 70% of the time and costs required for building a modern application. We abstract and simplify common development tasks behind a REST APIs, to help you develop your app in a fast and secure way. For full API documentation and tutorials go to [https://appwrite.io/docs](https://appwrite.io/docs)
 *
 * The version of the OpenAPI document: 0.9.3
 * Contact: team@appwrite.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DatabaseUpdateCollectionRequest model module.
 * @module model/DatabaseUpdateCollectionRequest
 * @version 0.9.3
 */
class DatabaseUpdateCollectionRequest {
    /**
     * Constructs a new <code>DatabaseUpdateCollectionRequest</code>.
     * @alias module:model/DatabaseUpdateCollectionRequest
     * @param name {String} Collection name. Max length: 128 chars.
     */
    constructor(name) { 
        
        DatabaseUpdateCollectionRequest.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>DatabaseUpdateCollectionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DatabaseUpdateCollectionRequest} obj Optional instance to populate.
     * @return {module:model/DatabaseUpdateCollectionRequest} The populated <code>DatabaseUpdateCollectionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DatabaseUpdateCollectionRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('read')) {
                obj['read'] = ApiClient.convertToType(data['read'], ['String']);
            }
            if (data.hasOwnProperty('rules')) {
                obj['rules'] = ApiClient.convertToType(data['rules'], ['String']);
            }
            if (data.hasOwnProperty('write')) {
                obj['write'] = ApiClient.convertToType(data['write'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DatabaseUpdateCollectionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DatabaseUpdateCollectionRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DatabaseUpdateCollectionRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['read'])) {
            throw new Error("Expected the field `read` to be an array in the JSON data but got " + data['read']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['rules'])) {
            throw new Error("Expected the field `rules` to be an array in the JSON data but got " + data['rules']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['write'])) {
            throw new Error("Expected the field `write` to be an array in the JSON data but got " + data['write']);
        }

        return true;
    }


}

DatabaseUpdateCollectionRequest.RequiredProperties = ["name"];

/**
 * Collection name. Max length: 128 chars.
 * @member {String} name
 */
DatabaseUpdateCollectionRequest.prototype['name'] = undefined;

/**
 * An array of strings with read permissions. By default inherits the existing read permissions. [learn more about permissions](/docs/permissions) and get a full list of available permissions.
 * @member {Array.<String>} read
 */
DatabaseUpdateCollectionRequest.prototype['read'] = undefined;

/**
 * Array of [rule objects](/docs/rules). Each rule define a collection field name, data type and validation.
 * @member {Array.<String>} rules
 */
DatabaseUpdateCollectionRequest.prototype['rules'] = undefined;

/**
 * An array of strings with write permissions. By default inherits the existing write permissions. [learn more about permissions](/docs/permissions) and get a full list of available permissions.
 * @member {Array.<String>} write
 */
DatabaseUpdateCollectionRequest.prototype['write'] = undefined;






export default DatabaseUpdateCollectionRequest;

