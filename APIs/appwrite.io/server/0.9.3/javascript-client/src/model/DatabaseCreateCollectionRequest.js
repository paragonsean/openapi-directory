/**
 * Appwrite
 * Appwrite backend as a service cuts up to 70% of the time and costs required for building a modern application. We abstract and simplify common development tasks behind a REST APIs, to help you develop your app in a fast and secure way. For full API documentation and tutorials go to [https://appwrite.io/docs](https://appwrite.io/docs)
 *
 * The version of the OpenAPI document: 0.9.3
 * Contact: team@appwrite.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DatabaseCreateCollectionRequest model module.
 * @module model/DatabaseCreateCollectionRequest
 * @version 0.9.3
 */
class DatabaseCreateCollectionRequest {
    /**
     * Constructs a new <code>DatabaseCreateCollectionRequest</code>.
     * @alias module:model/DatabaseCreateCollectionRequest
     * @param name {String} Collection name. Max length: 128 chars.
     * @param read {Array.<String>} An array of strings with read permissions. By default no user is granted with any read permissions. [learn more about permissions](/docs/permissions) and get a full list of available permissions.
     * @param rules {Array.<String>} Array of [rule objects](/docs/rules). Each rule define a collection field name, data type and validation.
     * @param write {Array.<String>} An array of strings with write permissions. By default no user is granted with any write permissions. [learn more about permissions](/docs/permissions) and get a full list of available permissions.
     */
    constructor(name, read, rules, write) { 
        
        DatabaseCreateCollectionRequest.initialize(this, name, read, rules, write);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, read, rules, write) { 
        obj['name'] = name;
        obj['read'] = read;
        obj['rules'] = rules;
        obj['write'] = write;
    }

    /**
     * Constructs a <code>DatabaseCreateCollectionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DatabaseCreateCollectionRequest} obj Optional instance to populate.
     * @return {module:model/DatabaseCreateCollectionRequest} The populated <code>DatabaseCreateCollectionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DatabaseCreateCollectionRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('read')) {
                obj['read'] = ApiClient.convertToType(data['read'], ['String']);
            }
            if (data.hasOwnProperty('rules')) {
                obj['rules'] = ApiClient.convertToType(data['rules'], ['String']);
            }
            if (data.hasOwnProperty('write')) {
                obj['write'] = ApiClient.convertToType(data['write'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DatabaseCreateCollectionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DatabaseCreateCollectionRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DatabaseCreateCollectionRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['read'])) {
            throw new Error("Expected the field `read` to be an array in the JSON data but got " + data['read']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['rules'])) {
            throw new Error("Expected the field `rules` to be an array in the JSON data but got " + data['rules']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['write'])) {
            throw new Error("Expected the field `write` to be an array in the JSON data but got " + data['write']);
        }

        return true;
    }


}

DatabaseCreateCollectionRequest.RequiredProperties = ["name", "read", "rules", "write"];

/**
 * Collection name. Max length: 128 chars.
 * @member {String} name
 */
DatabaseCreateCollectionRequest.prototype['name'] = undefined;

/**
 * An array of strings with read permissions. By default no user is granted with any read permissions. [learn more about permissions](/docs/permissions) and get a full list of available permissions.
 * @member {Array.<String>} read
 */
DatabaseCreateCollectionRequest.prototype['read'] = undefined;

/**
 * Array of [rule objects](/docs/rules). Each rule define a collection field name, data type and validation.
 * @member {Array.<String>} rules
 */
DatabaseCreateCollectionRequest.prototype['rules'] = undefined;

/**
 * An array of strings with write permissions. By default no user is granted with any write permissions. [learn more about permissions](/docs/permissions) and get a full list of available permissions.
 * @member {Array.<String>} write
 */
DatabaseCreateCollectionRequest.prototype['write'] = undefined;






export default DatabaseCreateCollectionRequest;

