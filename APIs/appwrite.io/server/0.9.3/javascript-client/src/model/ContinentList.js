/**
 * Appwrite
 * Appwrite backend as a service cuts up to 70% of the time and costs required for building a modern application. We abstract and simplify common development tasks behind a REST APIs, to help you develop your app in a fast and secure way. For full API documentation and tutorials go to [https://appwrite.io/docs](https://appwrite.io/docs)
 *
 * The version of the OpenAPI document: 0.9.3
 * Contact: team@appwrite.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Continent from './Continent';

/**
 * The ContinentList model module.
 * @module model/ContinentList
 * @version 0.9.3
 */
class ContinentList {
    /**
     * Constructs a new <code>ContinentList</code>.
     * Continents List
     * @alias module:model/ContinentList
     * @param continents {Array.<module:model/Continent>} List of continents.
     * @param sum {Number} Total sum of items in the list.
     */
    constructor(continents, sum) { 
        
        ContinentList.initialize(this, continents, sum);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, continents, sum) { 
        obj['continents'] = continents;
        obj['sum'] = sum;
    }

    /**
     * Constructs a <code>ContinentList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContinentList} obj Optional instance to populate.
     * @return {module:model/ContinentList} The populated <code>ContinentList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContinentList();

            if (data.hasOwnProperty('continents')) {
                obj['continents'] = ApiClient.convertToType(data['continents'], [Continent]);
            }
            if (data.hasOwnProperty('sum')) {
                obj['sum'] = ApiClient.convertToType(data['sum'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContinentList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContinentList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContinentList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['continents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['continents'])) {
                throw new Error("Expected the field `continents` to be an array in the JSON data but got " + data['continents']);
            }
            // validate the optional field `continents` (array)
            for (const item of data['continents']) {
                Continent.validateJSON(item);
            };
        }

        return true;
    }


}

ContinentList.RequiredProperties = ["continents", "sum"];

/**
 * List of continents.
 * @member {Array.<module:model/Continent>} continents
 */
ContinentList.prototype['continents'] = undefined;

/**
 * Total sum of items in the list.
 * @member {Number} sum
 */
ContinentList.prototype['sum'] = undefined;






export default ContinentList;

