/**
 * Appwrite
 * Appwrite backend as a service cuts up to 70% of the time and costs required for building a modern application. We abstract and simplify common development tasks behind a REST APIs, to help you develop your app in a fast and secure way. For full API documentation and tutorials go to [https://appwrite.io/docs](https://appwrite.io/docs)
 *
 * The version of the OpenAPI document: 0.9.3
 * Contact: team@appwrite.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Membership from './Membership';

/**
 * The MembershipList model module.
 * @module model/MembershipList
 * @version 0.9.3
 */
class MembershipList {
    /**
     * Constructs a new <code>MembershipList</code>.
     * Memberships List
     * @alias module:model/MembershipList
     * @param memberships {Array.<module:model/Membership>} List of memberships.
     * @param sum {Number} Total sum of items in the list.
     */
    constructor(memberships, sum) { 
        
        MembershipList.initialize(this, memberships, sum);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, memberships, sum) { 
        obj['memberships'] = memberships;
        obj['sum'] = sum;
    }

    /**
     * Constructs a <code>MembershipList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MembershipList} obj Optional instance to populate.
     * @return {module:model/MembershipList} The populated <code>MembershipList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MembershipList();

            if (data.hasOwnProperty('memberships')) {
                obj['memberships'] = ApiClient.convertToType(data['memberships'], [Membership]);
            }
            if (data.hasOwnProperty('sum')) {
                obj['sum'] = ApiClient.convertToType(data['sum'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MembershipList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MembershipList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MembershipList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['memberships']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['memberships'])) {
                throw new Error("Expected the field `memberships` to be an array in the JSON data but got " + data['memberships']);
            }
            // validate the optional field `memberships` (array)
            for (const item of data['memberships']) {
                Membership.validateJSON(item);
            };
        }

        return true;
    }


}

MembershipList.RequiredProperties = ["memberships", "sum"];

/**
 * List of memberships.
 * @member {Array.<module:model/Membership>} memberships
 */
MembershipList.prototype['memberships'] = undefined;

/**
 * Total sum of items in the list.
 * @member {Number} sum
 */
MembershipList.prototype['sum'] = undefined;






export default MembershipList;

