/**
 * API docs | logoraisr.com
 * <p style=\"font-size:110%;\">Dig into our logoraisr API reference documentation. We also offer an OpenAPI specification to allow easy integration into your systems. You can download the json file by clicking on the download button.<p><br><p style=\"font-size:110%; font-weight:bold\">OpenAPI 2.0 Validation Status</p><img src=\"https://online.swagger.io/validator?url=https://docs.logoraisr.com/\">
 *
 * The version of the OpenAPI document: v1
 * Contact: support@logoraisr.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Process from './Process';

/**
 * The Project model module.
 * @module model/Project
 * @version v1
 */
class Project {
    /**
     * Constructs a new <code>Project</code>.
     * @alias module:model/Project
     * @param processId {String} Process which should applied. Processes are \"color-classification\".
     */
    constructor(processId) { 
        
        Project.initialize(this, processId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, processId) { 
        obj['process_id'] = processId;
    }

    /**
     * Constructs a <code>Project</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Project} obj Optional instance to populate.
     * @return {module:model/Project} The populated <code>Project</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Project();

            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Date');
            }
            if (data.hasOwnProperty('file_id')) {
                obj['file_id'] = ApiClient.convertToType(data['file_id'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('paypal_payment_id')) {
                obj['paypal_payment_id'] = ApiClient.convertToType(data['paypal_payment_id'], 'String');
            }
            if (data.hasOwnProperty('process')) {
                obj['process'] = Process.constructFromObject(data['process']);
            }
            if (data.hasOwnProperty('process_id')) {
                obj['process_id'] = ApiClient.convertToType(data['process_id'], 'String');
            }
            if (data.hasOwnProperty('project_number')) {
                obj['project_number'] = ApiClient.convertToType(data['project_number'], 'Number');
            }
            if (data.hasOwnProperty('project_title')) {
                obj['project_title'] = ApiClient.convertToType(data['project_title'], 'String');
            }
            if (data.hasOwnProperty('result_file_id')) {
                obj['result_file_id'] = ApiClient.convertToType(data['result_file_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Project</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Project</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Project.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['file_id'] && !(typeof data['file_id'] === 'string' || data['file_id'] instanceof String)) {
            throw new Error("Expected the field `file_id` to be a primitive type in the JSON string but got " + data['file_id']);
        }
        // ensure the json data is a string
        if (data['paypal_payment_id'] && !(typeof data['paypal_payment_id'] === 'string' || data['paypal_payment_id'] instanceof String)) {
            throw new Error("Expected the field `paypal_payment_id` to be a primitive type in the JSON string but got " + data['paypal_payment_id']);
        }
        // validate the optional field `process`
        if (data['process']) { // data not null
          Process.validateJSON(data['process']);
        }
        // ensure the json data is a string
        if (data['process_id'] && !(typeof data['process_id'] === 'string' || data['process_id'] instanceof String)) {
            throw new Error("Expected the field `process_id` to be a primitive type in the JSON string but got " + data['process_id']);
        }
        // ensure the json data is a string
        if (data['project_title'] && !(typeof data['project_title'] === 'string' || data['project_title'] instanceof String)) {
            throw new Error("Expected the field `project_title` to be a primitive type in the JSON string but got " + data['project_title']);
        }
        // ensure the json data is a string
        if (data['result_file_id'] && !(typeof data['result_file_id'] === 'string' || data['result_file_id'] instanceof String)) {
            throw new Error("Expected the field `result_file_id` to be a primitive type in the JSON string but got " + data['result_file_id']);
        }

        return true;
    }


}

Project.RequiredProperties = ["process_id"];

/**
 * The Date when the project was created.
 * @member {Date} created
 */
Project.prototype['created'] = undefined;

/**
 * Id of the file on which the process was applied.
 * @member {String} file_id
 */
Project.prototype['file_id'] = undefined;

/**
 * Id of the created project.
 * @member {Number} id
 */
Project.prototype['id'] = undefined;

/**
 * Id of the paypal payment.
 * @member {String} paypal_payment_id
 */
Project.prototype['paypal_payment_id'] = undefined;

/**
 * @member {module:model/Process} process
 */
Project.prototype['process'] = undefined;

/**
 * Process which should applied. Processes are \"color-classification\".
 * @member {String} process_id
 */
Project.prototype['process_id'] = undefined;

/**
 * Number of the created project.
 * @member {Number} project_number
 */
Project.prototype['project_number'] = undefined;

/**
 * Name of the project with which it was saved.
 * @member {String} project_title
 */
Project.prototype['project_title'] = undefined;

/**
 * Id of the result file which the process created.
 * @member {String} result_file_id
 */
Project.prototype['result_file_id'] = undefined;






export default Project;

