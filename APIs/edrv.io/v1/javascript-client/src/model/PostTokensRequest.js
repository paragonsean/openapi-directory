/**
 * eDRV API
 * edrv.io API Documentation
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@edrv.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PostTokensRequest model module.
 * @module model/PostTokensRequest
 * @version v1
 */
class PostTokensRequest {
    /**
     * Constructs a new <code>PostTokensRequest</code>.
     * @alias module:model/PostTokensRequest
     * @param active {Boolean} 
     * @param channel {module:model/PostTokensRequest.ChannelEnum} 
     * @param driver {String} 
     * @param physicalId {String} 
     */
    constructor(active, channel, driver, physicalId) { 
        
        PostTokensRequest.initialize(this, active, channel, driver, physicalId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, active, channel, driver, physicalId) { 
        obj['active'] = active || true;
        obj['channel'] = channel;
        obj['driver'] = driver;
        obj['physicalId'] = physicalId;
    }

    /**
     * Constructs a <code>PostTokensRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostTokensRequest} obj Optional instance to populate.
     * @return {module:model/PostTokensRequest} The populated <code>PostTokensRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostTokensRequest();

            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('channel')) {
                obj['channel'] = ApiClient.convertToType(data['channel'], 'String');
            }
            if (data.hasOwnProperty('driver')) {
                obj['driver'] = ApiClient.convertToType(data['driver'], 'String');
            }
            if (data.hasOwnProperty('physicalId')) {
                obj['physicalId'] = ApiClient.convertToType(data['physicalId'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostTokensRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostTokensRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostTokensRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['channel'] && !(typeof data['channel'] === 'string' || data['channel'] instanceof String)) {
            throw new Error("Expected the field `channel` to be a primitive type in the JSON string but got " + data['channel']);
        }
        // ensure the json data is a string
        if (data['driver'] && !(typeof data['driver'] === 'string' || data['driver'] instanceof String)) {
            throw new Error("Expected the field `driver` to be a primitive type in the JSON string but got " + data['driver']);
        }
        // ensure the json data is a string
        if (data['physicalId'] && !(typeof data['physicalId'] === 'string' || data['physicalId'] instanceof String)) {
            throw new Error("Expected the field `physicalId` to be a primitive type in the JSON string but got " + data['physicalId']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

PostTokensRequest.RequiredProperties = ["active", "channel", "driver", "physicalId"];

/**
 * @member {Boolean} active
 * @default true
 */
PostTokensRequest.prototype['active'] = true;

/**
 * @member {module:model/PostTokensRequest.ChannelEnum} channel
 */
PostTokensRequest.prototype['channel'] = undefined;

/**
 * @member {String} driver
 */
PostTokensRequest.prototype['driver'] = undefined;

/**
 * @member {String} physicalId
 */
PostTokensRequest.prototype['physicalId'] = undefined;

/**
 * @member {String} type
 */
PostTokensRequest.prototype['type'] = undefined;





/**
 * Allowed values for the <code>channel</code> property.
 * @enum {String}
 * @readonly
 */
PostTokensRequest['ChannelEnum'] = {

    /**
     * value: "physical"
     * @const
     */
    "physical": "physical",

    /**
     * value: "slack"
     * @const
     */
    "slack": "slack",

    /**
     * value: "telegram"
     * @const
     */
    "telegram": "telegram",

    /**
     * value: "sms"
     * @const
     */
    "sms": "sms"
};



export default PostTokensRequest;

