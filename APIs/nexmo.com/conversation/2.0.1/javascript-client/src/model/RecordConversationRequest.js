/**
 * Conversation API
 * The Conversation API enables you to build conversation features where communication can take place across multiple mediums including IP Messaging, PSTN Voice, SMS and WebRTC Audio and Video. The context of the conversations is maintained though each communication event taking place within a conversation, no matter the medium.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Action from './Action';
import Format from './Format';

/**
 * The RecordConversationRequest model module.
 * @module model/RecordConversationRequest
 * @version 2.0.1
 */
class RecordConversationRequest {
    /**
     * Constructs a new <code>RecordConversationRequest</code>.
     * @alias module:model/RecordConversationRequest
     * @param action {module:model/Action} 
     */
    constructor(action) { 
        
        RecordConversationRequest.initialize(this, action);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, action) { 
        obj['action'] = action;
        obj['event_method'] = 'POST';
        obj['split'] = 'conversation';
    }

    /**
     * Constructs a <code>RecordConversationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecordConversationRequest} obj Optional instance to populate.
     * @return {module:model/RecordConversationRequest} The populated <code>RecordConversationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecordConversationRequest();

            if (data.hasOwnProperty('action')) {
                obj['action'] = Action.constructFromObject(data['action']);
            }
            if (data.hasOwnProperty('event_method')) {
                obj['event_method'] = ApiClient.convertToType(data['event_method'], 'String');
            }
            if (data.hasOwnProperty('event_url')) {
                obj['event_url'] = ApiClient.convertToType(data['event_url'], ['String']);
            }
            if (data.hasOwnProperty('format')) {
                obj['format'] = Format.constructFromObject(data['format']);
            }
            if (data.hasOwnProperty('split')) {
                obj['split'] = ApiClient.convertToType(data['split'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecordConversationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecordConversationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecordConversationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['event_method'] && !(typeof data['event_method'] === 'string' || data['event_method'] instanceof String)) {
            throw new Error("Expected the field `event_method` to be a primitive type in the JSON string but got " + data['event_method']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['event_url'])) {
            throw new Error("Expected the field `event_url` to be an array in the JSON data but got " + data['event_url']);
        }
        // ensure the json data is a string
        if (data['split'] && !(typeof data['split'] === 'string' || data['split'] instanceof String)) {
            throw new Error("Expected the field `split` to be a primitive type in the JSON string but got " + data['split']);
        }

        return true;
    }


}

RecordConversationRequest.RequiredProperties = ["action"];

/**
 * @member {module:model/Action} action
 */
RecordConversationRequest.prototype['action'] = undefined;

/**
 * The HTTP method used to send event information to event_url.
 * @member {String} event_method
 * @default 'POST'
 */
RecordConversationRequest.prototype['event_method'] = 'POST';

/**
 * The webhook endpoint where recording progress events are sent to.
 * @member {Array.<String>} event_url
 */
RecordConversationRequest.prototype['event_url'] = undefined;

/**
 * @member {module:model/Format} format
 */
RecordConversationRequest.prototype['format'] = undefined;

/**
 * Record the sent and received audio in separate channels of a stereo recording
 * @member {String} split
 * @default 'conversation'
 */
RecordConversationRequest.prototype['split'] = 'conversation';






export default RecordConversationRequest;

