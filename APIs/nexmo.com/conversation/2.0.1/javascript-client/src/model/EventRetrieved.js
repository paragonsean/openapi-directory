/**
 * Conversation API
 * The Conversation API enables you to build conversation features where communication can take place across multiple mediums including IP Messaging, PSTN Voice, SMS and WebRTC Audio and Video. The context of the conversations is maintained though each communication event taking place within a conversation, no matter the medium.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MemberState from './MemberState';

/**
 * The EventRetrieved model module.
 * @module model/EventRetrieved
 * @version 2.0.1
 */
class EventRetrieved {
    /**
     * Constructs a new <code>EventRetrieved</code>.
     * Retrieve Events Response Payload Object Item
     * @alias module:model/EventRetrieved
     * @param body {Object} Event Body
     * @param href {String} A link towards a conversation event included in Conversation API
     * @param id {String} Event id. This is a progressive integer
     * @param timestamp {String} Time of creation
     * @param type {String} Event type
     */
    constructor(body, href, id, timestamp, type) { 
        
        EventRetrieved.initialize(this, body, href, id, timestamp, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, body, href, id, timestamp, type) { 
        obj['body'] = body;
        obj['href'] = href;
        obj['id'] = id;
        obj['timestamp'] = timestamp;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>EventRetrieved</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventRetrieved} obj Optional instance to populate.
     * @return {module:model/EventRetrieved} The populated <code>EventRetrieved</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventRetrieved();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], Object);
            }
            if (data.hasOwnProperty('from')) {
                obj['from'] = ApiClient.convertToType(data['from'], 'String');
            }
            if (data.hasOwnProperty('href')) {
                obj['href'] = ApiClient.convertToType(data['href'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = MemberState.constructFromObject(data['state']);
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'String');
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ApiClient.convertToType(data['to'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventRetrieved</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventRetrieved</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EventRetrieved.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['from'] && !(typeof data['from'] === 'string' || data['from'] instanceof String)) {
            throw new Error("Expected the field `from` to be a primitive type in the JSON string but got " + data['from']);
        }
        // ensure the json data is a string
        if (data['href'] && !(typeof data['href'] === 'string' || data['href'] instanceof String)) {
            throw new Error("Expected the field `href` to be a primitive type in the JSON string but got " + data['href']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['timestamp'] && !(typeof data['timestamp'] === 'string' || data['timestamp'] instanceof String)) {
            throw new Error("Expected the field `timestamp` to be a primitive type in the JSON string but got " + data['timestamp']);
        }
        // ensure the json data is a string
        if (data['to'] && !(typeof data['to'] === 'string' || data['to'] instanceof String)) {
            throw new Error("Expected the field `to` to be a primitive type in the JSON string but got " + data['to']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

EventRetrieved.RequiredProperties = ["body", "href", "id", "timestamp", "type"];

/**
 * Event Body
 * @member {Object} body
 */
EventRetrieved.prototype['body'] = undefined;

/**
 * Member ID
 * @member {String} from
 */
EventRetrieved.prototype['from'] = undefined;

/**
 * A link towards a conversation event included in Conversation API
 * @member {String} href
 */
EventRetrieved.prototype['href'] = undefined;

/**
 * Event id. This is a progressive integer
 * @member {String} id
 */
EventRetrieved.prototype['id'] = undefined;

/**
 * @member {module:model/MemberState} state
 */
EventRetrieved.prototype['state'] = undefined;

/**
 * Time of creation
 * @member {String} timestamp
 */
EventRetrieved.prototype['timestamp'] = undefined;

/**
 * Member ID
 * @member {String} to
 */
EventRetrieved.prototype['to'] = undefined;

/**
 * Event type
 * @member {String} type
 */
EventRetrieved.prototype['type'] = undefined;






export default EventRetrieved;

