/**
 * Conversation API
 * The Conversation API enables you to build conversation features where communication can take place across multiple mediums including IP Messaging, PSTN Voice, SMS and WebRTC Audio and Video. The context of the conversations is maintained though each communication event taking place within a conversation, no matter the medium.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MemberState from './MemberState';

/**
 * The GetMembers200ResponseInner model module.
 * @module model/GetMembers200ResponseInner
 * @version 2.0.1
 */
class GetMembers200ResponseInner {
    /**
     * Constructs a new <code>GetMembers200ResponseInner</code>.
     * @alias module:model/GetMembers200ResponseInner
     * @param name {String} Unique name for a user
     * @param state {module:model/MemberState} 
     * @param userId {String} User ID
     * @param userName {String} Unique name for a user
     */
    constructor(name, state, userId, userName) { 
        
        GetMembers200ResponseInner.initialize(this, name, state, userId, userName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, state, userId, userName) { 
        obj['name'] = name;
        obj['state'] = state;
        obj['user_id'] = userId;
        obj['user_name'] = userName;
    }

    /**
     * Constructs a <code>GetMembers200ResponseInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetMembers200ResponseInner} obj Optional instance to populate.
     * @return {module:model/GetMembers200ResponseInner} The populated <code>GetMembers200ResponseInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetMembers200ResponseInner();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = MemberState.constructFromObject(data['state']);
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('user_name')) {
                obj['user_name'] = ApiClient.convertToType(data['user_name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetMembers200ResponseInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetMembers200ResponseInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetMembers200ResponseInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['user_name'] && !(typeof data['user_name'] === 'string' || data['user_name'] instanceof String)) {
            throw new Error("Expected the field `user_name` to be a primitive type in the JSON string but got " + data['user_name']);
        }

        return true;
    }


}

GetMembers200ResponseInner.RequiredProperties = ["name", "state", "user_id", "user_name"];

/**
 * Unique name for a user
 * @member {String} name
 */
GetMembers200ResponseInner.prototype['name'] = undefined;

/**
 * @member {module:model/MemberState} state
 */
GetMembers200ResponseInner.prototype['state'] = undefined;

/**
 * User ID
 * @member {String} user_id
 */
GetMembers200ResponseInner.prototype['user_id'] = undefined;

/**
 * Unique name for a user
 * @member {String} user_name
 */
GetMembers200ResponseInner.prototype['user_name'] = undefined;






export default GetMembers200ResponseInner;

