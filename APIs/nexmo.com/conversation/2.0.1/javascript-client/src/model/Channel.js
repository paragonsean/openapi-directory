/**
 * Conversation API
 * The Conversation API enables you to build conversation features where communication can take place across multiple mediums including IP Messaging, PSTN Voice, SMS and WebRTC Audio and Video. The context of the conversations is maintained though each communication event taking place within a conversation, no matter the medium.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChannelFrom from './ChannelFrom';
import ChannelLegIdsInner from './ChannelLegIdsInner';
import ChannelTo from './ChannelTo';
import ChannelType from './ChannelType';

/**
 * The Channel model module.
 * @module model/Channel
 * @version 2.0.1
 */
class Channel {
    /**
     * Constructs a new <code>Channel</code>.
     * A user who joins a conversation as a member can have one channel per membership type. Channels can be &#x60;app&#x60;, &#x60;phone&#x60;, &#x60;sip&#x60;, &#x60;websocket&#x60;, or &#x60;vbc&#x60;
     * @alias module:model/Channel
     */
    constructor() { 
        
        Channel.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Channel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Channel} obj Optional instance to populate.
     * @return {module:model/Channel} The populated <code>Channel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Channel();

            if (data.hasOwnProperty('from')) {
                obj['from'] = ChannelFrom.constructFromObject(data['from']);
            }
            if (data.hasOwnProperty('leg_id')) {
                obj['leg_id'] = ApiClient.convertToType(data['leg_id'], 'String');
            }
            if (data.hasOwnProperty('leg_ids')) {
                obj['leg_ids'] = ApiClient.convertToType(data['leg_ids'], [ChannelLegIdsInner]);
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ChannelTo.constructFromObject(data['to']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ChannelType.constructFromObject(data['type']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Channel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Channel</code>.
     */
    static validateJSON(data) {
        // validate the optional field `from`
        if (data['from']) { // data not null
          ChannelFrom.validateJSON(data['from']);
        }
        // ensure the json data is a string
        if (data['leg_id'] && !(typeof data['leg_id'] === 'string' || data['leg_id'] instanceof String)) {
            throw new Error("Expected the field `leg_id` to be a primitive type in the JSON string but got " + data['leg_id']);
        }
        if (data['leg_ids']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['leg_ids'])) {
                throw new Error("Expected the field `leg_ids` to be an array in the JSON data but got " + data['leg_ids']);
            }
            // validate the optional field `leg_ids` (array)
            for (const item of data['leg_ids']) {
                ChannelLegIdsInner.validateJSON(item);
            };
        }
        // validate the optional field `to`
        if (data['to']) { // data not null
          ChannelTo.validateJSON(data['to']);
        }

        return true;
    }


}



/**
 * @member {module:model/ChannelFrom} from
 */
Channel.prototype['from'] = undefined;

/**
 * The id of the leg. rtc_id and call_id are leg id
 * @member {String} leg_id
 */
Channel.prototype['leg_id'] = undefined;

/**
 * Leg ids associated with this Channel. The first item in the array represents the main active Leg. The second item, if exists, represents a screen-share Leg.
 * @member {Array.<module:model/ChannelLegIdsInner>} leg_ids
 */
Channel.prototype['leg_ids'] = undefined;

/**
 * @member {module:model/ChannelTo} to
 */
Channel.prototype['to'] = undefined;

/**
 * @member {module:model/ChannelType} type
 */
Channel.prototype['type'] = undefined;






export default Channel;

