/**
 * Nexmo Application API
 * <div class=\"Vlt-callout Vlt-callout--critical\"> <i></i> <div class=\"Vlt-callout__content\">   <h4>Applications V1 is deprecated</h4>   This version of the API has been deprecated. Please use <a href=\"/api/application.v2\">version 2</a> going forwards </div> </div> A Nexmo application contains the security and configuration information you need to connect to Nexmo endpoints and easily use our products.
 *
 * The version of the OpenAPI document: 1.0.2
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VoiceWebhooksInner model module.
 * @module model/VoiceWebhooksInner
 * @version 1.0.2
 */
class VoiceWebhooksInner {
    /**
     * Constructs a new <code>VoiceWebhooksInner</code>.
     * @alias module:model/VoiceWebhooksInner
     * @param endpoint {String} `answer_url`: The URL where your webhook delivers the Nexmo Call Control Object that governs this call. As soon as your user answers a call Nexmo makes a request to answer_url. `event_url`: Nexmo sends event information asynchronously to this URL when status changes.
     * @param endpointType {module:model/VoiceWebhooksInner.EndpointTypeEnum} 
     * @param httpMethod {module:model/VoiceWebhooksInner.HttpMethodEnum} The HTTP method used to send event information to the `event_url` or `answer_url`.
     */
    constructor(endpoint, endpointType, httpMethod) { 
        
        VoiceWebhooksInner.initialize(this, endpoint, endpointType, httpMethod);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endpoint, endpointType, httpMethod) { 
        obj['endpoint'] = endpoint;
        obj['endpoint_type'] = endpointType;
        obj['http_method'] = httpMethod;
    }

    /**
     * Constructs a <code>VoiceWebhooksInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VoiceWebhooksInner} obj Optional instance to populate.
     * @return {module:model/VoiceWebhooksInner} The populated <code>VoiceWebhooksInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VoiceWebhooksInner();

            if (data.hasOwnProperty('endpoint')) {
                obj['endpoint'] = ApiClient.convertToType(data['endpoint'], 'String');
            }
            if (data.hasOwnProperty('endpoint_type')) {
                obj['endpoint_type'] = ApiClient.convertToType(data['endpoint_type'], 'String');
            }
            if (data.hasOwnProperty('http_method')) {
                obj['http_method'] = ApiClient.convertToType(data['http_method'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VoiceWebhooksInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VoiceWebhooksInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VoiceWebhooksInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['endpoint'] && !(typeof data['endpoint'] === 'string' || data['endpoint'] instanceof String)) {
            throw new Error("Expected the field `endpoint` to be a primitive type in the JSON string but got " + data['endpoint']);
        }
        // ensure the json data is a string
        if (data['endpoint_type'] && !(typeof data['endpoint_type'] === 'string' || data['endpoint_type'] instanceof String)) {
            throw new Error("Expected the field `endpoint_type` to be a primitive type in the JSON string but got " + data['endpoint_type']);
        }
        // ensure the json data is a string
        if (data['http_method'] && !(typeof data['http_method'] === 'string' || data['http_method'] instanceof String)) {
            throw new Error("Expected the field `http_method` to be a primitive type in the JSON string but got " + data['http_method']);
        }

        return true;
    }


}

VoiceWebhooksInner.RequiredProperties = ["endpoint", "endpoint_type", "http_method"];

/**
 * `answer_url`: The URL where your webhook delivers the Nexmo Call Control Object that governs this call. As soon as your user answers a call Nexmo makes a request to answer_url. `event_url`: Nexmo sends event information asynchronously to this URL when status changes.
 * @member {String} endpoint
 */
VoiceWebhooksInner.prototype['endpoint'] = undefined;

/**
 * @member {module:model/VoiceWebhooksInner.EndpointTypeEnum} endpoint_type
 */
VoiceWebhooksInner.prototype['endpoint_type'] = undefined;

/**
 * The HTTP method used to send event information to the `event_url` or `answer_url`.
 * @member {module:model/VoiceWebhooksInner.HttpMethodEnum} http_method
 */
VoiceWebhooksInner.prototype['http_method'] = undefined;





/**
 * Allowed values for the <code>endpoint_type</code> property.
 * @enum {String}
 * @readonly
 */
VoiceWebhooksInner['EndpointTypeEnum'] = {

    /**
     * value: "answer_url"
     * @const
     */
    "answer_url": "answer_url",

    /**
     * value: "event_url"
     * @const
     */
    "event_url": "event_url"
};


/**
 * Allowed values for the <code>http_method</code> property.
 * @enum {String}
 * @readonly
 */
VoiceWebhooksInner['HttpMethodEnum'] = {

    /**
     * value: "GET"
     * @const
     */
    "GET": "GET",

    /**
     * value: "POST"
     * @const
     */
    "POST": "POST"
};



export default VoiceWebhooksInner;

