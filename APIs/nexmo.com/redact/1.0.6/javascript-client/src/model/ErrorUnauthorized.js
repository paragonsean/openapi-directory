/**
 * Redact API
 * The [Redact API](/redact/overview) helps organisations meet their privacy compliance obligations. It provides controlled, on-demand redaction of private information from transactional records in the long-term storage. Note, Redact API does not have the capability to redact the short-lived server logs that are retained for a few weeks. For SMS customers that need immediate redaction, Vonage suggests using [Advanced Auto-redact](/redact/overview#auto-redact-vs-redact-api).
 *
 * The version of the OpenAPI document: 1.0.6
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ErrorUnauthorized model module.
 * @module model/ErrorUnauthorized
 * @version 1.0.6
 */
class ErrorUnauthorized {
    /**
     * Constructs a new <code>ErrorUnauthorized</code>.
     * @alias module:model/ErrorUnauthorized
     * @param errorTitle {String} Error title
     * @param type {String} Machine readable error type
     */
    constructor(errorTitle, type) { 
        
        ErrorUnauthorized.initialize(this, errorTitle, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, errorTitle, type) { 
        obj['error_title'] = errorTitle;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ErrorUnauthorized</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ErrorUnauthorized} obj Optional instance to populate.
     * @return {module:model/ErrorUnauthorized} The populated <code>ErrorUnauthorized</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ErrorUnauthorized();

            if (data.hasOwnProperty('error_title')) {
                obj['error_title'] = ApiClient.convertToType(data['error_title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ErrorUnauthorized</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ErrorUnauthorized</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ErrorUnauthorized.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['error_title'] && !(typeof data['error_title'] === 'string' || data['error_title'] instanceof String)) {
            throw new Error("Expected the field `error_title` to be a primitive type in the JSON string but got " + data['error_title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ErrorUnauthorized.RequiredProperties = ["error_title", "type"];

/**
 * Error title
 * @member {String} error_title
 */
ErrorUnauthorized.prototype['error_title'] = undefined;

/**
 * Machine readable error type
 * @member {String} type
 */
ErrorUnauthorized.prototype['type'] = undefined;






export default ErrorUnauthorized;

