/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BaseMessageType from './BaseMessageType';
import VCard1Vcard from './VCard1Vcard';

/**
 * The VCard model module.
 * @module model/VCard
 * @version 1.4.0
 */
class VCard {
    /**
     * Constructs a new <code>VCard</code>.
     * @alias module:model/VCard
     * @implements module:model/BaseMessageType
     * @param messageType {module:model/VCard.MessageTypeEnum} The type of message to send. You must provide `vcard` in this field
     * @param vcard {module:model/VCard1Vcard} 
     */
    constructor(messageType, vcard) { 
        BaseMessageType.initialize(this);
        VCard.initialize(this, messageType, vcard);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, messageType, vcard) { 
        obj['message_type'] = messageType;
        obj['vcard'] = vcard;
    }

    /**
     * Constructs a <code>VCard</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VCard} obj Optional instance to populate.
     * @return {module:model/VCard} The populated <code>VCard</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VCard();
            BaseMessageType.constructFromObject(data, obj);

            if (data.hasOwnProperty('client_ref')) {
                obj['client_ref'] = ApiClient.convertToType(data['client_ref'], 'String');
            }
            if (data.hasOwnProperty('message_type')) {
                obj['message_type'] = ApiClient.convertToType(data['message_type'], 'String');
            }
            if (data.hasOwnProperty('vcard')) {
                obj['vcard'] = VCard1Vcard.constructFromObject(data['vcard']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VCard</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VCard</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VCard.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['client_ref'] && !(typeof data['client_ref'] === 'string' || data['client_ref'] instanceof String)) {
            throw new Error("Expected the field `client_ref` to be a primitive type in the JSON string but got " + data['client_ref']);
        }
        // ensure the json data is a string
        if (data['message_type'] && !(typeof data['message_type'] === 'string' || data['message_type'] instanceof String)) {
            throw new Error("Expected the field `message_type` to be a primitive type in the JSON string but got " + data['message_type']);
        }
        // validate the optional field `vcard`
        if (data['vcard']) { // data not null
          VCard1Vcard.validateJSON(data['vcard']);
        }

        return true;
    }


}

VCard.RequiredProperties = ["message_type", "vcard"];

/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
VCard.prototype['client_ref'] = undefined;

/**
 * The type of message to send. You must provide `vcard` in this field
 * @member {module:model/VCard.MessageTypeEnum} message_type
 */
VCard.prototype['message_type'] = undefined;

/**
 * @member {module:model/VCard1Vcard} vcard
 */
VCard.prototype['vcard'] = undefined;


// Implement BaseMessageType interface:
/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
BaseMessageType.prototype['client_ref'] = undefined;



/**
 * Allowed values for the <code>message_type</code> property.
 * @enum {String}
 * @readonly
 */
VCard['MessageTypeEnum'] = {

    /**
     * value: "vcard"
     * @const
     */
    "vcard": "vcard"
};



export default VCard;

