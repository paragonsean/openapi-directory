/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MMSOneOf from './MMSOneOf';
import MMSOneOf1 from './MMSOneOf1';
import MMSOneOf1AllOfVcard from './MMSOneOf1AllOfVcard';
import MMSOneOf2 from './MMSOneOf2';
import MMSOneOf2AllOfAudio from './MMSOneOf2AllOfAudio';
import MMSOneOf3 from './MMSOneOf3';
import MMSOneOf3AllOfVideo from './MMSOneOf3AllOfVideo';
import MMSOneOfAllOfImage from './MMSOneOfAllOfImage';

/**
 * The MMS model module.
 * @module model/MMS
 * @version 1.4.0
 */
class MMS {
    /**
     * Constructs a new <code>MMS</code>.
     * @alias module:model/MMS
     * @param {(module:model/MMSOneOf|module:model/MMSOneOf1|module:model/MMSOneOf2|module:model/MMSOneOf3)} instance The actual instance to initialize MMS.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            if (typeof instance === "MMSOneOf") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                MMSOneOf.validateJSON(instance); // throw an exception if no match
                // create MMSOneOf from JS object
                this.actualInstance = MMSOneOf.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into MMSOneOf
            errorMessages.push("Failed to construct MMSOneOf: " + err)
        }

        try {
            if (typeof instance === "MMSOneOf1") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                MMSOneOf1.validateJSON(instance); // throw an exception if no match
                // create MMSOneOf1 from JS object
                this.actualInstance = MMSOneOf1.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into MMSOneOf1
            errorMessages.push("Failed to construct MMSOneOf1: " + err)
        }

        try {
            if (typeof instance === "MMSOneOf2") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                MMSOneOf2.validateJSON(instance); // throw an exception if no match
                // create MMSOneOf2 from JS object
                this.actualInstance = MMSOneOf2.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into MMSOneOf2
            errorMessages.push("Failed to construct MMSOneOf2: " + err)
        }

        try {
            if (typeof instance === "MMSOneOf3") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                MMSOneOf3.validateJSON(instance); // throw an exception if no match
                // create MMSOneOf3 from JS object
                this.actualInstance = MMSOneOf3.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into MMSOneOf3
            errorMessages.push("Failed to construct MMSOneOf3: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `MMS` with oneOf schemas MMSOneOf, MMSOneOf1, MMSOneOf2, MMSOneOf3. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `MMS` with oneOf schemas MMSOneOf, MMSOneOf1, MMSOneOf2, MMSOneOf3. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>MMS</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MMS} obj Optional instance to populate.
     * @return {module:model/MMS} The populated <code>MMS</code> instance.
     */
    static constructFromObject(data, obj) {
        return new MMS(data);
    }

    /**
     * Gets the actual instance, which can be <code>MMSOneOf</code>, <code>MMSOneOf1</code>, <code>MMSOneOf2</code>, <code>MMSOneOf3</code>.
     * @return {(module:model/MMSOneOf|module:model/MMSOneOf1|module:model/MMSOneOf2|module:model/MMSOneOf3)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>MMSOneOf</code>, <code>MMSOneOf1</code>, <code>MMSOneOf2</code>, <code>MMSOneOf3</code>.
     * @param {(module:model/MMSOneOf|module:model/MMSOneOf1|module:model/MMSOneOf2|module:model/MMSOneOf3)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = MMS.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of MMS from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:model/MMS} An instance of MMS.
     */
    static fromJSON = function(json_string){
        return MMS.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
MMS.prototype['client_ref'] = undefined;

/**
 * @member {module:model/MMSOneOfAllOfImage} image
 */
MMS.prototype['image'] = undefined;

/**
 * The type of message to send. You must provide `video` in this field.  For best device and network support .mp4 is recommended. Not supported for US short codes. 
 * @member {module:model/MMS.MessageTypeEnum} message_type
 */
MMS.prototype['message_type'] = undefined;

/**
 * The channel to send to. You must provide `mms` in this field
 * @member {module:model/MMS.ChannelEnum} channel
 */
MMS.prototype['channel'] = undefined;

/**
 * The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
 * @member {String} from
 */
MMS.prototype['from'] = undefined;

/**
 * The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
 * @member {String} to
 */
MMS.prototype['to'] = undefined;

/**
 * @member {module:model/MMSOneOf1AllOfVcard} vcard
 */
MMS.prototype['vcard'] = undefined;

/**
 * @member {module:model/MMSOneOf2AllOfAudio} audio
 */
MMS.prototype['audio'] = undefined;

/**
 * @member {module:model/MMSOneOf3AllOfVideo} video
 */
MMS.prototype['video'] = undefined;


MMS.OneOf = ["MMSOneOf", "MMSOneOf1", "MMSOneOf2", "MMSOneOf3"];

export default MMS;

