/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Order1OrderProductItems from './Order1OrderProductItems';

/**
 * The Order1Order model module.
 * @module model/Order1Order
 * @version 1.4.0
 */
class Order1Order {
    /**
     * Constructs a new <code>Order1Order</code>.
     * @alias module:model/Order1Order
     * @param catalogId {String} The ID of the catalog containing the products in this order.
     * @param productItems {Array.<module:model/Order1OrderProductItems>} 
     */
    constructor(catalogId, productItems) { 
        
        Order1Order.initialize(this, catalogId, productItems);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, catalogId, productItems) { 
        obj['catalog_id'] = catalogId;
        obj['product_items'] = productItems;
    }

    /**
     * Constructs a <code>Order1Order</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Order1Order} obj Optional instance to populate.
     * @return {module:model/Order1Order} The populated <code>Order1Order</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Order1Order();

            if (data.hasOwnProperty('catalog_id')) {
                obj['catalog_id'] = ApiClient.convertToType(data['catalog_id'], 'String');
            }
            if (data.hasOwnProperty('product_items')) {
                obj['product_items'] = ApiClient.convertToType(data['product_items'], [Order1OrderProductItems]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Order1Order</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Order1Order</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Order1Order.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['catalog_id'] && !(typeof data['catalog_id'] === 'string' || data['catalog_id'] instanceof String)) {
            throw new Error("Expected the field `catalog_id` to be a primitive type in the JSON string but got " + data['catalog_id']);
        }
        if (data['product_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['product_items'])) {
                throw new Error("Expected the field `product_items` to be an array in the JSON data but got " + data['product_items']);
            }
            // validate the optional field `product_items` (array)
            for (const item of data['product_items']) {
                Order1OrderProductItems.validateJSON(item);
            };
        }

        return true;
    }


}

Order1Order.RequiredProperties = ["catalog_id", "product_items"];

/**
 * The ID of the catalog containing the products in this order.
 * @member {String} catalog_id
 */
Order1Order.prototype['catalog_id'] = undefined;

/**
 * @member {Array.<module:model/Order1OrderProductItems>} product_items
 */
Order1Order.prototype['product_items'] = undefined;






export default Order1Order;

