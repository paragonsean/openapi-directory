/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import InboundMessageSMSSms from './InboundMessageSMSSms';
import Usage from './Usage';

/**
 * The InboundMessageSMS model module.
 * @module model/InboundMessageSMS
 * @version 1.4.0
 */
class InboundMessageSMS {
    /**
     * Constructs a new <code>InboundMessageSMS</code>.
     * @alias module:model/InboundMessageSMS
     * @param channel {module:model/InboundMessageSMS.ChannelEnum} The channel the message came in on
     * @param from {String} The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
     * @param messageUuid {String} The UUID of the message
     * @param text {String} The UTF-8 encoded text of the inbound message. 
     * @param timestamp {String} The datetime of when the event occurred, in `ISO 8601` format.
     * @param to {String} The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
     */
    constructor(channel, from, messageUuid, text, timestamp, to) { 
        
        InboundMessageSMS.initialize(this, channel, from, messageUuid, text, timestamp, to);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channel, from, messageUuid, text, timestamp, to) { 
        obj['channel'] = channel;
        obj['from'] = from;
        obj['message_uuid'] = messageUuid;
        obj['text'] = text;
        obj['timestamp'] = timestamp;
        obj['to'] = to;
    }

    /**
     * Constructs a <code>InboundMessageSMS</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InboundMessageSMS} obj Optional instance to populate.
     * @return {module:model/InboundMessageSMS} The populated <code>InboundMessageSMS</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InboundMessageSMS();

            if (data.hasOwnProperty('channel')) {
                obj['channel'] = ApiClient.convertToType(data['channel'], 'String');
            }
            if (data.hasOwnProperty('from')) {
                obj['from'] = ApiClient.convertToType(data['from'], 'String');
            }
            if (data.hasOwnProperty('message_uuid')) {
                obj['message_uuid'] = ApiClient.convertToType(data['message_uuid'], 'String');
            }
            if (data.hasOwnProperty('sms')) {
                obj['sms'] = InboundMessageSMSSms.constructFromObject(data['sms']);
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'String');
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ApiClient.convertToType(data['to'], 'String');
            }
            if (data.hasOwnProperty('usage')) {
                obj['usage'] = Usage.constructFromObject(data['usage']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InboundMessageSMS</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InboundMessageSMS</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InboundMessageSMS.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['channel'] && !(typeof data['channel'] === 'string' || data['channel'] instanceof String)) {
            throw new Error("Expected the field `channel` to be a primitive type in the JSON string but got " + data['channel']);
        }
        // ensure the json data is a string
        if (data['from'] && !(typeof data['from'] === 'string' || data['from'] instanceof String)) {
            throw new Error("Expected the field `from` to be a primitive type in the JSON string but got " + data['from']);
        }
        // ensure the json data is a string
        if (data['message_uuid'] && !(typeof data['message_uuid'] === 'string' || data['message_uuid'] instanceof String)) {
            throw new Error("Expected the field `message_uuid` to be a primitive type in the JSON string but got " + data['message_uuid']);
        }
        // validate the optional field `sms`
        if (data['sms']) { // data not null
          InboundMessageSMSSms.validateJSON(data['sms']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        // ensure the json data is a string
        if (data['timestamp'] && !(typeof data['timestamp'] === 'string' || data['timestamp'] instanceof String)) {
            throw new Error("Expected the field `timestamp` to be a primitive type in the JSON string but got " + data['timestamp']);
        }
        // ensure the json data is a string
        if (data['to'] && !(typeof data['to'] === 'string' || data['to'] instanceof String)) {
            throw new Error("Expected the field `to` to be a primitive type in the JSON string but got " + data['to']);
        }
        // validate the optional field `usage`
        if (data['usage']) { // data not null
          Usage.validateJSON(data['usage']);
        }

        return true;
    }


}

InboundMessageSMS.RequiredProperties = ["channel", "from", "message_uuid", "text", "timestamp", "to"];

/**
 * The channel the message came in on
 * @member {module:model/InboundMessageSMS.ChannelEnum} channel
 */
InboundMessageSMS.prototype['channel'] = undefined;

/**
 * The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
 * @member {String} from
 */
InboundMessageSMS.prototype['from'] = undefined;

/**
 * The UUID of the message
 * @member {String} message_uuid
 */
InboundMessageSMS.prototype['message_uuid'] = undefined;

/**
 * @member {module:model/InboundMessageSMSSms} sms
 */
InboundMessageSMS.prototype['sms'] = undefined;

/**
 * The UTF-8 encoded text of the inbound message. 
 * @member {String} text
 */
InboundMessageSMS.prototype['text'] = undefined;

/**
 * The datetime of when the event occurred, in `ISO 8601` format.
 * @member {String} timestamp
 */
InboundMessageSMS.prototype['timestamp'] = undefined;

/**
 * The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
 * @member {String} to
 */
InboundMessageSMS.prototype['to'] = undefined;

/**
 * @member {module:model/Usage} usage
 */
InboundMessageSMS.prototype['usage'] = undefined;





/**
 * Allowed values for the <code>channel</code> property.
 * @enum {String}
 * @readonly
 */
InboundMessageSMS['ChannelEnum'] = {

    /**
     * value: "sms"
     * @const
     */
    "sms": "sms"
};



export default InboundMessageSMS;

