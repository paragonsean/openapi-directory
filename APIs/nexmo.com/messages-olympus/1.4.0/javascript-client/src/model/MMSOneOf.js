/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChannelOptionsMms from './ChannelOptionsMms';
import Image from './Image';
import MMSOneOfAllOfImage from './MMSOneOfAllOfImage';

/**
 * The MMSOneOf model module.
 * @module model/MMSOneOf
 * @version 1.4.0
 */
class MMSOneOf {
    /**
     * Constructs a new <code>MMSOneOf</code>.
     * @alias module:model/MMSOneOf
     * @implements module:model/Image
     * @implements module:model/ChannelOptionsMms
     * @param image {module:model/MMSOneOfAllOfImage} 
     * @param messageType {module:model/MMSOneOf.MessageTypeEnum} The type of message to send. You must provide `image` in this field
     * @param channel {module:model/MMSOneOf.ChannelEnum} The channel to send to. You must provide `mms` in this field
     * @param from {String} The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
     * @param to {String} The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
     */
    constructor(image, messageType, channel, from, to) { 
        Image.initialize(this, image, messageType);ChannelOptionsMms.initialize(this);
        MMSOneOf.initialize(this, image, messageType, channel, from, to);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, image, messageType, channel, from, to) { 
        obj['image'] = image;
        obj['message_type'] = messageType;
        obj['channel'] = channel;
        obj['from'] = from;
        obj['to'] = to;
    }

    /**
     * Constructs a <code>MMSOneOf</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MMSOneOf} obj Optional instance to populate.
     * @return {module:model/MMSOneOf} The populated <code>MMSOneOf</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MMSOneOf();
            Image.constructFromObject(data, obj);
            ChannelOptionsMms.constructFromObject(data, obj);

            if (data.hasOwnProperty('client_ref')) {
                obj['client_ref'] = ApiClient.convertToType(data['client_ref'], 'String');
            }
            if (data.hasOwnProperty('image')) {
                obj['image'] = MMSOneOfAllOfImage.constructFromObject(data['image']);
            }
            if (data.hasOwnProperty('message_type')) {
                obj['message_type'] = ApiClient.convertToType(data['message_type'], 'String');
            }
            if (data.hasOwnProperty('channel')) {
                obj['channel'] = ApiClient.convertToType(data['channel'], 'String');
            }
            if (data.hasOwnProperty('from')) {
                obj['from'] = ApiClient.convertToType(data['from'], 'String');
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ApiClient.convertToType(data['to'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MMSOneOf</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MMSOneOf</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MMSOneOf.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['client_ref'] && !(typeof data['client_ref'] === 'string' || data['client_ref'] instanceof String)) {
            throw new Error("Expected the field `client_ref` to be a primitive type in the JSON string but got " + data['client_ref']);
        }
        // validate the optional field `image`
        if (data['image']) { // data not null
          MMSOneOfAllOfImage.validateJSON(data['image']);
        }
        // ensure the json data is a string
        if (data['message_type'] && !(typeof data['message_type'] === 'string' || data['message_type'] instanceof String)) {
            throw new Error("Expected the field `message_type` to be a primitive type in the JSON string but got " + data['message_type']);
        }
        // ensure the json data is a string
        if (data['channel'] && !(typeof data['channel'] === 'string' || data['channel'] instanceof String)) {
            throw new Error("Expected the field `channel` to be a primitive type in the JSON string but got " + data['channel']);
        }
        // ensure the json data is a string
        if (data['from'] && !(typeof data['from'] === 'string' || data['from'] instanceof String)) {
            throw new Error("Expected the field `from` to be a primitive type in the JSON string but got " + data['from']);
        }
        // ensure the json data is a string
        if (data['to'] && !(typeof data['to'] === 'string' || data['to'] instanceof String)) {
            throw new Error("Expected the field `to` to be a primitive type in the JSON string but got " + data['to']);
        }

        return true;
    }


}

MMSOneOf.RequiredProperties = ["image", "message_type", "channel", "from", "to"];

/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
MMSOneOf.prototype['client_ref'] = undefined;

/**
 * @member {module:model/MMSOneOfAllOfImage} image
 */
MMSOneOf.prototype['image'] = undefined;

/**
 * The type of message to send. You must provide `image` in this field
 * @member {module:model/MMSOneOf.MessageTypeEnum} message_type
 */
MMSOneOf.prototype['message_type'] = undefined;

/**
 * The channel to send to. You must provide `mms` in this field
 * @member {module:model/MMSOneOf.ChannelEnum} channel
 */
MMSOneOf.prototype['channel'] = undefined;

/**
 * The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
 * @member {String} from
 */
MMSOneOf.prototype['from'] = undefined;

/**
 * The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
 * @member {String} to
 */
MMSOneOf.prototype['to'] = undefined;


// Implement Image interface:
/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
Image.prototype['client_ref'] = undefined;
/**
 * @member {module:model/Image1Image} image
 */
Image.prototype['image'] = undefined;
/**
 * The type of message to send. You must provide `image` in this field
 * @member {module:model/Image.MessageTypeEnum} message_type
 */
Image.prototype['message_type'] = undefined;
// Implement ChannelOptionsMms interface:
/**
 * The channel to send to. You must provide `mms` in this field
 * @member {module:model/ChannelOptionsMms.ChannelEnum} channel
 */
ChannelOptionsMms.prototype['channel'] = undefined;
/**
 * The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
 * @member {String} from
 */
ChannelOptionsMms.prototype['from'] = undefined;
/**
 * The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
 * @member {String} to
 */
ChannelOptionsMms.prototype['to'] = undefined;



/**
 * Allowed values for the <code>message_type</code> property.
 * @enum {String}
 * @readonly
 */
MMSOneOf['MessageTypeEnum'] = {

    /**
     * value: "image"
     * @const
     */
    "image": "image"
};


/**
 * Allowed values for the <code>channel</code> property.
 * @enum {String}
 * @readonly
 */
MMSOneOf['ChannelEnum'] = {

    /**
     * value: "mms"
     * @const
     */
    "mms": "mms"
};



export default MMSOneOf;

