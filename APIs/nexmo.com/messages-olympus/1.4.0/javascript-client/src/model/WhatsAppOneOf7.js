/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChannelOptionsWhatsapp from './ChannelOptionsWhatsapp';
import Custom from './Custom';

/**
 * The WhatsAppOneOf7 model module.
 * @module model/WhatsAppOneOf7
 * @version 1.4.0
 */
class WhatsAppOneOf7 {
    /**
     * Constructs a new <code>WhatsAppOneOf7</code>.
     * @alias module:model/WhatsAppOneOf7
     * @implements module:model/Custom
     * @implements module:model/ChannelOptionsWhatsapp
     * @param messageType {module:model/WhatsAppOneOf7.MessageTypeEnum} The type of message to send. You must provide `custom` in this field
     * @param channel {module:model/WhatsAppOneOf7.ChannelEnum} The channel to send to. You must provide `whatsapp` in this field
     * @param from {String} The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
     * @param to {String} The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
     */
    constructor(messageType, channel, from, to) { 
        Custom.initialize(this, messageType);ChannelOptionsWhatsapp.initialize(this);
        WhatsAppOneOf7.initialize(this, messageType, channel, from, to);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, messageType, channel, from, to) { 
        obj['message_type'] = messageType;
        obj['channel'] = channel;
        obj['from'] = from;
        obj['to'] = to;
    }

    /**
     * Constructs a <code>WhatsAppOneOf7</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WhatsAppOneOf7} obj Optional instance to populate.
     * @return {module:model/WhatsAppOneOf7} The populated <code>WhatsAppOneOf7</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WhatsAppOneOf7();
            Custom.constructFromObject(data, obj);
            ChannelOptionsWhatsapp.constructFromObject(data, obj);

            if (data.hasOwnProperty('client_ref')) {
                obj['client_ref'] = ApiClient.convertToType(data['client_ref'], 'String');
            }
            if (data.hasOwnProperty('custom')) {
                obj['custom'] = ApiClient.convertToType(data['custom'], {'String': Object});
            }
            if (data.hasOwnProperty('message_type')) {
                obj['message_type'] = ApiClient.convertToType(data['message_type'], 'String');
            }
            if (data.hasOwnProperty('channel')) {
                obj['channel'] = ApiClient.convertToType(data['channel'], 'String');
            }
            if (data.hasOwnProperty('from')) {
                obj['from'] = ApiClient.convertToType(data['from'], 'String');
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ApiClient.convertToType(data['to'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WhatsAppOneOf7</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WhatsAppOneOf7</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WhatsAppOneOf7.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['client_ref'] && !(typeof data['client_ref'] === 'string' || data['client_ref'] instanceof String)) {
            throw new Error("Expected the field `client_ref` to be a primitive type in the JSON string but got " + data['client_ref']);
        }
        // ensure the json data is a string
        if (data['message_type'] && !(typeof data['message_type'] === 'string' || data['message_type'] instanceof String)) {
            throw new Error("Expected the field `message_type` to be a primitive type in the JSON string but got " + data['message_type']);
        }
        // ensure the json data is a string
        if (data['channel'] && !(typeof data['channel'] === 'string' || data['channel'] instanceof String)) {
            throw new Error("Expected the field `channel` to be a primitive type in the JSON string but got " + data['channel']);
        }
        // ensure the json data is a string
        if (data['from'] && !(typeof data['from'] === 'string' || data['from'] instanceof String)) {
            throw new Error("Expected the field `from` to be a primitive type in the JSON string but got " + data['from']);
        }
        // ensure the json data is a string
        if (data['to'] && !(typeof data['to'] === 'string' || data['to'] instanceof String)) {
            throw new Error("Expected the field `to` to be a primitive type in the JSON string but got " + data['to']);
        }

        return true;
    }


}

WhatsAppOneOf7.RequiredProperties = ["message_type", "channel", "from", "to"];

/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
WhatsAppOneOf7.prototype['client_ref'] = undefined;

/**
 * A custom payload, which is passed directly to WhatsApp for certain features such as templates and interactive messages. The schema of a custom object can vary widely. [Read more about Custom Objects](https://developer.vonage.com/messages/concepts/custom-objects).
 * @member {Object.<String, Object>} custom
 */
WhatsAppOneOf7.prototype['custom'] = undefined;

/**
 * The type of message to send. You must provide `custom` in this field
 * @member {module:model/WhatsAppOneOf7.MessageTypeEnum} message_type
 */
WhatsAppOneOf7.prototype['message_type'] = undefined;

/**
 * The channel to send to. You must provide `whatsapp` in this field
 * @member {module:model/WhatsAppOneOf7.ChannelEnum} channel
 */
WhatsAppOneOf7.prototype['channel'] = undefined;

/**
 * The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
 * @member {String} from
 */
WhatsAppOneOf7.prototype['from'] = undefined;

/**
 * The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
 * @member {String} to
 */
WhatsAppOneOf7.prototype['to'] = undefined;


// Implement Custom interface:
/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
Custom.prototype['client_ref'] = undefined;
/**
 * A custom payload, which is passed directly to WhatsApp for certain features such as templates and interactive messages. The schema of a custom object can vary widely. [Read more about Custom Objects](https://developer.vonage.com/messages/concepts/custom-objects).
 * @member {Object.<String, Object>} custom
 */
Custom.prototype['custom'] = undefined;
/**
 * The type of message to send. You must provide `custom` in this field
 * @member {module:model/Custom.MessageTypeEnum} message_type
 */
Custom.prototype['message_type'] = undefined;
// Implement ChannelOptionsWhatsapp interface:
/**
 * The channel to send to. You must provide `whatsapp` in this field
 * @member {module:model/ChannelOptionsWhatsapp.ChannelEnum} channel
 */
ChannelOptionsWhatsapp.prototype['channel'] = undefined;
/**
 * The phone number of the message **sender** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. For SMS in certain localities alpha-numeric sender id's will work as well, see [Global Messaging](https://developer.nexmo.com/messaging/sms/guides/country-specific-features#country-specific-features) for more details 
 * @member {String} from
 */
ChannelOptionsWhatsapp.prototype['from'] = undefined;
/**
 * The phone number of the message **recipient** in the [E.164](https://en.wikipedia.org/wiki/E.164) format. Don't use a leading + or 00 when entering a phone number, start with the country code, for example, 447700900000. 
 * @member {String} to
 */
ChannelOptionsWhatsapp.prototype['to'] = undefined;



/**
 * Allowed values for the <code>message_type</code> property.
 * @enum {String}
 * @readonly
 */
WhatsAppOneOf7['MessageTypeEnum'] = {

    /**
     * value: "custom"
     * @const
     */
    "custom": "custom"
};


/**
 * Allowed values for the <code>channel</code> property.
 * @enum {String}
 * @readonly
 */
WhatsAppOneOf7['ChannelEnum'] = {

    /**
     * value: "whatsapp"
     * @const
     */
    "whatsapp": "whatsapp"
};



export default WhatsAppOneOf7;

