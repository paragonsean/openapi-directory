/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BaseMessageType from './BaseMessageType';
import Reply1Reply from './Reply1Reply';

/**
 * The Reply model module.
 * @module model/Reply
 * @version 1.4.0
 */
class Reply {
    /**
     * Constructs a new <code>Reply</code>.
     * @alias module:model/Reply
     * @implements module:model/BaseMessageType
     * @param messageType {module:model/Reply.MessageTypeEnum} The type of message to send. You must provide `custom` in this field.
     * @param reply {module:model/Reply1Reply} 
     */
    constructor(messageType, reply) { 
        BaseMessageType.initialize(this);
        Reply.initialize(this, messageType, reply);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, messageType, reply) { 
        obj['message_type'] = messageType;
        obj['reply'] = reply;
    }

    /**
     * Constructs a <code>Reply</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Reply} obj Optional instance to populate.
     * @return {module:model/Reply} The populated <code>Reply</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Reply();
            BaseMessageType.constructFromObject(data, obj);

            if (data.hasOwnProperty('client_ref')) {
                obj['client_ref'] = ApiClient.convertToType(data['client_ref'], 'String');
            }
            if (data.hasOwnProperty('message_type')) {
                obj['message_type'] = ApiClient.convertToType(data['message_type'], 'String');
            }
            if (data.hasOwnProperty('reply')) {
                obj['reply'] = Reply1Reply.constructFromObject(data['reply']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Reply</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Reply</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Reply.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['client_ref'] && !(typeof data['client_ref'] === 'string' || data['client_ref'] instanceof String)) {
            throw new Error("Expected the field `client_ref` to be a primitive type in the JSON string but got " + data['client_ref']);
        }
        // ensure the json data is a string
        if (data['message_type'] && !(typeof data['message_type'] === 'string' || data['message_type'] instanceof String)) {
            throw new Error("Expected the field `message_type` to be a primitive type in the JSON string but got " + data['message_type']);
        }
        // validate the optional field `reply`
        if (data['reply']) { // data not null
          Reply1Reply.validateJSON(data['reply']);
        }

        return true;
    }


}

Reply.RequiredProperties = ["message_type", "reply"];

/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
Reply.prototype['client_ref'] = undefined;

/**
 * The type of message to send. You must provide `custom` in this field.
 * @member {module:model/Reply.MessageTypeEnum} message_type
 */
Reply.prototype['message_type'] = undefined;

/**
 * @member {module:model/Reply1Reply} reply
 */
Reply.prototype['reply'] = undefined;


// Implement BaseMessageType interface:
/**
 * Client reference of up to 100 characters. The reference will be present in every message status.
 * @member {String} client_ref
 */
BaseMessageType.prototype['client_ref'] = undefined;



/**
 * Allowed values for the <code>message_type</code> property.
 * @enum {String}
 * @readonly
 */
Reply['MessageTypeEnum'] = {

    /**
     * value: "reply"
     * @const
     */
    "reply": "reply"
};



export default Reply;

