/**
 * Messages API
 * The Messages API consolidates and normalises exchanges across all messaging channels. It allows you to use a single API to interact with our various channels such as SMS, MMS, WhatsApp, Viber and Facebook Messenger
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: devrel@nexmo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ChannelOptionsViberVideoViberService model module.
 * @module model/ChannelOptionsViberVideoViberService
 * @version 1.4.0
 */
class ChannelOptionsViberVideoViberService {
    /**
     * Constructs a new <code>ChannelOptionsViberVideoViberService</code>.
     * @alias module:model/ChannelOptionsViberVideoViberService
     */
    constructor() { 
        
        ChannelOptionsViberVideoViberService.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ChannelOptionsViberVideoViberService</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChannelOptionsViberVideoViberService} obj Optional instance to populate.
     * @return {module:model/ChannelOptionsViberVideoViberService} The populated <code>ChannelOptionsViberVideoViberService</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChannelOptionsViberVideoViberService();

            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'String');
            }
            if (data.hasOwnProperty('file_size')) {
                obj['file_size'] = ApiClient.convertToType(data['file_size'], 'String');
            }
            if (data.hasOwnProperty('ttl')) {
                obj['ttl'] = ApiClient.convertToType(data['ttl'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChannelOptionsViberVideoViberService</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChannelOptionsViberVideoViberService</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['category'] && !(typeof data['category'] === 'string' || data['category'] instanceof String)) {
            throw new Error("Expected the field `category` to be a primitive type in the JSON string but got " + data['category']);
        }
        // ensure the json data is a string
        if (data['duration'] && !(typeof data['duration'] === 'string' || data['duration'] instanceof String)) {
            throw new Error("Expected the field `duration` to be a primitive type in the JSON string but got " + data['duration']);
        }
        // ensure the json data is a string
        if (data['file_size'] && !(typeof data['file_size'] === 'string' || data['file_size'] instanceof String)) {
            throw new Error("Expected the field `file_size` to be a primitive type in the JSON string but got " + data['file_size']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * The use of different category tags enables the business to send messages for different use cases. For Viber Business Messages the first message sent from a business to a user must be personal, informative & a targeted message - not promotional. By default Vonage sends the `transaction` category to Viber Business Messages.
 * @member {module:model/ChannelOptionsViberVideoViberService.CategoryEnum} category
 */
ChannelOptionsViberVideoViberService.prototype['category'] = undefined;

/**
 * The duration of the video in seconds.
 * @member {String} duration
 */
ChannelOptionsViberVideoViberService.prototype['duration'] = undefined;

/**
 * The file size of the video in MB.
 * @member {String} file_size
 */
ChannelOptionsViberVideoViberService.prototype['file_size'] = undefined;

/**
 * Set the time-to-live of message to be delivered in seconds. i.e. if the message is not delivered in 600 seconds then delete the message.
 * @member {Number} ttl
 */
ChannelOptionsViberVideoViberService.prototype['ttl'] = undefined;

/**
 * Viber-specific type definition. To use \"template\", please contact your Vonage Account Manager to setup your templates. To find out more please visit the [product page](https://www.vonage.com/communications-apis/messages/)
 * @member {String} type
 */
ChannelOptionsViberVideoViberService.prototype['type'] = undefined;





/**
 * Allowed values for the <code>category</code> property.
 * @enum {String}
 * @readonly
 */
ChannelOptionsViberVideoViberService['CategoryEnum'] = {

    /**
     * value: "transaction"
     * @const
     */
    "transaction": "transaction",

    /**
     * value: "promotion"
     * @const
     */
    "promotion": "promotion"
};



export default ChannelOptionsViberVideoViberService;

