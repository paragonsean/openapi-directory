/**
 * Dispatch API
 * The Dispatch API enables the developer to specify a multiple message workflow. A workflow follows a template. The first one we are adding is the failover template. The failover template instructs the Messages API to first send a message to the specified channel. If that message fails immediately or if the condition_status is not reached within the given time period the next message is sent. The developer will also receive status webhooks from the messages resource for each delivery and read event. This API is currently in Beta.
 *
 * The version of the OpenAPI document: 0.3.4
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AudioProperty from './AudioProperty';
import FileProperty from './FileProperty';
import ImageProperty from './ImageProperty';
import TemplateProperty from './TemplateProperty';
import VideoProperty from './VideoProperty';

/**
 * The MessagePropertyContent model module.
 * @module model/MessagePropertyContent
 * @version 0.3.4
 */
class MessagePropertyContent {
    /**
     * Constructs a new <code>MessagePropertyContent</code>.
     * @alias module:model/MessagePropertyContent
     * @param type {module:model/MessagePropertyContent.TypeEnum} The type of message that you are sending.  **Messenger**: supports `text`, `image`, `audio`, `video` and `file`.  **Viber Business Messages**: supports `image` and `text`.  **WhatsApp**: supports `template`, `text`, `image`, `audio`, `video` and `file`.  **SMS**: supports `text`. 
     */
    constructor(type) { 
        
        MessagePropertyContent.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['type'] = type;
    }

    /**
     * Constructs a <code>MessagePropertyContent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MessagePropertyContent} obj Optional instance to populate.
     * @return {module:model/MessagePropertyContent} The populated <code>MessagePropertyContent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MessagePropertyContent();

            if (data.hasOwnProperty('audio')) {
                obj['audio'] = AudioProperty.constructFromObject(data['audio']);
            }
            if (data.hasOwnProperty('file')) {
                obj['file'] = FileProperty.constructFromObject(data['file']);
            }
            if (data.hasOwnProperty('image')) {
                obj['image'] = ImageProperty.constructFromObject(data['image']);
            }
            if (data.hasOwnProperty('template')) {
                obj['template'] = TemplateProperty.constructFromObject(data['template']);
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('video')) {
                obj['video'] = VideoProperty.constructFromObject(data['video']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MessagePropertyContent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MessagePropertyContent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MessagePropertyContent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `audio`
        if (data['audio']) { // data not null
          AudioProperty.validateJSON(data['audio']);
        }
        // validate the optional field `file`
        if (data['file']) { // data not null
          FileProperty.validateJSON(data['file']);
        }
        // validate the optional field `image`
        if (data['image']) { // data not null
          ImageProperty.validateJSON(data['image']);
        }
        // validate the optional field `template`
        if (data['template']) { // data not null
          TemplateProperty.validateJSON(data['template']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // validate the optional field `video`
        if (data['video']) { // data not null
          VideoProperty.validateJSON(data['video']);
        }

        return true;
    }


}

MessagePropertyContent.RequiredProperties = ["type"];

/**
 * @member {module:model/AudioProperty} audio
 */
MessagePropertyContent.prototype['audio'] = undefined;

/**
 * @member {module:model/FileProperty} file
 */
MessagePropertyContent.prototype['file'] = undefined;

/**
 * @member {module:model/ImageProperty} image
 */
MessagePropertyContent.prototype['image'] = undefined;

/**
 * @member {module:model/TemplateProperty} template
 */
MessagePropertyContent.prototype['template'] = undefined;

/**
 * The text of the message.  **Messenger**: Is limited to 640 characters  **SMS** or **Viber**: Is 1000 characters  **WhatsApp**: is 4096 characters 
 * @member {String} text
 */
MessagePropertyContent.prototype['text'] = undefined;

/**
 * The type of message that you are sending.  **Messenger**: supports `text`, `image`, `audio`, `video` and `file`.  **Viber Business Messages**: supports `image` and `text`.  **WhatsApp**: supports `template`, `text`, `image`, `audio`, `video` and `file`.  **SMS**: supports `text`. 
 * @member {module:model/MessagePropertyContent.TypeEnum} type
 */
MessagePropertyContent.prototype['type'] = undefined;

/**
 * @member {module:model/VideoProperty} video
 */
MessagePropertyContent.prototype['video'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
MessagePropertyContent['TypeEnum'] = {

    /**
     * value: "text"
     * @const
     */
    "text": "text",

    /**
     * value: "image"
     * @const
     */
    "image": "image",

    /**
     * value: "audio"
     * @const
     */
    "audio": "audio",

    /**
     * value: "video"
     * @const
     */
    "video": "video",

    /**
     * value: "file"
     * @const
     */
    "file": "file",

    /**
     * value: "template"
     * @const
     */
    "template": "template",

    /**
     * value: "custom"
     * @const
     */
    "custom": "custom"
};



export default MessagePropertyContent;

