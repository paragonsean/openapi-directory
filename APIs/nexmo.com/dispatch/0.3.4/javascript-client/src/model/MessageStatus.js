/**
 * Dispatch API
 * The Dispatch API enables the developer to specify a multiple message workflow. A workflow follows a template. The first one we are adding is the failover template. The failover template instructs the Messages API to first send a message to the specified channel. If that message fails immediately or if the condition_status is not reached within the given time period the next message is sent. The developer will also receive status webhooks from the messages resource for each delivery and read event. This API is currently in Beta.
 *
 * The version of the OpenAPI document: 0.3.4
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FromProperty from './FromProperty';
import MessageStatusError from './MessageStatusError';
import MessageStatusLinks from './MessageStatusLinks';
import MessageStatusUsage from './MessageStatusUsage';
import ToProperty from './ToProperty';

/**
 * The MessageStatus model module.
 * @module model/MessageStatus
 * @version 0.3.4
 */
class MessageStatus {
    /**
     * Constructs a new <code>MessageStatus</code>.
     * The callbacks for the message status are the same as defined in the Messaging API. The only difference will be that dispatch_uuid and link will be appended.
     * @alias module:model/MessageStatus
     */
    constructor() { 
        
        MessageStatus.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>MessageStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MessageStatus} obj Optional instance to populate.
     * @return {module:model/MessageStatus} The populated <code>MessageStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MessageStatus();

            if (data.hasOwnProperty('_links')) {
                obj['_links'] = MessageStatusLinks.constructFromObject(data['_links']);
            }
            if (data.hasOwnProperty('error')) {
                obj['error'] = MessageStatusError.constructFromObject(data['error']);
            }
            if (data.hasOwnProperty('from')) {
                obj['from'] = FromProperty.constructFromObject(data['from']);
            }
            if (data.hasOwnProperty('message_uuid')) {
                obj['message_uuid'] = ApiClient.convertToType(data['message_uuid'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'String');
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ToProperty.constructFromObject(data['to']);
            }
            if (data.hasOwnProperty('usage')) {
                obj['usage'] = MessageStatusUsage.constructFromObject(data['usage']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MessageStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MessageStatus</code>.
     */
    static validateJSON(data) {
        // validate the optional field `_links`
        if (data['_links']) { // data not null
          MessageStatusLinks.validateJSON(data['_links']);
        }
        // validate the optional field `error`
        if (data['error']) { // data not null
          MessageStatusError.validateJSON(data['error']);
        }
        // validate the optional field `from`
        if (data['from']) { // data not null
          FromProperty.validateJSON(data['from']);
        }
        // ensure the json data is a string
        if (data['message_uuid'] && !(typeof data['message_uuid'] === 'string' || data['message_uuid'] instanceof String)) {
            throw new Error("Expected the field `message_uuid` to be a primitive type in the JSON string but got " + data['message_uuid']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['timestamp'] && !(typeof data['timestamp'] === 'string' || data['timestamp'] instanceof String)) {
            throw new Error("Expected the field `timestamp` to be a primitive type in the JSON string but got " + data['timestamp']);
        }
        // validate the optional field `to`
        if (data['to']) { // data not null
          ToProperty.validateJSON(data['to']);
        }
        // validate the optional field `usage`
        if (data['usage']) { // data not null
          MessageStatusUsage.validateJSON(data['usage']);
        }

        return true;
    }


}



/**
 * @member {module:model/MessageStatusLinks} _links
 */
MessageStatus.prototype['_links'] = undefined;

/**
 * @member {module:model/MessageStatusError} error
 */
MessageStatus.prototype['error'] = undefined;

/**
 * @member {module:model/FromProperty} from
 */
MessageStatus.prototype['from'] = undefined;

/**
 * @member {String} message_uuid
 */
MessageStatus.prototype['message_uuid'] = undefined;

/**
 * The status of the message.
 * @member {module:model/MessageStatus.StatusEnum} status
 */
MessageStatus.prototype['status'] = undefined;

/**
 * The datetime of when the event occurred.
 * @member {String} timestamp
 */
MessageStatus.prototype['timestamp'] = undefined;

/**
 * @member {module:model/ToProperty} to
 */
MessageStatus.prototype['to'] = undefined;

/**
 * @member {module:model/MessageStatusUsage} usage
 */
MessageStatus.prototype['usage'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
MessageStatus['StatusEnum'] = {

    /**
     * value: "submitted"
     * @const
     */
    "submitted": "submitted",

    /**
     * value: "delivered"
     * @const
     */
    "delivered": "delivered",

    /**
     * value: "read"
     * @const
     */
    "read": "read",

    /**
     * value: "rejected"
     * @const
     */
    "rejected": "rejected",

    /**
     * value: "undeliverable"
     * @const
     */
    "undeliverable": "undeliverable"
};



export default MessageStatus;

