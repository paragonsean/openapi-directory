/**
 * Dispatch API
 * The Dispatch API enables the developer to specify a multiple message workflow. A workflow follows a template. The first one we are adding is the failover template. The failover template instructs the Messages API to first send a message to the specified channel. If that message fails immediately or if the condition_status is not reached within the given time period the next message is sent. The developer will also receive status webhooks from the messages resource for each delivery and read event. This API is currently in Beta.
 *
 * The version of the OpenAPI document: 0.3.4
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CreateWorkflowWorkflowInner from './CreateWorkflowWorkflowInner';

/**
 * The CreateWorkflow model module.
 * @module model/CreateWorkflow
 * @version 0.3.4
 */
class CreateWorkflow {
    /**
     * Constructs a new <code>CreateWorkflow</code>.
     * @alias module:model/CreateWorkflow
     */
    constructor() { 
        
        CreateWorkflow.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CreateWorkflow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateWorkflow} obj Optional instance to populate.
     * @return {module:model/CreateWorkflow} The populated <code>CreateWorkflow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateWorkflow();

            if (data.hasOwnProperty('template')) {
                obj['template'] = ApiClient.convertToType(data['template'], 'String');
            }
            if (data.hasOwnProperty('workflow')) {
                obj['workflow'] = ApiClient.convertToType(data['workflow'], [CreateWorkflowWorkflowInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateWorkflow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateWorkflow</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['template'] && !(typeof data['template'] === 'string' || data['template'] instanceof String)) {
            throw new Error("Expected the field `template` to be a primitive type in the JSON string but got " + data['template']);
        }
        if (data['workflow']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['workflow'])) {
                throw new Error("Expected the field `workflow` to be an array in the JSON data but got " + data['workflow']);
            }
            // validate the optional field `workflow` (array)
            for (const item of data['workflow']) {
                CreateWorkflowWorkflowInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The template that the Dispatch API will execute. For the initial version of the API the only available value will be failover
 * @member {module:model/CreateWorkflow.TemplateEnum} template
 */
CreateWorkflow.prototype['template'] = undefined;

/**
 * Contains a message object that must reflect the current /messages resource. All parameters within the content object reflect the /messages docs.
 * @member {Array.<module:model/CreateWorkflowWorkflowInner>} workflow
 */
CreateWorkflow.prototype['workflow'] = undefined;





/**
 * Allowed values for the <code>template</code> property.
 * @enum {String}
 * @readonly
 */
CreateWorkflow['TemplateEnum'] = {

    /**
     * value: "failover"
     * @const
     */
    "failover": "failover"
};



export default CreateWorkflow;

