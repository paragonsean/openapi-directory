/**
 * Dispatch API
 * The Dispatch API enables the developer to specify a multiple message workflow. A workflow follows a template. The first one we are adding is the failover template. The failover template instructs the Messages API to first send a message to the specified channel. If that message fails immediately or if the condition_status is not reached within the given time period the next message is sent. The developer will also receive status webhooks from the messages resource for each delivery and read event. This API is currently in Beta.
 *
 * The version of the OpenAPI document: 0.3.4
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FromProperty from './FromProperty';
import MessageProperty from './MessageProperty';
import ToProperty from './ToProperty';

/**
 * The SendMessage model module.
 * @module model/SendMessage
 * @version 0.3.4
 */
class SendMessage {
    /**
     * Constructs a new <code>SendMessage</code>.
     * Send Message
     * @alias module:model/SendMessage
     * @param from {module:model/FromProperty} 
     * @param message {module:model/MessageProperty} 
     * @param to {module:model/ToProperty} 
     */
    constructor(from, message, to) { 
        
        SendMessage.initialize(this, from, message, to);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, from, message, to) { 
        obj['from'] = from;
        obj['message'] = message;
        obj['to'] = to;
    }

    /**
     * Constructs a <code>SendMessage</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SendMessage} obj Optional instance to populate.
     * @return {module:model/SendMessage} The populated <code>SendMessage</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SendMessage();

            if (data.hasOwnProperty('from')) {
                obj['from'] = FromProperty.constructFromObject(data['from']);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = MessageProperty.constructFromObject(data['message']);
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ToProperty.constructFromObject(data['to']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SendMessage</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SendMessage</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SendMessage.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `from`
        if (data['from']) { // data not null
          FromProperty.validateJSON(data['from']);
        }
        // validate the optional field `message`
        if (data['message']) { // data not null
          MessageProperty.validateJSON(data['message']);
        }
        // validate the optional field `to`
        if (data['to']) { // data not null
          ToProperty.validateJSON(data['to']);
        }

        return true;
    }


}

SendMessage.RequiredProperties = ["from", "message", "to"];

/**
 * @member {module:model/FromProperty} from
 */
SendMessage.prototype['from'] = undefined;

/**
 * @member {module:model/MessageProperty} message
 */
SendMessage.prototype['message'] = undefined;

/**
 * @member {module:model/ToProperty} to
 */
SendMessage.prototype['to'] = undefined;






export default SendMessage;

