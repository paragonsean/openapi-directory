# dispatch_api

DispatchApi - JavaScript client for dispatch_api
The Dispatch API enables the developer to specify a multiple message workflow. A workflow follows a template. The first one we are adding is the failover template. The failover template instructs the Messages API to first send a message to the specified channel. If that message fails immediately or if the condition_status is not reached within the given time period the next message is sent. The developer will also receive status webhooks from the messages resource for each delivery and read event. This API is currently in Beta.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 0.3.4
- Package version: 0.3.4
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://developer.nexmo.com/](https://developer.nexmo.com/)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install dispatch_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your dispatch_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var DispatchApi = require('dispatch_api');

var defaultClient = DispatchApi.ApiClient.instance;
// Configure HTTP basic authorization: basicAuth
var basicAuth = defaultClient.authentications['basicAuth'];
basicAuth.username = 'YOUR USERNAME'
basicAuth.password = 'YOUR PASSWORD'
// Configure Bearer (JWT) access token for authorization: bearerAuth
var bearerAuth = defaultClient.authentications['bearerAuth'];
bearerAuth.accessToken = "YOUR ACCESS TOKEN"

var api = new DispatchApi.DefaultApi()
var createWorkflow = new DispatchApi.CreateWorkflow(); // {CreateWorkflow} Please note that last message does not have failover attribute inside it. All other messages must contain a failover attribute.
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.createWorkflow(createWorkflow, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.nexmo.com/v0.1/dispatch*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*DispatchApi.DefaultApi* | [**createWorkflow**](docs/DefaultApi.md#createWorkflow) | **POST** / | Create a workflow


## Documentation for Models

 - [DispatchApi.AudioProperty](docs/AudioProperty.md)
 - [DispatchApi.CreateWorkflow](docs/CreateWorkflow.md)
 - [DispatchApi.CreateWorkflowWorkflowInner](docs/CreateWorkflowWorkflowInner.md)
 - [DispatchApi.Error](docs/Error.md)
 - [DispatchApi.ErrorInvalidParameters](docs/ErrorInvalidParameters.md)
 - [DispatchApi.FailoverProperty](docs/FailoverProperty.md)
 - [DispatchApi.FileProperty](docs/FileProperty.md)
 - [DispatchApi.FinalReport](docs/FinalReport.md)
 - [DispatchApi.FinalReportLinks](docs/FinalReportLinks.md)
 - [DispatchApi.FinalReportLinksMessagesInner](docs/FinalReportLinksMessagesInner.md)
 - [DispatchApi.FinalReportUsage](docs/FinalReportUsage.md)
 - [DispatchApi.FromProperty](docs/FromProperty.md)
 - [DispatchApi.ImageProperty](docs/ImageProperty.md)
 - [DispatchApi.MessageProperty](docs/MessageProperty.md)
 - [DispatchApi.MessagePropertyContent](docs/MessagePropertyContent.md)
 - [DispatchApi.MessagePropertyMessenger](docs/MessagePropertyMessenger.md)
 - [DispatchApi.MessagePropertyViberServiceMsg](docs/MessagePropertyViberServiceMsg.md)
 - [DispatchApi.MessagePropertyWhatsapp](docs/MessagePropertyWhatsapp.md)
 - [DispatchApi.MessageStatus](docs/MessageStatus.md)
 - [DispatchApi.MessageStatusError](docs/MessageStatusError.md)
 - [DispatchApi.MessageStatusLinks](docs/MessageStatusLinks.md)
 - [DispatchApi.MessageStatusLinksWorkflow](docs/MessageStatusLinksWorkflow.md)
 - [DispatchApi.MessageStatusUsage](docs/MessageStatusUsage.md)
 - [DispatchApi.Response](docs/Response.md)
 - [DispatchApi.SendMessage](docs/SendMessage.md)
 - [DispatchApi.SendWithFailoverMessage](docs/SendWithFailoverMessage.md)
 - [DispatchApi.TemplateProperty](docs/TemplateProperty.md)
 - [DispatchApi.TemplatePropertyParametersInner](docs/TemplatePropertyParametersInner.md)
 - [DispatchApi.ToProperty](docs/ToProperty.md)
 - [DispatchApi.VideoProperty](docs/VideoProperty.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### basicAuth

- **Type**: HTTP basic authentication

### bearerAuth

- **Type**: Bearer authentication (JWT)

