/**
 * Account API
 * Enables users to manage their Vonage API Account by programmable means. More information is available here: <https://developer.nexmo.com/account/overview>.
 *
 * The version of the OpenAPI document: 1.0.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RegisterEmailResponse model module.
 * @module model/RegisterEmailResponse
 * @version 1.0.4
 */
class RegisterEmailResponse {
    /**
     * Constructs a new <code>RegisterEmailResponse</code>.
     * @alias module:model/RegisterEmailResponse
     */
    constructor() { 
        
        RegisterEmailResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RegisterEmailResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RegisterEmailResponse} obj Optional instance to populate.
     * @return {module:model/RegisterEmailResponse} The populated <code>RegisterEmailResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RegisterEmailResponse();

            if (data.hasOwnProperty('application_ids')) {
                obj['application_ids'] = ApiClient.convertToType(data['application_ids'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RegisterEmailResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RegisterEmailResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['application_ids'])) {
            throw new Error("Expected the field `application_ids` to be an array in the JSON data but got " + data['application_ids']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}



/**
 * An array of ApplicationID strings attached to the value provided.
 * @member {Array.<String>} application_ids
 */
RegisterEmailResponse.prototype['application_ids'] = undefined;

/**
 * An optional name to be attached to this binding
 * @member {String} name
 */
RegisterEmailResponse.prototype['name'] = undefined;

/**
 * Enum identifer of provider type.
 * @member {module:model/RegisterEmailResponse.ProviderEnum} provider
 */
RegisterEmailResponse.prototype['provider'] = undefined;

/**
 * Value given to the provider to attach to the Application IDs.
 * @member {String} value
 */
RegisterEmailResponse.prototype['value'] = undefined;





/**
 * Allowed values for the <code>provider</code> property.
 * @enum {String}
 * @readonly
 */
RegisterEmailResponse['ProviderEnum'] = {

    /**
     * value: "email"
     * @const
     */
    "email": "email"
};



export default RegisterEmailResponse;

