/**
 * Account API
 * Enables users to manage their Vonage API Account by programmable means. More information is available here: <https://developer.nexmo.com/account/overview>.
 *
 * The version of the OpenAPI document: 1.0.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RegisterEmailRequest model module.
 * @module model/RegisterEmailRequest
 * @version 1.0.4
 */
class RegisterEmailRequest {
    /**
     * Constructs a new <code>RegisterEmailRequest</code>.
     * @alias module:model/RegisterEmailRequest
     * @param provider {module:model/RegisterEmailRequest.ProviderEnum} The delivery method by which the value would be assigned. Always `email`
     * @param value {String} The email adress to attach to the application(s)
     */
    constructor(provider, value) { 
        
        RegisterEmailRequest.initialize(this, provider, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, provider, value) { 
        obj['provider'] = provider;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>RegisterEmailRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RegisterEmailRequest} obj Optional instance to populate.
     * @return {module:model/RegisterEmailRequest} The populated <code>RegisterEmailRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RegisterEmailRequest();

            if (data.hasOwnProperty('application_ids')) {
                obj['application_ids'] = ApiClient.convertToType(data['application_ids'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RegisterEmailRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RegisterEmailRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RegisterEmailRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['application_ids'])) {
            throw new Error("Expected the field `application_ids` to be an array in the JSON data but got " + data['application_ids']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

RegisterEmailRequest.RequiredProperties = ["provider", "value"];

/**
 * An optional array of additional ApplicationID's that the value is to be assigned to.
 * @member {Array.<String>} application_ids
 */
RegisterEmailRequest.prototype['application_ids'] = undefined;

/**
 * An optional name to be attached to this binding
 * @member {String} name
 */
RegisterEmailRequest.prototype['name'] = undefined;

/**
 * The delivery method by which the value would be assigned. Always `email`
 * @member {module:model/RegisterEmailRequest.ProviderEnum} provider
 */
RegisterEmailRequest.prototype['provider'] = undefined;

/**
 * The email adress to attach to the application(s)
 * @member {String} value
 */
RegisterEmailRequest.prototype['value'] = undefined;





/**
 * Allowed values for the <code>provider</code> property.
 * @enum {String}
 * @readonly
 */
RegisterEmailRequest['ProviderEnum'] = {

    /**
     * value: "email"
     * @const
     */
    "email": "email"
};



export default RegisterEmailRequest;

