/*
 * Voice API
 * The Voice API lets you create outbound calls, control in-progress calls and get information about historical calls. More information about the Voice API can be found at <https://developer.nexmo.com/voice/voice-api/overview>.
 *
 * The version of the OpenAPI document: 1.3.10
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.client.model.EndpointPhoneTo;
import org.openapitools.client.model.EndpointSip;
import org.openapitools.client.model.EndpointVBCExtension;
import org.openapitools.client.model.EndpointWebsocket;
import org.openapitools.client.model.EndpointWebsocketHeaders;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:24:31.401268-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class CreateCallRequestBaseToInner extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CreateCallRequestBaseToInner.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CreateCallRequestBaseToInner.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CreateCallRequestBaseToInner' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<EndpointPhoneTo> adapterEndpointPhoneTo = gson.getDelegateAdapter(this, TypeToken.get(EndpointPhoneTo.class));
            final TypeAdapter<EndpointSip> adapterEndpointSip = gson.getDelegateAdapter(this, TypeToken.get(EndpointSip.class));
            final TypeAdapter<EndpointWebsocket> adapterEndpointWebsocket = gson.getDelegateAdapter(this, TypeToken.get(EndpointWebsocket.class));
            final TypeAdapter<EndpointVBCExtension> adapterEndpointVBCExtension = gson.getDelegateAdapter(this, TypeToken.get(EndpointVBCExtension.class));

            return (TypeAdapter<T>) new TypeAdapter<CreateCallRequestBaseToInner>() {
                @Override
                public void write(JsonWriter out, CreateCallRequestBaseToInner value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `EndpointPhoneTo`
                    if (value.getActualInstance() instanceof EndpointPhoneTo) {
                        JsonElement element = adapterEndpointPhoneTo.toJsonTree((EndpointPhoneTo)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `EndpointSip`
                    if (value.getActualInstance() instanceof EndpointSip) {
                        JsonElement element = adapterEndpointSip.toJsonTree((EndpointSip)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `EndpointWebsocket`
                    if (value.getActualInstance() instanceof EndpointWebsocket) {
                        JsonElement element = adapterEndpointWebsocket.toJsonTree((EndpointWebsocket)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `EndpointVBCExtension`
                    if (value.getActualInstance() instanceof EndpointVBCExtension) {
                        JsonElement element = adapterEndpointVBCExtension.toJsonTree((EndpointVBCExtension)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket");
                }

                @Override
                public CreateCallRequestBaseToInner read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize EndpointPhoneTo
                    try {
                        // validate the JSON object to see if any exception is thrown
                        EndpointPhoneTo.validateJsonElement(jsonElement);
                        actualAdapter = adapterEndpointPhoneTo;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'EndpointPhoneTo'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for EndpointPhoneTo failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'EndpointPhoneTo'", e);
                    }
                    // deserialize EndpointSip
                    try {
                        // validate the JSON object to see if any exception is thrown
                        EndpointSip.validateJsonElement(jsonElement);
                        actualAdapter = adapterEndpointSip;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'EndpointSip'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for EndpointSip failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'EndpointSip'", e);
                    }
                    // deserialize EndpointWebsocket
                    try {
                        // validate the JSON object to see if any exception is thrown
                        EndpointWebsocket.validateJsonElement(jsonElement);
                        actualAdapter = adapterEndpointWebsocket;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'EndpointWebsocket'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for EndpointWebsocket failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'EndpointWebsocket'", e);
                    }
                    // deserialize EndpointVBCExtension
                    try {
                        // validate the JSON object to see if any exception is thrown
                        EndpointVBCExtension.validateJsonElement(jsonElement);
                        actualAdapter = adapterEndpointVBCExtension;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'EndpointVBCExtension'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for EndpointVBCExtension failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'EndpointVBCExtension'", e);
                    }

                    if (match == 1) {
                        CreateCallRequestBaseToInner ret = new CreateCallRequestBaseToInner();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CreateCallRequestBaseToInner: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CreateCallRequestBaseToInner() {
        super("oneOf", Boolean.FALSE);
    }

    public CreateCallRequestBaseToInner(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("EndpointPhoneTo", EndpointPhoneTo.class);
        schemas.put("EndpointSip", EndpointSip.class);
        schemas.put("EndpointWebsocket", EndpointWebsocket.class);
        schemas.put("EndpointVBCExtension", EndpointVBCExtension.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CreateCallRequestBaseToInner.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof EndpointPhoneTo) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof EndpointSip) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof EndpointWebsocket) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof EndpointVBCExtension) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket");
    }

    /**
     * Get the actual instance, which can be the following:
     * EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket
     *
     * @return The actual instance (EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `EndpointPhoneTo`. If the actual instance is not `EndpointPhoneTo`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `EndpointPhoneTo`
     * @throws ClassCastException if the instance is not `EndpointPhoneTo`
     */
    public EndpointPhoneTo getEndpointPhoneTo() throws ClassCastException {
        return (EndpointPhoneTo)super.getActualInstance();
    }
    /**
     * Get the actual instance of `EndpointSip`. If the actual instance is not `EndpointSip`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `EndpointSip`
     * @throws ClassCastException if the instance is not `EndpointSip`
     */
    public EndpointSip getEndpointSip() throws ClassCastException {
        return (EndpointSip)super.getActualInstance();
    }
    /**
     * Get the actual instance of `EndpointWebsocket`. If the actual instance is not `EndpointWebsocket`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `EndpointWebsocket`
     * @throws ClassCastException if the instance is not `EndpointWebsocket`
     */
    public EndpointWebsocket getEndpointWebsocket() throws ClassCastException {
        return (EndpointWebsocket)super.getActualInstance();
    }
    /**
     * Get the actual instance of `EndpointVBCExtension`. If the actual instance is not `EndpointVBCExtension`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `EndpointVBCExtension`
     * @throws ClassCastException if the instance is not `EndpointVBCExtension`
     */
    public EndpointVBCExtension getEndpointVBCExtension() throws ClassCastException {
        return (EndpointVBCExtension)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CreateCallRequestBaseToInner
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with EndpointPhoneTo
        try {
            EndpointPhoneTo.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for EndpointPhoneTo failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with EndpointSip
        try {
            EndpointSip.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for EndpointSip failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with EndpointWebsocket
        try {
            EndpointWebsocket.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for EndpointWebsocket failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with EndpointVBCExtension
        try {
            EndpointVBCExtension.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for EndpointVBCExtension failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for CreateCallRequestBaseToInner with oneOf schemas: EndpointPhoneTo, EndpointSip, EndpointVBCExtension, EndpointWebsocket. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of CreateCallRequestBaseToInner given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CreateCallRequestBaseToInner
     * @throws IOException if the JSON string is invalid with respect to CreateCallRequestBaseToInner
     */
    public static CreateCallRequestBaseToInner fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CreateCallRequestBaseToInner.class);
    }

    /**
     * Convert an instance of CreateCallRequestBaseToInner to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

