/**
 * Voice API
 * The Voice API lets you create outbound calls, control in-progress calls and get information about historical calls. More information about the Voice API can be found at <https://developer.nexmo.com/voice/voice-api/overview>.
 *
 * The version of the OpenAPI document: 1.3.10
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EndpointWebsocketHeaders from './EndpointWebsocketHeaders';

/**
 * The EndpointWebsocket model module.
 * @module model/EndpointWebsocket
 * @version 1.3.10
 */
class EndpointWebsocket {
    /**
     * Constructs a new <code>EndpointWebsocket</code>.
     * Connect to a Websocket
     * @alias module:model/EndpointWebsocket
     * @param contentType {module:model/EndpointWebsocket.ContentTypeEnum} 
     * @param type {String} The type of connection. Must be `websocket`
     */
    constructor(contentType, type) { 
        
        EndpointWebsocket.initialize(this, contentType, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contentType, type) { 
        obj['content-type'] = contentType;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>EndpointWebsocket</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EndpointWebsocket} obj Optional instance to populate.
     * @return {module:model/EndpointWebsocket} The populated <code>EndpointWebsocket</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EndpointWebsocket();

            if (data.hasOwnProperty('content-type')) {
                obj['content-type'] = ApiClient.convertToType(data['content-type'], 'String');
            }
            if (data.hasOwnProperty('headers')) {
                obj['headers'] = EndpointWebsocketHeaders.constructFromObject(data['headers']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EndpointWebsocket</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EndpointWebsocket</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EndpointWebsocket.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['content-type'] && !(typeof data['content-type'] === 'string' || data['content-type'] instanceof String)) {
            throw new Error("Expected the field `content-type` to be a primitive type in the JSON string but got " + data['content-type']);
        }
        // validate the optional field `headers`
        if (data['headers']) { // data not null
          EndpointWebsocketHeaders.validateJSON(data['headers']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }

        return true;
    }


}

EndpointWebsocket.RequiredProperties = ["content-type", "type"];

/**
 * @member {module:model/EndpointWebsocket.ContentTypeEnum} content-type
 */
EndpointWebsocket.prototype['content-type'] = undefined;

/**
 * @member {module:model/EndpointWebsocketHeaders} headers
 */
EndpointWebsocket.prototype['headers'] = undefined;

/**
 * The type of connection. Must be `websocket`
 * @member {String} type
 */
EndpointWebsocket.prototype['type'] = undefined;

/**
 * @member {String} uri
 */
EndpointWebsocket.prototype['uri'] = undefined;





/**
 * Allowed values for the <code>content-type</code> property.
 * @enum {String}
 * @readonly
 */
EndpointWebsocket['ContentTypeEnum'] = {

    /**
     * value: "audio/l16;rate=8000"
     * @const
     */
    "8000": "audio/l16;rate=8000",

    /**
     * value: "audio/l16;rate=16000"
     * @const
     */
    "16000": "audio/l16;rate=16000"
};



export default EndpointWebsocket;

