/**
 * Voice API
 * The Voice API lets you create outbound calls, control in-progress calls and get information about historical calls. More information about the Voice API can be found at <https://developer.nexmo.com/voice/voice-api/overview>.
 *
 * The version of the OpenAPI document: 1.3.10
 * Contact: devrel@vonage.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EndpointVBCExtension model module.
 * @module model/EndpointVBCExtension
 * @version 1.3.10
 */
class EndpointVBCExtension {
    /**
     * Constructs a new <code>EndpointVBCExtension</code>.
     * Connect to a VBC extension
     * @alias module:model/EndpointVBCExtension
     * @param extension {String} 
     * @param type {String} The type of connection. Must be `vbc`
     */
    constructor(extension, type) { 
        
        EndpointVBCExtension.initialize(this, extension, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, extension, type) { 
        obj['extension'] = extension;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>EndpointVBCExtension</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EndpointVBCExtension} obj Optional instance to populate.
     * @return {module:model/EndpointVBCExtension} The populated <code>EndpointVBCExtension</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EndpointVBCExtension();

            if (data.hasOwnProperty('extension')) {
                obj['extension'] = ApiClient.convertToType(data['extension'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EndpointVBCExtension</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EndpointVBCExtension</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EndpointVBCExtension.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['extension'] && !(typeof data['extension'] === 'string' || data['extension'] instanceof String)) {
            throw new Error("Expected the field `extension` to be a primitive type in the JSON string but got " + data['extension']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

EndpointVBCExtension.RequiredProperties = ["extension", "type"];

/**
 * @member {String} extension
 */
EndpointVBCExtension.prototype['extension'] = undefined;

/**
 * The type of connection. Must be `vbc`
 * @member {String} type
 */
EndpointVBCExtension.prototype['type'] = undefined;






export default EndpointVBCExtension;

