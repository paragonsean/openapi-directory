# swagger_generator

SwaggerGenerator - JavaScript client for swagger_generator
This is an online swagger codegen server.  You can find out more at https://github.com/swagger-api/swagger-codegen or on [irc.freenode.net, #swagger](http://swagger.io/irc/).
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2.4.31
- Package version: 2.4.31
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install swagger_generator --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your swagger_generator from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var SwaggerGenerator = require('swagger_generator');


var api = new SwaggerGenerator.ClientsApi()
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.clientOptions(callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://generator.swagger.io/api*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*SwaggerGenerator.ClientsApi* | [**clientOptions**](docs/ClientsApi.md#clientOptions) | **GET** /gen/clients | Gets languages supported by the client generator
*SwaggerGenerator.ClientsApi* | [**downloadFile**](docs/ClientsApi.md#downloadFile) | **GET** /gen/download/{fileId} | Downloads a pre-generated file
*SwaggerGenerator.ClientsApi* | [**generateClient**](docs/ClientsApi.md#generateClient) | **POST** /gen/clients/{language} | Generates a client library
*SwaggerGenerator.ClientsApi* | [**getClientOptions**](docs/ClientsApi.md#getClientOptions) | **GET** /gen/clients/{language} | Returns options for a client library
*SwaggerGenerator.ServersApi* | [**downloadFile_0**](docs/ServersApi.md#downloadFile_0) | **GET** /gen/download/{fileId} | Downloads a pre-generated file
*SwaggerGenerator.ServersApi* | [**generateServerForLanguage**](docs/ServersApi.md#generateServerForLanguage) | **POST** /gen/servers/{framework} | Generates a server library
*SwaggerGenerator.ServersApi* | [**getServerOptions**](docs/ServersApi.md#getServerOptions) | **GET** /gen/servers/{framework} | Returns options for a server framework
*SwaggerGenerator.ServersApi* | [**serverOptions**](docs/ServersApi.md#serverOptions) | **GET** /gen/servers | Gets languages supported by the server generator


## Documentation for Models

 - [SwaggerGenerator.AuthorizationValue](docs/AuthorizationValue.md)
 - [SwaggerGenerator.CliOption](docs/CliOption.md)
 - [SwaggerGenerator.GeneratorInput](docs/GeneratorInput.md)
 - [SwaggerGenerator.ResponseCode](docs/ResponseCode.md)
 - [SwaggerGenerator.SecuritySchemeDefinition](docs/SecuritySchemeDefinition.md)


## Documentation for Authorization

Endpoints do not require authorization.

