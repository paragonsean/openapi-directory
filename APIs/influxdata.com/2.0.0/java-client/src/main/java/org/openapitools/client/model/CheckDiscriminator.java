/*
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.CheckBaseLinks;
import org.openapitools.client.model.CheckStatusLevel;
import org.openapitools.client.model.CustomCheck;
import org.openapitools.client.model.DashboardQuery;
import org.openapitools.client.model.DeadmanCheck;
import org.openapitools.client.model.DeadmanCheckAllOfTags;
import org.openapitools.client.model.Label;
import org.openapitools.client.model.TaskStatusType;
import org.openapitools.client.model.Threshold;
import org.openapitools.client.model.ThresholdCheck;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:22:51.322764-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class CheckDiscriminator extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CheckDiscriminator.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CheckDiscriminator.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CheckDiscriminator' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<DeadmanCheck> adapterDeadmanCheck = gson.getDelegateAdapter(this, TypeToken.get(DeadmanCheck.class));
            final TypeAdapter<ThresholdCheck> adapterThresholdCheck = gson.getDelegateAdapter(this, TypeToken.get(ThresholdCheck.class));
            final TypeAdapter<CustomCheck> adapterCustomCheck = gson.getDelegateAdapter(this, TypeToken.get(CustomCheck.class));

            return (TypeAdapter<T>) new TypeAdapter<CheckDiscriminator>() {
                @Override
                public void write(JsonWriter out, CheckDiscriminator value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `DeadmanCheck`
                    if (value.getActualInstance() instanceof DeadmanCheck) {
                        JsonElement element = adapterDeadmanCheck.toJsonTree((DeadmanCheck)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `ThresholdCheck`
                    if (value.getActualInstance() instanceof ThresholdCheck) {
                        JsonElement element = adapterThresholdCheck.toJsonTree((ThresholdCheck)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `CustomCheck`
                    if (value.getActualInstance() instanceof CustomCheck) {
                        JsonElement element = adapterCustomCheck.toJsonTree((CustomCheck)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: CustomCheck, DeadmanCheck, ThresholdCheck");
                }

                @Override
                public CheckDiscriminator read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize DeadmanCheck
                    try {
                        // validate the JSON object to see if any exception is thrown
                        DeadmanCheck.validateJsonElement(jsonElement);
                        actualAdapter = adapterDeadmanCheck;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'DeadmanCheck'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for DeadmanCheck failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'DeadmanCheck'", e);
                    }
                    // deserialize ThresholdCheck
                    try {
                        // validate the JSON object to see if any exception is thrown
                        ThresholdCheck.validateJsonElement(jsonElement);
                        actualAdapter = adapterThresholdCheck;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'ThresholdCheck'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for ThresholdCheck failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'ThresholdCheck'", e);
                    }
                    // deserialize CustomCheck
                    try {
                        // validate the JSON object to see if any exception is thrown
                        CustomCheck.validateJsonElement(jsonElement);
                        actualAdapter = adapterCustomCheck;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'CustomCheck'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for CustomCheck failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'CustomCheck'", e);
                    }

                    if (match == 1) {
                        CheckDiscriminator ret = new CheckDiscriminator();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CheckDiscriminator: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CheckDiscriminator() {
        super("oneOf", Boolean.FALSE);
    }

    public CheckDiscriminator(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("DeadmanCheck", DeadmanCheck.class);
        schemas.put("ThresholdCheck", ThresholdCheck.class);
        schemas.put("CustomCheck", CustomCheck.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CheckDiscriminator.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * CustomCheck, DeadmanCheck, ThresholdCheck
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof DeadmanCheck) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof ThresholdCheck) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof CustomCheck) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be CustomCheck, DeadmanCheck, ThresholdCheck");
    }

    /**
     * Get the actual instance, which can be the following:
     * CustomCheck, DeadmanCheck, ThresholdCheck
     *
     * @return The actual instance (CustomCheck, DeadmanCheck, ThresholdCheck)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `DeadmanCheck`. If the actual instance is not `DeadmanCheck`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `DeadmanCheck`
     * @throws ClassCastException if the instance is not `DeadmanCheck`
     */
    public DeadmanCheck getDeadmanCheck() throws ClassCastException {
        return (DeadmanCheck)super.getActualInstance();
    }
    /**
     * Get the actual instance of `ThresholdCheck`. If the actual instance is not `ThresholdCheck`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ThresholdCheck`
     * @throws ClassCastException if the instance is not `ThresholdCheck`
     */
    public ThresholdCheck getThresholdCheck() throws ClassCastException {
        return (ThresholdCheck)super.getActualInstance();
    }
    /**
     * Get the actual instance of `CustomCheck`. If the actual instance is not `CustomCheck`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `CustomCheck`
     * @throws ClassCastException if the instance is not `CustomCheck`
     */
    public CustomCheck getCustomCheck() throws ClassCastException {
        return (CustomCheck)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CheckDiscriminator
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with DeadmanCheck
        try {
            DeadmanCheck.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for DeadmanCheck failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with ThresholdCheck
        try {
            ThresholdCheck.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for ThresholdCheck failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with CustomCheck
        try {
            CustomCheck.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for CustomCheck failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for CheckDiscriminator with oneOf schemas: CustomCheck, DeadmanCheck, ThresholdCheck. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of CheckDiscriminator given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CheckDiscriminator
     * @throws IOException if the JSON string is invalid with respect to CheckDiscriminator
     */
    public static CheckDiscriminator fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CheckDiscriminator.class);
    }

    /**
     * Convert an instance of CheckDiscriminator to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

