/*
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.time.OffsetDateTime;
import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;
import org.openapitools.client.model.Dialect;
import org.openapitools.client.model.InfluxQLQuery;
import org.openapitools.client.model.ModelFile;
import org.openapitools.client.model.Query;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:22:51.322764-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class PostQueryRequest extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(PostQueryRequest.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!PostQueryRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'PostQueryRequest' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<Query> adapterQuery = gson.getDelegateAdapter(this, TypeToken.get(Query.class));
            final TypeAdapter<InfluxQLQuery> adapterInfluxQLQuery = gson.getDelegateAdapter(this, TypeToken.get(InfluxQLQuery.class));

            return (TypeAdapter<T>) new TypeAdapter<PostQueryRequest>() {
                @Override
                public void write(JsonWriter out, PostQueryRequest value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `Query`
                    if (value.getActualInstance() instanceof Query) {
                        JsonElement element = adapterQuery.toJsonTree((Query)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `InfluxQLQuery`
                    if (value.getActualInstance() instanceof InfluxQLQuery) {
                        JsonElement element = adapterInfluxQLQuery.toJsonTree((InfluxQLQuery)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: InfluxQLQuery, Query");
                }

                @Override
                public PostQueryRequest read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize Query
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Query.validateJsonElement(jsonElement);
                        actualAdapter = adapterQuery;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Query'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Query failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Query'", e);
                    }
                    // deserialize InfluxQLQuery
                    try {
                        // validate the JSON object to see if any exception is thrown
                        InfluxQLQuery.validateJsonElement(jsonElement);
                        actualAdapter = adapterInfluxQLQuery;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'InfluxQLQuery'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for InfluxQLQuery failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'InfluxQLQuery'", e);
                    }

                    if (match == 1) {
                        PostQueryRequest ret = new PostQueryRequest();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for PostQueryRequest: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public PostQueryRequest() {
        super("oneOf", Boolean.FALSE);
    }

    public PostQueryRequest(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Query", Query.class);
        schemas.put("InfluxQLQuery", InfluxQLQuery.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return PostQueryRequest.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * InfluxQLQuery, Query
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof Query) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof InfluxQLQuery) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be InfluxQLQuery, Query");
    }

    /**
     * Get the actual instance, which can be the following:
     * InfluxQLQuery, Query
     *
     * @return The actual instance (InfluxQLQuery, Query)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Query`. If the actual instance is not `Query`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Query`
     * @throws ClassCastException if the instance is not `Query`
     */
    public Query getQuery() throws ClassCastException {
        return (Query)super.getActualInstance();
    }
    /**
     * Get the actual instance of `InfluxQLQuery`. If the actual instance is not `InfluxQLQuery`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `InfluxQLQuery`
     * @throws ClassCastException if the instance is not `InfluxQLQuery`
     */
    public InfluxQLQuery getInfluxQLQuery() throws ClassCastException {
        return (InfluxQLQuery)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to PostQueryRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with Query
        try {
            Query.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Query failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with InfluxQLQuery
        try {
            InfluxQLQuery.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for InfluxQLQuery failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for PostQueryRequest with oneOf schemas: InfluxQLQuery, Query. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of PostQueryRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of PostQueryRequest
     * @throws IOException if the JSON string is invalid with respect to PostQueryRequest
     */
    public static PostQueryRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, PostQueryRequest.class);
    }

    /**
     * Convert an instance of PostQueryRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

