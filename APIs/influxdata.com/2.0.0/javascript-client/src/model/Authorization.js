/**
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AuthorizationAllOfLinks from './AuthorizationAllOfLinks';
import AuthorizationUpdateRequest from './AuthorizationUpdateRequest';
import Permission from './Permission';

/**
 * The Authorization model module.
 * @module model/Authorization
 * @version 2.0.0
 */
class Authorization {
    /**
     * Constructs a new <code>Authorization</code>.
     * @alias module:model/Authorization
     * @implements module:model/AuthorizationUpdateRequest
     * @param orgID {String} ID of org that authorization is scoped to.
     * @param permissions {Array.<module:model/Permission>} List of permissions for an auth.  An auth must have at least one Permission.
     */
    constructor(orgID, permissions) { 
        AuthorizationUpdateRequest.initialize(this);
        Authorization.initialize(this, orgID, permissions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orgID, permissions) { 
        obj['status'] = 'active';
        obj['orgID'] = orgID;
        obj['permissions'] = permissions;
    }

    /**
     * Constructs a <code>Authorization</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Authorization} obj Optional instance to populate.
     * @return {module:model/Authorization} The populated <code>Authorization</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Authorization();
            AuthorizationUpdateRequest.constructFromObject(data, obj);

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('createdAt')) {
                obj['createdAt'] = ApiClient.convertToType(data['createdAt'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = AuthorizationAllOfLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('org')) {
                obj['org'] = ApiClient.convertToType(data['org'], 'String');
            }
            if (data.hasOwnProperty('orgID')) {
                obj['orgID'] = ApiClient.convertToType(data['orgID'], 'String');
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], [Permission]);
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('updatedAt')) {
                obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Date');
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = ApiClient.convertToType(data['user'], 'String');
            }
            if (data.hasOwnProperty('userID')) {
                obj['userID'] = ApiClient.convertToType(data['userID'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Authorization</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Authorization</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Authorization.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          AuthorizationAllOfLinks.validateJSON(data['links']);
        }
        // ensure the json data is a string
        if (data['org'] && !(typeof data['org'] === 'string' || data['org'] instanceof String)) {
            throw new Error("Expected the field `org` to be a primitive type in the JSON string but got " + data['org']);
        }
        // ensure the json data is a string
        if (data['orgID'] && !(typeof data['orgID'] === 'string' || data['orgID'] instanceof String)) {
            throw new Error("Expected the field `orgID` to be a primitive type in the JSON string but got " + data['orgID']);
        }
        if (data['permissions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['permissions'])) {
                throw new Error("Expected the field `permissions` to be an array in the JSON data but got " + data['permissions']);
            }
            // validate the optional field `permissions` (array)
            for (const item of data['permissions']) {
                Permission.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['user'] && !(typeof data['user'] === 'string' || data['user'] instanceof String)) {
            throw new Error("Expected the field `user` to be a primitive type in the JSON string but got " + data['user']);
        }
        // ensure the json data is a string
        if (data['userID'] && !(typeof data['userID'] === 'string' || data['userID'] instanceof String)) {
            throw new Error("Expected the field `userID` to be a primitive type in the JSON string but got " + data['userID']);
        }

        return true;
    }


}

Authorization.RequiredProperties = ["orgID", "permissions"];

/**
 * A description of the token.
 * @member {String} description
 */
Authorization.prototype['description'] = undefined;

/**
 * If inactive the token is inactive and requests using the token will be rejected.
 * @member {module:model/Authorization.StatusEnum} status
 * @default 'active'
 */
Authorization.prototype['status'] = 'active';

/**
 * @member {Date} createdAt
 */
Authorization.prototype['createdAt'] = undefined;

/**
 * @member {String} id
 */
Authorization.prototype['id'] = undefined;

/**
 * @member {module:model/AuthorizationAllOfLinks} links
 */
Authorization.prototype['links'] = undefined;

/**
 * Name of the org token is scoped to.
 * @member {String} org
 */
Authorization.prototype['org'] = undefined;

/**
 * ID of org that authorization is scoped to.
 * @member {String} orgID
 */
Authorization.prototype['orgID'] = undefined;

/**
 * List of permissions for an auth.  An auth must have at least one Permission.
 * @member {Array.<module:model/Permission>} permissions
 */
Authorization.prototype['permissions'] = undefined;

/**
 * Passed via the Authorization Header and Token Authentication type.
 * @member {String} token
 */
Authorization.prototype['token'] = undefined;

/**
 * @member {Date} updatedAt
 */
Authorization.prototype['updatedAt'] = undefined;

/**
 * Name of user that created and owns the token.
 * @member {String} user
 */
Authorization.prototype['user'] = undefined;

/**
 * ID of user that created and owns the token.
 * @member {String} userID
 */
Authorization.prototype['userID'] = undefined;


// Implement AuthorizationUpdateRequest interface:
/**
 * A description of the token.
 * @member {String} description
 */
AuthorizationUpdateRequest.prototype['description'] = undefined;
/**
 * If inactive the token is inactive and requests using the token will be rejected.
 * @member {module:model/AuthorizationUpdateRequest.StatusEnum} status
 * @default 'active'
 */
AuthorizationUpdateRequest.prototype['status'] = 'active';



/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
Authorization['StatusEnum'] = {

    /**
     * value: "active"
     * @const
     */
    "active": "active",

    /**
     * value: "inactive"
     * @const
     */
    "inactive": "inactive"
};



export default Authorization;

