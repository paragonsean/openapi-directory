/**
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DocumentLinks from './DocumentLinks';
import DocumentMeta from './DocumentMeta';
import Label from './Label';

/**
 * The DocumentListEntry model module.
 * @module model/DocumentListEntry
 * @version 2.0.0
 */
class DocumentListEntry {
    /**
     * Constructs a new <code>DocumentListEntry</code>.
     * @alias module:model/DocumentListEntry
     * @param id {String} 
     * @param meta {module:model/DocumentMeta} 
     */
    constructor(id, meta) { 
        
        DocumentListEntry.initialize(this, id, meta);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, meta) { 
        obj['id'] = id;
        obj['meta'] = meta;
    }

    /**
     * Constructs a <code>DocumentListEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DocumentListEntry} obj Optional instance to populate.
     * @return {module:model/DocumentListEntry} The populated <code>DocumentListEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DocumentListEntry();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], [Label]);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = DocumentLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = DocumentMeta.constructFromObject(data['meta']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DocumentListEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DocumentListEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DocumentListEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['labels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['labels'])) {
                throw new Error("Expected the field `labels` to be an array in the JSON data but got " + data['labels']);
            }
            // validate the optional field `labels` (array)
            for (const item of data['labels']) {
                Label.validateJSON(item);
            };
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          DocumentLinks.validateJSON(data['links']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          DocumentMeta.validateJSON(data['meta']);
        }

        return true;
    }


}

DocumentListEntry.RequiredProperties = ["id", "meta"];

/**
 * @member {String} id
 */
DocumentListEntry.prototype['id'] = undefined;

/**
 * @member {Array.<module:model/Label>} labels
 */
DocumentListEntry.prototype['labels'] = undefined;

/**
 * @member {module:model/DocumentLinks} links
 */
DocumentListEntry.prototype['links'] = undefined;

/**
 * @member {module:model/DocumentMeta} meta
 */
DocumentListEntry.prototype['meta'] = undefined;






export default DocumentListEntry;

