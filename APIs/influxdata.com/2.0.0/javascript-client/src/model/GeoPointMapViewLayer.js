/**
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Axis from './Axis';
import DashboardColor from './DashboardColor';
import GeoViewLayerProperties from './GeoViewLayerProperties';

/**
 * The GeoPointMapViewLayer model module.
 * @module model/GeoPointMapViewLayer
 * @version 2.0.0
 */
class GeoPointMapViewLayer {
    /**
     * Constructs a new <code>GeoPointMapViewLayer</code>.
     * @alias module:model/GeoPointMapViewLayer
     * @implements module:model/GeoViewLayerProperties
     * @param type {module:model/GeoPointMapViewLayer.TypeEnum} 
     * @param colorDimension {module:model/Axis} 
     * @param colorField {String} Marker color field
     * @param colors {Array.<module:model/DashboardColor>} Colors define color encoding of data into a visualization
     */
    constructor(type, colorDimension, colorField, colors) { 
        GeoViewLayerProperties.initialize(this, type);
        GeoPointMapViewLayer.initialize(this, type, colorDimension, colorField, colors);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, colorDimension, colorField, colors) { 
        obj['type'] = type;
        obj['colorDimension'] = colorDimension;
        obj['colorField'] = colorField;
        obj['colors'] = colors;
    }

    /**
     * Constructs a <code>GeoPointMapViewLayer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GeoPointMapViewLayer} obj Optional instance to populate.
     * @return {module:model/GeoPointMapViewLayer} The populated <code>GeoPointMapViewLayer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GeoPointMapViewLayer();
            GeoViewLayerProperties.constructFromObject(data, obj);

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('colorDimension')) {
                obj['colorDimension'] = Axis.constructFromObject(data['colorDimension']);
            }
            if (data.hasOwnProperty('colorField')) {
                obj['colorField'] = ApiClient.convertToType(data['colorField'], 'String');
            }
            if (data.hasOwnProperty('colors')) {
                obj['colors'] = ApiClient.convertToType(data['colors'], [DashboardColor]);
            }
            if (data.hasOwnProperty('isClustered')) {
                obj['isClustered'] = ApiClient.convertToType(data['isClustered'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GeoPointMapViewLayer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GeoPointMapViewLayer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GeoPointMapViewLayer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // validate the optional field `colorDimension`
        if (data['colorDimension']) { // data not null
          Axis.validateJSON(data['colorDimension']);
        }
        // ensure the json data is a string
        if (data['colorField'] && !(typeof data['colorField'] === 'string' || data['colorField'] instanceof String)) {
            throw new Error("Expected the field `colorField` to be a primitive type in the JSON string but got " + data['colorField']);
        }
        if (data['colors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['colors'])) {
                throw new Error("Expected the field `colors` to be an array in the JSON data but got " + data['colors']);
            }
            // validate the optional field `colors` (array)
            for (const item of data['colors']) {
                DashboardColor.validateJSON(item);
            };
        }

        return true;
    }


}

GeoPointMapViewLayer.RequiredProperties = ["type", "colorDimension", "colorField", "colors"];

/**
 * @member {module:model/GeoPointMapViewLayer.TypeEnum} type
 */
GeoPointMapViewLayer.prototype['type'] = undefined;

/**
 * @member {module:model/Axis} colorDimension
 */
GeoPointMapViewLayer.prototype['colorDimension'] = undefined;

/**
 * Marker color field
 * @member {String} colorField
 */
GeoPointMapViewLayer.prototype['colorField'] = undefined;

/**
 * Colors define color encoding of data into a visualization
 * @member {Array.<module:model/DashboardColor>} colors
 */
GeoPointMapViewLayer.prototype['colors'] = undefined;

/**
 * Cluster close markers together
 * @member {Boolean} isClustered
 */
GeoPointMapViewLayer.prototype['isClustered'] = undefined;


// Implement GeoViewLayerProperties interface:
/**
 * @member {module:model/GeoViewLayerProperties.TypeEnum} type
 */
GeoViewLayerProperties.prototype['type'] = undefined;



/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
GeoPointMapViewLayer['TypeEnum'] = {

    /**
     * value: "heatmap"
     * @const
     */
    "heatmap": "heatmap",

    /**
     * value: "circleMap"
     * @const
     */
    "circleMap": "circleMap",

    /**
     * value: "pointMap"
     * @const
     */
    "pointMap": "pointMap",

    /**
     * value: "trackMap"
     * @const
     */
    "trackMap": "trackMap"
};



export default GeoPointMapViewLayer;

