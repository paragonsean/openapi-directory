/**
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RetentionRule model module.
 * @module model/RetentionRule
 * @version 2.0.0
 */
class RetentionRule {
    /**
     * Constructs a new <code>RetentionRule</code>.
     * @alias module:model/RetentionRule
     * @param everySeconds {Number} Duration in seconds for how long data will be kept in the database. 0 means infinite.
     * @param type {module:model/RetentionRule.TypeEnum} 
     */
    constructor(everySeconds, type) { 
        
        RetentionRule.initialize(this, everySeconds, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, everySeconds, type) { 
        obj['everySeconds'] = everySeconds;
        obj['type'] = type || 'expire';
    }

    /**
     * Constructs a <code>RetentionRule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RetentionRule} obj Optional instance to populate.
     * @return {module:model/RetentionRule} The populated <code>RetentionRule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RetentionRule();

            if (data.hasOwnProperty('everySeconds')) {
                obj['everySeconds'] = ApiClient.convertToType(data['everySeconds'], 'Number');
            }
            if (data.hasOwnProperty('shardGroupDurationSeconds')) {
                obj['shardGroupDurationSeconds'] = ApiClient.convertToType(data['shardGroupDurationSeconds'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RetentionRule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RetentionRule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RetentionRule.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

RetentionRule.RequiredProperties = ["everySeconds", "type"];

/**
 * Duration in seconds for how long data will be kept in the database. 0 means infinite.
 * @member {Number} everySeconds
 */
RetentionRule.prototype['everySeconds'] = undefined;

/**
 * Shard duration measured in seconds.
 * @member {Number} shardGroupDurationSeconds
 */
RetentionRule.prototype['shardGroupDurationSeconds'] = undefined;

/**
 * @member {module:model/RetentionRule.TypeEnum} type
 * @default 'expire'
 */
RetentionRule.prototype['type'] = 'expire';





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
RetentionRule['TypeEnum'] = {

    /**
     * value: "expire"
     * @const
     */
    "expire": "expire"
};



export default RetentionRule;

