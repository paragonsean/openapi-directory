/**
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SlackNotificationRuleBase model module.
 * @module model/SlackNotificationRuleBase
 * @version 2.0.0
 */
class SlackNotificationRuleBase {
    /**
     * Constructs a new <code>SlackNotificationRuleBase</code>.
     * @alias module:model/SlackNotificationRuleBase
     * @param messageTemplate {String} 
     * @param type {module:model/SlackNotificationRuleBase.TypeEnum} 
     */
    constructor(messageTemplate, type) { 
        
        SlackNotificationRuleBase.initialize(this, messageTemplate, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, messageTemplate, type) { 
        obj['messageTemplate'] = messageTemplate;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>SlackNotificationRuleBase</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SlackNotificationRuleBase} obj Optional instance to populate.
     * @return {module:model/SlackNotificationRuleBase} The populated <code>SlackNotificationRuleBase</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SlackNotificationRuleBase();

            if (data.hasOwnProperty('channel')) {
                obj['channel'] = ApiClient.convertToType(data['channel'], 'String');
            }
            if (data.hasOwnProperty('messageTemplate')) {
                obj['messageTemplate'] = ApiClient.convertToType(data['messageTemplate'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SlackNotificationRuleBase</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SlackNotificationRuleBase</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SlackNotificationRuleBase.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['channel'] && !(typeof data['channel'] === 'string' || data['channel'] instanceof String)) {
            throw new Error("Expected the field `channel` to be a primitive type in the JSON string but got " + data['channel']);
        }
        // ensure the json data is a string
        if (data['messageTemplate'] && !(typeof data['messageTemplate'] === 'string' || data['messageTemplate'] instanceof String)) {
            throw new Error("Expected the field `messageTemplate` to be a primitive type in the JSON string but got " + data['messageTemplate']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

SlackNotificationRuleBase.RequiredProperties = ["messageTemplate", "type"];

/**
 * @member {String} channel
 */
SlackNotificationRuleBase.prototype['channel'] = undefined;

/**
 * @member {String} messageTemplate
 */
SlackNotificationRuleBase.prototype['messageTemplate'] = undefined;

/**
 * @member {module:model/SlackNotificationRuleBase.TypeEnum} type
 */
SlackNotificationRuleBase.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
SlackNotificationRuleBase['TypeEnum'] = {

    /**
     * value: "slack"
     * @const
     */
    "slack": "slack"
};



export default SlackNotificationRuleBase;

