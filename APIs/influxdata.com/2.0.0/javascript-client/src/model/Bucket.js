/**
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BucketLinks from './BucketLinks';
import Label from './Label';
import RetentionRule from './RetentionRule';
import SchemaType from './SchemaType';

/**
 * The Bucket model module.
 * @module model/Bucket
 * @version 2.0.0
 */
class Bucket {
    /**
     * Constructs a new <code>Bucket</code>.
     * @alias module:model/Bucket
     * @param name {String} 
     * @param retentionRules {Array.<module:model/RetentionRule>} Rules to expire or retain data.  No rules means data never expires.
     */
    constructor(name, retentionRules) { 
        
        Bucket.initialize(this, name, retentionRules);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, retentionRules) { 
        obj['name'] = name;
        obj['retentionRules'] = retentionRules;
        obj['type'] = 'user';
    }

    /**
     * Constructs a <code>Bucket</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Bucket} obj Optional instance to populate.
     * @return {module:model/Bucket} The populated <code>Bucket</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Bucket();

            if (data.hasOwnProperty('createdAt')) {
                obj['createdAt'] = ApiClient.convertToType(data['createdAt'], 'Date');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], [Label]);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = BucketLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('orgID')) {
                obj['orgID'] = ApiClient.convertToType(data['orgID'], 'String');
            }
            if (data.hasOwnProperty('retentionRules')) {
                obj['retentionRules'] = ApiClient.convertToType(data['retentionRules'], [RetentionRule]);
            }
            if (data.hasOwnProperty('rp')) {
                obj['rp'] = ApiClient.convertToType(data['rp'], 'String');
            }
            if (data.hasOwnProperty('schemaType')) {
                obj['schemaType'] = SchemaType.constructFromObject(data['schemaType']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('updatedAt')) {
                obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Bucket</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Bucket</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Bucket.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['labels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['labels'])) {
                throw new Error("Expected the field `labels` to be an array in the JSON data but got " + data['labels']);
            }
            // validate the optional field `labels` (array)
            for (const item of data['labels']) {
                Label.validateJSON(item);
            };
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          BucketLinks.validateJSON(data['links']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['orgID'] && !(typeof data['orgID'] === 'string' || data['orgID'] instanceof String)) {
            throw new Error("Expected the field `orgID` to be a primitive type in the JSON string but got " + data['orgID']);
        }
        if (data['retentionRules']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['retentionRules'])) {
                throw new Error("Expected the field `retentionRules` to be an array in the JSON data but got " + data['retentionRules']);
            }
            // validate the optional field `retentionRules` (array)
            for (const item of data['retentionRules']) {
                RetentionRule.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['rp'] && !(typeof data['rp'] === 'string' || data['rp'] instanceof String)) {
            throw new Error("Expected the field `rp` to be a primitive type in the JSON string but got " + data['rp']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Bucket.RequiredProperties = ["name", "retentionRules"];

/**
 * @member {Date} createdAt
 */
Bucket.prototype['createdAt'] = undefined;

/**
 * @member {String} description
 */
Bucket.prototype['description'] = undefined;

/**
 * @member {String} id
 */
Bucket.prototype['id'] = undefined;

/**
 * @member {Array.<module:model/Label>} labels
 */
Bucket.prototype['labels'] = undefined;

/**
 * @member {module:model/BucketLinks} links
 */
Bucket.prototype['links'] = undefined;

/**
 * @member {String} name
 */
Bucket.prototype['name'] = undefined;

/**
 * @member {String} orgID
 */
Bucket.prototype['orgID'] = undefined;

/**
 * Rules to expire or retain data.  No rules means data never expires.
 * @member {Array.<module:model/RetentionRule>} retentionRules
 */
Bucket.prototype['retentionRules'] = undefined;

/**
 * @member {String} rp
 */
Bucket.prototype['rp'] = undefined;

/**
 * @member {module:model/SchemaType} schemaType
 */
Bucket.prototype['schemaType'] = undefined;

/**
 * @member {module:model/Bucket.TypeEnum} type
 * @default 'user'
 */
Bucket.prototype['type'] = 'user';

/**
 * @member {Date} updatedAt
 */
Bucket.prototype['updatedAt'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Bucket['TypeEnum'] = {

    /**
     * value: "user"
     * @const
     */
    "user": "user",

    /**
     * value: "system"
     * @const
     */
    "system": "system"
};



export default Bucket;

