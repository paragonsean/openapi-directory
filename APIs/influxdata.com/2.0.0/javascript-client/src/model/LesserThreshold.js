/**
 * Influx OSS API Service
 * # Authentication  &lt;!-- ReDoc-Inject: &lt;security-definitions&gt; --&gt;
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CheckStatusLevel from './CheckStatusLevel';
import ThresholdBase from './ThresholdBase';

/**
 * The LesserThreshold model module.
 * @module model/LesserThreshold
 * @version 2.0.0
 */
class LesserThreshold {
    /**
     * Constructs a new <code>LesserThreshold</code>.
     * @alias module:model/LesserThreshold
     * @implements module:model/ThresholdBase
     * @param type {module:model/LesserThreshold.TypeEnum} 
     * @param value {Number} 
     */
    constructor(type, value) { 
        ThresholdBase.initialize(this);
        LesserThreshold.initialize(this, type, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, value) { 
        obj['type'] = type;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>LesserThreshold</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LesserThreshold} obj Optional instance to populate.
     * @return {module:model/LesserThreshold} The populated <code>LesserThreshold</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LesserThreshold();
            ThresholdBase.constructFromObject(data, obj);

            if (data.hasOwnProperty('allValues')) {
                obj['allValues'] = ApiClient.convertToType(data['allValues'], 'Boolean');
            }
            if (data.hasOwnProperty('level')) {
                obj['level'] = CheckStatusLevel.constructFromObject(data['level']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LesserThreshold</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LesserThreshold</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LesserThreshold.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

LesserThreshold.RequiredProperties = ["type", "value"];

/**
 * If true, only alert if all values meet threshold.
 * @member {Boolean} allValues
 */
LesserThreshold.prototype['allValues'] = undefined;

/**
 * @member {module:model/CheckStatusLevel} level
 */
LesserThreshold.prototype['level'] = undefined;

/**
 * @member {module:model/LesserThreshold.TypeEnum} type
 */
LesserThreshold.prototype['type'] = undefined;

/**
 * @member {Number} value
 */
LesserThreshold.prototype['value'] = undefined;


// Implement ThresholdBase interface:
/**
 * If true, only alert if all values meet threshold.
 * @member {Boolean} allValues
 */
ThresholdBase.prototype['allValues'] = undefined;
/**
 * @member {module:model/CheckStatusLevel} level
 */
ThresholdBase.prototype['level'] = undefined;



/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
LesserThreshold['TypeEnum'] = {

    /**
     * value: "lesser"
     * @const
     */
    "lesser": "lesser"
};



export default LesserThreshold;

