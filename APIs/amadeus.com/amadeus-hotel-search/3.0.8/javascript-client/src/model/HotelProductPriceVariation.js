/**
 * Hotel Search API
 * Before using this API, we recommend you read our **[Authorization Guide](https://developers.amadeus.com/self-service/apis-docs/guides/authorization-262)** for more information on how to generate an access token.   Please also be aware that our test environment is based on a subset of the production for this API it may change dynamically. For your tests, use big cities like LON (London) or NYC (New-York).
 *
 * The version of the OpenAPI document: 3.0.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Markup from './Markup';

/**
 * The HotelProductPriceVariation model module.
 * @module model/HotelProductPriceVariation
 * @version 3.0.8
 */
class HotelProductPriceVariation {
    /**
     * Constructs a new <code>HotelProductPriceVariation</code>.
     * Some prices may vary during a stay, thus here you can see the daily price per period of the stay
     * @alias module:model/HotelProductPriceVariation
     * @param endDate {Date} End date of the period Format: YYYY-MM-DD
     * @param startDate {Date} Begin date of the period Format: YYYY-MM-DD
     */
    constructor(endDate, startDate) { 
        
        HotelProductPriceVariation.initialize(this, endDate, startDate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endDate, startDate) { 
        obj['endDate'] = endDate;
        obj['startDate'] = startDate;
    }

    /**
     * Constructs a <code>HotelProductPriceVariation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HotelProductPriceVariation} obj Optional instance to populate.
     * @return {module:model/HotelProductPriceVariation} The populated <code>HotelProductPriceVariation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HotelProductPriceVariation();

            if (data.hasOwnProperty('base')) {
                obj['base'] = ApiClient.convertToType(data['base'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            if (data.hasOwnProperty('markups')) {
                obj['markups'] = ApiClient.convertToType(data['markups'], [Markup]);
            }
            if (data.hasOwnProperty('sellingTotal')) {
                obj['sellingTotal'] = ApiClient.convertToType(data['sellingTotal'], 'String');
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HotelProductPriceVariation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HotelProductPriceVariation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of HotelProductPriceVariation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['base'] && !(typeof data['base'] === 'string' || data['base'] instanceof String)) {
            throw new Error("Expected the field `base` to be a primitive type in the JSON string but got " + data['base']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        if (data['markups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['markups'])) {
                throw new Error("Expected the field `markups` to be an array in the JSON data but got " + data['markups']);
            }
            // validate the optional field `markups` (array)
            for (const item of data['markups']) {
                Markup.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['sellingTotal'] && !(typeof data['sellingTotal'] === 'string' || data['sellingTotal'] instanceof String)) {
            throw new Error("Expected the field `sellingTotal` to be a primitive type in the JSON string but got " + data['sellingTotal']);
        }
        // ensure the json data is a string
        if (data['total'] && !(typeof data['total'] === 'string' || data['total'] instanceof String)) {
            throw new Error("Expected the field `total` to be a primitive type in the JSON string but got " + data['total']);
        }

        return true;
    }


}

HotelProductPriceVariation.RequiredProperties = ["endDate", "startDate"];

/**
 * @member {String} base
 */
HotelProductPriceVariation.prototype['base'] = undefined;

/**
 * currency Code apply to all elements of the price
 * @member {String} currency
 */
HotelProductPriceVariation.prototype['currency'] = undefined;

/**
 * End date of the period Format: YYYY-MM-DD
 * @member {Date} endDate
 */
HotelProductPriceVariation.prototype['endDate'] = undefined;

/**
 * @member {Array.<module:model/Markup>} markups
 */
HotelProductPriceVariation.prototype['markups'] = undefined;

/**
 * sellingTotal = Total + margins + markup + totalFees - discounts
 * @member {String} sellingTotal
 */
HotelProductPriceVariation.prototype['sellingTotal'] = undefined;

/**
 * Begin date of the period Format: YYYY-MM-DD
 * @member {Date} startDate
 */
HotelProductPriceVariation.prototype['startDate'] = undefined;

/**
 * total = base + totalTaxes
 * @member {String} total
 */
HotelProductPriceVariation.prototype['total'] = undefined;






export default HotelProductPriceVariation;

