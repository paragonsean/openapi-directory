# on_demand_flight_status

OnDemandFlightStatus - JavaScript client for on_demand_flight_status

Before using this API, we recommend you read our **[Authorization Guide](https://developers.amadeus.com/self-service/apis-docs/guides/authorization-262)** for more information on how to generate an access token. 
  
Please also be aware that our test environment is based on a subset of the production, to see what is included in test please refer to our **[data collection](https://github.com/amadeus4dev/data-collection)**.

This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2.0.2
- Package version: 2.0.2
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install on_demand_flight_status --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your on_demand_flight_status from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var OnDemandFlightStatus = require('on_demand_flight_status');


var api = new OnDemandFlightStatus.FlightsApi()
var carrierCode = "TP"; // {String} 2 to 3-character IATA carrier code ([IATA table codes](http://www.iata.org/publications/Pages/code-search.aspx)). 
var flightNumber = "487"; // {String} 1 to 4-digit number of the flight. e.g. 4537
var scheduledDepartureDate = new Date("2023-08-01"); // {Date} scheduled departure date of the flight, local to the departure airport, format YYYY-MM-DD.
var opts = {
  'operationalSuffix': "operationalSuffix_example" // {String} 1-letter operational suffix assigned by the carrier to differentiate flight in case of delay changing the departure date e.g. A 
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.getFlightsStatus(carrierCode, flightNumber, scheduledDepartureDate, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://test.api.amadeus.com/v2*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*OnDemandFlightStatus.FlightsApi* | [**getFlightsStatus**](docs/FlightsApi.md#getFlightsStatus) | **GET** /schedule/flights | Retrieves a unique flight by search criteria.


## Documentation for Models

 - [OnDemandFlightStatus.AircraftEquipment](docs/AircraftEquipment.md)
 - [OnDemandFlightStatus.Arrival](docs/Arrival.md)
 - [OnDemandFlightStatus.CollectionLinks](docs/CollectionLinks.md)
 - [OnDemandFlightStatus.CollectionMeta](docs/CollectionMeta.md)
 - [OnDemandFlightStatus.DatedFlight](docs/DatedFlight.md)
 - [OnDemandFlightStatus.Delay](docs/Delay.md)
 - [OnDemandFlightStatus.Departure](docs/Departure.md)
 - [OnDemandFlightStatus.Error400](docs/Error400.md)
 - [OnDemandFlightStatus.Error401](docs/Error401.md)
 - [OnDemandFlightStatus.Error500](docs/Error500.md)
 - [OnDemandFlightStatus.FlightDesignator](docs/FlightDesignator.md)
 - [OnDemandFlightStatus.FlightPoint](docs/FlightPoint.md)
 - [OnDemandFlightStatus.Gate](docs/Gate.md)
 - [OnDemandFlightStatus.Issue](docs/Issue.md)
 - [OnDemandFlightStatus.IssueSource](docs/IssueSource.md)
 - [OnDemandFlightStatus.Leg](docs/Leg.md)
 - [OnDemandFlightStatus.Partnership](docs/Partnership.md)
 - [OnDemandFlightStatus.Segment](docs/Segment.md)
 - [OnDemandFlightStatus.SuccessFlights](docs/SuccessFlights.md)
 - [OnDemandFlightStatus.Terminal](docs/Terminal.md)
 - [OnDemandFlightStatus.Timing](docs/Timing.md)


## Documentation for Authorization

Endpoints do not require authorization.

