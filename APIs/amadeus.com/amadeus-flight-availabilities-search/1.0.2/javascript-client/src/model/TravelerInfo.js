/**
 * Flight Availibilities Search
 *  Before using this API, we recommend you read our **[Authorization Guide](https://developers.amadeus.com/self-service/apis-docs/guides/authorization-262)** for more information on how to generate an access token.   Please also be aware that our test environment is based on a subset of the production, if you are not returning any results try with big cities/airports like LON (London) or NYC (New-York).
 *
 * The version of the OpenAPI document: 1.0.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TravelerType from './TravelerType';

/**
 * The TravelerInfo model module.
 * @module model/TravelerInfo
 * @version 1.0.2
 */
class TravelerInfo {
    /**
     * Constructs a new <code>TravelerInfo</code>.
     * @alias module:model/TravelerInfo
     * @param id {String} 
     * @param travelerType {module:model/TravelerType} 
     */
    constructor(id, travelerType) { 
        
        TravelerInfo.initialize(this, id, travelerType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, travelerType) { 
        obj['id'] = id;
        obj['travelerType'] = travelerType;
    }

    /**
     * Constructs a <code>TravelerInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TravelerInfo} obj Optional instance to populate.
     * @return {module:model/TravelerInfo} The populated <code>TravelerInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TravelerInfo();

            if (data.hasOwnProperty('associatedAdultId')) {
                obj['associatedAdultId'] = ApiClient.convertToType(data['associatedAdultId'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('travelerType')) {
                obj['travelerType'] = TravelerType.constructFromObject(data['travelerType']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TravelerInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TravelerInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TravelerInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['associatedAdultId'] && !(typeof data['associatedAdultId'] === 'string' || data['associatedAdultId'] instanceof String)) {
            throw new Error("Expected the field `associatedAdultId` to be a primitive type in the JSON string but got " + data['associatedAdultId']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }

        return true;
    }


}

TravelerInfo.RequiredProperties = ["id", "travelerType"];

/**
 * if type=\"HELD_INFANT\", corresponds to the adult travelers's id who will share the seat
 * @member {String} associatedAdultId
 */
TravelerInfo.prototype['associatedAdultId'] = undefined;

/**
 * @member {String} id
 */
TravelerInfo.prototype['id'] = undefined;

/**
 * @member {module:model/TravelerType} travelerType
 */
TravelerInfo.prototype['travelerType'] = undefined;






export default TravelerInfo;

