/**
 * Flight Availibilities Search
 *  Before using this API, we recommend you read our **[Authorization Guide](https://developers.amadeus.com/self-service/apis-docs/guides/authorization-262)** for more information on how to generate an access token.   Please also be aware that our test environment is based on a subset of the production, if you are not returning any results try with big cities/airports like LON (London) or NYC (New-York).
 *
 * The version of the OpenAPI document: 1.0.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExtendedOriginDestinationLight from './ExtendedOriginDestinationLight';
import ExtendedSearchCriteria from './ExtendedSearchCriteria';
import FlightOfferSource from './FlightOfferSource';
import TravelerInfo from './TravelerInfo';

/**
 * The GetFlightAvailabilitiesQuery model module.
 * @module model/GetFlightAvailabilitiesQuery
 * @version 1.0.2
 */
class GetFlightAvailabilitiesQuery {
    /**
     * Constructs a new <code>GetFlightAvailabilitiesQuery</code>.
     * @alias module:model/GetFlightAvailabilitiesQuery
     * @param originDestinations {Array.<module:model/ExtendedOriginDestinationLight>} Origins and Destinations must be properly ordered in time (chronological order in accordance with the timezone of each location) to describe the journey consistently. Dates and times must not be past nor more than 365 days in the future, according to provider settings.Number of Origins and Destinations must not exceed the limit defined in provider settings.
     * @param sources {Array.<module:model/FlightOfferSource>} Allows enable one or more sources. If present in the list, these sources will be called by the system.  GDS : Full service carriers
     * @param travelers {Array.<module:model/TravelerInfo>} List of travelers composing the travel
     */
    constructor(originDestinations, sources, travelers) { 
        
        GetFlightAvailabilitiesQuery.initialize(this, originDestinations, sources, travelers);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, originDestinations, sources, travelers) { 
        obj['originDestinations'] = originDestinations;
        obj['sources'] = sources;
        obj['travelers'] = travelers;
    }

    /**
     * Constructs a <code>GetFlightAvailabilitiesQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetFlightAvailabilitiesQuery} obj Optional instance to populate.
     * @return {module:model/GetFlightAvailabilitiesQuery} The populated <code>GetFlightAvailabilitiesQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetFlightAvailabilitiesQuery();

            if (data.hasOwnProperty('originDestinations')) {
                obj['originDestinations'] = ApiClient.convertToType(data['originDestinations'], [ExtendedOriginDestinationLight]);
            }
            if (data.hasOwnProperty('searchCriteria')) {
                obj['searchCriteria'] = ExtendedSearchCriteria.constructFromObject(data['searchCriteria']);
            }
            if (data.hasOwnProperty('sources')) {
                obj['sources'] = ApiClient.convertToType(data['sources'], [FlightOfferSource]);
            }
            if (data.hasOwnProperty('travelers')) {
                obj['travelers'] = ApiClient.convertToType(data['travelers'], [TravelerInfo]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetFlightAvailabilitiesQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetFlightAvailabilitiesQuery</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetFlightAvailabilitiesQuery.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['originDestinations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['originDestinations'])) {
                throw new Error("Expected the field `originDestinations` to be an array in the JSON data but got " + data['originDestinations']);
            }
            // validate the optional field `originDestinations` (array)
            for (const item of data['originDestinations']) {
                ExtendedOriginDestinationLight.validateJSON(item);
            };
        }
        // validate the optional field `searchCriteria`
        if (data['searchCriteria']) { // data not null
          ExtendedSearchCriteria.validateJSON(data['searchCriteria']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['sources'])) {
            throw new Error("Expected the field `sources` to be an array in the JSON data but got " + data['sources']);
        }
        if (data['travelers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['travelers'])) {
                throw new Error("Expected the field `travelers` to be an array in the JSON data but got " + data['travelers']);
            }
            // validate the optional field `travelers` (array)
            for (const item of data['travelers']) {
                TravelerInfo.validateJSON(item);
            };
        }

        return true;
    }


}

GetFlightAvailabilitiesQuery.RequiredProperties = ["originDestinations", "sources", "travelers"];

/**
 * Origins and Destinations must be properly ordered in time (chronological order in accordance with the timezone of each location) to describe the journey consistently. Dates and times must not be past nor more than 365 days in the future, according to provider settings.Number of Origins and Destinations must not exceed the limit defined in provider settings.
 * @member {Array.<module:model/ExtendedOriginDestinationLight>} originDestinations
 */
GetFlightAvailabilitiesQuery.prototype['originDestinations'] = undefined;

/**
 * @member {module:model/ExtendedSearchCriteria} searchCriteria
 */
GetFlightAvailabilitiesQuery.prototype['searchCriteria'] = undefined;

/**
 * Allows enable one or more sources. If present in the list, these sources will be called by the system.  GDS : Full service carriers
 * @member {Array.<module:model/FlightOfferSource>} sources
 */
GetFlightAvailabilitiesQuery.prototype['sources'] = undefined;

/**
 * List of travelers composing the travel
 * @member {Array.<module:model/TravelerInfo>} travelers
 */
GetFlightAvailabilitiesQuery.prototype['travelers'] = undefined;






export default GetFlightAvailabilitiesQuery;

