/**
 * Flight Offers Price
 * Before using this API, we recommend you read our **[Authorization Guide](https://developers.amadeus.com/self-service/apis-docs/guides/authorization-262)** for more information on how to generate an access token.   Please also be aware that our test environment is based on a subset of the production, if you are not returning any results try with big cities/airports like LON (London) or NYC (New-York).
 *
 * The version of the OpenAPI document: 1.2.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FlightOffer from './FlightOffer';
import Payment from './Payment';
import Traveler from './Traveler';

/**
 * The FlightOfferPricingIn model module.
 * @module model/FlightOfferPricingIn
 * @version 1.2.2
 */
class FlightOfferPricingIn {
    /**
     * Constructs a new <code>FlightOfferPricingIn</code>.
     * input parameter to price flight offers element
     * @alias module:model/FlightOfferPricingIn
     * @param flightOffers {Array.<module:model/FlightOffer>} list of flight offer to price
     * @param type {String} the resource name
     */
    constructor(flightOffers, type) { 
        
        FlightOfferPricingIn.initialize(this, flightOffers, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, flightOffers, type) { 
        obj['flightOffers'] = flightOffers;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>FlightOfferPricingIn</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FlightOfferPricingIn} obj Optional instance to populate.
     * @return {module:model/FlightOfferPricingIn} The populated <code>FlightOfferPricingIn</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FlightOfferPricingIn();

            if (data.hasOwnProperty('flightOffers')) {
                obj['flightOffers'] = ApiClient.convertToType(data['flightOffers'], [FlightOffer]);
            }
            if (data.hasOwnProperty('payments')) {
                obj['payments'] = ApiClient.convertToType(data['payments'], [Payment]);
            }
            if (data.hasOwnProperty('travelers')) {
                obj['travelers'] = ApiClient.convertToType(data['travelers'], [Traveler]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FlightOfferPricingIn</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FlightOfferPricingIn</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FlightOfferPricingIn.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['flightOffers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['flightOffers'])) {
                throw new Error("Expected the field `flightOffers` to be an array in the JSON data but got " + data['flightOffers']);
            }
            // validate the optional field `flightOffers` (array)
            for (const item of data['flightOffers']) {
                FlightOffer.validateJSON(item);
            };
        }
        if (data['payments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['payments'])) {
                throw new Error("Expected the field `payments` to be an array in the JSON data but got " + data['payments']);
            }
            // validate the optional field `payments` (array)
            for (const item of data['payments']) {
                Payment.validateJSON(item);
            };
        }
        if (data['travelers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['travelers'])) {
                throw new Error("Expected the field `travelers` to be an array in the JSON data but got " + data['travelers']);
            }
            // validate the optional field `travelers` (array)
            for (const item of data['travelers']) {
                Traveler.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

FlightOfferPricingIn.RequiredProperties = ["flightOffers", "type"];

/**
 * list of flight offer to price
 * @member {Array.<module:model/FlightOffer>} flightOffers
 */
FlightOfferPricingIn.prototype['flightOffers'] = undefined;

/**
 * payment information for retrieve eventual credit card fees
 * @member {Array.<module:model/Payment>} payments
 */
FlightOfferPricingIn.prototype['payments'] = undefined;

/**
 * list of travelers
 * @member {Array.<module:model/Traveler>} travelers
 */
FlightOfferPricingIn.prototype['travelers'] = undefined;

/**
 * the resource name
 * @member {String} type
 */
FlightOfferPricingIn.prototype['type'] = undefined;






export default FlightOfferPricingIn;

