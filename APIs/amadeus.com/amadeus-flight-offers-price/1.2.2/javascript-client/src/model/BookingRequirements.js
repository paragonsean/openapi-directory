/**
 * Flight Offers Price
 * Before using this API, we recommend you read our **[Authorization Guide](https://developers.amadeus.com/self-service/apis-docs/guides/authorization-262)** for more information on how to generate an access token.   Please also be aware that our test environment is based on a subset of the production, if you are not returning any results try with big cities/airports like LON (London) or NYC (New-York).
 *
 * The version of the OpenAPI document: 1.2.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PassengerConditions from './PassengerConditions';

/**
 * The BookingRequirements model module.
 * @module model/BookingRequirements
 * @version 1.2.2
 */
class BookingRequirements {
    /**
     * Constructs a new <code>BookingRequirements</code>.
     * pricing condition at booking level
     * @alias module:model/BookingRequirements
     */
    constructor() { 
        
        BookingRequirements.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BookingRequirements</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BookingRequirements} obj Optional instance to populate.
     * @return {module:model/BookingRequirements} The populated <code>BookingRequirements</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BookingRequirements();

            if (data.hasOwnProperty('emailAddressRequired')) {
                obj['emailAddressRequired'] = ApiClient.convertToType(data['emailAddressRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('invoiceAddressRequired')) {
                obj['invoiceAddressRequired'] = ApiClient.convertToType(data['invoiceAddressRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('mailingAddressRequired')) {
                obj['mailingAddressRequired'] = ApiClient.convertToType(data['mailingAddressRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('mobilePhoneNumberRequired')) {
                obj['mobilePhoneNumberRequired'] = ApiClient.convertToType(data['mobilePhoneNumberRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('phoneCountryCodeRequired')) {
                obj['phoneCountryCodeRequired'] = ApiClient.convertToType(data['phoneCountryCodeRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('phoneNumberRequired')) {
                obj['phoneNumberRequired'] = ApiClient.convertToType(data['phoneNumberRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('postalCodeRequired')) {
                obj['postalCodeRequired'] = ApiClient.convertToType(data['postalCodeRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('travelerRequirements')) {
                obj['travelerRequirements'] = ApiClient.convertToType(data['travelerRequirements'], [PassengerConditions]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BookingRequirements</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BookingRequirements</code>.
     */
    static validateJSON(data) {
        if (data['travelerRequirements']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['travelerRequirements'])) {
                throw new Error("Expected the field `travelerRequirements` to be an array in the JSON data but got " + data['travelerRequirements']);
            }
            // validate the optional field `travelerRequirements` (array)
            for (const item of data['travelerRequirements']) {
                PassengerConditions.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * If true, an email address is required for the creation of the flight-order
 * @member {Boolean} emailAddressRequired
 */
BookingRequirements.prototype['emailAddressRequired'] = undefined;

/**
 * If true, an invoice address is required for the creation of the flight-order
 * @member {Boolean} invoiceAddressRequired
 */
BookingRequirements.prototype['invoiceAddressRequired'] = undefined;

/**
 * If true, a postal address is required for the creation of the flight-order
 * @member {Boolean} mailingAddressRequired
 */
BookingRequirements.prototype['mailingAddressRequired'] = undefined;

/**
 * If true, a mobile phone number is required for the creation of the flight-order
 * @member {Boolean} mobilePhoneNumberRequired
 */
BookingRequirements.prototype['mobilePhoneNumberRequired'] = undefined;

/**
 * If true, the phone country code (e.g. '33') associated for each phone number is required for the creation of the flight-order
 * @member {Boolean} phoneCountryCodeRequired
 */
BookingRequirements.prototype['phoneCountryCodeRequired'] = undefined;

/**
 * If true, a phone number is required for the creation of the flight-order
 * @member {Boolean} phoneNumberRequired
 */
BookingRequirements.prototype['phoneNumberRequired'] = undefined;

/**
 * If true, a postal code is required for the creation of the flight-order
 * @member {Boolean} postalCodeRequired
 */
BookingRequirements.prototype['postalCodeRequired'] = undefined;

/**
 * traveler pricing condition
 * @member {Array.<module:model/PassengerConditions>} travelerRequirements
 */
BookingRequirements.prototype['travelerRequirements'] = undefined;






export default BookingRequirements;

