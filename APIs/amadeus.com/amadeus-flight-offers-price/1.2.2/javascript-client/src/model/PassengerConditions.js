/**
 * Flight Offers Price
 * Before using this API, we recommend you read our **[Authorization Guide](https://developers.amadeus.com/self-service/apis-docs/guides/authorization-262)** for more information on how to generate an access token.   Please also be aware that our test environment is based on a subset of the production, if you are not returning any results try with big cities/airports like LON (London) or NYC (New-York).
 *
 * The version of the OpenAPI document: 1.2.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PassengerConditions model module.
 * @module model/PassengerConditions
 * @version 1.2.2
 */
class PassengerConditions {
    /**
     * Constructs a new <code>PassengerConditions</code>.
     * @alias module:model/PassengerConditions
     */
    constructor() { 
        
        PassengerConditions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PassengerConditions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PassengerConditions} obj Optional instance to populate.
     * @return {module:model/PassengerConditions} The populated <code>PassengerConditions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PassengerConditions();

            if (data.hasOwnProperty('airFranceDiscountRequired')) {
                obj['airFranceDiscountRequired'] = ApiClient.convertToType(data['airFranceDiscountRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('dateOfBirthRequired')) {
                obj['dateOfBirthRequired'] = ApiClient.convertToType(data['dateOfBirthRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('documentIssuanceCityRequired')) {
                obj['documentIssuanceCityRequired'] = ApiClient.convertToType(data['documentIssuanceCityRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('documentRequired')) {
                obj['documentRequired'] = ApiClient.convertToType(data['documentRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('genderRequired')) {
                obj['genderRequired'] = ApiClient.convertToType(data['genderRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('redressRequiredIfAny')) {
                obj['redressRequiredIfAny'] = ApiClient.convertToType(data['redressRequiredIfAny'], 'Boolean');
            }
            if (data.hasOwnProperty('residenceRequired')) {
                obj['residenceRequired'] = ApiClient.convertToType(data['residenceRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('spanishResidentDiscountRequired')) {
                obj['spanishResidentDiscountRequired'] = ApiClient.convertToType(data['spanishResidentDiscountRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('travelerId')) {
                obj['travelerId'] = ApiClient.convertToType(data['travelerId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PassengerConditions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PassengerConditions</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['travelerId'] && !(typeof data['travelerId'] === 'string' || data['travelerId'] instanceof String)) {
            throw new Error("Expected the field `travelerId` to be a primitive type in the JSON string but got " + data['travelerId']);
        }

        return true;
    }


}



/**
 * If true, the Air France discount is required for the concerned traveler for the creation of the flight-order
 * @member {Boolean} airFranceDiscountRequired
 */
PassengerConditions.prototype['airFranceDiscountRequired'] = undefined;

/**
 * If true, the date of bitrth is required for the concerned traveler for the creation of the flight-order
 * @member {Boolean} dateOfBirthRequired
 */
PassengerConditions.prototype['dateOfBirthRequired'] = undefined;

/**
 * If true, the isuance city of the document is required for the concerned traveler for the creation of the flight-order
 * @member {Boolean} documentIssuanceCityRequired
 */
PassengerConditions.prototype['documentIssuanceCityRequired'] = undefined;

/**
 * If true, a document is required for the concerned traveler for the creation of the flight-order
 * @member {Boolean} documentRequired
 */
PassengerConditions.prototype['documentRequired'] = undefined;

/**
 * If true, the gender is required for the concerned traveler for the creation of the flight-order
 * @member {Boolean} genderRequired
 */
PassengerConditions.prototype['genderRequired'] = undefined;

/**
 * If true, the redress is required if any for the concerned traveler for the creation of the flight-order
 * @member {Boolean} redressRequiredIfAny
 */
PassengerConditions.prototype['redressRequiredIfAny'] = undefined;

/**
 * If true, the address is required for the concerned traveler for the creation of the flight-order
 * @member {Boolean} residenceRequired
 */
PassengerConditions.prototype['residenceRequired'] = undefined;

/**
 * If true, the Spanish resident discount is required for the concerned traveler for the creation of the flight-order
 * @member {Boolean} spanishResidentDiscountRequired
 */
PassengerConditions.prototype['spanishResidentDiscountRequired'] = undefined;

/**
 * Id of the traveler
 * @member {String} travelerId
 */
PassengerConditions.prototype['travelerId'] = undefined;






export default PassengerConditions;

