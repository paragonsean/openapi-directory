/**
 * ETSI GS MEC 010-2 - Part 2: Application lifecycle, rules and requirements management
 * ETSI GS MEC 010-2 - Part 2: Application lifecycle, rules and requirements management described using OpenAPI.
 *
 * The version of the OpenAPI document: 2.1.1
 * Contact: cti_support@etsi.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransportDescriptor from './TransportDescriptor';

/**
 * The TransportDependency model module.
 * @module model/TransportDependency
 * @version 2.1.1
 */
class TransportDependency {
    /**
     * Constructs a new <code>TransportDependency</code>.
     * @alias module:model/TransportDependency
     * @param labels {Array.<String>} Set of labels that allow to define groups of transport bindings. The mechanism of the grouping is defined below this table.
     * @param serializers {Array.<String>} Information about the serializers in this transport binding, as defined in the SerializerTypes type in ETSI GS MEC 011 [i.4]. Support for at least one of the entries is required in conjunction with the transport.
     * @param transport {module:model/TransportDescriptor} 
     */
    constructor(labels, serializers, transport) { 
        
        TransportDependency.initialize(this, labels, serializers, transport);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, labels, serializers, transport) { 
        obj['labels'] = labels;
        obj['serializers'] = serializers;
        obj['transport'] = transport;
    }

    /**
     * Constructs a <code>TransportDependency</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransportDependency} obj Optional instance to populate.
     * @return {module:model/TransportDependency} The populated <code>TransportDependency</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransportDependency();

            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], ['String']);
            }
            if (data.hasOwnProperty('serializers')) {
                obj['serializers'] = ApiClient.convertToType(data['serializers'], ['String']);
            }
            if (data.hasOwnProperty('transport')) {
                obj['transport'] = TransportDescriptor.constructFromObject(data['transport']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransportDependency</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransportDependency</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransportDependency.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['labels'])) {
            throw new Error("Expected the field `labels` to be an array in the JSON data but got " + data['labels']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['serializers'])) {
            throw new Error("Expected the field `serializers` to be an array in the JSON data but got " + data['serializers']);
        }
        // validate the optional field `transport`
        if (data['transport']) { // data not null
          TransportDescriptor.validateJSON(data['transport']);
        }

        return true;
    }


}

TransportDependency.RequiredProperties = ["labels", "serializers", "transport"];

/**
 * Set of labels that allow to define groups of transport bindings. The mechanism of the grouping is defined below this table.
 * @member {Array.<String>} labels
 */
TransportDependency.prototype['labels'] = undefined;

/**
 * Information about the serializers in this transport binding, as defined in the SerializerTypes type in ETSI GS MEC 011 [i.4]. Support for at least one of the entries is required in conjunction with the transport.
 * @member {Array.<String>} serializers
 */
TransportDependency.prototype['serializers'] = undefined;

/**
 * @member {module:model/TransportDescriptor} transport
 */
TransportDependency.prototype['transport'] = undefined;






export default TransportDependency;

