/**
 * AGCO API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The APIModelsCredentials model module.
 * @module model/APIModelsCredentials
 * @version v1
 */
class APIModelsCredentials {
    /**
     * Constructs a new <code>APIModelsCredentials</code>.
     * @alias module:model/APIModelsCredentials
     * @param password {String} A secret word or phrase that must be used to gain admission
     * @param username {String} A unique ID a user needs to login with
     */
    constructor(password, username) { 
        
        APIModelsCredentials.initialize(this, password, username);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, password, username) { 
        obj['password'] = password;
        obj['username'] = username;
    }

    /**
     * Constructs a <code>APIModelsCredentials</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/APIModelsCredentials} obj Optional instance to populate.
     * @return {module:model/APIModelsCredentials} The populated <code>APIModelsCredentials</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new APIModelsCredentials();

            if (data.hasOwnProperty('BearerAction')) {
                obj['BearerAction'] = ApiClient.convertToType(data['BearerAction'], 'String');
            }
            if (data.hasOwnProperty('MACAction')) {
                obj['MACAction'] = ApiClient.convertToType(data['MACAction'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>APIModelsCredentials</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>APIModelsCredentials</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of APIModelsCredentials.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['BearerAction'] && !(typeof data['BearerAction'] === 'string' || data['BearerAction'] instanceof String)) {
            throw new Error("Expected the field `BearerAction` to be a primitive type in the JSON string but got " + data['BearerAction']);
        }
        // ensure the json data is a string
        if (data['MACAction'] && !(typeof data['MACAction'] === 'string' || data['MACAction'] instanceof String)) {
            throw new Error("Expected the field `MACAction` to be a primitive type in the JSON string but got " + data['MACAction']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }

        return true;
    }


}

APIModelsCredentials.RequiredProperties = ["password", "username"];

/**
 * The action to perform on the bearer token. Optional. Defaults to ‘None’.
 * @member {module:model/APIModelsCredentials.BearerActionEnum} BearerAction
 */
APIModelsCredentials.prototype['BearerAction'] = undefined;

/**
 * The action to perform on the MAC token. Optional. Defaults to ‘None’.
 * @member {module:model/APIModelsCredentials.MACActionEnum} MACAction
 */
APIModelsCredentials.prototype['MACAction'] = undefined;

/**
 * A secret word or phrase that must be used to gain admission
 * @member {String} password
 */
APIModelsCredentials.prototype['password'] = undefined;

/**
 * A unique ID a user needs to login with
 * @member {String} username
 */
APIModelsCredentials.prototype['username'] = undefined;





/**
 * Allowed values for the <code>BearerAction</code> property.
 * @enum {String}
 * @readonly
 */
APIModelsCredentials['BearerActionEnum'] = {

    /**
     * value: "None"
     * @const
     */
    "None": "None",

    /**
     * value: "Reset"
     * @const
     */
    "Reset": "Reset",

    /**
     * value: "Disable"
     * @const
     */
    "Disable": "Disable"
};


/**
 * Allowed values for the <code>MACAction</code> property.
 * @enum {String}
 * @readonly
 */
APIModelsCredentials['MACActionEnum'] = {

    /**
     * value: "None"
     * @const
     */
    "None": "None",

    /**
     * value: "Reset"
     * @const
     */
    "Reset": "Reset",

    /**
     * value: "Disable"
     * @const
     */
    "Disable": "Disable"
};



export default APIModelsCredentials;

