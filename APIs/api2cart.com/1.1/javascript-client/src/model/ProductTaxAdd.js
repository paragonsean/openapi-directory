/**
 * Swagger API2Cart
 * API2Cart
 *
 * The version of the OpenAPI document: 1.1
 * Contact: contact@api2cart.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProductTaxAddTaxRatesInner from './ProductTaxAddTaxRatesInner';

/**
 * The ProductTaxAdd model module.
 * @module model/ProductTaxAdd
 * @version 1.1
 */
class ProductTaxAdd {
    /**
     * Constructs a new <code>ProductTaxAdd</code>.
     * @alias module:model/ProductTaxAdd
     * @param name {String} Defines tax class name where tax has to be added
     * @param taxRates {Array.<module:model/ProductTaxAddTaxRatesInner>} Defines tax rates of specified tax classes
     */
    constructor(name, taxRates) { 
        
        ProductTaxAdd.initialize(this, name, taxRates);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, taxRates) { 
        obj['name'] = name;
        obj['tax_rates'] = taxRates;
    }

    /**
     * Constructs a <code>ProductTaxAdd</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductTaxAdd} obj Optional instance to populate.
     * @return {module:model/ProductTaxAdd} The populated <code>ProductTaxAdd</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductTaxAdd();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('product_id')) {
                obj['product_id'] = ApiClient.convertToType(data['product_id'], 'String');
            }
            if (data.hasOwnProperty('tax_rates')) {
                obj['tax_rates'] = ApiClient.convertToType(data['tax_rates'], [ProductTaxAddTaxRatesInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductTaxAdd</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductTaxAdd</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProductTaxAdd.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['product_id'] && !(typeof data['product_id'] === 'string' || data['product_id'] instanceof String)) {
            throw new Error("Expected the field `product_id` to be a primitive type in the JSON string but got " + data['product_id']);
        }
        if (data['tax_rates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tax_rates'])) {
                throw new Error("Expected the field `tax_rates` to be an array in the JSON data but got " + data['tax_rates']);
            }
            // validate the optional field `tax_rates` (array)
            for (const item of data['tax_rates']) {
                ProductTaxAddTaxRatesInner.validateJSON(item);
            };
        }

        return true;
    }


}

ProductTaxAdd.RequiredProperties = ["name", "tax_rates"];

/**
 * Defines tax class name where tax has to be added
 * @member {String} name
 */
ProductTaxAdd.prototype['name'] = undefined;

/**
 * Defines products specified by product id
 * @member {String} product_id
 */
ProductTaxAdd.prototype['product_id'] = undefined;

/**
 * Defines tax rates of specified tax classes
 * @member {Array.<module:model/ProductTaxAddTaxRatesInner>} tax_rates
 */
ProductTaxAdd.prototype['tax_rates'] = undefined;






export default ProductTaxAdd;

