/**
 * Frankie Financial API
 * ------   This API allows developers to integrate the Frankie Financial Compliance Utility into their applications. The API allows:   - Checking name, address, date of birth against national databases   - Validating Australian driver's licences, passports, medicare, visas and other Australian national ID documents   - Validating Australian electricity bills   - Validating NZ driver's licences   - Validating Chinese bank cards and national ID card   - Validating International passports and national ID documents   - PEP, Sanctions, Watchlist and adverse media checking   - Australian visa checks    - Fraud list and fraud background checks   - ID validation and selfie check comparisons.    ------     Industry specific services    - Comparing Australian electricity retailers for a better deal.  ------     KYB specific services    - Query organisation ownership   - Perform KYC & AML checks on shareholders, beneficial owners and office bearers.   - Query credit score and credit reports   - International company searches   - International company profiles    ------   The full version of this documentation along with supplemental articles can be found here:   - https://apidocs.frankiefinancial.com/  The traditional Swagger view of this documentation can be found here:   - https://app.swaggerhub.com/apis-docs/FrankieFinancial/kycutility  ------   Sandbox base URL is:   - https://api.demo.frankiefinancial.io/compliance/v1.2      - We do have an old sandbox at https://sandbox.frankiefinancial.com/compliance/v1.2 but this has been retired.    - All calls are the same as production, only with canned data.     - Full Swagger definition, along with test data for playing in the sandbox can be obtained once initial commercial discussions have commenced.    - Production and optional UAT access will be opened up only to those with a signed commercial contract.    ------   Contact us at hello@frankiefinancial.com to speak with a sales rep about issuing a Customer ID and Sandbox api key. 
 *
 * The version of the OpenAPI document: 1.5.3
 * Contact: dev-support@frankiefinancial.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BackgroundCheckResultObjectContainer from './BackgroundCheckResultObjectContainer';

/**
 * The AMLResultSet model module.
 * @module model/AMLResultSet
 * @version 1.5.3
 */
class AMLResultSet {
    /**
     * Constructs a new <code>AMLResultSet</code>.
     * Wrapper object to contain a single set of AML check results.
     * @alias module:model/AMLResultSet
     */
    constructor() { 
        
        AMLResultSet.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AMLResultSet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AMLResultSet} obj Optional instance to populate.
     * @return {module:model/AMLResultSet} The populated <code>AMLResultSet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AMLResultSet();

            if (data.hasOwnProperty('checkResultsListMedia')) {
                obj['checkResultsListMedia'] = ApiClient.convertToType(data['checkResultsListMedia'], [BackgroundCheckResultObjectContainer]);
            }
            if (data.hasOwnProperty('checkResultsListPEP')) {
                obj['checkResultsListPEP'] = ApiClient.convertToType(data['checkResultsListPEP'], [BackgroundCheckResultObjectContainer]);
            }
            if (data.hasOwnProperty('checkResultsListSanctions')) {
                obj['checkResultsListSanctions'] = ApiClient.convertToType(data['checkResultsListSanctions'], [BackgroundCheckResultObjectContainer]);
            }
            if (data.hasOwnProperty('checkResultsListWatchlists')) {
                obj['checkResultsListWatchlists'] = ApiClient.convertToType(data['checkResultsListWatchlists'], [BackgroundCheckResultObjectContainer]);
            }
            if (data.hasOwnProperty('groupDetails')) {
                obj['groupDetails'] = BackgroundCheckResultObjectContainer.constructFromObject(data['groupDetails']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AMLResultSet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AMLResultSet</code>.
     */
    static validateJSON(data) {
        if (data['checkResultsListMedia']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checkResultsListMedia'])) {
                throw new Error("Expected the field `checkResultsListMedia` to be an array in the JSON data but got " + data['checkResultsListMedia']);
            }
            // validate the optional field `checkResultsListMedia` (array)
            for (const item of data['checkResultsListMedia']) {
                BackgroundCheckResultObjectContainer.validateJSON(item);
            };
        }
        if (data['checkResultsListPEP']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checkResultsListPEP'])) {
                throw new Error("Expected the field `checkResultsListPEP` to be an array in the JSON data but got " + data['checkResultsListPEP']);
            }
            // validate the optional field `checkResultsListPEP` (array)
            for (const item of data['checkResultsListPEP']) {
                BackgroundCheckResultObjectContainer.validateJSON(item);
            };
        }
        if (data['checkResultsListSanctions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checkResultsListSanctions'])) {
                throw new Error("Expected the field `checkResultsListSanctions` to be an array in the JSON data but got " + data['checkResultsListSanctions']);
            }
            // validate the optional field `checkResultsListSanctions` (array)
            for (const item of data['checkResultsListSanctions']) {
                BackgroundCheckResultObjectContainer.validateJSON(item);
            };
        }
        if (data['checkResultsListWatchlists']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checkResultsListWatchlists'])) {
                throw new Error("Expected the field `checkResultsListWatchlists` to be an array in the JSON data but got " + data['checkResultsListWatchlists']);
            }
            // validate the optional field `checkResultsListWatchlists` (array)
            for (const item of data['checkResultsListWatchlists']) {
                BackgroundCheckResultObjectContainer.validateJSON(item);
            };
        }
        // validate the optional field `groupDetails`
        if (data['groupDetails']) { // data not null
          BackgroundCheckResultObjectContainer.validateJSON(data['groupDetails']);
        }

        return true;
    }


}



/**
 * Collection of check results for the entity being found in any adverse media  An array sorted by type, then reverse chronological order of some/all background checks done on this entity. Older checks may have been previously done by you or another institution, and if so, these will be listed and appropriately anonymised/obfuscated. 
 * @member {Array.<module:model/BackgroundCheckResultObjectContainer>} checkResultsListMedia
 */
AMLResultSet.prototype['checkResultsListMedia'] = undefined;

/**
 * Collection of check results for the entity being a Politically Exposed Person  An array sorted by type, then reverse chronological order of some/all background checks done on this entity. Older checks may have been previously done by you or another institution, and if so, these will be listed and appropriately anonymised/obfuscated. 
 * @member {Array.<module:model/BackgroundCheckResultObjectContainer>} checkResultsListPEP
 */
AMLResultSet.prototype['checkResultsListPEP'] = undefined;

/**
 * Collection of check results for the entity being on a sanctions list  An array sorted by type, then reverse chronological order of some/all background checks done on this entity. Older checks may have been previously done by you or another institution, and if so, these will be listed and appropriately anonymised/obfuscated. 
 * @member {Array.<module:model/BackgroundCheckResultObjectContainer>} checkResultsListSanctions
 */
AMLResultSet.prototype['checkResultsListSanctions'] = undefined;

/**
 * Collection of check results for the entity being on a watchlist  An array sorted by type, then reverse chronological order of some/all background checks done on this entity. Older checks may have been previously done by you or another institution, and if so, these will be listed and appropriately anonymised/obfuscated. 
 * @member {Array.<module:model/BackgroundCheckResultObjectContainer>} checkResultsListWatchlists
 */
AMLResultSet.prototype['checkResultsListWatchlists'] = undefined;

/**
 * @member {module:model/BackgroundCheckResultObjectContainer} groupDetails
 */
AMLResultSet.prototype['groupDetails'] = undefined;






export default AMLResultSet;

