/**
 * Vectara REST API
 * Vectara is a neural search platform, built for developers to get the most out of their data.  You can sign up for an account at [https://vectara.com](https://vectara.com).
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@vectara.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ComvectaraStatus from './ComvectaraStatus';
import ServingResponse from './ServingResponse';
import ServingResponseSetDocument from './ServingResponseSetDocument';

/**
 * The ServingResponseSet model module.
 * @module model/ServingResponseSet
 * @version 1.0.0
 */
class ServingResponseSet {
    /**
     * Constructs a new <code>ServingResponseSet</code>.
     * @alias module:model/ServingResponseSet
     */
    constructor() { 
        
        ServingResponseSet.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ServingResponseSet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServingResponseSet} obj Optional instance to populate.
     * @return {module:model/ServingResponseSet} The populated <code>ServingResponseSet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServingResponseSet();

            if (data.hasOwnProperty('document')) {
                obj['document'] = ApiClient.convertToType(data['document'], [ServingResponseSetDocument]);
            }
            if (data.hasOwnProperty('response')) {
                obj['response'] = ApiClient.convertToType(data['response'], [ServingResponse]);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], [ComvectaraStatus]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServingResponseSet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServingResponseSet</code>.
     */
    static validateJSON(data) {
        if (data['document']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['document'])) {
                throw new Error("Expected the field `document` to be an array in the JSON data but got " + data['document']);
            }
            // validate the optional field `document` (array)
            for (const item of data['document']) {
                ServingResponseSetDocument.validateJSON(item);
            };
        }
        if (data['response']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['response'])) {
                throw new Error("Expected the field `response` to be an array in the JSON data but got " + data['response']);
            }
            // validate the optional field `response` (array)
            for (const item of data['response']) {
                ServingResponse.validateJSON(item);
            };
        }
        if (data['status']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['status'])) {
                throw new Error("Expected the field `status` to be an array in the JSON data but got " + data['status']);
            }
            // validate the optional field `status` (array)
            for (const item of data['status']) {
                ComvectaraStatus.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/ServingResponseSetDocument>} document
 */
ServingResponseSet.prototype['document'] = undefined;

/**
 * @member {Array.<module:model/ServingResponse>} response
 */
ServingResponseSet.prototype['response'] = undefined;

/**
 * Potentially multiple warnings.
 * @member {Array.<module:model/ComvectaraStatus>} status
 */
ServingResponseSet.prototype['status'] = undefined;






export default ServingResponseSet;

