/**
 * Vectara REST API
 * Vectara is a neural search platform, built for developers to get the most out of their data.  You can sign up for an account at [https://vectara.com](https://vectara.com).
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@vectara.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import QueryRequestRerankingConfig from './QueryRequestRerankingConfig';
import ServingCorpusKey from './ServingCorpusKey';

/**
 * The ServingQueryRequest model module.
 * @module model/ServingQueryRequest
 * @version 1.0.0
 */
class ServingQueryRequest {
    /**
     * Constructs a new <code>ServingQueryRequest</code>.
     * @alias module:model/ServingQueryRequest
     */
    constructor() { 
        
        ServingQueryRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ServingQueryRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServingQueryRequest} obj Optional instance to populate.
     * @return {module:model/ServingQueryRequest} The populated <code>ServingQueryRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServingQueryRequest();

            if (data.hasOwnProperty('corpusKey')) {
                obj['corpusKey'] = ApiClient.convertToType(data['corpusKey'], [ServingCorpusKey]);
            }
            if (data.hasOwnProperty('numResults')) {
                obj['numResults'] = ApiClient.convertToType(data['numResults'], 'Number');
            }
            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('rerankingConfig')) {
                obj['rerankingConfig'] = QueryRequestRerankingConfig.constructFromObject(data['rerankingConfig']);
            }
            if (data.hasOwnProperty('start')) {
                obj['start'] = ApiClient.convertToType(data['start'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServingQueryRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServingQueryRequest</code>.
     */
    static validateJSON(data) {
        if (data['corpusKey']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['corpusKey'])) {
                throw new Error("Expected the field `corpusKey` to be an array in the JSON data but got " + data['corpusKey']);
            }
            // validate the optional field `corpusKey` (array)
            for (const item of data['corpusKey']) {
                ServingCorpusKey.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // validate the optional field `rerankingConfig`
        if (data['rerankingConfig']) { // data not null
          QueryRequestRerankingConfig.validateJSON(data['rerankingConfig']);
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/ServingCorpusKey>} corpusKey
 */
ServingQueryRequest.prototype['corpusKey'] = undefined;

/**
 * The number of results to return.
 * @member {Number} numResults
 */
ServingQueryRequest.prototype['numResults'] = undefined;

/**
 * The query text to use from the end user.
 * @member {String} query
 */
ServingQueryRequest.prototype['query'] = undefined;

/**
 * @member {module:model/QueryRequestRerankingConfig} rerankingConfig
 */
ServingQueryRequest.prototype['rerankingConfig'] = undefined;

/**
 * @member {Number} start
 */
ServingQueryRequest.prototype['start'] = undefined;






export default ServingQueryRequest;

