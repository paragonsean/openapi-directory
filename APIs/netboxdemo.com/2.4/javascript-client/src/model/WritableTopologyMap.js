/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.4
 * Contact: netbox@digitalocean.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WritableTopologyMap model module.
 * @module model/WritableTopologyMap
 * @version 2.4
 */
class WritableTopologyMap {
    /**
     * Constructs a new <code>WritableTopologyMap</code>.
     * @alias module:model/WritableTopologyMap
     * @param devicePatterns {String} Identify devices to include in the diagram using regular expressions, one per line. Each line will result in a new tier of the drawing. Separate multiple regexes within a line using semicolons. Devices will be rendered in the order they are defined.
     * @param name {String} 
     * @param slug {String} 
     */
    constructor(devicePatterns, name, slug) { 
        
        WritableTopologyMap.initialize(this, devicePatterns, name, slug);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, devicePatterns, name, slug) { 
        obj['device_patterns'] = devicePatterns;
        obj['name'] = name;
        obj['slug'] = slug;
    }

    /**
     * Constructs a <code>WritableTopologyMap</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WritableTopologyMap} obj Optional instance to populate.
     * @return {module:model/WritableTopologyMap} The populated <code>WritableTopologyMap</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WritableTopologyMap();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('device_patterns')) {
                obj['device_patterns'] = ApiClient.convertToType(data['device_patterns'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('site')) {
                obj['site'] = ApiClient.convertToType(data['site'], 'Number');
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WritableTopologyMap</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WritableTopologyMap</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WritableTopologyMap.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['device_patterns'] && !(typeof data['device_patterns'] === 'string' || data['device_patterns'] instanceof String)) {
            throw new Error("Expected the field `device_patterns` to be a primitive type in the JSON string but got " + data['device_patterns']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }

        return true;
    }


}

WritableTopologyMap.RequiredProperties = ["device_patterns", "name", "slug"];

/**
 * @member {String} description
 */
WritableTopologyMap.prototype['description'] = undefined;

/**
 * Identify devices to include in the diagram using regular expressions, one per line. Each line will result in a new tier of the drawing. Separate multiple regexes within a line using semicolons. Devices will be rendered in the order they are defined.
 * @member {String} device_patterns
 */
WritableTopologyMap.prototype['device_patterns'] = undefined;

/**
 * @member {Number} id
 */
WritableTopologyMap.prototype['id'] = undefined;

/**
 * @member {String} name
 */
WritableTopologyMap.prototype['name'] = undefined;

/**
 * @member {Number} site
 */
WritableTopologyMap.prototype['site'] = undefined;

/**
 * @member {String} slug
 */
WritableTopologyMap.prototype['slug'] = undefined;






export default WritableTopologyMap;

