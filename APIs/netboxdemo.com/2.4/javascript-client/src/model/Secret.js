/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.4
 * Contact: netbox@digitalocean.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NestedDevice from './NestedDevice';
import NestedSecretRole from './NestedSecretRole';

/**
 * The Secret model module.
 * @module model/Secret
 * @version 2.4
 */
class Secret {
    /**
     * Constructs a new <code>Secret</code>.
     * @alias module:model/Secret
     * @param device {module:model/NestedDevice} 
     * @param plaintext {String} 
     * @param role {module:model/NestedSecretRole} 
     */
    constructor(device, plaintext, role) { 
        
        Secret.initialize(this, device, plaintext, role);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, device, plaintext, role) { 
        obj['device'] = device;
        obj['plaintext'] = plaintext;
        obj['role'] = role;
    }

    /**
     * Constructs a <code>Secret</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Secret} obj Optional instance to populate.
     * @return {module:model/Secret} The populated <code>Secret</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Secret();

            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Date');
            }
            if (data.hasOwnProperty('custom_fields')) {
                obj['custom_fields'] = ApiClient.convertToType(data['custom_fields'], Object);
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = NestedDevice.constructFromObject(data['device']);
            }
            if (data.hasOwnProperty('hash')) {
                obj['hash'] = ApiClient.convertToType(data['hash'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('plaintext')) {
                obj['plaintext'] = ApiClient.convertToType(data['plaintext'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = NestedSecretRole.constructFromObject(data['role']);
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Secret</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Secret</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Secret.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `device`
        if (data['device']) { // data not null
          NestedDevice.validateJSON(data['device']);
        }
        // ensure the json data is a string
        if (data['hash'] && !(typeof data['hash'] === 'string' || data['hash'] instanceof String)) {
            throw new Error("Expected the field `hash` to be a primitive type in the JSON string but got " + data['hash']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['plaintext'] && !(typeof data['plaintext'] === 'string' || data['plaintext'] instanceof String)) {
            throw new Error("Expected the field `plaintext` to be a primitive type in the JSON string but got " + data['plaintext']);
        }
        // validate the optional field `role`
        if (data['role']) { // data not null
          NestedSecretRole.validateJSON(data['role']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }

        return true;
    }


}

Secret.RequiredProperties = ["device", "plaintext", "role"];

/**
 * @member {Date} created
 */
Secret.prototype['created'] = undefined;

/**
 * @member {Object} custom_fields
 */
Secret.prototype['custom_fields'] = undefined;

/**
 * @member {module:model/NestedDevice} device
 */
Secret.prototype['device'] = undefined;

/**
 * @member {String} hash
 */
Secret.prototype['hash'] = undefined;

/**
 * @member {Number} id
 */
Secret.prototype['id'] = undefined;

/**
 * @member {Date} last_updated
 */
Secret.prototype['last_updated'] = undefined;

/**
 * @member {String} name
 */
Secret.prototype['name'] = undefined;

/**
 * @member {String} plaintext
 */
Secret.prototype['plaintext'] = undefined;

/**
 * @member {module:model/NestedSecretRole} role
 */
Secret.prototype['role'] = undefined;

/**
 * @member {Array.<String>} tags
 */
Secret.prototype['tags'] = undefined;






export default Secret;

