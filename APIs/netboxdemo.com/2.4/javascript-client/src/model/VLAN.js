/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.4
 * Contact: netbox@digitalocean.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NestedRole from './NestedRole';
import NestedSite from './NestedSite';
import NestedTenant from './NestedTenant';
import NestedVLANGroup from './NestedVLANGroup';
import Status from './Status';

/**
 * The VLAN model module.
 * @module model/VLAN
 * @version 2.4
 */
class VLAN {
    /**
     * Constructs a new <code>VLAN</code>.
     * @alias module:model/VLAN
     * @param name {String} 
     * @param vid {Number} 
     */
    constructor(name, vid) { 
        
        VLAN.initialize(this, name, vid);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, vid) { 
        obj['name'] = name;
        obj['vid'] = vid;
    }

    /**
     * Constructs a <code>VLAN</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VLAN} obj Optional instance to populate.
     * @return {module:model/VLAN} The populated <code>VLAN</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VLAN();

            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Date');
            }
            if (data.hasOwnProperty('custom_fields')) {
                obj['custom_fields'] = ApiClient.convertToType(data['custom_fields'], Object);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('display_name')) {
                obj['display_name'] = ApiClient.convertToType(data['display_name'], 'String');
            }
            if (data.hasOwnProperty('group')) {
                obj['group'] = NestedVLANGroup.constructFromObject(data['group']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = NestedRole.constructFromObject(data['role']);
            }
            if (data.hasOwnProperty('site')) {
                obj['site'] = NestedSite.constructFromObject(data['site']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = Status.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], ['String']);
            }
            if (data.hasOwnProperty('tenant')) {
                obj['tenant'] = NestedTenant.constructFromObject(data['tenant']);
            }
            if (data.hasOwnProperty('vid')) {
                obj['vid'] = ApiClient.convertToType(data['vid'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VLAN</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VLAN</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VLAN.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['display_name'] && !(typeof data['display_name'] === 'string' || data['display_name'] instanceof String)) {
            throw new Error("Expected the field `display_name` to be a primitive type in the JSON string but got " + data['display_name']);
        }
        // validate the optional field `group`
        if (data['group']) { // data not null
          NestedVLANGroup.validateJSON(data['group']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `role`
        if (data['role']) { // data not null
          NestedRole.validateJSON(data['role']);
        }
        // validate the optional field `site`
        if (data['site']) { // data not null
          NestedSite.validateJSON(data['site']);
        }
        // validate the optional field `status`
        if (data['status']) { // data not null
          Status.validateJSON(data['status']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }
        // validate the optional field `tenant`
        if (data['tenant']) { // data not null
          NestedTenant.validateJSON(data['tenant']);
        }

        return true;
    }


}

VLAN.RequiredProperties = ["name", "vid"];

/**
 * @member {Date} created
 */
VLAN.prototype['created'] = undefined;

/**
 * @member {Object} custom_fields
 */
VLAN.prototype['custom_fields'] = undefined;

/**
 * @member {String} description
 */
VLAN.prototype['description'] = undefined;

/**
 * @member {String} display_name
 */
VLAN.prototype['display_name'] = undefined;

/**
 * @member {module:model/NestedVLANGroup} group
 */
VLAN.prototype['group'] = undefined;

/**
 * @member {Number} id
 */
VLAN.prototype['id'] = undefined;

/**
 * @member {Date} last_updated
 */
VLAN.prototype['last_updated'] = undefined;

/**
 * @member {String} name
 */
VLAN.prototype['name'] = undefined;

/**
 * @member {module:model/NestedRole} role
 */
VLAN.prototype['role'] = undefined;

/**
 * @member {module:model/NestedSite} site
 */
VLAN.prototype['site'] = undefined;

/**
 * @member {module:model/Status} status
 */
VLAN.prototype['status'] = undefined;

/**
 * @member {Array.<String>} tags
 */
VLAN.prototype['tags'] = undefined;

/**
 * @member {module:model/NestedTenant} tenant
 */
VLAN.prototype['tenant'] = undefined;

/**
 * @member {Number} vid
 */
VLAN.prototype['vid'] = undefined;






export default VLAN;

