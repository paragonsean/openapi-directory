/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Provider model module.
 * @module model/Provider
 * @version 2.8
 */
class Provider {
    /**
     * Constructs a new <code>Provider</code>.
     * @alias module:model/Provider
     * @param name {String} 
     * @param slug {String} 
     */
    constructor(name, slug) { 
        
        Provider.initialize(this, name, slug);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, slug) { 
        obj['name'] = name;
        obj['slug'] = slug;
    }

    /**
     * Constructs a <code>Provider</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Provider} obj Optional instance to populate.
     * @return {module:model/Provider} The populated <code>Provider</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Provider();

            if (data.hasOwnProperty('account')) {
                obj['account'] = ApiClient.convertToType(data['account'], 'String');
            }
            if (data.hasOwnProperty('admin_contact')) {
                obj['admin_contact'] = ApiClient.convertToType(data['admin_contact'], 'String');
            }
            if (data.hasOwnProperty('asn')) {
                obj['asn'] = ApiClient.convertToType(data['asn'], 'Number');
            }
            if (data.hasOwnProperty('circuit_count')) {
                obj['circuit_count'] = ApiClient.convertToType(data['circuit_count'], 'Number');
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Date');
            }
            if (data.hasOwnProperty('custom_fields')) {
                obj['custom_fields'] = ApiClient.convertToType(data['custom_fields'], Object);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('noc_contact')) {
                obj['noc_contact'] = ApiClient.convertToType(data['noc_contact'], 'String');
            }
            if (data.hasOwnProperty('portal_url')) {
                obj['portal_url'] = ApiClient.convertToType(data['portal_url'], 'String');
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Provider</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Provider</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Provider.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account'] && !(typeof data['account'] === 'string' || data['account'] instanceof String)) {
            throw new Error("Expected the field `account` to be a primitive type in the JSON string but got " + data['account']);
        }
        // ensure the json data is a string
        if (data['admin_contact'] && !(typeof data['admin_contact'] === 'string' || data['admin_contact'] instanceof String)) {
            throw new Error("Expected the field `admin_contact` to be a primitive type in the JSON string but got " + data['admin_contact']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['noc_contact'] && !(typeof data['noc_contact'] === 'string' || data['noc_contact'] instanceof String)) {
            throw new Error("Expected the field `noc_contact` to be a primitive type in the JSON string but got " + data['noc_contact']);
        }
        // ensure the json data is a string
        if (data['portal_url'] && !(typeof data['portal_url'] === 'string' || data['portal_url'] instanceof String)) {
            throw new Error("Expected the field `portal_url` to be a primitive type in the JSON string but got " + data['portal_url']);
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }

        return true;
    }


}

Provider.RequiredProperties = ["name", "slug"];

/**
 * @member {String} account
 */
Provider.prototype['account'] = undefined;

/**
 * @member {String} admin_contact
 */
Provider.prototype['admin_contact'] = undefined;

/**
 * 32-bit autonomous system number
 * @member {Number} asn
 */
Provider.prototype['asn'] = undefined;

/**
 * @member {Number} circuit_count
 */
Provider.prototype['circuit_count'] = undefined;

/**
 * @member {String} comments
 */
Provider.prototype['comments'] = undefined;

/**
 * @member {Date} created
 */
Provider.prototype['created'] = undefined;

/**
 * @member {Object} custom_fields
 */
Provider.prototype['custom_fields'] = undefined;

/**
 * @member {Number} id
 */
Provider.prototype['id'] = undefined;

/**
 * @member {Date} last_updated
 */
Provider.prototype['last_updated'] = undefined;

/**
 * @member {String} name
 */
Provider.prototype['name'] = undefined;

/**
 * @member {String} noc_contact
 */
Provider.prototype['noc_contact'] = undefined;

/**
 * @member {String} portal_url
 */
Provider.prototype['portal_url'] = undefined;

/**
 * @member {String} slug
 */
Provider.prototype['slug'] = undefined;

/**
 * @member {Array.<String>} tags
 */
Provider.prototype['tags'] = undefined;






export default Provider;

