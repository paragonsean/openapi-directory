/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FrontPortRearPort from './FrontPortRearPort';
import NestedCable from './NestedCable';
import NestedDevice from './NestedDevice';
import Type2 from './Type2';

/**
 * The FrontPort model module.
 * @module model/FrontPort
 * @version 2.8
 */
class FrontPort {
    /**
     * Constructs a new <code>FrontPort</code>.
     * @alias module:model/FrontPort
     * @param device {module:model/NestedDevice} 
     * @param name {String} 
     * @param rearPort {module:model/FrontPortRearPort} 
     * @param type {module:model/Type2} 
     */
    constructor(device, name, rearPort, type) { 
        
        FrontPort.initialize(this, device, name, rearPort, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, device, name, rearPort, type) { 
        obj['device'] = device;
        obj['name'] = name;
        obj['rear_port'] = rearPort;
        obj['rear_port_position'] = 1;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>FrontPort</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FrontPort} obj Optional instance to populate.
     * @return {module:model/FrontPort} The populated <code>FrontPort</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FrontPort();

            if (data.hasOwnProperty('cable')) {
                obj['cable'] = NestedCable.constructFromObject(data['cable']);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = NestedDevice.constructFromObject(data['device']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('rear_port')) {
                obj['rear_port'] = FrontPortRearPort.constructFromObject(data['rear_port']);
            }
            if (data.hasOwnProperty('rear_port_position')) {
                obj['rear_port_position'] = ApiClient.convertToType(data['rear_port_position'], 'Number');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], ['String']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = Type2.constructFromObject(data['type']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FrontPort</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FrontPort</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FrontPort.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `cable`
        if (data['cable']) { // data not null
          NestedCable.validateJSON(data['cable']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // validate the optional field `device`
        if (data['device']) { // data not null
          NestedDevice.validateJSON(data['device']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `rear_port`
        if (data['rear_port']) { // data not null
          FrontPortRearPort.validateJSON(data['rear_port']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }
        // validate the optional field `type`
        if (data['type']) { // data not null
          Type2.validateJSON(data['type']);
        }

        return true;
    }


}

FrontPort.RequiredProperties = ["device", "name", "rear_port", "type"];

/**
 * @member {module:model/NestedCable} cable
 */
FrontPort.prototype['cable'] = undefined;

/**
 * @member {String} description
 */
FrontPort.prototype['description'] = undefined;

/**
 * @member {module:model/NestedDevice} device
 */
FrontPort.prototype['device'] = undefined;

/**
 * @member {Number} id
 */
FrontPort.prototype['id'] = undefined;

/**
 * @member {String} name
 */
FrontPort.prototype['name'] = undefined;

/**
 * @member {module:model/FrontPortRearPort} rear_port
 */
FrontPort.prototype['rear_port'] = undefined;

/**
 * @member {Number} rear_port_position
 * @default 1
 */
FrontPort.prototype['rear_port_position'] = 1;

/**
 * @member {Array.<String>} tags
 */
FrontPort.prototype['tags'] = undefined;

/**
 * @member {module:model/Type2} type
 */
FrontPort.prototype['type'] = undefined;






export default FrontPort;

