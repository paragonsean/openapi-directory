/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WritableRackGroup model module.
 * @module model/WritableRackGroup
 * @version 2.8
 */
class WritableRackGroup {
    /**
     * Constructs a new <code>WritableRackGroup</code>.
     * @alias module:model/WritableRackGroup
     * @param name {String} 
     * @param site {Number} 
     * @param slug {String} 
     */
    constructor(name, site, slug) { 
        
        WritableRackGroup.initialize(this, name, site, slug);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, site, slug) { 
        obj['name'] = name;
        obj['site'] = site;
        obj['slug'] = slug;
    }

    /**
     * Constructs a <code>WritableRackGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WritableRackGroup} obj Optional instance to populate.
     * @return {module:model/WritableRackGroup} The populated <code>WritableRackGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WritableRackGroup();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('parent')) {
                obj['parent'] = ApiClient.convertToType(data['parent'], 'Number');
            }
            if (data.hasOwnProperty('rack_count')) {
                obj['rack_count'] = ApiClient.convertToType(data['rack_count'], 'Number');
            }
            if (data.hasOwnProperty('site')) {
                obj['site'] = ApiClient.convertToType(data['site'], 'Number');
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WritableRackGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WritableRackGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WritableRackGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }

        return true;
    }


}

WritableRackGroup.RequiredProperties = ["name", "site", "slug"];

/**
 * @member {String} description
 */
WritableRackGroup.prototype['description'] = undefined;

/**
 * @member {Number} id
 */
WritableRackGroup.prototype['id'] = undefined;

/**
 * @member {String} name
 */
WritableRackGroup.prototype['name'] = undefined;

/**
 * @member {Number} parent
 */
WritableRackGroup.prototype['parent'] = undefined;

/**
 * @member {Number} rack_count
 */
WritableRackGroup.prototype['rack_count'] = undefined;

/**
 * @member {Number} site
 */
WritableRackGroup.prototype['site'] = undefined;

/**
 * @member {String} slug
 */
WritableRackGroup.prototype['slug'] = undefined;






export default WritableRackGroup;

