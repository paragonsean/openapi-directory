/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ConnectionStatus from './ConnectionStatus';
import NestedDevice from './NestedDevice';

/**
 * The NestedPowerPort model module.
 * @module model/NestedPowerPort
 * @version 2.8
 */
class NestedPowerPort {
    /**
     * Constructs a new <code>NestedPowerPort</code>.
     * @alias module:model/NestedPowerPort
     * @param name {String} 
     */
    constructor(name) { 
        
        NestedPowerPort.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>NestedPowerPort</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NestedPowerPort} obj Optional instance to populate.
     * @return {module:model/NestedPowerPort} The populated <code>NestedPowerPort</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NestedPowerPort();

            if (data.hasOwnProperty('cable')) {
                obj['cable'] = ApiClient.convertToType(data['cable'], 'Number');
            }
            if (data.hasOwnProperty('connection_status')) {
                obj['connection_status'] = ConnectionStatus.constructFromObject(data['connection_status']);
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = NestedDevice.constructFromObject(data['device']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NestedPowerPort</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NestedPowerPort</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NestedPowerPort.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `connection_status`
        if (data['connection_status']) { // data not null
          ConnectionStatus.validateJSON(data['connection_status']);
        }
        // validate the optional field `device`
        if (data['device']) { // data not null
          NestedDevice.validateJSON(data['device']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

NestedPowerPort.RequiredProperties = ["name"];

/**
 * @member {Number} cable
 */
NestedPowerPort.prototype['cable'] = undefined;

/**
 * @member {module:model/ConnectionStatus} connection_status
 */
NestedPowerPort.prototype['connection_status'] = undefined;

/**
 * @member {module:model/NestedDevice} device
 */
NestedPowerPort.prototype['device'] = undefined;

/**
 * @member {Number} id
 */
NestedPowerPort.prototype['id'] = undefined;

/**
 * @member {String} name
 */
NestedPowerPort.prototype['name'] = undefined;

/**
 * @member {String} url
 */
NestedPowerPort.prototype['url'] = undefined;






export default NestedPowerPort;

