/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 2.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NestedRackGroup from './NestedRackGroup';
import NestedSite from './NestedSite';

/**
 * The RackGroup model module.
 * @module model/RackGroup
 * @version 2.8
 */
class RackGroup {
    /**
     * Constructs a new <code>RackGroup</code>.
     * @alias module:model/RackGroup
     * @param name {String} 
     * @param site {module:model/NestedSite} 
     * @param slug {String} 
     */
    constructor(name, site, slug) { 
        
        RackGroup.initialize(this, name, site, slug);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, site, slug) { 
        obj['name'] = name;
        obj['site'] = site;
        obj['slug'] = slug;
    }

    /**
     * Constructs a <code>RackGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RackGroup} obj Optional instance to populate.
     * @return {module:model/RackGroup} The populated <code>RackGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RackGroup();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('parent')) {
                obj['parent'] = NestedRackGroup.constructFromObject(data['parent']);
            }
            if (data.hasOwnProperty('rack_count')) {
                obj['rack_count'] = ApiClient.convertToType(data['rack_count'], 'Number');
            }
            if (data.hasOwnProperty('site')) {
                obj['site'] = NestedSite.constructFromObject(data['site']);
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RackGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RackGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RackGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `parent`
        if (data['parent']) { // data not null
          NestedRackGroup.validateJSON(data['parent']);
        }
        // validate the optional field `site`
        if (data['site']) { // data not null
          NestedSite.validateJSON(data['site']);
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }

        return true;
    }


}

RackGroup.RequiredProperties = ["name", "site", "slug"];

/**
 * @member {String} description
 */
RackGroup.prototype['description'] = undefined;

/**
 * @member {Number} id
 */
RackGroup.prototype['id'] = undefined;

/**
 * @member {String} name
 */
RackGroup.prototype['name'] = undefined;

/**
 * @member {module:model/NestedRackGroup} parent
 */
RackGroup.prototype['parent'] = undefined;

/**
 * @member {Number} rack_count
 */
RackGroup.prototype['rack_count'] = undefined;

/**
 * @member {module:model/NestedSite} site
 */
RackGroup.prototype['site'] = undefined;

/**
 * @member {String} slug
 */
RackGroup.prototype['slug'] = undefined;






export default RackGroup;

