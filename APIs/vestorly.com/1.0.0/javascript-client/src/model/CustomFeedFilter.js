/**
 * Vestorly API
 * Vestorly Developers API
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CustomFeedFilter model module.
 * @module model/CustomFeedFilter
 * @version 1.0.0
 */
class CustomFeedFilter {
    /**
     * Constructs a new <code>CustomFeedFilter</code>.
     * @alias module:model/CustomFeedFilter
     * @param id {String} 
     * @param customFeedId {String} 
     */
    constructor(id, customFeedId) { 
        
        CustomFeedFilter.initialize(this, id, customFeedId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, customFeedId) { 
        obj['_id'] = id;
        obj['custom_feed_id'] = customFeedId;
    }

    /**
     * Constructs a <code>CustomFeedFilter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomFeedFilter} obj Optional instance to populate.
     * @return {module:model/CustomFeedFilter} The populated <code>CustomFeedFilter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomFeedFilter();

            if (data.hasOwnProperty('_id')) {
                obj['_id'] = ApiClient.convertToType(data['_id'], 'String');
            }
            if (data.hasOwnProperty('custom_feed_id')) {
                obj['custom_feed_id'] = ApiClient.convertToType(data['custom_feed_id'], 'String');
            }
            if (data.hasOwnProperty('source_ids')) {
                obj['source_ids'] = ApiClient.convertToType(data['source_ids'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomFeedFilter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomFeedFilter</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CustomFeedFilter.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['_id'] && !(typeof data['_id'] === 'string' || data['_id'] instanceof String)) {
            throw new Error("Expected the field `_id` to be a primitive type in the JSON string but got " + data['_id']);
        }
        // ensure the json data is a string
        if (data['custom_feed_id'] && !(typeof data['custom_feed_id'] === 'string' || data['custom_feed_id'] instanceof String)) {
            throw new Error("Expected the field `custom_feed_id` to be a primitive type in the JSON string but got " + data['custom_feed_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['source_ids'])) {
            throw new Error("Expected the field `source_ids` to be an array in the JSON data but got " + data['source_ids']);
        }

        return true;
    }


}

CustomFeedFilter.RequiredProperties = ["_id", "custom_feed_id"];

/**
 * @member {String} _id
 */
CustomFeedFilter.prototype['_id'] = undefined;

/**
 * @member {String} custom_feed_id
 */
CustomFeedFilter.prototype['custom_feed_id'] = undefined;

/**
 * @member {Array.<String>} source_ids
 */
CustomFeedFilter.prototype['source_ids'] = undefined;






export default CustomFeedFilter;

