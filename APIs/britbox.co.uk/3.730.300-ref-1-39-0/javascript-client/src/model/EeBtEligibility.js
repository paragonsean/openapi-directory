/**
 * Rocket Services
 * An Orchestration Layer that takes ISL services and packages them in a more targeted way for front-end applications. This in turn makes client integration easier and reduces the complexity and size of front-end applications.  Rocket is also customisable - allowing UI engineers to ‘remix’ the existing back-end services into something that best suits the application they are developing. 
 *
 * The version of the OpenAPI document: 3.730.300-ref-1-39-0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EeBtEligibility model module.
 * @module model/EeBtEligibility
 * @version 3.730.300-ref-1-39-0
 */
class EeBtEligibility {
    /**
     * Constructs a new <code>EeBtEligibility</code>.
     * @alias module:model/EeBtEligibility
     * @param eligible {Boolean} Whether or not user is eligible
     */
    constructor(eligible) { 
        
        EeBtEligibility.initialize(this, eligible);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, eligible) { 
        obj['eligible'] = eligible;
    }

    /**
     * Constructs a <code>EeBtEligibility</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EeBtEligibility} obj Optional instance to populate.
     * @return {module:model/EeBtEligibility} The populated <code>EeBtEligibility</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EeBtEligibility();

            if (data.hasOwnProperty('eligible')) {
                obj['eligible'] = ApiClient.convertToType(data['eligible'], 'Boolean');
            }
            if (data.hasOwnProperty('plan')) {
                obj['plan'] = ApiClient.convertToType(data['plan'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EeBtEligibility</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EeBtEligibility</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EeBtEligibility.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['plan'] && !(typeof data['plan'] === 'string' || data['plan'] instanceof String)) {
            throw new Error("Expected the field `plan` to be a primitive type in the JSON string but got " + data['plan']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

EeBtEligibility.RequiredProperties = ["eligible"];

/**
 * Whether or not user is eligible
 * @member {Boolean} eligible
 */
EeBtEligibility.prototype['eligible'] = undefined;

/**
 * The identifier of a plan.
 * @member {String} plan
 */
EeBtEligibility.prototype['plan'] = undefined;

/**
 * Stripe etc.
 * @member {String} source
 */
EeBtEligibility.prototype['source'] = undefined;






export default EeBtEligibility;

