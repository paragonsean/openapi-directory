/**
 * Rocket Services
 * An Orchestration Layer that takes ISL services and packages them in a more targeted way for front-end applications. This in turn makes client integration easier and reduces the complexity and size of front-end applications.  Rocket is also customisable - allowing UI engineers to ‘remix’ the existing back-end services into something that best suits the application they are developing. 
 *
 * The version of the OpenAPI document: 3.730.300-ref-1-39-0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ThemeColor from './ThemeColor';

/**
 * The Theme model module.
 * @module model/Theme
 * @version 3.730.300-ref-1-39-0
 */
class Theme {
    /**
     * Constructs a new <code>Theme</code>.
     * @alias module:model/Theme
     * @param colors {Array.<module:model/ThemeColor>} The list of colors defined for the theme.
     * @param type {module:model/Theme.TypeEnum} The type of theme.
     */
    constructor(colors, type) { 
        
        Theme.initialize(this, colors, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, colors, type) { 
        obj['colors'] = colors;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>Theme</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Theme} obj Optional instance to populate.
     * @return {module:model/Theme} The populated <code>Theme</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Theme();

            if (data.hasOwnProperty('colors')) {
                obj['colors'] = ApiClient.convertToType(data['colors'], [ThemeColor]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Theme</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Theme</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Theme.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['colors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['colors'])) {
                throw new Error("Expected the field `colors` to be an array in the JSON data but got " + data['colors']);
            }
            // validate the optional field `colors` (array)
            for (const item of data['colors']) {
                ThemeColor.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Theme.RequiredProperties = ["colors", "type"];

/**
 * The list of colors defined for the theme.
 * @member {Array.<module:model/ThemeColor>} colors
 */
Theme.prototype['colors'] = undefined;

/**
 * The type of theme.
 * @member {module:model/Theme.TypeEnum} type
 */
Theme.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Theme['TypeEnum'] = {

    /**
     * value: "Background"
     * @const
     */
    "Background": "Background",

    /**
     * value: "Text"
     * @const
     */
    "Text": "Text",

    /**
     * value: "Custom"
     * @const
     */
    "Custom": "Custom"
};



export default Theme;

