/**
 * Rocket Services
 * An Orchestration Layer that takes ISL services and packages them in a more targeted way for front-end applications. This in turn makes client integration easier and reduces the complexity and size of front-end applications.  Rocket is also customisable - allowing UI engineers to ‘remix’ the existing back-end services into something that best suits the application they are developing. 
 *
 * The version of the OpenAPI document: 3.730.300-ref-1-39-0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccountTokenRequest model module.
 * @module model/AccountTokenRequest
 * @version 3.730.300-ref-1-39-0
 */
class AccountTokenRequest {
    /**
     * Constructs a new <code>AccountTokenRequest</code>.
     * @alias module:model/AccountTokenRequest
     * @param email {String} The email associated with the account.
     * @param password {String} The password associated with the account.
     * @param scopes {Array.<module:model/AccountTokenRequest.ScopesEnum>} The scope(s) of the tokens required. For each scope listed an Account and Profile token of that scope will be returned 
     */
    constructor(email, password, scopes) { 
        
        AccountTokenRequest.initialize(this, email, password, scopes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, password, scopes) { 
        obj['email'] = email;
        obj['password'] = password;
        obj['scopes'] = scopes;
    }

    /**
     * Constructs a <code>AccountTokenRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccountTokenRequest} obj Optional instance to populate.
     * @return {module:model/AccountTokenRequest} The populated <code>AccountTokenRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccountTokenRequest();

            if (data.hasOwnProperty('cookieType')) {
                obj['cookieType'] = ApiClient.convertToType(data['cookieType'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('scopes')) {
                obj['scopes'] = ApiClient.convertToType(data['scopes'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccountTokenRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccountTokenRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AccountTokenRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cookieType'] && !(typeof data['cookieType'] === 'string' || data['cookieType'] instanceof String)) {
            throw new Error("Expected the field `cookieType` to be a primitive type in the JSON string but got " + data['cookieType']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scopes'])) {
            throw new Error("Expected the field `scopes` to be an array in the JSON data but got " + data['scopes']);
        }

        return true;
    }


}

AccountTokenRequest.RequiredProperties = ["email", "password", "scopes"];

/**
 * If you specify a cookie type then a content filter cookie will be returned along with the token(s). This is only intended for web based clients which need to pass the cookies to a server to render a page based on the user's content filters e.g subscription code.  If type `Session` the cookie will be session based. If type `Persistent` the cookie will have a medium term lifespan. If undefined no cookies will be set. 
 * @member {module:model/AccountTokenRequest.CookieTypeEnum} cookieType
 */
AccountTokenRequest.prototype['cookieType'] = undefined;

/**
 * The email associated with the account.
 * @member {String} email
 */
AccountTokenRequest.prototype['email'] = undefined;

/**
 * The password associated with the account.
 * @member {String} password
 */
AccountTokenRequest.prototype['password'] = undefined;

/**
 * The scope(s) of the tokens required. For each scope listed an Account and Profile token of that scope will be returned 
 * @member {Array.<module:model/AccountTokenRequest.ScopesEnum>} scopes
 */
AccountTokenRequest.prototype['scopes'] = undefined;





/**
 * Allowed values for the <code>cookieType</code> property.
 * @enum {String}
 * @readonly
 */
AccountTokenRequest['CookieTypeEnum'] = {

    /**
     * value: "Session"
     * @const
     */
    "Session": "Session",

    /**
     * value: "Persistent"
     * @const
     */
    "Persistent": "Persistent"
};


/**
 * Allowed values for the <code>scopes</code> property.
 * @enum {String}
 * @readonly
 */
AccountTokenRequest['ScopesEnum'] = {

    /**
     * value: "Catalog"
     * @const
     */
    "Catalog": "Catalog",

    /**
     * value: "Commerce"
     * @const
     */
    "Commerce": "Commerce",

    /**
     * value: "Settings"
     * @const
     */
    "Settings": "Settings",

    /**
     * value: "Playback"
     * @const
     */
    "Playback": "Playback"
};



export default AccountTokenRequest;

