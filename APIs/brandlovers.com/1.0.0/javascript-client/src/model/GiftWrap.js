/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GiftWrap model module.
 * @module model/GiftWrap
 * @version 1.0.0
 */
class GiftWrap {
    /**
     * Constructs a new <code>GiftWrap</code>.
     * @alias module:model/GiftWrap
     * @param available {Boolean} Flag that defines if this product is eligible for giftwrapping
     * @param value {Number} Amount charged for gift wrap. All prices must be provided in cents. No commas or periods are accepeted. For example one dollar should be informed as 100. $1,2345.67 should be informed solely as 1234567
     */
    constructor(available, value) { 
        
        GiftWrap.initialize(this, available, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, available, value) { 
        obj['available'] = available;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>GiftWrap</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GiftWrap} obj Optional instance to populate.
     * @return {module:model/GiftWrap} The populated <code>GiftWrap</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GiftWrap();

            if (data.hasOwnProperty('available')) {
                obj['available'] = ApiClient.convertToType(data['available'], 'Boolean');
            }
            if (data.hasOwnProperty('messageSupport')) {
                obj['messageSupport'] = ApiClient.convertToType(data['messageSupport'], 'Boolean');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GiftWrap</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GiftWrap</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GiftWrap.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

GiftWrap.RequiredProperties = ["available", "value"];

/**
 * Flag that defines if this product is eligible for giftwrapping
 * @member {Boolean} available
 */
GiftWrap.prototype['available'] = undefined;

/**
 * Flag that defines if seller supports giftwrapp with a message
 * @member {Boolean} messageSupport
 */
GiftWrap.prototype['messageSupport'] = undefined;

/**
 * Amount charged for gift wrap. All prices must be provided in cents. No commas or periods are accepeted. For example one dollar should be informed as 100. $1,2345.67 should be informed solely as 1234567
 * @member {Number} value
 */
GiftWrap.prototype['value'] = undefined;






export default GiftWrap;

