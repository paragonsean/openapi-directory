/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Freight from './Freight';
import OrderGiftWrap from './OrderGiftWrap';
import Promotion from './Promotion';

/**
 * The OrderItem model module.
 * @module model/OrderItem
 * @version 1.0.0
 */
class OrderItem {
    /**
     * Constructs a new <code>OrderItem</code>.
     * @alias module:model/OrderItem
     * @param freight {module:model/Freight} 
     * @param id {String} Unique Id representing this item
     * @param name {String} Product name
     * @param salePrice {Number} Product unit price
     * @param sent {Boolean} Flag if product was already shipped
     * @param skuSellerId {String} Unique Product Id (SKU) in the seller system
     */
    constructor(freight, id, name, salePrice, sent, skuSellerId) { 
        
        OrderItem.initialize(this, freight, id, name, salePrice, sent, skuSellerId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, freight, id, name, salePrice, sent, skuSellerId) { 
        obj['freight'] = freight;
        obj['id'] = id;
        obj['name'] = name;
        obj['salePrice'] = salePrice;
        obj['sent'] = sent;
        obj['skuSellerId'] = skuSellerId;
    }

    /**
     * Constructs a <code>OrderItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderItem} obj Optional instance to populate.
     * @return {module:model/OrderItem} The populated <code>OrderItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderItem();

            if (data.hasOwnProperty('freight')) {
                obj['freight'] = Freight.constructFromObject(data['freight']);
            }
            if (data.hasOwnProperty('giftWrap')) {
                obj['giftWrap'] = OrderGiftWrap.constructFromObject(data['giftWrap']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('promotions')) {
                obj['promotions'] = ApiClient.convertToType(data['promotions'], [Promotion]);
            }
            if (data.hasOwnProperty('salePrice')) {
                obj['salePrice'] = ApiClient.convertToType(data['salePrice'], 'Number');
            }
            if (data.hasOwnProperty('sent')) {
                obj['sent'] = ApiClient.convertToType(data['sent'], 'Boolean');
            }
            if (data.hasOwnProperty('skuSellerId')) {
                obj['skuSellerId'] = ApiClient.convertToType(data['skuSellerId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrderItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `freight`
        if (data['freight']) { // data not null
          Freight.validateJSON(data['freight']);
        }
        // validate the optional field `giftWrap`
        if (data['giftWrap']) { // data not null
          OrderGiftWrap.validateJSON(data['giftWrap']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['promotions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['promotions'])) {
                throw new Error("Expected the field `promotions` to be an array in the JSON data but got " + data['promotions']);
            }
            // validate the optional field `promotions` (array)
            for (const item of data['promotions']) {
                Promotion.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['skuSellerId'] && !(typeof data['skuSellerId'] === 'string' || data['skuSellerId'] instanceof String)) {
            throw new Error("Expected the field `skuSellerId` to be a primitive type in the JSON string but got " + data['skuSellerId']);
        }

        return true;
    }


}

OrderItem.RequiredProperties = ["freight", "id", "name", "salePrice", "sent", "skuSellerId"];

/**
 * @member {module:model/Freight} freight
 */
OrderItem.prototype['freight'] = undefined;

/**
 * @member {module:model/OrderGiftWrap} giftWrap
 */
OrderItem.prototype['giftWrap'] = undefined;

/**
 * Unique Id representing this item
 * @member {String} id
 */
OrderItem.prototype['id'] = undefined;

/**
 * Product name
 * @member {String} name
 */
OrderItem.prototype['name'] = undefined;

/**
 * Promotions array
 * @member {Array.<module:model/Promotion>} promotions
 */
OrderItem.prototype['promotions'] = undefined;

/**
 * Product unit price
 * @member {Number} salePrice
 */
OrderItem.prototype['salePrice'] = undefined;

/**
 * Flag if product was already shipped
 * @member {Boolean} sent
 */
OrderItem.prototype['sent'] = undefined;

/**
 * Unique Product Id (SKU) in the seller system
 * @member {String} skuSellerId
 */
OrderItem.prototype['skuSellerId'] = undefined;






export default OrderItem;

