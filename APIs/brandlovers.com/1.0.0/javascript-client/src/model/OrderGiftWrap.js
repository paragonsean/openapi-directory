/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GiftCard from './GiftCard';

/**
 * The OrderGiftWrap model module.
 * @module model/OrderGiftWrap
 * @version 1.0.0
 */
class OrderGiftWrap {
    /**
     * Constructs a new <code>OrderGiftWrap</code>.
     * @alias module:model/OrderGiftWrap
     * @param available {Boolean} Defines if giftwrpping is available for this product.
     * @param value {Number} Amount charged for gift wrap. All prices must be provided in cents. No commas or periods are accepeted. For example one dollar should be informed as 100. $1,2345.67 should be informed solely as 1234567
     */
    constructor(available, value) { 
        
        OrderGiftWrap.initialize(this, available, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, available, value) { 
        obj['available'] = available;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>OrderGiftWrap</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderGiftWrap} obj Optional instance to populate.
     * @return {module:model/OrderGiftWrap} The populated <code>OrderGiftWrap</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderGiftWrap();

            if (data.hasOwnProperty('available')) {
                obj['available'] = ApiClient.convertToType(data['available'], 'Boolean');
            }
            if (data.hasOwnProperty('giftCard')) {
                obj['giftCard'] = GiftCard.constructFromObject(data['giftCard']);
            }
            if (data.hasOwnProperty('messageSupport')) {
                obj['messageSupport'] = ApiClient.convertToType(data['messageSupport'], 'Boolean');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderGiftWrap</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderGiftWrap</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrderGiftWrap.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `giftCard`
        if (data['giftCard']) { // data not null
          GiftCard.validateJSON(data['giftCard']);
        }

        return true;
    }


}

OrderGiftWrap.RequiredProperties = ["available", "value"];

/**
 * Defines if giftwrpping is available for this product.
 * @member {Boolean} available
 */
OrderGiftWrap.prototype['available'] = undefined;

/**
 * @member {module:model/GiftCard} giftCard
 */
OrderGiftWrap.prototype['giftCard'] = undefined;

/**
 * Defines if giftwrpping is available for this product.
 * @member {Boolean} messageSupport
 */
OrderGiftWrap.prototype['messageSupport'] = undefined;

/**
 * Amount charged for gift wrap. All prices must be provided in cents. No commas or periods are accepeted. For example one dollar should be informed as 100. $1,2345.67 should be informed solely as 1234567
 * @member {Number} value
 */
OrderGiftWrap.prototype['value'] = undefined;






export default OrderGiftWrap;

