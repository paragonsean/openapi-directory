/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Stock from './Stock';

/**
 * The ProductStock model module.
 * @module model/ProductStock
 * @version 1.0.0
 */
class ProductStock {
    /**
     * Constructs a new <code>ProductStock</code>.
     * @alias module:model/ProductStock
     * @param skuSellerId {String} Unique Product Id (SKU) in the seller system
     * @param stocks {Array.<module:model/Stock>} Invetory information
     */
    constructor(skuSellerId, stocks) { 
        
        ProductStock.initialize(this, skuSellerId, stocks);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, skuSellerId, stocks) { 
        obj['skuSellerId'] = skuSellerId;
        obj['stocks'] = stocks;
    }

    /**
     * Constructs a <code>ProductStock</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductStock} obj Optional instance to populate.
     * @return {module:model/ProductStock} The populated <code>ProductStock</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductStock();

            if (data.hasOwnProperty('skuSellerId')) {
                obj['skuSellerId'] = ApiClient.convertToType(data['skuSellerId'], 'String');
            }
            if (data.hasOwnProperty('stocks')) {
                obj['stocks'] = ApiClient.convertToType(data['stocks'], [Stock]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductStock</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductStock</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProductStock.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['skuSellerId'] && !(typeof data['skuSellerId'] === 'string' || data['skuSellerId'] instanceof String)) {
            throw new Error("Expected the field `skuSellerId` to be a primitive type in the JSON string but got " + data['skuSellerId']);
        }
        if (data['stocks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stocks'])) {
                throw new Error("Expected the field `stocks` to be an array in the JSON data but got " + data['stocks']);
            }
            // validate the optional field `stocks` (array)
            for (const item of data['stocks']) {
                Stock.validateJSON(item);
            };
        }

        return true;
    }


}

ProductStock.RequiredProperties = ["skuSellerId", "stocks"];

/**
 * Unique Product Id (SKU) in the seller system
 * @member {String} skuSellerId
 */
ProductStock.prototype['skuSellerId'] = undefined;

/**
 * Invetory information
 * @member {Array.<module:model/Stock>} stocks
 */
ProductStock.prototype['stocks'] = undefined;






export default ProductStock;

