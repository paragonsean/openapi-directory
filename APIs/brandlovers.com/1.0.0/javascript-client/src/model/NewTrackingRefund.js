/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Courier from './Courier';
import OrderItemReference from './OrderItemReference';

/**
 * The NewTrackingRefund model module.
 * @module model/NewTrackingRefund
 * @version 1.0.0
 */
class NewTrackingRefund {
    /**
     * Constructs a new <code>NewTrackingRefund</code>.
     * @alias module:model/NewTrackingRefund
     * @param info {String} Aditional information about this shippment
     * @param items {Array.<module:model/OrderItemReference>} 
     */
    constructor(info, items) { 
        
        NewTrackingRefund.initialize(this, info, items);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, info, items) { 
        obj['info'] = info;
        obj['items'] = items;
    }

    /**
     * Constructs a <code>NewTrackingRefund</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NewTrackingRefund} obj Optional instance to populate.
     * @return {module:model/NewTrackingRefund} The populated <code>NewTrackingRefund</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NewTrackingRefund();

            if (data.hasOwnProperty('courier')) {
                obj['courier'] = Courier.constructFromObject(data['courier']);
            }
            if (data.hasOwnProperty('cte')) {
                obj['cte'] = ApiClient.convertToType(data['cte'], 'String');
            }
            if (data.hasOwnProperty('info')) {
                obj['info'] = ApiClient.convertToType(data['info'], 'String');
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [OrderItemReference]);
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
            if (data.hasOwnProperty('occurredAt')) {
                obj['occurredAt'] = ApiClient.convertToType(data['occurredAt'], 'Date');
            }
            if (data.hasOwnProperty('sellerShipmentId')) {
                obj['sellerShipmentId'] = ApiClient.convertToType(data['sellerShipmentId'], 'String');
            }
            if (data.hasOwnProperty('tranckingUrl')) {
                obj['tranckingUrl'] = ApiClient.convertToType(data['tranckingUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NewTrackingRefund</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NewTrackingRefund</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NewTrackingRefund.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `courier`
        if (data['courier']) { // data not null
          Courier.validateJSON(data['courier']);
        }
        // ensure the json data is a string
        if (data['cte'] && !(typeof data['cte'] === 'string' || data['cte'] instanceof String)) {
            throw new Error("Expected the field `cte` to be a primitive type in the JSON string but got " + data['cte']);
        }
        // ensure the json data is a string
        if (data['info'] && !(typeof data['info'] === 'string' || data['info'] instanceof String)) {
            throw new Error("Expected the field `info` to be a primitive type in the JSON string but got " + data['info']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                OrderItemReference.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }
        // ensure the json data is a string
        if (data['sellerShipmentId'] && !(typeof data['sellerShipmentId'] === 'string' || data['sellerShipmentId'] instanceof String)) {
            throw new Error("Expected the field `sellerShipmentId` to be a primitive type in the JSON string but got " + data['sellerShipmentId']);
        }
        // ensure the json data is a string
        if (data['tranckingUrl'] && !(typeof data['tranckingUrl'] === 'string' || data['tranckingUrl'] instanceof String)) {
            throw new Error("Expected the field `tranckingUrl` to be a primitive type in the JSON string but got " + data['tranckingUrl']);
        }

        return true;
    }


}

NewTrackingRefund.RequiredProperties = ["info", "items"];

/**
 * @member {module:model/Courier} courier
 */
NewTrackingRefund.prototype['courier'] = undefined;

/**
 * Conhecimento do Transporte Eletrônico
 * @member {String} cte
 */
NewTrackingRefund.prototype['cte'] = undefined;

/**
 * Aditional information about this shippment
 * @member {String} info
 */
NewTrackingRefund.prototype['info'] = undefined;

/**
 * @member {Array.<module:model/OrderItemReference>} items
 */
NewTrackingRefund.prototype['items'] = undefined;

/**
 * Tracking Id in the courier
 * @member {String} number
 */
NewTrackingRefund.prototype['number'] = undefined;

/**
 * Date time that this was created
 * @member {Date} occurredAt
 */
NewTrackingRefund.prototype['occurredAt'] = undefined;

/**
 * Unique Seller shipment Id. This must be unique across all orders and shipments
 * @member {String} sellerShipmentId
 */
NewTrackingRefund.prototype['sellerShipmentId'] = undefined;

/**
 * Courier tracking URL
 * @member {String} tranckingUrl
 */
NewTrackingRefund.prototype['tranckingUrl'] = undefined;






export default NewTrackingRefund;

