/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Courier from './Courier';
import Invoice from './Invoice';
import OrderItemReference from './OrderItemReference';

/**
 * The Shippment model module.
 * @module model/Shippment
 * @version 1.0.0
 */
class Shippment {
    /**
     * Constructs a new <code>Shippment</code>.
     * @alias module:model/Shippment
     * @param courier {module:model/Courier} 
     * @param invoice {module:model/Invoice} 
     * @param items {Array.<module:model/OrderItemReference>} List of items of this shippment
     * @param occurredAt {Date} Date time when this shippment happened
     * @param sellerShipmentId {String} Unique Seller shipment Id. This must be unique across all orders and shipments
     * @param status {String} Shipment status
     */
    constructor(courier, invoice, items, occurredAt, sellerShipmentId, status) { 
        
        Shippment.initialize(this, courier, invoice, items, occurredAt, sellerShipmentId, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, courier, invoice, items, occurredAt, sellerShipmentId, status) { 
        obj['courier'] = courier;
        obj['invoice'] = invoice;
        obj['items'] = items;
        obj['occurredAt'] = occurredAt;
        obj['sellerShipmentId'] = sellerShipmentId;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>Shippment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Shippment} obj Optional instance to populate.
     * @return {module:model/Shippment} The populated <code>Shippment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Shippment();

            if (data.hasOwnProperty('courier')) {
                obj['courier'] = Courier.constructFromObject(data['courier']);
            }
            if (data.hasOwnProperty('cte')) {
                obj['cte'] = ApiClient.convertToType(data['cte'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('invoice')) {
                obj['invoice'] = Invoice.constructFromObject(data['invoice']);
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [OrderItemReference]);
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
            if (data.hasOwnProperty('occurredAt')) {
                obj['occurredAt'] = ApiClient.convertToType(data['occurredAt'], 'Date');
            }
            if (data.hasOwnProperty('sellerShipmentId')) {
                obj['sellerShipmentId'] = ApiClient.convertToType(data['sellerShipmentId'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('trackingUrl')) {
                obj['trackingUrl'] = ApiClient.convertToType(data['trackingUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Shippment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Shippment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Shippment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `courier`
        if (data['courier']) { // data not null
          Courier.validateJSON(data['courier']);
        }
        // ensure the json data is a string
        if (data['cte'] && !(typeof data['cte'] === 'string' || data['cte'] instanceof String)) {
            throw new Error("Expected the field `cte` to be a primitive type in the JSON string but got " + data['cte']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `invoice`
        if (data['invoice']) { // data not null
          Invoice.validateJSON(data['invoice']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                OrderItemReference.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }
        // ensure the json data is a string
        if (data['sellerShipmentId'] && !(typeof data['sellerShipmentId'] === 'string' || data['sellerShipmentId'] instanceof String)) {
            throw new Error("Expected the field `sellerShipmentId` to be a primitive type in the JSON string but got " + data['sellerShipmentId']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['trackingUrl'] && !(typeof data['trackingUrl'] === 'string' || data['trackingUrl'] instanceof String)) {
            throw new Error("Expected the field `trackingUrl` to be a primitive type in the JSON string but got " + data['trackingUrl']);
        }

        return true;
    }


}

Shippment.RequiredProperties = ["courier", "invoice", "items", "occurredAt", "sellerShipmentId", "status"];

/**
 * @member {module:model/Courier} courier
 */
Shippment.prototype['courier'] = undefined;

/**
 * Conhecimento de Transporte Eletrônico
 * @member {String} cte
 */
Shippment.prototype['cte'] = undefined;

/**
 * Additinal shipment tracking information
 * @member {String} description
 */
Shippment.prototype['description'] = undefined;

/**
 * Shipment Id associated with this shippment used to group diferent item or items from a single order
 * @member {String} id
 */
Shippment.prototype['id'] = undefined;

/**
 * @member {module:model/Invoice} invoice
 */
Shippment.prototype['invoice'] = undefined;

/**
 * List of items of this shippment
 * @member {Array.<module:model/OrderItemReference>} items
 */
Shippment.prototype['items'] = undefined;

/**
 * Courier unique trackign Id associated with this shipment
 * @member {String} number
 */
Shippment.prototype['number'] = undefined;

/**
 * Date time when this shippment happened
 * @member {Date} occurredAt
 */
Shippment.prototype['occurredAt'] = undefined;

/**
 * Unique Seller shipment Id. This must be unique across all orders and shipments
 * @member {String} sellerShipmentId
 */
Shippment.prototype['sellerShipmentId'] = undefined;

/**
 * Shipment status
 * @member {String} status
 */
Shippment.prototype['status'] = undefined;

/**
 * Courier tracking URL
 * @member {String} trackingUrl
 */
Shippment.prototype['trackingUrl'] = undefined;






export default Shippment;

