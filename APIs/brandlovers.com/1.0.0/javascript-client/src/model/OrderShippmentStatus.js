/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Error from './Error';
import OrderItemReference from './OrderItemReference';

/**
 * The OrderShippmentStatus model module.
 * @module model/OrderShippmentStatus
 * @version 1.0.0
 */
class OrderShippmentStatus {
    /**
     * Constructs a new <code>OrderShippmentStatus</code>.
     * @alias module:model/OrderShippmentStatus
     */
    constructor() { 
        
        OrderShippmentStatus.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OrderShippmentStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderShippmentStatus} obj Optional instance to populate.
     * @return {module:model/OrderShippmentStatus} The populated <code>OrderShippmentStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderShippmentStatus();

            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [Error]);
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [OrderItemReference]);
            }
            if (data.hasOwnProperty('shipmentId')) {
                obj['shipmentId'] = ApiClient.convertToType(data['shipmentId'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('trackingUrl')) {
                obj['trackingUrl'] = ApiClient.convertToType(data['trackingUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderShippmentStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderShippmentStatus</code>.
     */
    static validateJSON(data) {
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                Error.validateJSON(item);
            };
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                OrderItemReference.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['shipmentId'] && !(typeof data['shipmentId'] === 'string' || data['shipmentId'] instanceof String)) {
            throw new Error("Expected the field `shipmentId` to be a primitive type in the JSON string but got " + data['shipmentId']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['trackingUrl'] && !(typeof data['trackingUrl'] === 'string' || data['trackingUrl'] instanceof String)) {
            throw new Error("Expected the field `trackingUrl` to be a primitive type in the JSON string but got " + data['trackingUrl']);
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/Error>} errors
 */
OrderShippmentStatus.prototype['errors'] = undefined;

/**
 * @member {Array.<module:model/OrderItemReference>} items
 */
OrderShippmentStatus.prototype['items'] = undefined;

/**
 * Shippment unique Id
 * @member {String} shipmentId
 */
OrderShippmentStatus.prototype['shipmentId'] = undefined;

/**
 * Shipment status
 * @member {String} status
 */
OrderShippmentStatus.prototype['status'] = undefined;

/**
 * Courier tracking URL
 * @member {String} trackingUrl
 */
OrderShippmentStatus.prototype['trackingUrl'] = undefined;






export default OrderShippmentStatus;

