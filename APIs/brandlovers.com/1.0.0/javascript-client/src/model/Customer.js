/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Phone from './Phone';

/**
 * The Customer model module.
 * @module model/Customer
 * @version 1.0.0
 */
class Customer {
    /**
     * Constructs a new <code>Customer</code>.
     * @alias module:model/Customer
     * @param documentNumber {String} Customer tax information
     * @param id {String} Customer unqiue Id
     * @param name {String} Customer Name
     * @param phones {Array.<module:model/Phone>} Customer phone number
     * @param type {String} Customer type: Enterprise or Consumer
     */
    constructor(documentNumber, id, name, phones, type) { 
        
        Customer.initialize(this, documentNumber, id, name, phones, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, documentNumber, id, name, phones, type) { 
        obj['documentNumber'] = documentNumber;
        obj['id'] = id;
        obj['name'] = name;
        obj['phones'] = phones;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>Customer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Customer} obj Optional instance to populate.
     * @return {module:model/Customer} The populated <code>Customer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Customer();

            if (data.hasOwnProperty('documentNumber')) {
                obj['documentNumber'] = ApiClient.convertToType(data['documentNumber'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('phones')) {
                obj['phones'] = ApiClient.convertToType(data['phones'], [Phone]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Customer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Customer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Customer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['documentNumber'] && !(typeof data['documentNumber'] === 'string' || data['documentNumber'] instanceof String)) {
            throw new Error("Expected the field `documentNumber` to be a primitive type in the JSON string but got " + data['documentNumber']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['phones']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['phones'])) {
                throw new Error("Expected the field `phones` to be an array in the JSON data but got " + data['phones']);
            }
            // validate the optional field `phones` (array)
            for (const item of data['phones']) {
                Phone.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Customer.RequiredProperties = ["documentNumber", "id", "name", "phones", "type"];

/**
 * Customer tax information
 * @member {String} documentNumber
 */
Customer.prototype['documentNumber'] = undefined;

/**
 * Customer Email (when available). Please note: Email is not a mandatory field
 * @member {String} email
 */
Customer.prototype['email'] = undefined;

/**
 * Customer unqiue Id
 * @member {String} id
 */
Customer.prototype['id'] = undefined;

/**
 * Customer Name
 * @member {String} name
 */
Customer.prototype['name'] = undefined;

/**
 * Customer phone number
 * @member {Array.<module:model/Phone>} phones
 */
Customer.prototype['phones'] = undefined;

/**
 * Customer type: Enterprise or Consumer
 * @member {String} type
 */
Customer.prototype['type'] = undefined;






export default Customer;

