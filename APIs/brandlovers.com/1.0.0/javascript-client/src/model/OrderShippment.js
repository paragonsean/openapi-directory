/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Courier from './Courier';
import Invoice from './Invoice';
import OrderItemReference from './OrderItemReference';

/**
 * The OrderShippment model module.
 * @module model/OrderShippment
 * @version 1.0.0
 */
class OrderShippment {
    /**
     * Constructs a new <code>OrderShippment</code>.
     * @alias module:model/OrderShippment
     */
    constructor() { 
        
        OrderShippment.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OrderShippment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderShippment} obj Optional instance to populate.
     * @return {module:model/OrderShippment} The populated <code>OrderShippment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderShippment();

            if (data.hasOwnProperty('courier')) {
                obj['courier'] = Courier.constructFromObject(data['courier']);
            }
            if (data.hasOwnProperty('cte')) {
                obj['cte'] = ApiClient.convertToType(data['cte'], 'String');
            }
            if (data.hasOwnProperty('invoice')) {
                obj['invoice'] = Invoice.constructFromObject(data['invoice']);
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [OrderItemReference]);
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
            if (data.hasOwnProperty('occurredAt')) {
                obj['occurredAt'] = ApiClient.convertToType(data['occurredAt'], 'Date');
            }
            if (data.hasOwnProperty('order')) {
                obj['order'] = ApiClient.convertToType(data['order'], 'String');
            }
            if (data.hasOwnProperty('sellerShipmentId')) {
                obj['sellerShipmentId'] = ApiClient.convertToType(data['sellerShipmentId'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('trackingUrl')) {
                obj['trackingUrl'] = ApiClient.convertToType(data['trackingUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderShippment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderShippment</code>.
     */
    static validateJSON(data) {
        // validate the optional field `courier`
        if (data['courier']) { // data not null
          Courier.validateJSON(data['courier']);
        }
        // ensure the json data is a string
        if (data['cte'] && !(typeof data['cte'] === 'string' || data['cte'] instanceof String)) {
            throw new Error("Expected the field `cte` to be a primitive type in the JSON string but got " + data['cte']);
        }
        // validate the optional field `invoice`
        if (data['invoice']) { // data not null
          Invoice.validateJSON(data['invoice']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                OrderItemReference.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }
        // ensure the json data is a string
        if (data['order'] && !(typeof data['order'] === 'string' || data['order'] instanceof String)) {
            throw new Error("Expected the field `order` to be a primitive type in the JSON string but got " + data['order']);
        }
        // ensure the json data is a string
        if (data['sellerShipmentId'] && !(typeof data['sellerShipmentId'] === 'string' || data['sellerShipmentId'] instanceof String)) {
            throw new Error("Expected the field `sellerShipmentId` to be a primitive type in the JSON string but got " + data['sellerShipmentId']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['trackingUrl'] && !(typeof data['trackingUrl'] === 'string' || data['trackingUrl'] instanceof String)) {
            throw new Error("Expected the field `trackingUrl` to be a primitive type in the JSON string but got " + data['trackingUrl']);
        }

        return true;
    }


}



/**
 * @member {module:model/Courier} courier
 */
OrderShippment.prototype['courier'] = undefined;

/**
 * Conhecimento de Transporte Eletôniconico
 * @member {String} cte
 */
OrderShippment.prototype['cte'] = undefined;

/**
 * @member {module:model/Invoice} invoice
 */
OrderShippment.prototype['invoice'] = undefined;

/**
 * @member {Array.<module:model/OrderItemReference>} items
 */
OrderShippment.prototype['items'] = undefined;

/**
 * Courier unique trackign Id associated with this shipment
 * @member {String} number
 */
OrderShippment.prototype['number'] = undefined;

/**
 * Date that this shippment was shiped
 * @member {Date} occurredAt
 */
OrderShippment.prototype['occurredAt'] = undefined;

/**
 * Order unique Id
 * @member {String} order
 */
OrderShippment.prototype['order'] = undefined;

/**
 * Unique Seller shipment Id. This must be unique across all orders and shipments
 * @member {String} sellerShipmentId
 */
OrderShippment.prototype['sellerShipmentId'] = undefined;

/**
 * Shipment status.
 * @member {String} status
 */
OrderShippment.prototype['status'] = undefined;

/**
 * Courier tracking URL
 * @member {String} trackingUrl
 */
OrderShippment.prototype['trackingUrl'] = undefined;






export default OrderShippment;

