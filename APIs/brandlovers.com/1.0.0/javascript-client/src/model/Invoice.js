/**
 * BrandLovers Marketplace API V1
 * Allows sellers to: 1) Load products definitions to the BrandLovers marktplace. 2) Receive and update orders status. 3) Receive and update shipping information. 4) Receive and update customer tickets. All requests consume and return application/json content. All request must be authenticated and use HTTPS.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Invoice model module.
 * @module model/Invoice
 * @version 1.0.0
 */
class Invoice {
    /**
     * Constructs a new <code>Invoice</code>.
     * @alias module:model/Invoice
     * @param accessKey {String} Número da chave de acesso à nota fiscal. A chave possui 44 dígitos e contém todas as informações da DANFE
     * @param number {String} Número da Nota Fiscal
     * @param serie {String} Número de serie da Nota Fiscal
     */
    constructor(accessKey, number, serie) { 
        
        Invoice.initialize(this, accessKey, number, serie);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accessKey, number, serie) { 
        obj['accessKey'] = accessKey;
        obj['number'] = number;
        obj['serie'] = serie;
    }

    /**
     * Constructs a <code>Invoice</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Invoice} obj Optional instance to populate.
     * @return {module:model/Invoice} The populated <code>Invoice</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Invoice();

            if (data.hasOwnProperty('accessKey')) {
                obj['accessKey'] = ApiClient.convertToType(data['accessKey'], 'String');
            }
            if (data.hasOwnProperty('cnpj')) {
                obj['cnpj'] = ApiClient.convertToType(data['cnpj'], 'String');
            }
            if (data.hasOwnProperty('issuedAt')) {
                obj['issuedAt'] = ApiClient.convertToType(data['issuedAt'], 'Date');
            }
            if (data.hasOwnProperty('linkDanfe')) {
                obj['linkDanfe'] = ApiClient.convertToType(data['linkDanfe'], 'String');
            }
            if (data.hasOwnProperty('linkXml')) {
                obj['linkXml'] = ApiClient.convertToType(data['linkXml'], 'String');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
            if (data.hasOwnProperty('serie')) {
                obj['serie'] = ApiClient.convertToType(data['serie'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Invoice</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Invoice</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Invoice.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['accessKey'] && !(typeof data['accessKey'] === 'string' || data['accessKey'] instanceof String)) {
            throw new Error("Expected the field `accessKey` to be a primitive type in the JSON string but got " + data['accessKey']);
        }
        // ensure the json data is a string
        if (data['cnpj'] && !(typeof data['cnpj'] === 'string' || data['cnpj'] instanceof String)) {
            throw new Error("Expected the field `cnpj` to be a primitive type in the JSON string but got " + data['cnpj']);
        }
        // ensure the json data is a string
        if (data['linkDanfe'] && !(typeof data['linkDanfe'] === 'string' || data['linkDanfe'] instanceof String)) {
            throw new Error("Expected the field `linkDanfe` to be a primitive type in the JSON string but got " + data['linkDanfe']);
        }
        // ensure the json data is a string
        if (data['linkXml'] && !(typeof data['linkXml'] === 'string' || data['linkXml'] instanceof String)) {
            throw new Error("Expected the field `linkXml` to be a primitive type in the JSON string but got " + data['linkXml']);
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }
        // ensure the json data is a string
        if (data['serie'] && !(typeof data['serie'] === 'string' || data['serie'] instanceof String)) {
            throw new Error("Expected the field `serie` to be a primitive type in the JSON string but got " + data['serie']);
        }

        return true;
    }


}

Invoice.RequiredProperties = ["accessKey", "number", "serie"];

/**
 * Número da chave de acesso à nota fiscal. A chave possui 44 dígitos e contém todas as informações da DANFE
 * @member {String} accessKey
 */
Invoice.prototype['accessKey'] = undefined;

/**
 * CNPJ responsável pelo envio dos produtos. Pode ser diferente caso a empresa possua diversos Centros de Distribuição (CDs)
 * @member {String} cnpj
 */
Invoice.prototype['cnpj'] = undefined;

/**
 * Data de emissão da Nota Fiscal
 * @member {Date} issuedAt
 */
Invoice.prototype['issuedAt'] = undefined;

/**
 * Url para consulta da DANFE
 * @member {String} linkDanfe
 */
Invoice.prototype['linkDanfe'] = undefined;

/**
 * Url para consulta da NFE
 * @member {String} linkXml
 */
Invoice.prototype['linkXml'] = undefined;

/**
 * Número da Nota Fiscal
 * @member {String} number
 */
Invoice.prototype['number'] = undefined;

/**
 * Número de serie da Nota Fiscal
 * @member {String} serie
 */
Invoice.prototype['serie'] = undefined;






export default Invoice;

