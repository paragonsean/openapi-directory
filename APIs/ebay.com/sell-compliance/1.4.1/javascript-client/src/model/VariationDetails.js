/**
 * Compliance API
 * Service for providing information to sellers about their listings being non-compliant, or at risk for becoming non-compliant, against eBay listing policies.
 *
 * The version of the OpenAPI document: 1.4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NameValueList from './NameValueList';

/**
 * The VariationDetails model module.
 * @module model/VariationDetails
 * @version 1.4.1
 */
class VariationDetails {
    /**
     * Constructs a new <code>VariationDetails</code>.
     * This type is used to identify the product variation that has the listing violation.
     * @alias module:model/VariationDetails
     */
    constructor() { 
        
        VariationDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>VariationDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VariationDetails} obj Optional instance to populate.
     * @return {module:model/VariationDetails} The populated <code>VariationDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VariationDetails();

            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], 'String');
            }
            if (data.hasOwnProperty('variationAspects')) {
                obj['variationAspects'] = ApiClient.convertToType(data['variationAspects'], [NameValueList]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VariationDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VariationDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['sku'] && !(typeof data['sku'] === 'string' || data['sku'] instanceof String)) {
            throw new Error("Expected the field `sku` to be a primitive type in the JSON string but got " + data['sku']);
        }
        if (data['variationAspects']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['variationAspects'])) {
                throw new Error("Expected the field `variationAspects` to be an array in the JSON data but got " + data['variationAspects']);
            }
            // validate the optional field `variationAspects` (array)
            for (const item of data['variationAspects']) {
                NameValueList.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The seller-defined SKU value of the variation within the multiple-variation listing with the violation{s). This field is only returned if a seller-defined SKU value is defined for the variation. SKU values are optional in listing except when creating listings using the Inventory API.
 * @member {String} sku
 */
VariationDetails.prototype['sku'] = undefined;

/**
 * An array of one or more variation aspects that define a variation within a multiple-variation listing. The aspect{s) returned here define the individual variation, because these aspects will differ for each variation. Common varying aspects include color and size.
 * @member {Array.<module:model/NameValueList>} variationAspects
 */
VariationDetails.prototype['variationAspects'] = undefined;






export default VariationDetails;

