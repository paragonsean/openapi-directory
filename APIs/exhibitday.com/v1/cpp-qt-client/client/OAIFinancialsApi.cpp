/**
 * Test the ExhibitDay API with Swagger
 * This API can be used to programmatically pull data out of ExhibitDay or push data into ExhibitDay -- allowing for automation between ExhibitDay and your internal systems (or other third-party software). To use the API, you'll need working knowledge of consuming REST APIs.<br /><br />Docs: https://api.exhibitday.com/swagger/docs/v1
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIFinancialsApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIFinancialsApi::OAIFinancialsApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIFinancialsApi::~OAIFinancialsApi() {
}

void OAIFinancialsApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.exhibitday.com/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("financialsEventCosts0Get", defaultConf);
    _serverIndices.insert("financialsEventCosts0Get", 0);
    _serverConfigs.insert("financialsMiscAnnualExpenseCosts0Get", defaultConf);
    _serverIndices.insert("financialsMiscAnnualExpenseCosts0Get", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIFinancialsApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIFinancialsApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIFinancialsApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIFinancialsApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIFinancialsApi::setUsername(const QString &username) {
    _username = username;
}

void OAIFinancialsApi::setPassword(const QString &password) {
    _password = password;
}


void OAIFinancialsApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIFinancialsApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIFinancialsApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIFinancialsApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIFinancialsApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIFinancialsApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIFinancialsApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIFinancialsApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIFinancialsApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIFinancialsApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIFinancialsApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIFinancialsApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIFinancialsApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIFinancialsApi::financialsEventCosts0Get(const QString &api_key, const ::OpenAPI::OptionalParam<double> &filter_by_event_id, const ::OpenAPI::OptionalParam<QDate> &filter_by_event_start_date_greater_than_or_equal_to, const ::OpenAPI::OptionalParam<QDate> &filter_by_event_start_date_smaller_than_or_equal_to, const ::OpenAPI::OptionalParam<QDate> &filter_by_event_end_date_greater_than_or_equal_to, const ::OpenAPI::OptionalParam<QDate> &filter_by_event_end_date_smaller_than_or_equal_to) {
    QString fullPath = QString(_serverConfigs["financialsEventCosts0Get"][_serverIndices.value("financialsEventCosts0Get")].URL()+"/v1/financials/event_costs");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(api_key).isEmpty()) {
            input.headers.insert("api_key", ::OpenAPI::toStringValue(api_key));
        }
        }
    if (filter_by_event_id.hasValue())
    {
        if (!::OpenAPI::toStringValue(filter_by_event_id.value()).isEmpty()) {
            input.headers.insert("filter_by_event_id", ::OpenAPI::toStringValue(filter_by_event_id.value()));
        }
        }
    if (filter_by_event_start_date_greater_than_or_equal_to.hasValue())
    {
        if (!::OpenAPI::toStringValue(filter_by_event_start_date_greater_than_or_equal_to.value()).isEmpty()) {
            input.headers.insert("filter_by_event_start_date_greater_than_or_equal_to", ::OpenAPI::toStringValue(filter_by_event_start_date_greater_than_or_equal_to.value()));
        }
        }
    if (filter_by_event_start_date_smaller_than_or_equal_to.hasValue())
    {
        if (!::OpenAPI::toStringValue(filter_by_event_start_date_smaller_than_or_equal_to.value()).isEmpty()) {
            input.headers.insert("filter_by_event_start_date_smaller_than_or_equal_to", ::OpenAPI::toStringValue(filter_by_event_start_date_smaller_than_or_equal_to.value()));
        }
        }
    if (filter_by_event_end_date_greater_than_or_equal_to.hasValue())
    {
        if (!::OpenAPI::toStringValue(filter_by_event_end_date_greater_than_or_equal_to.value()).isEmpty()) {
            input.headers.insert("filter_by_event_end_date_greater_than_or_equal_to", ::OpenAPI::toStringValue(filter_by_event_end_date_greater_than_or_equal_to.value()));
        }
        }
    if (filter_by_event_end_date_smaller_than_or_equal_to.hasValue())
    {
        if (!::OpenAPI::toStringValue(filter_by_event_end_date_smaller_than_or_equal_to.value()).isEmpty()) {
            input.headers.insert("filter_by_event_end_date_smaller_than_or_equal_to", ::OpenAPI::toStringValue(filter_by_event_end_date_smaller_than_or_equal_to.value()));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFinancialsApi::financialsEventCosts0GetCallback);
    connect(this, &OAIFinancialsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFinancialsApi::financialsEventCosts0GetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QString output;
    ::OpenAPI::fromStringValue(QString(worker->response), output);
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT financialsEventCosts0GetSignal(output);
        Q_EMIT financialsEventCosts0GetSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT financialsEventCosts0GetSignalE(output, error_type, error_str);
        Q_EMIT financialsEventCosts0GetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT financialsEventCosts0GetSignalError(output, error_type, error_str);
        Q_EMIT financialsEventCosts0GetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFinancialsApi::financialsMiscAnnualExpenseCosts0Get(const QString &api_key, const ::OpenAPI::OptionalParam<double> &budget_year) {
    QString fullPath = QString(_serverConfigs["financialsMiscAnnualExpenseCosts0Get"][_serverIndices.value("financialsMiscAnnualExpenseCosts0Get")].URL()+"/v1/financials/misc_annual_expense_costs");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    
    {
        if (!::OpenAPI::toStringValue(api_key).isEmpty()) {
            input.headers.insert("api_key", ::OpenAPI::toStringValue(api_key));
        }
        }
    if (budget_year.hasValue())
    {
        if (!::OpenAPI::toStringValue(budget_year.value()).isEmpty()) {
            input.headers.insert("budget_year", ::OpenAPI::toStringValue(budget_year.value()));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFinancialsApi::financialsMiscAnnualExpenseCosts0GetCallback);
    connect(this, &OAIFinancialsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFinancialsApi::financialsMiscAnnualExpenseCosts0GetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QString output;
    ::OpenAPI::fromStringValue(QString(worker->response), output);
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT financialsMiscAnnualExpenseCosts0GetSignal(output);
        Q_EMIT financialsMiscAnnualExpenseCosts0GetSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT financialsMiscAnnualExpenseCosts0GetSignalE(output, error_type, error_str);
        Q_EMIT financialsMiscAnnualExpenseCosts0GetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT financialsMiscAnnualExpenseCosts0GetSignalError(output, error_type, error_str);
        Q_EMIT financialsMiscAnnualExpenseCosts0GetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFinancialsApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
