/**
 * ecwid
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: api-v2
 * Contact: support@cloud-elements.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Swaggerparametersschema from './Swaggerparametersschema';

/**
 * The Swaggerparameters model module.
 * @module model/Swaggerparameters
 * @version api-v2
 */
class Swaggerparameters {
    /**
     * Constructs a new <code>Swaggerparameters</code>.
     * @alias module:model/Swaggerparameters
     */
    constructor() { 
        
        Swaggerparameters.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Swaggerparameters</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Swaggerparameters} obj Optional instance to populate.
     * @return {module:model/Swaggerparameters} The populated <code>Swaggerparameters</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Swaggerparameters();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('in')) {
                obj['in'] = ApiClient.convertToType(data['in'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('schema')) {
                obj['schema'] = Swaggerparametersschema.constructFromObject(data['schema']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Swaggerparameters</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Swaggerparameters</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['in'] && !(typeof data['in'] === 'string' || data['in'] instanceof String)) {
            throw new Error("Expected the field `in` to be a primitive type in the JSON string but got " + data['in']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `schema`
        if (data['schema']) { // data not null
          Swaggerparametersschema.validateJSON(data['schema']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * @member {String} description
 */
Swaggerparameters.prototype['description'] = undefined;

/**
 * @member {String} in
 */
Swaggerparameters.prototype['in'] = undefined;

/**
 * @member {String} name
 */
Swaggerparameters.prototype['name'] = undefined;

/**
 * @member {Boolean} required
 */
Swaggerparameters.prototype['required'] = undefined;

/**
 * @member {module:model/Swaggerparametersschema} schema
 */
Swaggerparameters.prototype['schema'] = undefined;

/**
 * @member {String} type
 */
Swaggerparameters.prototype['type'] = undefined;






export default Swaggerparameters;

