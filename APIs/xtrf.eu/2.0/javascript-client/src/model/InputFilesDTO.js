/**
 * XTRF Home Portal API
 * XTRF Home Portal API enables you to perform operations on Projects, Quotes, Customers, Vendors etc. as a XTRF Home Portal user. <br>The documentation is generated from OpenAPI specification 3.0 available <a href=\"/home-api/openapi.json\">here</a> <br>   The API client/consumer code may be easily generated in 60+ programming languages using an open source code generator available at the time of writing this documentation at <a href='https://editor.swagger.io/'>https://editor.swagger.io/</a> Thank you for using XTRF Application Programming interface (XTRF API). By using the API you agree to the terms below. If you disagree with any of these terms, XTRF does not grant you a license to use the XTRF API. XTRF reserves the right to update and change these terms from time to time without a prior notice of API users. You can always find the most recent version of these terms here: 
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FileDTO from './FileDTO';

/**
 * The InputFilesDTO model module.
 * @module model/InputFilesDTO
 * @version 2.0
 */
class InputFilesDTO {
    /**
     * Constructs a new <code>InputFilesDTO</code>.
     * @alias module:model/InputFilesDTO
     */
    constructor() { 
        
        InputFilesDTO.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InputFilesDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InputFilesDTO} obj Optional instance to populate.
     * @return {module:model/InputFilesDTO} The populated <code>InputFilesDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InputFilesDTO();

            if (data.hasOwnProperty('logFiles')) {
                obj['logFiles'] = ApiClient.convertToType(data['logFiles'], [FileDTO]);
            }
            if (data.hasOwnProperty('referenceFiles')) {
                obj['referenceFiles'] = ApiClient.convertToType(data['referenceFiles'], [FileDTO]);
            }
            if (data.hasOwnProperty('terminology')) {
                obj['terminology'] = ApiClient.convertToType(data['terminology'], [FileDTO]);
            }
            if (data.hasOwnProperty('tm')) {
                obj['tm'] = ApiClient.convertToType(data['tm'], [FileDTO]);
            }
            if (data.hasOwnProperty('workFiles')) {
                obj['workFiles'] = ApiClient.convertToType(data['workFiles'], [FileDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InputFilesDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InputFilesDTO</code>.
     */
    static validateJSON(data) {
        if (data['logFiles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['logFiles'])) {
                throw new Error("Expected the field `logFiles` to be an array in the JSON data but got " + data['logFiles']);
            }
            // validate the optional field `logFiles` (array)
            for (const item of data['logFiles']) {
                FileDTO.validateJSON(item);
            };
        }
        if (data['referenceFiles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['referenceFiles'])) {
                throw new Error("Expected the field `referenceFiles` to be an array in the JSON data but got " + data['referenceFiles']);
            }
            // validate the optional field `referenceFiles` (array)
            for (const item of data['referenceFiles']) {
                FileDTO.validateJSON(item);
            };
        }
        if (data['terminology']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['terminology'])) {
                throw new Error("Expected the field `terminology` to be an array in the JSON data but got " + data['terminology']);
            }
            // validate the optional field `terminology` (array)
            for (const item of data['terminology']) {
                FileDTO.validateJSON(item);
            };
        }
        if (data['tm']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tm'])) {
                throw new Error("Expected the field `tm` to be an array in the JSON data but got " + data['tm']);
            }
            // validate the optional field `tm` (array)
            for (const item of data['tm']) {
                FileDTO.validateJSON(item);
            };
        }
        if (data['workFiles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['workFiles'])) {
                throw new Error("Expected the field `workFiles` to be an array in the JSON data but got " + data['workFiles']);
            }
            // validate the optional field `workFiles` (array)
            for (const item of data['workFiles']) {
                FileDTO.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/FileDTO>} logFiles
 */
InputFilesDTO.prototype['logFiles'] = undefined;

/**
 * @member {Array.<module:model/FileDTO>} referenceFiles
 */
InputFilesDTO.prototype['referenceFiles'] = undefined;

/**
 * @member {Array.<module:model/FileDTO>} terminology
 */
InputFilesDTO.prototype['terminology'] = undefined;

/**
 * @member {Array.<module:model/FileDTO>} tm
 */
InputFilesDTO.prototype['tm'] = undefined;

/**
 * @member {Array.<module:model/FileDTO>} workFiles
 */
InputFilesDTO.prototype['workFiles'] = undefined;






export default InputFilesDTO;

