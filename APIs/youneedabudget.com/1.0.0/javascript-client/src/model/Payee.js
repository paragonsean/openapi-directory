/**
 * YNAB API Endpoints
 * Our API uses a REST based design, leverages the JSON data format, and relies upon HTTPS for transport. We respond with meaningful HTTP response codes and if an error occurs, we include error details in the response body.  API Documentation is at https://api.ynab.com
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Payee model module.
 * @module model/Payee
 * @version 1.0.0
 */
class Payee {
    /**
     * Constructs a new <code>Payee</code>.
     * @alias module:model/Payee
     * @param deleted {Boolean} Whether or not the payee has been deleted.  Deleted payees will only be included in delta requests.
     * @param id {String} 
     * @param name {String} 
     */
    constructor(deleted, id, name) { 
        
        Payee.initialize(this, deleted, id, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, deleted, id, name) { 
        obj['deleted'] = deleted;
        obj['id'] = id;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>Payee</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Payee} obj Optional instance to populate.
     * @return {module:model/Payee} The populated <code>Payee</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Payee();

            if (data.hasOwnProperty('deleted')) {
                obj['deleted'] = ApiClient.convertToType(data['deleted'], 'Boolean');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('transfer_account_id')) {
                obj['transfer_account_id'] = ApiClient.convertToType(data['transfer_account_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Payee</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Payee</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Payee.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['transfer_account_id'] && !(typeof data['transfer_account_id'] === 'string' || data['transfer_account_id'] instanceof String)) {
            throw new Error("Expected the field `transfer_account_id` to be a primitive type in the JSON string but got " + data['transfer_account_id']);
        }

        return true;
    }


}

Payee.RequiredProperties = ["deleted", "id", "name"];

/**
 * Whether or not the payee has been deleted.  Deleted payees will only be included in delta requests.
 * @member {Boolean} deleted
 */
Payee.prototype['deleted'] = undefined;

/**
 * @member {String} id
 */
Payee.prototype['id'] = undefined;

/**
 * @member {String} name
 */
Payee.prototype['name'] = undefined;

/**
 * If a transfer payee, the `account_id` to which this payee transfers to
 * @member {String} transfer_account_id
 */
Payee.prototype['transfer_account_id'] = undefined;






export default Payee;

