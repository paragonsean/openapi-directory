/**
 * YNAB API Endpoints
 * Our API uses a REST based design, leverages the JSON data format, and relies upon HTTPS for transport. We respond with meaningful HTTP response codes and if an error occurs, we include error details in the response body.  API Documentation is at https://api.ynab.com
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CategoryGroupWithCategories from './CategoryGroupWithCategories';

/**
 * The CategoriesResponseData model module.
 * @module model/CategoriesResponseData
 * @version 1.0.0
 */
class CategoriesResponseData {
    /**
     * Constructs a new <code>CategoriesResponseData</code>.
     * @alias module:model/CategoriesResponseData
     * @param categoryGroups {Array.<module:model/CategoryGroupWithCategories>} 
     * @param serverKnowledge {Number} The knowledge of the server
     */
    constructor(categoryGroups, serverKnowledge) { 
        
        CategoriesResponseData.initialize(this, categoryGroups, serverKnowledge);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categoryGroups, serverKnowledge) { 
        obj['category_groups'] = categoryGroups;
        obj['server_knowledge'] = serverKnowledge;
    }

    /**
     * Constructs a <code>CategoriesResponseData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CategoriesResponseData} obj Optional instance to populate.
     * @return {module:model/CategoriesResponseData} The populated <code>CategoriesResponseData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CategoriesResponseData();

            if (data.hasOwnProperty('category_groups')) {
                obj['category_groups'] = ApiClient.convertToType(data['category_groups'], [CategoryGroupWithCategories]);
            }
            if (data.hasOwnProperty('server_knowledge')) {
                obj['server_knowledge'] = ApiClient.convertToType(data['server_knowledge'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CategoriesResponseData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CategoriesResponseData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CategoriesResponseData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['category_groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['category_groups'])) {
                throw new Error("Expected the field `category_groups` to be an array in the JSON data but got " + data['category_groups']);
            }
            // validate the optional field `category_groups` (array)
            for (const item of data['category_groups']) {
                CategoryGroupWithCategories.validateJSON(item);
            };
        }

        return true;
    }


}

CategoriesResponseData.RequiredProperties = ["category_groups", "server_knowledge"];

/**
 * @member {Array.<module:model/CategoryGroupWithCategories>} category_groups
 */
CategoriesResponseData.prototype['category_groups'] = undefined;

/**
 * The knowledge of the server
 * @member {Number} server_knowledge
 */
CategoriesResponseData.prototype['server_knowledge'] = undefined;






export default CategoriesResponseData;

