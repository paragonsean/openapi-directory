/**
 * YNAB API Endpoints
 * Our API uses a REST based design, leverages the JSON data format, and relies upon HTTPS for transport. We respond with meaningful HTTP response codes and if an error occurs, we include error details in the response body.  API Documentation is at https://api.ynab.com
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PayeeLocation model module.
 * @module model/PayeeLocation
 * @version 1.0.0
 */
class PayeeLocation {
    /**
     * Constructs a new <code>PayeeLocation</code>.
     * @alias module:model/PayeeLocation
     * @param deleted {Boolean} Whether or not the payee location has been deleted.  Deleted payee locations will only be included in delta requests.
     * @param id {String} 
     * @param latitude {String} 
     * @param longitude {String} 
     * @param payeeId {String} 
     */
    constructor(deleted, id, latitude, longitude, payeeId) { 
        
        PayeeLocation.initialize(this, deleted, id, latitude, longitude, payeeId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, deleted, id, latitude, longitude, payeeId) { 
        obj['deleted'] = deleted;
        obj['id'] = id;
        obj['latitude'] = latitude;
        obj['longitude'] = longitude;
        obj['payee_id'] = payeeId;
    }

    /**
     * Constructs a <code>PayeeLocation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayeeLocation} obj Optional instance to populate.
     * @return {module:model/PayeeLocation} The populated <code>PayeeLocation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayeeLocation();

            if (data.hasOwnProperty('deleted')) {
                obj['deleted'] = ApiClient.convertToType(data['deleted'], 'Boolean');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('latitude')) {
                obj['latitude'] = ApiClient.convertToType(data['latitude'], 'String');
            }
            if (data.hasOwnProperty('longitude')) {
                obj['longitude'] = ApiClient.convertToType(data['longitude'], 'String');
            }
            if (data.hasOwnProperty('payee_id')) {
                obj['payee_id'] = ApiClient.convertToType(data['payee_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayeeLocation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayeeLocation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayeeLocation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['latitude'] && !(typeof data['latitude'] === 'string' || data['latitude'] instanceof String)) {
            throw new Error("Expected the field `latitude` to be a primitive type in the JSON string but got " + data['latitude']);
        }
        // ensure the json data is a string
        if (data['longitude'] && !(typeof data['longitude'] === 'string' || data['longitude'] instanceof String)) {
            throw new Error("Expected the field `longitude` to be a primitive type in the JSON string but got " + data['longitude']);
        }
        // ensure the json data is a string
        if (data['payee_id'] && !(typeof data['payee_id'] === 'string' || data['payee_id'] instanceof String)) {
            throw new Error("Expected the field `payee_id` to be a primitive type in the JSON string but got " + data['payee_id']);
        }

        return true;
    }


}

PayeeLocation.RequiredProperties = ["deleted", "id", "latitude", "longitude", "payee_id"];

/**
 * Whether or not the payee location has been deleted.  Deleted payee locations will only be included in delta requests.
 * @member {Boolean} deleted
 */
PayeeLocation.prototype['deleted'] = undefined;

/**
 * @member {String} id
 */
PayeeLocation.prototype['id'] = undefined;

/**
 * @member {String} latitude
 */
PayeeLocation.prototype['latitude'] = undefined;

/**
 * @member {String} longitude
 */
PayeeLocation.prototype['longitude'] = undefined;

/**
 * @member {String} payee_id
 */
PayeeLocation.prototype['payee_id'] = undefined;






export default PayeeLocation;

