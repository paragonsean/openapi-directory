/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FileKey from './FileKey';
import S3FileUploadPart from './S3FileUploadPart';

/**
 * The CompleteS3FileUploadRequest model module.
 * @module model/CompleteS3FileUploadRequest
 * @version 4.42.3
 */
class CompleteS3FileUploadRequest {
    /**
     * Constructs a new <code>CompleteS3FileUploadRequest</code>.
     * Request model for completing a S3 file upload
     * @alias module:model/CompleteS3FileUploadRequest
     * @param parts {Array.<module:model/S3FileUploadPart>} List of S3 file upload parts
     */
    constructor(parts) { 
        
        CompleteS3FileUploadRequest.initialize(this, parts);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, parts) { 
        obj['keepShareLinks'] = false;
        obj['parts'] = parts;
        obj['resolutionStrategy'] = 'autorename';
    }

    /**
     * Constructs a <code>CompleteS3FileUploadRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CompleteS3FileUploadRequest} obj Optional instance to populate.
     * @return {module:model/CompleteS3FileUploadRequest} The populated <code>CompleteS3FileUploadRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CompleteS3FileUploadRequest();

            if (data.hasOwnProperty('fileKey')) {
                obj['fileKey'] = FileKey.constructFromObject(data['fileKey']);
            }
            if (data.hasOwnProperty('fileName')) {
                obj['fileName'] = ApiClient.convertToType(data['fileName'], 'String');
            }
            if (data.hasOwnProperty('keepShareLinks')) {
                obj['keepShareLinks'] = ApiClient.convertToType(data['keepShareLinks'], 'Boolean');
            }
            if (data.hasOwnProperty('parts')) {
                obj['parts'] = ApiClient.convertToType(data['parts'], [S3FileUploadPart]);
            }
            if (data.hasOwnProperty('resolutionStrategy')) {
                obj['resolutionStrategy'] = ApiClient.convertToType(data['resolutionStrategy'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CompleteS3FileUploadRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CompleteS3FileUploadRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CompleteS3FileUploadRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `fileKey`
        if (data['fileKey']) { // data not null
          FileKey.validateJSON(data['fileKey']);
        }
        // ensure the json data is a string
        if (data['fileName'] && !(typeof data['fileName'] === 'string' || data['fileName'] instanceof String)) {
            throw new Error("Expected the field `fileName` to be a primitive type in the JSON string but got " + data['fileName']);
        }
        if (data['parts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['parts'])) {
                throw new Error("Expected the field `parts` to be an array in the JSON data but got " + data['parts']);
            }
            // validate the optional field `parts` (array)
            for (const item of data['parts']) {
                S3FileUploadPart.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['resolutionStrategy'] && !(typeof data['resolutionStrategy'] === 'string' || data['resolutionStrategy'] instanceof String)) {
            throw new Error("Expected the field `resolutionStrategy` to be a primitive type in the JSON string but got " + data['resolutionStrategy']);
        }

        return true;
    }


}

CompleteS3FileUploadRequest.RequiredProperties = ["parts"];

/**
 * @member {module:model/FileKey} fileKey
 */
CompleteS3FileUploadRequest.prototype['fileKey'] = undefined;

/**
 * New file name to store with
 * @member {String} fileName
 */
CompleteS3FileUploadRequest.prototype['fileName'] = undefined;

/**
 * Preserve Download Share Links and point them to the new node.
 * @member {Boolean} keepShareLinks
 * @default false
 */
CompleteS3FileUploadRequest.prototype['keepShareLinks'] = false;

/**
 * List of S3 file upload parts
 * @member {Array.<module:model/S3FileUploadPart>} parts
 */
CompleteS3FileUploadRequest.prototype['parts'] = undefined;

/**
 * Node conflict resolution strategy:  * `autorename`  * `overwrite`  * `fail`
 * @member {module:model/CompleteS3FileUploadRequest.ResolutionStrategyEnum} resolutionStrategy
 * @default 'autorename'
 */
CompleteS3FileUploadRequest.prototype['resolutionStrategy'] = 'autorename';





/**
 * Allowed values for the <code>resolutionStrategy</code> property.
 * @enum {String}
 * @readonly
 */
CompleteS3FileUploadRequest['ResolutionStrategyEnum'] = {

    /**
     * value: "autorename"
     * @const
     */
    "autorename": "autorename",

    /**
     * value: "overwrite"
     * @const
     */
    "overwrite": "overwrite",

    /**
     * value: "fail"
     * @const
     */
    "fail": "fail"
};



export default CompleteS3FileUploadRequest;

