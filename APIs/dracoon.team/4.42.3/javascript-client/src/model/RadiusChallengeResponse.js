/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RadiusChallengeResponse model module.
 * @module model/RadiusChallengeResponse
 * @version 4.42.3
 */
class RadiusChallengeResponse {
    /**
     * Constructs a new <code>RadiusChallengeResponse</code>.
     * RADIUS challenge reply
     * @alias module:model/RadiusChallengeResponse
     * @param code {Number} HTTP status code
     * @param message {String} HTTP status code description
     * @param replyMessage {String} RADIUS Reply-Message  Instruction how to handle the situation.
     * @param replyState {String} For RADIUS Access-Challenge  If a `replyState` is returned, it must be included as `state` in the following request.
     */
    constructor(code, message, replyMessage, replyState) { 
        
        RadiusChallengeResponse.initialize(this, code, message, replyMessage, replyState);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, code, message, replyMessage, replyState) { 
        obj['code'] = code;
        obj['message'] = message;
        obj['replyMessage'] = replyMessage;
        obj['replyState'] = replyState;
    }

    /**
     * Constructs a <code>RadiusChallengeResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RadiusChallengeResponse} obj Optional instance to populate.
     * @return {module:model/RadiusChallengeResponse} The populated <code>RadiusChallengeResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RadiusChallengeResponse();

            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'Number');
            }
            if (data.hasOwnProperty('debugInfo')) {
                obj['debugInfo'] = ApiClient.convertToType(data['debugInfo'], 'String');
            }
            if (data.hasOwnProperty('errorCode')) {
                obj['errorCode'] = ApiClient.convertToType(data['errorCode'], 'Number');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('replyMessage')) {
                obj['replyMessage'] = ApiClient.convertToType(data['replyMessage'], 'String');
            }
            if (data.hasOwnProperty('replyState')) {
                obj['replyState'] = ApiClient.convertToType(data['replyState'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RadiusChallengeResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RadiusChallengeResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RadiusChallengeResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['debugInfo'] && !(typeof data['debugInfo'] === 'string' || data['debugInfo'] instanceof String)) {
            throw new Error("Expected the field `debugInfo` to be a primitive type in the JSON string but got " + data['debugInfo']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['replyMessage'] && !(typeof data['replyMessage'] === 'string' || data['replyMessage'] instanceof String)) {
            throw new Error("Expected the field `replyMessage` to be a primitive type in the JSON string but got " + data['replyMessage']);
        }
        // ensure the json data is a string
        if (data['replyState'] && !(typeof data['replyState'] === 'string' || data['replyState'] instanceof String)) {
            throw new Error("Expected the field `replyState` to be a primitive type in the JSON string but got " + data['replyState']);
        }

        return true;
    }


}

RadiusChallengeResponse.RequiredProperties = ["code", "message", "replyMessage", "replyState"];

/**
 * HTTP status code
 * @member {Number} code
 */
RadiusChallengeResponse.prototype['code'] = undefined;

/**
 * Debug information
 * @member {String} debugInfo
 */
RadiusChallengeResponse.prototype['debugInfo'] = undefined;

/**
 * Internal error code
 * @member {Number} errorCode
 */
RadiusChallengeResponse.prototype['errorCode'] = undefined;

/**
 * HTTP status code description
 * @member {String} message
 */
RadiusChallengeResponse.prototype['message'] = undefined;

/**
 * RADIUS Reply-Message  Instruction how to handle the situation.
 * @member {String} replyMessage
 */
RadiusChallengeResponse.prototype['replyMessage'] = undefined;

/**
 * For RADIUS Access-Challenge  If a `replyState` is returned, it must be included as `state` in the following request.
 * @member {String} replyState
 */
RadiusChallengeResponse.prototype['replyState'] = undefined;






export default RadiusChallengeResponse;

