/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The S3FileUploadPart model module.
 * @module model/S3FileUploadPart
 * @version 4.42.3
 */
class S3FileUploadPart {
    /**
     * Constructs a new <code>S3FileUploadPart</code>.
     * S3 file upload part information
     * @alias module:model/S3FileUploadPart
     * @param partEtag {String} Corresponding part ETag
     * @param partNumber {Number} Corresponding part number
     */
    constructor(partEtag, partNumber) { 
        
        S3FileUploadPart.initialize(this, partEtag, partNumber);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, partEtag, partNumber) { 
        obj['partEtag'] = partEtag;
        obj['partNumber'] = partNumber;
    }

    /**
     * Constructs a <code>S3FileUploadPart</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/S3FileUploadPart} obj Optional instance to populate.
     * @return {module:model/S3FileUploadPart} The populated <code>S3FileUploadPart</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new S3FileUploadPart();

            if (data.hasOwnProperty('partEtag')) {
                obj['partEtag'] = ApiClient.convertToType(data['partEtag'], 'String');
            }
            if (data.hasOwnProperty('partNumber')) {
                obj['partNumber'] = ApiClient.convertToType(data['partNumber'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>S3FileUploadPart</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>S3FileUploadPart</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of S3FileUploadPart.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['partEtag'] && !(typeof data['partEtag'] === 'string' || data['partEtag'] instanceof String)) {
            throw new Error("Expected the field `partEtag` to be a primitive type in the JSON string but got " + data['partEtag']);
        }

        return true;
    }


}

S3FileUploadPart.RequiredProperties = ["partEtag", "partNumber"];

/**
 * Corresponding part ETag
 * @member {String} partEtag
 */
S3FileUploadPart.prototype['partEtag'] = undefined;

/**
 * Corresponding part number
 * @member {Number} partNumber
 */
S3FileUploadPart.prototype['partNumber'] = undefined;






export default S3FileUploadPart;

