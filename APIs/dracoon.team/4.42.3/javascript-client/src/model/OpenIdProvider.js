/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OpenIdProvider model module.
 * @module model/OpenIdProvider
 * @version 4.42.3
 */
class OpenIdProvider {
    /**
     * Constructs a new <code>OpenIdProvider</code>.
     * OpenID Connect provider information
     * @alias module:model/OpenIdProvider
     * @param id {Number} ID
     * @param isGlobalAvailable {Boolean} Is available for all customers
     * @param issuer {String} Issuer identifier of the IDP  The value is a case sensitive URL.
     * @param mappingClaim {String} Name of the claim which is used for the user mapping.
     * @param name {String} Name of the IDP
     */
    constructor(id, isGlobalAvailable, issuer, mappingClaim, name) { 
        
        OpenIdProvider.initialize(this, id, isGlobalAvailable, issuer, mappingClaim, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, isGlobalAvailable, issuer, mappingClaim, name) { 
        obj['id'] = id;
        obj['isGlobalAvailable'] = isGlobalAvailable;
        obj['issuer'] = issuer;
        obj['mappingClaim'] = mappingClaim;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>OpenIdProvider</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpenIdProvider} obj Optional instance to populate.
     * @return {module:model/OpenIdProvider} The populated <code>OpenIdProvider</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpenIdProvider();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('isGlobalAvailable')) {
                obj['isGlobalAvailable'] = ApiClient.convertToType(data['isGlobalAvailable'], 'Boolean');
            }
            if (data.hasOwnProperty('issuer')) {
                obj['issuer'] = ApiClient.convertToType(data['issuer'], 'String');
            }
            if (data.hasOwnProperty('mappingClaim')) {
                obj['mappingClaim'] = ApiClient.convertToType(data['mappingClaim'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('userManagementUrl')) {
                obj['userManagementUrl'] = ApiClient.convertToType(data['userManagementUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpenIdProvider</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpenIdProvider</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OpenIdProvider.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['issuer'] && !(typeof data['issuer'] === 'string' || data['issuer'] instanceof String)) {
            throw new Error("Expected the field `issuer` to be a primitive type in the JSON string but got " + data['issuer']);
        }
        // ensure the json data is a string
        if (data['mappingClaim'] && !(typeof data['mappingClaim'] === 'string' || data['mappingClaim'] instanceof String)) {
            throw new Error("Expected the field `mappingClaim` to be a primitive type in the JSON string but got " + data['mappingClaim']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['userManagementUrl'] && !(typeof data['userManagementUrl'] === 'string' || data['userManagementUrl'] instanceof String)) {
            throw new Error("Expected the field `userManagementUrl` to be a primitive type in the JSON string but got " + data['userManagementUrl']);
        }

        return true;
    }


}

OpenIdProvider.RequiredProperties = ["id", "isGlobalAvailable", "issuer", "mappingClaim", "name"];

/**
 * ID
 * @member {Number} id
 */
OpenIdProvider.prototype['id'] = undefined;

/**
 * Is available for all customers
 * @member {Boolean} isGlobalAvailable
 */
OpenIdProvider.prototype['isGlobalAvailable'] = undefined;

/**
 * Issuer identifier of the IDP  The value is a case sensitive URL.
 * @member {String} issuer
 */
OpenIdProvider.prototype['issuer'] = undefined;

/**
 * Name of the claim which is used for the user mapping.
 * @member {String} mappingClaim
 */
OpenIdProvider.prototype['mappingClaim'] = undefined;

/**
 * Name of the IDP
 * @member {String} name
 */
OpenIdProvider.prototype['name'] = undefined;

/**
 * URL of the user management UI.  Use empty string to remove.
 * @member {String} userManagementUrl
 */
OpenIdProvider.prototype['userManagementUrl'] = undefined;






export default OpenIdProvider;

