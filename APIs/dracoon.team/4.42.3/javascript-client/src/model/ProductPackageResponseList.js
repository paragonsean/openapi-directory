/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProductPackageResponse from './ProductPackageResponse';

/**
 * The ProductPackageResponseList model module.
 * @module model/ProductPackageResponseList
 * @version 4.42.3
 */
class ProductPackageResponseList {
    /**
     * Constructs a new <code>ProductPackageResponseList</code>.
     * ProductPackageResponseList
     * @alias module:model/ProductPackageResponseList
     * @param packages {Array.<module:model/ProductPackageResponse>} Packages
     */
    constructor(packages) { 
        
        ProductPackageResponseList.initialize(this, packages);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, packages) { 
        obj['packages'] = packages;
    }

    /**
     * Constructs a <code>ProductPackageResponseList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductPackageResponseList} obj Optional instance to populate.
     * @return {module:model/ProductPackageResponseList} The populated <code>ProductPackageResponseList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductPackageResponseList();

            if (data.hasOwnProperty('packages')) {
                obj['packages'] = ApiClient.convertToType(data['packages'], [ProductPackageResponse]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductPackageResponseList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductPackageResponseList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProductPackageResponseList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['packages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['packages'])) {
                throw new Error("Expected the field `packages` to be an array in the JSON data but got " + data['packages']);
            }
            // validate the optional field `packages` (array)
            for (const item of data['packages']) {
                ProductPackageResponse.validateJSON(item);
            };
        }

        return true;
    }


}

ProductPackageResponseList.RequiredProperties = ["packages"];

/**
 * Packages
 * @member {Array.<module:model/ProductPackageResponse>} packages
 */
ProductPackageResponseList.prototype['packages'] = undefined;






export default ProductPackageResponseList;

