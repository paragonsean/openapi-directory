/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NodePermissions from './NodePermissions';
import PublicKeyContainer from './PublicKeyContainer';
import UserInfo from './UserInfo';

/**
 * The RoomUser model module.
 * @module model/RoomUser
 * @version 4.42.3
 */
class RoomUser {
    /**
     * Constructs a new <code>RoomUser</code>.
     * User information
     * @alias module:model/RoomUser
     * @param displayName {String} &#128679; Deprecated since v4.11.0  Display name  use information from `UserInfo` instead to combine a display name
     * @param email {String} &#128679; Deprecated since v4.11.0  Email   use `email` from `UserInfo` instead
     * @param id {Number} &#128679; Deprecated since v4.11.0  Unique identifier for the user  use `id` from `UserInfo` instead
     * @param isGranted {Boolean} Is user granted room permissions
     * @param login {String} &#128679; Deprecated since v4.11.0  User login name
     * @param userInfo {module:model/UserInfo} 
     */
    constructor(displayName, email, id, isGranted, login, userInfo) { 
        
        RoomUser.initialize(this, displayName, email, id, isGranted, login, userInfo);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, displayName, email, id, isGranted, login, userInfo) { 
        obj['displayName'] = displayName;
        obj['email'] = email;
        obj['id'] = id;
        obj['isGranted'] = isGranted;
        obj['login'] = login;
        obj['userInfo'] = userInfo;
    }

    /**
     * Constructs a <code>RoomUser</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoomUser} obj Optional instance to populate.
     * @return {module:model/RoomUser} The populated <code>RoomUser</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoomUser();

            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('isGranted')) {
                obj['isGranted'] = ApiClient.convertToType(data['isGranted'], 'Boolean');
            }
            if (data.hasOwnProperty('login')) {
                obj['login'] = ApiClient.convertToType(data['login'], 'String');
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = NodePermissions.constructFromObject(data['permissions']);
            }
            if (data.hasOwnProperty('publicKeyContainer')) {
                obj['publicKeyContainer'] = PublicKeyContainer.constructFromObject(data['publicKeyContainer']);
            }
            if (data.hasOwnProperty('userInfo')) {
                obj['userInfo'] = UserInfo.constructFromObject(data['userInfo']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoomUser</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoomUser</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoomUser.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['login'] && !(typeof data['login'] === 'string' || data['login'] instanceof String)) {
            throw new Error("Expected the field `login` to be a primitive type in the JSON string but got " + data['login']);
        }
        // validate the optional field `permissions`
        if (data['permissions']) { // data not null
          NodePermissions.validateJSON(data['permissions']);
        }
        // validate the optional field `publicKeyContainer`
        if (data['publicKeyContainer']) { // data not null
          PublicKeyContainer.validateJSON(data['publicKeyContainer']);
        }
        // validate the optional field `userInfo`
        if (data['userInfo']) { // data not null
          UserInfo.validateJSON(data['userInfo']);
        }

        return true;
    }


}

RoomUser.RequiredProperties = ["displayName", "email", "id", "isGranted", "login", "userInfo"];

/**
 * &#128679; Deprecated since v4.11.0  Display name  use information from `UserInfo` instead to combine a display name
 * @member {String} displayName
 */
RoomUser.prototype['displayName'] = undefined;

/**
 * &#128679; Deprecated since v4.11.0  Email   use `email` from `UserInfo` instead
 * @member {String} email
 */
RoomUser.prototype['email'] = undefined;

/**
 * &#128679; Deprecated since v4.11.0  Unique identifier for the user  use `id` from `UserInfo` instead
 * @member {Number} id
 */
RoomUser.prototype['id'] = undefined;

/**
 * Is user granted room permissions
 * @member {Boolean} isGranted
 */
RoomUser.prototype['isGranted'] = undefined;

/**
 * &#128679; Deprecated since v4.11.0  User login name
 * @member {String} login
 */
RoomUser.prototype['login'] = undefined;

/**
 * @member {module:model/NodePermissions} permissions
 */
RoomUser.prototype['permissions'] = undefined;

/**
 * @member {module:model/PublicKeyContainer} publicKeyContainer
 */
RoomUser.prototype['publicKeyContainer'] = undefined;

/**
 * @member {module:model/UserInfo} userInfo
 */
RoomUser.prototype['userInfo'] = undefined;






export default RoomUser;

