/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NodePermissions from './NodePermissions';

/**
 * The RoomGroup model module.
 * @module model/RoomGroup
 * @version 4.42.3
 */
class RoomGroup {
    /**
     * Constructs a new <code>RoomGroup</code>.
     * Group information
     * @alias module:model/RoomGroup
     * @param id {Number} Unique identifier for the group
     * @param isGranted {Boolean} Is user granted room permissions
     * @param name {String} Group name
     */
    constructor(id, isGranted, name) { 
        
        RoomGroup.initialize(this, id, isGranted, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, isGranted, name) { 
        obj['id'] = id;
        obj['isGranted'] = isGranted;
        obj['name'] = name;
        obj['newGroupMemberAcceptance'] = 'autoallow';
    }

    /**
     * Constructs a <code>RoomGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoomGroup} obj Optional instance to populate.
     * @return {module:model/RoomGroup} The populated <code>RoomGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoomGroup();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('isGranted')) {
                obj['isGranted'] = ApiClient.convertToType(data['isGranted'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('newGroupMemberAcceptance')) {
                obj['newGroupMemberAcceptance'] = ApiClient.convertToType(data['newGroupMemberAcceptance'], 'String');
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = NodePermissions.constructFromObject(data['permissions']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoomGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoomGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoomGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['newGroupMemberAcceptance'] && !(typeof data['newGroupMemberAcceptance'] === 'string' || data['newGroupMemberAcceptance'] instanceof String)) {
            throw new Error("Expected the field `newGroupMemberAcceptance` to be a primitive type in the JSON string but got " + data['newGroupMemberAcceptance']);
        }
        // validate the optional field `permissions`
        if (data['permissions']) { // data not null
          NodePermissions.validateJSON(data['permissions']);
        }

        return true;
    }


}

RoomGroup.RequiredProperties = ["id", "isGranted", "name"];

/**
 * Unique identifier for the group
 * @member {Number} id
 */
RoomGroup.prototype['id'] = undefined;

/**
 * Is user granted room permissions
 * @member {Boolean} isGranted
 */
RoomGroup.prototype['isGranted'] = undefined;

/**
 * Group name
 * @member {String} name
 */
RoomGroup.prototype['name'] = undefined;

/**
 * Behaviour when new users are added to the group:  * `autoallow`  * `pending`    Only relevant if `adminGroupIds` has items.
 * @member {module:model/RoomGroup.NewGroupMemberAcceptanceEnum} newGroupMemberAcceptance
 * @default 'autoallow'
 */
RoomGroup.prototype['newGroupMemberAcceptance'] = 'autoallow';

/**
 * @member {module:model/NodePermissions} permissions
 */
RoomGroup.prototype['permissions'] = undefined;





/**
 * Allowed values for the <code>newGroupMemberAcceptance</code> property.
 * @enum {String}
 * @readonly
 */
RoomGroup['NewGroupMemberAcceptanceEnum'] = {

    /**
     * value: "autoallow"
     * @const
     */
    "autoallow": "autoallow",

    /**
     * value: "pending"
     * @const
     */
    "pending": "pending"
};



export default RoomGroup;

