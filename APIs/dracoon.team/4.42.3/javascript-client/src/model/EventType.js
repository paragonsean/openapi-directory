/**
 * DRACOON API
 * REST Web Services for DRACOON<br><br>This page provides an overview of all available and documented DRACOON APIs, which are grouped by tags.<br>Each tag provides a collection of APIs that are intended for a specific area of the DRACOON.<br><br><a title='Developer Information' href='https://developer.dracoon.com'>Developer Information</a>&emsp;&emsp;<a title='Get SDKs on GitHub' href='https://github.com/dracoon'>Get SDKs on GitHub</a><br><br><a title='Terms of service' href='https://www.dracoon.com/terms/general-terms-and-conditions/'>Terms of service</a>
 *
 * The version of the OpenAPI document: 4.42.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EventType model module.
 * @module model/EventType
 * @version 4.42.3
 */
class EventType {
    /**
     * Constructs a new <code>EventType</code>.
     * Event type information
     * @alias module:model/EventType
     * @param id {Number} ID
     * @param name {String} Name
     * @param usableCustomerAdminWebhook {Boolean} Usable as customer admin webhook
     * @param usableNodeWebhook {Boolean} Usable as node webhook
     * @param usablePushNotification {Boolean} Usable as push notification
     * @param usableTenantWebhook {Boolean} Usable as tenant webhook
     */
    constructor(id, name, usableCustomerAdminWebhook, usableNodeWebhook, usablePushNotification, usableTenantWebhook) { 
        
        EventType.initialize(this, id, name, usableCustomerAdminWebhook, usableNodeWebhook, usablePushNotification, usableTenantWebhook);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, usableCustomerAdminWebhook, usableNodeWebhook, usablePushNotification, usableTenantWebhook) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['usableCustomerAdminWebhook'] = usableCustomerAdminWebhook;
        obj['usableNodeWebhook'] = usableNodeWebhook;
        obj['usablePushNotification'] = usablePushNotification;
        obj['usableTenantWebhook'] = usableTenantWebhook;
    }

    /**
     * Constructs a <code>EventType</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventType} obj Optional instance to populate.
     * @return {module:model/EventType} The populated <code>EventType</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventType();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('usableCustomerAdminWebhook')) {
                obj['usableCustomerAdminWebhook'] = ApiClient.convertToType(data['usableCustomerAdminWebhook'], 'Boolean');
            }
            if (data.hasOwnProperty('usableNodeWebhook')) {
                obj['usableNodeWebhook'] = ApiClient.convertToType(data['usableNodeWebhook'], 'Boolean');
            }
            if (data.hasOwnProperty('usablePushNotification')) {
                obj['usablePushNotification'] = ApiClient.convertToType(data['usablePushNotification'], 'Boolean');
            }
            if (data.hasOwnProperty('usableTenantWebhook')) {
                obj['usableTenantWebhook'] = ApiClient.convertToType(data['usableTenantWebhook'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventType</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventType</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EventType.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

EventType.RequiredProperties = ["id", "name", "usableCustomerAdminWebhook", "usableNodeWebhook", "usablePushNotification", "usableTenantWebhook"];

/**
 * ID
 * @member {Number} id
 */
EventType.prototype['id'] = undefined;

/**
 * Name
 * @member {String} name
 */
EventType.prototype['name'] = undefined;

/**
 * Usable as customer admin webhook
 * @member {Boolean} usableCustomerAdminWebhook
 */
EventType.prototype['usableCustomerAdminWebhook'] = undefined;

/**
 * Usable as node webhook
 * @member {Boolean} usableNodeWebhook
 */
EventType.prototype['usableNodeWebhook'] = undefined;

/**
 * Usable as push notification
 * @member {Boolean} usablePushNotification
 */
EventType.prototype['usablePushNotification'] = undefined;

/**
 * Usable as tenant webhook
 * @member {Boolean} usableTenantWebhook
 */
EventType.prototype['usableTenantWebhook'] = undefined;






export default EventType;

