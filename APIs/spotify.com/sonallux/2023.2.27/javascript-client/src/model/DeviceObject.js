/**
 * Spotify Web API with fixes and improvements from sonallux
 * You can use Spotify's Web API to discover music and podcasts, manage your Spotify library, control audio playback, and much more. Browse our available Web API endpoints using the sidebar at left, or via the navigation bar on top of this page on smaller screens.  In order to make successful Web API requests your app will need a valid access token. One can be obtained through <a href=\"https://developer.spotify.com/documentation/general/guides/authorization-guide/\">OAuth 2.0</a>.  The base URI for all Web API requests is `https://api.spotify.com/v1`.  Need help? See our <a href=\"https://developer.spotify.com/documentation/web-api/guides/\">Web API guides</a> for more information, or visit the <a href=\"https://community.spotify.com/t5/Spotify-for-Developers/bd-p/Spotify_Developer\">Spotify for Developers community forum</a> to ask questions and connect with other developers. 
 *
 * The version of the OpenAPI document: 2023.2.27
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DeviceObject model module.
 * @module model/DeviceObject
 * @version 2023.2.27
 */
class DeviceObject {
    /**
     * Constructs a new <code>DeviceObject</code>.
     * @alias module:model/DeviceObject
     */
    constructor() { 
        
        DeviceObject.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DeviceObject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeviceObject} obj Optional instance to populate.
     * @return {module:model/DeviceObject} The populated <code>DeviceObject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeviceObject();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
            if (data.hasOwnProperty('is_private_session')) {
                obj['is_private_session'] = ApiClient.convertToType(data['is_private_session'], 'Boolean');
            }
            if (data.hasOwnProperty('is_restricted')) {
                obj['is_restricted'] = ApiClient.convertToType(data['is_restricted'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('volume_percent')) {
                obj['volume_percent'] = ApiClient.convertToType(data['volume_percent'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeviceObject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeviceObject</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * The device ID.
 * @member {String} id
 */
DeviceObject.prototype['id'] = undefined;

/**
 * If this device is the currently active device.
 * @member {Boolean} is_active
 */
DeviceObject.prototype['is_active'] = undefined;

/**
 * If this device is currently in a private session.
 * @member {Boolean} is_private_session
 */
DeviceObject.prototype['is_private_session'] = undefined;

/**
 * Whether controlling this device is restricted. At present if this is \"true\" then no Web API commands will be accepted by this device.
 * @member {Boolean} is_restricted
 */
DeviceObject.prototype['is_restricted'] = undefined;

/**
 * A human-readable name for the device. Some devices have a name that the user can configure (e.g. \\\"Loudest speaker\\\") and some devices have a generic name associated with the manufacturer or device model.
 * @member {String} name
 */
DeviceObject.prototype['name'] = undefined;

/**
 * Device type, such as \"computer\", \"smartphone\" or \"speaker\".
 * @member {String} type
 */
DeviceObject.prototype['type'] = undefined;

/**
 * The current volume in percent.
 * @member {Number} volume_percent
 */
DeviceObject.prototype['volume_percent'] = undefined;






export default DeviceObject;

