/**
 * Spotify Web API with fixes and improvements from sonallux
 * You can use Spotify's Web API to discover music and podcasts, manage your Spotify library, control audio playback, and much more. Browse our available Web API endpoints using the sidebar at left, or via the navigation bar on top of this page on smaller screens.  In order to make successful Web API requests your app will need a valid access token. One can be obtained through <a href=\"https://developer.spotify.com/documentation/general/guides/authorization-guide/\">OAuth 2.0</a>.  The base URI for all Web API requests is `https://api.spotify.com/v1`.  Need help? See our <a href=\"https://developer.spotify.com/documentation/web-api/guides/\">Web API guides</a> for more information, or visit the <a href=\"https://community.spotify.com/t5/Spotify-for-Developers/bd-p/Spotify_Developer\">Spotify for Developers community forum</a> to ask questions and connect with other developers. 
 *
 * The version of the OpenAPI document: 2023.2.27
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RecommendationSeedObject from './RecommendationSeedObject';
import TrackObject from './TrackObject';

/**
 * The RecommendationsObject model module.
 * @module model/RecommendationsObject
 * @version 2023.2.27
 */
class RecommendationsObject {
    /**
     * Constructs a new <code>RecommendationsObject</code>.
     * @alias module:model/RecommendationsObject
     * @param seeds {Array.<module:model/RecommendationSeedObject>} An array of recommendation seed objects. 
     * @param tracks {Array.<module:model/TrackObject>} An array of track objects ordered according to the parameters supplied. 
     */
    constructor(seeds, tracks) { 
        
        RecommendationsObject.initialize(this, seeds, tracks);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, seeds, tracks) { 
        obj['seeds'] = seeds;
        obj['tracks'] = tracks;
    }

    /**
     * Constructs a <code>RecommendationsObject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecommendationsObject} obj Optional instance to populate.
     * @return {module:model/RecommendationsObject} The populated <code>RecommendationsObject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecommendationsObject();

            if (data.hasOwnProperty('seeds')) {
                obj['seeds'] = ApiClient.convertToType(data['seeds'], [RecommendationSeedObject]);
            }
            if (data.hasOwnProperty('tracks')) {
                obj['tracks'] = ApiClient.convertToType(data['tracks'], [TrackObject]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecommendationsObject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecommendationsObject</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecommendationsObject.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['seeds']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['seeds'])) {
                throw new Error("Expected the field `seeds` to be an array in the JSON data but got " + data['seeds']);
            }
            // validate the optional field `seeds` (array)
            for (const item of data['seeds']) {
                RecommendationSeedObject.validateJSON(item);
            };
        }
        if (data['tracks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tracks'])) {
                throw new Error("Expected the field `tracks` to be an array in the JSON data but got " + data['tracks']);
            }
            // validate the optional field `tracks` (array)
            for (const item of data['tracks']) {
                TrackObject.validateJSON(item);
            };
        }

        return true;
    }


}

RecommendationsObject.RequiredProperties = ["seeds", "tracks"];

/**
 * An array of recommendation seed objects. 
 * @member {Array.<module:model/RecommendationSeedObject>} seeds
 */
RecommendationsObject.prototype['seeds'] = undefined;

/**
 * An array of track objects ordered according to the parameters supplied. 
 * @member {Array.<module:model/TrackObject>} tracks
 */
RecommendationsObject.prototype['tracks'] = undefined;






export default RecommendationsObject;

