/**
 * Spotify Web API
 * You can use Spotify's Web API to discover music and podcasts, manage your Spotify library, control audio playback, and much more. Browse our available Web API endpoints using the sidebar at left, or via the navigation bar on top of this page on smaller screens.  In order to make successful Web API requests your app will need a valid access token. One can be obtained through <a href=\"https://developer.spotify.com/documentation/general/guides/authorization-guide/\">OAuth 2.0</a>.  The base URI for all Web API requests is `https://api.spotify.com/v1`.  Need help? See our <a href=\"https://developer.spotify.com/documentation/web-api/guides/\">Web API guides</a> for more information, or visit the <a href=\"https://community.spotify.com/t5/Spotify-for-Developers/bd-p/Spotify_Developer\">Spotify for Developers community forum</a> to ask questions and connect with other developers. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ImageObject from './ImageObject';

/**
 * The CategoryObject model module.
 * @module model/CategoryObject
 * @version 1.0.0
 */
class CategoryObject {
    /**
     * Constructs a new <code>CategoryObject</code>.
     * @alias module:model/CategoryObject
     * @param href {String} A link to the Web API endpoint returning full details of the category. 
     * @param icons {Array.<module:model/ImageObject>} The category icon, in various sizes. 
     * @param id {String} The [Spotify category ID](/documentation/web-api/concepts/spotify-uris-ids) of the category. 
     * @param name {String} The name of the category. 
     */
    constructor(href, icons, id, name) { 
        
        CategoryObject.initialize(this, href, icons, id, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, href, icons, id, name) { 
        obj['href'] = href;
        obj['icons'] = icons;
        obj['id'] = id;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>CategoryObject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CategoryObject} obj Optional instance to populate.
     * @return {module:model/CategoryObject} The populated <code>CategoryObject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CategoryObject();

            if (data.hasOwnProperty('href')) {
                obj['href'] = ApiClient.convertToType(data['href'], 'String');
            }
            if (data.hasOwnProperty('icons')) {
                obj['icons'] = ApiClient.convertToType(data['icons'], [ImageObject]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CategoryObject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CategoryObject</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CategoryObject.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['href'] && !(typeof data['href'] === 'string' || data['href'] instanceof String)) {
            throw new Error("Expected the field `href` to be a primitive type in the JSON string but got " + data['href']);
        }
        if (data['icons']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['icons'])) {
                throw new Error("Expected the field `icons` to be an array in the JSON data but got " + data['icons']);
            }
            // validate the optional field `icons` (array)
            for (const item of data['icons']) {
                ImageObject.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

CategoryObject.RequiredProperties = ["href", "icons", "id", "name"];

/**
 * A link to the Web API endpoint returning full details of the category. 
 * @member {String} href
 */
CategoryObject.prototype['href'] = undefined;

/**
 * The category icon, in various sizes. 
 * @member {Array.<module:model/ImageObject>} icons
 */
CategoryObject.prototype['icons'] = undefined;

/**
 * The [Spotify category ID](/documentation/web-api/concepts/spotify-uris-ids) of the category. 
 * @member {String} id
 */
CategoryObject.prototype['id'] = undefined;

/**
 * The name of the category. 
 * @member {String} name
 */
CategoryObject.prototype['name'] = undefined;






export default CategoryObject;

