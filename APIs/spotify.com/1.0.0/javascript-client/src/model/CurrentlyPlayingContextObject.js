/**
 * Spotify Web API
 * You can use Spotify's Web API to discover music and podcasts, manage your Spotify library, control audio playback, and much more. Browse our available Web API endpoints using the sidebar at left, or via the navigation bar on top of this page on smaller screens.  In order to make successful Web API requests your app will need a valid access token. One can be obtained through <a href=\"https://developer.spotify.com/documentation/general/guides/authorization-guide/\">OAuth 2.0</a>.  The base URI for all Web API requests is `https://api.spotify.com/v1`.  Need help? See our <a href=\"https://developer.spotify.com/documentation/web-api/guides/\">Web API guides</a> for more information, or visit the <a href=\"https://community.spotify.com/t5/Spotify-for-Developers/bd-p/Spotify_Developer\">Spotify for Developers community forum</a> to ask questions and connect with other developers. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContextObject from './ContextObject';
import CurrentlyPlayingContextObjectItem from './CurrentlyPlayingContextObjectItem';
import DeviceObject from './DeviceObject';
import DisallowsObject from './DisallowsObject';

/**
 * The CurrentlyPlayingContextObject model module.
 * @module model/CurrentlyPlayingContextObject
 * @version 1.0.0
 */
class CurrentlyPlayingContextObject {
    /**
     * Constructs a new <code>CurrentlyPlayingContextObject</code>.
     * @alias module:model/CurrentlyPlayingContextObject
     */
    constructor() { 
        
        CurrentlyPlayingContextObject.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CurrentlyPlayingContextObject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurrentlyPlayingContextObject} obj Optional instance to populate.
     * @return {module:model/CurrentlyPlayingContextObject} The populated <code>CurrentlyPlayingContextObject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurrentlyPlayingContextObject();

            if (data.hasOwnProperty('actions')) {
                obj['actions'] = ApiClient.convertToType(data['actions'], DisallowsObject);
            }
            if (data.hasOwnProperty('context')) {
                obj['context'] = ApiClient.convertToType(data['context'], ContextObject);
            }
            if (data.hasOwnProperty('currently_playing_type')) {
                obj['currently_playing_type'] = ApiClient.convertToType(data['currently_playing_type'], 'String');
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = ApiClient.convertToType(data['device'], DeviceObject);
            }
            if (data.hasOwnProperty('is_playing')) {
                obj['is_playing'] = ApiClient.convertToType(data['is_playing'], 'Boolean');
            }
            if (data.hasOwnProperty('item')) {
                obj['item'] = CurrentlyPlayingContextObjectItem.constructFromObject(data['item']);
            }
            if (data.hasOwnProperty('progress_ms')) {
                obj['progress_ms'] = ApiClient.convertToType(data['progress_ms'], 'Number');
            }
            if (data.hasOwnProperty('repeat_state')) {
                obj['repeat_state'] = ApiClient.convertToType(data['repeat_state'], 'String');
            }
            if (data.hasOwnProperty('shuffle_state')) {
                obj['shuffle_state'] = ApiClient.convertToType(data['shuffle_state'], 'Boolean');
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurrentlyPlayingContextObject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurrentlyPlayingContextObject</code>.
     */
    static validateJSON(data) {
        // validate the optional field `actions`
        if (data['actions']) { // data not null
          DisallowsObject.validateJSON(data['actions']);
        }
        // validate the optional field `context`
        if (data['context']) { // data not null
          ContextObject.validateJSON(data['context']);
        }
        // ensure the json data is a string
        if (data['currently_playing_type'] && !(typeof data['currently_playing_type'] === 'string' || data['currently_playing_type'] instanceof String)) {
            throw new Error("Expected the field `currently_playing_type` to be a primitive type in the JSON string but got " + data['currently_playing_type']);
        }
        // validate the optional field `device`
        if (data['device']) { // data not null
          DeviceObject.validateJSON(data['device']);
        }
        // validate the optional field `item`
        if (data['item']) { // data not null
          CurrentlyPlayingContextObjectItem.validateJSON(data['item']);
        }
        // ensure the json data is a string
        if (data['repeat_state'] && !(typeof data['repeat_state'] === 'string' || data['repeat_state'] instanceof String)) {
            throw new Error("Expected the field `repeat_state` to be a primitive type in the JSON string but got " + data['repeat_state']);
        }

        return true;
    }


}



/**
 * Allows to update the user interface based on which playback actions are available within the current context. 
 * @member {module:model/DisallowsObject} actions
 */
CurrentlyPlayingContextObject.prototype['actions'] = undefined;

/**
 * A Context Object. Can be `null`.
 * @member {module:model/ContextObject} context
 */
CurrentlyPlayingContextObject.prototype['context'] = undefined;

/**
 * The object type of the currently playing item. Can be one of `track`, `episode`, `ad` or `unknown`. 
 * @member {String} currently_playing_type
 */
CurrentlyPlayingContextObject.prototype['currently_playing_type'] = undefined;

/**
 * The device that is currently active. 
 * @member {module:model/DeviceObject} device
 */
CurrentlyPlayingContextObject.prototype['device'] = undefined;

/**
 * If something is currently playing, return `true`.
 * @member {Boolean} is_playing
 */
CurrentlyPlayingContextObject.prototype['is_playing'] = undefined;

/**
 * @member {module:model/CurrentlyPlayingContextObjectItem} item
 */
CurrentlyPlayingContextObject.prototype['item'] = undefined;

/**
 * Progress into the currently playing track or episode. Can be `null`.
 * @member {Number} progress_ms
 */
CurrentlyPlayingContextObject.prototype['progress_ms'] = undefined;

/**
 * off, track, context
 * @member {String} repeat_state
 */
CurrentlyPlayingContextObject.prototype['repeat_state'] = undefined;

/**
 * If shuffle is on or off.
 * @member {Boolean} shuffle_state
 */
CurrentlyPlayingContextObject.prototype['shuffle_state'] = undefined;

/**
 * Unix Millisecond Timestamp when data was fetched.
 * @member {Number} timestamp
 */
CurrentlyPlayingContextObject.prototype['timestamp'] = undefined;






export default CurrentlyPlayingContextObject;

