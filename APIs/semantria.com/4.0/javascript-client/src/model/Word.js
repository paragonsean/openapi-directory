/**
 * Semantria
 * Semantria applies Text and Sentiment Analysis to tweets, facebook posts, surveys, reviews or enterprise content.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Word model module.
 * @module model/Word
 * @version 4.0
 */
class Word {
    /**
     * Constructs a new <code>Word</code>.
     * @alias module:model/Word
     * @param isNegated {Boolean} A flag indicating if the token is or is part of a sentiment inverting construction
     * @param sentimentScore {Number} The sentiment score associated with the word
     * @param stemmed {String} The stemmed form of the word
     * @param tag {String} POS marker of the word
     * @param title {String} The original word representation in text
     * @param type {String} Verbal representation of part of speech in addition to certain tag
     */
    constructor(isNegated, sentimentScore, stemmed, tag, title, type) { 
        
        Word.initialize(this, isNegated, sentimentScore, stemmed, tag, title, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, isNegated, sentimentScore, stemmed, tag, title, type) { 
        obj['is_negated'] = isNegated;
        obj['sentiment_score'] = sentimentScore;
        obj['stemmed'] = stemmed;
        obj['tag'] = tag;
        obj['title'] = title;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>Word</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Word} obj Optional instance to populate.
     * @return {module:model/Word} The populated <code>Word</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Word();

            if (data.hasOwnProperty('is_negated')) {
                obj['is_negated'] = ApiClient.convertToType(data['is_negated'], 'Boolean');
            }
            if (data.hasOwnProperty('sentiment_score')) {
                obj['sentiment_score'] = ApiClient.convertToType(data['sentiment_score'], 'Number');
            }
            if (data.hasOwnProperty('stemmed')) {
                obj['stemmed'] = ApiClient.convertToType(data['stemmed'], 'String');
            }
            if (data.hasOwnProperty('tag')) {
                obj['tag'] = ApiClient.convertToType(data['tag'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Word</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Word</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Word.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['stemmed'] && !(typeof data['stemmed'] === 'string' || data['stemmed'] instanceof String)) {
            throw new Error("Expected the field `stemmed` to be a primitive type in the JSON string but got " + data['stemmed']);
        }
        // ensure the json data is a string
        if (data['tag'] && !(typeof data['tag'] === 'string' || data['tag'] instanceof String)) {
            throw new Error("Expected the field `tag` to be a primitive type in the JSON string but got " + data['tag']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Word.RequiredProperties = ["is_negated", "sentiment_score", "stemmed", "tag", "title", "type"];

/**
 * A flag indicating if the token is or is part of a sentiment inverting construction
 * @member {Boolean} is_negated
 */
Word.prototype['is_negated'] = undefined;

/**
 * The sentiment score associated with the word
 * @member {Number} sentiment_score
 */
Word.prototype['sentiment_score'] = undefined;

/**
 * The stemmed form of the word
 * @member {String} stemmed
 */
Word.prototype['stemmed'] = undefined;

/**
 * POS marker of the word
 * @member {String} tag
 */
Word.prototype['tag'] = undefined;

/**
 * The original word representation in text
 * @member {String} title
 */
Word.prototype['title'] = undefined;

/**
 * Verbal representation of part of speech in addition to certain tag
 * @member {String} type
 */
Word.prototype['type'] = undefined;






export default Word;

