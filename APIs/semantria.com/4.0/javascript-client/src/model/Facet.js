/**
 * Semantria
 * Semantria applies Text and Sentiment Analysis to tweets, facebook posts, surveys, reviews or enterprise content.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Attribute from './Attribute';
import Mention from './Mention';

/**
 * The Facet model module.
 * @module model/Facet
 * @version 4.0
 */
class Facet {
    /**
     * Constructs a new <code>Facet</code>.
     * @alias module:model/Facet
     * @param attributes {Array.<module:model/Attribute>} Returns the attributes associated with this facet
     * @param count {Number} The count of occurrences of the facet in the text
     * @param label {String} The text of the facet
     * @param mentions {Array.<module:model/Mention>} Returns genuine forms of facet mentioned across the documents
     * @param negativeCount {Number} The count of negative occurrences of the facet across the documents
     * @param neutralCount {Number} The count of neutral occurrences of the facet across the documents
     * @param positiveCount {Number} The count of positive occurrences of the facet across the documents
     */
    constructor(attributes, count, label, mentions, negativeCount, neutralCount, positiveCount) { 
        
        Facet.initialize(this, attributes, count, label, mentions, negativeCount, neutralCount, positiveCount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, attributes, count, label, mentions, negativeCount, neutralCount, positiveCount) { 
        obj['attributes'] = attributes;
        obj['count'] = count;
        obj['label'] = label;
        obj['mentions'] = mentions;
        obj['negative_count'] = negativeCount;
        obj['neutral_count'] = neutralCount;
        obj['positive_count'] = positiveCount;
    }

    /**
     * Constructs a <code>Facet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Facet} obj Optional instance to populate.
     * @return {module:model/Facet} The populated <code>Facet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Facet();

            if (data.hasOwnProperty('attributes')) {
                obj['attributes'] = ApiClient.convertToType(data['attributes'], [Attribute]);
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('label')) {
                obj['label'] = ApiClient.convertToType(data['label'], 'String');
            }
            if (data.hasOwnProperty('mentions')) {
                obj['mentions'] = ApiClient.convertToType(data['mentions'], [Mention]);
            }
            if (data.hasOwnProperty('negative_count')) {
                obj['negative_count'] = ApiClient.convertToType(data['negative_count'], 'Number');
            }
            if (data.hasOwnProperty('neutral_count')) {
                obj['neutral_count'] = ApiClient.convertToType(data['neutral_count'], 'Number');
            }
            if (data.hasOwnProperty('positive_count')) {
                obj['positive_count'] = ApiClient.convertToType(data['positive_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Facet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Facet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Facet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['attributes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['attributes'])) {
                throw new Error("Expected the field `attributes` to be an array in the JSON data but got " + data['attributes']);
            }
            // validate the optional field `attributes` (array)
            for (const item of data['attributes']) {
                Attribute.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['label'] && !(typeof data['label'] === 'string' || data['label'] instanceof String)) {
            throw new Error("Expected the field `label` to be a primitive type in the JSON string but got " + data['label']);
        }
        if (data['mentions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['mentions'])) {
                throw new Error("Expected the field `mentions` to be an array in the JSON data but got " + data['mentions']);
            }
            // validate the optional field `mentions` (array)
            for (const item of data['mentions']) {
                Mention.validateJSON(item);
            };
        }

        return true;
    }


}

Facet.RequiredProperties = ["attributes", "count", "label", "mentions", "negative_count", "neutral_count", "positive_count"];

/**
 * Returns the attributes associated with this facet
 * @member {Array.<module:model/Attribute>} attributes
 */
Facet.prototype['attributes'] = undefined;

/**
 * The count of occurrences of the facet in the text
 * @member {Number} count
 */
Facet.prototype['count'] = undefined;

/**
 * The text of the facet
 * @member {String} label
 */
Facet.prototype['label'] = undefined;

/**
 * Returns genuine forms of facet mentioned across the documents
 * @member {Array.<module:model/Mention>} mentions
 */
Facet.prototype['mentions'] = undefined;

/**
 * The count of negative occurrences of the facet across the documents
 * @member {Number} negative_count
 */
Facet.prototype['negative_count'] = undefined;

/**
 * The count of neutral occurrences of the facet across the documents
 * @member {Number} neutral_count
 */
Facet.prototype['neutral_count'] = undefined;

/**
 * The count of positive occurrences of the facet across the documents
 * @member {Number} positive_count
 */
Facet.prototype['positive_count'] = undefined;






export default Facet;

