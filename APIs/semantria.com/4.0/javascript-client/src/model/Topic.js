/**
 * Semantria
 * Semantria applies Text and Sentiment Analysis to tweets, facebook posts, surveys, reviews or enterprise content.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Topic model module.
 * @module model/Topic
 * @version 4.0
 */
class Topic {
    /**
     * Constructs a new <code>Topic</code>.
     * @alias module:model/Topic
     * @param hitcount {Number} The number of documents within the collection that match the topic
     * @param id {String} Unique topic identifier
     * @param sentimentPolarity {module:model/Topic.SentimentPolarityEnum} Verbal representation of sentiment score. Can be \"negative\", \"positive\" or \"neutral\"
     * @param sentimentScore {Number} The sentiment score for documents content associated with the topic
     * @param title {String} The topic title, which is its label in the text
     * @param type {module:model/Topic.TypeEnum} Type of the topic; can be either \"concept\" or \"query\"
     */
    constructor(hitcount, id, sentimentPolarity, sentimentScore, title, type) { 
        
        Topic.initialize(this, hitcount, id, sentimentPolarity, sentimentScore, title, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hitcount, id, sentimentPolarity, sentimentScore, title, type) { 
        obj['hitcount'] = hitcount;
        obj['id'] = id;
        obj['sentiment_polarity'] = sentimentPolarity;
        obj['sentiment_score'] = sentimentScore;
        obj['title'] = title;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>Topic</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Topic} obj Optional instance to populate.
     * @return {module:model/Topic} The populated <code>Topic</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Topic();

            if (data.hasOwnProperty('hitcount')) {
                obj['hitcount'] = ApiClient.convertToType(data['hitcount'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('sentiment_polarity')) {
                obj['sentiment_polarity'] = ApiClient.convertToType(data['sentiment_polarity'], 'String');
            }
            if (data.hasOwnProperty('sentiment_score')) {
                obj['sentiment_score'] = ApiClient.convertToType(data['sentiment_score'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Topic</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Topic</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Topic.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['sentiment_polarity'] && !(typeof data['sentiment_polarity'] === 'string' || data['sentiment_polarity'] instanceof String)) {
            throw new Error("Expected the field `sentiment_polarity` to be a primitive type in the JSON string but got " + data['sentiment_polarity']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Topic.RequiredProperties = ["hitcount", "id", "sentiment_polarity", "sentiment_score", "title", "type"];

/**
 * The number of documents within the collection that match the topic
 * @member {Number} hitcount
 */
Topic.prototype['hitcount'] = undefined;

/**
 * Unique topic identifier
 * @member {String} id
 */
Topic.prototype['id'] = undefined;

/**
 * Verbal representation of sentiment score. Can be \"negative\", \"positive\" or \"neutral\"
 * @member {module:model/Topic.SentimentPolarityEnum} sentiment_polarity
 */
Topic.prototype['sentiment_polarity'] = undefined;

/**
 * The sentiment score for documents content associated with the topic
 * @member {Number} sentiment_score
 */
Topic.prototype['sentiment_score'] = undefined;

/**
 * The topic title, which is its label in the text
 * @member {String} title
 */
Topic.prototype['title'] = undefined;

/**
 * Type of the topic; can be either \"concept\" or \"query\"
 * @member {module:model/Topic.TypeEnum} type
 */
Topic.prototype['type'] = undefined;





/**
 * Allowed values for the <code>sentiment_polarity</code> property.
 * @enum {String}
 * @readonly
 */
Topic['SentimentPolarityEnum'] = {

    /**
     * value: "negative"
     * @const
     */
    "negative": "negative",

    /**
     * value: "positive"
     * @const
     */
    "positive": "positive",

    /**
     * value: "neutral"
     * @const
     */
    "neutral": "neutral"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Topic['TypeEnum'] = {

    /**
     * value: "concept"
     * @const
     */
    "concept": "concept",

    /**
     * value: "query"
     * @const
     */
    "query": "query"
};



export default Topic;

