/**
 * Semantria
 * Semantria applies Text and Sentiment Analysis to tweets, facebook posts, surveys, reviews or enterprise content.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SubCategory from './SubCategory';

/**
 * The AutoCategory model module.
 * @module model/AutoCategory
 * @version 4.0
 */
class AutoCategory {
    /**
     * Constructs a new <code>AutoCategory</code>.
     * @alias module:model/AutoCategory
     * @param categories {Array.<module:model/SubCategory>} List of sub-categories of the current category if applicable
     * @param sentimentPolarity {module:model/AutoCategory.SentimentPolarityEnum} Verbal representation of sentiment score. Can be \"negative\", \"positive\" or \"neutral\"
     * @param sentimentScore {Number} The sentiment score associated with this category
     * @param strengthScore {Number} Strength of the category matches with the document content
     * @param title {String} The category title, which is its label in the text
     * @param type {String} Type of category; can be either \"node\" (root level) or \"leaf\" (nested) value
     */
    constructor(categories, sentimentPolarity, sentimentScore, strengthScore, title, type) { 
        
        AutoCategory.initialize(this, categories, sentimentPolarity, sentimentScore, strengthScore, title, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categories, sentimentPolarity, sentimentScore, strengthScore, title, type) { 
        obj['categories'] = categories;
        obj['sentiment_polarity'] = sentimentPolarity;
        obj['sentiment_score'] = sentimentScore;
        obj['strength_score'] = strengthScore;
        obj['title'] = title;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>AutoCategory</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AutoCategory} obj Optional instance to populate.
     * @return {module:model/AutoCategory} The populated <code>AutoCategory</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AutoCategory();

            if (data.hasOwnProperty('categories')) {
                obj['categories'] = ApiClient.convertToType(data['categories'], [SubCategory]);
            }
            if (data.hasOwnProperty('sentiment_polarity')) {
                obj['sentiment_polarity'] = ApiClient.convertToType(data['sentiment_polarity'], 'String');
            }
            if (data.hasOwnProperty('sentiment_score')) {
                obj['sentiment_score'] = ApiClient.convertToType(data['sentiment_score'], 'Number');
            }
            if (data.hasOwnProperty('strength_score')) {
                obj['strength_score'] = ApiClient.convertToType(data['strength_score'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AutoCategory</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AutoCategory</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AutoCategory.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['categories']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['categories'])) {
                throw new Error("Expected the field `categories` to be an array in the JSON data but got " + data['categories']);
            }
            // validate the optional field `categories` (array)
            for (const item of data['categories']) {
                SubCategory.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['sentiment_polarity'] && !(typeof data['sentiment_polarity'] === 'string' || data['sentiment_polarity'] instanceof String)) {
            throw new Error("Expected the field `sentiment_polarity` to be a primitive type in the JSON string but got " + data['sentiment_polarity']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

AutoCategory.RequiredProperties = ["categories", "sentiment_polarity", "sentiment_score", "strength_score", "title", "type"];

/**
 * List of sub-categories of the current category if applicable
 * @member {Array.<module:model/SubCategory>} categories
 */
AutoCategory.prototype['categories'] = undefined;

/**
 * Verbal representation of sentiment score. Can be \"negative\", \"positive\" or \"neutral\"
 * @member {module:model/AutoCategory.SentimentPolarityEnum} sentiment_polarity
 */
AutoCategory.prototype['sentiment_polarity'] = undefined;

/**
 * The sentiment score associated with this category
 * @member {Number} sentiment_score
 */
AutoCategory.prototype['sentiment_score'] = undefined;

/**
 * Strength of the category matches with the document content
 * @member {Number} strength_score
 */
AutoCategory.prototype['strength_score'] = undefined;

/**
 * The category title, which is its label in the text
 * @member {String} title
 */
AutoCategory.prototype['title'] = undefined;

/**
 * Type of category; can be either \"node\" (root level) or \"leaf\" (nested) value
 * @member {String} type
 */
AutoCategory.prototype['type'] = undefined;





/**
 * Allowed values for the <code>sentiment_polarity</code> property.
 * @enum {String}
 * @readonly
 */
AutoCategory['SentimentPolarityEnum'] = {

    /**
     * value: "negative"
     * @const
     */
    "negative": "negative",

    /**
     * value: "positive"
     * @const
     */
    "positive": "positive",

    /**
     * value: "neutral"
     * @const
     */
    "neutral": "neutral"
};



export default AutoCategory;

