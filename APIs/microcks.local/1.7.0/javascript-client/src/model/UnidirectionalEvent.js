/**
 * Microcks API v1.7
 * API offered by Microcks, the Kubernetes native tools for API and microservices mocking and testing (microcks.io)
 *
 * The version of the OpenAPI document: 1.7.0
 * Contact: laurent@microcks.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AbstractExchange from './AbstractExchange';
import EventMessage from './EventMessage';

/**
 * The UnidirectionalEvent model module.
 * @module model/UnidirectionalEvent
 * @version 1.7.0
 */
class UnidirectionalEvent {
    /**
     * Constructs a new <code>UnidirectionalEvent</code>.
     * Representation of an unidirectional exchange as an event message
     * @alias module:model/UnidirectionalEvent
     * @implements module:model/AbstractExchange
     * @param type {module:model/UnidirectionalEvent.TypeEnum} Discriminant type for identifying kind of exchange
     * @param eventMessage {module:model/EventMessage} 
     */
    constructor(type, eventMessage) { 
        AbstractExchange.initialize(this, type);
        UnidirectionalEvent.initialize(this, type, eventMessage);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, eventMessage) { 
        obj['type'] = type;
        obj['eventMessage'] = eventMessage;
    }

    /**
     * Constructs a <code>UnidirectionalEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UnidirectionalEvent} obj Optional instance to populate.
     * @return {module:model/UnidirectionalEvent} The populated <code>UnidirectionalEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UnidirectionalEvent();
            AbstractExchange.constructFromObject(data, obj);

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('eventMessage')) {
                obj['eventMessage'] = EventMessage.constructFromObject(data['eventMessage']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UnidirectionalEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UnidirectionalEvent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UnidirectionalEvent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // validate the optional field `eventMessage`
        if (data['eventMessage']) { // data not null
          EventMessage.validateJSON(data['eventMessage']);
        }

        return true;
    }


}

UnidirectionalEvent.RequiredProperties = ["type", "eventMessage"];

/**
 * Discriminant type for identifying kind of exchange
 * @member {module:model/UnidirectionalEvent.TypeEnum} type
 */
UnidirectionalEvent.prototype['type'] = undefined;

/**
 * @member {module:model/EventMessage} eventMessage
 */
UnidirectionalEvent.prototype['eventMessage'] = undefined;


// Implement AbstractExchange interface:
/**
 * Discriminant type for identifying kind of exchange
 * @member {module:model/AbstractExchange.TypeEnum} type
 */
AbstractExchange.prototype['type'] = undefined;



/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
UnidirectionalEvent['TypeEnum'] = {

    /**
     * value: "reqRespPair"
     * @const
     */
    "reqRespPair": "reqRespPair",

    /**
     * value: "unidirEvent"
     * @const
     */
    "unidirEvent": "unidirEvent"
};



export default UnidirectionalEvent;

