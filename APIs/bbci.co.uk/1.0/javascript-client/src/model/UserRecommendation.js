/**
 * BBC iPlayer Business Layer
 * The definitive iPlayer API.
 *
 * The version of the OpenAPI document: 1.0
 * Contact: ibl-team@lists.forge.bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Episode from './Episode';

/**
 * The UserRecommendation model module.
 * @module model/UserRecommendation
 * @version 1.0
 */
class UserRecommendation {
    /**
     * Constructs a new <code>UserRecommendation</code>.
     * @alias module:model/UserRecommendation
     * @param algorithm {String} 
     * @param episode {module:model/Episode} 
     * @param type {module:model/UserRecommendation.TypeEnum} 
     */
    constructor(algorithm, episode, type) { 
        
        UserRecommendation.initialize(this, algorithm, episode, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, algorithm, episode, type) { 
        obj['algorithm'] = algorithm;
        obj['episode'] = episode;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>UserRecommendation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserRecommendation} obj Optional instance to populate.
     * @return {module:model/UserRecommendation} The populated <code>UserRecommendation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserRecommendation();

            if (data.hasOwnProperty('algorithm')) {
                obj['algorithm'] = ApiClient.convertToType(data['algorithm'], 'String');
            }
            if (data.hasOwnProperty('episode')) {
                obj['episode'] = Episode.constructFromObject(data['episode']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserRecommendation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserRecommendation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserRecommendation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['algorithm'] && !(typeof data['algorithm'] === 'string' || data['algorithm'] instanceof String)) {
            throw new Error("Expected the field `algorithm` to be a primitive type in the JSON string but got " + data['algorithm']);
        }
        // validate the optional field `episode`
        if (data['episode']) { // data not null
          Episode.validateJSON(data['episode']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

UserRecommendation.RequiredProperties = ["algorithm", "episode", "type"];

/**
 * @member {String} algorithm
 */
UserRecommendation.prototype['algorithm'] = undefined;

/**
 * @member {module:model/Episode} episode
 */
UserRecommendation.prototype['episode'] = undefined;

/**
 * @member {module:model/UserRecommendation.TypeEnum} type
 */
UserRecommendation.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
UserRecommendation['TypeEnum'] = {

    /**
     * value: "user_recommendation"
     * @const
     */
    "user_recommendation": "user_recommendation"
};



export default UserRecommendation;

