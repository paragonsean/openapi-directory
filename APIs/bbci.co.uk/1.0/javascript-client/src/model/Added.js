/**
 * BBC iPlayer Business Layer
 * The definitive iPlayer API.
 *
 * The version of the OpenAPI document: 1.0
 * Contact: ibl-team@lists.forge.bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Programme from './Programme';

/**
 * The Added model module.
 * @module model/Added
 * @version 1.0
 */
class Added {
    /**
     * Constructs a new <code>Added</code>.
     * @alias module:model/Added
     * @param programme {module:model/Programme} 
     * @param type {module:model/Added.TypeEnum} 
     * @param urn {String} 
     */
    constructor(programme, type, urn) { 
        
        Added.initialize(this, programme, type, urn);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, programme, type, urn) { 
        obj['programme'] = programme;
        obj['type'] = type;
        obj['urn'] = urn;
    }

    /**
     * Constructs a <code>Added</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Added} obj Optional instance to populate.
     * @return {module:model/Added} The populated <code>Added</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Added();

            if (data.hasOwnProperty('programme')) {
                obj['programme'] = Programme.constructFromObject(data['programme']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('urn')) {
                obj['urn'] = ApiClient.convertToType(data['urn'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Added</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Added</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Added.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `programme`
        if (data['programme']) { // data not null
          Programme.validateJSON(data['programme']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['urn'] && !(typeof data['urn'] === 'string' || data['urn'] instanceof String)) {
            throw new Error("Expected the field `urn` to be a primitive type in the JSON string but got " + data['urn']);
        }

        return true;
    }


}

Added.RequiredProperties = ["programme", "type", "urn"];

/**
 * @member {module:model/Programme} programme
 */
Added.prototype['programme'] = undefined;

/**
 * @member {module:model/Added.TypeEnum} type
 */
Added.prototype['type'] = undefined;

/**
 * @member {String} urn
 */
Added.prototype['urn'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Added['TypeEnum'] = {

    /**
     * value: "added"
     * @const
     */
    "added": "added"
};



export default Added;

