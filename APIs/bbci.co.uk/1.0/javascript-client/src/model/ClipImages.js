/**
 * BBC iPlayer Business Layer
 * The definitive iPlayer API.
 *
 * The version of the OpenAPI document: 1.0
 * Contact: ibl-team@lists.forge.bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ClipImages model module.
 * @module model/ClipImages
 * @version 1.0
 */
class ClipImages {
    /**
     * Constructs a new <code>ClipImages</code>.
     * @alias module:model/ClipImages
     * @param standard {String} 
     * @param type {module:model/ClipImages.TypeEnum} 
     */
    constructor(standard, type) { 
        
        ClipImages.initialize(this, standard, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, standard, type) { 
        obj['standard'] = standard;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ClipImages</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ClipImages} obj Optional instance to populate.
     * @return {module:model/ClipImages} The populated <code>ClipImages</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ClipImages();

            if (data.hasOwnProperty('inherited_from')) {
                obj['inherited_from'] = ApiClient.convertToType(data['inherited_from'], 'String');
            }
            if (data.hasOwnProperty('standard')) {
                obj['standard'] = ApiClient.convertToType(data['standard'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ClipImages</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ClipImages</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ClipImages.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['inherited_from'] && !(typeof data['inherited_from'] === 'string' || data['inherited_from'] instanceof String)) {
            throw new Error("Expected the field `inherited_from` to be a primitive type in the JSON string but got " + data['inherited_from']);
        }
        // ensure the json data is a string
        if (data['standard'] && !(typeof data['standard'] === 'string' || data['standard'] instanceof String)) {
            throw new Error("Expected the field `standard` to be a primitive type in the JSON string but got " + data['standard']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ClipImages.RequiredProperties = ["standard", "type"];

/**
 * @member {String} inherited_from
 */
ClipImages.prototype['inherited_from'] = undefined;

/**
 * @member {String} standard
 */
ClipImages.prototype['standard'] = undefined;

/**
 * @member {module:model/ClipImages.TypeEnum} type
 */
ClipImages.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ClipImages['TypeEnum'] = {

    /**
     * value: "image"
     * @const
     */
    "image": "image"
};



export default ClipImages;

