/**
 * BBC iPlayer Business Layer
 * The definitive iPlayer API.
 *
 * The version of the OpenAPI document: 1.0
 * Contact: ibl-team@lists.forge.bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Channel model module.
 * @module model/Channel
 * @version 1.0
 */
class Channel {
    /**
     * Constructs a new <code>Channel</code>.
     * @alias module:model/Channel
     * @param hasSchedule {Boolean} 
     * @param id {String} 
     * @param masterBrandId {String} 
     * @param masterBrandTitle {String} 
     * @param title {String} 
     * @param type {module:model/Channel.TypeEnum} 
     */
    constructor(hasSchedule, id, masterBrandId, masterBrandTitle, title, type) { 
        
        Channel.initialize(this, hasSchedule, id, masterBrandId, masterBrandTitle, title, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hasSchedule, id, masterBrandId, masterBrandTitle, title, type) { 
        obj['has_schedule'] = hasSchedule;
        obj['id'] = id;
        obj['master_brand_id'] = masterBrandId;
        obj['master_brand_title'] = masterBrandTitle;
        obj['title'] = title;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>Channel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Channel} obj Optional instance to populate.
     * @return {module:model/Channel} The populated <code>Channel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Channel();

            if (data.hasOwnProperty('has_schedule')) {
                obj['has_schedule'] = ApiClient.convertToType(data['has_schedule'], 'Boolean');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('master_brand_id')) {
                obj['master_brand_id'] = ApiClient.convertToType(data['master_brand_id'], 'String');
            }
            if (data.hasOwnProperty('master_brand_title')) {
                obj['master_brand_title'] = ApiClient.convertToType(data['master_brand_title'], 'String');
            }
            if (data.hasOwnProperty('on_air')) {
                obj['on_air'] = ApiClient.convertToType(data['on_air'], 'Boolean');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Channel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Channel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Channel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['master_brand_id'] && !(typeof data['master_brand_id'] === 'string' || data['master_brand_id'] instanceof String)) {
            throw new Error("Expected the field `master_brand_id` to be a primitive type in the JSON string but got " + data['master_brand_id']);
        }
        // ensure the json data is a string
        if (data['master_brand_title'] && !(typeof data['master_brand_title'] === 'string' || data['master_brand_title'] instanceof String)) {
            throw new Error("Expected the field `master_brand_title` to be a primitive type in the JSON string but got " + data['master_brand_title']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Channel.RequiredProperties = ["has_schedule", "id", "master_brand_id", "master_brand_title", "title", "type"];

/**
 * @member {Boolean} has_schedule
 */
Channel.prototype['has_schedule'] = undefined;

/**
 * @member {String} id
 */
Channel.prototype['id'] = undefined;

/**
 * @member {String} master_brand_id
 */
Channel.prototype['master_brand_id'] = undefined;

/**
 * @member {String} master_brand_title
 */
Channel.prototype['master_brand_title'] = undefined;

/**
 * @member {Boolean} on_air
 */
Channel.prototype['on_air'] = undefined;

/**
 * @member {String} title
 */
Channel.prototype['title'] = undefined;

/**
 * @member {module:model/Channel.TypeEnum} type
 */
Channel.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Channel['TypeEnum'] = {

    /**
     * value: "channel"
     * @const
     */
    "channel": "channel"
};



export default Channel;

