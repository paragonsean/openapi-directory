/**
 * BBC iPlayer Business Layer
 * The definitive iPlayer API.
 *
 * The version of the OpenAPI document: 1.0
 * Contact: ibl-team@lists.forge.bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Episode from './Episode';

/**
 * The SearchGroup model module.
 * @module model/SearchGroup
 * @version 1.0
 */
class SearchGroup {
    /**
     * Constructs a new <code>SearchGroup</code>.
     * @alias module:model/SearchGroup
     * @param episode {module:model/Episode} 
     * @param id {String} 
     * @param type {module:model/SearchGroup.TypeEnum} 
     */
    constructor(episode, id, type) { 
        
        SearchGroup.initialize(this, episode, id, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, episode, id, type) { 
        obj['episode'] = episode;
        obj['id'] = id;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>SearchGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SearchGroup} obj Optional instance to populate.
     * @return {module:model/SearchGroup} The populated <code>SearchGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SearchGroup();

            if (data.hasOwnProperty('episode')) {
                obj['episode'] = Episode.constructFromObject(data['episode']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SearchGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SearchGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SearchGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `episode`
        if (data['episode']) { // data not null
          Episode.validateJSON(data['episode']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

SearchGroup.RequiredProperties = ["episode", "id", "type"];

/**
 * @member {module:model/Episode} episode
 */
SearchGroup.prototype['episode'] = undefined;

/**
 * @member {String} id
 */
SearchGroup.prototype['id'] = undefined;

/**
 * @member {module:model/SearchGroup.TypeEnum} type
 */
SearchGroup.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
SearchGroup['TypeEnum'] = {

    /**
     * value: "search_group"
     * @const
     */
    "search_group": "search_group"
};



export default SearchGroup;

