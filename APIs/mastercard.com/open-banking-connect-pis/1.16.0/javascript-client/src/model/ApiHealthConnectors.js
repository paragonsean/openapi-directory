/**
 * Open Banking - Payments initiation service
 * Open Banking - Payments initiation service
 *
 * The version of the OpenAPI document: 1.16.0
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApiHealthConnectors model module.
 * @module model/ApiHealthConnectors
 * @version 1.16.0
 */
class ApiHealthConnectors {
    /**
     * Constructs a new <code>ApiHealthConnectors</code>.
     * @alias module:model/ApiHealthConnectors
     * @param name {String} Connector name
     * @param status {module:model/ApiHealthConnectors.StatusEnum} Overall connect platform availability status
     */
    constructor(name, status) { 
        
        ApiHealthConnectors.initialize(this, name, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, status) { 
        obj['name'] = name;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>ApiHealthConnectors</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiHealthConnectors} obj Optional instance to populate.
     * @return {module:model/ApiHealthConnectors} The populated <code>ApiHealthConnectors</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiHealthConnectors();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('statusMessage')) {
                obj['statusMessage'] = ApiClient.convertToType(data['statusMessage'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiHealthConnectors</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiHealthConnectors</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiHealthConnectors.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['statusMessage'] && !(typeof data['statusMessage'] === 'string' || data['statusMessage'] instanceof String)) {
            throw new Error("Expected the field `statusMessage` to be a primitive type in the JSON string but got " + data['statusMessage']);
        }

        return true;
    }


}

ApiHealthConnectors.RequiredProperties = ["name", "status"];

/**
 * Connector name
 * @member {String} name
 */
ApiHealthConnectors.prototype['name'] = undefined;

/**
 * Overall connect platform availability status
 * @member {module:model/ApiHealthConnectors.StatusEnum} status
 */
ApiHealthConnectors.prototype['status'] = undefined;

/**
 * Additional informational message
 * @member {String} statusMessage
 */
ApiHealthConnectors.prototype['statusMessage'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ApiHealthConnectors['StatusEnum'] = {

    /**
     * value: "UP"
     * @const
     */
    "UP": "UP",

    /**
     * value: "DOWN"
     * @const
     */
    "DOWN": "DOWN",

    /**
     * value: "DEGRADED"
     * @const
     */
    "DEGRADED": "DEGRADED"
};



export default ApiHealthConnectors;

