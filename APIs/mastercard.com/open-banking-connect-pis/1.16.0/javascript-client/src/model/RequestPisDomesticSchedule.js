/**
 * Open Banking - Payments initiation service
 * Open Banking - Payments initiation service
 *
 * The version of the OpenAPI document: 1.16.0
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RequestPisDomesticSchedule model module.
 * @module model/RequestPisDomesticSchedule
 * @version 1.16.0
 */
class RequestPisDomesticSchedule {
    /**
     * Constructs a new <code>RequestPisDomesticSchedule</code>.
     * @alias module:model/RequestPisDomesticSchedule
     * @param frequency {module:model/RequestPisDomesticSchedule.FrequencyEnum} Frequency type
     * @param startDate {Date} Start date for the schedule
     */
    constructor(frequency, startDate) { 
        
        RequestPisDomesticSchedule.initialize(this, frequency, startDate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, frequency, startDate) { 
        obj['frequency'] = frequency;
        obj['startDate'] = startDate;
    }

    /**
     * Constructs a <code>RequestPisDomesticSchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RequestPisDomesticSchedule} obj Optional instance to populate.
     * @return {module:model/RequestPisDomesticSchedule} The populated <code>RequestPisDomesticSchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RequestPisDomesticSchedule();

            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            if (data.hasOwnProperty('frequency')) {
                obj['frequency'] = ApiClient.convertToType(data['frequency'], 'String');
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RequestPisDomesticSchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RequestPisDomesticSchedule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RequestPisDomesticSchedule.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['frequency'] && !(typeof data['frequency'] === 'string' || data['frequency'] instanceof String)) {
            throw new Error("Expected the field `frequency` to be a primitive type in the JSON string but got " + data['frequency']);
        }

        return true;
    }


}

RequestPisDomesticSchedule.RequiredProperties = ["frequency", "startDate"];

/**
 * End date for the schedule
 * @member {Date} endDate
 */
RequestPisDomesticSchedule.prototype['endDate'] = undefined;

/**
 * Frequency type
 * @member {module:model/RequestPisDomesticSchedule.FrequencyEnum} frequency
 */
RequestPisDomesticSchedule.prototype['frequency'] = undefined;

/**
 * Start date for the schedule
 * @member {Date} startDate
 */
RequestPisDomesticSchedule.prototype['startDate'] = undefined;





/**
 * Allowed values for the <code>frequency</code> property.
 * @enum {String}
 * @readonly
 */
RequestPisDomesticSchedule['FrequencyEnum'] = {

    /**
     * value: "DAIL"
     * @const
     */
    "DAIL": "DAIL",

    /**
     * value: "WEEK"
     * @const
     */
    "WEEK": "WEEK",

    /**
     * value: "MNTH"
     * @const
     */
    "MNTH": "MNTH",

    /**
     * value: "QUTR"
     * @const
     */
    "QUTR": "QUTR",

    /**
     * value: "SEMI"
     * @const
     */
    "SEMI": "SEMI",

    /**
     * value: "YEAR"
     * @const
     */
    "YEAR": "YEAR"
};



export default RequestPisDomesticSchedule;

