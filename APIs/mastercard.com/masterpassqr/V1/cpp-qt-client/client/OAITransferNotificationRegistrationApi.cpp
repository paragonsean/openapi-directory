/**
 * Send Person to Merchant
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: V1
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAITransferNotificationRegistrationApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAITransferNotificationRegistrationApi::OAITransferNotificationRegistrationApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAITransferNotificationRegistrationApi::~OAITransferNotificationRegistrationApi() {
}

void OAITransferNotificationRegistrationApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.mastercard.com/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("createTransferNotificationRegistration", defaultConf);
    _serverIndices.insert("createTransferNotificationRegistration", 0);
    _serverConfigs.insert("deleteTransferNotificationRegistration", defaultConf);
    _serverIndices.insert("deleteTransferNotificationRegistration", 0);
    _serverConfigs.insert("notificationRegistrationAPIReadBy", defaultConf);
    _serverIndices.insert("notificationRegistrationAPIReadBy", 0);
    _serverConfigs.insert("notificationRegistrationAPIUpdate", defaultConf);
    _serverIndices.insert("notificationRegistrationAPIUpdate", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAITransferNotificationRegistrationApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAITransferNotificationRegistrationApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAITransferNotificationRegistrationApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAITransferNotificationRegistrationApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAITransferNotificationRegistrationApi::setUsername(const QString &username) {
    _username = username;
}

void OAITransferNotificationRegistrationApi::setPassword(const QString &password) {
    _password = password;
}


void OAITransferNotificationRegistrationApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAITransferNotificationRegistrationApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAITransferNotificationRegistrationApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAITransferNotificationRegistrationApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAITransferNotificationRegistrationApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAITransferNotificationRegistrationApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAITransferNotificationRegistrationApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAITransferNotificationRegistrationApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAITransferNotificationRegistrationApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAITransferNotificationRegistrationApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAITransferNotificationRegistrationApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAITransferNotificationRegistrationApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAITransferNotificationRegistrationApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAITransferNotificationRegistrationApi::createTransferNotificationRegistration(const QString &partner_id, const ::OpenAPI::OptionalParam<OAIAccountregistration_167_wrapper> &accountregistration) {
    QString fullPath = QString(_serverConfigs["createTransferNotificationRegistration"][_serverIndices.value("createTransferNotificationRegistration")].URL()+"/send/v1/partners/{partnerId}/notification-registries");
    
    
    {
        QString partner_idPathParam("{");
        partner_idPathParam.append("partnerId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "partnerId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"partnerId"+pathSuffix : pathPrefix;
        fullPath.replace(partner_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(partner_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (accountregistration.hasValue()){

        
        QByteArray output = accountregistration.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITransferNotificationRegistrationApi::createTransferNotificationRegistrationCallback);
    connect(this, &OAITransferNotificationRegistrationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAITransferNotificationRegistrationApi::createTransferNotificationRegistrationCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIAccountregistration_168_wrapper output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT createTransferNotificationRegistrationSignal(output);
        Q_EMIT createTransferNotificationRegistrationSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT createTransferNotificationRegistrationSignalE(output, error_type, error_str);
        Q_EMIT createTransferNotificationRegistrationSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT createTransferNotificationRegistrationSignalError(output, error_type, error_str);
        Q_EMIT createTransferNotificationRegistrationSignalErrorFull(worker, error_type, error_str);
    }
}

void OAITransferNotificationRegistrationApi::deleteTransferNotificationRegistration(const QString &partner_id, const QString &account_reg_ref) {
    QString fullPath = QString(_serverConfigs["deleteTransferNotificationRegistration"][_serverIndices.value("deleteTransferNotificationRegistration")].URL()+"/send/v1/partners/{partnerId}/notification-registries/{account-reg-ref}");
    
    
    {
        QString partner_idPathParam("{");
        partner_idPathParam.append("partnerId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "partnerId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"partnerId"+pathSuffix : pathPrefix;
        fullPath.replace(partner_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(partner_id)));
    }
    
    {
        QString account_reg_refPathParam("{");
        account_reg_refPathParam.append("account-reg-ref").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "account-reg-ref", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"account-reg-ref"+pathSuffix : pathPrefix;
        fullPath.replace(account_reg_refPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(account_reg_ref)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITransferNotificationRegistrationApi::deleteTransferNotificationRegistrationCallback);
    connect(this, &OAITransferNotificationRegistrationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAITransferNotificationRegistrationApi::deleteTransferNotificationRegistrationCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIAccountregistration_171_wrapper output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT deleteTransferNotificationRegistrationSignal(output);
        Q_EMIT deleteTransferNotificationRegistrationSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT deleteTransferNotificationRegistrationSignalE(output, error_type, error_str);
        Q_EMIT deleteTransferNotificationRegistrationSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT deleteTransferNotificationRegistrationSignalError(output, error_type, error_str);
        Q_EMIT deleteTransferNotificationRegistrationSignalErrorFull(worker, error_type, error_str);
    }
}

void OAITransferNotificationRegistrationApi::notificationRegistrationAPIReadBy(const QString &partner_id, const QString &account_reg_ref) {
    QString fullPath = QString(_serverConfigs["notificationRegistrationAPIReadBy"][_serverIndices.value("notificationRegistrationAPIReadBy")].URL()+"/send/v1/partners/{partnerId}/notification-registries/{account-reg-ref}");
    
    
    {
        QString partner_idPathParam("{");
        partner_idPathParam.append("partnerId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "partnerId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"partnerId"+pathSuffix : pathPrefix;
        fullPath.replace(partner_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(partner_id)));
    }
    
    {
        QString account_reg_refPathParam("{");
        account_reg_refPathParam.append("account-reg-ref").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "account-reg-ref", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"account-reg-ref"+pathSuffix : pathPrefix;
        fullPath.replace(account_reg_refPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(account_reg_ref)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITransferNotificationRegistrationApi::notificationRegistrationAPIReadByCallback);
    connect(this, &OAITransferNotificationRegistrationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAITransferNotificationRegistrationApi::notificationRegistrationAPIReadByCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIAccountregistration_172_wrapper output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT notificationRegistrationAPIReadBySignal(output);
        Q_EMIT notificationRegistrationAPIReadBySignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT notificationRegistrationAPIReadBySignalE(output, error_type, error_str);
        Q_EMIT notificationRegistrationAPIReadBySignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT notificationRegistrationAPIReadBySignalError(output, error_type, error_str);
        Q_EMIT notificationRegistrationAPIReadBySignalErrorFull(worker, error_type, error_str);
    }
}

void OAITransferNotificationRegistrationApi::notificationRegistrationAPIUpdate(const QString &partner_id, const QString &account_reg_ref, const ::OpenAPI::OptionalParam<OAIAccountregistration_169_wrapper> &accountregistration) {
    QString fullPath = QString(_serverConfigs["notificationRegistrationAPIUpdate"][_serverIndices.value("notificationRegistrationAPIUpdate")].URL()+"/send/v1/partners/{partnerId}/notification-registries/{account-reg-ref}");
    
    
    {
        QString partner_idPathParam("{");
        partner_idPathParam.append("partnerId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "partnerId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"partnerId"+pathSuffix : pathPrefix;
        fullPath.replace(partner_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(partner_id)));
    }
    
    {
        QString account_reg_refPathParam("{");
        account_reg_refPathParam.append("account-reg-ref").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "account-reg-ref", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"account-reg-ref"+pathSuffix : pathPrefix;
        fullPath.replace(account_reg_refPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(account_reg_ref)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PUT");

    if (accountregistration.hasValue()){

        
        QByteArray output = accountregistration.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITransferNotificationRegistrationApi::notificationRegistrationAPIUpdateCallback);
    connect(this, &OAITransferNotificationRegistrationApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAITransferNotificationRegistrationApi::notificationRegistrationAPIUpdateCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIAccountregistration_170_wrapper output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT notificationRegistrationAPIUpdateSignal(output);
        Q_EMIT notificationRegistrationAPIUpdateSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT notificationRegistrationAPIUpdateSignalE(output, error_type, error_str);
        Q_EMIT notificationRegistrationAPIUpdateSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT notificationRegistrationAPIUpdateSignalError(output, error_type, error_str);
        Q_EMIT notificationRegistrationAPIUpdateSignalErrorFull(worker, error_type, error_str);
    }
}

void OAITransferNotificationRegistrationApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
