# spending_pulse

SpendingPulse - JavaScript client for spending_pulse
This API will provide monthly data which includes metrics such as sales volume and growth rate.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0
- Package version: 1.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install spending_pulse --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your spending_pulse from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var SpendingPulse = require('spending_pulse');


var api = new SpendingPulse.GasWeeklyApi()
var opts = {
  'currentRow': "1", // {String} Starting record number to return.
  'offset': "25" // {String} Used to restrict the number of records returned if needed to be less than max.
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.gasweeklyGet(opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.mastercard.com/spendingpulse/v1/spulse.svc*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*SpendingPulse.GasWeeklyApi* | [**gasweeklyGet**](docs/GasWeeklyApi.md#gasweeklyGet) | **GET** /gasweekly | Returns the weekly gasoline report. This resource pulls back the report with ProductLine &#x3D; \&quot;US Weekly Gasoline Demand Report\&quot;. Keep in mind that you must be subscribed to the gasoline weekly report to be able to receive data back from this resource.
*SpendingPulse.ParametersApi* | [**parametersGet**](docs/ParametersApi.md#parametersGet) | **GET** /parameters | Returns a distinct list of all reports are available and that one is subscribed to.
*SpendingPulse.SpendingPulseReportApi* | [**spendingpulseGet**](docs/SpendingPulseReportApi.md#spendingpulseGet) | **GET** /spendingpulse | Returns all Spending Pulse reports (with the exception of the gasoline weekly report, which has its own resource), that one is subscribed to.
*SpendingPulse.SubscriptionApi* | [**subscriptionGet**](docs/SubscriptionApi.md#subscriptionGet) | **GET** /subscription | Returns a list of all reports one is currently subscribed to.


## Documentation for Models

 - [SpendingPulse.Error](docs/Error.md)
 - [SpendingPulse.ErrorSchema](docs/ErrorSchema.md)
 - [SpendingPulse.Errors](docs/Errors.md)
 - [SpendingPulse.GasWeeklyArray](docs/GasWeeklyArray.md)
 - [SpendingPulse.GasWeeklyList](docs/GasWeeklyList.md)
 - [SpendingPulse.GasWeeklyListResponse](docs/GasWeeklyListResponse.md)
 - [SpendingPulse.GasWeeklyRecord](docs/GasWeeklyRecord.md)
 - [SpendingPulse.Parameter](docs/Parameter.md)
 - [SpendingPulse.ParameterArray](docs/ParameterArray.md)
 - [SpendingPulse.ParameterList](docs/ParameterList.md)
 - [SpendingPulse.ParameterListResponse](docs/ParameterListResponse.md)
 - [SpendingPulse.SpendingPulseArray](docs/SpendingPulseArray.md)
 - [SpendingPulse.SpendingPulseList](docs/SpendingPulseList.md)
 - [SpendingPulse.SpendingPulseListResponse](docs/SpendingPulseListResponse.md)
 - [SpendingPulse.SpendingPulseRecord](docs/SpendingPulseRecord.md)
 - [SpendingPulse.Subscription](docs/Subscription.md)
 - [SpendingPulse.SubscriptionArray](docs/SubscriptionArray.md)
 - [SpendingPulse.SubscriptionList](docs/SubscriptionList.md)
 - [SpendingPulse.SubscriptionListResponse](docs/SubscriptionListResponse.md)


## Documentation for Authorization

Endpoints do not require authorization.

