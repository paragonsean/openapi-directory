/*
 * MATCH API
 * Helps acquirers identify potentially high-risk merchants before entering to a merchant agreement.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.Url;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * UrlGroup
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T10:09:23.990905-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class UrlGroup {
  public static final String SERIALIZED_NAME_CLOSE_MATCH_URLS = "CloseMatchUrls";
  @SerializedName(SERIALIZED_NAME_CLOSE_MATCH_URLS)
  private List<Url> closeMatchUrls = new ArrayList<>();

  public static final String SERIALIZED_NAME_EXACT_MATCH_URLS = "ExactMatchUrls";
  @SerializedName(SERIALIZED_NAME_EXACT_MATCH_URLS)
  private List<Url> exactMatchUrls = new ArrayList<>();

  public static final String SERIALIZED_NAME_NO_MATCH_URLS = "NoMatchUrls";
  @SerializedName(SERIALIZED_NAME_NO_MATCH_URLS)
  private Url noMatchUrls;

  public UrlGroup() {
  }

  public UrlGroup closeMatchUrls(List<Url> closeMatchUrls) {
    this.closeMatchUrls = closeMatchUrls;
    return this;
  }

  public UrlGroup addCloseMatchUrlsItem(Url closeMatchUrlsItem) {
    if (this.closeMatchUrls == null) {
      this.closeMatchUrls = new ArrayList<>();
    }
    this.closeMatchUrls.add(closeMatchUrlsItem);
    return this;
  }

  /**
   * The closely matched URL of Merchant.The results may return 0 or more URLs.
   * @return closeMatchUrls
   */
  @javax.annotation.Nullable
  public List<Url> getCloseMatchUrls() {
    return closeMatchUrls;
  }

  public void setCloseMatchUrls(List<Url> closeMatchUrls) {
    this.closeMatchUrls = closeMatchUrls;
  }


  public UrlGroup exactMatchUrls(List<Url> exactMatchUrls) {
    this.exactMatchUrls = exactMatchUrls;
    return this;
  }

  public UrlGroup addExactMatchUrlsItem(Url exactMatchUrlsItem) {
    if (this.exactMatchUrls == null) {
      this.exactMatchUrls = new ArrayList<>();
    }
    this.exactMatchUrls.add(exactMatchUrlsItem);
    return this;
  }

  /**
   * The exactly matched URL of Merchant.The results may return 0 or more URLs.
   * @return exactMatchUrls
   */
  @javax.annotation.Nullable
  public List<Url> getExactMatchUrls() {
    return exactMatchUrls;
  }

  public void setExactMatchUrls(List<Url> exactMatchUrls) {
    this.exactMatchUrls = exactMatchUrls;
  }


  public UrlGroup noMatchUrls(Url noMatchUrls) {
    this.noMatchUrls = noMatchUrls;
    return this;
  }

  /**
   * Get noMatchUrls
   * @return noMatchUrls
   */
  @javax.annotation.Nullable
  public Url getNoMatchUrls() {
    return noMatchUrls;
  }

  public void setNoMatchUrls(Url noMatchUrls) {
    this.noMatchUrls = noMatchUrls;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    UrlGroup urlGroup = (UrlGroup) o;
    return Objects.equals(this.closeMatchUrls, urlGroup.closeMatchUrls) &&
        Objects.equals(this.exactMatchUrls, urlGroup.exactMatchUrls) &&
        Objects.equals(this.noMatchUrls, urlGroup.noMatchUrls);
  }

  @Override
  public int hashCode() {
    return Objects.hash(closeMatchUrls, exactMatchUrls, noMatchUrls);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class UrlGroup {\n");
    sb.append("    closeMatchUrls: ").append(toIndentedString(closeMatchUrls)).append("\n");
    sb.append("    exactMatchUrls: ").append(toIndentedString(exactMatchUrls)).append("\n");
    sb.append("    noMatchUrls: ").append(toIndentedString(noMatchUrls)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("CloseMatchUrls");
    openapiFields.add("ExactMatchUrls");
    openapiFields.add("NoMatchUrls");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to UrlGroup
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!UrlGroup.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in UrlGroup is not found in the empty JSON string", UrlGroup.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!UrlGroup.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `UrlGroup` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("CloseMatchUrls") != null && !jsonObj.get("CloseMatchUrls").isJsonNull()) {
        JsonArray jsonArraycloseMatchUrls = jsonObj.getAsJsonArray("CloseMatchUrls");
        if (jsonArraycloseMatchUrls != null) {
          // ensure the json data is an array
          if (!jsonObj.get("CloseMatchUrls").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `CloseMatchUrls` to be an array in the JSON string but got `%s`", jsonObj.get("CloseMatchUrls").toString()));
          }

          // validate the optional field `CloseMatchUrls` (array)
          for (int i = 0; i < jsonArraycloseMatchUrls.size(); i++) {
            Url.validateJsonElement(jsonArraycloseMatchUrls.get(i));
          };
        }
      }
      if (jsonObj.get("ExactMatchUrls") != null && !jsonObj.get("ExactMatchUrls").isJsonNull()) {
        JsonArray jsonArrayexactMatchUrls = jsonObj.getAsJsonArray("ExactMatchUrls");
        if (jsonArrayexactMatchUrls != null) {
          // ensure the json data is an array
          if (!jsonObj.get("ExactMatchUrls").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `ExactMatchUrls` to be an array in the JSON string but got `%s`", jsonObj.get("ExactMatchUrls").toString()));
          }

          // validate the optional field `ExactMatchUrls` (array)
          for (int i = 0; i < jsonArrayexactMatchUrls.size(); i++) {
            Url.validateJsonElement(jsonArrayexactMatchUrls.get(i));
          };
        }
      }
      // validate the optional field `NoMatchUrls`
      if (jsonObj.get("NoMatchUrls") != null && !jsonObj.get("NoMatchUrls").isJsonNull()) {
        Url.validateJsonElement(jsonObj.get("NoMatchUrls"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!UrlGroup.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'UrlGroup' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<UrlGroup> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(UrlGroup.class));

       return (TypeAdapter<T>) new TypeAdapter<UrlGroup>() {
           @Override
           public void write(JsonWriter out, UrlGroup value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public UrlGroup read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of UrlGroup given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of UrlGroup
   * @throws IOException if the JSON string is invalid with respect to UrlGroup
   */
  public static UrlGroup fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, UrlGroup.class);
  }

  /**
   * Convert an instance of UrlGroup to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

