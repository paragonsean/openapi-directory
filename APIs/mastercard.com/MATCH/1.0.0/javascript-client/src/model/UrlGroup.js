/**
 * MATCH API
 * Helps acquirers identify potentially high-risk merchants before entering to a merchant agreement.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Url from './Url';

/**
 * The UrlGroup model module.
 * @module model/UrlGroup
 * @version 1.0.0
 */
class UrlGroup {
    /**
     * Constructs a new <code>UrlGroup</code>.
     * @alias module:model/UrlGroup
     */
    constructor() { 
        
        UrlGroup.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>UrlGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UrlGroup} obj Optional instance to populate.
     * @return {module:model/UrlGroup} The populated <code>UrlGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UrlGroup();

            if (data.hasOwnProperty('CloseMatchUrls')) {
                obj['CloseMatchUrls'] = ApiClient.convertToType(data['CloseMatchUrls'], [Url]);
            }
            if (data.hasOwnProperty('ExactMatchUrls')) {
                obj['ExactMatchUrls'] = ApiClient.convertToType(data['ExactMatchUrls'], [Url]);
            }
            if (data.hasOwnProperty('NoMatchUrls')) {
                obj['NoMatchUrls'] = Url.constructFromObject(data['NoMatchUrls']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UrlGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UrlGroup</code>.
     */
    static validateJSON(data) {
        if (data['CloseMatchUrls']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['CloseMatchUrls'])) {
                throw new Error("Expected the field `CloseMatchUrls` to be an array in the JSON data but got " + data['CloseMatchUrls']);
            }
            // validate the optional field `CloseMatchUrls` (array)
            for (const item of data['CloseMatchUrls']) {
                Url.validateJSON(item);
            };
        }
        if (data['ExactMatchUrls']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ExactMatchUrls'])) {
                throw new Error("Expected the field `ExactMatchUrls` to be an array in the JSON data but got " + data['ExactMatchUrls']);
            }
            // validate the optional field `ExactMatchUrls` (array)
            for (const item of data['ExactMatchUrls']) {
                Url.validateJSON(item);
            };
        }
        // validate the optional field `NoMatchUrls`
        if (data['NoMatchUrls']) { // data not null
          Url.validateJSON(data['NoMatchUrls']);
        }

        return true;
    }


}



/**
 * The closely matched URL of Merchant.The results may return 0 or more URLs.
 * @member {Array.<module:model/Url>} CloseMatchUrls
 */
UrlGroup.prototype['CloseMatchUrls'] = undefined;

/**
 * The exactly matched URL of Merchant.The results may return 0 or more URLs.
 * @member {Array.<module:model/Url>} ExactMatchUrls
 */
UrlGroup.prototype['ExactMatchUrls'] = undefined;

/**
 * @member {module:model/Url} NoMatchUrls
 */
UrlGroup.prototype['NoMatchUrls'] = undefined;






export default UrlGroup;

