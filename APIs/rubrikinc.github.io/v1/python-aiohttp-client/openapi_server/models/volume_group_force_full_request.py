# coding: utf-8

from datetime import date, datetime

from typing import List, Dict, Type

from openapi_server.models.base_model import Model
from openapi_server.models.force_full_volume_info import ForceFullVolumeInfo
from openapi_server import util


class VolumeGroupForceFullRequest(Model):
    """NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

    Do not edit the class manually.
    """

    def __init__(self, volume_infos: List[ForceFullVolumeInfo]=None):
        """VolumeGroupForceFullRequest - a model defined in OpenAPI

        :param volume_infos: The volume_infos of this VolumeGroupForceFullRequest.
        """
        self.openapi_types = {
            'volume_infos': List[ForceFullVolumeInfo]
        }

        self.attribute_map = {
            'volume_infos': 'volumeInfos'
        }

        self._volume_infos = volume_infos

    @classmethod
    def from_dict(cls, dikt: dict) -> 'VolumeGroupForceFullRequest':
        """Returns the dict as a model

        :param dikt: A dict.
        :return: The VolumeGroupForceFullRequest of this VolumeGroupForceFullRequest.
        """
        return util.deserialize_model(dikt, cls)

    @property
    def volume_infos(self):
        """Gets the volume_infos of this VolumeGroupForceFullRequest.

        Configuration for forcing a full snapshot for the listed volumes in the request. The configuration specifies which volumes in a Volume Group should get forced full snapshots, and whether to perform deduplication. If the configuration is missing, no forced full snapshot is requested. If the configuration contains an empty array, a forced full snapshot is requested for all volumes in the Volume Group and deduplication is performed by default. If the configuration array contains specific volumes, a forced full snapshot is requested for these volumes only. The shouldDedupe flag determines whether deduplication is performed. If a forced full snapshot is requested, the next backup job checks the configuration and takes the full snapshot according to the specification. After the full snapshot is taken, the backup job clears the configuration to prevent additional full snapshots from being taken.

        :return: The volume_infos of this VolumeGroupForceFullRequest.
        :rtype: List[ForceFullVolumeInfo]
        """
        return self._volume_infos

    @volume_infos.setter
    def volume_infos(self, volume_infos):
        """Sets the volume_infos of this VolumeGroupForceFullRequest.

        Configuration for forcing a full snapshot for the listed volumes in the request. The configuration specifies which volumes in a Volume Group should get forced full snapshots, and whether to perform deduplication. If the configuration is missing, no forced full snapshot is requested. If the configuration contains an empty array, a forced full snapshot is requested for all volumes in the Volume Group and deduplication is performed by default. If the configuration array contains specific volumes, a forced full snapshot is requested for these volumes only. The shouldDedupe flag determines whether deduplication is performed. If a forced full snapshot is requested, the next backup job checks the configuration and takes the full snapshot according to the specification. After the full snapshot is taken, the backup job clears the configuration to prevent additional full snapshots from being taken.

        :param volume_infos: The volume_infos of this VolumeGroupForceFullRequest.
        :type volume_infos: List[ForceFullVolumeInfo]
        """

        self._volume_infos = volume_infos
