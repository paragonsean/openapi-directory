/**
 * NaviPlan API
 * An API for accessing NaviPlan plan data for a client.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PublishedPlanModel from './PublishedPlanModel';

/**
 * The HouseholdExternalModel model module.
 * @module model/HouseholdExternalModel
 * @version v1
 */
class HouseholdExternalModel {
    /**
     * Constructs a new <code>HouseholdExternalModel</code>.
     * @alias module:model/HouseholdExternalModel
     */
    constructor() { 
        
        HouseholdExternalModel.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>HouseholdExternalModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HouseholdExternalModel} obj Optional instance to populate.
     * @return {module:model/HouseholdExternalModel} The populated <code>HouseholdExternalModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HouseholdExternalModel();

            if (data.hasOwnProperty('accessiblePlans')) {
                obj['accessiblePlans'] = ApiClient.convertToType(data['accessiblePlans'], [PublishedPlanModel]);
            }
            if (data.hasOwnProperty('clientDescription')) {
                obj['clientDescription'] = ApiClient.convertToType(data['clientDescription'], 'String');
            }
            if (data.hasOwnProperty('householdId')) {
                obj['householdId'] = ApiClient.convertToType(data['householdId'], 'Number');
            }
            if (data.hasOwnProperty('legacyClientId')) {
                obj['legacyClientId'] = ApiClient.convertToType(data['legacyClientId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HouseholdExternalModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HouseholdExternalModel</code>.
     */
    static validateJSON(data) {
        if (data['accessiblePlans']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['accessiblePlans'])) {
                throw new Error("Expected the field `accessiblePlans` to be an array in the JSON data but got " + data['accessiblePlans']);
            }
            // validate the optional field `accessiblePlans` (array)
            for (const item of data['accessiblePlans']) {
                PublishedPlanModel.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['clientDescription'] && !(typeof data['clientDescription'] === 'string' || data['clientDescription'] instanceof String)) {
            throw new Error("Expected the field `clientDescription` to be a primitive type in the JSON string but got " + data['clientDescription']);
        }
        // ensure the json data is a string
        if (data['legacyClientId'] && !(typeof data['legacyClientId'] === 'string' || data['legacyClientId'] instanceof String)) {
            throw new Error("Expected the field `legacyClientId` to be a primitive type in the JSON string but got " + data['legacyClientId']);
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/PublishedPlanModel>} accessiblePlans
 */
HouseholdExternalModel.prototype['accessiblePlans'] = undefined;

/**
 * @member {String} clientDescription
 */
HouseholdExternalModel.prototype['clientDescription'] = undefined;

/**
 * @member {Number} householdId
 */
HouseholdExternalModel.prototype['householdId'] = undefined;

/**
 * @member {String} legacyClientId
 */
HouseholdExternalModel.prototype['legacyClientId'] = undefined;






export default HouseholdExternalModel;

