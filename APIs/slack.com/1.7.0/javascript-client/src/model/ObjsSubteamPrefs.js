/**
 * Slack Web API
 * One way to interact with the Slack platform is its HTTP RPC-based Web API, a collection of methods requiring OAuth 2.0-based user, bot, or workspace tokens blessed with related OAuth scopes.
 *
 * The version of the OpenAPI document: 1.7.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ObjsSubteamPrefs model module.
 * @module model/ObjsSubteamPrefs
 * @version 1.7.0
 */
class ObjsSubteamPrefs {
    /**
     * Constructs a new <code>ObjsSubteamPrefs</code>.
     * @alias module:model/ObjsSubteamPrefs
     * @param channels {Array.<String>} 
     * @param groups {Array.<String>} 
     */
    constructor(channels, groups) { 
        
        ObjsSubteamPrefs.initialize(this, channels, groups);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channels, groups) { 
        obj['channels'] = channels;
        obj['groups'] = groups;
    }

    /**
     * Constructs a <code>ObjsSubteamPrefs</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ObjsSubteamPrefs} obj Optional instance to populate.
     * @return {module:model/ObjsSubteamPrefs} The populated <code>ObjsSubteamPrefs</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ObjsSubteamPrefs();

            if (data.hasOwnProperty('channels')) {
                obj['channels'] = ApiClient.convertToType(data['channels'], ['String']);
            }
            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ObjsSubteamPrefs</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ObjsSubteamPrefs</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ObjsSubteamPrefs.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['channels'])) {
            throw new Error("Expected the field `channels` to be an array in the JSON data but got " + data['channels']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['groups'])) {
            throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
        }

        return true;
    }


}

ObjsSubteamPrefs.RequiredProperties = ["channels", "groups"];

/**
 * @member {Array.<String>} channels
 */
ObjsSubteamPrefs.prototype['channels'] = undefined;

/**
 * @member {Array.<String>} groups
 */
ObjsSubteamPrefs.prototype['groups'] = undefined;






export default ObjsSubteamPrefs;

