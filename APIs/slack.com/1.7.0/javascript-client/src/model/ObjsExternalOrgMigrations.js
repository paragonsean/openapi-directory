/**
 * Slack Web API
 * One way to interact with the Slack platform is its HTTP RPC-based Web API, a collection of methods requiring OAuth 2.0-based user, bot, or workspace tokens blessed with related OAuth scopes.
 *
 * The version of the OpenAPI document: 1.7.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ObjsExternalOrgMigrationsCurrentInner from './ObjsExternalOrgMigrationsCurrentInner';

/**
 * The ObjsExternalOrgMigrations model module.
 * @module model/ObjsExternalOrgMigrations
 * @version 1.7.0
 */
class ObjsExternalOrgMigrations {
    /**
     * Constructs a new <code>ObjsExternalOrgMigrations</code>.
     * @alias module:model/ObjsExternalOrgMigrations
     * @param current {Array.<module:model/ObjsExternalOrgMigrationsCurrentInner>} 
     * @param dateUpdated {Number} 
     */
    constructor(current, dateUpdated) { 
        
        ObjsExternalOrgMigrations.initialize(this, current, dateUpdated);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, current, dateUpdated) { 
        obj['current'] = current;
        obj['date_updated'] = dateUpdated;
    }

    /**
     * Constructs a <code>ObjsExternalOrgMigrations</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ObjsExternalOrgMigrations} obj Optional instance to populate.
     * @return {module:model/ObjsExternalOrgMigrations} The populated <code>ObjsExternalOrgMigrations</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ObjsExternalOrgMigrations();

            if (data.hasOwnProperty('current')) {
                obj['current'] = ApiClient.convertToType(data['current'], [ObjsExternalOrgMigrationsCurrentInner]);
            }
            if (data.hasOwnProperty('date_updated')) {
                obj['date_updated'] = ApiClient.convertToType(data['date_updated'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ObjsExternalOrgMigrations</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ObjsExternalOrgMigrations</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ObjsExternalOrgMigrations.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['current']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['current'])) {
                throw new Error("Expected the field `current` to be an array in the JSON data but got " + data['current']);
            }
            // validate the optional field `current` (array)
            for (const item of data['current']) {
                ObjsExternalOrgMigrationsCurrentInner.validateJSON(item);
            };
        }

        return true;
    }


}

ObjsExternalOrgMigrations.RequiredProperties = ["current", "date_updated"];

/**
 * @member {Array.<module:model/ObjsExternalOrgMigrationsCurrentInner>} current
 */
ObjsExternalOrgMigrations.prototype['current'] = undefined;

/**
 * @member {Number} date_updated
 */
ObjsExternalOrgMigrations.prototype['date_updated'] = undefined;






export default ObjsExternalOrgMigrations;

