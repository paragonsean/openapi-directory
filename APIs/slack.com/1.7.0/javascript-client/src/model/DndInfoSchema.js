/**
 * Slack Web API
 * One way to interact with the Slack platform is its HTTP RPC-based Web API, a collection of methods requiring OAuth 2.0-based user, bot, or workspace tokens blessed with related OAuth scopes.
 *
 * The version of the OpenAPI document: 1.7.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DndInfoSchema model module.
 * @module model/DndInfoSchema
 * @version 1.7.0
 */
class DndInfoSchema {
    /**
     * Constructs a new <code>DndInfoSchema</code>.
     * Schema for successful response from dnd.info method
     * @alias module:model/DndInfoSchema
     * @param dndEnabled {Boolean} 
     * @param nextDndEndTs {Number} 
     * @param nextDndStartTs {Number} 
     * @param ok {Boolean} 
     */
    constructor(dndEnabled, nextDndEndTs, nextDndStartTs, ok) { 
        
        DndInfoSchema.initialize(this, dndEnabled, nextDndEndTs, nextDndStartTs, ok);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dndEnabled, nextDndEndTs, nextDndStartTs, ok) { 
        obj['dnd_enabled'] = dndEnabled;
        obj['next_dnd_end_ts'] = nextDndEndTs;
        obj['next_dnd_start_ts'] = nextDndStartTs;
        obj['ok'] = ok;
    }

    /**
     * Constructs a <code>DndInfoSchema</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DndInfoSchema} obj Optional instance to populate.
     * @return {module:model/DndInfoSchema} The populated <code>DndInfoSchema</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DndInfoSchema();

            if (data.hasOwnProperty('dnd_enabled')) {
                obj['dnd_enabled'] = ApiClient.convertToType(data['dnd_enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('next_dnd_end_ts')) {
                obj['next_dnd_end_ts'] = ApiClient.convertToType(data['next_dnd_end_ts'], 'Number');
            }
            if (data.hasOwnProperty('next_dnd_start_ts')) {
                obj['next_dnd_start_ts'] = ApiClient.convertToType(data['next_dnd_start_ts'], 'Number');
            }
            if (data.hasOwnProperty('ok')) {
                obj['ok'] = ApiClient.convertToType(data['ok'], 'Boolean');
            }
            if (data.hasOwnProperty('snooze_enabled')) {
                obj['snooze_enabled'] = ApiClient.convertToType(data['snooze_enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('snooze_endtime')) {
                obj['snooze_endtime'] = ApiClient.convertToType(data['snooze_endtime'], 'Number');
            }
            if (data.hasOwnProperty('snooze_remaining')) {
                obj['snooze_remaining'] = ApiClient.convertToType(data['snooze_remaining'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DndInfoSchema</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DndInfoSchema</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DndInfoSchema.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

DndInfoSchema.RequiredProperties = ["dnd_enabled", "next_dnd_end_ts", "next_dnd_start_ts", "ok"];

/**
 * @member {Boolean} dnd_enabled
 */
DndInfoSchema.prototype['dnd_enabled'] = undefined;

/**
 * @member {Number} next_dnd_end_ts
 */
DndInfoSchema.prototype['next_dnd_end_ts'] = undefined;

/**
 * @member {Number} next_dnd_start_ts
 */
DndInfoSchema.prototype['next_dnd_start_ts'] = undefined;

/**
 * @member {Boolean} ok
 */
DndInfoSchema.prototype['ok'] = undefined;

/**
 * @member {Boolean} snooze_enabled
 */
DndInfoSchema.prototype['snooze_enabled'] = undefined;

/**
 * @member {Number} snooze_endtime
 */
DndInfoSchema.prototype['snooze_endtime'] = undefined;

/**
 * @member {Number} snooze_remaining
 */
DndInfoSchema.prototype['snooze_remaining'] = undefined;






export default DndInfoSchema;

