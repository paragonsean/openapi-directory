/**
 * Slack Web API
 * One way to interact with the Slack platform is its HTTP RPC-based Web API, a collection of methods requiring OAuth 2.0-based user, bot, or workspace tokens blessed with related OAuth scopes.
 *
 * The version of the OpenAPI document: 1.7.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ObjsPaging from './ObjsPaging';
import ObjsResponseMetadataInner from './ObjsResponseMetadataInner';
import ReactionsListSchemaItemsInnerInner from './ReactionsListSchemaItemsInnerInner';

/**
 * The ReactionsListSchema model module.
 * @module model/ReactionsListSchema
 * @version 1.7.0
 */
class ReactionsListSchema {
    /**
     * Constructs a new <code>ReactionsListSchema</code>.
     * Schema for successful response from reactions.list method
     * @alias module:model/ReactionsListSchema
     * @param items {Array.<Array.<module:model/ReactionsListSchemaItemsInnerInner>>} 
     * @param ok {Boolean} 
     */
    constructor(items, ok) { 
        
        ReactionsListSchema.initialize(this, items, ok);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, items, ok) { 
        obj['items'] = items;
        obj['ok'] = ok;
    }

    /**
     * Constructs a <code>ReactionsListSchema</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReactionsListSchema} obj Optional instance to populate.
     * @return {module:model/ReactionsListSchema} The populated <code>ReactionsListSchema</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReactionsListSchema();

            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [[ReactionsListSchemaItemsInnerInner]]);
            }
            if (data.hasOwnProperty('ok')) {
                obj['ok'] = ApiClient.convertToType(data['ok'], 'Boolean');
            }
            if (data.hasOwnProperty('paging')) {
                obj['paging'] = ObjsPaging.constructFromObject(data['paging']);
            }
            if (data.hasOwnProperty('response_metadata')) {
                obj['response_metadata'] = ApiClient.convertToType(data['response_metadata'], [ObjsResponseMetadataInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReactionsListSchema</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReactionsListSchema</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReactionsListSchema.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['items'])) {
            throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
        }
        // validate the optional field `paging`
        if (data['paging']) { // data not null
          ObjsPaging.validateJSON(data['paging']);
        }
        if (data['response_metadata']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['response_metadata'])) {
                throw new Error("Expected the field `response_metadata` to be an array in the JSON data but got " + data['response_metadata']);
            }
            // validate the optional field `response_metadata` (array)
            for (const item of data['response_metadata']) {
                ObjsResponseMetadataInner.validateJSON(item);
            };
        }

        return true;
    }


}

ReactionsListSchema.RequiredProperties = ["items", "ok"];

/**
 * @member {Array.<Array.<module:model/ReactionsListSchemaItemsInnerInner>>} items
 */
ReactionsListSchema.prototype['items'] = undefined;

/**
 * @member {Boolean} ok
 */
ReactionsListSchema.prototype['ok'] = undefined;

/**
 * @member {module:model/ObjsPaging} paging
 */
ReactionsListSchema.prototype['paging'] = undefined;

/**
 * @member {Array.<module:model/ObjsResponseMetadataInner>} response_metadata
 */
ReactionsListSchema.prototype['response_metadata'] = undefined;






export default ReactionsListSchema;

