/**
 * Slack Web API
 * One way to interact with the Slack platform is its HTTP RPC-based Web API, a collection of methods requiring OAuth 2.0-based user, bot, or workspace tokens blessed with related OAuth scopes.
 *
 * The version of the OpenAPI document: 1.7.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ObjsConversationInner from './ObjsConversationInner';

/**
 * The ConversationsInfoSuccessSchema model module.
 * @module model/ConversationsInfoSuccessSchema
 * @version 1.7.0
 */
class ConversationsInfoSuccessSchema {
    /**
     * Constructs a new <code>ConversationsInfoSuccessSchema</code>.
     * Schema for successful response conversations.info
     * @alias module:model/ConversationsInfoSuccessSchema
     * @param channel {Array.<module:model/ObjsConversationInner>} 
     * @param ok {Boolean} 
     */
    constructor(channel, ok) { 
        
        ConversationsInfoSuccessSchema.initialize(this, channel, ok);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channel, ok) { 
        obj['channel'] = channel;
        obj['ok'] = ok;
    }

    /**
     * Constructs a <code>ConversationsInfoSuccessSchema</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ConversationsInfoSuccessSchema} obj Optional instance to populate.
     * @return {module:model/ConversationsInfoSuccessSchema} The populated <code>ConversationsInfoSuccessSchema</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ConversationsInfoSuccessSchema();

            if (data.hasOwnProperty('channel')) {
                obj['channel'] = ApiClient.convertToType(data['channel'], [ObjsConversationInner]);
            }
            if (data.hasOwnProperty('ok')) {
                obj['ok'] = ApiClient.convertToType(data['ok'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ConversationsInfoSuccessSchema</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ConversationsInfoSuccessSchema</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ConversationsInfoSuccessSchema.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['channel']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['channel'])) {
                throw new Error("Expected the field `channel` to be an array in the JSON data but got " + data['channel']);
            }
            // validate the optional field `channel` (array)
            for (const item of data['channel']) {
                ObjsConversationInner.validateJSON(item);
            };
        }

        return true;
    }


}

ConversationsInfoSuccessSchema.RequiredProperties = ["channel", "ok"];

/**
 * @member {Array.<module:model/ObjsConversationInner>} channel
 */
ConversationsInfoSuccessSchema.prototype['channel'] = undefined;

/**
 * @member {Boolean} ok
 */
ConversationsInfoSuccessSchema.prototype['ok'] = undefined;






export default ConversationsInfoSuccessSchema;

