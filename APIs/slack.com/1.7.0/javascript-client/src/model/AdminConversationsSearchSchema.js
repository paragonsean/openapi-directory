/**
 * Slack Web API
 * One way to interact with the Slack platform is its HTTP RPC-based Web API, a collection of methods requiring OAuth 2.0-based user, bot, or workspace tokens blessed with related OAuth scopes.
 *
 * The version of the OpenAPI document: 1.7.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ObjsChannel from './ObjsChannel';

/**
 * The AdminConversationsSearchSchema model module.
 * @module model/AdminConversationsSearchSchema
 * @version 1.7.0
 */
class AdminConversationsSearchSchema {
    /**
     * Constructs a new <code>AdminConversationsSearchSchema</code>.
     * Schema for successful response of admin.conversations.search
     * @alias module:model/AdminConversationsSearchSchema
     * @param channels {Array.<module:model/ObjsChannel>} 
     * @param nextCursor {String} 
     */
    constructor(channels, nextCursor) { 
        
        AdminConversationsSearchSchema.initialize(this, channels, nextCursor);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channels, nextCursor) { 
        obj['channels'] = channels;
        obj['next_cursor'] = nextCursor;
    }

    /**
     * Constructs a <code>AdminConversationsSearchSchema</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AdminConversationsSearchSchema} obj Optional instance to populate.
     * @return {module:model/AdminConversationsSearchSchema} The populated <code>AdminConversationsSearchSchema</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AdminConversationsSearchSchema();

            if (data.hasOwnProperty('channels')) {
                obj['channels'] = ApiClient.convertToType(data['channels'], [ObjsChannel]);
            }
            if (data.hasOwnProperty('next_cursor')) {
                obj['next_cursor'] = ApiClient.convertToType(data['next_cursor'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AdminConversationsSearchSchema</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AdminConversationsSearchSchema</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AdminConversationsSearchSchema.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['channels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['channels'])) {
                throw new Error("Expected the field `channels` to be an array in the JSON data but got " + data['channels']);
            }
            // validate the optional field `channels` (array)
            for (const item of data['channels']) {
                ObjsChannel.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['next_cursor'] && !(typeof data['next_cursor'] === 'string' || data['next_cursor'] instanceof String)) {
            throw new Error("Expected the field `next_cursor` to be a primitive type in the JSON string but got " + data['next_cursor']);
        }

        return true;
    }


}

AdminConversationsSearchSchema.RequiredProperties = ["channels", "next_cursor"];

/**
 * @member {Array.<module:model/ObjsChannel>} channels
 */
AdminConversationsSearchSchema.prototype['channels'] = undefined;

/**
 * @member {String} next_cursor
 */
AdminConversationsSearchSchema.prototype['next_cursor'] = undefined;






export default AdminConversationsSearchSchema;

