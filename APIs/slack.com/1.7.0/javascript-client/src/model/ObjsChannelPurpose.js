/**
 * Slack Web API
 * One way to interact with the Slack platform is its HTTP RPC-based Web API, a collection of methods requiring OAuth 2.0-based user, bot, or workspace tokens blessed with related OAuth scopes.
 *
 * The version of the OpenAPI document: 1.7.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ObjsChannelPurpose model module.
 * @module model/ObjsChannelPurpose
 * @version 1.7.0
 */
class ObjsChannelPurpose {
    /**
     * Constructs a new <code>ObjsChannelPurpose</code>.
     * @alias module:model/ObjsChannelPurpose
     * @param creator {String} 
     * @param lastSet {Number} 
     * @param value {String} 
     */
    constructor(creator, lastSet, value) { 
        
        ObjsChannelPurpose.initialize(this, creator, lastSet, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, creator, lastSet, value) { 
        obj['creator'] = creator;
        obj['last_set'] = lastSet;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>ObjsChannelPurpose</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ObjsChannelPurpose} obj Optional instance to populate.
     * @return {module:model/ObjsChannelPurpose} The populated <code>ObjsChannelPurpose</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ObjsChannelPurpose();

            if (data.hasOwnProperty('creator')) {
                obj['creator'] = ApiClient.convertToType(data['creator'], 'String');
            }
            if (data.hasOwnProperty('last_set')) {
                obj['last_set'] = ApiClient.convertToType(data['last_set'], 'Number');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ObjsChannelPurpose</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ObjsChannelPurpose</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ObjsChannelPurpose.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['creator'] && !(typeof data['creator'] === 'string' || data['creator'] instanceof String)) {
            throw new Error("Expected the field `creator` to be a primitive type in the JSON string but got " + data['creator']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

ObjsChannelPurpose.RequiredProperties = ["creator", "last_set", "value"];

/**
 * @member {String} creator
 */
ObjsChannelPurpose.prototype['creator'] = undefined;

/**
 * @member {Number} last_set
 */
ObjsChannelPurpose.prototype['last_set'] = undefined;

/**
 * @member {String} value
 */
ObjsChannelPurpose.prototype['value'] = undefined;






export default ObjsChannelPurpose;

