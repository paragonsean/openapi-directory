/**
 * Prime ReportStream
 * A router of public health data from multiple senders and receivers
 *
 * The version of the OpenAPI document: 0.2.0-oas3
 * Contact: reportstream@cdc.gov
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AS2Transport from './AS2Transport';
import BlobStoreTransport from './BlobStoreTransport';
import NullTransport from './NullTransport';
import SFTPTransport from './SFTPTransport';

/**
 * The StandardHL7ConfigurationTransport model module.
 * @module model/StandardHL7ConfigurationTransport
 * @version 0.2.0-oas3
 */
class StandardHL7ConfigurationTransport {
    /**
     * Constructs a new <code>StandardHL7ConfigurationTransport</code>.
     * @alias module:model/StandardHL7ConfigurationTransport
     * @param {(module:model/AS2Transport|module:model/BlobStoreTransport|module:model/NullTransport|module:model/SFTPTransport)} instance The actual instance to initialize StandardHL7ConfigurationTransport.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            if (typeof instance === "SFTPTransport") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                SFTPTransport.validateJSON(instance); // throw an exception if no match
                // create SFTPTransport from JS object
                this.actualInstance = SFTPTransport.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into SFTPTransport
            errorMessages.push("Failed to construct SFTPTransport: " + err)
        }

        try {
            if (typeof instance === "AS2Transport") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                AS2Transport.validateJSON(instance); // throw an exception if no match
                // create AS2Transport from JS object
                this.actualInstance = AS2Transport.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into AS2Transport
            errorMessages.push("Failed to construct AS2Transport: " + err)
        }

        try {
            if (typeof instance === "BlobStoreTransport") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                BlobStoreTransport.validateJSON(instance); // throw an exception if no match
                // create BlobStoreTransport from JS object
                this.actualInstance = BlobStoreTransport.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into BlobStoreTransport
            errorMessages.push("Failed to construct BlobStoreTransport: " + err)
        }

        try {
            if (typeof instance === "NullTransport") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                NullTransport.validateJSON(instance); // throw an exception if no match
                // create NullTransport from JS object
                this.actualInstance = NullTransport.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into NullTransport
            errorMessages.push("Failed to construct NullTransport: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `StandardHL7ConfigurationTransport` with oneOf schemas AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `StandardHL7ConfigurationTransport` with oneOf schemas AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>StandardHL7ConfigurationTransport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StandardHL7ConfigurationTransport} obj Optional instance to populate.
     * @return {module:model/StandardHL7ConfigurationTransport} The populated <code>StandardHL7ConfigurationTransport</code> instance.
     */
    static constructFromObject(data, obj) {
        return new StandardHL7ConfigurationTransport(data);
    }

    /**
     * Gets the actual instance, which can be <code>AS2Transport</code>, <code>BlobStoreTransport</code>, <code>NullTransport</code>, <code>SFTPTransport</code>.
     * @return {(module:model/AS2Transport|module:model/BlobStoreTransport|module:model/NullTransport|module:model/SFTPTransport)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>AS2Transport</code>, <code>BlobStoreTransport</code>, <code>NullTransport</code>, <code>SFTPTransport</code>.
     * @param {(module:model/AS2Transport|module:model/BlobStoreTransport|module:model/NullTransport|module:model/SFTPTransport)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = StandardHL7ConfigurationTransport.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of StandardHL7ConfigurationTransport from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:model/StandardHL7ConfigurationTransport} An instance of StandardHL7ConfigurationTransport.
     */
    static fromJSON = function(json_string){
        return StandardHL7ConfigurationTransport.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * The file path to place the report
 * @member {String} filePath
 */
StandardHL7ConfigurationTransport.prototype['filePath'] = undefined;

/**
 * Host name and domain
 * @member {String} host
 */
StandardHL7ConfigurationTransport.prototype['host'] = undefined;

/**
 * The port to use
 * @member {Number} port
 */
StandardHL7ConfigurationTransport.prototype['port'] = undefined;

/**
 * The discriminator
 * @member {String} type
 */
StandardHL7ConfigurationTransport.prototype['type'] = undefined;

/**
 * A description of the content of the message. Usually, the same for all messages.
 * @member {String} contentDescription
 * @default 'SARS-CoV-2 Electronic Lab Results'
 */
StandardHL7ConfigurationTransport.prototype['contentDescription'] = 'SARS-CoV-2 Electronic Lab Results';

/**
 * The MIME type of the message
 * @member {String} mimeType
 * @default 'application/hl7-v2'
 */
StandardHL7ConfigurationTransport.prototype['mimeType'] = 'application/hl7-v2';

/**
 * The AS2 id of the receiver. Usually, the same for all senders.
 * @member {String} receiverId
 */
StandardHL7ConfigurationTransport.prototype['receiverId'] = undefined;

/**
 * The URL to the AS2 end-point
 * @member {String} receiverUrl
 */
StandardHL7ConfigurationTransport.prototype['receiverUrl'] = undefined;

/**
 * The email address to contact someone about the message
 * @member {String} senderEmail
 * @default 'reportstream@cdc.gov'
 */
StandardHL7ConfigurationTransport.prototype['senderEmail'] = 'reportstream@cdc.gov';

/**
 * The AS2 id of the sender. Usually, assigned by receiver to PRIME.
 * @member {String} senderId
 */
StandardHL7ConfigurationTransport.prototype['senderId'] = undefined;

/**
 * This name of the Azure container
 * @member {String} containerName
 */
StandardHL7ConfigurationTransport.prototype['containerName'] = undefined;

/**
 * This looks for an env var with this name. env var value is the connection string
 * @member {String} storageName
 */
StandardHL7ConfigurationTransport.prototype['storageName'] = undefined;

/**
 * A dummy value
 * @member {String} dummy
 */
StandardHL7ConfigurationTransport.prototype['dummy'] = undefined;


StandardHL7ConfigurationTransport.OneOf = ["AS2Transport", "BlobStoreTransport", "NullTransport", "SFTPTransport"];

export default StandardHL7ConfigurationTransport;

