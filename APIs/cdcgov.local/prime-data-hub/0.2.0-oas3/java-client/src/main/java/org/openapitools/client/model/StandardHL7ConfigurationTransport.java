/*
 * Prime ReportStream
 * A router of public health data from multiple senders and receivers
 *
 * The version of the OpenAPI document: 0.2.0-oas3
 * Contact: reportstream@cdc.gov
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import org.openapitools.client.model.AS2Transport;
import org.openapitools.client.model.BlobStoreTransport;
import org.openapitools.client.model.NullTransport;
import org.openapitools.client.model.SFTPTransport;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T12:40:32.539681-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class StandardHL7ConfigurationTransport extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(StandardHL7ConfigurationTransport.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!StandardHL7ConfigurationTransport.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'StandardHL7ConfigurationTransport' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SFTPTransport> adapterSFTPTransport = gson.getDelegateAdapter(this, TypeToken.get(SFTPTransport.class));
            final TypeAdapter<AS2Transport> adapterAS2Transport = gson.getDelegateAdapter(this, TypeToken.get(AS2Transport.class));
            final TypeAdapter<BlobStoreTransport> adapterBlobStoreTransport = gson.getDelegateAdapter(this, TypeToken.get(BlobStoreTransport.class));
            final TypeAdapter<NullTransport> adapterNullTransport = gson.getDelegateAdapter(this, TypeToken.get(NullTransport.class));

            return (TypeAdapter<T>) new TypeAdapter<StandardHL7ConfigurationTransport>() {
                @Override
                public void write(JsonWriter out, StandardHL7ConfigurationTransport value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `SFTPTransport`
                    if (value.getActualInstance() instanceof SFTPTransport) {
                        JsonElement element = adapterSFTPTransport.toJsonTree((SFTPTransport)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `AS2Transport`
                    if (value.getActualInstance() instanceof AS2Transport) {
                        JsonElement element = adapterAS2Transport.toJsonTree((AS2Transport)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `BlobStoreTransport`
                    if (value.getActualInstance() instanceof BlobStoreTransport) {
                        JsonElement element = adapterBlobStoreTransport.toJsonTree((BlobStoreTransport)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `NullTransport`
                    if (value.getActualInstance() instanceof NullTransport) {
                        JsonElement element = adapterNullTransport.toJsonTree((NullTransport)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport");
                }

                @Override
                public StandardHL7ConfigurationTransport read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize SFTPTransport
                    try {
                        // validate the JSON object to see if any exception is thrown
                        SFTPTransport.validateJsonElement(jsonElement);
                        actualAdapter = adapterSFTPTransport;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'SFTPTransport'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for SFTPTransport failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'SFTPTransport'", e);
                    }
                    // deserialize AS2Transport
                    try {
                        // validate the JSON object to see if any exception is thrown
                        AS2Transport.validateJsonElement(jsonElement);
                        actualAdapter = adapterAS2Transport;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'AS2Transport'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for AS2Transport failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'AS2Transport'", e);
                    }
                    // deserialize BlobStoreTransport
                    try {
                        // validate the JSON object to see if any exception is thrown
                        BlobStoreTransport.validateJsonElement(jsonElement);
                        actualAdapter = adapterBlobStoreTransport;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'BlobStoreTransport'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for BlobStoreTransport failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'BlobStoreTransport'", e);
                    }
                    // deserialize NullTransport
                    try {
                        // validate the JSON object to see if any exception is thrown
                        NullTransport.validateJsonElement(jsonElement);
                        actualAdapter = adapterNullTransport;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'NullTransport'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for NullTransport failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'NullTransport'", e);
                    }

                    if (match == 1) {
                        StandardHL7ConfigurationTransport ret = new StandardHL7ConfigurationTransport();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for StandardHL7ConfigurationTransport: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public StandardHL7ConfigurationTransport() {
        super("oneOf", Boolean.FALSE);
    }

    public StandardHL7ConfigurationTransport(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("SFTPTransport", SFTPTransport.class);
        schemas.put("AS2Transport", AS2Transport.class);
        schemas.put("BlobStoreTransport", BlobStoreTransport.class);
        schemas.put("NullTransport", NullTransport.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return StandardHL7ConfigurationTransport.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof SFTPTransport) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof AS2Transport) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof BlobStoreTransport) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof NullTransport) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport");
    }

    /**
     * Get the actual instance, which can be the following:
     * AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport
     *
     * @return The actual instance (AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `SFTPTransport`. If the actual instance is not `SFTPTransport`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SFTPTransport`
     * @throws ClassCastException if the instance is not `SFTPTransport`
     */
    public SFTPTransport getSFTPTransport() throws ClassCastException {
        return (SFTPTransport)super.getActualInstance();
    }
    /**
     * Get the actual instance of `AS2Transport`. If the actual instance is not `AS2Transport`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `AS2Transport`
     * @throws ClassCastException if the instance is not `AS2Transport`
     */
    public AS2Transport getAS2Transport() throws ClassCastException {
        return (AS2Transport)super.getActualInstance();
    }
    /**
     * Get the actual instance of `BlobStoreTransport`. If the actual instance is not `BlobStoreTransport`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `BlobStoreTransport`
     * @throws ClassCastException if the instance is not `BlobStoreTransport`
     */
    public BlobStoreTransport getBlobStoreTransport() throws ClassCastException {
        return (BlobStoreTransport)super.getActualInstance();
    }
    /**
     * Get the actual instance of `NullTransport`. If the actual instance is not `NullTransport`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `NullTransport`
     * @throws ClassCastException if the instance is not `NullTransport`
     */
    public NullTransport getNullTransport() throws ClassCastException {
        return (NullTransport)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to StandardHL7ConfigurationTransport
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with SFTPTransport
        try {
            SFTPTransport.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for SFTPTransport failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with AS2Transport
        try {
            AS2Transport.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for AS2Transport failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with BlobStoreTransport
        try {
            BlobStoreTransport.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for BlobStoreTransport failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with NullTransport
        try {
            NullTransport.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for NullTransport failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for StandardHL7ConfigurationTransport with oneOf schemas: AS2Transport, BlobStoreTransport, NullTransport, SFTPTransport. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of StandardHL7ConfigurationTransport given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of StandardHL7ConfigurationTransport
     * @throws IOException if the JSON string is invalid with respect to StandardHL7ConfigurationTransport
     */
    public static StandardHL7ConfigurationTransport fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, StandardHL7ConfigurationTransport.class);
    }

    /**
     * Convert an instance of StandardHL7ConfigurationTransport to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

