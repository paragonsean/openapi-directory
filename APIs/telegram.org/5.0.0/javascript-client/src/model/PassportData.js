/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EncryptedCredentials from './EncryptedCredentials';
import EncryptedPassportElement from './EncryptedPassportElement';

/**
 * The PassportData model module.
 * @module model/PassportData
 * @version 5.0.0
 */
class PassportData {
    /**
     * Constructs a new <code>PassportData</code>.
     * Contains information about Telegram Passport data shared with the bot by the user.
     * @alias module:model/PassportData
     * @param credentials {module:model/EncryptedCredentials} 
     * @param data {Array.<module:model/EncryptedPassportElement>} Array with information about documents and other Telegram Passport elements that was shared with the bot
     */
    constructor(credentials, data) { 
        
        PassportData.initialize(this, credentials, data);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, credentials, data) { 
        obj['credentials'] = credentials;
        obj['data'] = data;
    }

    /**
     * Constructs a <code>PassportData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PassportData} obj Optional instance to populate.
     * @return {module:model/PassportData} The populated <code>PassportData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PassportData();

            if (data.hasOwnProperty('credentials')) {
                obj['credentials'] = EncryptedCredentials.constructFromObject(data['credentials']);
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [EncryptedPassportElement]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PassportData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PassportData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PassportData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `credentials`
        if (data['credentials']) { // data not null
          EncryptedCredentials.validateJSON(data['credentials']);
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                EncryptedPassportElement.validateJSON(item);
            };
        }

        return true;
    }


}

PassportData.RequiredProperties = ["credentials", "data"];

/**
 * @member {module:model/EncryptedCredentials} credentials
 */
PassportData.prototype['credentials'] = undefined;

/**
 * Array with information about documents and other Telegram Passport elements that was shared with the bot
 * @member {Array.<module:model/EncryptedPassportElement>} data
 */
PassportData.prototype['data'] = undefined;






export default PassportData;

