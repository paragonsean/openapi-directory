/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PhotoSize model module.
 * @module model/PhotoSize
 * @version 5.0.0
 */
class PhotoSize {
    /**
     * Constructs a new <code>PhotoSize</code>.
     * This object represents one size of a photo or a [file](https://core.telegram.org/bots/api/#document) / [sticker](https://core.telegram.org/bots/api/#sticker) thumbnail.
     * @alias module:model/PhotoSize
     * @param fileId {String} Identifier for this file, which can be used to download or reuse the file
     * @param fileUniqueId {String} Unique identifier for this file, which is supposed to be the same over time and for different bots. Can't be used to download or reuse the file.
     * @param height {Number} Photo height
     * @param width {Number} Photo width
     */
    constructor(fileId, fileUniqueId, height, width) { 
        
        PhotoSize.initialize(this, fileId, fileUniqueId, height, width);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fileId, fileUniqueId, height, width) { 
        obj['file_id'] = fileId;
        obj['file_unique_id'] = fileUniqueId;
        obj['height'] = height;
        obj['width'] = width;
    }

    /**
     * Constructs a <code>PhotoSize</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PhotoSize} obj Optional instance to populate.
     * @return {module:model/PhotoSize} The populated <code>PhotoSize</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PhotoSize();

            if (data.hasOwnProperty('file_id')) {
                obj['file_id'] = ApiClient.convertToType(data['file_id'], 'String');
            }
            if (data.hasOwnProperty('file_size')) {
                obj['file_size'] = ApiClient.convertToType(data['file_size'], 'Number');
            }
            if (data.hasOwnProperty('file_unique_id')) {
                obj['file_unique_id'] = ApiClient.convertToType(data['file_unique_id'], 'String');
            }
            if (data.hasOwnProperty('height')) {
                obj['height'] = ApiClient.convertToType(data['height'], 'Number');
            }
            if (data.hasOwnProperty('width')) {
                obj['width'] = ApiClient.convertToType(data['width'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PhotoSize</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PhotoSize</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PhotoSize.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['file_id'] && !(typeof data['file_id'] === 'string' || data['file_id'] instanceof String)) {
            throw new Error("Expected the field `file_id` to be a primitive type in the JSON string but got " + data['file_id']);
        }
        // ensure the json data is a string
        if (data['file_unique_id'] && !(typeof data['file_unique_id'] === 'string' || data['file_unique_id'] instanceof String)) {
            throw new Error("Expected the field `file_unique_id` to be a primitive type in the JSON string but got " + data['file_unique_id']);
        }

        return true;
    }


}

PhotoSize.RequiredProperties = ["file_id", "file_unique_id", "height", "width"];

/**
 * Identifier for this file, which can be used to download or reuse the file
 * @member {String} file_id
 */
PhotoSize.prototype['file_id'] = undefined;

/**
 * *Optional*. File size
 * @member {Number} file_size
 */
PhotoSize.prototype['file_size'] = undefined;

/**
 * Unique identifier for this file, which is supposed to be the same over time and for different bots. Can't be used to download or reuse the file.
 * @member {String} file_unique_id
 */
PhotoSize.prototype['file_unique_id'] = undefined;

/**
 * Photo height
 * @member {Number} height
 */
PhotoSize.prototype['height'] = undefined;

/**
 * Photo width
 * @member {Number} width
 */
PhotoSize.prototype['width'] = undefined;






export default PhotoSize;

