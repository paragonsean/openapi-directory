/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MaskPosition from './MaskPosition';
import PhotoSize from './PhotoSize';

/**
 * The Sticker model module.
 * @module model/Sticker
 * @version 5.0.0
 */
class Sticker {
    /**
     * Constructs a new <code>Sticker</code>.
     * This object represents a sticker.
     * @alias module:model/Sticker
     * @param fileId {String} Identifier for this file, which can be used to download or reuse the file
     * @param fileUniqueId {String} Unique identifier for this file, which is supposed to be the same over time and for different bots. Can't be used to download or reuse the file.
     * @param height {Number} Sticker height
     * @param isAnimated {Boolean} *True*, if the sticker is [animated](https://telegram.org/blog/animated-stickers)
     * @param width {Number} Sticker width
     */
    constructor(fileId, fileUniqueId, height, isAnimated, width) { 
        
        Sticker.initialize(this, fileId, fileUniqueId, height, isAnimated, width);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fileId, fileUniqueId, height, isAnimated, width) { 
        obj['file_id'] = fileId;
        obj['file_unique_id'] = fileUniqueId;
        obj['height'] = height;
        obj['is_animated'] = isAnimated;
        obj['width'] = width;
    }

    /**
     * Constructs a <code>Sticker</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Sticker} obj Optional instance to populate.
     * @return {module:model/Sticker} The populated <code>Sticker</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Sticker();

            if (data.hasOwnProperty('emoji')) {
                obj['emoji'] = ApiClient.convertToType(data['emoji'], 'String');
            }
            if (data.hasOwnProperty('file_id')) {
                obj['file_id'] = ApiClient.convertToType(data['file_id'], 'String');
            }
            if (data.hasOwnProperty('file_size')) {
                obj['file_size'] = ApiClient.convertToType(data['file_size'], 'Number');
            }
            if (data.hasOwnProperty('file_unique_id')) {
                obj['file_unique_id'] = ApiClient.convertToType(data['file_unique_id'], 'String');
            }
            if (data.hasOwnProperty('height')) {
                obj['height'] = ApiClient.convertToType(data['height'], 'Number');
            }
            if (data.hasOwnProperty('is_animated')) {
                obj['is_animated'] = ApiClient.convertToType(data['is_animated'], 'Boolean');
            }
            if (data.hasOwnProperty('mask_position')) {
                obj['mask_position'] = MaskPosition.constructFromObject(data['mask_position']);
            }
            if (data.hasOwnProperty('set_name')) {
                obj['set_name'] = ApiClient.convertToType(data['set_name'], 'String');
            }
            if (data.hasOwnProperty('thumb')) {
                obj['thumb'] = PhotoSize.constructFromObject(data['thumb']);
            }
            if (data.hasOwnProperty('width')) {
                obj['width'] = ApiClient.convertToType(data['width'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Sticker</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Sticker</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Sticker.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['emoji'] && !(typeof data['emoji'] === 'string' || data['emoji'] instanceof String)) {
            throw new Error("Expected the field `emoji` to be a primitive type in the JSON string but got " + data['emoji']);
        }
        // ensure the json data is a string
        if (data['file_id'] && !(typeof data['file_id'] === 'string' || data['file_id'] instanceof String)) {
            throw new Error("Expected the field `file_id` to be a primitive type in the JSON string but got " + data['file_id']);
        }
        // ensure the json data is a string
        if (data['file_unique_id'] && !(typeof data['file_unique_id'] === 'string' || data['file_unique_id'] instanceof String)) {
            throw new Error("Expected the field `file_unique_id` to be a primitive type in the JSON string but got " + data['file_unique_id']);
        }
        // validate the optional field `mask_position`
        if (data['mask_position']) { // data not null
          MaskPosition.validateJSON(data['mask_position']);
        }
        // ensure the json data is a string
        if (data['set_name'] && !(typeof data['set_name'] === 'string' || data['set_name'] instanceof String)) {
            throw new Error("Expected the field `set_name` to be a primitive type in the JSON string but got " + data['set_name']);
        }
        // validate the optional field `thumb`
        if (data['thumb']) { // data not null
          PhotoSize.validateJSON(data['thumb']);
        }

        return true;
    }


}

Sticker.RequiredProperties = ["file_id", "file_unique_id", "height", "is_animated", "width"];

/**
 * *Optional*. Emoji associated with the sticker
 * @member {String} emoji
 */
Sticker.prototype['emoji'] = undefined;

/**
 * Identifier for this file, which can be used to download or reuse the file
 * @member {String} file_id
 */
Sticker.prototype['file_id'] = undefined;

/**
 * *Optional*. File size
 * @member {Number} file_size
 */
Sticker.prototype['file_size'] = undefined;

/**
 * Unique identifier for this file, which is supposed to be the same over time and for different bots. Can't be used to download or reuse the file.
 * @member {String} file_unique_id
 */
Sticker.prototype['file_unique_id'] = undefined;

/**
 * Sticker height
 * @member {Number} height
 */
Sticker.prototype['height'] = undefined;

/**
 * *True*, if the sticker is [animated](https://telegram.org/blog/animated-stickers)
 * @member {Boolean} is_animated
 */
Sticker.prototype['is_animated'] = undefined;

/**
 * @member {module:model/MaskPosition} mask_position
 */
Sticker.prototype['mask_position'] = undefined;

/**
 * *Optional*. Name of the sticker set to which the sticker belongs
 * @member {String} set_name
 */
Sticker.prototype['set_name'] = undefined;

/**
 * @member {module:model/PhotoSize} thumb
 */
Sticker.prototype['thumb'] = undefined;

/**
 * Sticker width
 * @member {Number} width
 */
Sticker.prototype['width'] = undefined;






export default Sticker;

