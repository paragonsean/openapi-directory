/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PassportFile model module.
 * @module model/PassportFile
 * @version 5.0.0
 */
class PassportFile {
    /**
     * Constructs a new <code>PassportFile</code>.
     * This object represents a file uploaded to Telegram Passport. Currently all Telegram Passport files are in JPEG format when decrypted and don&#39;t exceed 10MB.
     * @alias module:model/PassportFile
     * @param fileDate {Number} Unix time when the file was uploaded
     * @param fileId {String} Identifier for this file, which can be used to download or reuse the file
     * @param fileSize {Number} File size
     * @param fileUniqueId {String} Unique identifier for this file, which is supposed to be the same over time and for different bots. Can't be used to download or reuse the file.
     */
    constructor(fileDate, fileId, fileSize, fileUniqueId) { 
        
        PassportFile.initialize(this, fileDate, fileId, fileSize, fileUniqueId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fileDate, fileId, fileSize, fileUniqueId) { 
        obj['file_date'] = fileDate;
        obj['file_id'] = fileId;
        obj['file_size'] = fileSize;
        obj['file_unique_id'] = fileUniqueId;
    }

    /**
     * Constructs a <code>PassportFile</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PassportFile} obj Optional instance to populate.
     * @return {module:model/PassportFile} The populated <code>PassportFile</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PassportFile();

            if (data.hasOwnProperty('file_date')) {
                obj['file_date'] = ApiClient.convertToType(data['file_date'], 'Number');
            }
            if (data.hasOwnProperty('file_id')) {
                obj['file_id'] = ApiClient.convertToType(data['file_id'], 'String');
            }
            if (data.hasOwnProperty('file_size')) {
                obj['file_size'] = ApiClient.convertToType(data['file_size'], 'Number');
            }
            if (data.hasOwnProperty('file_unique_id')) {
                obj['file_unique_id'] = ApiClient.convertToType(data['file_unique_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PassportFile</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PassportFile</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PassportFile.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['file_id'] && !(typeof data['file_id'] === 'string' || data['file_id'] instanceof String)) {
            throw new Error("Expected the field `file_id` to be a primitive type in the JSON string but got " + data['file_id']);
        }
        // ensure the json data is a string
        if (data['file_unique_id'] && !(typeof data['file_unique_id'] === 'string' || data['file_unique_id'] instanceof String)) {
            throw new Error("Expected the field `file_unique_id` to be a primitive type in the JSON string but got " + data['file_unique_id']);
        }

        return true;
    }


}

PassportFile.RequiredProperties = ["file_date", "file_id", "file_size", "file_unique_id"];

/**
 * Unix time when the file was uploaded
 * @member {Number} file_date
 */
PassportFile.prototype['file_date'] = undefined;

/**
 * Identifier for this file, which can be used to download or reuse the file
 * @member {String} file_id
 */
PassportFile.prototype['file_id'] = undefined;

/**
 * File size
 * @member {Number} file_size
 */
PassportFile.prototype['file_size'] = undefined;

/**
 * Unique identifier for this file, which is supposed to be the same over time and for different bots. Can't be used to download or reuse the file.
 * @member {String} file_unique_id
 */
PassportFile.prototype['file_unique_id'] = undefined;






export default PassportFile;

