/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PassportElementErrorReverseSide model module.
 * @module model/PassportElementErrorReverseSide
 * @version 5.0.0
 */
class PassportElementErrorReverseSide {
    /**
     * Constructs a new <code>PassportElementErrorReverseSide</code>.
     * Represents an issue with the reverse side of a document. The error is considered resolved when the file with reverse side of the document changes.
     * @alias module:model/PassportElementErrorReverseSide
     * @param fileHash {String} Base64-encoded hash of the file with the reverse side of the document
     * @param message {String} Error message
     * @param source {String} Error source, must be *reverse\\_side*
     * @param type {module:model/PassportElementErrorReverseSide.TypeEnum} The section of the user's Telegram Passport which has the issue, one of “driver\\_license”, “identity\\_card”
     */
    constructor(fileHash, message, source, type) { 
        
        PassportElementErrorReverseSide.initialize(this, fileHash, message, source, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fileHash, message, source, type) { 
        obj['file_hash'] = fileHash;
        obj['message'] = message;
        obj['source'] = source;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>PassportElementErrorReverseSide</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PassportElementErrorReverseSide} obj Optional instance to populate.
     * @return {module:model/PassportElementErrorReverseSide} The populated <code>PassportElementErrorReverseSide</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PassportElementErrorReverseSide();

            if (data.hasOwnProperty('file_hash')) {
                obj['file_hash'] = ApiClient.convertToType(data['file_hash'], 'String');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PassportElementErrorReverseSide</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PassportElementErrorReverseSide</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PassportElementErrorReverseSide.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['file_hash'] && !(typeof data['file_hash'] === 'string' || data['file_hash'] instanceof String)) {
            throw new Error("Expected the field `file_hash` to be a primitive type in the JSON string but got " + data['file_hash']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

PassportElementErrorReverseSide.RequiredProperties = ["file_hash", "message", "source", "type"];

/**
 * Base64-encoded hash of the file with the reverse side of the document
 * @member {String} file_hash
 */
PassportElementErrorReverseSide.prototype['file_hash'] = undefined;

/**
 * Error message
 * @member {String} message
 */
PassportElementErrorReverseSide.prototype['message'] = undefined;

/**
 * Error source, must be *reverse\\_side*
 * @member {String} source
 */
PassportElementErrorReverseSide.prototype['source'] = undefined;

/**
 * The section of the user's Telegram Passport which has the issue, one of “driver\\_license”, “identity\\_card”
 * @member {module:model/PassportElementErrorReverseSide.TypeEnum} type
 */
PassportElementErrorReverseSide.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
PassportElementErrorReverseSide['TypeEnum'] = {

    /**
     * value: "driver_license"
     * @const
     */
    "driver_license": "driver_license",

    /**
     * value: "identity_card"
     * @const
     */
    "identity_card": "identity_card"
};



export default PassportElementErrorReverseSide;

