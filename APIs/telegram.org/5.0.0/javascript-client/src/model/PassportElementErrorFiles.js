/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PassportElementErrorFiles model module.
 * @module model/PassportElementErrorFiles
 * @version 5.0.0
 */
class PassportElementErrorFiles {
    /**
     * Constructs a new <code>PassportElementErrorFiles</code>.
     * Represents an issue with a list of scans. The error is considered resolved when the list of files containing the scans changes.
     * @alias module:model/PassportElementErrorFiles
     * @param fileHashes {Array.<String>} List of base64-encoded file hashes
     * @param message {String} Error message
     * @param source {String} Error source, must be *files*
     * @param type {module:model/PassportElementErrorFiles.TypeEnum} The section of the user's Telegram Passport which has the issue, one of “utility\\_bill”, “bank\\_statement”, “rental\\_agreement”, “passport\\_registration”, “temporary\\_registration”
     */
    constructor(fileHashes, message, source, type) { 
        
        PassportElementErrorFiles.initialize(this, fileHashes, message, source, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fileHashes, message, source, type) { 
        obj['file_hashes'] = fileHashes;
        obj['message'] = message;
        obj['source'] = source;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>PassportElementErrorFiles</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PassportElementErrorFiles} obj Optional instance to populate.
     * @return {module:model/PassportElementErrorFiles} The populated <code>PassportElementErrorFiles</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PassportElementErrorFiles();

            if (data.hasOwnProperty('file_hashes')) {
                obj['file_hashes'] = ApiClient.convertToType(data['file_hashes'], ['String']);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PassportElementErrorFiles</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PassportElementErrorFiles</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PassportElementErrorFiles.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['file_hashes'])) {
            throw new Error("Expected the field `file_hashes` to be an array in the JSON data but got " + data['file_hashes']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

PassportElementErrorFiles.RequiredProperties = ["file_hashes", "message", "source", "type"];

/**
 * List of base64-encoded file hashes
 * @member {Array.<String>} file_hashes
 */
PassportElementErrorFiles.prototype['file_hashes'] = undefined;

/**
 * Error message
 * @member {String} message
 */
PassportElementErrorFiles.prototype['message'] = undefined;

/**
 * Error source, must be *files*
 * @member {String} source
 */
PassportElementErrorFiles.prototype['source'] = undefined;

/**
 * The section of the user's Telegram Passport which has the issue, one of “utility\\_bill”, “bank\\_statement”, “rental\\_agreement”, “passport\\_registration”, “temporary\\_registration”
 * @member {module:model/PassportElementErrorFiles.TypeEnum} type
 */
PassportElementErrorFiles.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
PassportElementErrorFiles['TypeEnum'] = {

    /**
     * value: "utility_bill"
     * @const
     */
    "utility_bill": "utility_bill",

    /**
     * value: "bank_statement"
     * @const
     */
    "bank_statement": "bank_statement",

    /**
     * value: "rental_agreement"
     * @const
     */
    "rental_agreement": "rental_agreement",

    /**
     * value: "passport_registration"
     * @const
     */
    "passport_registration": "passport_registration",

    /**
     * value: "temporary_registration"
     * @const
     */
    "temporary_registration": "temporary_registration"
};



export default PassportElementErrorFiles;

