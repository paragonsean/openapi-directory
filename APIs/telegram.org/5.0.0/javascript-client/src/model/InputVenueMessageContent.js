/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The InputVenueMessageContent model module.
 * @module model/InputVenueMessageContent
 * @version 5.0.0
 */
class InputVenueMessageContent {
    /**
     * Constructs a new <code>InputVenueMessageContent</code>.
     * Represents the [content](https://core.telegram.org/bots/api/#inputmessagecontent) of a venue message to be sent as the result of an inline query.
     * @alias module:model/InputVenueMessageContent
     * @param address {String} Address of the venue
     * @param latitude {Number} Latitude of the venue in degrees
     * @param longitude {Number} Longitude of the venue in degrees
     * @param title {String} Name of the venue
     */
    constructor(address, latitude, longitude, title) { 
        
        InputVenueMessageContent.initialize(this, address, latitude, longitude, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, address, latitude, longitude, title) { 
        obj['address'] = address;
        obj['latitude'] = latitude;
        obj['longitude'] = longitude;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>InputVenueMessageContent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InputVenueMessageContent} obj Optional instance to populate.
     * @return {module:model/InputVenueMessageContent} The populated <code>InputVenueMessageContent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InputVenueMessageContent();

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('foursquare_id')) {
                obj['foursquare_id'] = ApiClient.convertToType(data['foursquare_id'], 'String');
            }
            if (data.hasOwnProperty('foursquare_type')) {
                obj['foursquare_type'] = ApiClient.convertToType(data['foursquare_type'], 'String');
            }
            if (data.hasOwnProperty('google_place_id')) {
                obj['google_place_id'] = ApiClient.convertToType(data['google_place_id'], 'String');
            }
            if (data.hasOwnProperty('google_place_type')) {
                obj['google_place_type'] = ApiClient.convertToType(data['google_place_type'], 'String');
            }
            if (data.hasOwnProperty('latitude')) {
                obj['latitude'] = ApiClient.convertToType(data['latitude'], 'Number');
            }
            if (data.hasOwnProperty('longitude')) {
                obj['longitude'] = ApiClient.convertToType(data['longitude'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InputVenueMessageContent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InputVenueMessageContent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InputVenueMessageContent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['foursquare_id'] && !(typeof data['foursquare_id'] === 'string' || data['foursquare_id'] instanceof String)) {
            throw new Error("Expected the field `foursquare_id` to be a primitive type in the JSON string but got " + data['foursquare_id']);
        }
        // ensure the json data is a string
        if (data['foursquare_type'] && !(typeof data['foursquare_type'] === 'string' || data['foursquare_type'] instanceof String)) {
            throw new Error("Expected the field `foursquare_type` to be a primitive type in the JSON string but got " + data['foursquare_type']);
        }
        // ensure the json data is a string
        if (data['google_place_id'] && !(typeof data['google_place_id'] === 'string' || data['google_place_id'] instanceof String)) {
            throw new Error("Expected the field `google_place_id` to be a primitive type in the JSON string but got " + data['google_place_id']);
        }
        // ensure the json data is a string
        if (data['google_place_type'] && !(typeof data['google_place_type'] === 'string' || data['google_place_type'] instanceof String)) {
            throw new Error("Expected the field `google_place_type` to be a primitive type in the JSON string but got " + data['google_place_type']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

InputVenueMessageContent.RequiredProperties = ["address", "latitude", "longitude", "title"];

/**
 * Address of the venue
 * @member {String} address
 */
InputVenueMessageContent.prototype['address'] = undefined;

/**
 * *Optional*. Foursquare identifier of the venue, if known
 * @member {String} foursquare_id
 */
InputVenueMessageContent.prototype['foursquare_id'] = undefined;

/**
 * *Optional*. Foursquare type of the venue, if known. (For example, “arts\\_entertainment/default”, “arts\\_entertainment/aquarium” or “food/icecream”.)
 * @member {String} foursquare_type
 */
InputVenueMessageContent.prototype['foursquare_type'] = undefined;

/**
 * *Optional*. Google Places identifier of the venue
 * @member {String} google_place_id
 */
InputVenueMessageContent.prototype['google_place_id'] = undefined;

/**
 * *Optional*. Google Places type of the venue. (See [supported types](https://developers.google.com/places/web-service/supported_types).)
 * @member {String} google_place_type
 */
InputVenueMessageContent.prototype['google_place_type'] = undefined;

/**
 * Latitude of the venue in degrees
 * @member {Number} latitude
 */
InputVenueMessageContent.prototype['latitude'] = undefined;

/**
 * Longitude of the venue in degrees
 * @member {Number} longitude
 */
InputVenueMessageContent.prototype['longitude'] = undefined;

/**
 * Name of the venue
 * @member {String} title
 */
InputVenueMessageContent.prototype['title'] = undefined;






export default InputVenueMessageContent;

