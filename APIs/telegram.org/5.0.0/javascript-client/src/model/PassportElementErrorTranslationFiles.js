/**
 * Telegram Bot API
 * Auto-generated OpenAPI schema
 *
 * The version of the OpenAPI document: 5.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PassportElementErrorTranslationFiles model module.
 * @module model/PassportElementErrorTranslationFiles
 * @version 5.0.0
 */
class PassportElementErrorTranslationFiles {
    /**
     * Constructs a new <code>PassportElementErrorTranslationFiles</code>.
     * Represents an issue with the translated version of a document. The error is considered resolved when a file with the document translation change.
     * @alias module:model/PassportElementErrorTranslationFiles
     * @param fileHashes {Array.<String>} List of base64-encoded file hashes
     * @param message {String} Error message
     * @param source {String} Error source, must be *translation\\_files*
     * @param type {module:model/PassportElementErrorTranslationFiles.TypeEnum} Type of element of the user's Telegram Passport which has the issue, one of “passport”, “driver\\_license”, “identity\\_card”, “internal\\_passport”, “utility\\_bill”, “bank\\_statement”, “rental\\_agreement”, “passport\\_registration”, “temporary\\_registration”
     */
    constructor(fileHashes, message, source, type) { 
        
        PassportElementErrorTranslationFiles.initialize(this, fileHashes, message, source, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fileHashes, message, source, type) { 
        obj['file_hashes'] = fileHashes;
        obj['message'] = message;
        obj['source'] = source;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>PassportElementErrorTranslationFiles</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PassportElementErrorTranslationFiles} obj Optional instance to populate.
     * @return {module:model/PassportElementErrorTranslationFiles} The populated <code>PassportElementErrorTranslationFiles</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PassportElementErrorTranslationFiles();

            if (data.hasOwnProperty('file_hashes')) {
                obj['file_hashes'] = ApiClient.convertToType(data['file_hashes'], ['String']);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PassportElementErrorTranslationFiles</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PassportElementErrorTranslationFiles</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PassportElementErrorTranslationFiles.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['file_hashes'])) {
            throw new Error("Expected the field `file_hashes` to be an array in the JSON data but got " + data['file_hashes']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

PassportElementErrorTranslationFiles.RequiredProperties = ["file_hashes", "message", "source", "type"];

/**
 * List of base64-encoded file hashes
 * @member {Array.<String>} file_hashes
 */
PassportElementErrorTranslationFiles.prototype['file_hashes'] = undefined;

/**
 * Error message
 * @member {String} message
 */
PassportElementErrorTranslationFiles.prototype['message'] = undefined;

/**
 * Error source, must be *translation\\_files*
 * @member {String} source
 */
PassportElementErrorTranslationFiles.prototype['source'] = undefined;

/**
 * Type of element of the user's Telegram Passport which has the issue, one of “passport”, “driver\\_license”, “identity\\_card”, “internal\\_passport”, “utility\\_bill”, “bank\\_statement”, “rental\\_agreement”, “passport\\_registration”, “temporary\\_registration”
 * @member {module:model/PassportElementErrorTranslationFiles.TypeEnum} type
 */
PassportElementErrorTranslationFiles.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
PassportElementErrorTranslationFiles['TypeEnum'] = {

    /**
     * value: "passport"
     * @const
     */
    "passport": "passport",

    /**
     * value: "driver_license"
     * @const
     */
    "driver_license": "driver_license",

    /**
     * value: "identity_card"
     * @const
     */
    "identity_card": "identity_card",

    /**
     * value: "internal_passport"
     * @const
     */
    "internal_passport": "internal_passport",

    /**
     * value: "utility_bill"
     * @const
     */
    "utility_bill": "utility_bill",

    /**
     * value: "bank_statement"
     * @const
     */
    "bank_statement": "bank_statement",

    /**
     * value: "rental_agreement"
     * @const
     */
    "rental_agreement": "rental_agreement",

    /**
     * value: "passport_registration"
     * @const
     */
    "passport_registration": "passport_registration",

    /**
     * value: "temporary_registration"
     * @const
     */
    "temporary_registration": "temporary_registration"
};



export default PassportElementErrorTranslationFiles;

