/**
 * Pinecone API
 * Pinecone is a vector database. This is an unofficial, community-managed OpenAPI spec that (should) accurately model the Pinecone API. This project was developed independent of and is unaffiliated with Pinecone Systems. Users should switch to the official API spec, if and when Pinecone releases it.
 *
 * The version of the OpenAPI document: 20230406.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SparseVectorData from './SparseVectorData';

/**
 * The UpdateRequest model module.
 * @module model/UpdateRequest
 * @version 20230406.1
 */
class UpdateRequest {
    /**
     * Constructs a new <code>UpdateRequest</code>.
     * @alias module:model/UpdateRequest
     * @param id {String} The vector's unique ID
     */
    constructor(id) { 
        
        UpdateRequest.initialize(this, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id) { 
        obj['id'] = id;
    }

    /**
     * Constructs a <code>UpdateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateRequest} obj Optional instance to populate.
     * @return {module:model/UpdateRequest} The populated <code>UpdateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateRequest();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('namespace')) {
                obj['namespace'] = ApiClient.convertToType(data['namespace'], 'String');
            }
            if (data.hasOwnProperty('setMetadata')) {
                obj['setMetadata'] = ApiClient.convertToType(data['setMetadata'], {'String': Object});
            }
            if (data.hasOwnProperty('sparseValues')) {
                obj['sparseValues'] = SparseVectorData.constructFromObject(data['sparseValues']);
            }
            if (data.hasOwnProperty('values')) {
                obj['values'] = ApiClient.convertToType(data['values'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['namespace'] && !(typeof data['namespace'] === 'string' || data['namespace'] instanceof String)) {
            throw new Error("Expected the field `namespace` to be a primitive type in the JSON string but got " + data['namespace']);
        }
        // validate the optional field `sparseValues`
        if (data['sparseValues']) { // data not null
          SparseVectorData.validateJSON(data['sparseValues']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['values'])) {
            throw new Error("Expected the field `values` to be an array in the JSON data but got " + data['values']);
        }

        return true;
    }


}

UpdateRequest.RequiredProperties = ["id"];

/**
 * The vector's unique ID
 * @member {String} id
 */
UpdateRequest.prototype['id'] = undefined;

/**
 * An index namespace name
 * @member {String} namespace
 */
UpdateRequest.prototype['namespace'] = undefined;

/**
 * @member {Object.<String, Object>} setMetadata
 */
UpdateRequest.prototype['setMetadata'] = undefined;

/**
 * @member {module:model/SparseVectorData} sparseValues
 */
UpdateRequest.prototype['sparseValues'] = undefined;

/**
 * Vector dense data. This should be the same length as the dimension of the index being queried.
 * @member {Array.<Number>} values
 */
UpdateRequest.prototype['values'] = undefined;






export default UpdateRequest;

