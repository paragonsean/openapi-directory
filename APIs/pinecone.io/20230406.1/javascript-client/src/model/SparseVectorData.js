/**
 * Pinecone API
 * Pinecone is a vector database. This is an unofficial, community-managed OpenAPI spec that (should) accurately model the Pinecone API. This project was developed independent of and is unaffiliated with Pinecone Systems. Users should switch to the official API spec, if and when Pinecone releases it.
 *
 * The version of the OpenAPI document: 20230406.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SparseVectorData model module.
 * @module model/SparseVectorData
 * @version 20230406.1
 */
class SparseVectorData {
    /**
     * Constructs a new <code>SparseVectorData</code>.
     * Vector sparse data. Represented as a list of indices and a list of corresponded values, which must be the same length.
     * @alias module:model/SparseVectorData
     * @param indices {Array.<Number>} The indices of the sparse data.
     * @param values {Array.<Number>} The corresponding values of the sparse data, which must be the same length as the indices.
     */
    constructor(indices, values) { 
        
        SparseVectorData.initialize(this, indices, values);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, indices, values) { 
        obj['indices'] = indices;
        obj['values'] = values;
    }

    /**
     * Constructs a <code>SparseVectorData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SparseVectorData} obj Optional instance to populate.
     * @return {module:model/SparseVectorData} The populated <code>SparseVectorData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SparseVectorData();

            if (data.hasOwnProperty('indices')) {
                obj['indices'] = ApiClient.convertToType(data['indices'], ['Number']);
            }
            if (data.hasOwnProperty('values')) {
                obj['values'] = ApiClient.convertToType(data['values'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SparseVectorData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SparseVectorData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SparseVectorData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['indices'])) {
            throw new Error("Expected the field `indices` to be an array in the JSON data but got " + data['indices']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['values'])) {
            throw new Error("Expected the field `values` to be an array in the JSON data but got " + data['values']);
        }

        return true;
    }


}

SparseVectorData.RequiredProperties = ["indices", "values"];

/**
 * The indices of the sparse data.
 * @member {Array.<Number>} indices
 */
SparseVectorData.prototype['indices'] = undefined;

/**
 * The corresponding values of the sparse data, which must be the same length as the indices.
 * @member {Array.<Number>} values
 */
SparseVectorData.prototype['values'] = undefined;






export default SparseVectorData;

