/**
 * GOV.UK Pay API
 * GOV.UK Pay API (This version is no longer maintained. See openapi/publicapi_spec.json for latest API specification)
 *
 * The version of the OpenAPI document: 1.0.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Address from './Address';

/**
 * The CardDetails model module.
 * @module model/CardDetails
 * @version 1.0.3
 */
class CardDetails {
    /**
     * Constructs a new <code>CardDetails</code>.
     * A structure representing the payment card
     * @alias module:model/CardDetails
     */
    constructor() { 
        
        CardDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CardDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CardDetails} obj Optional instance to populate.
     * @return {module:model/CardDetails} The populated <code>CardDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CardDetails();

            if (data.hasOwnProperty('billing_address')) {
                obj['billing_address'] = Address.constructFromObject(data['billing_address']);
            }
            if (data.hasOwnProperty('card_brand')) {
                obj['card_brand'] = ApiClient.convertToType(data['card_brand'], 'String');
            }
            if (data.hasOwnProperty('card_type')) {
                obj['card_type'] = ApiClient.convertToType(data['card_type'], 'String');
            }
            if (data.hasOwnProperty('cardholder_name')) {
                obj['cardholder_name'] = ApiClient.convertToType(data['cardholder_name'], 'String');
            }
            if (data.hasOwnProperty('expiry_date')) {
                obj['expiry_date'] = ApiClient.convertToType(data['expiry_date'], 'String');
            }
            if (data.hasOwnProperty('first_digits_card_number')) {
                obj['first_digits_card_number'] = ApiClient.convertToType(data['first_digits_card_number'], 'String');
            }
            if (data.hasOwnProperty('last_digits_card_number')) {
                obj['last_digits_card_number'] = ApiClient.convertToType(data['last_digits_card_number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CardDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CardDetails</code>.
     */
    static validateJSON(data) {
        // validate the optional field `billing_address`
        if (data['billing_address']) { // data not null
          Address.validateJSON(data['billing_address']);
        }
        // ensure the json data is a string
        if (data['card_brand'] && !(typeof data['card_brand'] === 'string' || data['card_brand'] instanceof String)) {
            throw new Error("Expected the field `card_brand` to be a primitive type in the JSON string but got " + data['card_brand']);
        }
        // ensure the json data is a string
        if (data['card_type'] && !(typeof data['card_type'] === 'string' || data['card_type'] instanceof String)) {
            throw new Error("Expected the field `card_type` to be a primitive type in the JSON string but got " + data['card_type']);
        }
        // ensure the json data is a string
        if (data['cardholder_name'] && !(typeof data['cardholder_name'] === 'string' || data['cardholder_name'] instanceof String)) {
            throw new Error("Expected the field `cardholder_name` to be a primitive type in the JSON string but got " + data['cardholder_name']);
        }
        // ensure the json data is a string
        if (data['expiry_date'] && !(typeof data['expiry_date'] === 'string' || data['expiry_date'] instanceof String)) {
            throw new Error("Expected the field `expiry_date` to be a primitive type in the JSON string but got " + data['expiry_date']);
        }
        // ensure the json data is a string
        if (data['first_digits_card_number'] && !(typeof data['first_digits_card_number'] === 'string' || data['first_digits_card_number'] instanceof String)) {
            throw new Error("Expected the field `first_digits_card_number` to be a primitive type in the JSON string but got " + data['first_digits_card_number']);
        }
        // ensure the json data is a string
        if (data['last_digits_card_number'] && !(typeof data['last_digits_card_number'] === 'string' || data['last_digits_card_number'] instanceof String)) {
            throw new Error("Expected the field `last_digits_card_number` to be a primitive type in the JSON string but got " + data['last_digits_card_number']);
        }

        return true;
    }


}



/**
 * @member {module:model/Address} billing_address
 */
CardDetails.prototype['billing_address'] = undefined;

/**
 * @member {String} card_brand
 */
CardDetails.prototype['card_brand'] = undefined;

/**
 * The card type, `debit` or `credit` or `null` if not able to determine
 * @member {module:model/CardDetails.CardTypeEnum} card_type
 */
CardDetails.prototype['card_type'] = undefined;

/**
 * @member {String} cardholder_name
 */
CardDetails.prototype['cardholder_name'] = undefined;

/**
 * The expiry date of the card in MM/yy format
 * @member {String} expiry_date
 */
CardDetails.prototype['expiry_date'] = undefined;

/**
 * @member {String} first_digits_card_number
 */
CardDetails.prototype['first_digits_card_number'] = undefined;

/**
 * @member {String} last_digits_card_number
 */
CardDetails.prototype['last_digits_card_number'] = undefined;





/**
 * Allowed values for the <code>card_type</code> property.
 * @enum {String}
 * @readonly
 */
CardDetails['CardTypeEnum'] = {

    /**
     * value: "debit"
     * @const
     */
    "debit": "debit",

    /**
     * value: "credit"
     * @const
     */
    "credit": "credit",

    /**
     * value: "null"
     * @const
     */
    "null": "null"
};



export default CardDetails;

