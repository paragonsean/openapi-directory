/**
 * GOV.UK Pay API
 * GOV.UK Pay API (This version is no longer maintained. See openapi/publicapi_spec.json for latest API specification)
 *
 * The version of the OpenAPI document: 1.0.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaymentSettlementSummary model module.
 * @module model/PaymentSettlementSummary
 * @version 1.0.3
 */
class PaymentSettlementSummary {
    /**
     * Constructs a new <code>PaymentSettlementSummary</code>.
     * A structure representing information about a settlement
     * @alias module:model/PaymentSettlementSummary
     */
    constructor() { 
        
        PaymentSettlementSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PaymentSettlementSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentSettlementSummary} obj Optional instance to populate.
     * @return {module:model/PaymentSettlementSummary} The populated <code>PaymentSettlementSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentSettlementSummary();

            if (data.hasOwnProperty('capture_submit_time')) {
                obj['capture_submit_time'] = ApiClient.convertToType(data['capture_submit_time'], 'String');
            }
            if (data.hasOwnProperty('captured_date')) {
                obj['captured_date'] = ApiClient.convertToType(data['captured_date'], 'String');
            }
            if (data.hasOwnProperty('settled_date')) {
                obj['settled_date'] = ApiClient.convertToType(data['settled_date'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentSettlementSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentSettlementSummary</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['capture_submit_time'] && !(typeof data['capture_submit_time'] === 'string' || data['capture_submit_time'] instanceof String)) {
            throw new Error("Expected the field `capture_submit_time` to be a primitive type in the JSON string but got " + data['capture_submit_time']);
        }
        // ensure the json data is a string
        if (data['captured_date'] && !(typeof data['captured_date'] === 'string' || data['captured_date'] instanceof String)) {
            throw new Error("Expected the field `captured_date` to be a primitive type in the JSON string but got " + data['captured_date']);
        }
        // ensure the json data is a string
        if (data['settled_date'] && !(typeof data['settled_date'] === 'string' || data['settled_date'] instanceof String)) {
            throw new Error("Expected the field `settled_date` to be a primitive type in the JSON string but got " + data['settled_date']);
        }

        return true;
    }


}



/**
 * Date and time capture request has been submitted. May be null if capture request was not immediately acknowledged by payment gateway.
 * @member {String} capture_submit_time
 */
PaymentSettlementSummary.prototype['capture_submit_time'] = undefined;

/**
 * Date of the capture event.
 * @member {String} captured_date
 */
PaymentSettlementSummary.prototype['captured_date'] = undefined;

/**
 * The date that the transaction was paid into the service's account.
 * @member {String} settled_date
 */
PaymentSettlementSummary.prototype['settled_date'] = undefined;






export default PaymentSettlementSummary;

