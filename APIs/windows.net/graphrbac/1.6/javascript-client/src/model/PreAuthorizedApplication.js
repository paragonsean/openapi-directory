/**
 * GraphRbacManagementClient
 * The Graph RBAC Management Client
 *
 * The version of the OpenAPI document: 1.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PreAuthorizedApplicationExtension from './PreAuthorizedApplicationExtension';
import PreAuthorizedApplicationPermission from './PreAuthorizedApplicationPermission';

/**
 * The PreAuthorizedApplication model module.
 * @module model/PreAuthorizedApplication
 * @version 1.6
 */
class PreAuthorizedApplication {
    /**
     * Constructs a new <code>PreAuthorizedApplication</code>.
     * Contains information about pre authorized client application.
     * @alias module:model/PreAuthorizedApplication
     */
    constructor() { 
        
        PreAuthorizedApplication.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PreAuthorizedApplication</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PreAuthorizedApplication} obj Optional instance to populate.
     * @return {module:model/PreAuthorizedApplication} The populated <code>PreAuthorizedApplication</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PreAuthorizedApplication();

            if (data.hasOwnProperty('appId')) {
                obj['appId'] = ApiClient.convertToType(data['appId'], 'String');
            }
            if (data.hasOwnProperty('extensions')) {
                obj['extensions'] = ApiClient.convertToType(data['extensions'], [PreAuthorizedApplicationExtension]);
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], [PreAuthorizedApplicationPermission]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PreAuthorizedApplication</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PreAuthorizedApplication</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['appId'] && !(typeof data['appId'] === 'string' || data['appId'] instanceof String)) {
            throw new Error("Expected the field `appId` to be a primitive type in the JSON string but got " + data['appId']);
        }
        if (data['extensions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['extensions'])) {
                throw new Error("Expected the field `extensions` to be an array in the JSON data but got " + data['extensions']);
            }
            // validate the optional field `extensions` (array)
            for (const item of data['extensions']) {
                PreAuthorizedApplicationExtension.validateJSON(item);
            };
        }
        if (data['permissions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['permissions'])) {
                throw new Error("Expected the field `permissions` to be an array in the JSON data but got " + data['permissions']);
            }
            // validate the optional field `permissions` (array)
            for (const item of data['permissions']) {
                PreAuthorizedApplicationPermission.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Represents the application id.
 * @member {String} appId
 */
PreAuthorizedApplication.prototype['appId'] = undefined;

/**
 * Collection of extensions from the resource application.
 * @member {Array.<module:model/PreAuthorizedApplicationExtension>} extensions
 */
PreAuthorizedApplication.prototype['extensions'] = undefined;

/**
 * Collection of required app permissions/entitlements from the resource application.
 * @member {Array.<module:model/PreAuthorizedApplicationPermission>} permissions
 */
PreAuthorizedApplication.prototype['permissions'] = undefined;






export default PreAuthorizedApplication;

