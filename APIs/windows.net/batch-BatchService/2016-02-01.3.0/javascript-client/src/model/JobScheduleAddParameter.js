/**
 * BatchService
 * A client for issuing REST requests to the Azure Batch service.
 *
 * The version of the OpenAPI document: 2016-02-01.3.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JobSpecification from './JobSpecification';
import MetadataItem from './MetadataItem';
import Schedule from './Schedule';

/**
 * The JobScheduleAddParameter model module.
 * @module model/JobScheduleAddParameter
 * @version 2016-02-01.3.0
 */
class JobScheduleAddParameter {
    /**
     * Constructs a new <code>JobScheduleAddParameter</code>.
     * A job schedule that allows recurring jobs by specifying when to run jobs and a specification used to create each job.
     * @alias module:model/JobScheduleAddParameter
     * @param id {String} A string that uniquely identifies the schedule within the account. A GUID is recommended.
     * @param jobSpecification {module:model/JobSpecification} 
     * @param schedule {module:model/Schedule} 
     */
    constructor(id, jobSpecification, schedule) { 
        
        JobScheduleAddParameter.initialize(this, id, jobSpecification, schedule);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, jobSpecification, schedule) { 
        obj['id'] = id;
        obj['jobSpecification'] = jobSpecification;
        obj['schedule'] = schedule;
    }

    /**
     * Constructs a <code>JobScheduleAddParameter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobScheduleAddParameter} obj Optional instance to populate.
     * @return {module:model/JobScheduleAddParameter} The populated <code>JobScheduleAddParameter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobScheduleAddParameter();

            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('jobSpecification')) {
                obj['jobSpecification'] = JobSpecification.constructFromObject(data['jobSpecification']);
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], [MetadataItem]);
            }
            if (data.hasOwnProperty('schedule')) {
                obj['schedule'] = Schedule.constructFromObject(data['schedule']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobScheduleAddParameter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobScheduleAddParameter</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobScheduleAddParameter.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `jobSpecification`
        if (data['jobSpecification']) { // data not null
          JobSpecification.validateJSON(data['jobSpecification']);
        }
        if (data['metadata']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metadata'])) {
                throw new Error("Expected the field `metadata` to be an array in the JSON data but got " + data['metadata']);
            }
            // validate the optional field `metadata` (array)
            for (const item of data['metadata']) {
                MetadataItem.validateJSON(item);
            };
        }
        // validate the optional field `schedule`
        if (data['schedule']) { // data not null
          Schedule.validateJSON(data['schedule']);
        }

        return true;
    }


}

JobScheduleAddParameter.RequiredProperties = ["id", "jobSpecification", "schedule"];

/**
 * The display name for the schedule.
 * @member {String} displayName
 */
JobScheduleAddParameter.prototype['displayName'] = undefined;

/**
 * A string that uniquely identifies the schedule within the account. A GUID is recommended.
 * @member {String} id
 */
JobScheduleAddParameter.prototype['id'] = undefined;

/**
 * @member {module:model/JobSpecification} jobSpecification
 */
JobScheduleAddParameter.prototype['jobSpecification'] = undefined;

/**
 * A list of name-value pairs associated with the schedule as metadata.
 * @member {Array.<module:model/MetadataItem>} metadata
 */
JobScheduleAddParameter.prototype['metadata'] = undefined;

/**
 * @member {module:model/Schedule} schedule
 */
JobScheduleAddParameter.prototype['schedule'] = undefined;






export default JobScheduleAddParameter;

