/**
 * BatchService
 * A client for issuing REST requests to the Azure Batch service.
 *
 * The version of the OpenAPI document: 2017-05-01.5.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NodeUpdateUserParameter model module.
 * @module model/NodeUpdateUserParameter
 * @version 2017-05-01.5.0
 */
class NodeUpdateUserParameter {
    /**
     * Constructs a new <code>NodeUpdateUserParameter</code>.
     * @alias module:model/NodeUpdateUserParameter
     */
    constructor() { 
        
        NodeUpdateUserParameter.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NodeUpdateUserParameter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NodeUpdateUserParameter} obj Optional instance to populate.
     * @return {module:model/NodeUpdateUserParameter} The populated <code>NodeUpdateUserParameter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NodeUpdateUserParameter();

            if (data.hasOwnProperty('expiryTime')) {
                obj['expiryTime'] = ApiClient.convertToType(data['expiryTime'], 'Date');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('sshPublicKey')) {
                obj['sshPublicKey'] = ApiClient.convertToType(data['sshPublicKey'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NodeUpdateUserParameter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NodeUpdateUserParameter</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['sshPublicKey'] && !(typeof data['sshPublicKey'] === 'string' || data['sshPublicKey'] instanceof String)) {
            throw new Error("Expected the field `sshPublicKey` to be a primitive type in the JSON string but got " + data['sshPublicKey']);
        }

        return true;
    }


}



/**
 * If omitted, the default is 1 day from the current time. For Linux compute nodes, the expiryTime has a precision up to a day.
 * @member {Date} expiryTime
 */
NodeUpdateUserParameter.prototype['expiryTime'] = undefined;

/**
 * The password is required for Windows nodes (those created with 'cloudServiceConfiguration', or created with 'virtualMachineConfiguration' using a Windows image reference). For Linux compute nodes, the password can optionally be specified along with the sshPublicKey property. If omitted, any existing password is removed.
 * @member {String} password
 */
NodeUpdateUserParameter.prototype['password'] = undefined;

/**
 * The public key should be compatible with OpenSSH encoding and should be base 64 encoded. This property can be specified only for Linux nodes. If this is specified for a Windows node, then the Batch service rejects the request; if you are calling the REST API directly, the HTTP status code is 400 (Bad Request). If omitted, any existing SSH public key is removed.
 * @member {String} sshPublicKey
 */
NodeUpdateUserParameter.prototype['sshPublicKey'] = undefined;






export default NodeUpdateUserParameter;

