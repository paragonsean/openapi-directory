/**
 * BatchService
 * A client for issuing REST requests to the Azure Batch service.
 *
 * The version of the OpenAPI document: 2017-05-01.5.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExitCodeMapping from './ExitCodeMapping';
import ExitCodeRangeMapping from './ExitCodeRangeMapping';
import ExitOptions from './ExitOptions';

/**
 * The ExitConditions model module.
 * @module model/ExitConditions
 * @version 2017-05-01.5.0
 */
class ExitConditions {
    /**
     * Constructs a new <code>ExitConditions</code>.
     * @alias module:model/ExitConditions
     */
    constructor() { 
        
        ExitConditions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExitConditions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExitConditions} obj Optional instance to populate.
     * @return {module:model/ExitConditions} The populated <code>ExitConditions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExitConditions();

            if (data.hasOwnProperty('default')) {
                obj['default'] = ExitOptions.constructFromObject(data['default']);
            }
            if (data.hasOwnProperty('exitCodeRanges')) {
                obj['exitCodeRanges'] = ApiClient.convertToType(data['exitCodeRanges'], [ExitCodeRangeMapping]);
            }
            if (data.hasOwnProperty('exitCodes')) {
                obj['exitCodes'] = ApiClient.convertToType(data['exitCodes'], [ExitCodeMapping]);
            }
            if (data.hasOwnProperty('fileUploadError')) {
                obj['fileUploadError'] = ExitOptions.constructFromObject(data['fileUploadError']);
            }
            if (data.hasOwnProperty('preProcessingError')) {
                obj['preProcessingError'] = ExitOptions.constructFromObject(data['preProcessingError']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExitConditions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExitConditions</code>.
     */
    static validateJSON(data) {
        // validate the optional field `default`
        if (data['default']) { // data not null
          ExitOptions.validateJSON(data['default']);
        }
        if (data['exitCodeRanges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['exitCodeRanges'])) {
                throw new Error("Expected the field `exitCodeRanges` to be an array in the JSON data but got " + data['exitCodeRanges']);
            }
            // validate the optional field `exitCodeRanges` (array)
            for (const item of data['exitCodeRanges']) {
                ExitCodeRangeMapping.validateJSON(item);
            };
        }
        if (data['exitCodes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['exitCodes'])) {
                throw new Error("Expected the field `exitCodes` to be an array in the JSON data but got " + data['exitCodes']);
            }
            // validate the optional field `exitCodes` (array)
            for (const item of data['exitCodes']) {
                ExitCodeMapping.validateJSON(item);
            };
        }
        // validate the optional field `fileUploadError`
        if (data['fileUploadError']) { // data not null
          ExitOptions.validateJSON(data['fileUploadError']);
        }
        // validate the optional field `preProcessingError`
        if (data['preProcessingError']) { // data not null
          ExitOptions.validateJSON(data['preProcessingError']);
        }

        return true;
    }


}



/**
 * @member {module:model/ExitOptions} default
 */
ExitConditions.prototype['default'] = undefined;

/**
 * @member {Array.<module:model/ExitCodeRangeMapping>} exitCodeRanges
 */
ExitConditions.prototype['exitCodeRanges'] = undefined;

/**
 * @member {Array.<module:model/ExitCodeMapping>} exitCodes
 */
ExitConditions.prototype['exitCodes'] = undefined;

/**
 * @member {module:model/ExitOptions} fileUploadError
 */
ExitConditions.prototype['fileUploadError'] = undefined;

/**
 * @member {module:model/ExitOptions} preProcessingError
 */
ExitConditions.prototype['preProcessingError'] = undefined;






export default ExitConditions;

