/**
 * Wowza Streaming Cloud REST API Reference Documentation
 *  # About the REST API  The Wowza Streaming Cloud<sup>TM</sup> REST API (application programming interface) offers complete programmatic control over live streams, transcoders, stream sources, and stream targets. Anything you can do in the Wowza Streaming Cloud UI can also be achieved by making HTTP-based requests to cloud-based servers through the REST API.  The Wowza Streaming Cloud REST API features *cross-origin resource sharing*, or CORS. CORS is a [W3C specification](https://www.w3.org/TR/cors/) that provides headers in HTTP requests to enable a web server to safely make a network request to another domain.  In order to protect shared resources, the Wowza Streaming Cloud REST API is subject to limits. For details, see [Wowza Streaming Cloud REST API limits](https://www.wowza.com/docs/wowza-streaming-cloud-rest-api-limits). # About this documentation This reference documentation is based on the open-source [Swagger framework](http://swagger.io/specification/). It allows you to view the operations, parameters, and request and reponse schemas for every resource. Request samples are presented in cURL (Shell) and JavaScript; some samples also include just the JSON object. Response samples are all JSON.  For more information and examples on using the Wowza Streaming Cloud REST API, see our [library of Wowza Streaming Cloud REST API technical articles](https://www.wowza.com/docs/wowza-streaming-cloud-rest-api).  # Query requirements The Wowza Streaming Cloud REST API uses HTTP requests to retrieve data from cloud-based servers. Requests must contain proper JSON, two authentication keys, and the correct version number in the base path.  ## JSON The Wowza Streaming Cloud REST API uses the [JSON API specification](http://jsonapi.org/format/) to request and return data. This means requests must include the header `Content-Type: application/json` and must include a single resource object in JSON format as primary data.  Responses include HTTP status codes that indicate whether the query was successful. If there was an error, a description explains the problem so that you can fix it and try again.  ## Authentication Requests to the Wowza Streaming Cloud REST API must be authenticated with two keys: an API key and an access key. Each key is a 64-character alphanumeric string that you can find on the **API Access** page in Wowza Streaming Cloud.  Use the `wsc-api-key` and `wsc-access-key` headers to authenticate requests, like this (in cURL):  ```bash curl -H 'wsc-api-key: [64-character-api-key-goes-here]' -H 'wsc-access-key: [64-character-access-key-goes-here]' ```  <!-- ReDoc-Inject: <security-definitions> -->  ## Version The Wowza Streaming Cloud API is currently at version 1.0.0. Use `v1` in your base path in every request, like this path to the live_streams endpoint: ``` https://api.cloud.wowza.com/api/v1/live_streams ``` ## Example query Here is a complete example POST request, in cURL, with proper JSON syntax, headers, authentication, and version information: ```bash curl -H 'wsc-api-key: [64-character-api-key-goes-here]' -H 'wsc-access-key: [64-character-access-key-goes-here]'   -H 'Content-Type: application/json' -X POST -d '{     \"live_stream\": {       \"name\": \"My live Stream\",       \"...\": \"...\"     }   }' https://api.cloud.wowza.com/api/v1/live_streams ``` 
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Spec model module.
 * @module model/Spec
 * @version 1
 */
class Spec {
    /**
     * Constructs a new <code>Spec</code>.
     * @alias module:model/Spec
     */
    constructor() { 
        
        Spec.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Spec</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Spec} obj Optional instance to populate.
     * @return {module:model/Spec} The populated <code>Spec</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Spec();

            if (data.hasOwnProperty('basePath')) {
                obj['basePath'] = ApiClient.convertToType(data['basePath'], 'String');
            }
            if (data.hasOwnProperty('consumes')) {
                obj['consumes'] = ApiClient.convertToType(data['consumes'], ['String']);
            }
            if (data.hasOwnProperty('definitions')) {
                obj['definitions'] = ApiClient.convertToType(data['definitions'], Object);
            }
            if (data.hasOwnProperty('externalDocs')) {
                obj['externalDocs'] = ApiClient.convertToType(data['externalDocs'], Object);
            }
            if (data.hasOwnProperty('host')) {
                obj['host'] = ApiClient.convertToType(data['host'], 'String');
            }
            if (data.hasOwnProperty('info')) {
                obj['info'] = ApiClient.convertToType(data['info'], Object);
            }
            if (data.hasOwnProperty('paths')) {
                obj['paths'] = ApiClient.convertToType(data['paths'], Object);
            }
            if (data.hasOwnProperty('produces')) {
                obj['produces'] = ApiClient.convertToType(data['produces'], ['String']);
            }
            if (data.hasOwnProperty('schemes')) {
                obj['schemes'] = ApiClient.convertToType(data['schemes'], ['String']);
            }
            if (data.hasOwnProperty('security')) {
                obj['security'] = ApiClient.convertToType(data['security'], [Object]);
            }
            if (data.hasOwnProperty('securityDefinitions')) {
                obj['securityDefinitions'] = ApiClient.convertToType(data['securityDefinitions'], Object);
            }
            if (data.hasOwnProperty('swagger')) {
                obj['swagger'] = ApiClient.convertToType(data['swagger'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], [Object]);
            }
            if (data.hasOwnProperty('x-tagGroups')) {
                obj['x-tagGroups'] = ApiClient.convertToType(data['x-tagGroups'], [Object]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Spec</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Spec</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['basePath'] && !(typeof data['basePath'] === 'string' || data['basePath'] instanceof String)) {
            throw new Error("Expected the field `basePath` to be a primitive type in the JSON string but got " + data['basePath']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['consumes'])) {
            throw new Error("Expected the field `consumes` to be an array in the JSON data but got " + data['consumes']);
        }
        // ensure the json data is a string
        if (data['host'] && !(typeof data['host'] === 'string' || data['host'] instanceof String)) {
            throw new Error("Expected the field `host` to be a primitive type in the JSON string but got " + data['host']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['produces'])) {
            throw new Error("Expected the field `produces` to be an array in the JSON data but got " + data['produces']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['schemes'])) {
            throw new Error("Expected the field `schemes` to be an array in the JSON data but got " + data['schemes']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['security'])) {
            throw new Error("Expected the field `security` to be an array in the JSON data but got " + data['security']);
        }
        // ensure the json data is a string
        if (data['swagger'] && !(typeof data['swagger'] === 'string' || data['swagger'] instanceof String)) {
            throw new Error("Expected the field `swagger` to be a primitive type in the JSON string but got " + data['swagger']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['x-tagGroups'])) {
            throw new Error("Expected the field `x-tagGroups` to be an array in the JSON data but got " + data['x-tagGroups']);
        }

        return true;
    }


}



/**
 * The base path on which the API is served, relative to the **host**.
 * @member {String} basePath
 */
Spec.prototype['basePath'] = undefined;

/**
 * A list of MIME types that the API can consume.
 * @member {Array.<String>} consumes
 */
Spec.prototype['consumes'] = undefined;

/**
 * The data types produced and consumed by operations.
 * @member {Object} definitions
 */
Spec.prototype['definitions'] = undefined;

/**
 * Links to and descriptions of related external documentation.
 * @member {Object} externalDocs
 */
Spec.prototype['externalDocs'] = undefined;

/**
 * The host (domain name or IP address) serving the API.
 * @member {String} host
 */
Spec.prototype['host'] = undefined;

/**
 * Metadata about the API.
 * @member {Object} info
 */
Spec.prototype['info'] = undefined;

/**
 * The paths and operations available to the API.
 * @member {Object} paths
 */
Spec.prototype['paths'] = undefined;

/**
 * A list of MIME types that the API can produce.
 * @member {Array.<String>} produces
 */
Spec.prototype['produces'] = undefined;

/**
 * The transfer protocol being used by the API.
 * @member {Array.<String>} schemes
 */
Spec.prototype['schemes'] = undefined;

/**
 * A list of the security schemes being used by the API.
 * @member {Array.<Object>} security
 */
Spec.prototype['security'] = undefined;

/**
 * The security scheme definitions being used by the API.
 * @member {Object} securityDefinitions
 */
Spec.prototype['securityDefinitions'] = undefined;

/**
 * The version of the Swagger specification that's being used.
 * @member {String} swagger
 */
Spec.prototype['swagger'] = undefined;

/**
 * A list of tags used by the specification, with metadata.
 * @member {Array.<Object>} tags
 */
Spec.prototype['tags'] = undefined;

/**
 * A list of the groups and tags used in the left column of the Swagger page.
 * @member {Array.<Object>} x-tagGroups
 */
Spec.prototype['x-tagGroups'] = undefined;






export default Spec;

