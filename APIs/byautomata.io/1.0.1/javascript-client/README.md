# automata_market_intelligence_api

AutomataMarketIntelligenceApi - JavaScript client for automata_market_intelligence_api
This document provides the documentation for the Market Intelligence API by Automata. Get your API Key at https://apis.byautomata.io and check out our <a href='https://www.getpostman.com/collections/d182a1c78d4491d55e19'>Postman Collection</a>.<br><br>The root API endpoint is https://api.byautomata.io. Please refer to the code samples for examples of how to call the Market Intelligence API. The ContentPro endpoints (/contentpro-search and /contentpro-similar-text) are not included in the standard Market Intelligence API plans. Please contact support@byautomata.io for access.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.1
- Package version: 1.0.1
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install automata_market_intelligence_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your automata_market_intelligence_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var AutomataMarketIntelligenceApi = require('automata_market_intelligence_api');


var api = new AutomataMarketIntelligenceApi.ContentproSearchApi()
var terms = null; // {String} We provide information about related companies and articles based on the search terms you provide. Separate search terms with commas. Ex. https://api.byautomata.io/contentpro-search?terms=cloud+computing,enterprise,security
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.contentproSearchGet(terms, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.byautomata.io*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*AutomataMarketIntelligenceApi.ContentproSearchApi* | [**contentproSearchGet**](docs/ContentproSearchApi.md#contentproSearchGet) | **GET** /contentpro-search | Send search terms to receive the most relevant articles and companies.
*AutomataMarketIntelligenceApi.ContentproSimilarTextApi* | [**contentproSimilarTextPost**](docs/ContentproSimilarTextApi.md#contentproSimilarTextPost) | **POST** /contentpro-similar-text | The /contentpro-similar-text endpoint accepts and arbitrary piece of text and returns similar articles and blogs written by companies.
*AutomataMarketIntelligenceApi.SearchApi* | [**searchGet**](docs/SearchApi.md#searchGet) | **GET** /search | Send search terms to receive the most relevant companies along with text snippets.
*AutomataMarketIntelligenceApi.SimilarApi* | [**similarGet**](docs/SimilarApi.md#similarGet) | **GET** /similar | Send a company website to receive a list of companies related to them.


## Documentation for Models

 - [AutomataMarketIntelligenceApi.Article](docs/Article.md)
 - [AutomataMarketIntelligenceApi.ContentProCompany](docs/ContentProCompany.md)
 - [AutomataMarketIntelligenceApi.ContentProSnippets](docs/ContentProSnippets.md)
 - [AutomataMarketIntelligenceApi.ContentproSearchGet200Response](docs/ContentproSearchGet200Response.md)
 - [AutomataMarketIntelligenceApi.ContentproSearchGet200ResponseDataInner](docs/ContentproSearchGet200ResponseDataInner.md)
 - [AutomataMarketIntelligenceApi.ContentproSimilarTextPostRequest](docs/ContentproSimilarTextPostRequest.md)
 - [AutomataMarketIntelligenceApi.InputCompany](docs/InputCompany.md)
 - [AutomataMarketIntelligenceApi.SearchGet200Response](docs/SearchGet200Response.md)
 - [AutomataMarketIntelligenceApi.SimilarCompany](docs/SimilarCompany.md)
 - [AutomataMarketIntelligenceApi.SimilarCompanySearch](docs/SimilarCompanySearch.md)
 - [AutomataMarketIntelligenceApi.SimilarGet200Response](docs/SimilarGet200Response.md)
 - [AutomataMarketIntelligenceApi.Snippet](docs/Snippet.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### api_key


- **Type**: API key
- **API key parameter name**: x-api-key
- **Location**: HTTP header

