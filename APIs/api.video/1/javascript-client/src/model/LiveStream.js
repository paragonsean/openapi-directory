/**
 * api.video
 * api.video is an API that encodes on the go to facilitate immediate playback, enhancing viewer streaming experiences across multiple devices and platforms. You can stream live or on-demand online videos within minutes.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LiveStreamAssets from './LiveStreamAssets';

/**
 * The LiveStream model module.
 * @module model/LiveStream
 * @version 1
 */
class LiveStream {
    /**
     * Constructs a new <code>LiveStream</code>.
     * @alias module:model/LiveStream
     */
    constructor() { 
        
        LiveStream.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LiveStream</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LiveStream} obj Optional instance to populate.
     * @return {module:model/LiveStream} The populated <code>LiveStream</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LiveStream();

            if (data.hasOwnProperty('assets')) {
                obj['assets'] = LiveStreamAssets.constructFromObject(data['assets']);
            }
            if (data.hasOwnProperty('broadcasting')) {
                obj['broadcasting'] = ApiClient.convertToType(data['broadcasting'], 'Boolean');
            }
            if (data.hasOwnProperty('liveStreamId')) {
                obj['liveStreamId'] = ApiClient.convertToType(data['liveStreamId'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('playerId')) {
                obj['playerId'] = ApiClient.convertToType(data['playerId'], 'String');
            }
            if (data.hasOwnProperty('public')) {
                obj['public'] = ApiClient.convertToType(data['public'], 'Boolean');
            }
            if (data.hasOwnProperty('record')) {
                obj['record'] = ApiClient.convertToType(data['record'], 'Boolean');
            }
            if (data.hasOwnProperty('streamKey')) {
                obj['streamKey'] = ApiClient.convertToType(data['streamKey'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LiveStream</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LiveStream</code>.
     */
    static validateJSON(data) {
        // validate the optional field `assets`
        if (data['assets']) { // data not null
          LiveStreamAssets.validateJSON(data['assets']);
        }
        // ensure the json data is a string
        if (data['liveStreamId'] && !(typeof data['liveStreamId'] === 'string' || data['liveStreamId'] instanceof String)) {
            throw new Error("Expected the field `liveStreamId` to be a primitive type in the JSON string but got " + data['liveStreamId']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['playerId'] && !(typeof data['playerId'] === 'string' || data['playerId'] instanceof String)) {
            throw new Error("Expected the field `playerId` to be a primitive type in the JSON string but got " + data['playerId']);
        }
        // ensure the json data is a string
        if (data['streamKey'] && !(typeof data['streamKey'] === 'string' || data['streamKey'] instanceof String)) {
            throw new Error("Expected the field `streamKey` to be a primitive type in the JSON string but got " + data['streamKey']);
        }

        return true;
    }


}



/**
 * @member {module:model/LiveStreamAssets} assets
 */
LiveStream.prototype['assets'] = undefined;

/**
 * Whether or not you are broadcasting the live video you recorded for others to see. True means you are broadcasting to viewers, false means you are not.
 * @member {Boolean} broadcasting
 */
LiveStream.prototype['broadcasting'] = undefined;

/**
 * The unique identifier for the live stream. Live stream IDs begin with \"li.\"
 * @member {String} liveStreamId
 */
LiveStream.prototype['liveStreamId'] = undefined;

/**
 * The name of your live stream.
 * @member {String} name
 */
LiveStream.prototype['name'] = undefined;

/**
 * The unique identifier for the player.
 * @member {String} playerId
 */
LiveStream.prototype['playerId'] = undefined;

/**
 * BETA FEATURE Please limit all public = false (\"private\") livestreams to 3,000 users. Whether your video can be viewed by everyone, or requires authentication to see it. A setting of false will require a unique token for each view.
 * @member {Boolean} public
 */
LiveStream.prototype['public'] = undefined;

/**
 * Whether you are recording or not.
 * @member {Boolean} record
 */
LiveStream.prototype['record'] = undefined;

/**
 * The unique, private stream key that you use to begin streaming.
 * @member {String} streamKey
 */
LiveStream.prototype['streamKey'] = undefined;






export default LiveStream;

