/**
 * api.video
 * api.video is an API that encodes on the go to facilitate immediate playback, enhancing viewer streaming experiences across multiple devices and platforms. You can stream live or on-demand online videos within minutes.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VideoSessionReferrer model module.
 * @module model/VideoSessionReferrer
 * @version 1
 */
class VideoSessionReferrer {
    /**
     * Constructs a new <code>VideoSessionReferrer</code>.
     * @alias module:model/VideoSessionReferrer
     */
    constructor() { 
        
        VideoSessionReferrer.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>VideoSessionReferrer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VideoSessionReferrer} obj Optional instance to populate.
     * @return {module:model/VideoSessionReferrer} The populated <code>VideoSessionReferrer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VideoSessionReferrer();

            if (data.hasOwnProperty('medium')) {
                obj['medium'] = ApiClient.convertToType(data['medium'], 'String');
            }
            if (data.hasOwnProperty('searchTerm')) {
                obj['searchTerm'] = ApiClient.convertToType(data['searchTerm'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VideoSessionReferrer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VideoSessionReferrer</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['medium'] && !(typeof data['medium'] === 'string' || data['medium'] instanceof String)) {
            throw new Error("Expected the field `medium` to be a primitive type in the JSON string but got " + data['medium']);
        }
        // ensure the json data is a string
        if (data['searchTerm'] && !(typeof data['searchTerm'] === 'string' || data['searchTerm'] instanceof String)) {
            throw new Error("Expected the field `searchTerm` to be a primitive type in the JSON string but got " + data['searchTerm']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}



/**
 * How they arrived at the site, for example organic or paid. Organic meaning they found it themselves and paid meaning they followed a link from an advertisement.
 * @member {String} medium
 */
VideoSessionReferrer.prototype['medium'] = undefined;

/**
 * The search term they typed to arrive at the video session.
 * @member {String} searchTerm
 */
VideoSessionReferrer.prototype['searchTerm'] = undefined;

/**
 * The source the referrer came from to the video session. For example if they searched through google to find the stream.
 * @member {String} source
 */
VideoSessionReferrer.prototype['source'] = undefined;

/**
 * The link the viewer used to reach the video session.
 * @member {String} url
 */
VideoSessionReferrer.prototype['url'] = undefined;






export default VideoSessionReferrer;

