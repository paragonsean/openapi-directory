/**
 * Superset
 * Superset
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Datasource model module.
 * @module model/Datasource
 * @version v1
 */
class Datasource {
    /**
     * Constructs a new <code>Datasource</code>.
     * @alias module:model/Datasource
     * @param datasourceType {module:model/Datasource.DatasourceTypeEnum} The type of dataset/datasource identified on `datasource_id`.
     */
    constructor(datasourceType) { 
        
        Datasource.initialize(this, datasourceType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, datasourceType) { 
        obj['datasource_type'] = datasourceType;
    }

    /**
     * Constructs a <code>Datasource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Datasource} obj Optional instance to populate.
     * @return {module:model/Datasource} The populated <code>Datasource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Datasource();

            if (data.hasOwnProperty('database_name')) {
                obj['database_name'] = ApiClient.convertToType(data['database_name'], 'String');
            }
            if (data.hasOwnProperty('datasource_name')) {
                obj['datasource_name'] = ApiClient.convertToType(data['datasource_name'], 'String');
            }
            if (data.hasOwnProperty('datasource_type')) {
                obj['datasource_type'] = ApiClient.convertToType(data['datasource_type'], 'String');
            }
            if (data.hasOwnProperty('schema')) {
                obj['schema'] = ApiClient.convertToType(data['schema'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Datasource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Datasource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Datasource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['database_name'] && !(typeof data['database_name'] === 'string' || data['database_name'] instanceof String)) {
            throw new Error("Expected the field `database_name` to be a primitive type in the JSON string but got " + data['database_name']);
        }
        // ensure the json data is a string
        if (data['datasource_name'] && !(typeof data['datasource_name'] === 'string' || data['datasource_name'] instanceof String)) {
            throw new Error("Expected the field `datasource_name` to be a primitive type in the JSON string but got " + data['datasource_name']);
        }
        // ensure the json data is a string
        if (data['datasource_type'] && !(typeof data['datasource_type'] === 'string' || data['datasource_type'] instanceof String)) {
            throw new Error("Expected the field `datasource_type` to be a primitive type in the JSON string but got " + data['datasource_type']);
        }
        // ensure the json data is a string
        if (data['schema'] && !(typeof data['schema'] === 'string' || data['schema'] instanceof String)) {
            throw new Error("Expected the field `schema` to be a primitive type in the JSON string but got " + data['schema']);
        }

        return true;
    }


}

Datasource.RequiredProperties = ["datasource_type"];

/**
 * Datasource name
 * @member {String} database_name
 */
Datasource.prototype['database_name'] = undefined;

/**
 * The datasource name.
 * @member {String} datasource_name
 */
Datasource.prototype['datasource_name'] = undefined;

/**
 * The type of dataset/datasource identified on `datasource_id`.
 * @member {module:model/Datasource.DatasourceTypeEnum} datasource_type
 */
Datasource.prototype['datasource_type'] = undefined;

/**
 * Datasource schema
 * @member {String} schema
 */
Datasource.prototype['schema'] = undefined;





/**
 * Allowed values for the <code>datasource_type</code> property.
 * @enum {String}
 * @readonly
 */
Datasource['DatasourceTypeEnum'] = {

    /**
     * value: "druid"
     * @const
     */
    "druid": "druid",

    /**
     * value: "table"
     * @const
     */
    "table": "table",

    /**
     * value: "view"
     * @const
     */
    "view": "view"
};



export default Datasource;

