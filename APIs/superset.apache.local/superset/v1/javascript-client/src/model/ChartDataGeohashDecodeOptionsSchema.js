/**
 * Superset
 * Superset
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ChartDataGeohashDecodeOptionsSchema model module.
 * @module model/ChartDataGeohashDecodeOptionsSchema
 * @version v1
 */
class ChartDataGeohashDecodeOptionsSchema {
    /**
     * Constructs a new <code>ChartDataGeohashDecodeOptionsSchema</code>.
     * @alias module:model/ChartDataGeohashDecodeOptionsSchema
     * @param geohash {String} Name of source column containing geohash string
     * @param latitude {String} Name of target column for decoded latitude
     * @param longitude {String} Name of target column for decoded longitude
     */
    constructor(geohash, latitude, longitude) { 
        
        ChartDataGeohashDecodeOptionsSchema.initialize(this, geohash, latitude, longitude);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, geohash, latitude, longitude) { 
        obj['geohash'] = geohash;
        obj['latitude'] = latitude;
        obj['longitude'] = longitude;
    }

    /**
     * Constructs a <code>ChartDataGeohashDecodeOptionsSchema</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChartDataGeohashDecodeOptionsSchema} obj Optional instance to populate.
     * @return {module:model/ChartDataGeohashDecodeOptionsSchema} The populated <code>ChartDataGeohashDecodeOptionsSchema</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChartDataGeohashDecodeOptionsSchema();

            if (data.hasOwnProperty('geohash')) {
                obj['geohash'] = ApiClient.convertToType(data['geohash'], 'String');
            }
            if (data.hasOwnProperty('latitude')) {
                obj['latitude'] = ApiClient.convertToType(data['latitude'], 'String');
            }
            if (data.hasOwnProperty('longitude')) {
                obj['longitude'] = ApiClient.convertToType(data['longitude'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChartDataGeohashDecodeOptionsSchema</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChartDataGeohashDecodeOptionsSchema</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChartDataGeohashDecodeOptionsSchema.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['geohash'] && !(typeof data['geohash'] === 'string' || data['geohash'] instanceof String)) {
            throw new Error("Expected the field `geohash` to be a primitive type in the JSON string but got " + data['geohash']);
        }
        // ensure the json data is a string
        if (data['latitude'] && !(typeof data['latitude'] === 'string' || data['latitude'] instanceof String)) {
            throw new Error("Expected the field `latitude` to be a primitive type in the JSON string but got " + data['latitude']);
        }
        // ensure the json data is a string
        if (data['longitude'] && !(typeof data['longitude'] === 'string' || data['longitude'] instanceof String)) {
            throw new Error("Expected the field `longitude` to be a primitive type in the JSON string but got " + data['longitude']);
        }

        return true;
    }


}

ChartDataGeohashDecodeOptionsSchema.RequiredProperties = ["geohash", "latitude", "longitude"];

/**
 * Name of source column containing geohash string
 * @member {String} geohash
 */
ChartDataGeohashDecodeOptionsSchema.prototype['geohash'] = undefined;

/**
 * Name of target column for decoded latitude
 * @member {String} latitude
 */
ChartDataGeohashDecodeOptionsSchema.prototype['latitude'] = undefined;

/**
 * Name of target column for decoded longitude
 * @member {String} longitude
 */
ChartDataGeohashDecodeOptionsSchema.prototype['longitude'] = undefined;






export default ChartDataGeohashDecodeOptionsSchema;

