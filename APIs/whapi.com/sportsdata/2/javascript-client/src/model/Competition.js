/**
 * SportsData API
 * The William Hill SportsData REST API is a collection of GET methods to provide William Hill product data such as sport, competition, event, market and selection data (including prices).
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Competition model module.
 * @module model/Competition
 * @version 2
 */
class Competition {
    /**
     * Constructs a new <code>Competition</code>.
     * @alias module:model/Competition
     * @param id {String} The Id of the Competition
     * @param name {String} The name of the competition
     * @param status {String} The status of the competition (A for Active, S for Suspended)
     */
    constructor(id, name, status) { 
        
        Competition.initialize(this, id, name, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, status) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>Competition</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Competition} obj Optional instance to populate.
     * @return {module:model/Competition} The populated <code>Competition</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Competition();

            if (data.hasOwnProperty('betInRunningDelay')) {
                obj['betInRunningDelay'] = ApiClient.convertToType(data['betInRunningDelay'], 'Number');
            }
            if (data.hasOwnProperty('cashinAvailable')) {
                obj['cashinAvailable'] = ApiClient.convertToType(data['cashinAvailable'], 'Boolean');
            }
            if (data.hasOwnProperty('channels')) {
                obj['channels'] = ApiClient.convertToType(data['channels'], ['String']);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('displayed')) {
                obj['displayed'] = ApiClient.convertToType(data['displayed'], 'Boolean');
            }
            if (data.hasOwnProperty('flags')) {
                obj['flags'] = ApiClient.convertToType(data['flags'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('isPublished')) {
                obj['isPublished'] = ApiClient.convertToType(data['isPublished'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('order')) {
                obj['order'] = ApiClient.convertToType(data['order'], 'Number');
            }
            if (data.hasOwnProperty('parentIds')) {
                obj['parentIds'] = ApiClient.convertToType(data['parentIds'], ['String']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Competition</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Competition</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Competition.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['channels'])) {
            throw new Error("Expected the field `channels` to be an array in the JSON data but got " + data['channels']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['flags'])) {
            throw new Error("Expected the field `flags` to be an array in the JSON data but got " + data['flags']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['parentIds'])) {
            throw new Error("Expected the field `parentIds` to be an array in the JSON data but got " + data['parentIds']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

Competition.RequiredProperties = ["id", "name", "status"];

/**
 * Delay in seconds between bet being sent and bet being placed
 * @member {Number} betInRunningDelay
 */
Competition.prototype['betInRunningDelay'] = undefined;

/**
 * Indicates whether cashin is available on the events of this competition
 * @member {Boolean} cashinAvailable
 */
Competition.prototype['cashinAvailable'] = undefined;

/**
 * A list of channels that apply to this competition
 * @member {Array.<String>} channels
 */
Competition.prototype['channels'] = undefined;

/**
 * The description of the competition
 * @member {String} description
 */
Competition.prototype['description'] = undefined;

/**
 * Whether the competition should be displayed or not
 * @member {Boolean} displayed
 */
Competition.prototype['displayed'] = undefined;

/**
 * A list of flags that apply to this competition
 * @member {Array.<String>} flags
 */
Competition.prototype['flags'] = undefined;

/**
 * The Id of the Competition
 * @member {String} id
 */
Competition.prototype['id'] = undefined;

/**
 * Indicates if the item is published or not
 * @member {Boolean} isPublished
 */
Competition.prototype['isPublished'] = undefined;

/**
 * The name of the competition
 * @member {String} name
 */
Competition.prototype['name'] = undefined;

/**
 * The display order of the competition
 * @member {Number} order
 */
Competition.prototype['order'] = undefined;

/**
 * A list of IDs of parent entities
 * @member {Array.<String>} parentIds
 */
Competition.prototype['parentIds'] = undefined;

/**
 * The status of the competition (A for Active, S for Suspended)
 * @member {String} status
 */
Competition.prototype['status'] = undefined;






export default Competition;

