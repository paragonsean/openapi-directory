/**
 * Bets API
 * The Bets API methods are used to place single, multiple and complex bets and to retrieve a customer’s bet history. When retrieving a customer’s bet history you can organize the bets from the betting history in terms of date, bet type and whether the bet is settled or not. You can also specify what fields to be included/excluded or return a list of all default fields the method returns. <br /><br /> The Bets API will also generate a bet delay if you’re placing a single/multiple bet in-Play by creating a time margin to negate the effects of major changes to the market (for example, goals during a football match). Note that in version 2 of our APIs, Bets API contains the functionality of both Bets API v1 and the Betslips API v1.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: platform@williamhill.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BetPlaced model module.
 * @module model/BetPlaced
 * @version 2.0.0
 */
class BetPlaced {
    /**
     * Constructs a new <code>BetPlaced</code>.
     * @alias module:model/BetPlaced
     * @param receipt {String} The unique ID of the bet receipt
     */
    constructor(receipt) { 
        
        BetPlaced.initialize(this, receipt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, receipt) { 
        obj['receipt'] = receipt;
    }

    /**
     * Constructs a <code>BetPlaced</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BetPlaced} obj Optional instance to populate.
     * @return {module:model/BetPlaced} The populated <code>BetPlaced</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BetPlaced();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('numLines')) {
                obj['numLines'] = ApiClient.convertToType(data['numLines'], 'Number');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'Number');
            }
            if (data.hasOwnProperty('placedDateTime')) {
                obj['placedDateTime'] = ApiClient.convertToType(data['placedDateTime'], 'String');
            }
            if (data.hasOwnProperty('receipt')) {
                obj['receipt'] = ApiClient.convertToType(data['receipt'], 'String');
            }
            if (data.hasOwnProperty('totalStake')) {
                obj['totalStake'] = ApiClient.convertToType(data['totalStake'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BetPlaced</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BetPlaced</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BetPlaced.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['placedDateTime'] && !(typeof data['placedDateTime'] === 'string' || data['placedDateTime'] instanceof String)) {
            throw new Error("Expected the field `placedDateTime` to be a primitive type in the JSON string but got " + data['placedDateTime']);
        }
        // ensure the json data is a string
        if (data['receipt'] && !(typeof data['receipt'] === 'string' || data['receipt'] instanceof String)) {
            throw new Error("Expected the field `receipt` to be a primitive type in the JSON string but got " + data['receipt']);
        }

        return true;
    }


}

BetPlaced.RequiredProperties = ["receipt"];

/**
 * The bet id
 * @member {String} id
 */
BetPlaced.prototype['id'] = undefined;

/**
 * The number of lines the bet consists of
 * @member {Number} numLines
 */
BetPlaced.prototype['numLines'] = undefined;

/**
 * Number of the bet if this is part of a multiple bet
 * @member {Number} number
 */
BetPlaced.prototype['number'] = undefined;

/**
 * The time the bet was placed. For example: 2013-01-30TT13:34:20. The value of this element can be replaced with the current timestamp if it was missing.
 * @member {String} placedDateTime
 */
BetPlaced.prototype['placedDateTime'] = undefined;

/**
 * The unique ID of the bet receipt
 * @member {String} receipt
 */
BetPlaced.prototype['receipt'] = undefined;

/**
 * The total amount staked on the bet
 * @member {Number} totalStake
 */
BetPlaced.prototype['totalStake'] = undefined;






export default BetPlaced;

