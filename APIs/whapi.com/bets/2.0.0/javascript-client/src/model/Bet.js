/**
 * Bets API
 * The Bets API methods are used to place single, multiple and complex bets and to retrieve a customer’s bet history. When retrieving a customer’s bet history you can organize the bets from the betting history in terms of date, bet type and whether the bet is settled or not. You can also specify what fields to be included/excluded or return a list of all default fields the method returns. <br /><br /> The Bets API will also generate a bet delay if you’re placing a single/multiple bet in-Play by creating a time margin to negate the effects of major changes to the market (for example, goals during a football match). Note that in version 2 of our APIs, Bets API contains the functionality of both Bets API v1 and the Betslips API v1.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: platform@williamhill.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Leg from './Leg';

/**
 * The Bet model module.
 * @module model/Bet
 * @version 2.0.0
 */
class Bet {
    /**
     * Constructs a new <code>Bet</code>.
     * @alias module:model/Bet
     * @param legs {Array.<module:model/Leg>} 
     * @param number {Number} The bet number
     * @param stake {Number} The amount of the stake placed on the bet
     * @param typeCode {String} The bet type code of the bet. For example: TBL (Treble)
     */
    constructor(legs, number, stake, typeCode) { 
        
        Bet.initialize(this, legs, number, stake, typeCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, legs, number, stake, typeCode) { 
        obj['legs'] = legs;
        obj['number'] = number;
        obj['stake'] = stake;
        obj['typeCode'] = typeCode;
    }

    /**
     * Constructs a <code>Bet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Bet} obj Optional instance to populate.
     * @return {module:model/Bet} The populated <code>Bet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Bet();

            if (data.hasOwnProperty('delayedBetId')) {
                obj['delayedBetId'] = ApiClient.convertToType(data['delayedBetId'], 'String');
            }
            if (data.hasOwnProperty('freeBetId')) {
                obj['freeBetId'] = ApiClient.convertToType(data['freeBetId'], 'String');
            }
            if (data.hasOwnProperty('legs')) {
                obj['legs'] = ApiClient.convertToType(data['legs'], [Leg]);
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'Number');
            }
            if (data.hasOwnProperty('stake')) {
                obj['stake'] = ApiClient.convertToType(data['stake'], 'Number');
            }
            if (data.hasOwnProperty('typeCode')) {
                obj['typeCode'] = ApiClient.convertToType(data['typeCode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Bet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Bet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Bet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['delayedBetId'] && !(typeof data['delayedBetId'] === 'string' || data['delayedBetId'] instanceof String)) {
            throw new Error("Expected the field `delayedBetId` to be a primitive type in the JSON string but got " + data['delayedBetId']);
        }
        // ensure the json data is a string
        if (data['freeBetId'] && !(typeof data['freeBetId'] === 'string' || data['freeBetId'] instanceof String)) {
            throw new Error("Expected the field `freeBetId` to be a primitive type in the JSON string but got " + data['freeBetId']);
        }
        if (data['legs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['legs'])) {
                throw new Error("Expected the field `legs` to be an array in the JSON data but got " + data['legs']);
            }
            // validate the optional field `legs` (array)
            for (const item of data['legs']) {
                Leg.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['typeCode'] && !(typeof data['typeCode'] === 'string' || data['typeCode'] instanceof String)) {
            throw new Error("Expected the field `typeCode` to be a primitive type in the JSON string but got " + data['typeCode']);
        }

        return true;
    }


}

Bet.RequiredProperties = ["legs", "number", "stake", "typeCode"];

/**
 * The delayed bet identifier
 * @member {String} delayedBetId
 */
Bet.prototype['delayedBetId'] = undefined;

/**
 * The ID number of the free bet token if used in conjunction with this bet
 * @member {String} freeBetId
 */
Bet.prototype['freeBetId'] = undefined;

/**
 * @member {Array.<module:model/Leg>} legs
 */
Bet.prototype['legs'] = undefined;

/**
 * The bet number
 * @member {Number} number
 */
Bet.prototype['number'] = undefined;

/**
 * The amount of the stake placed on the bet
 * @member {Number} stake
 */
Bet.prototype['stake'] = undefined;

/**
 * The bet type code of the bet. For example: TBL (Treble)
 * @member {String} typeCode
 */
Bet.prototype['typeCode'] = undefined;






export default Bet;

