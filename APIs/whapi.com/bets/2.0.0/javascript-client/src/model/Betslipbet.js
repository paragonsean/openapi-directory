/**
 * Bets API
 * The Bets API methods are used to place single, multiple and complex bets and to retrieve a customer’s bet history. When retrieving a customer’s bet history you can organize the bets from the betting history in terms of date, bet type and whether the bet is settled or not. You can also specify what fields to be included/excluded or return a list of all default fields the method returns. <br /><br /> The Bets API will also generate a bet delay if you’re placing a single/multiple bet in-Play by creating a time margin to negate the effects of major changes to the market (for example, goals during a football match). Note that in version 2 of our APIs, Bets API contains the functionality of both Bets API v1 and the Betslips API v1.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: platform@williamhill.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Betslipleg from './Betslipleg';
import FreeBetDetail from './FreeBetDetail';

/**
 * The Betslipbet model module.
 * @module model/Betslipbet
 * @version 2.0.0
 */
class Betslipbet {
    /**
     * Constructs a new <code>Betslipbet</code>.
     * @alias module:model/Betslipbet
     * @param legs {Array.<module:model/Betslipleg>} 
     * @param number {Number} The bet number
     * @param typeCode {String} The bet type code of the bet. For example: TBL (Treble)
     */
    constructor(legs, number, typeCode) { 
        
        Betslipbet.initialize(this, legs, number, typeCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, legs, number, typeCode) { 
        obj['legs'] = legs;
        obj['number'] = number;
        obj['typeCode'] = typeCode;
    }

    /**
     * Constructs a <code>Betslipbet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Betslipbet} obj Optional instance to populate.
     * @return {module:model/Betslipbet} The populated <code>Betslipbet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Betslipbet();

            if (data.hasOwnProperty('betMultiplier')) {
                obj['betMultiplier'] = ApiClient.convertToType(data['betMultiplier'], 'Number');
            }
            if (data.hasOwnProperty('freeBets')) {
                obj['freeBets'] = ApiClient.convertToType(data['freeBets'], [FreeBetDetail]);
            }
            if (data.hasOwnProperty('legs')) {
                obj['legs'] = ApiClient.convertToType(data['legs'], [Betslipleg]);
            }
            if (data.hasOwnProperty('maxStake')) {
                obj['maxStake'] = ApiClient.convertToType(data['maxStake'], 'Number');
            }
            if (data.hasOwnProperty('minStake')) {
                obj['minStake'] = ApiClient.convertToType(data['minStake'], 'Number');
            }
            if (data.hasOwnProperty('numLines')) {
                obj['numLines'] = ApiClient.convertToType(data['numLines'], 'Number');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'Number');
            }
            if (data.hasOwnProperty('typeCode')) {
                obj['typeCode'] = ApiClient.convertToType(data['typeCode'], 'String');
            }
            if (data.hasOwnProperty('typeName')) {
                obj['typeName'] = ApiClient.convertToType(data['typeName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Betslipbet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Betslipbet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Betslipbet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['freeBets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['freeBets'])) {
                throw new Error("Expected the field `freeBets` to be an array in the JSON data but got " + data['freeBets']);
            }
            // validate the optional field `freeBets` (array)
            for (const item of data['freeBets']) {
                FreeBetDetail.validateJSON(item);
            };
        }
        if (data['legs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['legs'])) {
                throw new Error("Expected the field `legs` to be an array in the JSON data but got " + data['legs']);
            }
            // validate the optional field `legs` (array)
            for (const item of data['legs']) {
                Betslipleg.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['typeCode'] && !(typeof data['typeCode'] === 'string' || data['typeCode'] instanceof String)) {
            throw new Error("Expected the field `typeCode` to be a primitive type in the JSON string but got " + data['typeCode']);
        }
        // ensure the json data is a string
        if (data['typeName'] && !(typeof data['typeName'] === 'string' || data['typeName'] instanceof String)) {
            throw new Error("Expected the field `typeName` to be a primitive type in the JSON string but got " + data['typeName']);
        }

        return true;
    }


}

Betslipbet.RequiredProperties = ["legs", "number", "typeCode"];

/**
 * The total aggregated odds, as a decimal, of the multiple bet placed
 * @member {Number} betMultiplier
 */
Betslipbet.prototype['betMultiplier'] = undefined;

/**
 * Free bets that can be used with this bet
 * @member {Array.<module:model/FreeBetDetail>} freeBets
 */
Betslipbet.prototype['freeBets'] = undefined;

/**
 * @member {Array.<module:model/Betslipleg>} legs
 */
Betslipbet.prototype['legs'] = undefined;

/**
 * Maximum stake allowable on the bet. For example: 2500.00 (GBP)
 * @member {Number} maxStake
 */
Betslipbet.prototype['maxStake'] = undefined;

/**
 * Minumum stake allowable on the bet. For example: 0.05 (GBP)
 * @member {Number} minStake
 */
Betslipbet.prototype['minStake'] = undefined;

/**
 * The number of lines (selections) of a bet
 * @member {Number} numLines
 */
Betslipbet.prototype['numLines'] = undefined;

/**
 * The bet number
 * @member {Number} number
 */
Betslipbet.prototype['number'] = undefined;

/**
 * The bet type code of the bet. For example: TBL (Treble)
 * @member {String} typeCode
 */
Betslipbet.prototype['typeCode'] = undefined;

/**
 * The TypeCode of the bet. For example: TBL (Treble)
 * @member {String} typeName
 */
Betslipbet.prototype['typeName'] = undefined;






export default Betslipbet;

