# bets_api

BetsApi - JavaScript client for bets_api
The Bets API methods are used to place single, multiple and complex bets and to retrieve a customer’s bet history. When retrieving a customer’s bet history you can organize the bets from the betting history in terms of date, bet type and whether the bet is settled or not. You can also specify what fields to be included/excluded or return a list of all default fields the method returns. <br /><br /> The Bets API will also generate a bet delay if you’re placing a single/multiple bet in-Play by creating a time margin to negate the effects of major changes to the market (for example, goals during a football match). Note that in version 2 of our APIs, Bets API contains the functionality of both Bets API v1 and the Betslips API v1.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2.0.0
- Package version: 2.0.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [http://developer.williamhill.com](http://developer.williamhill.com)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install bets_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your bets_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var BetsApi = require('bets_api');


var api = new BetsApi.BetsApi()
var apiKey = "apiKey_example"; // {String} A unique identifier of your application that is generated by the API portal.
var apiSecret = "apiSecret_example"; // {String} Another unique identifier for your application.
var apiTicket = "apiTicket_example"; // {String} The ticket obtained from the sessions API
var betId = "betId_example"; // {String} The identifier of the bet
var cashInValue = 3.4; // {Number} The cash in value of the bet
var cashinBetDelayId = "cashinBetDelayId_example"; // {String} The ID of this bet delay
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.cashin(apiKey, apiSecret, apiTicket, betId, cashInValue, cashinBetDelayId, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://sandbox.whapi.com/v2/bets*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*BetsApi.BetsApi* | [**cashin**](docs/BetsApi.md#cashin) | **PUT** /{betId}/cashin | Allows a trusted application to cash in a bet (take a return on a bet) on behalf of the customer
*BetsApi.BetsApi* | [**getBetHistory**](docs/BetsApi.md#getBetHistory) | **GET** /history | Retrieves the customer’s bet history.
*BetsApi.BetsApi* | [**getFreeBets**](docs/BetsApi.md#getFreeBets) | **GET** /freebets | Returns available free bets
*BetsApi.BetsApi* | [**placeComplexBet**](docs/BetsApi.md#placeComplexBet) | **POST** /bet/complex | Places a multiple or a complex bet.
*BetsApi.BetsApi* | [**placeSingleBet**](docs/BetsApi.md#placeSingleBet) | **POST** /bet/single | Places a single bet
*BetsApi.BetsApi* | [**validateBetslip**](docs/BetsApi.md#validateBetslip) | **POST** /betslips | 


## Documentation for Models

 - [BetsApi.Bet](docs/Bet.md)
 - [BetsApi.BetDelayed](docs/BetDelayed.md)
 - [BetsApi.BetHistoryResponse](docs/BetHistoryResponse.md)
 - [BetsApi.BetPlaced](docs/BetPlaced.md)
 - [BetsApi.BetSlipRequest](docs/BetSlipRequest.md)
 - [BetsApi.BetSlipResponse](docs/BetSlipResponse.md)
 - [BetsApi.Betslipbet](docs/Betslipbet.md)
 - [BetsApi.Betslipleg](docs/Betslipleg.md)
 - [BetsApi.Betslippart](docs/Betslippart.md)
 - [BetsApi.CashInResponse](docs/CashInResponse.md)
 - [BetsApi.ComplexBetRequestBody](docs/ComplexBetRequestBody.md)
 - [BetsApi.Error](docs/Error.md)
 - [BetsApi.Errors](docs/Errors.md)
 - [BetsApi.FreeBetDetail](docs/FreeBetDetail.md)
 - [BetsApi.Leg](docs/Leg.md)
 - [BetsApi.Part](docs/Part.md)
 - [BetsApi.PlacedBet](docs/PlacedBet.md)
 - [BetsApi.PlacedBetLeg](docs/PlacedBetLeg.md)
 - [BetsApi.PlacedBetPart](docs/PlacedBetPart.md)
 - [BetsApi.PriceFormatted](docs/PriceFormatted.md)
 - [BetsApi.SingleBetRequestBody](docs/SingleBetRequestBody.md)
 - [BetsApi.Solution](docs/Solution.md)


## Documentation for Authorization

Endpoints do not require authorization.

