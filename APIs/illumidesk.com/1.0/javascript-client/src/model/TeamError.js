/**
 * IllumiDesk
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TeamError model module.
 * @module model/TeamError
 * @version 1.0
 */
class TeamError {
    /**
     * Constructs a new <code>TeamError</code>.
     * @alias module:model/TeamError
     */
    constructor() { 
        
        TeamError.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TeamError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TeamError} obj Optional instance to populate.
     * @return {module:model/TeamError} The populated <code>TeamError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TeamError();

            if (data.hasOwnProperty('avatar')) {
                obj['avatar'] = ApiClient.convertToType(data['avatar'], ['String']);
            }
            if (data.hasOwnProperty('avatar_url')) {
                obj['avatar_url'] = ApiClient.convertToType(data['avatar_url'], ['String']);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], ['String']);
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], ['String']);
            }
            if (data.hasOwnProperty('non_field_errors')) {
                obj['non_field_errors'] = ApiClient.convertToType(data['non_field_errors'], ['String']);
            }
            if (data.hasOwnProperty('website')) {
                obj['website'] = ApiClient.convertToType(data['website'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TeamError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TeamError</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['avatar'])) {
            throw new Error("Expected the field `avatar` to be an array in the JSON data but got " + data['avatar']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['avatar_url'])) {
            throw new Error("Expected the field `avatar_url` to be an array in the JSON data but got " + data['avatar_url']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['description'])) {
            throw new Error("Expected the field `description` to be an array in the JSON data but got " + data['description']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['id'])) {
            throw new Error("Expected the field `id` to be an array in the JSON data but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['location'])) {
            throw new Error("Expected the field `location` to be an array in the JSON data but got " + data['location']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['name'])) {
            throw new Error("Expected the field `name` to be an array in the JSON data but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['non_field_errors'])) {
            throw new Error("Expected the field `non_field_errors` to be an array in the JSON data but got " + data['non_field_errors']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['website'])) {
            throw new Error("Expected the field `website` to be an array in the JSON data but got " + data['website']);
        }

        return true;
    }


}



/**
 * Avatar field errors.
 * @member {Array.<String>} avatar
 */
TeamError.prototype['avatar'] = undefined;

/**
 * Avatar url field errors.
 * @member {Array.<String>} avatar_url
 */
TeamError.prototype['avatar_url'] = undefined;

/**
 * Description field errors.
 * @member {Array.<String>} description
 */
TeamError.prototype['description'] = undefined;

/**
 * Id field errors.
 * @member {Array.<String>} id
 */
TeamError.prototype['id'] = undefined;

/**
 * Location field errors.
 * @member {Array.<String>} location
 */
TeamError.prototype['location'] = undefined;

/**
 * Name field errors.
 * @member {Array.<String>} name
 */
TeamError.prototype['name'] = undefined;

/**
 * Errors not connected to any field.
 * @member {Array.<String>} non_field_errors
 */
TeamError.prototype['non_field_errors'] = undefined;

/**
 * Website field errors.
 * @member {Array.<String>} website
 */
TeamError.prototype['website'] = undefined;






export default TeamError;

