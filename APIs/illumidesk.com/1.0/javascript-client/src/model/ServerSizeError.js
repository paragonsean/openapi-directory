/**
 * IllumiDesk
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ServerSizeError model module.
 * @module model/ServerSizeError
 * @version 1.0
 */
class ServerSizeError {
    /**
     * Constructs a new <code>ServerSizeError</code>.
     * @alias module:model/ServerSizeError
     */
    constructor() { 
        
        ServerSizeError.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ServerSizeError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServerSizeError} obj Optional instance to populate.
     * @return {module:model/ServerSizeError} The populated <code>ServerSizeError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServerSizeError();

            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], ['String']);
            }
            if (data.hasOwnProperty('cpu')) {
                obj['cpu'] = ApiClient.convertToType(data['cpu'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], ['String']);
            }
            if (data.hasOwnProperty('memory')) {
                obj['memory'] = ApiClient.convertToType(data['memory'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], ['String']);
            }
            if (data.hasOwnProperty('non_field_errors')) {
                obj['non_field_errors'] = ApiClient.convertToType(data['non_field_errors'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServerSizeError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServerSizeError</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['active'])) {
            throw new Error("Expected the field `active` to be an array in the JSON data but got " + data['active']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['cpu'])) {
            throw new Error("Expected the field `cpu` to be an array in the JSON data but got " + data['cpu']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['id'])) {
            throw new Error("Expected the field `id` to be an array in the JSON data but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['memory'])) {
            throw new Error("Expected the field `memory` to be an array in the JSON data but got " + data['memory']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['name'])) {
            throw new Error("Expected the field `name` to be an array in the JSON data but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['non_field_errors'])) {
            throw new Error("Expected the field `non_field_errors` to be an array in the JSON data but got " + data['non_field_errors']);
        }

        return true;
    }


}



/**
 * Active field errors.
 * @member {Array.<String>} active
 */
ServerSizeError.prototype['active'] = undefined;

/**
 * CPU field errors.
 * @member {Array.<String>} cpu
 */
ServerSizeError.prototype['cpu'] = undefined;

/**
 * Id field errors.
 * @member {Array.<String>} id
 */
ServerSizeError.prototype['id'] = undefined;

/**
 * Memory field errors.
 * @member {Array.<String>} memory
 */
ServerSizeError.prototype['memory'] = undefined;

/**
 * Name field errors.
 * @member {Array.<String>} name
 */
ServerSizeError.prototype['name'] = undefined;

/**
 * Errors not connected to any field.
 * @member {Array.<String>} non_field_errors
 */
ServerSizeError.prototype['non_field_errors'] = undefined;






export default ServerSizeError;

