/**
 * IllumiDesk
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApplicationError model module.
 * @module model/ApplicationError
 * @version 1.0
 */
class ApplicationError {
    /**
     * Constructs a new <code>ApplicationError</code>.
     * @alias module:model/ApplicationError
     */
    constructor() { 
        
        ApplicationError.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ApplicationError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApplicationError} obj Optional instance to populate.
     * @return {module:model/ApplicationError} The populated <code>ApplicationError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApplicationError();

            if (data.hasOwnProperty('authorization_grant_type')) {
                obj['authorization_grant_type'] = ApiClient.convertToType(data['authorization_grant_type'], ['String']);
            }
            if (data.hasOwnProperty('client_type')) {
                obj['client_type'] = ApiClient.convertToType(data['client_type'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], ['String']);
            }
            if (data.hasOwnProperty('non_field_errors')) {
                obj['non_field_errors'] = ApiClient.convertToType(data['non_field_errors'], ['String']);
            }
            if (data.hasOwnProperty('redirect_uris')) {
                obj['redirect_uris'] = ApiClient.convertToType(data['redirect_uris'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApplicationError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApplicationError</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['authorization_grant_type'])) {
            throw new Error("Expected the field `authorization_grant_type` to be an array in the JSON data but got " + data['authorization_grant_type']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['client_type'])) {
            throw new Error("Expected the field `client_type` to be an array in the JSON data but got " + data['client_type']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['name'])) {
            throw new Error("Expected the field `name` to be an array in the JSON data but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['non_field_errors'])) {
            throw new Error("Expected the field `non_field_errors` to be an array in the JSON data but got " + data['non_field_errors']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['redirect_uris'])) {
            throw new Error("Expected the field `redirect_uris` to be an array in the JSON data but got " + data['redirect_uris']);
        }

        return true;
    }


}



/**
 * Authorization grant type field errors.
 * @member {Array.<String>} authorization_grant_type
 */
ApplicationError.prototype['authorization_grant_type'] = undefined;

/**
 * Client type field errors.
 * @member {Array.<String>} client_type
 */
ApplicationError.prototype['client_type'] = undefined;

/**
 * Name field errors.
 * @member {Array.<String>} name
 */
ApplicationError.prototype['name'] = undefined;

/**
 * Errors not connected to any field.
 * @member {Array.<String>} non_field_errors
 */
ApplicationError.prototype['non_field_errors'] = undefined;

/**
 * Redirect uris field errors.
 * @member {Array.<String>} redirect_uris
 */
ApplicationError.prototype['redirect_uris'] = undefined;






export default ApplicationError;

