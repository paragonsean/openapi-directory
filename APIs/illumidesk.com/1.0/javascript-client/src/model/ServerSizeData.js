/**
 * IllumiDesk
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ServerSizeData model module.
 * @module model/ServerSizeData
 * @version 1.0
 */
class ServerSizeData {
    /**
     * Constructs a new <code>ServerSizeData</code>.
     * @alias module:model/ServerSizeData
     * @param active {Boolean} Active or not active.
     * @param cpu {Number} CPU set for server size.
     * @param memory {Number} Memory set for server size.
     * @param name {String} Server size name.
     */
    constructor(active, cpu, memory, name) { 
        
        ServerSizeData.initialize(this, active, cpu, memory, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, active, cpu, memory, name) { 
        obj['active'] = active;
        obj['cpu'] = cpu;
        obj['memory'] = memory;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>ServerSizeData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServerSizeData} obj Optional instance to populate.
     * @return {module:model/ServerSizeData} The populated <code>ServerSizeData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServerSizeData();

            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('cpu')) {
                obj['cpu'] = ApiClient.convertToType(data['cpu'], 'Number');
            }
            if (data.hasOwnProperty('memory')) {
                obj['memory'] = ApiClient.convertToType(data['memory'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServerSizeData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServerSizeData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ServerSizeData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

ServerSizeData.RequiredProperties = ["active", "cpu", "memory", "name"];

/**
 * Active or not active.
 * @member {Boolean} active
 */
ServerSizeData.prototype['active'] = undefined;

/**
 * CPU set for server size.
 * @member {Number} cpu
 */
ServerSizeData.prototype['cpu'] = undefined;

/**
 * Memory set for server size.
 * @member {Number} memory
 */
ServerSizeData.prototype['memory'] = undefined;

/**
 * Server size name.
 * @member {String} name
 */
ServerSizeData.prototype['name'] = undefined;






export default ServerSizeData;

