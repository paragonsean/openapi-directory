/**
 * IllumiDesk
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SshTunnel model module.
 * @module model/SshTunnel
 * @version 1.0
 */
class SshTunnel {
    /**
     * Constructs a new <code>SshTunnel</code>.
     * @alias module:model/SshTunnel
     * @param endpoint {String} SSH tunnel destination endpoint.
     * @param host {String} Host, usually IPv4, for SSH tunnel.
     * @param localPort {Number} Local source port for SSH tunnel.
     * @param name {String} SSH tunnel name.
     * @param remotePort {Number} Remote port to establish SSH tunnel.
     * @param username {String} SSH tunnel user name.
     */
    constructor(endpoint, host, localPort, name, remotePort, username) { 
        
        SshTunnel.initialize(this, endpoint, host, localPort, name, remotePort, username);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endpoint, host, localPort, name, remotePort, username) { 
        obj['endpoint'] = endpoint;
        obj['host'] = host;
        obj['local_port'] = localPort;
        obj['name'] = name;
        obj['remote_port'] = remotePort;
        obj['username'] = username;
    }

    /**
     * Constructs a <code>SshTunnel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SshTunnel} obj Optional instance to populate.
     * @return {module:model/SshTunnel} The populated <code>SshTunnel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SshTunnel();

            if (data.hasOwnProperty('endpoint')) {
                obj['endpoint'] = ApiClient.convertToType(data['endpoint'], 'String');
            }
            if (data.hasOwnProperty('host')) {
                obj['host'] = ApiClient.convertToType(data['host'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('local_port')) {
                obj['local_port'] = ApiClient.convertToType(data['local_port'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('remote_port')) {
                obj['remote_port'] = ApiClient.convertToType(data['remote_port'], 'Number');
            }
            if (data.hasOwnProperty('server')) {
                obj['server'] = ApiClient.convertToType(data['server'], 'String');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SshTunnel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SshTunnel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SshTunnel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['endpoint'] && !(typeof data['endpoint'] === 'string' || data['endpoint'] instanceof String)) {
            throw new Error("Expected the field `endpoint` to be a primitive type in the JSON string but got " + data['endpoint']);
        }
        // ensure the json data is a string
        if (data['host'] && !(typeof data['host'] === 'string' || data['host'] instanceof String)) {
            throw new Error("Expected the field `host` to be a primitive type in the JSON string but got " + data['host']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['server'] && !(typeof data['server'] === 'string' || data['server'] instanceof String)) {
            throw new Error("Expected the field `server` to be a primitive type in the JSON string but got " + data['server']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }

        return true;
    }


}

SshTunnel.RequiredProperties = ["endpoint", "host", "local_port", "name", "remote_port", "username"];

/**
 * SSH tunnel destination endpoint.
 * @member {String} endpoint
 */
SshTunnel.prototype['endpoint'] = undefined;

/**
 * Host, usually IPv4, for SSH tunnel.
 * @member {String} host
 */
SshTunnel.prototype['host'] = undefined;

/**
 * SSH tunnel unique identifier in UUID format.
 * @member {String} id
 */
SshTunnel.prototype['id'] = undefined;

/**
 * Local source port for SSH tunnel.
 * @member {Number} local_port
 */
SshTunnel.prototype['local_port'] = undefined;

/**
 * SSH tunnel name.
 * @member {String} name
 */
SshTunnel.prototype['name'] = undefined;

/**
 * Remote port to establish SSH tunnel.
 * @member {Number} remote_port
 */
SshTunnel.prototype['remote_port'] = undefined;

/**
 * Server name.
 * @member {String} server
 */
SshTunnel.prototype['server'] = undefined;

/**
 * SSH tunnel user name.
 * @member {String} username
 */
SshTunnel.prototype['username'] = undefined;






export default SshTunnel;

