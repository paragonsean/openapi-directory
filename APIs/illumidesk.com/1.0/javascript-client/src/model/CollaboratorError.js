/**
 * IllumiDesk
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CollaboratorError model module.
 * @module model/CollaboratorError
 * @version 1.0
 */
class CollaboratorError {
    /**
     * Constructs a new <code>CollaboratorError</code>.
     * @alias module:model/CollaboratorError
     * @param member {Array.<String>} Member field errors.
     * @param permissions {Array.<String>} Permissions field errors.
     */
    constructor(member, permissions) { 
        
        CollaboratorError.initialize(this, member, permissions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, member, permissions) { 
        obj['member'] = member;
        obj['permissions'] = permissions;
    }

    /**
     * Constructs a <code>CollaboratorError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CollaboratorError} obj Optional instance to populate.
     * @return {module:model/CollaboratorError} The populated <code>CollaboratorError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CollaboratorError();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], ['String']);
            }
            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], ['String']);
            }
            if (data.hasOwnProperty('joined')) {
                obj['joined'] = ApiClient.convertToType(data['joined'], ['String']);
            }
            if (data.hasOwnProperty('last_name')) {
                obj['last_name'] = ApiClient.convertToType(data['last_name'], ['String']);
            }
            if (data.hasOwnProperty('member')) {
                obj['member'] = ApiClient.convertToType(data['member'], ['String']);
            }
            if (data.hasOwnProperty('non_field_errors')) {
                obj['non_field_errors'] = ApiClient.convertToType(data['non_field_errors'], ['String']);
            }
            if (data.hasOwnProperty('owner')) {
                obj['owner'] = ApiClient.convertToType(data['owner'], ['String']);
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], ['String']);
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CollaboratorError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CollaboratorError</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CollaboratorError.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['email'])) {
            throw new Error("Expected the field `email` to be an array in the JSON data but got " + data['email']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['first_name'])) {
            throw new Error("Expected the field `first_name` to be an array in the JSON data but got " + data['first_name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['id'])) {
            throw new Error("Expected the field `id` to be an array in the JSON data but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['joined'])) {
            throw new Error("Expected the field `joined` to be an array in the JSON data but got " + data['joined']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['last_name'])) {
            throw new Error("Expected the field `last_name` to be an array in the JSON data but got " + data['last_name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['member'])) {
            throw new Error("Expected the field `member` to be an array in the JSON data but got " + data['member']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['non_field_errors'])) {
            throw new Error("Expected the field `non_field_errors` to be an array in the JSON data but got " + data['non_field_errors']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['owner'])) {
            throw new Error("Expected the field `owner` to be an array in the JSON data but got " + data['owner']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['permissions'])) {
            throw new Error("Expected the field `permissions` to be an array in the JSON data but got " + data['permissions']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['username'])) {
            throw new Error("Expected the field `username` to be an array in the JSON data but got " + data['username']);
        }

        return true;
    }


}

CollaboratorError.RequiredProperties = ["member", "permissions"];

/**
 * Email field errors.
 * @member {Array.<String>} email
 */
CollaboratorError.prototype['email'] = undefined;

/**
 * First name field errors.
 * @member {Array.<String>} first_name
 */
CollaboratorError.prototype['first_name'] = undefined;

/**
 * Id field errors this
 * @member {Array.<String>} id
 */
CollaboratorError.prototype['id'] = undefined;

/**
 * Joined field errors.
 * @member {Array.<String>} joined
 */
CollaboratorError.prototype['joined'] = undefined;

/**
 * Last name field errors.
 * @member {Array.<String>} last_name
 */
CollaboratorError.prototype['last_name'] = undefined;

/**
 * Member field errors.
 * @member {Array.<String>} member
 */
CollaboratorError.prototype['member'] = undefined;

/**
 * Errors not connected to any field.
 * @member {Array.<String>} non_field_errors
 */
CollaboratorError.prototype['non_field_errors'] = undefined;

/**
 * Owner field errors.
 * @member {Array.<String>} owner
 */
CollaboratorError.prototype['owner'] = undefined;

/**
 * Permissions field errors.
 * @member {Array.<String>} permissions
 */
CollaboratorError.prototype['permissions'] = undefined;

/**
 * Username field errors.
 * @member {Array.<String>} username
 */
CollaboratorError.prototype['username'] = undefined;






export default CollaboratorError;

