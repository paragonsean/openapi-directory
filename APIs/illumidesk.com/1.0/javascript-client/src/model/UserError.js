/**
 * IllumiDesk
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UserProfileError from './UserProfileError';

/**
 * The UserError model module.
 * @module model/UserError
 * @version 1.0
 */
class UserError {
    /**
     * Constructs a new <code>UserError</code>.
     * @alias module:model/UserError
     */
    constructor() { 
        
        UserError.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>UserError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserError} obj Optional instance to populate.
     * @return {module:model/UserError} The populated <code>UserError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserError();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], ['String']);
            }
            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], ['String']);
            }
            if (data.hasOwnProperty('last_name')) {
                obj['last_name'] = ApiClient.convertToType(data['last_name'], ['String']);
            }
            if (data.hasOwnProperty('non_field_errors')) {
                obj['non_field_errors'] = ApiClient.convertToType(data['non_field_errors'], ['String']);
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], ['String']);
            }
            if (data.hasOwnProperty('profile')) {
                obj['profile'] = UserProfileError.constructFromObject(data['profile']);
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserError</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['email'])) {
            throw new Error("Expected the field `email` to be an array in the JSON data but got " + data['email']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['first_name'])) {
            throw new Error("Expected the field `first_name` to be an array in the JSON data but got " + data['first_name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['id'])) {
            throw new Error("Expected the field `id` to be an array in the JSON data but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['last_name'])) {
            throw new Error("Expected the field `last_name` to be an array in the JSON data but got " + data['last_name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['non_field_errors'])) {
            throw new Error("Expected the field `non_field_errors` to be an array in the JSON data but got " + data['non_field_errors']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['password'])) {
            throw new Error("Expected the field `password` to be an array in the JSON data but got " + data['password']);
        }
        // validate the optional field `profile`
        if (data['profile']) { // data not null
          UserProfileError.validateJSON(data['profile']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['username'])) {
            throw new Error("Expected the field `username` to be an array in the JSON data but got " + data['username']);
        }

        return true;
    }


}



/**
 * Email field errors.
 * @member {Array.<String>} email
 */
UserError.prototype['email'] = undefined;

/**
 * First name field errors.
 * @member {Array.<String>} first_name
 */
UserError.prototype['first_name'] = undefined;

/**
 * id field errors.
 * @member {Array.<String>} id
 */
UserError.prototype['id'] = undefined;

/**
 * Last name field errors.
 * @member {Array.<String>} last_name
 */
UserError.prototype['last_name'] = undefined;

/**
 * Errors not connected to any field.
 * @member {Array.<String>} non_field_errors
 */
UserError.prototype['non_field_errors'] = undefined;

/**
 * Password field errors.
 * @member {Array.<String>} password
 */
UserError.prototype['password'] = undefined;

/**
 * @member {module:model/UserProfileError} profile
 */
UserError.prototype['profile'] = undefined;

/**
 * Username field errors.
 * @member {Array.<String>} username
 */
UserError.prototype['username'] = undefined;






export default UserError;

