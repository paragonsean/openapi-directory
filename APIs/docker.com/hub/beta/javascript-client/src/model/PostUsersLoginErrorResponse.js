/**
 * Docker HUB API
 * Docker Hub is a service provided by Docker for finding and sharing container images with your team.  It is the world's largest library and community for container images.  In addition to the [Docker Hub UI](https://docs.docker.com/docker-hub/) and [Docker Hub CLI tool](https://github.com/docker/hub-tool#readme) (currently experimental),  Docker provides an API that allows you to interact with Docker Hub.  Browse through the Docker Hub API documentation to explore the supported endpoints. 
 *
 * The version of the OpenAPI document: beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PostUsersLoginErrorResponse model module.
 * @module model/PostUsersLoginErrorResponse
 * @version beta
 */
class PostUsersLoginErrorResponse {
    /**
     * Constructs a new <code>PostUsersLoginErrorResponse</code>.
     * failed user login response or second factor required
     * @alias module:model/PostUsersLoginErrorResponse
     * @param detail {String} Description of the error.
     */
    constructor(detail) { 
        
        PostUsersLoginErrorResponse.initialize(this, detail);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, detail) { 
        obj['detail'] = detail;
    }

    /**
     * Constructs a <code>PostUsersLoginErrorResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostUsersLoginErrorResponse} obj Optional instance to populate.
     * @return {module:model/PostUsersLoginErrorResponse} The populated <code>PostUsersLoginErrorResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostUsersLoginErrorResponse();

            if (data.hasOwnProperty('detail')) {
                obj['detail'] = ApiClient.convertToType(data['detail'], 'String');
            }
            if (data.hasOwnProperty('login_2fa_token')) {
                obj['login_2fa_token'] = ApiClient.convertToType(data['login_2fa_token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostUsersLoginErrorResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostUsersLoginErrorResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostUsersLoginErrorResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['detail'] && !(typeof data['detail'] === 'string' || data['detail'] instanceof String)) {
            throw new Error("Expected the field `detail` to be a primitive type in the JSON string but got " + data['detail']);
        }
        // ensure the json data is a string
        if (data['login_2fa_token'] && !(typeof data['login_2fa_token'] === 'string' || data['login_2fa_token'] instanceof String)) {
            throw new Error("Expected the field `login_2fa_token` to be a primitive type in the JSON string but got " + data['login_2fa_token']);
        }

        return true;
    }


}

PostUsersLoginErrorResponse.RequiredProperties = ["detail"];

/**
 * Description of the error.
 * @member {String} detail
 */
PostUsersLoginErrorResponse.prototype['detail'] = undefined;

/**
 * Short time lived token to be used on `/v2/users/2fa-login` to complete the authentication. This field is present only if 2FA is enabled.
 * @member {String} login_2fa_token
 */
PostUsersLoginErrorResponse.prototype['login_2fa_token'] = undefined;






export default PostUsersLoginErrorResponse;

