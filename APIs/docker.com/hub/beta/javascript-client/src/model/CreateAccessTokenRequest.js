/**
 * Docker HUB API
 * Docker Hub is a service provided by Docker for finding and sharing container images with your team.  It is the world's largest library and community for container images.  In addition to the [Docker Hub UI](https://docs.docker.com/docker-hub/) and [Docker Hub CLI tool](https://github.com/docker/hub-tool#readme) (currently experimental),  Docker provides an API that allows you to interact with Docker Hub.  Browse through the Docker Hub API documentation to explore the supported endpoints. 
 *
 * The version of the OpenAPI document: beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateAccessTokenRequest model module.
 * @module model/CreateAccessTokenRequest
 * @version beta
 */
class CreateAccessTokenRequest {
    /**
     * Constructs a new <code>CreateAccessTokenRequest</code>.
     * @alias module:model/CreateAccessTokenRequest
     * @param scopes {Array.<String>} Valid scopes: \"repo:admin\", \"repo:write\", \"repo:read\", \"repo:public_read\" 
     * @param tokenLabel {String} Friendly name for you to identify the token.
     */
    constructor(scopes, tokenLabel) { 
        
        CreateAccessTokenRequest.initialize(this, scopes, tokenLabel);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, scopes, tokenLabel) { 
        obj['scopes'] = scopes;
        obj['token_label'] = tokenLabel;
    }

    /**
     * Constructs a <code>CreateAccessTokenRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateAccessTokenRequest} obj Optional instance to populate.
     * @return {module:model/CreateAccessTokenRequest} The populated <code>CreateAccessTokenRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateAccessTokenRequest();

            if (data.hasOwnProperty('scopes')) {
                obj['scopes'] = ApiClient.convertToType(data['scopes'], ['String']);
            }
            if (data.hasOwnProperty('token_label')) {
                obj['token_label'] = ApiClient.convertToType(data['token_label'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateAccessTokenRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateAccessTokenRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateAccessTokenRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scopes'])) {
            throw new Error("Expected the field `scopes` to be an array in the JSON data but got " + data['scopes']);
        }
        // ensure the json data is a string
        if (data['token_label'] && !(typeof data['token_label'] === 'string' || data['token_label'] instanceof String)) {
            throw new Error("Expected the field `token_label` to be a primitive type in the JSON string but got " + data['token_label']);
        }

        return true;
    }


}

CreateAccessTokenRequest.RequiredProperties = ["scopes", "token_label"];

/**
 * Valid scopes: \"repo:admin\", \"repo:write\", \"repo:read\", \"repo:public_read\" 
 * @member {Array.<String>} scopes
 */
CreateAccessTokenRequest.prototype['scopes'] = undefined;

/**
 * Friendly name for you to identify the token.
 * @member {String} token_label
 */
CreateAccessTokenRequest.prototype['token_label'] = undefined;






export default CreateAccessTokenRequest;

