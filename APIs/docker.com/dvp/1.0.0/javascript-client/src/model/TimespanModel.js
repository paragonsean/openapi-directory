/**
 * DVP Data API
 * The Docker DVP Data API allows [Docker Verified Publishers](https://docs.docker.com/docker-hub/publish/) to view image pull analytics data for their namespaces. Analytics data can be retrieved as raw data, or in a summary format.    #### Summary data  In your summary data CSV, you will have access to the data points listed below. You can request summary data for a complete week (Monday through Sunday) or for a complete month (available on the first day of the following month).   There are two levels of summary data:  - Repository-level, a summary of every namespace and repository - Tag- or digest-level, a summary of every namespace, repository, and reference   (tag or digest)   The summary data formats contain the following data points:  - Unique IP address count - Pulls by tag count - Pulls by digest count - Version check count  #### Raw data  In your raw data CSV you will have access to the data points listed below. You can request raw data for a complete week (Monday through Sunday) or for a complete month (available on the first day of the following month). **Note:** each action is represented as a single row.  - Type (industry) - Host (cloud provider) - Country (geolocation) - Timestamp - Namespace - Repository - Reference (digest is always included, tag is provided when available) - HTTP request method - Action, one of the following:   - Pull by tag   - Pull by digest   - Version check - User-Agent 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MonthModel from './MonthModel';
import WeekModel from './WeekModel';

/**
 * The TimespanModel model module.
 * @module model/TimespanModel
 * @version 1.0.0
 */
class TimespanModel {
    /**
     * Constructs a new <code>TimespanModel</code>.
     * @alias module:model/TimespanModel
     * @param {(module:model/MonthModel|module:model/WeekModel)} instance The actual instance to initialize TimespanModel.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            if (typeof instance === "MonthModel") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                MonthModel.validateJSON(instance); // throw an exception if no match
                // create MonthModel from JS object
                this.actualInstance = MonthModel.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into MonthModel
            errorMessages.push("Failed to construct MonthModel: " + err)
        }

        try {
            if (typeof instance === "WeekModel") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                WeekModel.validateJSON(instance); // throw an exception if no match
                // create WeekModel from JS object
                this.actualInstance = WeekModel.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into WeekModel
            errorMessages.push("Failed to construct WeekModel: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `TimespanModel` with oneOf schemas MonthModel, WeekModel. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `TimespanModel` with oneOf schemas MonthModel, WeekModel. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>TimespanModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TimespanModel} obj Optional instance to populate.
     * @return {module:model/TimespanModel} The populated <code>TimespanModel</code> instance.
     */
    static constructFromObject(data, obj) {
        return new TimespanModel(data);
    }

    /**
     * Gets the actual instance, which can be <code>MonthModel</code>, <code>WeekModel</code>.
     * @return {(module:model/MonthModel|module:model/WeekModel)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>MonthModel</code>, <code>WeekModel</code>.
     * @param {(module:model/MonthModel|module:model/WeekModel)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = TimespanModel.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of TimespanModel from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:model/TimespanModel} An instance of TimespanModel.
     */
    static fromJSON = function(json_string){
        return TimespanModel.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * @member {Number} month
 */
TimespanModel.prototype['month'] = undefined;

/**
 * @member {Number} week
 */
TimespanModel.prototype['week'] = undefined;


TimespanModel.OneOf = ["MonthModel", "WeekModel"];

export default TimespanModel;

