/**
 * Bitbucket API
 * Code against the Bitbucket API to automate simple tasks, embed Bitbucket data into your own site, build mobile or desktop apps, or even add custom UI add-ons into Bitbucket itself using the Connect framework.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@bitbucket.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PipelineState from './PipelineState';
import PipelineStateInProgressStage from './PipelineStateInProgressStage';

/**
 * The PipelineStateInProgress model module.
 * @module model/PipelineStateInProgress
 * @version 2.0
 */
class PipelineStateInProgress {
    /**
     * Constructs a new <code>PipelineStateInProgress</code>.
     * @alias module:model/PipelineStateInProgress
     * @extends module:model/PipelineState
     * @implements module:model/PipelineState
     * @param type {String} 
     */
    constructor(type) { 
        PipelineState.initialize(this, type);
        PipelineStateInProgress.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
    }

    /**
     * Constructs a <code>PipelineStateInProgress</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PipelineStateInProgress} obj Optional instance to populate.
     * @return {module:model/PipelineStateInProgress} The populated <code>PipelineStateInProgress</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PipelineStateInProgress();
            PipelineState.constructFromObject(data, obj);
            PipelineState.constructFromObject(data, obj);

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('stage')) {
                obj['stage'] = PipelineStateInProgressStage.constructFromObject(data['stage']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PipelineStateInProgress</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PipelineStateInProgress</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PipelineStateInProgress.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `stage`
        if (data['stage']) { // data not null
          PipelineStateInProgressStage.validateJSON(data['stage']);
        }

        return true;
    }


}

PipelineStateInProgress.RequiredProperties = ["type"];

/**
 * The name of pipeline state (IN_PROGRESS).
 * @member {module:model/PipelineStateInProgress.NameEnum} name
 */
PipelineStateInProgress.prototype['name'] = undefined;

/**
 * @member {module:model/PipelineStateInProgressStage} stage
 */
PipelineStateInProgress.prototype['stage'] = undefined;


// Implement PipelineState interface:
/**
 * @member {String} type
 */
PipelineState.prototype['type'] = undefined;



/**
 * Allowed values for the <code>name</code> property.
 * @enum {String}
 * @readonly
 */
PipelineStateInProgress['NameEnum'] = {

    /**
     * value: "IN_PROGRESS"
     * @const
     */
    "IN_PROGRESS": "IN_PROGRESS"
};



export default PipelineStateInProgress;

