/**
 * Amazon Managed Grafana
 * <p>Amazon Managed Grafana is a fully managed and secure data visualization service that you can use to instantly query, correlate, and visualize operational metrics, logs, and traces from multiple sources. Amazon Managed Grafana makes it easy to deploy, operate, and scale Grafana, a widely deployed data visualization tool that is popular for its extensible data support.</p> <p>With Amazon Managed Grafana, you create logically isolated Grafana servers called <i>workspaces</i>. In a workspace, you can create Grafana dashboards and visualizations to analyze your metrics, logs, and traces without having to build, package, or deploy any hardware to run Grafana servers. </p>
 *
 * The version of the OpenAPI document: 2020-08-18
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PermissionEntryUser from './PermissionEntryUser';
import Role from './Role';

/**
 * The PermissionEntry model module.
 * @module model/PermissionEntry
 * @version 2020-08-18
 */
class PermissionEntry {
    /**
     * Constructs a new <code>PermissionEntry</code>.
     * A structure containing the identity of one user or group and the &lt;code&gt;Admin&lt;/code&gt;, &lt;code&gt;Editor&lt;/code&gt;, or &lt;code&gt;Viewer&lt;/code&gt; role that they have.
     * @alias module:model/PermissionEntry
     * @param role {module:model/Role} 
     * @param user {module:model/PermissionEntryUser} 
     */
    constructor(role, user) { 
        
        PermissionEntry.initialize(this, role, user);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, role, user) { 
        obj['role'] = role;
        obj['user'] = user;
    }

    /**
     * Constructs a <code>PermissionEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PermissionEntry} obj Optional instance to populate.
     * @return {module:model/PermissionEntry} The populated <code>PermissionEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PermissionEntry();

            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], Role);
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = PermissionEntryUser.constructFromObject(data['user']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PermissionEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PermissionEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PermissionEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `role`
        if (data['role']) { // data not null
          Role.validateJSON(data['role']);
        }
        // validate the optional field `user`
        if (data['user']) { // data not null
          PermissionEntryUser.validateJSON(data['user']);
        }

        return true;
    }


}

PermissionEntry.RequiredProperties = ["role", "user"];

/**
 * @member {module:model/Role} role
 */
PermissionEntry.prototype['role'] = undefined;

/**
 * @member {module:model/PermissionEntryUser} user
 */
PermissionEntry.prototype['user'] = undefined;






export default PermissionEntry;

