/**
 * Amazon Lex Model Building V2
 * <p/>
 *
 * The version of the OpenAPI document: 2020-08-07
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AnalyticsBinBySpecification from './AnalyticsBinBySpecification';
import AnalyticsIntentStageFilter from './AnalyticsIntentStageFilter';
import AnalyticsIntentStageGroupBySpecification from './AnalyticsIntentStageGroupBySpecification';
import AnalyticsIntentStageMetric from './AnalyticsIntentStageMetric';

/**
 * The ListIntentStageMetricsRequest model module.
 * @module model/ListIntentStageMetricsRequest
 * @version 2020-08-07
 */
class ListIntentStageMetricsRequest {
    /**
     * Constructs a new <code>ListIntentStageMetricsRequest</code>.
     * @alias module:model/ListIntentStageMetricsRequest
     * @param startDateTime {Date} The date and time that marks the beginning of the range of time for which you want to see intent stage metrics.
     * @param endDateTime {Date} The date and time that marks the end of the range of time for which you want to see intent stage metrics.
     * @param metrics {Array.<module:model/AnalyticsIntentStageMetric>} A list of objects, each of which contains a metric you want to list, the statistic for the metric you want to return, and the method by which to organize the results.
     */
    constructor(startDateTime, endDateTime, metrics) { 
        
        ListIntentStageMetricsRequest.initialize(this, startDateTime, endDateTime, metrics);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, startDateTime, endDateTime, metrics) { 
        obj['startDateTime'] = startDateTime;
        obj['endDateTime'] = endDateTime;
        obj['metrics'] = metrics;
    }

    /**
     * Constructs a <code>ListIntentStageMetricsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListIntentStageMetricsRequest} obj Optional instance to populate.
     * @return {module:model/ListIntentStageMetricsRequest} The populated <code>ListIntentStageMetricsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListIntentStageMetricsRequest();

            if (data.hasOwnProperty('startDateTime')) {
                obj['startDateTime'] = ApiClient.convertToType(data['startDateTime'], 'Date');
            }
            if (data.hasOwnProperty('endDateTime')) {
                obj['endDateTime'] = ApiClient.convertToType(data['endDateTime'], 'Date');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [AnalyticsIntentStageMetric]);
            }
            if (data.hasOwnProperty('binBy')) {
                obj['binBy'] = ApiClient.convertToType(data['binBy'], [AnalyticsBinBySpecification]);
            }
            if (data.hasOwnProperty('groupBy')) {
                obj['groupBy'] = ApiClient.convertToType(data['groupBy'], [AnalyticsIntentStageGroupBySpecification]);
            }
            if (data.hasOwnProperty('filters')) {
                obj['filters'] = ApiClient.convertToType(data['filters'], [AnalyticsIntentStageFilter]);
            }
            if (data.hasOwnProperty('maxResults')) {
                obj['maxResults'] = ApiClient.convertToType(data['maxResults'], 'Number');
            }
            if (data.hasOwnProperty('nextToken')) {
                obj['nextToken'] = ApiClient.convertToType(data['nextToken'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListIntentStageMetricsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListIntentStageMetricsRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ListIntentStageMetricsRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                AnalyticsIntentStageMetric.validateJSON(item);
            };
        }
        if (data['binBy']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['binBy'])) {
                throw new Error("Expected the field `binBy` to be an array in the JSON data but got " + data['binBy']);
            }
            // validate the optional field `binBy` (array)
            for (const item of data['binBy']) {
                AnalyticsBinBySpecification.validateJSON(item);
            };
        }
        if (data['groupBy']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groupBy'])) {
                throw new Error("Expected the field `groupBy` to be an array in the JSON data but got " + data['groupBy']);
            }
            // validate the optional field `groupBy` (array)
            for (const item of data['groupBy']) {
                AnalyticsIntentStageGroupBySpecification.validateJSON(item);
            };
        }
        if (data['filters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['filters'])) {
                throw new Error("Expected the field `filters` to be an array in the JSON data but got " + data['filters']);
            }
            // validate the optional field `filters` (array)
            for (const item of data['filters']) {
                AnalyticsIntentStageFilter.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextToken'] && !(typeof data['nextToken'] === 'string' || data['nextToken'] instanceof String)) {
            throw new Error("Expected the field `nextToken` to be a primitive type in the JSON string but got " + data['nextToken']);
        }

        return true;
    }


}

ListIntentStageMetricsRequest.RequiredProperties = ["startDateTime", "endDateTime", "metrics"];

/**
 * The date and time that marks the beginning of the range of time for which you want to see intent stage metrics.
 * @member {Date} startDateTime
 */
ListIntentStageMetricsRequest.prototype['startDateTime'] = undefined;

/**
 * The date and time that marks the end of the range of time for which you want to see intent stage metrics.
 * @member {Date} endDateTime
 */
ListIntentStageMetricsRequest.prototype['endDateTime'] = undefined;

/**
 * A list of objects, each of which contains a metric you want to list, the statistic for the metric you want to return, and the method by which to organize the results.
 * @member {Array.<module:model/AnalyticsIntentStageMetric>} metrics
 */
ListIntentStageMetricsRequest.prototype['metrics'] = undefined;

/**
 * A list of objects, each of which contains specifications for organizing the results by time.
 * @member {Array.<module:model/AnalyticsBinBySpecification>} binBy
 */
ListIntentStageMetricsRequest.prototype['binBy'] = undefined;

/**
 * <p>A list of objects, each of which specifies how to group the results. You can group by the following criteria:</p> <ul> <li> <p> <code>IntentStageName</code> – The name of the intent stage.</p> </li> <li> <p> <code>SwitchedToIntent</code> – The intent to which the conversation was switched (if any).</p> </li> </ul>
 * @member {Array.<module:model/AnalyticsIntentStageGroupBySpecification>} groupBy
 */
ListIntentStageMetricsRequest.prototype['groupBy'] = undefined;

/**
 * A list of objects, each of which describes a condition by which you want to filter the results.
 * @member {Array.<module:model/AnalyticsIntentStageFilter>} filters
 */
ListIntentStageMetricsRequest.prototype['filters'] = undefined;

/**
 * The maximum number of results to return in each page of results. If there are fewer results than the maximum page size, only the actual number of results are returned.
 * @member {Number} maxResults
 */
ListIntentStageMetricsRequest.prototype['maxResults'] = undefined;

/**
 * <p>If the response from the ListIntentStageMetrics operation contains more results than specified in the maxResults parameter, a token is returned in the response.</p> <p>Use the returned token in the nextToken parameter of a ListIntentStageMetrics request to return the next page of results. For a complete set of results, call the ListIntentStageMetrics operation until the nextToken returned in the response is null.</p>
 * @member {String} nextToken
 */
ListIntentStageMetricsRequest.prototype['nextToken'] = undefined;






export default ListIntentStageMetricsRequest;

