/**
 * Amazon Lex Model Building V2
 * <p/>
 *
 * The version of the OpenAPI document: 2020-08-07
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AnalyticsBinBySpecification from './AnalyticsBinBySpecification';
import AnalyticsSessionFilter from './AnalyticsSessionFilter';
import AnalyticsSessionGroupBySpecification from './AnalyticsSessionGroupBySpecification';
import AnalyticsSessionMetric from './AnalyticsSessionMetric';

/**
 * The ListSessionMetricsRequest model module.
 * @module model/ListSessionMetricsRequest
 * @version 2020-08-07
 */
class ListSessionMetricsRequest {
    /**
     * Constructs a new <code>ListSessionMetricsRequest</code>.
     * @alias module:model/ListSessionMetricsRequest
     * @param startDateTime {Date} The date and time that marks the beginning of the range of time for which you want to see session metrics.
     * @param endDateTime {Date} The date and time that marks the end of the range of time for which you want to see session metrics.
     * @param metrics {Array.<module:model/AnalyticsSessionMetric>} A list of objects, each of which contains a metric you want to list, the statistic for the metric you want to return, and the method by which to organize the results.
     */
    constructor(startDateTime, endDateTime, metrics) { 
        
        ListSessionMetricsRequest.initialize(this, startDateTime, endDateTime, metrics);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, startDateTime, endDateTime, metrics) { 
        obj['startDateTime'] = startDateTime;
        obj['endDateTime'] = endDateTime;
        obj['metrics'] = metrics;
    }

    /**
     * Constructs a <code>ListSessionMetricsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListSessionMetricsRequest} obj Optional instance to populate.
     * @return {module:model/ListSessionMetricsRequest} The populated <code>ListSessionMetricsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListSessionMetricsRequest();

            if (data.hasOwnProperty('startDateTime')) {
                obj['startDateTime'] = ApiClient.convertToType(data['startDateTime'], 'Date');
            }
            if (data.hasOwnProperty('endDateTime')) {
                obj['endDateTime'] = ApiClient.convertToType(data['endDateTime'], 'Date');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [AnalyticsSessionMetric]);
            }
            if (data.hasOwnProperty('binBy')) {
                obj['binBy'] = ApiClient.convertToType(data['binBy'], [AnalyticsBinBySpecification]);
            }
            if (data.hasOwnProperty('groupBy')) {
                obj['groupBy'] = ApiClient.convertToType(data['groupBy'], [AnalyticsSessionGroupBySpecification]);
            }
            if (data.hasOwnProperty('filters')) {
                obj['filters'] = ApiClient.convertToType(data['filters'], [AnalyticsSessionFilter]);
            }
            if (data.hasOwnProperty('maxResults')) {
                obj['maxResults'] = ApiClient.convertToType(data['maxResults'], 'Number');
            }
            if (data.hasOwnProperty('nextToken')) {
                obj['nextToken'] = ApiClient.convertToType(data['nextToken'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListSessionMetricsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListSessionMetricsRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ListSessionMetricsRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                AnalyticsSessionMetric.validateJSON(item);
            };
        }
        if (data['binBy']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['binBy'])) {
                throw new Error("Expected the field `binBy` to be an array in the JSON data but got " + data['binBy']);
            }
            // validate the optional field `binBy` (array)
            for (const item of data['binBy']) {
                AnalyticsBinBySpecification.validateJSON(item);
            };
        }
        if (data['groupBy']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groupBy'])) {
                throw new Error("Expected the field `groupBy` to be an array in the JSON data but got " + data['groupBy']);
            }
            // validate the optional field `groupBy` (array)
            for (const item of data['groupBy']) {
                AnalyticsSessionGroupBySpecification.validateJSON(item);
            };
        }
        if (data['filters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['filters'])) {
                throw new Error("Expected the field `filters` to be an array in the JSON data but got " + data['filters']);
            }
            // validate the optional field `filters` (array)
            for (const item of data['filters']) {
                AnalyticsSessionFilter.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextToken'] && !(typeof data['nextToken'] === 'string' || data['nextToken'] instanceof String)) {
            throw new Error("Expected the field `nextToken` to be a primitive type in the JSON string but got " + data['nextToken']);
        }

        return true;
    }


}

ListSessionMetricsRequest.RequiredProperties = ["startDateTime", "endDateTime", "metrics"];

/**
 * The date and time that marks the beginning of the range of time for which you want to see session metrics.
 * @member {Date} startDateTime
 */
ListSessionMetricsRequest.prototype['startDateTime'] = undefined;

/**
 * The date and time that marks the end of the range of time for which you want to see session metrics.
 * @member {Date} endDateTime
 */
ListSessionMetricsRequest.prototype['endDateTime'] = undefined;

/**
 * A list of objects, each of which contains a metric you want to list, the statistic for the metric you want to return, and the method by which to organize the results.
 * @member {Array.<module:model/AnalyticsSessionMetric>} metrics
 */
ListSessionMetricsRequest.prototype['metrics'] = undefined;

/**
 * A list of objects, each of which contains specifications for organizing the results by time.
 * @member {Array.<module:model/AnalyticsBinBySpecification>} binBy
 */
ListSessionMetricsRequest.prototype['binBy'] = undefined;

/**
 * <p>A list of objects, each of which specifies how to group the results. You can group by the following criteria:</p> <ul> <li> <p> <code>ConversationEndState</code> – The final state of the conversation. The possible end states are detailed in <a href=\"https://docs.aws.amazon.com/analytics-key-definitions-conversations\">Key definitions</a> in the user guide.</p> </li> <li> <p> <code>LocaleId</code> – The unique identifier of the bot locale.</p> </li> </ul>
 * @member {Array.<module:model/AnalyticsSessionGroupBySpecification>} groupBy
 */
ListSessionMetricsRequest.prototype['groupBy'] = undefined;

/**
 * A list of objects, each of which describes a condition by which you want to filter the results.
 * @member {Array.<module:model/AnalyticsSessionFilter>} filters
 */
ListSessionMetricsRequest.prototype['filters'] = undefined;

/**
 * The maximum number of results to return in each page of results. If there are fewer results than the maximum page size, only the actual number of results are returned.
 * @member {Number} maxResults
 */
ListSessionMetricsRequest.prototype['maxResults'] = undefined;

/**
 * <p>If the response from the ListSessionMetrics operation contains more results than specified in the maxResults parameter, a token is returned in the response.</p> <p>Use the returned token in the nextToken parameter of a ListSessionMetrics request to return the next page of results. For a complete set of results, call the ListSessionMetrics operation until the nextToken returned in the response is null.</p>
 * @member {String} nextToken
 */
ListSessionMetricsRequest.prototype['nextToken'] = undefined;






export default ListSessionMetricsRequest;

