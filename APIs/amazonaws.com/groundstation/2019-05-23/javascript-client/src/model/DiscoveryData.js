/**
 * AWS Ground Station
 * Welcome to the AWS Ground Station API Reference. AWS Ground Station is a fully managed service that enables you to control satellite communications, downlink and process satellite data, and scale your satellite operations efficiently and cost-effectively without having to build or manage your own ground station infrastructure.
 *
 * The version of the OpenAPI document: 2019-05-23
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DiscoveryData model module.
 * @module model/DiscoveryData
 * @version 2019-05-23
 */
class DiscoveryData {
    /**
     * Constructs a new <code>DiscoveryData</code>.
     * Data for agent discovery.
     * @alias module:model/DiscoveryData
     * @param capabilityArns {Array} 
     * @param privateIpAddresses {Array} 
     * @param publicIpAddresses {Array} 
     */
    constructor(capabilityArns, privateIpAddresses, publicIpAddresses) { 
        
        DiscoveryData.initialize(this, capabilityArns, privateIpAddresses, publicIpAddresses);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, capabilityArns, privateIpAddresses, publicIpAddresses) { 
        obj['capabilityArns'] = capabilityArns;
        obj['privateIpAddresses'] = privateIpAddresses;
        obj['publicIpAddresses'] = publicIpAddresses;
    }

    /**
     * Constructs a <code>DiscoveryData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DiscoveryData} obj Optional instance to populate.
     * @return {module:model/DiscoveryData} The populated <code>DiscoveryData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DiscoveryData();

            if (data.hasOwnProperty('capabilityArns')) {
                obj['capabilityArns'] = ApiClient.convertToType(data['capabilityArns'], Array);
            }
            if (data.hasOwnProperty('privateIpAddresses')) {
                obj['privateIpAddresses'] = ApiClient.convertToType(data['privateIpAddresses'], Array);
            }
            if (data.hasOwnProperty('publicIpAddresses')) {
                obj['publicIpAddresses'] = ApiClient.convertToType(data['publicIpAddresses'], Array);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DiscoveryData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DiscoveryData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DiscoveryData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `capabilityArns`
        if (data['capabilityArns']) { // data not null
          Array.validateJSON(data['capabilityArns']);
        }
        // validate the optional field `privateIpAddresses`
        if (data['privateIpAddresses']) { // data not null
          Array.validateJSON(data['privateIpAddresses']);
        }
        // validate the optional field `publicIpAddresses`
        if (data['publicIpAddresses']) { // data not null
          Array.validateJSON(data['publicIpAddresses']);
        }

        return true;
    }


}

DiscoveryData.RequiredProperties = ["capabilityArns", "privateIpAddresses", "publicIpAddresses"];

/**
 * @member {Array} capabilityArns
 */
DiscoveryData.prototype['capabilityArns'] = undefined;

/**
 * @member {Array} privateIpAddresses
 */
DiscoveryData.prototype['privateIpAddresses'] = undefined;

/**
 * @member {Array} publicIpAddresses
 */
DiscoveryData.prototype['publicIpAddresses'] = undefined;






export default DiscoveryData;

