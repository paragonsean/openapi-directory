/**
 * AWS Clean Rooms Service
 * <p>Welcome to the <i>Clean Rooms API Reference</i>.</p> <p>Clean Rooms is an Amazon Web Services service that helps multiple parties to join their data together in a secure collaboration workspace. In the collaboration, members who can query and receive results can get insights into the collective datasets without either party getting access to the other party's raw data.</p> <p>To learn more about Clean Rooms concepts, procedures, and best practices, see the <a href=\"https://docs.aws.amazon.com/clean-rooms/latest/userguide/what-is.html\">Clean Rooms User Guide</a>.</p>
 *
 * The version of the OpenAPI document: 2022-02-17
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateConfiguredTableAssociationRequest model module.
 * @module model/CreateConfiguredTableAssociationRequest
 * @version 2022-02-17
 */
class CreateConfiguredTableAssociationRequest {
    /**
     * Constructs a new <code>CreateConfiguredTableAssociationRequest</code>.
     * @alias module:model/CreateConfiguredTableAssociationRequest
     * @param name {String} The name of the configured table association. This name is used to query the underlying configured table.
     * @param configuredTableIdentifier {String} A unique identifier for the configured table to be associated to. Currently accepts a configured table ID.
     * @param roleArn {String} The service will assume this role to access catalog metadata and query the table.
     */
    constructor(name, configuredTableIdentifier, roleArn) { 
        
        CreateConfiguredTableAssociationRequest.initialize(this, name, configuredTableIdentifier, roleArn);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, configuredTableIdentifier, roleArn) { 
        obj['name'] = name;
        obj['configuredTableIdentifier'] = configuredTableIdentifier;
        obj['roleArn'] = roleArn;
    }

    /**
     * Constructs a <code>CreateConfiguredTableAssociationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateConfiguredTableAssociationRequest} obj Optional instance to populate.
     * @return {module:model/CreateConfiguredTableAssociationRequest} The populated <code>CreateConfiguredTableAssociationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateConfiguredTableAssociationRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('configuredTableIdentifier')) {
                obj['configuredTableIdentifier'] = ApiClient.convertToType(data['configuredTableIdentifier'], 'String');
            }
            if (data.hasOwnProperty('roleArn')) {
                obj['roleArn'] = ApiClient.convertToType(data['roleArn'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateConfiguredTableAssociationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateConfiguredTableAssociationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateConfiguredTableAssociationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['configuredTableIdentifier'] && !(typeof data['configuredTableIdentifier'] === 'string' || data['configuredTableIdentifier'] instanceof String)) {
            throw new Error("Expected the field `configuredTableIdentifier` to be a primitive type in the JSON string but got " + data['configuredTableIdentifier']);
        }
        // ensure the json data is a string
        if (data['roleArn'] && !(typeof data['roleArn'] === 'string' || data['roleArn'] instanceof String)) {
            throw new Error("Expected the field `roleArn` to be a primitive type in the JSON string but got " + data['roleArn']);
        }

        return true;
    }


}

CreateConfiguredTableAssociationRequest.RequiredProperties = ["name", "configuredTableIdentifier", "roleArn"];

/**
 * The name of the configured table association. This name is used to query the underlying configured table.
 * @member {String} name
 */
CreateConfiguredTableAssociationRequest.prototype['name'] = undefined;

/**
 * A description for the configured table association.
 * @member {String} description
 */
CreateConfiguredTableAssociationRequest.prototype['description'] = undefined;

/**
 * A unique identifier for the configured table to be associated to. Currently accepts a configured table ID.
 * @member {String} configuredTableIdentifier
 */
CreateConfiguredTableAssociationRequest.prototype['configuredTableIdentifier'] = undefined;

/**
 * The service will assume this role to access catalog metadata and query the table.
 * @member {String} roleArn
 */
CreateConfiguredTableAssociationRequest.prototype['roleArn'] = undefined;

/**
 * Map of tags assigned to a resource
 * @member {Object.<String, String>} tags
 */
CreateConfiguredTableAssociationRequest.prototype['tags'] = undefined;






export default CreateConfiguredTableAssociationRequest;

