/**
 * Amazon Simple Email Service
 * <fullname>Amazon SES API v2</fullname> <p> <a href=\"http://aws.amazon.com/ses\">Amazon SES</a> is an Amazon Web Services service that you can use to send email messages to your customers.</p> <p>If you're new to Amazon SES API v2, you might find it helpful to review the <a href=\"https://docs.aws.amazon.com/ses/latest/DeveloperGuide/\">Amazon Simple Email Service Developer Guide</a>. The <i>Amazon SES Developer Guide</i> provides information and code samples that demonstrate how to use Amazon SES API v2 features programmatically.</p>
 *
 * The version of the OpenAPI document: 2019-09-27
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Tag from './Tag';

/**
 * The CreateDedicatedIpPoolRequest model module.
 * @module model/CreateDedicatedIpPoolRequest
 * @version 2019-09-27
 */
class CreateDedicatedIpPoolRequest {
    /**
     * Constructs a new <code>CreateDedicatedIpPoolRequest</code>.
     * @alias module:model/CreateDedicatedIpPoolRequest
     * @param poolName {String} The name of a dedicated IP pool.
     */
    constructor(poolName) { 
        
        CreateDedicatedIpPoolRequest.initialize(this, poolName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, poolName) { 
        obj['PoolName'] = poolName;
    }

    /**
     * Constructs a <code>CreateDedicatedIpPoolRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateDedicatedIpPoolRequest} obj Optional instance to populate.
     * @return {module:model/CreateDedicatedIpPoolRequest} The populated <code>CreateDedicatedIpPoolRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateDedicatedIpPoolRequest();

            if (data.hasOwnProperty('PoolName')) {
                obj['PoolName'] = ApiClient.convertToType(data['PoolName'], 'String');
            }
            if (data.hasOwnProperty('Tags')) {
                obj['Tags'] = ApiClient.convertToType(data['Tags'], [Tag]);
            }
            if (data.hasOwnProperty('ScalingMode')) {
                obj['ScalingMode'] = ApiClient.convertToType(data['ScalingMode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateDedicatedIpPoolRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateDedicatedIpPoolRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateDedicatedIpPoolRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['PoolName'] && !(typeof data['PoolName'] === 'string' || data['PoolName'] instanceof String)) {
            throw new Error("Expected the field `PoolName` to be a primitive type in the JSON string but got " + data['PoolName']);
        }
        if (data['Tags']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Tags'])) {
                throw new Error("Expected the field `Tags` to be an array in the JSON data but got " + data['Tags']);
            }
            // validate the optional field `Tags` (array)
            for (const item of data['Tags']) {
                Tag.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['ScalingMode'] && !(typeof data['ScalingMode'] === 'string' || data['ScalingMode'] instanceof String)) {
            throw new Error("Expected the field `ScalingMode` to be a primitive type in the JSON string but got " + data['ScalingMode']);
        }

        return true;
    }


}

CreateDedicatedIpPoolRequest.RequiredProperties = ["PoolName"];

/**
 * The name of a dedicated IP pool.
 * @member {String} PoolName
 */
CreateDedicatedIpPoolRequest.prototype['PoolName'] = undefined;

/**
 * An object that defines the tags (keys and values) that you want to associate with the pool.
 * @member {Array.<module:model/Tag>} Tags
 */
CreateDedicatedIpPoolRequest.prototype['Tags'] = undefined;

/**
 * The type of scaling mode.
 * @member {module:model/CreateDedicatedIpPoolRequest.ScalingModeEnum} ScalingMode
 */
CreateDedicatedIpPoolRequest.prototype['ScalingMode'] = undefined;





/**
 * Allowed values for the <code>ScalingMode</code> property.
 * @enum {String}
 * @readonly
 */
CreateDedicatedIpPoolRequest['ScalingModeEnum'] = {

    /**
     * value: "STANDARD"
     * @const
     */
    "STANDARD": "STANDARD",

    /**
     * value: "MANAGED"
     * @const
     */
    "MANAGED": "MANAGED"
};



export default CreateDedicatedIpPoolRequest;

