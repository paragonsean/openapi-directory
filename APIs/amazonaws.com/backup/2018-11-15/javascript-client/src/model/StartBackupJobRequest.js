/**
 * AWS Backup
 * <fullname>Backup</fullname> <p>Backup is a unified backup service designed to protect Amazon Web Services services and their associated data. Backup simplifies the creation, migration, restoration, and deletion of backups, while also providing reporting and auditing.</p>
 *
 * The version of the OpenAPI document: 2018-11-15
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UpdateRecoveryPointLifecycleRequestLifecycle from './UpdateRecoveryPointLifecycleRequestLifecycle';

/**
 * The StartBackupJobRequest model module.
 * @module model/StartBackupJobRequest
 * @version 2018-11-15
 */
class StartBackupJobRequest {
    /**
     * Constructs a new <code>StartBackupJobRequest</code>.
     * @alias module:model/StartBackupJobRequest
     * @param backupVaultName {String} The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens.
     * @param resourceArn {String} An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type.
     * @param iamRoleArn {String} Specifies the IAM role ARN used to create the target recovery point; for example, <code>arn:aws:iam::123456789012:role/S3Access</code>.
     */
    constructor(backupVaultName, resourceArn, iamRoleArn) { 
        
        StartBackupJobRequest.initialize(this, backupVaultName, resourceArn, iamRoleArn);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, backupVaultName, resourceArn, iamRoleArn) { 
        obj['BackupVaultName'] = backupVaultName;
        obj['ResourceArn'] = resourceArn;
        obj['IamRoleArn'] = iamRoleArn;
    }

    /**
     * Constructs a <code>StartBackupJobRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StartBackupJobRequest} obj Optional instance to populate.
     * @return {module:model/StartBackupJobRequest} The populated <code>StartBackupJobRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StartBackupJobRequest();

            if (data.hasOwnProperty('BackupVaultName')) {
                obj['BackupVaultName'] = ApiClient.convertToType(data['BackupVaultName'], 'String');
            }
            if (data.hasOwnProperty('ResourceArn')) {
                obj['ResourceArn'] = ApiClient.convertToType(data['ResourceArn'], 'String');
            }
            if (data.hasOwnProperty('IamRoleArn')) {
                obj['IamRoleArn'] = ApiClient.convertToType(data['IamRoleArn'], 'String');
            }
            if (data.hasOwnProperty('IdempotencyToken')) {
                obj['IdempotencyToken'] = ApiClient.convertToType(data['IdempotencyToken'], 'String');
            }
            if (data.hasOwnProperty('StartWindowMinutes')) {
                obj['StartWindowMinutes'] = ApiClient.convertToType(data['StartWindowMinutes'], 'Number');
            }
            if (data.hasOwnProperty('CompleteWindowMinutes')) {
                obj['CompleteWindowMinutes'] = ApiClient.convertToType(data['CompleteWindowMinutes'], 'Number');
            }
            if (data.hasOwnProperty('Lifecycle')) {
                obj['Lifecycle'] = UpdateRecoveryPointLifecycleRequestLifecycle.constructFromObject(data['Lifecycle']);
            }
            if (data.hasOwnProperty('RecoveryPointTags')) {
                obj['RecoveryPointTags'] = ApiClient.convertToType(data['RecoveryPointTags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('BackupOptions')) {
                obj['BackupOptions'] = ApiClient.convertToType(data['BackupOptions'], {'String': 'String'});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StartBackupJobRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StartBackupJobRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StartBackupJobRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['BackupVaultName'] && !(typeof data['BackupVaultName'] === 'string' || data['BackupVaultName'] instanceof String)) {
            throw new Error("Expected the field `BackupVaultName` to be a primitive type in the JSON string but got " + data['BackupVaultName']);
        }
        // ensure the json data is a string
        if (data['ResourceArn'] && !(typeof data['ResourceArn'] === 'string' || data['ResourceArn'] instanceof String)) {
            throw new Error("Expected the field `ResourceArn` to be a primitive type in the JSON string but got " + data['ResourceArn']);
        }
        // ensure the json data is a string
        if (data['IamRoleArn'] && !(typeof data['IamRoleArn'] === 'string' || data['IamRoleArn'] instanceof String)) {
            throw new Error("Expected the field `IamRoleArn` to be a primitive type in the JSON string but got " + data['IamRoleArn']);
        }
        // ensure the json data is a string
        if (data['IdempotencyToken'] && !(typeof data['IdempotencyToken'] === 'string' || data['IdempotencyToken'] instanceof String)) {
            throw new Error("Expected the field `IdempotencyToken` to be a primitive type in the JSON string but got " + data['IdempotencyToken']);
        }
        // validate the optional field `Lifecycle`
        if (data['Lifecycle']) { // data not null
          UpdateRecoveryPointLifecycleRequestLifecycle.validateJSON(data['Lifecycle']);
        }

        return true;
    }


}

StartBackupJobRequest.RequiredProperties = ["BackupVaultName", "ResourceArn", "IamRoleArn"];

/**
 * The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens.
 * @member {String} BackupVaultName
 */
StartBackupJobRequest.prototype['BackupVaultName'] = undefined;

/**
 * An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type.
 * @member {String} ResourceArn
 */
StartBackupJobRequest.prototype['ResourceArn'] = undefined;

/**
 * Specifies the IAM role ARN used to create the target recovery point; for example, <code>arn:aws:iam::123456789012:role/S3Access</code>.
 * @member {String} IamRoleArn
 */
StartBackupJobRequest.prototype['IamRoleArn'] = undefined;

/**
 * A customer-chosen string that you can use to distinguish between otherwise identical calls to <code>StartBackupJob</code>. Retrying a successful request with the same idempotency token results in a success message with no action taken.
 * @member {String} IdempotencyToken
 */
StartBackupJobRequest.prototype['IdempotencyToken'] = undefined;

/**
 * <p>A value in minutes after a backup is scheduled before a job will be canceled if it doesn't start successfully. This value is optional, and the default is 8 hours. If this value is included, it must be at least 60 minutes to avoid errors.</p> <p>During the start window, the backup job status remains in <code>CREATED</code> status until it has successfully begun or until the start window time has run out. If within the start window time Backup receives an error that allows the job to be retried, Backup will automatically retry to begin the job at least every 10 minutes until the backup successfully begins (the job status changes to <code>RUNNING</code>) or until the job status changes to <code>EXPIRED</code> (which is expected to occur when the start window time is over).</p>
 * @member {Number} StartWindowMinutes
 */
StartBackupJobRequest.prototype['StartWindowMinutes'] = undefined;

/**
 * A value in minutes during which a successfully started backup must complete, or else Backup will cancel the job. This value is optional. This value begins counting down from when the backup was scheduled. It does not add additional time for <code>StartWindowMinutes</code>, or if the backup started later than scheduled.
 * @member {Number} CompleteWindowMinutes
 */
StartBackupJobRequest.prototype['CompleteWindowMinutes'] = undefined;

/**
 * @member {module:model/UpdateRecoveryPointLifecycleRequestLifecycle} Lifecycle
 */
StartBackupJobRequest.prototype['Lifecycle'] = undefined;

/**
 * To help organize your resources, you can assign your own metadata to the resources that you create. Each tag is a key-value pair.
 * @member {Object.<String, String>} RecoveryPointTags
 */
StartBackupJobRequest.prototype['RecoveryPointTags'] = undefined;

/**
 * <p>Specifies the backup option for a selected resource. This option is only available for Windows Volume Shadow Copy Service (VSS) backup jobs.</p> <p>Valid values: Set to <code>\"WindowsVSS\":\"enabled\"</code> to enable the <code>WindowsVSS</code> backup option and create a Windows VSS backup. Set to <code>\"WindowsVSS\"\"disabled\"</code> to create a regular backup. The <code>WindowsVSS</code> option is not enabled by default.</p>
 * @member {Object.<String, String>} BackupOptions
 */
StartBackupJobRequest.prototype['BackupOptions'] = undefined;






export default StartBackupJobRequest;

