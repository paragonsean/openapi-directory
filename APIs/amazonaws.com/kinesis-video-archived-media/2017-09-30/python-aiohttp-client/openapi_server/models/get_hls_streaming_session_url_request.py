# coding: utf-8

from datetime import date, datetime

from typing import List, Dict, Type

from openapi_server.models.base_model import Model
from openapi_server.models.get_hls_streaming_session_url_request_hls_fragment_selector import GetHLSStreamingSessionURLRequestHLSFragmentSelector
import re
from openapi_server import util


class GetHLSStreamingSessionURLRequest(Model):
    """NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

    Do not edit the class manually.
    """

    def __init__(self, stream_name: str=None, stream_arn: str=None, playback_mode: str=None, hls_fragment_selector: GetHLSStreamingSessionURLRequestHLSFragmentSelector=None, container_format: str=None, discontinuity_mode: str=None, display_fragment_timestamp: str=None, expires: int=None, max_media_playlist_fragment_results: int=None):
        """GetHLSStreamingSessionURLRequest - a model defined in OpenAPI

        :param stream_name: The stream_name of this GetHLSStreamingSessionURLRequest.
        :param stream_arn: The stream_arn of this GetHLSStreamingSessionURLRequest.
        :param playback_mode: The playback_mode of this GetHLSStreamingSessionURLRequest.
        :param hls_fragment_selector: The hls_fragment_selector of this GetHLSStreamingSessionURLRequest.
        :param container_format: The container_format of this GetHLSStreamingSessionURLRequest.
        :param discontinuity_mode: The discontinuity_mode of this GetHLSStreamingSessionURLRequest.
        :param display_fragment_timestamp: The display_fragment_timestamp of this GetHLSStreamingSessionURLRequest.
        :param expires: The expires of this GetHLSStreamingSessionURLRequest.
        :param max_media_playlist_fragment_results: The max_media_playlist_fragment_results of this GetHLSStreamingSessionURLRequest.
        """
        self.openapi_types = {
            'stream_name': str,
            'stream_arn': str,
            'playback_mode': str,
            'hls_fragment_selector': GetHLSStreamingSessionURLRequestHLSFragmentSelector,
            'container_format': str,
            'discontinuity_mode': str,
            'display_fragment_timestamp': str,
            'expires': int,
            'max_media_playlist_fragment_results': int
        }

        self.attribute_map = {
            'stream_name': 'StreamName',
            'stream_arn': 'StreamARN',
            'playback_mode': 'PlaybackMode',
            'hls_fragment_selector': 'HLSFragmentSelector',
            'container_format': 'ContainerFormat',
            'discontinuity_mode': 'DiscontinuityMode',
            'display_fragment_timestamp': 'DisplayFragmentTimestamp',
            'expires': 'Expires',
            'max_media_playlist_fragment_results': 'MaxMediaPlaylistFragmentResults'
        }

        self._stream_name = stream_name
        self._stream_arn = stream_arn
        self._playback_mode = playback_mode
        self._hls_fragment_selector = hls_fragment_selector
        self._container_format = container_format
        self._discontinuity_mode = discontinuity_mode
        self._display_fragment_timestamp = display_fragment_timestamp
        self._expires = expires
        self._max_media_playlist_fragment_results = max_media_playlist_fragment_results

    @classmethod
    def from_dict(cls, dikt: dict) -> 'GetHLSStreamingSessionURLRequest':
        """Returns the dict as a model

        :param dikt: A dict.
        :return: The GetHLSStreamingSessionURL_request of this GetHLSStreamingSessionURLRequest.
        """
        return util.deserialize_model(dikt, cls)

    @property
    def stream_name(self):
        """Gets the stream_name of this GetHLSStreamingSessionURLRequest.

        <p>The name of the stream for which to retrieve the HLS master playlist URL.</p> <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>

        :return: The stream_name of this GetHLSStreamingSessionURLRequest.
        :rtype: str
        """
        return self._stream_name

    @stream_name.setter
    def stream_name(self, stream_name):
        """Sets the stream_name of this GetHLSStreamingSessionURLRequest.

        <p>The name of the stream for which to retrieve the HLS master playlist URL.</p> <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>

        :param stream_name: The stream_name of this GetHLSStreamingSessionURLRequest.
        :type stream_name: str
        """
        if stream_name is not None and len(stream_name) > 256:
            raise ValueError("Invalid value for `stream_name`, length must be less than or equal to `256`")
        if stream_name is not None and len(stream_name) < 1:
            raise ValueError("Invalid value for `stream_name`, length must be greater than or equal to `1`")
        if stream_name is not None and not re.search(r'[a-zA-Z0-9_.-]+', stream_name):
            raise ValueError("Invalid value for `stream_name`, must be a follow pattern or equal to `/[a-zA-Z0-9_.-]+/`")

        self._stream_name = stream_name

    @property
    def stream_arn(self):
        """Gets the stream_arn of this GetHLSStreamingSessionURLRequest.

        <p>The Amazon Resource Name (ARN) of the stream for which to retrieve the HLS master playlist URL.</p> <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>

        :return: The stream_arn of this GetHLSStreamingSessionURLRequest.
        :rtype: str
        """
        return self._stream_arn

    @stream_arn.setter
    def stream_arn(self, stream_arn):
        """Sets the stream_arn of this GetHLSStreamingSessionURLRequest.

        <p>The Amazon Resource Name (ARN) of the stream for which to retrieve the HLS master playlist URL.</p> <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>

        :param stream_arn: The stream_arn of this GetHLSStreamingSessionURLRequest.
        :type stream_arn: str
        """
        if stream_arn is not None and len(stream_arn) > 1024:
            raise ValueError("Invalid value for `stream_arn`, length must be less than or equal to `1024`")
        if stream_arn is not None and len(stream_arn) < 1:
            raise ValueError("Invalid value for `stream_arn`, length must be greater than or equal to `1`")
        if stream_arn is not None and not re.search(r'arn:[a-z\d-]+:kinesisvideo:[a-z0-9-]+:[0-9]+:[a-z]+\/[a-zA-Z0-9_.-]+\/[0-9]+', stream_arn):
            raise ValueError("Invalid value for `stream_arn`, must be a follow pattern or equal to `/arn:[a-z\d-]+:kinesisvideo:[a-z0-9-]+:[0-9]+:[a-z]+\/[a-zA-Z0-9_.-]+\/[0-9]+/`")

        self._stream_arn = stream_arn

    @property
    def playback_mode(self):
        """Gets the playback_mode of this GetHLSStreamingSessionURLRequest.

        <p>Whether to retrieve live, live replay, or archived, on-demand data.</p> <p>Features of the three types of sessions include the following:</p> <ul> <li> <p> <b> <code>LIVE</code> </b>: For sessions of this type, the HLS media playlist is continually updated with the latest fragments as they become available. We recommend that the media player retrieve a new playlist on a one-second interval. When this type of session is played in a media player, the user interface typically displays a \"live\" notification, with no scrubber control for choosing the position in the playback window to display.</p> <note> <p>In <code>LIVE</code> mode, the newest available fragments are included in an HLS media playlist, even if there is a gap between fragments (that is, if a fragment is missing). A gap like this might cause a media player to halt or cause a jump in playback. In this mode, fragments are not added to the HLS media playlist if they are older than the newest fragment in the playlist. If the missing fragment becomes available after a subsequent fragment is added to the playlist, the older fragment is not added, and the gap is not filled.</p> </note> </li> <li> <p> <b> <code>LIVE_REPLAY</code> </b>: For sessions of this type, the HLS media playlist is updated similarly to how it is updated for <code>LIVE</code> mode except that it starts by including fragments from a given start time. Instead of fragments being added as they are ingested, fragments are added as the duration of the next fragment elapses. For example, if the fragments in the session are two seconds long, then a new fragment is added to the media playlist every two seconds. This mode is useful to be able to start playback from when an event is detected and continue live streaming media that has not yet been ingested as of the time of the session creation. This mode is also useful to stream previously archived media without being limited by the 1,000 fragment limit in the <code>ON_DEMAND</code> mode. </p> </li> <li> <p> <b> <code>ON_DEMAND</code> </b>: For sessions of this type, the HLS media playlist contains all the fragments for the session, up to the number that is specified in <code>MaxMediaPlaylistFragmentResults</code>. The playlist must be retrieved only once for each session. When this type of session is played in a media player, the user interface typically displays a scrubber control for choosing the position in the playback window to display.</p> </li> </ul> <p>In all playback modes, if <code>FragmentSelectorType</code> is <code>PRODUCER_TIMESTAMP</code>, and if there are multiple fragments with the same start timestamp, the fragment that has the largest fragment number (that is, the newest fragment) is included in the HLS media playlist. The other fragments are not included. Fragments that have different timestamps but have overlapping durations are still included in the HLS media playlist. This can lead to unexpected behavior in the media player.</p> <p>The default is <code>LIVE</code>.</p>

        :return: The playback_mode of this GetHLSStreamingSessionURLRequest.
        :rtype: str
        """
        return self._playback_mode

    @playback_mode.setter
    def playback_mode(self, playback_mode):
        """Sets the playback_mode of this GetHLSStreamingSessionURLRequest.

        <p>Whether to retrieve live, live replay, or archived, on-demand data.</p> <p>Features of the three types of sessions include the following:</p> <ul> <li> <p> <b> <code>LIVE</code> </b>: For sessions of this type, the HLS media playlist is continually updated with the latest fragments as they become available. We recommend that the media player retrieve a new playlist on a one-second interval. When this type of session is played in a media player, the user interface typically displays a \"live\" notification, with no scrubber control for choosing the position in the playback window to display.</p> <note> <p>In <code>LIVE</code> mode, the newest available fragments are included in an HLS media playlist, even if there is a gap between fragments (that is, if a fragment is missing). A gap like this might cause a media player to halt or cause a jump in playback. In this mode, fragments are not added to the HLS media playlist if they are older than the newest fragment in the playlist. If the missing fragment becomes available after a subsequent fragment is added to the playlist, the older fragment is not added, and the gap is not filled.</p> </note> </li> <li> <p> <b> <code>LIVE_REPLAY</code> </b>: For sessions of this type, the HLS media playlist is updated similarly to how it is updated for <code>LIVE</code> mode except that it starts by including fragments from a given start time. Instead of fragments being added as they are ingested, fragments are added as the duration of the next fragment elapses. For example, if the fragments in the session are two seconds long, then a new fragment is added to the media playlist every two seconds. This mode is useful to be able to start playback from when an event is detected and continue live streaming media that has not yet been ingested as of the time of the session creation. This mode is also useful to stream previously archived media without being limited by the 1,000 fragment limit in the <code>ON_DEMAND</code> mode. </p> </li> <li> <p> <b> <code>ON_DEMAND</code> </b>: For sessions of this type, the HLS media playlist contains all the fragments for the session, up to the number that is specified in <code>MaxMediaPlaylistFragmentResults</code>. The playlist must be retrieved only once for each session. When this type of session is played in a media player, the user interface typically displays a scrubber control for choosing the position in the playback window to display.</p> </li> </ul> <p>In all playback modes, if <code>FragmentSelectorType</code> is <code>PRODUCER_TIMESTAMP</code>, and if there are multiple fragments with the same start timestamp, the fragment that has the largest fragment number (that is, the newest fragment) is included in the HLS media playlist. The other fragments are not included. Fragments that have different timestamps but have overlapping durations are still included in the HLS media playlist. This can lead to unexpected behavior in the media player.</p> <p>The default is <code>LIVE</code>.</p>

        :param playback_mode: The playback_mode of this GetHLSStreamingSessionURLRequest.
        :type playback_mode: str
        """
        allowed_values = ["LIVE", "LIVE_REPLAY", "ON_DEMAND"]  # noqa: E501
        if playback_mode not in allowed_values:
            raise ValueError(
                "Invalid value for `playback_mode` ({0}), must be one of {1}"
                .format(playback_mode, allowed_values)
            )

        self._playback_mode = playback_mode

    @property
    def hls_fragment_selector(self):
        """Gets the hls_fragment_selector of this GetHLSStreamingSessionURLRequest.


        :return: The hls_fragment_selector of this GetHLSStreamingSessionURLRequest.
        :rtype: GetHLSStreamingSessionURLRequestHLSFragmentSelector
        """
        return self._hls_fragment_selector

    @hls_fragment_selector.setter
    def hls_fragment_selector(self, hls_fragment_selector):
        """Sets the hls_fragment_selector of this GetHLSStreamingSessionURLRequest.


        :param hls_fragment_selector: The hls_fragment_selector of this GetHLSStreamingSessionURLRequest.
        :type hls_fragment_selector: GetHLSStreamingSessionURLRequestHLSFragmentSelector
        """

        self._hls_fragment_selector = hls_fragment_selector

    @property
    def container_format(self):
        """Gets the container_format of this GetHLSStreamingSessionURLRequest.

        <p>Specifies which format should be used for packaging the media. Specifying the <code>FRAGMENTED_MP4</code> container format packages the media into MP4 fragments (fMP4 or CMAF). This is the recommended packaging because there is minimal packaging overhead. The other container format option is <code>MPEG_TS</code>. HLS has supported MPEG TS chunks since it was released and is sometimes the only supported packaging on older HLS players. MPEG TS typically has a 5-25 percent packaging overhead. This means MPEG TS typically requires 5-25 percent more bandwidth and cost than fMP4.</p> <p>The default is <code>FRAGMENTED_MP4</code>.</p>

        :return: The container_format of this GetHLSStreamingSessionURLRequest.
        :rtype: str
        """
        return self._container_format

    @container_format.setter
    def container_format(self, container_format):
        """Sets the container_format of this GetHLSStreamingSessionURLRequest.

        <p>Specifies which format should be used for packaging the media. Specifying the <code>FRAGMENTED_MP4</code> container format packages the media into MP4 fragments (fMP4 or CMAF). This is the recommended packaging because there is minimal packaging overhead. The other container format option is <code>MPEG_TS</code>. HLS has supported MPEG TS chunks since it was released and is sometimes the only supported packaging on older HLS players. MPEG TS typically has a 5-25 percent packaging overhead. This means MPEG TS typically requires 5-25 percent more bandwidth and cost than fMP4.</p> <p>The default is <code>FRAGMENTED_MP4</code>.</p>

        :param container_format: The container_format of this GetHLSStreamingSessionURLRequest.
        :type container_format: str
        """
        allowed_values = ["FRAGMENTED_MP4", "MPEG_TS"]  # noqa: E501
        if container_format not in allowed_values:
            raise ValueError(
                "Invalid value for `container_format` ({0}), must be one of {1}"
                .format(container_format, allowed_values)
            )

        self._container_format = container_format

    @property
    def discontinuity_mode(self):
        """Gets the discontinuity_mode of this GetHLSStreamingSessionURLRequest.

        <p>Specifies when flags marking discontinuities between fragments are added to the media playlists.</p> <p>Media players typically build a timeline of media content to play, based on the timestamps of each fragment. This means that if there is any overlap or gap between fragments (as is typical if <a>HLSFragmentSelector</a> is set to <code>SERVER_TIMESTAMP</code>), the media player timeline will also have small gaps between fragments in some places, and will overwrite frames in other places. Gaps in the media player timeline can cause playback to stall and overlaps can cause playback to be jittery. When there are discontinuity flags between fragments, the media player is expected to reset the timeline, resulting in the next fragment being played immediately after the previous fragment. </p> <p>The following modes are supported:</p> <ul> <li> <p> <code>ALWAYS</code>: a discontinuity marker is placed between every fragment in the HLS media playlist. It is recommended to use a value of <code>ALWAYS</code> if the fragment timestamps are not accurate.</p> </li> <li> <p> <code>NEVER</code>: no discontinuity markers are placed anywhere. It is recommended to use a value of <code>NEVER</code> to ensure the media player timeline most accurately maps to the producer timestamps. </p> </li> <li> <p> <code>ON_DISCONTINUITY</code>: a discontinuity marker is placed between fragments that have a gap or overlap of more than 50 milliseconds. For most playback scenarios, it is recommended to use a value of <code>ON_DISCONTINUITY</code> so that the media player timeline is only reset when there is a significant issue with the media timeline (e.g. a missing fragment).</p> </li> </ul> <p>The default is <code>ALWAYS</code> when <a>HLSFragmentSelector</a> is set to <code>SERVER_TIMESTAMP</code>, and <code>NEVER</code> when it is set to <code>PRODUCER_TIMESTAMP</code>.</p>

        :return: The discontinuity_mode of this GetHLSStreamingSessionURLRequest.
        :rtype: str
        """
        return self._discontinuity_mode

    @discontinuity_mode.setter
    def discontinuity_mode(self, discontinuity_mode):
        """Sets the discontinuity_mode of this GetHLSStreamingSessionURLRequest.

        <p>Specifies when flags marking discontinuities between fragments are added to the media playlists.</p> <p>Media players typically build a timeline of media content to play, based on the timestamps of each fragment. This means that if there is any overlap or gap between fragments (as is typical if <a>HLSFragmentSelector</a> is set to <code>SERVER_TIMESTAMP</code>), the media player timeline will also have small gaps between fragments in some places, and will overwrite frames in other places. Gaps in the media player timeline can cause playback to stall and overlaps can cause playback to be jittery. When there are discontinuity flags between fragments, the media player is expected to reset the timeline, resulting in the next fragment being played immediately after the previous fragment. </p> <p>The following modes are supported:</p> <ul> <li> <p> <code>ALWAYS</code>: a discontinuity marker is placed between every fragment in the HLS media playlist. It is recommended to use a value of <code>ALWAYS</code> if the fragment timestamps are not accurate.</p> </li> <li> <p> <code>NEVER</code>: no discontinuity markers are placed anywhere. It is recommended to use a value of <code>NEVER</code> to ensure the media player timeline most accurately maps to the producer timestamps. </p> </li> <li> <p> <code>ON_DISCONTINUITY</code>: a discontinuity marker is placed between fragments that have a gap or overlap of more than 50 milliseconds. For most playback scenarios, it is recommended to use a value of <code>ON_DISCONTINUITY</code> so that the media player timeline is only reset when there is a significant issue with the media timeline (e.g. a missing fragment).</p> </li> </ul> <p>The default is <code>ALWAYS</code> when <a>HLSFragmentSelector</a> is set to <code>SERVER_TIMESTAMP</code>, and <code>NEVER</code> when it is set to <code>PRODUCER_TIMESTAMP</code>.</p>

        :param discontinuity_mode: The discontinuity_mode of this GetHLSStreamingSessionURLRequest.
        :type discontinuity_mode: str
        """
        allowed_values = ["ALWAYS", "NEVER", "ON_DISCONTINUITY"]  # noqa: E501
        if discontinuity_mode not in allowed_values:
            raise ValueError(
                "Invalid value for `discontinuity_mode` ({0}), must be one of {1}"
                .format(discontinuity_mode, allowed_values)
            )

        self._discontinuity_mode = discontinuity_mode

    @property
    def display_fragment_timestamp(self):
        """Gets the display_fragment_timestamp of this GetHLSStreamingSessionURLRequest.

        <p>Specifies when the fragment start timestamps should be included in the HLS media playlist. Typically, media players report the playhead position as a time relative to the start of the first fragment in the playback session. However, when the start timestamps are included in the HLS media playlist, some media players might report the current playhead as an absolute time based on the fragment timestamps. This can be useful for creating a playback experience that shows viewers the wall-clock time of the media.</p> <p>The default is <code>NEVER</code>. When <a>HLSFragmentSelector</a> is <code>SERVER_TIMESTAMP</code>, the timestamps will be the server start timestamps. Similarly, when <a>HLSFragmentSelector</a> is <code>PRODUCER_TIMESTAMP</code>, the timestamps will be the producer start timestamps. </p>

        :return: The display_fragment_timestamp of this GetHLSStreamingSessionURLRequest.
        :rtype: str
        """
        return self._display_fragment_timestamp

    @display_fragment_timestamp.setter
    def display_fragment_timestamp(self, display_fragment_timestamp):
        """Sets the display_fragment_timestamp of this GetHLSStreamingSessionURLRequest.

        <p>Specifies when the fragment start timestamps should be included in the HLS media playlist. Typically, media players report the playhead position as a time relative to the start of the first fragment in the playback session. However, when the start timestamps are included in the HLS media playlist, some media players might report the current playhead as an absolute time based on the fragment timestamps. This can be useful for creating a playback experience that shows viewers the wall-clock time of the media.</p> <p>The default is <code>NEVER</code>. When <a>HLSFragmentSelector</a> is <code>SERVER_TIMESTAMP</code>, the timestamps will be the server start timestamps. Similarly, when <a>HLSFragmentSelector</a> is <code>PRODUCER_TIMESTAMP</code>, the timestamps will be the producer start timestamps. </p>

        :param display_fragment_timestamp: The display_fragment_timestamp of this GetHLSStreamingSessionURLRequest.
        :type display_fragment_timestamp: str
        """
        allowed_values = ["ALWAYS", "NEVER"]  # noqa: E501
        if display_fragment_timestamp not in allowed_values:
            raise ValueError(
                "Invalid value for `display_fragment_timestamp` ({0}), must be one of {1}"
                .format(display_fragment_timestamp, allowed_values)
            )

        self._display_fragment_timestamp = display_fragment_timestamp

    @property
    def expires(self):
        """Gets the expires of this GetHLSStreamingSessionURLRequest.

        <p>The time in seconds until the requested session expires. This value can be between 300 (5 minutes) and 43200 (12 hours).</p> <p>When a session expires, no new calls to <code>GetHLSMasterPlaylist</code>, <code>GetHLSMediaPlaylist</code>, <code>GetMP4InitFragment</code>, <code>GetMP4MediaFragment</code>, or <code>GetTSFragment</code> can be made for that session.</p> <p>The default is 300 (5 minutes).</p>

        :return: The expires of this GetHLSStreamingSessionURLRequest.
        :rtype: int
        """
        return self._expires

    @expires.setter
    def expires(self, expires):
        """Sets the expires of this GetHLSStreamingSessionURLRequest.

        <p>The time in seconds until the requested session expires. This value can be between 300 (5 minutes) and 43200 (12 hours).</p> <p>When a session expires, no new calls to <code>GetHLSMasterPlaylist</code>, <code>GetHLSMediaPlaylist</code>, <code>GetMP4InitFragment</code>, <code>GetMP4MediaFragment</code>, or <code>GetTSFragment</code> can be made for that session.</p> <p>The default is 300 (5 minutes).</p>

        :param expires: The expires of this GetHLSStreamingSessionURLRequest.
        :type expires: int
        """
        if expires is not None and expires > 43200:
            raise ValueError("Invalid value for `expires`, must be a value less than or equal to `43200`")
        if expires is not None and expires < 300:
            raise ValueError("Invalid value for `expires`, must be a value greater than or equal to `300`")

        self._expires = expires

    @property
    def max_media_playlist_fragment_results(self):
        """Gets the max_media_playlist_fragment_results of this GetHLSStreamingSessionURLRequest.

        <p>The maximum number of fragments that are returned in the HLS media playlists.</p> <p>When the <code>PlaybackMode</code> is <code>LIVE</code>, the most recent fragments are returned up to this value. When the <code>PlaybackMode</code> is <code>ON_DEMAND</code>, the oldest fragments are returned, up to this maximum number.</p> <p>When there are a higher number of fragments available in a live HLS media playlist, video players often buffer content before starting playback. Increasing the buffer size increases the playback latency, but it decreases the likelihood that rebuffering will occur during playback. We recommend that a live HLS media playlist have a minimum of 3 fragments and a maximum of 10 fragments.</p> <p>The default is 5 fragments if <code>PlaybackMode</code> is <code>LIVE</code> or <code>LIVE_REPLAY</code>, and 1,000 if <code>PlaybackMode</code> is <code>ON_DEMAND</code>. </p> <p>The maximum value of 5,000 fragments corresponds to more than 80 minutes of video on streams with 1-second fragments, and more than 13 hours of video on streams with 10-second fragments.</p>

        :return: The max_media_playlist_fragment_results of this GetHLSStreamingSessionURLRequest.
        :rtype: int
        """
        return self._max_media_playlist_fragment_results

    @max_media_playlist_fragment_results.setter
    def max_media_playlist_fragment_results(self, max_media_playlist_fragment_results):
        """Sets the max_media_playlist_fragment_results of this GetHLSStreamingSessionURLRequest.

        <p>The maximum number of fragments that are returned in the HLS media playlists.</p> <p>When the <code>PlaybackMode</code> is <code>LIVE</code>, the most recent fragments are returned up to this value. When the <code>PlaybackMode</code> is <code>ON_DEMAND</code>, the oldest fragments are returned, up to this maximum number.</p> <p>When there are a higher number of fragments available in a live HLS media playlist, video players often buffer content before starting playback. Increasing the buffer size increases the playback latency, but it decreases the likelihood that rebuffering will occur during playback. We recommend that a live HLS media playlist have a minimum of 3 fragments and a maximum of 10 fragments.</p> <p>The default is 5 fragments if <code>PlaybackMode</code> is <code>LIVE</code> or <code>LIVE_REPLAY</code>, and 1,000 if <code>PlaybackMode</code> is <code>ON_DEMAND</code>. </p> <p>The maximum value of 5,000 fragments corresponds to more than 80 minutes of video on streams with 1-second fragments, and more than 13 hours of video on streams with 10-second fragments.</p>

        :param max_media_playlist_fragment_results: The max_media_playlist_fragment_results of this GetHLSStreamingSessionURLRequest.
        :type max_media_playlist_fragment_results: int
        """
        if max_media_playlist_fragment_results is not None and max_media_playlist_fragment_results > 5000:
            raise ValueError("Invalid value for `max_media_playlist_fragment_results`, must be a value less than or equal to `5000`")
        if max_media_playlist_fragment_results is not None and max_media_playlist_fragment_results < 1:
            raise ValueError("Invalid value for `max_media_playlist_fragment_results`, must be a value greater than or equal to `1`")

        self._max_media_playlist_fragment_results = max_media_playlist_fragment_results
