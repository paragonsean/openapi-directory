/**
 * Amazon DynamoDB
 * <p>Amazon DynamoDB is a fast, highly scalable, highly available, cost-effective non-relational database service.</p> <p>Amazon DynamoDB removes traditional scalability limitations on data storage while maintaining low latency and predictable performance.</p>
 *
 * The version of the OpenAPI document: 2011-12-05
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ScanInputExclusiveStartKey from './ScanInputExclusiveStartKey';

/**
 * The ScanInput model module.
 * @module model/ScanInput
 * @version 2011-12-05
 */
class ScanInput {
    /**
     * Constructs a new <code>ScanInput</code>.
     * @alias module:model/ScanInput
     * @param tableName {String} 
     */
    constructor(tableName) { 
        
        ScanInput.initialize(this, tableName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tableName) { 
        obj['TableName'] = tableName;
    }

    /**
     * Constructs a <code>ScanInput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ScanInput} obj Optional instance to populate.
     * @return {module:model/ScanInput} The populated <code>ScanInput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ScanInput();

            if (data.hasOwnProperty('TableName')) {
                obj['TableName'] = ApiClient.convertToType(data['TableName'], 'String');
            }
            if (data.hasOwnProperty('AttributesToGet')) {
                obj['AttributesToGet'] = ApiClient.convertToType(data['AttributesToGet'], ['String']);
            }
            if (data.hasOwnProperty('Limit')) {
                obj['Limit'] = ApiClient.convertToType(data['Limit'], 'Number');
            }
            if (data.hasOwnProperty('Count')) {
                obj['Count'] = ApiClient.convertToType(data['Count'], 'Boolean');
            }
            if (data.hasOwnProperty('ScanFilter')) {
                obj['ScanFilter'] = ApiClient.convertToType(data['ScanFilter'], Object);
            }
            if (data.hasOwnProperty('ExclusiveStartKey')) {
                obj['ExclusiveStartKey'] = ScanInputExclusiveStartKey.constructFromObject(data['ExclusiveStartKey']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ScanInput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ScanInput</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ScanInput.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `TableName`
        if (data['TableName']) { // data not null
          String.validateJSON(data['TableName']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['AttributesToGet'])) {
            throw new Error("Expected the field `AttributesToGet` to be an array in the JSON data but got " + data['AttributesToGet']);
        }
        // validate the optional field `Limit`
        if (data['Limit']) { // data not null
          Number.validateJSON(data['Limit']);
        }
        // validate the optional field `Count`
        if (data['Count']) { // data not null
          Boolean.validateJSON(data['Count']);
        }
        // validate the optional field `ScanFilter`
        if (data['ScanFilter']) { // data not null
          Object.validateJSON(data['ScanFilter']);
        }
        // validate the optional field `ExclusiveStartKey`
        if (data['ExclusiveStartKey']) { // data not null
          ScanInputExclusiveStartKey.validateJSON(data['ExclusiveStartKey']);
        }

        return true;
    }


}

ScanInput.RequiredProperties = ["TableName"];

/**
 * @member {String} TableName
 */
ScanInput.prototype['TableName'] = undefined;

/**
 * List of <code>Attribute</code> names. If attribute names are not specified then all attributes will be returned. If some attributes are not found, they will not appear in the result.
 * @member {Array.<String>} AttributesToGet
 */
ScanInput.prototype['AttributesToGet'] = undefined;

/**
 * @member {Number} Limit
 */
ScanInput.prototype['Limit'] = undefined;

/**
 * @member {Boolean} Count
 */
ScanInput.prototype['Count'] = undefined;

/**
 * @member {Object} ScanFilter
 */
ScanInput.prototype['ScanFilter'] = undefined;

/**
 * @member {module:model/ScanInputExclusiveStartKey} ExclusiveStartKey
 */
ScanInput.prototype['ExclusiveStartKey'] = undefined;






export default ScanInput;

