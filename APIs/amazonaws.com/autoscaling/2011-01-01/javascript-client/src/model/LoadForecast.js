/**
 * Auto Scaling
 * <fullname>Amazon EC2 Auto Scaling</fullname> <p>Amazon EC2 Auto Scaling is designed to automatically launch and terminate EC2 instances based on user-defined scaling policies, scheduled actions, and health checks.</p> <p>For more information, see the <a href=\"https://docs.aws.amazon.com/autoscaling/ec2/userguide/\">Amazon EC2 Auto Scaling User Guide</a> and the <a href=\"https://docs.aws.amazon.com/autoscaling/ec2/APIReference/Welcome.html\">Amazon EC2 Auto Scaling API Reference</a>.</p>
 *
 * The version of the OpenAPI document: 2011-01-01
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LoadForecastMetricSpecification from './LoadForecastMetricSpecification';

/**
 * The LoadForecast model module.
 * @module model/LoadForecast
 * @version 2011-01-01
 */
class LoadForecast {
    /**
     * Constructs a new <code>LoadForecast</code>.
     * A &lt;code&gt;GetPredictiveScalingForecast&lt;/code&gt; call returns the load forecast for a predictive scaling policy. This structure includes the data points for that load forecast, along with the timestamps of those data points and the metric specification. 
     * @alias module:model/LoadForecast
     * @param timestamps {Array} 
     * @param values {Array} 
     * @param metricSpecification {module:model/LoadForecastMetricSpecification} 
     */
    constructor(timestamps, values, metricSpecification) { 
        
        LoadForecast.initialize(this, timestamps, values, metricSpecification);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, timestamps, values, metricSpecification) { 
        obj['Timestamps'] = timestamps;
        obj['Values'] = values;
        obj['MetricSpecification'] = metricSpecification;
    }

    /**
     * Constructs a <code>LoadForecast</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LoadForecast} obj Optional instance to populate.
     * @return {module:model/LoadForecast} The populated <code>LoadForecast</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LoadForecast();

            if (data.hasOwnProperty('Timestamps')) {
                obj['Timestamps'] = ApiClient.convertToType(data['Timestamps'], Array);
            }
            if (data.hasOwnProperty('Values')) {
                obj['Values'] = ApiClient.convertToType(data['Values'], Array);
            }
            if (data.hasOwnProperty('MetricSpecification')) {
                obj['MetricSpecification'] = LoadForecastMetricSpecification.constructFromObject(data['MetricSpecification']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LoadForecast</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LoadForecast</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LoadForecast.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `Timestamps`
        if (data['Timestamps']) { // data not null
          Array.validateJSON(data['Timestamps']);
        }
        // validate the optional field `Values`
        if (data['Values']) { // data not null
          Array.validateJSON(data['Values']);
        }
        // validate the optional field `MetricSpecification`
        if (data['MetricSpecification']) { // data not null
          LoadForecastMetricSpecification.validateJSON(data['MetricSpecification']);
        }

        return true;
    }


}

LoadForecast.RequiredProperties = ["Timestamps", "Values", "MetricSpecification"];

/**
 * @member {Array} Timestamps
 */
LoadForecast.prototype['Timestamps'] = undefined;

/**
 * @member {Array} Values
 */
LoadForecast.prototype['Values'] = undefined;

/**
 * @member {module:model/LoadForecastMetricSpecification} MetricSpecification
 */
LoadForecast.prototype['MetricSpecification'] = undefined;






export default LoadForecast;

