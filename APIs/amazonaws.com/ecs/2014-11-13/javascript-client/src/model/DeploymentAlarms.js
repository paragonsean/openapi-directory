/**
 * Amazon EC2 Container Service
 * <fullname>Amazon Elastic Container Service</fullname> <p>Amazon Elastic Container Service (Amazon ECS) is a highly scalable, fast, container management service. It makes it easy to run, stop, and manage Docker containers. You can host your cluster on a serverless infrastructure that's managed by Amazon ECS by launching your services or tasks on Fargate. For more control, you can host your tasks on a cluster of Amazon Elastic Compute Cloud (Amazon EC2) or External (on-premises) instances that you manage.</p> <p>Amazon ECS makes it easy to launch and stop container-based applications with simple API calls. This makes it easy to get the state of your cluster from a centralized service, and gives you access to many familiar Amazon EC2 features.</p> <p>You can use Amazon ECS to schedule the placement of containers across your cluster based on your resource needs, isolation policies, and availability requirements. With Amazon ECS, you don't need to operate your own cluster management and configuration management systems. You also don't need to worry about scaling your management infrastructure.</p>
 *
 * The version of the OpenAPI document: 2014-11-13
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DeploymentAlarms model module.
 * @module model/DeploymentAlarms
 * @version 2014-11-13
 */
class DeploymentAlarms {
    /**
     * Constructs a new <code>DeploymentAlarms</code>.
     * &lt;p&gt;One of the methods which provide a way for you to quickly identify when a deployment has failed, and then to optionally roll back the failure to the last working deployment.&lt;/p&gt; &lt;p&gt;When the alarms are generated, Amazon ECS sets the service deployment to failed. Set the rollback parameter to have Amazon ECS to roll back your service to the last completed deployment after a failure.&lt;/p&gt; &lt;p&gt;You can only use the &lt;code&gt;DeploymentAlarms&lt;/code&gt; method to detect failures when the &lt;code&gt;DeploymentController&lt;/code&gt; is set to &lt;code&gt;ECS&lt;/code&gt; (rolling update).&lt;/p&gt; &lt;p&gt;For more information, see &lt;a href&#x3D;\&quot;https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-type-ecs.html\&quot;&gt;Rolling update&lt;/a&gt; in the &lt;i&gt; &lt;i&gt;Amazon Elastic Container Service Developer Guide&lt;/i&gt; &lt;/i&gt;.&lt;/p&gt;
     * @alias module:model/DeploymentAlarms
     * @param alarmNames {Array} 
     * @param enable {Boolean} 
     * @param rollback {Boolean} 
     */
    constructor(alarmNames, enable, rollback) { 
        
        DeploymentAlarms.initialize(this, alarmNames, enable, rollback);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, alarmNames, enable, rollback) { 
        obj['alarmNames'] = alarmNames;
        obj['enable'] = enable;
        obj['rollback'] = rollback;
    }

    /**
     * Constructs a <code>DeploymentAlarms</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeploymentAlarms} obj Optional instance to populate.
     * @return {module:model/DeploymentAlarms} The populated <code>DeploymentAlarms</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeploymentAlarms();

            if (data.hasOwnProperty('alarmNames')) {
                obj['alarmNames'] = ApiClient.convertToType(data['alarmNames'], Array);
            }
            if (data.hasOwnProperty('enable')) {
                obj['enable'] = ApiClient.convertToType(data['enable'], 'Boolean');
            }
            if (data.hasOwnProperty('rollback')) {
                obj['rollback'] = ApiClient.convertToType(data['rollback'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeploymentAlarms</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeploymentAlarms</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeploymentAlarms.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `alarmNames`
        if (data['alarmNames']) { // data not null
          Array.validateJSON(data['alarmNames']);
        }
        // validate the optional field `enable`
        if (data['enable']) { // data not null
          Boolean.validateJSON(data['enable']);
        }
        // validate the optional field `rollback`
        if (data['rollback']) { // data not null
          Boolean.validateJSON(data['rollback']);
        }

        return true;
    }


}

DeploymentAlarms.RequiredProperties = ["alarmNames", "enable", "rollback"];

/**
 * @member {Array} alarmNames
 */
DeploymentAlarms.prototype['alarmNames'] = undefined;

/**
 * @member {Boolean} enable
 */
DeploymentAlarms.prototype['enable'] = undefined;

/**
 * @member {Boolean} rollback
 */
DeploymentAlarms.prototype['rollback'] = undefined;






export default DeploymentAlarms;

