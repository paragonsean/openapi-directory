/**
 * CloudWatch RUM
 * <p>With Amazon CloudWatch RUM, you can perform real-user monitoring to collect client-side data about your web application performance from actual user sessions in real time. The data collected includes page load times, client-side errors, and user behavior. When you view this data, you can see it all aggregated together and also see breakdowns by the browsers and devices that your customers use.</p> <p>You can use the collected data to quickly identify and debug client-side performance issues. CloudWatch RUM helps you visualize anomalies in your application performance and find relevant debugging data such as error messages, stack traces, and user sessions. You can also use RUM to understand the range of end-user impact including the number of users, geolocations, and browsers used.</p>
 *
 * The version of the OpenAPI document: 2018-05-10
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PutRumMetricsDestinationRequest model module.
 * @module model/PutRumMetricsDestinationRequest
 * @version 2018-05-10
 */
class PutRumMetricsDestinationRequest {
    /**
     * Constructs a new <code>PutRumMetricsDestinationRequest</code>.
     * @alias module:model/PutRumMetricsDestinationRequest
     * @param destination {module:model/PutRumMetricsDestinationRequest.DestinationEnum} Defines the destination to send the metrics to. Valid values are <code>CloudWatch</code> and <code>Evidently</code>. If you specify <code>Evidently</code>, you must also specify the ARN of the CloudWatchEvidently experiment that is to be the destination and an IAM role that has permission to write to the experiment.
     */
    constructor(destination) { 
        
        PutRumMetricsDestinationRequest.initialize(this, destination);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destination) { 
        obj['Destination'] = destination;
    }

    /**
     * Constructs a <code>PutRumMetricsDestinationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PutRumMetricsDestinationRequest} obj Optional instance to populate.
     * @return {module:model/PutRumMetricsDestinationRequest} The populated <code>PutRumMetricsDestinationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PutRumMetricsDestinationRequest();

            if (data.hasOwnProperty('Destination')) {
                obj['Destination'] = ApiClient.convertToType(data['Destination'], 'String');
            }
            if (data.hasOwnProperty('DestinationArn')) {
                obj['DestinationArn'] = ApiClient.convertToType(data['DestinationArn'], 'String');
            }
            if (data.hasOwnProperty('IamRoleArn')) {
                obj['IamRoleArn'] = ApiClient.convertToType(data['IamRoleArn'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PutRumMetricsDestinationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PutRumMetricsDestinationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PutRumMetricsDestinationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Destination'] && !(typeof data['Destination'] === 'string' || data['Destination'] instanceof String)) {
            throw new Error("Expected the field `Destination` to be a primitive type in the JSON string but got " + data['Destination']);
        }
        // ensure the json data is a string
        if (data['DestinationArn'] && !(typeof data['DestinationArn'] === 'string' || data['DestinationArn'] instanceof String)) {
            throw new Error("Expected the field `DestinationArn` to be a primitive type in the JSON string but got " + data['DestinationArn']);
        }
        // ensure the json data is a string
        if (data['IamRoleArn'] && !(typeof data['IamRoleArn'] === 'string' || data['IamRoleArn'] instanceof String)) {
            throw new Error("Expected the field `IamRoleArn` to be a primitive type in the JSON string but got " + data['IamRoleArn']);
        }

        return true;
    }


}

PutRumMetricsDestinationRequest.RequiredProperties = ["Destination"];

/**
 * Defines the destination to send the metrics to. Valid values are <code>CloudWatch</code> and <code>Evidently</code>. If you specify <code>Evidently</code>, you must also specify the ARN of the CloudWatchEvidently experiment that is to be the destination and an IAM role that has permission to write to the experiment.
 * @member {module:model/PutRumMetricsDestinationRequest.DestinationEnum} Destination
 */
PutRumMetricsDestinationRequest.prototype['Destination'] = undefined;

/**
 * Use this parameter only if <code>Destination</code> is <code>Evidently</code>. This parameter specifies the ARN of the Evidently experiment that will receive the extended metrics.
 * @member {String} DestinationArn
 */
PutRumMetricsDestinationRequest.prototype['DestinationArn'] = undefined;

/**
 * <p>This parameter is required if <code>Destination</code> is <code>Evidently</code>. If <code>Destination</code> is <code>CloudWatch</code>, do not use this parameter.</p> <p>This parameter specifies the ARN of an IAM role that RUM will assume to write to the Evidently experiment that you are sending metrics to. This role must have permission to write to that experiment.</p>
 * @member {String} IamRoleArn
 */
PutRumMetricsDestinationRequest.prototype['IamRoleArn'] = undefined;





/**
 * Allowed values for the <code>Destination</code> property.
 * @enum {String}
 * @readonly
 */
PutRumMetricsDestinationRequest['DestinationEnum'] = {

    /**
     * value: "CloudWatch"
     * @const
     */
    "CloudWatch": "CloudWatch",

    /**
     * value: "Evidently"
     * @const
     */
    "Evidently": "Evidently"
};



export default PutRumMetricsDestinationRequest;

