/**
 * Migration Hub Strategy Recommendations
 * <p><fullname>Migration Hub Strategy Recommendations</fullname> <p>This API reference provides descriptions, syntax, and other details about each of the actions and data types for Migration Hub Strategy Recommendations (Strategy Recommendations). The topic for each action shows the API request parameters and the response. Alternatively, you can use one of the AWS SDKs to access an API that is tailored to the programming language or platform that you're using. For more information, see <a href=\"http://aws.amazon.com/tools/#SDKs\">AWS SDKs</a>.</p></p>
 *
 * The version of the OpenAPI document: 2020-02-19
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AssessmentTarget from './AssessmentTarget';

/**
 * The StartAssessmentRequest model module.
 * @module model/StartAssessmentRequest
 * @version 2020-02-19
 */
class StartAssessmentRequest {
    /**
     * Constructs a new <code>StartAssessmentRequest</code>.
     * @alias module:model/StartAssessmentRequest
     */
    constructor() { 
        
        StartAssessmentRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>StartAssessmentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StartAssessmentRequest} obj Optional instance to populate.
     * @return {module:model/StartAssessmentRequest} The populated <code>StartAssessmentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StartAssessmentRequest();

            if (data.hasOwnProperty('assessmentTargets')) {
                obj['assessmentTargets'] = ApiClient.convertToType(data['assessmentTargets'], [AssessmentTarget]);
            }
            if (data.hasOwnProperty('s3bucketForAnalysisData')) {
                obj['s3bucketForAnalysisData'] = ApiClient.convertToType(data['s3bucketForAnalysisData'], 'String');
            }
            if (data.hasOwnProperty('s3bucketForReportData')) {
                obj['s3bucketForReportData'] = ApiClient.convertToType(data['s3bucketForReportData'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StartAssessmentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StartAssessmentRequest</code>.
     */
    static validateJSON(data) {
        if (data['assessmentTargets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assessmentTargets'])) {
                throw new Error("Expected the field `assessmentTargets` to be an array in the JSON data but got " + data['assessmentTargets']);
            }
            // validate the optional field `assessmentTargets` (array)
            for (const item of data['assessmentTargets']) {
                AssessmentTarget.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['s3bucketForAnalysisData'] && !(typeof data['s3bucketForAnalysisData'] === 'string' || data['s3bucketForAnalysisData'] instanceof String)) {
            throw new Error("Expected the field `s3bucketForAnalysisData` to be a primitive type in the JSON string but got " + data['s3bucketForAnalysisData']);
        }
        // ensure the json data is a string
        if (data['s3bucketForReportData'] && !(typeof data['s3bucketForReportData'] === 'string' || data['s3bucketForReportData'] instanceof String)) {
            throw new Error("Expected the field `s3bucketForReportData` to be a primitive type in the JSON string but got " + data['s3bucketForReportData']);
        }

        return true;
    }


}



/**
 * List of criteria for assessment.
 * @member {Array.<module:model/AssessmentTarget>} assessmentTargets
 */
StartAssessmentRequest.prototype['assessmentTargets'] = undefined;

/**
 *  The S3 bucket used by the collectors to send analysis data to the service. The bucket name must begin with <code>migrationhub-strategy-</code>. 
 * @member {String} s3bucketForAnalysisData
 */
StartAssessmentRequest.prototype['s3bucketForAnalysisData'] = undefined;

/**
 *  The S3 bucket where all the reports generated by the service are stored. The bucket name must begin with <code>migrationhub-strategy-</code>. 
 * @member {String} s3bucketForReportData
 */
StartAssessmentRequest.prototype['s3bucketForReportData'] = undefined;






export default StartAssessmentRequest;

