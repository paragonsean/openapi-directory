/**
 * Amazon Connect Customer Profiles
 * <fullname>Amazon Connect Customer Profiles</fullname> <p>Amazon Connect Customer Profiles is a unified customer profile for your contact center that has pre-built connectors powered by AppFlow that make it easy to combine customer information from third party applications, such as Salesforce (CRM), ServiceNow (ITSM), and your enterprise resource planning (ERP), with contact history from your Amazon Connect contact center. If you're new to Amazon Connect, you might find it helpful to review the <a href=\"https://docs.aws.amazon.com/connect/latest/adminguide/\">Amazon Connect Administrator Guide</a>.</p>
 *
 * The version of the OpenAPI document: 2020-08-15
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetSimilarProfilesRequest model module.
 * @module model/GetSimilarProfilesRequest
 * @version 2020-08-15
 */
class GetSimilarProfilesRequest {
    /**
     * Constructs a new <code>GetSimilarProfilesRequest</code>.
     * @alias module:model/GetSimilarProfilesRequest
     * @param matchType {module:model/GetSimilarProfilesRequest.MatchTypeEnum} Specify the type of matching to get similar profiles for.
     * @param searchKey {String} The string indicating the search key to be used.
     * @param searchValue {String} The string based on <code>SearchKey</code> to be searched for similar profiles.
     */
    constructor(matchType, searchKey, searchValue) { 
        
        GetSimilarProfilesRequest.initialize(this, matchType, searchKey, searchValue);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, matchType, searchKey, searchValue) { 
        obj['MatchType'] = matchType;
        obj['SearchKey'] = searchKey;
        obj['SearchValue'] = searchValue;
    }

    /**
     * Constructs a <code>GetSimilarProfilesRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetSimilarProfilesRequest} obj Optional instance to populate.
     * @return {module:model/GetSimilarProfilesRequest} The populated <code>GetSimilarProfilesRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetSimilarProfilesRequest();

            if (data.hasOwnProperty('MatchType')) {
                obj['MatchType'] = ApiClient.convertToType(data['MatchType'], 'String');
            }
            if (data.hasOwnProperty('SearchKey')) {
                obj['SearchKey'] = ApiClient.convertToType(data['SearchKey'], 'String');
            }
            if (data.hasOwnProperty('SearchValue')) {
                obj['SearchValue'] = ApiClient.convertToType(data['SearchValue'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetSimilarProfilesRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetSimilarProfilesRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetSimilarProfilesRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['MatchType'] && !(typeof data['MatchType'] === 'string' || data['MatchType'] instanceof String)) {
            throw new Error("Expected the field `MatchType` to be a primitive type in the JSON string but got " + data['MatchType']);
        }
        // ensure the json data is a string
        if (data['SearchKey'] && !(typeof data['SearchKey'] === 'string' || data['SearchKey'] instanceof String)) {
            throw new Error("Expected the field `SearchKey` to be a primitive type in the JSON string but got " + data['SearchKey']);
        }
        // ensure the json data is a string
        if (data['SearchValue'] && !(typeof data['SearchValue'] === 'string' || data['SearchValue'] instanceof String)) {
            throw new Error("Expected the field `SearchValue` to be a primitive type in the JSON string but got " + data['SearchValue']);
        }

        return true;
    }


}

GetSimilarProfilesRequest.RequiredProperties = ["MatchType", "SearchKey", "SearchValue"];

/**
 * Specify the type of matching to get similar profiles for.
 * @member {module:model/GetSimilarProfilesRequest.MatchTypeEnum} MatchType
 */
GetSimilarProfilesRequest.prototype['MatchType'] = undefined;

/**
 * The string indicating the search key to be used.
 * @member {String} SearchKey
 */
GetSimilarProfilesRequest.prototype['SearchKey'] = undefined;

/**
 * The string based on <code>SearchKey</code> to be searched for similar profiles.
 * @member {String} SearchValue
 */
GetSimilarProfilesRequest.prototype['SearchValue'] = undefined;





/**
 * Allowed values for the <code>MatchType</code> property.
 * @enum {String}
 * @readonly
 */
GetSimilarProfilesRequest['MatchTypeEnum'] = {

    /**
     * value: "RULE_BASED_MATCHING"
     * @const
     */
    "RULE_BASED_MATCHING": "RULE_BASED_MATCHING",

    /**
     * value: "ML_BASED_MATCHING"
     * @const
     */
    "ML_BASED_MATCHING": "ML_BASED_MATCHING"
};



export default GetSimilarProfilesRequest;

