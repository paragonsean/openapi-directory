/**
 * Amazon Machine Learning
 * Definition of the public APIs exposed by Amazon Machine Learning
 *
 * The version of the OpenAPI document: 2014-12-12
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RDSDatabaseCredentials from './RDSDatabaseCredentials';

/**
 * The RDSDataSpecDatabaseCredentials model module.
 * @module model/RDSDataSpecDatabaseCredentials
 * @version 2014-12-12
 */
class RDSDataSpecDatabaseCredentials {
    /**
     * Constructs a new <code>RDSDataSpecDatabaseCredentials</code>.
     * @alias module:model/RDSDataSpecDatabaseCredentials
     * @implements module:model/RDSDatabaseCredentials
     * @param username {String} The username to be used by Amazon ML to connect to database on an Amazon RDS instance. The username should have sufficient permissions to execute an <code>RDSSelectSqlQuery</code> query.
     * @param password {String} The password to be used by Amazon ML to connect to a database on an RDS DB instance. The password should have sufficient permissions to execute the <code>RDSSelectQuery</code> query.
     */
    constructor(username, password) { 
        RDSDatabaseCredentials.initialize(this, username, password);
        RDSDataSpecDatabaseCredentials.initialize(this, username, password);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, password) { 
        obj['Username'] = username;
        obj['Password'] = password;
    }

    /**
     * Constructs a <code>RDSDataSpecDatabaseCredentials</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RDSDataSpecDatabaseCredentials} obj Optional instance to populate.
     * @return {module:model/RDSDataSpecDatabaseCredentials} The populated <code>RDSDataSpecDatabaseCredentials</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RDSDataSpecDatabaseCredentials();
            RDSDatabaseCredentials.constructFromObject(data, obj);

            if (data.hasOwnProperty('Username')) {
                obj['Username'] = ApiClient.convertToType(data['Username'], 'String');
            }
            if (data.hasOwnProperty('Password')) {
                obj['Password'] = ApiClient.convertToType(data['Password'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RDSDataSpecDatabaseCredentials</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RDSDataSpecDatabaseCredentials</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RDSDataSpecDatabaseCredentials.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Username'] && !(typeof data['Username'] === 'string' || data['Username'] instanceof String)) {
            throw new Error("Expected the field `Username` to be a primitive type in the JSON string but got " + data['Username']);
        }
        // ensure the json data is a string
        if (data['Password'] && !(typeof data['Password'] === 'string' || data['Password'] instanceof String)) {
            throw new Error("Expected the field `Password` to be a primitive type in the JSON string but got " + data['Password']);
        }

        return true;
    }


}

RDSDataSpecDatabaseCredentials.RequiredProperties = ["Username", "Password"];

/**
 * The username to be used by Amazon ML to connect to database on an Amazon RDS instance. The username should have sufficient permissions to execute an <code>RDSSelectSqlQuery</code> query.
 * @member {String} Username
 */
RDSDataSpecDatabaseCredentials.prototype['Username'] = undefined;

/**
 * The password to be used by Amazon ML to connect to a database on an RDS DB instance. The password should have sufficient permissions to execute the <code>RDSSelectQuery</code> query.
 * @member {String} Password
 */
RDSDataSpecDatabaseCredentials.prototype['Password'] = undefined;


// Implement RDSDatabaseCredentials interface:
/**
 * The username to be used by Amazon ML to connect to database on an Amazon RDS instance. The username should have sufficient permissions to execute an <code>RDSSelectSqlQuery</code> query.
 * @member {String} Username
 */
RDSDatabaseCredentials.prototype['Username'] = undefined;
/**
 * The password to be used by Amazon ML to connect to a database on an RDS DB instance. The password should have sufficient permissions to execute the <code>RDSSelectQuery</code> query.
 * @member {String} Password
 */
RDSDatabaseCredentials.prototype['Password'] = undefined;




export default RDSDataSpecDatabaseCredentials;

