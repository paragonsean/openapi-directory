/**
 * Inspector2
 * Amazon Inspector is a vulnerability discovery service that automates continuous scanning for security vulnerabilities within your Amazon EC2 and Amazon ECR environments.
 *
 * The version of the OpenAPI document: 2020-06-08
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ResourceScanType from './ResourceScanType';

/**
 * The EnableRequest model module.
 * @module model/EnableRequest
 * @version 2020-06-08
 */
class EnableRequest {
    /**
     * Constructs a new <code>EnableRequest</code>.
     * @alias module:model/EnableRequest
     * @param resourceTypes {Array.<module:model/ResourceScanType>} The resource scan types you want to enable.
     */
    constructor(resourceTypes) { 
        
        EnableRequest.initialize(this, resourceTypes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, resourceTypes) { 
        obj['resourceTypes'] = resourceTypes;
    }

    /**
     * Constructs a <code>EnableRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EnableRequest} obj Optional instance to populate.
     * @return {module:model/EnableRequest} The populated <code>EnableRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EnableRequest();

            if (data.hasOwnProperty('accountIds')) {
                obj['accountIds'] = ApiClient.convertToType(data['accountIds'], ['String']);
            }
            if (data.hasOwnProperty('clientToken')) {
                obj['clientToken'] = ApiClient.convertToType(data['clientToken'], 'String');
            }
            if (data.hasOwnProperty('resourceTypes')) {
                obj['resourceTypes'] = ApiClient.convertToType(data['resourceTypes'], [ResourceScanType]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EnableRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EnableRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EnableRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['accountIds'])) {
            throw new Error("Expected the field `accountIds` to be an array in the JSON data but got " + data['accountIds']);
        }
        // ensure the json data is a string
        if (data['clientToken'] && !(typeof data['clientToken'] === 'string' || data['clientToken'] instanceof String)) {
            throw new Error("Expected the field `clientToken` to be a primitive type in the JSON string but got " + data['clientToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['resourceTypes'])) {
            throw new Error("Expected the field `resourceTypes` to be an array in the JSON data but got " + data['resourceTypes']);
        }

        return true;
    }


}

EnableRequest.RequiredProperties = ["resourceTypes"];

/**
 * A list of account IDs you want to enable Amazon Inspector scans for.
 * @member {Array.<String>} accountIds
 */
EnableRequest.prototype['accountIds'] = undefined;

/**
 * The idempotency token for the request.
 * @member {String} clientToken
 */
EnableRequest.prototype['clientToken'] = undefined;

/**
 * The resource scan types you want to enable.
 * @member {Array.<module:model/ResourceScanType>} resourceTypes
 */
EnableRequest.prototype['resourceTypes'] = undefined;






export default EnableRequest;

