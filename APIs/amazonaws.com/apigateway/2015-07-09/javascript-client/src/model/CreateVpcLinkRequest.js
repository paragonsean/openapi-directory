/**
 * Amazon API Gateway
 * <fullname>Amazon API Gateway</fullname> <p>Amazon API Gateway helps developers deliver robust, secure, and scalable mobile and web application back ends. API Gateway allows developers to securely connect mobile and web applications to APIs that run on AWS Lambda, Amazon EC2, or other publicly addressable web services that are hosted outside of AWS.</p>
 *
 * The version of the OpenAPI document: 2015-07-09
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateVpcLinkRequest model module.
 * @module model/CreateVpcLinkRequest
 * @version 2015-07-09
 */
class CreateVpcLinkRequest {
    /**
     * Constructs a new <code>CreateVpcLinkRequest</code>.
     * @alias module:model/CreateVpcLinkRequest
     * @param name {String} The name used to label and identify the VPC link.
     * @param targetArns {Array.<String>} The ARN of the network load balancer of the VPC targeted by the VPC link. The network load balancer must be owned by the same AWS account of the API owner.
     */
    constructor(name, targetArns) { 
        
        CreateVpcLinkRequest.initialize(this, name, targetArns);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, targetArns) { 
        obj['name'] = name;
        obj['targetArns'] = targetArns;
    }

    /**
     * Constructs a <code>CreateVpcLinkRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateVpcLinkRequest} obj Optional instance to populate.
     * @return {module:model/CreateVpcLinkRequest} The populated <code>CreateVpcLinkRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateVpcLinkRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('targetArns')) {
                obj['targetArns'] = ApiClient.convertToType(data['targetArns'], ['String']);
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateVpcLinkRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateVpcLinkRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateVpcLinkRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['targetArns'])) {
            throw new Error("Expected the field `targetArns` to be an array in the JSON data but got " + data['targetArns']);
        }

        return true;
    }


}

CreateVpcLinkRequest.RequiredProperties = ["name", "targetArns"];

/**
 * The name used to label and identify the VPC link.
 * @member {String} name
 */
CreateVpcLinkRequest.prototype['name'] = undefined;

/**
 * The description of the VPC link.
 * @member {String} description
 */
CreateVpcLinkRequest.prototype['description'] = undefined;

/**
 * The ARN of the network load balancer of the VPC targeted by the VPC link. The network load balancer must be owned by the same AWS account of the API owner.
 * @member {Array.<String>} targetArns
 */
CreateVpcLinkRequest.prototype['targetArns'] = undefined;

/**
 * The key-value map of strings. The valid character set is [a-zA-Z+-=._:/]. The tag key can be up to 128 characters and must not start with <code>aws:</code>. The tag value can be up to 256 characters.
 * @member {Object.<String, String>} tags
 */
CreateVpcLinkRequest.prototype['tags'] = undefined;






export default CreateVpcLinkRequest;

