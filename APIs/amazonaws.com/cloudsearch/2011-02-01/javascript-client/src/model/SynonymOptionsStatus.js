/**
 * Amazon CloudSearch
 * <fullname>Amazon CloudSearch Configuration Service</fullname> <p>You use the configuration service to create, configure, and manage search domains. Configuration service requests are submitted using the AWS Query protocol. AWS Query requests are HTTP or HTTPS requests submitted via HTTP GET or POST with a query parameter named Action.</p> <p>The endpoint for configuration service requests is region-specific: cloudsearch.<i>region</i>.amazonaws.com. For example, cloudsearch.us-east-1.amazonaws.com. For a current list of supported regions and endpoints, see <a href=\"http://docs.aws.amazon.com/general/latest/gr/rande.html#cloudsearch_region\">Regions and Endpoints</a>.</p>
 *
 * The version of the OpenAPI document: 2011-02-01
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OptionStatus from './OptionStatus';

/**
 * The SynonymOptionsStatus model module.
 * @module model/SynonymOptionsStatus
 * @version 2011-02-01
 */
class SynonymOptionsStatus {
    /**
     * Constructs a new <code>SynonymOptionsStatus</code>.
     * The synonym options configured for this search domain and the current status of those options.
     * @alias module:model/SynonymOptionsStatus
     * @param options {String} Maps terms to their synonyms, serialized as a JSON document. The document has a single object with one property \"synonyms\" whose value is an object mapping terms to their synonyms. Each synonym is a simple string or an array of strings. The maximum size of a stopwords document is 100 KB. Example: <code>{ \"synonyms\": {\"cat\": [\"feline\", \"kitten\"], \"puppy\": \"dog\"} }</code>
     * @param status {module:model/OptionStatus} 
     */
    constructor(options, status) { 
        
        SynonymOptionsStatus.initialize(this, options, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, options, status) { 
        obj['Options'] = options;
        obj['Status'] = status;
    }

    /**
     * Constructs a <code>SynonymOptionsStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SynonymOptionsStatus} obj Optional instance to populate.
     * @return {module:model/SynonymOptionsStatus} The populated <code>SynonymOptionsStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SynonymOptionsStatus();

            if (data.hasOwnProperty('Options')) {
                obj['Options'] = ApiClient.convertToType(data['Options'], 'String');
            }
            if (data.hasOwnProperty('Status')) {
                obj['Status'] = OptionStatus.constructFromObject(data['Status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SynonymOptionsStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SynonymOptionsStatus</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SynonymOptionsStatus.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Options'] && !(typeof data['Options'] === 'string' || data['Options'] instanceof String)) {
            throw new Error("Expected the field `Options` to be a primitive type in the JSON string but got " + data['Options']);
        }
        // validate the optional field `Status`
        if (data['Status']) { // data not null
          OptionStatus.validateJSON(data['Status']);
        }

        return true;
    }


}

SynonymOptionsStatus.RequiredProperties = ["Options", "Status"];

/**
 * Maps terms to their synonyms, serialized as a JSON document. The document has a single object with one property \"synonyms\" whose value is an object mapping terms to their synonyms. Each synonym is a simple string or an array of strings. The maximum size of a stopwords document is 100 KB. Example: <code>{ \"synonyms\": {\"cat\": [\"feline\", \"kitten\"], \"puppy\": \"dog\"} }</code>
 * @member {String} Options
 */
SynonymOptionsStatus.prototype['Options'] = undefined;

/**
 * @member {module:model/OptionStatus} Status
 */
SynonymOptionsStatus.prototype['Status'] = undefined;






export default SynonymOptionsStatus;

