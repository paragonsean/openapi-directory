/**
 * Amazon CloudSearch
 * <fullname>Amazon CloudSearch Configuration Service</fullname> <p>You use the Amazon CloudSearch configuration service to create, configure, and manage search domains. Configuration service requests are submitted using the AWS Query protocol. AWS Query requests are HTTP or HTTPS requests submitted via HTTP GET or POST with a query parameter named Action.</p> <p>The endpoint for configuration service requests is region-specific: cloudsearch.<i>region</i>.amazonaws.com. For example, cloudsearch.us-east-1.amazonaws.com. For a current list of supported regions and endpoints, see <a href=\"http://docs.aws.amazon.com/general/latest/gr/rande.html#cloudsearch_region\" target=\"_blank\">Regions and Endpoints</a>.</p>
 *
 * The version of the OpenAPI document: 2013-01-01
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DocumentSuggesterOptions from './DocumentSuggesterOptions';

/**
 * The Suggester model module.
 * @module model/Suggester
 * @version 2013-01-01
 */
class Suggester {
    /**
     * Constructs a new <code>Suggester</code>.
     * Configuration information for a search suggester. Each suggester has a unique name and specifies the text field you want to use for suggestions. The following options can be configured for a suggester: &lt;code&gt;FuzzyMatching&lt;/code&gt;, &lt;code&gt;SortExpression&lt;/code&gt;. 
     * @alias module:model/Suggester
     * @param suggesterName {String} Names must begin with a letter and can contain the following characters: a-z (lowercase), 0-9, and _ (underscore).
     * @param documentSuggesterOptions {module:model/DocumentSuggesterOptions} 
     */
    constructor(suggesterName, documentSuggesterOptions) { 
        
        Suggester.initialize(this, suggesterName, documentSuggesterOptions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, suggesterName, documentSuggesterOptions) { 
        obj['SuggesterName'] = suggesterName;
        obj['DocumentSuggesterOptions'] = documentSuggesterOptions;
    }

    /**
     * Constructs a <code>Suggester</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Suggester} obj Optional instance to populate.
     * @return {module:model/Suggester} The populated <code>Suggester</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Suggester();

            if (data.hasOwnProperty('SuggesterName')) {
                obj['SuggesterName'] = ApiClient.convertToType(data['SuggesterName'], 'String');
            }
            if (data.hasOwnProperty('DocumentSuggesterOptions')) {
                obj['DocumentSuggesterOptions'] = DocumentSuggesterOptions.constructFromObject(data['DocumentSuggesterOptions']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Suggester</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Suggester</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Suggester.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['SuggesterName'] && !(typeof data['SuggesterName'] === 'string' || data['SuggesterName'] instanceof String)) {
            throw new Error("Expected the field `SuggesterName` to be a primitive type in the JSON string but got " + data['SuggesterName']);
        }
        // validate the optional field `DocumentSuggesterOptions`
        if (data['DocumentSuggesterOptions']) { // data not null
          DocumentSuggesterOptions.validateJSON(data['DocumentSuggesterOptions']);
        }

        return true;
    }


}

Suggester.RequiredProperties = ["SuggesterName", "DocumentSuggesterOptions"];

/**
 * Names must begin with a letter and can contain the following characters: a-z (lowercase), 0-9, and _ (underscore).
 * @member {String} SuggesterName
 */
Suggester.prototype['SuggesterName'] = undefined;

/**
 * @member {module:model/DocumentSuggesterOptions} DocumentSuggesterOptions
 */
Suggester.prototype['DocumentSuggesterOptions'] = undefined;






export default Suggester;

