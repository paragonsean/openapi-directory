/**
 * Amazon Security Lake
 * <p>Amazon Security Lake is a fully managed security data lake service. You can use Security Lake to automatically centralize security data from cloud, on-premises, and custom sources into a data lake that's stored in your Amazon Web Services account. Amazon Web Services Organizations is an account management service that lets you consolidate multiple Amazon Web Services accounts into an organization that you create and centrally manage. With Organizations, you can create member accounts and invite existing accounts to join your organization. Security Lake helps you analyze security data for a more complete understanding of your security posture across the entire organization. It can also help you improve the protection of your workloads, applications, and data.</p> <p>The data lake is backed by Amazon Simple Storage Service (Amazon S3) buckets, and you retain ownership over your data.</p> <p>Amazon Security Lake integrates with CloudTrail, a service that provides a record of actions taken by a user, role, or an Amazon Web Services service. In Security Lake, CloudTrail captures API calls for Security Lake as events. The calls captured include calls from the Security Lake console and code calls to the Security Lake API operations. If you create a trail, you can enable continuous delivery of CloudTrail events to an Amazon S3 bucket, including events for Security Lake. If you don't configure a trail, you can still view the most recent events in the CloudTrail console in Event history. Using the information collected by CloudTrail you can determine the request that was made to Security Lake, the IP address from which the request was made, who made the request, when it was made, and additional details. To learn more about Security Lake information in CloudTrail, see the <a href=\"https://docs.aws.amazon.com/security-lake/latest/userguide/securitylake-cloudtrail.html\">Amazon Security Lake User Guide</a>.</p> <p>Security Lake automates the collection of security-related log and event data from integrated Amazon Web Services and third-party services. It also helps you manage the lifecycle of data with customizable retention and replication settings. Security Lake converts ingested data into Apache Parquet format and a standard open-source schema called the Open Cybersecurity Schema Framework (OCSF).</p> <p>Other Amazon Web Services and third-party services can subscribe to the data that's stored in Security Lake for incident response and security data analytics.</p>
 *
 * The version of the OpenAPI document: 2018-05-10
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AccessType from './AccessType';
import CreateSubscriberRequestSubscriberIdentity from './CreateSubscriberRequestSubscriberIdentity';
import LogSourceResource from './LogSourceResource';
import Tag from './Tag';

/**
 * The CreateSubscriberRequest model module.
 * @module model/CreateSubscriberRequest
 * @version 2018-05-10
 */
class CreateSubscriberRequest {
    /**
     * Constructs a new <code>CreateSubscriberRequest</code>.
     * @alias module:model/CreateSubscriberRequest
     * @param sources {Array.<module:model/LogSourceResource>} The supported Amazon Web Services from which logs and events are collected. Security Lake supports log and event collection for natively supported Amazon Web Services.
     * @param subscriberIdentity {module:model/CreateSubscriberRequestSubscriberIdentity} 
     * @param subscriberName {String} The name of your Security Lake subscriber account.
     */
    constructor(sources, subscriberIdentity, subscriberName) { 
        
        CreateSubscriberRequest.initialize(this, sources, subscriberIdentity, subscriberName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, sources, subscriberIdentity, subscriberName) { 
        obj['sources'] = sources;
        obj['subscriberIdentity'] = subscriberIdentity;
        obj['subscriberName'] = subscriberName;
    }

    /**
     * Constructs a <code>CreateSubscriberRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateSubscriberRequest} obj Optional instance to populate.
     * @return {module:model/CreateSubscriberRequest} The populated <code>CreateSubscriberRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateSubscriberRequest();

            if (data.hasOwnProperty('accessTypes')) {
                obj['accessTypes'] = ApiClient.convertToType(data['accessTypes'], [AccessType]);
            }
            if (data.hasOwnProperty('sources')) {
                obj['sources'] = ApiClient.convertToType(data['sources'], [LogSourceResource]);
            }
            if (data.hasOwnProperty('subscriberDescription')) {
                obj['subscriberDescription'] = ApiClient.convertToType(data['subscriberDescription'], 'String');
            }
            if (data.hasOwnProperty('subscriberIdentity')) {
                obj['subscriberIdentity'] = CreateSubscriberRequestSubscriberIdentity.constructFromObject(data['subscriberIdentity']);
            }
            if (data.hasOwnProperty('subscriberName')) {
                obj['subscriberName'] = ApiClient.convertToType(data['subscriberName'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], [Tag]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateSubscriberRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateSubscriberRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateSubscriberRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['accessTypes'])) {
            throw new Error("Expected the field `accessTypes` to be an array in the JSON data but got " + data['accessTypes']);
        }
        if (data['sources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sources'])) {
                throw new Error("Expected the field `sources` to be an array in the JSON data but got " + data['sources']);
            }
            // validate the optional field `sources` (array)
            for (const item of data['sources']) {
                LogSourceResource.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['subscriberDescription'] && !(typeof data['subscriberDescription'] === 'string' || data['subscriberDescription'] instanceof String)) {
            throw new Error("Expected the field `subscriberDescription` to be a primitive type in the JSON string but got " + data['subscriberDescription']);
        }
        // validate the optional field `subscriberIdentity`
        if (data['subscriberIdentity']) { // data not null
          CreateSubscriberRequestSubscriberIdentity.validateJSON(data['subscriberIdentity']);
        }
        // ensure the json data is a string
        if (data['subscriberName'] && !(typeof data['subscriberName'] === 'string' || data['subscriberName'] instanceof String)) {
            throw new Error("Expected the field `subscriberName` to be a primitive type in the JSON string but got " + data['subscriberName']);
        }
        if (data['tags']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tags'])) {
                throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
            }
            // validate the optional field `tags` (array)
            for (const item of data['tags']) {
                Tag.validateJSON(item);
            };
        }

        return true;
    }


}

CreateSubscriberRequest.RequiredProperties = ["sources", "subscriberIdentity", "subscriberName"];

/**
 * The Amazon S3 or Lake Formation access type.
 * @member {Array.<module:model/AccessType>} accessTypes
 */
CreateSubscriberRequest.prototype['accessTypes'] = undefined;

/**
 * The supported Amazon Web Services from which logs and events are collected. Security Lake supports log and event collection for natively supported Amazon Web Services.
 * @member {Array.<module:model/LogSourceResource>} sources
 */
CreateSubscriberRequest.prototype['sources'] = undefined;

/**
 * The description for your subscriber account in Security Lake.
 * @member {String} subscriberDescription
 */
CreateSubscriberRequest.prototype['subscriberDescription'] = undefined;

/**
 * @member {module:model/CreateSubscriberRequestSubscriberIdentity} subscriberIdentity
 */
CreateSubscriberRequest.prototype['subscriberIdentity'] = undefined;

/**
 * The name of your Security Lake subscriber account.
 * @member {String} subscriberName
 */
CreateSubscriberRequest.prototype['subscriberName'] = undefined;

/**
 * An array of objects, one for each tag to associate with the subscriber. For each tag, you must specify both a tag key and a tag value. A tag value cannot be null, but it can be an empty string.
 * @member {Array.<module:model/Tag>} tags
 */
CreateSubscriberRequest.prototype['tags'] = undefined;






export default CreateSubscriberRequest;

