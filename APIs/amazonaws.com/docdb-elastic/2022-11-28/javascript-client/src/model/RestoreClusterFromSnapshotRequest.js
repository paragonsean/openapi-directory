/**
 * Amazon DocumentDB Elastic Clusters
 * The new Amazon Elastic DocumentDB service endpoint.
 *
 * The version of the OpenAPI document: 2022-11-28
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RestoreClusterFromSnapshotRequest model module.
 * @module model/RestoreClusterFromSnapshotRequest
 * @version 2022-11-28
 */
class RestoreClusterFromSnapshotRequest {
    /**
     * Constructs a new <code>RestoreClusterFromSnapshotRequest</code>.
     * @alias module:model/RestoreClusterFromSnapshotRequest
     * @param clusterName {String} The name of the Elastic DocumentDB cluster.
     */
    constructor(clusterName) { 
        
        RestoreClusterFromSnapshotRequest.initialize(this, clusterName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, clusterName) { 
        obj['clusterName'] = clusterName;
    }

    /**
     * Constructs a <code>RestoreClusterFromSnapshotRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RestoreClusterFromSnapshotRequest} obj Optional instance to populate.
     * @return {module:model/RestoreClusterFromSnapshotRequest} The populated <code>RestoreClusterFromSnapshotRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RestoreClusterFromSnapshotRequest();

            if (data.hasOwnProperty('clusterName')) {
                obj['clusterName'] = ApiClient.convertToType(data['clusterName'], 'String');
            }
            if (data.hasOwnProperty('kmsKeyId')) {
                obj['kmsKeyId'] = ApiClient.convertToType(data['kmsKeyId'], 'String');
            }
            if (data.hasOwnProperty('subnetIds')) {
                obj['subnetIds'] = ApiClient.convertToType(data['subnetIds'], ['String']);
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('vpcSecurityGroupIds')) {
                obj['vpcSecurityGroupIds'] = ApiClient.convertToType(data['vpcSecurityGroupIds'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RestoreClusterFromSnapshotRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RestoreClusterFromSnapshotRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RestoreClusterFromSnapshotRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['clusterName'] && !(typeof data['clusterName'] === 'string' || data['clusterName'] instanceof String)) {
            throw new Error("Expected the field `clusterName` to be a primitive type in the JSON string but got " + data['clusterName']);
        }
        // ensure the json data is a string
        if (data['kmsKeyId'] && !(typeof data['kmsKeyId'] === 'string' || data['kmsKeyId'] instanceof String)) {
            throw new Error("Expected the field `kmsKeyId` to be a primitive type in the JSON string but got " + data['kmsKeyId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['subnetIds'])) {
            throw new Error("Expected the field `subnetIds` to be an array in the JSON data but got " + data['subnetIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['vpcSecurityGroupIds'])) {
            throw new Error("Expected the field `vpcSecurityGroupIds` to be an array in the JSON data but got " + data['vpcSecurityGroupIds']);
        }

        return true;
    }


}

RestoreClusterFromSnapshotRequest.RequiredProperties = ["clusterName"];

/**
 * The name of the Elastic DocumentDB cluster.
 * @member {String} clusterName
 */
RestoreClusterFromSnapshotRequest.prototype['clusterName'] = undefined;

/**
 * <p>The KMS key identifier to use to encrypt the new Elastic DocumentDB cluster.</p> <p>The KMS key identifier is the Amazon Resource Name (ARN) for the KMS encryption key. If you are creating a cluster using the same Amazon account that owns this KMS encryption key, you can use the KMS key alias instead of the ARN as the KMS encryption key.</p> <p>If an encryption key is not specified here, Elastic DocumentDB uses the default encryption key that KMS creates for your account. Your account has a different default encryption key for each Amazon Region.</p>
 * @member {String} kmsKeyId
 */
RestoreClusterFromSnapshotRequest.prototype['kmsKeyId'] = undefined;

/**
 * The Amazon EC2 subnet IDs for the Elastic DocumentDB cluster.
 * @member {Array.<String>} subnetIds
 */
RestoreClusterFromSnapshotRequest.prototype['subnetIds'] = undefined;

/**
 * A list of the tag names to be assigned to the restored DB cluster, in the form of an array of key-value pairs in which the key is the tag name and the value is the key value.
 * @member {Object.<String, String>} tags
 */
RestoreClusterFromSnapshotRequest.prototype['tags'] = undefined;

/**
 * A list of EC2 VPC security groups to associate with the Elastic DocumentDB cluster.
 * @member {Array.<String>} vpcSecurityGroupIds
 */
RestoreClusterFromSnapshotRequest.prototype['vpcSecurityGroupIds'] = undefined;






export default RestoreClusterFromSnapshotRequest;

