/**
 * Amazon CloudDirectory
 * <fullname>Amazon Cloud Directory</fullname> <p>Amazon Cloud Directory is a component of the AWS Directory Service that simplifies the development and management of cloud-scale web, mobile, and IoT applications. This guide describes the Cloud Directory operations that you can call programmatically and includes detailed information on data types and errors. For information about AWS Directory Services features, see <a href=\"https://aws.amazon.com/directoryservice/\">AWS Directory Service</a> and the <a href=\"http://docs.aws.amazon.com/directoryservice/latest/admin-guide/what_is.html\">AWS Directory Service Administration Guide</a>.</p>
 *
 * The version of the OpenAPI document: 2016-05-10
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FacetAttributeUpdate from './FacetAttributeUpdate';

/**
 * The UpdateFacetRequest model module.
 * @module model/UpdateFacetRequest
 * @version 2016-05-10
 */
class UpdateFacetRequest {
    /**
     * Constructs a new <code>UpdateFacetRequest</code>.
     * @alias module:model/UpdateFacetRequest
     * @param name {String} The name of the facet.
     */
    constructor(name) { 
        
        UpdateFacetRequest.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['Name'] = name;
    }

    /**
     * Constructs a <code>UpdateFacetRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateFacetRequest} obj Optional instance to populate.
     * @return {module:model/UpdateFacetRequest} The populated <code>UpdateFacetRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateFacetRequest();

            if (data.hasOwnProperty('Name')) {
                obj['Name'] = ApiClient.convertToType(data['Name'], 'String');
            }
            if (data.hasOwnProperty('AttributeUpdates')) {
                obj['AttributeUpdates'] = ApiClient.convertToType(data['AttributeUpdates'], [FacetAttributeUpdate]);
            }
            if (data.hasOwnProperty('ObjectType')) {
                obj['ObjectType'] = ApiClient.convertToType(data['ObjectType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateFacetRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateFacetRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateFacetRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Name'] && !(typeof data['Name'] === 'string' || data['Name'] instanceof String)) {
            throw new Error("Expected the field `Name` to be a primitive type in the JSON string but got " + data['Name']);
        }
        if (data['AttributeUpdates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['AttributeUpdates'])) {
                throw new Error("Expected the field `AttributeUpdates` to be an array in the JSON data but got " + data['AttributeUpdates']);
            }
            // validate the optional field `AttributeUpdates` (array)
            for (const item of data['AttributeUpdates']) {
                FacetAttributeUpdate.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['ObjectType'] && !(typeof data['ObjectType'] === 'string' || data['ObjectType'] instanceof String)) {
            throw new Error("Expected the field `ObjectType` to be a primitive type in the JSON string but got " + data['ObjectType']);
        }

        return true;
    }


}

UpdateFacetRequest.RequiredProperties = ["Name"];

/**
 * The name of the facet.
 * @member {String} Name
 */
UpdateFacetRequest.prototype['Name'] = undefined;

/**
 * List of attributes that need to be updated in a given schema <a>Facet</a>. Each attribute is followed by <code>AttributeAction</code>, which specifies the type of update operation to perform. 
 * @member {Array.<module:model/FacetAttributeUpdate>} AttributeUpdates
 */
UpdateFacetRequest.prototype['AttributeUpdates'] = undefined;

/**
 * The object type that is associated with the facet. See <a>CreateFacetRequest$ObjectType</a> for more details.
 * @member {module:model/UpdateFacetRequest.ObjectTypeEnum} ObjectType
 */
UpdateFacetRequest.prototype['ObjectType'] = undefined;





/**
 * Allowed values for the <code>ObjectType</code> property.
 * @enum {String}
 * @readonly
 */
UpdateFacetRequest['ObjectTypeEnum'] = {

    /**
     * value: "NODE"
     * @const
     */
    "NODE": "NODE",

    /**
     * value: "LEAF_NODE"
     * @const
     */
    "LEAF_NODE": "LEAF_NODE",

    /**
     * value: "POLICY"
     * @const
     */
    "POLICY": "POLICY",

    /**
     * value: "INDEX"
     * @const
     */
    "INDEX": "INDEX"
};



export default UpdateFacetRequest;

