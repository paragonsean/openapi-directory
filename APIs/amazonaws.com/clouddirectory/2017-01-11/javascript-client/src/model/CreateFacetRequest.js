/**
 * Amazon CloudDirectory
 * <fullname>Amazon Cloud Directory</fullname> <p>Amazon Cloud Directory is a component of the AWS Directory Service that simplifies the development and management of cloud-scale web, mobile, and IoT applications. This guide describes the Cloud Directory operations that you can call programmatically and includes detailed information on data types and errors. For information about Cloud Directory features, see <a href=\"https://aws.amazon.com/directoryservice/\">AWS Directory Service</a> and the <a href=\"https://docs.aws.amazon.com/clouddirectory/latest/developerguide/what_is_cloud_directory.html\">Amazon Cloud Directory Developer Guide</a>.</p>
 *
 * The version of the OpenAPI document: 2017-01-11
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FacetAttribute from './FacetAttribute';

/**
 * The CreateFacetRequest model module.
 * @module model/CreateFacetRequest
 * @version 2017-01-11
 */
class CreateFacetRequest {
    /**
     * Constructs a new <code>CreateFacetRequest</code>.
     * @alias module:model/CreateFacetRequest
     * @param name {String} The name of the <a>Facet</a>, which is unique for a given schema.
     */
    constructor(name) { 
        
        CreateFacetRequest.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['Name'] = name;
    }

    /**
     * Constructs a <code>CreateFacetRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateFacetRequest} obj Optional instance to populate.
     * @return {module:model/CreateFacetRequest} The populated <code>CreateFacetRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateFacetRequest();

            if (data.hasOwnProperty('Name')) {
                obj['Name'] = ApiClient.convertToType(data['Name'], 'String');
            }
            if (data.hasOwnProperty('Attributes')) {
                obj['Attributes'] = ApiClient.convertToType(data['Attributes'], [FacetAttribute]);
            }
            if (data.hasOwnProperty('ObjectType')) {
                obj['ObjectType'] = ApiClient.convertToType(data['ObjectType'], 'String');
            }
            if (data.hasOwnProperty('FacetStyle')) {
                obj['FacetStyle'] = ApiClient.convertToType(data['FacetStyle'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateFacetRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateFacetRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateFacetRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Name'] && !(typeof data['Name'] === 'string' || data['Name'] instanceof String)) {
            throw new Error("Expected the field `Name` to be a primitive type in the JSON string but got " + data['Name']);
        }
        if (data['Attributes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Attributes'])) {
                throw new Error("Expected the field `Attributes` to be an array in the JSON data but got " + data['Attributes']);
            }
            // validate the optional field `Attributes` (array)
            for (const item of data['Attributes']) {
                FacetAttribute.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['ObjectType'] && !(typeof data['ObjectType'] === 'string' || data['ObjectType'] instanceof String)) {
            throw new Error("Expected the field `ObjectType` to be a primitive type in the JSON string but got " + data['ObjectType']);
        }
        // ensure the json data is a string
        if (data['FacetStyle'] && !(typeof data['FacetStyle'] === 'string' || data['FacetStyle'] instanceof String)) {
            throw new Error("Expected the field `FacetStyle` to be a primitive type in the JSON string but got " + data['FacetStyle']);
        }

        return true;
    }


}

CreateFacetRequest.RequiredProperties = ["Name"];

/**
 * The name of the <a>Facet</a>, which is unique for a given schema.
 * @member {String} Name
 */
CreateFacetRequest.prototype['Name'] = undefined;

/**
 * The attributes that are associated with the <a>Facet</a>.
 * @member {Array.<module:model/FacetAttribute>} Attributes
 */
CreateFacetRequest.prototype['Attributes'] = undefined;

/**
 * <p>Specifies whether a given object created from this facet is of type node, leaf node, policy or index.</p> <ul> <li> <p>Node: Can have multiple children but one parent.</p> </li> </ul> <ul> <li> <p>Leaf node: Cannot have children but can have multiple parents.</p> </li> </ul> <ul> <li> <p>Policy: Allows you to store a policy document and policy type. For more information, see <a href=\"https://docs.aws.amazon.com/clouddirectory/latest/developerguide/key_concepts_directory.html#key_concepts_policies\">Policies</a>.</p> </li> </ul> <ul> <li> <p>Index: Can be created with the Index API.</p> </li> </ul>
 * @member {module:model/CreateFacetRequest.ObjectTypeEnum} ObjectType
 */
CreateFacetRequest.prototype['ObjectType'] = undefined;

/**
 * There are two different styles that you can define on any given facet, <code>Static</code> and <code>Dynamic</code>. For static facets, all attributes must be defined in the schema. For dynamic facets, attributes can be defined during data plane operations.
 * @member {module:model/CreateFacetRequest.FacetStyleEnum} FacetStyle
 */
CreateFacetRequest.prototype['FacetStyle'] = undefined;





/**
 * Allowed values for the <code>ObjectType</code> property.
 * @enum {String}
 * @readonly
 */
CreateFacetRequest['ObjectTypeEnum'] = {

    /**
     * value: "NODE"
     * @const
     */
    "NODE": "NODE",

    /**
     * value: "LEAF_NODE"
     * @const
     */
    "LEAF_NODE": "LEAF_NODE",

    /**
     * value: "POLICY"
     * @const
     */
    "POLICY": "POLICY",

    /**
     * value: "INDEX"
     * @const
     */
    "INDEX": "INDEX"
};


/**
 * Allowed values for the <code>FacetStyle</code> property.
 * @enum {String}
 * @readonly
 */
CreateFacetRequest['FacetStyleEnum'] = {

    /**
     * value: "STATIC"
     * @const
     */
    "STATIC": "STATIC",

    /**
     * value: "DYNAMIC"
     * @const
     */
    "DYNAMIC": "DYNAMIC"
};



export default CreateFacetRequest;

