/**
 * AWS Well-Architected Tool
 * <fullname>Well-Architected Tool</fullname> <p>This is the <i>Well-Architected Tool API Reference</i>. The WA Tool API provides programmatic access to the <a href=\"http://aws.amazon.com/well-architected-tool\">Well-Architected Tool</a> in the <a href=\"https://console.aws.amazon.com/wellarchitected\">Amazon Web Services Management Console</a>. For information about the Well-Architected Tool, see the <a href=\"https://docs.aws.amazon.com/wellarchitected/latest/userguide/intro.html\">Well-Architected Tool User Guide</a>.</p>
 *
 * The version of the OpenAPI document: 2020-03-31
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkloadImprovementStatus from './WorkloadImprovementStatus';

/**
 * The WorkloadSummary model module.
 * @module model/WorkloadSummary
 * @version 2020-03-31
 */
class WorkloadSummary {
    /**
     * Constructs a new <code>WorkloadSummary</code>.
     * A workload summary return object.
     * @alias module:model/WorkloadSummary
     */
    constructor() { 
        
        WorkloadSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WorkloadSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkloadSummary} obj Optional instance to populate.
     * @return {module:model/WorkloadSummary} The populated <code>WorkloadSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkloadSummary();

            if (data.hasOwnProperty('WorkloadId')) {
                obj['WorkloadId'] = ApiClient.convertToType(data['WorkloadId'], 'String');
            }
            if (data.hasOwnProperty('WorkloadArn')) {
                obj['WorkloadArn'] = ApiClient.convertToType(data['WorkloadArn'], 'String');
            }
            if (data.hasOwnProperty('WorkloadName')) {
                obj['WorkloadName'] = ApiClient.convertToType(data['WorkloadName'], 'String');
            }
            if (data.hasOwnProperty('Owner')) {
                obj['Owner'] = ApiClient.convertToType(data['Owner'], 'String');
            }
            if (data.hasOwnProperty('UpdatedAt')) {
                obj['UpdatedAt'] = ApiClient.convertToType(data['UpdatedAt'], 'Date');
            }
            if (data.hasOwnProperty('Lenses')) {
                obj['Lenses'] = ApiClient.convertToType(data['Lenses'], ['String']);
            }
            if (data.hasOwnProperty('RiskCounts')) {
                obj['RiskCounts'] = ApiClient.convertToType(data['RiskCounts'], {'String': 'Number'});
            }
            if (data.hasOwnProperty('ImprovementStatus')) {
                obj['ImprovementStatus'] = WorkloadImprovementStatus.constructFromObject(data['ImprovementStatus']);
            }
            if (data.hasOwnProperty('Profiles')) {
                obj['Profiles'] = ApiClient.convertToType(data['Profiles'], Array);
            }
            if (data.hasOwnProperty('PrioritizedRiskCounts')) {
                obj['PrioritizedRiskCounts'] = ApiClient.convertToType(data['PrioritizedRiskCounts'], {'String': 'Number'});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkloadSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkloadSummary</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['WorkloadId'] && !(typeof data['WorkloadId'] === 'string' || data['WorkloadId'] instanceof String)) {
            throw new Error("Expected the field `WorkloadId` to be a primitive type in the JSON string but got " + data['WorkloadId']);
        }
        // ensure the json data is a string
        if (data['WorkloadArn'] && !(typeof data['WorkloadArn'] === 'string' || data['WorkloadArn'] instanceof String)) {
            throw new Error("Expected the field `WorkloadArn` to be a primitive type in the JSON string but got " + data['WorkloadArn']);
        }
        // ensure the json data is a string
        if (data['WorkloadName'] && !(typeof data['WorkloadName'] === 'string' || data['WorkloadName'] instanceof String)) {
            throw new Error("Expected the field `WorkloadName` to be a primitive type in the JSON string but got " + data['WorkloadName']);
        }
        // ensure the json data is a string
        if (data['Owner'] && !(typeof data['Owner'] === 'string' || data['Owner'] instanceof String)) {
            throw new Error("Expected the field `Owner` to be a primitive type in the JSON string but got " + data['Owner']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['Lenses'])) {
            throw new Error("Expected the field `Lenses` to be an array in the JSON data but got " + data['Lenses']);
        }
        // validate the optional field `Profiles`
        if (data['Profiles']) { // data not null
          Array.validateJSON(data['Profiles']);
        }

        return true;
    }


}



/**
 * The ID assigned to the workload. This ID is unique within an Amazon Web Services Region.
 * @member {String} WorkloadId
 */
WorkloadSummary.prototype['WorkloadId'] = undefined;

/**
 * The ARN for the workload.
 * @member {String} WorkloadArn
 */
WorkloadSummary.prototype['WorkloadArn'] = undefined;

/**
 * <p>The name of the workload.</p> <p>The name must be unique within an account within an Amazon Web Services Region. Spaces and capitalization are ignored when checking for uniqueness.</p>
 * @member {String} WorkloadName
 */
WorkloadSummary.prototype['WorkloadName'] = undefined;

/**
 * An Amazon Web Services account ID.
 * @member {String} Owner
 */
WorkloadSummary.prototype['Owner'] = undefined;

/**
 * The date and time recorded.
 * @member {Date} UpdatedAt
 */
WorkloadSummary.prototype['UpdatedAt'] = undefined;

/**
 * The list of lenses associated with the workload. Each lens is identified by its <a>LensSummary$LensAlias</a>.
 * @member {Array.<String>} Lenses
 */
WorkloadSummary.prototype['Lenses'] = undefined;

/**
 * A map from risk names to the count of how many questions have that rating.
 * @member {Object.<String, Number>} RiskCounts
 */
WorkloadSummary.prototype['RiskCounts'] = undefined;

/**
 * @member {module:model/WorkloadImprovementStatus} ImprovementStatus
 */
WorkloadSummary.prototype['ImprovementStatus'] = undefined;

/**
 * @member {Array} Profiles
 */
WorkloadSummary.prototype['Profiles'] = undefined;

/**
 * A map from risk names to the count of how many questions have that rating.
 * @member {Object.<String, Number>} PrioritizedRiskCounts
 */
WorkloadSummary.prototype['PrioritizedRiskCounts'] = undefined;






export default WorkloadSummary;

