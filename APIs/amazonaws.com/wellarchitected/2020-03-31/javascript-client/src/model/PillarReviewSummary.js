/**
 * AWS Well-Architected Tool
 * <fullname>Well-Architected Tool</fullname> <p>This is the <i>Well-Architected Tool API Reference</i>. The WA Tool API provides programmatic access to the <a href=\"http://aws.amazon.com/well-architected-tool\">Well-Architected Tool</a> in the <a href=\"https://console.aws.amazon.com/wellarchitected\">Amazon Web Services Management Console</a>. For information about the Well-Architected Tool, see the <a href=\"https://docs.aws.amazon.com/wellarchitected/latest/userguide/intro.html\">Well-Architected Tool User Guide</a>.</p>
 *
 * The version of the OpenAPI document: 2020-03-31
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PillarReviewSummary model module.
 * @module model/PillarReviewSummary
 * @version 2020-03-31
 */
class PillarReviewSummary {
    /**
     * Constructs a new <code>PillarReviewSummary</code>.
     * A pillar review summary of a lens review.
     * @alias module:model/PillarReviewSummary
     */
    constructor() { 
        
        PillarReviewSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PillarReviewSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PillarReviewSummary} obj Optional instance to populate.
     * @return {module:model/PillarReviewSummary} The populated <code>PillarReviewSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PillarReviewSummary();

            if (data.hasOwnProperty('PillarId')) {
                obj['PillarId'] = ApiClient.convertToType(data['PillarId'], 'String');
            }
            if (data.hasOwnProperty('PillarName')) {
                obj['PillarName'] = ApiClient.convertToType(data['PillarName'], 'String');
            }
            if (data.hasOwnProperty('Notes')) {
                obj['Notes'] = ApiClient.convertToType(data['Notes'], 'String');
            }
            if (data.hasOwnProperty('RiskCounts')) {
                obj['RiskCounts'] = ApiClient.convertToType(data['RiskCounts'], {'String': 'Number'});
            }
            if (data.hasOwnProperty('PrioritizedRiskCounts')) {
                obj['PrioritizedRiskCounts'] = ApiClient.convertToType(data['PrioritizedRiskCounts'], {'String': 'Number'});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PillarReviewSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PillarReviewSummary</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['PillarId'] && !(typeof data['PillarId'] === 'string' || data['PillarId'] instanceof String)) {
            throw new Error("Expected the field `PillarId` to be a primitive type in the JSON string but got " + data['PillarId']);
        }
        // ensure the json data is a string
        if (data['PillarName'] && !(typeof data['PillarName'] === 'string' || data['PillarName'] instanceof String)) {
            throw new Error("Expected the field `PillarName` to be a primitive type in the JSON string but got " + data['PillarName']);
        }
        // ensure the json data is a string
        if (data['Notes'] && !(typeof data['Notes'] === 'string' || data['Notes'] instanceof String)) {
            throw new Error("Expected the field `Notes` to be a primitive type in the JSON string but got " + data['Notes']);
        }

        return true;
    }


}



/**
 * <p>The ID used to identify a pillar, for example, <code>security</code>.</p> <p>A pillar is identified by its <a>PillarReviewSummary$PillarId</a>.</p>
 * @member {String} PillarId
 */
PillarReviewSummary.prototype['PillarId'] = undefined;

/**
 * The name of the pillar.
 * @member {String} PillarName
 */
PillarReviewSummary.prototype['PillarName'] = undefined;

/**
 * The notes associated with the workload.
 * @member {String} Notes
 */
PillarReviewSummary.prototype['Notes'] = undefined;

/**
 * A map from risk names to the count of how many questions have that rating.
 * @member {Object.<String, Number>} RiskCounts
 */
PillarReviewSummary.prototype['RiskCounts'] = undefined;

/**
 * A map from risk names to the count of how many questions have that rating.
 * @member {Object.<String, Number>} PrioritizedRiskCounts
 */
PillarReviewSummary.prototype['PrioritizedRiskCounts'] = undefined;






export default PillarReviewSummary;

