/*
 * AWS AppSync
 * AppSync provides API actions for creating and interacting with data sources using GraphQL from your application.
 *
 * The version of the OpenAPI document: 2017-07-25
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * The &lt;code&gt;LambdaConflictHandlerConfig&lt;/code&gt; object when configuring &lt;code&gt;LAMBDA&lt;/code&gt; as the Conflict Handler.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T12:14:29.005234-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class LambdaConflictHandlerConfig {
  public static final String SERIALIZED_NAME_LAMBDA_CONFLICT_HANDLER_ARN = "lambdaConflictHandlerArn";
  @SerializedName(SERIALIZED_NAME_LAMBDA_CONFLICT_HANDLER_ARN)
  private String lambdaConflictHandlerArn;

  public LambdaConflictHandlerConfig() {
  }

  public LambdaConflictHandlerConfig lambdaConflictHandlerArn(String lambdaConflictHandlerArn) {
    this.lambdaConflictHandlerArn = lambdaConflictHandlerArn;
    return this;
  }

  /**
   * Get lambdaConflictHandlerArn
   * @return lambdaConflictHandlerArn
   */
  @javax.annotation.Nullable
  public String getLambdaConflictHandlerArn() {
    return lambdaConflictHandlerArn;
  }

  public void setLambdaConflictHandlerArn(String lambdaConflictHandlerArn) {
    this.lambdaConflictHandlerArn = lambdaConflictHandlerArn;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    LambdaConflictHandlerConfig lambdaConflictHandlerConfig = (LambdaConflictHandlerConfig) o;
    return Objects.equals(this.lambdaConflictHandlerArn, lambdaConflictHandlerConfig.lambdaConflictHandlerArn);
  }

  @Override
  public int hashCode() {
    return Objects.hash(lambdaConflictHandlerArn);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class LambdaConflictHandlerConfig {\n");
    sb.append("    lambdaConflictHandlerArn: ").append(toIndentedString(lambdaConflictHandlerArn)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("lambdaConflictHandlerArn");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to LambdaConflictHandlerConfig
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!LambdaConflictHandlerConfig.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in LambdaConflictHandlerConfig is not found in the empty JSON string", LambdaConflictHandlerConfig.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!LambdaConflictHandlerConfig.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `LambdaConflictHandlerConfig` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `lambdaConflictHandlerArn`
      if (jsonObj.get("lambdaConflictHandlerArn") != null && !jsonObj.get("lambdaConflictHandlerArn").isJsonNull()) {
        String.validateJsonElement(jsonObj.get("lambdaConflictHandlerArn"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!LambdaConflictHandlerConfig.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'LambdaConflictHandlerConfig' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<LambdaConflictHandlerConfig> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(LambdaConflictHandlerConfig.class));

       return (TypeAdapter<T>) new TypeAdapter<LambdaConflictHandlerConfig>() {
           @Override
           public void write(JsonWriter out, LambdaConflictHandlerConfig value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public LambdaConflictHandlerConfig read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of LambdaConflictHandlerConfig given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of LambdaConflictHandlerConfig
   * @throws IOException if the JSON string is invalid with respect to LambdaConflictHandlerConfig
   */
  public static LambdaConflictHandlerConfig fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, LambdaConflictHandlerConfig.class);
  }

  /**
   * Convert an instance of LambdaConflictHandlerConfig to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

