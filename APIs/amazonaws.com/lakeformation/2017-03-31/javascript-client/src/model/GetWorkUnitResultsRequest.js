/**
 * AWS Lake Formation
 * <fullname>Lake Formation</fullname> <p>Defines the public endpoint for the Lake Formation service.</p>
 *
 * The version of the OpenAPI document: 2017-03-31
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetWorkUnitResultsRequest model module.
 * @module model/GetWorkUnitResultsRequest
 * @version 2017-03-31
 */
class GetWorkUnitResultsRequest {
    /**
     * Constructs a new <code>GetWorkUnitResultsRequest</code>.
     * @alias module:model/GetWorkUnitResultsRequest
     * @param queryId {String} The ID of the plan query operation for which to get results.
     * @param workUnitId {Number} The work unit ID for which to get results. Value generated by enumerating <code>WorkUnitIdMin</code> to <code>WorkUnitIdMax</code> (inclusive) from the <code>WorkUnitRange</code> in the output of <code>GetWorkUnits</code>.
     * @param workUnitToken {String} A work token used to query the execution service. Token output from <code>GetWorkUnits</code>.
     */
    constructor(queryId, workUnitId, workUnitToken) { 
        
        GetWorkUnitResultsRequest.initialize(this, queryId, workUnitId, workUnitToken);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, queryId, workUnitId, workUnitToken) { 
        obj['QueryId'] = queryId;
        obj['WorkUnitId'] = workUnitId;
        obj['WorkUnitToken'] = workUnitToken;
    }

    /**
     * Constructs a <code>GetWorkUnitResultsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetWorkUnitResultsRequest} obj Optional instance to populate.
     * @return {module:model/GetWorkUnitResultsRequest} The populated <code>GetWorkUnitResultsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetWorkUnitResultsRequest();

            if (data.hasOwnProperty('QueryId')) {
                obj['QueryId'] = ApiClient.convertToType(data['QueryId'], 'String');
            }
            if (data.hasOwnProperty('WorkUnitId')) {
                obj['WorkUnitId'] = ApiClient.convertToType(data['WorkUnitId'], 'Number');
            }
            if (data.hasOwnProperty('WorkUnitToken')) {
                obj['WorkUnitToken'] = ApiClient.convertToType(data['WorkUnitToken'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetWorkUnitResultsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetWorkUnitResultsRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetWorkUnitResultsRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['QueryId'] && !(typeof data['QueryId'] === 'string' || data['QueryId'] instanceof String)) {
            throw new Error("Expected the field `QueryId` to be a primitive type in the JSON string but got " + data['QueryId']);
        }
        // ensure the json data is a string
        if (data['WorkUnitToken'] && !(typeof data['WorkUnitToken'] === 'string' || data['WorkUnitToken'] instanceof String)) {
            throw new Error("Expected the field `WorkUnitToken` to be a primitive type in the JSON string but got " + data['WorkUnitToken']);
        }

        return true;
    }


}

GetWorkUnitResultsRequest.RequiredProperties = ["QueryId", "WorkUnitId", "WorkUnitToken"];

/**
 * The ID of the plan query operation for which to get results.
 * @member {String} QueryId
 */
GetWorkUnitResultsRequest.prototype['QueryId'] = undefined;

/**
 * The work unit ID for which to get results. Value generated by enumerating <code>WorkUnitIdMin</code> to <code>WorkUnitIdMax</code> (inclusive) from the <code>WorkUnitRange</code> in the output of <code>GetWorkUnits</code>.
 * @member {Number} WorkUnitId
 */
GetWorkUnitResultsRequest.prototype['WorkUnitId'] = undefined;

/**
 * A work token used to query the execution service. Token output from <code>GetWorkUnits</code>.
 * @member {String} WorkUnitToken
 */
GetWorkUnitResultsRequest.prototype['WorkUnitToken'] = undefined;






export default GetWorkUnitResultsRequest;

