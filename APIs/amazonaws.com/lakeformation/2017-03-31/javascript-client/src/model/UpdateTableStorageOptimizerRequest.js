/**
 * AWS Lake Formation
 * <fullname>Lake Formation</fullname> <p>Defines the public endpoint for the Lake Formation service.</p>
 *
 * The version of the OpenAPI document: 2017-03-31
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdateTableStorageOptimizerRequest model module.
 * @module model/UpdateTableStorageOptimizerRequest
 * @version 2017-03-31
 */
class UpdateTableStorageOptimizerRequest {
    /**
     * Constructs a new <code>UpdateTableStorageOptimizerRequest</code>.
     * @alias module:model/UpdateTableStorageOptimizerRequest
     * @param databaseName {String} Name of the database where the table is present.
     * @param tableName {String} Name of the table for which to enable the storage optimizer.
     * @param storageOptimizerConfig {Object.<String, Object.<String, String>>} Name of the table for which to enable the storage optimizer.
     */
    constructor(databaseName, tableName, storageOptimizerConfig) { 
        
        UpdateTableStorageOptimizerRequest.initialize(this, databaseName, tableName, storageOptimizerConfig);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, databaseName, tableName, storageOptimizerConfig) { 
        obj['DatabaseName'] = databaseName;
        obj['TableName'] = tableName;
        obj['StorageOptimizerConfig'] = storageOptimizerConfig;
    }

    /**
     * Constructs a <code>UpdateTableStorageOptimizerRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateTableStorageOptimizerRequest} obj Optional instance to populate.
     * @return {module:model/UpdateTableStorageOptimizerRequest} The populated <code>UpdateTableStorageOptimizerRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateTableStorageOptimizerRequest();

            if (data.hasOwnProperty('CatalogId')) {
                obj['CatalogId'] = ApiClient.convertToType(data['CatalogId'], 'String');
            }
            if (data.hasOwnProperty('DatabaseName')) {
                obj['DatabaseName'] = ApiClient.convertToType(data['DatabaseName'], 'String');
            }
            if (data.hasOwnProperty('TableName')) {
                obj['TableName'] = ApiClient.convertToType(data['TableName'], 'String');
            }
            if (data.hasOwnProperty('StorageOptimizerConfig')) {
                obj['StorageOptimizerConfig'] = ApiClient.convertToType(data['StorageOptimizerConfig'], {'String': Object});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateTableStorageOptimizerRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateTableStorageOptimizerRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateTableStorageOptimizerRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['CatalogId'] && !(typeof data['CatalogId'] === 'string' || data['CatalogId'] instanceof String)) {
            throw new Error("Expected the field `CatalogId` to be a primitive type in the JSON string but got " + data['CatalogId']);
        }
        // ensure the json data is a string
        if (data['DatabaseName'] && !(typeof data['DatabaseName'] === 'string' || data['DatabaseName'] instanceof String)) {
            throw new Error("Expected the field `DatabaseName` to be a primitive type in the JSON string but got " + data['DatabaseName']);
        }
        // ensure the json data is a string
        if (data['TableName'] && !(typeof data['TableName'] === 'string' || data['TableName'] instanceof String)) {
            throw new Error("Expected the field `TableName` to be a primitive type in the JSON string but got " + data['TableName']);
        }

        return true;
    }


}

UpdateTableStorageOptimizerRequest.RequiredProperties = ["DatabaseName", "TableName", "StorageOptimizerConfig"];

/**
 * The Catalog ID of the table.
 * @member {String} CatalogId
 */
UpdateTableStorageOptimizerRequest.prototype['CatalogId'] = undefined;

/**
 * Name of the database where the table is present.
 * @member {String} DatabaseName
 */
UpdateTableStorageOptimizerRequest.prototype['DatabaseName'] = undefined;

/**
 * Name of the table for which to enable the storage optimizer.
 * @member {String} TableName
 */
UpdateTableStorageOptimizerRequest.prototype['TableName'] = undefined;

/**
 * Name of the table for which to enable the storage optimizer.
 * @member {Object.<String, Object.<String, String>>} StorageOptimizerConfig
 */
UpdateTableStorageOptimizerRequest.prototype['StorageOptimizerConfig'] = undefined;






export default UpdateTableStorageOptimizerRequest;

