/**
 * AWS Savings Plans
 * Savings Plans are a pricing model that offer significant savings on AWS usage (for example, on Amazon EC2 instances). You commit to a consistent amount of usage, in USD per hour, for a term of 1 or 3 years, and receive a lower price for that usage. For more information, see the <a href=\"https://docs.aws.amazon.com/savingsplans/latest/userguide/\">AWS Savings Plans User Guide</a>.
 *
 * The version of the OpenAPI document: 2019-06-28
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SavingsPlanRateFilter from './SavingsPlanRateFilter';

/**
 * The DescribeSavingsPlanRatesRequest model module.
 * @module model/DescribeSavingsPlanRatesRequest
 * @version 2019-06-28
 */
class DescribeSavingsPlanRatesRequest {
    /**
     * Constructs a new <code>DescribeSavingsPlanRatesRequest</code>.
     * @alias module:model/DescribeSavingsPlanRatesRequest
     * @param savingsPlanId {String} The ID of the Savings Plan.
     */
    constructor(savingsPlanId) { 
        
        DescribeSavingsPlanRatesRequest.initialize(this, savingsPlanId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, savingsPlanId) { 
        obj['savingsPlanId'] = savingsPlanId;
    }

    /**
     * Constructs a <code>DescribeSavingsPlanRatesRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DescribeSavingsPlanRatesRequest} obj Optional instance to populate.
     * @return {module:model/DescribeSavingsPlanRatesRequest} The populated <code>DescribeSavingsPlanRatesRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DescribeSavingsPlanRatesRequest();

            if (data.hasOwnProperty('savingsPlanId')) {
                obj['savingsPlanId'] = ApiClient.convertToType(data['savingsPlanId'], 'String');
            }
            if (data.hasOwnProperty('filters')) {
                obj['filters'] = ApiClient.convertToType(data['filters'], [SavingsPlanRateFilter]);
            }
            if (data.hasOwnProperty('nextToken')) {
                obj['nextToken'] = ApiClient.convertToType(data['nextToken'], 'String');
            }
            if (data.hasOwnProperty('maxResults')) {
                obj['maxResults'] = ApiClient.convertToType(data['maxResults'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DescribeSavingsPlanRatesRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DescribeSavingsPlanRatesRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DescribeSavingsPlanRatesRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['savingsPlanId'] && !(typeof data['savingsPlanId'] === 'string' || data['savingsPlanId'] instanceof String)) {
            throw new Error("Expected the field `savingsPlanId` to be a primitive type in the JSON string but got " + data['savingsPlanId']);
        }
        if (data['filters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['filters'])) {
                throw new Error("Expected the field `filters` to be an array in the JSON data but got " + data['filters']);
            }
            // validate the optional field `filters` (array)
            for (const item of data['filters']) {
                SavingsPlanRateFilter.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextToken'] && !(typeof data['nextToken'] === 'string' || data['nextToken'] instanceof String)) {
            throw new Error("Expected the field `nextToken` to be a primitive type in the JSON string but got " + data['nextToken']);
        }

        return true;
    }


}

DescribeSavingsPlanRatesRequest.RequiredProperties = ["savingsPlanId"];

/**
 * The ID of the Savings Plan.
 * @member {String} savingsPlanId
 */
DescribeSavingsPlanRatesRequest.prototype['savingsPlanId'] = undefined;

/**
 * The filters.
 * @member {Array.<module:model/SavingsPlanRateFilter>} filters
 */
DescribeSavingsPlanRatesRequest.prototype['filters'] = undefined;

/**
 * The token for the next page of results.
 * @member {String} nextToken
 */
DescribeSavingsPlanRatesRequest.prototype['nextToken'] = undefined;

/**
 * The maximum number of results to return with a single call. To retrieve additional results, make another call with the returned token value.
 * @member {Number} maxResults
 */
DescribeSavingsPlanRatesRequest.prototype['maxResults'] = undefined;






export default DescribeSavingsPlanRatesRequest;

