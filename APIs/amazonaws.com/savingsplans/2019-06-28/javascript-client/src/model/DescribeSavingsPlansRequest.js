/**
 * AWS Savings Plans
 * Savings Plans are a pricing model that offer significant savings on AWS usage (for example, on Amazon EC2 instances). You commit to a consistent amount of usage, in USD per hour, for a term of 1 or 3 years, and receive a lower price for that usage. For more information, see the <a href=\"https://docs.aws.amazon.com/savingsplans/latest/userguide/\">AWS Savings Plans User Guide</a>.
 *
 * The version of the OpenAPI document: 2019-06-28
 * Contact: mike.ralphson@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SavingsPlanFilter from './SavingsPlanFilter';
import SavingsPlanState from './SavingsPlanState';

/**
 * The DescribeSavingsPlansRequest model module.
 * @module model/DescribeSavingsPlansRequest
 * @version 2019-06-28
 */
class DescribeSavingsPlansRequest {
    /**
     * Constructs a new <code>DescribeSavingsPlansRequest</code>.
     * @alias module:model/DescribeSavingsPlansRequest
     */
    constructor() { 
        
        DescribeSavingsPlansRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DescribeSavingsPlansRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DescribeSavingsPlansRequest} obj Optional instance to populate.
     * @return {module:model/DescribeSavingsPlansRequest} The populated <code>DescribeSavingsPlansRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DescribeSavingsPlansRequest();

            if (data.hasOwnProperty('savingsPlanArns')) {
                obj['savingsPlanArns'] = ApiClient.convertToType(data['savingsPlanArns'], ['String']);
            }
            if (data.hasOwnProperty('savingsPlanIds')) {
                obj['savingsPlanIds'] = ApiClient.convertToType(data['savingsPlanIds'], ['String']);
            }
            if (data.hasOwnProperty('nextToken')) {
                obj['nextToken'] = ApiClient.convertToType(data['nextToken'], 'String');
            }
            if (data.hasOwnProperty('maxResults')) {
                obj['maxResults'] = ApiClient.convertToType(data['maxResults'], 'Number');
            }
            if (data.hasOwnProperty('states')) {
                obj['states'] = ApiClient.convertToType(data['states'], [SavingsPlanState]);
            }
            if (data.hasOwnProperty('filters')) {
                obj['filters'] = ApiClient.convertToType(data['filters'], [SavingsPlanFilter]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DescribeSavingsPlansRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DescribeSavingsPlansRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['savingsPlanArns'])) {
            throw new Error("Expected the field `savingsPlanArns` to be an array in the JSON data but got " + data['savingsPlanArns']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['savingsPlanIds'])) {
            throw new Error("Expected the field `savingsPlanIds` to be an array in the JSON data but got " + data['savingsPlanIds']);
        }
        // ensure the json data is a string
        if (data['nextToken'] && !(typeof data['nextToken'] === 'string' || data['nextToken'] instanceof String)) {
            throw new Error("Expected the field `nextToken` to be a primitive type in the JSON string but got " + data['nextToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['states'])) {
            throw new Error("Expected the field `states` to be an array in the JSON data but got " + data['states']);
        }
        if (data['filters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['filters'])) {
                throw new Error("Expected the field `filters` to be an array in the JSON data but got " + data['filters']);
            }
            // validate the optional field `filters` (array)
            for (const item of data['filters']) {
                SavingsPlanFilter.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The Amazon Resource Names (ARN) of the Savings Plans.
 * @member {Array.<String>} savingsPlanArns
 */
DescribeSavingsPlansRequest.prototype['savingsPlanArns'] = undefined;

/**
 * The IDs of the Savings Plans.
 * @member {Array.<String>} savingsPlanIds
 */
DescribeSavingsPlansRequest.prototype['savingsPlanIds'] = undefined;

/**
 * The token for the next page of results.
 * @member {String} nextToken
 */
DescribeSavingsPlansRequest.prototype['nextToken'] = undefined;

/**
 * The maximum number of results to return with a single call. To retrieve additional results, make another call with the returned token value.
 * @member {Number} maxResults
 */
DescribeSavingsPlansRequest.prototype['maxResults'] = undefined;

/**
 * The states.
 * @member {Array.<module:model/SavingsPlanState>} states
 */
DescribeSavingsPlansRequest.prototype['states'] = undefined;

/**
 * The filters.
 * @member {Array.<module:model/SavingsPlanFilter>} filters
 */
DescribeSavingsPlansRequest.prototype['filters'] = undefined;






export default DescribeSavingsPlansRequest;

