/**
 * TAGGUN Receipt OCR Scanning API
 * Expects only running software, real reactions, and beautifully crafted APIs to serve your every desire to transcribe a piece of paper to digital form.
 *
 * The version of the OpenAPI document: 1.15.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIClass4DeprecatedEndpointsApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIClass4DeprecatedEndpointsApi::OAIClass4DeprecatedEndpointsApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIClass4DeprecatedEndpointsApi::~OAIClass4DeprecatedEndpointsApi() {
}

void OAIClass4DeprecatedEndpointsApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.taggun.io/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("postApiReceiptV1MatchFile", defaultConf);
    _serverIndices.insert("postApiReceiptV1MatchFile", 0);
    _serverConfigs.insert("postApiReceiptV1SimpleStorage", defaultConf);
    _serverIndices.insert("postApiReceiptV1SimpleStorage", 0);
    _serverConfigs.insert("postApiReceiptV1VerboseStorage", defaultConf);
    _serverIndices.insert("postApiReceiptV1VerboseStorage", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIClass4DeprecatedEndpointsApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIClass4DeprecatedEndpointsApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIClass4DeprecatedEndpointsApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIClass4DeprecatedEndpointsApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIClass4DeprecatedEndpointsApi::setUsername(const QString &username) {
    _username = username;
}

void OAIClass4DeprecatedEndpointsApi::setPassword(const QString &password) {
    _password = password;
}


void OAIClass4DeprecatedEndpointsApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIClass4DeprecatedEndpointsApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIClass4DeprecatedEndpointsApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIClass4DeprecatedEndpointsApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIClass4DeprecatedEndpointsApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIClass4DeprecatedEndpointsApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIClass4DeprecatedEndpointsApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIClass4DeprecatedEndpointsApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIClass4DeprecatedEndpointsApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIClass4DeprecatedEndpointsApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIClass4DeprecatedEndpointsApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIClass4DeprecatedEndpointsApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIClass4DeprecatedEndpointsApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIClass4DeprecatedEndpointsApi::postApiReceiptV1MatchFile(const QString &apikey, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &file, const ::OpenAPI::OptionalParam<bool> &refresh, const ::OpenAPI::OptionalParam<bool> &incognito, const ::OpenAPI::OptionalParam<QString> &ip_address, const ::OpenAPI::OptionalParam<QList<QString>> &primary_keywords, const ::OpenAPI::OptionalParam<QList<QString>> &secondary_keywords, const ::OpenAPI::OptionalParam<QList<QString>> &stop_words, const ::OpenAPI::OptionalParam<QString> &language) {
    QString fullPath = QString(_serverConfigs["postApiReceiptV1MatchFile"][_serverIndices.value("postApiReceiptV1MatchFile")].URL()+"/api/receipt/v1/match/file");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (file.hasValue())
    {
        input.add_file("file", file.value().local_filename, file.value().request_filename, file.value().mime_type);
    }
    if (refresh.hasValue())
    {
        input.add_var("refresh", ::OpenAPI::toStringValue(refresh.value()));
    }
    if (incognito.hasValue())
    {
        input.add_var("incognito", ::OpenAPI::toStringValue(incognito.value()));
    }
    if (ip_address.hasValue())
    {
        input.add_var("ipAddress", ::OpenAPI::toStringValue(ip_address.value()));
    }
    if (primary_keywords.hasValue())
    {
        input.add_var("primaryKeywords", ::OpenAPI::toStringValue(primary_keywords.value()));
    }
    if (secondary_keywords.hasValue())
    {
        input.add_var("secondaryKeywords", ::OpenAPI::toStringValue(secondary_keywords.value()));
    }
    if (stop_words.hasValue())
    {
        input.add_var("stopWords", ::OpenAPI::toStringValue(stop_words.value()));
    }
    if (language.hasValue())
    {
        input.add_var("language", ::OpenAPI::toStringValue(language.value()));
    }

    
    {
        if (!::OpenAPI::toStringValue(apikey).isEmpty()) {
            input.headers.insert("apikey", ::OpenAPI::toStringValue(apikey));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIClass4DeprecatedEndpointsApi::postApiReceiptV1MatchFileCallback);
    connect(this, &OAIClass4DeprecatedEndpointsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIClass4DeprecatedEndpointsApi::postApiReceiptV1MatchFileCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIReceiptMatchResult output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postApiReceiptV1MatchFileSignal(output);
        Q_EMIT postApiReceiptV1MatchFileSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT postApiReceiptV1MatchFileSignalE(output, error_type, error_str);
        Q_EMIT postApiReceiptV1MatchFileSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT postApiReceiptV1MatchFileSignalError(output, error_type, error_str);
        Q_EMIT postApiReceiptV1MatchFileSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIClass4DeprecatedEndpointsApi::postApiReceiptV1SimpleStorage(const QString &apikey, const ::OpenAPI::OptionalParam<OAIStoragePayload> &body) {
    QString fullPath = QString(_serverConfigs["postApiReceiptV1SimpleStorage"][_serverIndices.value("postApiReceiptV1SimpleStorage")].URL()+"/api/receipt/v1/simple/storage");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (body.hasValue()){

        
        QByteArray output = body.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    
    {
        if (!::OpenAPI::toStringValue(apikey).isEmpty()) {
            input.headers.insert("apikey", ::OpenAPI::toStringValue(apikey));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIClass4DeprecatedEndpointsApi::postApiReceiptV1SimpleStorageCallback);
    connect(this, &OAIClass4DeprecatedEndpointsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIClass4DeprecatedEndpointsApi::postApiReceiptV1SimpleStorageCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIReceiptResult output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postApiReceiptV1SimpleStorageSignal(output);
        Q_EMIT postApiReceiptV1SimpleStorageSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT postApiReceiptV1SimpleStorageSignalE(output, error_type, error_str);
        Q_EMIT postApiReceiptV1SimpleStorageSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT postApiReceiptV1SimpleStorageSignalError(output, error_type, error_str);
        Q_EMIT postApiReceiptV1SimpleStorageSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIClass4DeprecatedEndpointsApi::postApiReceiptV1VerboseStorage(const QString &apikey, const ::OpenAPI::OptionalParam<OAIStoragePayload> &body) {
    QString fullPath = QString(_serverConfigs["postApiReceiptV1VerboseStorage"][_serverIndices.value("postApiReceiptV1VerboseStorage")].URL()+"/api/receipt/v1/verbose/storage");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (body.hasValue()){

        
        QByteArray output = body.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    
    {
        if (!::OpenAPI::toStringValue(apikey).isEmpty()) {
            input.headers.insert("apikey", ::OpenAPI::toStringValue(apikey));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIClass4DeprecatedEndpointsApi::postApiReceiptV1VerboseStorageCallback);
    connect(this, &OAIClass4DeprecatedEndpointsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIClass4DeprecatedEndpointsApi::postApiReceiptV1VerboseStorageCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIReceiptVerboseResult output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postApiReceiptV1VerboseStorageSignal(output);
        Q_EMIT postApiReceiptV1VerboseStorageSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT postApiReceiptV1VerboseStorageSignalE(output, error_type, error_str);
        Q_EMIT postApiReceiptV1VerboseStorageSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT postApiReceiptV1VerboseStorageSignalError(output, error_type, error_str);
        Q_EMIT postApiReceiptV1VerboseStorageSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIClass4DeprecatedEndpointsApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
