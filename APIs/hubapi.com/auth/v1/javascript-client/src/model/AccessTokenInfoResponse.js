/**
 * Auth Oauth
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccessTokenInfoResponse model module.
 * @module model/AccessTokenInfoResponse
 * @version v1
 */
class AccessTokenInfoResponse {
    /**
     * Constructs a new <code>AccessTokenInfoResponse</code>.
     * @alias module:model/AccessTokenInfoResponse
     * @param appId {Number} 
     * @param expiresIn {Number} 
     * @param hubId {Number} 
     * @param scopes {Array.<String>} 
     * @param token {String} 
     * @param tokenType {String} 
     * @param userId {Number} 
     */
    constructor(appId, expiresIn, hubId, scopes, token, tokenType, userId) { 
        
        AccessTokenInfoResponse.initialize(this, appId, expiresIn, hubId, scopes, token, tokenType, userId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, appId, expiresIn, hubId, scopes, token, tokenType, userId) { 
        obj['app_id'] = appId;
        obj['expires_in'] = expiresIn;
        obj['hub_id'] = hubId;
        obj['scopes'] = scopes;
        obj['token'] = token;
        obj['token_type'] = tokenType;
        obj['user_id'] = userId;
    }

    /**
     * Constructs a <code>AccessTokenInfoResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccessTokenInfoResponse} obj Optional instance to populate.
     * @return {module:model/AccessTokenInfoResponse} The populated <code>AccessTokenInfoResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccessTokenInfoResponse();

            if (data.hasOwnProperty('app_id')) {
                obj['app_id'] = ApiClient.convertToType(data['app_id'], 'Number');
            }
            if (data.hasOwnProperty('expires_in')) {
                obj['expires_in'] = ApiClient.convertToType(data['expires_in'], 'Number');
            }
            if (data.hasOwnProperty('hub_domain')) {
                obj['hub_domain'] = ApiClient.convertToType(data['hub_domain'], 'String');
            }
            if (data.hasOwnProperty('hub_id')) {
                obj['hub_id'] = ApiClient.convertToType(data['hub_id'], 'Number');
            }
            if (data.hasOwnProperty('scopes')) {
                obj['scopes'] = ApiClient.convertToType(data['scopes'], ['String']);
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('token_type')) {
                obj['token_type'] = ApiClient.convertToType(data['token_type'], 'String');
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = ApiClient.convertToType(data['user'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccessTokenInfoResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccessTokenInfoResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AccessTokenInfoResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['hub_domain'] && !(typeof data['hub_domain'] === 'string' || data['hub_domain'] instanceof String)) {
            throw new Error("Expected the field `hub_domain` to be a primitive type in the JSON string but got " + data['hub_domain']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scopes'])) {
            throw new Error("Expected the field `scopes` to be an array in the JSON data but got " + data['scopes']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['token_type'] && !(typeof data['token_type'] === 'string' || data['token_type'] instanceof String)) {
            throw new Error("Expected the field `token_type` to be a primitive type in the JSON string but got " + data['token_type']);
        }
        // ensure the json data is a string
        if (data['user'] && !(typeof data['user'] === 'string' || data['user'] instanceof String)) {
            throw new Error("Expected the field `user` to be a primitive type in the JSON string but got " + data['user']);
        }

        return true;
    }


}

AccessTokenInfoResponse.RequiredProperties = ["app_id", "expires_in", "hub_id", "scopes", "token", "token_type", "user_id"];

/**
 * 
 * @member {Number} app_id
 */
AccessTokenInfoResponse.prototype['app_id'] = undefined;

/**
 * 
 * @member {Number} expires_in
 */
AccessTokenInfoResponse.prototype['expires_in'] = undefined;

/**
 * 
 * @member {String} hub_domain
 */
AccessTokenInfoResponse.prototype['hub_domain'] = undefined;

/**
 * 
 * @member {Number} hub_id
 */
AccessTokenInfoResponse.prototype['hub_id'] = undefined;

/**
 * 
 * @member {Array.<String>} scopes
 */
AccessTokenInfoResponse.prototype['scopes'] = undefined;

/**
 * 
 * @member {String} token
 */
AccessTokenInfoResponse.prototype['token'] = undefined;

/**
 * 
 * @member {String} token_type
 */
AccessTokenInfoResponse.prototype['token_type'] = undefined;

/**
 * 
 * @member {String} user
 */
AccessTokenInfoResponse.prototype['user'] = undefined;

/**
 * 
 * @member {Number} user_id
 */
AccessTokenInfoResponse.prototype['user_id'] = undefined;






export default AccessTokenInfoResponse;

