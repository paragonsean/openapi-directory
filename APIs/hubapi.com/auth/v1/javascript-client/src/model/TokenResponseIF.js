/**
 * Auth Oauth
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TokenResponseIF model module.
 * @module model/TokenResponseIF
 * @version v1
 */
class TokenResponseIF {
    /**
     * Constructs a new <code>TokenResponseIF</code>.
     * @alias module:model/TokenResponseIF
     * @param accessToken {String} 
     * @param expiresIn {Number} 
     * @param refreshToken {String} 
     * @param tokenType {String} 
     */
    constructor(accessToken, expiresIn, refreshToken, tokenType) { 
        
        TokenResponseIF.initialize(this, accessToken, expiresIn, refreshToken, tokenType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accessToken, expiresIn, refreshToken, tokenType) { 
        obj['access_token'] = accessToken;
        obj['expires_in'] = expiresIn;
        obj['refresh_token'] = refreshToken;
        obj['token_type'] = tokenType;
    }

    /**
     * Constructs a <code>TokenResponseIF</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokenResponseIF} obj Optional instance to populate.
     * @return {module:model/TokenResponseIF} The populated <code>TokenResponseIF</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokenResponseIF();

            if (data.hasOwnProperty('access_token')) {
                obj['access_token'] = ApiClient.convertToType(data['access_token'], 'String');
            }
            if (data.hasOwnProperty('expires_in')) {
                obj['expires_in'] = ApiClient.convertToType(data['expires_in'], 'Number');
            }
            if (data.hasOwnProperty('id_token')) {
                obj['id_token'] = ApiClient.convertToType(data['id_token'], 'String');
            }
            if (data.hasOwnProperty('refresh_token')) {
                obj['refresh_token'] = ApiClient.convertToType(data['refresh_token'], 'String');
            }
            if (data.hasOwnProperty('token_type')) {
                obj['token_type'] = ApiClient.convertToType(data['token_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokenResponseIF</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokenResponseIF</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TokenResponseIF.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['access_token'] && !(typeof data['access_token'] === 'string' || data['access_token'] instanceof String)) {
            throw new Error("Expected the field `access_token` to be a primitive type in the JSON string but got " + data['access_token']);
        }
        // ensure the json data is a string
        if (data['id_token'] && !(typeof data['id_token'] === 'string' || data['id_token'] instanceof String)) {
            throw new Error("Expected the field `id_token` to be a primitive type in the JSON string but got " + data['id_token']);
        }
        // ensure the json data is a string
        if (data['refresh_token'] && !(typeof data['refresh_token'] === 'string' || data['refresh_token'] instanceof String)) {
            throw new Error("Expected the field `refresh_token` to be a primitive type in the JSON string but got " + data['refresh_token']);
        }
        // ensure the json data is a string
        if (data['token_type'] && !(typeof data['token_type'] === 'string' || data['token_type'] instanceof String)) {
            throw new Error("Expected the field `token_type` to be a primitive type in the JSON string but got " + data['token_type']);
        }

        return true;
    }


}

TokenResponseIF.RequiredProperties = ["access_token", "expires_in", "refresh_token", "token_type"];

/**
 * 
 * @member {String} access_token
 */
TokenResponseIF.prototype['access_token'] = undefined;

/**
 * 
 * @member {Number} expires_in
 */
TokenResponseIF.prototype['expires_in'] = undefined;

/**
 * 
 * @member {String} id_token
 */
TokenResponseIF.prototype['id_token'] = undefined;

/**
 * 
 * @member {String} refresh_token
 */
TokenResponseIF.prototype['refresh_token'] = undefined;

/**
 * 
 * @member {String} token_type
 */
TokenResponseIF.prototype['token_type'] = undefined;






export default TokenResponseIF;

