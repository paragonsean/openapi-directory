/**
 * Clever-Cloud API
 * Public API for managing Clever-Cloud data and products
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaymentData model module.
 * @module model/PaymentData
 * @version 1.0.0
 */
class PaymentData {
    /**
     * Constructs a new <code>PaymentData</code>.
     * @alias module:model/PaymentData
     * @param token {String} 
     * @param type {module:model/PaymentData.TypeEnum} 
     */
    constructor(token, type) { 
        
        PaymentData.initialize(this, token, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, token, type) { 
        obj['token'] = token;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>PaymentData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentData} obj Optional instance to populate.
     * @return {module:model/PaymentData} The populated <code>PaymentData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentData();

            if (data.hasOwnProperty('deviceData')) {
                obj['deviceData'] = ApiClient.convertToType(data['deviceData'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['deviceData'] && !(typeof data['deviceData'] === 'string' || data['deviceData'] instanceof String)) {
            throw new Error("Expected the field `deviceData` to be a primitive type in the JSON string but got " + data['deviceData']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

PaymentData.RequiredProperties = ["token", "type"];

/**
 * @member {String} deviceData
 */
PaymentData.prototype['deviceData'] = undefined;

/**
 * @member {String} token
 */
PaymentData.prototype['token'] = undefined;

/**
 * @member {module:model/PaymentData.TypeEnum} type
 */
PaymentData.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
PaymentData['TypeEnum'] = {

    /**
     * value: "NEW_CARD"
     * @const
     */
    "NEW_CARD": "NEW_CARD",

    /**
     * value: "EXISTING_CARD"
     * @const
     */
    "EXISTING_CARD": "EXISTING_CARD"
};



export default PaymentData;

