/**
 * Clever-Cloud API
 * Public API for managing Clever-Cloud data and products
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WannabeEnv from './WannabeEnv';

/**
 * The Env model module.
 * @module model/Env
 * @version 1.0.0
 */
class Env {
    /**
     * Constructs a new <code>Env</code>.
     * @alias module:model/Env
     * @param addonId {String} 
     * @param addonName {String} 
     * @param env {Array.<module:model/WannabeEnv>} 
     * @param providerId {String} 
     */
    constructor(addonId, addonName, env, providerId) { 
        
        Env.initialize(this, addonId, addonName, env, providerId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, addonId, addonName, env, providerId) { 
        obj['addon_id'] = addonId;
        obj['addon_name'] = addonName;
        obj['env'] = env;
        obj['provider_id'] = providerId;
    }

    /**
     * Constructs a <code>Env</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Env} obj Optional instance to populate.
     * @return {module:model/Env} The populated <code>Env</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Env();

            if (data.hasOwnProperty('addon_id')) {
                obj['addon_id'] = ApiClient.convertToType(data['addon_id'], 'String');
            }
            if (data.hasOwnProperty('addon_name')) {
                obj['addon_name'] = ApiClient.convertToType(data['addon_name'], 'String');
            }
            if (data.hasOwnProperty('env')) {
                obj['env'] = ApiClient.convertToType(data['env'], [WannabeEnv]);
            }
            if (data.hasOwnProperty('provider_id')) {
                obj['provider_id'] = ApiClient.convertToType(data['provider_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Env</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Env</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Env.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['addon_id'] && !(typeof data['addon_id'] === 'string' || data['addon_id'] instanceof String)) {
            throw new Error("Expected the field `addon_id` to be a primitive type in the JSON string but got " + data['addon_id']);
        }
        // ensure the json data is a string
        if (data['addon_name'] && !(typeof data['addon_name'] === 'string' || data['addon_name'] instanceof String)) {
            throw new Error("Expected the field `addon_name` to be a primitive type in the JSON string but got " + data['addon_name']);
        }
        if (data['env']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['env'])) {
                throw new Error("Expected the field `env` to be an array in the JSON data but got " + data['env']);
            }
            // validate the optional field `env` (array)
            for (const item of data['env']) {
                WannabeEnv.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['provider_id'] && !(typeof data['provider_id'] === 'string' || data['provider_id'] instanceof String)) {
            throw new Error("Expected the field `provider_id` to be a primitive type in the JSON string but got " + data['provider_id']);
        }

        return true;
    }


}

Env.RequiredProperties = ["addon_id", "addon_name", "env", "provider_id"];

/**
 * @member {String} addon_id
 */
Env.prototype['addon_id'] = undefined;

/**
 * @member {String} addon_name
 */
Env.prototype['addon_name'] = undefined;

/**
 * @member {Array.<module:model/WannabeEnv>} env
 */
Env.prototype['env'] = undefined;

/**
 * @member {String} provider_id
 */
Env.prototype['provider_id'] = undefined;






export default Env;

