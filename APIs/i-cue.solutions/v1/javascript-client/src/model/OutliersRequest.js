/**
 * Growth Services
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PlanningLevelDataDto from './PlanningLevelDataDto';

/**
 * The OutliersRequest model module.
 * @module model/OutliersRequest
 * @version v1
 */
class OutliersRequest {
    /**
     * Constructs a new <code>OutliersRequest</code>.
     * @alias module:model/OutliersRequest
     * @param planningLevelId {String} 
     * @param startDate {String} 
     */
    constructor(planningLevelId, startDate) { 
        
        OutliersRequest.initialize(this, planningLevelId, startDate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, planningLevelId, startDate) { 
        obj['planningLevelId'] = planningLevelId;
        obj['startDate'] = startDate;
    }

    /**
     * Constructs a <code>OutliersRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OutliersRequest} obj Optional instance to populate.
     * @return {module:model/OutliersRequest} The populated <code>OutliersRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OutliersRequest();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [PlanningLevelDataDto]);
            }
            if (data.hasOwnProperty('planningLevelId')) {
                obj['planningLevelId'] = ApiClient.convertToType(data['planningLevelId'], 'String');
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OutliersRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OutliersRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OutliersRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                PlanningLevelDataDto.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['planningLevelId'] && !(typeof data['planningLevelId'] === 'string' || data['planningLevelId'] instanceof String)) {
            throw new Error("Expected the field `planningLevelId` to be a primitive type in the JSON string but got " + data['planningLevelId']);
        }
        // ensure the json data is a string
        if (data['startDate'] && !(typeof data['startDate'] === 'string' || data['startDate'] instanceof String)) {
            throw new Error("Expected the field `startDate` to be a primitive type in the JSON string but got " + data['startDate']);
        }

        return true;
    }


}

OutliersRequest.RequiredProperties = ["planningLevelId", "startDate"];

/**
 * @member {Array.<module:model/PlanningLevelDataDto>} data
 */
OutliersRequest.prototype['data'] = undefined;

/**
 * @member {String} planningLevelId
 */
OutliersRequest.prototype['planningLevelId'] = undefined;

/**
 * @member {String} startDate
 */
OutliersRequest.prototype['startDate'] = undefined;






export default OutliersRequest;

