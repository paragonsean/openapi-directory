# most_popular_api

MostPopularApi - JavaScript client for most_popular_api
Get lists of NYT Articles based on shares, emails, and views.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2.0.0
- Package version: 2.0.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install most_popular_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your most_popular_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var MostPopularApi = require('most_popular_api');

var defaultClient = MostPopularApi.ApiClient.instance;
// Configure API key authorization: api-key
var api-key = defaultClient.authentications['api-key'];
api-key.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//api-key.apiKeyPrefix['api-key'] = "Token"

var api = new MostPopularApi.DefaultApi()
var section = "section_example"; // {String} Limits the results by one or more sections. You can use `all-sections` or one or more section names seperated by semicolons. See `viewed/sections.json` call to get a list of sections.  
var timePeriod = "timePeriod_example"; // {String} Number of days `1 | 7 | 30 ` corresponds to a day, a week, or a month of content.
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.gETMostemailedSectionTimePeriodJson(section, timePeriod, callback);

```

## Documentation for API Endpoints

All URIs are relative to *http://api.nytimes.com/svc/mostpopular/v2*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*MostPopularApi.DefaultApi* | [**gETMostemailedSectionTimePeriodJson**](docs/DefaultApi.md#gETMostemailedSectionTimePeriodJson) | **GET** /mostemailed/{section}/{time-period}.json | Most Emailed by Section &amp; Time Period
*MostPopularApi.DefaultApi* | [**gETMostsharedSectionTimePeriodJson**](docs/DefaultApi.md#gETMostsharedSectionTimePeriodJson) | **GET** /mostshared/{section}/{time-period}.json | Most Shared by Section &amp; Time Period
*MostPopularApi.DefaultApi* | [**gETMostviewedSectionTimePeriodJson**](docs/DefaultApi.md#gETMostviewedSectionTimePeriodJson) | **GET** /mostviewed/{section}/{time-period}.json | Most Viewed by Section &amp; Time Period


## Documentation for Models

 - [MostPopularApi.Article](docs/Article.md)
 - [MostPopularApi.ArticleWithCountType](docs/ArticleWithCountType.md)
 - [MostPopularApi.ArticleWithCountTypeMediaInner](docs/ArticleWithCountTypeMediaInner.md)
 - [MostPopularApi.ArticleWithCountTypeMediaInnerMediaMetadata](docs/ArticleWithCountTypeMediaInnerMediaMetadata.md)
 - [MostPopularApi.GETMostemailedSectionTimePeriodJson200Response](docs/GETMostemailedSectionTimePeriodJson200Response.md)
 - [MostPopularApi.GETMostemailedSectionTimePeriodJson400Response](docs/GETMostemailedSectionTimePeriodJson400Response.md)
 - [MostPopularApi.GETMostsharedSectionTimePeriodJson200Response](docs/GETMostsharedSectionTimePeriodJson200Response.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### api-key


- **Type**: API key
- **API key parameter name**: api-key
- **Location**: URL query string

