/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DataSubjectRightStatusResponse model module.
 * @module model/DataSubjectRightStatusResponse
 * @version v0.1
 */
class DataSubjectRightStatusResponse {
    /**
     * Constructs a new <code>DataSubjectRightStatusResponse</code>.
     * @alias module:model/DataSubjectRightStatusResponse
     * @param message {String} explanation message of the status
     * @param status {module:model/DataSubjectRightStatusResponse.StatusEnum} Status of data subject right request
     */
    constructor(message, status) { 
        
        DataSubjectRightStatusResponse.initialize(this, message, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, message, status) { 
        obj['message'] = message;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>DataSubjectRightStatusResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DataSubjectRightStatusResponse} obj Optional instance to populate.
     * @return {module:model/DataSubjectRightStatusResponse} The populated <code>DataSubjectRightStatusResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DataSubjectRightStatusResponse();

            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('sasUrl')) {
                obj['sasUrl'] = ApiClient.convertToType(data['sasUrl'], 'String');
            }
            if (data.hasOwnProperty('sasUrlExpired')) {
                obj['sasUrlExpired'] = ApiClient.convertToType(data['sasUrlExpired'], 'Boolean');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DataSubjectRightStatusResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DataSubjectRightStatusResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DataSubjectRightStatusResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['sasUrl'] && !(typeof data['sasUrl'] === 'string' || data['sasUrl'] instanceof String)) {
            throw new Error("Expected the field `sasUrl` to be a primitive type in the JSON string but got " + data['sasUrl']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

DataSubjectRightStatusResponse.RequiredProperties = ["message", "status"];

/**
 * explanation message of the status
 * @member {String} message
 */
DataSubjectRightStatusResponse.prototype['message'] = undefined;

/**
 * Azure Storage shared access signature (SAS) URL for exported user data.
 * @member {String} sasUrl
 */
DataSubjectRightStatusResponse.prototype['sasUrl'] = undefined;

/**
 * Whether Azure Storage shared access signature (SAS) URL has expired or not.
 * @member {Boolean} sasUrlExpired
 */
DataSubjectRightStatusResponse.prototype['sasUrlExpired'] = undefined;

/**
 * Status of data subject right request
 * @member {module:model/DataSubjectRightStatusResponse.StatusEnum} status
 */
DataSubjectRightStatusResponse.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
DataSubjectRightStatusResponse['StatusEnum'] = {

    /**
     * value: "None"
     * @const
     */
    "None": "None",

    /**
     * value: "Created"
     * @const
     */
    "Created": "Created",

    /**
     * value: "Queued"
     * @const
     */
    "Queued": "Queued",

    /**
     * value: "InProgress"
     * @const
     */
    "InProgress": "InProgress",

    /**
     * value: "Completed"
     * @const
     */
    "Completed": "Completed",

    /**
     * value: "Failed"
     * @const
     */
    "Failed": "Failed"
};



export default DataSubjectRightStatusResponse;

