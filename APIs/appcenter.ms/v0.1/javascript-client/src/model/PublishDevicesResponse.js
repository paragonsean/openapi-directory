/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PublishDevicesResponse model module.
 * @module model/PublishDevicesResponse
 * @version v0.1
 */
class PublishDevicesResponse {
    /**
     * Constructs a new <code>PublishDevicesResponse</code>.
     * The information for a single iOS device
     * @alias module:model/PublishDevicesResponse
     * @param profilesZipBase64 {String} The updated provisioning profiles zip base64 encoded.
     */
    constructor(profilesZipBase64) { 
        
        PublishDevicesResponse.initialize(this, profilesZipBase64);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, profilesZipBase64) { 
        obj['profiles_zip_base64'] = profilesZipBase64;
    }

    /**
     * Constructs a <code>PublishDevicesResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PublishDevicesResponse} obj Optional instance to populate.
     * @return {module:model/PublishDevicesResponse} The populated <code>PublishDevicesResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PublishDevicesResponse();

            if (data.hasOwnProperty('profile_file_name')) {
                obj['profile_file_name'] = ApiClient.convertToType(data['profile_file_name'], 'String');
            }
            if (data.hasOwnProperty('profiles_zip_base64')) {
                obj['profiles_zip_base64'] = ApiClient.convertToType(data['profiles_zip_base64'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PublishDevicesResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PublishDevicesResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PublishDevicesResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['profile_file_name'] && !(typeof data['profile_file_name'] === 'string' || data['profile_file_name'] instanceof String)) {
            throw new Error("Expected the field `profile_file_name` to be a primitive type in the JSON string but got " + data['profile_file_name']);
        }
        // ensure the json data is a string
        if (data['profiles_zip_base64'] && !(typeof data['profiles_zip_base64'] === 'string' || data['profiles_zip_base64'] instanceof String)) {
            throw new Error("Expected the field `profiles_zip_base64` to be a primitive type in the JSON string but got " + data['profiles_zip_base64']);
        }

        return true;
    }


}

PublishDevicesResponse.RequiredProperties = ["profiles_zip_base64"];

/**
 * The file name for the provisioning profile.
 * @member {String} profile_file_name
 */
PublishDevicesResponse.prototype['profile_file_name'] = undefined;

/**
 * The updated provisioning profiles zip base64 encoded.
 * @member {String} profiles_zip_base64
 */
PublishDevicesResponse.prototype['profiles_zip_base64'] = undefined;






export default PublishDevicesResponse;

