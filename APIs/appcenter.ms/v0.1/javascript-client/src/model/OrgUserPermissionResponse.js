/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OrgUserPermissionResponse model module.
 * @module model/OrgUserPermissionResponse
 * @version v0.1
 */
class OrgUserPermissionResponse {
    /**
     * Constructs a new <code>OrgUserPermissionResponse</code>.
     * @alias module:model/OrgUserPermissionResponse
     * @param orgId {String} The unique id (UUID) of the org
     * @param userRole {module:model/OrgUserPermissionResponse.UserRoleEnum} The user role for the org
     */
    constructor(orgId, userRole) { 
        
        OrgUserPermissionResponse.initialize(this, orgId, userRole);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orgId, userRole) { 
        obj['orgId'] = orgId;
        obj['userRole'] = userRole;
    }

    /**
     * Constructs a <code>OrgUserPermissionResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrgUserPermissionResponse} obj Optional instance to populate.
     * @return {module:model/OrgUserPermissionResponse} The populated <code>OrgUserPermissionResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrgUserPermissionResponse();

            if (data.hasOwnProperty('orgId')) {
                obj['orgId'] = ApiClient.convertToType(data['orgId'], 'String');
            }
            if (data.hasOwnProperty('userRole')) {
                obj['userRole'] = ApiClient.convertToType(data['userRole'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrgUserPermissionResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrgUserPermissionResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrgUserPermissionResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['orgId'] && !(typeof data['orgId'] === 'string' || data['orgId'] instanceof String)) {
            throw new Error("Expected the field `orgId` to be a primitive type in the JSON string but got " + data['orgId']);
        }
        // ensure the json data is a string
        if (data['userRole'] && !(typeof data['userRole'] === 'string' || data['userRole'] instanceof String)) {
            throw new Error("Expected the field `userRole` to be a primitive type in the JSON string but got " + data['userRole']);
        }

        return true;
    }


}

OrgUserPermissionResponse.RequiredProperties = ["orgId", "userRole"];

/**
 * The unique id (UUID) of the org
 * @member {String} orgId
 */
OrgUserPermissionResponse.prototype['orgId'] = undefined;

/**
 * The user role for the org
 * @member {module:model/OrgUserPermissionResponse.UserRoleEnum} userRole
 */
OrgUserPermissionResponse.prototype['userRole'] = undefined;





/**
 * Allowed values for the <code>userRole</code> property.
 * @enum {String}
 * @readonly
 */
OrgUserPermissionResponse['UserRoleEnum'] = {

    /**
     * value: "admin"
     * @const
     */
    "admin": "admin",

    /**
     * value: "collaborator"
     * @const
     */
    "collaborator": "collaborator"
};



export default OrgUserPermissionResponse;

