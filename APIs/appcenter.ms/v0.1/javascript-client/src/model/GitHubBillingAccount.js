/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GitHubBillingAccount model module.
 * @module model/GitHubBillingAccount
 * @version v0.1
 */
class GitHubBillingAccount {
    /**
     * Constructs a new <code>GitHubBillingAccount</code>.
     * App Center account details from GitHub billing
     * @alias module:model/GitHubBillingAccount
     * @param displayName {String} The account's display name
     * @param id {String} The unique id (UUID) of the account
     * @param name {String} The unique name that used to identify the owner
     * @param type {module:model/GitHubBillingAccount.TypeEnum} The owner type. Can either be 'org' or 'user'
     */
    constructor(displayName, id, name, type) { 
        
        GitHubBillingAccount.initialize(this, displayName, id, name, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, displayName, id, name, type) { 
        obj['display_name'] = displayName;
        obj['id'] = id;
        obj['name'] = name;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>GitHubBillingAccount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GitHubBillingAccount} obj Optional instance to populate.
     * @return {module:model/GitHubBillingAccount} The populated <code>GitHubBillingAccount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GitHubBillingAccount();

            if (data.hasOwnProperty('display_name')) {
                obj['display_name'] = ApiClient.convertToType(data['display_name'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GitHubBillingAccount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GitHubBillingAccount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GitHubBillingAccount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['display_name'] && !(typeof data['display_name'] === 'string' || data['display_name'] instanceof String)) {
            throw new Error("Expected the field `display_name` to be a primitive type in the JSON string but got " + data['display_name']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

GitHubBillingAccount.RequiredProperties = ["display_name", "id", "name", "type"];

/**
 * The account's display name
 * @member {String} display_name
 */
GitHubBillingAccount.prototype['display_name'] = undefined;

/**
 * The unique id (UUID) of the account
 * @member {String} id
 */
GitHubBillingAccount.prototype['id'] = undefined;

/**
 * The unique name that used to identify the owner
 * @member {String} name
 */
GitHubBillingAccount.prototype['name'] = undefined;

/**
 * The owner type. Can either be 'org' or 'user'
 * @member {module:model/GitHubBillingAccount.TypeEnum} type
 */
GitHubBillingAccount.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
GitHubBillingAccount['TypeEnum'] = {

    /**
     * value: "org"
     * @const
     */
    "org": "org",

    /**
     * value: "user"
     * @const
     */
    "user": "user"
};



export default GitHubBillingAccount;

