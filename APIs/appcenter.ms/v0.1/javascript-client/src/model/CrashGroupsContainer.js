/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CrashGroupsList200ResponseCrashGroupsInner from './CrashGroupsList200ResponseCrashGroupsInner';

/**
 * The CrashGroupsContainer model module.
 * @module model/CrashGroupsContainer
 * @version v0.1
 */
class CrashGroupsContainer {
    /**
     * Constructs a new <code>CrashGroupsContainer</code>.
     * @alias module:model/CrashGroupsContainer
     * @param crashGroups {Array.<module:model/CrashGroupsList200ResponseCrashGroupsInner>} 
     * @param limitedResultSet {Boolean} 
     */
    constructor(crashGroups, limitedResultSet) { 
        
        CrashGroupsContainer.initialize(this, crashGroups, limitedResultSet);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, crashGroups, limitedResultSet) { 
        obj['crash_groups'] = crashGroups;
        obj['limited_result_set'] = limitedResultSet;
    }

    /**
     * Constructs a <code>CrashGroupsContainer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CrashGroupsContainer} obj Optional instance to populate.
     * @return {module:model/CrashGroupsContainer} The populated <code>CrashGroupsContainer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CrashGroupsContainer();

            if (data.hasOwnProperty('continuation_token')) {
                obj['continuation_token'] = ApiClient.convertToType(data['continuation_token'], 'String');
            }
            if (data.hasOwnProperty('crash_groups')) {
                obj['crash_groups'] = ApiClient.convertToType(data['crash_groups'], [CrashGroupsList200ResponseCrashGroupsInner]);
            }
            if (data.hasOwnProperty('limited_result_set')) {
                obj['limited_result_set'] = ApiClient.convertToType(data['limited_result_set'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CrashGroupsContainer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CrashGroupsContainer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CrashGroupsContainer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['continuation_token'] && !(typeof data['continuation_token'] === 'string' || data['continuation_token'] instanceof String)) {
            throw new Error("Expected the field `continuation_token` to be a primitive type in the JSON string but got " + data['continuation_token']);
        }
        if (data['crash_groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['crash_groups'])) {
                throw new Error("Expected the field `crash_groups` to be an array in the JSON data but got " + data['crash_groups']);
            }
            // validate the optional field `crash_groups` (array)
            for (const item of data['crash_groups']) {
                CrashGroupsList200ResponseCrashGroupsInner.validateJSON(item);
            };
        }

        return true;
    }


}

CrashGroupsContainer.RequiredProperties = ["crash_groups", "limited_result_set"];

/**
 * Cassandra request continuation token. The token is used for pagination.
 * @member {String} continuation_token
 */
CrashGroupsContainer.prototype['continuation_token'] = undefined;

/**
 * @member {Array.<module:model/CrashGroupsList200ResponseCrashGroupsInner>} crash_groups
 */
CrashGroupsContainer.prototype['crash_groups'] = undefined;

/**
 * @member {Boolean} limited_result_set
 */
CrashGroupsContainer.prototype['limited_result_set'] = undefined;






export default CrashGroupsContainer;

