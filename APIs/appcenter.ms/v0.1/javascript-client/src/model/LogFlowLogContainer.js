/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AnalyticsLogFlow200ResponseLogsInner from './AnalyticsLogFlow200ResponseLogsInner';

/**
 * The LogFlowLogContainer model module.
 * @module model/LogFlowLogContainer
 * @version v0.1
 */
class LogFlowLogContainer {
    /**
     * Constructs a new <code>LogFlowLogContainer</code>.
     * @alias module:model/LogFlowLogContainer
     * @param logs {Array.<module:model/AnalyticsLogFlow200ResponseLogsInner>} the list of logs
     */
    constructor(logs) { 
        
        LogFlowLogContainer.initialize(this, logs);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, logs) { 
        obj['logs'] = logs;
    }

    /**
     * Constructs a <code>LogFlowLogContainer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogFlowLogContainer} obj Optional instance to populate.
     * @return {module:model/LogFlowLogContainer} The populated <code>LogFlowLogContainer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogFlowLogContainer();

            if (data.hasOwnProperty('exceeded_max_limit')) {
                obj['exceeded_max_limit'] = ApiClient.convertToType(data['exceeded_max_limit'], 'Boolean');
            }
            if (data.hasOwnProperty('last_received_log_timestamp')) {
                obj['last_received_log_timestamp'] = ApiClient.convertToType(data['last_received_log_timestamp'], 'Date');
            }
            if (data.hasOwnProperty('logs')) {
                obj['logs'] = ApiClient.convertToType(data['logs'], [AnalyticsLogFlow200ResponseLogsInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogFlowLogContainer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogFlowLogContainer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LogFlowLogContainer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['logs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['logs'])) {
                throw new Error("Expected the field `logs` to be an array in the JSON data but got " + data['logs']);
            }
            // validate the optional field `logs` (array)
            for (const item of data['logs']) {
                AnalyticsLogFlow200ResponseLogsInner.validateJSON(item);
            };
        }

        return true;
    }


}

LogFlowLogContainer.RequiredProperties = ["logs"];

/**
 * indicates if the number of available logs are more than the max allowed return limit(100).
 * @member {Boolean} exceeded_max_limit
 */
LogFlowLogContainer.prototype['exceeded_max_limit'] = undefined;

/**
 * the timestamp of the last log received. This value can be used as the start time parameter in the consecutive API call.
 * @member {Date} last_received_log_timestamp
 */
LogFlowLogContainer.prototype['last_received_log_timestamp'] = undefined;

/**
 * the list of logs
 * @member {Array.<module:model/AnalyticsLogFlow200ResponseLogsInner>} logs
 */
LogFlowLogContainer.prototype['logs'] = undefined;






export default LogFlowLogContainer;

