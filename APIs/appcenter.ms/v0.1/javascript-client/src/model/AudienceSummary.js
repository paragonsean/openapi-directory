/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AudienceSummary model module.
 * @module model/AudienceSummary
 * @version v0.1
 */
class AudienceSummary {
    /**
     * Constructs a new <code>AudienceSummary</code>.
     * Audience definition.
     * @alias module:model/AudienceSummary
     */
    constructor() { 
        
        AudienceSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AudienceSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AudienceSummary} obj Optional instance to populate.
     * @return {module:model/AudienceSummary} The populated <code>AudienceSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AudienceSummary();

            if (data.hasOwnProperty('definition')) {
                obj['definition'] = ApiClient.convertToType(data['definition'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('estimated_count')) {
                obj['estimated_count'] = ApiClient.convertToType(data['estimated_count'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AudienceSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AudienceSummary</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['definition'] && !(typeof data['definition'] === 'string' || data['definition'] instanceof String)) {
            throw new Error("Expected the field `definition` to be a primitive type in the JSON string but got " + data['definition']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}



/**
 * Audience definition in OData format.
 * @member {String} definition
 */
AudienceSummary.prototype['definition'] = undefined;

/**
 * Audience description.
 * @member {String} description
 */
AudienceSummary.prototype['description'] = undefined;

/**
 * Estimated audience size.
 * @member {Number} estimated_count
 */
AudienceSummary.prototype['estimated_count'] = undefined;

/**
 * Audience name.
 * @member {String} name
 */
AudienceSummary.prototype['name'] = undefined;

/**
 * Audience state.
 * @member {module:model/AudienceSummary.StateEnum} state
 */
AudienceSummary.prototype['state'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
AudienceSummary['StateEnum'] = {

    /**
     * value: "Calculating"
     * @const
     */
    "Calculating": "Calculating",

    /**
     * value: "Ready"
     * @const
     */
    "Ready": "Ready",

    /**
     * value: "Disabled"
     * @const
     */
    "Disabled": "Disabled"
};



export default AudienceSummary;

