/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AlertBugTrackerRepoOwner from './AlertBugTrackerRepoOwner';

/**
 * The AlertBugTrackerRepo model module.
 * @module model/AlertBugTrackerRepo
 * @version v0.1
 */
class AlertBugTrackerRepo {
    /**
     * Constructs a new <code>AlertBugTrackerRepo</code>.
     * Repostiory object
     * @alias module:model/AlertBugTrackerRepo
     * @param id {String} 
     * @param name {String} 
     * @param url {String} 
     */
    constructor(id, name, url) { 
        
        AlertBugTrackerRepo.initialize(this, id, name, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, url) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>AlertBugTrackerRepo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AlertBugTrackerRepo} obj Optional instance to populate.
     * @return {module:model/AlertBugTrackerRepo} The populated <code>AlertBugTrackerRepo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AlertBugTrackerRepo();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('owner')) {
                obj['owner'] = AlertBugTrackerRepoOwner.constructFromObject(data['owner']);
            }
            if (data.hasOwnProperty('private')) {
                obj['private'] = ApiClient.convertToType(data['private'], 'Boolean');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AlertBugTrackerRepo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AlertBugTrackerRepo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AlertBugTrackerRepo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `owner`
        if (data['owner']) { // data not null
          AlertBugTrackerRepoOwner.validateJSON(data['owner']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

AlertBugTrackerRepo.RequiredProperties = ["id", "name", "url"];

/**
 * @member {String} description
 */
AlertBugTrackerRepo.prototype['description'] = undefined;

/**
 * @member {String} id
 */
AlertBugTrackerRepo.prototype['id'] = undefined;

/**
 * @member {String} name
 */
AlertBugTrackerRepo.prototype['name'] = undefined;

/**
 * @member {module:model/AlertBugTrackerRepoOwner} owner
 */
AlertBugTrackerRepo.prototype['owner'] = undefined;

/**
 * @member {Boolean} private
 */
AlertBugTrackerRepo.prototype['private'] = undefined;

/**
 * @member {String} url
 */
AlertBugTrackerRepo.prototype['url'] = undefined;






export default AlertBugTrackerRepo;

