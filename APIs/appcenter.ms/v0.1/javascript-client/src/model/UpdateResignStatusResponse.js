/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdateResignStatusResponse model module.
 * @module model/UpdateResignStatusResponse
 * @version v0.1
 */
class UpdateResignStatusResponse {
    /**
     * Constructs a new <code>UpdateResignStatusResponse</code>.
     * URL that can be used to check the status of the update devices operation and the updated profiles.
     * @alias module:model/UpdateResignStatusResponse
     * @param status {String} The status.
     */
    constructor(status) { 
        
        UpdateResignStatusResponse.initialize(this, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, status) { 
        obj['status'] = status;
    }

    /**
     * Constructs a <code>UpdateResignStatusResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateResignStatusResponse} obj Optional instance to populate.
     * @return {module:model/UpdateResignStatusResponse} The populated <code>UpdateResignStatusResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateResignStatusResponse();

            if (data.hasOwnProperty('profiles_zip_base64')) {
                obj['profiles_zip_base64'] = ApiClient.convertToType(data['profiles_zip_base64'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateResignStatusResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateResignStatusResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateResignStatusResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['profiles_zip_base64'] && !(typeof data['profiles_zip_base64'] === 'string' || data['profiles_zip_base64'] instanceof String)) {
            throw new Error("Expected the field `profiles_zip_base64` to be a primitive type in the JSON string but got " + data['profiles_zip_base64']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

UpdateResignStatusResponse.RequiredProperties = ["status"];

/**
 * A zip of the updated provisioning profiles. Base64 encoded.
 * @member {String} profiles_zip_base64
 */
UpdateResignStatusResponse.prototype['profiles_zip_base64'] = undefined;

/**
 * The status.
 * @member {String} status
 */
UpdateResignStatusResponse.prototype['status'] = undefined;






export default UpdateResignStatusResponse;

