/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GDPRValidationRequest model module.
 * @module model/GDPRValidationRequest
 * @version v0.1
 */
class GDPRValidationRequest {
    /**
     * Constructs a new <code>GDPRValidationRequest</code>.
     * A request containing a set of release ids to validate
     * @alias module:model/GDPRValidationRequest
     * @param releaseIds {Array.<Number>} a list of release ids to validate (AC flow)
     */
    constructor(releaseIds) { 
        
        GDPRValidationRequest.initialize(this, releaseIds);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, releaseIds) { 
        obj['release_ids'] = releaseIds;
    }

    /**
     * Constructs a <code>GDPRValidationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GDPRValidationRequest} obj Optional instance to populate.
     * @return {module:model/GDPRValidationRequest} The populated <code>GDPRValidationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GDPRValidationRequest();

            if (data.hasOwnProperty('hockeyapp_release_ids')) {
                obj['hockeyapp_release_ids'] = ApiClient.convertToType(data['hockeyapp_release_ids'], ['Number']);
            }
            if (data.hasOwnProperty('release_ids')) {
                obj['release_ids'] = ApiClient.convertToType(data['release_ids'], ['Number']);
            }
            if (data.hasOwnProperty('release_upload_ids')) {
                obj['release_upload_ids'] = ApiClient.convertToType(data['release_upload_ids'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GDPRValidationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GDPRValidationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GDPRValidationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['hockeyapp_release_ids'])) {
            throw new Error("Expected the field `hockeyapp_release_ids` to be an array in the JSON data but got " + data['hockeyapp_release_ids']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['release_ids'])) {
            throw new Error("Expected the field `release_ids` to be an array in the JSON data but got " + data['release_ids']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['release_upload_ids'])) {
            throw new Error("Expected the field `release_upload_ids` to be an array in the JSON data but got " + data['release_upload_ids']);
        }

        return true;
    }


}

GDPRValidationRequest.RequiredProperties = ["release_ids"];

/**
 * a list of release ids to validate (HA flow)
 * @member {Array.<Number>} hockeyapp_release_ids
 */
GDPRValidationRequest.prototype['hockeyapp_release_ids'] = undefined;

/**
 * a list of release ids to validate (AC flow)
 * @member {Array.<Number>} release_ids
 */
GDPRValidationRequest.prototype['release_ids'] = undefined;

/**
 * a list of release uploads ids to validate
 * @member {Array.<String>} release_upload_ids
 */
GDPRValidationRequest.prototype['release_upload_ids'] = undefined;






export default GDPRValidationRequest;

