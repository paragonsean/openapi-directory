/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TestCloudHashUploadStatus from './TestCloudHashUploadStatus';

/**
 * The TestCloudFileHashResponse model module.
 * @module model/TestCloudFileHashResponse
 * @version v0.1
 */
class TestCloudFileHashResponse {
    /**
     * Constructs a new <code>TestCloudFileHashResponse</code>.
     * Response message for single uploaded file hash
     * @alias module:model/TestCloudFileHashResponse
     * @param checksum {String} SHA256 hash of the file
     * @param fileType {module:model/TestCloudFileHashResponse.FileTypeEnum} Type of the file
     * @param uploadStatus {module:model/TestCloudHashUploadStatus} 
     */
    constructor(checksum, fileType, uploadStatus) { 
        
        TestCloudFileHashResponse.initialize(this, checksum, fileType, uploadStatus);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, checksum, fileType, uploadStatus) { 
        obj['checksum'] = checksum;
        obj['fileType'] = fileType;
        obj['uploadStatus'] = uploadStatus;
    }

    /**
     * Constructs a <code>TestCloudFileHashResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TestCloudFileHashResponse} obj Optional instance to populate.
     * @return {module:model/TestCloudFileHashResponse} The populated <code>TestCloudFileHashResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TestCloudFileHashResponse();

            if (data.hasOwnProperty('checksum')) {
                obj['checksum'] = ApiClient.convertToType(data['checksum'], 'String');
            }
            if (data.hasOwnProperty('fileType')) {
                obj['fileType'] = ApiClient.convertToType(data['fileType'], 'String');
            }
            if (data.hasOwnProperty('relativePath')) {
                obj['relativePath'] = ApiClient.convertToType(data['relativePath'], 'String');
            }
            if (data.hasOwnProperty('uploadStatus')) {
                obj['uploadStatus'] = TestCloudHashUploadStatus.constructFromObject(data['uploadStatus']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TestCloudFileHashResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TestCloudFileHashResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TestCloudFileHashResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['checksum'] && !(typeof data['checksum'] === 'string' || data['checksum'] instanceof String)) {
            throw new Error("Expected the field `checksum` to be a primitive type in the JSON string but got " + data['checksum']);
        }
        // ensure the json data is a string
        if (data['fileType'] && !(typeof data['fileType'] === 'string' || data['fileType'] instanceof String)) {
            throw new Error("Expected the field `fileType` to be a primitive type in the JSON string but got " + data['fileType']);
        }
        // ensure the json data is a string
        if (data['relativePath'] && !(typeof data['relativePath'] === 'string' || data['relativePath'] instanceof String)) {
            throw new Error("Expected the field `relativePath` to be a primitive type in the JSON string but got " + data['relativePath']);
        }
        // validate the optional field `uploadStatus`
        if (data['uploadStatus']) { // data not null
          TestCloudHashUploadStatus.validateJSON(data['uploadStatus']);
        }

        return true;
    }


}

TestCloudFileHashResponse.RequiredProperties = ["checksum", "fileType", "uploadStatus"];

/**
 * SHA256 hash of the file
 * @member {String} checksum
 */
TestCloudFileHashResponse.prototype['checksum'] = undefined;

/**
 * Type of the file
 * @member {module:model/TestCloudFileHashResponse.FileTypeEnum} fileType
 */
TestCloudFileHashResponse.prototype['fileType'] = undefined;

/**
 * Relative path of the file
 * @member {String} relativePath
 */
TestCloudFileHashResponse.prototype['relativePath'] = undefined;

/**
 * @member {module:model/TestCloudHashUploadStatus} uploadStatus
 */
TestCloudFileHashResponse.prototype['uploadStatus'] = undefined;





/**
 * Allowed values for the <code>fileType</code> property.
 * @enum {String}
 * @readonly
 */
TestCloudFileHashResponse['FileTypeEnum'] = {

    /**
     * value: "dsym-file"
     * @const
     */
    "dsym-file": "dsym-file",

    /**
     * value: "app-file"
     * @const
     */
    "app-file": "app-file",

    /**
     * value: "test-file"
     * @const
     */
    "test-file": "test-file"
};



export default TestCloudFileHashResponse;

