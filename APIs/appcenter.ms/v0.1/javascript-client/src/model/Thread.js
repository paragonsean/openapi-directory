/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Exception from './Exception';
import ExceptionFramesInner from './ExceptionFramesInner';

/**
 * The Thread model module.
 * @module model/Thread
 * @version v0.1
 */
class Thread {
    /**
     * Constructs a new <code>Thread</code>.
     * a thread representation
     * @alias module:model/Thread
     * @param frames {Array.<module:model/ExceptionFramesInner>} frames of that thread
     * @param title {String} name of the thread
     */
    constructor(frames, title) { 
        
        Thread.initialize(this, frames, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, frames, title) { 
        obj['frames'] = frames;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>Thread</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Thread} obj Optional instance to populate.
     * @return {module:model/Thread} The populated <code>Thread</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Thread();

            if (data.hasOwnProperty('crashed')) {
                obj['crashed'] = ApiClient.convertToType(data['crashed'], 'Boolean');
            }
            if (data.hasOwnProperty('exception')) {
                obj['exception'] = Exception.constructFromObject(data['exception']);
            }
            if (data.hasOwnProperty('frames')) {
                obj['frames'] = ApiClient.convertToType(data['frames'], [ExceptionFramesInner]);
            }
            if (data.hasOwnProperty('platform')) {
                obj['platform'] = ApiClient.convertToType(data['platform'], 'String');
            }
            if (data.hasOwnProperty('relevant')) {
                obj['relevant'] = ApiClient.convertToType(data['relevant'], 'Boolean');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Thread</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Thread</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Thread.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `exception`
        if (data['exception']) { // data not null
          Exception.validateJSON(data['exception']);
        }
        if (data['frames']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['frames'])) {
                throw new Error("Expected the field `frames` to be an array in the JSON data but got " + data['frames']);
            }
            // validate the optional field `frames` (array)
            for (const item of data['frames']) {
                ExceptionFramesInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['platform'] && !(typeof data['platform'] === 'string' || data['platform'] instanceof String)) {
            throw new Error("Expected the field `platform` to be a primitive type in the JSON string but got " + data['platform']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

Thread.RequiredProperties = ["frames", "title"];

/**
 * True if this thread crashed
 * @member {Boolean} crashed
 */
Thread.prototype['crashed'] = undefined;

/**
 * @member {module:model/Exception} exception
 */
Thread.prototype['exception'] = undefined;

/**
 * frames of that thread
 * @member {Array.<module:model/ExceptionFramesInner>} frames
 */
Thread.prototype['frames'] = undefined;

/**
 * SDK/Platform this thread is beeing generated from
 * @member {module:model/Thread.PlatformEnum} platform
 */
Thread.prototype['platform'] = undefined;

/**
 * Shows if a thread is relevant or not. Is false if all frames are non relevant, otherwise true
 * @member {Boolean} relevant
 */
Thread.prototype['relevant'] = undefined;

/**
 * name of the thread
 * @member {String} title
 */
Thread.prototype['title'] = undefined;





/**
 * Allowed values for the <code>platform</code> property.
 * @enum {String}
 * @readonly
 */
Thread['PlatformEnum'] = {

    /**
     * value: "ios"
     * @const
     */
    "ios": "ios",

    /**
     * value: "android"
     * @const
     */
    "android": "android",

    /**
     * value: "xamarin"
     * @const
     */
    "xamarin": "xamarin",

    /**
     * value: "react-native"
     * @const
     */
    "react-native": "react-native",

    /**
     * value: "ndk"
     * @const
     */
    "ndk": "ndk",

    /**
     * value: "unity"
     * @const
     */
    "unity": "unity",

    /**
     * value: "other"
     * @const
     */
    "other": "other"
};



export default Thread;

