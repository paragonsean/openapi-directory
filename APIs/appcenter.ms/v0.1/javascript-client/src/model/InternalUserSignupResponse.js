/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The InternalUserSignupResponse model module.
 * @module model/InternalUserSignupResponse
 * @version v0.1
 */
class InternalUserSignupResponse {
    /**
     * Constructs a new <code>InternalUserSignupResponse</code>.
     * @alias module:model/InternalUserSignupResponse
     * @param displayName {String} The full name of the user. Might for example be first and last name
     * @param email {String} The email address of the user
     * @param id {String} The unique id (UUID) of the user
     * @param name {String} The unique name that is used to identify the user.
     */
    constructor(displayName, email, id, name) { 
        
        InternalUserSignupResponse.initialize(this, displayName, email, id, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, displayName, email, id, name) { 
        obj['display_name'] = displayName;
        obj['email'] = email;
        obj['id'] = id;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>InternalUserSignupResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InternalUserSignupResponse} obj Optional instance to populate.
     * @return {module:model/InternalUserSignupResponse} The populated <code>InternalUserSignupResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InternalUserSignupResponse();

            if (data.hasOwnProperty('display_name')) {
                obj['display_name'] = ApiClient.convertToType(data['display_name'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('external_provider')) {
                obj['external_provider'] = ApiClient.convertToType(data['external_provider'], 'String');
            }
            if (data.hasOwnProperty('external_user_id')) {
                obj['external_user_id'] = ApiClient.convertToType(data['external_user_id'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InternalUserSignupResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InternalUserSignupResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InternalUserSignupResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['display_name'] && !(typeof data['display_name'] === 'string' || data['display_name'] instanceof String)) {
            throw new Error("Expected the field `display_name` to be a primitive type in the JSON string but got " + data['display_name']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['external_provider'] && !(typeof data['external_provider'] === 'string' || data['external_provider'] instanceof String)) {
            throw new Error("Expected the field `external_provider` to be a primitive type in the JSON string but got " + data['external_provider']);
        }
        // ensure the json data is a string
        if (data['external_user_id'] && !(typeof data['external_user_id'] === 'string' || data['external_user_id'] instanceof String)) {
            throw new Error("Expected the field `external_user_id` to be a primitive type in the JSON string but got " + data['external_user_id']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

InternalUserSignupResponse.RequiredProperties = ["display_name", "email", "id", "name"];

/**
 * The full name of the user. Might for example be first and last name
 * @member {String} display_name
 */
InternalUserSignupResponse.prototype['display_name'] = undefined;

/**
 * The email address of the user
 * @member {String} email
 */
InternalUserSignupResponse.prototype['email'] = undefined;

/**
 * The name of the external auth provider
 * @member {String} external_provider
 */
InternalUserSignupResponse.prototype['external_provider'] = undefined;

/**
 * The user ID given by the external provider
 * @member {String} external_user_id
 */
InternalUserSignupResponse.prototype['external_user_id'] = undefined;

/**
 * The unique id (UUID) of the user
 * @member {String} id
 */
InternalUserSignupResponse.prototype['id'] = undefined;

/**
 * The unique name that is used to identify the user.
 * @member {String} name
 */
InternalUserSignupResponse.prototype['name'] = undefined;

/**
 * The current status of the user record after signup
 * @member {module:model/InternalUserSignupResponse.StatusEnum} status
 */
InternalUserSignupResponse.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
InternalUserSignupResponse['StatusEnum'] = {

    /**
     * value: "Complete"
     * @const
     */
    "Complete": "Complete",

    /**
     * value: "NeedsVerification"
     * @const
     */
    "NeedsVerification": "NeedsVerification"
};



export default InternalUserSignupResponse;

