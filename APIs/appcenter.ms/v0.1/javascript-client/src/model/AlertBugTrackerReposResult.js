/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AlertBugTrackerReposResultRepositoriesInner from './AlertBugTrackerReposResultRepositoriesInner';

/**
 * The AlertBugTrackerReposResult model module.
 * @module model/AlertBugTrackerReposResult
 * @version v0.1
 */
class AlertBugTrackerReposResult {
    /**
     * Constructs a new <code>AlertBugTrackerReposResult</code>.
     * List of bug tracker repositories
     * @alias module:model/AlertBugTrackerReposResult
     * @param repositories {Array.<module:model/AlertBugTrackerReposResultRepositoriesInner>} 
     */
    constructor(repositories) { 
        
        AlertBugTrackerReposResult.initialize(this, repositories);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, repositories) { 
        obj['repositories'] = repositories;
    }

    /**
     * Constructs a <code>AlertBugTrackerReposResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AlertBugTrackerReposResult} obj Optional instance to populate.
     * @return {module:model/AlertBugTrackerReposResult} The populated <code>AlertBugTrackerReposResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AlertBugTrackerReposResult();

            if (data.hasOwnProperty('repo_type')) {
                obj['repo_type'] = ApiClient.convertToType(data['repo_type'], 'String');
            }
            if (data.hasOwnProperty('repositories')) {
                obj['repositories'] = ApiClient.convertToType(data['repositories'], [AlertBugTrackerReposResultRepositoriesInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AlertBugTrackerReposResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AlertBugTrackerReposResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AlertBugTrackerReposResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['repo_type'] && !(typeof data['repo_type'] === 'string' || data['repo_type'] instanceof String)) {
            throw new Error("Expected the field `repo_type` to be a primitive type in the JSON string but got " + data['repo_type']);
        }
        if (data['repositories']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['repositories'])) {
                throw new Error("Expected the field `repositories` to be an array in the JSON data but got " + data['repositories']);
            }
            // validate the optional field `repositories` (array)
            for (const item of data['repositories']) {
                AlertBugTrackerReposResultRepositoriesInner.validateJSON(item);
            };
        }

        return true;
    }


}

AlertBugTrackerReposResult.RequiredProperties = ["repositories"];

/**
 * @member {module:model/AlertBugTrackerReposResult.RepoTypeEnum} repo_type
 */
AlertBugTrackerReposResult.prototype['repo_type'] = undefined;

/**
 * @member {Array.<module:model/AlertBugTrackerReposResultRepositoriesInner>} repositories
 */
AlertBugTrackerReposResult.prototype['repositories'] = undefined;





/**
 * Allowed values for the <code>repo_type</code> property.
 * @enum {String}
 * @readonly
 */
AlertBugTrackerReposResult['RepoTypeEnum'] = {

    /**
     * value: "github"
     * @const
     */
    "github": "github",

    /**
     * value: "vsts"
     * @const
     */
    "vsts": "vsts",

    /**
     * value: "jira"
     * @const
     */
    "jira": "jira"
};



export default AlertBugTrackerReposResult;

