/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AlertWebhook model module.
 * @module model/AlertWebhook
 * @version v0.1
 */
class AlertWebhook {
    /**
     * Constructs a new <code>AlertWebhook</code>.
     * Alerting webhook
     * @alias module:model/AlertWebhook
     * @param eventTypes {Array.<module:model/AlertWebhook.EventTypesEnum>} Event types enabled for webhook
     * @param name {String} display name of the webhook
     * @param url {String} target url of the webhook
     */
    constructor(eventTypes, name, url) { 
        
        AlertWebhook.initialize(this, eventTypes, name, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, eventTypes, name, url) { 
        obj['event_types'] = eventTypes;
        obj['name'] = name;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>AlertWebhook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AlertWebhook} obj Optional instance to populate.
     * @return {module:model/AlertWebhook} The populated <code>AlertWebhook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AlertWebhook();

            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('event_types')) {
                obj['event_types'] = ApiClient.convertToType(data['event_types'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AlertWebhook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AlertWebhook</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AlertWebhook.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['event_types'])) {
            throw new Error("Expected the field `event_types` to be an array in the JSON data but got " + data['event_types']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

AlertWebhook.RequiredProperties = ["event_types", "name", "url"];

/**
 * Allows eanble/disable webhook
 * @member {Boolean} enabled
 */
AlertWebhook.prototype['enabled'] = undefined;

/**
 * Event types enabled for webhook
 * @member {Array.<module:model/AlertWebhook.EventTypesEnum>} event_types
 */
AlertWebhook.prototype['event_types'] = undefined;

/**
 * The unique id (UUID) of the webhook
 * @member {String} id
 */
AlertWebhook.prototype['id'] = undefined;

/**
 * display name of the webhook
 * @member {String} name
 */
AlertWebhook.prototype['name'] = undefined;

/**
 * target url of the webhook
 * @member {String} url
 */
AlertWebhook.prototype['url'] = undefined;





/**
 * Allowed values for the <code>eventTypes</code> property.
 * @enum {String}
 * @readonly
 */
AlertWebhook['EventTypesEnum'] = {

    /**
     * value: "newCrashGroupCreated"
     * @const
     */
    "newCrashGroupCreated": "newCrashGroupCreated",

    /**
     * value: "newAppReleased"
     * @const
     */
    "newAppReleased": "newAppReleased"
};



export default AlertWebhook;

