/**
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AlertingError model module.
 * @module model/AlertingError
 * @version v0.1
 */
class AlertingError {
    /**
     * Constructs a new <code>AlertingError</code>.
     * Alerting service error
     * @alias module:model/AlertingError
     * @param requestId {String} Unique request identifier for tracking
     * @param code {module:model/AlertingError.CodeEnum} The status code return by the API. It can be 400 or 404 or 409 or 500.
     */
    constructor(requestId, code) { 
        
        AlertingError.initialize(this, requestId, code);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, requestId, code) { 
        obj['request_id'] = requestId;
        obj['code'] = code;
    }

    /**
     * Constructs a <code>AlertingError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AlertingError} obj Optional instance to populate.
     * @return {module:model/AlertingError} The populated <code>AlertingError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AlertingError();

            if (data.hasOwnProperty('request_id')) {
                obj['request_id'] = ApiClient.convertToType(data['request_id'], 'String');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'Number');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AlertingError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AlertingError</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AlertingError.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['request_id'] && !(typeof data['request_id'] === 'string' || data['request_id'] instanceof String)) {
            throw new Error("Expected the field `request_id` to be a primitive type in the JSON string but got " + data['request_id']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }

        return true;
    }


}

AlertingError.RequiredProperties = ["request_id", "code"];

/**
 * Unique request identifier for tracking
 * @member {String} request_id
 */
AlertingError.prototype['request_id'] = undefined;

/**
 * The status code return by the API. It can be 400 or 404 or 409 or 500.
 * @member {module:model/AlertingError.CodeEnum} code
 */
AlertingError.prototype['code'] = undefined;

/**
 * The reason for the request failed
 * @member {String} message
 */
AlertingError.prototype['message'] = undefined;





/**
 * Allowed values for the <code>code</code> property.
 * @enum {Number}
 * @readonly
 */
AlertingError['CodeEnum'] = {

    /**
     * value: 400
     * @const
     */
    "400": 400,

    /**
     * value: 404
     * @const
     */
    "404": 404,

    /**
     * value: 409
     * @const
     */
    "409": 409,

    /**
     * value: 500
     * @const
     */
    "500": 500
};



export default AlertingError;

