/*
 * App Center Client
 * Microsoft Visual Studio App Center API
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.AlertBugTrackerReposResultRepositoriesInner;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * List of bug tracker repositories
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T09:56:40.008147-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class AlertBugTrackerReposResult {
  /**
   * Gets or Sets repoType
   */
  @JsonAdapter(RepoTypeEnum.Adapter.class)
  public enum RepoTypeEnum {
    GITHUB("github"),
    
    VSTS("vsts"),
    
    JIRA("jira");

    private String value;

    RepoTypeEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static RepoTypeEnum fromValue(String value) {
      for (RepoTypeEnum b : RepoTypeEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<RepoTypeEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final RepoTypeEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public RepoTypeEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return RepoTypeEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      RepoTypeEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_REPO_TYPE = "repo_type";
  @SerializedName(SERIALIZED_NAME_REPO_TYPE)
  private RepoTypeEnum repoType;

  public static final String SERIALIZED_NAME_REPOSITORIES = "repositories";
  @SerializedName(SERIALIZED_NAME_REPOSITORIES)
  private List<AlertBugTrackerReposResultRepositoriesInner> repositories = new ArrayList<>();

  public AlertBugTrackerReposResult() {
  }

  public AlertBugTrackerReposResult repoType(RepoTypeEnum repoType) {
    this.repoType = repoType;
    return this;
  }

  /**
   * Get repoType
   * @return repoType
   */
  @javax.annotation.Nullable
  public RepoTypeEnum getRepoType() {
    return repoType;
  }

  public void setRepoType(RepoTypeEnum repoType) {
    this.repoType = repoType;
  }


  public AlertBugTrackerReposResult repositories(List<AlertBugTrackerReposResultRepositoriesInner> repositories) {
    this.repositories = repositories;
    return this;
  }

  public AlertBugTrackerReposResult addRepositoriesItem(AlertBugTrackerReposResultRepositoriesInner repositoriesItem) {
    if (this.repositories == null) {
      this.repositories = new ArrayList<>();
    }
    this.repositories.add(repositoriesItem);
    return this;
  }

  /**
   * Get repositories
   * @return repositories
   */
  @javax.annotation.Nonnull
  public List<AlertBugTrackerReposResultRepositoriesInner> getRepositories() {
    return repositories;
  }

  public void setRepositories(List<AlertBugTrackerReposResultRepositoriesInner> repositories) {
    this.repositories = repositories;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AlertBugTrackerReposResult alertBugTrackerReposResult = (AlertBugTrackerReposResult) o;
    return Objects.equals(this.repoType, alertBugTrackerReposResult.repoType) &&
        Objects.equals(this.repositories, alertBugTrackerReposResult.repositories);
  }

  @Override
  public int hashCode() {
    return Objects.hash(repoType, repositories);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class AlertBugTrackerReposResult {\n");
    sb.append("    repoType: ").append(toIndentedString(repoType)).append("\n");
    sb.append("    repositories: ").append(toIndentedString(repositories)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("repo_type");
    openapiFields.add("repositories");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("repositories");
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to AlertBugTrackerReposResult
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!AlertBugTrackerReposResult.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in AlertBugTrackerReposResult is not found in the empty JSON string", AlertBugTrackerReposResult.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!AlertBugTrackerReposResult.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `AlertBugTrackerReposResult` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : AlertBugTrackerReposResult.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("repo_type") != null && !jsonObj.get("repo_type").isJsonNull()) && !jsonObj.get("repo_type").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `repo_type` to be a primitive type in the JSON string but got `%s`", jsonObj.get("repo_type").toString()));
      }
      // validate the optional field `repo_type`
      if (jsonObj.get("repo_type") != null && !jsonObj.get("repo_type").isJsonNull()) {
        RepoTypeEnum.validateJsonElement(jsonObj.get("repo_type"));
      }
      // ensure the json data is an array
      if (!jsonObj.get("repositories").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `repositories` to be an array in the JSON string but got `%s`", jsonObj.get("repositories").toString()));
      }

      JsonArray jsonArrayrepositories = jsonObj.getAsJsonArray("repositories");
      // validate the required field `repositories` (array)
      for (int i = 0; i < jsonArrayrepositories.size(); i++) {
        AlertBugTrackerReposResultRepositoriesInner.validateJsonElement(jsonArrayrepositories.get(i));
      };
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!AlertBugTrackerReposResult.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'AlertBugTrackerReposResult' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<AlertBugTrackerReposResult> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(AlertBugTrackerReposResult.class));

       return (TypeAdapter<T>) new TypeAdapter<AlertBugTrackerReposResult>() {
           @Override
           public void write(JsonWriter out, AlertBugTrackerReposResult value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public AlertBugTrackerReposResult read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of AlertBugTrackerReposResult given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of AlertBugTrackerReposResult
   * @throws IOException if the JSON string is invalid with respect to AlertBugTrackerReposResult
   */
  public static AlertBugTrackerReposResult fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, AlertBugTrackerReposResult.class);
  }

  /**
   * Convert an instance of AlertBugTrackerReposResult to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

