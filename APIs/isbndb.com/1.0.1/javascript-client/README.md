# isb_ndb_api

IsbNdbApi - JavaScript client for isb_ndb_api
Definition of ISBNdb.com API
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.1
- Package version: 1.0.1
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install isb_ndb_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your isb_ndb_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var IsbNdbApi = require('isb_ndb_api');

var defaultClient = IsbNdbApi.ApiClient.instance;
// Configure API key authorization: api_key
var api_key = defaultClient.authentications['api_key'];
api_key.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//api_key.apiKeyPrefix['x-api-key'] = "Token"

var api = new IsbNdbApi.AuthorApi()
var name = "name_example"; // {String} The name of an author in the Author's database
var opts = {
  'page': "page_example", // {String} The number of page to retrieve, please note the API will not return more than 10,000 results no matter how you paginate them
  'pageSize': "pageSize_example" // {String} How many items should be returned per page, maximum of 1,000
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.authorNameGet(name, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.isbndb.com*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*IsbNdbApi.AuthorApi* | [**authorNameGet**](docs/AuthorApi.md#authorNameGet) | **GET** /author/{name} | Gets author details
*IsbNdbApi.AuthorApi* | [**authorsQueryGet**](docs/AuthorApi.md#authorsQueryGet) | **GET** /authors/{query} | Search authors
*IsbNdbApi.BookApi* | [**bookIsbnGet**](docs/BookApi.md#bookIsbnGet) | **GET** /book/{isbn} | Gets book details
*IsbNdbApi.BookApi* | [**booksQueryGet**](docs/BookApi.md#booksQueryGet) | **GET** /books/{query} | Search books
*IsbNdbApi.PublisherApi* | [**publisherNameGet**](docs/PublisherApi.md#publisherNameGet) | **GET** /publisher/{name} | Gets publisher details
*IsbNdbApi.PublisherApi* | [**publishersQueryGet**](docs/PublisherApi.md#publishersQueryGet) | **GET** /publishers/{query} | Search publishers
*IsbNdbApi.SearchApi* | [**searchGet**](docs/SearchApi.md#searchGet) | **GET** /search | Search all ISBNDB databases
*IsbNdbApi.StatsApi* | [**statsGet**](docs/StatsApi.md#statsGet) | **GET** /stats | Gets status on the ISBNDB Database
*IsbNdbApi.SubjectApi* | [**subjectNameGet**](docs/SubjectApi.md#subjectNameGet) | **GET** /subject/{name} | Gets subject details
*IsbNdbApi.SubjectApi* | [**subjectsQueryGet**](docs/SubjectApi.md#subjectsQueryGet) | **GET** /subjects/{query} | Search subjects


## Documentation for Models

 - [IsbNdbApi.Author](docs/Author.md)
 - [IsbNdbApi.AuthorQueryResults](docs/AuthorQueryResults.md)
 - [IsbNdbApi.Book](docs/Book.md)
 - [IsbNdbApi.Publisher](docs/Publisher.md)
 - [IsbNdbApi.PublisherBooksInner](docs/PublisherBooksInner.md)
 - [IsbNdbApi.Subject](docs/Subject.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### api_key


- **Type**: API key
- **API key parameter name**: x-api-key
- **Location**: HTTP header

