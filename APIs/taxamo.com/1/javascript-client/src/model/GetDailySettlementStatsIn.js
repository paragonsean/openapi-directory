/**
 * Taxamo
 * Taxamo’s elegant suite of APIs and comprehensive reporting dashboard enables digital merchants to easily comply with EU regulatory requirements on tax calculation, evidence collection, tax return creation and data storage.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetDailySettlementStatsIn model module.
 * @module model/GetDailySettlementStatsIn
 * @version 1
 */
class GetDailySettlementStatsIn {
    /**
     * Constructs a new <code>GetDailySettlementStatsIn</code>.
     * @alias module:model/GetDailySettlementStatsIn
     * @param dateFrom {String} Date from in yyyy-MM format.
     * @param dateTo {String} Date to in yyyy-MM format.
     * @param interval {String} Interval type - day, week, month.
     */
    constructor(dateFrom, dateTo, interval) { 
        
        GetDailySettlementStatsIn.initialize(this, dateFrom, dateTo, interval);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dateFrom, dateTo, interval) { 
        obj['date_from'] = dateFrom;
        obj['date_to'] = dateTo;
        obj['interval'] = interval;
    }

    /**
     * Constructs a <code>GetDailySettlementStatsIn</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetDailySettlementStatsIn} obj Optional instance to populate.
     * @return {module:model/GetDailySettlementStatsIn} The populated <code>GetDailySettlementStatsIn</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetDailySettlementStatsIn();

            if (data.hasOwnProperty('date_from')) {
                obj['date_from'] = ApiClient.convertToType(data['date_from'], 'String');
            }
            if (data.hasOwnProperty('date_to')) {
                obj['date_to'] = ApiClient.convertToType(data['date_to'], 'String');
            }
            if (data.hasOwnProperty('interval')) {
                obj['interval'] = ApiClient.convertToType(data['interval'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetDailySettlementStatsIn</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetDailySettlementStatsIn</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetDailySettlementStatsIn.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['date_from'] && !(typeof data['date_from'] === 'string' || data['date_from'] instanceof String)) {
            throw new Error("Expected the field `date_from` to be a primitive type in the JSON string but got " + data['date_from']);
        }
        // ensure the json data is a string
        if (data['date_to'] && !(typeof data['date_to'] === 'string' || data['date_to'] instanceof String)) {
            throw new Error("Expected the field `date_to` to be a primitive type in the JSON string but got " + data['date_to']);
        }
        // ensure the json data is a string
        if (data['interval'] && !(typeof data['interval'] === 'string' || data['interval'] instanceof String)) {
            throw new Error("Expected the field `interval` to be a primitive type in the JSON string but got " + data['interval']);
        }

        return true;
    }


}

GetDailySettlementStatsIn.RequiredProperties = ["date_from", "date_to", "interval"];

/**
 * Date from in yyyy-MM format.
 * @member {String} date_from
 */
GetDailySettlementStatsIn.prototype['date_from'] = undefined;

/**
 * Date to in yyyy-MM format.
 * @member {String} date_to
 */
GetDailySettlementStatsIn.prototype['date_to'] = undefined;

/**
 * Interval type - day, week, month.
 * @member {String} interval
 */
GetDailySettlementStatsIn.prototype['interval'] = undefined;






export default GetDailySettlementStatsIn;

