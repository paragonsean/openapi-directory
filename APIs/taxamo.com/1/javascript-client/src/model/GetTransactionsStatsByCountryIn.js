/**
 * Taxamo
 * Taxamo’s elegant suite of APIs and comprehensive reporting dashboard enables digital merchants to easily comply with EU regulatory requirements on tax calculation, evidence collection, tax return creation and data storage.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetTransactionsStatsByCountryIn model module.
 * @module model/GetTransactionsStatsByCountryIn
 * @version 1
 */
class GetTransactionsStatsByCountryIn {
    /**
     * Constructs a new <code>GetTransactionsStatsByCountryIn</code>.
     * @alias module:model/GetTransactionsStatsByCountryIn
     * @param dateFrom {String} Date from in yyyy-MM format.
     * @param dateTo {String} Date to in yyyy-MM format.
     */
    constructor(dateFrom, dateTo) { 
        
        GetTransactionsStatsByCountryIn.initialize(this, dateFrom, dateTo);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dateFrom, dateTo) { 
        obj['date_from'] = dateFrom;
        obj['date_to'] = dateTo;
    }

    /**
     * Constructs a <code>GetTransactionsStatsByCountryIn</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetTransactionsStatsByCountryIn} obj Optional instance to populate.
     * @return {module:model/GetTransactionsStatsByCountryIn} The populated <code>GetTransactionsStatsByCountryIn</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetTransactionsStatsByCountryIn();

            if (data.hasOwnProperty('date_from')) {
                obj['date_from'] = ApiClient.convertToType(data['date_from'], 'String');
            }
            if (data.hasOwnProperty('date_to')) {
                obj['date_to'] = ApiClient.convertToType(data['date_to'], 'String');
            }
            if (data.hasOwnProperty('global_currency_code')) {
                obj['global_currency_code'] = ApiClient.convertToType(data['global_currency_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetTransactionsStatsByCountryIn</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetTransactionsStatsByCountryIn</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetTransactionsStatsByCountryIn.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['date_from'] && !(typeof data['date_from'] === 'string' || data['date_from'] instanceof String)) {
            throw new Error("Expected the field `date_from` to be a primitive type in the JSON string but got " + data['date_from']);
        }
        // ensure the json data is a string
        if (data['date_to'] && !(typeof data['date_to'] === 'string' || data['date_to'] instanceof String)) {
            throw new Error("Expected the field `date_to` to be a primitive type in the JSON string but got " + data['date_to']);
        }
        // ensure the json data is a string
        if (data['global_currency_code'] && !(typeof data['global_currency_code'] === 'string' || data['global_currency_code'] instanceof String)) {
            throw new Error("Expected the field `global_currency_code` to be a primitive type in the JSON string but got " + data['global_currency_code']);
        }

        return true;
    }


}

GetTransactionsStatsByCountryIn.RequiredProperties = ["date_from", "date_to"];

/**
 * Date from in yyyy-MM format.
 * @member {String} date_from
 */
GetTransactionsStatsByCountryIn.prototype['date_from'] = undefined;

/**
 * Date to in yyyy-MM format.
 * @member {String} date_to
 */
GetTransactionsStatsByCountryIn.prototype['date_to'] = undefined;

/**
 * Global currency code to use for conversion - in addition to country's currency if rate is available. Conversion is indicative and based on most-recent rate from ECB.
 * @member {String} global_currency_code
 */
GetTransactionsStatsByCountryIn.prototype['global_currency_code'] = undefined;






export default GetTransactionsStatsByCountryIn;

