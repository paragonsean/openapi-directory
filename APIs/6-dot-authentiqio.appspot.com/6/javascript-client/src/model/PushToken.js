/**
 * Authentiq API
 * Strong authentication, without the passwords.
 *
 * The version of the OpenAPI document: 6
 * Contact: hello@authentiq.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PushToken model module.
 * @module model/PushToken
 * @version 6
 */
class PushToken {
    /**
     * Constructs a new <code>PushToken</code>.
     * PushToken in JWT format, self-signed.  
     * @alias module:model/PushToken
     * @param aud {String} audience (URI)
     * @param iss {String} issuer (URI)
     * @param sub {String} UUID and public signing key
     */
    constructor(aud, iss, sub) { 
        
        PushToken.initialize(this, aud, iss, sub);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, aud, iss, sub) { 
        obj['aud'] = aud;
        obj['iss'] = iss;
        obj['sub'] = sub;
    }

    /**
     * Constructs a <code>PushToken</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PushToken} obj Optional instance to populate.
     * @return {module:model/PushToken} The populated <code>PushToken</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PushToken();

            if (data.hasOwnProperty('aud')) {
                obj['aud'] = ApiClient.convertToType(data['aud'], 'String');
            }
            if (data.hasOwnProperty('exp')) {
                obj['exp'] = ApiClient.convertToType(data['exp'], 'Number');
            }
            if (data.hasOwnProperty('iat')) {
                obj['iat'] = ApiClient.convertToType(data['iat'], 'Number');
            }
            if (data.hasOwnProperty('iss')) {
                obj['iss'] = ApiClient.convertToType(data['iss'], 'String');
            }
            if (data.hasOwnProperty('nbf')) {
                obj['nbf'] = ApiClient.convertToType(data['nbf'], 'Number');
            }
            if (data.hasOwnProperty('sub')) {
                obj['sub'] = ApiClient.convertToType(data['sub'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PushToken</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PushToken</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PushToken.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['aud'] && !(typeof data['aud'] === 'string' || data['aud'] instanceof String)) {
            throw new Error("Expected the field `aud` to be a primitive type in the JSON string but got " + data['aud']);
        }
        // ensure the json data is a string
        if (data['iss'] && !(typeof data['iss'] === 'string' || data['iss'] instanceof String)) {
            throw new Error("Expected the field `iss` to be a primitive type in the JSON string but got " + data['iss']);
        }
        // ensure the json data is a string
        if (data['sub'] && !(typeof data['sub'] === 'string' || data['sub'] instanceof String)) {
            throw new Error("Expected the field `sub` to be a primitive type in the JSON string but got " + data['sub']);
        }

        return true;
    }


}

PushToken.RequiredProperties = ["aud", "iss", "sub"];

/**
 * audience (URI)
 * @member {String} aud
 */
PushToken.prototype['aud'] = undefined;

/**
 * @member {Number} exp
 */
PushToken.prototype['exp'] = undefined;

/**
 * @member {Number} iat
 */
PushToken.prototype['iat'] = undefined;

/**
 * issuer (URI)
 * @member {String} iss
 */
PushToken.prototype['iss'] = undefined;

/**
 * @member {Number} nbf
 */
PushToken.prototype['nbf'] = undefined;

/**
 * UUID and public signing key
 * @member {String} sub
 */
PushToken.prototype['sub'] = undefined;






export default PushToken;

