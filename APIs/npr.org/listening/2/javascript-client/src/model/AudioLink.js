/**
 * NPR Listening Service
 * Audio recommendations tailored to a user's preferences
 *
 * The version of the OpenAPI document: 2
 * Contact: NPROneEnterprise@npr.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AbstractLink from './AbstractLink';

/**
 * The AudioLink model module.
 * @module model/AudioLink
 * @version 2
 */
class AudioLink {
    /**
     * Constructs a new <code>AudioLink</code>.
     * A link to audio files for the item
     * @alias module:model/AudioLink
     * @implements module:model/AbstractLink
     * @param href {String} The link to be followed
     * @param contentType {module:model/AudioLink.ContentTypeEnum} The MIME type of the response of this link; note that the enumerated list of possible values is not exhaustive and other MIME types could occur. The list should be treated as examples, rather than absolutes.
     */
    constructor(href, contentType) { 
        AbstractLink.initialize(this, href);
        AudioLink.initialize(this, href, contentType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, href, contentType) { 
        obj['href'] = href;
        obj['content-type'] = contentType || 'audio/aac';
    }

    /**
     * Constructs a <code>AudioLink</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AudioLink} obj Optional instance to populate.
     * @return {module:model/AudioLink} The populated <code>AudioLink</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AudioLink();
            AbstractLink.constructFromObject(data, obj);

            if (data.hasOwnProperty('href')) {
                obj['href'] = ApiClient.convertToType(data['href'], 'String');
            }
            if (data.hasOwnProperty('content-type')) {
                obj['content-type'] = ApiClient.convertToType(data['content-type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AudioLink</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AudioLink</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AudioLink.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['href'] && !(typeof data['href'] === 'string' || data['href'] instanceof String)) {
            throw new Error("Expected the field `href` to be a primitive type in the JSON string but got " + data['href']);
        }
        // ensure the json data is a string
        if (data['content-type'] && !(typeof data['content-type'] === 'string' || data['content-type'] instanceof String)) {
            throw new Error("Expected the field `content-type` to be a primitive type in the JSON string but got " + data['content-type']);
        }

        return true;
    }


}

AudioLink.RequiredProperties = ["href", "content-type"];

/**
 * The link to be followed
 * @member {String} href
 */
AudioLink.prototype['href'] = undefined;

/**
 * The MIME type of the response of this link; note that the enumerated list of possible values is not exhaustive and other MIME types could occur. The list should be treated as examples, rather than absolutes.
 * @member {module:model/AudioLink.ContentTypeEnum} content-type
 * @default 'audio/aac'
 */
AudioLink.prototype['content-type'] = 'audio/aac';


// Implement AbstractLink interface:
/**
 * The link to be followed
 * @member {String} href
 */
AbstractLink.prototype['href'] = undefined;



/**
 * Allowed values for the <code>content-type</code> property.
 * @enum {String}
 * @readonly
 */
AudioLink['ContentTypeEnum'] = {

    /**
     * value: "audio/mp3"
     * @const
     */
    "audio/mp3": "audio/mp3",

    /**
     * value: "audio/aac"
     * @const
     */
    "audio/aac": "audio/aac",

    /**
     * value: "audio/3gp"
     * @const
     */
    "audio/3gp": "audio/3gp",

    /**
     * value: "application/vnd.apple.mpegurl"
     * @const
     */
    "application/vnd.apple.mpegurl": "application/vnd.apple.mpegurl",

    /**
     * value: "audio/x-ms-wax"
     * @const
     */
    "audio/x-ms-wax": "audio/x-ms-wax"
};



export default AudioLink;

