/**
 * NPR Sponsorship Service
 * Sponsorship for non-NPR One client applications
 *
 * The version of the OpenAPI document: 2
 * Contact: NPROneEnterprise@npr.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AdTrackingDocument from './AdTrackingDocument';
import CollectionDocument from './CollectionDocument';
import UserAdData from './UserAdData';

/**
 * The UserAdDocument model module.
 * @module model/UserAdDocument
 * @version 2
 */
class UserAdDocument {
    /**
     * Constructs a new <code>UserAdDocument</code>.
     * All information relevant to a user who has requested sponsorship or submitted tracking information
     * @alias module:model/UserAdDocument
     * @implements module:model/CollectionDocument
     * @param attributes {module:model/UserAdData} 
     * @param errors {Array.<Object>} A list of encountered errors, ignored on POST, PUT
     * @param href {String} A URL representation of the resource; should generally be ignored by clients unless noted otherwise
     * @param items {Array.<module:model/AdTrackingDocument>} All the information needed for the Sponsorship Service to send back tracking information to our external sponsorship provider
     * @param links {Object} 
     * @param version {String} The version of the Collection.Doc+JSON spec being used
     */
    constructor(attributes, errors, href, items, links, version) { 
        CollectionDocument.initialize(this, attributes, errors, href, items, links, version);
        UserAdDocument.initialize(this, attributes, errors, href, items, links, version);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, attributes, errors, href, items, links, version) { 
        obj['attributes'] = attributes;
        obj['errors'] = errors;
        obj['href'] = href;
        obj['items'] = items;
        obj['links'] = links;
        obj['version'] = version || '1.0';
    }

    /**
     * Constructs a <code>UserAdDocument</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserAdDocument} obj Optional instance to populate.
     * @return {module:model/UserAdDocument} The populated <code>UserAdDocument</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserAdDocument();
            CollectionDocument.constructFromObject(data, obj);

            if (data.hasOwnProperty('attributes')) {
                obj['attributes'] = UserAdData.constructFromObject(data['attributes']);
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [Object]);
            }
            if (data.hasOwnProperty('href')) {
                obj['href'] = ApiClient.convertToType(data['href'], 'String');
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [AdTrackingDocument]);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = ApiClient.convertToType(data['links'], Object);
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserAdDocument</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserAdDocument</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserAdDocument.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `attributes`
        if (data['attributes']) { // data not null
          UserAdData.validateJSON(data['attributes']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['errors'])) {
            throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
        }
        // ensure the json data is a string
        if (data['href'] && !(typeof data['href'] === 'string' || data['href'] instanceof String)) {
            throw new Error("Expected the field `href` to be a primitive type in the JSON string but got " + data['href']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                AdTrackingDocument.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }

        return true;
    }


}

UserAdDocument.RequiredProperties = ["attributes", "errors", "href", "items", "links", "version"];

/**
 * @member {module:model/UserAdData} attributes
 */
UserAdDocument.prototype['attributes'] = undefined;

/**
 * A list of encountered errors, ignored on POST, PUT
 * @member {Array.<Object>} errors
 */
UserAdDocument.prototype['errors'] = undefined;

/**
 * A URL representation of the resource; should generally be ignored by clients unless noted otherwise
 * @member {String} href
 */
UserAdDocument.prototype['href'] = undefined;

/**
 * All the information needed for the Sponsorship Service to send back tracking information to our external sponsorship provider
 * @member {Array.<module:model/AdTrackingDocument>} items
 */
UserAdDocument.prototype['items'] = undefined;

/**
 * @member {Object} links
 */
UserAdDocument.prototype['links'] = undefined;

/**
 * The version of the Collection.Doc+JSON spec being used
 * @member {String} version
 * @default '1.0'
 */
UserAdDocument.prototype['version'] = '1.0';


// Implement CollectionDocument interface:
/**
 * @member {Object} attributes
 */
CollectionDocument.prototype['attributes'] = undefined;
/**
 * A list of encountered errors, ignored on POST, PUT
 * @member {Array.<Object>} errors
 */
CollectionDocument.prototype['errors'] = undefined;
/**
 * A URL representation of the resource; should generally be ignored by clients unless noted otherwise
 * @member {String} href
 */
CollectionDocument.prototype['href'] = undefined;
/**
 * @member {Array.<Object>} items
 */
CollectionDocument.prototype['items'] = undefined;
/**
 * @member {Object} links
 */
CollectionDocument.prototype['links'] = undefined;
/**
 * The version of the Collection.Doc+JSON spec being used
 * @member {String} version
 * @default '1.0'
 */
CollectionDocument.prototype['version'] = '1.0';




export default UserAdDocument;

