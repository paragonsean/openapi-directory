/**
 * NPR Authorization Service
 * The interface to our OAuth2 server
 *
 * The version of the OpenAPI document: 2
 * Contact: NPROneEnterprise@npr.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccessTokenData model module.
 * @module model/AccessTokenData
 * @version 2
 */
class AccessTokenData {
    /**
     * Constructs a new <code>AccessTokenData</code>.
     * @alias module:model/AccessTokenData
     * @param accessToken {String} The access token to use for all future calls
     * @param expiresIn {Number} The remaining lifetime of the access token (in seconds)
     * @param tokenType {module:model/AccessTokenData.TokenTypeEnum} Identifies the type of token returned. At this time, this field always has the value `Bearer`.
     */
    constructor(accessToken, expiresIn, tokenType) { 
        
        AccessTokenData.initialize(this, accessToken, expiresIn, tokenType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accessToken, expiresIn, tokenType) { 
        obj['access_token'] = accessToken;
        obj['expires_in'] = expiresIn;
        obj['token_type'] = tokenType;
    }

    /**
     * Constructs a <code>AccessTokenData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccessTokenData} obj Optional instance to populate.
     * @return {module:model/AccessTokenData} The populated <code>AccessTokenData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccessTokenData();

            if (data.hasOwnProperty('access_token')) {
                obj['access_token'] = ApiClient.convertToType(data['access_token'], 'String');
            }
            if (data.hasOwnProperty('expires_in')) {
                obj['expires_in'] = ApiClient.convertToType(data['expires_in'], 'Number');
            }
            if (data.hasOwnProperty('refresh_token')) {
                obj['refresh_token'] = ApiClient.convertToType(data['refresh_token'], 'String');
            }
            if (data.hasOwnProperty('token_type')) {
                obj['token_type'] = ApiClient.convertToType(data['token_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccessTokenData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccessTokenData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AccessTokenData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['access_token'] && !(typeof data['access_token'] === 'string' || data['access_token'] instanceof String)) {
            throw new Error("Expected the field `access_token` to be a primitive type in the JSON string but got " + data['access_token']);
        }
        // ensure the json data is a string
        if (data['refresh_token'] && !(typeof data['refresh_token'] === 'string' || data['refresh_token'] instanceof String)) {
            throw new Error("Expected the field `refresh_token` to be a primitive type in the JSON string but got " + data['refresh_token']);
        }
        // ensure the json data is a string
        if (data['token_type'] && !(typeof data['token_type'] === 'string' || data['token_type'] instanceof String)) {
            throw new Error("Expected the field `token_type` to be a primitive type in the JSON string but got " + data['token_type']);
        }

        return true;
    }


}

AccessTokenData.RequiredProperties = ["access_token", "expires_in", "token_type"];

/**
 * The access token to use for all future calls
 * @member {String} access_token
 */
AccessTokenData.prototype['access_token'] = undefined;

/**
 * The remaining lifetime of the access token (in seconds)
 * @member {Number} expires_in
 */
AccessTokenData.prototype['expires_in'] = undefined;

/**
 * The refresh token that can be used to obtain a new access token if the old one expires; if a refresh token is returned, it is the client's responsibility to securely cache it for future use.
 * @member {String} refresh_token
 */
AccessTokenData.prototype['refresh_token'] = undefined;

/**
 * Identifies the type of token returned. At this time, this field always has the value `Bearer`.
 * @member {module:model/AccessTokenData.TokenTypeEnum} token_type
 */
AccessTokenData.prototype['token_type'] = undefined;





/**
 * Allowed values for the <code>token_type</code> property.
 * @enum {String}
 * @readonly
 */
AccessTokenData['TokenTypeEnum'] = {

    /**
     * value: "Bearer"
     * @const
     */
    "Bearer": "Bearer",

    /**
     * value: "MAC"
     * @const
     */
    "MAC": "MAC"
};



export default AccessTokenData;

