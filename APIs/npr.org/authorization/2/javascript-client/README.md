# npr_authorization_service

NprAuthorizationService - JavaScript client for npr_authorization_service
The interface to our OAuth2 server
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2
- Package version: 2
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://dev.npr.org](https://dev.npr.org)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install npr_authorization_service --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your npr_authorization_service from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var NprAuthorizationService = require('npr_authorization_service');


var api = new NprAuthorizationService.AuthorizationApi()
var grantType = "grantType_example"; // {String} The type of grant the client is requesting
var clientId = "clientId_example"; // {String} The client's ID, required for all grant types.
var clientSecret = "clientSecret_example"; // {String} The client's secret, required for all grant types.
var opts = {
  'code': "code_example", // {String} Required for `authorization_code` and `device_code` grant types. The authorization code from a successful call to `/v2/authorize`, or a device code from a successful call to `/v2/device`.
  'redirectUri': "redirectUri_example", // {String} Required for `authorization_code` grant type. The requested redirect_uri.
  'username': "username_example", // {String} Required for `password` grant type. The email address of an NPR user.
  'password': "password_example", // {String} Required for `password` grant type. The password that matches the user specified with the username parameter.
  'service': "service_example", // {String} Required for `third_party` grant type. The name of the third-party login provider.
  'refreshToken': "refreshToken_example", // {String} Required for `refresh_token` grant type. A valid refresh token from a previous successful call to `POST /v2/token`.
  'scope': "scope_example", // {String} Required for third-party developers using the `device_code` and `third_party` grant types. Optionally used by the `refresh_token` grant type. A space-separated list of scope(s) requested by the application.
  'tokenTypeHint': "tokenTypeHint_example" // {String} A hint about the type of the token submitted for a new access and refresh token. If unspecified, the default value is assumed to be `refresh_token`.
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.createToken(grantType, clientId, clientSecret, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://authorization.api.npr.org*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*NprAuthorizationService.AuthorizationApi* | [**createToken**](docs/AuthorizationApi.md#createToken) | **POST** /v2/token | Create a new OAuth2 access token
*NprAuthorizationService.AuthorizationApi* | [**generateDeviceCode**](docs/AuthorizationApi.md#generateDeviceCode) | **POST** /v2/device | Initiate an OAuth2 login flow for limited input devices
*NprAuthorizationService.AuthorizationApi* | [**revokeToken**](docs/AuthorizationApi.md#revokeToken) | **POST** /v2/token/revoke | Revoke an existing OAuth2 access token


## Documentation for Models

 - [NprAuthorizationService.AbstractCDocLink](docs/AbstractCDocLink.md)
 - [NprAuthorizationService.AbstractLink](docs/AbstractLink.md)
 - [NprAuthorizationService.AccessTokenData](docs/AccessTokenData.md)
 - [NprAuthorizationService.Affiliation](docs/Affiliation.md)
 - [NprAuthorizationService.CollectionDocument](docs/CollectionDocument.md)
 - [NprAuthorizationService.DeviceCodeData](docs/DeviceCodeData.md)
 - [NprAuthorizationService.Error](docs/Error.md)
 - [NprAuthorizationService.ErrorDocument](docs/ErrorDocument.md)
 - [NprAuthorizationService.SimpleError](docs/SimpleError.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### oauth2


- **Type**: OAuth
- **Flow**: accessCode
- **Authorization URL**: https://authorization.api.npr.org/v2/authorize
- **Scopes**: 
  - identity.readonly: See your personal information, such as your first name, last name, and favorite station.
  - identity.write: Update your personal information, such as your favorite station(s) or program(s) you follow, on your behalf.
  - listening.readonly: See your NPR One listening history and get audio recommendations.
  - listening.write: Record that you have heard, marked as interesting, and/or skipped NPR One stories in order to personalize future audio recommendations.
  - localactivation: Connect you with your local NPR member station for communication purposes.

