/**
 * Turbine Labs API
 * The Turbine Labs API provides CRUD operations for core object types, and is mostly RESTy. The easiest way to interact with the API is with [tbnctl](https://docs.turbinelabs.io/advanced/tbnctl.html). If you want to make direct HTTP calls, however, you can obtain an access token using tbnctl, and then pass it in the Authorization header, prefixed by `Token `: ```console curl -H \"Authorization: Token <access token>\" https://api.turbinelabs.io/v1.0/cluster ``` 
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OutlierDetection model module.
 * @module model/OutlierDetection
 * @version 1.0
 */
class OutlierDetection {
    /**
     * Constructs a new <code>OutlierDetection</code>.
     * A form of passive health checking that dynamically determines whether instances in a cluster are performing unlike others and preemptively removes them from a load balancing set. 
     * @alias module:model/OutlierDetection
     */
    constructor() { 
        
        OutlierDetection.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OutlierDetection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OutlierDetection} obj Optional instance to populate.
     * @return {module:model/OutlierDetection} The populated <code>OutlierDetection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OutlierDetection();

            if (data.hasOwnProperty('base_ejection_time_msec')) {
                obj['base_ejection_time_msec'] = ApiClient.convertToType(data['base_ejection_time_msec'], 'Number');
            }
            if (data.hasOwnProperty('consecutive_5xx')) {
                obj['consecutive_5xx'] = ApiClient.convertToType(data['consecutive_5xx'], 'Number');
            }
            if (data.hasOwnProperty('consecutive_gateway_failure')) {
                obj['consecutive_gateway_failure'] = ApiClient.convertToType(data['consecutive_gateway_failure'], 'Number');
            }
            if (data.hasOwnProperty('enforcing_consecutive_5xx')) {
                obj['enforcing_consecutive_5xx'] = ApiClient.convertToType(data['enforcing_consecutive_5xx'], 'Number');
            }
            if (data.hasOwnProperty('enforcing_consecutive_gateway_failure')) {
                obj['enforcing_consecutive_gateway_failure'] = ApiClient.convertToType(data['enforcing_consecutive_gateway_failure'], 'Number');
            }
            if (data.hasOwnProperty('enforcing_success_rate')) {
                obj['enforcing_success_rate'] = ApiClient.convertToType(data['enforcing_success_rate'], 'Number');
            }
            if (data.hasOwnProperty('interval_msec')) {
                obj['interval_msec'] = ApiClient.convertToType(data['interval_msec'], 'Number');
            }
            if (data.hasOwnProperty('max_ejection_percent')) {
                obj['max_ejection_percent'] = ApiClient.convertToType(data['max_ejection_percent'], 'Number');
            }
            if (data.hasOwnProperty('success_rate_minimum_hosts')) {
                obj['success_rate_minimum_hosts'] = ApiClient.convertToType(data['success_rate_minimum_hosts'], 'Number');
            }
            if (data.hasOwnProperty('success_rate_request_volume')) {
                obj['success_rate_request_volume'] = ApiClient.convertToType(data['success_rate_request_volume'], 'Number');
            }
            if (data.hasOwnProperty('success_rate_stdev_factor')) {
                obj['success_rate_stdev_factor'] = ApiClient.convertToType(data['success_rate_stdev_factor'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OutlierDetection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OutlierDetection</code>.
     */
    static validateJSON(data) {

        return true;
    }


}



/**
 * The base time that a host is ejected for. The real time is equal to the base time multiplied by the number of times the host has been ejected. Defaults to 30s. Setting this to 0 means that no host will be ejected for longer than `interval_msec`. 
 * @member {Number} base_ejection_time_msec
 */
OutlierDetection.prototype['base_ejection_time_msec'] = undefined;

/**
 * The number of consecutive 5xx responses before a consecutive 5xx ejection occurs. Defaults to 5. Setting this to 0 effectively turns off the consecutive 5xx detector. 
 * @member {Number} consecutive_5xx
 */
OutlierDetection.prototype['consecutive_5xx'] = undefined;

/**
 * The number of consecutive gateway failures (502, 503, 504 status or connection errors that are mapped to one of those status codes) before a consecutive gateway failure ejection occurs. Defaults to 5. Setting this to 0 effectively turns off the consecutive gateway failure detector. 
 * @member {Number} consecutive_gateway_failure
 */
OutlierDetection.prototype['consecutive_gateway_failure'] = undefined;

/**
 * The % chance that a host will be actually ejected when an outlier status is detected through consecutive 5xx. This setting can be used to disable ejection or to ramp it up slowly. Defaults to 100. 
 * @member {Number} enforcing_consecutive_5xx
 */
OutlierDetection.prototype['enforcing_consecutive_5xx'] = undefined;

/**
 * The % chance that a host will be actually ejected when an outlier status is detected through consecutive gateway failures. This setting can be used to disable ejection or to ramp it up slowly. Defaults to 0. 
 * @member {Number} enforcing_consecutive_gateway_failure
 */
OutlierDetection.prototype['enforcing_consecutive_gateway_failure'] = undefined;

/**
 * The % chance that a host will be actually ejected when an outlier status is detected through success rate statistics. This setting can be used to disable ejection or to ramp it up slowly. Defaults to 100. 
 * @member {Number} enforcing_success_rate
 */
OutlierDetection.prototype['enforcing_success_rate'] = undefined;

/**
 * The time interval between ejection analysis sweeps. This can result in both new ejections due to success rate outlier detection as well as hosts being returned to service. Defaults to 10s and must be greater than 0. 
 * @member {Number} interval_msec
 */
OutlierDetection.prototype['interval_msec'] = undefined;

/**
 * The maximum % of an upstream cluster that can be ejected due to outlier detection. Defaults to 10% but will always eject at least one host. 
 * @member {Number} max_ejection_percent
 */
OutlierDetection.prototype['max_ejection_percent'] = undefined;

/**
 * The number of hosts in a cluster that must have enough request volume to detect success rate outliers. If the number of hosts is less than this setting, outlier detection via success rate statistics is not performed for any host in the cluster. Defaults to 5. Setting this to 0 effectively triggers the success rate detector regardless of the number of valid hosts during an interval (as determined by `success_rate_request_volume`). 
 * @member {Number} success_rate_minimum_hosts
 */
OutlierDetection.prototype['success_rate_minimum_hosts'] = undefined;

/**
 * The minimum number of total requests that must be collected in one interval (as defined by interval_msec) to include this host in success rate based outlier detection. If the volume is lower than this setting, outlier detection via success rate statistics is not performed for that host. Defaults to 100. Must be greater than 0. 
 * @member {Number} success_rate_request_volume
 */
OutlierDetection.prototype['success_rate_request_volume'] = undefined;

/**
 * This factor is used to determine the ejection threshold for success rate outlier ejection. The ejection threshold is the difference between the mean success rate, and the product of this factor and the standard deviation of the mean success rate: mean - (stdev * success_rate_stdev_factor). This factor is divided by a thousand to get a double. That is, if the desired factor is 1.9, the runtime value should be 1900. Defaults to 1900. Setting this to 0 effectively turns off the success rate detector. 
 * @member {Number} success_rate_stdev_factor
 */
OutlierDetection.prototype['success_rate_stdev_factor'] = undefined;






export default OutlierDetection;

