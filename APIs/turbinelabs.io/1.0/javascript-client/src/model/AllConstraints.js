/**
 * Turbine Labs API
 * The Turbine Labs API provides CRUD operations for core object types, and is mostly RESTy. The easiest way to interact with the API is with [tbnctl](https://docs.turbinelabs.io/advanced/tbnctl.html). If you want to make direct HTTP calls, however, you can obtain an access token using tbnctl, and then pass it in the Authorization header, prefixed by `Token `: ```console curl -H \"Authorization: Token <access token>\" https://api.turbinelabs.io/v1.0/cluster ``` 
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ClusterConstraint from './ClusterConstraint';

/**
 * The AllConstraints model module.
 * @module model/AllConstraints
 * @version 1.0
 */
class AllConstraints {
    /**
     * Constructs a new <code>AllConstraints</code>.
     * @alias module:model/AllConstraints
     * @param light {Array.<module:model/ClusterConstraint>} 
     */
    constructor(light) { 
        
        AllConstraints.initialize(this, light);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, light) { 
        obj['light'] = light;
    }

    /**
     * Constructs a <code>AllConstraints</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AllConstraints} obj Optional instance to populate.
     * @return {module:model/AllConstraints} The populated <code>AllConstraints</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AllConstraints();

            if (data.hasOwnProperty('dark')) {
                obj['dark'] = ApiClient.convertToType(data['dark'], [ClusterConstraint]);
            }
            if (data.hasOwnProperty('light')) {
                obj['light'] = ApiClient.convertToType(data['light'], [ClusterConstraint]);
            }
            if (data.hasOwnProperty('tap')) {
                obj['tap'] = ApiClient.convertToType(data['tap'], [ClusterConstraint]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AllConstraints</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AllConstraints</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AllConstraints.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['dark']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dark'])) {
                throw new Error("Expected the field `dark` to be an array in the JSON data but got " + data['dark']);
            }
            // validate the optional field `dark` (array)
            for (const item of data['dark']) {
                ClusterConstraint.validateJSON(item);
            };
        }
        if (data['light']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['light'])) {
                throw new Error("Expected the field `light` to be an array in the JSON data but got " + data['light']);
            }
            // validate the optional field `light` (array)
            for (const item of data['light']) {
                ClusterConstraint.validateJSON(item);
            };
        }
        if (data['tap']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tap'])) {
                throw new Error("Expected the field `tap` to be an array in the JSON data but got " + data['tap']);
            }
            // validate the optional field `tap` (array)
            for (const item of data['tap']) {
                ClusterConstraint.validateJSON(item);
            };
        }

        return true;
    }


}

AllConstraints.RequiredProperties = ["light"];

/**
 * @member {Array.<module:model/ClusterConstraint>} dark
 */
AllConstraints.prototype['dark'] = undefined;

/**
 * @member {Array.<module:model/ClusterConstraint>} light
 */
AllConstraints.prototype['light'] = undefined;

/**
 * @member {Array.<module:model/ClusterConstraint>} tap
 */
AllConstraints.prototype['tap'] = undefined;






export default AllConstraints;

