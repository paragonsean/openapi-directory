openapi: 3.0.1
info:
  description: A simple API to get the current time based on a request with a timezone.
  title: World Time API
  version: "20210108"
  x-apisguru-categories:
  - location
  x-origin:
  - format: openapi
    url: http://worldtimeapi.org/api
    version: "3.0"
  x-providerName: worldtimeapi.org
servers:
- url: http://worldtimeapi.org/api/
paths:
  /ip:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DateTimeJsonResponse'
          description: the current time for the timezone requested in JSON format
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorJsonResponse'
          description: an error response in JSON format
      summary: "request the current time based on the ip of the request. note: this\
        \ is a \"best guess\" obtained from open-source data."
      x-accepts:
      - application/json
  /ip.txt:
    get:
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/DateTimeTextResponse'
          description: the current time for the timezone requested in plain text
        default:
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ErrorTextResponse'
          description: an error response in plain text
      summary: "request the current time based on the ip of the request. note: this\
        \ is a \"best guess\" obtained from open-source data."
      x-accepts:
      - text/plain
  /ip/{ipv4}:
    get:
      parameters:
      - explode: false
        in: path
        name: ipv4
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DateTimeJsonResponse'
          description: the current time for the timezone requested in JSON format
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorJsonResponse'
          description: an error response in JSON format
      summary: "request the current time based on the ip of the request. note: this\
        \ is a \"best guess\" obtained from open-source data."
      x-accepts:
      - application/json
  /ip/{ipv4}.txt:
    get:
      parameters:
      - explode: false
        in: path
        name: ipv4
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/DateTimeTextResponse'
          description: the current time for the timezone requested in plain text
        default:
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ErrorTextResponse'
          description: an error response in plain text
      summary: "request the current time based on the ip of the request. note: this\
        \ is a \"best guess\" obtained from open-source data."
      x-accepts:
      - text/plain
  /timezone:
    get:
      responses:
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListJsonResponse'
          description: the list of available timezones in JSON format
      summary: a listing of all timezones.
      x-accepts:
      - application/json
  /timezone.txt:
    get:
      responses:
        default:
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ListTextResponse'
          description: the list of available timezones in plain text
      summary: a listing of all timezones.
      x-accepts:
      - text/plain
  /timezone/{area}:
    get:
      parameters:
      - explode: false
        in: path
        name: area
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListJsonResponse'
          description: the list of available timezones in JSON format
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorJsonResponse'
          description: an error response in JSON format
      summary: a listing of all timezones available for that area.
      x-accepts:
      - application/json
  /timezone/{area}.txt:
    get:
      parameters:
      - explode: false
        in: path
        name: area
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ListTextResponse'
          description: the list of available timezones in plain text
        default:
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ErrorTextResponse'
          description: an error response in plain text
      summary: a listing of all timezones available for that area.
      x-accepts:
      - text/plain
  /timezone/{area}/{location}:
    get:
      parameters:
      - explode: false
        in: path
        name: area
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: path
        name: location
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DateTimeJsonResponse'
          description: the current time for the timezone requested in JSON format
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorJsonResponse'
          description: an error response in JSON format
      summary: request the current time for a timezone.
      x-accepts:
      - application/json
  /timezone/{area}/{location}.txt:
    get:
      parameters:
      - explode: false
        in: path
        name: area
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: path
        name: location
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/DateTimeTextResponse'
          description: the current time for the timezone requested in plain text
        default:
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ErrorTextResponse'
          description: an error response in plain text
      summary: request the current time for a timezone.
      x-accepts:
      - text/plain
  /timezone/{area}/{location}/{region}:
    get:
      parameters:
      - explode: false
        in: path
        name: area
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: path
        name: location
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: path
        name: region
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DateTimeJsonResponse'
          description: the current time for the timezone requested in JSON format
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorJsonResponse'
          description: an error response in JSON format
      summary: request the current time for a timezone.
      x-accepts:
      - application/json
  /timezone/{area}/{location}/{region}.txt:
    get:
      parameters:
      - explode: false
        in: path
        name: area
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: path
        name: location
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: path
        name: region
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/DateTimeTextResponse'
          description: the current time for the timezone requested in plain text
        default:
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ErrorTextResponse'
          description: an error response in plain text
      summary: request the current time for a timezone.
      x-accepts:
      - text/plain
components:
  responses:
    ErrorJsonResponse:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorJsonResponse'
      description: an error response in JSON format
    ErrorTextResponse:
      content:
        text/plain:
          schema:
            $ref: '#/components/schemas/ErrorTextResponse'
      description: an error response in plain text
    SuccessfulDateTimeJsonResponse:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/DateTimeJsonResponse'
      description: the current time for the timezone requested in JSON format
    SuccessfulDateTimeTextResponse:
      content:
        text/plain:
          schema:
            $ref: '#/components/schemas/DateTimeTextResponse'
      description: the current time for the timezone requested in plain text
    SuccessfulListJsonResponse:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ListJsonResponse'
      description: the list of available timezones in JSON format
    SuccessfulListTextResponse:
      content:
        text/plain:
          schema:
            $ref: '#/components/schemas/ListTextResponse'
      description: the list of available timezones in plain text
  schemas:
    DateTimeJsonResponse:
      example:
        unixtime: 5
        utc_offset: utc_offset
        dst: true
        day_of_year: 6
        timezone: timezone
        abbreviation: abbreviation
        dst_offset: 1
        utc_datetime: utc_datetime
        datetime: datetime
        dst_until: dst_until
        client_ip: client_ip
        dst_from: dst_from
        week_number: 2
        day_of_week: 0
        raw_offset: 5
      properties:
        abbreviation:
          description: the abbreviated name of the timezone
          type: string
        client_ip:
          description: the IP of the client making the request
          type: string
        datetime:
          description: "an ISO8601-valid string representing the current, local date/time"
          type: string
        day_of_week:
          description: "current day number of the week, where sunday is 0"
          type: integer
        day_of_year:
          description: ordinal date of the current year
          type: integer
        dst:
          description: flag indicating whether the local time is in daylight savings
          type: boolean
        dst_from:
          description: an ISO8601-valid string representing the datetime when daylight
            savings started for this timezone
          type: string
        dst_offset:
          description: the difference in seconds between the current local time and
            daylight saving time for the location
          type: integer
        dst_until:
          description: an ISO8601-valid string representing the datetime when daylight
            savings will end for this timezone
          type: string
        raw_offset:
          description: "the difference in seconds between the current local time and\
            \ the time in UTC, excluding any daylight saving difference (see dst_offset)"
          type: integer
        timezone:
          description: timezone in `Area/Location` or `Area/Location/Region` format
          type: string
        unixtime:
          description: number of seconds since the Epoch
          type: integer
        utc_datetime:
          description: an ISO8601-valid string representing the current date/time
            in UTC
          type: string
        utc_offset:
          description: an ISO8601-valid string representing the offset from UTC
          type: string
        week_number:
          description: the current week number
          type: integer
      required:
      - abbreviation
      - client_ip
      - datetime
      - day_of_week
      - day_of_year
      - dst
      - dst_offset
      - timezone
      - unixtime
      - utc_datetime
      - utc_offset
      - week_number
    DateTimeTextResponse:
      description: "time zone details, as per the DateTimeJsonResponse response, in\
        \ the format `key: value`, one item per line"
      type: string
    ErrorJsonResponse:
      example:
        error: error
      properties:
        error:
          description: details about the error encountered
          type: string
      required:
      - error
    ErrorTextResponse:
      description: details about the error encountered in plain text
      type: string
    ListJsonResponse:
      description: a list of available timezones
      items:
        type: string
      type: array
    ListTextResponse:
      description: "a list of available timezones, one per line"
      type: string

