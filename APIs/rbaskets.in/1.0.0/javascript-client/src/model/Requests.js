/**
 * Request Baskets API
 * RESTful API of [Request Baskets](https://rbaskets.in) service.  Request Baskets is an open source project of a service to collect HTTP requests and inspect them via RESTful API or web UI.  Check out the [project page](https://github.com/darklynx/request-baskets) for more detailed description. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Request from './Request';

/**
 * The Requests model module.
 * @module model/Requests
 * @version 1.0.0
 */
class Requests {
    /**
     * Constructs a new <code>Requests</code>.
     * @alias module:model/Requests
     * @param hasMore {Boolean} Indicates if there are more requests collected by basket to fetch
     * @param requests {Array.<module:model/Request>} Collection of collected requests
     */
    constructor(hasMore, requests) { 
        
        Requests.initialize(this, hasMore, requests);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hasMore, requests) { 
        obj['has_more'] = hasMore;
        obj['requests'] = requests;
    }

    /**
     * Constructs a <code>Requests</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Requests} obj Optional instance to populate.
     * @return {module:model/Requests} The populated <code>Requests</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Requests();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('has_more')) {
                obj['has_more'] = ApiClient.convertToType(data['has_more'], 'Boolean');
            }
            if (data.hasOwnProperty('requests')) {
                obj['requests'] = ApiClient.convertToType(data['requests'], [Request]);
            }
            if (data.hasOwnProperty('total_count')) {
                obj['total_count'] = ApiClient.convertToType(data['total_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Requests</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Requests</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Requests.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['requests']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['requests'])) {
                throw new Error("Expected the field `requests` to be an array in the JSON data but got " + data['requests']);
            }
            // validate the optional field `requests` (array)
            for (const item of data['requests']) {
                Request.validateJSON(item);
            };
        }

        return true;
    }


}

Requests.RequiredProperties = ["has_more", "requests"];

/**
 * Current number of collected requests hold by basket; not present if query is applied
 * @member {Number} count
 */
Requests.prototype['count'] = undefined;

/**
 * Indicates if there are more requests collected by basket to fetch
 * @member {Boolean} has_more
 */
Requests.prototype['has_more'] = undefined;

/**
 * Collection of collected requests
 * @member {Array.<module:model/Request>} requests
 */
Requests.prototype['requests'] = undefined;

/**
 * Total number of all requests passed through this basket; not present if query is applied
 * @member {Number} total_count
 */
Requests.prototype['total_count'] = undefined;






export default Requests;

