/**
 * Runscope API
 * Manage Runscope programmatically.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAITestStepsApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAITestStepsApi::OAITestStepsApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAITestStepsApi::~OAITestStepsApi() {
}

void OAITestStepsApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.runscope.com/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("bucketsBucketKeyTestsTestIdStepsGet", defaultConf);
    _serverIndices.insert("bucketsBucketKeyTestsTestIdStepsGet", 0);
    _serverConfigs.insert("bucketsBucketKeyTestsTestIdStepsPost", defaultConf);
    _serverIndices.insert("bucketsBucketKeyTestsTestIdStepsPost", 0);
    _serverConfigs.insert("bucketsBucketKeyTestsTestIdStepsStepIdDelete", defaultConf);
    _serverIndices.insert("bucketsBucketKeyTestsTestIdStepsStepIdDelete", 0);
    _serverConfigs.insert("bucketsBucketKeyTestsTestIdStepsStepIdPut", defaultConf);
    _serverIndices.insert("bucketsBucketKeyTestsTestIdStepsStepIdPut", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAITestStepsApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAITestStepsApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAITestStepsApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAITestStepsApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAITestStepsApi::setUsername(const QString &username) {
    _username = username;
}

void OAITestStepsApi::setPassword(const QString &password) {
    _password = password;
}


void OAITestStepsApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAITestStepsApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAITestStepsApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAITestStepsApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAITestStepsApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAITestStepsApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAITestStepsApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAITestStepsApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAITestStepsApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAITestStepsApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAITestStepsApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAITestStepsApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAITestStepsApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsGet(const QString &bucket_key, const QString &test_id) {
    QString fullPath = QString(_serverConfigs["bucketsBucketKeyTestsTestIdStepsGet"][_serverIndices.value("bucketsBucketKeyTestsTestIdStepsGet")].URL()+"/buckets/{bucketKey}/tests/{testId}/steps");
    
    
    {
        QString bucket_keyPathParam("{");
        bucket_keyPathParam.append("bucketKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "bucketKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"bucketKey"+pathSuffix : pathPrefix;
        fullPath.replace(bucket_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(bucket_key)));
    }
    
    {
        QString test_idPathParam("{");
        test_idPathParam.append("testId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "testId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"testId"+pathSuffix : pathPrefix;
        fullPath.replace(test_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(test_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsGetCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });
    _OauthMethod = 2;
    _implicitFlow.unlink();
    _credentialFlow.unlink();
    _passwordFlow.unlink();
    _authFlow.link();
    QStringList scope;
    scope.append("api:read");
    scope.append("test:read");
    auto token = _authFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsGetCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;




    worker->execute(&input);
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsGetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT bucketsBucketKeyTestsTestIdStepsGetSignal();
        Q_EMIT bucketsBucketKeyTestsTestIdStepsGetSignalFull(worker);
    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_authFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        scope.append("api:read");
        scope.append("test:read");
        QString scopeStr = scope.join(" ");
        QString authorizationUrl("https://www.runscope.com/signin/oauth/authorize");
        QString tokenUrl("https://www.runscope.com/signin/oauth/access_token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _authFlow.setVariables(authorizationUrl, tokenUrl, scopeStr, "state" , "http://127.0.0.1:9999", "clientId", "clientSecret");
        Q_EMIT _authFlow.authenticationNeeded();



    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsGetSignalE(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsGetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsGetSignalError(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsGetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsPost(const QString &bucket_key, const QString &test_id, const OAITestStep &test_step) {
    QString fullPath = QString(_serverConfigs["bucketsBucketKeyTestsTestIdStepsPost"][_serverIndices.value("bucketsBucketKeyTestsTestIdStepsPost")].URL()+"/buckets/{bucketKey}/tests/{testId}/steps");
    
    
    {
        QString bucket_keyPathParam("{");
        bucket_keyPathParam.append("bucketKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "bucketKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"bucketKey"+pathSuffix : pathPrefix;
        fullPath.replace(bucket_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(bucket_key)));
    }
    
    {
        QString test_idPathParam("{");
        test_idPathParam.append("testId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "testId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"testId"+pathSuffix : pathPrefix;
        fullPath.replace(test_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(test_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    {

        
        QByteArray output = test_step.asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsPostCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });
    _OauthMethod = 2;
    _implicitFlow.unlink();
    _credentialFlow.unlink();
    _passwordFlow.unlink();
    _authFlow.link();
    QStringList scope;
    scope.append("api:read");
    scope.append("test:write");
    auto token = _authFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsPostCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;




    worker->execute(&input);
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsPostCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT bucketsBucketKeyTestsTestIdStepsPostSignal();
        Q_EMIT bucketsBucketKeyTestsTestIdStepsPostSignalFull(worker);
    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_authFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        scope.append("api:read");
        scope.append("test:write");
        QString scopeStr = scope.join(" ");
        QString authorizationUrl("https://www.runscope.com/signin/oauth/authorize");
        QString tokenUrl("https://www.runscope.com/signin/oauth/access_token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _authFlow.setVariables(authorizationUrl, tokenUrl, scopeStr, "state" , "http://127.0.0.1:9999", "clientId", "clientSecret");
        Q_EMIT _authFlow.authenticationNeeded();



    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsPostSignalE(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsPostSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsPostSignalError(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsPostSignalErrorFull(worker, error_type, error_str);
    }
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdDelete(const QString &bucket_key, const QString &test_id, const QString &step_id) {
    QString fullPath = QString(_serverConfigs["bucketsBucketKeyTestsTestIdStepsStepIdDelete"][_serverIndices.value("bucketsBucketKeyTestsTestIdStepsStepIdDelete")].URL()+"/buckets/{bucketKey}/tests/{testId}/steps/{stepId}");
    
    
    {
        QString bucket_keyPathParam("{");
        bucket_keyPathParam.append("bucketKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "bucketKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"bucketKey"+pathSuffix : pathPrefix;
        fullPath.replace(bucket_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(bucket_key)));
    }
    
    {
        QString test_idPathParam("{");
        test_idPathParam.append("testId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "testId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"testId"+pathSuffix : pathPrefix;
        fullPath.replace(test_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(test_id)));
    }
    
    {
        QString step_idPathParam("{");
        step_idPathParam.append("stepId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "stepId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"stepId"+pathSuffix : pathPrefix;
        fullPath.replace(step_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(step_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdDeleteCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });
    _OauthMethod = 2;
    _implicitFlow.unlink();
    _credentialFlow.unlink();
    _passwordFlow.unlink();
    _authFlow.link();
    QStringList scope;
    scope.append("api:read");
    scope.append("test:write");
    auto token = _authFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdDeleteCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;




    worker->execute(&input);
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdDeleteCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdDeleteSignal();
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdDeleteSignalFull(worker);
    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_authFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        scope.append("api:read");
        scope.append("test:write");
        QString scopeStr = scope.join(" ");
        QString authorizationUrl("https://www.runscope.com/signin/oauth/authorize");
        QString tokenUrl("https://www.runscope.com/signin/oauth/access_token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _authFlow.setVariables(authorizationUrl, tokenUrl, scopeStr, "state" , "http://127.0.0.1:9999", "clientId", "clientSecret");
        Q_EMIT _authFlow.authenticationNeeded();



    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdDeleteSignalE(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdDeleteSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdDeleteSignalError(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdDeleteSignalErrorFull(worker, error_type, error_str);
    }
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdPut(const QString &bucket_key, const QString &test_id, const QString &step_id, const OAITestStep &test_step) {
    QString fullPath = QString(_serverConfigs["bucketsBucketKeyTestsTestIdStepsStepIdPut"][_serverIndices.value("bucketsBucketKeyTestsTestIdStepsStepIdPut")].URL()+"/buckets/{bucketKey}/tests/{testId}/steps/{stepId}");
    
    
    {
        QString bucket_keyPathParam("{");
        bucket_keyPathParam.append("bucketKey").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "bucketKey", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"bucketKey"+pathSuffix : pathPrefix;
        fullPath.replace(bucket_keyPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(bucket_key)));
    }
    
    {
        QString test_idPathParam("{");
        test_idPathParam.append("testId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "testId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"testId"+pathSuffix : pathPrefix;
        fullPath.replace(test_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(test_id)));
    }
    
    {
        QString step_idPathParam("{");
        step_idPathParam.append("stepId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "stepId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"stepId"+pathSuffix : pathPrefix;
        fullPath.replace(step_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(step_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PUT");

    {

        
        QByteArray output = test_step.asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdPutCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });
    _OauthMethod = 2;
    _implicitFlow.unlink();
    _credentialFlow.unlink();
    _passwordFlow.unlink();
    _authFlow.link();
    QStringList scope;
    scope.append("api:read");
    scope.append("test:write");
    auto token = _authFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdPutCallback);
    connect(this, &OAITestStepsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;




    worker->execute(&input);
}

void OAITestStepsApi::bucketsBucketKeyTestsTestIdStepsStepIdPutCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdPutSignal();
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdPutSignalFull(worker);
    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_authFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        scope.append("api:read");
        scope.append("test:write");
        QString scopeStr = scope.join(" ");
        QString authorizationUrl("https://www.runscope.com/signin/oauth/authorize");
        QString tokenUrl("https://www.runscope.com/signin/oauth/access_token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _authFlow.setVariables(authorizationUrl, tokenUrl, scopeStr, "state" , "http://127.0.0.1:9999", "clientId", "clientSecret");
        Q_EMIT _authFlow.authenticationNeeded();



    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdPutSignalE(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdPutSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdPutSignalError(error_type, error_str);
        Q_EMIT bucketsBucketKeyTestsTestIdStepsStepIdPutSignalErrorFull(worker, error_type, error_str);
    }
}

void OAITestStepsApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
