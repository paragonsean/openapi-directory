/**
 * Bungie.Net API
 * These endpoints constitute the functionality exposed by Bungie.net, both for more traditional website functionality and for connectivity to Bungie video games and their related functionality.
 *
 * The version of the OpenAPI document: 2.18.0
 * Contact: support@bungie.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ApplicationsSeries from './ApplicationsSeries';

/**
 * The ApplicationsApiUsage model module.
 * @module model/ApplicationsApiUsage
 * @version 2.18.0
 */
class ApplicationsApiUsage {
    /**
     * Constructs a new <code>ApplicationsApiUsage</code>.
     * @alias module:model/ApplicationsApiUsage
     */
    constructor() { 
        
        ApplicationsApiUsage.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ApplicationsApiUsage</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApplicationsApiUsage} obj Optional instance to populate.
     * @return {module:model/ApplicationsApiUsage} The populated <code>ApplicationsApiUsage</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApplicationsApiUsage();

            if (data.hasOwnProperty('apiCalls')) {
                obj['apiCalls'] = ApiClient.convertToType(data['apiCalls'], [ApplicationsSeries]);
            }
            if (data.hasOwnProperty('throttledRequests')) {
                obj['throttledRequests'] = ApiClient.convertToType(data['throttledRequests'], [ApplicationsSeries]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApplicationsApiUsage</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApplicationsApiUsage</code>.
     */
    static validateJSON(data) {
        if (data['apiCalls']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['apiCalls'])) {
                throw new Error("Expected the field `apiCalls` to be an array in the JSON data but got " + data['apiCalls']);
            }
            // validate the optional field `apiCalls` (array)
            for (const item of data['apiCalls']) {
                ApplicationsSeries.validateJSON(item);
            };
        }
        if (data['throttledRequests']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['throttledRequests'])) {
                throw new Error("Expected the field `throttledRequests` to be an array in the JSON data but got " + data['throttledRequests']);
            }
            // validate the optional field `throttledRequests` (array)
            for (const item of data['throttledRequests']) {
                ApplicationsSeries.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Counts for on API calls made for the time range.
 * @member {Array.<module:model/ApplicationsSeries>} apiCalls
 */
ApplicationsApiUsage.prototype['apiCalls'] = undefined;

/**
 * Instances of blocked requests or requests that crossed the warn threshold during the time range.
 * @member {Array.<module:model/ApplicationsSeries>} throttledRequests
 */
ApplicationsApiUsage.prototype['throttledRequests'] = undefined;






export default ApplicationsApiUsage;

