/**
 * Bungie.Net API
 * These endpoints constitute the functionality exposed by Bungie.net, both for more traditional website functionality and for connectivity to Bungie video games and their related functionality.
 *
 * The version of the OpenAPI document: 2.18.0
 * Contact: support@bungie.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ForumForumRecruitmentDetail from './ForumForumRecruitmentDetail';
import ForumPollResponse from './ForumPollResponse';
import ForumPostResponse from './ForumPostResponse';
import GroupsV2GroupResponse from './GroupsV2GroupResponse';
import QueriesPagedQuery from './QueriesPagedQuery';
import TagsModelsContractsTagResponse from './TagsModelsContractsTagResponse';
import UserGeneralUser from './UserGeneralUser';

/**
 * The ForumPostSearchResponse model module.
 * @module model/ForumPostSearchResponse
 * @version 2.18.0
 */
class ForumPostSearchResponse {
    /**
     * Constructs a new <code>ForumPostSearchResponse</code>.
     * @alias module:model/ForumPostSearchResponse
     */
    constructor() { 
        
        ForumPostSearchResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ForumPostSearchResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ForumPostSearchResponse} obj Optional instance to populate.
     * @return {module:model/ForumPostSearchResponse} The populated <code>ForumPostSearchResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ForumPostSearchResponse();

            if (data.hasOwnProperty('authors')) {
                obj['authors'] = ApiClient.convertToType(data['authors'], [UserGeneralUser]);
            }
            if (data.hasOwnProperty('availablePages')) {
                obj['availablePages'] = ApiClient.convertToType(data['availablePages'], 'Number');
            }
            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], [GroupsV2GroupResponse]);
            }
            if (data.hasOwnProperty('hasMore')) {
                obj['hasMore'] = ApiClient.convertToType(data['hasMore'], 'Boolean');
            }
            if (data.hasOwnProperty('polls')) {
                obj['polls'] = ApiClient.convertToType(data['polls'], [ForumPollResponse]);
            }
            if (data.hasOwnProperty('query')) {
                obj['query'] = QueriesPagedQuery.constructFromObject(data['query']);
            }
            if (data.hasOwnProperty('recruitmentDetails')) {
                obj['recruitmentDetails'] = ApiClient.convertToType(data['recruitmentDetails'], [ForumForumRecruitmentDetail]);
            }
            if (data.hasOwnProperty('relatedPosts')) {
                obj['relatedPosts'] = ApiClient.convertToType(data['relatedPosts'], [ForumPostResponse]);
            }
            if (data.hasOwnProperty('replacementContinuationToken')) {
                obj['replacementContinuationToken'] = ApiClient.convertToType(data['replacementContinuationToken'], 'String');
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [ForumPostResponse]);
            }
            if (data.hasOwnProperty('searchedTags')) {
                obj['searchedTags'] = ApiClient.convertToType(data['searchedTags'], [TagsModelsContractsTagResponse]);
            }
            if (data.hasOwnProperty('totalResults')) {
                obj['totalResults'] = ApiClient.convertToType(data['totalResults'], 'Number');
            }
            if (data.hasOwnProperty('useTotalResults')) {
                obj['useTotalResults'] = ApiClient.convertToType(data['useTotalResults'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ForumPostSearchResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ForumPostSearchResponse</code>.
     */
    static validateJSON(data) {
        if (data['authors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['authors'])) {
                throw new Error("Expected the field `authors` to be an array in the JSON data but got " + data['authors']);
            }
            // validate the optional field `authors` (array)
            for (const item of data['authors']) {
                UserGeneralUser.validateJSON(item);
            };
        }
        if (data['groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groups'])) {
                throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
            }
            // validate the optional field `groups` (array)
            for (const item of data['groups']) {
                GroupsV2GroupResponse.validateJSON(item);
            };
        }
        if (data['polls']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['polls'])) {
                throw new Error("Expected the field `polls` to be an array in the JSON data but got " + data['polls']);
            }
            // validate the optional field `polls` (array)
            for (const item of data['polls']) {
                ForumPollResponse.validateJSON(item);
            };
        }
        // validate the optional field `query`
        if (data['query']) { // data not null
          QueriesPagedQuery.validateJSON(data['query']);
        }
        if (data['recruitmentDetails']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['recruitmentDetails'])) {
                throw new Error("Expected the field `recruitmentDetails` to be an array in the JSON data but got " + data['recruitmentDetails']);
            }
            // validate the optional field `recruitmentDetails` (array)
            for (const item of data['recruitmentDetails']) {
                ForumForumRecruitmentDetail.validateJSON(item);
            };
        }
        if (data['relatedPosts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['relatedPosts'])) {
                throw new Error("Expected the field `relatedPosts` to be an array in the JSON data but got " + data['relatedPosts']);
            }
            // validate the optional field `relatedPosts` (array)
            for (const item of data['relatedPosts']) {
                ForumPostResponse.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['replacementContinuationToken'] && !(typeof data['replacementContinuationToken'] === 'string' || data['replacementContinuationToken'] instanceof String)) {
            throw new Error("Expected the field `replacementContinuationToken` to be a primitive type in the JSON string but got " + data['replacementContinuationToken']);
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                ForumPostResponse.validateJSON(item);
            };
        }
        if (data['searchedTags']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['searchedTags'])) {
                throw new Error("Expected the field `searchedTags` to be an array in the JSON data but got " + data['searchedTags']);
            }
            // validate the optional field `searchedTags` (array)
            for (const item of data['searchedTags']) {
                TagsModelsContractsTagResponse.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/UserGeneralUser>} authors
 */
ForumPostSearchResponse.prototype['authors'] = undefined;

/**
 * @member {Number} availablePages
 */
ForumPostSearchResponse.prototype['availablePages'] = undefined;

/**
 * @member {Array.<module:model/GroupsV2GroupResponse>} groups
 */
ForumPostSearchResponse.prototype['groups'] = undefined;

/**
 * @member {Boolean} hasMore
 */
ForumPostSearchResponse.prototype['hasMore'] = undefined;

/**
 * @member {Array.<module:model/ForumPollResponse>} polls
 */
ForumPostSearchResponse.prototype['polls'] = undefined;

/**
 * @member {module:model/QueriesPagedQuery} query
 */
ForumPostSearchResponse.prototype['query'] = undefined;

/**
 * @member {Array.<module:model/ForumForumRecruitmentDetail>} recruitmentDetails
 */
ForumPostSearchResponse.prototype['recruitmentDetails'] = undefined;

/**
 * @member {Array.<module:model/ForumPostResponse>} relatedPosts
 */
ForumPostSearchResponse.prototype['relatedPosts'] = undefined;

/**
 * @member {String} replacementContinuationToken
 */
ForumPostSearchResponse.prototype['replacementContinuationToken'] = undefined;

/**
 * @member {Array.<module:model/ForumPostResponse>} results
 */
ForumPostSearchResponse.prototype['results'] = undefined;

/**
 * @member {Array.<module:model/TagsModelsContractsTagResponse>} searchedTags
 */
ForumPostSearchResponse.prototype['searchedTags'] = undefined;

/**
 * @member {Number} totalResults
 */
ForumPostSearchResponse.prototype['totalResults'] = undefined;

/**
 * If useTotalResults is true, then totalResults represents an accurate count.  If False, it does not, and may be estimated/only the size of the current page.  Either way, you should probably always only trust hasMore.  This is a long-held historical throwback to when we used to do paging with known total results. Those queries toasted our database, and we were left to hastily alter our endpoints and create backward- compatible shims, of which useTotalResults is one.
 * @member {Boolean} useTotalResults
 */
ForumPostSearchResponse.prototype['useTotalResults'] = undefined;






export default ForumPostSearchResponse;

