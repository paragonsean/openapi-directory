/**
 * Bungie.Net API
 * These endpoints constitute the functionality exposed by Bungie.net, both for more traditional website functionality and for connectivity to Bungie video games and their related functionality.
 *
 * The version of the OpenAPI document: 2.18.0
 * Contact: support@bungie.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DestinyEntitiesItemsDestinyItemSocketState model module.
 * @module model/DestinyEntitiesItemsDestinyItemSocketState
 * @version 2.18.0
 */
class DestinyEntitiesItemsDestinyItemSocketState {
    /**
     * Constructs a new <code>DestinyEntitiesItemsDestinyItemSocketState</code>.
     * The status of a given item&#39;s socket. (which plug is inserted, if any: whether it is enabled, what \&quot;reusable\&quot; plugs can be inserted, etc...)  If I had it to do over, this would probably have a DestinyItemPlug representing the inserted item instead of most of these properties. :shrug:
     * @alias module:model/DestinyEntitiesItemsDestinyItemSocketState
     */
    constructor() { 
        
        DestinyEntitiesItemsDestinyItemSocketState.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DestinyEntitiesItemsDestinyItemSocketState</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DestinyEntitiesItemsDestinyItemSocketState} obj Optional instance to populate.
     * @return {module:model/DestinyEntitiesItemsDestinyItemSocketState} The populated <code>DestinyEntitiesItemsDestinyItemSocketState</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DestinyEntitiesItemsDestinyItemSocketState();

            if (data.hasOwnProperty('enableFailIndexes')) {
                obj['enableFailIndexes'] = ApiClient.convertToType(data['enableFailIndexes'], ['Number']);
            }
            if (data.hasOwnProperty('isEnabled')) {
                obj['isEnabled'] = ApiClient.convertToType(data['isEnabled'], 'Boolean');
            }
            if (data.hasOwnProperty('isVisible')) {
                obj['isVisible'] = ApiClient.convertToType(data['isVisible'], 'Boolean');
            }
            if (data.hasOwnProperty('plugHash')) {
                obj['plugHash'] = ApiClient.convertToType(data['plugHash'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DestinyEntitiesItemsDestinyItemSocketState</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DestinyEntitiesItemsDestinyItemSocketState</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['enableFailIndexes'])) {
            throw new Error("Expected the field `enableFailIndexes` to be an array in the JSON data but got " + data['enableFailIndexes']);
        }

        return true;
    }


}



/**
 * If a plug is inserted but not enabled, this will be populated with indexes into the plug item definition's plug.enabledRules property, so that you can show the reasons why it is not enabled.
 * @member {Array.<Number>} enableFailIndexes
 */
DestinyEntitiesItemsDestinyItemSocketState.prototype['enableFailIndexes'] = undefined;

/**
 * Even if a plug is inserted, it doesn't mean it's enabled.  This flag indicates whether the plug is active and providing its benefits.
 * @member {Boolean} isEnabled
 */
DestinyEntitiesItemsDestinyItemSocketState.prototype['isEnabled'] = undefined;

/**
 * A plug may theoretically provide benefits but not be visible - for instance, some older items use a plug's damage type perk to modify their own damage type. These, though they are not visible, still affect the item. This field indicates that state.  An invisible plug, while it provides benefits if it is Enabled, cannot be directly modified by the user.
 * @member {Boolean} isVisible
 */
DestinyEntitiesItemsDestinyItemSocketState.prototype['isVisible'] = undefined;

/**
 * The currently active plug, if any.  Note that, because all plugs are statically defined, its effect on stats and perks can be statically determined using the plug item's definition. The stats and perks can be taken at face value on the plug item as the stats and perks it will provide to the user/item.
 * @member {Number} plugHash
 */
DestinyEntitiesItemsDestinyItemSocketState.prototype['plugHash'] = undefined;






export default DestinyEntitiesItemsDestinyItemSocketState;

