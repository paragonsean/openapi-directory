/**
 * Business Registries
 * # Introduction The Business Registries API is built on HTTP. The API is RESTful. It has predictable resource URIs.    The API is documented in <a target=\"_blank\" href=\"https://github.com/OAI/OpenAPI-Specification\">OpenAPI</a> format.   In addition to the standard OpenAPI syntax we use a few   <a target=\"_blank\" href=\"https://github.com/Rebilly/ReDoc/blob/master/docs/redoc-vendor-extensions.md\">vendor extensions</a>.  # Overview The following sections describe the resources that make up the Business Registries REST API. ## Current Version By default, all requests to https://api.abr.ato.gov.au receive the `v1` version of the REST API. We encourage you to explicitly request this version via the `Accept` header.      Accept: application/vnd.abr-ato.v1+json  ## Schema All API access is over HTTPS, and accessed from https://api.abr.ato.gov.au. All data is sent and received as JSON. Blank fields are included.    All dates use the [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format:      YYYY-MM-DD    For example: `2017-07-01` (the 1st of July 2017)    All timestamps use the [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format:      YYYY-MM-DDTHH:MM:SSZ    For example: `2017-07-01T11:05:06+10:00`  ## Timezones Some requests allow for specifying timestamps or generate timestamps with time zone information. We apply the following rules, in order of priority, to determine timezone information for API calls. ### Explicitly provide an ISO 8601 timestamp with timezone information For API calls that allow for a timestamp to be specified, we use that exact timestamp.    For example: `2017-07-01T11:05:06+10:00`  ## Pagination Information about pagination is provided in the [Link](https://tools.ietf.org/html/rfc5988#page-6) header.    For example:      Link: <https://api.abr.ato.gov.au/individuals?page=2>; rel=\"next\",           <https://api.abr.ato.gov.au/individuals?page=34>; rel=\"last\"  `rel=\"next\"` states that the next page is `page=2`. This makes sense, since by default, all paginated queries start at page `1`. `rel=\"last\"` provides some more information, stating that the last page of results is on `page 34`. Accordingly, we have 33 more pages of information that we can consume. ## Parameters Many API methods take optional parameters:      GET /individuals/1234/addresses/?addressType='Mailing'  In this example, the '1234' value is provided for the `:partyId` parameter in the path while `:addressType` is passed in the query string. For POST, PATCH, PUT, and DELETE requests, parameters not included in the URL should be encoded as JSON with a Content-Type of 'application/json'. ## Metadata The API provides **metadata services** that you can use to discover information about the classifcation schemes and values used by the Registry.    For example:      GET /classifications/roles    Sample response:      [       {         \"id\": \"123e4567-e89b-12d3-a456-426655440001\",         \"role\": \"Director\",         \"roleDescription\": \"An individual responsible for managing a company's ...\",         \"relationship\": \"Directorship\",         \"reciprocalRole\": \"Company\",         \"reciprocalRoleDescription\": \"An incorporated legal entity.\"       },       {         ...       }     ]  ## Root Endpoint You can issue a GET request to the root endpoint (also known as the service root) to get all the endpoint categories that the REST API supports:      curl https://api.abr.ato.gov.au  ## Authentication The Business Registries API supports API Key authentication.    When you sign up for an account, you are given your first API key. You can generate additional API keys, and delete   API keys (as you may need to rotate your keys in the future). You authenticate to the Business Registries API by   providing your secret key in the request header.    **Note:** Some requests will return `404 Not Found`, instead of `403 Permission Denied`. This is to prevent the   accidental leakage of information to unauthorised users. 
 *
 * The version of the OpenAPI document: 0.0.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ElectronicAddress from './ElectronicAddress';
import OrganisationAddress from './OrganisationAddress';
import OrganisationName from './OrganisationName';
import PartyId from './PartyId';
import RegisteredIdentifier from './RegisteredIdentifier';

/**
 * The Organisation model module.
 * @module model/Organisation
 * @version 0.0.6
 */
class Organisation {
    /**
     * Constructs a new <code>Organisation</code>.
     * The Organisation resource.
     * @alias module:model/Organisation
     * @param establishmentDate {Date} The organisation's establishment date, for example, `1928-03-03` (in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format).
     * @param legalEntityType {module:model/Organisation.LegalEntityTypeEnum} The organisation's legal entity type.
     */
    constructor(establishmentDate, legalEntityType) { 
        
        Organisation.initialize(this, establishmentDate, legalEntityType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, establishmentDate, legalEntityType) { 
        obj['establishmentDate'] = establishmentDate;
        obj['legalEntityType'] = legalEntityType || 'Company';
    }

    /**
     * Constructs a <code>Organisation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Organisation} obj Optional instance to populate.
     * @return {module:model/Organisation} The populated <code>Organisation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Organisation();

            if (data.hasOwnProperty('addresses')) {
                obj['addresses'] = ApiClient.convertToType(data['addresses'], [OrganisationAddress]);
            }
            if (data.hasOwnProperty('electronicAddresses')) {
                obj['electronicAddresses'] = ApiClient.convertToType(data['electronicAddresses'], [ElectronicAddress]);
            }
            if (data.hasOwnProperty('establishmentDate')) {
                obj['establishmentDate'] = ApiClient.convertToType(data['establishmentDate'], 'Date');
            }
            if (data.hasOwnProperty('fromDate')) {
                obj['fromDate'] = ApiClient.convertToType(data['fromDate'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], PartyId);
            }
            if (data.hasOwnProperty('legalEntityType')) {
                obj['legalEntityType'] = ApiClient.convertToType(data['legalEntityType'], 'String');
            }
            if (data.hasOwnProperty('names')) {
                obj['names'] = ApiClient.convertToType(data['names'], [OrganisationName]);
            }
            if (data.hasOwnProperty('registeredIdentifiers')) {
                obj['registeredIdentifiers'] = ApiClient.convertToType(data['registeredIdentifiers'], [RegisteredIdentifier]);
            }
            if (data.hasOwnProperty('toDate')) {
                obj['toDate'] = ApiClient.convertToType(data['toDate'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Organisation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Organisation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Organisation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['addresses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['addresses'])) {
                throw new Error("Expected the field `addresses` to be an array in the JSON data but got " + data['addresses']);
            }
            // validate the optional field `addresses` (array)
            for (const item of data['addresses']) {
                OrganisationAddress.validateJSON(item);
            };
        }
        if (data['electronicAddresses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['electronicAddresses'])) {
                throw new Error("Expected the field `electronicAddresses` to be an array in the JSON data but got " + data['electronicAddresses']);
            }
            // validate the optional field `electronicAddresses` (array)
            for (const item of data['electronicAddresses']) {
                ElectronicAddress.validateJSON(item);
            };
        }
        // validate the optional field `id`
        if (data['id']) { // data not null
          PartyId.validateJSON(data['id']);
        }
        // ensure the json data is a string
        if (data['legalEntityType'] && !(typeof data['legalEntityType'] === 'string' || data['legalEntityType'] instanceof String)) {
            throw new Error("Expected the field `legalEntityType` to be a primitive type in the JSON string but got " + data['legalEntityType']);
        }
        if (data['names']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['names'])) {
                throw new Error("Expected the field `names` to be an array in the JSON data but got " + data['names']);
            }
            // validate the optional field `names` (array)
            for (const item of data['names']) {
                OrganisationName.validateJSON(item);
            };
        }
        if (data['registeredIdentifiers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['registeredIdentifiers'])) {
                throw new Error("Expected the field `registeredIdentifiers` to be an array in the JSON data but got " + data['registeredIdentifiers']);
            }
            // validate the optional field `registeredIdentifiers` (array)
            for (const item of data['registeredIdentifiers']) {
                RegisteredIdentifier.validateJSON(item);
            };
        }

        return true;
    }


}

Organisation.RequiredProperties = ["establishmentDate", "legalEntityType"];

/**
 * @member {Array.<module:model/OrganisationAddress>} addresses
 */
Organisation.prototype['addresses'] = undefined;

/**
 * @member {Array.<module:model/ElectronicAddress>} electronicAddresses
 */
Organisation.prototype['electronicAddresses'] = undefined;

/**
 * The organisation's establishment date, for example, `1928-03-03` (in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format).
 * @member {Date} establishmentDate
 */
Organisation.prototype['establishmentDate'] = undefined;

/**
 * The date and time the resource became active in the format defined by [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601).
 * @member {Date} fromDate
 */
Organisation.prototype['fromDate'] = undefined;

/**
 * The party's unique identifier.
 * @member {module:model/PartyId} id
 */
Organisation.prototype['id'] = undefined;

/**
 * The organisation's legal entity type.
 * @member {module:model/Organisation.LegalEntityTypeEnum} legalEntityType
 * @default 'Company'
 */
Organisation.prototype['legalEntityType'] = 'Company';

/**
 * @member {Array.<module:model/OrganisationName>} names
 */
Organisation.prototype['names'] = undefined;

/**
 * @member {Array.<module:model/RegisteredIdentifier>} registeredIdentifiers
 */
Organisation.prototype['registeredIdentifiers'] = undefined;

/**
 * The date and time the resource became inactive in the format defined by [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601).
 * @member {Date} toDate
 */
Organisation.prototype['toDate'] = undefined;





/**
 * Allowed values for the <code>legalEntityType</code> property.
 * @enum {String}
 * @readonly
 */
Organisation['LegalEntityTypeEnum'] = {

    /**
     * value: "Company"
     * @const
     */
    "Company": "Company",

    /**
     * value: "Partnership"
     * @const
     */
    "Partnership": "Partnership",

    /**
     * value: "Trust"
     * @const
     */
    "Trust": "Trust",

    /**
     * value: "Joint Venture"
     * @const
     */
    "Joint Venture": "Joint Venture"
};



export default Organisation;

