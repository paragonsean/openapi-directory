/**
 * The Blue Alliance API v3
 * # Overview    Information and statistics about FIRST Robotics Competition teams and events.   # Authentication   All endpoints require an Auth Key to be passed in the header `X-TBA-Auth-Key`. If you do not have an auth key yet, you can obtain one from your [Account Page](/account).
 *
 * The version of the OpenAPI document: 3.8.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EventRankingExtraStatsInfoInner from './EventRankingExtraStatsInfoInner';
import EventRankingRankingsInner from './EventRankingRankingsInner';
import EventRankingSortOrderInfoInner from './EventRankingSortOrderInfoInner';

/**
 * The EventRanking model module.
 * @module model/EventRanking
 * @version 3.8.2
 */
class EventRanking {
    /**
     * Constructs a new <code>EventRanking</code>.
     * @alias module:model/EventRanking
     * @param rankings {Array.<module:model/EventRankingRankingsInner>} List of rankings at the event.
     * @param sortOrderInfo {Array.<module:model/EventRankingSortOrderInfoInner>} List of year-specific values provided in the `sort_orders` array for each team.
     */
    constructor(rankings, sortOrderInfo) { 
        
        EventRanking.initialize(this, rankings, sortOrderInfo);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, rankings, sortOrderInfo) { 
        obj['rankings'] = rankings;
        obj['sort_order_info'] = sortOrderInfo;
    }

    /**
     * Constructs a <code>EventRanking</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventRanking} obj Optional instance to populate.
     * @return {module:model/EventRanking} The populated <code>EventRanking</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventRanking();

            if (data.hasOwnProperty('extra_stats_info')) {
                obj['extra_stats_info'] = ApiClient.convertToType(data['extra_stats_info'], [EventRankingExtraStatsInfoInner]);
            }
            if (data.hasOwnProperty('rankings')) {
                obj['rankings'] = ApiClient.convertToType(data['rankings'], [EventRankingRankingsInner]);
            }
            if (data.hasOwnProperty('sort_order_info')) {
                obj['sort_order_info'] = ApiClient.convertToType(data['sort_order_info'], [EventRankingSortOrderInfoInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventRanking</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventRanking</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EventRanking.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['extra_stats_info']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['extra_stats_info'])) {
                throw new Error("Expected the field `extra_stats_info` to be an array in the JSON data but got " + data['extra_stats_info']);
            }
            // validate the optional field `extra_stats_info` (array)
            for (const item of data['extra_stats_info']) {
                EventRankingExtraStatsInfoInner.validateJSON(item);
            };
        }
        if (data['rankings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rankings'])) {
                throw new Error("Expected the field `rankings` to be an array in the JSON data but got " + data['rankings']);
            }
            // validate the optional field `rankings` (array)
            for (const item of data['rankings']) {
                EventRankingRankingsInner.validateJSON(item);
            };
        }
        if (data['sort_order_info']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sort_order_info'])) {
                throw new Error("Expected the field `sort_order_info` to be an array in the JSON data but got " + data['sort_order_info']);
            }
            // validate the optional field `sort_order_info` (array)
            for (const item of data['sort_order_info']) {
                EventRankingSortOrderInfoInner.validateJSON(item);
            };
        }

        return true;
    }


}

EventRanking.RequiredProperties = ["rankings", "sort_order_info"];

/**
 * List of special TBA-generated values provided in the `extra_stats` array for each item.
 * @member {Array.<module:model/EventRankingExtraStatsInfoInner>} extra_stats_info
 */
EventRanking.prototype['extra_stats_info'] = undefined;

/**
 * List of rankings at the event.
 * @member {Array.<module:model/EventRankingRankingsInner>} rankings
 */
EventRanking.prototype['rankings'] = undefined;

/**
 * List of year-specific values provided in the `sort_orders` array for each team.
 * @member {Array.<module:model/EventRankingSortOrderInfoInner>} sort_order_info
 */
EventRanking.prototype['sort_order_info'] = undefined;






export default EventRanking;

