/**
 * The Blue Alliance API v3
 * # Overview    Information and statistics about FIRST Robotics Competition teams and events.   # Authentication   All endpoints require an Auth Key to be passed in the header `X-TBA-Auth-Key`. If you do not have an auth key yet, you can obtain one from your [Account Page](/account).
 *
 * The version of the OpenAPI document: 3.8.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DistrictRankingEventPointsInner model module.
 * @module model/DistrictRankingEventPointsInner
 * @version 3.8.2
 */
class DistrictRankingEventPointsInner {
    /**
     * Constructs a new <code>DistrictRankingEventPointsInner</code>.
     * @alias module:model/DistrictRankingEventPointsInner
     * @param alliancePoints {Number} Points awarded for alliance selection.
     * @param awardPoints {Number} Points awarded for event awards.
     * @param districtCmp {Boolean} `true` if this event is a District Championship event.
     * @param elimPoints {Number} Points awarded for elimination match performance.
     * @param eventKey {String} TBA Event key for this event.
     * @param qualPoints {Number} Points awarded for qualification match performance.
     * @param total {Number} Total points awarded at this event.
     */
    constructor(alliancePoints, awardPoints, districtCmp, elimPoints, eventKey, qualPoints, total) { 
        
        DistrictRankingEventPointsInner.initialize(this, alliancePoints, awardPoints, districtCmp, elimPoints, eventKey, qualPoints, total);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, alliancePoints, awardPoints, districtCmp, elimPoints, eventKey, qualPoints, total) { 
        obj['alliance_points'] = alliancePoints;
        obj['award_points'] = awardPoints;
        obj['district_cmp'] = districtCmp;
        obj['elim_points'] = elimPoints;
        obj['event_key'] = eventKey;
        obj['qual_points'] = qualPoints;
        obj['total'] = total;
    }

    /**
     * Constructs a <code>DistrictRankingEventPointsInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DistrictRankingEventPointsInner} obj Optional instance to populate.
     * @return {module:model/DistrictRankingEventPointsInner} The populated <code>DistrictRankingEventPointsInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DistrictRankingEventPointsInner();

            if (data.hasOwnProperty('alliance_points')) {
                obj['alliance_points'] = ApiClient.convertToType(data['alliance_points'], 'Number');
            }
            if (data.hasOwnProperty('award_points')) {
                obj['award_points'] = ApiClient.convertToType(data['award_points'], 'Number');
            }
            if (data.hasOwnProperty('district_cmp')) {
                obj['district_cmp'] = ApiClient.convertToType(data['district_cmp'], 'Boolean');
            }
            if (data.hasOwnProperty('elim_points')) {
                obj['elim_points'] = ApiClient.convertToType(data['elim_points'], 'Number');
            }
            if (data.hasOwnProperty('event_key')) {
                obj['event_key'] = ApiClient.convertToType(data['event_key'], 'String');
            }
            if (data.hasOwnProperty('qual_points')) {
                obj['qual_points'] = ApiClient.convertToType(data['qual_points'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DistrictRankingEventPointsInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DistrictRankingEventPointsInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DistrictRankingEventPointsInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['event_key'] && !(typeof data['event_key'] === 'string' || data['event_key'] instanceof String)) {
            throw new Error("Expected the field `event_key` to be a primitive type in the JSON string but got " + data['event_key']);
        }

        return true;
    }


}

DistrictRankingEventPointsInner.RequiredProperties = ["alliance_points", "award_points", "district_cmp", "elim_points", "event_key", "qual_points", "total"];

/**
 * Points awarded for alliance selection.
 * @member {Number} alliance_points
 */
DistrictRankingEventPointsInner.prototype['alliance_points'] = undefined;

/**
 * Points awarded for event awards.
 * @member {Number} award_points
 */
DistrictRankingEventPointsInner.prototype['award_points'] = undefined;

/**
 * `true` if this event is a District Championship event.
 * @member {Boolean} district_cmp
 */
DistrictRankingEventPointsInner.prototype['district_cmp'] = undefined;

/**
 * Points awarded for elimination match performance.
 * @member {Number} elim_points
 */
DistrictRankingEventPointsInner.prototype['elim_points'] = undefined;

/**
 * TBA Event key for this event.
 * @member {String} event_key
 */
DistrictRankingEventPointsInner.prototype['event_key'] = undefined;

/**
 * Points awarded for qualification match performance.
 * @member {Number} qual_points
 */
DistrictRankingEventPointsInner.prototype['qual_points'] = undefined;

/**
 * Total points awarded at this event.
 * @member {Number} total
 */
DistrictRankingEventPointsInner.prototype['total'] = undefined;






export default DistrictRankingEventPointsInner;

