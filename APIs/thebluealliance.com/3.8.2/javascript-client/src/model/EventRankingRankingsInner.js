/**
 * The Blue Alliance API v3
 * # Overview    Information and statistics about FIRST Robotics Competition teams and events.   # Authentication   All endpoints require an Auth Key to be passed in the header `X-TBA-Auth-Key`. If you do not have an auth key yet, you can obtain one from your [Account Page](/account).
 *
 * The version of the OpenAPI document: 3.8.2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WLTRecord from './WLTRecord';

/**
 * The EventRankingRankingsInner model module.
 * @module model/EventRankingRankingsInner
 * @version 3.8.2
 */
class EventRankingRankingsInner {
    /**
     * Constructs a new <code>EventRankingRankingsInner</code>.
     * @alias module:model/EventRankingRankingsInner
     * @param dq {Number} Number of times disqualified.
     * @param matchesPlayed {Number} Number of matches played by this team.
     * @param rank {Number} The team's rank at the event as provided by FIRST.
     * @param record {module:model/WLTRecord} 
     * @param teamKey {String} The team with this rank.
     */
    constructor(dq, matchesPlayed, rank, record, teamKey) { 
        
        EventRankingRankingsInner.initialize(this, dq, matchesPlayed, rank, record, teamKey);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dq, matchesPlayed, rank, record, teamKey) { 
        obj['dq'] = dq;
        obj['matches_played'] = matchesPlayed;
        obj['rank'] = rank;
        obj['record'] = record;
        obj['team_key'] = teamKey;
    }

    /**
     * Constructs a <code>EventRankingRankingsInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventRankingRankingsInner} obj Optional instance to populate.
     * @return {module:model/EventRankingRankingsInner} The populated <code>EventRankingRankingsInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventRankingRankingsInner();

            if (data.hasOwnProperty('dq')) {
                obj['dq'] = ApiClient.convertToType(data['dq'], 'Number');
            }
            if (data.hasOwnProperty('extra_stats')) {
                obj['extra_stats'] = ApiClient.convertToType(data['extra_stats'], ['Number']);
            }
            if (data.hasOwnProperty('matches_played')) {
                obj['matches_played'] = ApiClient.convertToType(data['matches_played'], 'Number');
            }
            if (data.hasOwnProperty('qual_average')) {
                obj['qual_average'] = ApiClient.convertToType(data['qual_average'], 'Number');
            }
            if (data.hasOwnProperty('rank')) {
                obj['rank'] = ApiClient.convertToType(data['rank'], 'Number');
            }
            if (data.hasOwnProperty('record')) {
                obj['record'] = WLTRecord.constructFromObject(data['record']);
            }
            if (data.hasOwnProperty('sort_orders')) {
                obj['sort_orders'] = ApiClient.convertToType(data['sort_orders'], ['Number']);
            }
            if (data.hasOwnProperty('team_key')) {
                obj['team_key'] = ApiClient.convertToType(data['team_key'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventRankingRankingsInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventRankingRankingsInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EventRankingRankingsInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['extra_stats'])) {
            throw new Error("Expected the field `extra_stats` to be an array in the JSON data but got " + data['extra_stats']);
        }
        // validate the optional field `record`
        if (data['record']) { // data not null
          WLTRecord.validateJSON(data['record']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['sort_orders'])) {
            throw new Error("Expected the field `sort_orders` to be an array in the JSON data but got " + data['sort_orders']);
        }
        // ensure the json data is a string
        if (data['team_key'] && !(typeof data['team_key'] === 'string' || data['team_key'] instanceof String)) {
            throw new Error("Expected the field `team_key` to be a primitive type in the JSON string but got " + data['team_key']);
        }

        return true;
    }


}

EventRankingRankingsInner.RequiredProperties = ["dq", "matches_played", "rank", "record", "team_key"];

/**
 * Number of times disqualified.
 * @member {Number} dq
 */
EventRankingRankingsInner.prototype['dq'] = undefined;

/**
 * Additional special data on the team's performance calculated by TBA.
 * @member {Array.<Number>} extra_stats
 */
EventRankingRankingsInner.prototype['extra_stats'] = undefined;

/**
 * Number of matches played by this team.
 * @member {Number} matches_played
 */
EventRankingRankingsInner.prototype['matches_played'] = undefined;

/**
 * The average match score during qualifications. Year specific. May be null if not relevant for a given year.
 * @member {Number} qual_average
 */
EventRankingRankingsInner.prototype['qual_average'] = undefined;

/**
 * The team's rank at the event as provided by FIRST.
 * @member {Number} rank
 */
EventRankingRankingsInner.prototype['rank'] = undefined;

/**
 * @member {module:model/WLTRecord} record
 */
EventRankingRankingsInner.prototype['record'] = undefined;

/**
 * Additional year-specific information, may be null. See parent `sort_order_info` for details.
 * @member {Array.<Number>} sort_orders
 */
EventRankingRankingsInner.prototype['sort_orders'] = undefined;

/**
 * The team with this rank.
 * @member {String} team_key
 */
EventRankingRankingsInner.prototype['team_key'] = undefined;






export default EventRankingRankingsInner;

