# storm_glass_marine_weather

StormGlassMarineWeather - JavaScript client for storm_glass_marine_weather
Global marine weather data from multiple sources in one single API with hourly resolution. Get your API key by visiting the Storm Glass web site.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.1
- Package version: 1.0.1
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install storm_glass_marine_weather --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your storm_glass_marine_weather from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var StormGlassMarineWeather = require('storm_glass_marine_weather');

var defaultClient = StormGlassMarineWeather.ApiClient.instance;
// Configure API key authorization: authenticationToken
var authenticationToken = defaultClient.authentications['authenticationToken'];
authenticationToken.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//authenticationToken.apiKeyPrefix['Authentication-Token'] = "Token"

var api = new StormGlassMarineWeather.ForecastApi()
var lat = 3.4; // {Number} The latitude for a location. Valid input is a number between -90 and 90.
var lng = 3.4; // {Number} The longitude for a location. Valid input is a number between -180 and 180.
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.getForecast(lat, lng, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.stormglass.io*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*StormGlassMarineWeather.ForecastApi* | [**getForecast**](docs/ForecastApi.md#getForecast) | **GET** /forecast | Get hourly forecasts by coordinates


## Documentation for Models

 - [StormGlassMarineWeather.Forecast](docs/Forecast.md)
 - [StormGlassMarineWeather.ForecastHoursInner](docs/ForecastHoursInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerAirTemperatureInner](docs/ForecastHoursInnerAirTemperatureInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerSwellDirectionInner](docs/ForecastHoursInnerSwellDirectionInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerSwellHeightInner](docs/ForecastHoursInnerSwellHeightInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerSwellPeriodInner](docs/ForecastHoursInnerSwellPeriodInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerWaterTemperatureInner](docs/ForecastHoursInnerWaterTemperatureInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerWaveDirectionInner](docs/ForecastHoursInnerWaveDirectionInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerWaveHeightInner](docs/ForecastHoursInnerWaveHeightInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerWavePeriodInner](docs/ForecastHoursInnerWavePeriodInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerWindDirectionInner](docs/ForecastHoursInnerWindDirectionInner.md)
 - [StormGlassMarineWeather.ForecastHoursInnerWindSpeedInner](docs/ForecastHoursInnerWindSpeedInner.md)
 - [StormGlassMarineWeather.ForecastMeta](docs/ForecastMeta.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### authenticationToken


- **Type**: API key
- **API key parameter name**: Authentication-Token
- **Location**: HTTP header

