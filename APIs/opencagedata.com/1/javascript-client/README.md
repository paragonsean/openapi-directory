# open_cage_geocoder

OpenCageGeocoder - JavaScript client for open_cage_geocoder
Worldwide forward and reverse geocoding
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1
- Package version: 1
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://opencagedata.com/contact](https://opencagedata.com/contact)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install open_cage_geocoder --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your open_cage_geocoder from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var OpenCageGeocoder = require('open_cage_geocoder');


var api = new OpenCageGeocoder.DefaultApi()
var version = 56; // {Number} API version.
var format = "format_example"; // {String} format of the response. One of 'json', 'xml' or 'map'.
var q = "q_example"; // {String} string or lat,lng to be geocoded.
var key = "key_example"; // {String} an application key.
var opts = {
  'abbrv': true, // {Boolean} when true we attempt to abbreviate the formatted field of results.
  'addressOnly': true, // {Boolean} when true we include only address details in the formatted field of results.
  'addRequest': true, // {Boolean} if true the request is included in the response.
  'bounds': "bounds_example", // {String} four coordinate points forming the south-west and north-east corners of a bounding box (min long, min lat, max long, max lat).
  'countrycode': "countrycode_example", // {String} two letter code ISO 3166-1 Alpha 2 code to limit results to that country.
  'jsonp': "jsonp_example", // {String} wraps the returned JSON with a function name.
  'language': "language_example", // {String} an IETF format language code (ex: 'es' or 'pt-BR').
  'limit': 56, // {Number} maximum number of results to return. Default is 10. Maximum is 100.
  'minConfidence': 56, // {Number} integer from 1-10. Only results with at least this confidence are returned.
  'noAnnotations': true, // {Boolean} when true annotations are not added to results.
  'noDedupe': true, // {Boolean} when true results are not deduplicated.
  'noRecord': true, // {Boolean} when true query content is not logged.
  'pretty': true, // {Boolean} when true results are pretty printed. Useful for debugging.
  'proximity': "proximity_example", // {String} lat,lng to bias results.
  'roadinfo': true // {Boolean} match nearest road, include roadinfo annotation
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.vversionFormatGet(version, format, q, key, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.opencagedata.com/geocode*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*OpenCageGeocoder.DefaultApi* | [**vversionFormatGet**](docs/DefaultApi.md#vversionFormatGet) | **GET** /v{version}/{format} | 


## Documentation for Models

 - [OpenCageGeocoder.LatLng](docs/LatLng.md)
 - [OpenCageGeocoder.Response](docs/Response.md)
 - [OpenCageGeocoder.ResponseLicensesInner](docs/ResponseLicensesInner.md)
 - [OpenCageGeocoder.ResponseRate](docs/ResponseRate.md)
 - [OpenCageGeocoder.ResponseResultsInner](docs/ResponseResultsInner.md)
 - [OpenCageGeocoder.ResponseResultsInnerBounds](docs/ResponseResultsInnerBounds.md)
 - [OpenCageGeocoder.ResponseStatus](docs/ResponseStatus.md)
 - [OpenCageGeocoder.ResponseStayInformed](docs/ResponseStayInformed.md)
 - [OpenCageGeocoder.ResponseTimestamp](docs/ResponseTimestamp.md)


## Documentation for Authorization

Endpoints do not require authorization.

