/**
 * Labs64 NetLicensing RESTful API Test Center
 * The Labs64 <a href='https://netlicensing.io/wiki/restful-api' target='_blank'>NetLicensing RESTful API</a> gives you access to NetLicensing’s core features.<br/><br/><strong>Authentication</strong><br/>You authenticate to the NetLicensing API by providing your account credentials or simply use our demo account - <code>demo:demo</code><br/><br/>Find out more about Labs64 NetLicensing at <a href='https://netlicensing.io' target='_blank'>netlicensing.io</a>
 *
 * The version of the OpenAPI document: 2.x
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAILicenseTemplateApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAILicenseTemplateApi::OAILicenseTemplateApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAILicenseTemplateApi::~OAILicenseTemplateApi() {
}

void OAILicenseTemplateApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://go.netlicensing.io/core/v2/rest"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("createLicenseTemplate", defaultConf);
    _serverIndices.insert("createLicenseTemplate", 0);
    _serverConfigs.insert("deleteLicenseTemplate", defaultConf);
    _serverIndices.insert("deleteLicenseTemplate", 0);
    _serverConfigs.insert("getLicenseTemplate", defaultConf);
    _serverIndices.insert("getLicenseTemplate", 0);
    _serverConfigs.insert("listLicenseTemplates", defaultConf);
    _serverIndices.insert("listLicenseTemplates", 0);
    _serverConfigs.insert("updateLicenseTemplate", defaultConf);
    _serverIndices.insert("updateLicenseTemplate", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAILicenseTemplateApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAILicenseTemplateApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAILicenseTemplateApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAILicenseTemplateApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAILicenseTemplateApi::setUsername(const QString &username) {
    _username = username;
}

void OAILicenseTemplateApi::setPassword(const QString &password) {
    _password = password;
}


void OAILicenseTemplateApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAILicenseTemplateApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAILicenseTemplateApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAILicenseTemplateApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAILicenseTemplateApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAILicenseTemplateApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAILicenseTemplateApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAILicenseTemplateApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAILicenseTemplateApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAILicenseTemplateApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAILicenseTemplateApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAILicenseTemplateApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAILicenseTemplateApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAILicenseTemplateApi::createLicenseTemplate(const bool &active, const QString &license_type, const QString &name, const QString &product_module_number, const ::OpenAPI::OptionalParam<bool> &automatic, const ::OpenAPI::OptionalParam<QString> &currency, const ::OpenAPI::OptionalParam<bool> &hidden, const ::OpenAPI::OptionalParam<bool> &hide_licenses, const ::OpenAPI::OptionalParam<QString> &max_sessions, const ::OpenAPI::OptionalParam<QString> &number, const ::OpenAPI::OptionalParam<double> &price, const ::OpenAPI::OptionalParam<QString> &quantity, const ::OpenAPI::OptionalParam<QString> &quota, const ::OpenAPI::OptionalParam<QString> &time_volume, const ::OpenAPI::OptionalParam<QString> &time_volume_period) {
    QString fullPath = QString(_serverConfigs["createLicenseTemplate"][_serverIndices.value("createLicenseTemplate")].URL()+"/licensetemplate");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    
    {
        input.add_var("active", ::OpenAPI::toStringValue(active));
    }
    if (automatic.hasValue())
    {
        input.add_var("automatic", ::OpenAPI::toStringValue(automatic.value()));
    }
    if (currency.hasValue())
    {
        input.add_var("currency", ::OpenAPI::toStringValue(currency.value()));
    }
    if (hidden.hasValue())
    {
        input.add_var("hidden", ::OpenAPI::toStringValue(hidden.value()));
    }
    if (hide_licenses.hasValue())
    {
        input.add_var("hideLicenses", ::OpenAPI::toStringValue(hide_licenses.value()));
    }
    
    {
        input.add_var("licenseType", ::OpenAPI::toStringValue(license_type));
    }
    if (max_sessions.hasValue())
    {
        input.add_var("maxSessions", ::OpenAPI::toStringValue(max_sessions.value()));
    }
    
    {
        input.add_var("name", ::OpenAPI::toStringValue(name));
    }
    if (number.hasValue())
    {
        input.add_var("number", ::OpenAPI::toStringValue(number.value()));
    }
    if (price.hasValue())
    {
        input.add_var("price", ::OpenAPI::toStringValue(price.value()));
    }
    
    {
        input.add_var("productModuleNumber", ::OpenAPI::toStringValue(product_module_number));
    }
    if (quantity.hasValue())
    {
        input.add_var("quantity", ::OpenAPI::toStringValue(quantity.value()));
    }
    if (quota.hasValue())
    {
        input.add_var("quota", ::OpenAPI::toStringValue(quota.value()));
    }
    if (time_volume.hasValue())
    {
        input.add_var("timeVolume", ::OpenAPI::toStringValue(time_volume.value()));
    }
    if (time_volume_period.hasValue())
    {
        input.add_var("timeVolumePeriod", ::OpenAPI::toStringValue(time_volume_period.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAILicenseTemplateApi::createLicenseTemplateCallback);
    connect(this, &OAILicenseTemplateApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAILicenseTemplateApi::createLicenseTemplateCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINetlicensing output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT createLicenseTemplateSignal(output);
        Q_EMIT createLicenseTemplateSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT createLicenseTemplateSignalE(output, error_type, error_str);
        Q_EMIT createLicenseTemplateSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT createLicenseTemplateSignalError(output, error_type, error_str);
        Q_EMIT createLicenseTemplateSignalErrorFull(worker, error_type, error_str);
    }
}

void OAILicenseTemplateApi::deleteLicenseTemplate(const QString &license_template_number, const ::OpenAPI::OptionalParam<bool> &force_cascade) {
    QString fullPath = QString(_serverConfigs["deleteLicenseTemplate"][_serverIndices.value("deleteLicenseTemplate")].URL()+"/licensetemplate/{licenseTemplateNumber}");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString license_template_numberPathParam("{");
        license_template_numberPathParam.append("licenseTemplateNumber").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "licenseTemplateNumber", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"licenseTemplateNumber"+pathSuffix : pathPrefix;
        fullPath.replace(license_template_numberPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(license_template_number)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (force_cascade.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "forceCascade", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("forceCascade")).append(querySuffix).append(QUrl::toPercentEncoding(force_cascade.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAILicenseTemplateApi::deleteLicenseTemplateCallback);
    connect(this, &OAILicenseTemplateApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAILicenseTemplateApi::deleteLicenseTemplateCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINetlicensing output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT deleteLicenseTemplateSignal(output);
        Q_EMIT deleteLicenseTemplateSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT deleteLicenseTemplateSignalE(output, error_type, error_str);
        Q_EMIT deleteLicenseTemplateSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT deleteLicenseTemplateSignalError(output, error_type, error_str);
        Q_EMIT deleteLicenseTemplateSignalErrorFull(worker, error_type, error_str);
    }
}

void OAILicenseTemplateApi::getLicenseTemplate(const QString &license_template_number) {
    QString fullPath = QString(_serverConfigs["getLicenseTemplate"][_serverIndices.value("getLicenseTemplate")].URL()+"/licensetemplate/{licenseTemplateNumber}");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString license_template_numberPathParam("{");
        license_template_numberPathParam.append("licenseTemplateNumber").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "licenseTemplateNumber", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"licenseTemplateNumber"+pathSuffix : pathPrefix;
        fullPath.replace(license_template_numberPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(license_template_number)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAILicenseTemplateApi::getLicenseTemplateCallback);
    connect(this, &OAILicenseTemplateApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAILicenseTemplateApi::getLicenseTemplateCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINetlicensing output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getLicenseTemplateSignal(output);
        Q_EMIT getLicenseTemplateSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getLicenseTemplateSignalE(output, error_type, error_str);
        Q_EMIT getLicenseTemplateSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getLicenseTemplateSignalError(output, error_type, error_str);
        Q_EMIT getLicenseTemplateSignalErrorFull(worker, error_type, error_str);
    }
}

void OAILicenseTemplateApi::listLicenseTemplates() {
    QString fullPath = QString(_serverConfigs["listLicenseTemplates"][_serverIndices.value("listLicenseTemplates")].URL()+"/licensetemplate");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAILicenseTemplateApi::listLicenseTemplatesCallback);
    connect(this, &OAILicenseTemplateApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAILicenseTemplateApi::listLicenseTemplatesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QList<OAINetlicensing> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAINetlicensing val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT listLicenseTemplatesSignal(output);
        Q_EMIT listLicenseTemplatesSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT listLicenseTemplatesSignalE(output, error_type, error_str);
        Q_EMIT listLicenseTemplatesSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT listLicenseTemplatesSignalError(output, error_type, error_str);
        Q_EMIT listLicenseTemplatesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAILicenseTemplateApi::updateLicenseTemplate(const QString &license_template_number, const ::OpenAPI::OptionalParam<bool> &active, const ::OpenAPI::OptionalParam<bool> &automatic, const ::OpenAPI::OptionalParam<QString> &currency, const ::OpenAPI::OptionalParam<bool> &hidden, const ::OpenAPI::OptionalParam<bool> &hide_licenses, const ::OpenAPI::OptionalParam<QString> &license_type, const ::OpenAPI::OptionalParam<QString> &max_sessions, const ::OpenAPI::OptionalParam<QString> &name, const ::OpenAPI::OptionalParam<QString> &number, const ::OpenAPI::OptionalParam<double> &price, const ::OpenAPI::OptionalParam<QString> &quantity, const ::OpenAPI::OptionalParam<QString> &quota, const ::OpenAPI::OptionalParam<QString> &time_volume, const ::OpenAPI::OptionalParam<QString> &time_volume_period) {
    QString fullPath = QString(_serverConfigs["updateLicenseTemplate"][_serverIndices.value("updateLicenseTemplate")].URL()+"/licensetemplate/{licenseTemplateNumber}");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString license_template_numberPathParam("{");
        license_template_numberPathParam.append("licenseTemplateNumber").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "licenseTemplateNumber", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"licenseTemplateNumber"+pathSuffix : pathPrefix;
        fullPath.replace(license_template_numberPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(license_template_number)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (active.hasValue())
    {
        input.add_var("active", ::OpenAPI::toStringValue(active.value()));
    }
    if (automatic.hasValue())
    {
        input.add_var("automatic", ::OpenAPI::toStringValue(automatic.value()));
    }
    if (currency.hasValue())
    {
        input.add_var("currency", ::OpenAPI::toStringValue(currency.value()));
    }
    if (hidden.hasValue())
    {
        input.add_var("hidden", ::OpenAPI::toStringValue(hidden.value()));
    }
    if (hide_licenses.hasValue())
    {
        input.add_var("hideLicenses", ::OpenAPI::toStringValue(hide_licenses.value()));
    }
    if (license_type.hasValue())
    {
        input.add_var("licenseType", ::OpenAPI::toStringValue(license_type.value()));
    }
    if (max_sessions.hasValue())
    {
        input.add_var("maxSessions", ::OpenAPI::toStringValue(max_sessions.value()));
    }
    if (name.hasValue())
    {
        input.add_var("name", ::OpenAPI::toStringValue(name.value()));
    }
    if (number.hasValue())
    {
        input.add_var("number", ::OpenAPI::toStringValue(number.value()));
    }
    if (price.hasValue())
    {
        input.add_var("price", ::OpenAPI::toStringValue(price.value()));
    }
    if (quantity.hasValue())
    {
        input.add_var("quantity", ::OpenAPI::toStringValue(quantity.value()));
    }
    if (quota.hasValue())
    {
        input.add_var("quota", ::OpenAPI::toStringValue(quota.value()));
    }
    if (time_volume.hasValue())
    {
        input.add_var("timeVolume", ::OpenAPI::toStringValue(time_volume.value()));
    }
    if (time_volume_period.hasValue())
    {
        input.add_var("timeVolumePeriod", ::OpenAPI::toStringValue(time_volume_period.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAILicenseTemplateApi::updateLicenseTemplateCallback);
    connect(this, &OAILicenseTemplateApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAILicenseTemplateApi::updateLicenseTemplateCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINetlicensing output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT updateLicenseTemplateSignal(output);
        Q_EMIT updateLicenseTemplateSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT updateLicenseTemplateSignalE(output, error_type, error_str);
        Q_EMIT updateLicenseTemplateSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT updateLicenseTemplateSignalError(output, error_type, error_str);
        Q_EMIT updateLicenseTemplateSignalErrorFull(worker, error_type, error_str);
    }
}

void OAILicenseTemplateApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
