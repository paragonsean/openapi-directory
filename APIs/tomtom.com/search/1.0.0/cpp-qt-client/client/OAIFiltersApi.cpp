/**
 * Search
 * Search API is a RESTful API that allows developers to run a single line fuzzy search for addresses and POIs. Search API returns the latitude/longitude of a specific address, cross street, geographic feature, or point of interest (POI).
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIFiltersApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIFiltersApi::OAIFiltersApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIFiltersApi::~OAIFiltersApi() {
}

void OAIFiltersApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://api.tomtom.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("searchVersionNumberGeometryFilterExtGet", defaultConf);
    _serverIndices.insert("searchVersionNumberGeometryFilterExtGet", 0);
    _serverConfigs.insert("searchVersionNumberGeometryFilterExtPost", defaultConf);
    _serverIndices.insert("searchVersionNumberGeometryFilterExtPost", 0);
    _serverConfigs.insert("searchVersionNumberRoutedFilterPositionHeadingExtGet", defaultConf);
    _serverIndices.insert("searchVersionNumberRoutedFilterPositionHeadingExtGet", 0);
    _serverConfigs.insert("searchVersionNumberRoutedFilterPositionHeadingExtPost", defaultConf);
    _serverIndices.insert("searchVersionNumberRoutedFilterPositionHeadingExtPost", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIFiltersApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIFiltersApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIFiltersApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIFiltersApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIFiltersApi::setUsername(const QString &username) {
    _username = username;
}

void OAIFiltersApi::setPassword(const QString &password) {
    _password = password;
}


void OAIFiltersApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIFiltersApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIFiltersApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIFiltersApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIFiltersApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIFiltersApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIFiltersApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIFiltersApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIFiltersApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIFiltersApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIFiltersApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIFiltersApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIFiltersApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIFiltersApi::searchVersionNumberGeometryFilterExtGet(const qint32 &version_number, const QString &ext, const QString &geometry_list, const QString &poi_list) {
    QString fullPath = QString(_serverConfigs["searchVersionNumberGeometryFilterExtGet"][_serverIndices.value("searchVersionNumberGeometryFilterExtGet")].URL()+"/search/{versionNumber}/geometryFilter.{ext}");
    
    if (_apiKeys.contains("api_key")) {
        if (fullPath.indexOf("?") > 0)
            fullPath.append("&");
        else
            fullPath.append("?");
        fullPath.append("api_key=").append(_apiKeys.find("api_key").value());
    }
    
    
    {
        QString version_numberPathParam("{");
        version_numberPathParam.append("versionNumber").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "versionNumber", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"versionNumber"+pathSuffix : pathPrefix;
        fullPath.replace(version_numberPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(version_number)));
    }
    
    {
        QString extPathParam("{");
        extPathParam.append("ext").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "ext", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"ext"+pathSuffix : pathPrefix;
        fullPath.replace(extPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(ext)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "geometryList", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("geometryList")).append(querySuffix).append(QUrl::toPercentEncoding(geometry_list));
    }
    
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "poiList", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("poiList")).append(querySuffix).append(QUrl::toPercentEncoding(poi_list));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFiltersApi::searchVersionNumberGeometryFilterExtGetCallback);
    connect(this, &OAIFiltersApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFiltersApi::searchVersionNumberGeometryFilterExtGetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT searchVersionNumberGeometryFilterExtGetSignal();
        Q_EMIT searchVersionNumberGeometryFilterExtGetSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT searchVersionNumberGeometryFilterExtGetSignalE(error_type, error_str);
        Q_EMIT searchVersionNumberGeometryFilterExtGetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT searchVersionNumberGeometryFilterExtGetSignalError(error_type, error_str);
        Q_EMIT searchVersionNumberGeometryFilterExtGetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFiltersApi::searchVersionNumberGeometryFilterExtPost(const qint32 &version_number, const QString &ext, const ::OpenAPI::OptionalParam<OAI_search__versionNumber__geometryFilter__ext__post_request> &oai_search__version_number__geometry_filter__ext__post_request) {
    QString fullPath = QString(_serverConfigs["searchVersionNumberGeometryFilterExtPost"][_serverIndices.value("searchVersionNumberGeometryFilterExtPost")].URL()+"/search/{versionNumber}/geometryFilter.{ext}");
    
    if (_apiKeys.contains("api_key")) {
        if (fullPath.indexOf("?") > 0)
            fullPath.append("&");
        else
            fullPath.append("?");
        fullPath.append("api_key=").append(_apiKeys.find("api_key").value());
    }
    
    
    {
        QString version_numberPathParam("{");
        version_numberPathParam.append("versionNumber").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "versionNumber", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"versionNumber"+pathSuffix : pathPrefix;
        fullPath.replace(version_numberPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(version_number)));
    }
    
    {
        QString extPathParam("{");
        extPathParam.append("ext").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "ext", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"ext"+pathSuffix : pathPrefix;
        fullPath.replace(extPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(ext)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (oai_search__version_number__geometry_filter__ext__post_request.hasValue()){

        
        QByteArray output = oai_search__version_number__geometry_filter__ext__post_request.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFiltersApi::searchVersionNumberGeometryFilterExtPostCallback);
    connect(this, &OAIFiltersApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFiltersApi::searchVersionNumberGeometryFilterExtPostCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT searchVersionNumberGeometryFilterExtPostSignal();
        Q_EMIT searchVersionNumberGeometryFilterExtPostSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT searchVersionNumberGeometryFilterExtPostSignalE(error_type, error_str);
        Q_EMIT searchVersionNumberGeometryFilterExtPostSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT searchVersionNumberGeometryFilterExtPostSignalError(error_type, error_str);
        Q_EMIT searchVersionNumberGeometryFilterExtPostSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFiltersApi::searchVersionNumberRoutedFilterPositionHeadingExtGet(const qint32 &version_number, const QString &position, const float &heading, const QString &ext, const QString &poi_list, const ::OpenAPI::OptionalParam<qint32> &routing_timeout) {
    QString fullPath = QString(_serverConfigs["searchVersionNumberRoutedFilterPositionHeadingExtGet"][_serverIndices.value("searchVersionNumberRoutedFilterPositionHeadingExtGet")].URL()+"/search/{versionNumber}/routedFilter/{position}/{heading}.{ext}");
    
    if (_apiKeys.contains("api_key")) {
        if (fullPath.indexOf("?") > 0)
            fullPath.append("&");
        else
            fullPath.append("?");
        fullPath.append("api_key=").append(_apiKeys.find("api_key").value());
    }
    
    
    {
        QString version_numberPathParam("{");
        version_numberPathParam.append("versionNumber").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "versionNumber", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"versionNumber"+pathSuffix : pathPrefix;
        fullPath.replace(version_numberPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(version_number)));
    }
    
    {
        QString positionPathParam("{");
        positionPathParam.append("position").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "position", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"position"+pathSuffix : pathPrefix;
        fullPath.replace(positionPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(position)));
    }
    
    {
        QString headingPathParam("{");
        headingPathParam.append("heading").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "heading", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"heading"+pathSuffix : pathPrefix;
        fullPath.replace(headingPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(heading)));
    }
    
    {
        QString extPathParam("{");
        extPathParam.append("ext").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "ext", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"ext"+pathSuffix : pathPrefix;
        fullPath.replace(extPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(ext)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "poiList", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("poiList")).append(querySuffix).append(QUrl::toPercentEncoding(poi_list));
    }
    if (routing_timeout.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "routingTimeout", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("routingTimeout")).append(querySuffix).append(QUrl::toPercentEncoding(routing_timeout.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFiltersApi::searchVersionNumberRoutedFilterPositionHeadingExtGetCallback);
    connect(this, &OAIFiltersApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFiltersApi::searchVersionNumberRoutedFilterPositionHeadingExtGetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtGetSignal();
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtGetSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtGetSignalE(error_type, error_str);
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtGetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtGetSignalError(error_type, error_str);
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtGetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFiltersApi::searchVersionNumberRoutedFilterPositionHeadingExtPost(const qint32 &version_number, const QString &position, const float &heading, const QString &ext, const ::OpenAPI::OptionalParam<qint32> &routing_timeout, const ::OpenAPI::OptionalParam<OAI_search__versionNumber__routedFilter__position___heading___ext__post_request> &oai_search__version_number__routed_filter__position___heading___ext__post_request) {
    QString fullPath = QString(_serverConfigs["searchVersionNumberRoutedFilterPositionHeadingExtPost"][_serverIndices.value("searchVersionNumberRoutedFilterPositionHeadingExtPost")].URL()+"/search/{versionNumber}/routedFilter/{position}/{heading}.{ext}");
    
    if (_apiKeys.contains("api_key")) {
        if (fullPath.indexOf("?") > 0)
            fullPath.append("&");
        else
            fullPath.append("?");
        fullPath.append("api_key=").append(_apiKeys.find("api_key").value());
    }
    
    
    {
        QString version_numberPathParam("{");
        version_numberPathParam.append("versionNumber").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "versionNumber", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"versionNumber"+pathSuffix : pathPrefix;
        fullPath.replace(version_numberPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(version_number)));
    }
    
    {
        QString positionPathParam("{");
        positionPathParam.append("position").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "position", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"position"+pathSuffix : pathPrefix;
        fullPath.replace(positionPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(position)));
    }
    
    {
        QString headingPathParam("{");
        headingPathParam.append("heading").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "heading", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"heading"+pathSuffix : pathPrefix;
        fullPath.replace(headingPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(heading)));
    }
    
    {
        QString extPathParam("{");
        extPathParam.append("ext").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "ext", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"ext"+pathSuffix : pathPrefix;
        fullPath.replace(extPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(ext)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (routing_timeout.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "routingTimeout", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("routingTimeout")).append(querySuffix).append(QUrl::toPercentEncoding(routing_timeout.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (oai_search__version_number__routed_filter__position___heading___ext__post_request.hasValue()){

        
        QByteArray output = oai_search__version_number__routed_filter__position___heading___ext__post_request.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFiltersApi::searchVersionNumberRoutedFilterPositionHeadingExtPostCallback);
    connect(this, &OAIFiltersApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFiltersApi::searchVersionNumberRoutedFilterPositionHeadingExtPostCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtPostSignal();
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtPostSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtPostSignalE(error_type, error_str);
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtPostSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtPostSignalError(error_type, error_str);
        Q_EMIT searchVersionNumberRoutedFilterPositionHeadingExtPostSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFiltersApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
