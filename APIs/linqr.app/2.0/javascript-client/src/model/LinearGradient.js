/**
 * LinQR
 * This is LinQR QR Code API documentation. This API allows you to generate custom, visually attractive QR Codes. The cloud infrastructure guarantees high availability and autoscalability of the service. You can generate hundreds of thousands of images this way and use them however you like.  We realize that your API use case may require custom solutions, and perhaps we lack functionality that is very important to you. In that case feel free to write an email to our support and tell us about it. We have repeatedly added new functions of our service directly after the requests of our users.  **General remarks:**  - maximum request size is fixed at 32MB.  - request timeout is fixed at 180 seconds.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@linqr.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GradientStop from './GradientStop';

/**
 * The LinearGradient model module.
 * @module model/LinearGradient
 * @version 2.0
 */
class LinearGradient {
    /**
     * Constructs a new <code>LinearGradient</code>.
     * @alias module:model/LinearGradient
     */
    constructor() { 
        
        LinearGradient.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['angle'] = 0;
        obj['type'] = 'linear';
    }

    /**
     * Constructs a <code>LinearGradient</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LinearGradient} obj Optional instance to populate.
     * @return {module:model/LinearGradient} The populated <code>LinearGradient</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LinearGradient();

            if (data.hasOwnProperty('angle')) {
                obj['angle'] = ApiClient.convertToType(data['angle'], 'Number');
            }
            if (data.hasOwnProperty('stops')) {
                obj['stops'] = ApiClient.convertToType(data['stops'], [GradientStop]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LinearGradient</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LinearGradient</code>.
     */
    static validateJSON(data) {
        if (data['stops']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stops'])) {
                throw new Error("Expected the field `stops` to be an array in the JSON data but got " + data['stops']);
            }
            // validate the optional field `stops` (array)
            for (const item of data['stops']) {
                GradientStop.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * Gradient angle (direction) in degrees. By default, horizontal gradient is applied. To reverse color direction use value of 180. Vertical gradients are indicated by values of 90 and 270. You can choose any angle value within the given range.
 * @member {Number} angle
 * @default 0
 */
LinearGradient.prototype['angle'] = 0;

/**
 * Gradient is created by specifying a list of (at least two) points called a `stop`. Each of them specifies a `offset` (in percent) along gradient fill and the `color` that the fill get at the given point.
 * @member {Array.<module:model/GradientStop>} stops
 */
LinearGradient.prototype['stops'] = undefined;

/**
 * @member {module:model/LinearGradient.TypeEnum} type
 * @default 'linear'
 */
LinearGradient.prototype['type'] = 'linear';





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
LinearGradient['TypeEnum'] = {

    /**
     * value: "linear"
     * @const
     */
    "linear": "linear"
};



export default LinearGradient;

