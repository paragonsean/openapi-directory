/**
 * LinQR
 * This is LinQR QR Code API documentation. This API allows you to generate custom, visually attractive QR Codes. The cloud infrastructure guarantees high availability and autoscalability of the service. You can generate hundreds of thousands of images this way and use them however you like.  We realize that your API use case may require custom solutions, and perhaps we lack functionality that is very important to you. In that case feel free to write an email to our support and tell us about it. We have repeatedly added new functions of our service directly after the requests of our users.  **General remarks:**  - maximum request size is fixed at 32MB.  - request timeout is fixed at 180 seconds.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@linqr.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GradientStop from './GradientStop';

/**
 * The RadialGradient model module.
 * @module model/RadialGradient
 * @version 2.0
 */
class RadialGradient {
    /**
     * Constructs a new <code>RadialGradient</code>.
     * @alias module:model/RadialGradient
     */
    constructor() { 
        
        RadialGradient.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['type'] = 'radial';
    }

    /**
     * Constructs a <code>RadialGradient</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RadialGradient} obj Optional instance to populate.
     * @return {module:model/RadialGradient} The populated <code>RadialGradient</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RadialGradient();

            if (data.hasOwnProperty('stops')) {
                obj['stops'] = ApiClient.convertToType(data['stops'], [GradientStop]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RadialGradient</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RadialGradient</code>.
     */
    static validateJSON(data) {
        if (data['stops']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stops'])) {
                throw new Error("Expected the field `stops` to be an array in the JSON data but got " + data['stops']);
            }
            // validate the optional field `stops` (array)
            for (const item of data['stops']) {
                GradientStop.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * Gradient is created by specifying a list of (at least two) points called a `stop`. Each of them specifies a `offset` (in percent) along gradient fill and the `color` that the fill get at the given point.
 * @member {Array.<module:model/GradientStop>} stops
 */
RadialGradient.prototype['stops'] = undefined;

/**
 * @member {module:model/RadialGradient.TypeEnum} type
 * @default 'radial'
 */
RadialGradient.prototype['type'] = 'radial';





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
RadialGradient['TypeEnum'] = {

    /**
     * value: "radial"
     * @const
     */
    "radial": "radial"
};



export default RadialGradient;

