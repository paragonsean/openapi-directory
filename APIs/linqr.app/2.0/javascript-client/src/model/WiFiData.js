/**
 * LinQR
 * This is LinQR QR Code API documentation. This API allows you to generate custom, visually attractive QR Codes. The cloud infrastructure guarantees high availability and autoscalability of the service. You can generate hundreds of thousands of images this way and use them however you like.  We realize that your API use case may require custom solutions, and perhaps we lack functionality that is very important to you. In that case feel free to write an email to our support and tell us about it. We have repeatedly added new functions of our service directly after the requests of our users.  **General remarks:**  - maximum request size is fixed at 32MB.  - request timeout is fixed at 180 seconds.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@linqr.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WiFiSecurity from './WiFiSecurity';

/**
 * The WiFiData model module.
 * @module model/WiFiData
 * @version 2.0
 */
class WiFiData {
    /**
     * Constructs a new <code>WiFiData</code>.
     * @alias module:model/WiFiData
     * @param ssid {String} Network SSID (name).
     */
    constructor(ssid) { 
        
        WiFiData.initialize(this, ssid);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ssid) { 
        obj['hidden'] = false;
        obj['ssid'] = ssid;
    }

    /**
     * Constructs a <code>WiFiData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WiFiData} obj Optional instance to populate.
     * @return {module:model/WiFiData} The populated <code>WiFiData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WiFiData();

            if (data.hasOwnProperty('hidden')) {
                obj['hidden'] = ApiClient.convertToType(data['hidden'], 'Boolean');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('security')) {
                obj['security'] = ApiClient.convertToType(data['security'], WiFiSecurity);
            }
            if (data.hasOwnProperty('ssid')) {
                obj['ssid'] = ApiClient.convertToType(data['ssid'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WiFiData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WiFiData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WiFiData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['ssid'] && !(typeof data['ssid'] === 'string' || data['ssid'] instanceof String)) {
            throw new Error("Expected the field `ssid` to be a primitive type in the JSON string but got " + data['ssid']);
        }

        return true;
    }


}

WiFiData.RequiredProperties = ["ssid"];

/**
 * Hidden network. Set to `true` if the SSID broadcasting is disabled (network is hidden).
 * @member {Boolean} hidden
 * @default false
 */
WiFiData.prototype['hidden'] = false;

/**
 * Network password. The value is not required for the public network.
 * @member {String} password
 */
WiFiData.prototype['password'] = undefined;

/**
 * Network authentication type. Value `nopass` is used to set explicitly no access password (public network) and is an equivalent for left the password unset. In that case, the value may be also omitted.
 * @member {module:model/WiFiSecurity} security
 */
WiFiData.prototype['security'] = undefined;

/**
 * Network SSID (name).
 * @member {String} ssid
 */
WiFiData.prototype['ssid'] = undefined;






export default WiFiData;

