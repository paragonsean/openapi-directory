/**
 * Transportation Laws and Incentives
 * Query our database of federal and state laws and incentives for alternative fuels and vehicles, air quality, fuel efficiency, and other transportation-related topics. This dataset powers the <a href=\"https://afdc.energy.gov/laws\">Federal and State Laws and Incentives</a> on the Alternative Fuels Data Center.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FullCategory model module.
 * @module model/FullCategory
 * @version 0.1.0
 */
class FullCategory {
    /**
     * Constructs a new <code>FullCategory</code>.
     * @alias module:model/FullCategory
     * @param categoryType {String} The type of category. (E.g. tech, incentive, regulation, user)
     * @param code {String} The code used to identify the category
     * @param sortOrder {String} The sort order for the categories
     * @param title {String} The full name of the category
     */
    constructor(categoryType, code, sortOrder, title) { 
        
        FullCategory.initialize(this, categoryType, code, sortOrder, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categoryType, code, sortOrder, title) { 
        obj['category_type'] = categoryType;
        obj['code'] = code;
        obj['sort_order'] = sortOrder;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>FullCategory</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FullCategory} obj Optional instance to populate.
     * @return {module:model/FullCategory} The populated <code>FullCategory</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FullCategory();

            if (data.hasOwnProperty('category_type')) {
                obj['category_type'] = ApiClient.convertToType(data['category_type'], 'String');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('help_text')) {
                obj['help_text'] = ApiClient.convertToType(data['help_text'], 'String');
            }
            if (data.hasOwnProperty('sort_order')) {
                obj['sort_order'] = ApiClient.convertToType(data['sort_order'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FullCategory</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FullCategory</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FullCategory.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['category_type'] && !(typeof data['category_type'] === 'string' || data['category_type'] instanceof String)) {
            throw new Error("Expected the field `category_type` to be a primitive type in the JSON string but got " + data['category_type']);
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['help_text'] && !(typeof data['help_text'] === 'string' || data['help_text'] instanceof String)) {
            throw new Error("Expected the field `help_text` to be a primitive type in the JSON string but got " + data['help_text']);
        }
        // ensure the json data is a string
        if (data['sort_order'] && !(typeof data['sort_order'] === 'string' || data['sort_order'] instanceof String)) {
            throw new Error("Expected the field `sort_order` to be a primitive type in the JSON string but got " + data['sort_order']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

FullCategory.RequiredProperties = ["category_type", "code", "sort_order", "title"];

/**
 * The type of category. (E.g. tech, incentive, regulation, user)
 * @member {String} category_type
 */
FullCategory.prototype['category_type'] = undefined;

/**
 * The code used to identify the category
 * @member {String} code
 */
FullCategory.prototype['code'] = undefined;

/**
 * A short description of what the law category encompasses
 * @member {String} help_text
 */
FullCategory.prototype['help_text'] = undefined;

/**
 * The sort order for the categories
 * @member {String} sort_order
 */
FullCategory.prototype['sort_order'] = undefined;

/**
 * The full name of the category
 * @member {String} title
 */
FullCategory.prototype['title'] = undefined;






export default FullCategory;

