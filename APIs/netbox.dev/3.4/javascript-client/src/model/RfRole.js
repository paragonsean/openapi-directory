/**
 * NetBox API
 * API to access NetBox
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RfRole model module.
 * @module model/RfRole
 * @version 3.4
 */
class RfRole {
    /**
     * Constructs a new <code>RfRole</code>.
     * @alias module:model/RfRole
     * @param label {module:model/RfRole.LabelEnum} 
     * @param value {module:model/RfRole.ValueEnum} 
     */
    constructor(label, value) { 
        
        RfRole.initialize(this, label, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, label, value) { 
        obj['label'] = label;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>RfRole</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RfRole} obj Optional instance to populate.
     * @return {module:model/RfRole} The populated <code>RfRole</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RfRole();

            if (data.hasOwnProperty('label')) {
                obj['label'] = ApiClient.convertToType(data['label'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RfRole</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RfRole</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RfRole.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['label'] && !(typeof data['label'] === 'string' || data['label'] instanceof String)) {
            throw new Error("Expected the field `label` to be a primitive type in the JSON string but got " + data['label']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

RfRole.RequiredProperties = ["label", "value"];

/**
 * @member {module:model/RfRole.LabelEnum} label
 */
RfRole.prototype['label'] = undefined;

/**
 * @member {module:model/RfRole.ValueEnum} value
 */
RfRole.prototype['value'] = undefined;





/**
 * Allowed values for the <code>label</code> property.
 * @enum {String}
 * @readonly
 */
RfRole['LabelEnum'] = {

    /**
     * value: "Access point"
     * @const
     */
    "Access point": "Access point",

    /**
     * value: "Station"
     * @const
     */
    "Station": "Station"
};


/**
 * Allowed values for the <code>value</code> property.
 * @enum {String}
 * @readonly
 */
RfRole['ValueEnum'] = {

    /**
     * value: "ap"
     * @const
     */
    "ap": "ap",

    /**
     * value: "station"
     * @const
     */
    "station": "station"
};



export default RfRole;

