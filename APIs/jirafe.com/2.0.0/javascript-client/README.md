# jirafe_events

JirafeEvents - JavaScript client for jirafe_events
API endpoins for sending Jirafe events
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2.0.0
- Package version: 2.0.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install jirafe_events --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your jirafe_events from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var JirafeEvents = require('jirafe_events');

var defaultClient = JirafeEvents.ApiClient.instance;
// Configure OAuth2 access token for authorization: oauth2_accessCode
var oauth2_accessCode = defaultClient.authentications['oauth2_accessCode'];
oauth2_accessCode.accessToken = "YOUR ACCESS TOKEN"
// Configure OAuth2 access token for authorization: oauth2_implicit
var oauth2_implicit = defaultClient.authentications['oauth2_implicit'];
oauth2_implicit.accessToken = "YOUR ACCESS TOKEN"

var api = new JirafeEvents.BatchApi()
var siteId = "siteId_example"; // {String} ID site to send the event
var body = new JirafeEvents.Batch(); // {Batch} batch json of events
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.postBatch(siteId, body, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://event.jirafe.com/v2*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*JirafeEvents.BatchApi* | [**postBatch**](docs/BatchApi.md#postBatch) | **POST** /{siteId}/batch | Send a batch for the given site
*JirafeEvents.CartApi* | [**postCart**](docs/CartApi.md#postCart) | **POST** /{siteId}/cart | Send a cart for the given site
*JirafeEvents.CategoryApi* | [**postCategory**](docs/CategoryApi.md#postCategory) | **POST** /{siteId}/category | Send a category for the given site
*JirafeEvents.CustomerApi* | [**postCustomer**](docs/CustomerApi.md#postCustomer) | **POST** /{siteId}/customer | Send a customer for the given site
*JirafeEvents.OrderApi* | [**postOrderCancelled**](docs/OrderApi.md#postOrderCancelled) | **POST** /{siteId}/order | Send a order for the given site
*JirafeEvents.ProductApi* | [**postProduct**](docs/ProductApi.md#postProduct) | **POST** /{siteId}/product | Send a product for the given site


## Documentation for Models

 - [JirafeEvents.Attribute](docs/Attribute.md)
 - [JirafeEvents.BaseProduct](docs/BaseProduct.md)
 - [JirafeEvents.Batch](docs/Batch.md)
 - [JirafeEvents.Cart](docs/Cart.md)
 - [JirafeEvents.CartItem](docs/CartItem.md)
 - [JirafeEvents.Catalog](docs/Catalog.md)
 - [JirafeEvents.Category](docs/Category.md)
 - [JirafeEvents.Customer](docs/Customer.md)
 - [JirafeEvents.Order](docs/Order.md)
 - [JirafeEvents.OrderCancelled](docs/OrderCancelled.md)
 - [JirafeEvents.OrderItem](docs/OrderItem.md)
 - [JirafeEvents.ParentCategory](docs/ParentCategory.md)
 - [JirafeEvents.Product](docs/Product.md)
 - [JirafeEvents.Url](docs/Url.md)
 - [JirafeEvents.Vendor](docs/Vendor.md)
 - [JirafeEvents.Visit](docs/Visit.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### oauth2_accessCode


- **Type**: OAuth
- **Flow**: accessCode
- **Authorization URL**: https://account.jirafe.com/oauth2/auth_token
- **Scopes**: 
  - write: Send events

### oauth2_implicit


- **Type**: OAuth
- **Flow**: implicit
- **Authorization URL**: https://account.jirafe.com/oauth2/authorize
- **Scopes**: 
  - write: Send events

