/**
 * U.S. EPA Enforcement and Compliance History Online (ECHO) - Enforcement Case Search
 * Enforcement and Compliance History Online (ECHO) is a tool developed and maintained by EPA's Office of Enforcement and Compliance Assurance for public use. ECHO provides integrated compliance and enforcement information for over 1 million regulated facilities nationwide.  CASE Rest Services provide multiple service endpoints, each with specific capabilities, to search and retrieve data on civil cases entered into the  Integrated Compliance Information System (ICIS) and criminal cases entered into the Summary of Criminal Prosecutions database.   See Enforcement Case Search Help (https://echo.epa.gov/help/enforcement-case-search-help) for additional information on searching civil and criminal cases.  \\ The get_cases, get_map, get_qid, and get_download end points are meant to be used together, while the enhanced get_case_info end point is self contained..    The recommended use scenario for get_cases, get_qid, get_map, and get_downoad is: \\  <b>1)</b>  Use get_cases to validate passed query parameters, obtain summary statistics and to obtain a query_id (QID).  QIDs are time sensitive and will be valid for approximately 30 minutes.  <b>2)</b>  Use get_qid, with the returned QID, to paginate through arrays of case results.  <b>3)</b>  Use get_map, with the returned QID, to zoom in/out and pan on the clustered and individual facility coordinates, related to the returned cases, that meet the QID query criteria.  <b>4)</b>  Use get_download, with the returned QID, to generate a Comma Separated Value (CSV) file of facility information that meets the QID query criteria. \\ In addition to the service endpoints listed above there are two detailed case report services, one for civil cases (get_case_report) and one for criminal cases (get_crcase_report).  See the Civil Enforcement Case Report Help (https://echo.epa.gov/help/reports/enforcement-case-report-help) and the Criminal Case Report Help (https://echo.epa.gov/help/reports/criminal-case-report-help) for additional information  on then data returned from these two services.    \\ Additional ECHO Resources:   <a href=\"https://echo.epa.gov/tools/web-services\">Web Services</a>, <a href=\"https://echo.epa.gov/resources/echo-data/about-the-data\">About ECHO's Data</a>, <a href=\"https://echo.epa.gov/tools/data-downloads\">Data Downloads</a>  
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Crs3CRDefendants from './Crs3CRDefendants';
import Crs3CRDetails from './Crs3CRDetails';
import Crs3CaseInformation from './Crs3CaseInformation';
import Crs3Locations from './Crs3Locations';

/**
 * The Crs3Results model module.
 * @module model/Crs3Results
 * @version 1.0.0
 */
class Crs3Results {
    /**
     * Constructs a new <code>Crs3Results</code>.
     * The data returned by the service call.
     * @alias module:model/Crs3Results
     * @param cRCaseInformation {module:model/Crs3CaseInformation} 
     * @param cRDefendants {Array.<module:model/Crs3CRDefendants>} 
     * @param cRDetails {Array.<module:model/Crs3CRDetails>} 
     * @param locations {Array.<module:model/Crs3Locations>} 
     * @param message {String} Field to record messages (typically performance-related) about packet processing
     */
    constructor(cRCaseInformation, cRDefendants, cRDetails, locations, message) { 
        
        Crs3Results.initialize(this, cRCaseInformation, cRDefendants, cRDetails, locations, message);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cRCaseInformation, cRDefendants, cRDetails, locations, message) { 
        obj['CRCaseInformation'] = cRCaseInformation;
        obj['CRDefendants'] = cRDefendants;
        obj['CRDetails'] = cRDetails;
        obj['Locations'] = locations;
        obj['Message'] = message;
    }

    /**
     * Constructs a <code>Crs3Results</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Crs3Results} obj Optional instance to populate.
     * @return {module:model/Crs3Results} The populated <code>Crs3Results</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Crs3Results();

            if (data.hasOwnProperty('CRCaseInformation')) {
                obj['CRCaseInformation'] = Crs3CaseInformation.constructFromObject(data['CRCaseInformation']);
            }
            if (data.hasOwnProperty('CRDefendants')) {
                obj['CRDefendants'] = ApiClient.convertToType(data['CRDefendants'], [Crs3CRDefendants]);
            }
            if (data.hasOwnProperty('CRDetails')) {
                obj['CRDetails'] = ApiClient.convertToType(data['CRDetails'], [Crs3CRDetails]);
            }
            if (data.hasOwnProperty('Locations')) {
                obj['Locations'] = ApiClient.convertToType(data['Locations'], [Crs3Locations]);
            }
            if (data.hasOwnProperty('Message')) {
                obj['Message'] = ApiClient.convertToType(data['Message'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Crs3Results</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Crs3Results</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Crs3Results.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `CRCaseInformation`
        if (data['CRCaseInformation']) { // data not null
          Crs3CaseInformation.validateJSON(data['CRCaseInformation']);
        }
        if (data['CRDefendants']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['CRDefendants'])) {
                throw new Error("Expected the field `CRDefendants` to be an array in the JSON data but got " + data['CRDefendants']);
            }
            // validate the optional field `CRDefendants` (array)
            for (const item of data['CRDefendants']) {
                Crs3CRDefendants.validateJSON(item);
            };
        }
        if (data['CRDetails']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['CRDetails'])) {
                throw new Error("Expected the field `CRDetails` to be an array in the JSON data but got " + data['CRDetails']);
            }
            // validate the optional field `CRDetails` (array)
            for (const item of data['CRDetails']) {
                Crs3CRDetails.validateJSON(item);
            };
        }
        if (data['Locations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Locations'])) {
                throw new Error("Expected the field `Locations` to be an array in the JSON data but got " + data['Locations']);
            }
            // validate the optional field `Locations` (array)
            for (const item of data['Locations']) {
                Crs3Locations.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['Message'] && !(typeof data['Message'] === 'string' || data['Message'] instanceof String)) {
            throw new Error("Expected the field `Message` to be a primitive type in the JSON string but got " + data['Message']);
        }

        return true;
    }


}

Crs3Results.RequiredProperties = ["CRCaseInformation", "CRDefendants", "CRDetails", "Locations", "Message"];

/**
 * @member {module:model/Crs3CaseInformation} CRCaseInformation
 */
Crs3Results.prototype['CRCaseInformation'] = undefined;

/**
 * @member {Array.<module:model/Crs3CRDefendants>} CRDefendants
 */
Crs3Results.prototype['CRDefendants'] = undefined;

/**
 * @member {Array.<module:model/Crs3CRDetails>} CRDetails
 */
Crs3Results.prototype['CRDetails'] = undefined;

/**
 * @member {Array.<module:model/Crs3Locations>} Locations
 */
Crs3Results.prototype['Locations'] = undefined;

/**
 * Field to record messages (typically performance-related) about packet processing
 * @member {String} Message
 */
Crs3Results.prototype['Message'] = undefined;






export default Crs3Results;

