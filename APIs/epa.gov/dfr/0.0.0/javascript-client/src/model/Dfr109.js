/**
 * U.S. EPA Enforcement and Compliance History Online (ECHO) - Detailed Facility Report (DFR)
 * Enforcement and Compliance History Online (ECHO) is a tool developed and maintained by EPA's Office of Enforcement and Compliance Assurance for public use. ECHO provides integrated compliance and enforcement information for over 1 million regulated facilities nationwide.  DFR Rest Services provide multiple service endpoints, to retrieve detailed facility location, enforcement, compliance monitoring, and pollutant information for any single facility.  See the Detailed Facility Report (DFR) Help Page (https://echo.epa.gov/help/reports/detailed-facility-report-help) for additional information on the DFR.  Additionally, a Data Dictionary (https://echo.epa.gov/help/reports/dfr-data-dictionary) is also available.  There is one primary service end point, get_dfr, that provides all available DFR data.  All other service end points that are exposed, will return data on a single section of the DFR.     \\ Additional ECHO Resources:   <a href=\"https://echo.epa.gov/tools/web-services\">Web Services</a>, <a href=\"https://echo.epa.gov/resources/echo-data/about-the-data\">About ECHO's Data</a>, <a href=\"https://echo.epa.gov/tools/data-downloads\">Data Downloads</a>  
 *
 * The version of the OpenAPI document: 0.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Dfr109 model module.
 * @module model/Dfr109
 * @version 0.0.0
 */
class Dfr109 {
    /**
     * Constructs a new <code>Dfr109</code>.
     * Enforcement Actions Object
     * @alias module:model/Dfr109
     * @param agency {String} Identifies the lead agency (e.g., EPA, State) conducting the inspection.  Values: State abbreviation or \"EPA\"
     * @param enforcementActionTypeDesc {String} Text description of type of enforcement action.
     * @param enforcementDate {String} Date of Enforcement.
     * @param enforcementId {String} The internal numerick key identifier used to inuqiely identify enforcement case activities.
     * @param enforcementType {String} A text description of the Enforcement Action type.
     */
    constructor(agency, enforcementActionTypeDesc, enforcementDate, enforcementId, enforcementType) { 
        
        Dfr109.initialize(this, agency, enforcementActionTypeDesc, enforcementDate, enforcementId, enforcementType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, agency, enforcementActionTypeDesc, enforcementDate, enforcementId, enforcementType) { 
        obj['Agency'] = agency;
        obj['EnforcementActionTypeDesc'] = enforcementActionTypeDesc;
        obj['EnforcementDate'] = enforcementDate;
        obj['EnforcementId'] = enforcementId;
        obj['EnforcementType'] = enforcementType;
    }

    /**
     * Constructs a <code>Dfr109</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Dfr109} obj Optional instance to populate.
     * @return {module:model/Dfr109} The populated <code>Dfr109</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Dfr109();

            if (data.hasOwnProperty('Agency')) {
                obj['Agency'] = ApiClient.convertToType(data['Agency'], 'String');
            }
            if (data.hasOwnProperty('EnforcementActionTypeDesc')) {
                obj['EnforcementActionTypeDesc'] = ApiClient.convertToType(data['EnforcementActionTypeDesc'], 'String');
            }
            if (data.hasOwnProperty('EnforcementDate')) {
                obj['EnforcementDate'] = ApiClient.convertToType(data['EnforcementDate'], 'String');
            }
            if (data.hasOwnProperty('EnforcementId')) {
                obj['EnforcementId'] = ApiClient.convertToType(data['EnforcementId'], 'String');
            }
            if (data.hasOwnProperty('EnforcementType')) {
                obj['EnforcementType'] = ApiClient.convertToType(data['EnforcementType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Dfr109</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Dfr109</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Dfr109.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Agency'] && !(typeof data['Agency'] === 'string' || data['Agency'] instanceof String)) {
            throw new Error("Expected the field `Agency` to be a primitive type in the JSON string but got " + data['Agency']);
        }
        // ensure the json data is a string
        if (data['EnforcementActionTypeDesc'] && !(typeof data['EnforcementActionTypeDesc'] === 'string' || data['EnforcementActionTypeDesc'] instanceof String)) {
            throw new Error("Expected the field `EnforcementActionTypeDesc` to be a primitive type in the JSON string but got " + data['EnforcementActionTypeDesc']);
        }
        // ensure the json data is a string
        if (data['EnforcementDate'] && !(typeof data['EnforcementDate'] === 'string' || data['EnforcementDate'] instanceof String)) {
            throw new Error("Expected the field `EnforcementDate` to be a primitive type in the JSON string but got " + data['EnforcementDate']);
        }
        // ensure the json data is a string
        if (data['EnforcementId'] && !(typeof data['EnforcementId'] === 'string' || data['EnforcementId'] instanceof String)) {
            throw new Error("Expected the field `EnforcementId` to be a primitive type in the JSON string but got " + data['EnforcementId']);
        }
        // ensure the json data is a string
        if (data['EnforcementType'] && !(typeof data['EnforcementType'] === 'string' || data['EnforcementType'] instanceof String)) {
            throw new Error("Expected the field `EnforcementType` to be a primitive type in the JSON string but got " + data['EnforcementType']);
        }

        return true;
    }


}

Dfr109.RequiredProperties = ["Agency", "EnforcementActionTypeDesc", "EnforcementDate", "EnforcementId", "EnforcementType"];

/**
 * Identifies the lead agency (e.g., EPA, State) conducting the inspection.  Values: State abbreviation or \"EPA\"
 * @member {String} Agency
 */
Dfr109.prototype['Agency'] = undefined;

/**
 * Text description of type of enforcement action.
 * @member {String} EnforcementActionTypeDesc
 */
Dfr109.prototype['EnforcementActionTypeDesc'] = undefined;

/**
 * Date of Enforcement.
 * @member {String} EnforcementDate
 */
Dfr109.prototype['EnforcementDate'] = undefined;

/**
 * The internal numerick key identifier used to inuqiely identify enforcement case activities.
 * @member {String} EnforcementId
 */
Dfr109.prototype['EnforcementId'] = undefined;

/**
 * A text description of the Enforcement Action type.
 * @member {String} EnforcementType
 */
Dfr109.prototype['EnforcementType'] = undefined;






export default Dfr109;

