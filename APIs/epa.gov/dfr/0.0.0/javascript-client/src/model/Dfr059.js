/**
 * U.S. EPA Enforcement and Compliance History Online (ECHO) - Detailed Facility Report (DFR)
 * Enforcement and Compliance History Online (ECHO) is a tool developed and maintained by EPA's Office of Enforcement and Compliance Assurance for public use. ECHO provides integrated compliance and enforcement information for over 1 million regulated facilities nationwide.  DFR Rest Services provide multiple service endpoints, to retrieve detailed facility location, enforcement, compliance monitoring, and pollutant information for any single facility.  See the Detailed Facility Report (DFR) Help Page (https://echo.epa.gov/help/reports/detailed-facility-report-help) for additional information on the DFR.  Additionally, a Data Dictionary (https://echo.epa.gov/help/reports/dfr-data-dictionary) is also available.  There is one primary service end point, get_dfr, that provides all available DFR data.  All other service end points that are exposed, will return data on a single section of the DFR.     \\ Additional ECHO Resources:   <a href=\"https://echo.epa.gov/tools/web-services\">Web Services</a>, <a href=\"https://echo.epa.gov/resources/echo-data/about-the-data\">About ECHO's Data</a>, <a href=\"https://echo.epa.gov/tools/data-downloads\">Data Downloads</a>  
 *
 * The version of the OpenAPI document: 0.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Dfr018ProgramDates from './Dfr018ProgramDates';
import Dfr060 from './Dfr060';

/**
 * The Dfr059 model module.
 * @module model/Dfr059
 * @version 0.0.0
 */
class Dfr059 {
    /**
     * Constructs a new <code>Dfr059</code>.
     * Compliance Summary Object
     * @alias module:model/Dfr059
     * @param programDates {Array.<module:model/Dfr018ProgramDates>} 
     * @param source {Array.<module:model/Dfr060>} 
     */
    constructor(programDates, source) { 
        
        Dfr059.initialize(this, programDates, source);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, programDates, source) { 
        obj['ProgramDates'] = programDates;
        obj['Source'] = source;
    }

    /**
     * Constructs a <code>Dfr059</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Dfr059} obj Optional instance to populate.
     * @return {module:model/Dfr059} The populated <code>Dfr059</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Dfr059();

            if (data.hasOwnProperty('ProgramDates')) {
                obj['ProgramDates'] = ApiClient.convertToType(data['ProgramDates'], [Dfr018ProgramDates]);
            }
            if (data.hasOwnProperty('Source')) {
                obj['Source'] = ApiClient.convertToType(data['Source'], [Dfr060]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Dfr059</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Dfr059</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Dfr059.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['ProgramDates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ProgramDates'])) {
                throw new Error("Expected the field `ProgramDates` to be an array in the JSON data but got " + data['ProgramDates']);
            }
            // validate the optional field `ProgramDates` (array)
            for (const item of data['ProgramDates']) {
                Dfr018ProgramDates.validateJSON(item);
            };
        }
        if (data['Source']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Source'])) {
                throw new Error("Expected the field `Source` to be an array in the JSON data but got " + data['Source']);
            }
            // validate the optional field `Source` (array)
            for (const item of data['Source']) {
                Dfr060.validateJSON(item);
            };
        }

        return true;
    }


}

Dfr059.RequiredProperties = ["ProgramDates", "Source"];

/**
 * @member {Array.<module:model/Dfr018ProgramDates>} ProgramDates
 */
Dfr059.prototype['ProgramDates'] = undefined;

/**
 * @member {Array.<module:model/Dfr060>} Source
 */
Dfr059.prototype['Source'] = undefined;






export default Dfr059;

