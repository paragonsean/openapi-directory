/**
 * U.S. EPA Enforcement and Compliance History Online (ECHO) - Detailed Facility Report (DFR)
 * Enforcement and Compliance History Online (ECHO) is a tool developed and maintained by EPA's Office of Enforcement and Compliance Assurance for public use. ECHO provides integrated compliance and enforcement information for over 1 million regulated facilities nationwide.  DFR Rest Services provide multiple service endpoints, to retrieve detailed facility location, enforcement, compliance monitoring, and pollutant information for any single facility.  See the Detailed Facility Report (DFR) Help Page (https://echo.epa.gov/help/reports/detailed-facility-report-help) for additional information on the DFR.  Additionally, a Data Dictionary (https://echo.epa.gov/help/reports/dfr-data-dictionary) is also available.  There is one primary service end point, get_dfr, that provides all available DFR data.  All other service end points that are exposed, will return data on a single section of the DFR.     \\ Additional ECHO Resources:   <a href=\"https://echo.epa.gov/tools/web-services\">Web Services</a>, <a href=\"https://echo.epa.gov/resources/echo-data/about-the-data\">About ECHO's Data</a>, <a href=\"https://echo.epa.gov/tools/data-downloads\">Data Downloads</a>  
 *
 * The version of the OpenAPI document: 0.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Dfr018WebFireDocuments model module.
 * @module model/Dfr018WebFireDocuments
 * @version 0.0.0
 */
class Dfr018WebFireDocuments {
    /**
     * Constructs a new <code>Dfr018WebFireDocuments</code>.
     * Web Fire Documents
     * @alias module:model/Dfr018WebFireDocuments
     * @param documentLink {String} URLS for documents that EPA has colleted that are related to this Case.
     * @param documentName {String} The most severe compliance status for an outfall and parameter during the entire selected date range
     * @param documentType {String} The most severe compliance status for an outfall and parameter during the entire selected date range
     * @param facilityName {String} The name of the facility.
     * @param registryId {String} 12-digit Facility Registry Service (FRS) identification number.  FRS uniquely identifies a facility by assigning an identification number (FRS ID), and uses this FRS ID to link together all EPA regulatory program database records
     * @param reportDocType {String} The most severe compliance status for an outfall and parameter during the entire selected date range
     * @param reportSubmissionDate {String} The most severe compliance status for an outfall and parameter during the entire selected date range
     */
    constructor(documentLink, documentName, documentType, facilityName, registryId, reportDocType, reportSubmissionDate) { 
        
        Dfr018WebFireDocuments.initialize(this, documentLink, documentName, documentType, facilityName, registryId, reportDocType, reportSubmissionDate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, documentLink, documentName, documentType, facilityName, registryId, reportDocType, reportSubmissionDate) { 
        obj['DocumentLink'] = documentLink;
        obj['DocumentName'] = documentName;
        obj['DocumentType'] = documentType;
        obj['FacilityName'] = facilityName;
        obj['RegistryId'] = registryId;
        obj['ReportDocType'] = reportDocType;
        obj['ReportSubmissionDate'] = reportSubmissionDate;
    }

    /**
     * Constructs a <code>Dfr018WebFireDocuments</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Dfr018WebFireDocuments} obj Optional instance to populate.
     * @return {module:model/Dfr018WebFireDocuments} The populated <code>Dfr018WebFireDocuments</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Dfr018WebFireDocuments();

            if (data.hasOwnProperty('DocumentLink')) {
                obj['DocumentLink'] = ApiClient.convertToType(data['DocumentLink'], 'String');
            }
            if (data.hasOwnProperty('DocumentName')) {
                obj['DocumentName'] = ApiClient.convertToType(data['DocumentName'], 'String');
            }
            if (data.hasOwnProperty('DocumentType')) {
                obj['DocumentType'] = ApiClient.convertToType(data['DocumentType'], 'String');
            }
            if (data.hasOwnProperty('FacilityName')) {
                obj['FacilityName'] = ApiClient.convertToType(data['FacilityName'], 'String');
            }
            if (data.hasOwnProperty('RegistryId')) {
                obj['RegistryId'] = ApiClient.convertToType(data['RegistryId'], 'String');
            }
            if (data.hasOwnProperty('ReportDocType')) {
                obj['ReportDocType'] = ApiClient.convertToType(data['ReportDocType'], 'String');
            }
            if (data.hasOwnProperty('ReportSubmissionDate')) {
                obj['ReportSubmissionDate'] = ApiClient.convertToType(data['ReportSubmissionDate'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Dfr018WebFireDocuments</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Dfr018WebFireDocuments</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Dfr018WebFireDocuments.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['DocumentLink'] && !(typeof data['DocumentLink'] === 'string' || data['DocumentLink'] instanceof String)) {
            throw new Error("Expected the field `DocumentLink` to be a primitive type in the JSON string but got " + data['DocumentLink']);
        }
        // ensure the json data is a string
        if (data['DocumentName'] && !(typeof data['DocumentName'] === 'string' || data['DocumentName'] instanceof String)) {
            throw new Error("Expected the field `DocumentName` to be a primitive type in the JSON string but got " + data['DocumentName']);
        }
        // ensure the json data is a string
        if (data['DocumentType'] && !(typeof data['DocumentType'] === 'string' || data['DocumentType'] instanceof String)) {
            throw new Error("Expected the field `DocumentType` to be a primitive type in the JSON string but got " + data['DocumentType']);
        }
        // ensure the json data is a string
        if (data['FacilityName'] && !(typeof data['FacilityName'] === 'string' || data['FacilityName'] instanceof String)) {
            throw new Error("Expected the field `FacilityName` to be a primitive type in the JSON string but got " + data['FacilityName']);
        }
        // ensure the json data is a string
        if (data['RegistryId'] && !(typeof data['RegistryId'] === 'string' || data['RegistryId'] instanceof String)) {
            throw new Error("Expected the field `RegistryId` to be a primitive type in the JSON string but got " + data['RegistryId']);
        }
        // ensure the json data is a string
        if (data['ReportDocType'] && !(typeof data['ReportDocType'] === 'string' || data['ReportDocType'] instanceof String)) {
            throw new Error("Expected the field `ReportDocType` to be a primitive type in the JSON string but got " + data['ReportDocType']);
        }
        // ensure the json data is a string
        if (data['ReportSubmissionDate'] && !(typeof data['ReportSubmissionDate'] === 'string' || data['ReportSubmissionDate'] instanceof String)) {
            throw new Error("Expected the field `ReportSubmissionDate` to be a primitive type in the JSON string but got " + data['ReportSubmissionDate']);
        }

        return true;
    }


}

Dfr018WebFireDocuments.RequiredProperties = ["DocumentLink", "DocumentName", "DocumentType", "FacilityName", "RegistryId", "ReportDocType", "ReportSubmissionDate"];

/**
 * URLS for documents that EPA has colleted that are related to this Case.
 * @member {String} DocumentLink
 */
Dfr018WebFireDocuments.prototype['DocumentLink'] = undefined;

/**
 * The most severe compliance status for an outfall and parameter during the entire selected date range
 * @member {String} DocumentName
 */
Dfr018WebFireDocuments.prototype['DocumentName'] = undefined;

/**
 * The most severe compliance status for an outfall and parameter during the entire selected date range
 * @member {String} DocumentType
 */
Dfr018WebFireDocuments.prototype['DocumentType'] = undefined;

/**
 * The name of the facility.
 * @member {String} FacilityName
 */
Dfr018WebFireDocuments.prototype['FacilityName'] = undefined;

/**
 * 12-digit Facility Registry Service (FRS) identification number.  FRS uniquely identifies a facility by assigning an identification number (FRS ID), and uses this FRS ID to link together all EPA regulatory program database records
 * @member {String} RegistryId
 */
Dfr018WebFireDocuments.prototype['RegistryId'] = undefined;

/**
 * The most severe compliance status for an outfall and parameter during the entire selected date range
 * @member {String} ReportDocType
 */
Dfr018WebFireDocuments.prototype['ReportDocType'] = undefined;

/**
 * The most severe compliance status for an outfall and parameter during the entire selected date range
 * @member {String} ReportSubmissionDate
 */
Dfr018WebFireDocuments.prototype['ReportSubmissionDate'] = undefined;






export default Dfr018WebFireDocuments;

