/**
 * Anchore Engine API Server
 * This is the Anchore Engine API. Provides the primary external API for users of the service.
 *
 * The version of the OpenAPI document: 0.1.20
 * Contact: nurmi@anchore.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RegistryDigestSource model module.
 * @module model/RegistryDigestSource
 * @version 0.1.20
 */
class RegistryDigestSource {
    /**
     * Constructs a new <code>RegistryDigestSource</code>.
     * An image reference using a digest in a registry, includes some extra tag and timestamp info in addition to the pull string to allow proper tag history reconstruction.
     * @alias module:model/RegistryDigestSource
     * @param pullstring {String} A digest-based pullstring (e.g. docker.io/nginx@sha256:123abc)
     * @param tag {String} A valid docker tag reference (e.g. docker.io/nginx:latest) that will be associated with the image but not used to pull the image.
     */
    constructor(pullstring, tag) { 
        
        RegistryDigestSource.initialize(this, pullstring, tag);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, pullstring, tag) { 
        obj['pullstring'] = pullstring;
        obj['tag'] = tag;
    }

    /**
     * Constructs a <code>RegistryDigestSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RegistryDigestSource} obj Optional instance to populate.
     * @return {module:model/RegistryDigestSource} The populated <code>RegistryDigestSource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RegistryDigestSource();

            if (data.hasOwnProperty('creation_timestamp_override')) {
                obj['creation_timestamp_override'] = ApiClient.convertToType(data['creation_timestamp_override'], 'Date');
            }
            if (data.hasOwnProperty('dockerfile')) {
                obj['dockerfile'] = ApiClient.convertToType(data['dockerfile'], 'String');
            }
            if (data.hasOwnProperty('pullstring')) {
                obj['pullstring'] = ApiClient.convertToType(data['pullstring'], 'String');
            }
            if (data.hasOwnProperty('tag')) {
                obj['tag'] = ApiClient.convertToType(data['tag'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RegistryDigestSource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RegistryDigestSource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RegistryDigestSource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['dockerfile'] && !(typeof data['dockerfile'] === 'string' || data['dockerfile'] instanceof String)) {
            throw new Error("Expected the field `dockerfile` to be a primitive type in the JSON string but got " + data['dockerfile']);
        }
        // ensure the json data is a string
        if (data['pullstring'] && !(typeof data['pullstring'] === 'string' || data['pullstring'] instanceof String)) {
            throw new Error("Expected the field `pullstring` to be a primitive type in the JSON string but got " + data['pullstring']);
        }
        // ensure the json data is a string
        if (data['tag'] && !(typeof data['tag'] === 'string' || data['tag'] instanceof String)) {
            throw new Error("Expected the field `tag` to be a primitive type in the JSON string but got " + data['tag']);
        }

        return true;
    }


}

RegistryDigestSource.RequiredProperties = ["pullstring", "tag"];

/**
 * Optional override of the image creation time to support proper tag history construction in cases of out-of-order analysis compared to registry history for the tag
 * @member {Date} creation_timestamp_override
 */
RegistryDigestSource.prototype['creation_timestamp_override'] = undefined;

/**
 * Base64 encoded content of the dockerfile used to build the image, if available.
 * @member {String} dockerfile
 */
RegistryDigestSource.prototype['dockerfile'] = undefined;

/**
 * A digest-based pullstring (e.g. docker.io/nginx@sha256:123abc)
 * @member {String} pullstring
 */
RegistryDigestSource.prototype['pullstring'] = undefined;

/**
 * A valid docker tag reference (e.g. docker.io/nginx:latest) that will be associated with the image but not used to pull the image.
 * @member {String} tag
 */
RegistryDigestSource.prototype['tag'] = undefined;






export default RegistryDigestSource;

