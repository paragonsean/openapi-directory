/**
 * Anchore Engine API Server
 * This is the Anchore Engine API. Provides the primary external API for users of the service.
 *
 * The version of the OpenAPI document: 0.1.20
 * Contact: nurmi@anchore.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WhitelistItem model module.
 * @module model/WhitelistItem
 * @version 0.1.20
 */
class WhitelistItem {
    /**
     * Constructs a new <code>WhitelistItem</code>.
     * Identifies a specific gate and trigger match from a policy against an image and indicates it should be ignored in final policy decisions
     * @alias module:model/WhitelistItem
     * @param gate {String} 
     * @param triggerId {String} 
     */
    constructor(gate, triggerId) { 
        
        WhitelistItem.initialize(this, gate, triggerId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, gate, triggerId) { 
        obj['gate'] = gate;
        obj['trigger_id'] = triggerId;
    }

    /**
     * Constructs a <code>WhitelistItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WhitelistItem} obj Optional instance to populate.
     * @return {module:model/WhitelistItem} The populated <code>WhitelistItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WhitelistItem();

            if (data.hasOwnProperty('expires_on')) {
                obj['expires_on'] = ApiClient.convertToType(data['expires_on'], 'Date');
            }
            if (data.hasOwnProperty('gate')) {
                obj['gate'] = ApiClient.convertToType(data['gate'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('trigger_id')) {
                obj['trigger_id'] = ApiClient.convertToType(data['trigger_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WhitelistItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WhitelistItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WhitelistItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['gate'] && !(typeof data['gate'] === 'string' || data['gate'] instanceof String)) {
            throw new Error("Expected the field `gate` to be a primitive type in the JSON string but got " + data['gate']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['trigger_id'] && !(typeof data['trigger_id'] === 'string' || data['trigger_id'] instanceof String)) {
            throw new Error("Expected the field `trigger_id` to be a primitive type in the JSON string but got " + data['trigger_id']);
        }

        return true;
    }


}

WhitelistItem.RequiredProperties = ["gate", "trigger_id"];

/**
 * @member {Date} expires_on
 */
WhitelistItem.prototype['expires_on'] = undefined;

/**
 * @member {String} gate
 */
WhitelistItem.prototype['gate'] = undefined;

/**
 * @member {String} id
 */
WhitelistItem.prototype['id'] = undefined;

/**
 * @member {String} trigger_id
 */
WhitelistItem.prototype['trigger_id'] = undefined;






export default WhitelistItem;

