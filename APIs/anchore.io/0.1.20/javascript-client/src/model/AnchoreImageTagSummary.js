/**
 * Anchore Engine API Server
 * This is the Anchore Engine API. Provides the primary external API for users of the service.
 *
 * The version of the OpenAPI document: 0.1.20
 * Contact: nurmi@anchore.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AnchoreImageTagSummary model module.
 * @module model/AnchoreImageTagSummary
 * @version 0.1.20
 */
class AnchoreImageTagSummary {
    /**
     * Constructs a new <code>AnchoreImageTagSummary</code>.
     * A unique image in the engine.
     * @alias module:model/AnchoreImageTagSummary
     */
    constructor() { 
        
        AnchoreImageTagSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AnchoreImageTagSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AnchoreImageTagSummary} obj Optional instance to populate.
     * @return {module:model/AnchoreImageTagSummary} The populated <code>AnchoreImageTagSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AnchoreImageTagSummary();

            if (data.hasOwnProperty('analysis_status')) {
                obj['analysis_status'] = ApiClient.convertToType(data['analysis_status'], 'String');
            }
            if (data.hasOwnProperty('analyzed_at')) {
                obj['analyzed_at'] = ApiClient.convertToType(data['analyzed_at'], 'Number');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Number');
            }
            if (data.hasOwnProperty('fulltag')) {
                obj['fulltag'] = ApiClient.convertToType(data['fulltag'], 'String');
            }
            if (data.hasOwnProperty('imageDigest')) {
                obj['imageDigest'] = ApiClient.convertToType(data['imageDigest'], 'String');
            }
            if (data.hasOwnProperty('imageId')) {
                obj['imageId'] = ApiClient.convertToType(data['imageId'], 'String');
            }
            if (data.hasOwnProperty('image_status')) {
                obj['image_status'] = ApiClient.convertToType(data['image_status'], 'String');
            }
            if (data.hasOwnProperty('parentDigest')) {
                obj['parentDigest'] = ApiClient.convertToType(data['parentDigest'], 'String');
            }
            if (data.hasOwnProperty('tag_detected_at')) {
                obj['tag_detected_at'] = ApiClient.convertToType(data['tag_detected_at'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AnchoreImageTagSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AnchoreImageTagSummary</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['analysis_status'] && !(typeof data['analysis_status'] === 'string' || data['analysis_status'] instanceof String)) {
            throw new Error("Expected the field `analysis_status` to be a primitive type in the JSON string but got " + data['analysis_status']);
        }
        // ensure the json data is a string
        if (data['fulltag'] && !(typeof data['fulltag'] === 'string' || data['fulltag'] instanceof String)) {
            throw new Error("Expected the field `fulltag` to be a primitive type in the JSON string but got " + data['fulltag']);
        }
        // ensure the json data is a string
        if (data['imageDigest'] && !(typeof data['imageDigest'] === 'string' || data['imageDigest'] instanceof String)) {
            throw new Error("Expected the field `imageDigest` to be a primitive type in the JSON string but got " + data['imageDigest']);
        }
        // ensure the json data is a string
        if (data['imageId'] && !(typeof data['imageId'] === 'string' || data['imageId'] instanceof String)) {
            throw new Error("Expected the field `imageId` to be a primitive type in the JSON string but got " + data['imageId']);
        }
        // ensure the json data is a string
        if (data['image_status'] && !(typeof data['image_status'] === 'string' || data['image_status'] instanceof String)) {
            throw new Error("Expected the field `image_status` to be a primitive type in the JSON string but got " + data['image_status']);
        }
        // ensure the json data is a string
        if (data['parentDigest'] && !(typeof data['parentDigest'] === 'string' || data['parentDigest'] instanceof String)) {
            throw new Error("Expected the field `parentDigest` to be a primitive type in the JSON string but got " + data['parentDigest']);
        }

        return true;
    }


}



/**
 * @member {String} analysis_status
 */
AnchoreImageTagSummary.prototype['analysis_status'] = undefined;

/**
 * @member {Number} analyzed_at
 */
AnchoreImageTagSummary.prototype['analyzed_at'] = undefined;

/**
 * @member {Number} created_at
 */
AnchoreImageTagSummary.prototype['created_at'] = undefined;

/**
 * @member {String} fulltag
 */
AnchoreImageTagSummary.prototype['fulltag'] = undefined;

/**
 * @member {String} imageDigest
 */
AnchoreImageTagSummary.prototype['imageDigest'] = undefined;

/**
 * @member {String} imageId
 */
AnchoreImageTagSummary.prototype['imageId'] = undefined;

/**
 * @member {String} image_status
 */
AnchoreImageTagSummary.prototype['image_status'] = undefined;

/**
 * @member {String} parentDigest
 */
AnchoreImageTagSummary.prototype['parentDigest'] = undefined;

/**
 * @member {Number} tag_detected_at
 */
AnchoreImageTagSummary.prototype['tag_detected_at'] = undefined;






export default AnchoreImageTagSummary;

