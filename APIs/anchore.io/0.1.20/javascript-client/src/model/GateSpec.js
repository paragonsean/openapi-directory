/**
 * Anchore Engine API Server
 * This is the Anchore Engine API. Provides the primary external API for users of the service.
 *
 * The version of the OpenAPI document: 0.1.20
 * Contact: nurmi@anchore.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TriggerSpec from './TriggerSpec';

/**
 * The GateSpec model module.
 * @module model/GateSpec
 * @version 0.1.20
 */
class GateSpec {
    /**
     * Constructs a new <code>GateSpec</code>.
     * A description of the set of gates available in this engine and the triggers and parameters supported
     * @alias module:model/GateSpec
     */
    constructor() { 
        
        GateSpec.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GateSpec</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GateSpec} obj Optional instance to populate.
     * @return {module:model/GateSpec} The populated <code>GateSpec</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GateSpec();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('superceded_by')) {
                obj['superceded_by'] = ApiClient.convertToType(data['superceded_by'], 'String');
            }
            if (data.hasOwnProperty('triggers')) {
                obj['triggers'] = ApiClient.convertToType(data['triggers'], [TriggerSpec]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GateSpec</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GateSpec</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['superceded_by'] && !(typeof data['superceded_by'] === 'string' || data['superceded_by'] instanceof String)) {
            throw new Error("Expected the field `superceded_by` to be a primitive type in the JSON string but got " + data['superceded_by']);
        }
        if (data['triggers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['triggers'])) {
                throw new Error("Expected the field `triggers` to be an array in the JSON data but got " + data['triggers']);
            }
            // validate the optional field `triggers` (array)
            for (const item of data['triggers']) {
                TriggerSpec.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Description of the gate
 * @member {String} description
 */
GateSpec.prototype['description'] = undefined;

/**
 * Gate name, as it would appear in a policy document
 * @member {String} name
 */
GateSpec.prototype['name'] = undefined;

/**
 * State of the gate and transitively all triggers it contains if not 'active'
 * @member {module:model/GateSpec.StateEnum} state
 */
GateSpec.prototype['state'] = undefined;

/**
 * The name of another trigger that supercedes this on functionally if this is deprecated
 * @member {String} superceded_by
 */
GateSpec.prototype['superceded_by'] = undefined;

/**
 * List of the triggers that can fire for this Gate
 * @member {Array.<module:model/TriggerSpec>} triggers
 */
GateSpec.prototype['triggers'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
GateSpec['StateEnum'] = {

    /**
     * value: "active"
     * @const
     */
    "active": "active",

    /**
     * value: "deprecated"
     * @const
     */
    "deprecated": "deprecated",

    /**
     * value: "eol"
     * @const
     */
    "eol": "eol"
};



export default GateSpec;

