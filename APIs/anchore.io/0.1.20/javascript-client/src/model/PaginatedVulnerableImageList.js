/**
 * Anchore Engine API Server
 * This is the Anchore Engine API. Provides the primary external API for users of the service.
 *
 * The version of the OpenAPI document: 0.1.20
 * Contact: nurmi@anchore.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PaginationProperties from './PaginationProperties';
import VulnerableImage from './VulnerableImage';

/**
 * The PaginatedVulnerableImageList model module.
 * @module model/PaginatedVulnerableImageList
 * @version 0.1.20
 */
class PaginatedVulnerableImageList {
    /**
     * Constructs a new <code>PaginatedVulnerableImageList</code>.
     * Pagination wrapped list of images with vulnerabilties that match some filter
     * @alias module:model/PaginatedVulnerableImageList
     * @implements module:model/PaginationProperties
     */
    constructor() { 
        PaginationProperties.initialize(this);
        PaginatedVulnerableImageList.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PaginatedVulnerableImageList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaginatedVulnerableImageList} obj Optional instance to populate.
     * @return {module:model/PaginatedVulnerableImageList} The populated <code>PaginatedVulnerableImageList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaginatedVulnerableImageList();
            PaginationProperties.constructFromObject(data, obj);

            if (data.hasOwnProperty('next_page')) {
                obj['next_page'] = ApiClient.convertToType(data['next_page'], 'String');
            }
            if (data.hasOwnProperty('page')) {
                obj['page'] = ApiClient.convertToType(data['page'], 'String');
            }
            if (data.hasOwnProperty('returned_count')) {
                obj['returned_count'] = ApiClient.convertToType(data['returned_count'], 'Number');
            }
            if (data.hasOwnProperty('images')) {
                obj['images'] = ApiClient.convertToType(data['images'], [VulnerableImage]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaginatedVulnerableImageList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaginatedVulnerableImageList</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['next_page'] && !(typeof data['next_page'] === 'string' || data['next_page'] instanceof String)) {
            throw new Error("Expected the field `next_page` to be a primitive type in the JSON string but got " + data['next_page']);
        }
        // ensure the json data is a string
        if (data['page'] && !(typeof data['page'] === 'string' || data['page'] instanceof String)) {
            throw new Error("Expected the field `page` to be a primitive type in the JSON string but got " + data['page']);
        }
        if (data['images']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['images'])) {
                throw new Error("Expected the field `images` to be an array in the JSON data but got " + data['images']);
            }
            // validate the optional field `images` (array)
            for (const item of data['images']) {
                VulnerableImage.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * True if additional pages exist (page + 1) or False if this is the last page
 * @member {String} next_page
 */
PaginatedVulnerableImageList.prototype['next_page'] = undefined;

/**
 * The page number returned (should match the requested page query string param)
 * @member {String} page
 */
PaginatedVulnerableImageList.prototype['page'] = undefined;

/**
 * The number of items sent in this response
 * @member {Number} returned_count
 */
PaginatedVulnerableImageList.prototype['returned_count'] = undefined;

/**
 * @member {Array.<module:model/VulnerableImage>} images
 */
PaginatedVulnerableImageList.prototype['images'] = undefined;


// Implement PaginationProperties interface:
/**
 * True if additional pages exist (page + 1) or False if this is the last page
 * @member {String} next_page
 */
PaginationProperties.prototype['next_page'] = undefined;
/**
 * The page number returned (should match the requested page query string param)
 * @member {String} page
 */
PaginationProperties.prototype['page'] = undefined;
/**
 * The number of items sent in this response
 * @member {Number} returned_count
 */
PaginationProperties.prototype['returned_count'] = undefined;




export default PaginatedVulnerableImageList;

