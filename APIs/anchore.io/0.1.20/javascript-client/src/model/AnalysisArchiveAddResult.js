/**
 * Anchore Engine API Server
 * This is the Anchore Engine API. Provides the primary external API for users of the service.
 *
 * The version of the OpenAPI document: 0.1.20
 * Contact: nurmi@anchore.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AnalysisArchiveAddResult model module.
 * @module model/AnalysisArchiveAddResult
 * @version 0.1.20
 */
class AnalysisArchiveAddResult {
    /**
     * Constructs a new <code>AnalysisArchiveAddResult</code>.
     * The result of adding a single digest to the archive
     * @alias module:model/AnalysisArchiveAddResult
     */
    constructor() { 
        
        AnalysisArchiveAddResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AnalysisArchiveAddResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AnalysisArchiveAddResult} obj Optional instance to populate.
     * @return {module:model/AnalysisArchiveAddResult} The populated <code>AnalysisArchiveAddResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AnalysisArchiveAddResult();

            if (data.hasOwnProperty('detail')) {
                obj['detail'] = ApiClient.convertToType(data['detail'], 'String');
            }
            if (data.hasOwnProperty('digest')) {
                obj['digest'] = ApiClient.convertToType(data['digest'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AnalysisArchiveAddResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AnalysisArchiveAddResult</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['detail'] && !(typeof data['detail'] === 'string' || data['detail'] instanceof String)) {
            throw new Error("Expected the field `detail` to be a primitive type in the JSON string but got " + data['detail']);
        }
        // ensure the json data is a string
        if (data['digest'] && !(typeof data['digest'] === 'string' || data['digest'] instanceof String)) {
            throw new Error("Expected the field `digest` to be a primitive type in the JSON string but got " + data['digest']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}



/**
 * Details on the status, e.g. the error message
 * @member {String} detail
 */
AnalysisArchiveAddResult.prototype['detail'] = undefined;

/**
 * The image digest requested to be added
 * @member {String} digest
 */
AnalysisArchiveAddResult.prototype['digest'] = undefined;

/**
 * The status of the archive add operation. Typically either 'archived' or 'error'
 * @member {module:model/AnalysisArchiveAddResult.StatusEnum} status
 */
AnalysisArchiveAddResult.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
AnalysisArchiveAddResult['StatusEnum'] = {

    /**
     * value: "archived"
     * @const
     */
    "archived": "archived",

    /**
     * value: "archiving"
     * @const
     */
    "archiving": "archiving",

    /**
     * value: "error"
     * @const
     */
    "error": "error"
};



export default AnalysisArchiveAddResult;

