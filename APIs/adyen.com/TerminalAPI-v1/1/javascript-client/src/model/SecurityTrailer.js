/**
 * Adyen Terminal API
 * The Adyen Terminal API lets you make payments, issue refunds, collect shopper information, and perform other shopper-terminal interactions using a payment terminal supplied by Adyen.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SecurityTrailer model module.
 * @module model/SecurityTrailer
 * @version 1
 */
class SecurityTrailer {
    /**
     * Constructs a new <code>SecurityTrailer</code>.
     * It contains information related to the security of the message. SecurityTrailer as used by Adyen.
     * @alias module:model/SecurityTrailer
     * @param adyenCryptoVersion {Number} 
     * @param hmac {String} 
     * @param keyIdentifier {String} 
     * @param keyVersion {Number} 
     * @param nonce {String} 
     */
    constructor(adyenCryptoVersion, hmac, keyIdentifier, keyVersion, nonce) { 
        
        SecurityTrailer.initialize(this, adyenCryptoVersion, hmac, keyIdentifier, keyVersion, nonce);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, adyenCryptoVersion, hmac, keyIdentifier, keyVersion, nonce) { 
        obj['AdyenCryptoVersion'] = adyenCryptoVersion;
        obj['Hmac'] = hmac;
        obj['KeyIdentifier'] = keyIdentifier;
        obj['KeyVersion'] = keyVersion;
        obj['Nonce'] = nonce;
    }

    /**
     * Constructs a <code>SecurityTrailer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SecurityTrailer} obj Optional instance to populate.
     * @return {module:model/SecurityTrailer} The populated <code>SecurityTrailer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SecurityTrailer();

            if (data.hasOwnProperty('AdyenCryptoVersion')) {
                obj['AdyenCryptoVersion'] = ApiClient.convertToType(data['AdyenCryptoVersion'], 'Number');
            }
            if (data.hasOwnProperty('Hmac')) {
                obj['Hmac'] = ApiClient.convertToType(data['Hmac'], 'String');
            }
            if (data.hasOwnProperty('KeyIdentifier')) {
                obj['KeyIdentifier'] = ApiClient.convertToType(data['KeyIdentifier'], 'String');
            }
            if (data.hasOwnProperty('KeyVersion')) {
                obj['KeyVersion'] = ApiClient.convertToType(data['KeyVersion'], 'Number');
            }
            if (data.hasOwnProperty('Nonce')) {
                obj['Nonce'] = ApiClient.convertToType(data['Nonce'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SecurityTrailer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SecurityTrailer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SecurityTrailer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Hmac'] && !(typeof data['Hmac'] === 'string' || data['Hmac'] instanceof String)) {
            throw new Error("Expected the field `Hmac` to be a primitive type in the JSON string but got " + data['Hmac']);
        }
        // ensure the json data is a string
        if (data['KeyIdentifier'] && !(typeof data['KeyIdentifier'] === 'string' || data['KeyIdentifier'] instanceof String)) {
            throw new Error("Expected the field `KeyIdentifier` to be a primitive type in the JSON string but got " + data['KeyIdentifier']);
        }
        // ensure the json data is a string
        if (data['Nonce'] && !(typeof data['Nonce'] === 'string' || data['Nonce'] instanceof String)) {
            throw new Error("Expected the field `Nonce` to be a primitive type in the JSON string but got " + data['Nonce']);
        }

        return true;
    }


}

SecurityTrailer.RequiredProperties = ["AdyenCryptoVersion", "Hmac", "KeyIdentifier", "KeyVersion", "Nonce"];

/**
 * @member {Number} AdyenCryptoVersion
 */
SecurityTrailer.prototype['AdyenCryptoVersion'] = undefined;

/**
 * @member {String} Hmac
 */
SecurityTrailer.prototype['Hmac'] = undefined;

/**
 * @member {String} KeyIdentifier
 */
SecurityTrailer.prototype['KeyIdentifier'] = undefined;

/**
 * @member {Number} KeyVersion
 */
SecurityTrailer.prototype['KeyVersion'] = undefined;

/**
 * @member {String} Nonce
 */
SecurityTrailer.prototype['Nonce'] = undefined;






export default SecurityTrailer;

