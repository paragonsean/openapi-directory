/**
 * Adyen Terminal API
 * The Adyen Terminal API lets you make payments, issue refunds, collect shopper information, and perform other shopper-terminal interactions using a payment terminal supplied by Adyen.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Response from './Response';
import TransactionTotals from './TransactionTotals';

/**
 * The GetTotalsResponse model module.
 * @module model/GetTotalsResponse
 * @version 1
 */
class GetTotalsResponse {
    /**
     * Constructs a new <code>GetTotalsResponse</code>.
     * It conveys Information related to the Reconciliation transaction processed by the POI System. Content of the Reconciliation Response message.
     * @alias module:model/GetTotalsResponse
     * @param pOIReconciliationID {Number} Identification of the reconciliation period between Sale and POI.
     * @param response {module:model/Response} 
     */
    constructor(pOIReconciliationID, response) { 
        
        GetTotalsResponse.initialize(this, pOIReconciliationID, response);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, pOIReconciliationID, response) { 
        obj['POIReconciliationID'] = pOIReconciliationID;
        obj['Response'] = response;
    }

    /**
     * Constructs a <code>GetTotalsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetTotalsResponse} obj Optional instance to populate.
     * @return {module:model/GetTotalsResponse} The populated <code>GetTotalsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetTotalsResponse();

            if (data.hasOwnProperty('POIReconciliationID')) {
                obj['POIReconciliationID'] = ApiClient.convertToType(data['POIReconciliationID'], 'Number');
            }
            if (data.hasOwnProperty('Response')) {
                obj['Response'] = Response.constructFromObject(data['Response']);
            }
            if (data.hasOwnProperty('TransactionTotals')) {
                obj['TransactionTotals'] = ApiClient.convertToType(data['TransactionTotals'], [TransactionTotals]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetTotalsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetTotalsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetTotalsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `Response`
        if (data['Response']) { // data not null
          Response.validateJSON(data['Response']);
        }
        if (data['TransactionTotals']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['TransactionTotals'])) {
                throw new Error("Expected the field `TransactionTotals` to be an array in the JSON data but got " + data['TransactionTotals']);
            }
            // validate the optional field `TransactionTotals` (array)
            for (const item of data['TransactionTotals']) {
                TransactionTotals.validateJSON(item);
            };
        }

        return true;
    }


}

GetTotalsResponse.RequiredProperties = ["POIReconciliationID", "Response"];

/**
 * Identification of the reconciliation period between Sale and POI.
 * @member {Number} POIReconciliationID
 */
GetTotalsResponse.prototype['POIReconciliationID'] = undefined;

/**
 * @member {module:model/Response} Response
 */
GetTotalsResponse.prototype['Response'] = undefined;

/**
 * @member {Array.<module:model/TransactionTotals>} TransactionTotals
 */
GetTotalsResponse.prototype['TransactionTotals'] = undefined;






export default GetTotalsResponse;

