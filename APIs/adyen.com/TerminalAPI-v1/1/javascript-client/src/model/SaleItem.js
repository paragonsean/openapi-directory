/**
 * Adyen Terminal API
 * The Adyen Terminal API lets you make payments, issue refunds, collect shopper information, and perform other shopper-terminal interactions using a payment terminal supplied by Adyen.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UnitOfMeasure from './UnitOfMeasure';

/**
 * The SaleItem model module.
 * @module model/SaleItem
 * @version 1
 */
class SaleItem {
    /**
     * Constructs a new <code>SaleItem</code>.
     * In loyalty or value added payment card transaction, the items of the sale are entering in the processing of the transaction. Sale items of a transaction.
     * @alias module:model/SaleItem
     * @param itemAmount {Number} Total amount of the item line.
     * @param itemID {Number} Item identification inside a transaction (0 to n).
     * @param productCode {Number} Product code of item purchased with the transaction.
     */
    constructor(itemAmount, itemID, productCode) { 
        
        SaleItem.initialize(this, itemAmount, itemID, productCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, itemAmount, itemID, productCode) { 
        obj['ItemAmount'] = itemAmount;
        obj['ItemID'] = itemID;
        obj['ProductCode'] = productCode;
    }

    /**
     * Constructs a <code>SaleItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SaleItem} obj Optional instance to populate.
     * @return {module:model/SaleItem} The populated <code>SaleItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SaleItem();

            if (data.hasOwnProperty('AdditionalProductInfo')) {
                obj['AdditionalProductInfo'] = ApiClient.convertToType(data['AdditionalProductInfo'], 'String');
            }
            if (data.hasOwnProperty('EanUpc')) {
                obj['EanUpc'] = ApiClient.convertToType(data['EanUpc'], 'Number');
            }
            if (data.hasOwnProperty('ItemAmount')) {
                obj['ItemAmount'] = ApiClient.convertToType(data['ItemAmount'], 'Number');
            }
            if (data.hasOwnProperty('ItemID')) {
                obj['ItemID'] = ApiClient.convertToType(data['ItemID'], 'Number');
            }
            if (data.hasOwnProperty('ProductCode')) {
                obj['ProductCode'] = ApiClient.convertToType(data['ProductCode'], 'Number');
            }
            if (data.hasOwnProperty('ProductLabel')) {
                obj['ProductLabel'] = ApiClient.convertToType(data['ProductLabel'], 'String');
            }
            if (data.hasOwnProperty('Quantity')) {
                obj['Quantity'] = ApiClient.convertToType(data['Quantity'], 'String');
            }
            if (data.hasOwnProperty('SaleChannel')) {
                obj['SaleChannel'] = ApiClient.convertToType(data['SaleChannel'], 'Number');
            }
            if (data.hasOwnProperty('TaxCode')) {
                obj['TaxCode'] = ApiClient.convertToType(data['TaxCode'], 'Number');
            }
            if (data.hasOwnProperty('UnitOfMeasure')) {
                obj['UnitOfMeasure'] = UnitOfMeasure.constructFromObject(data['UnitOfMeasure']);
            }
            if (data.hasOwnProperty('UnitPrice')) {
                obj['UnitPrice'] = ApiClient.convertToType(data['UnitPrice'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SaleItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SaleItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SaleItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['AdditionalProductInfo'] && !(typeof data['AdditionalProductInfo'] === 'string' || data['AdditionalProductInfo'] instanceof String)) {
            throw new Error("Expected the field `AdditionalProductInfo` to be a primitive type in the JSON string but got " + data['AdditionalProductInfo']);
        }
        // ensure the json data is a string
        if (data['ProductLabel'] && !(typeof data['ProductLabel'] === 'string' || data['ProductLabel'] instanceof String)) {
            throw new Error("Expected the field `ProductLabel` to be a primitive type in the JSON string but got " + data['ProductLabel']);
        }
        // ensure the json data is a string
        if (data['Quantity'] && !(typeof data['Quantity'] === 'string' || data['Quantity'] instanceof String)) {
            throw new Error("Expected the field `Quantity` to be a primitive type in the JSON string but got " + data['Quantity']);
        }

        return true;
    }


}

SaleItem.RequiredProperties = ["ItemAmount", "ItemID", "ProductCode"];

/**
 * If data sent, POI has to store it and send it if the host protocol allows it.
 * @member {String} AdditionalProductInfo
 */
SaleItem.prototype['AdditionalProductInfo'] = undefined;

/**
 * If data sent, POI has to store it and send it if the host protocol allows it.
 * @member {Number} EanUpc
 */
SaleItem.prototype['EanUpc'] = undefined;

/**
 * Total amount of the item line.
 * @member {Number} ItemAmount
 */
SaleItem.prototype['ItemAmount'] = undefined;

/**
 * Item identification inside a transaction (0 to n).
 * @member {Number} ItemID
 */
SaleItem.prototype['ItemID'] = undefined;

/**
 * Product code of item purchased with the transaction.
 * @member {Number} ProductCode
 */
SaleItem.prototype['ProductCode'] = undefined;

/**
 * @member {String} ProductLabel
 */
SaleItem.prototype['ProductLabel'] = undefined;

/**
 * If data sent, POI has to store it and send it if the host protocol allows it.
 * @member {String} Quantity
 */
SaleItem.prototype['Quantity'] = undefined;

/**
 * If data sent, POI has to store it and send it if the host protocol allows it.
 * @member {Number} SaleChannel
 */
SaleItem.prototype['SaleChannel'] = undefined;

/**
 * If data sent, POI has to store it and send it if the host protocol allows it.
 * @member {Number} TaxCode
 */
SaleItem.prototype['TaxCode'] = undefined;

/**
 * @member {module:model/UnitOfMeasure} UnitOfMeasure
 */
SaleItem.prototype['UnitOfMeasure'] = undefined;

/**
 * if Quantity present.
 * @member {Number} UnitPrice
 */
SaleItem.prototype['UnitPrice'] = undefined;






export default SaleItem;

