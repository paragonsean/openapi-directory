/**
 * Adyen Terminal API
 * The Adyen Terminal API lets you make payments, issue refunds, collect shopper information, and perform other shopper-terminal interactions using a payment terminal supplied by Adyen.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReconciliationType from './ReconciliationType';
import Response from './Response';
import TransactionTotals from './TransactionTotals';

/**
 * The ReconciliationResponse model module.
 * @module model/ReconciliationResponse
 * @version 1
 */
class ReconciliationResponse {
    /**
     * Constructs a new <code>ReconciliationResponse</code>.
     * It conveys Information related to the Reconciliation transaction processed by the POI System. Content of the Reconciliation Response message.
     * @alias module:model/ReconciliationResponse
     * @param reconciliationType {module:model/ReconciliationType} 
     * @param response {module:model/Response} 
     */
    constructor(reconciliationType, response) { 
        
        ReconciliationResponse.initialize(this, reconciliationType, response);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, reconciliationType, response) { 
        obj['ReconciliationType'] = reconciliationType;
        obj['Response'] = response;
    }

    /**
     * Constructs a <code>ReconciliationResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReconciliationResponse} obj Optional instance to populate.
     * @return {module:model/ReconciliationResponse} The populated <code>ReconciliationResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReconciliationResponse();

            if (data.hasOwnProperty('POIReconciliationID')) {
                obj['POIReconciliationID'] = ApiClient.convertToType(data['POIReconciliationID'], 'Number');
            }
            if (data.hasOwnProperty('ReconciliationType')) {
                obj['ReconciliationType'] = ReconciliationType.constructFromObject(data['ReconciliationType']);
            }
            if (data.hasOwnProperty('Response')) {
                obj['Response'] = Response.constructFromObject(data['Response']);
            }
            if (data.hasOwnProperty('TransactionTotals')) {
                obj['TransactionTotals'] = ApiClient.convertToType(data['TransactionTotals'], [TransactionTotals]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReconciliationResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReconciliationResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReconciliationResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `Response`
        if (data['Response']) { // data not null
          Response.validateJSON(data['Response']);
        }
        if (data['TransactionTotals']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['TransactionTotals'])) {
                throw new Error("Expected the field `TransactionTotals` to be an array in the JSON data but got " + data['TransactionTotals']);
            }
            // validate the optional field `TransactionTotals` (array)
            for (const item of data['TransactionTotals']) {
                TransactionTotals.validateJSON(item);
            };
        }

        return true;
    }


}

ReconciliationResponse.RequiredProperties = ["ReconciliationType", "Response"];

/**
 * Absent if ReconciliationType is AcquirerReconciliation.
 * @member {Number} POIReconciliationID
 */
ReconciliationResponse.prototype['POIReconciliationID'] = undefined;

/**
 * @member {module:model/ReconciliationType} ReconciliationType
 */
ReconciliationResponse.prototype['ReconciliationType'] = undefined;

/**
 * @member {module:model/Response} Response
 */
ReconciliationResponse.prototype['Response'] = undefined;

/**
 * @member {Array.<module:model/TransactionTotals>} TransactionTotals
 */
ReconciliationResponse.prototype['TransactionTotals'] = undefined;






export default ReconciliationResponse;

