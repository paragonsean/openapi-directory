/**
 * POS Terminal Management API
 * This API provides endpoints for managing your point-of-sale (POS) payment terminals. You can use the API to obtain information about a specific terminal, retrieve overviews of your terminals and stores, and assign terminals to a merchant account or store.  For more information, refer to [Assign terminals](https://docs.adyen.com/point-of-sale/automating-terminal-management/assign-terminals-api).  ## Authentication Each request to the Terminal Management API must be signed with an API key. For this, obtain an API Key from your Customer Area, as described in [How to get the API key](https://docs.adyen.com/development-resources/api-credentials#generate-api-key). Then set this key to the `X-API-Key` header value, for example:  ``` curl -H \"Content-Type: application/json\" \\ -H \"X-API-Key: Your_API_key\" \\ ... ``` Note that when going live, you need to generate new web service user credentials to access the [live endpoints](https://docs.adyen.com/development-resources/live-endpoints).  ## Versioning Terminal Management API supports [versioning](https://docs.adyen.com/development-resources/versioning) using a version suffix in the endpoint URL. This suffix has the following format: \"vXX\", where XX is the version number.  For example: ``` https://postfmapi-test.adyen.com/postfmapi/terminal/v1/getTerminalsUnderAccount ``` When using versioned endpoints, Boolean response values are returned in string format: `\"true\"` or `\"false\"`. If you omit the version from the endpoint URL, Boolean response values are returned like this: `true` or `false`.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Store from './Store';

/**
 * The MerchantAccount model module.
 * @module model/MerchantAccount
 * @version 1
 */
class MerchantAccount {
    /**
     * Constructs a new <code>MerchantAccount</code>.
     * @alias module:model/MerchantAccount
     * @param merchantAccount {String} The merchant account.
     */
    constructor(merchantAccount) { 
        
        MerchantAccount.initialize(this, merchantAccount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, merchantAccount) { 
        obj['merchantAccount'] = merchantAccount;
    }

    /**
     * Constructs a <code>MerchantAccount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MerchantAccount} obj Optional instance to populate.
     * @return {module:model/MerchantAccount} The populated <code>MerchantAccount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MerchantAccount();

            if (data.hasOwnProperty('inStoreTerminals')) {
                obj['inStoreTerminals'] = ApiClient.convertToType(data['inStoreTerminals'], ['String']);
            }
            if (data.hasOwnProperty('inventoryTerminals')) {
                obj['inventoryTerminals'] = ApiClient.convertToType(data['inventoryTerminals'], ['String']);
            }
            if (data.hasOwnProperty('merchantAccount')) {
                obj['merchantAccount'] = ApiClient.convertToType(data['merchantAccount'], 'String');
            }
            if (data.hasOwnProperty('stores')) {
                obj['stores'] = ApiClient.convertToType(data['stores'], [Store]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MerchantAccount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MerchantAccount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MerchantAccount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['inStoreTerminals'])) {
            throw new Error("Expected the field `inStoreTerminals` to be an array in the JSON data but got " + data['inStoreTerminals']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['inventoryTerminals'])) {
            throw new Error("Expected the field `inventoryTerminals` to be an array in the JSON data but got " + data['inventoryTerminals']);
        }
        // ensure the json data is a string
        if (data['merchantAccount'] && !(typeof data['merchantAccount'] === 'string' || data['merchantAccount'] instanceof String)) {
            throw new Error("Expected the field `merchantAccount` to be a primitive type in the JSON string but got " + data['merchantAccount']);
        }
        if (data['stores']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stores'])) {
                throw new Error("Expected the field `stores` to be an array in the JSON data but got " + data['stores']);
            }
            // validate the optional field `stores` (array)
            for (const item of data['stores']) {
                Store.validateJSON(item);
            };
        }

        return true;
    }


}

MerchantAccount.RequiredProperties = ["merchantAccount"];

/**
 * List of terminals assigned to this merchant account as in-store terminals. This means that the terminal is ready to be boarded, or is already boarded.
 * @member {Array.<String>} inStoreTerminals
 */
MerchantAccount.prototype['inStoreTerminals'] = undefined;

/**
 * List of terminals assigned to the inventory of this merchant account.
 * @member {Array.<String>} inventoryTerminals
 */
MerchantAccount.prototype['inventoryTerminals'] = undefined;

/**
 * The merchant account.
 * @member {String} merchantAccount
 */
MerchantAccount.prototype['merchantAccount'] = undefined;

/**
 * Array of stores under this merchant account.
 * @member {Array.<module:model/Store>} stores
 */
MerchantAccount.prototype['stores'] = undefined;






export default MerchantAccount;

