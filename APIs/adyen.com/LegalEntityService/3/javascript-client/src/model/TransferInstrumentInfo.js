/**
 * Legal Entity Management API
 * The Legal Entity Management API enables you to manage legal entities that contain information required for verification.  ## Authentication Your Adyen contact will provide your API credential and an API key. To connect to the API, add an `X-API-Key` header with the API key as the value. For example:  ``` curl -H \"X-API-Key: YOUR_API_KEY\" \\ -H \"Content-Type: application/json\" \\ ... ``` Alternatively, you can use the username and password of your API credential to connect to the API using basic authentication. For example:  ``` curl -U \"ws_123456@Scope.Company_YOUR_COMPANY_ACCOUNT\":\"YourWsPassword\" \\ -H \"Content-Type: application/json\" \\ ... ``` ## Versioning The Legal Entity Management API supports [versioning](https://docs.adyen.com/development-resources/versioning) using a version suffix in the endpoint URL. This suffix has the following format: \"vXX\", where XX is the version number.  For example: ``` https://kyc-test.adyen.com/lem/v3/legalEntities ``` >If you are using hosted onboarding and just beginning your integration, use v3 for your API requests. Otherwise, use v2.  ## Going live When going live, your Adyen contact will provide your API credential for the live environment. You can then use the API key or the username and password to send requests to `https://kyc-live.adyen.com/lem/v3`.  
 *
 * The version of the OpenAPI document: 3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BankAccountInfo from './BankAccountInfo';

/**
 * The TransferInstrumentInfo model module.
 * @module model/TransferInstrumentInfo
 * @version 3
 */
class TransferInstrumentInfo {
    /**
     * Constructs a new <code>TransferInstrumentInfo</code>.
     * @alias module:model/TransferInstrumentInfo
     * @param bankAccount {module:model/BankAccountInfo} Contains information about the legal entity's bank account.
     * @param legalEntityId {String} The unique identifier of the [legal entity](https://docs.adyen.com/api-explorer/legalentity/latest/post/legalEntities#responses-200-id) that owns the transfer instrument.
     * @param type {module:model/TransferInstrumentInfo.TypeEnum} The type of transfer instrument.  Possible value: **bankAccount**.
     */
    constructor(bankAccount, legalEntityId, type) { 
        
        TransferInstrumentInfo.initialize(this, bankAccount, legalEntityId, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bankAccount, legalEntityId, type) { 
        obj['bankAccount'] = bankAccount;
        obj['legalEntityId'] = legalEntityId;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>TransferInstrumentInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransferInstrumentInfo} obj Optional instance to populate.
     * @return {module:model/TransferInstrumentInfo} The populated <code>TransferInstrumentInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransferInstrumentInfo();

            if (data.hasOwnProperty('bankAccount')) {
                obj['bankAccount'] = BankAccountInfo.constructFromObject(data['bankAccount']);
            }
            if (data.hasOwnProperty('legalEntityId')) {
                obj['legalEntityId'] = ApiClient.convertToType(data['legalEntityId'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransferInstrumentInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransferInstrumentInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransferInstrumentInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `bankAccount`
        if (data['bankAccount']) { // data not null
          BankAccountInfo.validateJSON(data['bankAccount']);
        }
        // ensure the json data is a string
        if (data['legalEntityId'] && !(typeof data['legalEntityId'] === 'string' || data['legalEntityId'] instanceof String)) {
            throw new Error("Expected the field `legalEntityId` to be a primitive type in the JSON string but got " + data['legalEntityId']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

TransferInstrumentInfo.RequiredProperties = ["bankAccount", "legalEntityId", "type"];

/**
 * Contains information about the legal entity's bank account.
 * @member {module:model/BankAccountInfo} bankAccount
 */
TransferInstrumentInfo.prototype['bankAccount'] = undefined;

/**
 * The unique identifier of the [legal entity](https://docs.adyen.com/api-explorer/legalentity/latest/post/legalEntities#responses-200-id) that owns the transfer instrument.
 * @member {String} legalEntityId
 */
TransferInstrumentInfo.prototype['legalEntityId'] = undefined;

/**
 * The type of transfer instrument.  Possible value: **bankAccount**.
 * @member {module:model/TransferInstrumentInfo.TypeEnum} type
 */
TransferInstrumentInfo.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
TransferInstrumentInfo['TypeEnum'] = {

    /**
     * value: "bankAccount"
     * @const
     */
    "bankAccount": "bankAccount",

    /**
     * value: "recurringDetail"
     * @const
     */
    "recurringDetail": "recurringDetail"
};



export default TransferInstrumentInfo;

