/**
 * Management API
 * Configure and manage your Adyen company and merchant accounts, stores, and payment terminals. ## Authentication Each request to the Management API must be signed with an API key. [Generate your API key](https://docs.adyen.com/development-resources/api-credentials#generate-api-key) in the Customer Area and then set this key to the `X-API-Key` header value.  To access the live endpoints, you need to generate a new API key in your live Customer Area. ## Versioning  Management API handles versioning as part of the endpoint URL. For example, to send a request to this version of the `/companies/{companyId}/webhooks` endpoint, use:  ```text https://management-test.adyen.com/v3/companies/{companyId}/webhooks ```  ## Going live  To access the live endpoints, you need an API key from your live Customer Area. Use this API key to make requests to:  ```text https://management-live.adyen.com/v3 ```  ## Release notes Have a look at the [release notes](https://docs.adyen.com/release-notes/management-api) to find out what changed in this version!
 *
 * The version of the OpenAPI document: 3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Passcodes model module.
 * @module model/Passcodes
 * @version 3
 */
class Passcodes {
    /**
     * Constructs a new <code>Passcodes</code>.
     * @alias module:model/Passcodes
     */
    constructor() { 
        
        Passcodes.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Passcodes</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Passcodes} obj Optional instance to populate.
     * @return {module:model/Passcodes} The populated <code>Passcodes</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Passcodes();

            if (data.hasOwnProperty('adminMenuPin')) {
                obj['adminMenuPin'] = ApiClient.convertToType(data['adminMenuPin'], 'String');
            }
            if (data.hasOwnProperty('refundPin')) {
                obj['refundPin'] = ApiClient.convertToType(data['refundPin'], 'String');
            }
            if (data.hasOwnProperty('screenLockPin')) {
                obj['screenLockPin'] = ApiClient.convertToType(data['screenLockPin'], 'String');
            }
            if (data.hasOwnProperty('txMenuPin')) {
                obj['txMenuPin'] = ApiClient.convertToType(data['txMenuPin'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Passcodes</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Passcodes</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['adminMenuPin'] && !(typeof data['adminMenuPin'] === 'string' || data['adminMenuPin'] instanceof String)) {
            throw new Error("Expected the field `adminMenuPin` to be a primitive type in the JSON string but got " + data['adminMenuPin']);
        }
        // ensure the json data is a string
        if (data['refundPin'] && !(typeof data['refundPin'] === 'string' || data['refundPin'] instanceof String)) {
            throw new Error("Expected the field `refundPin` to be a primitive type in the JSON string but got " + data['refundPin']);
        }
        // ensure the json data is a string
        if (data['screenLockPin'] && !(typeof data['screenLockPin'] === 'string' || data['screenLockPin'] instanceof String)) {
            throw new Error("Expected the field `screenLockPin` to be a primitive type in the JSON string but got " + data['screenLockPin']);
        }
        // ensure the json data is a string
        if (data['txMenuPin'] && !(typeof data['txMenuPin'] === 'string' || data['txMenuPin'] instanceof String)) {
            throw new Error("Expected the field `txMenuPin` to be a primitive type in the JSON string but got " + data['txMenuPin']);
        }

        return true;
    }


}



/**
 * The passcode for the Admin menu and the Settings menu.
 * @member {String} adminMenuPin
 */
Passcodes.prototype['adminMenuPin'] = undefined;

/**
 * The passcode for referenced and unreferenced refunds on standalone terminals.
 * @member {String} refundPin
 */
Passcodes.prototype['refundPin'] = undefined;

/**
 * The passcode to unlock the terminal screen after a timeout.
 * @member {String} screenLockPin
 */
Passcodes.prototype['screenLockPin'] = undefined;

/**
 * The passcode for the Transactions menu.
 * @member {String} txMenuPin
 */
Passcodes.prototype['txMenuPin'] = undefined;






export default Passcodes;

