/**
 * Management API
 * Configure and manage your Adyen company and merchant accounts, stores, and payment terminals. ## Authentication Each request to the Management API must be signed with an API key. [Generate your API key](https://docs.adyen.com/development-resources/api-credentials#generate-api-key) in the Customer Area and then set this key to the `X-API-Key` header value.  To access the live endpoints, you need to generate a new API key in your live Customer Area. ## Versioning  Management API handles versioning as part of the endpoint URL. For example, to send a request to this version of the `/companies/{companyId}/webhooks` endpoint, use:  ```text https://management-test.adyen.com/v3/companies/{companyId}/webhooks ```  ## Going live  To access the live endpoints, you need an API key from your live Customer Area. Use this API key to make requests to:  ```text https://management-live.adyen.com/v3 ```  ## Release notes Have a look at the [release notes](https://docs.adyen.com/release-notes/management-api) to find out what changed in this version!
 *
 * The version of the OpenAPI document: 3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LinksElement from './LinksElement';

/**
 * The PaginationLinks model module.
 * @module model/PaginationLinks
 * @version 3
 */
class PaginationLinks {
    /**
     * Constructs a new <code>PaginationLinks</code>.
     * @alias module:model/PaginationLinks
     * @param first {module:model/LinksElement} The first page.
     * @param last {module:model/LinksElement} The last page.
     * @param self {module:model/LinksElement} The current page.
     */
    constructor(first, last, self) { 
        
        PaginationLinks.initialize(this, first, last, self);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, first, last, self) { 
        obj['first'] = first;
        obj['last'] = last;
        obj['self'] = self;
    }

    /**
     * Constructs a <code>PaginationLinks</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaginationLinks} obj Optional instance to populate.
     * @return {module:model/PaginationLinks} The populated <code>PaginationLinks</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaginationLinks();

            if (data.hasOwnProperty('first')) {
                obj['first'] = LinksElement.constructFromObject(data['first']);
            }
            if (data.hasOwnProperty('last')) {
                obj['last'] = LinksElement.constructFromObject(data['last']);
            }
            if (data.hasOwnProperty('next')) {
                obj['next'] = LinksElement.constructFromObject(data['next']);
            }
            if (data.hasOwnProperty('prev')) {
                obj['prev'] = LinksElement.constructFromObject(data['prev']);
            }
            if (data.hasOwnProperty('self')) {
                obj['self'] = LinksElement.constructFromObject(data['self']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaginationLinks</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaginationLinks</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaginationLinks.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `first`
        if (data['first']) { // data not null
          LinksElement.validateJSON(data['first']);
        }
        // validate the optional field `last`
        if (data['last']) { // data not null
          LinksElement.validateJSON(data['last']);
        }
        // validate the optional field `next`
        if (data['next']) { // data not null
          LinksElement.validateJSON(data['next']);
        }
        // validate the optional field `prev`
        if (data['prev']) { // data not null
          LinksElement.validateJSON(data['prev']);
        }
        // validate the optional field `self`
        if (data['self']) { // data not null
          LinksElement.validateJSON(data['self']);
        }

        return true;
    }


}

PaginationLinks.RequiredProperties = ["first", "last", "self"];

/**
 * The first page.
 * @member {module:model/LinksElement} first
 */
PaginationLinks.prototype['first'] = undefined;

/**
 * The last page.
 * @member {module:model/LinksElement} last
 */
PaginationLinks.prototype['last'] = undefined;

/**
 * The next page. Only present if there is a next page.
 * @member {module:model/LinksElement} next
 */
PaginationLinks.prototype['next'] = undefined;

/**
 * The previous page. Only present if there is a previous page.
 * @member {module:model/LinksElement} prev
 */
PaginationLinks.prototype['prev'] = undefined;

/**
 * The current page.
 * @member {module:model/LinksElement} self
 */
PaginationLinks.prototype['self'] = undefined;






export default PaginationLinks;

