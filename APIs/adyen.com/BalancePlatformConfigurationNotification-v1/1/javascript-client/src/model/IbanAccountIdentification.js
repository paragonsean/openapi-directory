/**
 * Configuration webhooks
 * Adyen sends webhooks to inform your system about events that occur in your platform. These events include, for example, when an account holder's capabilities are updated, or when a sweep configuration is created or updated.  When an event occurs, Adyen makes an HTTP POST request to a URL on your server and includes the details of the event in the request body.  You can use these webhooks to build your implementation. For example, you can use this information to update internal statuses when the status of a capability is changed.
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IbanAccountIdentification model module.
 * @module model/IbanAccountIdentification
 * @version 1
 */
class IbanAccountIdentification {
    /**
     * Constructs a new <code>IbanAccountIdentification</code>.
     * @alias module:model/IbanAccountIdentification
     * @param iban {String} The international bank account number as defined in the [ISO-13616](https://www.iso.org/standard/81090.html) standard.
     * @param type {module:model/IbanAccountIdentification.TypeEnum} **iban**
     */
    constructor(iban, type) { 
        
        IbanAccountIdentification.initialize(this, iban, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, iban, type) { 
        obj['formFactor'] = 'physical';
        obj['iban'] = iban;
        obj['type'] = type || 'iban';
    }

    /**
     * Constructs a <code>IbanAccountIdentification</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IbanAccountIdentification} obj Optional instance to populate.
     * @return {module:model/IbanAccountIdentification} The populated <code>IbanAccountIdentification</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IbanAccountIdentification();

            if (data.hasOwnProperty('formFactor')) {
                obj['formFactor'] = ApiClient.convertToType(data['formFactor'], 'String');
            }
            if (data.hasOwnProperty('iban')) {
                obj['iban'] = ApiClient.convertToType(data['iban'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IbanAccountIdentification</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IbanAccountIdentification</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IbanAccountIdentification.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['formFactor'] && !(typeof data['formFactor'] === 'string' || data['formFactor'] instanceof String)) {
            throw new Error("Expected the field `formFactor` to be a primitive type in the JSON string but got " + data['formFactor']);
        }
        // ensure the json data is a string
        if (data['iban'] && !(typeof data['iban'] === 'string' || data['iban'] instanceof String)) {
            throw new Error("Expected the field `iban` to be a primitive type in the JSON string but got " + data['iban']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

IbanAccountIdentification.RequiredProperties = ["iban", "type"];

/**
 * The form factor of bank account.
 * @member {String} formFactor
 * @default 'physical'
 */
IbanAccountIdentification.prototype['formFactor'] = 'physical';

/**
 * The international bank account number as defined in the [ISO-13616](https://www.iso.org/standard/81090.html) standard.
 * @member {String} iban
 */
IbanAccountIdentification.prototype['iban'] = undefined;

/**
 * **iban**
 * @member {module:model/IbanAccountIdentification.TypeEnum} type
 * @default 'iban'
 */
IbanAccountIdentification.prototype['type'] = 'iban';





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
IbanAccountIdentification['TypeEnum'] = {

    /**
     * value: "iban"
     * @const
     */
    "iban": "iban"
};



export default IbanAccountIdentification;

