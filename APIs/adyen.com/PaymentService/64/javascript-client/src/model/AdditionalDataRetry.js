/**
 * Adyen Payment API
 * A set of API endpoints that allow you to initiate, settle, and modify payments on the Adyen payments platform. You can use the API to accept card payments (including One-Click and 3D Secure), bank transfers, ewallets, and many other payment methods.  To learn more about the API, visit [Classic integration](https://docs.adyen.com/classic-integration).  ## Authentication You need an [API credential](https://docs.adyen.com/development-resources/api-credentials) to authenticate to the API.  If using an API key, add an `X-API-Key` header with the API key as the value, for example:   ``` curl -H \"Content-Type: application/json\" \\ -H \"X-API-Key: YOUR_API_KEY\" \\ ... ```  Alternatively, you can use the username and password to connect to the API using basic authentication, for example:  ``` curl -U \"ws@Company.YOUR_COMPANY_ACCOUNT\":\"YOUR_BASIC_AUTHENTICATION_PASSWORD\" \\ -H \"Content-Type: application/json\" \\ ... ```  ## Versioning Payments API supports [versioning](https://docs.adyen.com/development-resources/versioning) using a version suffix in the endpoint URL. This suffix has the following format: \"vXX\", where XX is the version number.  For example: ``` https://pal-test.adyen.com/pal/servlet/Payment/v64/authorise ```  ## Going live  To authenticate to the live endpoints, you need an [API credential](https://docs.adyen.com/development-resources/api-credentials) from your live Customer Area.  The live endpoint URLs contain a prefix which is unique to your company account: ```  https://{PREFIX}-pal-live.adyenpayments.com/pal/servlet/Payment/v64/authorise ```  Get your `{PREFIX}` from your live Customer Area under **Developers** > **API URLs** > **Prefix**.
 *
 * The version of the OpenAPI document: 64
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AdditionalDataRetry model module.
 * @module model/AdditionalDataRetry
 * @version 64
 */
class AdditionalDataRetry {
    /**
     * Constructs a new <code>AdditionalDataRetry</code>.
     * @alias module:model/AdditionalDataRetry
     */
    constructor() { 
        
        AdditionalDataRetry.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AdditionalDataRetry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AdditionalDataRetry} obj Optional instance to populate.
     * @return {module:model/AdditionalDataRetry} The populated <code>AdditionalDataRetry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AdditionalDataRetry();

            if (data.hasOwnProperty('retry.chainAttemptNumber')) {
                obj['retry.chainAttemptNumber'] = ApiClient.convertToType(data['retry.chainAttemptNumber'], 'String');
            }
            if (data.hasOwnProperty('retry.orderAttemptNumber')) {
                obj['retry.orderAttemptNumber'] = ApiClient.convertToType(data['retry.orderAttemptNumber'], 'String');
            }
            if (data.hasOwnProperty('retry.skipRetry')) {
                obj['retry.skipRetry'] = ApiClient.convertToType(data['retry.skipRetry'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AdditionalDataRetry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AdditionalDataRetry</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['retry.chainAttemptNumber'] && !(typeof data['retry.chainAttemptNumber'] === 'string' || data['retry.chainAttemptNumber'] instanceof String)) {
            throw new Error("Expected the field `retry.chainAttemptNumber` to be a primitive type in the JSON string but got " + data['retry.chainAttemptNumber']);
        }
        // ensure the json data is a string
        if (data['retry.orderAttemptNumber'] && !(typeof data['retry.orderAttemptNumber'] === 'string' || data['retry.orderAttemptNumber'] instanceof String)) {
            throw new Error("Expected the field `retry.orderAttemptNumber` to be a primitive type in the JSON string but got " + data['retry.orderAttemptNumber']);
        }
        // ensure the json data is a string
        if (data['retry.skipRetry'] && !(typeof data['retry.skipRetry'] === 'string' || data['retry.skipRetry'] instanceof String)) {
            throw new Error("Expected the field `retry.skipRetry` to be a primitive type in the JSON string but got " + data['retry.skipRetry']);
        }

        return true;
    }


}



/**
 * The number of times the transaction (not order) has been retried between different payment service providers. For instance, the `chainAttemptNumber` set to 2 means that this transaction has been recently tried on another provider before being sent to Adyen.  > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values, we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts together.
 * @member {String} retry.chainAttemptNumber
 */
AdditionalDataRetry.prototype['retry.chainAttemptNumber'] = undefined;

/**
 * The index of the attempt to bill a particular order, which is identified by the `merchantOrderReference` field. For example, if a recurring transaction fails and is retried one day later, then the order number for these attempts would be 1 and 2, respectively.  > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values, we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts together.
 * @member {String} retry.orderAttemptNumber
 */
AdditionalDataRetry.prototype['retry.orderAttemptNumber'] = undefined;

/**
 * The Boolean value indicating whether Adyen should skip or retry this transaction, if possible.  > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values, we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts together.
 * @member {String} retry.skipRetry
 */
AdditionalDataRetry.prototype['retry.skipRetry'] = undefined;






export default AdditionalDataRetry;

