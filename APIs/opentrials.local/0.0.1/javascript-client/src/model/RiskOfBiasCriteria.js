/**
 * OpenTrials API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RiskOfBiasCriteria model module.
 * @module model/RiskOfBiasCriteria
 * @version 0.0.1
 */
class RiskOfBiasCriteria {
    /**
     * Constructs a new <code>RiskOfBiasCriteria</code>.
     * @alias module:model/RiskOfBiasCriteria
     * @param id {String} 
     * @param name {String} 
     * @param value {module:model/RiskOfBiasCriteria.ValueEnum} 
     */
    constructor(id, name, value) { 
        
        RiskOfBiasCriteria.initialize(this, id, name, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, value) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>RiskOfBiasCriteria</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RiskOfBiasCriteria} obj Optional instance to populate.
     * @return {module:model/RiskOfBiasCriteria} The populated <code>RiskOfBiasCriteria</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RiskOfBiasCriteria();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RiskOfBiasCriteria</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RiskOfBiasCriteria</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RiskOfBiasCriteria.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

RiskOfBiasCriteria.RequiredProperties = ["id", "name", "value"];

/**
 * @member {String} id
 */
RiskOfBiasCriteria.prototype['id'] = undefined;

/**
 * @member {String} name
 */
RiskOfBiasCriteria.prototype['name'] = undefined;

/**
 * @member {module:model/RiskOfBiasCriteria.ValueEnum} value
 */
RiskOfBiasCriteria.prototype['value'] = undefined;





/**
 * Allowed values for the <code>value</code> property.
 * @enum {String}
 * @readonly
 */
RiskOfBiasCriteria['ValueEnum'] = {

    /**
     * value: "unknown"
     * @const
     */
    "unknown": "unknown"
};



export default RiskOfBiasCriteria;

