/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIConnectionReadList.h"

#include <QDebug>
#include <QJsonArray>
#include <QJsonDocument>
#include <QObject>

#include "OAIHelpers.h"

namespace OpenAPI {

OAIConnectionReadList::OAIConnectionReadList(QString json) {
    this->initializeModel();
    this->fromJson(json);
}

OAIConnectionReadList::OAIConnectionReadList() {
    this->initializeModel();
}

OAIConnectionReadList::~OAIConnectionReadList() {}

void OAIConnectionReadList::initializeModel() {

    m_connections_isSet = false;
    m_connections_isValid = false;
}

void OAIConnectionReadList::fromJson(QString jsonString) {
    QByteArray array(jsonString.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonObject jsonObject = doc.object();
    this->fromJsonObject(jsonObject);
}

void OAIConnectionReadList::fromJsonObject(QJsonObject json) {

    m_connections_isValid = ::OpenAPI::fromJsonValue(m_connections, json[QString("connections")]);
    m_connections_isSet = !json[QString("connections")].isNull() && m_connections_isValid;
}

QString OAIConnectionReadList::asJson() const {
    QJsonObject obj = this->asJsonObject();
    QJsonDocument doc(obj);
    QByteArray bytes = doc.toJson();
    return QString(bytes);
}

QJsonObject OAIConnectionReadList::asJsonObject() const {
    QJsonObject obj;
    if (m_connections.size() > 0) {
        obj.insert(QString("connections"), ::OpenAPI::toJsonValue(m_connections));
    }
    return obj;
}

QList<OAIConnectionRead> OAIConnectionReadList::getConnections() const {
    return m_connections;
}
void OAIConnectionReadList::setConnections(const QList<OAIConnectionRead> &connections) {
    m_connections = connections;
    m_connections_isSet = true;
}

bool OAIConnectionReadList::is_connections_Set() const{
    return m_connections_isSet;
}

bool OAIConnectionReadList::is_connections_Valid() const{
    return m_connections_isValid;
}

bool OAIConnectionReadList::isSet() const {
    bool isObjectUpdated = false;
    do {
        if (m_connections.size() > 0) {
            isObjectUpdated = true;
            break;
        }
    } while (false);
    return isObjectUpdated;
}

bool OAIConnectionReadList::isValid() const {
    // only required properties are required for the object to be considered valid
    return m_connections_isValid && true;
}

} // namespace OpenAPI
