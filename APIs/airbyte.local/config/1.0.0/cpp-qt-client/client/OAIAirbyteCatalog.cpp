/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIAirbyteCatalog.h"

#include <QDebug>
#include <QJsonArray>
#include <QJsonDocument>
#include <QObject>

#include "OAIHelpers.h"

namespace OpenAPI {

OAIAirbyteCatalog::OAIAirbyteCatalog(QString json) {
    this->initializeModel();
    this->fromJson(json);
}

OAIAirbyteCatalog::OAIAirbyteCatalog() {
    this->initializeModel();
}

OAIAirbyteCatalog::~OAIAirbyteCatalog() {}

void OAIAirbyteCatalog::initializeModel() {

    m_streams_isSet = false;
    m_streams_isValid = false;
}

void OAIAirbyteCatalog::fromJson(QString jsonString) {
    QByteArray array(jsonString.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonObject jsonObject = doc.object();
    this->fromJsonObject(jsonObject);
}

void OAIAirbyteCatalog::fromJsonObject(QJsonObject json) {

    m_streams_isValid = ::OpenAPI::fromJsonValue(m_streams, json[QString("streams")]);
    m_streams_isSet = !json[QString("streams")].isNull() && m_streams_isValid;
}

QString OAIAirbyteCatalog::asJson() const {
    QJsonObject obj = this->asJsonObject();
    QJsonDocument doc(obj);
    QByteArray bytes = doc.toJson();
    return QString(bytes);
}

QJsonObject OAIAirbyteCatalog::asJsonObject() const {
    QJsonObject obj;
    if (m_streams.size() > 0) {
        obj.insert(QString("streams"), ::OpenAPI::toJsonValue(m_streams));
    }
    return obj;
}

QList<OAIAirbyteStreamAndConfiguration> OAIAirbyteCatalog::getStreams() const {
    return m_streams;
}
void OAIAirbyteCatalog::setStreams(const QList<OAIAirbyteStreamAndConfiguration> &streams) {
    m_streams = streams;
    m_streams_isSet = true;
}

bool OAIAirbyteCatalog::is_streams_Set() const{
    return m_streams_isSet;
}

bool OAIAirbyteCatalog::is_streams_Valid() const{
    return m_streams_isValid;
}

bool OAIAirbyteCatalog::isSet() const {
    bool isObjectUpdated = false;
    do {
        if (m_streams.size() > 0) {
            isObjectUpdated = true;
            break;
        }
    } while (false);
    return isObjectUpdated;
}

bool OAIAirbyteCatalog::isValid() const {
    // only required properties are required for the object to be considered valid
    return m_streams_isValid && true;
}

} // namespace OpenAPI
