/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAISelectedFieldInfo.h"

#include <QDebug>
#include <QJsonArray>
#include <QJsonDocument>
#include <QObject>

#include "OAIHelpers.h"

namespace OpenAPI {

OAISelectedFieldInfo::OAISelectedFieldInfo(QString json) {
    this->initializeModel();
    this->fromJson(json);
}

OAISelectedFieldInfo::OAISelectedFieldInfo() {
    this->initializeModel();
}

OAISelectedFieldInfo::~OAISelectedFieldInfo() {}

void OAISelectedFieldInfo::initializeModel() {

    m_field_path_isSet = false;
    m_field_path_isValid = false;
}

void OAISelectedFieldInfo::fromJson(QString jsonString) {
    QByteArray array(jsonString.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonObject jsonObject = doc.object();
    this->fromJsonObject(jsonObject);
}

void OAISelectedFieldInfo::fromJsonObject(QJsonObject json) {

    m_field_path_isValid = ::OpenAPI::fromJsonValue(m_field_path, json[QString("fieldPath")]);
    m_field_path_isSet = !json[QString("fieldPath")].isNull() && m_field_path_isValid;
}

QString OAISelectedFieldInfo::asJson() const {
    QJsonObject obj = this->asJsonObject();
    QJsonDocument doc(obj);
    QByteArray bytes = doc.toJson();
    return QString(bytes);
}

QJsonObject OAISelectedFieldInfo::asJsonObject() const {
    QJsonObject obj;
    if (m_field_path.size() > 0) {
        obj.insert(QString("fieldPath"), ::OpenAPI::toJsonValue(m_field_path));
    }
    return obj;
}

QList<QString> OAISelectedFieldInfo::getFieldPath() const {
    return m_field_path;
}
void OAISelectedFieldInfo::setFieldPath(const QList<QString> &field_path) {
    m_field_path = field_path;
    m_field_path_isSet = true;
}

bool OAISelectedFieldInfo::is_field_path_Set() const{
    return m_field_path_isSet;
}

bool OAISelectedFieldInfo::is_field_path_Valid() const{
    return m_field_path_isValid;
}

bool OAISelectedFieldInfo::isSet() const {
    bool isObjectUpdated = false;
    do {
        if (m_field_path.size() > 0) {
            isObjectUpdated = true;
            break;
        }
    } while (false);
    return isObjectUpdated;
}

bool OAISelectedFieldInfo::isValid() const {
    // only required properties are required for the object to be considered valid
    return true;
}

} // namespace OpenAPI
