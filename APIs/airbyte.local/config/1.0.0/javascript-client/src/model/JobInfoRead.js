/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AttemptInfoRead from './AttemptInfoRead';
import JobRead from './JobRead';

/**
 * The JobInfoRead model module.
 * @module model/JobInfoRead
 * @version 1.0.0
 */
class JobInfoRead {
    /**
     * Constructs a new <code>JobInfoRead</code>.
     * @alias module:model/JobInfoRead
     * @param attempts {Array.<module:model/AttemptInfoRead>} 
     * @param job {module:model/JobRead} 
     */
    constructor(attempts, job) { 
        
        JobInfoRead.initialize(this, attempts, job);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, attempts, job) { 
        obj['attempts'] = attempts;
        obj['job'] = job;
    }

    /**
     * Constructs a <code>JobInfoRead</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobInfoRead} obj Optional instance to populate.
     * @return {module:model/JobInfoRead} The populated <code>JobInfoRead</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobInfoRead();

            if (data.hasOwnProperty('attempts')) {
                obj['attempts'] = ApiClient.convertToType(data['attempts'], [AttemptInfoRead]);
            }
            if (data.hasOwnProperty('job')) {
                obj['job'] = JobRead.constructFromObject(data['job']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobInfoRead</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobInfoRead</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobInfoRead.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['attempts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['attempts'])) {
                throw new Error("Expected the field `attempts` to be an array in the JSON data but got " + data['attempts']);
            }
            // validate the optional field `attempts` (array)
            for (const item of data['attempts']) {
                AttemptInfoRead.validateJSON(item);
            };
        }
        // validate the optional field `job`
        if (data['job']) { // data not null
          JobRead.validateJSON(data['job']);
        }

        return true;
    }


}

JobInfoRead.RequiredProperties = ["attempts", "job"];

/**
 * @member {Array.<module:model/AttemptInfoRead>} attempts
 */
JobInfoRead.prototype['attempts'] = undefined;

/**
 * @member {module:model/JobRead} job
 */
JobInfoRead.prototype['job'] = undefined;






export default JobInfoRead;

