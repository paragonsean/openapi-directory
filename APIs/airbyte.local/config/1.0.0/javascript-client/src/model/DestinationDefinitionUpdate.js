/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ActorDefinitionResourceRequirements from './ActorDefinitionResourceRequirements';

/**
 * The DestinationDefinitionUpdate model module.
 * @module model/DestinationDefinitionUpdate
 * @version 1.0.0
 */
class DestinationDefinitionUpdate {
    /**
     * Constructs a new <code>DestinationDefinitionUpdate</code>.
     * @alias module:model/DestinationDefinitionUpdate
     * @param destinationDefinitionId {String} 
     */
    constructor(destinationDefinitionId) { 
        
        DestinationDefinitionUpdate.initialize(this, destinationDefinitionId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationDefinitionId) { 
        obj['destinationDefinitionId'] = destinationDefinitionId;
    }

    /**
     * Constructs a <code>DestinationDefinitionUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DestinationDefinitionUpdate} obj Optional instance to populate.
     * @return {module:model/DestinationDefinitionUpdate} The populated <code>DestinationDefinitionUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DestinationDefinitionUpdate();

            if (data.hasOwnProperty('destinationDefinitionId')) {
                obj['destinationDefinitionId'] = ApiClient.convertToType(data['destinationDefinitionId'], 'String');
            }
            if (data.hasOwnProperty('dockerImageTag')) {
                obj['dockerImageTag'] = ApiClient.convertToType(data['dockerImageTag'], 'String');
            }
            if (data.hasOwnProperty('resourceRequirements')) {
                obj['resourceRequirements'] = ActorDefinitionResourceRequirements.constructFromObject(data['resourceRequirements']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DestinationDefinitionUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DestinationDefinitionUpdate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DestinationDefinitionUpdate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['destinationDefinitionId'] && !(typeof data['destinationDefinitionId'] === 'string' || data['destinationDefinitionId'] instanceof String)) {
            throw new Error("Expected the field `destinationDefinitionId` to be a primitive type in the JSON string but got " + data['destinationDefinitionId']);
        }
        // ensure the json data is a string
        if (data['dockerImageTag'] && !(typeof data['dockerImageTag'] === 'string' || data['dockerImageTag'] instanceof String)) {
            throw new Error("Expected the field `dockerImageTag` to be a primitive type in the JSON string but got " + data['dockerImageTag']);
        }
        // validate the optional field `resourceRequirements`
        if (data['resourceRequirements']) { // data not null
          ActorDefinitionResourceRequirements.validateJSON(data['resourceRequirements']);
        }

        return true;
    }


}

DestinationDefinitionUpdate.RequiredProperties = ["destinationDefinitionId"];

/**
 * @member {String} destinationDefinitionId
 */
DestinationDefinitionUpdate.prototype['destinationDefinitionId'] = undefined;

/**
 * @member {String} dockerImageTag
 */
DestinationDefinitionUpdate.prototype['dockerImageTag'] = undefined;

/**
 * @member {module:model/ActorDefinitionResourceRequirements} resourceRequirements
 */
DestinationDefinitionUpdate.prototype['resourceRequirements'] = undefined;






export default DestinationDefinitionUpdate;

