/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ActorDefinitionResourceRequirements from './ActorDefinitionResourceRequirements';

/**
 * The DestinationDefinitionCreate model module.
 * @module model/DestinationDefinitionCreate
 * @version 1.0.0
 */
class DestinationDefinitionCreate {
    /**
     * Constructs a new <code>DestinationDefinitionCreate</code>.
     * @alias module:model/DestinationDefinitionCreate
     * @param dockerImageTag {String} 
     * @param dockerRepository {String} 
     * @param documentationUrl {String} 
     * @param name {String} 
     */
    constructor(dockerImageTag, dockerRepository, documentationUrl, name) { 
        
        DestinationDefinitionCreate.initialize(this, dockerImageTag, dockerRepository, documentationUrl, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dockerImageTag, dockerRepository, documentationUrl, name) { 
        obj['dockerImageTag'] = dockerImageTag;
        obj['dockerRepository'] = dockerRepository;
        obj['documentationUrl'] = documentationUrl;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>DestinationDefinitionCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DestinationDefinitionCreate} obj Optional instance to populate.
     * @return {module:model/DestinationDefinitionCreate} The populated <code>DestinationDefinitionCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DestinationDefinitionCreate();

            if (data.hasOwnProperty('dockerImageTag')) {
                obj['dockerImageTag'] = ApiClient.convertToType(data['dockerImageTag'], 'String');
            }
            if (data.hasOwnProperty('dockerRepository')) {
                obj['dockerRepository'] = ApiClient.convertToType(data['dockerRepository'], 'String');
            }
            if (data.hasOwnProperty('documentationUrl')) {
                obj['documentationUrl'] = ApiClient.convertToType(data['documentationUrl'], 'String');
            }
            if (data.hasOwnProperty('icon')) {
                obj['icon'] = ApiClient.convertToType(data['icon'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('resourceRequirements')) {
                obj['resourceRequirements'] = ActorDefinitionResourceRequirements.constructFromObject(data['resourceRequirements']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DestinationDefinitionCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DestinationDefinitionCreate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DestinationDefinitionCreate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['dockerImageTag'] && !(typeof data['dockerImageTag'] === 'string' || data['dockerImageTag'] instanceof String)) {
            throw new Error("Expected the field `dockerImageTag` to be a primitive type in the JSON string but got " + data['dockerImageTag']);
        }
        // ensure the json data is a string
        if (data['dockerRepository'] && !(typeof data['dockerRepository'] === 'string' || data['dockerRepository'] instanceof String)) {
            throw new Error("Expected the field `dockerRepository` to be a primitive type in the JSON string but got " + data['dockerRepository']);
        }
        // ensure the json data is a string
        if (data['documentationUrl'] && !(typeof data['documentationUrl'] === 'string' || data['documentationUrl'] instanceof String)) {
            throw new Error("Expected the field `documentationUrl` to be a primitive type in the JSON string but got " + data['documentationUrl']);
        }
        // ensure the json data is a string
        if (data['icon'] && !(typeof data['icon'] === 'string' || data['icon'] instanceof String)) {
            throw new Error("Expected the field `icon` to be a primitive type in the JSON string but got " + data['icon']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `resourceRequirements`
        if (data['resourceRequirements']) { // data not null
          ActorDefinitionResourceRequirements.validateJSON(data['resourceRequirements']);
        }

        return true;
    }


}

DestinationDefinitionCreate.RequiredProperties = ["dockerImageTag", "dockerRepository", "documentationUrl", "name"];

/**
 * @member {String} dockerImageTag
 */
DestinationDefinitionCreate.prototype['dockerImageTag'] = undefined;

/**
 * @member {String} dockerRepository
 */
DestinationDefinitionCreate.prototype['dockerRepository'] = undefined;

/**
 * @member {String} documentationUrl
 */
DestinationDefinitionCreate.prototype['documentationUrl'] = undefined;

/**
 * @member {String} icon
 */
DestinationDefinitionCreate.prototype['icon'] = undefined;

/**
 * @member {String} name
 */
DestinationDefinitionCreate.prototype['name'] = undefined;

/**
 * @member {module:model/ActorDefinitionResourceRequirements} resourceRequirements
 */
DestinationDefinitionCreate.prototype['resourceRequirements'] = undefined;






export default DestinationDefinitionCreate;

