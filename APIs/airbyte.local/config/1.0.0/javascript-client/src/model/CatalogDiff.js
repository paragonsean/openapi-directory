/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import StreamTransform from './StreamTransform';

/**
 * The CatalogDiff model module.
 * @module model/CatalogDiff
 * @version 1.0.0
 */
class CatalogDiff {
    /**
     * Constructs a new <code>CatalogDiff</code>.
     * Describes the difference between two Airbyte catalogs.
     * @alias module:model/CatalogDiff
     * @param transforms {Array.<module:model/StreamTransform>} list of stream transformations. order does not matter.
     */
    constructor(transforms) { 
        
        CatalogDiff.initialize(this, transforms);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, transforms) { 
        obj['transforms'] = transforms;
    }

    /**
     * Constructs a <code>CatalogDiff</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CatalogDiff} obj Optional instance to populate.
     * @return {module:model/CatalogDiff} The populated <code>CatalogDiff</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CatalogDiff();

            if (data.hasOwnProperty('transforms')) {
                obj['transforms'] = ApiClient.convertToType(data['transforms'], [StreamTransform]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CatalogDiff</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CatalogDiff</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CatalogDiff.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['transforms']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['transforms'])) {
                throw new Error("Expected the field `transforms` to be an array in the JSON data but got " + data['transforms']);
            }
            // validate the optional field `transforms` (array)
            for (const item of data['transforms']) {
                StreamTransform.validateJSON(item);
            };
        }

        return true;
    }


}

CatalogDiff.RequiredProperties = ["transforms"];

/**
 * list of stream transformations. order does not matter.
 * @member {Array.<module:model/StreamTransform>} transforms
 */
CatalogDiff.prototype['transforms'] = undefined;






export default CatalogDiff;

