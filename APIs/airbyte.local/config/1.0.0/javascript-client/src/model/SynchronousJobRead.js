/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JobConfigType from './JobConfigType';
import LogRead from './LogRead';

/**
 * The SynchronousJobRead model module.
 * @module model/SynchronousJobRead
 * @version 1.0.0
 */
class SynchronousJobRead {
    /**
     * Constructs a new <code>SynchronousJobRead</code>.
     * @alias module:model/SynchronousJobRead
     * @param configType {module:model/JobConfigType} 
     * @param createdAt {Number} 
     * @param endedAt {Number} 
     * @param id {String} 
     * @param succeeded {Boolean} 
     */
    constructor(configType, createdAt, endedAt, id, succeeded) { 
        
        SynchronousJobRead.initialize(this, configType, createdAt, endedAt, id, succeeded);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, configType, createdAt, endedAt, id, succeeded) { 
        obj['configType'] = configType;
        obj['connectorConfigurationUpdated'] = false;
        obj['createdAt'] = createdAt;
        obj['endedAt'] = endedAt;
        obj['id'] = id;
        obj['succeeded'] = succeeded;
    }

    /**
     * Constructs a <code>SynchronousJobRead</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SynchronousJobRead} obj Optional instance to populate.
     * @return {module:model/SynchronousJobRead} The populated <code>SynchronousJobRead</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SynchronousJobRead();

            if (data.hasOwnProperty('configId')) {
                obj['configId'] = ApiClient.convertToType(data['configId'], 'String');
            }
            if (data.hasOwnProperty('configType')) {
                obj['configType'] = JobConfigType.constructFromObject(data['configType']);
            }
            if (data.hasOwnProperty('connectorConfigurationUpdated')) {
                obj['connectorConfigurationUpdated'] = ApiClient.convertToType(data['connectorConfigurationUpdated'], 'Boolean');
            }
            if (data.hasOwnProperty('createdAt')) {
                obj['createdAt'] = ApiClient.convertToType(data['createdAt'], 'Number');
            }
            if (data.hasOwnProperty('endedAt')) {
                obj['endedAt'] = ApiClient.convertToType(data['endedAt'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('logs')) {
                obj['logs'] = LogRead.constructFromObject(data['logs']);
            }
            if (data.hasOwnProperty('succeeded')) {
                obj['succeeded'] = ApiClient.convertToType(data['succeeded'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SynchronousJobRead</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SynchronousJobRead</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SynchronousJobRead.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['configId'] && !(typeof data['configId'] === 'string' || data['configId'] instanceof String)) {
            throw new Error("Expected the field `configId` to be a primitive type in the JSON string but got " + data['configId']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `logs`
        if (data['logs']) { // data not null
          LogRead.validateJSON(data['logs']);
        }

        return true;
    }


}

SynchronousJobRead.RequiredProperties = ["configType", "createdAt", "endedAt", "id", "succeeded"];

/**
 * only present if a config id was provided.
 * @member {String} configId
 */
SynchronousJobRead.prototype['configId'] = undefined;

/**
 * @member {module:model/JobConfigType} configType
 */
SynchronousJobRead.prototype['configType'] = undefined;

/**
 * @member {Boolean} connectorConfigurationUpdated
 * @default false
 */
SynchronousJobRead.prototype['connectorConfigurationUpdated'] = false;

/**
 * @member {Number} createdAt
 */
SynchronousJobRead.prototype['createdAt'] = undefined;

/**
 * @member {Number} endedAt
 */
SynchronousJobRead.prototype['endedAt'] = undefined;

/**
 * @member {String} id
 */
SynchronousJobRead.prototype['id'] = undefined;

/**
 * @member {module:model/LogRead} logs
 */
SynchronousJobRead.prototype['logs'] = undefined;

/**
 * @member {Boolean} succeeded
 */
SynchronousJobRead.prototype['succeeded'] = undefined;






export default SynchronousJobRead;

