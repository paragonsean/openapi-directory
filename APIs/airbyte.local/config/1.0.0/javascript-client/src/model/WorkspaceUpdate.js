/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Geography from './Geography';
import Notification from './Notification';
import WebhookConfigWrite from './WebhookConfigWrite';

/**
 * The WorkspaceUpdate model module.
 * @module model/WorkspaceUpdate
 * @version 1.0.0
 */
class WorkspaceUpdate {
    /**
     * Constructs a new <code>WorkspaceUpdate</code>.
     * Used to apply a patch-style update to a workspace, which means that null properties remain unchanged
     * @alias module:model/WorkspaceUpdate
     * @param workspaceId {String} 
     */
    constructor(workspaceId) { 
        
        WorkspaceUpdate.initialize(this, workspaceId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, workspaceId) { 
        obj['workspaceId'] = workspaceId;
    }

    /**
     * Constructs a <code>WorkspaceUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkspaceUpdate} obj Optional instance to populate.
     * @return {module:model/WorkspaceUpdate} The populated <code>WorkspaceUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkspaceUpdate();

            if (data.hasOwnProperty('anonymousDataCollection')) {
                obj['anonymousDataCollection'] = ApiClient.convertToType(data['anonymousDataCollection'], 'Boolean');
            }
            if (data.hasOwnProperty('defaultGeography')) {
                obj['defaultGeography'] = Geography.constructFromObject(data['defaultGeography']);
            }
            if (data.hasOwnProperty('displaySetupWizard')) {
                obj['displaySetupWizard'] = ApiClient.convertToType(data['displaySetupWizard'], 'Boolean');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('initialSetupComplete')) {
                obj['initialSetupComplete'] = ApiClient.convertToType(data['initialSetupComplete'], 'Boolean');
            }
            if (data.hasOwnProperty('news')) {
                obj['news'] = ApiClient.convertToType(data['news'], 'Boolean');
            }
            if (data.hasOwnProperty('notifications')) {
                obj['notifications'] = ApiClient.convertToType(data['notifications'], [Notification]);
            }
            if (data.hasOwnProperty('securityUpdates')) {
                obj['securityUpdates'] = ApiClient.convertToType(data['securityUpdates'], 'Boolean');
            }
            if (data.hasOwnProperty('webhookConfigs')) {
                obj['webhookConfigs'] = ApiClient.convertToType(data['webhookConfigs'], [WebhookConfigWrite]);
            }
            if (data.hasOwnProperty('workspaceId')) {
                obj['workspaceId'] = ApiClient.convertToType(data['workspaceId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkspaceUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkspaceUpdate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WorkspaceUpdate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        if (data['notifications']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['notifications'])) {
                throw new Error("Expected the field `notifications` to be an array in the JSON data but got " + data['notifications']);
            }
            // validate the optional field `notifications` (array)
            for (const item of data['notifications']) {
                Notification.validateJSON(item);
            };
        }
        if (data['webhookConfigs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['webhookConfigs'])) {
                throw new Error("Expected the field `webhookConfigs` to be an array in the JSON data but got " + data['webhookConfigs']);
            }
            // validate the optional field `webhookConfigs` (array)
            for (const item of data['webhookConfigs']) {
                WebhookConfigWrite.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['workspaceId'] && !(typeof data['workspaceId'] === 'string' || data['workspaceId'] instanceof String)) {
            throw new Error("Expected the field `workspaceId` to be a primitive type in the JSON string but got " + data['workspaceId']);
        }

        return true;
    }


}

WorkspaceUpdate.RequiredProperties = ["workspaceId"];

/**
 * @member {Boolean} anonymousDataCollection
 */
WorkspaceUpdate.prototype['anonymousDataCollection'] = undefined;

/**
 * @member {module:model/Geography} defaultGeography
 */
WorkspaceUpdate.prototype['defaultGeography'] = undefined;

/**
 * @member {Boolean} displaySetupWizard
 */
WorkspaceUpdate.prototype['displaySetupWizard'] = undefined;

/**
 * @member {String} email
 */
WorkspaceUpdate.prototype['email'] = undefined;

/**
 * @member {Boolean} initialSetupComplete
 */
WorkspaceUpdate.prototype['initialSetupComplete'] = undefined;

/**
 * @member {Boolean} news
 */
WorkspaceUpdate.prototype['news'] = undefined;

/**
 * @member {Array.<module:model/Notification>} notifications
 */
WorkspaceUpdate.prototype['notifications'] = undefined;

/**
 * @member {Boolean} securityUpdates
 */
WorkspaceUpdate.prototype['securityUpdates'] = undefined;

/**
 * @member {Array.<module:model/WebhookConfigWrite>} webhookConfigs
 */
WorkspaceUpdate.prototype['webhookConfigs'] = undefined;

/**
 * @member {String} workspaceId
 */
WorkspaceUpdate.prototype['workspaceId'] = undefined;






export default WorkspaceUpdate;

