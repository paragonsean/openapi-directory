/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PrivateDestinationDefinitionRead from './PrivateDestinationDefinitionRead';

/**
 * The PrivateDestinationDefinitionReadList model module.
 * @module model/PrivateDestinationDefinitionReadList
 * @version 1.0.0
 */
class PrivateDestinationDefinitionReadList {
    /**
     * Constructs a new <code>PrivateDestinationDefinitionReadList</code>.
     * @alias module:model/PrivateDestinationDefinitionReadList
     * @param destinationDefinitions {Array.<module:model/PrivateDestinationDefinitionRead>} 
     */
    constructor(destinationDefinitions) { 
        
        PrivateDestinationDefinitionReadList.initialize(this, destinationDefinitions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationDefinitions) { 
        obj['destinationDefinitions'] = destinationDefinitions;
    }

    /**
     * Constructs a <code>PrivateDestinationDefinitionReadList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PrivateDestinationDefinitionReadList} obj Optional instance to populate.
     * @return {module:model/PrivateDestinationDefinitionReadList} The populated <code>PrivateDestinationDefinitionReadList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PrivateDestinationDefinitionReadList();

            if (data.hasOwnProperty('destinationDefinitions')) {
                obj['destinationDefinitions'] = ApiClient.convertToType(data['destinationDefinitions'], [PrivateDestinationDefinitionRead]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PrivateDestinationDefinitionReadList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PrivateDestinationDefinitionReadList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PrivateDestinationDefinitionReadList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['destinationDefinitions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['destinationDefinitions'])) {
                throw new Error("Expected the field `destinationDefinitions` to be an array in the JSON data but got " + data['destinationDefinitions']);
            }
            // validate the optional field `destinationDefinitions` (array)
            for (const item of data['destinationDefinitions']) {
                PrivateDestinationDefinitionRead.validateJSON(item);
            };
        }

        return true;
    }


}

PrivateDestinationDefinitionReadList.RequiredProperties = ["destinationDefinitions"];

/**
 * @member {Array.<module:model/PrivateDestinationDefinitionRead>} destinationDefinitions
 */
PrivateDestinationDefinitionReadList.prototype['destinationDefinitions'] = undefined;






export default PrivateDestinationDefinitionReadList;

