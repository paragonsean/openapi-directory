/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WebBackendWorkspaceStateResult model module.
 * @module model/WebBackendWorkspaceStateResult
 * @version 1.0.0
 */
class WebBackendWorkspaceStateResult {
    /**
     * Constructs a new <code>WebBackendWorkspaceStateResult</code>.
     * @alias module:model/WebBackendWorkspaceStateResult
     * @param hasConnections {Boolean} 
     * @param hasDestinations {Boolean} 
     * @param hasSources {Boolean} 
     */
    constructor(hasConnections, hasDestinations, hasSources) { 
        
        WebBackendWorkspaceStateResult.initialize(this, hasConnections, hasDestinations, hasSources);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hasConnections, hasDestinations, hasSources) { 
        obj['hasConnections'] = hasConnections;
        obj['hasDestinations'] = hasDestinations;
        obj['hasSources'] = hasSources;
    }

    /**
     * Constructs a <code>WebBackendWorkspaceStateResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WebBackendWorkspaceStateResult} obj Optional instance to populate.
     * @return {module:model/WebBackendWorkspaceStateResult} The populated <code>WebBackendWorkspaceStateResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebBackendWorkspaceStateResult();

            if (data.hasOwnProperty('hasConnections')) {
                obj['hasConnections'] = ApiClient.convertToType(data['hasConnections'], 'Boolean');
            }
            if (data.hasOwnProperty('hasDestinations')) {
                obj['hasDestinations'] = ApiClient.convertToType(data['hasDestinations'], 'Boolean');
            }
            if (data.hasOwnProperty('hasSources')) {
                obj['hasSources'] = ApiClient.convertToType(data['hasSources'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebBackendWorkspaceStateResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebBackendWorkspaceStateResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WebBackendWorkspaceStateResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

WebBackendWorkspaceStateResult.RequiredProperties = ["hasConnections", "hasDestinations", "hasSources"];

/**
 * @member {Boolean} hasConnections
 */
WebBackendWorkspaceStateResult.prototype['hasConnections'] = undefined;

/**
 * @member {Boolean} hasDestinations
 */
WebBackendWorkspaceStateResult.prototype['hasDestinations'] = undefined;

/**
 * @member {Boolean} hasSources
 */
WebBackendWorkspaceStateResult.prototype['hasSources'] = undefined;






export default WebBackendWorkspaceStateResult;

