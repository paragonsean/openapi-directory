/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SetWorkflowInAttemptRequestBody model module.
 * @module model/SetWorkflowInAttemptRequestBody
 * @version 1.0.0
 */
class SetWorkflowInAttemptRequestBody {
    /**
     * Constructs a new <code>SetWorkflowInAttemptRequestBody</code>.
     * @alias module:model/SetWorkflowInAttemptRequestBody
     * @param attemptNumber {Number} 
     * @param jobId {Number} 
     * @param workflowId {String} 
     */
    constructor(attemptNumber, jobId, workflowId) { 
        
        SetWorkflowInAttemptRequestBody.initialize(this, attemptNumber, jobId, workflowId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, attemptNumber, jobId, workflowId) { 
        obj['attemptNumber'] = attemptNumber;
        obj['jobId'] = jobId;
        obj['processingTaskQueue'] = '';
        obj['workflowId'] = workflowId;
    }

    /**
     * Constructs a <code>SetWorkflowInAttemptRequestBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SetWorkflowInAttemptRequestBody} obj Optional instance to populate.
     * @return {module:model/SetWorkflowInAttemptRequestBody} The populated <code>SetWorkflowInAttemptRequestBody</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SetWorkflowInAttemptRequestBody();

            if (data.hasOwnProperty('attemptNumber')) {
                obj['attemptNumber'] = ApiClient.convertToType(data['attemptNumber'], 'Number');
            }
            if (data.hasOwnProperty('jobId')) {
                obj['jobId'] = ApiClient.convertToType(data['jobId'], 'Number');
            }
            if (data.hasOwnProperty('processingTaskQueue')) {
                obj['processingTaskQueue'] = ApiClient.convertToType(data['processingTaskQueue'], 'String');
            }
            if (data.hasOwnProperty('workflowId')) {
                obj['workflowId'] = ApiClient.convertToType(data['workflowId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SetWorkflowInAttemptRequestBody</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SetWorkflowInAttemptRequestBody</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SetWorkflowInAttemptRequestBody.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['processingTaskQueue'] && !(typeof data['processingTaskQueue'] === 'string' || data['processingTaskQueue'] instanceof String)) {
            throw new Error("Expected the field `processingTaskQueue` to be a primitive type in the JSON string but got " + data['processingTaskQueue']);
        }
        // ensure the json data is a string
        if (data['workflowId'] && !(typeof data['workflowId'] === 'string' || data['workflowId'] instanceof String)) {
            throw new Error("Expected the field `workflowId` to be a primitive type in the JSON string but got " + data['workflowId']);
        }

        return true;
    }


}

SetWorkflowInAttemptRequestBody.RequiredProperties = ["attemptNumber", "jobId", "workflowId"];

/**
 * @member {Number} attemptNumber
 */
SetWorkflowInAttemptRequestBody.prototype['attemptNumber'] = undefined;

/**
 * @member {Number} jobId
 */
SetWorkflowInAttemptRequestBody.prototype['jobId'] = undefined;

/**
 * @member {String} processingTaskQueue
 * @default ''
 */
SetWorkflowInAttemptRequestBody.prototype['processingTaskQueue'] = '';

/**
 * @member {String} workflowId
 */
SetWorkflowInAttemptRequestBody.prototype['workflowId'] = undefined;






export default SetWorkflowInAttemptRequestBody;

