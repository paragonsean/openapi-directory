/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ConnectionState from './ConnectionState';

/**
 * The AttemptSyncConfig model module.
 * @module model/AttemptSyncConfig
 * @version 1.0.0
 */
class AttemptSyncConfig {
    /**
     * Constructs a new <code>AttemptSyncConfig</code>.
     * @alias module:model/AttemptSyncConfig
     * @param destinationConfiguration {Object} The values required to configure the destination. The schema for this must match the schema return by destination_definition_specifications/get for the destinationDefinition.
     * @param sourceConfiguration {Object} The values required to configure the source. The schema for this must match the schema return by source_definition_specifications/get for the source.
     */
    constructor(destinationConfiguration, sourceConfiguration) { 
        
        AttemptSyncConfig.initialize(this, destinationConfiguration, sourceConfiguration);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationConfiguration, sourceConfiguration) { 
        obj['destinationConfiguration'] = destinationConfiguration;
        obj['sourceConfiguration'] = sourceConfiguration;
    }

    /**
     * Constructs a <code>AttemptSyncConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AttemptSyncConfig} obj Optional instance to populate.
     * @return {module:model/AttemptSyncConfig} The populated <code>AttemptSyncConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AttemptSyncConfig();

            if (data.hasOwnProperty('destinationConfiguration')) {
                obj['destinationConfiguration'] = ApiClient.convertToType(data['destinationConfiguration'], Object);
            }
            if (data.hasOwnProperty('sourceConfiguration')) {
                obj['sourceConfiguration'] = ApiClient.convertToType(data['sourceConfiguration'], Object);
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ConnectionState.constructFromObject(data['state']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AttemptSyncConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AttemptSyncConfig</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AttemptSyncConfig.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `state`
        if (data['state']) { // data not null
          ConnectionState.validateJSON(data['state']);
        }

        return true;
    }


}

AttemptSyncConfig.RequiredProperties = ["destinationConfiguration", "sourceConfiguration"];

/**
 * The values required to configure the destination. The schema for this must match the schema return by destination_definition_specifications/get for the destinationDefinition.
 * @member {Object} destinationConfiguration
 */
AttemptSyncConfig.prototype['destinationConfiguration'] = undefined;

/**
 * The values required to configure the source. The schema for this must match the schema return by source_definition_specifications/get for the source.
 * @member {Object} sourceConfiguration
 */
AttemptSyncConfig.prototype['sourceConfiguration'] = undefined;

/**
 * @member {module:model/ConnectionState} state
 */
AttemptSyncConfig.prototype['state'] = undefined;






export default AttemptSyncConfig;

