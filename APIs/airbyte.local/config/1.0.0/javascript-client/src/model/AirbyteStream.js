/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SyncMode from './SyncMode';

/**
 * The AirbyteStream model module.
 * @module model/AirbyteStream
 * @version 1.0.0
 */
class AirbyteStream {
    /**
     * Constructs a new <code>AirbyteStream</code>.
     * the immutable schema defined by the source
     * @alias module:model/AirbyteStream
     * @param name {String} Stream's name.
     */
    constructor(name) { 
        
        AirbyteStream.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>AirbyteStream</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AirbyteStream} obj Optional instance to populate.
     * @return {module:model/AirbyteStream} The populated <code>AirbyteStream</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AirbyteStream();

            if (data.hasOwnProperty('defaultCursorField')) {
                obj['defaultCursorField'] = ApiClient.convertToType(data['defaultCursorField'], ['String']);
            }
            if (data.hasOwnProperty('jsonSchema')) {
                obj['jsonSchema'] = ApiClient.convertToType(data['jsonSchema'], Object);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('namespace')) {
                obj['namespace'] = ApiClient.convertToType(data['namespace'], 'String');
            }
            if (data.hasOwnProperty('sourceDefinedCursor')) {
                obj['sourceDefinedCursor'] = ApiClient.convertToType(data['sourceDefinedCursor'], 'Boolean');
            }
            if (data.hasOwnProperty('sourceDefinedPrimaryKey')) {
                obj['sourceDefinedPrimaryKey'] = ApiClient.convertToType(data['sourceDefinedPrimaryKey'], [['String']]);
            }
            if (data.hasOwnProperty('supportedSyncModes')) {
                obj['supportedSyncModes'] = ApiClient.convertToType(data['supportedSyncModes'], [SyncMode]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AirbyteStream</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AirbyteStream</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AirbyteStream.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['defaultCursorField'])) {
            throw new Error("Expected the field `defaultCursorField` to be an array in the JSON data but got " + data['defaultCursorField']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['namespace'] && !(typeof data['namespace'] === 'string' || data['namespace'] instanceof String)) {
            throw new Error("Expected the field `namespace` to be a primitive type in the JSON string but got " + data['namespace']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['sourceDefinedPrimaryKey'])) {
            throw new Error("Expected the field `sourceDefinedPrimaryKey` to be an array in the JSON data but got " + data['sourceDefinedPrimaryKey']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['supportedSyncModes'])) {
            throw new Error("Expected the field `supportedSyncModes` to be an array in the JSON data but got " + data['supportedSyncModes']);
        }

        return true;
    }


}

AirbyteStream.RequiredProperties = ["name"];

/**
 * Path to the field that will be used to determine if a record is new or modified since the last sync. If not provided by the source, the end user will have to specify the comparable themselves.
 * @member {Array.<String>} defaultCursorField
 */
AirbyteStream.prototype['defaultCursorField'] = undefined;

/**
 * Stream schema using Json Schema specs.
 * @member {Object} jsonSchema
 */
AirbyteStream.prototype['jsonSchema'] = undefined;

/**
 * Stream's name.
 * @member {String} name
 */
AirbyteStream.prototype['name'] = undefined;

/**
 * Optional Source-defined namespace. Airbyte streams from the same sources should have the same namespace. Currently only used by JDBC destinations to determine what schema to write to.
 * @member {String} namespace
 */
AirbyteStream.prototype['namespace'] = undefined;

/**
 * If the source defines the cursor field, then any other cursor field inputs will be ignored. If it does not, either the user_provided one is used, or the default one is used as a backup.
 * @member {Boolean} sourceDefinedCursor
 */
AirbyteStream.prototype['sourceDefinedCursor'] = undefined;

/**
 * If the source defines the primary key, paths to the fields that will be used as a primary key. If not provided by the source, the end user will have to specify the primary key themselves.
 * @member {Array.<Array.<String>>} sourceDefinedPrimaryKey
 */
AirbyteStream.prototype['sourceDefinedPrimaryKey'] = undefined;

/**
 * @member {Array.<module:model/SyncMode>} supportedSyncModes
 */
AirbyteStream.prototype['supportedSyncModes'] = undefined;






export default AirbyteStream;

