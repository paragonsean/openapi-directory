/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NormalizationDestinationDefinitionConfig model module.
 * @module model/NormalizationDestinationDefinitionConfig
 * @version 1.0.0
 */
class NormalizationDestinationDefinitionConfig {
    /**
     * Constructs a new <code>NormalizationDestinationDefinitionConfig</code>.
     * describes a normalization config for destination definition
     * @alias module:model/NormalizationDestinationDefinitionConfig
     * @param supported {Boolean} whether the destination definition supports normalization.
     */
    constructor(supported) { 
        
        NormalizationDestinationDefinitionConfig.initialize(this, supported);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, supported) { 
        obj['supported'] = supported || false;
    }

    /**
     * Constructs a <code>NormalizationDestinationDefinitionConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NormalizationDestinationDefinitionConfig} obj Optional instance to populate.
     * @return {module:model/NormalizationDestinationDefinitionConfig} The populated <code>NormalizationDestinationDefinitionConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NormalizationDestinationDefinitionConfig();

            if (data.hasOwnProperty('normalizationIntegrationType')) {
                obj['normalizationIntegrationType'] = ApiClient.convertToType(data['normalizationIntegrationType'], 'String');
            }
            if (data.hasOwnProperty('normalizationRepository')) {
                obj['normalizationRepository'] = ApiClient.convertToType(data['normalizationRepository'], 'String');
            }
            if (data.hasOwnProperty('normalizationTag')) {
                obj['normalizationTag'] = ApiClient.convertToType(data['normalizationTag'], 'String');
            }
            if (data.hasOwnProperty('supported')) {
                obj['supported'] = ApiClient.convertToType(data['supported'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NormalizationDestinationDefinitionConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NormalizationDestinationDefinitionConfig</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NormalizationDestinationDefinitionConfig.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['normalizationIntegrationType'] && !(typeof data['normalizationIntegrationType'] === 'string' || data['normalizationIntegrationType'] instanceof String)) {
            throw new Error("Expected the field `normalizationIntegrationType` to be a primitive type in the JSON string but got " + data['normalizationIntegrationType']);
        }
        // ensure the json data is a string
        if (data['normalizationRepository'] && !(typeof data['normalizationRepository'] === 'string' || data['normalizationRepository'] instanceof String)) {
            throw new Error("Expected the field `normalizationRepository` to be a primitive type in the JSON string but got " + data['normalizationRepository']);
        }
        // ensure the json data is a string
        if (data['normalizationTag'] && !(typeof data['normalizationTag'] === 'string' || data['normalizationTag'] instanceof String)) {
            throw new Error("Expected the field `normalizationTag` to be a primitive type in the JSON string but got " + data['normalizationTag']);
        }

        return true;
    }


}

NormalizationDestinationDefinitionConfig.RequiredProperties = ["supported"];

/**
 * a field indicating the type of integration dialect to use for normalization.
 * @member {String} normalizationIntegrationType
 */
NormalizationDestinationDefinitionConfig.prototype['normalizationIntegrationType'] = undefined;

/**
 * a field indicating the name of the repository to be used for normalization. If the value of the flag is NULL - normalization is not used.
 * @member {String} normalizationRepository
 */
NormalizationDestinationDefinitionConfig.prototype['normalizationRepository'] = undefined;

/**
 * a field indicating the tag of the docker repository to be used for normalization.
 * @member {String} normalizationTag
 */
NormalizationDestinationDefinitionConfig.prototype['normalizationTag'] = undefined;

/**
 * whether the destination definition supports normalization.
 * @member {Boolean} supported
 * @default false
 */
NormalizationDestinationDefinitionConfig.prototype['supported'] = false;






export default NormalizationDestinationDefinitionConfig;

