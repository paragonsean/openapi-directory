/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OAuth2Specification from './OAuth2Specification';

/**
 * The AuthSpecification model module.
 * @module model/AuthSpecification
 * @version 1.0.0
 */
class AuthSpecification {
    /**
     * Constructs a new <code>AuthSpecification</code>.
     * @alias module:model/AuthSpecification
     */
    constructor() { 
        
        AuthSpecification.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AuthSpecification</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AuthSpecification} obj Optional instance to populate.
     * @return {module:model/AuthSpecification} The populated <code>AuthSpecification</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AuthSpecification();

            if (data.hasOwnProperty('auth_type')) {
                obj['auth_type'] = ApiClient.convertToType(data['auth_type'], 'String');
            }
            if (data.hasOwnProperty('oauth2Specification')) {
                obj['oauth2Specification'] = OAuth2Specification.constructFromObject(data['oauth2Specification']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AuthSpecification</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AuthSpecification</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['auth_type'] && !(typeof data['auth_type'] === 'string' || data['auth_type'] instanceof String)) {
            throw new Error("Expected the field `auth_type` to be a primitive type in the JSON string but got " + data['auth_type']);
        }
        // validate the optional field `oauth2Specification`
        if (data['oauth2Specification']) { // data not null
          OAuth2Specification.validateJSON(data['oauth2Specification']);
        }

        return true;
    }


}



/**
 * @member {module:model/AuthSpecification.AuthTypeEnum} auth_type
 */
AuthSpecification.prototype['auth_type'] = undefined;

/**
 * @member {module:model/OAuth2Specification} oauth2Specification
 */
AuthSpecification.prototype['oauth2Specification'] = undefined;





/**
 * Allowed values for the <code>auth_type</code> property.
 * @enum {String}
 * @readonly
 */
AuthSpecification['AuthTypeEnum'] = {

    /**
     * value: "oauth2.0"
     * @const
     */
    "oauth2.0": "oauth2.0"
};



export default AuthSpecification;

