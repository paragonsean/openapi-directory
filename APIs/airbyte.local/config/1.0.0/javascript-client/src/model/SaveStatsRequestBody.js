/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AttemptStats from './AttemptStats';
import AttemptStreamStats from './AttemptStreamStats';

/**
 * The SaveStatsRequestBody model module.
 * @module model/SaveStatsRequestBody
 * @version 1.0.0
 */
class SaveStatsRequestBody {
    /**
     * Constructs a new <code>SaveStatsRequestBody</code>.
     * @alias module:model/SaveStatsRequestBody
     * @param attemptNumber {Number} 
     * @param jobId {Number} 
     * @param stats {module:model/AttemptStats} 
     */
    constructor(attemptNumber, jobId, stats) { 
        
        SaveStatsRequestBody.initialize(this, attemptNumber, jobId, stats);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, attemptNumber, jobId, stats) { 
        obj['attemptNumber'] = attemptNumber;
        obj['jobId'] = jobId;
        obj['stats'] = stats;
    }

    /**
     * Constructs a <code>SaveStatsRequestBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SaveStatsRequestBody} obj Optional instance to populate.
     * @return {module:model/SaveStatsRequestBody} The populated <code>SaveStatsRequestBody</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SaveStatsRequestBody();

            if (data.hasOwnProperty('attemptNumber')) {
                obj['attemptNumber'] = ApiClient.convertToType(data['attemptNumber'], 'Number');
            }
            if (data.hasOwnProperty('jobId')) {
                obj['jobId'] = ApiClient.convertToType(data['jobId'], 'Number');
            }
            if (data.hasOwnProperty('stats')) {
                obj['stats'] = AttemptStats.constructFromObject(data['stats']);
            }
            if (data.hasOwnProperty('streamStats')) {
                obj['streamStats'] = ApiClient.convertToType(data['streamStats'], [AttemptStreamStats]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SaveStatsRequestBody</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SaveStatsRequestBody</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SaveStatsRequestBody.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `stats`
        if (data['stats']) { // data not null
          AttemptStats.validateJSON(data['stats']);
        }
        if (data['streamStats']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['streamStats'])) {
                throw new Error("Expected the field `streamStats` to be an array in the JSON data but got " + data['streamStats']);
            }
            // validate the optional field `streamStats` (array)
            for (const item of data['streamStats']) {
                AttemptStreamStats.validateJSON(item);
            };
        }

        return true;
    }


}

SaveStatsRequestBody.RequiredProperties = ["attemptNumber", "jobId", "stats"];

/**
 * @member {Number} attemptNumber
 */
SaveStatsRequestBody.prototype['attemptNumber'] = undefined;

/**
 * @member {Number} jobId
 */
SaveStatsRequestBody.prototype['jobId'] = undefined;

/**
 * @member {module:model/AttemptStats} stats
 */
SaveStatsRequestBody.prototype['stats'] = undefined;

/**
 * @member {Array.<module:model/AttemptStreamStats>} streamStats
 */
SaveStatsRequestBody.prototype['streamStats'] = undefined;






export default SaveStatsRequestBody;

