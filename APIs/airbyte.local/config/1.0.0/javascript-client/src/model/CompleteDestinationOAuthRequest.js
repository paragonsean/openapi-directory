/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CompleteDestinationOAuthRequest model module.
 * @module model/CompleteDestinationOAuthRequest
 * @version 1.0.0
 */
class CompleteDestinationOAuthRequest {
    /**
     * Constructs a new <code>CompleteDestinationOAuthRequest</code>.
     * @alias module:model/CompleteDestinationOAuthRequest
     * @param destinationDefinitionId {String} 
     * @param workspaceId {String} 
     */
    constructor(destinationDefinitionId, workspaceId) { 
        
        CompleteDestinationOAuthRequest.initialize(this, destinationDefinitionId, workspaceId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationDefinitionId, workspaceId) { 
        obj['destinationDefinitionId'] = destinationDefinitionId;
        obj['workspaceId'] = workspaceId;
    }

    /**
     * Constructs a <code>CompleteDestinationOAuthRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CompleteDestinationOAuthRequest} obj Optional instance to populate.
     * @return {module:model/CompleteDestinationOAuthRequest} The populated <code>CompleteDestinationOAuthRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CompleteDestinationOAuthRequest();

            if (data.hasOwnProperty('destinationDefinitionId')) {
                obj['destinationDefinitionId'] = ApiClient.convertToType(data['destinationDefinitionId'], 'String');
            }
            if (data.hasOwnProperty('destinationId')) {
                obj['destinationId'] = ApiClient.convertToType(data['destinationId'], 'String');
            }
            if (data.hasOwnProperty('oAuthInputConfiguration')) {
                obj['oAuthInputConfiguration'] = ApiClient.convertToType(data['oAuthInputConfiguration'], Object);
            }
            if (data.hasOwnProperty('queryParams')) {
                obj['queryParams'] = ApiClient.convertToType(data['queryParams'], {'String': Object});
            }
            if (data.hasOwnProperty('redirectUrl')) {
                obj['redirectUrl'] = ApiClient.convertToType(data['redirectUrl'], 'String');
            }
            if (data.hasOwnProperty('workspaceId')) {
                obj['workspaceId'] = ApiClient.convertToType(data['workspaceId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CompleteDestinationOAuthRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CompleteDestinationOAuthRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CompleteDestinationOAuthRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['destinationDefinitionId'] && !(typeof data['destinationDefinitionId'] === 'string' || data['destinationDefinitionId'] instanceof String)) {
            throw new Error("Expected the field `destinationDefinitionId` to be a primitive type in the JSON string but got " + data['destinationDefinitionId']);
        }
        // ensure the json data is a string
        if (data['destinationId'] && !(typeof data['destinationId'] === 'string' || data['destinationId'] instanceof String)) {
            throw new Error("Expected the field `destinationId` to be a primitive type in the JSON string but got " + data['destinationId']);
        }
        // ensure the json data is a string
        if (data['redirectUrl'] && !(typeof data['redirectUrl'] === 'string' || data['redirectUrl'] instanceof String)) {
            throw new Error("Expected the field `redirectUrl` to be a primitive type in the JSON string but got " + data['redirectUrl']);
        }
        // ensure the json data is a string
        if (data['workspaceId'] && !(typeof data['workspaceId'] === 'string' || data['workspaceId'] instanceof String)) {
            throw new Error("Expected the field `workspaceId` to be a primitive type in the JSON string but got " + data['workspaceId']);
        }

        return true;
    }


}

CompleteDestinationOAuthRequest.RequiredProperties = ["destinationDefinitionId", "workspaceId"];

/**
 * @member {String} destinationDefinitionId
 */
CompleteDestinationOAuthRequest.prototype['destinationDefinitionId'] = undefined;

/**
 * @member {String} destinationId
 */
CompleteDestinationOAuthRequest.prototype['destinationId'] = undefined;

/**
 * The values required to configure OAuth flows. The schema for this must match the `OAuthConfigSpecification.oauthUserInputFromConnectorConfigSpecification` schema.
 * @member {Object} oAuthInputConfiguration
 */
CompleteDestinationOAuthRequest.prototype['oAuthInputConfiguration'] = undefined;

/**
 * The query parameters present in the redirect URL after a user granted consent e.g auth code
 * @member {Object.<String, Object>} queryParams
 */
CompleteDestinationOAuthRequest.prototype['queryParams'] = undefined;

/**
 * When completing OAuth flow to gain an access token, some API sometimes requires to verify that the app re-send the redirectUrl that was used when consent was given.
 * @member {String} redirectUrl
 */
CompleteDestinationOAuthRequest.prototype['redirectUrl'] = undefined;

/**
 * @member {String} workspaceId
 */
CompleteDestinationOAuthRequest.prototype['workspaceId'] = undefined;






export default CompleteDestinationOAuthRequest;

