/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AdvancedAuth from './AdvancedAuth';
import AuthSpecification from './AuthSpecification';
import DestinationSyncMode from './DestinationSyncMode';
import SynchronousJobRead from './SynchronousJobRead';

/**
 * The DestinationDefinitionSpecificationRead model module.
 * @module model/DestinationDefinitionSpecificationRead
 * @version 1.0.0
 */
class DestinationDefinitionSpecificationRead {
    /**
     * Constructs a new <code>DestinationDefinitionSpecificationRead</code>.
     * @alias module:model/DestinationDefinitionSpecificationRead
     * @param destinationDefinitionId {String} 
     * @param jobInfo {module:model/SynchronousJobRead} 
     */
    constructor(destinationDefinitionId, jobInfo) { 
        
        DestinationDefinitionSpecificationRead.initialize(this, destinationDefinitionId, jobInfo);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationDefinitionId, jobInfo) { 
        obj['destinationDefinitionId'] = destinationDefinitionId;
        obj['jobInfo'] = jobInfo;
    }

    /**
     * Constructs a <code>DestinationDefinitionSpecificationRead</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DestinationDefinitionSpecificationRead} obj Optional instance to populate.
     * @return {module:model/DestinationDefinitionSpecificationRead} The populated <code>DestinationDefinitionSpecificationRead</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DestinationDefinitionSpecificationRead();

            if (data.hasOwnProperty('advancedAuth')) {
                obj['advancedAuth'] = AdvancedAuth.constructFromObject(data['advancedAuth']);
            }
            if (data.hasOwnProperty('authSpecification')) {
                obj['authSpecification'] = AuthSpecification.constructFromObject(data['authSpecification']);
            }
            if (data.hasOwnProperty('connectionSpecification')) {
                obj['connectionSpecification'] = ApiClient.convertToType(data['connectionSpecification'], Object);
            }
            if (data.hasOwnProperty('destinationDefinitionId')) {
                obj['destinationDefinitionId'] = ApiClient.convertToType(data['destinationDefinitionId'], 'String');
            }
            if (data.hasOwnProperty('documentationUrl')) {
                obj['documentationUrl'] = ApiClient.convertToType(data['documentationUrl'], 'String');
            }
            if (data.hasOwnProperty('jobInfo')) {
                obj['jobInfo'] = SynchronousJobRead.constructFromObject(data['jobInfo']);
            }
            if (data.hasOwnProperty('supportedDestinationSyncModes')) {
                obj['supportedDestinationSyncModes'] = ApiClient.convertToType(data['supportedDestinationSyncModes'], [DestinationSyncMode]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DestinationDefinitionSpecificationRead</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DestinationDefinitionSpecificationRead</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DestinationDefinitionSpecificationRead.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `advancedAuth`
        if (data['advancedAuth']) { // data not null
          AdvancedAuth.validateJSON(data['advancedAuth']);
        }
        // validate the optional field `authSpecification`
        if (data['authSpecification']) { // data not null
          AuthSpecification.validateJSON(data['authSpecification']);
        }
        // ensure the json data is a string
        if (data['destinationDefinitionId'] && !(typeof data['destinationDefinitionId'] === 'string' || data['destinationDefinitionId'] instanceof String)) {
            throw new Error("Expected the field `destinationDefinitionId` to be a primitive type in the JSON string but got " + data['destinationDefinitionId']);
        }
        // ensure the json data is a string
        if (data['documentationUrl'] && !(typeof data['documentationUrl'] === 'string' || data['documentationUrl'] instanceof String)) {
            throw new Error("Expected the field `documentationUrl` to be a primitive type in the JSON string but got " + data['documentationUrl']);
        }
        // validate the optional field `jobInfo`
        if (data['jobInfo']) { // data not null
          SynchronousJobRead.validateJSON(data['jobInfo']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['supportedDestinationSyncModes'])) {
            throw new Error("Expected the field `supportedDestinationSyncModes` to be an array in the JSON data but got " + data['supportedDestinationSyncModes']);
        }

        return true;
    }


}

DestinationDefinitionSpecificationRead.RequiredProperties = ["destinationDefinitionId", "jobInfo"];

/**
 * @member {module:model/AdvancedAuth} advancedAuth
 */
DestinationDefinitionSpecificationRead.prototype['advancedAuth'] = undefined;

/**
 * @member {module:model/AuthSpecification} authSpecification
 */
DestinationDefinitionSpecificationRead.prototype['authSpecification'] = undefined;

/**
 * The specification for what values are required to configure the destinationDefinition.
 * @member {Object} connectionSpecification
 */
DestinationDefinitionSpecificationRead.prototype['connectionSpecification'] = undefined;

/**
 * @member {String} destinationDefinitionId
 */
DestinationDefinitionSpecificationRead.prototype['destinationDefinitionId'] = undefined;

/**
 * @member {String} documentationUrl
 */
DestinationDefinitionSpecificationRead.prototype['documentationUrl'] = undefined;

/**
 * @member {module:model/SynchronousJobRead} jobInfo
 */
DestinationDefinitionSpecificationRead.prototype['jobInfo'] = undefined;

/**
 * @member {Array.<module:model/DestinationSyncMode>} supportedDestinationSyncModes
 */
DestinationDefinitionSpecificationRead.prototype['supportedDestinationSyncModes'] = undefined;






export default DestinationDefinitionSpecificationRead;

