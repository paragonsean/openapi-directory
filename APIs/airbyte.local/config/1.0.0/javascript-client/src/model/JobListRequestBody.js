/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JobConfigType from './JobConfigType';
import Pagination from './Pagination';

/**
 * The JobListRequestBody model module.
 * @module model/JobListRequestBody
 * @version 1.0.0
 */
class JobListRequestBody {
    /**
     * Constructs a new <code>JobListRequestBody</code>.
     * @alias module:model/JobListRequestBody
     * @param configId {String} 
     * @param configTypes {Array.<module:model/JobConfigType>} 
     */
    constructor(configId, configTypes) { 
        
        JobListRequestBody.initialize(this, configId, configTypes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, configId, configTypes) { 
        obj['configId'] = configId;
        obj['configTypes'] = configTypes;
    }

    /**
     * Constructs a <code>JobListRequestBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobListRequestBody} obj Optional instance to populate.
     * @return {module:model/JobListRequestBody} The populated <code>JobListRequestBody</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobListRequestBody();

            if (data.hasOwnProperty('configId')) {
                obj['configId'] = ApiClient.convertToType(data['configId'], 'String');
            }
            if (data.hasOwnProperty('configTypes')) {
                obj['configTypes'] = ApiClient.convertToType(data['configTypes'], [JobConfigType]);
            }
            if (data.hasOwnProperty('includingJobId')) {
                obj['includingJobId'] = ApiClient.convertToType(data['includingJobId'], 'Number');
            }
            if (data.hasOwnProperty('pagination')) {
                obj['pagination'] = Pagination.constructFromObject(data['pagination']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobListRequestBody</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobListRequestBody</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobListRequestBody.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['configId'] && !(typeof data['configId'] === 'string' || data['configId'] instanceof String)) {
            throw new Error("Expected the field `configId` to be a primitive type in the JSON string but got " + data['configId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['configTypes'])) {
            throw new Error("Expected the field `configTypes` to be an array in the JSON data but got " + data['configTypes']);
        }
        // validate the optional field `pagination`
        if (data['pagination']) { // data not null
          Pagination.validateJSON(data['pagination']);
        }

        return true;
    }


}

JobListRequestBody.RequiredProperties = ["configId", "configTypes"];

/**
 * @member {String} configId
 */
JobListRequestBody.prototype['configId'] = undefined;

/**
 * @member {Array.<module:model/JobConfigType>} configTypes
 */
JobListRequestBody.prototype['configTypes'] = undefined;

/**
 * @member {Number} includingJobId
 */
JobListRequestBody.prototype['includingJobId'] = undefined;

/**
 * @member {module:model/Pagination} pagination
 */
JobListRequestBody.prototype['pagination'] = undefined;






export default JobListRequestBody;

