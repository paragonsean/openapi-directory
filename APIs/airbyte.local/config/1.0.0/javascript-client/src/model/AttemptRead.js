/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AttemptFailureSummary from './AttemptFailureSummary';
import AttemptStats from './AttemptStats';
import AttemptStatus from './AttemptStatus';
import AttemptStreamStats from './AttemptStreamStats';

/**
 * The AttemptRead model module.
 * @module model/AttemptRead
 * @version 1.0.0
 */
class AttemptRead {
    /**
     * Constructs a new <code>AttemptRead</code>.
     * @alias module:model/AttemptRead
     * @param createdAt {Number} 
     * @param id {Number} 
     * @param status {module:model/AttemptStatus} 
     * @param updatedAt {Number} 
     */
    constructor(createdAt, id, status, updatedAt) { 
        
        AttemptRead.initialize(this, createdAt, id, status, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, createdAt, id, status, updatedAt) { 
        obj['createdAt'] = createdAt;
        obj['id'] = id;
        obj['status'] = status;
        obj['updatedAt'] = updatedAt;
    }

    /**
     * Constructs a <code>AttemptRead</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AttemptRead} obj Optional instance to populate.
     * @return {module:model/AttemptRead} The populated <code>AttemptRead</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AttemptRead();

            if (data.hasOwnProperty('bytesSynced')) {
                obj['bytesSynced'] = ApiClient.convertToType(data['bytesSynced'], 'Number');
            }
            if (data.hasOwnProperty('createdAt')) {
                obj['createdAt'] = ApiClient.convertToType(data['createdAt'], 'Number');
            }
            if (data.hasOwnProperty('endedAt')) {
                obj['endedAt'] = ApiClient.convertToType(data['endedAt'], 'Number');
            }
            if (data.hasOwnProperty('failureSummary')) {
                obj['failureSummary'] = AttemptFailureSummary.constructFromObject(data['failureSummary']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('recordsSynced')) {
                obj['recordsSynced'] = ApiClient.convertToType(data['recordsSynced'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = AttemptStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('streamStats')) {
                obj['streamStats'] = ApiClient.convertToType(data['streamStats'], [AttemptStreamStats]);
            }
            if (data.hasOwnProperty('totalStats')) {
                obj['totalStats'] = AttemptStats.constructFromObject(data['totalStats']);
            }
            if (data.hasOwnProperty('updatedAt')) {
                obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AttemptRead</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AttemptRead</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AttemptRead.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `failureSummary`
        if (data['failureSummary']) { // data not null
          AttemptFailureSummary.validateJSON(data['failureSummary']);
        }
        if (data['streamStats']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['streamStats'])) {
                throw new Error("Expected the field `streamStats` to be an array in the JSON data but got " + data['streamStats']);
            }
            // validate the optional field `streamStats` (array)
            for (const item of data['streamStats']) {
                AttemptStreamStats.validateJSON(item);
            };
        }
        // validate the optional field `totalStats`
        if (data['totalStats']) { // data not null
          AttemptStats.validateJSON(data['totalStats']);
        }

        return true;
    }


}

AttemptRead.RequiredProperties = ["createdAt", "id", "status", "updatedAt"];

/**
 * @member {Number} bytesSynced
 */
AttemptRead.prototype['bytesSynced'] = undefined;

/**
 * @member {Number} createdAt
 */
AttemptRead.prototype['createdAt'] = undefined;

/**
 * @member {Number} endedAt
 */
AttemptRead.prototype['endedAt'] = undefined;

/**
 * @member {module:model/AttemptFailureSummary} failureSummary
 */
AttemptRead.prototype['failureSummary'] = undefined;

/**
 * @member {Number} id
 */
AttemptRead.prototype['id'] = undefined;

/**
 * @member {Number} recordsSynced
 */
AttemptRead.prototype['recordsSynced'] = undefined;

/**
 * @member {module:model/AttemptStatus} status
 */
AttemptRead.prototype['status'] = undefined;

/**
 * @member {Array.<module:model/AttemptStreamStats>} streamStats
 */
AttemptRead.prototype['streamStats'] = undefined;

/**
 * @member {module:model/AttemptStats} totalStats
 */
AttemptRead.prototype['totalStats'] = undefined;

/**
 * @member {Number} updatedAt
 */
AttemptRead.prototype['updatedAt'] = undefined;






export default AttemptRead;

