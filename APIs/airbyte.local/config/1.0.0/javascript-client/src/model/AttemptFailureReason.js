/**
 * Airbyte Configuration API
 * Airbyte Configuration API [https://airbyte.io](https://airbyte.io).  This API is a collection of HTTP RPC-style methods. While it is not a REST API, those familiar with REST should find the conventions of this API recognizable.  Here are some conventions that this API follows: * All endpoints are http POST methods. * All endpoints accept data via `application/json` request bodies. The API does not accept any data via query params. * The naming convention for endpoints is: localhost:8000/{VERSION}/{METHOD_FAMILY}/{METHOD_NAME} e.g. `localhost:8000/v1/connections/create`. * For all `update` methods, the whole object must be passed in, even the fields that did not change.  Authentication (OSS): * When authenticating to the Configuration API, you must use Basic Authentication by setting the Authentication Header to Basic and base64 encoding the username and password (which are `airbyte` and `password` by default - so base64 encoding `airbyte:password` results in `YWlyYnl0ZTpwYXNzd29yZA==`). So the full header reads `'Authorization': \"Basic YWlyYnl0ZTpwYXNzd29yZA==\"` 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@airbyte.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AttemptFailureOrigin from './AttemptFailureOrigin';
import AttemptFailureType from './AttemptFailureType';

/**
 * The AttemptFailureReason model module.
 * @module model/AttemptFailureReason
 * @version 1.0.0
 */
class AttemptFailureReason {
    /**
     * Constructs a new <code>AttemptFailureReason</code>.
     * @alias module:model/AttemptFailureReason
     * @param timestamp {Number} 
     */
    constructor(timestamp) { 
        
        AttemptFailureReason.initialize(this, timestamp);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, timestamp) { 
        obj['timestamp'] = timestamp;
    }

    /**
     * Constructs a <code>AttemptFailureReason</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AttemptFailureReason} obj Optional instance to populate.
     * @return {module:model/AttemptFailureReason} The populated <code>AttemptFailureReason</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AttemptFailureReason();

            if (data.hasOwnProperty('externalMessage')) {
                obj['externalMessage'] = ApiClient.convertToType(data['externalMessage'], 'String');
            }
            if (data.hasOwnProperty('failureOrigin')) {
                obj['failureOrigin'] = AttemptFailureOrigin.constructFromObject(data['failureOrigin']);
            }
            if (data.hasOwnProperty('failureType')) {
                obj['failureType'] = AttemptFailureType.constructFromObject(data['failureType']);
            }
            if (data.hasOwnProperty('internalMessage')) {
                obj['internalMessage'] = ApiClient.convertToType(data['internalMessage'], 'String');
            }
            if (data.hasOwnProperty('retryable')) {
                obj['retryable'] = ApiClient.convertToType(data['retryable'], 'Boolean');
            }
            if (data.hasOwnProperty('stacktrace')) {
                obj['stacktrace'] = ApiClient.convertToType(data['stacktrace'], 'String');
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AttemptFailureReason</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AttemptFailureReason</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AttemptFailureReason.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['externalMessage'] && !(typeof data['externalMessage'] === 'string' || data['externalMessage'] instanceof String)) {
            throw new Error("Expected the field `externalMessage` to be a primitive type in the JSON string but got " + data['externalMessage']);
        }
        // ensure the json data is a string
        if (data['internalMessage'] && !(typeof data['internalMessage'] === 'string' || data['internalMessage'] instanceof String)) {
            throw new Error("Expected the field `internalMessage` to be a primitive type in the JSON string but got " + data['internalMessage']);
        }
        // ensure the json data is a string
        if (data['stacktrace'] && !(typeof data['stacktrace'] === 'string' || data['stacktrace'] instanceof String)) {
            throw new Error("Expected the field `stacktrace` to be a primitive type in the JSON string but got " + data['stacktrace']);
        }

        return true;
    }


}

AttemptFailureReason.RequiredProperties = ["timestamp"];

/**
 * @member {String} externalMessage
 */
AttemptFailureReason.prototype['externalMessage'] = undefined;

/**
 * @member {module:model/AttemptFailureOrigin} failureOrigin
 */
AttemptFailureReason.prototype['failureOrigin'] = undefined;

/**
 * @member {module:model/AttemptFailureType} failureType
 */
AttemptFailureReason.prototype['failureType'] = undefined;

/**
 * @member {String} internalMessage
 */
AttemptFailureReason.prototype['internalMessage'] = undefined;

/**
 * True if it is known that retrying may succeed, e.g. for a transient failure. False if it is known that a retry will not succeed, e.g. for a configuration issue. If not set, retryable status is not well known.
 * @member {Boolean} retryable
 */
AttemptFailureReason.prototype['retryable'] = undefined;

/**
 * @member {String} stacktrace
 */
AttemptFailureReason.prototype['stacktrace'] = undefined;

/**
 * @member {Number} timestamp
 */
AttemptFailureReason.prototype['timestamp'] = undefined;






export default AttemptFailureReason;

