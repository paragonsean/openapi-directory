/**
 * Authorized Partner API Specification
 * To access files in user’s DigiLocker account from your application, you must first obtain user’s authorization.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@digitallocker.gov.in
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIDigiLockerMetaAPIsApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIDigiLockerMetaAPIsApi::OAIDigiLockerMetaAPIsApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIDigiLockerMetaAPIsApi::~OAIDigiLockerMetaAPIsApi() {
}

void OAIDigiLockerMetaAPIsApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://betaapi.digitallocker.gov.in/public"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("get_List_of_Documents_Provided_by_an_Issuer_id", defaultConf);
    _serverIndices.insert("get_List_of_Documents_Provided_by_an_Issuer_id", 0);
    _serverConfigs.insert("get_List_of_Issuers_id", defaultConf);
    _serverIndices.insert("get_List_of_Issuers_id", 0);
    _serverConfigs.insert("get_Search_Parameters_for_a_Document_id", defaultConf);
    _serverIndices.insert("get_Search_Parameters_for_a_Document_id", 0);
    _serverConfigs.insert("get_Statistics_id", defaultConf);
    _serverIndices.insert("get_Statistics_id", 0);
    _serverConfigs.insert("push_URI_to_Account_id", defaultConf);
    _serverIndices.insert("push_URI_to_Account_id", 0);
    _serverConfigs.insert("verify_Account_id", defaultConf);
    _serverIndices.insert("verify_Account_id", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIDigiLockerMetaAPIsApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIDigiLockerMetaAPIsApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIDigiLockerMetaAPIsApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIDigiLockerMetaAPIsApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIDigiLockerMetaAPIsApi::setUsername(const QString &username) {
    _username = username;
}

void OAIDigiLockerMetaAPIsApi::setPassword(const QString &password) {
    _password = password;
}


void OAIDigiLockerMetaAPIsApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIDigiLockerMetaAPIsApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIDigiLockerMetaAPIsApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIDigiLockerMetaAPIsApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIDigiLockerMetaAPIsApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIDigiLockerMetaAPIsApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIDigiLockerMetaAPIsApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIDigiLockerMetaAPIsApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIDigiLockerMetaAPIsApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIDigiLockerMetaAPIsApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIDigiLockerMetaAPIsApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIDigiLockerMetaAPIsApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIDigiLockerMetaAPIsApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIDigiLockerMetaAPIsApi::get_List_of_Documents_Provided_by_an_Issuer_id(const ::OpenAPI::OptionalParam<QString> &clientid, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &hmac, const ::OpenAPI::OptionalParam<QString> &orgid, const ::OpenAPI::OptionalParam<QString> &ts) {
    QString fullPath = QString(_serverConfigs["get_List_of_Documents_Provided_by_an_Issuer_id"][_serverIndices.value("get_List_of_Documents_Provided_by_an_Issuer_id")].URL()+"/oauth2/1/pull/doctype");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (clientid.hasValue())
    {
        input.add_var("clientid", ::OpenAPI::toStringValue(clientid.value()));
    }
    if (hmac.hasValue())
    {
        input.add_file("hmac", hmac.value().local_filename, hmac.value().request_filename, hmac.value().mime_type);
    }
    if (orgid.hasValue())
    {
        input.add_var("orgid", ::OpenAPI::toStringValue(orgid.value()));
    }
    if (ts.hasValue())
    {
        input.add_var("ts", ::OpenAPI::toStringValue(ts.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_List_of_Documents_Provided_by_an_Issuer_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _OauthMethod = 3;
    _authFlow.unlink();
    _implicitFlow.unlink();
    _passwordFlow.unlink();
    _credentialFlow.link();
    QStringList scope;
    auto token = _credentialFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_List_of_Documents_Provided_by_an_Issuer_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;


    worker->execute(&input);
}

void OAIDigiLockerMetaAPIsApi::get_List_of_Documents_Provided_by_an_Issuer_idCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIDocTypeResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT get_List_of_Documents_Provided_by_an_Issuer_idSignal(output);
        Q_EMIT get_List_of_Documents_Provided_by_an_Issuer_idSignalFull(worker, output);

    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_credentialFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        QString scopeStr = scope.join(" ");
        QString tokenUrl("https://betaapi.digitallocker.gov.in/public/oauth2/1/token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _credentialFlow.setVariables(tokenUrl , scopeStr, "clientId", "clientSecret");
        Q_EMIT _credentialFlow.authenticationNeeded();

    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT get_List_of_Documents_Provided_by_an_Issuer_idSignalE(output, error_type, error_str);
        Q_EMIT get_List_of_Documents_Provided_by_an_Issuer_idSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT get_List_of_Documents_Provided_by_an_Issuer_idSignalError(output, error_type, error_str);
        Q_EMIT get_List_of_Documents_Provided_by_an_Issuer_idSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIDigiLockerMetaAPIsApi::get_List_of_Issuers_id(const ::OpenAPI::OptionalParam<QString> &clientid, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &hmac, const ::OpenAPI::OptionalParam<QString> &ts) {
    QString fullPath = QString(_serverConfigs["get_List_of_Issuers_id"][_serverIndices.value("get_List_of_Issuers_id")].URL()+"/oauth2/1/pull/issuers");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (clientid.hasValue())
    {
        input.add_var("clientid", ::OpenAPI::toStringValue(clientid.value()));
    }
    if (hmac.hasValue())
    {
        input.add_file("hmac", hmac.value().local_filename, hmac.value().request_filename, hmac.value().mime_type);
    }
    if (ts.hasValue())
    {
        input.add_var("ts", ::OpenAPI::toStringValue(ts.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_List_of_Issuers_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _OauthMethod = 3;
    _authFlow.unlink();
    _implicitFlow.unlink();
    _passwordFlow.unlink();
    _credentialFlow.link();
    QStringList scope;
    auto token = _credentialFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_List_of_Issuers_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;


    worker->execute(&input);
}

void OAIDigiLockerMetaAPIsApi::get_List_of_Issuers_idCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIIssuerResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT get_List_of_Issuers_idSignal(output);
        Q_EMIT get_List_of_Issuers_idSignalFull(worker, output);

    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_credentialFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        QString scopeStr = scope.join(" ");
        QString tokenUrl("https://betaapi.digitallocker.gov.in/public/oauth2/1/token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _credentialFlow.setVariables(tokenUrl , scopeStr, "clientId", "clientSecret");
        Q_EMIT _credentialFlow.authenticationNeeded();

    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT get_List_of_Issuers_idSignalE(output, error_type, error_str);
        Q_EMIT get_List_of_Issuers_idSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT get_List_of_Issuers_idSignalError(output, error_type, error_str);
        Q_EMIT get_List_of_Issuers_idSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIDigiLockerMetaAPIsApi::get_Search_Parameters_for_a_Document_id(const ::OpenAPI::OptionalParam<QString> &clientid, const ::OpenAPI::OptionalParam<QString> &doctype, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &hmac, const ::OpenAPI::OptionalParam<QString> &orgid, const ::OpenAPI::OptionalParam<QString> &ts) {
    QString fullPath = QString(_serverConfigs["get_Search_Parameters_for_a_Document_id"][_serverIndices.value("get_Search_Parameters_for_a_Document_id")].URL()+"/oauth2/1/pull/parameters");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (clientid.hasValue())
    {
        input.add_var("clientid", ::OpenAPI::toStringValue(clientid.value()));
    }
    if (doctype.hasValue())
    {
        input.add_var("doctype", ::OpenAPI::toStringValue(doctype.value()));
    }
    if (hmac.hasValue())
    {
        input.add_file("hmac", hmac.value().local_filename, hmac.value().request_filename, hmac.value().mime_type);
    }
    if (orgid.hasValue())
    {
        input.add_var("orgid", ::OpenAPI::toStringValue(orgid.value()));
    }
    if (ts.hasValue())
    {
        input.add_var("ts", ::OpenAPI::toStringValue(ts.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_Search_Parameters_for_a_Document_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _OauthMethod = 3;
    _authFlow.unlink();
    _implicitFlow.unlink();
    _passwordFlow.unlink();
    _credentialFlow.link();
    QStringList scope;
    auto token = _credentialFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_Search_Parameters_for_a_Document_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;


    worker->execute(&input);
}

void OAIDigiLockerMetaAPIsApi::get_Search_Parameters_for_a_Document_idCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QSet<OAISearchParametersResponse_inner> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAISearchParametersResponse_inner val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.insert(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT get_Search_Parameters_for_a_Document_idSignal(output);
        Q_EMIT get_Search_Parameters_for_a_Document_idSignalFull(worker, output);

    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_credentialFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        QString scopeStr = scope.join(" ");
        QString tokenUrl("https://betaapi.digitallocker.gov.in/public/oauth2/1/token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _credentialFlow.setVariables(tokenUrl , scopeStr, "clientId", "clientSecret");
        Q_EMIT _credentialFlow.authenticationNeeded();

    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT get_Search_Parameters_for_a_Document_idSignalE(output, error_type, error_str);
        Q_EMIT get_Search_Parameters_for_a_Document_idSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT get_Search_Parameters_for_a_Document_idSignalError(output, error_type, error_str);
        Q_EMIT get_Search_Parameters_for_a_Document_idSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIDigiLockerMetaAPIsApi::get_Statistics_id(const ::OpenAPI::OptionalParam<QString> &clientid, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &hmac, const ::OpenAPI::OptionalParam<QString> &ts) {
    QString fullPath = QString(_serverConfigs["get_Statistics_id"][_serverIndices.value("get_Statistics_id")].URL()+"/statistics/1/counts");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (clientid.hasValue())
    {
        input.add_var("clientid", ::OpenAPI::toStringValue(clientid.value()));
    }
    if (hmac.hasValue())
    {
        input.add_file("hmac", hmac.value().local_filename, hmac.value().request_filename, hmac.value().mime_type);
    }
    if (ts.hasValue())
    {
        input.add_var("ts", ::OpenAPI::toStringValue(ts.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_Statistics_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _OauthMethod = 3;
    _authFlow.unlink();
    _implicitFlow.unlink();
    _passwordFlow.unlink();
    _credentialFlow.link();
    QStringList scope;
    auto token = _credentialFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::get_Statistics_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;


    worker->execute(&input);
}

void OAIDigiLockerMetaAPIsApi::get_Statistics_idCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIGetStatisticsResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT get_Statistics_idSignal(output);
        Q_EMIT get_Statistics_idSignalFull(worker, output);

    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_credentialFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        QString scopeStr = scope.join(" ");
        QString tokenUrl("https://betaapi.digitallocker.gov.in/public/oauth2/1/token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _credentialFlow.setVariables(tokenUrl , scopeStr, "clientId", "clientSecret");
        Q_EMIT _credentialFlow.authenticationNeeded();

    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT get_Statistics_idSignalE(output, error_type, error_str);
        Q_EMIT get_Statistics_idSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT get_Statistics_idSignalError(output, error_type, error_str);
        Q_EMIT get_Statistics_idSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIDigiLockerMetaAPIsApi::push_URI_to_Account_id(const ::OpenAPI::OptionalParam<QString> &action, const ::OpenAPI::OptionalParam<QString> &clientid, const ::OpenAPI::OptionalParam<qint32> &digilockerid, const ::OpenAPI::OptionalParam<QString> &docid, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &hmac, const ::OpenAPI::OptionalParam<QString> &issuedate, const ::OpenAPI::OptionalParam<QString> &ts, const ::OpenAPI::OptionalParam<QString> &uri, const ::OpenAPI::OptionalParam<qint32> &validfrom, const ::OpenAPI::OptionalParam<QString> &validto) {
    QString fullPath = QString(_serverConfigs["push_URI_to_Account_id"][_serverIndices.value("push_URI_to_Account_id")].URL()+"/account/1/pushuri");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (action.hasValue())
    {
        input.add_var("action", ::OpenAPI::toStringValue(action.value()));
    }
    if (clientid.hasValue())
    {
        input.add_var("clientid", ::OpenAPI::toStringValue(clientid.value()));
    }
    if (digilockerid.hasValue())
    {
        input.add_var("digilockerid", ::OpenAPI::toStringValue(digilockerid.value()));
    }
    if (docid.hasValue())
    {
        input.add_var("docid", ::OpenAPI::toStringValue(docid.value()));
    }
    if (hmac.hasValue())
    {
        input.add_file("hmac", hmac.value().local_filename, hmac.value().request_filename, hmac.value().mime_type);
    }
    if (issuedate.hasValue())
    {
        input.add_var("issuedate", ::OpenAPI::toStringValue(issuedate.value()));
    }
    if (ts.hasValue())
    {
        input.add_var("ts", ::OpenAPI::toStringValue(ts.value()));
    }
    if (uri.hasValue())
    {
        input.add_var("uri", ::OpenAPI::toStringValue(uri.value()));
    }
    if (validfrom.hasValue())
    {
        input.add_var("validfrom", ::OpenAPI::toStringValue(validfrom.value()));
    }
    if (validto.hasValue())
    {
        input.add_var("validto", ::OpenAPI::toStringValue(validto.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::push_URI_to_Account_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _OauthMethod = 3;
    _authFlow.unlink();
    _implicitFlow.unlink();
    _passwordFlow.unlink();
    _credentialFlow.link();
    QStringList scope;
    auto token = _credentialFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::push_URI_to_Account_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;


    worker->execute(&input);
}

void OAIDigiLockerMetaAPIsApi::push_URI_to_Account_idCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QJsonValue output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT push_URI_to_Account_idSignal(output);
        Q_EMIT push_URI_to_Account_idSignalFull(worker, output);

    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_credentialFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        QString scopeStr = scope.join(" ");
        QString tokenUrl("https://betaapi.digitallocker.gov.in/public/oauth2/1/token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _credentialFlow.setVariables(tokenUrl , scopeStr, "clientId", "clientSecret");
        Q_EMIT _credentialFlow.authenticationNeeded();

    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT push_URI_to_Account_idSignalE(output, error_type, error_str);
        Q_EMIT push_URI_to_Account_idSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT push_URI_to_Account_idSignalError(output, error_type, error_str);
        Q_EMIT push_URI_to_Account_idSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIDigiLockerMetaAPIsApi::verify_Account_id(const ::OpenAPI::OptionalParam<QString> &clientid, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &hmac, const ::OpenAPI::OptionalParam<qint32> &mobile, const ::OpenAPI::OptionalParam<QString> &ts, const ::OpenAPI::OptionalParam<qint32> &uid) {
    QString fullPath = QString(_serverConfigs["verify_Account_id"][_serverIndices.value("verify_Account_id")].URL()+"/account/2/verify");
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (clientid.hasValue())
    {
        input.add_var("clientid", ::OpenAPI::toStringValue(clientid.value()));
    }
    if (hmac.hasValue())
    {
        input.add_file("hmac", hmac.value().local_filename, hmac.value().request_filename, hmac.value().mime_type);
    }
    if (mobile.hasValue())
    {
        input.add_var("mobile", ::OpenAPI::toStringValue(mobile.value()));
    }
    if (ts.hasValue())
    {
        input.add_var("ts", ::OpenAPI::toStringValue(ts.value()));
    }
    if (uid.hasValue())
    {
        input.add_var("uid", ::OpenAPI::toStringValue(uid.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::verify_Account_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _OauthMethod = 3;
    _authFlow.unlink();
    _implicitFlow.unlink();
    _passwordFlow.unlink();
    _credentialFlow.link();
    QStringList scope;
    auto token = _credentialFlow.getToken(scope.join(" "));
    if(token.isValid())
        input.headers.insert("Authorization", "Bearer " + token.getToken());

    _latestWorker = new OAIHttpRequestWorker(this, _manager);
    _latestWorker->setTimeOut(_timeOut);
    _latestWorker->setWorkingDirectory(_workingDirectory);

    connect(_latestWorker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIDigiLockerMetaAPIsApi::verify_Account_idCallback);
    connect(this, &OAIDigiLockerMetaAPIsApi::abortRequestsSignal, _latestWorker, &QObject::deleteLater);
    connect(_latestWorker, &QObject::destroyed, [this](){
        if(findChildren<OAIHttpRequestWorker*>().count() == 0){
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    _latestInput = input;
    _latestScope = scope;


    worker->execute(&input);
}

void OAIDigiLockerMetaAPIsApi::verify_Account_idCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIVerifyAccountResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT verify_Account_idSignal(output);
        Q_EMIT verify_Account_idSignalFull(worker, output);

    } else if(worker->error_type == QNetworkReply::AuthenticationRequiredError){
        connect(&_credentialFlow, SIGNAL(tokenReceived()), this, SLOT(tokenAvailable()));
        QStringList scope;
        QString scopeStr = scope.join(" ");
        QString tokenUrl("https://betaapi.digitallocker.gov.in/public/oauth2/1/token");
        //TODO get clientID and Secret and state in the config? https://swagger.io/docs/specification/authentication/oauth2/ states that you should do as you like
        _credentialFlow.setVariables(tokenUrl , scopeStr, "clientId", "clientSecret");
        Q_EMIT _credentialFlow.authenticationNeeded();

    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT verify_Account_idSignalE(output, error_type, error_str);
        Q_EMIT verify_Account_idSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT verify_Account_idSignalError(output, error_type, error_str);
        Q_EMIT verify_Account_idSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIDigiLockerMetaAPIsApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
