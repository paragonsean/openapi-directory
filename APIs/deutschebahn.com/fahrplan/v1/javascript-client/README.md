# fahrplan_free

FahrplanFree - JavaScript client for fahrplan_free
A RESTful webservice to request a railway journey - FREE plan with restricted access (max. 10 requests per minute). Please ignore the message in the API Console about the access token.

Register to use an less restricted version, which requires an access token.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: v1
- Package version: v1
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://developer.deutschebahn.com/store/](https://developer.deutschebahn.com/store/)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install fahrplan_free --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your fahrplan_free from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var FahrplanFree = require('fahrplan_free');


var api = new FahrplanFree.DefaultApi()
var id = "id_example"; // {String} Id of location. Use attribute 'id' from the result of 'location'
var date = "date_example"; // {String} Date and time in ISO-8601 format, yyyy-mm-ddThh:mm:ss, example: 2017-04-01 or 2017-04-01T10:30
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.arrivalBoardIdGet(id, date, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://api.deutschebahn.com/freeplan/v1*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*FahrplanFree.DefaultApi* | [**arrivalBoardIdGet**](docs/DefaultApi.md#arrivalBoardIdGet) | **GET** /arrivalBoard/{id} | Get arrival board of a location
*FahrplanFree.DefaultApi* | [**departureBoardIdGet**](docs/DefaultApi.md#departureBoardIdGet) | **GET** /departureBoard/{id} | Get departure board of a location
*FahrplanFree.DefaultApi* | [**journeyDetailsIdGet**](docs/DefaultApi.md#journeyDetailsIdGet) | **GET** /journeyDetails/{id} | Get details about a single journey
*FahrplanFree.DefaultApi* | [**locationNameGet**](docs/DefaultApi.md#locationNameGet) | **GET** /location/{name} | Get location information


## Documentation for Models

 - [FahrplanFree.Board](docs/Board.md)
 - [FahrplanFree.BoardResponse](docs/BoardResponse.md)
 - [FahrplanFree.ErrorModel](docs/ErrorModel.md)
 - [FahrplanFree.JourneyResponse](docs/JourneyResponse.md)
 - [FahrplanFree.Location](docs/Location.md)
 - [FahrplanFree.LocationResponse](docs/LocationResponse.md)
 - [FahrplanFree.TrainLocation](docs/TrainLocation.md)


## Documentation for Authorization

Endpoints do not require authorization.

