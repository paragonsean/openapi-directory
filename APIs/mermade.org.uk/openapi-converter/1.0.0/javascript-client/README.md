# swagger2_open_api_converter

Swagger2OpenApiConverter - JavaScript client for swagger2_open_api_converter
Converter and validator for Swagger 2.0 to OpenAPI 3.0.x definitions
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen
For more information, please visit [https://github.com/mermade/swagger2openapi/issues](https://github.com/mermade/swagger2openapi/issues)

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install swagger2_open_api_converter --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your swagger2_open_api_converter from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var Swagger2OpenApiConverter = require('swagger2_open_api_converter');


var api = new Swagger2OpenApiConverter.ConversionApi()
var opts = {
  'filename': "filename_example", // {String} The file to upload and convert
  'source': "source_example", // {String} The text of a Swagger 2.0 definition to convert
  'validate': "validate_example" // {String} 
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.convert(opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://mermade.org.uk/api/v1*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*Swagger2OpenApiConverter.ConversionApi* | [**convert**](docs/ConversionApi.md#convert) | **POST** /convert | Convert a Swagger 2.0 definition passed in the body to OpenAPI 3.0.x 
*Swagger2OpenApiConverter.ConversionApi* | [**convertUrl**](docs/ConversionApi.md#convertUrl) | **GET** /convert | Convert a Swagger 2.0 definition to OpenAPI 3.0.x from a URL
*Swagger2OpenApiConverter.MetadataApi* | [**getStatus**](docs/MetadataApi.md#getStatus) | **GET** /status | Get the status of the API
*Swagger2OpenApiConverter.ValidationApi* | [**getBadge**](docs/ValidationApi.md#getBadge) | **GET** /badge | Return a redirect to a badge svg file depending on a source definition&#39;s validity
*Swagger2OpenApiConverter.ValidationApi* | [**validate**](docs/ValidationApi.md#validate) | **POST** /validate | Validate an OpenAPI 3.0.x definition supplied in the body of the request
*Swagger2OpenApiConverter.ValidationApi* | [**validateUrl**](docs/ValidationApi.md#validateUrl) | **GET** /validate | Validate an OpenAPI 3.0.x definition


## Documentation for Models

 - [Swagger2OpenApiConverter.ValidationResult](docs/ValidationResult.md)


## Documentation for Authorization

Endpoints do not require authorization.

