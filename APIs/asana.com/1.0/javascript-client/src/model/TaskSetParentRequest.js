/**
 * Asana
 * This is the interface for interacting with the [Asana Platform](https://developers.asana.com). Our API reference is generated from our [OpenAPI spec] (https://raw.githubusercontent.com/Asana/developer-docs/master/defs/asana_oas.yaml).
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TaskSetParentRequest model module.
 * @module model/TaskSetParentRequest
 * @version 1.0
 */
class TaskSetParentRequest {
    /**
     * Constructs a new <code>TaskSetParentRequest</code>.
     * @alias module:model/TaskSetParentRequest
     * @param parent {String} The new parent of the task, or `null` for no parent.
     */
    constructor(parent) { 
        
        TaskSetParentRequest.initialize(this, parent);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, parent) { 
        obj['parent'] = parent;
    }

    /**
     * Constructs a <code>TaskSetParentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TaskSetParentRequest} obj Optional instance to populate.
     * @return {module:model/TaskSetParentRequest} The populated <code>TaskSetParentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TaskSetParentRequest();

            if (data.hasOwnProperty('insert_after')) {
                obj['insert_after'] = ApiClient.convertToType(data['insert_after'], 'String');
            }
            if (data.hasOwnProperty('insert_before')) {
                obj['insert_before'] = ApiClient.convertToType(data['insert_before'], 'String');
            }
            if (data.hasOwnProperty('parent')) {
                obj['parent'] = ApiClient.convertToType(data['parent'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TaskSetParentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TaskSetParentRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TaskSetParentRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['insert_after'] && !(typeof data['insert_after'] === 'string' || data['insert_after'] instanceof String)) {
            throw new Error("Expected the field `insert_after` to be a primitive type in the JSON string but got " + data['insert_after']);
        }
        // ensure the json data is a string
        if (data['insert_before'] && !(typeof data['insert_before'] === 'string' || data['insert_before'] instanceof String)) {
            throw new Error("Expected the field `insert_before` to be a primitive type in the JSON string but got " + data['insert_before']);
        }
        // ensure the json data is a string
        if (data['parent'] && !(typeof data['parent'] === 'string' || data['parent'] instanceof String)) {
            throw new Error("Expected the field `parent` to be a primitive type in the JSON string but got " + data['parent']);
        }

        return true;
    }


}

TaskSetParentRequest.RequiredProperties = ["parent"];

/**
 * A subtask of the parent to insert the task after, or `null` to insert at the beginning of the list.
 * @member {String} insert_after
 */
TaskSetParentRequest.prototype['insert_after'] = undefined;

/**
 * A subtask of the parent to insert the task before, or `null` to insert at the end of the list.
 * @member {String} insert_before
 */
TaskSetParentRequest.prototype['insert_before'] = undefined;

/**
 * The new parent of the task, or `null` for no parent.
 * @member {String} parent
 */
TaskSetParentRequest.prototype['parent'] = undefined;






export default TaskSetParentRequest;

