/**
 * Asana
 * This is the interface for interacting with the [Asana Platform](https://developers.asana.com). Our API reference is generated from our [OpenAPI spec] (https://raw.githubusercontent.com/Asana/developer-docs/master/defs/asana_oas.yaml).
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GoalAddSubgoalRequest model module.
 * @module model/GoalAddSubgoalRequest
 * @version 1.0
 */
class GoalAddSubgoalRequest {
    /**
     * Constructs a new <code>GoalAddSubgoalRequest</code>.
     * @alias module:model/GoalAddSubgoalRequest
     * @param subgoal {String} The goal gid to add as subgoal to a parent goal
     */
    constructor(subgoal) { 
        
        GoalAddSubgoalRequest.initialize(this, subgoal);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, subgoal) { 
        obj['subgoal'] = subgoal;
    }

    /**
     * Constructs a <code>GoalAddSubgoalRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoalAddSubgoalRequest} obj Optional instance to populate.
     * @return {module:model/GoalAddSubgoalRequest} The populated <code>GoalAddSubgoalRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoalAddSubgoalRequest();

            if (data.hasOwnProperty('insert_after')) {
                obj['insert_after'] = ApiClient.convertToType(data['insert_after'], 'String');
            }
            if (data.hasOwnProperty('insert_before')) {
                obj['insert_before'] = ApiClient.convertToType(data['insert_before'], 'String');
            }
            if (data.hasOwnProperty('subgoal')) {
                obj['subgoal'] = ApiClient.convertToType(data['subgoal'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoalAddSubgoalRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoalAddSubgoalRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GoalAddSubgoalRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['insert_after'] && !(typeof data['insert_after'] === 'string' || data['insert_after'] instanceof String)) {
            throw new Error("Expected the field `insert_after` to be a primitive type in the JSON string but got " + data['insert_after']);
        }
        // ensure the json data is a string
        if (data['insert_before'] && !(typeof data['insert_before'] === 'string' || data['insert_before'] instanceof String)) {
            throw new Error("Expected the field `insert_before` to be a primitive type in the JSON string but got " + data['insert_before']);
        }
        // ensure the json data is a string
        if (data['subgoal'] && !(typeof data['subgoal'] === 'string' || data['subgoal'] instanceof String)) {
            throw new Error("Expected the field `subgoal` to be a primitive type in the JSON string but got " + data['subgoal']);
        }

        return true;
    }


}

GoalAddSubgoalRequest.RequiredProperties = ["subgoal"];

/**
 * An id of a subgoal of this parent goal. The new subgoal will be added after the one specified here. `insert_before` and `insert_after` parameters cannot both be specified.
 * @member {String} insert_after
 */
GoalAddSubgoalRequest.prototype['insert_after'] = undefined;

/**
 * An id of a subgoal of this parent goal. The new subgoal will be added before the one specified here. `insert_before` and `insert_after` parameters cannot both be specified.
 * @member {String} insert_before
 */
GoalAddSubgoalRequest.prototype['insert_before'] = undefined;

/**
 * The goal gid to add as subgoal to a parent goal
 * @member {String} subgoal
 */
GoalAddSubgoalRequest.prototype['subgoal'] = undefined;






export default GoalAddSubgoalRequest;

