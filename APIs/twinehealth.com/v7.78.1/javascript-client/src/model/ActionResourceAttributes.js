/**
 * Fitbit Plus API
 * # Overview The Fitbit Plus API is a RESTful API. The requests and responses are formated according to the [JSON API](http://jsonapi.org/format/1.0/) specification.  In addition to this documentation, we also provide an [OpenAPI](https://github.com/OAI/OpenAPI-Specification/blob/master/versions/2.0.md) \"yaml\" file describing the API: [Fitbit Plus API Specification](swagger.yaml).  # Authentication Authentication for the Fitbit Plus API is based on the [OAuth 2.0 Authorization Framework](https://tools.ietf.org/html/rfc6749). Fitbit Plus currently supports grant types of **client_credentials** and **refresh_token**.  See [POST /oauth/token](#operation/createToken) for details on the request and response formats. <!-- ReDoc-Inject: <security-definitions> -->  ## Building Integrations We will provide customers with unique client credentials for each application/integration they build, allowing us to enforce appropriate access controls and monitor API usage. The client credentials will be scoped to the organization, and allow full access to all patients and related data within that organization.  These credentials are appropriate for creating an integration that does one of the following:  - background reporting/analysis  - synchronizing data with another system (such as an EMR)  The API credentials and oauth flows we currently support are **not** well suited for creating a user-facing application that allows a user (patient, coach, or admin) to login and have access to data which is appropriate to that specific user. It is possible to build such an application, but it is not possible to use Fitbit Plus as a federated identity provider. You would need to have a separate means of verifying a user's identity. We do not currently support the required password-based oauth flow to make this possible.  # Paging The Fitbit Plus API supports two different pagination strategies for GET collection endpoints.  #### Skip-based paging  Skip-based paging uses the query parameters `page[size]` and `page[number]` to specify the max number of resources returned and the page number. We default to skip-based paging if there are no page parameters. The response will include a `links` object containing links to the first, last, prev, and next pages of data.  If the contents of the collection change while you are iterating through the collection, you will see duplicate or missing documents. For example, if you are iterating through the `calender_event` resource via `GET /pub/calendar_event?sort=start_at&page[size]=50&page[number]=1`, and a new `calendar_event` is created that has a `start_at` value before the first `calendar_event`, when you fetch the next page at `GET /pub/calendar_event?sort=start_at&page[size]=50&page[number]=2`, the first entry in the second response will be a duplicate of the last entry in the first response.  #### Cursor-based paging Cursor-based paging uses the query parameters `page[limit]` and `page[after]` to specify the max number of entries returned and identify where to begin the next page. Add `page[limit]` to the parameters to use cursor-based paging. The response will include a `links` object containing a link to the next page of data, if the next page exists.  Cursor-based paging is not subject to duplication if new resources are added to the collection. For example, if you are iterating through the `calender_event` resource via `GET /pub/calendar_event?sort=start_at&page[limit]=50`, and a new `calendar_event` is created that has a `start_at` value before the first `calendar_event`, you will not see a duplicate entry when you fetch the next page at `GET /pub/calendar_event?sort=start_at&page[limit]=50&page[after]=<cursor>`.  We encourage the use of cursor-based paging for performance reasons.  In either form of paging, you can determine whether any resources were missed by comparing the number of fetched resources against `meta.count`. Set `page[size]` or `page[limit]` to 0 to get only the count.  It is not valid to mix the two strategies. 
 *
 * The version of the OpenAPI document: v7.78.1
 * Contact: apiteam@twinehealth.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ActionMetric from './ActionMetric';
import ActionResourceAttributesAdherence from './ActionResourceAttributesAdherence';
import ActionResourceAttributesFrequencyGoal from './ActionResourceAttributesFrequencyGoal';
import ActionWindow from './ActionWindow';
import Identifier from './Identifier';

/**
 * The ActionResourceAttributes model module.
 * @module model/ActionResourceAttributes
 * @version v7.78.1
 */
class ActionResourceAttributes {
    /**
     * Constructs a new <code>ActionResourceAttributes</code>.
     * @alias module:model/ActionResourceAttributes
     * @param effectiveFrom {String} 
     * @param title {String} 
     * @param type {module:model/ActionResourceAttributes.TypeEnum} 
     */
    constructor(effectiveFrom, title, type) { 
        
        ActionResourceAttributes.initialize(this, effectiveFrom, title, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, effectiveFrom, title, type) { 
        obj['effective_from'] = effectiveFrom;
        obj['title'] = title;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ActionResourceAttributes</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ActionResourceAttributes} obj Optional instance to populate.
     * @return {module:model/ActionResourceAttributes} The populated <code>ActionResourceAttributes</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ActionResourceAttributes();

            if (data.hasOwnProperty('_thread')) {
                obj['_thread'] = ApiClient.convertToType(data['_thread'], 'String');
            }
            if (data.hasOwnProperty('adherence')) {
                obj['adherence'] = ActionResourceAttributesAdherence.constructFromObject(data['adherence']);
            }
            if (data.hasOwnProperty('details')) {
                obj['details'] = ApiClient.convertToType(data['details'], {'String': Object});
            }
            if (data.hasOwnProperty('effective_from')) {
                obj['effective_from'] = ApiClient.convertToType(data['effective_from'], 'String');
            }
            if (data.hasOwnProperty('effective_to')) {
                obj['effective_to'] = ApiClient.convertToType(data['effective_to'], 'String');
            }
            if (data.hasOwnProperty('frequency_goal')) {
                obj['frequency_goal'] = ActionResourceAttributesFrequencyGoal.constructFromObject(data['frequency_goal']);
            }
            if (data.hasOwnProperty('identifiers')) {
                obj['identifiers'] = ApiClient.convertToType(data['identifiers'], [Identifier]);
            }
            if (data.hasOwnProperty('intake')) {
                obj['intake'] = ApiClient.convertToType(data['intake'], {'String': Object});
            }
            if (data.hasOwnProperty('metric_required')) {
                obj['metric_required'] = ApiClient.convertToType(data['metric_required'], 'Boolean');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [ActionMetric]);
            }
            if (data.hasOwnProperty('static_title')) {
                obj['static_title'] = ApiClient.convertToType(data['static_title'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('tracking')) {
                obj['tracking'] = ApiClient.convertToType(data['tracking'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('windows')) {
                obj['windows'] = ApiClient.convertToType(data['windows'], [ActionWindow]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ActionResourceAttributes</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ActionResourceAttributes</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ActionResourceAttributes.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['_thread'] && !(typeof data['_thread'] === 'string' || data['_thread'] instanceof String)) {
            throw new Error("Expected the field `_thread` to be a primitive type in the JSON string but got " + data['_thread']);
        }
        // validate the optional field `adherence`
        if (data['adherence']) { // data not null
          ActionResourceAttributesAdherence.validateJSON(data['adherence']);
        }
        // ensure the json data is a string
        if (data['effective_from'] && !(typeof data['effective_from'] === 'string' || data['effective_from'] instanceof String)) {
            throw new Error("Expected the field `effective_from` to be a primitive type in the JSON string but got " + data['effective_from']);
        }
        // ensure the json data is a string
        if (data['effective_to'] && !(typeof data['effective_to'] === 'string' || data['effective_to'] instanceof String)) {
            throw new Error("Expected the field `effective_to` to be a primitive type in the JSON string but got " + data['effective_to']);
        }
        // validate the optional field `frequency_goal`
        if (data['frequency_goal']) { // data not null
          ActionResourceAttributesFrequencyGoal.validateJSON(data['frequency_goal']);
        }
        if (data['identifiers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['identifiers'])) {
                throw new Error("Expected the field `identifiers` to be an array in the JSON data but got " + data['identifiers']);
            }
            // validate the optional field `identifiers` (array)
            for (const item of data['identifiers']) {
                Identifier.validateJSON(item);
            };
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                ActionMetric.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['static_title'] && !(typeof data['static_title'] === 'string' || data['static_title'] instanceof String)) {
            throw new Error("Expected the field `static_title` to be a primitive type in the JSON string but got " + data['static_title']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        if (data['windows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['windows'])) {
                throw new Error("Expected the field `windows` to be an array in the JSON data but got " + data['windows']);
            }
            // validate the optional field `windows` (array)
            for (const item of data['windows']) {
                ActionWindow.validateJSON(item);
            };
        }

        return true;
    }


}

ActionResourceAttributes.RequiredProperties = ["effective_from", "title", "type"];

/**
 * @member {String} _thread
 */
ActionResourceAttributes.prototype['_thread'] = undefined;

/**
 * @member {module:model/ActionResourceAttributesAdherence} adherence
 */
ActionResourceAttributes.prototype['adherence'] = undefined;

/**
 * @member {Object.<String, Object>} details
 */
ActionResourceAttributes.prototype['details'] = undefined;

/**
 * @member {String} effective_from
 */
ActionResourceAttributes.prototype['effective_from'] = undefined;

/**
 * @member {String} effective_to
 */
ActionResourceAttributes.prototype['effective_to'] = undefined;

/**
 * @member {module:model/ActionResourceAttributesFrequencyGoal} frequency_goal
 */
ActionResourceAttributes.prototype['frequency_goal'] = undefined;

/**
 * @member {Array.<module:model/Identifier>} identifiers
 */
ActionResourceAttributes.prototype['identifiers'] = undefined;

/**
 * @member {Object.<String, Object>} intake
 */
ActionResourceAttributes.prototype['intake'] = undefined;

/**
 * @member {Boolean} metric_required
 */
ActionResourceAttributes.prototype['metric_required'] = undefined;

/**
 * @member {Array.<module:model/ActionMetric>} metrics
 */
ActionResourceAttributes.prototype['metrics'] = undefined;

/**
 * @member {String} static_title
 */
ActionResourceAttributes.prototype['static_title'] = undefined;

/**
 * @member {String} title
 */
ActionResourceAttributes.prototype['title'] = undefined;

/**
 * @member {Boolean} tracking
 */
ActionResourceAttributes.prototype['tracking'] = undefined;

/**
 * @member {module:model/ActionResourceAttributes.TypeEnum} type
 */
ActionResourceAttributes.prototype['type'] = undefined;

/**
 * @member {Array.<module:model/ActionWindow>} windows
 */
ActionResourceAttributes.prototype['windows'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ActionResourceAttributes['TypeEnum'] = {

    /**
     * value: "other_lifestyle"
     * @const
     */
    "other_lifestyle": "other_lifestyle"
};



export default ActionResourceAttributes;

