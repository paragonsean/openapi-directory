/**
 * The Jira Cloud platform REST API
 * Jira Cloud platform REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0-SNAPSHOT
 * Contact: ecosystem@atlassian.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SharePermission from './SharePermission';

/**
 * The DashboardDetails model module.
 * @module model/DashboardDetails
 * @version 1001.0.0-SNAPSHOT
 */
class DashboardDetails {
    /**
     * Constructs a new <code>DashboardDetails</code>.
     * Details of a dashboard.
     * @alias module:model/DashboardDetails
     * @param editPermissions {Array.<module:model/SharePermission>} The edit permissions for the dashboard.
     * @param name {String} The name of the dashboard.
     * @param sharePermissions {Array.<module:model/SharePermission>} The share permissions for the dashboard.
     */
    constructor(editPermissions, name, sharePermissions) { 
        
        DashboardDetails.initialize(this, editPermissions, name, sharePermissions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, editPermissions, name, sharePermissions) { 
        obj['editPermissions'] = editPermissions;
        obj['name'] = name;
        obj['sharePermissions'] = sharePermissions;
    }

    /**
     * Constructs a <code>DashboardDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DashboardDetails} obj Optional instance to populate.
     * @return {module:model/DashboardDetails} The populated <code>DashboardDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DashboardDetails();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('editPermissions')) {
                obj['editPermissions'] = ApiClient.convertToType(data['editPermissions'], [SharePermission]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('sharePermissions')) {
                obj['sharePermissions'] = ApiClient.convertToType(data['sharePermissions'], [SharePermission]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DashboardDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DashboardDetails</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DashboardDetails.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['editPermissions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['editPermissions'])) {
                throw new Error("Expected the field `editPermissions` to be an array in the JSON data but got " + data['editPermissions']);
            }
            // validate the optional field `editPermissions` (array)
            for (const item of data['editPermissions']) {
                SharePermission.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['sharePermissions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sharePermissions'])) {
                throw new Error("Expected the field `sharePermissions` to be an array in the JSON data but got " + data['sharePermissions']);
            }
            // validate the optional field `sharePermissions` (array)
            for (const item of data['sharePermissions']) {
                SharePermission.validateJSON(item);
            };
        }

        return true;
    }


}

DashboardDetails.RequiredProperties = ["editPermissions", "name", "sharePermissions"];

/**
 * The description of the dashboard.
 * @member {String} description
 */
DashboardDetails.prototype['description'] = undefined;

/**
 * The edit permissions for the dashboard.
 * @member {Array.<module:model/SharePermission>} editPermissions
 */
DashboardDetails.prototype['editPermissions'] = undefined;

/**
 * The name of the dashboard.
 * @member {String} name
 */
DashboardDetails.prototype['name'] = undefined;

/**
 * The share permissions for the dashboard.
 * @member {Array.<module:model/SharePermission>} sharePermissions
 */
DashboardDetails.prototype['sharePermissions'] = undefined;






export default DashboardDetails;

