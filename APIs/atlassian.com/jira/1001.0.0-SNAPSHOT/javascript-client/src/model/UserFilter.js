/**
 * The Jira Cloud platform REST API
 * Jira Cloud platform REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0-SNAPSHOT
 * Contact: ecosystem@atlassian.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserFilter model module.
 * @module model/UserFilter
 * @version 1001.0.0-SNAPSHOT
 */
class UserFilter {
    /**
     * Constructs a new <code>UserFilter</code>.
     * Filter for a User Picker (single) custom field.
     * @alias module:model/UserFilter
     * @param enabled {Boolean} Whether the filter is enabled.
     */
    constructor(enabled) { 
        
        UserFilter.initialize(this, enabled);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, enabled) { 
        obj['enabled'] = enabled;
    }

    /**
     * Constructs a <code>UserFilter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserFilter} obj Optional instance to populate.
     * @return {module:model/UserFilter} The populated <code>UserFilter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserFilter();

            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], ['String']);
            }
            if (data.hasOwnProperty('roleIds')) {
                obj['roleIds'] = ApiClient.convertToType(data['roleIds'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserFilter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserFilter</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserFilter.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['groups'])) {
            throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['roleIds'])) {
            throw new Error("Expected the field `roleIds` to be an array in the JSON data but got " + data['roleIds']);
        }

        return true;
    }


}

UserFilter.RequiredProperties = ["enabled"];

/**
 * Whether the filter is enabled.
 * @member {Boolean} enabled
 */
UserFilter.prototype['enabled'] = undefined;

/**
 * User groups autocomplete suggestion users must belong to. If not provided, the default values are used. A maximum of 10 groups can be provided.
 * @member {Array.<String>} groups
 */
UserFilter.prototype['groups'] = undefined;

/**
 * Roles that autocomplete suggestion users must belong to. If not provided, the default values are used. A maximum of 10 roles can be provided.
 * @member {Array.<Number>} roleIds
 */
UserFilter.prototype['roleIds'] = undefined;






export default UserFilter;

