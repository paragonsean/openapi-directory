/**
 * The Jira Cloud platform REST API
 * Jira Cloud platform REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0-SNAPSHOT
 * Contact: ecosystem@atlassian.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GroupName from './GroupName';
import RestrictedPermission from './RestrictedPermission';

/**
 * The NotificationRecipientsRestrictions model module.
 * @module model/NotificationRecipientsRestrictions
 * @version 1001.0.0-SNAPSHOT
 */
class NotificationRecipientsRestrictions {
    /**
     * Constructs a new <code>NotificationRecipientsRestrictions</code>.
     * Details of the group membership or permissions needed to receive the notification.
     * @alias module:model/NotificationRecipientsRestrictions
     */
    constructor() { 
        
        NotificationRecipientsRestrictions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NotificationRecipientsRestrictions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NotificationRecipientsRestrictions} obj Optional instance to populate.
     * @return {module:model/NotificationRecipientsRestrictions} The populated <code>NotificationRecipientsRestrictions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NotificationRecipientsRestrictions();

            if (data.hasOwnProperty('groupIds')) {
                obj['groupIds'] = ApiClient.convertToType(data['groupIds'], ['String']);
            }
            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], [GroupName]);
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], [RestrictedPermission]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NotificationRecipientsRestrictions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NotificationRecipientsRestrictions</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['groupIds'])) {
            throw new Error("Expected the field `groupIds` to be an array in the JSON data but got " + data['groupIds']);
        }
        if (data['groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groups'])) {
                throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
            }
            // validate the optional field `groups` (array)
            for (const item of data['groups']) {
                GroupName.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['permissions'])) {
            throw new Error("Expected the field `permissions` to be an array in the JSON data but got " + data['permissions']);
        }

        return true;
    }


}



/**
 * List of groupId memberships required to receive the notification.
 * @member {Array.<String>} groupIds
 */
NotificationRecipientsRestrictions.prototype['groupIds'] = undefined;

/**
 * List of group memberships required to receive the notification.
 * @member {Array.<module:model/GroupName>} groups
 */
NotificationRecipientsRestrictions.prototype['groups'] = undefined;

/**
 * List of permissions required to receive the notification.
 * @member {Array.<module:model/RestrictedPermission>} permissions
 */
NotificationRecipientsRestrictions.prototype['permissions'] = undefined;






export default NotificationRecipientsRestrictions;

