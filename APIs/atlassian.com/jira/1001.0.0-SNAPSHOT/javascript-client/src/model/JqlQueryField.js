/**
 * The Jira Cloud platform REST API
 * Jira Cloud platform REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0-SNAPSHOT
 * Contact: ecosystem@atlassian.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JqlQueryFieldEntityProperty from './JqlQueryFieldEntityProperty';

/**
 * The JqlQueryField model module.
 * @module model/JqlQueryField
 * @version 1001.0.0-SNAPSHOT
 */
class JqlQueryField {
    /**
     * Constructs a new <code>JqlQueryField</code>.
     * A field used in a JQL query. See [Advanced searching - fields reference](https://confluence.atlassian.com/x/dAiiLQ) for more information about fields in JQL queries.
     * @alias module:model/JqlQueryField
     * @param name {String} The name of the field.
     */
    constructor(name) { 
        
        JqlQueryField.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>JqlQueryField</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JqlQueryField} obj Optional instance to populate.
     * @return {module:model/JqlQueryField} The populated <code>JqlQueryField</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JqlQueryField();

            if (data.hasOwnProperty('encodedName')) {
                obj['encodedName'] = ApiClient.convertToType(data['encodedName'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('property')) {
                obj['property'] = ApiClient.convertToType(data['property'], [JqlQueryFieldEntityProperty]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JqlQueryField</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JqlQueryField</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JqlQueryField.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['encodedName'] && !(typeof data['encodedName'] === 'string' || data['encodedName'] instanceof String)) {
            throw new Error("Expected the field `encodedName` to be a primitive type in the JSON string but got " + data['encodedName']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['property']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['property'])) {
                throw new Error("Expected the field `property` to be an array in the JSON data but got " + data['property']);
            }
            // validate the optional field `property` (array)
            for (const item of data['property']) {
                JqlQueryFieldEntityProperty.validateJSON(item);
            };
        }

        return true;
    }


}

JqlQueryField.RequiredProperties = ["name"];

/**
 * The encoded name of the field, which can be used directly in a JQL query.
 * @member {String} encodedName
 */
JqlQueryField.prototype['encodedName'] = undefined;

/**
 * The name of the field.
 * @member {String} name
 */
JqlQueryField.prototype['name'] = undefined;

/**
 * When the field refers to a value in an entity property, details of the entity property value.
 * @member {Array.<module:model/JqlQueryFieldEntityProperty>} property
 */
JqlQueryField.prototype['property'] = undefined;






export default JqlQueryField;

