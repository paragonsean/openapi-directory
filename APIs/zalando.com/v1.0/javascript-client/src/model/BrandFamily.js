/**
 * Zalando Shop
 * The shop API empowers developers to build amazing new apps or websites using Zalando shop data and services.
 *
 * The version of the OpenAPI document: v1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BrandFamily model module.
 * @module model/BrandFamily
 * @version v1.0
 */
class BrandFamily {
    /**
     * Constructs a new <code>BrandFamily</code>.
     * Zalando API BrandFamily Schema
     * @alias module:model/BrandFamily
     * @param key {String} The unique key for a brandFamily
     * @param name {String} Name of the brandFamily
     * @param shopUrl {String} The url of the brandFamily within the Zalando web shop
     */
    constructor(key, name, shopUrl) { 
        
        BrandFamily.initialize(this, key, name, shopUrl);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, key, name, shopUrl) { 
        obj['key'] = key;
        obj['name'] = name;
        obj['shopUrl'] = shopUrl;
    }

    /**
     * Constructs a <code>BrandFamily</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BrandFamily} obj Optional instance to populate.
     * @return {module:model/BrandFamily} The populated <code>BrandFamily</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BrandFamily();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('shopUrl')) {
                obj['shopUrl'] = ApiClient.convertToType(data['shopUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BrandFamily</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BrandFamily</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BrandFamily.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['shopUrl'] && !(typeof data['shopUrl'] === 'string' || data['shopUrl'] instanceof String)) {
            throw new Error("Expected the field `shopUrl` to be a primitive type in the JSON string but got " + data['shopUrl']);
        }

        return true;
    }


}

BrandFamily.RequiredProperties = ["key", "name", "shopUrl"];

/**
 * The unique key for a brandFamily
 * @member {String} key
 */
BrandFamily.prototype['key'] = undefined;

/**
 * Name of the brandFamily
 * @member {String} name
 */
BrandFamily.prototype['name'] = undefined;

/**
 * The url of the brandFamily within the Zalando web shop
 * @member {String} shopUrl
 */
BrandFamily.prototype['shopUrl'] = undefined;






export default BrandFamily;

