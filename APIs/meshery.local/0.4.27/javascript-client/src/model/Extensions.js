/**
 * Meshery API.
 * the purpose of this application is to provide an application that is using plain go code to define an API  This should demonstrate all the possible comment annotations that are available to turn go code into a fully compliant swagger 2.0 spec
 *
 * The version of the OpenAPI document: 0.4.27
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GraphQLExtension from './GraphQLExtension';
import NavigatorExtension from './NavigatorExtension';
import UserPrefsExtension from './UserPrefsExtension';

/**
 * The Extensions model module.
 * @module model/Extensions
 * @version 0.4.27
 */
class Extensions {
    /**
     * Constructs a new <code>Extensions</code>.
     * Extensions defines the UI extension points
     * @alias module:model/Extensions
     */
    constructor() { 
        
        Extensions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Extensions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Extensions} obj Optional instance to populate.
     * @return {module:model/Extensions} The populated <code>Extensions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Extensions();

            if (data.hasOwnProperty('graphql')) {
                obj['graphql'] = ApiClient.convertToType(data['graphql'], [GraphQLExtension]);
            }
            if (data.hasOwnProperty('navigator')) {
                obj['navigator'] = ApiClient.convertToType(data['navigator'], [NavigatorExtension]);
            }
            if (data.hasOwnProperty('user_prefs')) {
                obj['user_prefs'] = ApiClient.convertToType(data['user_prefs'], [UserPrefsExtension]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Extensions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Extensions</code>.
     */
    static validateJSON(data) {
        if (data['graphql']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['graphql'])) {
                throw new Error("Expected the field `graphql` to be an array in the JSON data but got " + data['graphql']);
            }
            // validate the optional field `graphql` (array)
            for (const item of data['graphql']) {
                GraphQLExtension.validateJSON(item);
            };
        }
        if (data['navigator']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['navigator'])) {
                throw new Error("Expected the field `navigator` to be an array in the JSON data but got " + data['navigator']);
            }
            // validate the optional field `navigator` (array)
            for (const item of data['navigator']) {
                NavigatorExtension.validateJSON(item);
            };
        }
        if (data['user_prefs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['user_prefs'])) {
                throw new Error("Expected the field `user_prefs` to be an array in the JSON data but got " + data['user_prefs']);
            }
            // validate the optional field `user_prefs` (array)
            for (const item of data['user_prefs']) {
                UserPrefsExtension.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * GraphQLExtensions is a collection of GraphQLExtension endpoints
 * @member {Array.<module:model/GraphQLExtension>} graphql
 */
Extensions.prototype['graphql'] = undefined;

/**
 * NavigatorExtensions is a collection of NavigatorExtension
 * @member {Array.<module:model/NavigatorExtension>} navigator
 */
Extensions.prototype['navigator'] = undefined;

/**
 * UserPrefsExtensions is a collection of UserPrefsExtension
 * @member {Array.<module:model/UserPrefsExtension>} user_prefs
 */
Extensions.prototype['user_prefs'] = undefined;






export default Extensions;

