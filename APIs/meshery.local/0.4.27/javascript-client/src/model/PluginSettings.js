/**
 * Meshery API.
 * the purpose of this application is to provide an application that is using plain go code to define an API  This should demonstrate all the possible comment annotations that are available to turn go code into a fully compliant swagger 2.0 spec
 *
 * The version of the OpenAPI document: 0.4.27
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PluginDevice from './PluginDevice';
import PluginMount from './PluginMount';

/**
 * The PluginSettings model module.
 * @module model/PluginSettings
 * @version 0.4.27
 */
class PluginSettings {
    /**
     * Constructs a new <code>PluginSettings</code>.
     * @alias module:model/PluginSettings
     * @param args {Array.<String>} args
     * @param devices {Array.<module:model/PluginDevice>} devices
     * @param env {Array.<String>} env
     * @param mounts {Array.<module:model/PluginMount>} mounts
     */
    constructor(args, devices, env, mounts) { 
        
        PluginSettings.initialize(this, args, devices, env, mounts);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, args, devices, env, mounts) { 
        obj['Args'] = args;
        obj['Devices'] = devices;
        obj['Env'] = env;
        obj['Mounts'] = mounts;
    }

    /**
     * Constructs a <code>PluginSettings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PluginSettings} obj Optional instance to populate.
     * @return {module:model/PluginSettings} The populated <code>PluginSettings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PluginSettings();

            if (data.hasOwnProperty('Args')) {
                obj['Args'] = ApiClient.convertToType(data['Args'], ['String']);
            }
            if (data.hasOwnProperty('Devices')) {
                obj['Devices'] = ApiClient.convertToType(data['Devices'], [PluginDevice]);
            }
            if (data.hasOwnProperty('Env')) {
                obj['Env'] = ApiClient.convertToType(data['Env'], ['String']);
            }
            if (data.hasOwnProperty('Mounts')) {
                obj['Mounts'] = ApiClient.convertToType(data['Mounts'], [PluginMount]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PluginSettings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PluginSettings</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PluginSettings.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['Args'])) {
            throw new Error("Expected the field `Args` to be an array in the JSON data but got " + data['Args']);
        }
        if (data['Devices']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Devices'])) {
                throw new Error("Expected the field `Devices` to be an array in the JSON data but got " + data['Devices']);
            }
            // validate the optional field `Devices` (array)
            for (const item of data['Devices']) {
                PluginDevice.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['Env'])) {
            throw new Error("Expected the field `Env` to be an array in the JSON data but got " + data['Env']);
        }
        if (data['Mounts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Mounts'])) {
                throw new Error("Expected the field `Mounts` to be an array in the JSON data but got " + data['Mounts']);
            }
            // validate the optional field `Mounts` (array)
            for (const item of data['Mounts']) {
                PluginMount.validateJSON(item);
            };
        }

        return true;
    }


}

PluginSettings.RequiredProperties = ["Args", "Devices", "Env", "Mounts"];

/**
 * args
 * @member {Array.<String>} Args
 */
PluginSettings.prototype['Args'] = undefined;

/**
 * devices
 * @member {Array.<module:model/PluginDevice>} Devices
 */
PluginSettings.prototype['Devices'] = undefined;

/**
 * env
 * @member {Array.<String>} Env
 */
PluginSettings.prototype['Env'] = undefined;

/**
 * mounts
 * @member {Array.<module:model/PluginMount>} Mounts
 */
PluginSettings.prototype['Mounts'] = undefined;






export default PluginSettings;

