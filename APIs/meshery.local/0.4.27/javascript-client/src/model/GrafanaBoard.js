/**
 * Meshery API.
 * the purpose of this application is to provide an application that is using plain go code to define an API  This should demonstrate all the possible comment annotations that are available to turn go code into a fully compliant swagger 2.0 spec
 *
 * The version of the OpenAPI document: 0.4.27
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GrafanaTemplateVars from './GrafanaTemplateVars';
import Panel from './Panel';

/**
 * The GrafanaBoard model module.
 * @module model/GrafanaBoard
 * @version 0.4.27
 */
class GrafanaBoard {
    /**
     * Constructs a new <code>GrafanaBoard</code>.
     * GrafanaBoard represents a Grafana board
     * @alias module:model/GrafanaBoard
     */
    constructor() { 
        
        GrafanaBoard.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GrafanaBoard</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GrafanaBoard} obj Optional instance to populate.
     * @return {module:model/GrafanaBoard} The populated <code>GrafanaBoard</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GrafanaBoard();

            if (data.hasOwnProperty('org_id')) {
                obj['org_id'] = ApiClient.convertToType(data['org_id'], 'Number');
            }
            if (data.hasOwnProperty('panels')) {
                obj['panels'] = ApiClient.convertToType(data['panels'], [Panel]);
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
            if (data.hasOwnProperty('template_vars')) {
                obj['template_vars'] = ApiClient.convertToType(data['template_vars'], [GrafanaTemplateVars]);
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('uid')) {
                obj['uid'] = ApiClient.convertToType(data['uid'], 'String');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GrafanaBoard</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GrafanaBoard</code>.
     */
    static validateJSON(data) {
        if (data['panels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['panels'])) {
                throw new Error("Expected the field `panels` to be an array in the JSON data but got " + data['panels']);
            }
            // validate the optional field `panels` (array)
            for (const item of data['panels']) {
                Panel.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }
        if (data['template_vars']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['template_vars'])) {
                throw new Error("Expected the field `template_vars` to be an array in the JSON data but got " + data['template_vars']);
            }
            // validate the optional field `template_vars` (array)
            for (const item of data['template_vars']) {
                GrafanaTemplateVars.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['uid'] && !(typeof data['uid'] === 'string' || data['uid'] instanceof String)) {
            throw new Error("Expected the field `uid` to be a primitive type in the JSON string but got " + data['uid']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }

        return true;
    }


}



/**
 * @member {Number} org_id
 */
GrafanaBoard.prototype['org_id'] = undefined;

/**
 * @member {Array.<module:model/Panel>} panels
 */
GrafanaBoard.prototype['panels'] = undefined;

/**
 * @member {String} slug
 */
GrafanaBoard.prototype['slug'] = undefined;

/**
 * @member {Array.<module:model/GrafanaTemplateVars>} template_vars
 */
GrafanaBoard.prototype['template_vars'] = undefined;

/**
 * @member {String} title
 */
GrafanaBoard.prototype['title'] = undefined;

/**
 * @member {String} uid
 */
GrafanaBoard.prototype['uid'] = undefined;

/**
 * @member {String} uri
 */
GrafanaBoard.prototype['uri'] = undefined;






export default GrafanaBoard;

