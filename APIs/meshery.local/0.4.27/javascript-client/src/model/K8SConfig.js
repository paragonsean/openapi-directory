/**
 * Meshery API.
 * the purpose of this application is to provide an application that is using plain go code to define an API  This should demonstrate all the possible comment annotations that are available to turn go code into a fully compliant swagger 2.0 spec
 *
 * The version of the OpenAPI document: 0.4.27
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import K8SNode from './K8SNode';

/**
 * The K8SConfig model module.
 * @module model/K8SConfig
 * @version 0.4.27
 */
class K8SConfig {
    /**
     * Constructs a new <code>K8SConfig</code>.
     * K8SConfig represents all the k8s session config
     * @alias module:model/K8SConfig
     */
    constructor() { 
        
        K8SConfig.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>K8SConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/K8SConfig} obj Optional instance to populate.
     * @return {module:model/K8SConfig} The populated <code>K8SConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new K8SConfig();

            if (data.hasOwnProperty('clusterConfigured')) {
                obj['clusterConfigured'] = ApiClient.convertToType(data['clusterConfigured'], 'Boolean');
            }
            if (data.hasOwnProperty('config')) {
                obj['config'] = ApiClient.convertToType(data['config'], ['Number']);
            }
            if (data.hasOwnProperty('configuredServer')) {
                obj['configuredServer'] = ApiClient.convertToType(data['configuredServer'], 'String');
            }
            if (data.hasOwnProperty('contextName')) {
                obj['contextName'] = ApiClient.convertToType(data['contextName'], 'String');
            }
            if (data.hasOwnProperty('inClusterConfig')) {
                obj['inClusterConfig'] = ApiClient.convertToType(data['inClusterConfig'], 'Boolean');
            }
            if (data.hasOwnProperty('k8sfile')) {
                obj['k8sfile'] = ApiClient.convertToType(data['k8sfile'], 'String');
            }
            if (data.hasOwnProperty('nodes')) {
                obj['nodes'] = ApiClient.convertToType(data['nodes'], [K8SNode]);
            }
            if (data.hasOwnProperty('server_version')) {
                obj['server_version'] = ApiClient.convertToType(data['server_version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>K8SConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>K8SConfig</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['config'])) {
            throw new Error("Expected the field `config` to be an array in the JSON data but got " + data['config']);
        }
        // ensure the json data is a string
        if (data['configuredServer'] && !(typeof data['configuredServer'] === 'string' || data['configuredServer'] instanceof String)) {
            throw new Error("Expected the field `configuredServer` to be a primitive type in the JSON string but got " + data['configuredServer']);
        }
        // ensure the json data is a string
        if (data['contextName'] && !(typeof data['contextName'] === 'string' || data['contextName'] instanceof String)) {
            throw new Error("Expected the field `contextName` to be a primitive type in the JSON string but got " + data['contextName']);
        }
        // ensure the json data is a string
        if (data['k8sfile'] && !(typeof data['k8sfile'] === 'string' || data['k8sfile'] instanceof String)) {
            throw new Error("Expected the field `k8sfile` to be a primitive type in the JSON string but got " + data['k8sfile']);
        }
        if (data['nodes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['nodes'])) {
                throw new Error("Expected the field `nodes` to be an array in the JSON data but got " + data['nodes']);
            }
            // validate the optional field `nodes` (array)
            for (const item of data['nodes']) {
                K8SNode.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['server_version'] && !(typeof data['server_version'] === 'string' || data['server_version'] instanceof String)) {
            throw new Error("Expected the field `server_version` to be a primitive type in the JSON string but got " + data['server_version']);
        }

        return true;
    }


}



/**
 * @member {Boolean} clusterConfigured
 */
K8SConfig.prototype['clusterConfigured'] = undefined;

/**
 * @member {Array.<Number>} config
 */
K8SConfig.prototype['config'] = undefined;

/**
 * @member {String} configuredServer
 */
K8SConfig.prototype['configuredServer'] = undefined;

/**
 * @member {String} contextName
 */
K8SConfig.prototype['contextName'] = undefined;

/**
 * @member {Boolean} inClusterConfig
 */
K8SConfig.prototype['inClusterConfig'] = undefined;

/**
 * @member {String} k8sfile
 */
K8SConfig.prototype['k8sfile'] = undefined;

/**
 * @member {Array.<module:model/K8SNode>} nodes
 */
K8SConfig.prototype['nodes'] = undefined;

/**
 * @member {String} server_version
 */
K8SConfig.prototype['server_version'] = undefined;






export default K8SConfig;

