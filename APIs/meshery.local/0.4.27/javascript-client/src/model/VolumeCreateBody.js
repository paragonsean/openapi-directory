/**
 * Meshery API.
 * the purpose of this application is to provide an application that is using plain go code to define an API  This should demonstrate all the possible comment annotations that are available to turn go code into a fully compliant swagger 2.0 spec
 *
 * The version of the OpenAPI document: 0.4.27
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VolumeCreateBody model module.
 * @module model/VolumeCreateBody
 * @version 0.4.27
 */
class VolumeCreateBody {
    /**
     * Constructs a new <code>VolumeCreateBody</code>.
     * VolumeCreateBody Volume configuration
     * @alias module:model/VolumeCreateBody
     * @param driver {String} Name of the volume driver to use.
     * @param driverOpts {Object.<String, String>} A mapping of driver options and values. These options are passed directly to the driver and are driver specific.
     * @param labels {Object.<String, String>} User-defined key/value metadata.
     * @param name {String} The new volume's name. If not specified, Docker generates a name.
     */
    constructor(driver, driverOpts, labels, name) { 
        
        VolumeCreateBody.initialize(this, driver, driverOpts, labels, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, driver, driverOpts, labels, name) { 
        obj['Driver'] = driver;
        obj['DriverOpts'] = driverOpts;
        obj['Labels'] = labels;
        obj['Name'] = name;
    }

    /**
     * Constructs a <code>VolumeCreateBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VolumeCreateBody} obj Optional instance to populate.
     * @return {module:model/VolumeCreateBody} The populated <code>VolumeCreateBody</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VolumeCreateBody();

            if (data.hasOwnProperty('Driver')) {
                obj['Driver'] = ApiClient.convertToType(data['Driver'], 'String');
            }
            if (data.hasOwnProperty('DriverOpts')) {
                obj['DriverOpts'] = ApiClient.convertToType(data['DriverOpts'], {'String': 'String'});
            }
            if (data.hasOwnProperty('Labels')) {
                obj['Labels'] = ApiClient.convertToType(data['Labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('Name')) {
                obj['Name'] = ApiClient.convertToType(data['Name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VolumeCreateBody</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VolumeCreateBody</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VolumeCreateBody.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Driver'] && !(typeof data['Driver'] === 'string' || data['Driver'] instanceof String)) {
            throw new Error("Expected the field `Driver` to be a primitive type in the JSON string but got " + data['Driver']);
        }
        // ensure the json data is a string
        if (data['Name'] && !(typeof data['Name'] === 'string' || data['Name'] instanceof String)) {
            throw new Error("Expected the field `Name` to be a primitive type in the JSON string but got " + data['Name']);
        }

        return true;
    }


}

VolumeCreateBody.RequiredProperties = ["Driver", "DriverOpts", "Labels", "Name"];

/**
 * Name of the volume driver to use.
 * @member {String} Driver
 */
VolumeCreateBody.prototype['Driver'] = undefined;

/**
 * A mapping of driver options and values. These options are passed directly to the driver and are driver specific.
 * @member {Object.<String, String>} DriverOpts
 */
VolumeCreateBody.prototype['DriverOpts'] = undefined;

/**
 * User-defined key/value metadata.
 * @member {Object.<String, String>} Labels
 */
VolumeCreateBody.prototype['Labels'] = undefined;

/**
 * The new volume's name. If not specified, Docker generates a name.
 * @member {String} Name
 */
VolumeCreateBody.prototype['Name'] = undefined;






export default VolumeCreateBody;

