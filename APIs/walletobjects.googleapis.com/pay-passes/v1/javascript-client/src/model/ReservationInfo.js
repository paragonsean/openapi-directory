/**
 * Google Pay Passes API
 * API for issuers to save and manage Google Wallet Objects.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FrequentFlyerInfo from './FrequentFlyerInfo';

/**
 * The ReservationInfo model module.
 * @module model/ReservationInfo
 * @version v1
 */
class ReservationInfo {
    /**
     * Constructs a new <code>ReservationInfo</code>.
     * @alias module:model/ReservationInfo
     */
    constructor() { 
        
        ReservationInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReservationInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReservationInfo} obj Optional instance to populate.
     * @return {module:model/ReservationInfo} The populated <code>ReservationInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReservationInfo();

            if (data.hasOwnProperty('confirmationCode')) {
                obj['confirmationCode'] = ApiClient.convertToType(data['confirmationCode'], 'String');
            }
            if (data.hasOwnProperty('eticketNumber')) {
                obj['eticketNumber'] = ApiClient.convertToType(data['eticketNumber'], 'String');
            }
            if (data.hasOwnProperty('frequentFlyerInfo')) {
                obj['frequentFlyerInfo'] = FrequentFlyerInfo.constructFromObject(data['frequentFlyerInfo']);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReservationInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReservationInfo</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['confirmationCode'] && !(typeof data['confirmationCode'] === 'string' || data['confirmationCode'] instanceof String)) {
            throw new Error("Expected the field `confirmationCode` to be a primitive type in the JSON string but got " + data['confirmationCode']);
        }
        // ensure the json data is a string
        if (data['eticketNumber'] && !(typeof data['eticketNumber'] === 'string' || data['eticketNumber'] instanceof String)) {
            throw new Error("Expected the field `eticketNumber` to be a primitive type in the JSON string but got " + data['eticketNumber']);
        }
        // validate the optional field `frequentFlyerInfo`
        if (data['frequentFlyerInfo']) { // data not null
          FrequentFlyerInfo.validateJSON(data['frequentFlyerInfo']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }

        return true;
    }


}



/**
 * Confirmation code needed to check into this flight. This is the number that the passenger would enter into a kiosk at the airport to look up the flight and print a boarding pass.
 * @member {String} confirmationCode
 */
ReservationInfo.prototype['confirmationCode'] = undefined;

/**
 * E-ticket number.
 * @member {String} eticketNumber
 */
ReservationInfo.prototype['eticketNumber'] = undefined;

/**
 * @member {module:model/FrequentFlyerInfo} frequentFlyerInfo
 */
ReservationInfo.prototype['frequentFlyerInfo'] = undefined;

/**
 * Identifies what kind of resource this is. Value: the fixed string `\"walletobjects#reservationInfo\"`.
 * @member {String} kind
 */
ReservationInfo.prototype['kind'] = undefined;






export default ReservationInfo;

