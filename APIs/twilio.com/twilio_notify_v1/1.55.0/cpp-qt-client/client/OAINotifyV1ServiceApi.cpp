/**
 * Twilio - Notify
 * This is the public Twilio REST API.
 *
 * The version of the OpenAPI document: 1.55.0
 * Contact: support@twilio.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAINotifyV1ServiceApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAINotifyV1ServiceApi::OAINotifyV1ServiceApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAINotifyV1ServiceApi::~OAINotifyV1ServiceApi() {
}

void OAINotifyV1ServiceApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://notify.twilio.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    serverConf.append(OAIServerConfiguration(
    QUrl("https://notify.twilio.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("createService", serverConf);
    _serverIndices.insert("createService", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("https://notify.twilio.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("deleteService", serverConf);
    _serverIndices.insert("deleteService", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("https://notify.twilio.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("fetchService", serverConf);
    _serverIndices.insert("fetchService", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("https://notify.twilio.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("listService", serverConf);
    _serverIndices.insert("listService", 0);
    serverConf.append(OAIServerConfiguration(
    QUrl("https://notify.twilio.com"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("updateService", serverConf);
    _serverIndices.insert("updateService", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAINotifyV1ServiceApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAINotifyV1ServiceApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAINotifyV1ServiceApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAINotifyV1ServiceApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAINotifyV1ServiceApi::setUsername(const QString &username) {
    _username = username;
}

void OAINotifyV1ServiceApi::setPassword(const QString &password) {
    _password = password;
}


void OAINotifyV1ServiceApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAINotifyV1ServiceApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAINotifyV1ServiceApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAINotifyV1ServiceApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAINotifyV1ServiceApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAINotifyV1ServiceApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAINotifyV1ServiceApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAINotifyV1ServiceApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAINotifyV1ServiceApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAINotifyV1ServiceApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAINotifyV1ServiceApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAINotifyV1ServiceApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAINotifyV1ServiceApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAINotifyV1ServiceApi::createService(const ::OpenAPI::OptionalParam<QString> &alexa_skill_id, const ::OpenAPI::OptionalParam<QString> &apn_credential_sid, const ::OpenAPI::OptionalParam<QString> &default_alexa_notification_protocol_version, const ::OpenAPI::OptionalParam<QString> &default_apn_notification_protocol_version, const ::OpenAPI::OptionalParam<QString> &default_fcm_notification_protocol_version, const ::OpenAPI::OptionalParam<QString> &default_gcm_notification_protocol_version, const ::OpenAPI::OptionalParam<bool> &delivery_callback_enabled, const ::OpenAPI::OptionalParam<QString> &delivery_callback_url, const ::OpenAPI::OptionalParam<QString> &facebook_messenger_page_id, const ::OpenAPI::OptionalParam<QString> &fcm_credential_sid, const ::OpenAPI::OptionalParam<QString> &friendly_name, const ::OpenAPI::OptionalParam<QString> &gcm_credential_sid, const ::OpenAPI::OptionalParam<bool> &log_enabled, const ::OpenAPI::OptionalParam<QString> &messaging_service_sid) {
    QString fullPath = QString(_serverConfigs["createService"][_serverIndices.value("createService")].URL()+"/v1/Services");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (alexa_skill_id.hasValue())
    {
        input.add_var("AlexaSkillId", ::OpenAPI::toStringValue(alexa_skill_id.value()));
    }
    if (apn_credential_sid.hasValue())
    {
        input.add_var("ApnCredentialSid", ::OpenAPI::toStringValue(apn_credential_sid.value()));
    }
    if (default_alexa_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultAlexaNotificationProtocolVersion", ::OpenAPI::toStringValue(default_alexa_notification_protocol_version.value()));
    }
    if (default_apn_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultApnNotificationProtocolVersion", ::OpenAPI::toStringValue(default_apn_notification_protocol_version.value()));
    }
    if (default_fcm_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultFcmNotificationProtocolVersion", ::OpenAPI::toStringValue(default_fcm_notification_protocol_version.value()));
    }
    if (default_gcm_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultGcmNotificationProtocolVersion", ::OpenAPI::toStringValue(default_gcm_notification_protocol_version.value()));
    }
    if (delivery_callback_enabled.hasValue())
    {
        input.add_var("DeliveryCallbackEnabled", ::OpenAPI::toStringValue(delivery_callback_enabled.value()));
    }
    if (delivery_callback_url.hasValue())
    {
        input.add_var("DeliveryCallbackUrl", ::OpenAPI::toStringValue(delivery_callback_url.value()));
    }
    if (facebook_messenger_page_id.hasValue())
    {
        input.add_var("FacebookMessengerPageId", ::OpenAPI::toStringValue(facebook_messenger_page_id.value()));
    }
    if (fcm_credential_sid.hasValue())
    {
        input.add_var("FcmCredentialSid", ::OpenAPI::toStringValue(fcm_credential_sid.value()));
    }
    if (friendly_name.hasValue())
    {
        input.add_var("FriendlyName", ::OpenAPI::toStringValue(friendly_name.value()));
    }
    if (gcm_credential_sid.hasValue())
    {
        input.add_var("GcmCredentialSid", ::OpenAPI::toStringValue(gcm_credential_sid.value()));
    }
    if (log_enabled.hasValue())
    {
        input.add_var("LogEnabled", ::OpenAPI::toStringValue(log_enabled.value()));
    }
    if (messaging_service_sid.hasValue())
    {
        input.add_var("MessagingServiceSid", ::OpenAPI::toStringValue(messaging_service_sid.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotifyV1ServiceApi::createServiceCallback);
    connect(this, &OAINotifyV1ServiceApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotifyV1ServiceApi::createServiceCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINotify_v1_service output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT createServiceSignal(output);
        Q_EMIT createServiceSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT createServiceSignalE(output, error_type, error_str);
        Q_EMIT createServiceSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT createServiceSignalError(output, error_type, error_str);
        Q_EMIT createServiceSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotifyV1ServiceApi::deleteService(const QString &sid) {
    QString fullPath = QString(_serverConfigs["deleteService"][_serverIndices.value("deleteService")].URL()+"/v1/Services/{Sid}");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString sidPathParam("{");
        sidPathParam.append("Sid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "Sid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"Sid"+pathSuffix : pathPrefix;
        fullPath.replace(sidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(sid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotifyV1ServiceApi::deleteServiceCallback);
    connect(this, &OAINotifyV1ServiceApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotifyV1ServiceApi::deleteServiceCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT deleteServiceSignal();
        Q_EMIT deleteServiceSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT deleteServiceSignalE(error_type, error_str);
        Q_EMIT deleteServiceSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT deleteServiceSignalError(error_type, error_str);
        Q_EMIT deleteServiceSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotifyV1ServiceApi::fetchService(const QString &sid) {
    QString fullPath = QString(_serverConfigs["fetchService"][_serverIndices.value("fetchService")].URL()+"/v1/Services/{Sid}");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString sidPathParam("{");
        sidPathParam.append("Sid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "Sid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"Sid"+pathSuffix : pathPrefix;
        fullPath.replace(sidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(sid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotifyV1ServiceApi::fetchServiceCallback);
    connect(this, &OAINotifyV1ServiceApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotifyV1ServiceApi::fetchServiceCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINotify_v1_service output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT fetchServiceSignal(output);
        Q_EMIT fetchServiceSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT fetchServiceSignalE(output, error_type, error_str);
        Q_EMIT fetchServiceSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT fetchServiceSignalError(output, error_type, error_str);
        Q_EMIT fetchServiceSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotifyV1ServiceApi::listService(const ::OpenAPI::OptionalParam<QString> &friendly_name, const ::OpenAPI::OptionalParam<qint32> &page_size, const ::OpenAPI::OptionalParam<qint32> &page, const ::OpenAPI::OptionalParam<QString> &page_token) {
    QString fullPath = QString(_serverConfigs["listService"][_serverIndices.value("listService")].URL()+"/v1/Services");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (friendly_name.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "FriendlyName", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("FriendlyName")).append(querySuffix).append(QUrl::toPercentEncoding(friendly_name.stringValue()));
    }
    if (page_size.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "PageSize", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("PageSize")).append(querySuffix).append(QUrl::toPercentEncoding(page_size.stringValue()));
    }
    if (page.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "Page", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("Page")).append(querySuffix).append(QUrl::toPercentEncoding(page.stringValue()));
    }
    if (page_token.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "PageToken", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("PageToken")).append(querySuffix).append(QUrl::toPercentEncoding(page_token.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotifyV1ServiceApi::listServiceCallback);
    connect(this, &OAINotifyV1ServiceApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotifyV1ServiceApi::listServiceCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIListServiceResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT listServiceSignal(output);
        Q_EMIT listServiceSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT listServiceSignalE(output, error_type, error_str);
        Q_EMIT listServiceSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT listServiceSignalError(output, error_type, error_str);
        Q_EMIT listServiceSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotifyV1ServiceApi::updateService(const QString &sid, const ::OpenAPI::OptionalParam<QString> &alexa_skill_id, const ::OpenAPI::OptionalParam<QString> &apn_credential_sid, const ::OpenAPI::OptionalParam<QString> &default_alexa_notification_protocol_version, const ::OpenAPI::OptionalParam<QString> &default_apn_notification_protocol_version, const ::OpenAPI::OptionalParam<QString> &default_fcm_notification_protocol_version, const ::OpenAPI::OptionalParam<QString> &default_gcm_notification_protocol_version, const ::OpenAPI::OptionalParam<bool> &delivery_callback_enabled, const ::OpenAPI::OptionalParam<QString> &delivery_callback_url, const ::OpenAPI::OptionalParam<QString> &facebook_messenger_page_id, const ::OpenAPI::OptionalParam<QString> &fcm_credential_sid, const ::OpenAPI::OptionalParam<QString> &friendly_name, const ::OpenAPI::OptionalParam<QString> &gcm_credential_sid, const ::OpenAPI::OptionalParam<bool> &log_enabled, const ::OpenAPI::OptionalParam<QString> &messaging_service_sid) {
    QString fullPath = QString(_serverConfigs["updateService"][_serverIndices.value("updateService")].URL()+"/v1/Services/{Sid}");
    
    if (!_username.isEmpty() && !_password.isEmpty()) {
        QByteArray b64;
        b64.append(_username.toUtf8() + ":" + _password.toUtf8());
        addHeaders("Authorization","Basic " + b64.toBase64());
    }
    
    {
        QString sidPathParam("{");
        sidPathParam.append("Sid").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "Sid", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"Sid"+pathSuffix : pathPrefix;
        fullPath.replace(sidPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(sid)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (alexa_skill_id.hasValue())
    {
        input.add_var("AlexaSkillId", ::OpenAPI::toStringValue(alexa_skill_id.value()));
    }
    if (apn_credential_sid.hasValue())
    {
        input.add_var("ApnCredentialSid", ::OpenAPI::toStringValue(apn_credential_sid.value()));
    }
    if (default_alexa_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultAlexaNotificationProtocolVersion", ::OpenAPI::toStringValue(default_alexa_notification_protocol_version.value()));
    }
    if (default_apn_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultApnNotificationProtocolVersion", ::OpenAPI::toStringValue(default_apn_notification_protocol_version.value()));
    }
    if (default_fcm_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultFcmNotificationProtocolVersion", ::OpenAPI::toStringValue(default_fcm_notification_protocol_version.value()));
    }
    if (default_gcm_notification_protocol_version.hasValue())
    {
        input.add_var("DefaultGcmNotificationProtocolVersion", ::OpenAPI::toStringValue(default_gcm_notification_protocol_version.value()));
    }
    if (delivery_callback_enabled.hasValue())
    {
        input.add_var("DeliveryCallbackEnabled", ::OpenAPI::toStringValue(delivery_callback_enabled.value()));
    }
    if (delivery_callback_url.hasValue())
    {
        input.add_var("DeliveryCallbackUrl", ::OpenAPI::toStringValue(delivery_callback_url.value()));
    }
    if (facebook_messenger_page_id.hasValue())
    {
        input.add_var("FacebookMessengerPageId", ::OpenAPI::toStringValue(facebook_messenger_page_id.value()));
    }
    if (fcm_credential_sid.hasValue())
    {
        input.add_var("FcmCredentialSid", ::OpenAPI::toStringValue(fcm_credential_sid.value()));
    }
    if (friendly_name.hasValue())
    {
        input.add_var("FriendlyName", ::OpenAPI::toStringValue(friendly_name.value()));
    }
    if (gcm_credential_sid.hasValue())
    {
        input.add_var("GcmCredentialSid", ::OpenAPI::toStringValue(gcm_credential_sid.value()));
    }
    if (log_enabled.hasValue())
    {
        input.add_var("LogEnabled", ::OpenAPI::toStringValue(log_enabled.value()));
    }
    if (messaging_service_sid.hasValue())
    {
        input.add_var("MessagingServiceSid", ::OpenAPI::toStringValue(messaging_service_sid.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotifyV1ServiceApi::updateServiceCallback);
    connect(this, &OAINotifyV1ServiceApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotifyV1ServiceApi::updateServiceCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINotify_v1_service output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT updateServiceSignal(output);
        Q_EMIT updateServiceSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT updateServiceSignalE(output, error_type, error_str);
        Q_EMIT updateServiceSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT updateServiceSignalError(output, error_type, error_str);
        Q_EMIT updateServiceSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotifyV1ServiceApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
