/**
 * Netlify's API documentation
 * Netlify is a hosting service for the programmable web. It understands your documents and provides an API to handle atomic deploys of websites, manage form submissions, inject JavaScript snippets, and much more. This is a REST-style API that uses JSON for serialization and OAuth 2 for authentication.  This document is an OpenAPI reference for the Netlify API that you can explore. For more detailed instructions for common uses, please visit the [online documentation](https://www.netlify.com/docs/api/). Visit our Community forum to join the conversation about [understanding and using Netlify’s API](https://community.netlify.com/t/common-issue-understanding-and-using-netlifys-api/160).  Additionally, we have two API clients for your convenience: - [Go Client](https://github.com/netlify/open-api#go-client) - [JS Client](https://github.com/netlify/build/tree/main/packages/js-client)
 *
 * The version of the OpenAPI document: 2.16.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnvVarUser from './EnvVarUser';
import EnvVarValue from './EnvVarValue';

/**
 * The EnvVar model module.
 * @module model/EnvVar
 * @version 2.16.0
 */
class EnvVar {
    /**
     * Constructs a new <code>EnvVar</code>.
     * Environment variable model definition
     * @alias module:model/EnvVar
     */
    constructor() { 
        
        EnvVar.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EnvVar</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EnvVar} obj Optional instance to populate.
     * @return {module:model/EnvVar} The populated <code>EnvVar</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EnvVar();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('scopes')) {
                obj['scopes'] = ApiClient.convertToType(data['scopes'], ['String']);
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_by')) {
                obj['updated_by'] = EnvVarUser.constructFromObject(data['updated_by']);
            }
            if (data.hasOwnProperty('values')) {
                obj['values'] = ApiClient.convertToType(data['values'], [EnvVarValue]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EnvVar</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EnvVar</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scopes'])) {
            throw new Error("Expected the field `scopes` to be an array in the JSON data but got " + data['scopes']);
        }
        // validate the optional field `updated_by`
        if (data['updated_by']) { // data not null
          EnvVarUser.validateJSON(data['updated_by']);
        }
        if (data['values']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['values'])) {
                throw new Error("Expected the field `values` to be an array in the JSON data but got " + data['values']);
            }
            // validate the optional field `values` (array)
            for (const item of data['values']) {
                EnvVarValue.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The environment variable key, like ALGOLIA_ID (case-sensitive)
 * @member {String} key
 */
EnvVar.prototype['key'] = undefined;

/**
 * The scopes that this environment variable is set to
 * @member {Array.<module:model/EnvVar.ScopesEnum>} scopes
 */
EnvVar.prototype['scopes'] = undefined;

/**
 * The timestamp of when the value was last updated
 * @member {Date} updated_at
 */
EnvVar.prototype['updated_at'] = undefined;

/**
 * @member {module:model/EnvVarUser} updated_by
 */
EnvVar.prototype['updated_by'] = undefined;

/**
 * An array of Value objects containing values and metadata
 * @member {Array.<module:model/EnvVarValue>} values
 */
EnvVar.prototype['values'] = undefined;





/**
 * Allowed values for the <code>scopes</code> property.
 * @enum {String}
 * @readonly
 */
EnvVar['ScopesEnum'] = {

    /**
     * value: "builds"
     * @const
     */
    "builds": "builds",

    /**
     * value: "functions"
     * @const
     */
    "functions": "functions",

    /**
     * value: "runtime"
     * @const
     */
    "runtime": "runtime",

    /**
     * value: "post-processing"
     * @const
     */
    "post-processing": "post-processing"
};



export default EnvVar;

