/**
 * Travel Partner API
 * The Travel Partner API provides you with a RESTful interface to the Google Hotel Center platform. It enables an app to efficiently retrieve and change Hotel Center data, and is thus suitable for managing large or complex accounts.
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ModelDate from './ModelDate';

/**
 * The FreeBookingLinksResult model module.
 * @module model/FreeBookingLinksResult
 * @version v3
 */
class FreeBookingLinksResult {
    /**
     * Constructs a new <code>FreeBookingLinksResult</code>.
     * **DEPRECATED:** Use &#x60;PropertyPerformanceResult&#x60; with &#x60;PropertyPerformanceReportService&#x60; instead. Represents a result from querying for free booking link stats for an account.
     * @alias module:model/FreeBookingLinksResult
     */
    constructor() { 
        
        FreeBookingLinksResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FreeBookingLinksResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FreeBookingLinksResult} obj Optional instance to populate.
     * @return {module:model/FreeBookingLinksResult} The populated <code>FreeBookingLinksResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FreeBookingLinksResult();

            if (data.hasOwnProperty('clickCount')) {
                obj['clickCount'] = ApiClient.convertToType(data['clickCount'], 'String');
            }
            if (data.hasOwnProperty('date')) {
                obj['date'] = ModelDate.constructFromObject(data['date']);
            }
            if (data.hasOwnProperty('deviceType')) {
                obj['deviceType'] = ApiClient.convertToType(data['deviceType'], 'String');
            }
            if (data.hasOwnProperty('partnerHotelDisplayName')) {
                obj['partnerHotelDisplayName'] = ApiClient.convertToType(data['partnerHotelDisplayName'], 'String');
            }
            if (data.hasOwnProperty('partnerHotelId')) {
                obj['partnerHotelId'] = ApiClient.convertToType(data['partnerHotelId'], 'String');
            }
            if (data.hasOwnProperty('userRegionCode')) {
                obj['userRegionCode'] = ApiClient.convertToType(data['userRegionCode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FreeBookingLinksResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FreeBookingLinksResult</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['clickCount'] && !(typeof data['clickCount'] === 'string' || data['clickCount'] instanceof String)) {
            throw new Error("Expected the field `clickCount` to be a primitive type in the JSON string but got " + data['clickCount']);
        }
        // validate the optional field `date`
        if (data['date']) { // data not null
          ModelDate.validateJSON(data['date']);
        }
        // ensure the json data is a string
        if (data['deviceType'] && !(typeof data['deviceType'] === 'string' || data['deviceType'] instanceof String)) {
            throw new Error("Expected the field `deviceType` to be a primitive type in the JSON string but got " + data['deviceType']);
        }
        // ensure the json data is a string
        if (data['partnerHotelDisplayName'] && !(typeof data['partnerHotelDisplayName'] === 'string' || data['partnerHotelDisplayName'] instanceof String)) {
            throw new Error("Expected the field `partnerHotelDisplayName` to be a primitive type in the JSON string but got " + data['partnerHotelDisplayName']);
        }
        // ensure the json data is a string
        if (data['partnerHotelId'] && !(typeof data['partnerHotelId'] === 'string' || data['partnerHotelId'] instanceof String)) {
            throw new Error("Expected the field `partnerHotelId` to be a primitive type in the JSON string but got " + data['partnerHotelId']);
        }
        // ensure the json data is a string
        if (data['userRegionCode'] && !(typeof data['userRegionCode'] === 'string' || data['userRegionCode'] instanceof String)) {
            throw new Error("Expected the field `userRegionCode` to be a primitive type in the JSON string but got " + data['userRegionCode']);
        }

        return true;
    }


}



/**
 * The total number of clicks that were recorded for this result.
 * @member {String} clickCount
 */
FreeBookingLinksResult.prototype['clickCount'] = undefined;

/**
 * @member {module:model/ModelDate} date
 */
FreeBookingLinksResult.prototype['date'] = undefined;

/**
 * The user’s device type. Only present if `deviceType` is specified in `aggregateBy` in the request.
 * @member {module:model/FreeBookingLinksResult.DeviceTypeEnum} deviceType
 */
FreeBookingLinksResult.prototype['deviceType'] = undefined;

/**
 * Partner's hotel name. Only present if `partnerHotelDisplayName` is specified in `aggregateBy` in the request.
 * @member {String} partnerHotelDisplayName
 */
FreeBookingLinksResult.prototype['partnerHotelDisplayName'] = undefined;

/**
 * Partner's hotel ID. Only present if `partnerHotelId` is specified in `aggregateBy` in the request.
 * @member {String} partnerHotelId
 */
FreeBookingLinksResult.prototype['partnerHotelId'] = undefined;

/**
 * ISO 3116 region code of the country/region of the user. Only present if `userRegionCode` is specified in `aggregateBy` in the request
 * @member {String} userRegionCode
 */
FreeBookingLinksResult.prototype['userRegionCode'] = undefined;





/**
 * Allowed values for the <code>deviceType</code> property.
 * @enum {String}
 * @readonly
 */
FreeBookingLinksResult['DeviceTypeEnum'] = {

    /**
     * value: "DEVICE_UNSPECIFIED"
     * @const
     */
    "DEVICE_UNSPECIFIED": "DEVICE_UNSPECIFIED",

    /**
     * value: "DEVICE_UNKNOWN"
     * @const
     */
    "DEVICE_UNKNOWN": "DEVICE_UNKNOWN",

    /**
     * value: "DESKTOP"
     * @const
     */
    "DESKTOP": "DESKTOP",

    /**
     * value: "MOBILE"
     * @const
     */
    "MOBILE": "MOBILE",

    /**
     * value: "TABLET"
     * @const
     */
    "TABLET": "TABLET"
};



export default FreeBookingLinksResult;

