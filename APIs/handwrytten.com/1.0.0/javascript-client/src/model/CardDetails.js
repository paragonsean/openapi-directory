/**
 * Handwrytten API
 * This is the Handwrytten API for sending cards written in the handwriting of your choice. Using this api, you can send cards to users.  You can also customize cards with logos, which can be saved and then used like any other card in the system. For a \"sandbox\" account, please contact contact@handwrytten.com To move from credit card per-transaction to monthly invoicing, also contact us. [https://www.handwrytten.com](https://www.handwrytten.com) 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: contact@handwrytten.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ImageArray from './ImageArray';

/**
 * The CardDetails model module.
 * @module model/CardDetails
 * @version 1.0.0
 */
class CardDetails {
    /**
     * Constructs a new <code>CardDetails</code>.
     * @alias module:model/CardDetails
     */
    constructor() { 
        
        CardDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CardDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CardDetails} obj Optional instance to populate.
     * @return {module:model/CardDetails} The populated <code>CardDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CardDetails();

            if (data.hasOwnProperty('available_free')) {
                obj['available_free'] = ApiClient.convertToType(data['available_free'], 'Number');
            }
            if (data.hasOwnProperty('category_id')) {
                obj['category_id'] = ApiClient.convertToType(data['category_id'], 'Number');
            }
            if (data.hasOwnProperty('cover')) {
                obj['cover'] = ApiClient.convertToType(data['cover'], 'String');
            }
            if (data.hasOwnProperty('cover_height')) {
                obj['cover_height'] = ApiClient.convertToType(data['cover_height'], 'String');
            }
            if (data.hasOwnProperty('cover_width')) {
                obj['cover_width'] = ApiClient.convertToType(data['cover_width'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('images')) {
                obj['images'] = ApiClient.convertToType(data['images'], [ImageArray]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('orientation')) {
                obj['orientation'] = ApiClient.convertToType(data['orientation'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CardDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CardDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['cover'] && !(typeof data['cover'] === 'string' || data['cover'] instanceof String)) {
            throw new Error("Expected the field `cover` to be a primitive type in the JSON string but got " + data['cover']);
        }
        // ensure the json data is a string
        if (data['cover_height'] && !(typeof data['cover_height'] === 'string' || data['cover_height'] instanceof String)) {
            throw new Error("Expected the field `cover_height` to be a primitive type in the JSON string but got " + data['cover_height']);
        }
        // ensure the json data is a string
        if (data['cover_width'] && !(typeof data['cover_width'] === 'string' || data['cover_width'] instanceof String)) {
            throw new Error("Expected the field `cover_width` to be a primitive type in the JSON string but got " + data['cover_width']);
        }
        if (data['images']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['images'])) {
                throw new Error("Expected the field `images` to be an array in the JSON data but got " + data['images']);
            }
            // validate the optional field `images` (array)
            for (const item of data['images']) {
                ImageArray.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['orientation'] && !(typeof data['orientation'] === 'string' || data['orientation'] instanceof String)) {
            throw new Error("Expected the field `orientation` to be a primitive type in the JSON string but got " + data['orientation']);
        }

        return true;
    }


}



/**
 * 1 for cards that are available free
 * @member {Number} available_free
 */
CardDetails.prototype['available_free'] = undefined;

/**
 * the associated category id
 * @member {Number} category_id
 */
CardDetails.prototype['category_id'] = undefined;

/**
 * cover image of card
 * @member {String} cover
 */
CardDetails.prototype['cover'] = undefined;

/**
 * the height in px of the cover image
 * @member {String} cover_height
 */
CardDetails.prototype['cover_height'] = undefined;

/**
 * the width in px of the cover image
 * @member {String} cover_width
 */
CardDetails.prototype['cover_width'] = undefined;

/**
 * @member {Number} id
 */
CardDetails.prototype['id'] = undefined;

/**
 * common return value includes items for front, back, inside and envelope
 * @member {Array.<module:model/ImageArray>} images
 */
CardDetails.prototype['images'] = undefined;

/**
 * @member {String} name
 */
CardDetails.prototype['name'] = undefined;

/**
 * landscape or portrait
 * @member {String} orientation
 */
CardDetails.prototype['orientation'] = undefined;

/**
 * the base price of a card (in USD)
 * @member {Number} price
 */
CardDetails.prototype['price'] = undefined;






export default CardDetails;

