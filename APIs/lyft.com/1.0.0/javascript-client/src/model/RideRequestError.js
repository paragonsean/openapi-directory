/**
 * Lyft
 * Drive your app to success with Lyft's API
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: api-support@lyft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ErrorDetail from './ErrorDetail';

/**
 * The RideRequestError model module.
 * @module model/RideRequestError
 * @version 1.0.0
 */
class RideRequestError {
    /**
     * Constructs a new <code>RideRequestError</code>.
     * Details about why a request failed, such as missing or invalid parameters
     * @alias module:model/RideRequestError
     * @param error {String} A \"slug\" that serves as the error code (eg. \"bad_parameter\")
     */
    constructor(error) { 
        
        RideRequestError.initialize(this, error);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, error) { 
        obj['error'] = error;
    }

    /**
     * Constructs a <code>RideRequestError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RideRequestError} obj Optional instance to populate.
     * @return {module:model/RideRequestError} The populated <code>RideRequestError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RideRequestError();

            if (data.hasOwnProperty('cost_token')) {
                obj['cost_token'] = ApiClient.convertToType(data['cost_token'], 'String');
            }
            if (data.hasOwnProperty('error')) {
                obj['error'] = ApiClient.convertToType(data['error'], 'String');
            }
            if (data.hasOwnProperty('error_description')) {
                obj['error_description'] = ApiClient.convertToType(data['error_description'], 'String');
            }
            if (data.hasOwnProperty('error_detail')) {
                obj['error_detail'] = ApiClient.convertToType(data['error_detail'], [ErrorDetail]);
            }
            if (data.hasOwnProperty('error_uri')) {
                obj['error_uri'] = ApiClient.convertToType(data['error_uri'], 'String');
            }
            if (data.hasOwnProperty('primetime_confirmation_token')) {
                obj['primetime_confirmation_token'] = ApiClient.convertToType(data['primetime_confirmation_token'], 'String');
            }
            if (data.hasOwnProperty('primetime_multiplier')) {
                obj['primetime_multiplier'] = ApiClient.convertToType(data['primetime_multiplier'], 'Number');
            }
            if (data.hasOwnProperty('primetime_percentage')) {
                obj['primetime_percentage'] = ApiClient.convertToType(data['primetime_percentage'], 'String');
            }
            if (data.hasOwnProperty('token_duration')) {
                obj['token_duration'] = ApiClient.convertToType(data['token_duration'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RideRequestError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RideRequestError</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RideRequestError.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cost_token'] && !(typeof data['cost_token'] === 'string' || data['cost_token'] instanceof String)) {
            throw new Error("Expected the field `cost_token` to be a primitive type in the JSON string but got " + data['cost_token']);
        }
        // ensure the json data is a string
        if (data['error'] && !(typeof data['error'] === 'string' || data['error'] instanceof String)) {
            throw new Error("Expected the field `error` to be a primitive type in the JSON string but got " + data['error']);
        }
        // ensure the json data is a string
        if (data['error_description'] && !(typeof data['error_description'] === 'string' || data['error_description'] instanceof String)) {
            throw new Error("Expected the field `error_description` to be a primitive type in the JSON string but got " + data['error_description']);
        }
        if (data['error_detail']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['error_detail'])) {
                throw new Error("Expected the field `error_detail` to be an array in the JSON data but got " + data['error_detail']);
            }
            // validate the optional field `error_detail` (array)
            for (const item of data['error_detail']) {
                ErrorDetail.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['error_uri'] && !(typeof data['error_uri'] === 'string' || data['error_uri'] instanceof String)) {
            throw new Error("Expected the field `error_uri` to be a primitive type in the JSON string but got " + data['error_uri']);
        }
        // ensure the json data is a string
        if (data['primetime_confirmation_token'] && !(typeof data['primetime_confirmation_token'] === 'string' || data['primetime_confirmation_token'] instanceof String)) {
            throw new Error("Expected the field `primetime_confirmation_token` to be a primitive type in the JSON string but got " + data['primetime_confirmation_token']);
        }
        // ensure the json data is a string
        if (data['primetime_percentage'] && !(typeof data['primetime_percentage'] === 'string' || data['primetime_percentage'] instanceof String)) {
            throw new Error("Expected the field `primetime_percentage` to be a primitive type in the JSON string but got " + data['primetime_percentage']);
        }
        // ensure the json data is a string
        if (data['token_duration'] && !(typeof data['token_duration'] === 'string' || data['token_duration'] instanceof String)) {
            throw new Error("Expected the field `token_duration` to be a primitive type in the JSON string but got " + data['token_duration']);
        }

        return true;
    }


}

RideRequestError.RequiredProperties = ["error"];

/**
 * A token that confirms the user has accepted current Prime Time and/or fixed price charges
 * @member {String} cost_token
 */
RideRequestError.prototype['cost_token'] = undefined;

/**
 * A \"slug\" that serves as the error code (eg. \"bad_parameter\")
 * @member {String} error
 */
RideRequestError.prototype['error'] = undefined;

/**
 * A user-friendly description of the error (appropriate to show to an end-user)
 * @member {String} error_description
 */
RideRequestError.prototype['error_description'] = undefined;

/**
 * @member {Array.<module:model/ErrorDetail>} error_detail
 */
RideRequestError.prototype['error_detail'] = undefined;

/**
 * When a user must go through another flow before requesting a ride, this URI specifies which flow to use (e.g. an account challenge flow in a web view)
 * @member {String} error_uri
 */
RideRequestError.prototype['error_uri'] = undefined;

/**
 * A token that confirms the user has accepted current Prime Time charges (Deprecated)
 * @member {String} primetime_confirmation_token
 */
RideRequestError.prototype['primetime_confirmation_token'] = undefined;

/**
 * Current Prime Time multiplier (eg. if primetime_percentage is 100%, primetime_multiplier will be 2.0)
 * @member {Number} primetime_multiplier
 */
RideRequestError.prototype['primetime_multiplier'] = undefined;

/**
 * Current Prime Time percentage
 * @member {String} primetime_percentage
 */
RideRequestError.prototype['primetime_percentage'] = undefined;

/**
 * Validity of the token in seconds
 * @member {String} token_duration
 */
RideRequestError.prototype['token_duration'] = undefined;






export default RideRequestError;

