/**
 * Radio & Music Services
 * We encapsulate Radio & Music business logic for iPlayer Radio and BBC Music products on all platforms. We add value by reliably providing the right blend of metadata needed by clients.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ProgrammeSummary model module.
 * @module model/ProgrammeSummary
 * @version 1.0.0
 */
class ProgrammeSummary {
    /**
     * Constructs a new <code>ProgrammeSummary</code>.
     * @alias module:model/ProgrammeSummary
     * @param pid {String} 
     * @param type {module:model/ProgrammeSummary.TypeEnum} 
     */
    constructor(pid, type) { 
        
        ProgrammeSummary.initialize(this, pid, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, pid, type) { 
        obj['pid'] = pid;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ProgrammeSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProgrammeSummary} obj Optional instance to populate.
     * @return {module:model/ProgrammeSummary} The populated <code>ProgrammeSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProgrammeSummary();

            if (data.hasOwnProperty('pid')) {
                obj['pid'] = ApiClient.convertToType(data['pid'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProgrammeSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProgrammeSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProgrammeSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['pid'] && !(typeof data['pid'] === 'string' || data['pid'] instanceof String)) {
            throw new Error("Expected the field `pid` to be a primitive type in the JSON string but got " + data['pid']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ProgrammeSummary.RequiredProperties = ["pid", "type"];

/**
 * @member {String} pid
 */
ProgrammeSummary.prototype['pid'] = undefined;

/**
 * @member {module:model/ProgrammeSummary.TypeEnum} type
 */
ProgrammeSummary.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ProgrammeSummary['TypeEnum'] = {

    /**
     * value: "brand_summary"
     * @const
     */
    "brand_summary": "brand_summary",

    /**
     * value: "series_summary"
     * @const
     */
    "series_summary": "series_summary",

    /**
     * value: "episode_summary"
     * @const
     */
    "episode_summary": "episode_summary",

    /**
     * value: "clip_summary"
     * @const
     */
    "clip_summary": "clip_summary"
};



export default ProgrammeSummary;

