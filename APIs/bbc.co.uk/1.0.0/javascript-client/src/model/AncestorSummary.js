/**
 * Radio & Music Services
 * We encapsulate Radio & Music business logic for iPlayer Radio and BBC Music products on all platforms. We add value by reliably providing the right blend of metadata needed by clients.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AncestorTitles from './AncestorTitles';
import Image from './Image';

/**
 * The AncestorSummary model module.
 * @module model/AncestorSummary
 * @version 1.0.0
 */
class AncestorSummary {
    /**
     * Constructs a new <code>AncestorSummary</code>.
     * @alias module:model/AncestorSummary
     * @param ancestorType {String} 
     * @param id {String} 
     * @param images {Array.<module:model/Image>} 
     * @param titles {module:model/AncestorTitles} 
     * @param type {String} 
     */
    constructor(ancestorType, id, images, titles, type) { 
        
        AncestorSummary.initialize(this, ancestorType, id, images, titles, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ancestorType, id, images, titles, type) { 
        obj['ancestor_type'] = ancestorType;
        obj['id'] = id;
        obj['images'] = images;
        obj['titles'] = titles;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>AncestorSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AncestorSummary} obj Optional instance to populate.
     * @return {module:model/AncestorSummary} The populated <code>AncestorSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AncestorSummary();

            if (data.hasOwnProperty('ancestor_type')) {
                obj['ancestor_type'] = ApiClient.convertToType(data['ancestor_type'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('images')) {
                obj['images'] = ApiClient.convertToType(data['images'], [Image]);
            }
            if (data.hasOwnProperty('titles')) {
                obj['titles'] = AncestorTitles.constructFromObject(data['titles']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AncestorSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AncestorSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AncestorSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ancestor_type'] && !(typeof data['ancestor_type'] === 'string' || data['ancestor_type'] instanceof String)) {
            throw new Error("Expected the field `ancestor_type` to be a primitive type in the JSON string but got " + data['ancestor_type']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['images']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['images'])) {
                throw new Error("Expected the field `images` to be an array in the JSON data but got " + data['images']);
            }
            // validate the optional field `images` (array)
            for (const item of data['images']) {
                Image.validateJSON(item);
            };
        }
        // validate the optional field `titles`
        if (data['titles']) { // data not null
          AncestorTitles.validateJSON(data['titles']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

AncestorSummary.RequiredProperties = ["ancestor_type", "id", "images", "titles", "type"];

/**
 * @member {String} ancestor_type
 */
AncestorSummary.prototype['ancestor_type'] = undefined;

/**
 * @member {String} id
 */
AncestorSummary.prototype['id'] = undefined;

/**
 * @member {Array.<module:model/Image>} images
 */
AncestorSummary.prototype['images'] = undefined;

/**
 * @member {module:model/AncestorTitles} titles
 */
AncestorSummary.prototype['titles'] = undefined;

/**
 * @member {String} type
 */
AncestorSummary.prototype['type'] = undefined;






export default AncestorSummary;

