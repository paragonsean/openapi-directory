/**
 * Radio & Music Services
 * We encapsulate Radio & Music business logic for iPlayer Radio and BBC Music products on all platforms. We add value by reliably providing the right blend of metadata needed by clients.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CategorySummary model module.
 * @module model/CategorySummary
 * @version 1.0.0
 */
class CategorySummary {
    /**
     * Constructs a new <code>CategorySummary</code>.
     * @alias module:model/CategorySummary
     * @param categoryType {String} 
     * @param id {String} 
     * @param key {String} 
     * @param title {String} 
     * @param type {String} 
     */
    constructor(categoryType, id, key, title, type) { 
        
        CategorySummary.initialize(this, categoryType, id, key, title, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categoryType, id, key, title, type) { 
        obj['category_type'] = categoryType;
        obj['id'] = id;
        obj['key'] = key;
        obj['title'] = title;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>CategorySummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CategorySummary} obj Optional instance to populate.
     * @return {module:model/CategorySummary} The populated <code>CategorySummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CategorySummary();

            if (data.hasOwnProperty('category_type')) {
                obj['category_type'] = ApiClient.convertToType(data['category_type'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CategorySummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CategorySummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CategorySummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['category_type'] && !(typeof data['category_type'] === 'string' || data['category_type'] instanceof String)) {
            throw new Error("Expected the field `category_type` to be a primitive type in the JSON string but got " + data['category_type']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

CategorySummary.RequiredProperties = ["category_type", "id", "key", "title", "type"];

/**
 * @member {String} category_type
 */
CategorySummary.prototype['category_type'] = undefined;

/**
 * @member {String} id
 */
CategorySummary.prototype['id'] = undefined;

/**
 * @member {String} key
 */
CategorySummary.prototype['key'] = undefined;

/**
 * @member {String} title
 */
CategorySummary.prototype['title'] = undefined;

/**
 * @member {String} type
 */
CategorySummary.prototype['type'] = undefined;






export default CategorySummary;

