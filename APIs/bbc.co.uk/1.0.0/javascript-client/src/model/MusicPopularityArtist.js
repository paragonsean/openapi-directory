/**
 * Radio & Music Services
 * We encapsulate Radio & Music business logic for iPlayer Radio and BBC Music products on all platforms. We add value by reliably providing the right blend of metadata needed by clients.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MusicPopularityArtist model module.
 * @module model/MusicPopularityArtist
 * @version 1.0.0
 */
class MusicPopularityArtist {
    /**
     * Constructs a new <code>MusicPopularityArtist</code>.
     * @alias module:model/MusicPopularityArtist
     * @param gid {String} 
     * @param id {String} 
     * @param imagePid {String} 
     * @param name {String} 
     * @param score {Number} 
     * @param sortName {String} 
     */
    constructor(gid, id, imagePid, name, score, sortName) { 
        
        MusicPopularityArtist.initialize(this, gid, id, imagePid, name, score, sortName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, gid, id, imagePid, name, score, sortName) { 
        obj['gid'] = gid;
        obj['id'] = id;
        obj['imagePid'] = imagePid;
        obj['name'] = name;
        obj['score'] = score;
        obj['sortName'] = sortName;
    }

    /**
     * Constructs a <code>MusicPopularityArtist</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MusicPopularityArtist} obj Optional instance to populate.
     * @return {module:model/MusicPopularityArtist} The populated <code>MusicPopularityArtist</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MusicPopularityArtist();

            if (data.hasOwnProperty('gid')) {
                obj['gid'] = ApiClient.convertToType(data['gid'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('imagePid')) {
                obj['imagePid'] = ApiClient.convertToType(data['imagePid'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'Number');
            }
            if (data.hasOwnProperty('sortName')) {
                obj['sortName'] = ApiClient.convertToType(data['sortName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MusicPopularityArtist</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MusicPopularityArtist</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MusicPopularityArtist.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['gid'] && !(typeof data['gid'] === 'string' || data['gid'] instanceof String)) {
            throw new Error("Expected the field `gid` to be a primitive type in the JSON string but got " + data['gid']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['imagePid'] && !(typeof data['imagePid'] === 'string' || data['imagePid'] instanceof String)) {
            throw new Error("Expected the field `imagePid` to be a primitive type in the JSON string but got " + data['imagePid']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['sortName'] && !(typeof data['sortName'] === 'string' || data['sortName'] instanceof String)) {
            throw new Error("Expected the field `sortName` to be a primitive type in the JSON string but got " + data['sortName']);
        }

        return true;
    }


}

MusicPopularityArtist.RequiredProperties = ["gid", "id", "imagePid", "name", "score", "sortName"];

/**
 * @member {String} gid
 */
MusicPopularityArtist.prototype['gid'] = undefined;

/**
 * @member {String} id
 */
MusicPopularityArtist.prototype['id'] = undefined;

/**
 * @member {String} imagePid
 */
MusicPopularityArtist.prototype['imagePid'] = undefined;

/**
 * @member {String} name
 */
MusicPopularityArtist.prototype['name'] = undefined;

/**
 * @member {Number} score
 */
MusicPopularityArtist.prototype['score'] = undefined;

/**
 * @member {String} sortName
 */
MusicPopularityArtist.prototype['sortName'] = undefined;






export default MusicPopularityArtist;

