/**
 * Radio & Music Services
 * We encapsulate Radio & Music business logic for iPlayer Radio and BBC Music products on all platforms. We add value by reliably providing the right blend of metadata needed by clients.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PersonalisedMusicTrackImage from './PersonalisedMusicTrackImage';

/**
 * The PersonalisedMusicTrackContribution model module.
 * @module model/PersonalisedMusicTrackContribution
 * @version 1.0.0
 */
class PersonalisedMusicTrackContribution {
    /**
     * Constructs a new <code>PersonalisedMusicTrackContribution</code>.
     * @alias module:model/PersonalisedMusicTrackContribution
     * @param id {String} 
     * @param images {Array.<module:model/PersonalisedMusicTrackImage>} 
     * @param name {String} 
     * @param role {String} 
     */
    constructor(id, images, name, role) { 
        
        PersonalisedMusicTrackContribution.initialize(this, id, images, name, role);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, images, name, role) { 
        obj['id'] = id;
        obj['images'] = images;
        obj['name'] = name;
        obj['role'] = role;
    }

    /**
     * Constructs a <code>PersonalisedMusicTrackContribution</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PersonalisedMusicTrackContribution} obj Optional instance to populate.
     * @return {module:model/PersonalisedMusicTrackContribution} The populated <code>PersonalisedMusicTrackContribution</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PersonalisedMusicTrackContribution();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('images')) {
                obj['images'] = ApiClient.convertToType(data['images'], [PersonalisedMusicTrackImage]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PersonalisedMusicTrackContribution</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PersonalisedMusicTrackContribution</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PersonalisedMusicTrackContribution.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['images']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['images'])) {
                throw new Error("Expected the field `images` to be an array in the JSON data but got " + data['images']);
            }
            // validate the optional field `images` (array)
            for (const item of data['images']) {
                PersonalisedMusicTrackImage.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }

        return true;
    }


}

PersonalisedMusicTrackContribution.RequiredProperties = ["id", "images", "name", "role"];

/**
 * @member {String} id
 */
PersonalisedMusicTrackContribution.prototype['id'] = undefined;

/**
 * @member {Array.<module:model/PersonalisedMusicTrackImage>} images
 */
PersonalisedMusicTrackContribution.prototype['images'] = undefined;

/**
 * @member {String} name
 */
PersonalisedMusicTrackContribution.prototype['name'] = undefined;

/**
 * @member {String} role
 */
PersonalisedMusicTrackContribution.prototype['role'] = undefined;






export default PersonalisedMusicTrackContribution;

