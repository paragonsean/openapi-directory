/**
 * Akeneo PIM REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIPAMAssetReferenceFileApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIPAMAssetReferenceFileApi::OAIPAMAssetReferenceFileApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIPAMAssetReferenceFileApi::~OAIPAMAssetReferenceFileApi() {
}

void OAIPAMAssetReferenceFileApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("http://demo.akeneo.com/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("getReferenceFilesChannelCodeLocaleCodeDownload", defaultConf);
    _serverIndices.insert("getReferenceFilesChannelCodeLocaleCodeDownload", 0);
    _serverConfigs.insert("getReferenceFilesLocaleCode", defaultConf);
    _serverIndices.insert("getReferenceFilesLocaleCode", 0);
    _serverConfigs.insert("postReferenceFilesLocaleCode", defaultConf);
    _serverIndices.insert("postReferenceFilesLocaleCode", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIPAMAssetReferenceFileApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIPAMAssetReferenceFileApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIPAMAssetReferenceFileApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIPAMAssetReferenceFileApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIPAMAssetReferenceFileApi::setUsername(const QString &username) {
    _username = username;
}

void OAIPAMAssetReferenceFileApi::setPassword(const QString &password) {
    _password = password;
}


void OAIPAMAssetReferenceFileApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIPAMAssetReferenceFileApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIPAMAssetReferenceFileApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIPAMAssetReferenceFileApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIPAMAssetReferenceFileApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIPAMAssetReferenceFileApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIPAMAssetReferenceFileApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIPAMAssetReferenceFileApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIPAMAssetReferenceFileApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIPAMAssetReferenceFileApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIPAMAssetReferenceFileApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIPAMAssetReferenceFileApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIPAMAssetReferenceFileApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIPAMAssetReferenceFileApi::getReferenceFilesChannelCodeLocaleCodeDownload(const QString &asset_code, const QString &locale_code) {
    QString fullPath = QString(_serverConfigs["getReferenceFilesChannelCodeLocaleCodeDownload"][_serverIndices.value("getReferenceFilesChannelCodeLocaleCodeDownload")].URL()+"/api/rest/v1/assets/{asset_code}/reference-files/{locale_code}/download");
    
    
    {
        QString asset_codePathParam("{");
        asset_codePathParam.append("asset_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "asset_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"asset_code"+pathSuffix : pathPrefix;
        fullPath.replace(asset_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(asset_code)));
    }
    
    {
        QString locale_codePathParam("{");
        locale_codePathParam.append("locale_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "locale_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"locale_code"+pathSuffix : pathPrefix;
        fullPath.replace(locale_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(locale_code)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPAMAssetReferenceFileApi::getReferenceFilesChannelCodeLocaleCodeDownloadCallback);
    connect(this, &OAIPAMAssetReferenceFileApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPAMAssetReferenceFileApi::getReferenceFilesChannelCodeLocaleCodeDownloadCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getReferenceFilesChannelCodeLocaleCodeDownloadSignal();
        Q_EMIT getReferenceFilesChannelCodeLocaleCodeDownloadSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getReferenceFilesChannelCodeLocaleCodeDownloadSignalE(error_type, error_str);
        Q_EMIT getReferenceFilesChannelCodeLocaleCodeDownloadSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getReferenceFilesChannelCodeLocaleCodeDownloadSignalError(error_type, error_str);
        Q_EMIT getReferenceFilesChannelCodeLocaleCodeDownloadSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPAMAssetReferenceFileApi::getReferenceFilesLocaleCode(const QString &asset_code, const QString &locale_code) {
    QString fullPath = QString(_serverConfigs["getReferenceFilesLocaleCode"][_serverIndices.value("getReferenceFilesLocaleCode")].URL()+"/api/rest/v1/assets/{asset_code}/reference-files/{locale_code}");
    
    
    {
        QString asset_codePathParam("{");
        asset_codePathParam.append("asset_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "asset_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"asset_code"+pathSuffix : pathPrefix;
        fullPath.replace(asset_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(asset_code)));
    }
    
    {
        QString locale_codePathParam("{");
        locale_codePathParam.append("locale_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "locale_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"locale_code"+pathSuffix : pathPrefix;
        fullPath.replace(locale_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(locale_code)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPAMAssetReferenceFileApi::getReferenceFilesLocaleCodeCallback);
    connect(this, &OAIPAMAssetReferenceFileApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPAMAssetReferenceFileApi::getReferenceFilesLocaleCodeCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIGet_reference_files__locale_code__200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getReferenceFilesLocaleCodeSignal(output);
        Q_EMIT getReferenceFilesLocaleCodeSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getReferenceFilesLocaleCodeSignalE(output, error_type, error_str);
        Q_EMIT getReferenceFilesLocaleCodeSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getReferenceFilesLocaleCodeSignalError(output, error_type, error_str);
        Q_EMIT getReferenceFilesLocaleCodeSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPAMAssetReferenceFileApi::postReferenceFilesLocaleCode(const QString &asset_code, const QString &locale_code, const QString &content_type, const ::OpenAPI::OptionalParam<OAIPost_reference_files__locale_code__request> &body) {
    QString fullPath = QString(_serverConfigs["postReferenceFilesLocaleCode"][_serverIndices.value("postReferenceFilesLocaleCode")].URL()+"/api/rest/v1/assets/{asset_code}/reference-files/{locale_code}");
    
    
    {
        QString asset_codePathParam("{");
        asset_codePathParam.append("asset_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "asset_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"asset_code"+pathSuffix : pathPrefix;
        fullPath.replace(asset_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(asset_code)));
    }
    
    {
        QString locale_codePathParam("{");
        locale_codePathParam.append("locale_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "locale_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"locale_code"+pathSuffix : pathPrefix;
        fullPath.replace(locale_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(locale_code)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (body.hasValue()){

        
        QByteArray output = body.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    
    {
        if (!::OpenAPI::toStringValue(content_type).isEmpty()) {
            input.headers.insert("Content-type", ::OpenAPI::toStringValue(content_type));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPAMAssetReferenceFileApi::postReferenceFilesLocaleCodeCallback);
    connect(this, &OAIPAMAssetReferenceFileApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPAMAssetReferenceFileApi::postReferenceFilesLocaleCodeCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIPost_reference_files__locale_code__201_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postReferenceFilesLocaleCodeSignal(output);
        Q_EMIT postReferenceFilesLocaleCodeSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT postReferenceFilesLocaleCodeSignalE(output, error_type, error_str);
        Q_EMIT postReferenceFilesLocaleCodeSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT postReferenceFilesLocaleCodeSignalError(output, error_type, error_str);
        Q_EMIT postReferenceFilesLocaleCodeSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPAMAssetReferenceFileApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
