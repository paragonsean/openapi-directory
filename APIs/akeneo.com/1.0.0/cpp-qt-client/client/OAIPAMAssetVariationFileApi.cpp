/**
 * Akeneo PIM REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIPAMAssetVariationFileApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIPAMAssetVariationFileApi::OAIPAMAssetVariationFileApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIPAMAssetVariationFileApi::~OAIPAMAssetVariationFileApi() {
}

void OAIPAMAssetVariationFileApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("http://demo.akeneo.com/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("getVariationFilesChannelCodeLocaleCode", defaultConf);
    _serverIndices.insert("getVariationFilesChannelCodeLocaleCode", 0);
    _serverConfigs.insert("getVariationFilesChannelCodeLocaleCodeDownload", defaultConf);
    _serverIndices.insert("getVariationFilesChannelCodeLocaleCodeDownload", 0);
    _serverConfigs.insert("postVariationFilesChannelCodeLocaleCode", defaultConf);
    _serverIndices.insert("postVariationFilesChannelCodeLocaleCode", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAIPAMAssetVariationFileApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAIPAMAssetVariationFileApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIPAMAssetVariationFileApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIPAMAssetVariationFileApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIPAMAssetVariationFileApi::setUsername(const QString &username) {
    _username = username;
}

void OAIPAMAssetVariationFileApi::setPassword(const QString &password) {
    _password = password;
}


void OAIPAMAssetVariationFileApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIPAMAssetVariationFileApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIPAMAssetVariationFileApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAIPAMAssetVariationFileApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIPAMAssetVariationFileApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAIPAMAssetVariationFileApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIPAMAssetVariationFileApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIPAMAssetVariationFileApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIPAMAssetVariationFileApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIPAMAssetVariationFileApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIPAMAssetVariationFileApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIPAMAssetVariationFileApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIPAMAssetVariationFileApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIPAMAssetVariationFileApi::getVariationFilesChannelCodeLocaleCode(const QString &asset_code, const QString &channel_code, const QString &locale_code) {
    QString fullPath = QString(_serverConfigs["getVariationFilesChannelCodeLocaleCode"][_serverIndices.value("getVariationFilesChannelCodeLocaleCode")].URL()+"/api/rest/v1/assets/{asset_code}/variation-files/{channel_code}/{locale_code}");
    
    
    {
        QString asset_codePathParam("{");
        asset_codePathParam.append("asset_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "asset_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"asset_code"+pathSuffix : pathPrefix;
        fullPath.replace(asset_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(asset_code)));
    }
    
    {
        QString channel_codePathParam("{");
        channel_codePathParam.append("channel_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "channel_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"channel_code"+pathSuffix : pathPrefix;
        fullPath.replace(channel_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(channel_code)));
    }
    
    {
        QString locale_codePathParam("{");
        locale_codePathParam.append("locale_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "locale_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"locale_code"+pathSuffix : pathPrefix;
        fullPath.replace(locale_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(locale_code)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPAMAssetVariationFileApi::getVariationFilesChannelCodeLocaleCodeCallback);
    connect(this, &OAIPAMAssetVariationFileApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPAMAssetVariationFileApi::getVariationFilesChannelCodeLocaleCodeCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIGet_variation_files__channel_code__locale_code_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getVariationFilesChannelCodeLocaleCodeSignal(output);
        Q_EMIT getVariationFilesChannelCodeLocaleCodeSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getVariationFilesChannelCodeLocaleCodeSignalE(output, error_type, error_str);
        Q_EMIT getVariationFilesChannelCodeLocaleCodeSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getVariationFilesChannelCodeLocaleCodeSignalError(output, error_type, error_str);
        Q_EMIT getVariationFilesChannelCodeLocaleCodeSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPAMAssetVariationFileApi::getVariationFilesChannelCodeLocaleCodeDownload(const QString &asset_code, const QString &channel_code, const QString &locale_code) {
    QString fullPath = QString(_serverConfigs["getVariationFilesChannelCodeLocaleCodeDownload"][_serverIndices.value("getVariationFilesChannelCodeLocaleCodeDownload")].URL()+"/api/rest/v1/assets/{asset_code}/variation-files/{channel_code}/{locale_code}/download");
    
    
    {
        QString asset_codePathParam("{");
        asset_codePathParam.append("asset_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "asset_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"asset_code"+pathSuffix : pathPrefix;
        fullPath.replace(asset_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(asset_code)));
    }
    
    {
        QString channel_codePathParam("{");
        channel_codePathParam.append("channel_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "channel_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"channel_code"+pathSuffix : pathPrefix;
        fullPath.replace(channel_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(channel_code)));
    }
    
    {
        QString locale_codePathParam("{");
        locale_codePathParam.append("locale_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "locale_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"locale_code"+pathSuffix : pathPrefix;
        fullPath.replace(locale_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(locale_code)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPAMAssetVariationFileApi::getVariationFilesChannelCodeLocaleCodeDownloadCallback);
    connect(this, &OAIPAMAssetVariationFileApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPAMAssetVariationFileApi::getVariationFilesChannelCodeLocaleCodeDownloadCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getVariationFilesChannelCodeLocaleCodeDownloadSignal();
        Q_EMIT getVariationFilesChannelCodeLocaleCodeDownloadSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getVariationFilesChannelCodeLocaleCodeDownloadSignalE(error_type, error_str);
        Q_EMIT getVariationFilesChannelCodeLocaleCodeDownloadSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getVariationFilesChannelCodeLocaleCodeDownloadSignalError(error_type, error_str);
        Q_EMIT getVariationFilesChannelCodeLocaleCodeDownloadSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPAMAssetVariationFileApi::postVariationFilesChannelCodeLocaleCode(const QString &asset_code, const QString &channel_code, const QString &locale_code, const QString &content_type, const ::OpenAPI::OptionalParam<OAIPost_reference_files__locale_code__request> &body) {
    QString fullPath = QString(_serverConfigs["postVariationFilesChannelCodeLocaleCode"][_serverIndices.value("postVariationFilesChannelCodeLocaleCode")].URL()+"/api/rest/v1/assets/{asset_code}/variation-files/{channel_code}/{locale_code}");
    
    
    {
        QString asset_codePathParam("{");
        asset_codePathParam.append("asset_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "asset_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"asset_code"+pathSuffix : pathPrefix;
        fullPath.replace(asset_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(asset_code)));
    }
    
    {
        QString channel_codePathParam("{");
        channel_codePathParam.append("channel_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "channel_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"channel_code"+pathSuffix : pathPrefix;
        fullPath.replace(channel_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(channel_code)));
    }
    
    {
        QString locale_codePathParam("{");
        locale_codePathParam.append("locale_code").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "locale_code", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"locale_code"+pathSuffix : pathPrefix;
        fullPath.replace(locale_codePathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(locale_code)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (body.hasValue()){

        
        QByteArray output = body.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    
    {
        if (!::OpenAPI::toStringValue(content_type).isEmpty()) {
            input.headers.insert("Content-type", ::OpenAPI::toStringValue(content_type));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPAMAssetVariationFileApi::postVariationFilesChannelCodeLocaleCodeCallback);
    connect(this, &OAIPAMAssetVariationFileApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPAMAssetVariationFileApi::postVariationFilesChannelCodeLocaleCodeCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postVariationFilesChannelCodeLocaleCodeSignal();
        Q_EMIT postVariationFilesChannelCodeLocaleCodeSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT postVariationFilesChannelCodeLocaleCodeSignalE(error_type, error_str);
        Q_EMIT postVariationFilesChannelCodeLocaleCodeSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT postVariationFilesChannelCodeLocaleCodeSignalError(error_type, error_str);
        Q_EMIT postVariationFilesChannelCodeLocaleCodeSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPAMAssetVariationFileApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
