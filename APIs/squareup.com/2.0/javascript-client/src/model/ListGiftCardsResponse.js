/**
 * Square Connect API
 * Client library for accessing the Square Connect APIs
 *
 * The version of the OpenAPI document: 2.0
 * Contact: developers@squareup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Error from './Error';
import GiftCard from './GiftCard';

/**
 * The ListGiftCardsResponse model module.
 * @module model/ListGiftCardsResponse
 * @version 2.0
 */
class ListGiftCardsResponse {
    /**
     * Constructs a new <code>ListGiftCardsResponse</code>.
     * A response that contains one or more &#x60;GiftCard&#x60;. The response might contain a set of &#x60;Error&#x60; objects if the request resulted in errors.
     * @alias module:model/ListGiftCardsResponse
     */
    constructor() { 
        
        ListGiftCardsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListGiftCardsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListGiftCardsResponse} obj Optional instance to populate.
     * @return {module:model/ListGiftCardsResponse} The populated <code>ListGiftCardsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListGiftCardsResponse();

            if (data.hasOwnProperty('cursor')) {
                obj['cursor'] = ApiClient.convertToType(data['cursor'], 'String');
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [Error]);
            }
            if (data.hasOwnProperty('gift_cards')) {
                obj['gift_cards'] = ApiClient.convertToType(data['gift_cards'], [GiftCard]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListGiftCardsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListGiftCardsResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['cursor'] && !(typeof data['cursor'] === 'string' || data['cursor'] instanceof String)) {
            throw new Error("Expected the field `cursor` to be a primitive type in the JSON string but got " + data['cursor']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                Error.validateJSON(item);
            };
        }
        if (data['gift_cards']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['gift_cards'])) {
                throw new Error("Expected the field `gift_cards` to be an array in the JSON data but got " + data['gift_cards']);
            }
            // validate the optional field `gift_cards` (array)
            for (const item of data['gift_cards']) {
                GiftCard.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * When a response is truncated, it includes a cursor that you can use in a subsequent request to fetch the next set of gift cards. If empty, this is the final response.
 * @member {String} cursor
 */
ListGiftCardsResponse.prototype['cursor'] = undefined;

/**
 * Any errors that occurred during the request.
 * @member {Array.<module:model/Error>} errors
 */
ListGiftCardsResponse.prototype['errors'] = undefined;

/**
 * Gift cards retrieved.
 * @member {Array.<module:model/GiftCard>} gift_cards
 */
ListGiftCardsResponse.prototype['gift_cards'] = undefined;






export default ListGiftCardsResponse;

