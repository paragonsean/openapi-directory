/**
 * Square Connect API
 * Client library for accessing the Square Connect APIs
 *
 * The version of the OpenAPI document: 2.0
 * Contact: developers@squareup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Money from './Money';

/**
 * The GiftCardActivityRefund model module.
 * @module model/GiftCardActivityRefund
 * @version 2.0
 */
class GiftCardActivityRefund {
    /**
     * Constructs a new <code>GiftCardActivityRefund</code>.
     * Present only when &#x60;GiftCardActivityType&#x60; is REFUND.
     * @alias module:model/GiftCardActivityRefund
     * @param redeemActivityId {String} The ID for the Redeem activity that needs to be refunded. Hence, the activity it refers to has to be of the REDEEM type.
     */
    constructor(redeemActivityId) { 
        
        GiftCardActivityRefund.initialize(this, redeemActivityId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, redeemActivityId) { 
        obj['redeem_activity_id'] = redeemActivityId;
    }

    /**
     * Constructs a <code>GiftCardActivityRefund</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GiftCardActivityRefund} obj Optional instance to populate.
     * @return {module:model/GiftCardActivityRefund} The populated <code>GiftCardActivityRefund</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GiftCardActivityRefund();

            if (data.hasOwnProperty('amount_money')) {
                obj['amount_money'] = Money.constructFromObject(data['amount_money']);
            }
            if (data.hasOwnProperty('payment_id')) {
                obj['payment_id'] = ApiClient.convertToType(data['payment_id'], 'String');
            }
            if (data.hasOwnProperty('redeem_activity_id')) {
                obj['redeem_activity_id'] = ApiClient.convertToType(data['redeem_activity_id'], 'String');
            }
            if (data.hasOwnProperty('reference_id')) {
                obj['reference_id'] = ApiClient.convertToType(data['reference_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GiftCardActivityRefund</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GiftCardActivityRefund</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GiftCardActivityRefund.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `amount_money`
        if (data['amount_money']) { // data not null
          Money.validateJSON(data['amount_money']);
        }
        // ensure the json data is a string
        if (data['payment_id'] && !(typeof data['payment_id'] === 'string' || data['payment_id'] instanceof String)) {
            throw new Error("Expected the field `payment_id` to be a primitive type in the JSON string but got " + data['payment_id']);
        }
        // ensure the json data is a string
        if (data['redeem_activity_id'] && !(typeof data['redeem_activity_id'] === 'string' || data['redeem_activity_id'] instanceof String)) {
            throw new Error("Expected the field `redeem_activity_id` to be a primitive type in the JSON string but got " + data['redeem_activity_id']);
        }
        // ensure the json data is a string
        if (data['reference_id'] && !(typeof data['reference_id'] === 'string' || data['reference_id'] instanceof String)) {
            throw new Error("Expected the field `reference_id` to be a primitive type in the JSON string but got " + data['reference_id']);
        }

        return true;
    }


}

GiftCardActivityRefund.RequiredProperties = ["redeem_activity_id"];

/**
 * @member {module:model/Money} amount_money
 */
GiftCardActivityRefund.prototype['amount_money'] = undefined;

/**
 * When the Square Payments API is used, Refund is not called on the Gift Cards API. However, when Square reads a Refund activity from the Gift Cards API, the developer needs to know the ID of the payment (made using this gift card) that is being refunded.
 * @member {String} payment_id
 */
GiftCardActivityRefund.prototype['payment_id'] = undefined;

/**
 * The ID for the Redeem activity that needs to be refunded. Hence, the activity it refers to has to be of the REDEEM type.
 * @member {String} redeem_activity_id
 */
GiftCardActivityRefund.prototype['redeem_activity_id'] = undefined;

/**
 * A client-specified ID to associate an entity, in another system, with this gift card activity. This can be used to track the order or payment related information when the Square Orders API is not being used.
 * @member {String} reference_id
 */
GiftCardActivityRefund.prototype['reference_id'] = undefined;






export default GiftCardActivityRefund;

