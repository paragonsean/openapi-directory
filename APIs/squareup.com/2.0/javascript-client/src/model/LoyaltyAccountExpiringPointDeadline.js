/**
 * Square Connect API
 * Client library for accessing the Square Connect APIs
 *
 * The version of the OpenAPI document: 2.0
 * Contact: developers@squareup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The LoyaltyAccountExpiringPointDeadline model module.
 * @module model/LoyaltyAccountExpiringPointDeadline
 * @version 2.0
 */
class LoyaltyAccountExpiringPointDeadline {
    /**
     * Constructs a new <code>LoyaltyAccountExpiringPointDeadline</code>.
     * Represents a set of points for a loyalty account that are scheduled to expire on a specific date.
     * @alias module:model/LoyaltyAccountExpiringPointDeadline
     * @param expiresAt {String} The timestamp of when the points are scheduled to expire, in RFC 3339 format.
     * @param points {Number} The number of points scheduled to expire at the `expires_at` timestamp.
     */
    constructor(expiresAt, points) { 
        
        LoyaltyAccountExpiringPointDeadline.initialize(this, expiresAt, points);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, expiresAt, points) { 
        obj['expires_at'] = expiresAt;
        obj['points'] = points;
    }

    /**
     * Constructs a <code>LoyaltyAccountExpiringPointDeadline</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LoyaltyAccountExpiringPointDeadline} obj Optional instance to populate.
     * @return {module:model/LoyaltyAccountExpiringPointDeadline} The populated <code>LoyaltyAccountExpiringPointDeadline</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LoyaltyAccountExpiringPointDeadline();

            if (data.hasOwnProperty('expires_at')) {
                obj['expires_at'] = ApiClient.convertToType(data['expires_at'], 'String');
            }
            if (data.hasOwnProperty('points')) {
                obj['points'] = ApiClient.convertToType(data['points'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LoyaltyAccountExpiringPointDeadline</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LoyaltyAccountExpiringPointDeadline</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LoyaltyAccountExpiringPointDeadline.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['expires_at'] && !(typeof data['expires_at'] === 'string' || data['expires_at'] instanceof String)) {
            throw new Error("Expected the field `expires_at` to be a primitive type in the JSON string but got " + data['expires_at']);
        }

        return true;
    }


}

LoyaltyAccountExpiringPointDeadline.RequiredProperties = ["expires_at", "points"];

/**
 * The timestamp of when the points are scheduled to expire, in RFC 3339 format.
 * @member {String} expires_at
 */
LoyaltyAccountExpiringPointDeadline.prototype['expires_at'] = undefined;

/**
 * The number of points scheduled to expire at the `expires_at` timestamp.
 * @member {Number} points
 */
LoyaltyAccountExpiringPointDeadline.prototype['points'] = undefined;






export default LoyaltyAccountExpiringPointDeadline;

