/**
 * Square Connect API
 * Client library for accessing the Square Connect APIs
 *
 * The version of the OpenAPI document: 2.0
 * Contact: developers@squareup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The LoyaltyEventRedeemReward model module.
 * @module model/LoyaltyEventRedeemReward
 * @version 2.0
 */
class LoyaltyEventRedeemReward {
    /**
     * Constructs a new <code>LoyaltyEventRedeemReward</code>.
     * Provides metadata when the event &#x60;type&#x60; is &#x60;REDEEM_REWARD&#x60;.
     * @alias module:model/LoyaltyEventRedeemReward
     * @param loyaltyProgramId {String} The ID of the [loyalty program](https://developer.squareup.com/reference/square_2021-08-18/objects/LoyaltyProgram).
     */
    constructor(loyaltyProgramId) { 
        
        LoyaltyEventRedeemReward.initialize(this, loyaltyProgramId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, loyaltyProgramId) { 
        obj['loyalty_program_id'] = loyaltyProgramId;
    }

    /**
     * Constructs a <code>LoyaltyEventRedeemReward</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LoyaltyEventRedeemReward} obj Optional instance to populate.
     * @return {module:model/LoyaltyEventRedeemReward} The populated <code>LoyaltyEventRedeemReward</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LoyaltyEventRedeemReward();

            if (data.hasOwnProperty('loyalty_program_id')) {
                obj['loyalty_program_id'] = ApiClient.convertToType(data['loyalty_program_id'], 'String');
            }
            if (data.hasOwnProperty('order_id')) {
                obj['order_id'] = ApiClient.convertToType(data['order_id'], 'String');
            }
            if (data.hasOwnProperty('reward_id')) {
                obj['reward_id'] = ApiClient.convertToType(data['reward_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LoyaltyEventRedeemReward</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LoyaltyEventRedeemReward</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LoyaltyEventRedeemReward.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['loyalty_program_id'] && !(typeof data['loyalty_program_id'] === 'string' || data['loyalty_program_id'] instanceof String)) {
            throw new Error("Expected the field `loyalty_program_id` to be a primitive type in the JSON string but got " + data['loyalty_program_id']);
        }
        // ensure the json data is a string
        if (data['order_id'] && !(typeof data['order_id'] === 'string' || data['order_id'] instanceof String)) {
            throw new Error("Expected the field `order_id` to be a primitive type in the JSON string but got " + data['order_id']);
        }
        // ensure the json data is a string
        if (data['reward_id'] && !(typeof data['reward_id'] === 'string' || data['reward_id'] instanceof String)) {
            throw new Error("Expected the field `reward_id` to be a primitive type in the JSON string but got " + data['reward_id']);
        }

        return true;
    }


}

LoyaltyEventRedeemReward.RequiredProperties = ["loyalty_program_id"];

/**
 * The ID of the [loyalty program](https://developer.squareup.com/reference/square_2021-08-18/objects/LoyaltyProgram).
 * @member {String} loyalty_program_id
 */
LoyaltyEventRedeemReward.prototype['loyalty_program_id'] = undefined;

/**
 * The ID of the [order](https://developer.squareup.com/reference/square_2021-08-18/objects/Order) that redeemed the reward. This field is returned only if the Orders API is used to process orders.
 * @member {String} order_id
 */
LoyaltyEventRedeemReward.prototype['order_id'] = undefined;

/**
 * The ID of the redeemed [loyalty reward](https://developer.squareup.com/reference/square_2021-08-18/objects/LoyaltyReward). This field is returned only if the event source is `LOYALTY_API`.
 * @member {String} reward_id
 */
LoyaltyEventRedeemReward.prototype['reward_id'] = undefined;






export default LoyaltyEventRedeemReward;

