/**
 * Square Connect API
 * Client library for accessing the Square Connect APIs
 *
 * The version of the OpenAPI document: 2.0
 * Contact: developers@squareup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Error from './Error';
import Shift from './Shift';

/**
 * The SearchShiftsResponse model module.
 * @module model/SearchShiftsResponse
 * @version 2.0
 */
class SearchShiftsResponse {
    /**
     * Constructs a new <code>SearchShiftsResponse</code>.
     * The response to a request for &#x60;Shift&#x60; objects. The response contains the requested &#x60;Shift&#x60; objects and might contain a set of &#x60;Error&#x60; objects if the request resulted in errors.
     * @alias module:model/SearchShiftsResponse
     */
    constructor() { 
        
        SearchShiftsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SearchShiftsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SearchShiftsResponse} obj Optional instance to populate.
     * @return {module:model/SearchShiftsResponse} The populated <code>SearchShiftsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SearchShiftsResponse();

            if (data.hasOwnProperty('cursor')) {
                obj['cursor'] = ApiClient.convertToType(data['cursor'], 'String');
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [Error]);
            }
            if (data.hasOwnProperty('shifts')) {
                obj['shifts'] = ApiClient.convertToType(data['shifts'], [Shift]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SearchShiftsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SearchShiftsResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['cursor'] && !(typeof data['cursor'] === 'string' || data['cursor'] instanceof String)) {
            throw new Error("Expected the field `cursor` to be a primitive type in the JSON string but got " + data['cursor']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                Error.validateJSON(item);
            };
        }
        if (data['shifts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['shifts'])) {
                throw new Error("Expected the field `shifts` to be an array in the JSON data but got " + data['shifts']);
            }
            // validate the optional field `shifts` (array)
            for (const item of data['shifts']) {
                Shift.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * An opaque cursor for fetching the next page.
 * @member {String} cursor
 */
SearchShiftsResponse.prototype['cursor'] = undefined;

/**
 * Any errors that occurred during the request.
 * @member {Array.<module:model/Error>} errors
 */
SearchShiftsResponse.prototype['errors'] = undefined;

/**
 * Shifts.
 * @member {Array.<module:model/Shift>} shifts
 */
SearchShiftsResponse.prototype['shifts'] = undefined;






export default SearchShiftsResponse;

