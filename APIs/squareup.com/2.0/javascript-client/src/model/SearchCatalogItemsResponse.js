/**
 * Square Connect API
 * Client library for accessing the Square Connect APIs
 *
 * The version of the OpenAPI document: 2.0
 * Contact: developers@squareup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CatalogObject from './CatalogObject';
import Error from './Error';

/**
 * The SearchCatalogItemsResponse model module.
 * @module model/SearchCatalogItemsResponse
 * @version 2.0
 */
class SearchCatalogItemsResponse {
    /**
     * Constructs a new <code>SearchCatalogItemsResponse</code>.
     * Defines the response body returned from the [SearchCatalogItems](https://developer.squareup.com/reference/square_2021-08-18/catalog-api/search-catalog-items) endpoint.
     * @alias module:model/SearchCatalogItemsResponse
     */
    constructor() { 
        
        SearchCatalogItemsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SearchCatalogItemsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SearchCatalogItemsResponse} obj Optional instance to populate.
     * @return {module:model/SearchCatalogItemsResponse} The populated <code>SearchCatalogItemsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SearchCatalogItemsResponse();

            if (data.hasOwnProperty('cursor')) {
                obj['cursor'] = ApiClient.convertToType(data['cursor'], 'String');
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [Error]);
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [CatalogObject]);
            }
            if (data.hasOwnProperty('matched_variation_ids')) {
                obj['matched_variation_ids'] = ApiClient.convertToType(data['matched_variation_ids'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SearchCatalogItemsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SearchCatalogItemsResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['cursor'] && !(typeof data['cursor'] === 'string' || data['cursor'] instanceof String)) {
            throw new Error("Expected the field `cursor` to be a primitive type in the JSON string but got " + data['cursor']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                Error.validateJSON(item);
            };
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                CatalogObject.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['matched_variation_ids'])) {
            throw new Error("Expected the field `matched_variation_ids` to be an array in the JSON data but got " + data['matched_variation_ids']);
        }

        return true;
    }


}



/**
 * Pagination token used in the next request to return more of the search result.
 * @member {String} cursor
 */
SearchCatalogItemsResponse.prototype['cursor'] = undefined;

/**
 * Any errors that occurred during the request.
 * @member {Array.<module:model/Error>} errors
 */
SearchCatalogItemsResponse.prototype['errors'] = undefined;

/**
 * Returned items matching the specified query expressions.
 * @member {Array.<module:model/CatalogObject>} items
 */
SearchCatalogItemsResponse.prototype['items'] = undefined;

/**
 * Ids of returned item variations matching the specified query expression.
 * @member {Array.<String>} matched_variation_ids
 */
SearchCatalogItemsResponse.prototype['matched_variation_ids'] = undefined;






export default SearchCatalogItemsResponse;

