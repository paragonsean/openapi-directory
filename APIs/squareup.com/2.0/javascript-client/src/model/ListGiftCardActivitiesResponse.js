/**
 * Square Connect API
 * Client library for accessing the Square Connect APIs
 *
 * The version of the OpenAPI document: 2.0
 * Contact: developers@squareup.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Error from './Error';
import GiftCardActivity from './GiftCardActivity';

/**
 * The ListGiftCardActivitiesResponse model module.
 * @module model/ListGiftCardActivitiesResponse
 * @version 2.0
 */
class ListGiftCardActivitiesResponse {
    /**
     * Constructs a new <code>ListGiftCardActivitiesResponse</code>.
     * A response that contains one or more &#x60;GiftCardActivity&#x60;. The response might contain a set of &#x60;Error&#x60; objects if the request resulted in errors.
     * @alias module:model/ListGiftCardActivitiesResponse
     */
    constructor() { 
        
        ListGiftCardActivitiesResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListGiftCardActivitiesResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListGiftCardActivitiesResponse} obj Optional instance to populate.
     * @return {module:model/ListGiftCardActivitiesResponse} The populated <code>ListGiftCardActivitiesResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListGiftCardActivitiesResponse();

            if (data.hasOwnProperty('cursor')) {
                obj['cursor'] = ApiClient.convertToType(data['cursor'], 'String');
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [Error]);
            }
            if (data.hasOwnProperty('gift_card_activities')) {
                obj['gift_card_activities'] = ApiClient.convertToType(data['gift_card_activities'], [GiftCardActivity]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListGiftCardActivitiesResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListGiftCardActivitiesResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['cursor'] && !(typeof data['cursor'] === 'string' || data['cursor'] instanceof String)) {
            throw new Error("Expected the field `cursor` to be a primitive type in the JSON string but got " + data['cursor']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                Error.validateJSON(item);
            };
        }
        if (data['gift_card_activities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['gift_card_activities'])) {
                throw new Error("Expected the field `gift_card_activities` to be an array in the JSON data but got " + data['gift_card_activities']);
            }
            // validate the optional field `gift_card_activities` (array)
            for (const item of data['gift_card_activities']) {
                GiftCardActivity.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * When a response is truncated, it includes a cursor that you can use in a subsequent request to fetch the next set of activities. If empty, this is the final response.
 * @member {String} cursor
 */
ListGiftCardActivitiesResponse.prototype['cursor'] = undefined;

/**
 * Any errors that occurred during the request.
 * @member {Array.<module:model/Error>} errors
 */
ListGiftCardActivitiesResponse.prototype['errors'] = undefined;

/**
 * Gift card activities retrieved.
 * @member {Array.<module:model/GiftCardActivity>} gift_card_activities
 */
ListGiftCardActivitiesResponse.prototype['gift_card_activities'] = undefined;






export default ListGiftCardActivitiesResponse;

