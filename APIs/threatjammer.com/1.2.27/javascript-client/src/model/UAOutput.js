/**
 * ThreatJammer.com User API
 *  The public API open to the users. [Read the docs and learn more.](https://threatjammer.com/docs).  ## General information  ### Description Threat Jammer supports two end-user REST APIs: the User API and the Report API. The end-user uses the User API to interact with the different databases, heuristics, and machine learning processes. Devices use the Report API to interact with Threat Jammer. This document will explain how to use the User API and interact with the different services, create a token, interpret the quota information, and create the HTTP request to interact with the User API.  ### Authentication The API is protected by a **Bearer authentication** schema. **Bearer authentication** (also called **token authentication**) is an HTTP authentication scheme that involves security tokens called bearer tokens. It is used to authenticate the user. All the different endpoints expect a `Bearer` token in the `Authorization` header.  Example:  ``` curl -X 'GET'   'https://dublin.api.threatjammer.com/test'   -H 'accept: application/json'   -H 'Authorization: Bearer YOUR_API_KEY' ```  You can obtain a token after registering on the [ThreatJammer.com](https://threatjammer.com) website for free.   ### Region specific tokens All the `Bearer` tokens contain information about the authorized region. The developers have to use a token created for the region they want to use. A token used in a different region will return a `401 Unauthorized` error.  ### Global errors  The API will return the following permanent errors: - a `401 Unauthorized` error if the token is not valid, or does not belong to the region. - a `401 Unauthorized` error if the token does not exist. - a `401 Unauthorized` error if the token is malformed. - a `403 Forbidden` error if the subscription level is not enough. Some endpoints are only available for paid subscription levels.  And these temporary errors: - a `429 Too Many Requests` error if the quota is exceeded (see below).  ### Quota limits  **Every request to the User API will consume one (1) quota point.**  The API has two rate limiting processes: - a quota limit of **5000** requests per month for the `FREE` account. The limit is reset every month. - a quota limit of **10** requests per minute for the `FREE` account. The limit is reset every minute and implements a sliding window mechanism.  
 *
 * The version of the OpenAPI document: 1.2.27
 * Contact: support@threatjammer.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UAOutput model module.
 * @module model/UAOutput
 * @version 1.2.27
 */
class UAOutput {
    /**
     * Constructs a new <code>UAOutput</code>.
     * @alias module:model/UAOutput
     * @param agent {String} 
     * @param classification {module:model/UAOutput.ClassificationEnum} 
     * @param device {String} 
     * @param engine {String} 
     * @param family {String} 
     * @param frequent {module:model/UAOutput.FrequentEnum} 
     * @param latest {String} 
     * @param os {String} 
     * @param string {String} 
     * @param type {String} 
     * @param vendor {String} 
     * @param version {String} 
     */
    constructor(agent, classification, device, engine, family, frequent, latest, os, string, type, vendor, version) { 
        
        UAOutput.initialize(this, agent, classification, device, engine, family, frequent, latest, os, string, type, vendor, version);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, agent, classification, device, engine, family, frequent, latest, os, string, type, vendor, version) { 
        obj['agent'] = agent;
        obj['classification'] = classification;
        obj['device'] = device;
        obj['engine'] = engine;
        obj['family'] = family;
        obj['frequent'] = frequent;
        obj['latest'] = latest;
        obj['os'] = os;
        obj['string'] = string;
        obj['type'] = type;
        obj['vendor'] = vendor;
        obj['version'] = version;
    }

    /**
     * Constructs a <code>UAOutput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UAOutput} obj Optional instance to populate.
     * @return {module:model/UAOutput} The populated <code>UAOutput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UAOutput();

            if (data.hasOwnProperty('agent')) {
                obj['agent'] = ApiClient.convertToType(data['agent'], 'String');
            }
            if (data.hasOwnProperty('classification')) {
                obj['classification'] = ApiClient.convertToType(data['classification'], 'String');
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = ApiClient.convertToType(data['device'], 'String');
            }
            if (data.hasOwnProperty('engine')) {
                obj['engine'] = ApiClient.convertToType(data['engine'], 'String');
            }
            if (data.hasOwnProperty('family')) {
                obj['family'] = ApiClient.convertToType(data['family'], 'String');
            }
            if (data.hasOwnProperty('frequent')) {
                obj['frequent'] = ApiClient.convertToType(data['frequent'], 'String');
            }
            if (data.hasOwnProperty('latest')) {
                obj['latest'] = ApiClient.convertToType(data['latest'], 'String');
            }
            if (data.hasOwnProperty('os')) {
                obj['os'] = ApiClient.convertToType(data['os'], 'String');
            }
            if (data.hasOwnProperty('self')) {
                obj['self'] = ApiClient.convertToType(data['self'], 'String');
            }
            if (data.hasOwnProperty('string')) {
                obj['string'] = ApiClient.convertToType(data['string'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('vendor')) {
                obj['vendor'] = ApiClient.convertToType(data['vendor'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UAOutput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UAOutput</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UAOutput.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['agent'] && !(typeof data['agent'] === 'string' || data['agent'] instanceof String)) {
            throw new Error("Expected the field `agent` to be a primitive type in the JSON string but got " + data['agent']);
        }
        // ensure the json data is a string
        if (data['classification'] && !(typeof data['classification'] === 'string' || data['classification'] instanceof String)) {
            throw new Error("Expected the field `classification` to be a primitive type in the JSON string but got " + data['classification']);
        }
        // ensure the json data is a string
        if (data['device'] && !(typeof data['device'] === 'string' || data['device'] instanceof String)) {
            throw new Error("Expected the field `device` to be a primitive type in the JSON string but got " + data['device']);
        }
        // ensure the json data is a string
        if (data['engine'] && !(typeof data['engine'] === 'string' || data['engine'] instanceof String)) {
            throw new Error("Expected the field `engine` to be a primitive type in the JSON string but got " + data['engine']);
        }
        // ensure the json data is a string
        if (data['family'] && !(typeof data['family'] === 'string' || data['family'] instanceof String)) {
            throw new Error("Expected the field `family` to be a primitive type in the JSON string but got " + data['family']);
        }
        // ensure the json data is a string
        if (data['frequent'] && !(typeof data['frequent'] === 'string' || data['frequent'] instanceof String)) {
            throw new Error("Expected the field `frequent` to be a primitive type in the JSON string but got " + data['frequent']);
        }
        // ensure the json data is a string
        if (data['latest'] && !(typeof data['latest'] === 'string' || data['latest'] instanceof String)) {
            throw new Error("Expected the field `latest` to be a primitive type in the JSON string but got " + data['latest']);
        }
        // ensure the json data is a string
        if (data['os'] && !(typeof data['os'] === 'string' || data['os'] instanceof String)) {
            throw new Error("Expected the field `os` to be a primitive type in the JSON string but got " + data['os']);
        }
        // ensure the json data is a string
        if (data['self'] && !(typeof data['self'] === 'string' || data['self'] instanceof String)) {
            throw new Error("Expected the field `self` to be a primitive type in the JSON string but got " + data['self']);
        }
        // ensure the json data is a string
        if (data['string'] && !(typeof data['string'] === 'string' || data['string'] instanceof String)) {
            throw new Error("Expected the field `string` to be a primitive type in the JSON string but got " + data['string']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['vendor'] && !(typeof data['vendor'] === 'string' || data['vendor'] instanceof String)) {
            throw new Error("Expected the field `vendor` to be a primitive type in the JSON string but got " + data['vendor']);
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }

        return true;
    }


}

UAOutput.RequiredProperties = ["agent", "classification", "device", "engine", "family", "frequent", "latest", "os", "string", "type", "vendor", "version"];

/**
 * @member {String} agent
 */
UAOutput.prototype['agent'] = undefined;

/**
 * @member {module:model/UAOutput.ClassificationEnum} classification
 */
UAOutput.prototype['classification'] = undefined;

/**
 * @member {String} device
 */
UAOutput.prototype['device'] = undefined;

/**
 * @member {String} engine
 */
UAOutput.prototype['engine'] = undefined;

/**
 * @member {String} family
 */
UAOutput.prototype['family'] = undefined;

/**
 * @member {module:model/UAOutput.FrequentEnum} frequent
 */
UAOutput.prototype['frequent'] = undefined;

/**
 * @member {String} latest
 */
UAOutput.prototype['latest'] = undefined;

/**
 * @member {String} os
 */
UAOutput.prototype['os'] = undefined;

/**
 * @member {String} self
 */
UAOutput.prototype['self'] = undefined;

/**
 * @member {String} string
 */
UAOutput.prototype['string'] = undefined;

/**
 * @member {String} type
 */
UAOutput.prototype['type'] = undefined;

/**
 * @member {String} vendor
 */
UAOutput.prototype['vendor'] = undefined;

/**
 * @member {String} version
 */
UAOutput.prototype['version'] = undefined;





/**
 * Allowed values for the <code>classification</code> property.
 * @enum {String}
 * @readonly
 */
UAOutput['ClassificationEnum'] = {

    /**
     * value: "CRAWLER"
     * @const
     */
    "CRAWLER": "CRAWLER",

    /**
     * value: "CLIENT"
     * @const
     */
    "CLIENT": "CLIENT",

    /**
     * value: "UNKNOWN"
     * @const
     */
    "UNKNOWN": "UNKNOWN"
};


/**
 * Allowed values for the <code>frequent</code> property.
 * @enum {String}
 * @readonly
 */
UAOutput['FrequentEnum'] = {

    /**
     * value: "COMMON"
     * @const
     */
    "COMMON": "COMMON",

    /**
     * value: "RARE"
     * @const
     */
    "RARE": "RARE",

    /**
     * value: "VERY_RARE"
     * @const
     */
    "VERY_RARE": "VERY_RARE",

    /**
     * value: "UNKNOWN"
     * @const
     */
    "UNKNOWN": "UNKNOWN"
};



export default UAOutput;

