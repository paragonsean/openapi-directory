/**
 * ThreatJammer.com User API
 *  The public API open to the users. [Read the docs and learn more.](https://threatjammer.com/docs).  ## General information  ### Description Threat Jammer supports two end-user REST APIs: the User API and the Report API. The end-user uses the User API to interact with the different databases, heuristics, and machine learning processes. Devices use the Report API to interact with Threat Jammer. This document will explain how to use the User API and interact with the different services, create a token, interpret the quota information, and create the HTTP request to interact with the User API.  ### Authentication The API is protected by a **Bearer authentication** schema. **Bearer authentication** (also called **token authentication**) is an HTTP authentication scheme that involves security tokens called bearer tokens. It is used to authenticate the user. All the different endpoints expect a `Bearer` token in the `Authorization` header.  Example:  ``` curl -X 'GET'   'https://dublin.api.threatjammer.com/test'   -H 'accept: application/json'   -H 'Authorization: Bearer YOUR_API_KEY' ```  You can obtain a token after registering on the [ThreatJammer.com](https://threatjammer.com) website for free.   ### Region specific tokens All the `Bearer` tokens contain information about the authorized region. The developers have to use a token created for the region they want to use. A token used in a different region will return a `401 Unauthorized` error.  ### Global errors  The API will return the following permanent errors: - a `401 Unauthorized` error if the token is not valid, or does not belong to the region. - a `401 Unauthorized` error if the token does not exist. - a `401 Unauthorized` error if the token is malformed. - a `403 Forbidden` error if the subscription level is not enough. Some endpoints are only available for paid subscription levels.  And these temporary errors: - a `429 Too Many Requests` error if the quota is exceeded (see below).  ### Quota limits  **Every request to the User API will consume one (1) quota point.**  The API has two rate limiting processes: - a quota limit of **5000** requests per month for the `FREE` account. The limit is reset every month. - a quota limit of **10** requests per minute for the `FREE` account. The limit is reset every minute and implements a sliding window mechanism.  
 *
 * The version of the OpenAPI document: 1.2.27
 * Contact: support@threatjammer.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DatacenterPrefixOutput model module.
 * @module model/DatacenterPrefixOutput
 * @version 1.2.27
 */
class DatacenterPrefixOutput {
    /**
     * Constructs a new <code>DatacenterPrefixOutput</code>.
     * @alias module:model/DatacenterPrefixOutput
     * @param datacenter {String} 
     */
    constructor(datacenter) { 
        
        DatacenterPrefixOutput.initialize(this, datacenter);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, datacenter) { 
        obj['datacenter'] = datacenter;
        obj['ip_abuse_total'] = 0;
        obj['max_score'] = 0;
        obj['min_score'] = 0;
    }

    /**
     * Constructs a <code>DatacenterPrefixOutput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DatacenterPrefixOutput} obj Optional instance to populate.
     * @return {module:model/DatacenterPrefixOutput} The populated <code>DatacenterPrefixOutput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DatacenterPrefixOutput();

            if (data.hasOwnProperty('datacenter')) {
                obj['datacenter'] = ApiClient.convertToType(data['datacenter'], 'String');
            }
            if (data.hasOwnProperty('ip_abuse_total')) {
                obj['ip_abuse_total'] = ApiClient.convertToType(data['ip_abuse_total'], 'Number');
            }
            if (data.hasOwnProperty('max_score')) {
                obj['max_score'] = ApiClient.convertToType(data['max_score'], 'Number');
            }
            if (data.hasOwnProperty('min_score')) {
                obj['min_score'] = ApiClient.convertToType(data['min_score'], 'Number');
            }
            if (data.hasOwnProperty('risk')) {
                obj['risk'] = ApiClient.convertToType(data['risk'], 'String');
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'Number');
            }
            if (data.hasOwnProperty('self')) {
                obj['self'] = ApiClient.convertToType(data['self'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DatacenterPrefixOutput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DatacenterPrefixOutput</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DatacenterPrefixOutput.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['datacenter'] && !(typeof data['datacenter'] === 'string' || data['datacenter'] instanceof String)) {
            throw new Error("Expected the field `datacenter` to be a primitive type in the JSON string but got " + data['datacenter']);
        }
        // ensure the json data is a string
        if (data['risk'] && !(typeof data['risk'] === 'string' || data['risk'] instanceof String)) {
            throw new Error("Expected the field `risk` to be a primitive type in the JSON string but got " + data['risk']);
        }
        // ensure the json data is a string
        if (data['self'] && !(typeof data['self'] === 'string' || data['self'] instanceof String)) {
            throw new Error("Expected the field `self` to be a primitive type in the JSON string but got " + data['self']);
        }

        return true;
    }


}

DatacenterPrefixOutput.RequiredProperties = ["datacenter"];

/**
 * @member {String} datacenter
 */
DatacenterPrefixOutput.prototype['datacenter'] = undefined;

/**
 * @member {Number} ip_abuse_total
 * @default 0
 */
DatacenterPrefixOutput.prototype['ip_abuse_total'] = 0;

/**
 * @member {Number} max_score
 * @default 0
 */
DatacenterPrefixOutput.prototype['max_score'] = 0;

/**
 * @member {Number} min_score
 * @default 0
 */
DatacenterPrefixOutput.prototype['min_score'] = 0;

/**
 * @member {module:model/DatacenterPrefixOutput.RiskEnum} risk
 */
DatacenterPrefixOutput.prototype['risk'] = undefined;

/**
 * @member {Number} score
 */
DatacenterPrefixOutput.prototype['score'] = undefined;

/**
 * @member {String} self
 */
DatacenterPrefixOutput.prototype['self'] = undefined;





/**
 * Allowed values for the <code>risk</code> property.
 * @enum {String}
 * @readonly
 */
DatacenterPrefixOutput['RiskEnum'] = {

    /**
     * value: "UNKNOWN"
     * @const
     */
    "UNKNOWN": "UNKNOWN",

    /**
     * value: "LOW"
     * @const
     */
    "LOW": "LOW",

    /**
     * value: "MEDIUM"
     * @const
     */
    "MEDIUM": "MEDIUM",

    /**
     * value: "HIGH"
     * @const
     */
    "HIGH": "HIGH"
};



export default DatacenterPrefixOutput;

