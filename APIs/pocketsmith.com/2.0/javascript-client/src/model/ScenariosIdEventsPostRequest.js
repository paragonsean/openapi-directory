/**
 * PocketSmith
 * The PocketSmith API
 *
 * The version of the OpenAPI document: 2.0
 * Contact: api@pocketsmith.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ScenariosIdEventsPostRequest model module.
 * @module model/ScenariosIdEventsPostRequest
 * @version 2.0
 */
class ScenariosIdEventsPostRequest {
    /**
     * Constructs a new <code>ScenariosIdEventsPostRequest</code>.
     * @alias module:model/ScenariosIdEventsPostRequest
     * @param amount {Number} The amount of the event. A positive amount is a credit, and a negative amount is a debit.
     * @param categoryId {Number} The unique identifier of the category for the event.
     * @param date {String} The starting date of the event.
     * @param repeatType {module:model/ScenariosIdEventsPostRequest.RepeatTypeEnum} The repeat type of the event.
     */
    constructor(amount, categoryId, date, repeatType) { 
        
        ScenariosIdEventsPostRequest.initialize(this, amount, categoryId, date, repeatType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, categoryId, date, repeatType) { 
        obj['amount'] = amount;
        obj['category_id'] = categoryId;
        obj['date'] = date;
        obj['repeat_interval'] = 1;
        obj['repeat_type'] = repeatType;
    }

    /**
     * Constructs a <code>ScenariosIdEventsPostRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ScenariosIdEventsPostRequest} obj Optional instance to populate.
     * @return {module:model/ScenariosIdEventsPostRequest} The populated <code>ScenariosIdEventsPostRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ScenariosIdEventsPostRequest();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('category_id')) {
                obj['category_id'] = ApiClient.convertToType(data['category_id'], 'Number');
            }
            if (data.hasOwnProperty('date')) {
                obj['date'] = ApiClient.convertToType(data['date'], 'String');
            }
            if (data.hasOwnProperty('note')) {
                obj['note'] = ApiClient.convertToType(data['note'], 'String');
            }
            if (data.hasOwnProperty('repeat_interval')) {
                obj['repeat_interval'] = ApiClient.convertToType(data['repeat_interval'], 'Number');
            }
            if (data.hasOwnProperty('repeat_type')) {
                obj['repeat_type'] = ApiClient.convertToType(data['repeat_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ScenariosIdEventsPostRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ScenariosIdEventsPostRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ScenariosIdEventsPostRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['date'] && !(typeof data['date'] === 'string' || data['date'] instanceof String)) {
            throw new Error("Expected the field `date` to be a primitive type in the JSON string but got " + data['date']);
        }
        // ensure the json data is a string
        if (data['note'] && !(typeof data['note'] === 'string' || data['note'] instanceof String)) {
            throw new Error("Expected the field `note` to be a primitive type in the JSON string but got " + data['note']);
        }
        // ensure the json data is a string
        if (data['repeat_type'] && !(typeof data['repeat_type'] === 'string' || data['repeat_type'] instanceof String)) {
            throw new Error("Expected the field `repeat_type` to be a primitive type in the JSON string but got " + data['repeat_type']);
        }

        return true;
    }


}

ScenariosIdEventsPostRequest.RequiredProperties = ["amount", "category_id", "date", "repeat_type"];

/**
 * The amount of the event. A positive amount is a credit, and a negative amount is a debit.
 * @member {Number} amount
 */
ScenariosIdEventsPostRequest.prototype['amount'] = undefined;

/**
 * The unique identifier of the category for the event.
 * @member {Number} category_id
 */
ScenariosIdEventsPostRequest.prototype['category_id'] = undefined;

/**
 * The starting date of the event.
 * @member {String} date
 */
ScenariosIdEventsPostRequest.prototype['date'] = undefined;

/**
 * A note for the event.
 * @member {String} note
 */
ScenariosIdEventsPostRequest.prototype['note'] = undefined;

/**
 * The repeat interval of the event.
 * @member {Number} repeat_interval
 * @default 1
 */
ScenariosIdEventsPostRequest.prototype['repeat_interval'] = 1;

/**
 * The repeat type of the event.
 * @member {module:model/ScenariosIdEventsPostRequest.RepeatTypeEnum} repeat_type
 */
ScenariosIdEventsPostRequest.prototype['repeat_type'] = undefined;





/**
 * Allowed values for the <code>repeat_type</code> property.
 * @enum {String}
 * @readonly
 */
ScenariosIdEventsPostRequest['RepeatTypeEnum'] = {

    /**
     * value: "once"
     * @const
     */
    "once": "once",

    /**
     * value: "daily"
     * @const
     */
    "daily": "daily",

    /**
     * value: "weekly"
     * @const
     */
    "weekly": "weekly",

    /**
     * value: "fortnightly"
     * @const
     */
    "fortnightly": "fortnightly",

    /**
     * value: "monthly"
     * @const
     */
    "monthly": "monthly",

    /**
     * value: "yearly"
     * @const
     */
    "yearly": "yearly",

    /**
     * value: "each weekday"
     * @const
     */
    "each weekday": "each weekday"
};



export default ScenariosIdEventsPostRequest;

