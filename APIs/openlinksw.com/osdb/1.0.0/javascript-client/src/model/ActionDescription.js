/**
 * OSDB REST API v1
 * An OpenAPI description of the OpenLink Smart Data Bot REST API v1
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: osdb@openlinksw.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EntryPoint from './EntryPoint';

/**
 * The ActionDescription model module.
 * @module model/ActionDescription
 * @version 1.0.0
 */
class ActionDescription {
    /**
     * Constructs a new <code>ActionDescription</code>.
     * @alias module:model/ActionDescription
     * @param actionId {String} A unique one word identifier for the action.
     * @param entryPoint {module:model/EntryPoint} 
     */
    constructor(actionId, entryPoint) { 
        
        ActionDescription.initialize(this, actionId, entryPoint);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, actionId, entryPoint) { 
        obj['action_id'] = actionId;
        obj['entry_point'] = entryPoint;
    }

    /**
     * Constructs a <code>ActionDescription</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ActionDescription} obj Optional instance to populate.
     * @return {module:model/ActionDescription} The populated <code>ActionDescription</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ActionDescription();

            if (data.hasOwnProperty('action_id')) {
                obj['action_id'] = ApiClient.convertToType(data['action_id'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('entry_point')) {
                obj['entry_point'] = EntryPoint.constructFromObject(data['entry_point']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ActionDescription</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ActionDescription</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ActionDescription.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['action_id'] && !(typeof data['action_id'] === 'string' || data['action_id'] instanceof String)) {
            throw new Error("Expected the field `action_id` to be a primitive type in the JSON string but got " + data['action_id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // validate the optional field `entry_point`
        if (data['entry_point']) { // data not null
          EntryPoint.validateJSON(data['entry_point']);
        }

        return true;
    }


}

ActionDescription.RequiredProperties = ["action_id", "entry_point"];

/**
 * A unique one word identifier for the action.
 * @member {String} action_id
 */
ActionDescription.prototype['action_id'] = undefined;

/**
 * A short description of the action.
 * @member {String} description
 */
ActionDescription.prototype['description'] = undefined;

/**
 * @member {module:model/EntryPoint} entry_point
 */
ActionDescription.prototype['entry_point'] = undefined;






export default ActionDescription;

