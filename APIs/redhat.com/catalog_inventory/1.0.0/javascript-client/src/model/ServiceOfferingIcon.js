/**
 * Catalog Inventory
 * Catalog Inventory
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@redhat.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ServiceOfferingIcon model module.
 * @module model/ServiceOfferingIcon
 * @version 1.0.0
 */
class ServiceOfferingIcon {
    /**
     * Constructs a new <code>ServiceOfferingIcon</code>.
     * @alias module:model/ServiceOfferingIcon
     */
    constructor() { 
        
        ServiceOfferingIcon.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ServiceOfferingIcon</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServiceOfferingIcon} obj Optional instance to populate.
     * @return {module:model/ServiceOfferingIcon} The populated <code>ServiceOfferingIcon</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServiceOfferingIcon();

            if (data.hasOwnProperty('archived_at')) {
                obj['archived_at'] = ApiClient.convertToType(data['archived_at'], 'Date');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('last_seen_at')) {
                obj['last_seen_at'] = ApiClient.convertToType(data['last_seen_at'], 'Date');
            }
            if (data.hasOwnProperty('refresh_state_part_id')) {
                obj['refresh_state_part_id'] = ApiClient.convertToType(data['refresh_state_part_id'], 'String');
            }
            if (data.hasOwnProperty('source_id')) {
                obj['source_id'] = ApiClient.convertToType(data['source_id'], 'String');
            }
            if (data.hasOwnProperty('source_ref')) {
                obj['source_ref'] = ApiClient.convertToType(data['source_ref'], 'String');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServiceOfferingIcon</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServiceOfferingIcon</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['data'] && !(typeof data['data'] === 'string' || data['data'] instanceof String)) {
            throw new Error("Expected the field `data` to be a primitive type in the JSON string but got " + data['data']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['refresh_state_part_id'] && !(typeof data['refresh_state_part_id'] === 'string' || data['refresh_state_part_id'] instanceof String)) {
            throw new Error("Expected the field `refresh_state_part_id` to be a primitive type in the JSON string but got " + data['refresh_state_part_id']);
        }
        // ensure the json data is a string
        if (data['source_id'] && !(typeof data['source_id'] === 'string' || data['source_id'] instanceof String)) {
            throw new Error("Expected the field `source_id` to be a primitive type in the JSON string but got " + data['source_id']);
        }
        // ensure the json data is a string
        if (data['source_ref'] && !(typeof data['source_ref'] === 'string' || data['source_ref'] instanceof String)) {
            throw new Error("Expected the field `source_ref` to be a primitive type in the JSON string but got " + data['source_ref']);
        }

        return true;
    }


}



/**
 * @member {Date} archived_at
 */
ServiceOfferingIcon.prototype['archived_at'] = undefined;

/**
 * @member {Date} created_at
 */
ServiceOfferingIcon.prototype['created_at'] = undefined;

/**
 * Raw icon data
 * @member {String} data
 */
ServiceOfferingIcon.prototype['data'] = undefined;

/**
 * ID of the resource
 * @member {String} id
 */
ServiceOfferingIcon.prototype['id'] = undefined;

/**
 * @member {Date} last_seen_at
 */
ServiceOfferingIcon.prototype['last_seen_at'] = undefined;

/**
 * ID of the resource
 * @member {String} refresh_state_part_id
 */
ServiceOfferingIcon.prototype['refresh_state_part_id'] = undefined;

/**
 * ID of the resource
 * @member {String} source_id
 */
ServiceOfferingIcon.prototype['source_id'] = undefined;

/**
 * @member {String} source_ref
 */
ServiceOfferingIcon.prototype['source_ref'] = undefined;

/**
 * @member {Date} updated_at
 */
ServiceOfferingIcon.prototype['updated_at'] = undefined;






export default ServiceOfferingIcon;

