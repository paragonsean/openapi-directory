# ip_quality_score_api

IpQualityScoreApi - JavaScript client for ip_quality_score_api
No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install ip_quality_score_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your ip_quality_score_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var IpQualityScoreApi = require('ip_quality_score_api');


var api = new IpQualityScoreApi.EmailValidationApi()
var YOUR_API_KEY_HERE = "asd24#sdfs322#"; // {String} (Required) YOUR_API_KEY_HERE
var USER_EMAIL_HERE = "example@example.com"; // {String} (Required) USER_EMAIL_HERE
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.emailValidation(YOUR_API_KEY_HERE, USER_EMAIL_HERE, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://ipqualityscore.com/api*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*IpQualityScoreApi.EmailValidationApi* | [**emailValidation**](docs/EmailValidationApi.md#emailValidation) | **GET** /json/email/{YOUR_API_KEY_HERE}/{USER_EMAIL_HERE} | Email Validation
*IpQualityScoreApi.MaliciousUrlScannerApi* | [**maliciousUrlScanner**](docs/MaliciousUrlScannerApi.md#maliciousUrlScanner) | **GET** /json/url/{YOUR_API_KEY_HERE}/{URL_HERE} | Malicious URL Scanner
*IpQualityScoreApi.PhoneValidationApi* | [**phoneValidation**](docs/PhoneValidationApi.md#phoneValidation) | **GET** /json/phone/{YOUR_API_KEY_HERE}/{USER_PHONE_HERE} | Phone Validation


## Documentation for Models

 - [IpQualityScoreApi.EmailValidation200Response](docs/EmailValidation200Response.md)
 - [IpQualityScoreApi.EmailValidation200ResponseAssociatedNames](docs/EmailValidation200ResponseAssociatedNames.md)
 - [IpQualityScoreApi.EmailValidation200ResponseAssociatedPhoneNumbers](docs/EmailValidation200ResponseAssociatedPhoneNumbers.md)
 - [IpQualityScoreApi.EmailValidation200ResponseDomainAge](docs/EmailValidation200ResponseDomainAge.md)
 - [IpQualityScoreApi.EmailValidation400Response](docs/EmailValidation400Response.md)
 - [IpQualityScoreApi.MaliciousUrlScanner200Response](docs/MaliciousUrlScanner200Response.md)
 - [IpQualityScoreApi.MaliciousUrlScanner200ResponseDomainAge](docs/MaliciousUrlScanner200ResponseDomainAge.md)
 - [IpQualityScoreApi.PhoneValidation200Response](docs/PhoneValidation200Response.md)
 - [IpQualityScoreApi.PhoneValidation200ResponseAssociatedEmailAddresses](docs/PhoneValidation200ResponseAssociatedEmailAddresses.md)


## Documentation for Authorization

Endpoints do not require authorization.

