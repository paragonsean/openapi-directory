/**
 * AppVeyor REST API
 * AppVeyor is a hosted continuous integration service which runs on Microsoft Windows.  The AppVeyor REST API provides a RESTful way to interact with the AppVeyor service.  This includes managing projects, builds, deployments, and the teams that build them.  Additional help and discussion of the AppVeyor REST API is available at http://help.appveyor.com/discussions  This Swagger definition is an **unofficial** description of the AppVeyor REST API maintained at https://github.com/kevinoid/appveyor-swagger Please report any issues or suggestions for this Swagger definition at https://github.com/kevinoid/appveyor-swagger/issues/new  #### API Conventions  Fields which are missing from update operations (`PUT` requests) are typically reset to their default values.  So although most fields are not technically required, they should usually be specified in practice. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: team@appveyor.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GroupName from './GroupName';
import PermissionState from './PermissionState';

/**
 * The GroupPermissions model module.
 * @module model/GroupPermissions
 * @version 1.0.0
 */
class GroupPermissions {
    /**
     * Constructs a new <code>GroupPermissions</code>.
     * @alias module:model/GroupPermissions
     * @param name {module:model/GroupName} 
     * @param permissions {Array.<module:model/PermissionState>} 
     */
    constructor(name, permissions) { 
        
        GroupPermissions.initialize(this, name, permissions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, permissions) { 
        obj['name'] = name;
        obj['permissions'] = permissions;
    }

    /**
     * Constructs a <code>GroupPermissions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupPermissions} obj Optional instance to populate.
     * @return {module:model/GroupPermissions} The populated <code>GroupPermissions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupPermissions();

            if (data.hasOwnProperty('name')) {
                obj['name'] = GroupName.constructFromObject(data['name']);
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], [PermissionState]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupPermissions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupPermissions</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GroupPermissions.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['permissions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['permissions'])) {
                throw new Error("Expected the field `permissions` to be an array in the JSON data but got " + data['permissions']);
            }
            // validate the optional field `permissions` (array)
            for (const item of data['permissions']) {
                PermissionState.validateJSON(item);
            };
        }

        return true;
    }


}

GroupPermissions.RequiredProperties = ["name", "permissions"];

/**
 * @member {module:model/GroupName} name
 */
GroupPermissions.prototype['name'] = undefined;

/**
 * @member {Array.<module:model/PermissionState>} permissions
 */
GroupPermissions.prototype['permissions'] = undefined;






export default GroupPermissions;

