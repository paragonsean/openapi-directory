/**
 * The Water Linked Underwater GPS API
 * API for the Water Linked Underwater GPS. For more details: http://www.waterlinked.com  Recommended approach for connecting to a Underwater GPS via the API is: - If \"GET /api/\" times out, the Underwater GPS is not running (on this IP address) - If \"GET /api/\" responds with 200 OK check that the api version returrned (eg \"v1\") is supported by the client (eg: also supports \"v1\"). - If the api version returned does not match what the client supports: give an error to the user and recommend upgrading. (Eg: response is \"v2\" while client only supports \"v1\") - If \"GET /api/\" responds with 301 Moved permanently. \"GET /api/v1/version\" to check if the kit has a version earlier than 1.5. - \"GET /api/v1/version\" will always respond with 200 OK on Underwater GPS earlier than 1.5 release.  Configuration API is is not considered stable and will potentially be changed
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SetMasterExternalPayload model module.
 * @module model/SetMasterExternalPayload
 * @version 1.0.0
 */
class SetMasterExternalPayload {
    /**
     * Constructs a new <code>SetMasterExternalPayload</code>.
     * Global master position from external source
     * @alias module:model/SetMasterExternalPayload
     * @param lat {Number} Current Latitude
     * @param lon {Number} Current Longitude
     * @param orientation {Number} Current orientation/compass heading (degrees)
     */
    constructor(lat, lon, orientation) { 
        
        SetMasterExternalPayload.initialize(this, lat, lon, orientation);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, lat, lon, orientation) { 
        obj['lat'] = lat;
        obj['lon'] = lon;
        obj['orientation'] = orientation;
    }

    /**
     * Constructs a <code>SetMasterExternalPayload</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SetMasterExternalPayload} obj Optional instance to populate.
     * @return {module:model/SetMasterExternalPayload} The populated <code>SetMasterExternalPayload</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SetMasterExternalPayload();

            if (data.hasOwnProperty('cog')) {
                obj['cog'] = ApiClient.convertToType(data['cog'], 'Number');
            }
            if (data.hasOwnProperty('fix_quality')) {
                obj['fix_quality'] = ApiClient.convertToType(data['fix_quality'], 'Number');
            }
            if (data.hasOwnProperty('hdop')) {
                obj['hdop'] = ApiClient.convertToType(data['hdop'], 'Number');
            }
            if (data.hasOwnProperty('lat')) {
                obj['lat'] = ApiClient.convertToType(data['lat'], 'Number');
            }
            if (data.hasOwnProperty('lon')) {
                obj['lon'] = ApiClient.convertToType(data['lon'], 'Number');
            }
            if (data.hasOwnProperty('numsats')) {
                obj['numsats'] = ApiClient.convertToType(data['numsats'], 'Number');
            }
            if (data.hasOwnProperty('orientation')) {
                obj['orientation'] = ApiClient.convertToType(data['orientation'], 'Number');
            }
            if (data.hasOwnProperty('sog')) {
                obj['sog'] = ApiClient.convertToType(data['sog'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SetMasterExternalPayload</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SetMasterExternalPayload</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SetMasterExternalPayload.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

SetMasterExternalPayload.RequiredProperties = ["lat", "lon", "orientation"];

/**
 * Course over ground (degrees). -1 means no data.
 * @member {Number} cog
 */
SetMasterExternalPayload.prototype['cog'] = undefined;

/**
 * Fix quality. 0 if no data.
 * @member {Number} fix_quality
 */
SetMasterExternalPayload.prototype['fix_quality'] = undefined;

/**
 * Horizontal dilution of precision. -1 means no data.
 * @member {Number} hdop
 */
SetMasterExternalPayload.prototype['hdop'] = undefined;

/**
 * Current Latitude
 * @member {Number} lat
 */
SetMasterExternalPayload.prototype['lat'] = undefined;

/**
 * Current Longitude
 * @member {Number} lon
 */
SetMasterExternalPayload.prototype['lon'] = undefined;

/**
 * Number of satellites. -1 means no data.
 * @member {Number} numsats
 */
SetMasterExternalPayload.prototype['numsats'] = undefined;

/**
 * Current orientation/compass heading (degrees)
 * @member {Number} orientation
 */
SetMasterExternalPayload.prototype['orientation'] = undefined;

/**
 * Speed over ground (km/h). -1 means no data.
 * @member {Number} sog
 */
SetMasterExternalPayload.prototype['sog'] = undefined;






export default SetMasterExternalPayload;

