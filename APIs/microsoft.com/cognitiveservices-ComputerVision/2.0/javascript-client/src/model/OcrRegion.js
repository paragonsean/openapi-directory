/**
 * Computer Vision Client
 * The Computer Vision API provides state-of-the-art algorithms to process images and return information. For example, it can be used to determine if an image contains mature content, or it can be used to find all the faces in an image.  It also has other features like estimating dominant and accent colors, categorizing the content of images, and describing an image with complete English sentences.  Additionally, it can also intelligently generate images thumbnails for displaying large images effectively.
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OcrLine from './OcrLine';

/**
 * The OcrRegion model module.
 * @module model/OcrRegion
 * @version 2.0
 */
class OcrRegion {
    /**
     * Constructs a new <code>OcrRegion</code>.
     * A region consists of multiple lines (e.g. a column of text in a multi-column document).
     * @alias module:model/OcrRegion
     */
    constructor() { 
        
        OcrRegion.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OcrRegion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OcrRegion} obj Optional instance to populate.
     * @return {module:model/OcrRegion} The populated <code>OcrRegion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OcrRegion();

            if (data.hasOwnProperty('boundingBox')) {
                obj['boundingBox'] = ApiClient.convertToType(data['boundingBox'], 'String');
            }
            if (data.hasOwnProperty('lines')) {
                obj['lines'] = ApiClient.convertToType(data['lines'], [OcrLine]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OcrRegion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OcrRegion</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['boundingBox'] && !(typeof data['boundingBox'] === 'string' || data['boundingBox'] instanceof String)) {
            throw new Error("Expected the field `boundingBox` to be a primitive type in the JSON string but got " + data['boundingBox']);
        }
        if (data['lines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['lines'])) {
                throw new Error("Expected the field `lines` to be an array in the JSON data but got " + data['lines']);
            }
            // validate the optional field `lines` (array)
            for (const item of data['lines']) {
                OcrLine.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Bounding box of a recognized region. The four integers represent the x-coordinate of the left edge, the y-coordinate of the top edge, width, and height of the bounding box, in the coordinate system of the input image, after it has been rotated around its center according to the detected text angle (see textAngle property), with the origin at the top-left corner, and the y-axis pointing down.
 * @member {String} boundingBox
 */
OcrRegion.prototype['boundingBox'] = undefined;

/**
 * An array of recognized lines of text.
 * @member {Array.<module:model/OcrLine>} lines
 */
OcrRegion.prototype['lines'] = undefined;






export default OcrRegion;

