/**
 * Image Search Client
 * The Image Search API lets you send a search query to Bing and get back a list of relevant images. This section provides technical details about the query parameters and headers that you use to request images and the JSON response objects that contain them. For examples that show how to make requests, see [Searching the Web for Images](https://docs.microsoft.com/azure/cognitive-services/bing-image-search/search-the-web).
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NormalizedRectangle from './NormalizedRectangle';
import RecognizedEntity from './RecognizedEntity';
import Response from './Response';

/**
 * The RecognizedEntityRegion model module.
 * @module model/RecognizedEntityRegion
 * @version 1.0
 */
class RecognizedEntityRegion {
    /**
     * Constructs a new <code>RecognizedEntityRegion</code>.
     * Defines a region of the image where an entity was found and a list of entities that might match it.
     * @alias module:model/RecognizedEntityRegion
     * @extends module:model/Response
     * @implements module:model/Response
     * @param type {String} 
     */
    constructor(type) { 
        Response.initialize(this, type);
        RecognizedEntityRegion.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
    }

    /**
     * Constructs a <code>RecognizedEntityRegion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecognizedEntityRegion} obj Optional instance to populate.
     * @return {module:model/RecognizedEntityRegion} The populated <code>RecognizedEntityRegion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecognizedEntityRegion();
            Response.constructFromObject(data, obj);
            Response.constructFromObject(data, obj);

            if (data.hasOwnProperty('matchingEntities')) {
                obj['matchingEntities'] = ApiClient.convertToType(data['matchingEntities'], [RecognizedEntity]);
            }
            if (data.hasOwnProperty('region')) {
                obj['region'] = NormalizedRectangle.constructFromObject(data['region']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecognizedEntityRegion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecognizedEntityRegion</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecognizedEntityRegion.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['matchingEntities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['matchingEntities'])) {
                throw new Error("Expected the field `matchingEntities` to be an array in the JSON data but got " + data['matchingEntities']);
            }
            // validate the optional field `matchingEntities` (array)
            for (const item of data['matchingEntities']) {
                RecognizedEntity.validateJSON(item);
            };
        }
        // validate the optional field `region`
        if (data['region']) { // data not null
          NormalizedRectangle.validateJSON(data['region']);
        }

        return true;
    }


}

RecognizedEntityRegion.RequiredProperties = ["_type"];

/**
 * A list of entities that Bing believes match the entity found in the region. The entities are in descending order of confidence (see the matchConfidence field of RecognizedEntity).
 * @member {Array.<module:model/RecognizedEntity>} matchingEntities
 */
RecognizedEntityRegion.prototype['matchingEntities'] = undefined;

/**
 * @member {module:model/NormalizedRectangle} region
 */
RecognizedEntityRegion.prototype['region'] = undefined;


// Implement Response interface:
/**
 * A String identifier.
 * @member {String} id
 */
Response.prototype['id'] = undefined;
/**
 * @member {String} _type
 */
Response.prototype['_type'] = undefined;




export default RecognizedEntityRegion;

