/**
 * Video Search Client
 * The Video Search API lets you search on Bing for video that are relevant to the user's search query, for insights about a video or for videos that are trending based on search requests made by others. This section provides technical details about the query parameters and headers that you use to request videos and the JSON response objects that contain them. For examples that show how to make requests, see [Searching the Web for Videos](https://docs.microsoft.com/azure/cognitive-services/bing-video-search/search-the-web).
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PivotSuggestions from './PivotSuggestions';
import Query from './Query';
import SearchResultsAnswer from './SearchResultsAnswer';
import VideoObject from './VideoObject';

/**
 * The Videos model module.
 * @module model/Videos
 * @version 1.0
 */
class Videos {
    /**
     * Constructs a new <code>Videos</code>.
     * Defines a video answer.
     * @alias module:model/Videos
     * @extends module:model/SearchResultsAnswer
     * @implements module:model/SearchResultsAnswer
     * @param type {String} 
     */
    constructor(type) { 
        SearchResultsAnswer.initialize(this, type);
        Videos.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['value'] = value;
    }

    /**
     * Constructs a <code>Videos</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Videos} obj Optional instance to populate.
     * @return {module:model/Videos} The populated <code>Videos</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Videos();
            SearchResultsAnswer.constructFromObject(data, obj);
            SearchResultsAnswer.constructFromObject(data, obj);

            if (data.hasOwnProperty('nextOffset')) {
                obj['nextOffset'] = ApiClient.convertToType(data['nextOffset'], 'Number');
            }
            if (data.hasOwnProperty('pivotSuggestions')) {
                obj['pivotSuggestions'] = ApiClient.convertToType(data['pivotSuggestions'], [PivotSuggestions]);
            }
            if (data.hasOwnProperty('queryExpansions')) {
                obj['queryExpansions'] = ApiClient.convertToType(data['queryExpansions'], [Query]);
            }
            if (data.hasOwnProperty('scenario')) {
                obj['scenario'] = ApiClient.convertToType(data['scenario'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], [VideoObject]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Videos</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Videos</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Videos.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['pivotSuggestions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pivotSuggestions'])) {
                throw new Error("Expected the field `pivotSuggestions` to be an array in the JSON data but got " + data['pivotSuggestions']);
            }
            // validate the optional field `pivotSuggestions` (array)
            for (const item of data['pivotSuggestions']) {
                PivotSuggestions.validateJSON(item);
            };
        }
        if (data['queryExpansions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['queryExpansions'])) {
                throw new Error("Expected the field `queryExpansions` to be an array in the JSON data but got " + data['queryExpansions']);
            }
            // validate the optional field `queryExpansions` (array)
            for (const item of data['queryExpansions']) {
                Query.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['scenario'] && !(typeof data['scenario'] === 'string' || data['scenario'] instanceof String)) {
            throw new Error("Expected the field `scenario` to be a primitive type in the JSON string but got " + data['scenario']);
        }
        if (data['value']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['value'])) {
                throw new Error("Expected the field `value` to be an array in the JSON data but got " + data['value']);
            }
            // validate the optional field `value` (array)
            for (const item of data['value']) {
                VideoObject.validateJSON(item);
            };
        }

        return true;
    }


}

Videos.RequiredProperties = ["value", "_type"];

/**
 * @member {Number} nextOffset
 */
Videos.prototype['nextOffset'] = undefined;

/**
 * @member {Array.<module:model/PivotSuggestions>} pivotSuggestions
 */
Videos.prototype['pivotSuggestions'] = undefined;

/**
 * @member {Array.<module:model/Query>} queryExpansions
 */
Videos.prototype['queryExpansions'] = undefined;

/**
 * @member {module:model/Videos.ScenarioEnum} scenario
 */
Videos.prototype['scenario'] = undefined;

/**
 * A list of video objects that are relevant to the query.
 * @member {Array.<module:model/VideoObject>} value
 */
Videos.prototype['value'] = undefined;


// Implement SearchResultsAnswer interface:
/**
 * @member {Array.<module:model/Query>} followUpQueries
 */
SearchResultsAnswer.prototype['followUpQueries'] = undefined;
/**
 * The URL To Bing's search result for this item.
 * @member {String} webSearchUrl
 */
SearchResultsAnswer.prototype['webSearchUrl'] = undefined;
/**
 * A String identifier.
 * @member {String} id
 */
SearchResultsAnswer.prototype['id'] = undefined;
/**
 * @member {String} _type
 */
SearchResultsAnswer.prototype['_type'] = undefined;



/**
 * Allowed values for the <code>scenario</code> property.
 * @enum {String}
 * @readonly
 */
Videos['ScenarioEnum'] = {

    /**
     * value: "List"
     * @const
     */
    "List": "List",

    /**
     * value: "SingleDominantVideo"
     * @const
     */
    "SingleDominantVideo": "SingleDominantVideo"
};



export default Videos;

