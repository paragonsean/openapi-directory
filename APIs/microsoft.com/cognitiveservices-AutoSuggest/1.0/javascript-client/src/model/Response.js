/**
 * AutoSuggest Client
 * Autosuggest supplies search terms derived from a root text sent to the service.  The terms Autosuggest supplies are related to the root text based on similarity and their frequency or ratings of usefulness in other searches. For examples that show how to use Autosuggest, see [Search using AutoSuggest](https://docs.microsoft.com/en-us/rest/api/cognitiveservices/bing-autosuggest-api-v7-reference).
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Action from './Action';
import Identifiable from './Identifiable';

/**
 * The Response model module.
 * @module model/Response
 * @version 1.0
 */
class Response {
    /**
     * Constructs a new <code>Response</code>.
     * Defines a response. All schemas that could be returned at the root of a response should inherit from this
     * @alias module:model/Response
     * @extends module:model/Identifiable
     * @implements module:model/Identifiable
     * @param type {String} 
     */
    constructor(type) { 
        Identifiable.initialize(this, type);
        Response.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
    }

    /**
     * Constructs a <code>Response</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Response} obj Optional instance to populate.
     * @return {module:model/Response} The populated <code>Response</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Response();
            Identifiable.constructFromObject(data, obj);
            Identifiable.constructFromObject(data, obj);

            if (data.hasOwnProperty('adaptiveCard')) {
                obj['adaptiveCard'] = ApiClient.convertToType(data['adaptiveCard'], 'String');
            }
            if (data.hasOwnProperty('immediateAction')) {
                obj['immediateAction'] = ApiClient.convertToType(data['immediateAction'], [Action]);
            }
            if (data.hasOwnProperty('potentialAction')) {
                obj['potentialAction'] = ApiClient.convertToType(data['potentialAction'], [Action]);
            }
            if (data.hasOwnProperty('preferredClickthroughUrl')) {
                obj['preferredClickthroughUrl'] = ApiClient.convertToType(data['preferredClickthroughUrl'], 'String');
            }
            if (data.hasOwnProperty('readLink')) {
                obj['readLink'] = ApiClient.convertToType(data['readLink'], 'String');
            }
            if (data.hasOwnProperty('webSearchUrl')) {
                obj['webSearchUrl'] = ApiClient.convertToType(data['webSearchUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Response</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Response</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Response.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['adaptiveCard'] && !(typeof data['adaptiveCard'] === 'string' || data['adaptiveCard'] instanceof String)) {
            throw new Error("Expected the field `adaptiveCard` to be a primitive type in the JSON string but got " + data['adaptiveCard']);
        }
        if (data['immediateAction']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['immediateAction'])) {
                throw new Error("Expected the field `immediateAction` to be an array in the JSON data but got " + data['immediateAction']);
            }
            // validate the optional field `immediateAction` (array)
            for (const item of data['immediateAction']) {
                Action.validateJSON(item);
            };
        }
        if (data['potentialAction']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['potentialAction'])) {
                throw new Error("Expected the field `potentialAction` to be an array in the JSON data but got " + data['potentialAction']);
            }
            // validate the optional field `potentialAction` (array)
            for (const item of data['potentialAction']) {
                Action.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['preferredClickthroughUrl'] && !(typeof data['preferredClickthroughUrl'] === 'string' || data['preferredClickthroughUrl'] instanceof String)) {
            throw new Error("Expected the field `preferredClickthroughUrl` to be a primitive type in the JSON string but got " + data['preferredClickthroughUrl']);
        }
        // ensure the json data is a string
        if (data['readLink'] && !(typeof data['readLink'] === 'string' || data['readLink'] instanceof String)) {
            throw new Error("Expected the field `readLink` to be a primitive type in the JSON string but got " + data['readLink']);
        }
        // ensure the json data is a string
        if (data['webSearchUrl'] && !(typeof data['webSearchUrl'] === 'string' || data['webSearchUrl'] instanceof String)) {
            throw new Error("Expected the field `webSearchUrl` to be a primitive type in the JSON string but got " + data['webSearchUrl']);
        }

        return true;
    }


}

Response.RequiredProperties = ["_type"];

/**
 * @member {String} adaptiveCard
 */
Response.prototype['adaptiveCard'] = undefined;

/**
 * @member {Array.<module:model/Action>} immediateAction
 */
Response.prototype['immediateAction'] = undefined;

/**
 * @member {Array.<module:model/Action>} potentialAction
 */
Response.prototype['potentialAction'] = undefined;

/**
 * @member {String} preferredClickthroughUrl
 */
Response.prototype['preferredClickthroughUrl'] = undefined;

/**
 * The URL that returns this resource.
 * @member {String} readLink
 */
Response.prototype['readLink'] = undefined;

/**
 * The URL To Bing's search result for this item.
 * @member {String} webSearchUrl
 */
Response.prototype['webSearchUrl'] = undefined;


// Implement Identifiable interface:
/**
 * @member {String} _type
 */
Identifiable.prototype['_type'] = undefined;




export default Response;

