/**
 * AutoSuggest Client
 * Autosuggest supplies search terms derived from a root text sent to the service.  The terms Autosuggest supplies are related to the root text based on similarity and their frequency or ratings of usefulness in other searches. For examples that show how to use Autosuggest, see [Search using AutoSuggest](https://docs.microsoft.com/en-us/rest/api/cognitiveservices/bing-autosuggest-api-v7-reference).
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SearchAction from './SearchAction';

/**
 * The SuggestionsSuggestionGroup model module.
 * @module model/SuggestionsSuggestionGroup
 * @version 1.0
 */
class SuggestionsSuggestionGroup {
    /**
     * Constructs a new <code>SuggestionsSuggestionGroup</code>.
     * @alias module:model/SuggestionsSuggestionGroup
     * @param type {String} 
     * @param name {module:model/SuggestionsSuggestionGroup.NameEnum} 
     * @param searchSuggestions {Array.<module:model/SearchAction>} 
     */
    constructor(type, name, searchSuggestions) { 
        
        SuggestionsSuggestionGroup.initialize(this, type, name, searchSuggestions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, name, searchSuggestions) { 
        obj['_type'] = type;
        obj['name'] = name || 'Unknown';
        obj['searchSuggestions'] = searchSuggestions;
    }

    /**
     * Constructs a <code>SuggestionsSuggestionGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SuggestionsSuggestionGroup} obj Optional instance to populate.
     * @return {module:model/SuggestionsSuggestionGroup} The populated <code>SuggestionsSuggestionGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SuggestionsSuggestionGroup();

            if (data.hasOwnProperty('_type')) {
                obj['_type'] = ApiClient.convertToType(data['_type'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('searchSuggestions')) {
                obj['searchSuggestions'] = ApiClient.convertToType(data['searchSuggestions'], [SearchAction]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SuggestionsSuggestionGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SuggestionsSuggestionGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SuggestionsSuggestionGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['_type'] && !(typeof data['_type'] === 'string' || data['_type'] instanceof String)) {
            throw new Error("Expected the field `_type` to be a primitive type in the JSON string but got " + data['_type']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['searchSuggestions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['searchSuggestions'])) {
                throw new Error("Expected the field `searchSuggestions` to be an array in the JSON data but got " + data['searchSuggestions']);
            }
            // validate the optional field `searchSuggestions` (array)
            for (const item of data['searchSuggestions']) {
                SearchAction.validateJSON(item);
            };
        }

        return true;
    }


}

SuggestionsSuggestionGroup.RequiredProperties = ["_type", "name", "searchSuggestions"];

/**
 * @member {String} _type
 */
SuggestionsSuggestionGroup.prototype['_type'] = undefined;

/**
 * @member {module:model/SuggestionsSuggestionGroup.NameEnum} name
 * @default 'Unknown'
 */
SuggestionsSuggestionGroup.prototype['name'] = 'Unknown';

/**
 * @member {Array.<module:model/SearchAction>} searchSuggestions
 */
SuggestionsSuggestionGroup.prototype['searchSuggestions'] = undefined;





/**
 * Allowed values for the <code>name</code> property.
 * @enum {String}
 * @readonly
 */
SuggestionsSuggestionGroup['NameEnum'] = {

    /**
     * value: "Unknown"
     * @const
     */
    "Unknown": "Unknown",

    /**
     * value: "Web"
     * @const
     */
    "Web": "Web",

    /**
     * value: "StoreApps"
     * @const
     */
    "StoreApps": "StoreApps",

    /**
     * value: "SearchHistory"
     * @const
     */
    "SearchHistory": "SearchHistory",

    /**
     * value: "PersonalSearchDocuments"
     * @const
     */
    "PersonalSearchDocuments": "PersonalSearchDocuments",

    /**
     * value: "PersonalSearchTags"
     * @const
     */
    "PersonalSearchTags": "PersonalSearchTags",

    /**
     * value: "Custom"
     * @const
     */
    "Custom": "Custom"
};



export default SuggestionsSuggestionGroup;

