# computer_vision_client

ComputerVisionClient - JavaScript client for computer_vision_client
The Computer Vision API provides state-of-the-art algorithms to process images and return information. For example, it can be used to determine if an image contains mature content, or it can be used to find all the faces in an image.  It also has other features like estimating dominant and accent colors, categorizing the content of images, and describing an image with complete English sentences.  Additionally, it can also intelligently generate images thumbnails for displaying large images effectively.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2.0
- Package version: 2.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install computer_vision_client --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your computer_vision_client from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var ComputerVisionClient = require('computer_vision_client');

var defaultClient = ComputerVisionClient.ApiClient.instance;
// Configure API key authorization: apim_key
var apim_key = defaultClient.authentications['apim_key'];
apim_key.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//apim_key.apiKeyPrefix['Ocp-Apim-Subscription-Key'] = "Token"

var api = new ComputerVisionClient.DefaultApi()
var imageUrl = new ComputerVisionClient.ImageUrl(); // {ImageUrl} A JSON document with a URL pointing to the image that is to be analyzed.
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.batchReadFile(imageUrl, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://westcentralus.api.cognitive.microsoft.com/vision/v2.0*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*ComputerVisionClient.DefaultApi* | [**batchReadFile**](docs/DefaultApi.md#batchReadFile) | **POST** /read/core/asyncBatchAnalyze | 
*ComputerVisionClient.DefaultApi* | [**getReadOperationResult**](docs/DefaultApi.md#getReadOperationResult) | **GET** /read/operations/{operationId} | 
*ComputerVisionClient.DefaultApi* | [**getTextOperationResult**](docs/DefaultApi.md#getTextOperationResult) | **GET** /textOperations/{operationId} | 
*ComputerVisionClient.DefaultApi* | [**recognizeText**](docs/DefaultApi.md#recognizeText) | **POST** /recognizeText | 


## Documentation for Models

 - [ComputerVisionClient.ComputerVisionError](docs/ComputerVisionError.md)
 - [ComputerVisionClient.ImageUrl](docs/ImageUrl.md)
 - [ComputerVisionClient.Line](docs/Line.md)
 - [ComputerVisionClient.OperationStatus](docs/OperationStatus.md)
 - [ComputerVisionClient.ReadOperationResult](docs/ReadOperationResult.md)
 - [ComputerVisionClient.TextOperationResult](docs/TextOperationResult.md)
 - [ComputerVisionClient.TextRecognitionResult](docs/TextRecognitionResult.md)
 - [ComputerVisionClient.Word](docs/Word.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### apim_key


- **Type**: API key
- **API key parameter name**: Ocp-Apim-Subscription-Key
- **Location**: HTTP header

