/**
 * Control API v1
 * Use the Control API to manage your applications, namespaces, keys, queues, rules, and more.  Detailed information on using this API can be found in the Ably <a href=\"https://ably.com/documentation/control-api\">developer documentation</a>.  Control API is currently in Beta. 
 *
 * The version of the OpenAPI document: 1.0.14
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RuleSource model module.
 * @module model/RuleSource
 * @version 1.0.14
 */
class RuleSource {
    /**
     * Constructs a new <code>RuleSource</code>.
     * @alias module:model/RuleSource
     * @param channelFilter {String} This field allows you to filter your rule based on a regular expression that is matched against the complete channel name. Leave this empty if you want the rule to apply to all channels.
     * @param type {module:model/RuleSource.TypeEnum} The type `channel.message` delivers all messages published on a channel. The type `channel.presence` delivers all enter, update and leave events for members present on a channel. The type `channel.lifecycle` events for this rule type are currently not supported. Get in touch (https://ably.com/contact) if you need this feature. The type `channel.occupancy` delivers all occupancy events for the channel.
     */
    constructor(channelFilter, type) { 
        
        RuleSource.initialize(this, channelFilter, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channelFilter, type) { 
        obj['channelFilter'] = channelFilter;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>RuleSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RuleSource} obj Optional instance to populate.
     * @return {module:model/RuleSource} The populated <code>RuleSource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RuleSource();

            if (data.hasOwnProperty('channelFilter')) {
                obj['channelFilter'] = ApiClient.convertToType(data['channelFilter'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RuleSource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RuleSource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RuleSource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['channelFilter'] && !(typeof data['channelFilter'] === 'string' || data['channelFilter'] instanceof String)) {
            throw new Error("Expected the field `channelFilter` to be a primitive type in the JSON string but got " + data['channelFilter']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

RuleSource.RequiredProperties = ["channelFilter", "type"];

/**
 * This field allows you to filter your rule based on a regular expression that is matched against the complete channel name. Leave this empty if you want the rule to apply to all channels.
 * @member {String} channelFilter
 */
RuleSource.prototype['channelFilter'] = undefined;

/**
 * The type `channel.message` delivers all messages published on a channel. The type `channel.presence` delivers all enter, update and leave events for members present on a channel. The type `channel.lifecycle` events for this rule type are currently not supported. Get in touch (https://ably.com/contact) if you need this feature. The type `channel.occupancy` delivers all occupancy events for the channel.
 * @member {module:model/RuleSource.TypeEnum} type
 */
RuleSource.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
RuleSource['TypeEnum'] = {

    /**
     * value: "channel.message"
     * @const
     */
    "message": "channel.message",

    /**
     * value: "channel.presence"
     * @const
     */
    "presence": "channel.presence",

    /**
     * value: "channel.lifecycle"
     * @const
     */
    "lifecycle": "channel.lifecycle",

    /**
     * value: "channel.occupancy"
     * @const
     */
    "occupancy": "channel.occupancy"
};



export default RuleSource;

