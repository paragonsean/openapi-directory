/*
 * Orbit API
 * Please see the complete Orbit API documentation at [https://api.orbit.love/](https://api.orbit.love/).
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.client.model.Activity;
import org.openapitools.client.model.PostActivity;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:20:44.660005-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class CustomOrPostActivity extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CustomOrPostActivity.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CustomOrPostActivity.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CustomOrPostActivity' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<Activity> adapterActivity = gson.getDelegateAdapter(this, TypeToken.get(Activity.class));
            final TypeAdapter<PostActivity> adapterPostActivity = gson.getDelegateAdapter(this, TypeToken.get(PostActivity.class));

            return (TypeAdapter<T>) new TypeAdapter<CustomOrPostActivity>() {
                @Override
                public void write(JsonWriter out, CustomOrPostActivity value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `Activity`
                    if (value.getActualInstance() instanceof Activity) {
                        JsonElement element = adapterActivity.toJsonTree((Activity)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `PostActivity`
                    if (value.getActualInstance() instanceof PostActivity) {
                        JsonElement element = adapterPostActivity.toJsonTree((PostActivity)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: Activity, PostActivity");
                }

                @Override
                public CustomOrPostActivity read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize Activity
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Activity.validateJsonElement(jsonElement);
                        actualAdapter = adapterActivity;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Activity'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Activity failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Activity'", e);
                    }
                    // deserialize PostActivity
                    try {
                        // validate the JSON object to see if any exception is thrown
                        PostActivity.validateJsonElement(jsonElement);
                        actualAdapter = adapterPostActivity;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'PostActivity'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for PostActivity failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'PostActivity'", e);
                    }

                    if (match == 1) {
                        CustomOrPostActivity ret = new CustomOrPostActivity();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CustomOrPostActivity: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CustomOrPostActivity() {
        super("oneOf", Boolean.FALSE);
    }

    public CustomOrPostActivity(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Activity", Activity.class);
        schemas.put("PostActivity", PostActivity.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CustomOrPostActivity.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * Activity, PostActivity
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof Activity) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof PostActivity) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Activity, PostActivity");
    }

    /**
     * Get the actual instance, which can be the following:
     * Activity, PostActivity
     *
     * @return The actual instance (Activity, PostActivity)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Activity`. If the actual instance is not `Activity`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Activity`
     * @throws ClassCastException if the instance is not `Activity`
     */
    public Activity getActivity() throws ClassCastException {
        return (Activity)super.getActualInstance();
    }
    /**
     * Get the actual instance of `PostActivity`. If the actual instance is not `PostActivity`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `PostActivity`
     * @throws ClassCastException if the instance is not `PostActivity`
     */
    public PostActivity getPostActivity() throws ClassCastException {
        return (PostActivity)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CustomOrPostActivity
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with Activity
        try {
            Activity.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Activity failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with PostActivity
        try {
            PostActivity.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for PostActivity failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for CustomOrPostActivity with oneOf schemas: Activity, PostActivity. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of CustomOrPostActivity given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CustomOrPostActivity
     * @throws IOException if the JSON string is invalid with respect to CustomOrPostActivity
     */
    public static CustomOrPostActivity fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CustomOrPostActivity.class);
    }

    /**
     * Convert an instance of CustomOrPostActivity to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

