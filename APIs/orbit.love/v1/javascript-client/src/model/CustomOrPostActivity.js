/**
 * Orbit API
 * Please see the complete Orbit API documentation at [https://api.orbit.love/](https://api.orbit.love/).
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Activity from './Activity';
import PostActivity from './PostActivity';

/**
 * The CustomOrPostActivity model module.
 * @module model/CustomOrPostActivity
 * @version v1
 */
class CustomOrPostActivity {
    /**
     * Constructs a new <code>CustomOrPostActivity</code>.
     * @alias module:model/CustomOrPostActivity
     * @param {(module:model/Activity|module:model/PostActivity)} instance The actual instance to initialize CustomOrPostActivity.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            if (typeof instance === "Activity") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                Activity.validateJSON(instance); // throw an exception if no match
                // create Activity from JS object
                this.actualInstance = Activity.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into Activity
            errorMessages.push("Failed to construct Activity: " + err)
        }

        try {
            if (typeof instance === "PostActivity") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                PostActivity.validateJSON(instance); // throw an exception if no match
                // create PostActivity from JS object
                this.actualInstance = PostActivity.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into PostActivity
            errorMessages.push("Failed to construct PostActivity: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `CustomOrPostActivity` with oneOf schemas Activity, PostActivity. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `CustomOrPostActivity` with oneOf schemas Activity, PostActivity. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>CustomOrPostActivity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomOrPostActivity} obj Optional instance to populate.
     * @return {module:model/CustomOrPostActivity} The populated <code>CustomOrPostActivity</code> instance.
     */
    static constructFromObject(data, obj) {
        return new CustomOrPostActivity(data);
    }

    /**
     * Gets the actual instance, which can be <code>Activity</code>, <code>PostActivity</code>.
     * @return {(module:model/Activity|module:model/PostActivity)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>Activity</code>, <code>PostActivity</code>.
     * @param {(module:model/Activity|module:model/PostActivity)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = CustomOrPostActivity.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of CustomOrPostActivity from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:model/CustomOrPostActivity} An instance of CustomOrPostActivity.
     */
    static fromJSON = function(json_string){
        return CustomOrPostActivity.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * @member {module:model/CustomOrPostActivity.ActivityTypeEnum} activity_type
 */
CustomOrPostActivity.prototype['activity_type'] = undefined;

/**
 * The key for a custom activity type for the workspace. Will create a new activity type if it does not exist.
 * @member {String} activity_type_key
 */
CustomOrPostActivity.prototype['activity_type_key'] = undefined;

/**
 * A description of the activity; displayed in the timeline
 * @member {String} description
 */
CustomOrPostActivity.prototype['description'] = undefined;

/**
 * Supply a key that must be unique or leave blank to have one generated.
 * @member {String} key
 */
CustomOrPostActivity.prototype['key'] = undefined;

/**
 * A URL for the activity; displayed in the timeline
 * @member {String} link
 */
CustomOrPostActivity.prototype['link'] = undefined;

/**
 * The text for the timeline link
 * @member {String} link_text
 */
CustomOrPostActivity.prototype['link_text'] = undefined;

/**
 * The date and time at which the content was published; defaults to now
 * @member {String} occurred_at
 */
CustomOrPostActivity.prototype['occurred_at'] = undefined;

/**
 * Key-value pairs to provide contextual metadata about an activity.
 * @member {Object} properties
 */
CustomOrPostActivity.prototype['properties'] = undefined;

/**
 * A title for the activity; displayed in the timeline
 * @member {String} title
 */
CustomOrPostActivity.prototype['title'] = undefined;

/**
 * A custom weight to be used in filters and reports; defaults to 1.
 * @member {String} weight
 */
CustomOrPostActivity.prototype['weight'] = undefined;

/**
 * The url representing the post
 * @member {String} url
 */
CustomOrPostActivity.prototype['url'] = undefined;


CustomOrPostActivity.OneOf = ["Activity", "PostActivity"];

export default CustomOrPostActivity;

