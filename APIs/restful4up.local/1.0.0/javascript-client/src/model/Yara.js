/**
 * RESTful4Up
 * RESTful API 4 Unipacker
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import YaraMeta from './YaraMeta';

/**
 * The Yara model module.
 * @module model/Yara
 * @version 1.0.0
 */
class Yara {
    /**
     * Constructs a new <code>Yara</code>.
     * @alias module:model/Yara
     * @param meta {module:model/YaraMeta} 
     * @param name {String} Yara rule name
     * @param strings {Array.<Array.<String>>} Yara rule string section
     */
    constructor(meta, name, strings) { 
        
        Yara.initialize(this, meta, name, strings);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, meta, name, strings) { 
        obj['meta'] = meta;
        obj['name'] = name;
        obj['strings'] = strings;
    }

    /**
     * Constructs a <code>Yara</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Yara} obj Optional instance to populate.
     * @return {module:model/Yara} The populated <code>Yara</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Yara();

            if (data.hasOwnProperty('meta')) {
                obj['meta'] = YaraMeta.constructFromObject(data['meta']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('strings')) {
                obj['strings'] = ApiClient.convertToType(data['strings'], [['String']]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Yara</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Yara</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Yara.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          YaraMeta.validateJSON(data['meta']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['strings'])) {
            throw new Error("Expected the field `strings` to be an array in the JSON data but got " + data['strings']);
        }

        return true;
    }


}

Yara.RequiredProperties = ["meta", "name", "strings"];

/**
 * @member {module:model/YaraMeta} meta
 */
Yara.prototype['meta'] = undefined;

/**
 * Yara rule name
 * @member {String} name
 */
Yara.prototype['name'] = undefined;

/**
 * Yara rule string section
 * @member {Array.<Array.<String>>} strings
 */
Yara.prototype['strings'] = undefined;






export default Yara;

