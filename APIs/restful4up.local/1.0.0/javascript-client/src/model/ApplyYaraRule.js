/**
 * RESTful4Up
 * RESTful API 4 Unipacker
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ApplyYaraRuleMatchedYaraRulesInnerInner from './ApplyYaraRuleMatchedYaraRulesInnerInner';

/**
 * The ApplyYaraRule model module.
 * @module model/ApplyYaraRule
 * @version 1.0.0
 */
class ApplyYaraRule {
    /**
     * Constructs a new <code>ApplyYaraRule</code>.
     * @alias module:model/ApplyYaraRule
     * @param isSuccess {Boolean} is operation success
     * @param yaraCommand {String} Yara command executed
     */
    constructor(isSuccess, yaraCommand) { 
        
        ApplyYaraRule.initialize(this, isSuccess, yaraCommand);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, isSuccess, yaraCommand) { 
        obj['is_success'] = isSuccess;
        obj['yara_command'] = yaraCommand;
    }

    /**
     * Constructs a <code>ApplyYaraRule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApplyYaraRule} obj Optional instance to populate.
     * @return {module:model/ApplyYaraRule} The populated <code>ApplyYaraRule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApplyYaraRule();

            if (data.hasOwnProperty('error_message')) {
                obj['error_message'] = ApiClient.convertToType(data['error_message'], 'String');
            }
            if (data.hasOwnProperty('is_success')) {
                obj['is_success'] = ApiClient.convertToType(data['is_success'], 'Boolean');
            }
            if (data.hasOwnProperty('matched_yara_rules')) {
                obj['matched_yara_rules'] = ApiClient.convertToType(data['matched_yara_rules'], [[ApplyYaraRuleMatchedYaraRulesInnerInner]]);
            }
            if (data.hasOwnProperty('yara_command')) {
                obj['yara_command'] = ApiClient.convertToType(data['yara_command'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApplyYaraRule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApplyYaraRule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApplyYaraRule.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['error_message'] && !(typeof data['error_message'] === 'string' || data['error_message'] instanceof String)) {
            throw new Error("Expected the field `error_message` to be a primitive type in the JSON string but got " + data['error_message']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['matched_yara_rules'])) {
            throw new Error("Expected the field `matched_yara_rules` to be an array in the JSON data but got " + data['matched_yara_rules']);
        }
        // ensure the json data is a string
        if (data['yara_command'] && !(typeof data['yara_command'] === 'string' || data['yara_command'] instanceof String)) {
            throw new Error("Expected the field `yara_command` to be a primitive type in the JSON string but got " + data['yara_command']);
        }

        return true;
    }


}

ApplyYaraRule.RequiredProperties = ["is_success", "yara_command"];

/**
 * error message, if YARA failed
 * @member {String} error_message
 */
ApplyYaraRule.prototype['error_message'] = undefined;

/**
 * is operation success
 * @member {Boolean} is_success
 */
ApplyYaraRule.prototype['is_success'] = undefined;

/**
 * matched YARA rules
 * @member {Array.<Array.<module:model/ApplyYaraRuleMatchedYaraRulesInnerInner>>} matched_yara_rules
 */
ApplyYaraRule.prototype['matched_yara_rules'] = undefined;

/**
 * Yara command executed
 * @member {String} yara_command
 */
ApplyYaraRule.prototype['yara_command'] = undefined;






export default ApplyYaraRule;

