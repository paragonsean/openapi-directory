/**
 * Mailscript
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DomainResponseRecordsInner from './DomainResponseRecordsInner';

/**
 * The DomainResponse model module.
 * @module model/DomainResponse
 * @version 0.4.0
 */
class DomainResponse {
    /**
     * Constructs a new <code>DomainResponse</code>.
     * @alias module:model/DomainResponse
     * @param domain {String} 
     * @param records {Array.<module:model/DomainResponseRecordsInner>} 
     */
    constructor(domain, records) { 
        
        DomainResponse.initialize(this, domain, records);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, domain, records) { 
        obj['domain'] = domain;
        obj['records'] = records;
    }

    /**
     * Constructs a <code>DomainResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DomainResponse} obj Optional instance to populate.
     * @return {module:model/DomainResponse} The populated <code>DomainResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DomainResponse();

            if (data.hasOwnProperty('domain')) {
                obj['domain'] = ApiClient.convertToType(data['domain'], 'String');
            }
            if (data.hasOwnProperty('records')) {
                obj['records'] = ApiClient.convertToType(data['records'], [DomainResponseRecordsInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DomainResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DomainResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DomainResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['domain'] && !(typeof data['domain'] === 'string' || data['domain'] instanceof String)) {
            throw new Error("Expected the field `domain` to be a primitive type in the JSON string but got " + data['domain']);
        }
        if (data['records']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['records'])) {
                throw new Error("Expected the field `records` to be an array in the JSON data but got " + data['records']);
            }
            // validate the optional field `records` (array)
            for (const item of data['records']) {
                DomainResponseRecordsInner.validateJSON(item);
            };
        }

        return true;
    }


}

DomainResponse.RequiredProperties = ["domain", "records"];

/**
 * @member {String} domain
 */
DomainResponse.prototype['domain'] = undefined;

/**
 * @member {Array.<module:model/DomainResponseRecordsInner>} records
 */
DomainResponse.prototype['records'] = undefined;






export default DomainResponse;

