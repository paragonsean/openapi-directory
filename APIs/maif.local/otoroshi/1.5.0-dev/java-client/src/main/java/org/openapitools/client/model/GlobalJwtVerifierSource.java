/*
 * Otoroshi Admin API
 * Admin API of the Otoroshi reverse proxy
 *
 * The version of the OpenAPI document: 1.5.0-dev
 * Contact: oss@maif.fr
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.client.model.InCookie;
import org.openapitools.client.model.InHeader;
import org.openapitools.client.model.InQueryParam;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T10:11:27.562730-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class GlobalJwtVerifierSource extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(GlobalJwtVerifierSource.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GlobalJwtVerifierSource.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GlobalJwtVerifierSource' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<InQueryParam> adapterInQueryParam = gson.getDelegateAdapter(this, TypeToken.get(InQueryParam.class));
            final TypeAdapter<InHeader> adapterInHeader = gson.getDelegateAdapter(this, TypeToken.get(InHeader.class));
            final TypeAdapter<InCookie> adapterInCookie = gson.getDelegateAdapter(this, TypeToken.get(InCookie.class));

            return (TypeAdapter<T>) new TypeAdapter<GlobalJwtVerifierSource>() {
                @Override
                public void write(JsonWriter out, GlobalJwtVerifierSource value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `InQueryParam`
                    if (value.getActualInstance() instanceof InQueryParam) {
                        JsonElement element = adapterInQueryParam.toJsonTree((InQueryParam)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `InHeader`
                    if (value.getActualInstance() instanceof InHeader) {
                        JsonElement element = adapterInHeader.toJsonTree((InHeader)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `InCookie`
                    if (value.getActualInstance() instanceof InCookie) {
                        JsonElement element = adapterInCookie.toJsonTree((InCookie)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: InCookie, InHeader, InQueryParam");
                }

                @Override
                public GlobalJwtVerifierSource read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize InQueryParam
                    try {
                        // validate the JSON object to see if any exception is thrown
                        InQueryParam.validateJsonElement(jsonElement);
                        actualAdapter = adapterInQueryParam;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'InQueryParam'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for InQueryParam failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'InQueryParam'", e);
                    }
                    // deserialize InHeader
                    try {
                        // validate the JSON object to see if any exception is thrown
                        InHeader.validateJsonElement(jsonElement);
                        actualAdapter = adapterInHeader;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'InHeader'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for InHeader failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'InHeader'", e);
                    }
                    // deserialize InCookie
                    try {
                        // validate the JSON object to see if any exception is thrown
                        InCookie.validateJsonElement(jsonElement);
                        actualAdapter = adapterInCookie;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'InCookie'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for InCookie failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'InCookie'", e);
                    }

                    if (match == 1) {
                        GlobalJwtVerifierSource ret = new GlobalJwtVerifierSource();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for GlobalJwtVerifierSource: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public GlobalJwtVerifierSource() {
        super("oneOf", Boolean.FALSE);
    }

    public GlobalJwtVerifierSource(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("InQueryParam", InQueryParam.class);
        schemas.put("InHeader", InHeader.class);
        schemas.put("InCookie", InCookie.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return GlobalJwtVerifierSource.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * InCookie, InHeader, InQueryParam
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof InQueryParam) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof InHeader) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof InCookie) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be InCookie, InHeader, InQueryParam");
    }

    /**
     * Get the actual instance, which can be the following:
     * InCookie, InHeader, InQueryParam
     *
     * @return The actual instance (InCookie, InHeader, InQueryParam)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `InQueryParam`. If the actual instance is not `InQueryParam`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `InQueryParam`
     * @throws ClassCastException if the instance is not `InQueryParam`
     */
    public InQueryParam getInQueryParam() throws ClassCastException {
        return (InQueryParam)super.getActualInstance();
    }
    /**
     * Get the actual instance of `InHeader`. If the actual instance is not `InHeader`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `InHeader`
     * @throws ClassCastException if the instance is not `InHeader`
     */
    public InHeader getInHeader() throws ClassCastException {
        return (InHeader)super.getActualInstance();
    }
    /**
     * Get the actual instance of `InCookie`. If the actual instance is not `InCookie`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `InCookie`
     * @throws ClassCastException if the instance is not `InCookie`
     */
    public InCookie getInCookie() throws ClassCastException {
        return (InCookie)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GlobalJwtVerifierSource
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with InQueryParam
        try {
            InQueryParam.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for InQueryParam failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with InHeader
        try {
            InHeader.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for InHeader failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with InCookie
        try {
            InCookie.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for InCookie failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for GlobalJwtVerifierSource with oneOf schemas: InCookie, InHeader, InQueryParam. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of GlobalJwtVerifierSource given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GlobalJwtVerifierSource
     * @throws IOException if the JSON string is invalid with respect to GlobalJwtVerifierSource
     */
    public static GlobalJwtVerifierSource fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GlobalJwtVerifierSource.class);
    }

    /**
     * Convert an instance of GlobalJwtVerifierSource to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

