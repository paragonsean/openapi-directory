/**
 * Otoroshi Admin API
 * Admin API of the Otoroshi reverse proxy
 *
 * The version of the OpenAPI document: 1.5.0-dev
 * Contact: oss@maif.fr
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RSAlgoSettings model module.
 * @module model/RSAlgoSettings
 * @version 1.5.0-dev
 */
class RSAlgoSettings {
    /**
     * Constructs a new <code>RSAlgoSettings</code>.
     * Settings for an HMAC + SHA signing algorithm
     * @alias module:model/RSAlgoSettings
     * @param publicKey {String} The public key for the RSA function
     * @param size {Number} Size for SHA function. can be 256, 384 or 512
     * @param type {String} String with value RSAlgoSettings
     */
    constructor(publicKey, size, type) { 
        
        RSAlgoSettings.initialize(this, publicKey, size, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, publicKey, size, type) { 
        obj['publicKey'] = publicKey;
        obj['size'] = size;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>RSAlgoSettings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RSAlgoSettings} obj Optional instance to populate.
     * @return {module:model/RSAlgoSettings} The populated <code>RSAlgoSettings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RSAlgoSettings();

            if (data.hasOwnProperty('privateKey')) {
                obj['privateKey'] = ApiClient.convertToType(data['privateKey'], 'String');
            }
            if (data.hasOwnProperty('publicKey')) {
                obj['publicKey'] = ApiClient.convertToType(data['publicKey'], 'String');
            }
            if (data.hasOwnProperty('size')) {
                obj['size'] = ApiClient.convertToType(data['size'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RSAlgoSettings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RSAlgoSettings</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RSAlgoSettings.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['privateKey'] && !(typeof data['privateKey'] === 'string' || data['privateKey'] instanceof String)) {
            throw new Error("Expected the field `privateKey` to be a primitive type in the JSON string but got " + data['privateKey']);
        }
        // ensure the json data is a string
        if (data['publicKey'] && !(typeof data['publicKey'] === 'string' || data['publicKey'] instanceof String)) {
            throw new Error("Expected the field `publicKey` to be a primitive type in the JSON string but got " + data['publicKey']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

RSAlgoSettings.RequiredProperties = ["publicKey", "size", "type"];

/**
 * The private key for the RSA function
 * @member {String} privateKey
 */
RSAlgoSettings.prototype['privateKey'] = undefined;

/**
 * The public key for the RSA function
 * @member {String} publicKey
 */
RSAlgoSettings.prototype['publicKey'] = undefined;

/**
 * Size for SHA function. can be 256, 384 or 512
 * @member {Number} size
 */
RSAlgoSettings.prototype['size'] = undefined;

/**
 * String with value RSAlgoSettings
 * @member {String} type
 */
RSAlgoSettings.prototype['type'] = undefined;






export default RSAlgoSettings;

