/**
 * Otoroshi Admin API
 * Admin API of the Otoroshi reverse proxy
 *
 * The version of the OpenAPI document: 1.5.0-dev
 * Contact: oss@maif.fr
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RefJwtVerifier model module.
 * @module model/RefJwtVerifier
 * @version 1.5.0-dev
 */
class RefJwtVerifier {
    /**
     * Constructs a new <code>RefJwtVerifier</code>.
     * Reference to a global JWT verifier
     * @alias module:model/RefJwtVerifier
     * @param enabled {Boolean} Is it enabled
     * @param id {String} The id of the GlobalJWTVerifier
     * @param type {String} A string with value 'ref'
     */
    constructor(enabled, id, type) { 
        
        RefJwtVerifier.initialize(this, enabled, id, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, enabled, id, type) { 
        obj['enabled'] = enabled;
        obj['id'] = id;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>RefJwtVerifier</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RefJwtVerifier} obj Optional instance to populate.
     * @return {module:model/RefJwtVerifier} The populated <code>RefJwtVerifier</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RefJwtVerifier();

            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RefJwtVerifier</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RefJwtVerifier</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RefJwtVerifier.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

RefJwtVerifier.RequiredProperties = ["enabled", "id", "type"];

/**
 * Is it enabled
 * @member {Boolean} enabled
 */
RefJwtVerifier.prototype['enabled'] = undefined;

/**
 * The id of the GlobalJWTVerifier
 * @member {String} id
 */
RefJwtVerifier.prototype['id'] = undefined;

/**
 * A string with value 'ref'
 * @member {String} type
 */
RefJwtVerifier.prototype['type'] = undefined;






export default RefJwtVerifier;

