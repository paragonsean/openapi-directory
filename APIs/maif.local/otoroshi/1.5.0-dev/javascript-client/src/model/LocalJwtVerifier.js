/**
 * Otoroshi Admin API
 * Admin API of the Otoroshi reverse proxy
 *
 * The version of the OpenAPI document: 1.5.0-dev
 * Contact: oss@maif.fr
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GlobalJwtVerifierAlgoSettings from './GlobalJwtVerifierAlgoSettings';
import GlobalJwtVerifierSource from './GlobalJwtVerifierSource';
import GlobalJwtVerifierStrategy from './GlobalJwtVerifierStrategy';

/**
 * The LocalJwtVerifier model module.
 * @module model/LocalJwtVerifier
 * @version 1.5.0-dev
 */
class LocalJwtVerifier {
    /**
     * Constructs a new <code>LocalJwtVerifier</code>.
     * A JWT verifier used only for the current service descriptor
     * @alias module:model/LocalJwtVerifier
     * @param algoSettings {module:model/GlobalJwtVerifierAlgoSettings} 
     * @param enabled {Boolean} Is it enabled
     * @param source {module:model/GlobalJwtVerifierSource} 
     * @param strategy {module:model/GlobalJwtVerifierStrategy} 
     * @param strict {Boolean} Does it fail if JWT not found
     * @param type {String} A string with value 'local'
     */
    constructor(algoSettings, enabled, source, strategy, strict, type) { 
        
        LocalJwtVerifier.initialize(this, algoSettings, enabled, source, strategy, strict, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, algoSettings, enabled, source, strategy, strict, type) { 
        obj['algoSettings'] = algoSettings;
        obj['enabled'] = enabled;
        obj['source'] = source;
        obj['strategy'] = strategy;
        obj['strict'] = strict;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>LocalJwtVerifier</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LocalJwtVerifier} obj Optional instance to populate.
     * @return {module:model/LocalJwtVerifier} The populated <code>LocalJwtVerifier</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LocalJwtVerifier();

            if (data.hasOwnProperty('algoSettings')) {
                obj['algoSettings'] = GlobalJwtVerifierAlgoSettings.constructFromObject(data['algoSettings']);
            }
            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = GlobalJwtVerifierSource.constructFromObject(data['source']);
            }
            if (data.hasOwnProperty('strategy')) {
                obj['strategy'] = GlobalJwtVerifierStrategy.constructFromObject(data['strategy']);
            }
            if (data.hasOwnProperty('strict')) {
                obj['strict'] = ApiClient.convertToType(data['strict'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LocalJwtVerifier</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LocalJwtVerifier</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LocalJwtVerifier.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `algoSettings`
        if (data['algoSettings']) { // data not null
          GlobalJwtVerifierAlgoSettings.validateJSON(data['algoSettings']);
        }
        // validate the optional field `source`
        if (data['source']) { // data not null
          GlobalJwtVerifierSource.validateJSON(data['source']);
        }
        // validate the optional field `strategy`
        if (data['strategy']) { // data not null
          GlobalJwtVerifierStrategy.validateJSON(data['strategy']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

LocalJwtVerifier.RequiredProperties = ["algoSettings", "enabled", "source", "strategy", "strict", "type"];

/**
 * @member {module:model/GlobalJwtVerifierAlgoSettings} algoSettings
 */
LocalJwtVerifier.prototype['algoSettings'] = undefined;

/**
 * Is it enabled
 * @member {Boolean} enabled
 */
LocalJwtVerifier.prototype['enabled'] = undefined;

/**
 * @member {module:model/GlobalJwtVerifierSource} source
 */
LocalJwtVerifier.prototype['source'] = undefined;

/**
 * @member {module:model/GlobalJwtVerifierStrategy} strategy
 */
LocalJwtVerifier.prototype['strategy'] = undefined;

/**
 * Does it fail if JWT not found
 * @member {Boolean} strict
 */
LocalJwtVerifier.prototype['strict'] = undefined;

/**
 * A string with value 'local'
 * @member {String} type
 */
LocalJwtVerifier.prototype['type'] = undefined;






export default LocalJwtVerifier;

