/**
 * Impala Hotel Booking API
 * Add room selling to your app with ease, or expand your existing hotel portfolio. Access all the marketing material you need to sell a room, from hotel amenities to images. Constantly updated, ever expanding and always correct. Impala allows you to start selling hotel rooms and earn a commission with every booking in hours.  Getting started is easy:  > **1. Sign-up within seconds**: Head to the [Impala website](https://impala.travel), enter your details and receive your sandbox API key immediately (no credit card needed).  > **2. Start building within minutes**: Build against a sandbox API with realistic test data. Use a [step-by-step walkthrough](https://impala.travel) to see what's possible. Check for available hotels and their rates. Make and manage fake bookings without any risk. Add room booking to your existing website or app, or start an online travel agency from scratch. [![Run in Postman](https://run.pstmn.io/button.svg)](https://god.gw.postman.com/run-collection/11303451-9b241872-9961-424d-9ef4-9d949534567d?action=collection%2Ffork&collection-url=entityId%3D11303451-9b241872-9961-424d-9ef4-9d949534567d%26entityType%3Dcollection%26workspaceId%3D5ae55f12-332c-4e7d-8acc-3e75df3b2ee2)  > **3. Go live within hours**: Once your app or integration is ready for prime time, move to production within your Impala dashboard and start making real hotel bookings for your customers right away.  Want to see how it all works? Watch [a 5-minute walkthrough](https://www.youtube.com/watch?v=7B3evCL3nrY) of the two API requests you need to make a booking, and a demo of one of our customer's apps.
 *
 * The version of the OpenAPI document: 1.003
 * Contact: support@impala.travel
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BookingRequestRoomsInner from './BookingRequestRoomsInner';
import NotesBooking from './NotesBooking';
import PersonInfo from './PersonInfo';

/**
 * The BookingRequest model module.
 * @module model/BookingRequest
 * @version 1.003
 */
class BookingRequest {
    /**
     * Constructs a new <code>BookingRequest</code>.
     * @alias module:model/BookingRequest
     * @param bookingContact {module:model/PersonInfo} Details of your guest (will be provided to the hotel in case of questions).
     * @param end {Date} The last day of the desired stay range in ISO 8601 format YYYY-MM-DD.
     * @param rooms {Array.<module:model/BookingRequestRoomsInner>} List of room type identifiers to be booked.
     * @param start {Date} The first day of the desired stay range in ISO 8601 format YYYY-MM-DD.
     */
    constructor(bookingContact, end, rooms, start) { 
        
        BookingRequest.initialize(this, bookingContact, end, rooms, start);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bookingContact, end, rooms, start) { 
        obj['bookingContact'] = bookingContact;
        obj['end'] = end;
        obj['rooms'] = rooms;
        obj['start'] = start;
    }

    /**
     * Constructs a <code>BookingRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BookingRequest} obj Optional instance to populate.
     * @return {module:model/BookingRequest} The populated <code>BookingRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BookingRequest();

            if (data.hasOwnProperty('bookingContact')) {
                obj['bookingContact'] = ApiClient.convertToType(data['bookingContact'], PersonInfo);
            }
            if (data.hasOwnProperty('end')) {
                obj['end'] = ApiClient.convertToType(data['end'], 'Date');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = NotesBooking.constructFromObject(data['notes']);
            }
            if (data.hasOwnProperty('paymentType')) {
                obj['paymentType'] = ApiClient.convertToType(data['paymentType'], 'String');
            }
            if (data.hasOwnProperty('rooms')) {
                obj['rooms'] = ApiClient.convertToType(data['rooms'], [BookingRequestRoomsInner]);
            }
            if (data.hasOwnProperty('start')) {
                obj['start'] = ApiClient.convertToType(data['start'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BookingRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BookingRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BookingRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `bookingContact`
        if (data['bookingContact']) { // data not null
          PersonInfo.validateJSON(data['bookingContact']);
        }
        // validate the optional field `notes`
        if (data['notes']) { // data not null
          NotesBooking.validateJSON(data['notes']);
        }
        // ensure the json data is a string
        if (data['paymentType'] && !(typeof data['paymentType'] === 'string' || data['paymentType'] instanceof String)) {
            throw new Error("Expected the field `paymentType` to be a primitive type in the JSON string but got " + data['paymentType']);
        }
        if (data['rooms']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rooms'])) {
                throw new Error("Expected the field `rooms` to be an array in the JSON data but got " + data['rooms']);
            }
            // validate the optional field `rooms` (array)
            for (const item of data['rooms']) {
                BookingRequestRoomsInner.validateJSON(item);
            };
        }

        return true;
    }


}

BookingRequest.RequiredProperties = ["bookingContact", "end", "rooms", "start"];

/**
 * Details of your guest (will be provided to the hotel in case of questions).
 * @member {module:model/PersonInfo} bookingContact
 */
BookingRequest.prototype['bookingContact'] = undefined;

/**
 * The last day of the desired stay range in ISO 8601 format YYYY-MM-DD.
 * @member {Date} end
 */
BookingRequest.prototype['end'] = undefined;

/**
 * @member {module:model/NotesBooking} notes
 */
BookingRequest.prototype['notes'] = undefined;

/**
 * How will the guest make payment for this booking?
 * @member {module:model/BookingRequest.PaymentTypeEnum} paymentType
 */
BookingRequest.prototype['paymentType'] = undefined;

/**
 * List of room type identifiers to be booked.
 * @member {Array.<module:model/BookingRequestRoomsInner>} rooms
 */
BookingRequest.prototype['rooms'] = undefined;

/**
 * The first day of the desired stay range in ISO 8601 format YYYY-MM-DD.
 * @member {Date} start
 */
BookingRequest.prototype['start'] = undefined;





/**
 * Allowed values for the <code>paymentType</code> property.
 * @enum {String}
 * @readonly
 */
BookingRequest['PaymentTypeEnum'] = {

    /**
     * value: "API"
     * @const
     */
    "API": "API"
};



export default BookingRequest;

