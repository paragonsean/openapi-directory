/**
 * Impala Hotel Booking API
 * Add room selling to your app with ease, or expand your existing hotel portfolio. Access all the marketing material you need to sell a room, from hotel amenities to images. Constantly updated, ever expanding and always correct. Impala allows you to start selling hotel rooms and earn a commission with every booking in hours.  Getting started is easy:  > **1. Sign-up within seconds**: Head to the [Impala website](https://impala.travel), enter your details and receive your sandbox API key immediately (no credit card needed).  > **2. Start building within minutes**: Build against a sandbox API with realistic test data. Use a [step-by-step walkthrough](https://impala.travel) to see what's possible. Check for available hotels and their rates. Make and manage fake bookings without any risk. Add room booking to your existing website or app, or start an online travel agency from scratch. [![Run in Postman](https://run.pstmn.io/button.svg)](https://god.gw.postman.com/run-collection/11303451-9b241872-9961-424d-9ef4-9d949534567d?action=collection%2Ffork&collection-url=entityId%3D11303451-9b241872-9961-424d-9ef4-9d949534567d%26entityType%3Dcollection%26workspaceId%3D5ae55f12-332c-4e7d-8acc-3e75df3b2ee2)  > **3. Go live within hours**: Once your app or integration is ready for prime time, move to production within your Impala dashboard and start making real hotel bookings for your customers right away.  Want to see how it all works? Watch [a 5-minute walkthrough](https://www.youtube.com/watch?v=7B3evCL3nrY) of the two API requests you need to make a booking, and a demo of one of our customer's apps.
 *
 * The version of the OpenAPI document: 1.003
 * Contact: support@impala.travel
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Conditions from './Conditions';
import RateAdjustment from './RateAdjustment';
import RateComponent from './RateComponent';
import RatePlanRestrictions from './RatePlanRestrictions';
import RatePlanRoomType from './RatePlanRoomType';

/**
 * The RatePlan model module.
 * @module model/RatePlan
 * @version 1.003
 */
class RatePlan {
    /**
     * Constructs a new <code>RatePlan</code>.
     * Rate plan encapsulates rates given a set of internal conditions like cancellation policy or meal plan.
     * @alias module:model/RatePlan
     * @param conditions {module:model/Conditions} 
     * @param description {String} Human-readable summary describing this rate plan.
     * @param hotelId {String} The unique identifier of the hotel this rate plan is available for.
     * @param ratePlanId {Number} The integer identifier of this rate plan.
     * @param restrictions {module:model/RatePlanRestrictions} 
     * @param roomTypes {Array.<module:model/RatePlanRoomType>} A list of room types this rate plan is bookable for.
     */
    constructor(conditions, description, hotelId, ratePlanId, restrictions, roomTypes) { 
        
        RatePlan.initialize(this, conditions, description, hotelId, ratePlanId, restrictions, roomTypes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, conditions, description, hotelId, ratePlanId, restrictions, roomTypes) { 
        obj['conditions'] = conditions;
        obj['description'] = description;
        obj['hotelId'] = hotelId;
        obj['ratePlanId'] = ratePlanId;
        obj['restrictions'] = restrictions;
        obj['roomTypes'] = roomTypes;
    }

    /**
     * Constructs a <code>RatePlan</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RatePlan} obj Optional instance to populate.
     * @return {module:model/RatePlan} The populated <code>RatePlan</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RatePlan();

            if (data.hasOwnProperty('adjustments')) {
                obj['adjustments'] = ApiClient.convertToType(data['adjustments'], [RateAdjustment]);
            }
            if (data.hasOwnProperty('components')) {
                obj['components'] = ApiClient.convertToType(data['components'], [RateComponent]);
            }
            if (data.hasOwnProperty('conditions')) {
                obj['conditions'] = Conditions.constructFromObject(data['conditions']);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('hotelId')) {
                obj['hotelId'] = ApiClient.convertToType(data['hotelId'], 'String');
            }
            if (data.hasOwnProperty('ratePlanId')) {
                obj['ratePlanId'] = ApiClient.convertToType(data['ratePlanId'], 'Number');
            }
            if (data.hasOwnProperty('restrictions')) {
                obj['restrictions'] = RatePlanRestrictions.constructFromObject(data['restrictions']);
            }
            if (data.hasOwnProperty('roomTypes')) {
                obj['roomTypes'] = ApiClient.convertToType(data['roomTypes'], [RatePlanRoomType]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RatePlan</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RatePlan</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RatePlan.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['adjustments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['adjustments'])) {
                throw new Error("Expected the field `adjustments` to be an array in the JSON data but got " + data['adjustments']);
            }
            // validate the optional field `adjustments` (array)
            for (const item of data['adjustments']) {
                RateAdjustment.validateJSON(item);
            };
        }
        if (data['components']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['components'])) {
                throw new Error("Expected the field `components` to be an array in the JSON data but got " + data['components']);
            }
            // validate the optional field `components` (array)
            for (const item of data['components']) {
                RateComponent.validateJSON(item);
            };
        }
        // validate the optional field `conditions`
        if (data['conditions']) { // data not null
          Conditions.validateJSON(data['conditions']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['hotelId'] && !(typeof data['hotelId'] === 'string' || data['hotelId'] instanceof String)) {
            throw new Error("Expected the field `hotelId` to be a primitive type in the JSON string but got " + data['hotelId']);
        }
        // validate the optional field `restrictions`
        if (data['restrictions']) { // data not null
          RatePlanRestrictions.validateJSON(data['restrictions']);
        }
        if (data['roomTypes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['roomTypes'])) {
                throw new Error("Expected the field `roomTypes` to be an array in the JSON data but got " + data['roomTypes']);
            }
            // validate the optional field `roomTypes` (array)
            for (const item of data['roomTypes']) {
                RatePlanRoomType.validateJSON(item);
            };
        }

        return true;
    }


}

RatePlan.RequiredProperties = ["conditions", "description", "hotelId", "ratePlanId", "restrictions", "roomTypes"];

/**
 * A list of adjustments that could apply to this rate.
 * @member {Array.<module:model/RateAdjustment>} adjustments
 */
RatePlan.prototype['adjustments'] = undefined;

/**
 * @member {Array.<module:model/RateComponent>} components
 */
RatePlan.prototype['components'] = undefined;

/**
 * @member {module:model/Conditions} conditions
 */
RatePlan.prototype['conditions'] = undefined;

/**
 * Human-readable summary describing this rate plan.
 * @member {String} description
 */
RatePlan.prototype['description'] = undefined;

/**
 * The unique identifier of the hotel this rate plan is available for.
 * @member {String} hotelId
 */
RatePlan.prototype['hotelId'] = undefined;

/**
 * The integer identifier of this rate plan.
 * @member {Number} ratePlanId
 */
RatePlan.prototype['ratePlanId'] = undefined;

/**
 * @member {module:model/RatePlanRestrictions} restrictions
 */
RatePlan.prototype['restrictions'] = undefined;

/**
 * A list of room types this rate plan is bookable for.
 * @member {Array.<module:model/RatePlanRoomType>} roomTypes
 */
RatePlan.prototype['roomTypes'] = undefined;






export default RatePlan;

