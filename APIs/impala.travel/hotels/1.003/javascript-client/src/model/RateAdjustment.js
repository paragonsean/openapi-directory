/**
 * Impala Hotel Booking API
 * Add room selling to your app with ease, or expand your existing hotel portfolio. Access all the marketing material you need to sell a room, from hotel amenities to images. Constantly updated, ever expanding and always correct. Impala allows you to start selling hotel rooms and earn a commission with every booking in hours.  Getting started is easy:  > **1. Sign-up within seconds**: Head to the [Impala website](https://impala.travel), enter your details and receive your sandbox API key immediately (no credit card needed).  > **2. Start building within minutes**: Build against a sandbox API with realistic test data. Use a [step-by-step walkthrough](https://impala.travel) to see what's possible. Check for available hotels and their rates. Make and manage fake bookings without any risk. Add room booking to your existing website or app, or start an online travel agency from scratch. [![Run in Postman](https://run.pstmn.io/button.svg)](https://god.gw.postman.com/run-collection/11303451-9b241872-9961-424d-9ef4-9d949534567d?action=collection%2Ffork&collection-url=entityId%3D11303451-9b241872-9961-424d-9ef4-9d949534567d%26entityType%3Dcollection%26workspaceId%3D5ae55f12-332c-4e7d-8acc-3e75df3b2ee2)  > **3. Go live within hours**: Once your app or integration is ready for prime time, move to production within your Impala dashboard and start making real hotel bookings for your customers right away.  Want to see how it all works? Watch [a 5-minute walkthrough](https://www.youtube.com/watch?v=7B3evCL3nrY) of the two API requests you need to make a booking, and a demo of one of our customer's apps.
 *
 * The version of the OpenAPI document: 1.003
 * Contact: support@impala.travel
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AdjustmentConditionLengthOfStayRule from './AdjustmentConditionLengthOfStayRule';

/**
 * The RateAdjustment model module.
 * @module model/RateAdjustment
 * @version 1.003
 */
class RateAdjustment {
    /**
     * Constructs a new <code>RateAdjustment</code>.
     * @alias module:model/RateAdjustment
     * @param amount {Number} The percentage discount between 0 and 100.
     * @param conditions {Array.<module:model/AdjustmentConditionLengthOfStayRule>} A list of conditions for the adjustment to apply.
     * @param type {module:model/RateAdjustment.TypeEnum} The adjustment type.
     */
    constructor(amount, conditions, type) { 
        
        RateAdjustment.initialize(this, amount, conditions, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, conditions, type) { 
        obj['amount'] = amount;
        obj['conditions'] = conditions;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>RateAdjustment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RateAdjustment} obj Optional instance to populate.
     * @return {module:model/RateAdjustment} The populated <code>RateAdjustment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RateAdjustment();

            if (data.hasOwnProperty('adjustmentId')) {
                obj['adjustmentId'] = ApiClient.convertToType(data['adjustmentId'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('conditions')) {
                obj['conditions'] = ApiClient.convertToType(data['conditions'], [AdjustmentConditionLengthOfStayRule]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RateAdjustment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RateAdjustment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RateAdjustment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['adjustmentId'] && !(typeof data['adjustmentId'] === 'string' || data['adjustmentId'] instanceof String)) {
            throw new Error("Expected the field `adjustmentId` to be a primitive type in the JSON string but got " + data['adjustmentId']);
        }
        if (data['conditions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['conditions'])) {
                throw new Error("Expected the field `conditions` to be an array in the JSON data but got " + data['conditions']);
            }
            // validate the optional field `conditions` (array)
            for (const item of data['conditions']) {
                AdjustmentConditionLengthOfStayRule.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

RateAdjustment.RequiredProperties = ["amount", "conditions", "type"];

/**
 * Unique identifier of this rate adjustment.
 * @member {String} adjustmentId
 */
RateAdjustment.prototype['adjustmentId'] = undefined;

/**
 * The percentage discount between 0 and 100.
 * @member {Number} amount
 */
RateAdjustment.prototype['amount'] = undefined;

/**
 * A list of conditions for the adjustment to apply.
 * @member {Array.<module:model/AdjustmentConditionLengthOfStayRule>} conditions
 */
RateAdjustment.prototype['conditions'] = undefined;

/**
 * The adjustment type.
 * @member {module:model/RateAdjustment.TypeEnum} type
 */
RateAdjustment.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
RateAdjustment['TypeEnum'] = {

    /**
     * value: "PERCENTAGE"
     * @const
     */
    "PERCENTAGE": "PERCENTAGE"
};



export default RateAdjustment;

