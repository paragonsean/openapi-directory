/**
 * Impala Hotel Booking API
 * Add room selling to your app with ease, or expand your existing hotel portfolio. Access all the marketing material you need to sell a room, from hotel amenities to images. Constantly updated, ever expanding and always correct. Impala allows you to start selling hotel rooms and earn a commission with every booking in hours.  Getting started is easy:  > **1. Sign-up within seconds**: Head to the [Impala website](https://impala.travel), enter your details and receive your sandbox API key immediately (no credit card needed).  > **2. Start building within minutes**: Build against a sandbox API with realistic test data. Use a [step-by-step walkthrough](https://impala.travel) to see what's possible. Check for available hotels and their rates. Make and manage fake bookings without any risk. Add room booking to your existing website or app, or start an online travel agency from scratch. [![Run in Postman](https://run.pstmn.io/button.svg)](https://god.gw.postman.com/run-collection/11303451-9b241872-9961-424d-9ef4-9d949534567d?action=collection%2Ffork&collection-url=entityId%3D11303451-9b241872-9961-424d-9ef4-9d949534567d%26entityType%3Dcollection%26workspaceId%3D5ae55f12-332c-4e7d-8acc-3e75df3b2ee2)  > **3. Go live within hours**: Once your app or integration is ready for prime time, move to production within your Impala dashboard and start making real hotel bookings for your customers right away.  Want to see how it all works? Watch [a 5-minute walkthrough](https://www.youtube.com/watch?v=7B3evCL3nrY) of the two API requests you need to make a booking, and a demo of one of our customer's apps.
 *
 * The version of the OpenAPI document: 1.003
 * Contact: support@impala.travel
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Amenity from './Amenity';
import Image from './Image';
import Rate from './Rate';

/**
 * The RoomType model module.
 * @module model/RoomType
 * @version 1.003
 */
class RoomType {
    /**
     * Constructs a new <code>RoomType</code>.
     * Bookable category of bookable rooms within a hotel
     * @alias module:model/RoomType
     * @param amenities {Array.<module:model/Amenity>} 
     * @param description {String} Description
     * @param images {Array.<module:model/Image>} Lists photos of this room type
     * @param maxOccupancy {Number} Maximum number of adults that can occupy the room.
     * @param name {String} Name
     * @param roomTypeId {String} Unique identifier of the room type within the Impala platform.
     */
    constructor(amenities, description, images, maxOccupancy, name, roomTypeId) { 
        
        RoomType.initialize(this, amenities, description, images, maxOccupancy, name, roomTypeId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amenities, description, images, maxOccupancy, name, roomTypeId) { 
        obj['amenities'] = amenities;
        obj['description'] = description;
        obj['images'] = images;
        obj['maxOccupancy'] = maxOccupancy;
        obj['name'] = name;
        obj['roomTypeId'] = roomTypeId;
    }

    /**
     * Constructs a <code>RoomType</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoomType} obj Optional instance to populate.
     * @return {module:model/RoomType} The populated <code>RoomType</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoomType();

            if (data.hasOwnProperty('amenities')) {
                obj['amenities'] = ApiClient.convertToType(data['amenities'], [Amenity]);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('images')) {
                obj['images'] = ApiClient.convertToType(data['images'], [Image]);
            }
            if (data.hasOwnProperty('maxOccupancy')) {
                obj['maxOccupancy'] = ApiClient.convertToType(data['maxOccupancy'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('rates')) {
                obj['rates'] = ApiClient.convertToType(data['rates'], [Rate]);
            }
            if (data.hasOwnProperty('roomTypeId')) {
                obj['roomTypeId'] = ApiClient.convertToType(data['roomTypeId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoomType</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoomType</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoomType.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['amenities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['amenities'])) {
                throw new Error("Expected the field `amenities` to be an array in the JSON data but got " + data['amenities']);
            }
            // validate the optional field `amenities` (array)
            for (const item of data['amenities']) {
                Amenity.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['images']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['images'])) {
                throw new Error("Expected the field `images` to be an array in the JSON data but got " + data['images']);
            }
            // validate the optional field `images` (array)
            for (const item of data['images']) {
                Image.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['rates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rates'])) {
                throw new Error("Expected the field `rates` to be an array in the JSON data but got " + data['rates']);
            }
            // validate the optional field `rates` (array)
            for (const item of data['rates']) {
                Rate.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['roomTypeId'] && !(typeof data['roomTypeId'] === 'string' || data['roomTypeId'] instanceof String)) {
            throw new Error("Expected the field `roomTypeId` to be a primitive type in the JSON string but got " + data['roomTypeId']);
        }

        return true;
    }


}

RoomType.RequiredProperties = ["amenities", "description", "images", "maxOccupancy", "name", "roomTypeId"];

/**
 * @member {Array.<module:model/Amenity>} amenities
 */
RoomType.prototype['amenities'] = undefined;

/**
 * Description
 * @member {String} description
 */
RoomType.prototype['description'] = undefined;

/**
 * Lists photos of this room type
 * @member {Array.<module:model/Image>} images
 */
RoomType.prototype['images'] = undefined;

/**
 * Maximum number of adults that can occupy the room.
 * @member {Number} maxOccupancy
 */
RoomType.prototype['maxOccupancy'] = undefined;

/**
 * Name
 * @member {String} name
 */
RoomType.prototype['name'] = undefined;

/**
 * Bookable rates for this room type and the start and end dates of your availability search.
 * @member {Array.<module:model/Rate>} rates
 */
RoomType.prototype['rates'] = undefined;

/**
 * Unique identifier of the room type within the Impala platform.
 * @member {String} roomTypeId
 */
RoomType.prototype['roomTypeId'] = undefined;






export default RoomType;

