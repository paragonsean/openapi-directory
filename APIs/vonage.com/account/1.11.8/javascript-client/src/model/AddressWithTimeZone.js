/**
 * Account API
 * The Vonage Business Cloud Account API enables you to retrieve information about accounts.  Your application must subscribe to the Provisioning API suite to use this API. 
 *
 * The version of the OpenAPI document: 1.11.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AddressWithTimeZone model module.
 * @module model/AddressWithTimeZone
 * @version 1.11.8
 */
class AddressWithTimeZone {
    /**
     * Constructs a new <code>AddressWithTimeZone</code>.
     * @alias module:model/AddressWithTimeZone
     */
    constructor() { 
        
        AddressWithTimeZone.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AddressWithTimeZone</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddressWithTimeZone} obj Optional instance to populate.
     * @return {module:model/AddressWithTimeZone} The populated <code>AddressWithTimeZone</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddressWithTimeZone();

            if (data.hasOwnProperty('address_1')) {
                obj['address_1'] = ApiClient.convertToType(data['address_1'], 'String');
            }
            if (data.hasOwnProperty('address_2')) {
                obj['address_2'] = ApiClient.convertToType(data['address_2'], 'String');
            }
            if (data.hasOwnProperty('city')) {
                obj['city'] = ApiClient.convertToType(data['city'], 'String');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
            if (data.hasOwnProperty('postal_code')) {
                obj['postal_code'] = ApiClient.convertToType(data['postal_code'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('time_zone')) {
                obj['time_zone'] = ApiClient.convertToType(data['time_zone'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddressWithTimeZone</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddressWithTimeZone</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['address_1'] && !(typeof data['address_1'] === 'string' || data['address_1'] instanceof String)) {
            throw new Error("Expected the field `address_1` to be a primitive type in the JSON string but got " + data['address_1']);
        }
        // ensure the json data is a string
        if (data['address_2'] && !(typeof data['address_2'] === 'string' || data['address_2'] instanceof String)) {
            throw new Error("Expected the field `address_2` to be a primitive type in the JSON string but got " + data['address_2']);
        }
        // ensure the json data is a string
        if (data['city'] && !(typeof data['city'] === 'string' || data['city'] instanceof String)) {
            throw new Error("Expected the field `city` to be a primitive type in the JSON string but got " + data['city']);
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }
        // ensure the json data is a string
        if (data['postal_code'] && !(typeof data['postal_code'] === 'string' || data['postal_code'] instanceof String)) {
            throw new Error("Expected the field `postal_code` to be a primitive type in the JSON string but got " + data['postal_code']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['time_zone'] && !(typeof data['time_zone'] === 'string' || data['time_zone'] instanceof String)) {
            throw new Error("Expected the field `time_zone` to be a primitive type in the JSON string but got " + data['time_zone']);
        }

        return true;
    }


}



/**
 * Street portion of the address
 * @member {String} address_1
 */
AddressWithTimeZone.prototype['address_1'] = undefined;

/**
 * Additional address information
 * @member {String} address_2
 */
AddressWithTimeZone.prototype['address_2'] = undefined;

/**
 * City name
 * @member {String} city
 */
AddressWithTimeZone.prototype['city'] = undefined;

/**
 * Country code
 * @member {String} country
 */
AddressWithTimeZone.prototype['country'] = undefined;

/**
 * Postal code
 * @member {String} postal_code
 */
AddressWithTimeZone.prototype['postal_code'] = undefined;

/**
 * State/Province code
 * @member {String} state
 */
AddressWithTimeZone.prototype['state'] = undefined;

/**
 * Time zone
 * @member {String} time_zone
 */
AddressWithTimeZone.prototype['time_zone'] = undefined;






export default AddressWithTimeZone;

