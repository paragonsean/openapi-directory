/**
 * MotaWord API
 * Use MotaWord API to post and track your translation projects.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SimpleModel from './SimpleModel';

/**
 * The FilterContents model module.
 * @module model/FilterContents
 * @version 1.0
 */
class FilterContents {
    /**
     * Constructs a new <code>FilterContents</code>.
     * @alias module:model/FilterContents
     */
    constructor() { 
        
        FilterContents.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FilterContents</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FilterContents} obj Optional instance to populate.
     * @return {module:model/FilterContents} The populated <code>FilterContents</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FilterContents();

            if (data.hasOwnProperty('budget_codes')) {
                obj['budget_codes'] = ApiClient.convertToType(data['budget_codes'], ['String']);
            }
            if (data.hasOwnProperty('categories')) {
                obj['categories'] = ApiClient.convertToType(data['categories'], ['String']);
            }
            if (data.hasOwnProperty('clients')) {
                obj['clients'] = ApiClient.convertToType(data['clients'], [SimpleModel]);
            }
            if (data.hasOwnProperty('documents')) {
                obj['documents'] = ApiClient.convertToType(data['documents'], [SimpleModel]);
            }
            if (data.hasOwnProperty('projects')) {
                obj['projects'] = ApiClient.convertToType(data['projects'], [SimpleModel]);
            }
            if (data.hasOwnProperty('severities')) {
                obj['severities'] = ApiClient.convertToType(data['severities'], ['String']);
            }
            if (data.hasOwnProperty('source_languages')) {
                obj['source_languages'] = ApiClient.convertToType(data['source_languages'], ['String']);
            }
            if (data.hasOwnProperty('subjects')) {
                obj['subjects'] = ApiClient.convertToType(data['subjects'], ['String']);
            }
            if (data.hasOwnProperty('target_languages')) {
                obj['target_languages'] = ApiClient.convertToType(data['target_languages'], ['String']);
            }
            if (data.hasOwnProperty('vendors')) {
                obj['vendors'] = ApiClient.convertToType(data['vendors'], [SimpleModel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FilterContents</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FilterContents</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['budget_codes'])) {
            throw new Error("Expected the field `budget_codes` to be an array in the JSON data but got " + data['budget_codes']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['categories'])) {
            throw new Error("Expected the field `categories` to be an array in the JSON data but got " + data['categories']);
        }
        if (data['clients']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['clients'])) {
                throw new Error("Expected the field `clients` to be an array in the JSON data but got " + data['clients']);
            }
            // validate the optional field `clients` (array)
            for (const item of data['clients']) {
                SimpleModel.validateJSON(item);
            };
        }
        if (data['documents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['documents'])) {
                throw new Error("Expected the field `documents` to be an array in the JSON data but got " + data['documents']);
            }
            // validate the optional field `documents` (array)
            for (const item of data['documents']) {
                SimpleModel.validateJSON(item);
            };
        }
        if (data['projects']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['projects'])) {
                throw new Error("Expected the field `projects` to be an array in the JSON data but got " + data['projects']);
            }
            // validate the optional field `projects` (array)
            for (const item of data['projects']) {
                SimpleModel.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['severities'])) {
            throw new Error("Expected the field `severities` to be an array in the JSON data but got " + data['severities']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['source_languages'])) {
            throw new Error("Expected the field `source_languages` to be an array in the JSON data but got " + data['source_languages']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['subjects'])) {
            throw new Error("Expected the field `subjects` to be an array in the JSON data but got " + data['subjects']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['target_languages'])) {
            throw new Error("Expected the field `target_languages` to be an array in the JSON data but got " + data['target_languages']);
        }
        if (data['vendors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['vendors'])) {
                throw new Error("Expected the field `vendors` to be an array in the JSON data but got " + data['vendors']);
            }
            // validate the optional field `vendors` (array)
            for (const item of data['vendors']) {
                SimpleModel.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<String>} budget_codes
 */
FilterContents.prototype['budget_codes'] = undefined;

/**
 * @member {Array.<String>} categories
 */
FilterContents.prototype['categories'] = undefined;

/**
 * @member {Array.<module:model/SimpleModel>} clients
 */
FilterContents.prototype['clients'] = undefined;

/**
 * @member {Array.<module:model/SimpleModel>} documents
 */
FilterContents.prototype['documents'] = undefined;

/**
 * @member {Array.<module:model/SimpleModel>} projects
 */
FilterContents.prototype['projects'] = undefined;

/**
 * @member {Array.<String>} severities
 */
FilterContents.prototype['severities'] = undefined;

/**
 * @member {Array.<String>} source_languages
 */
FilterContents.prototype['source_languages'] = undefined;

/**
 * @member {Array.<String>} subjects
 */
FilterContents.prototype['subjects'] = undefined;

/**
 * @member {Array.<String>} target_languages
 */
FilterContents.prototype['target_languages'] = undefined;

/**
 * @member {Array.<module:model/SimpleModel>} vendors
 */
FilterContents.prototype['vendors'] = undefined;






export default FilterContents;

