/**
 * OSF APIv2 Documentation
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@osf.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Relationships11 model module.
 * @module model/Relationships11
 * @version 2.0
 */
class Relationships11 {
    /**
     * Constructs a new <code>Relationships11</code>.
     * URLs to other entities or entity collections that have a relationship to the preprint entity.
     * @alias module:model/Relationships11
     * @param node {String} A relationship to the node that was created for the preprint, or from which the preprint was created.
     * @param primaryFile {String} A relationship to the file that is designated as the preprint's primary file, or the manuscript of the preprint.
     * @param provider {String} A relationship to the preprint provider under which the preprint was created (OSF, socarxiv, psyarxiv, etc.).
     */
    constructor(node, primaryFile, provider) { 
        
        Relationships11.initialize(this, node, primaryFile, provider);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, node, primaryFile, provider) { 
        obj['node'] = node;
        obj['primary_file'] = primaryFile;
        obj['provider'] = provider;
    }

    /**
     * Constructs a <code>Relationships11</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Relationships11} obj Optional instance to populate.
     * @return {module:model/Relationships11} The populated <code>Relationships11</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Relationships11();

            if (data.hasOwnProperty('bibliographic_contributors')) {
                obj['bibliographic_contributors'] = ApiClient.convertToType(data['bibliographic_contributors'], 'String');
            }
            if (data.hasOwnProperty('citation')) {
                obj['citation'] = ApiClient.convertToType(data['citation'], 'String');
            }
            if (data.hasOwnProperty('identifiers')) {
                obj['identifiers'] = ApiClient.convertToType(data['identifiers'], 'String');
            }
            if (data.hasOwnProperty('license')) {
                obj['license'] = ApiClient.convertToType(data['license'], 'String');
            }
            if (data.hasOwnProperty('node')) {
                obj['node'] = ApiClient.convertToType(data['node'], 'String');
            }
            if (data.hasOwnProperty('primary_file')) {
                obj['primary_file'] = ApiClient.convertToType(data['primary_file'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Relationships11</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Relationships11</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Relationships11.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['bibliographic_contributors'] && !(typeof data['bibliographic_contributors'] === 'string' || data['bibliographic_contributors'] instanceof String)) {
            throw new Error("Expected the field `bibliographic_contributors` to be a primitive type in the JSON string but got " + data['bibliographic_contributors']);
        }
        // ensure the json data is a string
        if (data['citation'] && !(typeof data['citation'] === 'string' || data['citation'] instanceof String)) {
            throw new Error("Expected the field `citation` to be a primitive type in the JSON string but got " + data['citation']);
        }
        // ensure the json data is a string
        if (data['identifiers'] && !(typeof data['identifiers'] === 'string' || data['identifiers'] instanceof String)) {
            throw new Error("Expected the field `identifiers` to be a primitive type in the JSON string but got " + data['identifiers']);
        }
        // ensure the json data is a string
        if (data['license'] && !(typeof data['license'] === 'string' || data['license'] instanceof String)) {
            throw new Error("Expected the field `license` to be a primitive type in the JSON string but got " + data['license']);
        }
        // ensure the json data is a string
        if (data['node'] && !(typeof data['node'] === 'string' || data['node'] instanceof String)) {
            throw new Error("Expected the field `node` to be a primitive type in the JSON string but got " + data['node']);
        }
        // ensure the json data is a string
        if (data['primary_file'] && !(typeof data['primary_file'] === 'string' || data['primary_file'] instanceof String)) {
            throw new Error("Expected the field `primary_file` to be a primitive type in the JSON string but got " + data['primary_file']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }

        return true;
    }


}

Relationships11.RequiredProperties = ["node", "primary_file", "provider"];

/**
 * A relationship to the bibliographic contributors that are listed as contributing to this preprint.
 * @member {String} bibliographic_contributors
 */
Relationships11.prototype['bibliographic_contributors'] = undefined;

/**
 * A relationship to the citation of the preprint.
 * @member {String} citation
 */
Relationships11.prototype['citation'] = undefined;

/**
 * A relationship to the identifiers associated with the preprint.
 * @member {String} identifiers
 */
Relationships11.prototype['identifiers'] = undefined;

/**
 * A relationship to the license that has been applied to the preprint.
 * @member {String} license
 */
Relationships11.prototype['license'] = undefined;

/**
 * A relationship to the node that was created for the preprint, or from which the preprint was created.
 * @member {String} node
 */
Relationships11.prototype['node'] = undefined;

/**
 * A relationship to the file that is designated as the preprint's primary file, or the manuscript of the preprint.
 * @member {String} primary_file
 */
Relationships11.prototype['primary_file'] = undefined;

/**
 * A relationship to the preprint provider under which the preprint was created (OSF, socarxiv, psyarxiv, etc.).
 * @member {String} provider
 */
Relationships11.prototype['provider'] = undefined;






export default Relationships11;

