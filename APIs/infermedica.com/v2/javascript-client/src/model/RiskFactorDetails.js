/**
 * Infermedica API
 * Empower your healthcare services with intelligent diagnostic insights of Infermedica API.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RiskFactorDetails model module.
 * @module model/RiskFactorDetails
 * @version v2
 */
class RiskFactorDetails {
    /**
     * Constructs a new <code>RiskFactorDetails</code>.
     * @alias module:model/RiskFactorDetails
     * @param id {String} 
     * @param name {String} 
     * @param question {String} only available in object details, not in listing
     * @param sexFilter {module:model/RiskFactorDetails.SexFilterEnum} 
     */
    constructor(id, name, question, sexFilter) { 
        
        RiskFactorDetails.initialize(this, id, name, question, sexFilter);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, question, sexFilter) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['question'] = question;
        obj['sex_filter'] = sexFilter;
    }

    /**
     * Constructs a <code>RiskFactorDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RiskFactorDetails} obj Optional instance to populate.
     * @return {module:model/RiskFactorDetails} The populated <code>RiskFactorDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RiskFactorDetails();

            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('common_name')) {
                obj['common_name'] = ApiClient.convertToType(data['common_name'], 'String');
            }
            if (data.hasOwnProperty('extras')) {
                obj['extras'] = ApiClient.convertToType(data['extras'], {'String': Object});
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('image_source')) {
                obj['image_source'] = ApiClient.convertToType(data['image_source'], 'String');
            }
            if (data.hasOwnProperty('image_url')) {
                obj['image_url'] = ApiClient.convertToType(data['image_url'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('question')) {
                obj['question'] = ApiClient.convertToType(data['question'], 'String');
            }
            if (data.hasOwnProperty('sex_filter')) {
                obj['sex_filter'] = ApiClient.convertToType(data['sex_filter'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RiskFactorDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RiskFactorDetails</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RiskFactorDetails.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['category'] && !(typeof data['category'] === 'string' || data['category'] instanceof String)) {
            throw new Error("Expected the field `category` to be a primitive type in the JSON string but got " + data['category']);
        }
        // ensure the json data is a string
        if (data['common_name'] && !(typeof data['common_name'] === 'string' || data['common_name'] instanceof String)) {
            throw new Error("Expected the field `common_name` to be a primitive type in the JSON string but got " + data['common_name']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['image_source'] && !(typeof data['image_source'] === 'string' || data['image_source'] instanceof String)) {
            throw new Error("Expected the field `image_source` to be a primitive type in the JSON string but got " + data['image_source']);
        }
        // ensure the json data is a string
        if (data['image_url'] && !(typeof data['image_url'] === 'string' || data['image_url'] instanceof String)) {
            throw new Error("Expected the field `image_url` to be a primitive type in the JSON string but got " + data['image_url']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['question'] && !(typeof data['question'] === 'string' || data['question'] instanceof String)) {
            throw new Error("Expected the field `question` to be a primitive type in the JSON string but got " + data['question']);
        }
        // ensure the json data is a string
        if (data['sex_filter'] && !(typeof data['sex_filter'] === 'string' || data['sex_filter'] instanceof String)) {
            throw new Error("Expected the field `sex_filter` to be a primitive type in the JSON string but got " + data['sex_filter']);
        }

        return true;
    }


}

RiskFactorDetails.RequiredProperties = ["id", "name", "question", "sex_filter"];

/**
 * @member {String} category
 */
RiskFactorDetails.prototype['category'] = undefined;

/**
 * @member {String} common_name
 */
RiskFactorDetails.prototype['common_name'] = undefined;

/**
 * additional content, like custom properties or images
 * @member {Object.<String, Object>} extras
 */
RiskFactorDetails.prototype['extras'] = undefined;

/**
 * @member {String} id
 */
RiskFactorDetails.prototype['id'] = undefined;

/**
 * @member {String} image_source
 */
RiskFactorDetails.prototype['image_source'] = undefined;

/**
 * @member {String} image_url
 */
RiskFactorDetails.prototype['image_url'] = undefined;

/**
 * @member {String} name
 */
RiskFactorDetails.prototype['name'] = undefined;

/**
 * only available in object details, not in listing
 * @member {String} question
 */
RiskFactorDetails.prototype['question'] = undefined;

/**
 * @member {module:model/RiskFactorDetails.SexFilterEnum} sex_filter
 */
RiskFactorDetails.prototype['sex_filter'] = undefined;





/**
 * Allowed values for the <code>sex_filter</code> property.
 * @enum {String}
 * @readonly
 */
RiskFactorDetails['SexFilterEnum'] = {

    /**
     * value: "both"
     * @const
     */
    "both": "both",

    /**
     * value: "male"
     * @const
     */
    "male": "male",

    /**
     * value: "female"
     * @const
     */
    "female": "female"
};



export default RiskFactorDetails;

