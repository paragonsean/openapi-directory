/**
 * Infermedica API
 * Empower your healthcare services with intelligent diagnostic insights of Infermedica API.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SeriousObservation model module.
 * @module model/SeriousObservation
 * @version v2
 */
class SeriousObservation {
    /**
     * Constructs a new <code>SeriousObservation</code>.
     * @alias module:model/SeriousObservation
     * @param id {String} observation id
     * @param name {String} observation name
     */
    constructor(id, name) { 
        
        SeriousObservation.initialize(this, id, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name) { 
        obj['id'] = id;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>SeriousObservation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SeriousObservation} obj Optional instance to populate.
     * @return {module:model/SeriousObservation} The populated <code>SeriousObservation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SeriousObservation();

            if (data.hasOwnProperty('common_name')) {
                obj['common_name'] = ApiClient.convertToType(data['common_name'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('is_emergency')) {
                obj['is_emergency'] = ApiClient.convertToType(data['is_emergency'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SeriousObservation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SeriousObservation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SeriousObservation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['common_name'] && !(typeof data['common_name'] === 'string' || data['common_name'] instanceof String)) {
            throw new Error("Expected the field `common_name` to be a primitive type in the JSON string but got " + data['common_name']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

SeriousObservation.RequiredProperties = ["id", "name"];

/**
 * observation common name
 * @member {String} common_name
 */
SeriousObservation.prototype['common_name'] = undefined;

/**
 * observation id
 * @member {String} id
 */
SeriousObservation.prototype['id'] = undefined;

/**
 * @member {Boolean} is_emergency
 */
SeriousObservation.prototype['is_emergency'] = undefined;

/**
 * observation name
 * @member {String} name
 */
SeriousObservation.prototype['name'] = undefined;






export default SeriousObservation;

