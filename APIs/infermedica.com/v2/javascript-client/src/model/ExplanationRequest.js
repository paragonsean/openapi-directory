/**
 * Infermedica API
 * Empower your healthcare services with intelligent diagnostic insights of Infermedica API.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Evidence from './Evidence';

/**
 * The ExplanationRequest model module.
 * @module model/ExplanationRequest
 * @version v2
 */
class ExplanationRequest {
    /**
     * Constructs a new <code>ExplanationRequest</code>.
     * @alias module:model/ExplanationRequest
     * @param age {Object} 
     * @param sex {module:model/ExplanationRequest.SexEnum} 
     * @param target {String} target condition id
     */
    constructor(age, sex, target) { 
        
        ExplanationRequest.initialize(this, age, sex, target);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, age, sex, target) { 
        obj['age'] = age;
        obj['sex'] = sex;
        obj['target'] = target;
    }

    /**
     * Constructs a <code>ExplanationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExplanationRequest} obj Optional instance to populate.
     * @return {module:model/ExplanationRequest} The populated <code>ExplanationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExplanationRequest();

            if (data.hasOwnProperty('age')) {
                obj['age'] = ApiClient.convertToType(data['age'], Object);
            }
            if (data.hasOwnProperty('evaluated_at')) {
                obj['evaluated_at'] = ApiClient.convertToType(data['evaluated_at'], 'String');
            }
            if (data.hasOwnProperty('evidence')) {
                obj['evidence'] = ApiClient.convertToType(data['evidence'], [Evidence]);
            }
            if (data.hasOwnProperty('extras')) {
                obj['extras'] = ApiClient.convertToType(data['extras'], {'String': Object});
            }
            if (data.hasOwnProperty('sex')) {
                obj['sex'] = ApiClient.convertToType(data['sex'], 'String');
            }
            if (data.hasOwnProperty('target')) {
                obj['target'] = ApiClient.convertToType(data['target'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExplanationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExplanationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExplanationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['evaluated_at'] && !(typeof data['evaluated_at'] === 'string' || data['evaluated_at'] instanceof String)) {
            throw new Error("Expected the field `evaluated_at` to be a primitive type in the JSON string but got " + data['evaluated_at']);
        }
        if (data['evidence']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['evidence'])) {
                throw new Error("Expected the field `evidence` to be an array in the JSON data but got " + data['evidence']);
            }
            // validate the optional field `evidence` (array)
            for (const item of data['evidence']) {
                Evidence.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['sex'] && !(typeof data['sex'] === 'string' || data['sex'] instanceof String)) {
            throw new Error("Expected the field `sex` to be a primitive type in the JSON string but got " + data['sex']);
        }
        // ensure the json data is a string
        if (data['target'] && !(typeof data['target'] === 'string' || data['target'] instanceof String)) {
            throw new Error("Expected the field `target` to be a primitive type in the JSON string but got " + data['target']);
        }

        return true;
    }


}

ExplanationRequest.RequiredProperties = ["age", "sex", "target"];

/**
 * @member {Object} age
 */
ExplanationRequest.prototype['age'] = undefined;

/**
 * time when diagnosis was evaluated in ISO 8601 format
 * @member {String} evaluated_at
 */
ExplanationRequest.prototype['evaluated_at'] = undefined;

/**
 * @member {Array.<module:model/Evidence>} evidence
 */
ExplanationRequest.prototype['evidence'] = undefined;

/**
 * @member {Object.<String, Object>} extras
 */
ExplanationRequest.prototype['extras'] = undefined;

/**
 * @member {module:model/ExplanationRequest.SexEnum} sex
 */
ExplanationRequest.prototype['sex'] = undefined;

/**
 * target condition id
 * @member {String} target
 */
ExplanationRequest.prototype['target'] = undefined;





/**
 * Allowed values for the <code>sex</code> property.
 * @enum {String}
 * @readonly
 */
ExplanationRequest['SexEnum'] = {

    /**
     * value: "male"
     * @const
     */
    "male": "male",

    /**
     * value: "female"
     * @const
     */
    "female": "female"
};



export default ExplanationRequest;

