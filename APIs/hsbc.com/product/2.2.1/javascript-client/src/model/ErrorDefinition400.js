/**
 * Product Finder API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ErrorDefinition400 model module.
 * @module model/ErrorDefinition400
 * @version 2.2.1
 */
class ErrorDefinition400 {
    /**
     * Constructs a new <code>ErrorDefinition400</code>.
     * @alias module:model/ErrorDefinition400
     * @param description {module:model/ErrorDefinition400.DescriptionEnum} 
     * @param status {module:model/ErrorDefinition400.StatusEnum} 
     * @param title {module:model/ErrorDefinition400.TitleEnum} 
     */
    constructor(description, status, title) { 
        
        ErrorDefinition400.initialize(this, description, status, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, description, status, title) { 
        obj['description'] = description;
        obj['status'] = status;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>ErrorDefinition400</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ErrorDefinition400} obj Optional instance to populate.
     * @return {module:model/ErrorDefinition400} The populated <code>ErrorDefinition400</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ErrorDefinition400();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ErrorDefinition400</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ErrorDefinition400</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ErrorDefinition400.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

ErrorDefinition400.RequiredProperties = ["description", "status", "title"];

/**
 * @member {module:model/ErrorDefinition400.DescriptionEnum} description
 */
ErrorDefinition400.prototype['description'] = undefined;

/**
 * @member {module:model/ErrorDefinition400.StatusEnum} status
 */
ErrorDefinition400.prototype['status'] = undefined;

/**
 * @member {module:model/ErrorDefinition400.TitleEnum} title
 */
ErrorDefinition400.prototype['title'] = undefined;





/**
 * Allowed values for the <code>description</code> property.
 * @enum {String}
 * @readonly
 */
ErrorDefinition400['DescriptionEnum'] = {

    /**
     * value: "You have sent a request which could not be understood."
     * @const
     */
    "You have sent a request which could not be understood.": "You have sent a request which could not be understood."
};


/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ErrorDefinition400['StatusEnum'] = {

    /**
     * value: "400"
     * @const
     */
    "400": "400"
};


/**
 * Allowed values for the <code>title</code> property.
 * @enum {String}
 * @readonly
 */
ErrorDefinition400['TitleEnum'] = {

    /**
     * value: "Bad request"
     * @const
     */
    "Bad request": "Bad request"
};



export default ErrorDefinition400;

