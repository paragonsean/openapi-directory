/**
 * ATM Locator API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ErrorDefinition500 model module.
 * @module model/ErrorDefinition500
 * @version 2.2.1
 */
class ErrorDefinition500 {
    /**
     * Constructs a new <code>ErrorDefinition500</code>.
     * @alias module:model/ErrorDefinition500
     * @param description {module:model/ErrorDefinition500.DescriptionEnum} 
     * @param status {module:model/ErrorDefinition500.StatusEnum} 
     * @param title {module:model/ErrorDefinition500.TitleEnum} 
     */
    constructor(description, status, title) { 
        
        ErrorDefinition500.initialize(this, description, status, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, description, status, title) { 
        obj['description'] = description;
        obj['status'] = status;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>ErrorDefinition500</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ErrorDefinition500} obj Optional instance to populate.
     * @return {module:model/ErrorDefinition500} The populated <code>ErrorDefinition500</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ErrorDefinition500();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ErrorDefinition500</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ErrorDefinition500</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ErrorDefinition500.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

ErrorDefinition500.RequiredProperties = ["description", "status", "title"];

/**
 * @member {module:model/ErrorDefinition500.DescriptionEnum} description
 */
ErrorDefinition500.prototype['description'] = undefined;

/**
 * @member {module:model/ErrorDefinition500.StatusEnum} status
 */
ErrorDefinition500.prototype['status'] = undefined;

/**
 * @member {module:model/ErrorDefinition500.TitleEnum} title
 */
ErrorDefinition500.prototype['title'] = undefined;





/**
 * Allowed values for the <code>description</code> property.
 * @enum {String}
 * @readonly
 */
ErrorDefinition500['DescriptionEnum'] = {

    /**
     * value: "An error occurred on the server. No further information is available."
     * @const
     */
    "An error occurred on the server. No further information is available.": "An error occurred on the server. No further information is available."
};


/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ErrorDefinition500['StatusEnum'] = {

    /**
     * value: "500"
     * @const
     */
    "500": "500"
};


/**
 * Allowed values for the <code>title</code> property.
 * @enum {String}
 * @readonly
 */
ErrorDefinition500['TitleEnum'] = {

    /**
     * value: "Internal server error"
     * @const
     */
    "Internal server error": "Internal server error"
};



export default ErrorDefinition500;

