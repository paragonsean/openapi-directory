/*
 * WhatsApp Business API
 * See https://developers.facebook.com/docs/whatsapp
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import java.util.Map;
import org.openapitools.client.model.GatewayNodeStatus;
import org.openapitools.client.model.GatewayStatus;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:59:33.067920-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class CheckHealthResponseHealth extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CheckHealthResponseHealth.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CheckHealthResponseHealth.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CheckHealthResponseHealth' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GatewayStatus> adapterGatewayStatus = gson.getDelegateAdapter(this, TypeToken.get(GatewayStatus.class));
            final Type typeInstanceMapStringGatewayNodeStatus = new TypeToken<Map<String, GatewayNodeStatus>>(){}.getType();
            final TypeAdapter<Map<String, GatewayNodeStatus>> adapterMapStringGatewayNodeStatus = (TypeAdapter<Map<String, GatewayNodeStatus>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceMapStringGatewayNodeStatus));

            return (TypeAdapter<T>) new TypeAdapter<CheckHealthResponseHealth>() {
                @Override
                public void write(JsonWriter out, CheckHealthResponseHealth value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `GatewayStatus`
                    if (value.getActualInstance() instanceof GatewayStatus) {
                        JsonElement element = adapterGatewayStatus.toJsonTree((GatewayStatus)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Map<String, GatewayNodeStatus>`
                    if (value.getActualInstance() instanceof Map<?, ?>) {
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: GatewayStatus, Map<String, GatewayNodeStatus>");
                }

                @Override
                public CheckHealthResponseHealth read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize GatewayStatus
                    try {
                        // validate the JSON object to see if any exception is thrown
                        GatewayStatus.validateJsonElement(jsonElement);
                        actualAdapter = adapterGatewayStatus;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'GatewayStatus'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for GatewayStatus failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'GatewayStatus'", e);
                    }
                    // deserialize Map<String, GatewayNodeStatus>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonObject()) {
                            throw new IllegalArgumentException(String.format("Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        Map<String, JsonElement> map = jsonElement.getAsJsonObject().asMap();
                        // validate map items
                        for(JsonElement element : map.values()) {
                            GatewayNodeStatus.validateJsonElement(element);
                        }
                        actualAdapter = adapterMapStringGatewayNodeStatus;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Map<String, GatewayNodeStatus>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Map<String, GatewayNodeStatus> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Map<String, GatewayNodeStatus>'", e);
                    }

                    if (match == 1) {
                        CheckHealthResponseHealth ret = new CheckHealthResponseHealth();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CheckHealthResponseHealth: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CheckHealthResponseHealth() {
        super("oneOf", Boolean.FALSE);
    }

    public CheckHealthResponseHealth(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("GatewayStatus", GatewayStatus.class);
        schemas.put("Map<String, GatewayNodeStatus>", Map.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CheckHealthResponseHealth.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * GatewayStatus, Map<String, GatewayNodeStatus>
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof GatewayStatus) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Map<?, ?>) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be GatewayStatus, Map<String, GatewayNodeStatus>");
    }

    /**
     * Get the actual instance, which can be the following:
     * GatewayStatus, Map<String, GatewayNodeStatus>
     *
     * @return The actual instance (GatewayStatus, Map<String, GatewayNodeStatus>)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `GatewayStatus`. If the actual instance is not `GatewayStatus`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `GatewayStatus`
     * @throws ClassCastException if the instance is not `GatewayStatus`
     */
    public GatewayStatus getGatewayStatus() throws ClassCastException {
        return (GatewayStatus)super.getActualInstance();
    }
    /**
     * Get the actual instance of `Map<String, GatewayNodeStatus>`. If the actual instance is not `Map<String, GatewayNodeStatus>`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Map<String, GatewayNodeStatus>`
     * @throws ClassCastException if the instance is not `Map<String, GatewayNodeStatus>`
     */
    public Map<String, GatewayNodeStatus> getMapStringGatewayNodeStatus() throws ClassCastException {
        return (Map<String, GatewayNodeStatus>)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CheckHealthResponseHealth
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with GatewayStatus
        try {
            GatewayStatus.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for GatewayStatus failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Map<String, GatewayNodeStatus>
        try {
            if (!jsonElement.isJsonObject()) {
                throw new IllegalArgumentException(String.format("Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
            }

            Map<String, JsonElement> map = jsonElement.getAsJsonObject().asMap();
            // validate map items
            for(JsonElement element : map.values()) {
                GatewayNodeStatus.validateJsonElement(element);
            }
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Map<String, GatewayNodeStatus> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for CheckHealthResponseHealth with oneOf schemas: GatewayStatus, Map<String, GatewayNodeStatus>. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of CheckHealthResponseHealth given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CheckHealthResponseHealth
     * @throws IOException if the JSON string is invalid with respect to CheckHealthResponseHealth
     */
    public static CheckHealthResponseHealth fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CheckHealthResponseHealth.class);
    }

    /**
     * Convert an instance of CheckHealthResponseHealth to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

