/*
 * WhatsApp Business API
 * See https://developers.facebook.com/docs/whatsapp
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.client.model.AudioById;
import org.openapitools.client.model.AudioByProvider;
import org.openapitools.client.model.Provider;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:59:33.067920-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class Audio extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(Audio.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Audio.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Audio' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AudioById> adapterAudioById = gson.getDelegateAdapter(this, TypeToken.get(AudioById.class));
            final TypeAdapter<AudioByProvider> adapterAudioByProvider = gson.getDelegateAdapter(this, TypeToken.get(AudioByProvider.class));

            return (TypeAdapter<T>) new TypeAdapter<Audio>() {
                @Override
                public void write(JsonWriter out, Audio value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `AudioById`
                    if (value.getActualInstance() instanceof AudioById) {
                        JsonElement element = adapterAudioById.toJsonTree((AudioById)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `AudioByProvider`
                    if (value.getActualInstance() instanceof AudioByProvider) {
                        JsonElement element = adapterAudioByProvider.toJsonTree((AudioByProvider)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: AudioById, AudioByProvider");
                }

                @Override
                public Audio read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize AudioById
                    try {
                        // validate the JSON object to see if any exception is thrown
                        AudioById.validateJsonElement(jsonElement);
                        actualAdapter = adapterAudioById;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'AudioById'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for AudioById failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'AudioById'", e);
                    }
                    // deserialize AudioByProvider
                    try {
                        // validate the JSON object to see if any exception is thrown
                        AudioByProvider.validateJsonElement(jsonElement);
                        actualAdapter = adapterAudioByProvider;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'AudioByProvider'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for AudioByProvider failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'AudioByProvider'", e);
                    }

                    if (match == 1) {
                        Audio ret = new Audio();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for Audio: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public Audio() {
        super("oneOf", Boolean.FALSE);
    }

    public Audio(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("AudioById", AudioById.class);
        schemas.put("AudioByProvider", AudioByProvider.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return Audio.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * AudioById, AudioByProvider
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof AudioById) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof AudioByProvider) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be AudioById, AudioByProvider");
    }

    /**
     * Get the actual instance, which can be the following:
     * AudioById, AudioByProvider
     *
     * @return The actual instance (AudioById, AudioByProvider)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `AudioById`. If the actual instance is not `AudioById`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `AudioById`
     * @throws ClassCastException if the instance is not `AudioById`
     */
    public AudioById getAudioById() throws ClassCastException {
        return (AudioById)super.getActualInstance();
    }
    /**
     * Get the actual instance of `AudioByProvider`. If the actual instance is not `AudioByProvider`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `AudioByProvider`
     * @throws ClassCastException if the instance is not `AudioByProvider`
     */
    public AudioByProvider getAudioByProvider() throws ClassCastException {
        return (AudioByProvider)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to Audio
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with AudioById
        try {
            AudioById.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for AudioById failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with AudioByProvider
        try {
            AudioByProvider.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for AudioByProvider failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for Audio with oneOf schemas: AudioById, AudioByProvider. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of Audio given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Audio
     * @throws IOException if the JSON string is invalid with respect to Audio
     */
    public static Audio fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Audio.class);
    }

    /**
     * Convert an instance of Audio to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

