/*
 * WhatsApp Business API
 * See https://developers.facebook.com/docs/whatsapp
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.client.model.ImageById;
import org.openapitools.client.model.ImageByProvider;
import org.openapitools.client.model.Provider;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:59:33.067920-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class Image extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(Image.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Image.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Image' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ImageById> adapterImageById = gson.getDelegateAdapter(this, TypeToken.get(ImageById.class));
            final TypeAdapter<ImageByProvider> adapterImageByProvider = gson.getDelegateAdapter(this, TypeToken.get(ImageByProvider.class));

            return (TypeAdapter<T>) new TypeAdapter<Image>() {
                @Override
                public void write(JsonWriter out, Image value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `ImageById`
                    if (value.getActualInstance() instanceof ImageById) {
                        JsonElement element = adapterImageById.toJsonTree((ImageById)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `ImageByProvider`
                    if (value.getActualInstance() instanceof ImageByProvider) {
                        JsonElement element = adapterImageByProvider.toJsonTree((ImageByProvider)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: ImageById, ImageByProvider");
                }

                @Override
                public Image read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize ImageById
                    try {
                        // validate the JSON object to see if any exception is thrown
                        ImageById.validateJsonElement(jsonElement);
                        actualAdapter = adapterImageById;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'ImageById'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for ImageById failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'ImageById'", e);
                    }
                    // deserialize ImageByProvider
                    try {
                        // validate the JSON object to see if any exception is thrown
                        ImageByProvider.validateJsonElement(jsonElement);
                        actualAdapter = adapterImageByProvider;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'ImageByProvider'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for ImageByProvider failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'ImageByProvider'", e);
                    }

                    if (match == 1) {
                        Image ret = new Image();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for Image: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public Image() {
        super("oneOf", Boolean.FALSE);
    }

    public Image(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("ImageById", ImageById.class);
        schemas.put("ImageByProvider", ImageByProvider.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return Image.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * ImageById, ImageByProvider
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof ImageById) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof ImageByProvider) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be ImageById, ImageByProvider");
    }

    /**
     * Get the actual instance, which can be the following:
     * ImageById, ImageByProvider
     *
     * @return The actual instance (ImageById, ImageByProvider)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `ImageById`. If the actual instance is not `ImageById`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ImageById`
     * @throws ClassCastException if the instance is not `ImageById`
     */
    public ImageById getImageById() throws ClassCastException {
        return (ImageById)super.getActualInstance();
    }
    /**
     * Get the actual instance of `ImageByProvider`. If the actual instance is not `ImageByProvider`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ImageByProvider`
     * @throws ClassCastException if the instance is not `ImageByProvider`
     */
    public ImageByProvider getImageByProvider() throws ClassCastException {
        return (ImageByProvider)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to Image
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with ImageById
        try {
            ImageById.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for ImageById failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with ImageByProvider
        try {
            ImageByProvider.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for ImageByProvider failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for Image with oneOf schemas: ImageById, ImageByProvider. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of Image given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Image
     * @throws IOException if the JSON string is invalid with respect to Image
     */
    public static Image fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Image.class);
    }

    /**
     * Convert an instance of Image to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

