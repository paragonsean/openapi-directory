/**
 * ElevenLabs API Documentation
 * This is the documentation for the ElevenLabs API. You can use this API to use our service programmatically, this is done by using your xi-api-key. <br/> You can view your xi-api-key using the 'Profile' tab on https://beta.elevenlabs.io. Our API is experimental so all endpoints are subject to change.
 *
 * The version of the OpenAPI document: 1.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAISamplesApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAISamplesApi::OAISamplesApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAISamplesApi::~OAISamplesApi() {
}

void OAISamplesApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("/"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("deleteSampleV1VoicesVoiceIdSamplesSampleIdDelete", defaultConf);
    _serverIndices.insert("deleteSampleV1VoicesVoiceIdSamplesSampleIdDelete", 0);
    _serverConfigs.insert("getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGet", defaultConf);
    _serverIndices.insert("getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGet", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAISamplesApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAISamplesApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAISamplesApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAISamplesApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAISamplesApi::setUsername(const QString &username) {
    _username = username;
}

void OAISamplesApi::setPassword(const QString &password) {
    _password = password;
}


void OAISamplesApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAISamplesApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAISamplesApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAISamplesApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAISamplesApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAISamplesApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAISamplesApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAISamplesApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAISamplesApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAISamplesApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAISamplesApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAISamplesApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAISamplesApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAISamplesApi::deleteSampleV1VoicesVoiceIdSamplesSampleIdDelete(const QString &voice_id, const QString &sample_id, const ::OpenAPI::OptionalParam<QString> &xi_api_key) {
    QString fullPath = QString(_serverConfigs["deleteSampleV1VoicesVoiceIdSamplesSampleIdDelete"][_serverIndices.value("deleteSampleV1VoicesVoiceIdSamplesSampleIdDelete")].URL()+"/v1/voices/{voice_id}/samples/{sample_id}");
    
    
    {
        QString voice_idPathParam("{");
        voice_idPathParam.append("voice_id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "voice_id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"voice_id"+pathSuffix : pathPrefix;
        fullPath.replace(voice_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(voice_id)));
    }
    
    {
        QString sample_idPathParam("{");
        sample_idPathParam.append("sample_id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "sample_id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"sample_id"+pathSuffix : pathPrefix;
        fullPath.replace(sample_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(sample_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    if (xi_api_key.hasValue())
    {
        if (!::OpenAPI::toStringValue(xi_api_key.value()).isEmpty()) {
            input.headers.insert("xi-api-key", ::OpenAPI::toStringValue(xi_api_key.value()));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISamplesApi::deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteCallback);
    connect(this, &OAISamplesApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISamplesApi::deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QJsonValue output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteSignal(output);
        Q_EMIT deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteSignalE(output, error_type, error_str);
        Q_EMIT deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteSignalError(output, error_type, error_str);
        Q_EMIT deleteSampleV1VoicesVoiceIdSamplesSampleIdDeleteSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISamplesApi::getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGet(const QString &voice_id, const QString &sample_id, const ::OpenAPI::OptionalParam<QString> &xi_api_key) {
    QString fullPath = QString(_serverConfigs["getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGet"][_serverIndices.value("getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGet")].URL()+"/v1/voices/{voice_id}/samples/{sample_id}/audio");
    
    
    {
        QString voice_idPathParam("{");
        voice_idPathParam.append("voice_id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "voice_id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"voice_id"+pathSuffix : pathPrefix;
        fullPath.replace(voice_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(voice_id)));
    }
    
    {
        QString sample_idPathParam("{");
        sample_idPathParam.append("sample_id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "sample_id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"sample_id"+pathSuffix : pathPrefix;
        fullPath.replace(sample_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(sample_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    if (xi_api_key.hasValue())
    {
        if (!::OpenAPI::toStringValue(xi_api_key.value()).isEmpty()) {
            input.headers.insert("xi-api-key", ::OpenAPI::toStringValue(xi_api_key.value()));
        }
        }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAISamplesApi::getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetCallback);
    connect(this, &OAISamplesApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAISamplesApi::getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetSignal();
        Q_EMIT getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetSignalE(error_type, error_str);
        Q_EMIT getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetSignalError(error_type, error_str);
        Q_EMIT getAudioFromSampleV1VoicesVoiceIdSamplesSampleIdAudioGetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAISamplesApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
