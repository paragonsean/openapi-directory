/**
 * SheerSEO API
 * Sheerseo API has 2 stages:<br>First stage - initiating the task: You fill in your task and receive in return the task id. <br>Second stage - collecting the results: send a request containing the task ids you got at the first stage and collecting the results.
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KeywordTask from './KeywordTask';

/**
 * The RankSubmitResponse model module.
 * @module model/RankSubmitResponse
 * @version 0.0.1
 */
class RankSubmitResponse {
    /**
     * Constructs a new <code>RankSubmitResponse</code>.
     * Describes the result of uploading an image resource
     * @alias module:model/RankSubmitResponse
     * @param responseCode {Number} The request response_code.
     */
    constructor(responseCode) { 
        
        RankSubmitResponse.initialize(this, responseCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, responseCode) { 
        obj['response_code'] = responseCode;
    }

    /**
     * Constructs a <code>RankSubmitResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RankSubmitResponse} obj Optional instance to populate.
     * @return {module:model/RankSubmitResponse} The populated <code>RankSubmitResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RankSubmitResponse();

            if (data.hasOwnProperty('error_message')) {
                obj['error_message'] = ApiClient.convertToType(data['error_message'], 'String');
            }
            if (data.hasOwnProperty('response_code')) {
                obj['response_code'] = ApiClient.convertToType(data['response_code'], 'Number');
            }
            if (data.hasOwnProperty('tasks')) {
                obj['tasks'] = ApiClient.convertToType(data['tasks'], [KeywordTask]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RankSubmitResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RankSubmitResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RankSubmitResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['error_message'] && !(typeof data['error_message'] === 'string' || data['error_message'] instanceof String)) {
            throw new Error("Expected the field `error_message` to be a primitive type in the JSON string but got " + data['error_message']);
        }
        if (data['tasks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tasks'])) {
                throw new Error("Expected the field `tasks` to be an array in the JSON data but got " + data['tasks']);
            }
            // validate the optional field `tasks` (array)
            for (const item of data['tasks']) {
                KeywordTask.validateJSON(item);
            };
        }

        return true;
    }


}

RankSubmitResponse.RequiredProperties = ["response_code"];

/**
 * @member {String} error_message
 */
RankSubmitResponse.prototype['error_message'] = undefined;

/**
 * The request response_code.
 * @member {Number} response_code
 */
RankSubmitResponse.prototype['response_code'] = undefined;

/**
 * @member {Array.<module:model/KeywordTask>} tasks
 */
RankSubmitResponse.prototype['tasks'] = undefined;






export default RankSubmitResponse;

