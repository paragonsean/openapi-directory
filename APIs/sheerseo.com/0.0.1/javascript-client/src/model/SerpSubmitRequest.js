/**
 * SheerSEO API
 * Sheerseo API has 2 stages:<br>First stage - initiating the task: You fill in your task and receive in return the task id. <br>Second stage - collecting the results: send a request containing the task ids you got at the first stage and collecting the results.
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KeywordJobSerp from './KeywordJobSerp';

/**
 * The SerpSubmitRequest model module.
 * @module model/SerpSubmitRequest
 * @version 0.0.1
 */
class SerpSubmitRequest {
    /**
     * Constructs a new <code>SerpSubmitRequest</code>.
     * @alias module:model/SerpSubmitRequest
     * @param tasks {Array.<module:model/KeywordJobSerp>} The keyword SERP jobs. Maximum 1000 jobs per a single request.
     */
    constructor(tasks) { 
        
        SerpSubmitRequest.initialize(this, tasks);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tasks) { 
        obj['tasks'] = tasks;
    }

    /**
     * Constructs a <code>SerpSubmitRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SerpSubmitRequest} obj Optional instance to populate.
     * @return {module:model/SerpSubmitRequest} The populated <code>SerpSubmitRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SerpSubmitRequest();

            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'String');
            }
            if (data.hasOwnProperty('tasks')) {
                obj['tasks'] = ApiClient.convertToType(data['tasks'], [KeywordJobSerp]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SerpSubmitRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SerpSubmitRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SerpSubmitRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['priority'] && !(typeof data['priority'] === 'string' || data['priority'] instanceof String)) {
            throw new Error("Expected the field `priority` to be a primitive type in the JSON string but got " + data['priority']);
        }
        if (data['tasks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tasks'])) {
                throw new Error("Expected the field `tasks` to be an array in the JSON data but got " + data['tasks']);
            }
            // validate the optional field `tasks` (array)
            for (const item of data['tasks']) {
                KeywordJobSerp.validateJSON(item);
            };
        }

        return true;
    }


}

SerpSubmitRequest.RequiredProperties = ["tasks"];

/**
 * The priority for the keyword jobs submitted. High priority costs double credits. Default value is low.
 * @member {module:model/SerpSubmitRequest.PriorityEnum} priority
 */
SerpSubmitRequest.prototype['priority'] = undefined;

/**
 * The keyword SERP jobs. Maximum 1000 jobs per a single request.
 * @member {Array.<module:model/KeywordJobSerp>} tasks
 */
SerpSubmitRequest.prototype['tasks'] = undefined;





/**
 * Allowed values for the <code>priority</code> property.
 * @enum {String}
 * @readonly
 */
SerpSubmitRequest['PriorityEnum'] = {

    /**
     * value: "low"
     * @const
     */
    "low": "low",

    /**
     * value: "high"
     * @const
     */
    "high": "high"
};



export default SerpSubmitRequest;

