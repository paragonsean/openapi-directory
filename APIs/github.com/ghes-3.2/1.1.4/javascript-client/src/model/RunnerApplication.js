/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RunnerApplication model module.
 * @module model/RunnerApplication
 * @version 1.1.4
 */
class RunnerApplication {
    /**
     * Constructs a new <code>RunnerApplication</code>.
     * Runner Application
     * @alias module:model/RunnerApplication
     * @param architecture {String} 
     * @param downloadUrl {String} 
     * @param filename {String} 
     * @param os {String} 
     */
    constructor(architecture, downloadUrl, filename, os) { 
        
        RunnerApplication.initialize(this, architecture, downloadUrl, filename, os);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, architecture, downloadUrl, filename, os) { 
        obj['architecture'] = architecture;
        obj['download_url'] = downloadUrl;
        obj['filename'] = filename;
        obj['os'] = os;
    }

    /**
     * Constructs a <code>RunnerApplication</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RunnerApplication} obj Optional instance to populate.
     * @return {module:model/RunnerApplication} The populated <code>RunnerApplication</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RunnerApplication();

            if (data.hasOwnProperty('architecture')) {
                obj['architecture'] = ApiClient.convertToType(data['architecture'], 'String');
            }
            if (data.hasOwnProperty('download_url')) {
                obj['download_url'] = ApiClient.convertToType(data['download_url'], 'String');
            }
            if (data.hasOwnProperty('filename')) {
                obj['filename'] = ApiClient.convertToType(data['filename'], 'String');
            }
            if (data.hasOwnProperty('os')) {
                obj['os'] = ApiClient.convertToType(data['os'], 'String');
            }
            if (data.hasOwnProperty('sha256_checksum')) {
                obj['sha256_checksum'] = ApiClient.convertToType(data['sha256_checksum'], 'String');
            }
            if (data.hasOwnProperty('temp_download_token')) {
                obj['temp_download_token'] = ApiClient.convertToType(data['temp_download_token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RunnerApplication</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RunnerApplication</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RunnerApplication.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['architecture'] && !(typeof data['architecture'] === 'string' || data['architecture'] instanceof String)) {
            throw new Error("Expected the field `architecture` to be a primitive type in the JSON string but got " + data['architecture']);
        }
        // ensure the json data is a string
        if (data['download_url'] && !(typeof data['download_url'] === 'string' || data['download_url'] instanceof String)) {
            throw new Error("Expected the field `download_url` to be a primitive type in the JSON string but got " + data['download_url']);
        }
        // ensure the json data is a string
        if (data['filename'] && !(typeof data['filename'] === 'string' || data['filename'] instanceof String)) {
            throw new Error("Expected the field `filename` to be a primitive type in the JSON string but got " + data['filename']);
        }
        // ensure the json data is a string
        if (data['os'] && !(typeof data['os'] === 'string' || data['os'] instanceof String)) {
            throw new Error("Expected the field `os` to be a primitive type in the JSON string but got " + data['os']);
        }
        // ensure the json data is a string
        if (data['sha256_checksum'] && !(typeof data['sha256_checksum'] === 'string' || data['sha256_checksum'] instanceof String)) {
            throw new Error("Expected the field `sha256_checksum` to be a primitive type in the JSON string but got " + data['sha256_checksum']);
        }
        // ensure the json data is a string
        if (data['temp_download_token'] && !(typeof data['temp_download_token'] === 'string' || data['temp_download_token'] instanceof String)) {
            throw new Error("Expected the field `temp_download_token` to be a primitive type in the JSON string but got " + data['temp_download_token']);
        }

        return true;
    }


}

RunnerApplication.RequiredProperties = ["architecture", "download_url", "filename", "os"];

/**
 * @member {String} architecture
 */
RunnerApplication.prototype['architecture'] = undefined;

/**
 * @member {String} download_url
 */
RunnerApplication.prototype['download_url'] = undefined;

/**
 * @member {String} filename
 */
RunnerApplication.prototype['filename'] = undefined;

/**
 * @member {String} os
 */
RunnerApplication.prototype['os'] = undefined;

/**
 * @member {String} sha256_checksum
 */
RunnerApplication.prototype['sha256_checksum'] = undefined;

/**
 * A short lived bearer token used to download the runner, if needed.
 * @member {String} temp_download_token
 */
RunnerApplication.prototype['temp_download_token'] = undefined;






export default RunnerApplication;

