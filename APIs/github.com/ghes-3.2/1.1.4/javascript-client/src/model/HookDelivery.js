/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HookDeliveryRequest from './HookDeliveryRequest';
import HookDeliveryResponse from './HookDeliveryResponse';

/**
 * The HookDelivery model module.
 * @module model/HookDelivery
 * @version 1.1.4
 */
class HookDelivery {
    /**
     * Constructs a new <code>HookDelivery</code>.
     * Delivery made by a webhook.
     * @alias module:model/HookDelivery
     * @param action {String} The type of activity for the event that triggered the delivery.
     * @param deliveredAt {Date} Time when the delivery was delivered.
     * @param duration {Number} Time spent delivering.
     * @param event {String} The event that triggered the delivery.
     * @param guid {String} Unique identifier for the event (shared with all deliveries for all webhooks that subscribe to this event).
     * @param id {Number} Unique identifier of the delivery.
     * @param installationId {Number} The id of the GitHub App installation associated with this event.
     * @param redelivery {Boolean} Whether the delivery is a redelivery.
     * @param repositoryId {Number} The id of the repository associated with this event.
     * @param request {module:model/HookDeliveryRequest} 
     * @param response {module:model/HookDeliveryResponse} 
     * @param status {String} Description of the status of the attempted delivery
     * @param statusCode {Number} Status code received when delivery was made.
     */
    constructor(action, deliveredAt, duration, event, guid, id, installationId, redelivery, repositoryId, request, response, status, statusCode) { 
        
        HookDelivery.initialize(this, action, deliveredAt, duration, event, guid, id, installationId, redelivery, repositoryId, request, response, status, statusCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, action, deliveredAt, duration, event, guid, id, installationId, redelivery, repositoryId, request, response, status, statusCode) { 
        obj['action'] = action;
        obj['delivered_at'] = deliveredAt;
        obj['duration'] = duration;
        obj['event'] = event;
        obj['guid'] = guid;
        obj['id'] = id;
        obj['installation_id'] = installationId;
        obj['redelivery'] = redelivery;
        obj['repository_id'] = repositoryId;
        obj['request'] = request;
        obj['response'] = response;
        obj['status'] = status;
        obj['status_code'] = statusCode;
    }

    /**
     * Constructs a <code>HookDelivery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HookDelivery} obj Optional instance to populate.
     * @return {module:model/HookDelivery} The populated <code>HookDelivery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HookDelivery();

            if (data.hasOwnProperty('action')) {
                obj['action'] = ApiClient.convertToType(data['action'], 'String');
            }
            if (data.hasOwnProperty('delivered_at')) {
                obj['delivered_at'] = ApiClient.convertToType(data['delivered_at'], 'Date');
            }
            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'Number');
            }
            if (data.hasOwnProperty('event')) {
                obj['event'] = ApiClient.convertToType(data['event'], 'String');
            }
            if (data.hasOwnProperty('guid')) {
                obj['guid'] = ApiClient.convertToType(data['guid'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('installation_id')) {
                obj['installation_id'] = ApiClient.convertToType(data['installation_id'], 'Number');
            }
            if (data.hasOwnProperty('redelivery')) {
                obj['redelivery'] = ApiClient.convertToType(data['redelivery'], 'Boolean');
            }
            if (data.hasOwnProperty('repository_id')) {
                obj['repository_id'] = ApiClient.convertToType(data['repository_id'], 'Number');
            }
            if (data.hasOwnProperty('request')) {
                obj['request'] = HookDeliveryRequest.constructFromObject(data['request']);
            }
            if (data.hasOwnProperty('response')) {
                obj['response'] = HookDeliveryResponse.constructFromObject(data['response']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('status_code')) {
                obj['status_code'] = ApiClient.convertToType(data['status_code'], 'Number');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HookDelivery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HookDelivery</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of HookDelivery.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['action'] && !(typeof data['action'] === 'string' || data['action'] instanceof String)) {
            throw new Error("Expected the field `action` to be a primitive type in the JSON string but got " + data['action']);
        }
        // ensure the json data is a string
        if (data['event'] && !(typeof data['event'] === 'string' || data['event'] instanceof String)) {
            throw new Error("Expected the field `event` to be a primitive type in the JSON string but got " + data['event']);
        }
        // ensure the json data is a string
        if (data['guid'] && !(typeof data['guid'] === 'string' || data['guid'] instanceof String)) {
            throw new Error("Expected the field `guid` to be a primitive type in the JSON string but got " + data['guid']);
        }
        // validate the optional field `request`
        if (data['request']) { // data not null
          HookDeliveryRequest.validateJSON(data['request']);
        }
        // validate the optional field `response`
        if (data['response']) { // data not null
          HookDeliveryResponse.validateJSON(data['response']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

HookDelivery.RequiredProperties = ["action", "delivered_at", "duration", "event", "guid", "id", "installation_id", "redelivery", "repository_id", "request", "response", "status", "status_code"];

/**
 * The type of activity for the event that triggered the delivery.
 * @member {String} action
 */
HookDelivery.prototype['action'] = undefined;

/**
 * Time when the delivery was delivered.
 * @member {Date} delivered_at
 */
HookDelivery.prototype['delivered_at'] = undefined;

/**
 * Time spent delivering.
 * @member {Number} duration
 */
HookDelivery.prototype['duration'] = undefined;

/**
 * The event that triggered the delivery.
 * @member {String} event
 */
HookDelivery.prototype['event'] = undefined;

/**
 * Unique identifier for the event (shared with all deliveries for all webhooks that subscribe to this event).
 * @member {String} guid
 */
HookDelivery.prototype['guid'] = undefined;

/**
 * Unique identifier of the delivery.
 * @member {Number} id
 */
HookDelivery.prototype['id'] = undefined;

/**
 * The id of the GitHub App installation associated with this event.
 * @member {Number} installation_id
 */
HookDelivery.prototype['installation_id'] = undefined;

/**
 * Whether the delivery is a redelivery.
 * @member {Boolean} redelivery
 */
HookDelivery.prototype['redelivery'] = undefined;

/**
 * The id of the repository associated with this event.
 * @member {Number} repository_id
 */
HookDelivery.prototype['repository_id'] = undefined;

/**
 * @member {module:model/HookDeliveryRequest} request
 */
HookDelivery.prototype['request'] = undefined;

/**
 * @member {module:model/HookDeliveryResponse} response
 */
HookDelivery.prototype['response'] = undefined;

/**
 * Description of the status of the attempted delivery
 * @member {String} status
 */
HookDelivery.prototype['status'] = undefined;

/**
 * Status code received when delivery was made.
 * @member {Number} status_code
 */
HookDelivery.prototype['status_code'] = undefined;

/**
 * The URL target of the delivery.
 * @member {String} url
 */
HookDelivery.prototype['url'] = undefined;






export default HookDelivery;

