/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReposCreatePagesSiteRequestSource model module.
 * @module model/ReposCreatePagesSiteRequestSource
 * @version 1.1.4
 */
class ReposCreatePagesSiteRequestSource {
    /**
     * Constructs a new <code>ReposCreatePagesSiteRequestSource</code>.
     * The source branch and directory used to publish your Pages site.
     * @alias module:model/ReposCreatePagesSiteRequestSource
     * @param branch {String} The repository branch used to publish your site's source files.
     */
    constructor(branch) { 
        
        ReposCreatePagesSiteRequestSource.initialize(this, branch);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, branch) { 
        obj['branch'] = branch;
        obj['path'] = '/';
    }

    /**
     * Constructs a <code>ReposCreatePagesSiteRequestSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReposCreatePagesSiteRequestSource} obj Optional instance to populate.
     * @return {module:model/ReposCreatePagesSiteRequestSource} The populated <code>ReposCreatePagesSiteRequestSource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReposCreatePagesSiteRequestSource();

            if (data.hasOwnProperty('branch')) {
                obj['branch'] = ApiClient.convertToType(data['branch'], 'String');
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReposCreatePagesSiteRequestSource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReposCreatePagesSiteRequestSource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReposCreatePagesSiteRequestSource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['branch'] && !(typeof data['branch'] === 'string' || data['branch'] instanceof String)) {
            throw new Error("Expected the field `branch` to be a primitive type in the JSON string but got " + data['branch']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }

        return true;
    }


}

ReposCreatePagesSiteRequestSource.RequiredProperties = ["branch"];

/**
 * The repository branch used to publish your site's source files.
 * @member {String} branch
 */
ReposCreatePagesSiteRequestSource.prototype['branch'] = undefined;

/**
 * The repository directory that includes the source files for the Pages site. Allowed paths are `/` or `/docs`. Default: `/`
 * @member {module:model/ReposCreatePagesSiteRequestSource.PathEnum} path
 * @default '/'
 */
ReposCreatePagesSiteRequestSource.prototype['path'] = '/';





/**
 * Allowed values for the <code>path</code> property.
 * @enum {String}
 * @readonly
 */
ReposCreatePagesSiteRequestSource['PathEnum'] = {

    /**
     * value: "/"
     * @const
     */
    "SLASH": "/",

    /**
     * value: "/docs"
     * @const
     */
    "docs": "/docs"
};



export default ReposCreatePagesSiteRequestSource;

