/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CodeScanningAnalysisDeletion model module.
 * @module model/CodeScanningAnalysisDeletion
 * @version 1.1.4
 */
class CodeScanningAnalysisDeletion {
    /**
     * Constructs a new <code>CodeScanningAnalysisDeletion</code>.
     * Successful deletion of a code scanning analysis
     * @alias module:model/CodeScanningAnalysisDeletion
     * @param confirmDeleteUrl {String} Next deletable analysis in chain, with last analysis deletion confirmation
     * @param nextAnalysisUrl {String} Next deletable analysis in chain, without last analysis deletion confirmation
     */
    constructor(confirmDeleteUrl, nextAnalysisUrl) { 
        
        CodeScanningAnalysisDeletion.initialize(this, confirmDeleteUrl, nextAnalysisUrl);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, confirmDeleteUrl, nextAnalysisUrl) { 
        obj['confirm_delete_url'] = confirmDeleteUrl;
        obj['next_analysis_url'] = nextAnalysisUrl;
    }

    /**
     * Constructs a <code>CodeScanningAnalysisDeletion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CodeScanningAnalysisDeletion} obj Optional instance to populate.
     * @return {module:model/CodeScanningAnalysisDeletion} The populated <code>CodeScanningAnalysisDeletion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CodeScanningAnalysisDeletion();

            if (data.hasOwnProperty('confirm_delete_url')) {
                obj['confirm_delete_url'] = ApiClient.convertToType(data['confirm_delete_url'], 'String');
            }
            if (data.hasOwnProperty('next_analysis_url')) {
                obj['next_analysis_url'] = ApiClient.convertToType(data['next_analysis_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CodeScanningAnalysisDeletion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CodeScanningAnalysisDeletion</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CodeScanningAnalysisDeletion.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['confirm_delete_url'] && !(typeof data['confirm_delete_url'] === 'string' || data['confirm_delete_url'] instanceof String)) {
            throw new Error("Expected the field `confirm_delete_url` to be a primitive type in the JSON string but got " + data['confirm_delete_url']);
        }
        // ensure the json data is a string
        if (data['next_analysis_url'] && !(typeof data['next_analysis_url'] === 'string' || data['next_analysis_url'] instanceof String)) {
            throw new Error("Expected the field `next_analysis_url` to be a primitive type in the JSON string but got " + data['next_analysis_url']);
        }

        return true;
    }


}

CodeScanningAnalysisDeletion.RequiredProperties = ["confirm_delete_url", "next_analysis_url"];

/**
 * Next deletable analysis in chain, with last analysis deletion confirmation
 * @member {String} confirm_delete_url
 */
CodeScanningAnalysisDeletion.prototype['confirm_delete_url'] = undefined;

/**
 * Next deletable analysis in chain, without last analysis deletion confirmation
 * @member {String} next_analysis_url
 */
CodeScanningAnalysisDeletion.prototype['next_analysis_url'] = undefined;






export default CodeScanningAnalysisDeletion;

