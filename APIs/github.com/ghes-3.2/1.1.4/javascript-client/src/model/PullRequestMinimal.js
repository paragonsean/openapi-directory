/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PullRequestMinimalBase from './PullRequestMinimalBase';

/**
 * The PullRequestMinimal model module.
 * @module model/PullRequestMinimal
 * @version 1.1.4
 */
class PullRequestMinimal {
    /**
     * Constructs a new <code>PullRequestMinimal</code>.
     * @alias module:model/PullRequestMinimal
     * @param base {module:model/PullRequestMinimalBase} 
     * @param head {module:model/PullRequestMinimalBase} 
     * @param id {Number} 
     * @param number {Number} 
     * @param url {String} 
     */
    constructor(base, head, id, number, url) { 
        
        PullRequestMinimal.initialize(this, base, head, id, number, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, base, head, id, number, url) { 
        obj['base'] = base;
        obj['head'] = head;
        obj['id'] = id;
        obj['number'] = number;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>PullRequestMinimal</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PullRequestMinimal} obj Optional instance to populate.
     * @return {module:model/PullRequestMinimal} The populated <code>PullRequestMinimal</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PullRequestMinimal();

            if (data.hasOwnProperty('base')) {
                obj['base'] = PullRequestMinimalBase.constructFromObject(data['base']);
            }
            if (data.hasOwnProperty('head')) {
                obj['head'] = PullRequestMinimalBase.constructFromObject(data['head']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'Number');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PullRequestMinimal</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PullRequestMinimal</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PullRequestMinimal.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `base`
        if (data['base']) { // data not null
          PullRequestMinimalBase.validateJSON(data['base']);
        }
        // validate the optional field `head`
        if (data['head']) { // data not null
          PullRequestMinimalBase.validateJSON(data['head']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

PullRequestMinimal.RequiredProperties = ["base", "head", "id", "number", "url"];

/**
 * @member {module:model/PullRequestMinimalBase} base
 */
PullRequestMinimal.prototype['base'] = undefined;

/**
 * @member {module:model/PullRequestMinimalBase} head
 */
PullRequestMinimal.prototype['head'] = undefined;

/**
 * @member {Number} id
 */
PullRequestMinimal.prototype['id'] = undefined;

/**
 * @member {Number} number
 */
PullRequestMinimal.prototype['number'] = undefined;

/**
 * @member {String} url
 */
PullRequestMinimal.prototype['url'] = undefined;






export default PullRequestMinimal;

