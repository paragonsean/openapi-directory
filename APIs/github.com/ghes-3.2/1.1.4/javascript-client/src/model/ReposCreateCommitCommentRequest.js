/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReposCreateCommitCommentRequest model module.
 * @module model/ReposCreateCommitCommentRequest
 * @version 1.1.4
 */
class ReposCreateCommitCommentRequest {
    /**
     * Constructs a new <code>ReposCreateCommitCommentRequest</code>.
     * @alias module:model/ReposCreateCommitCommentRequest
     * @param body {String} The contents of the comment.
     */
    constructor(body) { 
        
        ReposCreateCommitCommentRequest.initialize(this, body);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, body) { 
        obj['body'] = body;
    }

    /**
     * Constructs a <code>ReposCreateCommitCommentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReposCreateCommitCommentRequest} obj Optional instance to populate.
     * @return {module:model/ReposCreateCommitCommentRequest} The populated <code>ReposCreateCommitCommentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReposCreateCommitCommentRequest();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('line')) {
                obj['line'] = ApiClient.convertToType(data['line'], 'Number');
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('position')) {
                obj['position'] = ApiClient.convertToType(data['position'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReposCreateCommitCommentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReposCreateCommitCommentRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReposCreateCommitCommentRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }

        return true;
    }


}

ReposCreateCommitCommentRequest.RequiredProperties = ["body"];

/**
 * The contents of the comment.
 * @member {String} body
 */
ReposCreateCommitCommentRequest.prototype['body'] = undefined;

/**
 * **Deprecated**. Use **position** parameter instead. Line number in the file to comment on.
 * @member {Number} line
 */
ReposCreateCommitCommentRequest.prototype['line'] = undefined;

/**
 * Relative path of the file to comment on.
 * @member {String} path
 */
ReposCreateCommitCommentRequest.prototype['path'] = undefined;

/**
 * Line index in the diff to comment on.
 * @member {Number} position
 */
ReposCreateCommitCommentRequest.prototype['position'] = undefined;






export default ReposCreateCommitCommentRequest;

