/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import StatusCheckPolicyChecksInner from './StatusCheckPolicyChecksInner';

/**
 * The StatusCheckPolicy model module.
 * @module model/StatusCheckPolicy
 * @version 1.1.4
 */
class StatusCheckPolicy {
    /**
     * Constructs a new <code>StatusCheckPolicy</code>.
     * Status Check Policy
     * @alias module:model/StatusCheckPolicy
     * @param checks {Array.<module:model/StatusCheckPolicyChecksInner>} 
     * @param contexts {Array.<String>} 
     * @param contextsUrl {String} 
     * @param strict {Boolean} 
     * @param url {String} 
     */
    constructor(checks, contexts, contextsUrl, strict, url) { 
        
        StatusCheckPolicy.initialize(this, checks, contexts, contextsUrl, strict, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, checks, contexts, contextsUrl, strict, url) { 
        obj['checks'] = checks;
        obj['contexts'] = contexts;
        obj['contexts_url'] = contextsUrl;
        obj['strict'] = strict;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>StatusCheckPolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StatusCheckPolicy} obj Optional instance to populate.
     * @return {module:model/StatusCheckPolicy} The populated <code>StatusCheckPolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StatusCheckPolicy();

            if (data.hasOwnProperty('checks')) {
                obj['checks'] = ApiClient.convertToType(data['checks'], [StatusCheckPolicyChecksInner]);
            }
            if (data.hasOwnProperty('contexts')) {
                obj['contexts'] = ApiClient.convertToType(data['contexts'], ['String']);
            }
            if (data.hasOwnProperty('contexts_url')) {
                obj['contexts_url'] = ApiClient.convertToType(data['contexts_url'], 'String');
            }
            if (data.hasOwnProperty('strict')) {
                obj['strict'] = ApiClient.convertToType(data['strict'], 'Boolean');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StatusCheckPolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StatusCheckPolicy</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StatusCheckPolicy.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['checks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checks'])) {
                throw new Error("Expected the field `checks` to be an array in the JSON data but got " + data['checks']);
            }
            // validate the optional field `checks` (array)
            for (const item of data['checks']) {
                StatusCheckPolicyChecksInner.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['contexts'])) {
            throw new Error("Expected the field `contexts` to be an array in the JSON data but got " + data['contexts']);
        }
        // ensure the json data is a string
        if (data['contexts_url'] && !(typeof data['contexts_url'] === 'string' || data['contexts_url'] instanceof String)) {
            throw new Error("Expected the field `contexts_url` to be a primitive type in the JSON string but got " + data['contexts_url']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

StatusCheckPolicy.RequiredProperties = ["checks", "contexts", "contexts_url", "strict", "url"];

/**
 * @member {Array.<module:model/StatusCheckPolicyChecksInner>} checks
 */
StatusCheckPolicy.prototype['checks'] = undefined;

/**
 * @member {Array.<String>} contexts
 */
StatusCheckPolicy.prototype['contexts'] = undefined;

/**
 * @member {String} contexts_url
 */
StatusCheckPolicy.prototype['contexts_url'] = undefined;

/**
 * @member {Boolean} strict
 */
StatusCheckPolicy.prototype['strict'] = undefined;

/**
 * @member {String} url
 */
StatusCheckPolicy.prototype['url'] = undefined;






export default StatusCheckPolicy;

