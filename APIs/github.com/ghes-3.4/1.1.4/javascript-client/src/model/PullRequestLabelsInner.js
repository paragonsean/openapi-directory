/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PullRequestLabelsInner model module.
 * @module model/PullRequestLabelsInner
 * @version 1.1.4
 */
class PullRequestLabelsInner {
    /**
     * Constructs a new <code>PullRequestLabelsInner</code>.
     * @alias module:model/PullRequestLabelsInner
     * @param color {String} 
     * @param _default {Boolean} 
     * @param description {String} 
     * @param id {Number} 
     * @param name {String} 
     * @param nodeId {String} 
     * @param url {String} 
     */
    constructor(color, _default, description, id, name, nodeId, url) { 
        
        PullRequestLabelsInner.initialize(this, color, _default, description, id, name, nodeId, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, color, _default, description, id, name, nodeId, url) { 
        obj['color'] = color;
        obj['default'] = _default;
        obj['description'] = description;
        obj['id'] = id;
        obj['name'] = name;
        obj['node_id'] = nodeId;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>PullRequestLabelsInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PullRequestLabelsInner} obj Optional instance to populate.
     * @return {module:model/PullRequestLabelsInner} The populated <code>PullRequestLabelsInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PullRequestLabelsInner();

            if (data.hasOwnProperty('color')) {
                obj['color'] = ApiClient.convertToType(data['color'], 'String');
            }
            if (data.hasOwnProperty('default')) {
                obj['default'] = ApiClient.convertToType(data['default'], 'Boolean');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('node_id')) {
                obj['node_id'] = ApiClient.convertToType(data['node_id'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PullRequestLabelsInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PullRequestLabelsInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PullRequestLabelsInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['color'] && !(typeof data['color'] === 'string' || data['color'] instanceof String)) {
            throw new Error("Expected the field `color` to be a primitive type in the JSON string but got " + data['color']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['node_id'] && !(typeof data['node_id'] === 'string' || data['node_id'] instanceof String)) {
            throw new Error("Expected the field `node_id` to be a primitive type in the JSON string but got " + data['node_id']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

PullRequestLabelsInner.RequiredProperties = ["color", "default", "description", "id", "name", "node_id", "url"];

/**
 * @member {String} color
 */
PullRequestLabelsInner.prototype['color'] = undefined;

/**
 * @member {Boolean} default
 */
PullRequestLabelsInner.prototype['default'] = undefined;

/**
 * @member {String} description
 */
PullRequestLabelsInner.prototype['description'] = undefined;

/**
 * @member {Number} id
 */
PullRequestLabelsInner.prototype['id'] = undefined;

/**
 * @member {String} name
 */
PullRequestLabelsInner.prototype['name'] = undefined;

/**
 * @member {String} node_id
 */
PullRequestLabelsInner.prototype['node_id'] = undefined;

/**
 * @member {String} url
 */
PullRequestLabelsInner.prototype['url'] = undefined;






export default PullRequestLabelsInner;

