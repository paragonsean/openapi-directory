/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DependabotPublicKey model module.
 * @module model/DependabotPublicKey
 * @version 1.1.4
 */
class DependabotPublicKey {
    /**
     * Constructs a new <code>DependabotPublicKey</code>.
     * The public key used for setting Dependabot Secrets.
     * @alias module:model/DependabotPublicKey
     * @param key {String} The Base64 encoded public key.
     * @param keyId {String} The identifier for the key.
     */
    constructor(key, keyId) { 
        
        DependabotPublicKey.initialize(this, key, keyId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, key, keyId) { 
        obj['key'] = key;
        obj['key_id'] = keyId;
    }

    /**
     * Constructs a <code>DependabotPublicKey</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DependabotPublicKey} obj Optional instance to populate.
     * @return {module:model/DependabotPublicKey} The populated <code>DependabotPublicKey</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DependabotPublicKey();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('key_id')) {
                obj['key_id'] = ApiClient.convertToType(data['key_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DependabotPublicKey</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DependabotPublicKey</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DependabotPublicKey.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['key_id'] && !(typeof data['key_id'] === 'string' || data['key_id'] instanceof String)) {
            throw new Error("Expected the field `key_id` to be a primitive type in the JSON string but got " + data['key_id']);
        }

        return true;
    }


}

DependabotPublicKey.RequiredProperties = ["key", "key_id"];

/**
 * The Base64 encoded public key.
 * @member {String} key
 */
DependabotPublicKey.prototype['key'] = undefined;

/**
 * The identifier for the key.
 * @member {String} key_id
 */
DependabotPublicKey.prototype['key_id'] = undefined;






export default DependabotPublicKey;

