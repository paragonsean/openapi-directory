/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IssuesCreateMilestoneRequest model module.
 * @module model/IssuesCreateMilestoneRequest
 * @version 1.1.4
 */
class IssuesCreateMilestoneRequest {
    /**
     * Constructs a new <code>IssuesCreateMilestoneRequest</code>.
     * @alias module:model/IssuesCreateMilestoneRequest
     * @param title {String} The title of the milestone.
     */
    constructor(title) { 
        
        IssuesCreateMilestoneRequest.initialize(this, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, title) { 
        obj['state'] = 'open';
        obj['title'] = title;
    }

    /**
     * Constructs a <code>IssuesCreateMilestoneRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IssuesCreateMilestoneRequest} obj Optional instance to populate.
     * @return {module:model/IssuesCreateMilestoneRequest} The populated <code>IssuesCreateMilestoneRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IssuesCreateMilestoneRequest();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('due_on')) {
                obj['due_on'] = ApiClient.convertToType(data['due_on'], 'Date');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IssuesCreateMilestoneRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IssuesCreateMilestoneRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IssuesCreateMilestoneRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

IssuesCreateMilestoneRequest.RequiredProperties = ["title"];

/**
 * A description of the milestone.
 * @member {String} description
 */
IssuesCreateMilestoneRequest.prototype['description'] = undefined;

/**
 * The milestone due date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.
 * @member {Date} due_on
 */
IssuesCreateMilestoneRequest.prototype['due_on'] = undefined;

/**
 * The state of the milestone. Either `open` or `closed`.
 * @member {module:model/IssuesCreateMilestoneRequest.StateEnum} state
 * @default 'open'
 */
IssuesCreateMilestoneRequest.prototype['state'] = 'open';

/**
 * The title of the milestone.
 * @member {String} title
 */
IssuesCreateMilestoneRequest.prototype['title'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
IssuesCreateMilestoneRequest['StateEnum'] = {

    /**
     * value: "open"
     * @const
     */
    "open": "open",

    /**
     * value: "closed"
     * @const
     */
    "closed": "closed"
};



export default IssuesCreateMilestoneRequest;

