/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GitCreateTreeRequestTreeInner model module.
 * @module model/GitCreateTreeRequestTreeInner
 * @version 1.1.4
 */
class GitCreateTreeRequestTreeInner {
    /**
     * Constructs a new <code>GitCreateTreeRequestTreeInner</code>.
     * @alias module:model/GitCreateTreeRequestTreeInner
     */
    constructor() { 
        
        GitCreateTreeRequestTreeInner.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GitCreateTreeRequestTreeInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GitCreateTreeRequestTreeInner} obj Optional instance to populate.
     * @return {module:model/GitCreateTreeRequestTreeInner} The populated <code>GitCreateTreeRequestTreeInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GitCreateTreeRequestTreeInner();

            if (data.hasOwnProperty('content')) {
                obj['content'] = ApiClient.convertToType(data['content'], 'String');
            }
            if (data.hasOwnProperty('mode')) {
                obj['mode'] = ApiClient.convertToType(data['mode'], 'String');
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('sha')) {
                obj['sha'] = ApiClient.convertToType(data['sha'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GitCreateTreeRequestTreeInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GitCreateTreeRequestTreeInner</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['content'] && !(typeof data['content'] === 'string' || data['content'] instanceof String)) {
            throw new Error("Expected the field `content` to be a primitive type in the JSON string but got " + data['content']);
        }
        // ensure the json data is a string
        if (data['mode'] && !(typeof data['mode'] === 'string' || data['mode'] instanceof String)) {
            throw new Error("Expected the field `mode` to be a primitive type in the JSON string but got " + data['mode']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }
        // ensure the json data is a string
        if (data['sha'] && !(typeof data['sha'] === 'string' || data['sha'] instanceof String)) {
            throw new Error("Expected the field `sha` to be a primitive type in the JSON string but got " + data['sha']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * The content you want this file to have. GitHub will write this blob out and use that SHA for this entry. Use either this, or `tree.sha`.      **Note:** Use either `tree.sha` or `content` to specify the contents of the entry. Using both `tree.sha` and `content` will return an error.
 * @member {String} content
 */
GitCreateTreeRequestTreeInner.prototype['content'] = undefined;

/**
 * The file mode; one of `100644` for file (blob), `100755` for executable (blob), `040000` for subdirectory (tree), `160000` for submodule (commit), or `120000` for a blob that specifies the path of a symlink.
 * @member {module:model/GitCreateTreeRequestTreeInner.ModeEnum} mode
 */
GitCreateTreeRequestTreeInner.prototype['mode'] = undefined;

/**
 * The file referenced in the tree.
 * @member {String} path
 */
GitCreateTreeRequestTreeInner.prototype['path'] = undefined;

/**
 * The SHA1 checksum ID of the object in the tree. Also called `tree.sha`. If the value is `null` then the file will be deleted.      **Note:** Use either `tree.sha` or `content` to specify the contents of the entry. Using both `tree.sha` and `content` will return an error.
 * @member {String} sha
 */
GitCreateTreeRequestTreeInner.prototype['sha'] = undefined;

/**
 * Either `blob`, `tree`, or `commit`.
 * @member {module:model/GitCreateTreeRequestTreeInner.TypeEnum} type
 */
GitCreateTreeRequestTreeInner.prototype['type'] = undefined;





/**
 * Allowed values for the <code>mode</code> property.
 * @enum {String}
 * @readonly
 */
GitCreateTreeRequestTreeInner['ModeEnum'] = {

    /**
     * value: "100644"
     * @const
     */
    "100644": "100644",

    /**
     * value: "100755"
     * @const
     */
    "100755": "100755",

    /**
     * value: "040000"
     * @const
     */
    "040000": "040000",

    /**
     * value: "160000"
     * @const
     */
    "160000": "160000",

    /**
     * value: "120000"
     * @const
     */
    "120000": "120000"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
GitCreateTreeRequestTreeInner['TypeEnum'] = {

    /**
     * value: "blob"
     * @const
     */
    "blob": "blob",

    /**
     * value: "tree"
     * @const
     */
    "tree": "tree",

    /**
     * value: "commit"
     * @const
     */
    "commit": "commit"
};



export default GitCreateTreeRequestTreeInner;

