/*
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.net.URI;
import java.util.Arrays;
import org.openapitools.jackson.nullable.JsonNullable;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * CodeScanningSarifsStatus
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T10:07:04.504978-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class CodeScanningSarifsStatus {
  public static final String SERIALIZED_NAME_ANALYSES_URL = "analyses_url";
  @SerializedName(SERIALIZED_NAME_ANALYSES_URL)
  private URI analysesUrl;

  /**
   * &#x60;pending&#x60; files have not yet been processed, while &#x60;complete&#x60; means all results in the SARIF have been stored.
   */
  @JsonAdapter(ProcessingStatusEnum.Adapter.class)
  public enum ProcessingStatusEnum {
    PENDING("pending"),
    
    COMPLETE("complete");

    private String value;

    ProcessingStatusEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static ProcessingStatusEnum fromValue(String value) {
      for (ProcessingStatusEnum b : ProcessingStatusEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<ProcessingStatusEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final ProcessingStatusEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public ProcessingStatusEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return ProcessingStatusEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      ProcessingStatusEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_PROCESSING_STATUS = "processing_status";
  @SerializedName(SERIALIZED_NAME_PROCESSING_STATUS)
  private ProcessingStatusEnum processingStatus;

  public CodeScanningSarifsStatus() {
  }

  public CodeScanningSarifsStatus(
     URI analysesUrl
  ) {
    this();
    this.analysesUrl = analysesUrl;
  }

  /**
   * The REST API URL for getting the analyses associated with the upload.
   * @return analysesUrl
   */
  @javax.annotation.Nullable
  public URI getAnalysesUrl() {
    return analysesUrl;
  }



  public CodeScanningSarifsStatus processingStatus(ProcessingStatusEnum processingStatus) {
    this.processingStatus = processingStatus;
    return this;
  }

  /**
   * &#x60;pending&#x60; files have not yet been processed, while &#x60;complete&#x60; means all results in the SARIF have been stored.
   * @return processingStatus
   */
  @javax.annotation.Nullable
  public ProcessingStatusEnum getProcessingStatus() {
    return processingStatus;
  }

  public void setProcessingStatus(ProcessingStatusEnum processingStatus) {
    this.processingStatus = processingStatus;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CodeScanningSarifsStatus codeScanningSarifsStatus = (CodeScanningSarifsStatus) o;
    return Objects.equals(this.analysesUrl, codeScanningSarifsStatus.analysesUrl) &&
        Objects.equals(this.processingStatus, codeScanningSarifsStatus.processingStatus);
  }

  private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
    return a == b || (a != null && b != null && a.isPresent() && b.isPresent() && Objects.deepEquals(a.get(), b.get()));
  }

  @Override
  public int hashCode() {
    return Objects.hash(analysesUrl, processingStatus);
  }

  private static <T> int hashCodeNullable(JsonNullable<T> a) {
    if (a == null) {
      return 1;
    }
    return a.isPresent() ? Arrays.deepHashCode(new Object[]{a.get()}) : 31;
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class CodeScanningSarifsStatus {\n");
    sb.append("    analysesUrl: ").append(toIndentedString(analysesUrl)).append("\n");
    sb.append("    processingStatus: ").append(toIndentedString(processingStatus)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("analyses_url");
    openapiFields.add("processing_status");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to CodeScanningSarifsStatus
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!CodeScanningSarifsStatus.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in CodeScanningSarifsStatus is not found in the empty JSON string", CodeScanningSarifsStatus.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!CodeScanningSarifsStatus.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `CodeScanningSarifsStatus` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("analyses_url") != null && !jsonObj.get("analyses_url").isJsonNull()) && !jsonObj.get("analyses_url").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `analyses_url` to be a primitive type in the JSON string but got `%s`", jsonObj.get("analyses_url").toString()));
      }
      if ((jsonObj.get("processing_status") != null && !jsonObj.get("processing_status").isJsonNull()) && !jsonObj.get("processing_status").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `processing_status` to be a primitive type in the JSON string but got `%s`", jsonObj.get("processing_status").toString()));
      }
      // validate the optional field `processing_status`
      if (jsonObj.get("processing_status") != null && !jsonObj.get("processing_status").isJsonNull()) {
        ProcessingStatusEnum.validateJsonElement(jsonObj.get("processing_status"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!CodeScanningSarifsStatus.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'CodeScanningSarifsStatus' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<CodeScanningSarifsStatus> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(CodeScanningSarifsStatus.class));

       return (TypeAdapter<T>) new TypeAdapter<CodeScanningSarifsStatus>() {
           @Override
           public void write(JsonWriter out, CodeScanningSarifsStatus value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public CodeScanningSarifsStatus read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of CodeScanningSarifsStatus given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of CodeScanningSarifsStatus
   * @throws IOException if the JSON string is invalid with respect to CodeScanningSarifsStatus
   */
  public static CodeScanningSarifsStatus fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CodeScanningSarifsStatus.class);
  }

  /**
   * Convert an instance of CodeScanningSarifsStatus to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

