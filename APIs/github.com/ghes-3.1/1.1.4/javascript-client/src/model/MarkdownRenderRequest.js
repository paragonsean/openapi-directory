/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MarkdownRenderRequest model module.
 * @module model/MarkdownRenderRequest
 * @version 1.1.4
 */
class MarkdownRenderRequest {
    /**
     * Constructs a new <code>MarkdownRenderRequest</code>.
     * @alias module:model/MarkdownRenderRequest
     * @param text {String} The Markdown text to render in HTML.
     */
    constructor(text) { 
        
        MarkdownRenderRequest.initialize(this, text);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, text) { 
        obj['mode'] = 'markdown';
        obj['text'] = text;
    }

    /**
     * Constructs a <code>MarkdownRenderRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MarkdownRenderRequest} obj Optional instance to populate.
     * @return {module:model/MarkdownRenderRequest} The populated <code>MarkdownRenderRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MarkdownRenderRequest();

            if (data.hasOwnProperty('context')) {
                obj['context'] = ApiClient.convertToType(data['context'], 'String');
            }
            if (data.hasOwnProperty('mode')) {
                obj['mode'] = ApiClient.convertToType(data['mode'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MarkdownRenderRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MarkdownRenderRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MarkdownRenderRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['context'] && !(typeof data['context'] === 'string' || data['context'] instanceof String)) {
            throw new Error("Expected the field `context` to be a primitive type in the JSON string but got " + data['context']);
        }
        // ensure the json data is a string
        if (data['mode'] && !(typeof data['mode'] === 'string' || data['mode'] instanceof String)) {
            throw new Error("Expected the field `mode` to be a primitive type in the JSON string but got " + data['mode']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }

        return true;
    }


}

MarkdownRenderRequest.RequiredProperties = ["text"];

/**
 * The repository context to use when creating references in `gfm` mode.  For example, setting `context` to `octo-org/octo-repo` will change the text `#42` into an HTML link to issue 42 in the `octo-org/octo-repo` repository.
 * @member {String} context
 */
MarkdownRenderRequest.prototype['context'] = undefined;

/**
 * The rendering mode. Can be either `markdown` or `gfm`.
 * @member {module:model/MarkdownRenderRequest.ModeEnum} mode
 * @default 'markdown'
 */
MarkdownRenderRequest.prototype['mode'] = 'markdown';

/**
 * The Markdown text to render in HTML.
 * @member {String} text
 */
MarkdownRenderRequest.prototype['text'] = undefined;





/**
 * Allowed values for the <code>mode</code> property.
 * @enum {String}
 * @readonly
 */
MarkdownRenderRequest['ModeEnum'] = {

    /**
     * value: "markdown"
     * @const
     */
    "markdown": "markdown",

    /**
     * value: "gfm"
     * @const
     */
    "gfm": "gfm"
};



export default MarkdownRenderRequest;

