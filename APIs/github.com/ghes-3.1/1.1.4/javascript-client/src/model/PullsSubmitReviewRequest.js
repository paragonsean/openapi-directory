/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PullsSubmitReviewRequest model module.
 * @module model/PullsSubmitReviewRequest
 * @version 1.1.4
 */
class PullsSubmitReviewRequest {
    /**
     * Constructs a new <code>PullsSubmitReviewRequest</code>.
     * @alias module:model/PullsSubmitReviewRequest
     * @param event {module:model/PullsSubmitReviewRequest.EventEnum} The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. When you leave this blank, the API returns _HTTP 422 (Unrecognizable entity)_ and sets the review action state to `PENDING`, which means you will need to re-submit the pull request review using a review action.
     */
    constructor(event) { 
        
        PullsSubmitReviewRequest.initialize(this, event);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, event) { 
        obj['event'] = event;
    }

    /**
     * Constructs a <code>PullsSubmitReviewRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PullsSubmitReviewRequest} obj Optional instance to populate.
     * @return {module:model/PullsSubmitReviewRequest} The populated <code>PullsSubmitReviewRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PullsSubmitReviewRequest();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('event')) {
                obj['event'] = ApiClient.convertToType(data['event'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PullsSubmitReviewRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PullsSubmitReviewRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PullsSubmitReviewRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['event'] && !(typeof data['event'] === 'string' || data['event'] instanceof String)) {
            throw new Error("Expected the field `event` to be a primitive type in the JSON string but got " + data['event']);
        }

        return true;
    }


}

PullsSubmitReviewRequest.RequiredProperties = ["event"];

/**
 * The body text of the pull request review
 * @member {String} body
 */
PullsSubmitReviewRequest.prototype['body'] = undefined;

/**
 * The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. When you leave this blank, the API returns _HTTP 422 (Unrecognizable entity)_ and sets the review action state to `PENDING`, which means you will need to re-submit the pull request review using a review action.
 * @member {module:model/PullsSubmitReviewRequest.EventEnum} event
 */
PullsSubmitReviewRequest.prototype['event'] = undefined;





/**
 * Allowed values for the <code>event</code> property.
 * @enum {String}
 * @readonly
 */
PullsSubmitReviewRequest['EventEnum'] = {

    /**
     * value: "APPROVE"
     * @const
     */
    "APPROVE": "APPROVE",

    /**
     * value: "REQUEST_CHANGES"
     * @const
     */
    "REQUEST_CHANGES": "REQUEST_CHANGES",

    /**
     * value: "COMMENT"
     * @const
     */
    "COMMENT": "COMMENT"
};



export default PullsSubmitReviewRequest;

