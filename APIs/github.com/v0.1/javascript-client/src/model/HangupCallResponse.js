/**
 * Eqivo API
 * Eqivo OpenApi Specification
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The HangupCallResponse model module.
 * @module model/HangupCallResponse
 * @version v0.1
 */
class HangupCallResponse {
    /**
     * Constructs a new <code>HangupCallResponse</code>.
     * @alias module:model/HangupCallResponse
     * @param message {module:model/HangupCallResponse.MessageEnum} Response message
     * @param success {Boolean} Whether the request was successful or not
     */
    constructor(message, success) { 
        
        HangupCallResponse.initialize(this, message, success);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, message, success) { 
        obj['Message'] = message;
        obj['Success'] = success;
    }

    /**
     * Constructs a <code>HangupCallResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HangupCallResponse} obj Optional instance to populate.
     * @return {module:model/HangupCallResponse} The populated <code>HangupCallResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HangupCallResponse();

            if (data.hasOwnProperty('Message')) {
                obj['Message'] = ApiClient.convertToType(data['Message'], 'String');
            }
            if (data.hasOwnProperty('Success')) {
                obj['Success'] = ApiClient.convertToType(data['Success'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HangupCallResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HangupCallResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of HangupCallResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Message'] && !(typeof data['Message'] === 'string' || data['Message'] instanceof String)) {
            throw new Error("Expected the field `Message` to be a primitive type in the JSON string but got " + data['Message']);
        }

        return true;
    }


}

HangupCallResponse.RequiredProperties = ["Message", "Success"];

/**
 * Response message
 * @member {module:model/HangupCallResponse.MessageEnum} Message
 */
HangupCallResponse.prototype['Message'] = undefined;

/**
 * Whether the request was successful or not
 * @member {Boolean} Success
 */
HangupCallResponse.prototype['Success'] = undefined;





/**
 * Allowed values for the <code>Message</code> property.
 * @enum {String}
 * @readonly
 */
HangupCallResponse['MessageEnum'] = {

    /**
     * value: "Hangup Call Executed"
     * @const
     */
    "Hangup Call Executed": "Hangup Call Executed",

    /**
     * value: "CallUUID or RequestUUID Parameter must be present"
     * @const
     */
    "CallUUID or RequestUUID Parameter must be present": "CallUUID or RequestUUID Parameter must be present",

    /**
     * value: "Both CallUUID and RequestUUID Parameters cannot be present"
     * @const
     */
    "Both CallUUID and RequestUUID Parameters cannot be present": "Both CallUUID and RequestUUID Parameters cannot be present",

    /**
     * value: "Hangup Call Failed"
     * @const
     */
    "Hangup Call Failed": "Hangup Call Failed"
};



export default HangupCallResponse;

