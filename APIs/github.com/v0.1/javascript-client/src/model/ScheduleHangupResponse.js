/**
 * Eqivo API
 * Eqivo OpenApi Specification
 *
 * The version of the OpenAPI document: v0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ScheduleHangupResponse model module.
 * @module model/ScheduleHangupResponse
 * @version v0.1
 */
class ScheduleHangupResponse {
    /**
     * Constructs a new <code>ScheduleHangupResponse</code>.
     * @alias module:model/ScheduleHangupResponse
     * @param message {module:model/ScheduleHangupResponse.MessageEnum} Response message
     * @param schedHangupId {String} Unique identifier of the scheduled hangup request (UUIDv4)
     * @param success {Boolean} Whether the request was successful or not
     */
    constructor(message, schedHangupId, success) { 
        
        ScheduleHangupResponse.initialize(this, message, schedHangupId, success);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, message, schedHangupId, success) { 
        obj['Message'] = message;
        obj['SchedHangupId'] = schedHangupId;
        obj['Success'] = success;
    }

    /**
     * Constructs a <code>ScheduleHangupResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ScheduleHangupResponse} obj Optional instance to populate.
     * @return {module:model/ScheduleHangupResponse} The populated <code>ScheduleHangupResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ScheduleHangupResponse();

            if (data.hasOwnProperty('Message')) {
                obj['Message'] = ApiClient.convertToType(data['Message'], 'String');
            }
            if (data.hasOwnProperty('SchedHangupId')) {
                obj['SchedHangupId'] = ApiClient.convertToType(data['SchedHangupId'], 'String');
            }
            if (data.hasOwnProperty('Success')) {
                obj['Success'] = ApiClient.convertToType(data['Success'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ScheduleHangupResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ScheduleHangupResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ScheduleHangupResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['Message'] && !(typeof data['Message'] === 'string' || data['Message'] instanceof String)) {
            throw new Error("Expected the field `Message` to be a primitive type in the JSON string but got " + data['Message']);
        }
        // ensure the json data is a string
        if (data['SchedHangupId'] && !(typeof data['SchedHangupId'] === 'string' || data['SchedHangupId'] instanceof String)) {
            throw new Error("Expected the field `SchedHangupId` to be a primitive type in the JSON string but got " + data['SchedHangupId']);
        }

        return true;
    }


}

ScheduleHangupResponse.RequiredProperties = ["Message", "SchedHangupId", "Success"];

/**
 * Response message
 * @member {module:model/ScheduleHangupResponse.MessageEnum} Message
 */
ScheduleHangupResponse.prototype['Message'] = undefined;

/**
 * Unique identifier of the scheduled hangup request (UUIDv4)
 * @member {String} SchedHangupId
 */
ScheduleHangupResponse.prototype['SchedHangupId'] = undefined;

/**
 * Whether the request was successful or not
 * @member {Boolean} Success
 */
ScheduleHangupResponse.prototype['Success'] = undefined;





/**
 * Allowed values for the <code>Message</code> property.
 * @enum {String}
 * @readonly
 */
ScheduleHangupResponse['MessageEnum'] = {

    /**
     * value: "ScheduleHangup Executed"
     * @const
     */
    "ScheduleHangup Executed": "ScheduleHangup Executed",

    /**
     * value: "CallUUID Parameter must be present"
     * @const
     */
    "CallUUID Parameter must be present": "CallUUID Parameter must be present",

    /**
     * value: "Time Parameter must be present"
     * @const
     */
    "Time Parameter must be present": "Time Parameter must be present",

    /**
     * value: "Time Parameter must be > 0!"
     * @const
     */
    "Time Parameter must be &gt; 0!": "Time Parameter must be > 0!",

    /**
     * value: "ScheduleHangup Failed -- Call not found"
     * @const
     */
    "ScheduleHangup Failed -- Call not found": "ScheduleHangup Failed -- Call not found",

    /**
     * value: "ScheduleHangup Failed"
     * @const
     */
    "ScheduleHangup Failed": "ScheduleHangup Failed"
};



export default ScheduleHangupResponse;

