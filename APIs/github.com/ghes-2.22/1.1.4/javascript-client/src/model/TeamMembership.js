/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TeamMembership model module.
 * @module model/TeamMembership
 * @version 1.1.4
 */
class TeamMembership {
    /**
     * Constructs a new <code>TeamMembership</code>.
     * Team Membership
     * @alias module:model/TeamMembership
     * @param role {module:model/TeamMembership.RoleEnum} The role of the user in the team.
     * @param state {module:model/TeamMembership.StateEnum} The state of the user's membership in the team.
     * @param url {String} 
     */
    constructor(role, state, url) { 
        
        TeamMembership.initialize(this, role, state, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, role, state, url) { 
        obj['role'] = role || 'member';
        obj['state'] = state;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>TeamMembership</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TeamMembership} obj Optional instance to populate.
     * @return {module:model/TeamMembership} The populated <code>TeamMembership</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TeamMembership();

            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TeamMembership</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TeamMembership</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TeamMembership.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

TeamMembership.RequiredProperties = ["role", "state", "url"];

/**
 * The role of the user in the team.
 * @member {module:model/TeamMembership.RoleEnum} role
 * @default 'member'
 */
TeamMembership.prototype['role'] = 'member';

/**
 * The state of the user's membership in the team.
 * @member {module:model/TeamMembership.StateEnum} state
 */
TeamMembership.prototype['state'] = undefined;

/**
 * @member {String} url
 */
TeamMembership.prototype['url'] = undefined;





/**
 * Allowed values for the <code>role</code> property.
 * @enum {String}
 * @readonly
 */
TeamMembership['RoleEnum'] = {

    /**
     * value: "member"
     * @const
     */
    "member": "member",

    /**
     * value: "maintainer"
     * @const
     */
    "maintainer": "maintainer"
};


/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
TeamMembership['StateEnum'] = {

    /**
     * value: "active"
     * @const
     */
    "active": "active",

    /**
     * value: "pending"
     * @const
     */
    "pending": "pending"
};



export default TeamMembership;

