/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApiOverview model module.
 * @module model/ApiOverview
 * @version 1.1.4
 */
class ApiOverview {
    /**
     * Constructs a new <code>ApiOverview</code>.
     * Api Overview
     * @alias module:model/ApiOverview
     * @param verifiablePasswordAuthentication {Boolean} 
     */
    constructor(verifiablePasswordAuthentication) { 
        
        ApiOverview.initialize(this, verifiablePasswordAuthentication);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, verifiablePasswordAuthentication) { 
        obj['verifiable_password_authentication'] = verifiablePasswordAuthentication;
    }

    /**
     * Constructs a <code>ApiOverview</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiOverview} obj Optional instance to populate.
     * @return {module:model/ApiOverview} The populated <code>ApiOverview</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiOverview();

            if (data.hasOwnProperty('dependabot')) {
                obj['dependabot'] = ApiClient.convertToType(data['dependabot'], ['String']);
            }
            if (data.hasOwnProperty('installed_version')) {
                obj['installed_version'] = ApiClient.convertToType(data['installed_version'], 'String');
            }
            if (data.hasOwnProperty('packages')) {
                obj['packages'] = ApiClient.convertToType(data['packages'], ['String']);
            }
            if (data.hasOwnProperty('verifiable_password_authentication')) {
                obj['verifiable_password_authentication'] = ApiClient.convertToType(data['verifiable_password_authentication'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiOverview</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiOverview</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiOverview.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['dependabot'])) {
            throw new Error("Expected the field `dependabot` to be an array in the JSON data but got " + data['dependabot']);
        }
        // ensure the json data is a string
        if (data['installed_version'] && !(typeof data['installed_version'] === 'string' || data['installed_version'] instanceof String)) {
            throw new Error("Expected the field `installed_version` to be a primitive type in the JSON string but got " + data['installed_version']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['packages'])) {
            throw new Error("Expected the field `packages` to be an array in the JSON data but got " + data['packages']);
        }

        return true;
    }


}

ApiOverview.RequiredProperties = ["verifiable_password_authentication"];

/**
 * @member {Array.<String>} dependabot
 */
ApiOverview.prototype['dependabot'] = undefined;

/**
 * @member {String} installed_version
 */
ApiOverview.prototype['installed_version'] = undefined;

/**
 * @member {Array.<String>} packages
 */
ApiOverview.prototype['packages'] = undefined;

/**
 * @member {Boolean} verifiable_password_authentication
 */
ApiOverview.prototype['verifiable_password_authentication'] = undefined;






export default ApiOverview;

