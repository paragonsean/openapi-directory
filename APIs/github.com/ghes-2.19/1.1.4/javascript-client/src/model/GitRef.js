/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GitRefObject from './GitRefObject';

/**
 * The GitRef model module.
 * @module model/GitRef
 * @version 1.1.4
 */
class GitRef {
    /**
     * Constructs a new <code>GitRef</code>.
     * Git references within a repository
     * @alias module:model/GitRef
     * @param nodeId {String} 
     * @param object {module:model/GitRefObject} 
     * @param ref {String} 
     * @param url {String} 
     */
    constructor(nodeId, object, ref, url) { 
        
        GitRef.initialize(this, nodeId, object, ref, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, nodeId, object, ref, url) { 
        obj['node_id'] = nodeId;
        obj['object'] = object;
        obj['ref'] = ref;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>GitRef</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GitRef} obj Optional instance to populate.
     * @return {module:model/GitRef} The populated <code>GitRef</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GitRef();

            if (data.hasOwnProperty('node_id')) {
                obj['node_id'] = ApiClient.convertToType(data['node_id'], 'String');
            }
            if (data.hasOwnProperty('object')) {
                obj['object'] = GitRefObject.constructFromObject(data['object']);
            }
            if (data.hasOwnProperty('ref')) {
                obj['ref'] = ApiClient.convertToType(data['ref'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GitRef</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GitRef</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GitRef.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['node_id'] && !(typeof data['node_id'] === 'string' || data['node_id'] instanceof String)) {
            throw new Error("Expected the field `node_id` to be a primitive type in the JSON string but got " + data['node_id']);
        }
        // validate the optional field `object`
        if (data['object']) { // data not null
          GitRefObject.validateJSON(data['object']);
        }
        // ensure the json data is a string
        if (data['ref'] && !(typeof data['ref'] === 'string' || data['ref'] instanceof String)) {
            throw new Error("Expected the field `ref` to be a primitive type in the JSON string but got " + data['ref']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

GitRef.RequiredProperties = ["node_id", "object", "ref", "url"];

/**
 * @member {String} node_id
 */
GitRef.prototype['node_id'] = undefined;

/**
 * @member {module:model/GitRefObject} object
 */
GitRef.prototype['object'] = undefined;

/**
 * @member {String} ref
 */
GitRef.prototype['ref'] = undefined;

/**
 * @member {String} url
 */
GitRef.prototype['url'] = undefined;






export default GitRef;

