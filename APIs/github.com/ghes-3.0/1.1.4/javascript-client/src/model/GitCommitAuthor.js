/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GitCommitAuthor model module.
 * @module model/GitCommitAuthor
 * @version 1.1.4
 */
class GitCommitAuthor {
    /**
     * Constructs a new <code>GitCommitAuthor</code>.
     * Identifying information for the git-user
     * @alias module:model/GitCommitAuthor
     * @param date {Date} Timestamp of the commit
     * @param email {String} Git email address of the user
     * @param name {String} Name of the git user
     */
    constructor(date, email, name) { 
        
        GitCommitAuthor.initialize(this, date, email, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, date, email, name) { 
        obj['date'] = date;
        obj['email'] = email;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>GitCommitAuthor</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GitCommitAuthor} obj Optional instance to populate.
     * @return {module:model/GitCommitAuthor} The populated <code>GitCommitAuthor</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GitCommitAuthor();

            if (data.hasOwnProperty('date')) {
                obj['date'] = ApiClient.convertToType(data['date'], 'Date');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GitCommitAuthor</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GitCommitAuthor</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GitCommitAuthor.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

GitCommitAuthor.RequiredProperties = ["date", "email", "name"];

/**
 * Timestamp of the commit
 * @member {Date} date
 */
GitCommitAuthor.prototype['date'] = undefined;

/**
 * Git email address of the user
 * @member {String} email
 */
GitCommitAuthor.prototype['email'] = undefined;

/**
 * Name of the git user
 * @member {String} name
 */
GitCommitAuthor.prototype['name'] = undefined;






export default GitCommitAuthor;

