/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CommitSearchResultItemCommit from './CommitSearchResultItemCommit';
import CommitSearchResultItemParentsInner from './CommitSearchResultItemParentsInner';
import MinimalRepository from './MinimalRepository';
import NullableGitUser from './NullableGitUser';
import NullableSimpleUser from './NullableSimpleUser';
import SearchResultTextMatchesInner from './SearchResultTextMatchesInner';

/**
 * The CommitSearchResultItem model module.
 * @module model/CommitSearchResultItem
 * @version 1.1.4
 */
class CommitSearchResultItem {
    /**
     * Constructs a new <code>CommitSearchResultItem</code>.
     * Commit Search Result Item
     * @alias module:model/CommitSearchResultItem
     * @param author {module:model/NullableSimpleUser} 
     * @param commentsUrl {String} 
     * @param commit {module:model/CommitSearchResultItemCommit} 
     * @param committer {module:model/NullableGitUser} 
     * @param htmlUrl {String} 
     * @param nodeId {String} 
     * @param parents {Array.<module:model/CommitSearchResultItemParentsInner>} 
     * @param repository {module:model/MinimalRepository} 
     * @param score {Number} 
     * @param sha {String} 
     * @param url {String} 
     */
    constructor(author, commentsUrl, commit, committer, htmlUrl, nodeId, parents, repository, score, sha, url) { 
        
        CommitSearchResultItem.initialize(this, author, commentsUrl, commit, committer, htmlUrl, nodeId, parents, repository, score, sha, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, author, commentsUrl, commit, committer, htmlUrl, nodeId, parents, repository, score, sha, url) { 
        obj['author'] = author;
        obj['comments_url'] = commentsUrl;
        obj['commit'] = commit;
        obj['committer'] = committer;
        obj['html_url'] = htmlUrl;
        obj['node_id'] = nodeId;
        obj['parents'] = parents;
        obj['repository'] = repository;
        obj['score'] = score;
        obj['sha'] = sha;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>CommitSearchResultItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CommitSearchResultItem} obj Optional instance to populate.
     * @return {module:model/CommitSearchResultItem} The populated <code>CommitSearchResultItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CommitSearchResultItem();

            if (data.hasOwnProperty('author')) {
                obj['author'] = NullableSimpleUser.constructFromObject(data['author']);
            }
            if (data.hasOwnProperty('comments_url')) {
                obj['comments_url'] = ApiClient.convertToType(data['comments_url'], 'String');
            }
            if (data.hasOwnProperty('commit')) {
                obj['commit'] = CommitSearchResultItemCommit.constructFromObject(data['commit']);
            }
            if (data.hasOwnProperty('committer')) {
                obj['committer'] = NullableGitUser.constructFromObject(data['committer']);
            }
            if (data.hasOwnProperty('html_url')) {
                obj['html_url'] = ApiClient.convertToType(data['html_url'], 'String');
            }
            if (data.hasOwnProperty('node_id')) {
                obj['node_id'] = ApiClient.convertToType(data['node_id'], 'String');
            }
            if (data.hasOwnProperty('parents')) {
                obj['parents'] = ApiClient.convertToType(data['parents'], [CommitSearchResultItemParentsInner]);
            }
            if (data.hasOwnProperty('repository')) {
                obj['repository'] = MinimalRepository.constructFromObject(data['repository']);
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'Number');
            }
            if (data.hasOwnProperty('sha')) {
                obj['sha'] = ApiClient.convertToType(data['sha'], 'String');
            }
            if (data.hasOwnProperty('text_matches')) {
                obj['text_matches'] = ApiClient.convertToType(data['text_matches'], [SearchResultTextMatchesInner]);
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CommitSearchResultItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CommitSearchResultItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CommitSearchResultItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `author`
        if (data['author']) { // data not null
          NullableSimpleUser.validateJSON(data['author']);
        }
        // ensure the json data is a string
        if (data['comments_url'] && !(typeof data['comments_url'] === 'string' || data['comments_url'] instanceof String)) {
            throw new Error("Expected the field `comments_url` to be a primitive type in the JSON string but got " + data['comments_url']);
        }
        // validate the optional field `commit`
        if (data['commit']) { // data not null
          CommitSearchResultItemCommit.validateJSON(data['commit']);
        }
        // validate the optional field `committer`
        if (data['committer']) { // data not null
          NullableGitUser.validateJSON(data['committer']);
        }
        // ensure the json data is a string
        if (data['html_url'] && !(typeof data['html_url'] === 'string' || data['html_url'] instanceof String)) {
            throw new Error("Expected the field `html_url` to be a primitive type in the JSON string but got " + data['html_url']);
        }
        // ensure the json data is a string
        if (data['node_id'] && !(typeof data['node_id'] === 'string' || data['node_id'] instanceof String)) {
            throw new Error("Expected the field `node_id` to be a primitive type in the JSON string but got " + data['node_id']);
        }
        if (data['parents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['parents'])) {
                throw new Error("Expected the field `parents` to be an array in the JSON data but got " + data['parents']);
            }
            // validate the optional field `parents` (array)
            for (const item of data['parents']) {
                CommitSearchResultItemParentsInner.validateJSON(item);
            };
        }
        // validate the optional field `repository`
        if (data['repository']) { // data not null
          MinimalRepository.validateJSON(data['repository']);
        }
        // ensure the json data is a string
        if (data['sha'] && !(typeof data['sha'] === 'string' || data['sha'] instanceof String)) {
            throw new Error("Expected the field `sha` to be a primitive type in the JSON string but got " + data['sha']);
        }
        if (data['text_matches']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['text_matches'])) {
                throw new Error("Expected the field `text_matches` to be an array in the JSON data but got " + data['text_matches']);
            }
            // validate the optional field `text_matches` (array)
            for (const item of data['text_matches']) {
                SearchResultTextMatchesInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

CommitSearchResultItem.RequiredProperties = ["author", "comments_url", "commit", "committer", "html_url", "node_id", "parents", "repository", "score", "sha", "url"];

/**
 * @member {module:model/NullableSimpleUser} author
 */
CommitSearchResultItem.prototype['author'] = undefined;

/**
 * @member {String} comments_url
 */
CommitSearchResultItem.prototype['comments_url'] = undefined;

/**
 * @member {module:model/CommitSearchResultItemCommit} commit
 */
CommitSearchResultItem.prototype['commit'] = undefined;

/**
 * @member {module:model/NullableGitUser} committer
 */
CommitSearchResultItem.prototype['committer'] = undefined;

/**
 * @member {String} html_url
 */
CommitSearchResultItem.prototype['html_url'] = undefined;

/**
 * @member {String} node_id
 */
CommitSearchResultItem.prototype['node_id'] = undefined;

/**
 * @member {Array.<module:model/CommitSearchResultItemParentsInner>} parents
 */
CommitSearchResultItem.prototype['parents'] = undefined;

/**
 * @member {module:model/MinimalRepository} repository
 */
CommitSearchResultItem.prototype['repository'] = undefined;

/**
 * @member {Number} score
 */
CommitSearchResultItem.prototype['score'] = undefined;

/**
 * @member {String} sha
 */
CommitSearchResultItem.prototype['sha'] = undefined;

/**
 * @member {Array.<module:model/SearchResultTextMatchesInner>} text_matches
 */
CommitSearchResultItem.prototype['text_matches'] = undefined;

/**
 * @member {String} url
 */
CommitSearchResultItem.prototype['url'] = undefined;






export default CommitSearchResultItem;

