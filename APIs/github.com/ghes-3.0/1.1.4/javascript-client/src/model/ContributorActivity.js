/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContributorActivityWeeksInner from './ContributorActivityWeeksInner';
import NullableSimpleUser from './NullableSimpleUser';

/**
 * The ContributorActivity model module.
 * @module model/ContributorActivity
 * @version 1.1.4
 */
class ContributorActivity {
    /**
     * Constructs a new <code>ContributorActivity</code>.
     * Contributor Activity
     * @alias module:model/ContributorActivity
     * @param author {module:model/NullableSimpleUser} 
     * @param total {Number} 
     * @param weeks {Array.<module:model/ContributorActivityWeeksInner>} 
     */
    constructor(author, total, weeks) { 
        
        ContributorActivity.initialize(this, author, total, weeks);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, author, total, weeks) { 
        obj['author'] = author;
        obj['total'] = total;
        obj['weeks'] = weeks;
    }

    /**
     * Constructs a <code>ContributorActivity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContributorActivity} obj Optional instance to populate.
     * @return {module:model/ContributorActivity} The populated <code>ContributorActivity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContributorActivity();

            if (data.hasOwnProperty('author')) {
                obj['author'] = NullableSimpleUser.constructFromObject(data['author']);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('weeks')) {
                obj['weeks'] = ApiClient.convertToType(data['weeks'], [ContributorActivityWeeksInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContributorActivity</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContributorActivity</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContributorActivity.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `author`
        if (data['author']) { // data not null
          NullableSimpleUser.validateJSON(data['author']);
        }
        if (data['weeks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['weeks'])) {
                throw new Error("Expected the field `weeks` to be an array in the JSON data but got " + data['weeks']);
            }
            // validate the optional field `weeks` (array)
            for (const item of data['weeks']) {
                ContributorActivityWeeksInner.validateJSON(item);
            };
        }

        return true;
    }


}

ContributorActivity.RequiredProperties = ["author", "total", "weeks"];

/**
 * @member {module:model/NullableSimpleUser} author
 */
ContributorActivity.prototype['author'] = undefined;

/**
 * @member {Number} total
 */
ContributorActivity.prototype['total'] = undefined;

/**
 * @member {Array.<module:model/ContributorActivityWeeksInner>} weeks
 */
ContributorActivity.prototype['weeks'] = undefined;






export default ContributorActivity;

