/*
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.net.URI;
import java.time.OffsetDateTime;
import java.util.Arrays;
import org.openapitools.client.model.Enterprise;
import org.openapitools.client.model.SimpleUser;
import org.openapitools.jackson.nullable.JsonNullable;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T10:08:22.800879-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class InstallationAccount extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(InstallationAccount.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!InstallationAccount.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'InstallationAccount' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SimpleUser> adapterSimpleUser = gson.getDelegateAdapter(this, TypeToken.get(SimpleUser.class));
            final TypeAdapter<Enterprise> adapterEnterprise = gson.getDelegateAdapter(this, TypeToken.get(Enterprise.class));

            return (TypeAdapter<T>) new TypeAdapter<InstallationAccount>() {
                @Override
                public void write(JsonWriter out, InstallationAccount value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `SimpleUser`
                    if (value.getActualInstance() instanceof SimpleUser) {
                        JsonElement element = adapterSimpleUser.toJsonTree((SimpleUser)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Enterprise`
                    if (value.getActualInstance() instanceof Enterprise) {
                        JsonElement element = adapterEnterprise.toJsonTree((Enterprise)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match anyOf schemas: Enterprise, SimpleUser");
                }

                @Override
                public InstallationAccount read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize SimpleUser
                    try {
                        // validate the JSON object to see if any exception is thrown
                        SimpleUser.validateJsonElement(jsonElement);
                        actualAdapter = adapterSimpleUser;
                        InstallationAccount ret = new InstallationAccount();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for SimpleUser failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'SimpleUser'", e);
                    }
                    // deserialize Enterprise
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Enterprise.validateJsonElement(jsonElement);
                        actualAdapter = adapterEnterprise;
                        InstallationAccount ret = new InstallationAccount();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Enterprise failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Enterprise'", e);
                    }

                    throw new IOException(String.format("Failed deserialization for InstallationAccount: no class matches result, expected at least 1. Detailed failure message for anyOf schemas: %s. JSON: %s", errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in anyOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public InstallationAccount() {
        super("anyOf", Boolean.TRUE);
    }

    public InstallationAccount(Object o) {
        super("anyOf", Boolean.TRUE);
        setActualInstance(o);
    }

    static {
        schemas.put("SimpleUser", SimpleUser.class);
        schemas.put("Enterprise", Enterprise.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return InstallationAccount.schemas;
    }

    /**
     * Set the instance that matches the anyOf child schema, check
     * the instance parameter is valid against the anyOf child schemas:
     * Enterprise, SimpleUser
     *
     * It could be an instance of the 'anyOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance == null) {
           super.setActualInstance(instance);
           return;
        }

        if (instance instanceof SimpleUser) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Enterprise) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Enterprise, SimpleUser");
    }

    /**
     * Get the actual instance, which can be the following:
     * Enterprise, SimpleUser
     *
     * @return The actual instance (Enterprise, SimpleUser)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `SimpleUser`. If the actual instance is not `SimpleUser`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SimpleUser`
     * @throws ClassCastException if the instance is not `SimpleUser`
     */
    public SimpleUser getSimpleUser() throws ClassCastException {
        return (SimpleUser)super.getActualInstance();
    }
    /**
     * Get the actual instance of `Enterprise`. If the actual instance is not `Enterprise`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Enterprise`
     * @throws ClassCastException if the instance is not `Enterprise`
     */
    public Enterprise getEnterprise() throws ClassCastException {
        return (Enterprise)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to InstallationAccount
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate anyOf schemas one by one
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with SimpleUser
        try {
            SimpleUser.validateJsonElement(jsonElement);
            return;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for SimpleUser failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Enterprise
        try {
            Enterprise.validateJsonElement(jsonElement);
            return;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Enterprise failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        throw new IOException(String.format("The JSON string is invalid for InstallationAccount with anyOf schemas: Enterprise, SimpleUser. no class match the result, expected at least 1. Detailed failure message for anyOf schemas: %s. JSON: %s", errorMessages, jsonElement.toString()));
    }

    /**
     * Create an instance of InstallationAccount given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of InstallationAccount
     * @throws IOException if the JSON string is invalid with respect to InstallationAccount
     */
    public static InstallationAccount fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, InstallationAccount.class);
    }

    /**
     * Convert an instance of InstallationAccount to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

