/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PullRequestMinimalBaseRepo from './PullRequestMinimalBaseRepo';

/**
 * The PullRequestMinimalBase model module.
 * @module model/PullRequestMinimalBase
 * @version 1.1.4
 */
class PullRequestMinimalBase {
    /**
     * Constructs a new <code>PullRequestMinimalBase</code>.
     * @alias module:model/PullRequestMinimalBase
     * @param ref {String} 
     * @param repo {module:model/PullRequestMinimalBaseRepo} 
     * @param sha {String} 
     */
    constructor(ref, repo, sha) { 
        
        PullRequestMinimalBase.initialize(this, ref, repo, sha);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ref, repo, sha) { 
        obj['ref'] = ref;
        obj['repo'] = repo;
        obj['sha'] = sha;
    }

    /**
     * Constructs a <code>PullRequestMinimalBase</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PullRequestMinimalBase} obj Optional instance to populate.
     * @return {module:model/PullRequestMinimalBase} The populated <code>PullRequestMinimalBase</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PullRequestMinimalBase();

            if (data.hasOwnProperty('ref')) {
                obj['ref'] = ApiClient.convertToType(data['ref'], 'String');
            }
            if (data.hasOwnProperty('repo')) {
                obj['repo'] = PullRequestMinimalBaseRepo.constructFromObject(data['repo']);
            }
            if (data.hasOwnProperty('sha')) {
                obj['sha'] = ApiClient.convertToType(data['sha'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PullRequestMinimalBase</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PullRequestMinimalBase</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PullRequestMinimalBase.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ref'] && !(typeof data['ref'] === 'string' || data['ref'] instanceof String)) {
            throw new Error("Expected the field `ref` to be a primitive type in the JSON string but got " + data['ref']);
        }
        // validate the optional field `repo`
        if (data['repo']) { // data not null
          PullRequestMinimalBaseRepo.validateJSON(data['repo']);
        }
        // ensure the json data is a string
        if (data['sha'] && !(typeof data['sha'] === 'string' || data['sha'] instanceof String)) {
            throw new Error("Expected the field `sha` to be a primitive type in the JSON string but got " + data['sha']);
        }

        return true;
    }


}

PullRequestMinimalBase.RequiredProperties = ["ref", "repo", "sha"];

/**
 * @member {String} ref
 */
PullRequestMinimalBase.prototype['ref'] = undefined;

/**
 * @member {module:model/PullRequestMinimalBaseRepo} repo
 */
PullRequestMinimalBase.prototype['repo'] = undefined;

/**
 * @member {String} sha
 */
PullRequestMinimalBase.prototype['sha'] = undefined;






export default PullRequestMinimalBase;

