/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PublicKeyFull model module.
 * @module model/PublicKeyFull
 * @version 1.1.4
 */
class PublicKeyFull {
    /**
     * Constructs a new <code>PublicKeyFull</code>.
     * @alias module:model/PublicKeyFull
     * @param createdAt {Date} 
     * @param id {Number} 
     * @param key {String} 
     * @param readOnly {Boolean} 
     * @param repositoryId {Number} 
     * @param title {String} 
     * @param url {String} 
     * @param userId {Number} 
     * @param verified {Boolean} 
     */
    constructor(createdAt, id, key, readOnly, repositoryId, title, url, userId, verified) { 
        
        PublicKeyFull.initialize(this, createdAt, id, key, readOnly, repositoryId, title, url, userId, verified);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, createdAt, id, key, readOnly, repositoryId, title, url, userId, verified) { 
        obj['created_at'] = createdAt;
        obj['id'] = id;
        obj['key'] = key;
        obj['read_only'] = readOnly;
        obj['repository_id'] = repositoryId;
        obj['title'] = title;
        obj['url'] = url;
        obj['user_id'] = userId;
        obj['verified'] = verified;
    }

    /**
     * Constructs a <code>PublicKeyFull</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PublicKeyFull} obj Optional instance to populate.
     * @return {module:model/PublicKeyFull} The populated <code>PublicKeyFull</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PublicKeyFull();

            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('read_only')) {
                obj['read_only'] = ApiClient.convertToType(data['read_only'], 'Boolean');
            }
            if (data.hasOwnProperty('repository_id')) {
                obj['repository_id'] = ApiClient.convertToType(data['repository_id'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'Number');
            }
            if (data.hasOwnProperty('verified')) {
                obj['verified'] = ApiClient.convertToType(data['verified'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PublicKeyFull</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PublicKeyFull</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PublicKeyFull.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

PublicKeyFull.RequiredProperties = ["created_at", "id", "key", "read_only", "repository_id", "title", "url", "user_id", "verified"];

/**
 * @member {Date} created_at
 */
PublicKeyFull.prototype['created_at'] = undefined;

/**
 * @member {Number} id
 */
PublicKeyFull.prototype['id'] = undefined;

/**
 * @member {String} key
 */
PublicKeyFull.prototype['key'] = undefined;

/**
 * @member {Boolean} read_only
 */
PublicKeyFull.prototype['read_only'] = undefined;

/**
 * @member {Number} repository_id
 */
PublicKeyFull.prototype['repository_id'] = undefined;

/**
 * @member {String} title
 */
PublicKeyFull.prototype['title'] = undefined;

/**
 * @member {String} url
 */
PublicKeyFull.prototype['url'] = undefined;

/**
 * @member {Number} user_id
 */
PublicKeyFull.prototype['user_id'] = undefined;

/**
 * @member {Boolean} verified
 */
PublicKeyFull.prototype['verified'] = undefined;






export default PublicKeyFull;

