/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReposCreateAutolinkRequest model module.
 * @module model/ReposCreateAutolinkRequest
 * @version 1.1.4
 */
class ReposCreateAutolinkRequest {
    /**
     * Constructs a new <code>ReposCreateAutolinkRequest</code>.
     * @alias module:model/ReposCreateAutolinkRequest
     * @param keyPrefix {String} The prefix appended by a number will generate a link any time it is found in an issue, pull request, or commit.
     * @param urlTemplate {String} The URL must contain `<num>` for the reference number.
     */
    constructor(keyPrefix, urlTemplate) { 
        
        ReposCreateAutolinkRequest.initialize(this, keyPrefix, urlTemplate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, keyPrefix, urlTemplate) { 
        obj['is_alphanumeric'] = true;
        obj['key_prefix'] = keyPrefix;
        obj['url_template'] = urlTemplate;
    }

    /**
     * Constructs a <code>ReposCreateAutolinkRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReposCreateAutolinkRequest} obj Optional instance to populate.
     * @return {module:model/ReposCreateAutolinkRequest} The populated <code>ReposCreateAutolinkRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReposCreateAutolinkRequest();

            if (data.hasOwnProperty('is_alphanumeric')) {
                obj['is_alphanumeric'] = ApiClient.convertToType(data['is_alphanumeric'], 'Boolean');
            }
            if (data.hasOwnProperty('key_prefix')) {
                obj['key_prefix'] = ApiClient.convertToType(data['key_prefix'], 'String');
            }
            if (data.hasOwnProperty('url_template')) {
                obj['url_template'] = ApiClient.convertToType(data['url_template'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReposCreateAutolinkRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReposCreateAutolinkRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReposCreateAutolinkRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key_prefix'] && !(typeof data['key_prefix'] === 'string' || data['key_prefix'] instanceof String)) {
            throw new Error("Expected the field `key_prefix` to be a primitive type in the JSON string but got " + data['key_prefix']);
        }
        // ensure the json data is a string
        if (data['url_template'] && !(typeof data['url_template'] === 'string' || data['url_template'] instanceof String)) {
            throw new Error("Expected the field `url_template` to be a primitive type in the JSON string but got " + data['url_template']);
        }

        return true;
    }


}

ReposCreateAutolinkRequest.RequiredProperties = ["key_prefix", "url_template"];

/**
 * Whether this autolink reference matches alphanumeric characters. If true, the `<num>` parameter of the `url_template` matches alphanumeric characters `A-Z` (case insensitive), `0-9`, and `-`. If false, this autolink reference only matches numeric characters.
 * @member {Boolean} is_alphanumeric
 * @default true
 */
ReposCreateAutolinkRequest.prototype['is_alphanumeric'] = true;

/**
 * The prefix appended by a number will generate a link any time it is found in an issue, pull request, or commit.
 * @member {String} key_prefix
 */
ReposCreateAutolinkRequest.prototype['key_prefix'] = undefined;

/**
 * The URL must contain `<num>` for the reference number.
 * @member {String} url_template
 */
ReposCreateAutolinkRequest.prototype['url_template'] = undefined;






export default ReposCreateAutolinkRequest;

