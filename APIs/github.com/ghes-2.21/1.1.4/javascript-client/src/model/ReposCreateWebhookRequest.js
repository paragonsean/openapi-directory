/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReposCreateWebhookRequestConfig from './ReposCreateWebhookRequestConfig';

/**
 * The ReposCreateWebhookRequest model module.
 * @module model/ReposCreateWebhookRequest
 * @version 1.1.4
 */
class ReposCreateWebhookRequest {
    /**
     * Constructs a new <code>ReposCreateWebhookRequest</code>.
     * @alias module:model/ReposCreateWebhookRequest
     */
    constructor() { 
        
        ReposCreateWebhookRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['active'] = true;
    }

    /**
     * Constructs a <code>ReposCreateWebhookRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReposCreateWebhookRequest} obj Optional instance to populate.
     * @return {module:model/ReposCreateWebhookRequest} The populated <code>ReposCreateWebhookRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReposCreateWebhookRequest();

            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('config')) {
                obj['config'] = ReposCreateWebhookRequestConfig.constructFromObject(data['config']);
            }
            if (data.hasOwnProperty('events')) {
                obj['events'] = ApiClient.convertToType(data['events'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReposCreateWebhookRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReposCreateWebhookRequest</code>.
     */
    static validateJSON(data) {
        // validate the optional field `config`
        if (data['config']) { // data not null
          ReposCreateWebhookRequestConfig.validateJSON(data['config']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['events'])) {
            throw new Error("Expected the field `events` to be an array in the JSON data but got " + data['events']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}



/**
 * Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.
 * @member {Boolean} active
 * @default true
 */
ReposCreateWebhookRequest.prototype['active'] = true;

/**
 * @member {module:model/ReposCreateWebhookRequestConfig} config
 */
ReposCreateWebhookRequest.prototype['config'] = undefined;

/**
 * Determines what [events](https://docs.github.com/enterprise-server@2.21/webhooks/event-payloads) the hook is triggered for.
 * @member {Array.<String>} events
 */
ReposCreateWebhookRequest.prototype['events'] = undefined;

/**
 * Use `web` to create a webhook. Default: `web`. This parameter only accepts the value `web`.
 * @member {String} name
 */
ReposCreateWebhookRequest.prototype['name'] = undefined;






export default ReposCreateWebhookRequest;

