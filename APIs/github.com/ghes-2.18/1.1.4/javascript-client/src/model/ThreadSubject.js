/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ThreadSubject model module.
 * @module model/ThreadSubject
 * @version 1.1.4
 */
class ThreadSubject {
    /**
     * Constructs a new <code>ThreadSubject</code>.
     * @alias module:model/ThreadSubject
     * @param latestCommentUrl {String} 
     * @param title {String} 
     * @param type {String} 
     * @param url {String} 
     */
    constructor(latestCommentUrl, title, type, url) { 
        
        ThreadSubject.initialize(this, latestCommentUrl, title, type, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, latestCommentUrl, title, type, url) { 
        obj['latest_comment_url'] = latestCommentUrl;
        obj['title'] = title;
        obj['type'] = type;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>ThreadSubject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ThreadSubject} obj Optional instance to populate.
     * @return {module:model/ThreadSubject} The populated <code>ThreadSubject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ThreadSubject();

            if (data.hasOwnProperty('latest_comment_url')) {
                obj['latest_comment_url'] = ApiClient.convertToType(data['latest_comment_url'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ThreadSubject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ThreadSubject</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ThreadSubject.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['latest_comment_url'] && !(typeof data['latest_comment_url'] === 'string' || data['latest_comment_url'] instanceof String)) {
            throw new Error("Expected the field `latest_comment_url` to be a primitive type in the JSON string but got " + data['latest_comment_url']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

ThreadSubject.RequiredProperties = ["latest_comment_url", "title", "type", "url"];

/**
 * @member {String} latest_comment_url
 */
ThreadSubject.prototype['latest_comment_url'] = undefined;

/**
 * @member {String} title
 */
ThreadSubject.prototype['title'] = undefined;

/**
 * @member {String} type
 */
ThreadSubject.prototype['type'] = undefined;

/**
 * @member {String} url
 */
ThreadSubject.prototype['url'] = undefined;






export default ThreadSubject;

