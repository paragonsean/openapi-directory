/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UsersCreatePublicSshKeyForAuthenticatedRequest model module.
 * @module model/UsersCreatePublicSshKeyForAuthenticatedRequest
 * @version 1.1.4
 */
class UsersCreatePublicSshKeyForAuthenticatedRequest {
    /**
     * Constructs a new <code>UsersCreatePublicSshKeyForAuthenticatedRequest</code>.
     * @alias module:model/UsersCreatePublicSshKeyForAuthenticatedRequest
     * @param key {String} The public SSH key to add to your GitHub account.
     */
    constructor(key) { 
        
        UsersCreatePublicSshKeyForAuthenticatedRequest.initialize(this, key);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, key) { 
        obj['key'] = key;
    }

    /**
     * Constructs a <code>UsersCreatePublicSshKeyForAuthenticatedRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UsersCreatePublicSshKeyForAuthenticatedRequest} obj Optional instance to populate.
     * @return {module:model/UsersCreatePublicSshKeyForAuthenticatedRequest} The populated <code>UsersCreatePublicSshKeyForAuthenticatedRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UsersCreatePublicSshKeyForAuthenticatedRequest();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UsersCreatePublicSshKeyForAuthenticatedRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UsersCreatePublicSshKeyForAuthenticatedRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UsersCreatePublicSshKeyForAuthenticatedRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

UsersCreatePublicSshKeyForAuthenticatedRequest.RequiredProperties = ["key"];

/**
 * The public SSH key to add to your GitHub account.
 * @member {String} key
 */
UsersCreatePublicSshKeyForAuthenticatedRequest.prototype['key'] = undefined;

/**
 * A descriptive name for the new key.
 * @member {String} title
 */
UsersCreatePublicSshKeyForAuthenticatedRequest.prototype['title'] = undefined;






export default UsersCreatePublicSshKeyForAuthenticatedRequest;

