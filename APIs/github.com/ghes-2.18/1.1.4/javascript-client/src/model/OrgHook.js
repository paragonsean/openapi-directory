/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OrgHookConfig from './OrgHookConfig';

/**
 * The OrgHook model module.
 * @module model/OrgHook
 * @version 1.1.4
 */
class OrgHook {
    /**
     * Constructs a new <code>OrgHook</code>.
     * Org Hook
     * @alias module:model/OrgHook
     * @param active {Boolean} 
     * @param config {module:model/OrgHookConfig} 
     * @param createdAt {Date} 
     * @param events {Array.<String>} 
     * @param id {Number} 
     * @param name {String} 
     * @param pingUrl {String} 
     * @param type {String} 
     * @param updatedAt {Date} 
     * @param url {String} 
     */
    constructor(active, config, createdAt, events, id, name, pingUrl, type, updatedAt, url) { 
        
        OrgHook.initialize(this, active, config, createdAt, events, id, name, pingUrl, type, updatedAt, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, active, config, createdAt, events, id, name, pingUrl, type, updatedAt, url) { 
        obj['active'] = active;
        obj['config'] = config;
        obj['created_at'] = createdAt;
        obj['events'] = events;
        obj['id'] = id;
        obj['name'] = name;
        obj['ping_url'] = pingUrl;
        obj['type'] = type;
        obj['updated_at'] = updatedAt;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>OrgHook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrgHook} obj Optional instance to populate.
     * @return {module:model/OrgHook} The populated <code>OrgHook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrgHook();

            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('config')) {
                obj['config'] = OrgHookConfig.constructFromObject(data['config']);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('events')) {
                obj['events'] = ApiClient.convertToType(data['events'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('ping_url')) {
                obj['ping_url'] = ApiClient.convertToType(data['ping_url'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrgHook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrgHook</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrgHook.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `config`
        if (data['config']) { // data not null
          OrgHookConfig.validateJSON(data['config']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['events'])) {
            throw new Error("Expected the field `events` to be an array in the JSON data but got " + data['events']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['ping_url'] && !(typeof data['ping_url'] === 'string' || data['ping_url'] instanceof String)) {
            throw new Error("Expected the field `ping_url` to be a primitive type in the JSON string but got " + data['ping_url']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

OrgHook.RequiredProperties = ["active", "config", "created_at", "events", "id", "name", "ping_url", "type", "updated_at", "url"];

/**
 * @member {Boolean} active
 */
OrgHook.prototype['active'] = undefined;

/**
 * @member {module:model/OrgHookConfig} config
 */
OrgHook.prototype['config'] = undefined;

/**
 * @member {Date} created_at
 */
OrgHook.prototype['created_at'] = undefined;

/**
 * @member {Array.<String>} events
 */
OrgHook.prototype['events'] = undefined;

/**
 * @member {Number} id
 */
OrgHook.prototype['id'] = undefined;

/**
 * @member {String} name
 */
OrgHook.prototype['name'] = undefined;

/**
 * @member {String} ping_url
 */
OrgHook.prototype['ping_url'] = undefined;

/**
 * @member {String} type
 */
OrgHook.prototype['type'] = undefined;

/**
 * @member {Date} updated_at
 */
OrgHook.prototype['updated_at'] = undefined;

/**
 * @member {String} url
 */
OrgHook.prototype['url'] = undefined;






export default OrgHook;

