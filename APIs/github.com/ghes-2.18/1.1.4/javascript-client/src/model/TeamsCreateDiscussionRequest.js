/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TeamsCreateDiscussionRequest model module.
 * @module model/TeamsCreateDiscussionRequest
 * @version 1.1.4
 */
class TeamsCreateDiscussionRequest {
    /**
     * Constructs a new <code>TeamsCreateDiscussionRequest</code>.
     * @alias module:model/TeamsCreateDiscussionRequest
     * @param body {String} The discussion post's body text.
     * @param title {String} The discussion post's title.
     */
    constructor(body, title) { 
        
        TeamsCreateDiscussionRequest.initialize(this, body, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, body, title) { 
        obj['body'] = body;
        obj['private'] = false;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>TeamsCreateDiscussionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TeamsCreateDiscussionRequest} obj Optional instance to populate.
     * @return {module:model/TeamsCreateDiscussionRequest} The populated <code>TeamsCreateDiscussionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TeamsCreateDiscussionRequest();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('private')) {
                obj['private'] = ApiClient.convertToType(data['private'], 'Boolean');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TeamsCreateDiscussionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TeamsCreateDiscussionRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TeamsCreateDiscussionRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

TeamsCreateDiscussionRequest.RequiredProperties = ["body", "title"];

/**
 * The discussion post's body text.
 * @member {String} body
 */
TeamsCreateDiscussionRequest.prototype['body'] = undefined;

/**
 * Private posts are only visible to team members, organization owners, and team maintainers. Public posts are visible to all members of the organization. Set to `true` to create a private post.
 * @member {Boolean} private
 * @default false
 */
TeamsCreateDiscussionRequest.prototype['private'] = false;

/**
 * The discussion post's title.
 * @member {String} title
 */
TeamsCreateDiscussionRequest.prototype['title'] = undefined;






export default TeamsCreateDiscussionRequest;

