/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PostCharactersCharacterIdMailRecipient from './PostCharactersCharacterIdMailRecipient';

/**
 * The PostCharactersCharacterIdMailMail model module.
 * @module model/PostCharactersCharacterIdMailMail
 * @version 0.8.6
 */
class PostCharactersCharacterIdMailMail {
    /**
     * Constructs a new <code>PostCharactersCharacterIdMailMail</code>.
     * mail object
     * @alias module:model/PostCharactersCharacterIdMailMail
     * @param body {String} body string
     * @param recipients {Array.<module:model/PostCharactersCharacterIdMailRecipient>} recipients array
     * @param subject {String} subject string
     */
    constructor(body, recipients, subject) { 
        
        PostCharactersCharacterIdMailMail.initialize(this, body, recipients, subject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, body, recipients, subject) { 
        obj['approved_cost'] = 0;
        obj['body'] = body;
        obj['recipients'] = recipients;
        obj['subject'] = subject;
    }

    /**
     * Constructs a <code>PostCharactersCharacterIdMailMail</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostCharactersCharacterIdMailMail} obj Optional instance to populate.
     * @return {module:model/PostCharactersCharacterIdMailMail} The populated <code>PostCharactersCharacterIdMailMail</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostCharactersCharacterIdMailMail();

            if (data.hasOwnProperty('approved_cost')) {
                obj['approved_cost'] = ApiClient.convertToType(data['approved_cost'], 'Number');
            }
            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('recipients')) {
                obj['recipients'] = ApiClient.convertToType(data['recipients'], [PostCharactersCharacterIdMailRecipient]);
            }
            if (data.hasOwnProperty('subject')) {
                obj['subject'] = ApiClient.convertToType(data['subject'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostCharactersCharacterIdMailMail</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostCharactersCharacterIdMailMail</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostCharactersCharacterIdMailMail.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        if (data['recipients']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['recipients'])) {
                throw new Error("Expected the field `recipients` to be an array in the JSON data but got " + data['recipients']);
            }
            // validate the optional field `recipients` (array)
            for (const item of data['recipients']) {
                PostCharactersCharacterIdMailRecipient.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['subject'] && !(typeof data['subject'] === 'string' || data['subject'] instanceof String)) {
            throw new Error("Expected the field `subject` to be a primitive type in the JSON string but got " + data['subject']);
        }

        return true;
    }


}

PostCharactersCharacterIdMailMail.RequiredProperties = ["body", "recipients", "subject"];

/**
 * approved_cost integer
 * @member {Number} approved_cost
 * @default 0
 */
PostCharactersCharacterIdMailMail.prototype['approved_cost'] = 0;

/**
 * body string
 * @member {String} body
 */
PostCharactersCharacterIdMailMail.prototype['body'] = undefined;

/**
 * recipients array
 * @member {Array.<module:model/PostCharactersCharacterIdMailRecipient>} recipients
 */
PostCharactersCharacterIdMailMail.prototype['recipients'] = undefined;

/**
 * subject string
 * @member {String} subject
 */
PostCharactersCharacterIdMailMail.prototype['subject'] = undefined;






export default PostCharactersCharacterIdMailMail;

