/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GetLoyaltyStoresCorporationIdOffersRequiredItem from './GetLoyaltyStoresCorporationIdOffersRequiredItem';

/**
 * The GetLoyaltyStoresCorporationIdOffers200Ok model module.
 * @module model/GetLoyaltyStoresCorporationIdOffers200Ok
 * @version 0.8.6
 */
class GetLoyaltyStoresCorporationIdOffers200Ok {
    /**
     * Constructs a new <code>GetLoyaltyStoresCorporationIdOffers200Ok</code>.
     * 200 ok object
     * @alias module:model/GetLoyaltyStoresCorporationIdOffers200Ok
     * @param iskCost {Number} isk_cost integer
     * @param lpCost {Number} lp_cost integer
     * @param offerId {Number} offer_id integer
     * @param quantity {Number} quantity integer
     * @param requiredItems {Array.<module:model/GetLoyaltyStoresCorporationIdOffersRequiredItem>} required_items array
     * @param typeId {Number} type_id integer
     */
    constructor(iskCost, lpCost, offerId, quantity, requiredItems, typeId) { 
        
        GetLoyaltyStoresCorporationIdOffers200Ok.initialize(this, iskCost, lpCost, offerId, quantity, requiredItems, typeId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, iskCost, lpCost, offerId, quantity, requiredItems, typeId) { 
        obj['isk_cost'] = iskCost;
        obj['lp_cost'] = lpCost;
        obj['offer_id'] = offerId;
        obj['quantity'] = quantity;
        obj['required_items'] = requiredItems;
        obj['type_id'] = typeId;
    }

    /**
     * Constructs a <code>GetLoyaltyStoresCorporationIdOffers200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetLoyaltyStoresCorporationIdOffers200Ok} obj Optional instance to populate.
     * @return {module:model/GetLoyaltyStoresCorporationIdOffers200Ok} The populated <code>GetLoyaltyStoresCorporationIdOffers200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetLoyaltyStoresCorporationIdOffers200Ok();

            if (data.hasOwnProperty('ak_cost')) {
                obj['ak_cost'] = ApiClient.convertToType(data['ak_cost'], 'Number');
            }
            if (data.hasOwnProperty('isk_cost')) {
                obj['isk_cost'] = ApiClient.convertToType(data['isk_cost'], 'Number');
            }
            if (data.hasOwnProperty('lp_cost')) {
                obj['lp_cost'] = ApiClient.convertToType(data['lp_cost'], 'Number');
            }
            if (data.hasOwnProperty('offer_id')) {
                obj['offer_id'] = ApiClient.convertToType(data['offer_id'], 'Number');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('required_items')) {
                obj['required_items'] = ApiClient.convertToType(data['required_items'], [GetLoyaltyStoresCorporationIdOffersRequiredItem]);
            }
            if (data.hasOwnProperty('type_id')) {
                obj['type_id'] = ApiClient.convertToType(data['type_id'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetLoyaltyStoresCorporationIdOffers200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetLoyaltyStoresCorporationIdOffers200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetLoyaltyStoresCorporationIdOffers200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['required_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['required_items'])) {
                throw new Error("Expected the field `required_items` to be an array in the JSON data but got " + data['required_items']);
            }
            // validate the optional field `required_items` (array)
            for (const item of data['required_items']) {
                GetLoyaltyStoresCorporationIdOffersRequiredItem.validateJSON(item);
            };
        }

        return true;
    }


}

GetLoyaltyStoresCorporationIdOffers200Ok.RequiredProperties = ["isk_cost", "lp_cost", "offer_id", "quantity", "required_items", "type_id"];

/**
 * Analysis kredit cost
 * @member {Number} ak_cost
 */
GetLoyaltyStoresCorporationIdOffers200Ok.prototype['ak_cost'] = undefined;

/**
 * isk_cost integer
 * @member {Number} isk_cost
 */
GetLoyaltyStoresCorporationIdOffers200Ok.prototype['isk_cost'] = undefined;

/**
 * lp_cost integer
 * @member {Number} lp_cost
 */
GetLoyaltyStoresCorporationIdOffers200Ok.prototype['lp_cost'] = undefined;

/**
 * offer_id integer
 * @member {Number} offer_id
 */
GetLoyaltyStoresCorporationIdOffers200Ok.prototype['offer_id'] = undefined;

/**
 * quantity integer
 * @member {Number} quantity
 */
GetLoyaltyStoresCorporationIdOffers200Ok.prototype['quantity'] = undefined;

/**
 * required_items array
 * @member {Array.<module:model/GetLoyaltyStoresCorporationIdOffersRequiredItem>} required_items
 */
GetLoyaltyStoresCorporationIdOffers200Ok.prototype['required_items'] = undefined;

/**
 * type_id integer
 * @member {Number} type_id
 */
GetLoyaltyStoresCorporationIdOffers200Ok.prototype['type_id'] = undefined;






export default GetLoyaltyStoresCorporationIdOffers200Ok;

