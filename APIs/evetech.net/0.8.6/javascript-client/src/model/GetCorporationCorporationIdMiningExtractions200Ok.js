/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetCorporationCorporationIdMiningExtractions200Ok model module.
 * @module model/GetCorporationCorporationIdMiningExtractions200Ok
 * @version 0.8.6
 */
class GetCorporationCorporationIdMiningExtractions200Ok {
    /**
     * Constructs a new <code>GetCorporationCorporationIdMiningExtractions200Ok</code>.
     * 200 ok object
     * @alias module:model/GetCorporationCorporationIdMiningExtractions200Ok
     * @param chunkArrivalTime {Date} The time at which the chunk being extracted will arrive and can be fractured by the moon mining drill. 
     * @param extractionStartTime {Date} The time at which the current extraction was initiated. 
     * @param moonId {Number} moon_id integer
     * @param naturalDecayTime {Date} The time at which the chunk being extracted will naturally fracture if it is not first fractured by the moon mining drill. 
     * @param structureId {Number} structure_id integer
     */
    constructor(chunkArrivalTime, extractionStartTime, moonId, naturalDecayTime, structureId) { 
        
        GetCorporationCorporationIdMiningExtractions200Ok.initialize(this, chunkArrivalTime, extractionStartTime, moonId, naturalDecayTime, structureId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, chunkArrivalTime, extractionStartTime, moonId, naturalDecayTime, structureId) { 
        obj['chunk_arrival_time'] = chunkArrivalTime;
        obj['extraction_start_time'] = extractionStartTime;
        obj['moon_id'] = moonId;
        obj['natural_decay_time'] = naturalDecayTime;
        obj['structure_id'] = structureId;
    }

    /**
     * Constructs a <code>GetCorporationCorporationIdMiningExtractions200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetCorporationCorporationIdMiningExtractions200Ok} obj Optional instance to populate.
     * @return {module:model/GetCorporationCorporationIdMiningExtractions200Ok} The populated <code>GetCorporationCorporationIdMiningExtractions200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetCorporationCorporationIdMiningExtractions200Ok();

            if (data.hasOwnProperty('chunk_arrival_time')) {
                obj['chunk_arrival_time'] = ApiClient.convertToType(data['chunk_arrival_time'], 'Date');
            }
            if (data.hasOwnProperty('extraction_start_time')) {
                obj['extraction_start_time'] = ApiClient.convertToType(data['extraction_start_time'], 'Date');
            }
            if (data.hasOwnProperty('moon_id')) {
                obj['moon_id'] = ApiClient.convertToType(data['moon_id'], 'Number');
            }
            if (data.hasOwnProperty('natural_decay_time')) {
                obj['natural_decay_time'] = ApiClient.convertToType(data['natural_decay_time'], 'Date');
            }
            if (data.hasOwnProperty('structure_id')) {
                obj['structure_id'] = ApiClient.convertToType(data['structure_id'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetCorporationCorporationIdMiningExtractions200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetCorporationCorporationIdMiningExtractions200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetCorporationCorporationIdMiningExtractions200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

GetCorporationCorporationIdMiningExtractions200Ok.RequiredProperties = ["chunk_arrival_time", "extraction_start_time", "moon_id", "natural_decay_time", "structure_id"];

/**
 * The time at which the chunk being extracted will arrive and can be fractured by the moon mining drill. 
 * @member {Date} chunk_arrival_time
 */
GetCorporationCorporationIdMiningExtractions200Ok.prototype['chunk_arrival_time'] = undefined;

/**
 * The time at which the current extraction was initiated. 
 * @member {Date} extraction_start_time
 */
GetCorporationCorporationIdMiningExtractions200Ok.prototype['extraction_start_time'] = undefined;

/**
 * moon_id integer
 * @member {Number} moon_id
 */
GetCorporationCorporationIdMiningExtractions200Ok.prototype['moon_id'] = undefined;

/**
 * The time at which the chunk being extracted will naturally fracture if it is not first fractured by the moon mining drill. 
 * @member {Date} natural_decay_time
 */
GetCorporationCorporationIdMiningExtractions200Ok.prototype['natural_decay_time'] = undefined;

/**
 * structure_id integer
 * @member {Number} structure_id
 */
GetCorporationCorporationIdMiningExtractions200Ok.prototype['structure_id'] = undefined;






export default GetCorporationCorporationIdMiningExtractions200Ok;

