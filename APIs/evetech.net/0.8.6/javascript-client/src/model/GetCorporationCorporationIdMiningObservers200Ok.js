/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetCorporationCorporationIdMiningObservers200Ok model module.
 * @module model/GetCorporationCorporationIdMiningObservers200Ok
 * @version 0.8.6
 */
class GetCorporationCorporationIdMiningObservers200Ok {
    /**
     * Constructs a new <code>GetCorporationCorporationIdMiningObservers200Ok</code>.
     * 200 ok object
     * @alias module:model/GetCorporationCorporationIdMiningObservers200Ok
     * @param lastUpdated {Date} last_updated string
     * @param observerId {Number} The entity that was observing the asteroid field when it was mined. 
     * @param observerType {module:model/GetCorporationCorporationIdMiningObservers200Ok.ObserverTypeEnum} The category of the observing entity
     */
    constructor(lastUpdated, observerId, observerType) { 
        
        GetCorporationCorporationIdMiningObservers200Ok.initialize(this, lastUpdated, observerId, observerType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, lastUpdated, observerId, observerType) { 
        obj['last_updated'] = lastUpdated;
        obj['observer_id'] = observerId;
        obj['observer_type'] = observerType;
    }

    /**
     * Constructs a <code>GetCorporationCorporationIdMiningObservers200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetCorporationCorporationIdMiningObservers200Ok} obj Optional instance to populate.
     * @return {module:model/GetCorporationCorporationIdMiningObservers200Ok} The populated <code>GetCorporationCorporationIdMiningObservers200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetCorporationCorporationIdMiningObservers200Ok();

            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('observer_id')) {
                obj['observer_id'] = ApiClient.convertToType(data['observer_id'], 'Number');
            }
            if (data.hasOwnProperty('observer_type')) {
                obj['observer_type'] = ApiClient.convertToType(data['observer_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetCorporationCorporationIdMiningObservers200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetCorporationCorporationIdMiningObservers200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetCorporationCorporationIdMiningObservers200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['observer_type'] && !(typeof data['observer_type'] === 'string' || data['observer_type'] instanceof String)) {
            throw new Error("Expected the field `observer_type` to be a primitive type in the JSON string but got " + data['observer_type']);
        }

        return true;
    }


}

GetCorporationCorporationIdMiningObservers200Ok.RequiredProperties = ["last_updated", "observer_id", "observer_type"];

/**
 * last_updated string
 * @member {Date} last_updated
 */
GetCorporationCorporationIdMiningObservers200Ok.prototype['last_updated'] = undefined;

/**
 * The entity that was observing the asteroid field when it was mined. 
 * @member {Number} observer_id
 */
GetCorporationCorporationIdMiningObservers200Ok.prototype['observer_id'] = undefined;

/**
 * The category of the observing entity
 * @member {module:model/GetCorporationCorporationIdMiningObservers200Ok.ObserverTypeEnum} observer_type
 */
GetCorporationCorporationIdMiningObservers200Ok.prototype['observer_type'] = undefined;





/**
 * Allowed values for the <code>observer_type</code> property.
 * @enum {String}
 * @readonly
 */
GetCorporationCorporationIdMiningObservers200Ok['ObserverTypeEnum'] = {

    /**
     * value: "structure"
     * @const
     */
    "structure": "structure"
};



export default GetCorporationCorporationIdMiningObservers200Ok;

