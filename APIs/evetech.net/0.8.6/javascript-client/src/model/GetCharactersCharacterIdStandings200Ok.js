/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetCharactersCharacterIdStandings200Ok model module.
 * @module model/GetCharactersCharacterIdStandings200Ok
 * @version 0.8.6
 */
class GetCharactersCharacterIdStandings200Ok {
    /**
     * Constructs a new <code>GetCharactersCharacterIdStandings200Ok</code>.
     * 200 ok object
     * @alias module:model/GetCharactersCharacterIdStandings200Ok
     * @param fromId {Number} from_id integer
     * @param fromType {module:model/GetCharactersCharacterIdStandings200Ok.FromTypeEnum} from_type string
     * @param standing {Number} standing number
     */
    constructor(fromId, fromType, standing) { 
        
        GetCharactersCharacterIdStandings200Ok.initialize(this, fromId, fromType, standing);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fromId, fromType, standing) { 
        obj['from_id'] = fromId;
        obj['from_type'] = fromType;
        obj['standing'] = standing;
    }

    /**
     * Constructs a <code>GetCharactersCharacterIdStandings200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetCharactersCharacterIdStandings200Ok} obj Optional instance to populate.
     * @return {module:model/GetCharactersCharacterIdStandings200Ok} The populated <code>GetCharactersCharacterIdStandings200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetCharactersCharacterIdStandings200Ok();

            if (data.hasOwnProperty('from_id')) {
                obj['from_id'] = ApiClient.convertToType(data['from_id'], 'Number');
            }
            if (data.hasOwnProperty('from_type')) {
                obj['from_type'] = ApiClient.convertToType(data['from_type'], 'String');
            }
            if (data.hasOwnProperty('standing')) {
                obj['standing'] = ApiClient.convertToType(data['standing'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetCharactersCharacterIdStandings200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetCharactersCharacterIdStandings200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetCharactersCharacterIdStandings200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['from_type'] && !(typeof data['from_type'] === 'string' || data['from_type'] instanceof String)) {
            throw new Error("Expected the field `from_type` to be a primitive type in the JSON string but got " + data['from_type']);
        }

        return true;
    }


}

GetCharactersCharacterIdStandings200Ok.RequiredProperties = ["from_id", "from_type", "standing"];

/**
 * from_id integer
 * @member {Number} from_id
 */
GetCharactersCharacterIdStandings200Ok.prototype['from_id'] = undefined;

/**
 * from_type string
 * @member {module:model/GetCharactersCharacterIdStandings200Ok.FromTypeEnum} from_type
 */
GetCharactersCharacterIdStandings200Ok.prototype['from_type'] = undefined;

/**
 * standing number
 * @member {Number} standing
 */
GetCharactersCharacterIdStandings200Ok.prototype['standing'] = undefined;





/**
 * Allowed values for the <code>from_type</code> property.
 * @enum {String}
 * @readonly
 */
GetCharactersCharacterIdStandings200Ok['FromTypeEnum'] = {

    /**
     * value: "agent"
     * @const
     */
    "agent": "agent",

    /**
     * value: "npc_corp"
     * @const
     */
    "npc_corp": "npc_corp",

    /**
     * value: "faction"
     * @const
     */
    "faction": "faction"
};



export default GetCharactersCharacterIdStandings200Ok;

