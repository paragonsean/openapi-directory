/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetCorporationsCorporationIdContacts200Ok model module.
 * @module model/GetCorporationsCorporationIdContacts200Ok
 * @version 0.8.6
 */
class GetCorporationsCorporationIdContacts200Ok {
    /**
     * Constructs a new <code>GetCorporationsCorporationIdContacts200Ok</code>.
     * 200 ok object
     * @alias module:model/GetCorporationsCorporationIdContacts200Ok
     * @param contactId {Number} contact_id integer
     * @param contactType {module:model/GetCorporationsCorporationIdContacts200Ok.ContactTypeEnum} contact_type string
     * @param standing {Number} Standing of the contact
     */
    constructor(contactId, contactType, standing) { 
        
        GetCorporationsCorporationIdContacts200Ok.initialize(this, contactId, contactType, standing);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contactId, contactType, standing) { 
        obj['contact_id'] = contactId;
        obj['contact_type'] = contactType;
        obj['standing'] = standing;
    }

    /**
     * Constructs a <code>GetCorporationsCorporationIdContacts200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetCorporationsCorporationIdContacts200Ok} obj Optional instance to populate.
     * @return {module:model/GetCorporationsCorporationIdContacts200Ok} The populated <code>GetCorporationsCorporationIdContacts200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetCorporationsCorporationIdContacts200Ok();

            if (data.hasOwnProperty('contact_id')) {
                obj['contact_id'] = ApiClient.convertToType(data['contact_id'], 'Number');
            }
            if (data.hasOwnProperty('contact_type')) {
                obj['contact_type'] = ApiClient.convertToType(data['contact_type'], 'String');
            }
            if (data.hasOwnProperty('is_watched')) {
                obj['is_watched'] = ApiClient.convertToType(data['is_watched'], 'Boolean');
            }
            if (data.hasOwnProperty('label_ids')) {
                obj['label_ids'] = ApiClient.convertToType(data['label_ids'], ['Number']);
            }
            if (data.hasOwnProperty('standing')) {
                obj['standing'] = ApiClient.convertToType(data['standing'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetCorporationsCorporationIdContacts200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetCorporationsCorporationIdContacts200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetCorporationsCorporationIdContacts200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['contact_type'] && !(typeof data['contact_type'] === 'string' || data['contact_type'] instanceof String)) {
            throw new Error("Expected the field `contact_type` to be a primitive type in the JSON string but got " + data['contact_type']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['label_ids'])) {
            throw new Error("Expected the field `label_ids` to be an array in the JSON data but got " + data['label_ids']);
        }

        return true;
    }


}

GetCorporationsCorporationIdContacts200Ok.RequiredProperties = ["contact_id", "contact_type", "standing"];

/**
 * contact_id integer
 * @member {Number} contact_id
 */
GetCorporationsCorporationIdContacts200Ok.prototype['contact_id'] = undefined;

/**
 * contact_type string
 * @member {module:model/GetCorporationsCorporationIdContacts200Ok.ContactTypeEnum} contact_type
 */
GetCorporationsCorporationIdContacts200Ok.prototype['contact_type'] = undefined;

/**
 * Whether this contact is being watched
 * @member {Boolean} is_watched
 */
GetCorporationsCorporationIdContacts200Ok.prototype['is_watched'] = undefined;

/**
 * label_ids array
 * @member {Array.<Number>} label_ids
 */
GetCorporationsCorporationIdContacts200Ok.prototype['label_ids'] = undefined;

/**
 * Standing of the contact
 * @member {Number} standing
 */
GetCorporationsCorporationIdContacts200Ok.prototype['standing'] = undefined;





/**
 * Allowed values for the <code>contact_type</code> property.
 * @enum {String}
 * @readonly
 */
GetCorporationsCorporationIdContacts200Ok['ContactTypeEnum'] = {

    /**
     * value: "character"
     * @const
     */
    "character": "character",

    /**
     * value: "corporation"
     * @const
     */
    "corporation": "corporation",

    /**
     * value: "alliance"
     * @const
     */
    "alliance": "alliance",

    /**
     * value: "faction"
     * @const
     */
    "faction": "faction"
};



export default GetCorporationsCorporationIdContacts200Ok;

