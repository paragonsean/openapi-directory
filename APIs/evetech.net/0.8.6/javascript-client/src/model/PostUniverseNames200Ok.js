/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PostUniverseNames200Ok model module.
 * @module model/PostUniverseNames200Ok
 * @version 0.8.6
 */
class PostUniverseNames200Ok {
    /**
     * Constructs a new <code>PostUniverseNames200Ok</code>.
     * 200 ok object
     * @alias module:model/PostUniverseNames200Ok
     * @param category {module:model/PostUniverseNames200Ok.CategoryEnum} category string
     * @param id {Number} id integer
     * @param name {String} name string
     */
    constructor(category, id, name) { 
        
        PostUniverseNames200Ok.initialize(this, category, id, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, category, id, name) { 
        obj['category'] = category;
        obj['id'] = id;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>PostUniverseNames200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostUniverseNames200Ok} obj Optional instance to populate.
     * @return {module:model/PostUniverseNames200Ok} The populated <code>PostUniverseNames200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostUniverseNames200Ok();

            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostUniverseNames200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostUniverseNames200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostUniverseNames200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['category'] && !(typeof data['category'] === 'string' || data['category'] instanceof String)) {
            throw new Error("Expected the field `category` to be a primitive type in the JSON string but got " + data['category']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

PostUniverseNames200Ok.RequiredProperties = ["category", "id", "name"];

/**
 * category string
 * @member {module:model/PostUniverseNames200Ok.CategoryEnum} category
 */
PostUniverseNames200Ok.prototype['category'] = undefined;

/**
 * id integer
 * @member {Number} id
 */
PostUniverseNames200Ok.prototype['id'] = undefined;

/**
 * name string
 * @member {String} name
 */
PostUniverseNames200Ok.prototype['name'] = undefined;





/**
 * Allowed values for the <code>category</code> property.
 * @enum {String}
 * @readonly
 */
PostUniverseNames200Ok['CategoryEnum'] = {

    /**
     * value: "alliance"
     * @const
     */
    "alliance": "alliance",

    /**
     * value: "character"
     * @const
     */
    "character": "character",

    /**
     * value: "constellation"
     * @const
     */
    "constellation": "constellation",

    /**
     * value: "corporation"
     * @const
     */
    "corporation": "corporation",

    /**
     * value: "inventory_type"
     * @const
     */
    "inventory_type": "inventory_type",

    /**
     * value: "region"
     * @const
     */
    "region": "region",

    /**
     * value: "solar_system"
     * @const
     */
    "solar_system": "solar_system",

    /**
     * value: "station"
     * @const
     */
    "station": "station"
};



export default PostUniverseNames200Ok;

