/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetStatusOk model module.
 * @module model/GetStatusOk
 * @version 0.8.6
 */
class GetStatusOk {
    /**
     * Constructs a new <code>GetStatusOk</code>.
     * 200 ok object
     * @alias module:model/GetStatusOk
     * @param players {Number} Current online player count
     * @param serverVersion {String} Running version as string
     * @param startTime {Date} Server start timestamp
     */
    constructor(players, serverVersion, startTime) { 
        
        GetStatusOk.initialize(this, players, serverVersion, startTime);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, players, serverVersion, startTime) { 
        obj['players'] = players;
        obj['server_version'] = serverVersion;
        obj['start_time'] = startTime;
    }

    /**
     * Constructs a <code>GetStatusOk</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetStatusOk} obj Optional instance to populate.
     * @return {module:model/GetStatusOk} The populated <code>GetStatusOk</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetStatusOk();

            if (data.hasOwnProperty('players')) {
                obj['players'] = ApiClient.convertToType(data['players'], 'Number');
            }
            if (data.hasOwnProperty('server_version')) {
                obj['server_version'] = ApiClient.convertToType(data['server_version'], 'String');
            }
            if (data.hasOwnProperty('start_time')) {
                obj['start_time'] = ApiClient.convertToType(data['start_time'], 'Date');
            }
            if (data.hasOwnProperty('vip')) {
                obj['vip'] = ApiClient.convertToType(data['vip'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetStatusOk</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetStatusOk</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetStatusOk.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['server_version'] && !(typeof data['server_version'] === 'string' || data['server_version'] instanceof String)) {
            throw new Error("Expected the field `server_version` to be a primitive type in the JSON string but got " + data['server_version']);
        }

        return true;
    }


}

GetStatusOk.RequiredProperties = ["players", "server_version", "start_time"];

/**
 * Current online player count
 * @member {Number} players
 */
GetStatusOk.prototype['players'] = undefined;

/**
 * Running version as string
 * @member {String} server_version
 */
GetStatusOk.prototype['server_version'] = undefined;

/**
 * Server start timestamp
 * @member {Date} start_time
 */
GetStatusOk.prototype['start_time'] = undefined;

/**
 * If the server is in VIP mode
 * @member {Boolean} vip
 */
GetStatusOk.prototype['vip'] = undefined;






export default GetStatusOk;

