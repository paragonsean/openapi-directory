/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetCorporationsCorporationIdStarbases200Ok model module.
 * @module model/GetCorporationsCorporationIdStarbases200Ok
 * @version 0.8.6
 */
class GetCorporationsCorporationIdStarbases200Ok {
    /**
     * Constructs a new <code>GetCorporationsCorporationIdStarbases200Ok</code>.
     * 200 ok object
     * @alias module:model/GetCorporationsCorporationIdStarbases200Ok
     * @param starbaseId {Number} Unique ID for this starbase (POS)
     * @param systemId {Number} The solar system this starbase (POS) is in, unanchored POSes have this information
     * @param typeId {Number} Starbase (POS) type
     */
    constructor(starbaseId, systemId, typeId) { 
        
        GetCorporationsCorporationIdStarbases200Ok.initialize(this, starbaseId, systemId, typeId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, starbaseId, systemId, typeId) { 
        obj['starbase_id'] = starbaseId;
        obj['system_id'] = systemId;
        obj['type_id'] = typeId;
    }

    /**
     * Constructs a <code>GetCorporationsCorporationIdStarbases200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetCorporationsCorporationIdStarbases200Ok} obj Optional instance to populate.
     * @return {module:model/GetCorporationsCorporationIdStarbases200Ok} The populated <code>GetCorporationsCorporationIdStarbases200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetCorporationsCorporationIdStarbases200Ok();

            if (data.hasOwnProperty('moon_id')) {
                obj['moon_id'] = ApiClient.convertToType(data['moon_id'], 'Number');
            }
            if (data.hasOwnProperty('onlined_since')) {
                obj['onlined_since'] = ApiClient.convertToType(data['onlined_since'], 'Date');
            }
            if (data.hasOwnProperty('reinforced_until')) {
                obj['reinforced_until'] = ApiClient.convertToType(data['reinforced_until'], 'Date');
            }
            if (data.hasOwnProperty('starbase_id')) {
                obj['starbase_id'] = ApiClient.convertToType(data['starbase_id'], 'Number');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('system_id')) {
                obj['system_id'] = ApiClient.convertToType(data['system_id'], 'Number');
            }
            if (data.hasOwnProperty('type_id')) {
                obj['type_id'] = ApiClient.convertToType(data['type_id'], 'Number');
            }
            if (data.hasOwnProperty('unanchor_at')) {
                obj['unanchor_at'] = ApiClient.convertToType(data['unanchor_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetCorporationsCorporationIdStarbases200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetCorporationsCorporationIdStarbases200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetCorporationsCorporationIdStarbases200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}

GetCorporationsCorporationIdStarbases200Ok.RequiredProperties = ["starbase_id", "system_id", "type_id"];

/**
 * The moon this starbase (POS) is anchored on, unanchored POSes do not have this information
 * @member {Number} moon_id
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['moon_id'] = undefined;

/**
 * When the POS onlined, for starbases (POSes) in online state
 * @member {Date} onlined_since
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['onlined_since'] = undefined;

/**
 * When the POS will be out of reinforcement, for starbases (POSes) in reinforced state
 * @member {Date} reinforced_until
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['reinforced_until'] = undefined;

/**
 * Unique ID for this starbase (POS)
 * @member {Number} starbase_id
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['starbase_id'] = undefined;

/**
 * state string
 * @member {module:model/GetCorporationsCorporationIdStarbases200Ok.StateEnum} state
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['state'] = undefined;

/**
 * The solar system this starbase (POS) is in, unanchored POSes have this information
 * @member {Number} system_id
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['system_id'] = undefined;

/**
 * Starbase (POS) type
 * @member {Number} type_id
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['type_id'] = undefined;

/**
 * When the POS started unanchoring, for starbases (POSes) in unanchoring state
 * @member {Date} unanchor_at
 */
GetCorporationsCorporationIdStarbases200Ok.prototype['unanchor_at'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
GetCorporationsCorporationIdStarbases200Ok['StateEnum'] = {

    /**
     * value: "offline"
     * @const
     */
    "offline": "offline",

    /**
     * value: "online"
     * @const
     */
    "online": "online",

    /**
     * value: "onlining"
     * @const
     */
    "onlining": "onlining",

    /**
     * value: "reinforced"
     * @const
     */
    "reinforced": "reinforced",

    /**
     * value: "unanchoring"
     * @const
     */
    "unanchoring": "unanchoring"
};



export default GetCorporationsCorporationIdStarbases200Ok;

