/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetCharactersCharacterIdMailRecipient model module.
 * @module model/GetCharactersCharacterIdMailRecipient
 * @version 0.8.6
 */
class GetCharactersCharacterIdMailRecipient {
    /**
     * Constructs a new <code>GetCharactersCharacterIdMailRecipient</code>.
     * recipient object
     * @alias module:model/GetCharactersCharacterIdMailRecipient
     * @param recipientId {Number} recipient_id integer
     * @param recipientType {module:model/GetCharactersCharacterIdMailRecipient.RecipientTypeEnum} recipient_type string
     */
    constructor(recipientId, recipientType) { 
        
        GetCharactersCharacterIdMailRecipient.initialize(this, recipientId, recipientType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, recipientId, recipientType) { 
        obj['recipient_id'] = recipientId;
        obj['recipient_type'] = recipientType;
    }

    /**
     * Constructs a <code>GetCharactersCharacterIdMailRecipient</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetCharactersCharacterIdMailRecipient} obj Optional instance to populate.
     * @return {module:model/GetCharactersCharacterIdMailRecipient} The populated <code>GetCharactersCharacterIdMailRecipient</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetCharactersCharacterIdMailRecipient();

            if (data.hasOwnProperty('recipient_id')) {
                obj['recipient_id'] = ApiClient.convertToType(data['recipient_id'], 'Number');
            }
            if (data.hasOwnProperty('recipient_type')) {
                obj['recipient_type'] = ApiClient.convertToType(data['recipient_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetCharactersCharacterIdMailRecipient</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetCharactersCharacterIdMailRecipient</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetCharactersCharacterIdMailRecipient.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['recipient_type'] && !(typeof data['recipient_type'] === 'string' || data['recipient_type'] instanceof String)) {
            throw new Error("Expected the field `recipient_type` to be a primitive type in the JSON string but got " + data['recipient_type']);
        }

        return true;
    }


}

GetCharactersCharacterIdMailRecipient.RequiredProperties = ["recipient_id", "recipient_type"];

/**
 * recipient_id integer
 * @member {Number} recipient_id
 */
GetCharactersCharacterIdMailRecipient.prototype['recipient_id'] = undefined;

/**
 * recipient_type string
 * @member {module:model/GetCharactersCharacterIdMailRecipient.RecipientTypeEnum} recipient_type
 */
GetCharactersCharacterIdMailRecipient.prototype['recipient_type'] = undefined;





/**
 * Allowed values for the <code>recipient_type</code> property.
 * @enum {String}
 * @readonly
 */
GetCharactersCharacterIdMailRecipient['RecipientTypeEnum'] = {

    /**
     * value: "alliance"
     * @const
     */
    "alliance": "alliance",

    /**
     * value: "character"
     * @const
     */
    "character": "character",

    /**
     * value: "corporation"
     * @const
     */
    "corporation": "corporation",

    /**
     * value: "mailing_list"
     * @const
     */
    "mailing_list": "mailing_list"
};



export default GetCharactersCharacterIdMailRecipient;

