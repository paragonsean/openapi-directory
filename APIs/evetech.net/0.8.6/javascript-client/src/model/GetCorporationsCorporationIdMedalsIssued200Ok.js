/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetCorporationsCorporationIdMedalsIssued200Ok model module.
 * @module model/GetCorporationsCorporationIdMedalsIssued200Ok
 * @version 0.8.6
 */
class GetCorporationsCorporationIdMedalsIssued200Ok {
    /**
     * Constructs a new <code>GetCorporationsCorporationIdMedalsIssued200Ok</code>.
     * 200 ok object
     * @alias module:model/GetCorporationsCorporationIdMedalsIssued200Ok
     * @param characterId {Number} ID of the character who was rewarded this medal
     * @param issuedAt {Date} issued_at string
     * @param issuerId {Number} ID of the character who issued the medal
     * @param medalId {Number} medal_id integer
     * @param reason {String} reason string
     * @param status {module:model/GetCorporationsCorporationIdMedalsIssued200Ok.StatusEnum} status string
     */
    constructor(characterId, issuedAt, issuerId, medalId, reason, status) { 
        
        GetCorporationsCorporationIdMedalsIssued200Ok.initialize(this, characterId, issuedAt, issuerId, medalId, reason, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, characterId, issuedAt, issuerId, medalId, reason, status) { 
        obj['character_id'] = characterId;
        obj['issued_at'] = issuedAt;
        obj['issuer_id'] = issuerId;
        obj['medal_id'] = medalId;
        obj['reason'] = reason;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>GetCorporationsCorporationIdMedalsIssued200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetCorporationsCorporationIdMedalsIssued200Ok} obj Optional instance to populate.
     * @return {module:model/GetCorporationsCorporationIdMedalsIssued200Ok} The populated <code>GetCorporationsCorporationIdMedalsIssued200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetCorporationsCorporationIdMedalsIssued200Ok();

            if (data.hasOwnProperty('character_id')) {
                obj['character_id'] = ApiClient.convertToType(data['character_id'], 'Number');
            }
            if (data.hasOwnProperty('issued_at')) {
                obj['issued_at'] = ApiClient.convertToType(data['issued_at'], 'Date');
            }
            if (data.hasOwnProperty('issuer_id')) {
                obj['issuer_id'] = ApiClient.convertToType(data['issuer_id'], 'Number');
            }
            if (data.hasOwnProperty('medal_id')) {
                obj['medal_id'] = ApiClient.convertToType(data['medal_id'], 'Number');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetCorporationsCorporationIdMedalsIssued200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetCorporationsCorporationIdMedalsIssued200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetCorporationsCorporationIdMedalsIssued200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

GetCorporationsCorporationIdMedalsIssued200Ok.RequiredProperties = ["character_id", "issued_at", "issuer_id", "medal_id", "reason", "status"];

/**
 * ID of the character who was rewarded this medal
 * @member {Number} character_id
 */
GetCorporationsCorporationIdMedalsIssued200Ok.prototype['character_id'] = undefined;

/**
 * issued_at string
 * @member {Date} issued_at
 */
GetCorporationsCorporationIdMedalsIssued200Ok.prototype['issued_at'] = undefined;

/**
 * ID of the character who issued the medal
 * @member {Number} issuer_id
 */
GetCorporationsCorporationIdMedalsIssued200Ok.prototype['issuer_id'] = undefined;

/**
 * medal_id integer
 * @member {Number} medal_id
 */
GetCorporationsCorporationIdMedalsIssued200Ok.prototype['medal_id'] = undefined;

/**
 * reason string
 * @member {String} reason
 */
GetCorporationsCorporationIdMedalsIssued200Ok.prototype['reason'] = undefined;

/**
 * status string
 * @member {module:model/GetCorporationsCorporationIdMedalsIssued200Ok.StatusEnum} status
 */
GetCorporationsCorporationIdMedalsIssued200Ok.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
GetCorporationsCorporationIdMedalsIssued200Ok['StatusEnum'] = {

    /**
     * value: "private"
     * @const
     */
    "private": "private",

    /**
     * value: "public"
     * @const
     */
    "public": "public"
};



export default GetCorporationsCorporationIdMedalsIssued200Ok;

