/**
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 0.8.6
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GetIndustrySystemsCostIndice from './GetIndustrySystemsCostIndice';

/**
 * The GetIndustrySystems200Ok model module.
 * @module model/GetIndustrySystems200Ok
 * @version 0.8.6
 */
class GetIndustrySystems200Ok {
    /**
     * Constructs a new <code>GetIndustrySystems200Ok</code>.
     * 200 ok object
     * @alias module:model/GetIndustrySystems200Ok
     * @param costIndices {Array.<module:model/GetIndustrySystemsCostIndice>} cost_indices array
     * @param solarSystemId {Number} solar_system_id integer
     */
    constructor(costIndices, solarSystemId) { 
        
        GetIndustrySystems200Ok.initialize(this, costIndices, solarSystemId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, costIndices, solarSystemId) { 
        obj['cost_indices'] = costIndices;
        obj['solar_system_id'] = solarSystemId;
    }

    /**
     * Constructs a <code>GetIndustrySystems200Ok</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetIndustrySystems200Ok} obj Optional instance to populate.
     * @return {module:model/GetIndustrySystems200Ok} The populated <code>GetIndustrySystems200Ok</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetIndustrySystems200Ok();

            if (data.hasOwnProperty('cost_indices')) {
                obj['cost_indices'] = ApiClient.convertToType(data['cost_indices'], [GetIndustrySystemsCostIndice]);
            }
            if (data.hasOwnProperty('solar_system_id')) {
                obj['solar_system_id'] = ApiClient.convertToType(data['solar_system_id'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetIndustrySystems200Ok</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetIndustrySystems200Ok</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetIndustrySystems200Ok.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['cost_indices']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['cost_indices'])) {
                throw new Error("Expected the field `cost_indices` to be an array in the JSON data but got " + data['cost_indices']);
            }
            // validate the optional field `cost_indices` (array)
            for (const item of data['cost_indices']) {
                GetIndustrySystemsCostIndice.validateJSON(item);
            };
        }

        return true;
    }


}

GetIndustrySystems200Ok.RequiredProperties = ["cost_indices", "solar_system_id"];

/**
 * cost_indices array
 * @member {Array.<module:model/GetIndustrySystemsCostIndice>} cost_indices
 */
GetIndustrySystems200Ok.prototype['cost_indices'] = undefined;

/**
 * solar_system_id integer
 * @member {Number} solar_system_id
 */
GetIndustrySystems200Ok.prototype['solar_system_id'] = undefined;






export default GetIndustrySystems200Ok;

