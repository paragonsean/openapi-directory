/**
 * Listen API: Podcast Search, Directory, and Insights API
 * Simple & no-nonsense podcast search & directory API. Search all podcasts and episodes by people, places, or topics. 
 *
 * The version of the OpenAPI document: 2.0
 * Contact: hello@listennotes.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EpisodeSimple from './EpisodeSimple';
import PodcastSimple from './PodcastSimple';

/**
 * The GetPodcastsInBatchResponse model module.
 * @module model/GetPodcastsInBatchResponse
 * @version 2.0
 */
class GetPodcastsInBatchResponse {
    /**
     * Constructs a new <code>GetPodcastsInBatchResponse</code>.
     * @alias module:model/GetPodcastsInBatchResponse
     * @param podcasts {Array.<module:model/PodcastSimple>} 
     */
    constructor(podcasts) { 
        
        GetPodcastsInBatchResponse.initialize(this, podcasts);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, podcasts) { 
        obj['podcasts'] = podcasts;
    }

    /**
     * Constructs a <code>GetPodcastsInBatchResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetPodcastsInBatchResponse} obj Optional instance to populate.
     * @return {module:model/GetPodcastsInBatchResponse} The populated <code>GetPodcastsInBatchResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetPodcastsInBatchResponse();

            if (data.hasOwnProperty('latest_episodes')) {
                obj['latest_episodes'] = ApiClient.convertToType(data['latest_episodes'], [EpisodeSimple]);
            }
            if (data.hasOwnProperty('podcasts')) {
                obj['podcasts'] = ApiClient.convertToType(data['podcasts'], [PodcastSimple]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetPodcastsInBatchResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetPodcastsInBatchResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetPodcastsInBatchResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['latest_episodes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['latest_episodes'])) {
                throw new Error("Expected the field `latest_episodes` to be an array in the JSON data but got " + data['latest_episodes']);
            }
            // validate the optional field `latest_episodes` (array)
            for (const item of data['latest_episodes']) {
                EpisodeSimple.validateJSON(item);
            };
        }
        if (data['podcasts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['podcasts'])) {
                throw new Error("Expected the field `podcasts` to be an array in the JSON data but got " + data['podcasts']);
            }
            // validate the optional field `podcasts` (array)
            for (const item of data['podcasts']) {
                PodcastSimple.validateJSON(item);
            };
        }

        return true;
    }


}

GetPodcastsInBatchResponse.RequiredProperties = ["podcasts"];

/**
 * Up to 10 latest episodes from these podcasts, sorted by **pub_date**. This field shows up only when **show_latest_episodes** is 1. 
 * @member {Array.<module:model/EpisodeSimple>} latest_episodes
 */
GetPodcastsInBatchResponse.prototype['latest_episodes'] = undefined;

/**
 * @member {Array.<module:model/PodcastSimple>} podcasts
 */
GetPodcastsInBatchResponse.prototype['podcasts'] = undefined;






export default GetPodcastsInBatchResponse;

