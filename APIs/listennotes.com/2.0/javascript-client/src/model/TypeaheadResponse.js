/**
 * Listen API: Podcast Search, Directory, and Insights API
 * Simple & no-nonsense podcast search & directory API. Search all podcasts and episodes by people, places, or topics. 
 *
 * The version of the OpenAPI document: 2.0
 * Contact: hello@listennotes.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Genre from './Genre';
import PodcastTypeaheadResult from './PodcastTypeaheadResult';

/**
 * The TypeaheadResponse model module.
 * @module model/TypeaheadResponse
 * @version 2.0
 */
class TypeaheadResponse {
    /**
     * Constructs a new <code>TypeaheadResponse</code>.
     * @alias module:model/TypeaheadResponse
     * @param terms {Array.<String>} Search term suggestions.
     */
    constructor(terms) { 
        
        TypeaheadResponse.initialize(this, terms);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, terms) { 
        obj['terms'] = terms;
    }

    /**
     * Constructs a <code>TypeaheadResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TypeaheadResponse} obj Optional instance to populate.
     * @return {module:model/TypeaheadResponse} The populated <code>TypeaheadResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TypeaheadResponse();

            if (data.hasOwnProperty('genres')) {
                obj['genres'] = ApiClient.convertToType(data['genres'], [Genre]);
            }
            if (data.hasOwnProperty('podcasts')) {
                obj['podcasts'] = ApiClient.convertToType(data['podcasts'], [PodcastTypeaheadResult]);
            }
            if (data.hasOwnProperty('terms')) {
                obj['terms'] = ApiClient.convertToType(data['terms'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TypeaheadResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TypeaheadResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TypeaheadResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['genres']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['genres'])) {
                throw new Error("Expected the field `genres` to be an array in the JSON data but got " + data['genres']);
            }
            // validate the optional field `genres` (array)
            for (const item of data['genres']) {
                Genre.validateJSON(item);
            };
        }
        if (data['podcasts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['podcasts'])) {
                throw new Error("Expected the field `podcasts` to be an array in the JSON data but got " + data['podcasts']);
            }
            // validate the optional field `podcasts` (array)
            for (const item of data['podcasts']) {
                PodcastTypeaheadResult.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['terms'])) {
            throw new Error("Expected the field `terms` to be an array in the JSON data but got " + data['terms']);
        }

        return true;
    }


}

TypeaheadResponse.RequiredProperties = ["terms"];

/**
 * Genre suggestions. It'll show up when the **show_genres** parameter is *1*.
 * @member {Array.<module:model/Genre>} genres
 */
TypeaheadResponse.prototype['genres'] = undefined;

/**
 * Podcast suggestions. It'll show up when the **show_podcasts** parameter is 1.
 * @member {Array.<module:model/PodcastTypeaheadResult>} podcasts
 */
TypeaheadResponse.prototype['podcasts'] = undefined;

/**
 * Search term suggestions.
 * @member {Array.<String>} terms
 */
TypeaheadResponse.prototype['terms'] = undefined;






export default TypeaheadResponse;

