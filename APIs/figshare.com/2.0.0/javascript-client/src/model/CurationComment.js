/**
 * Figshare API
 * Figshare apiv2. Using Swagger 2.0
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CurationComment model module.
 * @module model/CurationComment
 * @version 2.0.0
 */
class CurationComment {
    /**
     * Constructs a new <code>CurationComment</code>.
     * @alias module:model/CurationComment
     * @param accountId {Number} The ID of the account which generated this comment.
     * @param id {Number} The ID of the comment.
     * @param text {String} The value/content of the comment.
     * @param type {module:model/CurationComment.TypeEnum} The ID of the account which generated this comment.
     */
    constructor(accountId, id, text, type) { 
        
        CurationComment.initialize(this, accountId, id, text, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountId, id, text, type) { 
        obj['account_id'] = accountId;
        obj['id'] = id;
        obj['text'] = text;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>CurationComment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurationComment} obj Optional instance to populate.
     * @return {module:model/CurationComment} The populated <code>CurationComment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurationComment();

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurationComment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurationComment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CurationComment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

CurationComment.RequiredProperties = ["account_id", "id", "text", "type"];

/**
 * The ID of the account which generated this comment.
 * @member {Number} account_id
 */
CurationComment.prototype['account_id'] = undefined;

/**
 * The ID of the comment.
 * @member {Number} id
 */
CurationComment.prototype['id'] = undefined;

/**
 * The value/content of the comment.
 * @member {String} text
 */
CurationComment.prototype['text'] = undefined;

/**
 * The ID of the account which generated this comment.
 * @member {module:model/CurationComment.TypeEnum} type
 */
CurationComment.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
CurationComment['TypeEnum'] = {

    /**
     * value: "comment"
     * @const
     */
    "comment": "comment",

    /**
     * value: "approved"
     * @const
     */
    "approved": "approved",

    /**
     * value: "rejected"
     * @const
     */
    "rejected": "rejected",

    /**
     * value: "closed"
     * @const
     */
    "closed": "closed"
};



export default CurationComment;

