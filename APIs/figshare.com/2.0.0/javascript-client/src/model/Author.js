/**
 * Figshare API
 * Figshare apiv2. Using Swagger 2.0
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Author model module.
 * @module model/Author
 * @version 2.0.0
 */
class Author {
    /**
     * Constructs a new <code>Author</code>.
     * @alias module:model/Author
     * @param fullName {String} Author full name
     * @param id {Number} Author id
     * @param isActive {Boolean} True if author has published items
     * @param orcidId {String} Author Orcid
     * @param urlName {String} Author url name
     */
    constructor(fullName, id, isActive, orcidId, urlName) { 
        
        Author.initialize(this, fullName, id, isActive, orcidId, urlName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fullName, id, isActive, orcidId, urlName) { 
        obj['full_name'] = fullName;
        obj['id'] = id;
        obj['is_active'] = isActive;
        obj['orcid_id'] = orcidId;
        obj['url_name'] = urlName;
    }

    /**
     * Constructs a <code>Author</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Author} obj Optional instance to populate.
     * @return {module:model/Author} The populated <code>Author</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Author();

            if (data.hasOwnProperty('full_name')) {
                obj['full_name'] = ApiClient.convertToType(data['full_name'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
            if (data.hasOwnProperty('orcid_id')) {
                obj['orcid_id'] = ApiClient.convertToType(data['orcid_id'], 'String');
            }
            if (data.hasOwnProperty('url_name')) {
                obj['url_name'] = ApiClient.convertToType(data['url_name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Author</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Author</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Author.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['full_name'] && !(typeof data['full_name'] === 'string' || data['full_name'] instanceof String)) {
            throw new Error("Expected the field `full_name` to be a primitive type in the JSON string but got " + data['full_name']);
        }
        // ensure the json data is a string
        if (data['orcid_id'] && !(typeof data['orcid_id'] === 'string' || data['orcid_id'] instanceof String)) {
            throw new Error("Expected the field `orcid_id` to be a primitive type in the JSON string but got " + data['orcid_id']);
        }
        // ensure the json data is a string
        if (data['url_name'] && !(typeof data['url_name'] === 'string' || data['url_name'] instanceof String)) {
            throw new Error("Expected the field `url_name` to be a primitive type in the JSON string but got " + data['url_name']);
        }

        return true;
    }


}

Author.RequiredProperties = ["full_name", "id", "is_active", "orcid_id", "url_name"];

/**
 * Author full name
 * @member {String} full_name
 */
Author.prototype['full_name'] = undefined;

/**
 * Author id
 * @member {Number} id
 */
Author.prototype['id'] = undefined;

/**
 * True if author has published items
 * @member {Boolean} is_active
 */
Author.prototype['is_active'] = undefined;

/**
 * Author Orcid
 * @member {String} orcid_id
 */
Author.prototype['orcid_id'] = undefined;

/**
 * Author url name
 * @member {String} url_name
 */
Author.prototype['url_name'] = undefined;






export default Author;

