/**
 * Neutrino API
 * The general-purpose API
 *
 * The version of the OpenAPI document: 3.6.4
 * Contact: ops@neutrinoapi.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EmailValidateResponse model module.
 * @module model/EmailValidateResponse
 * @version 3.6.4
 */
class EmailValidateResponse {
    /**
     * Constructs a new <code>EmailValidateResponse</code>.
     * @alias module:model/EmailValidateResponse
     * @param domain {String} The email domain
     * @param domainError {Boolean} True if this address has a domain error (e.g. no valid mail server records)
     * @param email {String} The email address. If you have used the fix-typos option then this will be the fixed address
     * @param isDisposable {Boolean} True if this address is a disposable, temporary or darknet related email address
     * @param isFreemail {Boolean} True if this address is a free-mail address
     * @param isPersonal {Boolean} True if this address belongs to a person. False if this is a role based address, e.g. admin@, help@, office@, etc.
     * @param provider {String} The email service provider domain
     * @param syntaxError {Boolean} True if this address has a syntax error
     * @param typosFixed {Boolean} True if typos have been fixed
     * @param valid {Boolean} Is this a valid email
     */
    constructor(domain, domainError, email, isDisposable, isFreemail, isPersonal, provider, syntaxError, typosFixed, valid) { 
        
        EmailValidateResponse.initialize(this, domain, domainError, email, isDisposable, isFreemail, isPersonal, provider, syntaxError, typosFixed, valid);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, domain, domainError, email, isDisposable, isFreemail, isPersonal, provider, syntaxError, typosFixed, valid) { 
        obj['domain'] = domain;
        obj['domain-error'] = domainError;
        obj['email'] = email;
        obj['is-disposable'] = isDisposable;
        obj['is-freemail'] = isFreemail;
        obj['is-personal'] = isPersonal;
        obj['provider'] = provider;
        obj['syntax-error'] = syntaxError;
        obj['typos-fixed'] = typosFixed;
        obj['valid'] = valid;
    }

    /**
     * Constructs a <code>EmailValidateResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmailValidateResponse} obj Optional instance to populate.
     * @return {module:model/EmailValidateResponse} The populated <code>EmailValidateResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmailValidateResponse();

            if (data.hasOwnProperty('domain')) {
                obj['domain'] = ApiClient.convertToType(data['domain'], 'String');
            }
            if (data.hasOwnProperty('domain-error')) {
                obj['domain-error'] = ApiClient.convertToType(data['domain-error'], 'Boolean');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('is-disposable')) {
                obj['is-disposable'] = ApiClient.convertToType(data['is-disposable'], 'Boolean');
            }
            if (data.hasOwnProperty('is-freemail')) {
                obj['is-freemail'] = ApiClient.convertToType(data['is-freemail'], 'Boolean');
            }
            if (data.hasOwnProperty('is-personal')) {
                obj['is-personal'] = ApiClient.convertToType(data['is-personal'], 'Boolean');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('syntax-error')) {
                obj['syntax-error'] = ApiClient.convertToType(data['syntax-error'], 'Boolean');
            }
            if (data.hasOwnProperty('typos-fixed')) {
                obj['typos-fixed'] = ApiClient.convertToType(data['typos-fixed'], 'Boolean');
            }
            if (data.hasOwnProperty('valid')) {
                obj['valid'] = ApiClient.convertToType(data['valid'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmailValidateResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmailValidateResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EmailValidateResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['domain'] && !(typeof data['domain'] === 'string' || data['domain'] instanceof String)) {
            throw new Error("Expected the field `domain` to be a primitive type in the JSON string but got " + data['domain']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }

        return true;
    }


}

EmailValidateResponse.RequiredProperties = ["domain", "domain-error", "email", "is-disposable", "is-freemail", "is-personal", "provider", "syntax-error", "typos-fixed", "valid"];

/**
 * The email domain
 * @member {String} domain
 */
EmailValidateResponse.prototype['domain'] = undefined;

/**
 * True if this address has a domain error (e.g. no valid mail server records)
 * @member {Boolean} domain-error
 */
EmailValidateResponse.prototype['domain-error'] = undefined;

/**
 * The email address. If you have used the fix-typos option then this will be the fixed address
 * @member {String} email
 */
EmailValidateResponse.prototype['email'] = undefined;

/**
 * True if this address is a disposable, temporary or darknet related email address
 * @member {Boolean} is-disposable
 */
EmailValidateResponse.prototype['is-disposable'] = undefined;

/**
 * True if this address is a free-mail address
 * @member {Boolean} is-freemail
 */
EmailValidateResponse.prototype['is-freemail'] = undefined;

/**
 * True if this address belongs to a person. False if this is a role based address, e.g. admin@, help@, office@, etc.
 * @member {Boolean} is-personal
 */
EmailValidateResponse.prototype['is-personal'] = undefined;

/**
 * The email service provider domain
 * @member {String} provider
 */
EmailValidateResponse.prototype['provider'] = undefined;

/**
 * True if this address has a syntax error
 * @member {Boolean} syntax-error
 */
EmailValidateResponse.prototype['syntax-error'] = undefined;

/**
 * True if typos have been fixed
 * @member {Boolean} typos-fixed
 */
EmailValidateResponse.prototype['typos-fixed'] = undefined;

/**
 * Is this a valid email
 * @member {Boolean} valid
 */
EmailValidateResponse.prototype['valid'] = undefined;






export default EmailValidateResponse;

