/**
 * API Endpoints
 * Create beautiful product and API documentation with our developer friendly platform.
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Changelog model module.
 * @module model/Changelog
 * @version 2.0.0
 */
class Changelog {
    /**
     * Constructs a new <code>Changelog</code>.
     * @alias module:model/Changelog
     * @param body {String} Body content of the changelog
     * @param title {String} Title of the changelog
     */
    constructor(body, title) { 
        
        Changelog.initialize(this, body, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, body, title) { 
        obj['body'] = body;
        obj['hidden'] = true;
        obj['title'] = title;
        obj['type'] = '';
    }

    /**
     * Constructs a <code>Changelog</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Changelog} obj Optional instance to populate.
     * @return {module:model/Changelog} The populated <code>Changelog</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Changelog();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('hidden')) {
                obj['hidden'] = ApiClient.convertToType(data['hidden'], 'Boolean');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Changelog</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Changelog</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Changelog.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Changelog.RequiredProperties = ["body", "title"];

/**
 * Body content of the changelog
 * @member {String} body
 */
Changelog.prototype['body'] = undefined;

/**
 * Visibility of the changelog
 * @member {Boolean} hidden
 * @default true
 */
Changelog.prototype['hidden'] = true;

/**
 * Title of the changelog
 * @member {String} title
 */
Changelog.prototype['title'] = undefined;

/**
 * @member {module:model/Changelog.TypeEnum} type
 * @default ''
 */
Changelog.prototype['type'] = '';





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Changelog['TypeEnum'] = {

    /**
     * value: ""
     * @const
     */
    "empty": "",

    /**
     * value: "added"
     * @const
     */
    "added": "added",

    /**
     * value: "fixed"
     * @const
     */
    "fixed": "fixed",

    /**
     * value: "improved"
     * @const
     */
    "improved": "improved",

    /**
     * value: "deprecated"
     * @const
     */
    "deprecated": "deprecated",

    /**
     * value: "removed"
     * @const
     */
    "removed": "removed"
};



export default Changelog;

