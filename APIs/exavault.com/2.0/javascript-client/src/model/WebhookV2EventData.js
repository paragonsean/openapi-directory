/**
 * ExaVault
 * ExaVaults API allows you to incorporate ExaVaults suite of file transfer and user management tools into your own application.\\nExaVault supports both POST (recommended when requesting large data sets) and GET operations, and requires an API key in order to use.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@exavault.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WebhookV2EventDataResourcesInner from './WebhookV2EventDataResourcesInner';
import WebhookV2EventDataShareInner from './WebhookV2EventDataShareInner';

/**
 * The WebhookV2EventData model module.
 * @module model/WebhookV2EventData
 * @version 2.0
 */
class WebhookV2EventData {
    /**
     * Constructs a new <code>WebhookV2EventData</code>.
     * @alias module:model/WebhookV2EventData
     */
    constructor() { 
        
        WebhookV2EventData.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WebhookV2EventData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WebhookV2EventData} obj Optional instance to populate.
     * @return {module:model/WebhookV2EventData} The populated <code>WebhookV2EventData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebhookV2EventData();

            if (data.hasOwnProperty('formDetails')) {
                obj['formDetails'] = ApiClient.convertToType(data['formDetails'], [Object]);
            }
            if (data.hasOwnProperty('resources')) {
                obj['resources'] = ApiClient.convertToType(data['resources'], [WebhookV2EventDataResourcesInner]);
            }
            if (data.hasOwnProperty('share')) {
                obj['share'] = ApiClient.convertToType(data['share'], [WebhookV2EventDataShareInner]);
            }
            if (data.hasOwnProperty('transferStatus')) {
                obj['transferStatus'] = ApiClient.convertToType(data['transferStatus'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebhookV2EventData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebhookV2EventData</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['formDetails'])) {
            throw new Error("Expected the field `formDetails` to be an array in the JSON data but got " + data['formDetails']);
        }
        if (data['resources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['resources'])) {
                throw new Error("Expected the field `resources` to be an array in the JSON data but got " + data['resources']);
            }
            // validate the optional field `resources` (array)
            for (const item of data['resources']) {
                WebhookV2EventDataResourcesInner.validateJSON(item);
            };
        }
        if (data['share']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['share'])) {
                throw new Error("Expected the field `share` to be an array in the JSON data but got " + data['share']);
            }
            // validate the optional field `share` (array)
            for (const item of data['share']) {
                WebhookV2EventDataShareInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['transferStatus'] && !(typeof data['transferStatus'] === 'string' || data['transferStatus'] instanceof String)) {
            throw new Error("Expected the field `transferStatus` to be a primitive type in the JSON string but got " + data['transferStatus']);
        }

        return true;
    }


}



/**
 * @member {Array.<Object>} formDetails
 */
WebhookV2EventData.prototype['formDetails'] = undefined;

/**
 * @member {Array.<module:model/WebhookV2EventDataResourcesInner>} resources
 */
WebhookV2EventData.prototype['resources'] = undefined;

/**
 * @member {Array.<module:model/WebhookV2EventDataShareInner>} share
 */
WebhookV2EventData.prototype['share'] = undefined;

/**
 * For uploads, and downloads, whether the file transferred OK. `success` means the transfer did not have errors
 * @member {String} transferStatus
 */
WebhookV2EventData.prototype['transferStatus'] = undefined;






export default WebhookV2EventData;

