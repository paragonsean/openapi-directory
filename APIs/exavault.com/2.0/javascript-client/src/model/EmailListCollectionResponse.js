/**
 * ExaVault
 * ExaVaults API allows you to incorporate ExaVaults suite of file transfer and user management tools into your own application.\\nExaVault supports both POST (recommended when requesting large data sets) and GET operations, and requires an API key in order to use.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@exavault.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EmailList from './EmailList';
import User from './User';

/**
 * The EmailListCollectionResponse model module.
 * @module model/EmailListCollectionResponse
 * @version 2.0
 */
class EmailListCollectionResponse {
    /**
     * Constructs a new <code>EmailListCollectionResponse</code>.
     * Response object for list of email lists
     * @alias module:model/EmailListCollectionResponse
     */
    constructor() { 
        
        EmailListCollectionResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EmailListCollectionResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmailListCollectionResponse} obj Optional instance to populate.
     * @return {module:model/EmailListCollectionResponse} The populated <code>EmailListCollectionResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmailListCollectionResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [EmailList]);
            }
            if (data.hasOwnProperty('included')) {
                obj['included'] = ApiClient.convertToType(data['included'], [User]);
            }
            if (data.hasOwnProperty('responseStatus')) {
                obj['responseStatus'] = ApiClient.convertToType(data['responseStatus'], 'Number');
            }
            if (data.hasOwnProperty('returnedResults')) {
                obj['returnedResults'] = ApiClient.convertToType(data['returnedResults'], 'Number');
            }
            if (data.hasOwnProperty('totalResults')) {
                obj['totalResults'] = ApiClient.convertToType(data['totalResults'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmailListCollectionResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmailListCollectionResponse</code>.
     */
    static validateJSON(data) {
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                EmailList.validateJSON(item);
            };
        }
        if (data['included']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['included'])) {
                throw new Error("Expected the field `included` to be an array in the JSON data but got " + data['included']);
            }
            // validate the optional field `included` (array)
            for (const item of data['included']) {
                User.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/EmailList>} data
 */
EmailListCollectionResponse.prototype['data'] = undefined;

/**
 * @member {Array.<module:model/User>} included
 */
EmailListCollectionResponse.prototype['included'] = undefined;

/**
 * Http status of response
 * @member {Number} responseStatus
 */
EmailListCollectionResponse.prototype['responseStatus'] = undefined;

/**
 * @member {Number} returnedResults
 */
EmailListCollectionResponse.prototype['returnedResults'] = undefined;

/**
 * @member {Number} totalResults
 */
EmailListCollectionResponse.prototype['totalResults'] = undefined;






export default EmailListCollectionResponse;

