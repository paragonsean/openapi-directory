/**
 * ExaVault
 * ExaVaults API allows you to incorporate ExaVaults suite of file transfer and user management tools into your own application.\\nExaVault supports both POST (recommended when requesting large data sets) and GET operations, and requires an API key in order to use.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@exavault.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AddSSHKeyRequestBody model module.
 * @module model/AddSSHKeyRequestBody
 * @version 2.0
 */
class AddSSHKeyRequestBody {
    /**
     * Constructs a new <code>AddSSHKeyRequestBody</code>.
     * @alias module:model/AddSSHKeyRequestBody
     * @param publicKey {String} Public Key to provide ExaVault. You can provide the Public Key as formatted from the ssh-keygen command or a standard rfc-4716 format. 
     * @param userId {Number} ID of the user to assign the new key to. 
     */
    constructor(publicKey, userId) { 
        
        AddSSHKeyRequestBody.initialize(this, publicKey, userId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, publicKey, userId) { 
        obj['publicKey'] = publicKey;
        obj['userId'] = userId;
    }

    /**
     * Constructs a <code>AddSSHKeyRequestBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddSSHKeyRequestBody} obj Optional instance to populate.
     * @return {module:model/AddSSHKeyRequestBody} The populated <code>AddSSHKeyRequestBody</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddSSHKeyRequestBody();

            if (data.hasOwnProperty('publicKey')) {
                obj['publicKey'] = ApiClient.convertToType(data['publicKey'], 'String');
            }
            if (data.hasOwnProperty('userId')) {
                obj['userId'] = ApiClient.convertToType(data['userId'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddSSHKeyRequestBody</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddSSHKeyRequestBody</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddSSHKeyRequestBody.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['publicKey'] && !(typeof data['publicKey'] === 'string' || data['publicKey'] instanceof String)) {
            throw new Error("Expected the field `publicKey` to be a primitive type in the JSON string but got " + data['publicKey']);
        }

        return true;
    }


}

AddSSHKeyRequestBody.RequiredProperties = ["publicKey", "userId"];

/**
 * Public Key to provide ExaVault. You can provide the Public Key as formatted from the ssh-keygen command or a standard rfc-4716 format. 
 * @member {String} publicKey
 */
AddSSHKeyRequestBody.prototype['publicKey'] = undefined;

/**
 * ID of the user to assign the new key to. 
 * @member {Number} userId
 */
AddSSHKeyRequestBody.prototype['userId'] = undefined;






export default AddSSHKeyRequestBody;

