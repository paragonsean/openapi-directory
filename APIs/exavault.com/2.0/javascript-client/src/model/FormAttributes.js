/**
 * ExaVault
 * ExaVaults API allows you to incorporate ExaVaults suite of file transfer and user management tools into your own application.\\nExaVault supports both POST (recommended when requesting large data sets) and GET operations, and requires an API key in order to use.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@exavault.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FormField from './FormField';

/**
 * The FormAttributes model module.
 * @module model/FormAttributes
 * @version 2.0
 */
class FormAttributes {
    /**
     * Constructs a new <code>FormAttributes</code>.
     * Attributes of the form including it&#39;s included fields and css styles
     * @alias module:model/FormAttributes
     */
    constructor() { 
        
        FormAttributes.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FormAttributes</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FormAttributes} obj Optional instance to populate.
     * @return {module:model/FormAttributes} The populated <code>FormAttributes</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FormAttributes();

            if (data.hasOwnProperty('cssStyles')) {
                obj['cssStyles'] = ApiClient.convertToType(data['cssStyles'], 'String');
            }
            if (data.hasOwnProperty('elements')) {
                obj['elements'] = ApiClient.convertToType(data['elements'], [FormField]);
            }
            if (data.hasOwnProperty('formDescription')) {
                obj['formDescription'] = ApiClient.convertToType(data['formDescription'], 'String');
            }
            if (data.hasOwnProperty('submitButtonText')) {
                obj['submitButtonText'] = ApiClient.convertToType(data['submitButtonText'], 'String');
            }
            if (data.hasOwnProperty('successMessage')) {
                obj['successMessage'] = ApiClient.convertToType(data['successMessage'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FormAttributes</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FormAttributes</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['cssStyles'] && !(typeof data['cssStyles'] === 'string' || data['cssStyles'] instanceof String)) {
            throw new Error("Expected the field `cssStyles` to be a primitive type in the JSON string but got " + data['cssStyles']);
        }
        if (data['elements']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['elements'])) {
                throw new Error("Expected the field `elements` to be an array in the JSON data but got " + data['elements']);
            }
            // validate the optional field `elements` (array)
            for (const item of data['elements']) {
                FormField.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['formDescription'] && !(typeof data['formDescription'] === 'string' || data['formDescription'] instanceof String)) {
            throw new Error("Expected the field `formDescription` to be a primitive type in the JSON string but got " + data['formDescription']);
        }
        // ensure the json data is a string
        if (data['submitButtonText'] && !(typeof data['submitButtonText'] === 'string' || data['submitButtonText'] instanceof String)) {
            throw new Error("Expected the field `submitButtonText` to be a primitive type in the JSON string but got " + data['submitButtonText']);
        }
        // ensure the json data is a string
        if (data['successMessage'] && !(typeof data['successMessage'] === 'string' || data['successMessage'] instanceof String)) {
            throw new Error("Expected the field `successMessage` to be a primitive type in the JSON string but got " + data['successMessage']);
        }

        return true;
    }


}



/**
 * CSS Styles of the form.
 * @member {String} cssStyles
 */
FormAttributes.prototype['cssStyles'] = undefined;

/**
 * Array of form fields defined for the form
 * @member {Array.<module:model/FormField>} elements
 */
FormAttributes.prototype['elements'] = undefined;

/**
 * Text that appears at the top of a receive form
 * @member {String} formDescription
 */
FormAttributes.prototype['formDescription'] = undefined;

/**
 * Text that appears on the submit button for the form
 * @member {String} submitButtonText
 */
FormAttributes.prototype['submitButtonText'] = undefined;

/**
 * Message displayed to submitter after files are uploaded
 * @member {String} successMessage
 */
FormAttributes.prototype['successMessage'] = undefined;






export default FormAttributes;

