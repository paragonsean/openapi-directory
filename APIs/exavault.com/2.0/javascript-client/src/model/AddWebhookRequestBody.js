/**
 * ExaVault
 * ExaVaults API allows you to incorporate ExaVaults suite of file transfer and user management tools into your own application.\\nExaVault supports both POST (recommended when requesting large data sets) and GET operations, and requires an API key in order to use.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@exavault.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WebhookTriggers from './WebhookTriggers';

/**
 * The AddWebhookRequestBody model module.
 * @module model/AddWebhookRequestBody
 * @version 2.0
 */
class AddWebhookRequestBody {
    /**
     * Constructs a new <code>AddWebhookRequestBody</code>.
     * @alias module:model/AddWebhookRequestBody
     * @param endpointUrl {String} The endpoint is where the webhook request will be sent.
     */
    constructor(endpointUrl) { 
        
        AddWebhookRequestBody.initialize(this, endpointUrl);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endpointUrl) { 
        obj['endpointUrl'] = endpointUrl;
    }

    /**
     * Constructs a <code>AddWebhookRequestBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddWebhookRequestBody} obj Optional instance to populate.
     * @return {module:model/AddWebhookRequestBody} The populated <code>AddWebhookRequestBody</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddWebhookRequestBody();

            if (data.hasOwnProperty('endpointUrl')) {
                obj['endpointUrl'] = ApiClient.convertToType(data['endpointUrl'], 'String');
            }
            if (data.hasOwnProperty('resource')) {
                obj['resource'] = ApiClient.convertToType(data['resource'], 'String');
            }
            if (data.hasOwnProperty('responseVersion')) {
                obj['responseVersion'] = ApiClient.convertToType(data['responseVersion'], 'String');
            }
            if (data.hasOwnProperty('triggers')) {
                obj['triggers'] = WebhookTriggers.constructFromObject(data['triggers']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddWebhookRequestBody</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddWebhookRequestBody</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddWebhookRequestBody.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['endpointUrl'] && !(typeof data['endpointUrl'] === 'string' || data['endpointUrl'] instanceof String)) {
            throw new Error("Expected the field `endpointUrl` to be a primitive type in the JSON string but got " + data['endpointUrl']);
        }
        // ensure the json data is a string
        if (data['resource'] && !(typeof data['resource'] === 'string' || data['resource'] instanceof String)) {
            throw new Error("Expected the field `resource` to be a primitive type in the JSON string but got " + data['resource']);
        }
        // ensure the json data is a string
        if (data['responseVersion'] && !(typeof data['responseVersion'] === 'string' || data['responseVersion'] instanceof String)) {
            throw new Error("Expected the field `responseVersion` to be a primitive type in the JSON string but got " + data['responseVersion']);
        }
        // validate the optional field `triggers`
        if (data['triggers']) { // data not null
          WebhookTriggers.validateJSON(data['triggers']);
        }

        return true;
    }


}

AddWebhookRequestBody.RequiredProperties = ["endpointUrl"];

/**
 * The endpoint is where the webhook request will be sent.
 * @member {String} endpointUrl
 */
AddWebhookRequestBody.prototype['endpointUrl'] = undefined;

/**
 * Resource identifier for the top folder this webhook is associated with
 * @member {String} resource
 */
AddWebhookRequestBody.prototype['resource'] = undefined;

/**
 * What version of webhook request should be sent to the endpoint URL when messages are sent
 * @member {module:model/AddWebhookRequestBody.ResponseVersionEnum} responseVersion
 */
AddWebhookRequestBody.prototype['responseVersion'] = undefined;

/**
 * @member {module:model/WebhookTriggers} triggers
 */
AddWebhookRequestBody.prototype['triggers'] = undefined;





/**
 * Allowed values for the <code>responseVersion</code> property.
 * @enum {String}
 * @readonly
 */
AddWebhookRequestBody['ResponseVersionEnum'] = {

    /**
     * value: "v1"
     * @const
     */
    "v1": "v1",

    /**
     * value: "v2"
     * @const
     */
    "v2": "v2"
};



export default AddWebhookRequestBody;

