/**
 * ExaVault
 * ExaVaults API allows you to incorporate ExaVaults suite of file transfer and user management tools into your own application.\\nExaVault supports both POST (recommended when requesting large data sets) and GET operations, and requires an API key in order to use.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@exavault.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ShareRecipient model module.
 * @module model/ShareRecipient
 * @version 2.0
 */
class ShareRecipient {
    /**
     * Constructs a new <code>ShareRecipient</code>.
     * @alias module:model/ShareRecipient
     */
    constructor() { 
        
        ShareRecipient.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ShareRecipient</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ShareRecipient} obj Optional instance to populate.
     * @return {module:model/ShareRecipient} The populated <code>ShareRecipient</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ShareRecipient();

            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Date');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('hash')) {
                obj['hash'] = ApiClient.convertToType(data['hash'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('received')) {
                obj['received'] = ApiClient.convertToType(data['received'], 'Boolean');
            }
            if (data.hasOwnProperty('sent')) {
                obj['sent'] = ApiClient.convertToType(data['sent'], 'Boolean');
            }
            if (data.hasOwnProperty('shareId')) {
                obj['shareId'] = ApiClient.convertToType(data['shareId'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ShareRecipient</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ShareRecipient</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['hash'] && !(typeof data['hash'] === 'string' || data['hash'] instanceof String)) {
            throw new Error("Expected the field `hash` to be a primitive type in the JSON string but got " + data['hash']);
        }
        // ensure the json data is a string
        if (data['shareId'] && !(typeof data['shareId'] === 'string' || data['shareId'] instanceof String)) {
            throw new Error("Expected the field `shareId` to be a primitive type in the JSON string but got " + data['shareId']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * Timestamp of adding recipient to the share.
 * @member {Date} created
 */
ShareRecipient.prototype['created'] = undefined;

/**
 * Recipient email address.
 * @member {String} email
 */
ShareRecipient.prototype['email'] = undefined;

/**
 * Share hash.
 * @member {String} hash
 */
ShareRecipient.prototype['hash'] = undefined;

/**
 * ID of the recipient.
 * @member {Number} id
 */
ShareRecipient.prototype['id'] = undefined;

/**
 * Set to true if recipient has accessed the share. Note this is set to true when the recipient clicks the link to access the share; not when they download a file.
 * @member {Boolean} received
 */
ShareRecipient.prototype['received'] = undefined;

/**
 * Set to true if invite email was sent; false otherwise.
 * @member {Boolean} sent
 */
ShareRecipient.prototype['sent'] = undefined;

/**
 * ID of the share that the recipoient belongs to.
 * @member {String} shareId
 */
ShareRecipient.prototype['shareId'] = undefined;

/**
 * Type of the recipient.
 * @member {module:model/ShareRecipient.TypeEnum} type
 */
ShareRecipient.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ShareRecipient['TypeEnum'] = {

    /**
     * value: "owner"
     * @const
     */
    "owner": "owner",

    /**
     * value: "direct"
     * @const
     */
    "direct": "direct"
};



export default ShareRecipient;

