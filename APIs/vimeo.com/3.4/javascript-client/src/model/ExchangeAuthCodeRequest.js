/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExchangeAuthCodeRequest model module.
 * @module model/ExchangeAuthCodeRequest
 * @version 3.4
 */
class ExchangeAuthCodeRequest {
    /**
     * Constructs a new <code>ExchangeAuthCodeRequest</code>.
     * @alias module:model/ExchangeAuthCodeRequest
     * @param code {String} The authorization code received from the authorization server.
     * @param grantType {module:model/ExchangeAuthCodeRequest.GrantTypeEnum} The grant type. Must be set to `authorization_code`.
     * @param redirectUri {String} The redirect URI. Must match the URI from `/oauth/authorize`.
     */
    constructor(code, grantType, redirectUri) { 
        
        ExchangeAuthCodeRequest.initialize(this, code, grantType, redirectUri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, code, grantType, redirectUri) { 
        obj['code'] = code;
        obj['grant_type'] = grantType;
        obj['redirect_uri'] = redirectUri;
    }

    /**
     * Constructs a <code>ExchangeAuthCodeRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExchangeAuthCodeRequest} obj Optional instance to populate.
     * @return {module:model/ExchangeAuthCodeRequest} The populated <code>ExchangeAuthCodeRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExchangeAuthCodeRequest();

            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('grant_type')) {
                obj['grant_type'] = ApiClient.convertToType(data['grant_type'], 'String');
            }
            if (data.hasOwnProperty('redirect_uri')) {
                obj['redirect_uri'] = ApiClient.convertToType(data['redirect_uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExchangeAuthCodeRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExchangeAuthCodeRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExchangeAuthCodeRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['grant_type'] && !(typeof data['grant_type'] === 'string' || data['grant_type'] instanceof String)) {
            throw new Error("Expected the field `grant_type` to be a primitive type in the JSON string but got " + data['grant_type']);
        }
        // ensure the json data is a string
        if (data['redirect_uri'] && !(typeof data['redirect_uri'] === 'string' || data['redirect_uri'] instanceof String)) {
            throw new Error("Expected the field `redirect_uri` to be a primitive type in the JSON string but got " + data['redirect_uri']);
        }

        return true;
    }


}

ExchangeAuthCodeRequest.RequiredProperties = ["code", "grant_type", "redirect_uri"];

/**
 * The authorization code received from the authorization server.
 * @member {String} code
 */
ExchangeAuthCodeRequest.prototype['code'] = undefined;

/**
 * The grant type. Must be set to `authorization_code`.
 * @member {module:model/ExchangeAuthCodeRequest.GrantTypeEnum} grant_type
 */
ExchangeAuthCodeRequest.prototype['grant_type'] = undefined;

/**
 * The redirect URI. Must match the URI from `/oauth/authorize`.
 * @member {String} redirect_uri
 */
ExchangeAuthCodeRequest.prototype['redirect_uri'] = undefined;





/**
 * Allowed values for the <code>grant_type</code> property.
 * @enum {String}
 * @readonly
 */
ExchangeAuthCodeRequest['GrantTypeEnum'] = {

    /**
     * value: "authorization_code"
     * @const
     */
    "authorization_code": "authorization_code"
};



export default ExchangeAuthCodeRequest;

