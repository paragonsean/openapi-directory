/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OnDemandPromotionCode model module.
 * @module model/OnDemandPromotionCode
 * @version 3.4
 */
class OnDemandPromotionCode {
    /**
     * Constructs a new <code>OnDemandPromotionCode</code>.
     * @alias module:model/OnDemandPromotionCode
     * @param code {String} A promotion code that can be redeemed on Vimeo.
     * @param link {String} A link to redeem the promotion code instantly.
     * @param maxUses {Number} The total amount of times this code can be used.
     * @param uses {Number} The number of times that this code has been used.
     */
    constructor(code, link, maxUses, uses) { 
        
        OnDemandPromotionCode.initialize(this, code, link, maxUses, uses);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, code, link, maxUses, uses) { 
        obj['code'] = code;
        obj['link'] = link;
        obj['max_uses'] = maxUses;
        obj['uses'] = uses;
    }

    /**
     * Constructs a <code>OnDemandPromotionCode</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OnDemandPromotionCode} obj Optional instance to populate.
     * @return {module:model/OnDemandPromotionCode} The populated <code>OnDemandPromotionCode</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OnDemandPromotionCode();

            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('link')) {
                obj['link'] = ApiClient.convertToType(data['link'], 'String');
            }
            if (data.hasOwnProperty('max_uses')) {
                obj['max_uses'] = ApiClient.convertToType(data['max_uses'], 'Number');
            }
            if (data.hasOwnProperty('uses')) {
                obj['uses'] = ApiClient.convertToType(data['uses'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OnDemandPromotionCode</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OnDemandPromotionCode</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OnDemandPromotionCode.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['link'] && !(typeof data['link'] === 'string' || data['link'] instanceof String)) {
            throw new Error("Expected the field `link` to be a primitive type in the JSON string but got " + data['link']);
        }

        return true;
    }


}

OnDemandPromotionCode.RequiredProperties = ["code", "link", "max_uses", "uses"];

/**
 * A promotion code that can be redeemed on Vimeo.
 * @member {String} code
 */
OnDemandPromotionCode.prototype['code'] = undefined;

/**
 * A link to redeem the promotion code instantly.
 * @member {String} link
 */
OnDemandPromotionCode.prototype['link'] = undefined;

/**
 * The total amount of times this code can be used.
 * @member {Number} max_uses
 */
OnDemandPromotionCode.prototype['max_uses'] = undefined;

/**
 * The number of times that this code has been used.
 * @member {Number} uses
 */
OnDemandPromotionCode.prototype['uses'] = undefined;






export default OnDemandPromotionCode;

