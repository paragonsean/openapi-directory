/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OnDemandGenreInteractions from './OnDemandGenreInteractions';
import OnDemandGenreMetadata from './OnDemandGenreMetadata';

/**
 * The OnDemandGenre model module.
 * @module model/OnDemandGenre
 * @version 3.4
 */
class OnDemandGenre {
    /**
     * Constructs a new <code>OnDemandGenre</code>.
     * @alias module:model/OnDemandGenre
     * @param canonical {String} The canonical name or URL slug of the genre.
     * @param interactions {module:model/OnDemandGenreInteractions} 
     * @param link {String} The Vimeo URL for this genre.
     * @param metadata {module:model/OnDemandGenreMetadata} 
     * @param name {String} The descriptive name of the genre.
     * @param uri {String} The relative URI of the On Demand genre.
     */
    constructor(canonical, interactions, link, metadata, name, uri) { 
        
        OnDemandGenre.initialize(this, canonical, interactions, link, metadata, name, uri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, canonical, interactions, link, metadata, name, uri) { 
        obj['canonical'] = canonical;
        obj['interactions'] = interactions;
        obj['link'] = link;
        obj['metadata'] = metadata;
        obj['name'] = name;
        obj['uri'] = uri;
    }

    /**
     * Constructs a <code>OnDemandGenre</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OnDemandGenre} obj Optional instance to populate.
     * @return {module:model/OnDemandGenre} The populated <code>OnDemandGenre</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OnDemandGenre();

            if (data.hasOwnProperty('canonical')) {
                obj['canonical'] = ApiClient.convertToType(data['canonical'], 'String');
            }
            if (data.hasOwnProperty('interactions')) {
                obj['interactions'] = OnDemandGenreInteractions.constructFromObject(data['interactions']);
            }
            if (data.hasOwnProperty('link')) {
                obj['link'] = ApiClient.convertToType(data['link'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = OnDemandGenreMetadata.constructFromObject(data['metadata']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OnDemandGenre</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OnDemandGenre</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OnDemandGenre.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['canonical'] && !(typeof data['canonical'] === 'string' || data['canonical'] instanceof String)) {
            throw new Error("Expected the field `canonical` to be a primitive type in the JSON string but got " + data['canonical']);
        }
        // validate the optional field `interactions`
        if (data['interactions']) { // data not null
          OnDemandGenreInteractions.validateJSON(data['interactions']);
        }
        // ensure the json data is a string
        if (data['link'] && !(typeof data['link'] === 'string' || data['link'] instanceof String)) {
            throw new Error("Expected the field `link` to be a primitive type in the JSON string but got " + data['link']);
        }
        // validate the optional field `metadata`
        if (data['metadata']) { // data not null
          OnDemandGenreMetadata.validateJSON(data['metadata']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }

        return true;
    }


}

OnDemandGenre.RequiredProperties = ["canonical", "interactions", "link", "metadata", "name", "uri"];

/**
 * The canonical name or URL slug of the genre.
 * @member {String} canonical
 */
OnDemandGenre.prototype['canonical'] = undefined;

/**
 * @member {module:model/OnDemandGenreInteractions} interactions
 */
OnDemandGenre.prototype['interactions'] = undefined;

/**
 * The Vimeo URL for this genre.
 * @member {String} link
 */
OnDemandGenre.prototype['link'] = undefined;

/**
 * @member {module:model/OnDemandGenreMetadata} metadata
 */
OnDemandGenre.prototype['metadata'] = undefined;

/**
 * The descriptive name of the genre.
 * @member {String} name
 */
OnDemandGenre.prototype['name'] = undefined;

/**
 * The relative URI of the On Demand genre.
 * @member {String} uri
 */
OnDemandGenre.prototype['uri'] = undefined;






export default OnDemandGenre;

