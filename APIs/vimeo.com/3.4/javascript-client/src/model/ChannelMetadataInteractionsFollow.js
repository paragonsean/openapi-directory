/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ChannelMetadataInteractionsFollow model module.
 * @module model/ChannelMetadataInteractionsFollow
 * @version 3.4
 */
class ChannelMetadataInteractionsFollow {
    /**
     * Constructs a new <code>ChannelMetadataInteractionsFollow</code>.
     * An action indicating if the authenticated user has followed this channel. This data requires a bearer token with the &#x60;private&#x60; scope.
     * @alias module:model/ChannelMetadataInteractionsFollow
     * @param added {Boolean} Whether the authenticated user has followed this channel. This data requires a bearer token with the `private` scope.
     * @param addedTime {String} The time in ISO 8601 format that the user followed this channel, or the null value if the user hasn't followed the channel. This data requires a bearer token with the `private` scope.
     * @param type {module:model/ChannelMetadataInteractionsFollow.TypeEnum} Whether the authenticated user is a moderator or subscriber. This data requires a bearer token with the `private` scope.  Option descriptions:  * `moderator` - The authenticated user is a moderator.  * `subscriber` - The authenticated user is a subscriber. 
     * @param uri {String} The URI for following or unfollowing this channel. PUT to this URI to follow the channel, or DELETE to this URI to unfollow the channel. This data requires a bearer token with the `private` scope.
     */
    constructor(added, addedTime, type, uri) { 
        
        ChannelMetadataInteractionsFollow.initialize(this, added, addedTime, type, uri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, added, addedTime, type, uri) { 
        obj['added'] = added;
        obj['added_time'] = addedTime;
        obj['type'] = type;
        obj['uri'] = uri;
    }

    /**
     * Constructs a <code>ChannelMetadataInteractionsFollow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChannelMetadataInteractionsFollow} obj Optional instance to populate.
     * @return {module:model/ChannelMetadataInteractionsFollow} The populated <code>ChannelMetadataInteractionsFollow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChannelMetadataInteractionsFollow();

            if (data.hasOwnProperty('added')) {
                obj['added'] = ApiClient.convertToType(data['added'], 'Boolean');
            }
            if (data.hasOwnProperty('added_time')) {
                obj['added_time'] = ApiClient.convertToType(data['added_time'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChannelMetadataInteractionsFollow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChannelMetadataInteractionsFollow</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChannelMetadataInteractionsFollow.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['added_time'] && !(typeof data['added_time'] === 'string' || data['added_time'] instanceof String)) {
            throw new Error("Expected the field `added_time` to be a primitive type in the JSON string but got " + data['added_time']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }

        return true;
    }


}

ChannelMetadataInteractionsFollow.RequiredProperties = ["added", "added_time", "type", "uri"];

/**
 * Whether the authenticated user has followed this channel. This data requires a bearer token with the `private` scope.
 * @member {Boolean} added
 */
ChannelMetadataInteractionsFollow.prototype['added'] = undefined;

/**
 * The time in ISO 8601 format that the user followed this channel, or the null value if the user hasn't followed the channel. This data requires a bearer token with the `private` scope.
 * @member {String} added_time
 */
ChannelMetadataInteractionsFollow.prototype['added_time'] = undefined;

/**
 * Whether the authenticated user is a moderator or subscriber. This data requires a bearer token with the `private` scope.  Option descriptions:  * `moderator` - The authenticated user is a moderator.  * `subscriber` - The authenticated user is a subscriber. 
 * @member {module:model/ChannelMetadataInteractionsFollow.TypeEnum} type
 */
ChannelMetadataInteractionsFollow.prototype['type'] = undefined;

/**
 * The URI for following or unfollowing this channel. PUT to this URI to follow the channel, or DELETE to this URI to unfollow the channel. This data requires a bearer token with the `private` scope.
 * @member {String} uri
 */
ChannelMetadataInteractionsFollow.prototype['uri'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ChannelMetadataInteractionsFollow['TypeEnum'] = {

    /**
     * value: "moderator"
     * @const
     */
    "moderator": "moderator",

    /**
     * value: "subscriber"
     * @const
     */
    "subscriber": "subscriber"
};



export default ChannelMetadataInteractionsFollow;

