/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import User from './User';
import Video from './Video';

/**
 * The Credit model module.
 * @module model/Credit
 * @version 3.4
 */
class Credit {
    /**
     * Constructs a new <code>Credit</code>.
     * @alias module:model/Credit
     * @param name {String} The name of the person credited.
     * @param role {String} The character that this person portrayed, or the job that this person performed.
     * @param uri {String} The unique identifier to access the credits resource.
     */
    constructor(name, role, uri) { 
        
        Credit.initialize(this, name, role, uri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, role, uri) { 
        obj['name'] = name;
        obj['role'] = role;
        obj['uri'] = uri;
    }

    /**
     * Constructs a <code>Credit</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Credit} obj Optional instance to populate.
     * @return {module:model/Credit} The populated <code>Credit</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Credit();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = ApiClient.convertToType(data['user'], User);
            }
            if (data.hasOwnProperty('video')) {
                obj['video'] = ApiClient.convertToType(data['video'], Video);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Credit</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Credit</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Credit.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }
        // validate the optional field `user`
        if (data['user']) { // data not null
          User.validateJSON(data['user']);
        }
        // validate the optional field `video`
        if (data['video']) { // data not null
          Video.validateJSON(data['video']);
        }

        return true;
    }


}

Credit.RequiredProperties = ["name", "role", "uri"];

/**
 * The name of the person credited.
 * @member {String} name
 */
Credit.prototype['name'] = undefined;

/**
 * The character that this person portrayed, or the job that this person performed.
 * @member {String} role
 */
Credit.prototype['role'] = undefined;

/**
 * The unique identifier to access the credits resource.
 * @member {String} uri
 */
Credit.prototype['uri'] = undefined;

/**
 * The Vimeo user associated with this credit.
 * @member {module:model/User} user
 */
Credit.prototype['user'] = undefined;

/**
 * The video associated with this credit.
 * @member {module:model/Video} video
 */
Credit.prototype['video'] = undefined;






export default Credit;

