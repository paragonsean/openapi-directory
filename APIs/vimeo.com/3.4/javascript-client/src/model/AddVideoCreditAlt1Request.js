/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AddVideoCreditAlt1Request model module.
 * @module model/AddVideoCreditAlt1Request
 * @version 3.4
 */
class AddVideoCreditAlt1Request {
    /**
     * Constructs a new <code>AddVideoCreditAlt1Request</code>.
     * @alias module:model/AddVideoCreditAlt1Request
     * @param email {String} The email address of the credited person.
     * @param name {String} The name of the credited person.
     * @param role {String} The role of the credited person.
     * @param userUri {String} The URI of the Vimeo user who should be given credit in this video.
     */
    constructor(email, name, role, userUri) { 
        
        AddVideoCreditAlt1Request.initialize(this, email, name, role, userUri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, name, role, userUri) { 
        obj['email'] = email;
        obj['name'] = name;
        obj['role'] = role;
        obj['user_uri'] = userUri;
    }

    /**
     * Constructs a <code>AddVideoCreditAlt1Request</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AddVideoCreditAlt1Request} obj Optional instance to populate.
     * @return {module:model/AddVideoCreditAlt1Request} The populated <code>AddVideoCreditAlt1Request</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AddVideoCreditAlt1Request();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('user_uri')) {
                obj['user_uri'] = ApiClient.convertToType(data['user_uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AddVideoCreditAlt1Request</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AddVideoCreditAlt1Request</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AddVideoCreditAlt1Request.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }
        // ensure the json data is a string
        if (data['user_uri'] && !(typeof data['user_uri'] === 'string' || data['user_uri'] instanceof String)) {
            throw new Error("Expected the field `user_uri` to be a primitive type in the JSON string but got " + data['user_uri']);
        }

        return true;
    }


}

AddVideoCreditAlt1Request.RequiredProperties = ["email", "name", "role", "user_uri"];

/**
 * The email address of the credited person.
 * @member {String} email
 */
AddVideoCreditAlt1Request.prototype['email'] = undefined;

/**
 * The name of the credited person.
 * @member {String} name
 */
AddVideoCreditAlt1Request.prototype['name'] = undefined;

/**
 * The role of the credited person.
 * @member {String} role
 */
AddVideoCreditAlt1Request.prototype['role'] = undefined;

/**
 * The URI of the Vimeo user who should be given credit in this video.
 * @member {String} user_uri
 */
AddVideoCreditAlt1Request.prototype['user_uri'] = undefined;






export default AddVideoCreditAlt1Request;

