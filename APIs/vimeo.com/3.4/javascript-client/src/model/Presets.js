/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PresetsMetadata from './PresetsMetadata';
import PresetsSettings from './PresetsSettings';
import User from './User';

/**
 * The Presets model module.
 * @module model/Presets
 * @version 3.4
 */
class Presets {
    /**
     * Constructs a new <code>Presets</code>.
     * @alias module:model/Presets
     * @param metadata {module:model/PresetsMetadata} 
     * @param name {String} The display name of the presets group.
     * @param settings {module:model/PresetsSettings} 
     * @param uri {String} The canonical relative URI of the presets object.
     * @param user {module:model/User} The owner of the preset.
     */
    constructor(metadata, name, settings, uri, user) { 
        
        Presets.initialize(this, metadata, name, settings, uri, user);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, metadata, name, settings, uri, user) { 
        obj['metadata'] = metadata;
        obj['name'] = name;
        obj['settings'] = settings;
        obj['uri'] = uri;
        obj['user'] = user;
    }

    /**
     * Constructs a <code>Presets</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Presets} obj Optional instance to populate.
     * @return {module:model/Presets} The populated <code>Presets</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Presets();

            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = PresetsMetadata.constructFromObject(data['metadata']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('settings')) {
                obj['settings'] = PresetsSettings.constructFromObject(data['settings']);
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = ApiClient.convertToType(data['user'], User);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Presets</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Presets</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Presets.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `metadata`
        if (data['metadata']) { // data not null
          PresetsMetadata.validateJSON(data['metadata']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `settings`
        if (data['settings']) { // data not null
          PresetsSettings.validateJSON(data['settings']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }
        // validate the optional field `user`
        if (data['user']) { // data not null
          User.validateJSON(data['user']);
        }

        return true;
    }


}

Presets.RequiredProperties = ["metadata", "name", "settings", "uri", "user"];

/**
 * @member {module:model/PresetsMetadata} metadata
 */
Presets.prototype['metadata'] = undefined;

/**
 * The display name of the presets group.
 * @member {String} name
 */
Presets.prototype['name'] = undefined;

/**
 * @member {module:model/PresetsSettings} settings
 */
Presets.prototype['settings'] = undefined;

/**
 * The canonical relative URI of the presets object.
 * @member {String} uri
 */
Presets.prototype['uri'] = undefined;

/**
 * The owner of the preset.
 * @member {module:model/User} user
 */
Presets.prototype['user'] = undefined;






export default Presets;

