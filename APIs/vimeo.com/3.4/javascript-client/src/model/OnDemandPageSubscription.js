/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OnDemandPageSubscription model module.
 * @module model/OnDemandPageSubscription
 * @version 3.4
 */
class OnDemandPageSubscription {
    /**
     * Constructs a new <code>OnDemandPageSubscription</code>.
     * Information about subscribing to this On Demand page, if enabled.
     * @alias module:model/OnDemandPageSubscription
     * @param active {Boolean} Whether this product is active.
     * @param link {String} The link to this product on Vimeo.
     * @param price {Object} The accepted currencies and respective pricing for this product.
     */
    constructor(active, link, price) { 
        
        OnDemandPageSubscription.initialize(this, active, link, price);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, active, link, price) { 
        obj['active'] = active;
        obj['link'] = link;
        obj['price'] = price;
    }

    /**
     * Constructs a <code>OnDemandPageSubscription</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OnDemandPageSubscription} obj Optional instance to populate.
     * @return {module:model/OnDemandPageSubscription} The populated <code>OnDemandPageSubscription</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OnDemandPageSubscription();

            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('link')) {
                obj['link'] = ApiClient.convertToType(data['link'], 'String');
            }
            if (data.hasOwnProperty('period')) {
                obj['period'] = ApiClient.convertToType(data['period'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OnDemandPageSubscription</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OnDemandPageSubscription</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OnDemandPageSubscription.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['link'] && !(typeof data['link'] === 'string' || data['link'] instanceof String)) {
            throw new Error("Expected the field `link` to be a primitive type in the JSON string but got " + data['link']);
        }
        // ensure the json data is a string
        if (data['period'] && !(typeof data['period'] === 'string' || data['period'] instanceof String)) {
            throw new Error("Expected the field `period` to be a primitive type in the JSON string but got " + data['period']);
        }

        return true;
    }


}

OnDemandPageSubscription.RequiredProperties = ["active", "link", "price"];

/**
 * Whether this product is active.
 * @member {Boolean} active
 */
OnDemandPageSubscription.prototype['active'] = undefined;

/**
 * The link to this product on Vimeo.
 * @member {String} link
 */
OnDemandPageSubscription.prototype['link'] = undefined;

/**
 * The product's rental period.
 * @member {String} period
 */
OnDemandPageSubscription.prototype['period'] = undefined;

/**
 * The accepted currencies and respective pricing for this product.
 * @member {Object} price
 */
OnDemandPageSubscription.prototype['price'] = undefined;






export default OnDemandPageSubscription;

