/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserMetadataConnectionsPictures model module.
 * @module model/UserMetadataConnectionsPictures
 * @version 3.4
 */
class UserMetadataConnectionsPictures {
    /**
     * Constructs a new <code>UserMetadataConnectionsPictures</code>.
     * Information about this user&#39;s portraits.
     * @alias module:model/UserMetadataConnectionsPictures
     * @param options {Array.<String>} An array of HTTP methods permitted on this URI.
     * @param total {Number} The total number of pictures on this connection.
     * @param uri {String} The API URI that resolves to the connection data.
     */
    constructor(options, total, uri) { 
        
        UserMetadataConnectionsPictures.initialize(this, options, total, uri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, options, total, uri) { 
        obj['options'] = options;
        obj['total'] = total;
        obj['uri'] = uri;
    }

    /**
     * Constructs a <code>UserMetadataConnectionsPictures</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserMetadataConnectionsPictures} obj Optional instance to populate.
     * @return {module:model/UserMetadataConnectionsPictures} The populated <code>UserMetadataConnectionsPictures</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserMetadataConnectionsPictures();

            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], ['String']);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserMetadataConnectionsPictures</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserMetadataConnectionsPictures</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserMetadataConnectionsPictures.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['options'])) {
            throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }

        return true;
    }


}

UserMetadataConnectionsPictures.RequiredProperties = ["options", "total", "uri"];

/**
 * An array of HTTP methods permitted on this URI.
 * @member {Array.<String>} options
 */
UserMetadataConnectionsPictures.prototype['options'] = undefined;

/**
 * The total number of pictures on this connection.
 * @member {Number} total
 */
UserMetadataConnectionsPictures.prototype['total'] = undefined;

/**
 * The API URI that resolves to the connection data.
 * @member {String} uri
 */
UserMetadataConnectionsPictures.prototype['uri'] = undefined;






export default UserMetadataConnectionsPictures;

