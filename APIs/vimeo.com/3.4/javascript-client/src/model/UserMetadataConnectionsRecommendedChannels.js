/**
 * Vimeo
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserMetadataConnectionsRecommendedChannels model module.
 * @module model/UserMetadataConnectionsRecommendedChannels
 * @version 3.4
 */
class UserMetadataConnectionsRecommendedChannels {
    /**
     * Constructs a new <code>UserMetadataConnectionsRecommendedChannels</code>.
     * A collection of recommended channels for the current user to follow. This data requires a bearer token with the &#x60;private&#x60; scope.
     * @alias module:model/UserMetadataConnectionsRecommendedChannels
     * @param options {Array.<String>} An array of HTTP methods permitted on this URI. This data requires a bearer token with the `private` scope.
     * @param total {Number} The total number of channels on this connection. This data requires a bearer token with the `private` scope.
     * @param uri {String} The API URI that resolves to the connection data. This data requires a bearer token with the `private` scope.
     */
    constructor(options, total, uri) { 
        
        UserMetadataConnectionsRecommendedChannels.initialize(this, options, total, uri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, options, total, uri) { 
        obj['options'] = options;
        obj['total'] = total;
        obj['uri'] = uri;
    }

    /**
     * Constructs a <code>UserMetadataConnectionsRecommendedChannels</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserMetadataConnectionsRecommendedChannels} obj Optional instance to populate.
     * @return {module:model/UserMetadataConnectionsRecommendedChannels} The populated <code>UserMetadataConnectionsRecommendedChannels</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserMetadataConnectionsRecommendedChannels();

            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], ['String']);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('uri')) {
                obj['uri'] = ApiClient.convertToType(data['uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserMetadataConnectionsRecommendedChannels</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserMetadataConnectionsRecommendedChannels</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserMetadataConnectionsRecommendedChannels.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['options'])) {
            throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
        }
        // ensure the json data is a string
        if (data['uri'] && !(typeof data['uri'] === 'string' || data['uri'] instanceof String)) {
            throw new Error("Expected the field `uri` to be a primitive type in the JSON string but got " + data['uri']);
        }

        return true;
    }


}

UserMetadataConnectionsRecommendedChannels.RequiredProperties = ["options", "total", "uri"];

/**
 * An array of HTTP methods permitted on this URI. This data requires a bearer token with the `private` scope.
 * @member {Array.<String>} options
 */
UserMetadataConnectionsRecommendedChannels.prototype['options'] = undefined;

/**
 * The total number of channels on this connection. This data requires a bearer token with the `private` scope.
 * @member {Number} total
 */
UserMetadataConnectionsRecommendedChannels.prototype['total'] = undefined;

/**
 * The API URI that resolves to the connection data. This data requires a bearer token with the `private` scope.
 * @member {String} uri
 */
UserMetadataConnectionsRecommendedChannels.prototype['uri'] = undefined;






export default UserMetadataConnectionsRecommendedChannels;

