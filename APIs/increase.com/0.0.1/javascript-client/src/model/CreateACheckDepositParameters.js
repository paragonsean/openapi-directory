/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateACheckDepositParameters model module.
 * @module model/CreateACheckDepositParameters
 * @version 0.0.1
 */
class CreateACheckDepositParameters {
    /**
     * Constructs a new <code>CreateACheckDepositParameters</code>.
     * @alias module:model/CreateACheckDepositParameters
     * @param accountId {String} The identifier for the Account to deposit the check in.
     * @param amount {Number} The deposit amount in the minor unit of the account currency. For dollars, for example, this is cents.
     * @param backImageFileId {String} The File containing the check's back image.
     * @param currency {String} The currency to use for the deposit.
     * @param frontImageFileId {String} The File containing the check's front image.
     */
    constructor(accountId, amount, backImageFileId, currency, frontImageFileId) { 
        
        CreateACheckDepositParameters.initialize(this, accountId, amount, backImageFileId, currency, frontImageFileId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountId, amount, backImageFileId, currency, frontImageFileId) { 
        obj['account_id'] = accountId;
        obj['amount'] = amount;
        obj['back_image_file_id'] = backImageFileId;
        obj['currency'] = currency;
        obj['front_image_file_id'] = frontImageFileId;
    }

    /**
     * Constructs a <code>CreateACheckDepositParameters</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateACheckDepositParameters} obj Optional instance to populate.
     * @return {module:model/CreateACheckDepositParameters} The populated <code>CreateACheckDepositParameters</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateACheckDepositParameters();

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('back_image_file_id')) {
                obj['back_image_file_id'] = ApiClient.convertToType(data['back_image_file_id'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('front_image_file_id')) {
                obj['front_image_file_id'] = ApiClient.convertToType(data['front_image_file_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateACheckDepositParameters</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateACheckDepositParameters</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateACheckDepositParameters.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['back_image_file_id'] && !(typeof data['back_image_file_id'] === 'string' || data['back_image_file_id'] instanceof String)) {
            throw new Error("Expected the field `back_image_file_id` to be a primitive type in the JSON string but got " + data['back_image_file_id']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['front_image_file_id'] && !(typeof data['front_image_file_id'] === 'string' || data['front_image_file_id'] instanceof String)) {
            throw new Error("Expected the field `front_image_file_id` to be a primitive type in the JSON string but got " + data['front_image_file_id']);
        }

        return true;
    }


}

CreateACheckDepositParameters.RequiredProperties = ["account_id", "amount", "back_image_file_id", "currency", "front_image_file_id"];

/**
 * The identifier for the Account to deposit the check in.
 * @member {String} account_id
 */
CreateACheckDepositParameters.prototype['account_id'] = undefined;

/**
 * The deposit amount in the minor unit of the account currency. For dollars, for example, this is cents.
 * @member {Number} amount
 */
CreateACheckDepositParameters.prototype['amount'] = undefined;

/**
 * The File containing the check's back image.
 * @member {String} back_image_file_id
 */
CreateACheckDepositParameters.prototype['back_image_file_id'] = undefined;

/**
 * The currency to use for the deposit.
 * @member {String} currency
 */
CreateACheckDepositParameters.prototype['currency'] = undefined;

/**
 * The File containing the check's front image.
 * @member {String} front_image_file_id
 */
CreateACheckDepositParameters.prototype['front_image_file_id'] = undefined;






export default CreateACheckDepositParameters;

