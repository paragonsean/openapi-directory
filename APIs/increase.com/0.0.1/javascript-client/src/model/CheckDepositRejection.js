/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CheckDepositRejection model module.
 * @module model/CheckDepositRejection
 * @version 0.0.1
 */
class CheckDepositRejection {
    /**
     * Constructs a new <code>CheckDepositRejection</code>.
     * If your deposit is rejected by Increase, this will contain details as to why it was rejected.
     * @alias module:model/CheckDepositRejection
     * @param amount {Number} The rejected amount in the minor unit of check's currency. For dollars, for example, this is cents.
     * @param currency {module:model/CheckDepositRejection.CurrencyEnum} The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) code for the check's currency.
     * @param reason {module:model/CheckDepositRejection.ReasonEnum} Why the check deposit was rejected.
     * @param rejectedAt {Date} The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time at which the check deposit was rejected.
     */
    constructor(amount, currency, reason, rejectedAt) { 
        
        CheckDepositRejection.initialize(this, amount, currency, reason, rejectedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, currency, reason, rejectedAt) { 
        obj['amount'] = amount;
        obj['currency'] = currency;
        obj['reason'] = reason;
        obj['rejected_at'] = rejectedAt;
    }

    /**
     * Constructs a <code>CheckDepositRejection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CheckDepositRejection} obj Optional instance to populate.
     * @return {module:model/CheckDepositRejection} The populated <code>CheckDepositRejection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CheckDepositRejection();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('rejected_at')) {
                obj['rejected_at'] = ApiClient.convertToType(data['rejected_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CheckDepositRejection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CheckDepositRejection</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CheckDepositRejection.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }

        return true;
    }


}

CheckDepositRejection.RequiredProperties = ["amount", "currency", "reason", "rejected_at"];

/**
 * The rejected amount in the minor unit of check's currency. For dollars, for example, this is cents.
 * @member {Number} amount
 */
CheckDepositRejection.prototype['amount'] = undefined;

/**
 * The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) code for the check's currency.
 * @member {module:model/CheckDepositRejection.CurrencyEnum} currency
 */
CheckDepositRejection.prototype['currency'] = undefined;

/**
 * Why the check deposit was rejected.
 * @member {module:model/CheckDepositRejection.ReasonEnum} reason
 */
CheckDepositRejection.prototype['reason'] = undefined;

/**
 * The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date and time at which the check deposit was rejected.
 * @member {Date} rejected_at
 */
CheckDepositRejection.prototype['rejected_at'] = undefined;





/**
 * Allowed values for the <code>currency</code> property.
 * @enum {String}
 * @readonly
 */
CheckDepositRejection['CurrencyEnum'] = {

    /**
     * value: "CAD"
     * @const
     */
    "CAD": "CAD",

    /**
     * value: "CHF"
     * @const
     */
    "CHF": "CHF",

    /**
     * value: "EUR"
     * @const
     */
    "EUR": "EUR",

    /**
     * value: "GBP"
     * @const
     */
    "GBP": "GBP",

    /**
     * value: "JPY"
     * @const
     */
    "JPY": "JPY",

    /**
     * value: "USD"
     * @const
     */
    "USD": "USD"
};


/**
 * Allowed values for the <code>reason</code> property.
 * @enum {String}
 * @readonly
 */
CheckDepositRejection['ReasonEnum'] = {

    /**
     * value: "incomplete_image"
     * @const
     */
    "incomplete_image": "incomplete_image",

    /**
     * value: "duplicate"
     * @const
     */
    "duplicate": "duplicate",

    /**
     * value: "poor_image_quality"
     * @const
     */
    "poor_image_quality": "poor_image_quality",

    /**
     * value: "incorrect_amount"
     * @const
     */
    "incorrect_amount": "incorrect_amount",

    /**
     * value: "incorrect_recipient"
     * @const
     */
    "incorrect_recipient": "incorrect_recipient",

    /**
     * value: "not_eligible_for_mobile_deposit"
     * @const
     */
    "not_eligible_for_mobile_deposit": "not_eligible_for_mobile_deposit",

    /**
     * value: "unknown"
     * @const
     */
    "unknown": "unknown"
};



export default CheckDepositRejection;

