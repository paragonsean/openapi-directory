/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EntriesElement model module.
 * @module model/EntriesElement
 * @version 0.0.1
 */
class EntriesElement {
    /**
     * Constructs a new <code>EntriesElement</code>.
     * @alias module:model/EntriesElement
     * @param accountId {String} The bookkeeping account impacted by the entry.
     * @param amount {Number} The amount of the entry in minor units.
     * @param id {String} The entry identifier.
     */
    constructor(accountId, amount, id) { 
        
        EntriesElement.initialize(this, accountId, amount, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountId, amount, id) { 
        obj['account_id'] = accountId;
        obj['amount'] = amount;
        obj['id'] = id;
    }

    /**
     * Constructs a <code>EntriesElement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EntriesElement} obj Optional instance to populate.
     * @return {module:model/EntriesElement} The populated <code>EntriesElement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EntriesElement();

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EntriesElement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EntriesElement</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EntriesElement.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }

        return true;
    }


}

EntriesElement.RequiredProperties = ["account_id", "amount", "id"];

/**
 * The bookkeeping account impacted by the entry.
 * @member {String} account_id
 */
EntriesElement.prototype['account_id'] = undefined;

/**
 * The amount of the entry in minor units.
 * @member {Number} amount
 */
EntriesElement.prototype['amount'] = undefined;

/**
 * The entry identifier.
 * @member {String} id
 */
EntriesElement.prototype['id'] = undefined;






export default EntriesElement;

