/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DigitalWallet model module.
 * @module model/DigitalWallet
 * @version 0.0.1
 */
class DigitalWallet {
    /**
     * Constructs a new <code>DigitalWallet</code>.
     * The contact information used in the two-factor steps for digital wallet card creation. At least one field must be present to complete the digital wallet steps.
     * @alias module:model/DigitalWallet
     * @param cardProfileId {String} The card profile assigned to this digital card. Card profiles may also be assigned at the program level.
     * @param email {String} An email address that can be used to verify the cardholder via one-time passcode over email.
     * @param phone {String} A phone number that can be used to verify the cardholder via one-time passcode over SMS.
     */
    constructor(cardProfileId, email, phone) { 
        
        DigitalWallet.initialize(this, cardProfileId, email, phone);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cardProfileId, email, phone) { 
        obj['card_profile_id'] = cardProfileId;
        obj['email'] = email;
        obj['phone'] = phone;
    }

    /**
     * Constructs a <code>DigitalWallet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DigitalWallet} obj Optional instance to populate.
     * @return {module:model/DigitalWallet} The populated <code>DigitalWallet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DigitalWallet();

            if (data.hasOwnProperty('card_profile_id')) {
                obj['card_profile_id'] = ApiClient.convertToType(data['card_profile_id'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('phone')) {
                obj['phone'] = ApiClient.convertToType(data['phone'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DigitalWallet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DigitalWallet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DigitalWallet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['card_profile_id'] && !(typeof data['card_profile_id'] === 'string' || data['card_profile_id'] instanceof String)) {
            throw new Error("Expected the field `card_profile_id` to be a primitive type in the JSON string but got " + data['card_profile_id']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['phone'] && !(typeof data['phone'] === 'string' || data['phone'] instanceof String)) {
            throw new Error("Expected the field `phone` to be a primitive type in the JSON string but got " + data['phone']);
        }

        return true;
    }


}

DigitalWallet.RequiredProperties = ["card_profile_id", "email", "phone"];

/**
 * The card profile assigned to this digital card. Card profiles may also be assigned at the program level.
 * @member {String} card_profile_id
 */
DigitalWallet.prototype['card_profile_id'] = undefined;

/**
 * An email address that can be used to verify the cardholder via one-time passcode over email.
 * @member {String} email
 */
DigitalWallet.prototype['email'] = undefined;

/**
 * A phone number that can be used to verify the cardholder via one-time passcode over SMS.
 * @member {String} phone
 */
DigitalWallet.prototype['phone'] = undefined;






export default DigitalWallet;

