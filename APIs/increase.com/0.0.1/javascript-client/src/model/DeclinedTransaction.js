/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DeclinedTransactionSource from './DeclinedTransactionSource';

/**
 * The DeclinedTransaction model module.
 * @module model/DeclinedTransaction
 * @version 0.0.1
 */
class DeclinedTransaction {
    /**
     * Constructs a new <code>DeclinedTransaction</code>.
     * If the ACH Transfer attempt fails, this will contain the resulting [Declined Transaction](#declined-transactions) object. The Declined Transaction&#39;s &#x60;source&#x60; will be of &#x60;category: inbound_ach_transfer&#x60;.
     * @alias module:model/DeclinedTransaction
     * @param accountId {String} The identifier for the Account the Declined Transaction belongs to.
     * @param amount {Number} The Declined Transaction amount in the minor unit of its currency. For dollars, for example, this is cents.
     * @param createdAt {Date} The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date on which the Transaction occured.
     * @param currency {module:model/DeclinedTransaction.CurrencyEnum} The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) code for the Declined Transaction's currency. This will match the currency on the Declined Transcation's Account.
     * @param description {String} This is the description the vendor provides.
     * @param id {String} The Declined Transaction identifier.
     * @param routeId {String} The identifier for the route this Declined Transaction came through. Routes are things like cards and ACH details.
     * @param routeType {module:model/DeclinedTransaction.RouteTypeEnum} The type of the route this Declined Transaction came through.
     * @param source {module:model/DeclinedTransactionSource} 
     * @param type {module:model/DeclinedTransaction.TypeEnum} A constant representing the object's type. For this resource it will always be `declined_transaction`.
     */
    constructor(accountId, amount, createdAt, currency, description, id, routeId, routeType, source, type) { 
        
        DeclinedTransaction.initialize(this, accountId, amount, createdAt, currency, description, id, routeId, routeType, source, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountId, amount, createdAt, currency, description, id, routeId, routeType, source, type) { 
        obj['account_id'] = accountId;
        obj['amount'] = amount;
        obj['created_at'] = createdAt;
        obj['currency'] = currency;
        obj['description'] = description;
        obj['id'] = id;
        obj['route_id'] = routeId;
        obj['route_type'] = routeType;
        obj['source'] = source;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>DeclinedTransaction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeclinedTransaction} obj Optional instance to populate.
     * @return {module:model/DeclinedTransaction} The populated <code>DeclinedTransaction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeclinedTransaction();

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('route_id')) {
                obj['route_id'] = ApiClient.convertToType(data['route_id'], 'String');
            }
            if (data.hasOwnProperty('route_type')) {
                obj['route_type'] = ApiClient.convertToType(data['route_type'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = DeclinedTransactionSource.constructFromObject(data['source']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeclinedTransaction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeclinedTransaction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeclinedTransaction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['route_id'] && !(typeof data['route_id'] === 'string' || data['route_id'] instanceof String)) {
            throw new Error("Expected the field `route_id` to be a primitive type in the JSON string but got " + data['route_id']);
        }
        // ensure the json data is a string
        if (data['route_type'] && !(typeof data['route_type'] === 'string' || data['route_type'] instanceof String)) {
            throw new Error("Expected the field `route_type` to be a primitive type in the JSON string but got " + data['route_type']);
        }
        // validate the optional field `source`
        if (data['source']) { // data not null
          DeclinedTransactionSource.validateJSON(data['source']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

DeclinedTransaction.RequiredProperties = ["account_id", "amount", "created_at", "currency", "description", "id", "route_id", "route_type", "source", "type"];

/**
 * The identifier for the Account the Declined Transaction belongs to.
 * @member {String} account_id
 */
DeclinedTransaction.prototype['account_id'] = undefined;

/**
 * The Declined Transaction amount in the minor unit of its currency. For dollars, for example, this is cents.
 * @member {Number} amount
 */
DeclinedTransaction.prototype['amount'] = undefined;

/**
 * The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date on which the Transaction occured.
 * @member {Date} created_at
 */
DeclinedTransaction.prototype['created_at'] = undefined;

/**
 * The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) code for the Declined Transaction's currency. This will match the currency on the Declined Transcation's Account.
 * @member {module:model/DeclinedTransaction.CurrencyEnum} currency
 */
DeclinedTransaction.prototype['currency'] = undefined;

/**
 * This is the description the vendor provides.
 * @member {String} description
 */
DeclinedTransaction.prototype['description'] = undefined;

/**
 * The Declined Transaction identifier.
 * @member {String} id
 */
DeclinedTransaction.prototype['id'] = undefined;

/**
 * The identifier for the route this Declined Transaction came through. Routes are things like cards and ACH details.
 * @member {String} route_id
 */
DeclinedTransaction.prototype['route_id'] = undefined;

/**
 * The type of the route this Declined Transaction came through.
 * @member {module:model/DeclinedTransaction.RouteTypeEnum} route_type
 */
DeclinedTransaction.prototype['route_type'] = undefined;

/**
 * @member {module:model/DeclinedTransactionSource} source
 */
DeclinedTransaction.prototype['source'] = undefined;

/**
 * A constant representing the object's type. For this resource it will always be `declined_transaction`.
 * @member {module:model/DeclinedTransaction.TypeEnum} type
 */
DeclinedTransaction.prototype['type'] = undefined;





/**
 * Allowed values for the <code>currency</code> property.
 * @enum {String}
 * @readonly
 */
DeclinedTransaction['CurrencyEnum'] = {

    /**
     * value: "CAD"
     * @const
     */
    "CAD": "CAD",

    /**
     * value: "CHF"
     * @const
     */
    "CHF": "CHF",

    /**
     * value: "EUR"
     * @const
     */
    "EUR": "EUR",

    /**
     * value: "GBP"
     * @const
     */
    "GBP": "GBP",

    /**
     * value: "JPY"
     * @const
     */
    "JPY": "JPY",

    /**
     * value: "USD"
     * @const
     */
    "USD": "USD"
};


/**
 * Allowed values for the <code>route_type</code> property.
 * @enum {String}
 * @readonly
 */
DeclinedTransaction['RouteTypeEnum'] = {

    /**
     * value: "account_number"
     * @const
     */
    "account_number": "account_number",

    /**
     * value: "card"
     * @const
     */
    "card": "card"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
DeclinedTransaction['TypeEnum'] = {

    /**
     * value: "declined_transaction"
     * @const
     */
    "declined_transaction": "declined_transaction"
};



export default DeclinedTransaction;

