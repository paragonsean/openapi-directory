/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Export from './Export';

/**
 * The ExportList model module.
 * @module model/ExportList
 * @version 0.0.1
 */
class ExportList {
    /**
     * Constructs a new <code>ExportList</code>.
     * A list of Export objects
     * @alias module:model/ExportList
     * @param data {Array.<module:model/Export>} The contents of the list.
     * @param nextCursor {String} A pointer to a place in the list.
     */
    constructor(data, nextCursor) { 
        
        ExportList.initialize(this, data, nextCursor);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, nextCursor) { 
        obj['data'] = data;
        obj['next_cursor'] = nextCursor;
    }

    /**
     * Constructs a <code>ExportList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExportList} obj Optional instance to populate.
     * @return {module:model/ExportList} The populated <code>ExportList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExportList();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [Export]);
            }
            if (data.hasOwnProperty('next_cursor')) {
                obj['next_cursor'] = ApiClient.convertToType(data['next_cursor'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExportList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExportList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExportList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                Export.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['next_cursor'] && !(typeof data['next_cursor'] === 'string' || data['next_cursor'] instanceof String)) {
            throw new Error("Expected the field `next_cursor` to be a primitive type in the JSON string but got " + data['next_cursor']);
        }

        return true;
    }


}

ExportList.RequiredProperties = ["data", "next_cursor"];

/**
 * The contents of the list.
 * @member {Array.<module:model/Export>} data
 */
ExportList.prototype['data'] = undefined;

/**
 * A pointer to a place in the list.
 * @member {String} next_cursor
 */
ExportList.prototype['next_cursor'] = undefined;






export default ExportList;

