/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BookkeepingAccount model module.
 * @module model/BookkeepingAccount
 * @version 0.0.1
 */
class BookkeepingAccount {
    /**
     * Constructs a new <code>BookkeepingAccount</code>.
     * Accounts are T-accounts. They can store accounting entries.
     * @alias module:model/BookkeepingAccount
     * @param accountId {String} The API Account associated with this bookkeeping account.
     * @param complianceCategory {module:model/BookkeepingAccount.ComplianceCategoryEnum} The compliance category of the account.
     * @param entityId {String} The Entity associated with this bookkeeping account.
     * @param id {String} The account identifier.
     * @param name {String} The name you choose for the account.
     * @param type {module:model/BookkeepingAccount.TypeEnum} A constant representing the object's type. For this resource it will always be `bookkeeping_account`.
     */
    constructor(accountId, complianceCategory, entityId, id, name, type) { 
        
        BookkeepingAccount.initialize(this, accountId, complianceCategory, entityId, id, name, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountId, complianceCategory, entityId, id, name, type) { 
        obj['account_id'] = accountId;
        obj['compliance_category'] = complianceCategory;
        obj['entity_id'] = entityId;
        obj['id'] = id;
        obj['name'] = name;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>BookkeepingAccount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BookkeepingAccount} obj Optional instance to populate.
     * @return {module:model/BookkeepingAccount} The populated <code>BookkeepingAccount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BookkeepingAccount();

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('compliance_category')) {
                obj['compliance_category'] = ApiClient.convertToType(data['compliance_category'], 'String');
            }
            if (data.hasOwnProperty('entity_id')) {
                obj['entity_id'] = ApiClient.convertToType(data['entity_id'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BookkeepingAccount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BookkeepingAccount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BookkeepingAccount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['compliance_category'] && !(typeof data['compliance_category'] === 'string' || data['compliance_category'] instanceof String)) {
            throw new Error("Expected the field `compliance_category` to be a primitive type in the JSON string but got " + data['compliance_category']);
        }
        // ensure the json data is a string
        if (data['entity_id'] && !(typeof data['entity_id'] === 'string' || data['entity_id'] instanceof String)) {
            throw new Error("Expected the field `entity_id` to be a primitive type in the JSON string but got " + data['entity_id']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

BookkeepingAccount.RequiredProperties = ["account_id", "compliance_category", "entity_id", "id", "name", "type"];

/**
 * The API Account associated with this bookkeeping account.
 * @member {String} account_id
 */
BookkeepingAccount.prototype['account_id'] = undefined;

/**
 * The compliance category of the account.
 * @member {module:model/BookkeepingAccount.ComplianceCategoryEnum} compliance_category
 */
BookkeepingAccount.prototype['compliance_category'] = undefined;

/**
 * The Entity associated with this bookkeeping account.
 * @member {String} entity_id
 */
BookkeepingAccount.prototype['entity_id'] = undefined;

/**
 * The account identifier.
 * @member {String} id
 */
BookkeepingAccount.prototype['id'] = undefined;

/**
 * The name you choose for the account.
 * @member {String} name
 */
BookkeepingAccount.prototype['name'] = undefined;

/**
 * A constant representing the object's type. For this resource it will always be `bookkeeping_account`.
 * @member {module:model/BookkeepingAccount.TypeEnum} type
 */
BookkeepingAccount.prototype['type'] = undefined;





/**
 * Allowed values for the <code>compliance_category</code> property.
 * @enum {String}
 * @readonly
 */
BookkeepingAccount['ComplianceCategoryEnum'] = {

    /**
     * value: "commingled_cash"
     * @const
     */
    "commingled_cash": "commingled_cash",

    /**
     * value: "customer_balance"
     * @const
     */
    "customer_balance": "customer_balance"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
BookkeepingAccount['TypeEnum'] = {

    /**
     * value: "bookkeeping_account"
     * @const
     */
    "bookkeeping_account": "bookkeeping_account"
};



export default BookkeepingAccount;

