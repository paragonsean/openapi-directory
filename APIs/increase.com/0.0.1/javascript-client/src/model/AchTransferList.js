/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AchTransfer from './AchTransfer';

/**
 * The AchTransferList model module.
 * @module model/AchTransferList
 * @version 0.0.1
 */
class AchTransferList {
    /**
     * Constructs a new <code>AchTransferList</code>.
     * A list of ACH Transfer objects
     * @alias module:model/AchTransferList
     * @param data {Array.<module:model/AchTransfer>} The contents of the list.
     * @param nextCursor {String} A pointer to a place in the list.
     */
    constructor(data, nextCursor) { 
        
        AchTransferList.initialize(this, data, nextCursor);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, nextCursor) { 
        obj['data'] = data;
        obj['next_cursor'] = nextCursor;
    }

    /**
     * Constructs a <code>AchTransferList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AchTransferList} obj Optional instance to populate.
     * @return {module:model/AchTransferList} The populated <code>AchTransferList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AchTransferList();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [AchTransfer]);
            }
            if (data.hasOwnProperty('next_cursor')) {
                obj['next_cursor'] = ApiClient.convertToType(data['next_cursor'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AchTransferList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AchTransferList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AchTransferList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                AchTransfer.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['next_cursor'] && !(typeof data['next_cursor'] === 'string' || data['next_cursor'] instanceof String)) {
            throw new Error("Expected the field `next_cursor` to be a primitive type in the JSON string but got " + data['next_cursor']);
        }

        return true;
    }


}

AchTransferList.RequiredProperties = ["data", "next_cursor"];

/**
 * The contents of the list.
 * @member {Array.<module:model/AchTransfer>} data
 */
AchTransferList.prototype['data'] = undefined;

/**
 * A pointer to a place in the list.
 * @member {String} next_cursor
 */
AchTransferList.prototype['next_cursor'] = undefined;






export default AchTransferList;

