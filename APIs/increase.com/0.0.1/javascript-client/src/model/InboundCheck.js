/**
 * Increase API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The InboundCheck model module.
 * @module model/InboundCheck
 * @version 0.0.1
 */
class InboundCheck {
    /**
     * Constructs a new <code>InboundCheck</code>.
     * A Inbound Check object. This field will be present in the JSON response if and only if &#x60;category&#x60; is equal to &#x60;inbound_check&#x60;.
     * @alias module:model/InboundCheck
     * @param amount {Number} The amount in the minor unit of the destination account currency. For dollars, for example, this is cents.
     * @param checkFrontImageFileId {String} 
     * @param checkNumber {String} 
     * @param checkRearImageFileId {String} 
     * @param currency {module:model/InboundCheck.CurrencyEnum} The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) code for the transaction's currency.
     */
    constructor(amount, checkFrontImageFileId, checkNumber, checkRearImageFileId, currency) { 
        
        InboundCheck.initialize(this, amount, checkFrontImageFileId, checkNumber, checkRearImageFileId, currency);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, checkFrontImageFileId, checkNumber, checkRearImageFileId, currency) { 
        obj['amount'] = amount;
        obj['check_front_image_file_id'] = checkFrontImageFileId;
        obj['check_number'] = checkNumber;
        obj['check_rear_image_file_id'] = checkRearImageFileId;
        obj['currency'] = currency;
    }

    /**
     * Constructs a <code>InboundCheck</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InboundCheck} obj Optional instance to populate.
     * @return {module:model/InboundCheck} The populated <code>InboundCheck</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InboundCheck();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('check_front_image_file_id')) {
                obj['check_front_image_file_id'] = ApiClient.convertToType(data['check_front_image_file_id'], 'String');
            }
            if (data.hasOwnProperty('check_number')) {
                obj['check_number'] = ApiClient.convertToType(data['check_number'], 'String');
            }
            if (data.hasOwnProperty('check_rear_image_file_id')) {
                obj['check_rear_image_file_id'] = ApiClient.convertToType(data['check_rear_image_file_id'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InboundCheck</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InboundCheck</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InboundCheck.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['check_front_image_file_id'] && !(typeof data['check_front_image_file_id'] === 'string' || data['check_front_image_file_id'] instanceof String)) {
            throw new Error("Expected the field `check_front_image_file_id` to be a primitive type in the JSON string but got " + data['check_front_image_file_id']);
        }
        // ensure the json data is a string
        if (data['check_number'] && !(typeof data['check_number'] === 'string' || data['check_number'] instanceof String)) {
            throw new Error("Expected the field `check_number` to be a primitive type in the JSON string but got " + data['check_number']);
        }
        // ensure the json data is a string
        if (data['check_rear_image_file_id'] && !(typeof data['check_rear_image_file_id'] === 'string' || data['check_rear_image_file_id'] instanceof String)) {
            throw new Error("Expected the field `check_rear_image_file_id` to be a primitive type in the JSON string but got " + data['check_rear_image_file_id']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }

        return true;
    }


}

InboundCheck.RequiredProperties = ["amount", "check_front_image_file_id", "check_number", "check_rear_image_file_id", "currency"];

/**
 * The amount in the minor unit of the destination account currency. For dollars, for example, this is cents.
 * @member {Number} amount
 */
InboundCheck.prototype['amount'] = undefined;

/**
 * 
 * @member {String} check_front_image_file_id
 */
InboundCheck.prototype['check_front_image_file_id'] = undefined;

/**
 * 
 * @member {String} check_number
 */
InboundCheck.prototype['check_number'] = undefined;

/**
 * 
 * @member {String} check_rear_image_file_id
 */
InboundCheck.prototype['check_rear_image_file_id'] = undefined;

/**
 * The [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) code for the transaction's currency.
 * @member {module:model/InboundCheck.CurrencyEnum} currency
 */
InboundCheck.prototype['currency'] = undefined;





/**
 * Allowed values for the <code>currency</code> property.
 * @enum {String}
 * @readonly
 */
InboundCheck['CurrencyEnum'] = {

    /**
     * value: "CAD"
     * @const
     */
    "CAD": "CAD",

    /**
     * value: "CHF"
     * @const
     */
    "CHF": "CHF",

    /**
     * value: "EUR"
     * @const
     */
    "EUR": "EUR",

    /**
     * value: "GBP"
     * @const
     */
    "GBP": "GBP",

    /**
     * value: "JPY"
     * @const
     */
    "JPY": "JPY",

    /**
     * value: "USD"
     * @const
     */
    "USD": "USD"
};



export default InboundCheck;

