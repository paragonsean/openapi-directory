/*
 * BBC Nitro API
 * BBC Nitro is the BBC's application programming interface (API) for BBC Programmes Metadata.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: nitro@bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.client.model.PreviousNextMixinPreviousNextPreviousNext;
import org.openapitools.client.model.Reference;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * PreviousNextMixinPreviousNext
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T10:00:25.242429-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class PreviousNextMixinPreviousNext {
  public static final String SERIALIZED_NAME_NEXT = "next";
  @SerializedName(SERIALIZED_NAME_NEXT)
  private Reference next;

  public static final String SERIALIZED_NAME_PREVIOUS = "previous";
  @SerializedName(SERIALIZED_NAME_PREVIOUS)
  private Reference previous;

  public static final String SERIALIZED_NAME_PREVIOUS_NEXT = "previous_next";
  @SerializedName(SERIALIZED_NAME_PREVIOUS_NEXT)
  private PreviousNextMixinPreviousNextPreviousNext previousNext;

  public PreviousNextMixinPreviousNext() {
  }

  public PreviousNextMixinPreviousNext next(Reference next) {
    this.next = next;
    return this;
  }

  /**
   * Get next
   * @return next
   */
  @javax.annotation.Nullable
  public Reference getNext() {
    return next;
  }

  public void setNext(Reference next) {
    this.next = next;
  }


  public PreviousNextMixinPreviousNext previous(Reference previous) {
    this.previous = previous;
    return this;
  }

  /**
   * Get previous
   * @return previous
   */
  @javax.annotation.Nullable
  public Reference getPrevious() {
    return previous;
  }

  public void setPrevious(Reference previous) {
    this.previous = previous;
  }


  public PreviousNextMixinPreviousNext previousNext(PreviousNextMixinPreviousNextPreviousNext previousNext) {
    this.previousNext = previousNext;
    return this;
  }

  /**
   * Get previousNext
   * @return previousNext
   */
  @javax.annotation.Nonnull
  public PreviousNextMixinPreviousNextPreviousNext getPreviousNext() {
    return previousNext;
  }

  public void setPreviousNext(PreviousNextMixinPreviousNextPreviousNext previousNext) {
    this.previousNext = previousNext;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    PreviousNextMixinPreviousNext previousNextMixinPreviousNext = (PreviousNextMixinPreviousNext) o;
    return Objects.equals(this.next, previousNextMixinPreviousNext.next) &&
        Objects.equals(this.previous, previousNextMixinPreviousNext.previous) &&
        Objects.equals(this.previousNext, previousNextMixinPreviousNext.previousNext);
  }

  @Override
  public int hashCode() {
    return Objects.hash(next, previous, previousNext);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class PreviousNextMixinPreviousNext {\n");
    sb.append("    next: ").append(toIndentedString(next)).append("\n");
    sb.append("    previous: ").append(toIndentedString(previous)).append("\n");
    sb.append("    previousNext: ").append(toIndentedString(previousNext)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("next");
    openapiFields.add("previous");
    openapiFields.add("previous_next");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("previous_next");
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to PreviousNextMixinPreviousNext
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!PreviousNextMixinPreviousNext.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in PreviousNextMixinPreviousNext is not found in the empty JSON string", PreviousNextMixinPreviousNext.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!PreviousNextMixinPreviousNext.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `PreviousNextMixinPreviousNext` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : PreviousNextMixinPreviousNext.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `next`
      if (jsonObj.get("next") != null && !jsonObj.get("next").isJsonNull()) {
        Reference.validateJsonElement(jsonObj.get("next"));
      }
      // validate the optional field `previous`
      if (jsonObj.get("previous") != null && !jsonObj.get("previous").isJsonNull()) {
        Reference.validateJsonElement(jsonObj.get("previous"));
      }
      // validate the required field `previous_next`
      PreviousNextMixinPreviousNextPreviousNext.validateJsonElement(jsonObj.get("previous_next"));
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!PreviousNextMixinPreviousNext.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'PreviousNextMixinPreviousNext' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<PreviousNextMixinPreviousNext> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(PreviousNextMixinPreviousNext.class));

       return (TypeAdapter<T>) new TypeAdapter<PreviousNextMixinPreviousNext>() {
           @Override
           public void write(JsonWriter out, PreviousNextMixinPreviousNext value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public PreviousNextMixinPreviousNext read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of PreviousNextMixinPreviousNext given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of PreviousNextMixinPreviousNext
   * @throws IOException if the JSON string is invalid with respect to PreviousNextMixinPreviousNext
   */
  public static PreviousNextMixinPreviousNext fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, PreviousNextMixinPreviousNext.class);
  }

  /**
   * Convert an instance of PreviousNextMixinPreviousNext to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

