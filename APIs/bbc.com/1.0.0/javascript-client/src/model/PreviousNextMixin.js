/**
 * BBC Nitro API
 * BBC Nitro is the BBC's application programming interface (API) for BBC Programmes Metadata.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: nitro@bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PreviousNextMixinPreviousNext from './PreviousNextMixinPreviousNext';

/**
 * The PreviousNextMixin model module.
 * @module model/PreviousNextMixin
 * @version 1.0.0
 */
class PreviousNextMixin {
    /**
     * Constructs a new <code>PreviousNextMixin</code>.
     * @alias module:model/PreviousNextMixin
     * @param previousNext {module:model/PreviousNextMixinPreviousNext} 
     */
    constructor(previousNext) { 
        
        PreviousNextMixin.initialize(this, previousNext);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, previousNext) { 
        obj['previous_next'] = previousNext;
    }

    /**
     * Constructs a <code>PreviousNextMixin</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PreviousNextMixin} obj Optional instance to populate.
     * @return {module:model/PreviousNextMixin} The populated <code>PreviousNextMixin</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PreviousNextMixin();

            if (data.hasOwnProperty('previous_next')) {
                obj['previous_next'] = PreviousNextMixinPreviousNext.constructFromObject(data['previous_next']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PreviousNextMixin</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PreviousNextMixin</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PreviousNextMixin.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `previous_next`
        if (data['previous_next']) { // data not null
          PreviousNextMixinPreviousNext.validateJSON(data['previous_next']);
        }

        return true;
    }


}

PreviousNextMixin.RequiredProperties = ["previous_next"];

/**
 * @member {module:model/PreviousNextMixinPreviousNext} previous_next
 */
PreviousNextMixin.prototype['previous_next'] = undefined;






export default PreviousNextMixin;

