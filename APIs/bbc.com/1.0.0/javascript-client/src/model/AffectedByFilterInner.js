/**
 * BBC Nitro API
 * BBC Nitro is the BBC's application programming interface (API) for BBC Programmes Metadata.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: nitro@bbc.co.uk
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AffectedByFilterInnerFilterInner from './AffectedByFilterInnerFilterInner';

/**
 * The AffectedByFilterInner model module.
 * @module model/AffectedByFilterInner
 * @version 1.0.0
 */
class AffectedByFilterInner {
    /**
     * Constructs a new <code>AffectedByFilterInner</code>.
     * @alias module:model/AffectedByFilterInner
     * @param description {String} 
     * @param name {String} 
     */
    constructor(description, name) { 
        
        AffectedByFilterInner.initialize(this, description, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, description, name) { 
        obj['description'] = description;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>AffectedByFilterInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AffectedByFilterInner} obj Optional instance to populate.
     * @return {module:model/AffectedByFilterInner} The populated <code>AffectedByFilterInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AffectedByFilterInner();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('filter')) {
                obj['filter'] = ApiClient.convertToType(data['filter'], [AffectedByFilterInnerFilterInner]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AffectedByFilterInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AffectedByFilterInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AffectedByFilterInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['filter']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['filter'])) {
                throw new Error("Expected the field `filter` to be an array in the JSON data but got " + data['filter']);
            }
            // validate the optional field `filter` (array)
            for (const item of data['filter']) {
                AffectedByFilterInnerFilterInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

AffectedByFilterInner.RequiredProperties = ["description", "name"];

/**
 * @member {String} description
 */
AffectedByFilterInner.prototype['description'] = undefined;

/**
 * @member {Array.<module:model/AffectedByFilterInnerFilterInner>} filter
 */
AffectedByFilterInner.prototype['filter'] = undefined;

/**
 * @member {String} name
 */
AffectedByFilterInner.prototype['name'] = undefined;






export default AffectedByFilterInner;

