/**
 * PI Web API 2018 SP1 Swagger Spec
 * Swagger Spec file that describes PI Web API
 *
 * The version of the OpenAPI document: 1.11.1.5383
 * Contact: techsupport@osisoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAINotificationRuleSubscriberApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAINotificationRuleSubscriberApi::OAINotificationRuleSubscriberApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAINotificationRuleSubscriberApi::~OAINotificationRuleSubscriberApi() {
}

void OAINotificationRuleSubscriberApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://devdata.osisoft.com/piwebapi"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("notificationRuleSubscriberDelete", defaultConf);
    _serverIndices.insert("notificationRuleSubscriberDelete", 0);
    _serverConfigs.insert("notificationRuleSubscriberGet", defaultConf);
    _serverIndices.insert("notificationRuleSubscriberGet", 0);
    _serverConfigs.insert("notificationRuleSubscriberGetByPath", defaultConf);
    _serverIndices.insert("notificationRuleSubscriberGetByPath", 0);
    _serverConfigs.insert("notificationRuleSubscriberGetNotificationRuleSubscribers", defaultConf);
    _serverIndices.insert("notificationRuleSubscriberGetNotificationRuleSubscribers", 0);
    _serverConfigs.insert("notificationRuleSubscriberUpdate", defaultConf);
    _serverIndices.insert("notificationRuleSubscriberUpdate", 0);
}

/**
* returns 0 on success and -1, -2 or -3 on failure.
* -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
*/
int OAINotificationRuleSubscriberApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}
void OAINotificationRuleSubscriberApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAINotificationRuleSubscriberApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAINotificationRuleSubscriberApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAINotificationRuleSubscriberApi::setUsername(const QString &username) {
    _username = username;
}

void OAINotificationRuleSubscriberApi::setPassword(const QString &password) {
    _password = password;
}


void OAINotificationRuleSubscriberApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAINotificationRuleSubscriberApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAINotificationRuleSubscriberApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
    * Appends a new ServerConfiguration to the config map for a specific operation.
    * @param operation The id to the target operation.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    * returns the index of the new server config on success and -1 if the operation is not found
    */
int OAINotificationRuleSubscriberApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
    * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
    * @param url A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAINotificationRuleSubscriberApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
    * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
    * @param URL A string that contains the URL of the server
    * @param description A String that describes the server
    * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
    */
void OAINotificationRuleSubscriberApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAINotificationRuleSubscriberApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAINotificationRuleSubscriberApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAINotificationRuleSubscriberApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAINotificationRuleSubscriberApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAINotificationRuleSubscriberApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAINotificationRuleSubscriberApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAINotificationRuleSubscriberApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberDelete(const QString &web_id) {
    QString fullPath = QString(_serverConfigs["notificationRuleSubscriberDelete"][_serverIndices.value("notificationRuleSubscriberDelete")].URL()+"/notificationrulesubscribers/{webId}");
    
    
    {
        QString web_idPathParam("{");
        web_idPathParam.append("webId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webId"+pathSuffix : pathPrefix;
        fullPath.replace(web_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(web_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotificationRuleSubscriberApi::notificationRuleSubscriberDeleteCallback);
    connect(this, &OAINotificationRuleSubscriberApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberDeleteCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT notificationRuleSubscriberDeleteSignal();
        Q_EMIT notificationRuleSubscriberDeleteSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT notificationRuleSubscriberDeleteSignalE(error_type, error_str);
        Q_EMIT notificationRuleSubscriberDeleteSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT notificationRuleSubscriberDeleteSignalError(error_type, error_str);
        Q_EMIT notificationRuleSubscriberDeleteSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberGet(const QString &web_id, const ::OpenAPI::OptionalParam<QString> &selected_fields, const ::OpenAPI::OptionalParam<QString> &web_id_type) {
    QString fullPath = QString(_serverConfigs["notificationRuleSubscriberGet"][_serverIndices.value("notificationRuleSubscriberGet")].URL()+"/notificationrulesubscribers/{webId}");
    
    
    {
        QString web_idPathParam("{");
        web_idPathParam.append("webId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webId"+pathSuffix : pathPrefix;
        fullPath.replace(web_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(web_id)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (selected_fields.hasValue())
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "selectedFields", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("selectedFields")).append(querySuffix).append(QUrl::toPercentEncoding(selected_fields.stringValue()));
    }
    if (web_id_type.hasValue())
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "webIdType", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("webIdType")).append(querySuffix).append(QUrl::toPercentEncoding(web_id_type.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetCallback);
    connect(this, &OAINotificationRuleSubscriberApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINotificationRuleSubscriber output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT notificationRuleSubscriberGetSignal(output);
        Q_EMIT notificationRuleSubscriberGetSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT notificationRuleSubscriberGetSignalE(output, error_type, error_str);
        Q_EMIT notificationRuleSubscriberGetSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT notificationRuleSubscriberGetSignalError(output, error_type, error_str);
        Q_EMIT notificationRuleSubscriberGetSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetByPath(const QString &path, const ::OpenAPI::OptionalParam<QString> &selected_fields, const ::OpenAPI::OptionalParam<QString> &web_id_type) {
    QString fullPath = QString(_serverConfigs["notificationRuleSubscriberGetByPath"][_serverIndices.value("notificationRuleSubscriberGetByPath")].URL()+"/notificationrulesubscribers");
    
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "path", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("path")).append(querySuffix).append(QUrl::toPercentEncoding(path));
    }
    if (selected_fields.hasValue())
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "selectedFields", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("selectedFields")).append(querySuffix).append(QUrl::toPercentEncoding(selected_fields.stringValue()));
    }
    if (web_id_type.hasValue())
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "webIdType", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("webIdType")).append(querySuffix).append(QUrl::toPercentEncoding(web_id_type.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetByPathCallback);
    connect(this, &OAINotificationRuleSubscriberApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetByPathCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAINotificationRuleSubscriber output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT notificationRuleSubscriberGetByPathSignal(output);
        Q_EMIT notificationRuleSubscriberGetByPathSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT notificationRuleSubscriberGetByPathSignalE(output, error_type, error_str);
        Q_EMIT notificationRuleSubscriberGetByPathSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT notificationRuleSubscriberGetByPathSignalError(output, error_type, error_str);
        Q_EMIT notificationRuleSubscriberGetByPathSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetNotificationRuleSubscribers(const QString &web_id, const ::OpenAPI::OptionalParam<QString> &selected_fields, const ::OpenAPI::OptionalParam<QString> &web_id_type) {
    QString fullPath = QString(_serverConfigs["notificationRuleSubscriberGetNotificationRuleSubscribers"][_serverIndices.value("notificationRuleSubscriberGetNotificationRuleSubscribers")].URL()+"/notificationrulesubscribers/{webId}/notificationrulesubscribers");
    
    
    {
        QString web_idPathParam("{");
        web_idPathParam.append("webId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webId"+pathSuffix : pathPrefix;
        fullPath.replace(web_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(web_id)));
    }
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (selected_fields.hasValue())
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "selectedFields", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("selectedFields")).append(querySuffix).append(QUrl::toPercentEncoding(selected_fields.stringValue()));
    }
    if (web_id_type.hasValue())
    {
        queryStyle = "";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "webIdType", false);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("webIdType")).append(querySuffix).append(QUrl::toPercentEncoding(web_id_type.stringValue()));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetNotificationRuleSubscribersCallback);
    connect(this, &OAINotificationRuleSubscriberApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberGetNotificationRuleSubscribersCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIItems_NotificationRuleSubscriber output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT notificationRuleSubscriberGetNotificationRuleSubscribersSignal(output);
        Q_EMIT notificationRuleSubscriberGetNotificationRuleSubscribersSignalFull(worker, output);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT notificationRuleSubscriberGetNotificationRuleSubscribersSignalE(output, error_type, error_str);
        Q_EMIT notificationRuleSubscriberGetNotificationRuleSubscribersSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT notificationRuleSubscriberGetNotificationRuleSubscribersSignalError(output, error_type, error_str);
        Q_EMIT notificationRuleSubscriberGetNotificationRuleSubscribersSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberUpdate(const QString &web_id, const OAINotificationRuleSubscriber &notification_rule_subscriber) {
    QString fullPath = QString(_serverConfigs["notificationRuleSubscriberUpdate"][_serverIndices.value("notificationRuleSubscriberUpdate")].URL()+"/notificationrulesubscribers/{webId}");
    
    
    {
        QString web_idPathParam("{");
        web_idPathParam.append("webId").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "webId", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"webId"+pathSuffix : pathPrefix;
        fullPath.replace(web_idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(web_id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PATCH");

    {

        
        QByteArray output = notification_rule_subscriber.asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAINotificationRuleSubscriberApi::notificationRuleSubscriberUpdateCallback);
    connect(this, &OAINotificationRuleSubscriberApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this]() {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAINotificationRuleSubscriberApi::notificationRuleSubscriberUpdateCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT notificationRuleSubscriberUpdateSignal();
        Q_EMIT notificationRuleSubscriberUpdateSignalFull(worker);
    } else {

#if defined(_MSC_VER)
// For MSVC
#pragma warning(push)
#pragma warning(disable : 4996)
#elif defined(__clang__)
// For Clang
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdeprecated-declarations"
#elif defined(__GNUC__)
// For GCC
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wdeprecated-declarations"
#endif

        Q_EMIT notificationRuleSubscriberUpdateSignalE(error_type, error_str);
        Q_EMIT notificationRuleSubscriberUpdateSignalEFull(worker, error_type, error_str);

#if defined(_MSC_VER)
#pragma warning(pop)
#elif defined(__clang__)
#pragma clang diagnostic pop
#elif defined(__GNUC__)
#pragma GCC diagnostic pop
#endif

        Q_EMIT notificationRuleSubscriberUpdateSignalError(error_type, error_str);
        Q_EMIT notificationRuleSubscriberUpdateSignalErrorFull(worker, error_type, error_str);
    }
}

void OAINotificationRuleSubscriberApi::tokenAvailable(){

    oauthToken token;
    switch (_OauthMethod) {
    case 1: //implicit flow
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 2: //authorization flow
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 3: //client credentials flow
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case 4: //resource owner password flow
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
