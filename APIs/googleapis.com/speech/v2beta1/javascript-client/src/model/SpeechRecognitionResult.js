/**
 * Cloud Speech-to-Text API
 * Converts audio to text by applying powerful neural network models.
 *
 * The version of the OpenAPI document: v2beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SpeechRecognitionAlternative from './SpeechRecognitionAlternative';

/**
 * The SpeechRecognitionResult model module.
 * @module model/SpeechRecognitionResult
 * @version v2beta1
 */
class SpeechRecognitionResult {
    /**
     * Constructs a new <code>SpeechRecognitionResult</code>.
     * A speech recognition result corresponding to a portion of the audio.
     * @alias module:model/SpeechRecognitionResult
     */
    constructor() { 
        
        SpeechRecognitionResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SpeechRecognitionResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SpeechRecognitionResult} obj Optional instance to populate.
     * @return {module:model/SpeechRecognitionResult} The populated <code>SpeechRecognitionResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SpeechRecognitionResult();

            if (data.hasOwnProperty('alternatives')) {
                obj['alternatives'] = ApiClient.convertToType(data['alternatives'], [SpeechRecognitionAlternative]);
            }
            if (data.hasOwnProperty('channelTag')) {
                obj['channelTag'] = ApiClient.convertToType(data['channelTag'], 'Number');
            }
            if (data.hasOwnProperty('languageCode')) {
                obj['languageCode'] = ApiClient.convertToType(data['languageCode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SpeechRecognitionResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SpeechRecognitionResult</code>.
     */
    static validateJSON(data) {
        if (data['alternatives']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['alternatives'])) {
                throw new Error("Expected the field `alternatives` to be an array in the JSON data but got " + data['alternatives']);
            }
            // validate the optional field `alternatives` (array)
            for (const item of data['alternatives']) {
                SpeechRecognitionAlternative.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['languageCode'] && !(typeof data['languageCode'] === 'string' || data['languageCode'] instanceof String)) {
            throw new Error("Expected the field `languageCode` to be a primitive type in the JSON string but got " + data['languageCode']);
        }

        return true;
    }


}



/**
 * Output only. May contain one or more recognition hypotheses (up to the maximum specified in `max_alternatives`). These alternatives are ordered in terms of accuracy, with the top (first) alternative being the most probable, as ranked by the recognizer.
 * @member {Array.<module:model/SpeechRecognitionAlternative>} alternatives
 */
SpeechRecognitionResult.prototype['alternatives'] = undefined;

/**
 * Output only. For multi-channel audio, this is the channel number corresponding to the recognized result for the audio from that channel. For `audio_channel_count` = N, its output values can range from `1` to `N`.
 * @member {Number} channelTag
 */
SpeechRecognitionResult.prototype['channelTag'] = undefined;

/**
 * Output only. The [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt) language tag of the language in this result. This language code was detected to have the most likelihood of being spoken in the audio.
 * @member {String} languageCode
 */
SpeechRecognitionResult.prototype['languageCode'] = undefined;






export default SpeechRecognitionResult;

