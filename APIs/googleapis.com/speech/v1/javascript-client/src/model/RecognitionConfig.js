/**
 * Cloud Speech-to-Text API
 * Converts audio to text by applying powerful neural network models.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RecognitionMetadata from './RecognitionMetadata';
import SpeakerDiarizationConfig from './SpeakerDiarizationConfig';
import SpeechAdaptation from './SpeechAdaptation';
import SpeechContext from './SpeechContext';
import TranscriptNormalization from './TranscriptNormalization';

/**
 * The RecognitionConfig model module.
 * @module model/RecognitionConfig
 * @version v1
 */
class RecognitionConfig {
    /**
     * Constructs a new <code>RecognitionConfig</code>.
     * Provides information to the recognizer that specifies how to process the request.
     * @alias module:model/RecognitionConfig
     */
    constructor() { 
        
        RecognitionConfig.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RecognitionConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecognitionConfig} obj Optional instance to populate.
     * @return {module:model/RecognitionConfig} The populated <code>RecognitionConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecognitionConfig();

            if (data.hasOwnProperty('adaptation')) {
                obj['adaptation'] = SpeechAdaptation.constructFromObject(data['adaptation']);
            }
            if (data.hasOwnProperty('alternativeLanguageCodes')) {
                obj['alternativeLanguageCodes'] = ApiClient.convertToType(data['alternativeLanguageCodes'], ['String']);
            }
            if (data.hasOwnProperty('audioChannelCount')) {
                obj['audioChannelCount'] = ApiClient.convertToType(data['audioChannelCount'], 'Number');
            }
            if (data.hasOwnProperty('diarizationConfig')) {
                obj['diarizationConfig'] = SpeakerDiarizationConfig.constructFromObject(data['diarizationConfig']);
            }
            if (data.hasOwnProperty('enableAutomaticPunctuation')) {
                obj['enableAutomaticPunctuation'] = ApiClient.convertToType(data['enableAutomaticPunctuation'], 'Boolean');
            }
            if (data.hasOwnProperty('enableSeparateRecognitionPerChannel')) {
                obj['enableSeparateRecognitionPerChannel'] = ApiClient.convertToType(data['enableSeparateRecognitionPerChannel'], 'Boolean');
            }
            if (data.hasOwnProperty('enableSpokenEmojis')) {
                obj['enableSpokenEmojis'] = ApiClient.convertToType(data['enableSpokenEmojis'], 'Boolean');
            }
            if (data.hasOwnProperty('enableSpokenPunctuation')) {
                obj['enableSpokenPunctuation'] = ApiClient.convertToType(data['enableSpokenPunctuation'], 'Boolean');
            }
            if (data.hasOwnProperty('enableWordConfidence')) {
                obj['enableWordConfidence'] = ApiClient.convertToType(data['enableWordConfidence'], 'Boolean');
            }
            if (data.hasOwnProperty('enableWordTimeOffsets')) {
                obj['enableWordTimeOffsets'] = ApiClient.convertToType(data['enableWordTimeOffsets'], 'Boolean');
            }
            if (data.hasOwnProperty('encoding')) {
                obj['encoding'] = ApiClient.convertToType(data['encoding'], 'String');
            }
            if (data.hasOwnProperty('languageCode')) {
                obj['languageCode'] = ApiClient.convertToType(data['languageCode'], 'String');
            }
            if (data.hasOwnProperty('maxAlternatives')) {
                obj['maxAlternatives'] = ApiClient.convertToType(data['maxAlternatives'], 'Number');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = RecognitionMetadata.constructFromObject(data['metadata']);
            }
            if (data.hasOwnProperty('model')) {
                obj['model'] = ApiClient.convertToType(data['model'], 'String');
            }
            if (data.hasOwnProperty('profanityFilter')) {
                obj['profanityFilter'] = ApiClient.convertToType(data['profanityFilter'], 'Boolean');
            }
            if (data.hasOwnProperty('sampleRateHertz')) {
                obj['sampleRateHertz'] = ApiClient.convertToType(data['sampleRateHertz'], 'Number');
            }
            if (data.hasOwnProperty('speechContexts')) {
                obj['speechContexts'] = ApiClient.convertToType(data['speechContexts'], [SpeechContext]);
            }
            if (data.hasOwnProperty('transcriptNormalization')) {
                obj['transcriptNormalization'] = TranscriptNormalization.constructFromObject(data['transcriptNormalization']);
            }
            if (data.hasOwnProperty('useEnhanced')) {
                obj['useEnhanced'] = ApiClient.convertToType(data['useEnhanced'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecognitionConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecognitionConfig</code>.
     */
    static validateJSON(data) {
        // validate the optional field `adaptation`
        if (data['adaptation']) { // data not null
          SpeechAdaptation.validateJSON(data['adaptation']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['alternativeLanguageCodes'])) {
            throw new Error("Expected the field `alternativeLanguageCodes` to be an array in the JSON data but got " + data['alternativeLanguageCodes']);
        }
        // validate the optional field `diarizationConfig`
        if (data['diarizationConfig']) { // data not null
          SpeakerDiarizationConfig.validateJSON(data['diarizationConfig']);
        }
        // ensure the json data is a string
        if (data['encoding'] && !(typeof data['encoding'] === 'string' || data['encoding'] instanceof String)) {
            throw new Error("Expected the field `encoding` to be a primitive type in the JSON string but got " + data['encoding']);
        }
        // ensure the json data is a string
        if (data['languageCode'] && !(typeof data['languageCode'] === 'string' || data['languageCode'] instanceof String)) {
            throw new Error("Expected the field `languageCode` to be a primitive type in the JSON string but got " + data['languageCode']);
        }
        // validate the optional field `metadata`
        if (data['metadata']) { // data not null
          RecognitionMetadata.validateJSON(data['metadata']);
        }
        // ensure the json data is a string
        if (data['model'] && !(typeof data['model'] === 'string' || data['model'] instanceof String)) {
            throw new Error("Expected the field `model` to be a primitive type in the JSON string but got " + data['model']);
        }
        if (data['speechContexts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['speechContexts'])) {
                throw new Error("Expected the field `speechContexts` to be an array in the JSON data but got " + data['speechContexts']);
            }
            // validate the optional field `speechContexts` (array)
            for (const item of data['speechContexts']) {
                SpeechContext.validateJSON(item);
            };
        }
        // validate the optional field `transcriptNormalization`
        if (data['transcriptNormalization']) { // data not null
          TranscriptNormalization.validateJSON(data['transcriptNormalization']);
        }

        return true;
    }


}



/**
 * @member {module:model/SpeechAdaptation} adaptation
 */
RecognitionConfig.prototype['adaptation'] = undefined;

/**
 * A list of up to 3 additional [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt) language tags, listing possible alternative languages of the supplied audio. See [Language Support](https://cloud.google.com/speech-to-text/docs/languages) for a list of the currently supported language codes. If alternative languages are listed, recognition result will contain recognition in the most likely language detected including the main language_code. The recognition result will include the language tag of the language detected in the audio. Note: This feature is only supported for Voice Command and Voice Search use cases and performance may vary for other use cases (e.g., phone call transcription).
 * @member {Array.<String>} alternativeLanguageCodes
 */
RecognitionConfig.prototype['alternativeLanguageCodes'] = undefined;

/**
 * The number of channels in the input audio data. ONLY set this for MULTI-CHANNEL recognition. Valid values for LINEAR16, OGG_OPUS and FLAC are `1`-`8`. Valid value for MULAW, AMR, AMR_WB and SPEEX_WITH_HEADER_BYTE is only `1`. If `0` or omitted, defaults to one channel (mono). Note: We only recognize the first channel by default. To perform independent recognition on each channel set `enable_separate_recognition_per_channel` to 'true'.
 * @member {Number} audioChannelCount
 */
RecognitionConfig.prototype['audioChannelCount'] = undefined;

/**
 * @member {module:model/SpeakerDiarizationConfig} diarizationConfig
 */
RecognitionConfig.prototype['diarizationConfig'] = undefined;

/**
 * If 'true', adds punctuation to recognition result hypotheses. This feature is only available in select languages. Setting this for requests in other languages has no effect at all. The default 'false' value does not add punctuation to result hypotheses.
 * @member {Boolean} enableAutomaticPunctuation
 */
RecognitionConfig.prototype['enableAutomaticPunctuation'] = undefined;

/**
 * This needs to be set to `true` explicitly and `audio_channel_count` > 1 to get each channel recognized separately. The recognition result will contain a `channel_tag` field to state which channel that result belongs to. If this is not true, we will only recognize the first channel. The request is billed cumulatively for all channels recognized: `audio_channel_count` multiplied by the length of the audio.
 * @member {Boolean} enableSeparateRecognitionPerChannel
 */
RecognitionConfig.prototype['enableSeparateRecognitionPerChannel'] = undefined;

/**
 * The spoken emoji behavior for the call If not set, uses default behavior based on model of choice If 'true', adds spoken emoji formatting for the request. This will replace spoken emojis with the corresponding Unicode symbols in the final transcript. If 'false', spoken emojis are not replaced.
 * @member {Boolean} enableSpokenEmojis
 */
RecognitionConfig.prototype['enableSpokenEmojis'] = undefined;

/**
 * The spoken punctuation behavior for the call If not set, uses default behavior based on model of choice e.g. command_and_search will enable spoken punctuation by default If 'true', replaces spoken punctuation with the corresponding symbols in the request. For example, \"how are you question mark\" becomes \"how are you?\". See https://cloud.google.com/speech-to-text/docs/spoken-punctuation for support. If 'false', spoken punctuation is not replaced.
 * @member {Boolean} enableSpokenPunctuation
 */
RecognitionConfig.prototype['enableSpokenPunctuation'] = undefined;

/**
 * If `true`, the top result includes a list of words and the confidence for those words. If `false`, no word-level confidence information is returned. The default is `false`.
 * @member {Boolean} enableWordConfidence
 */
RecognitionConfig.prototype['enableWordConfidence'] = undefined;

/**
 * If `true`, the top result includes a list of words and the start and end time offsets (timestamps) for those words. If `false`, no word-level time offset information is returned. The default is `false`.
 * @member {Boolean} enableWordTimeOffsets
 */
RecognitionConfig.prototype['enableWordTimeOffsets'] = undefined;

/**
 * Encoding of audio data sent in all `RecognitionAudio` messages. This field is optional for `FLAC` and `WAV` audio files and required for all other audio formats. For details, see AudioEncoding.
 * @member {module:model/RecognitionConfig.EncodingEnum} encoding
 */
RecognitionConfig.prototype['encoding'] = undefined;

/**
 * Required. The language of the supplied audio as a [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt) language tag. Example: \"en-US\". See [Language Support](https://cloud.google.com/speech-to-text/docs/languages) for a list of the currently supported language codes.
 * @member {String} languageCode
 */
RecognitionConfig.prototype['languageCode'] = undefined;

/**
 * Maximum number of recognition hypotheses to be returned. Specifically, the maximum number of `SpeechRecognitionAlternative` messages within each `SpeechRecognitionResult`. The server may return fewer than `max_alternatives`. Valid values are `0`-`30`. A value of `0` or `1` will return a maximum of one. If omitted, will return a maximum of one.
 * @member {Number} maxAlternatives
 */
RecognitionConfig.prototype['maxAlternatives'] = undefined;

/**
 * @member {module:model/RecognitionMetadata} metadata
 */
RecognitionConfig.prototype['metadata'] = undefined;

/**
 * Which model to select for the given request. Select the model best suited to your domain to get best results. If a model is not explicitly specified, then we auto-select a model based on the parameters in the RecognitionConfig. *Model* *Description* latest_long Best for long form content like media or conversation. latest_short Best for short form content like commands or single shot directed speech. command_and_search Best for short queries such as voice commands or voice search. phone_call Best for audio that originated from a phone call (typically recorded at an 8khz sampling rate). video Best for audio that originated from video or includes multiple speakers. Ideally the audio is recorded at a 16khz or greater sampling rate. This is a premium model that costs more than the standard rate. default Best for audio that is not one of the specific audio models. For example, long-form audio. Ideally the audio is high-fidelity, recorded at a 16khz or greater sampling rate. medical_conversation Best for audio that originated from a conversation between a medical provider and patient. medical_dictation Best for audio that originated from dictation notes by a medical provider. 
 * @member {String} model
 */
RecognitionConfig.prototype['model'] = undefined;

/**
 * If set to `true`, the server will attempt to filter out profanities, replacing all but the initial character in each filtered word with asterisks, e.g. \"f***\". If set to `false` or omitted, profanities won't be filtered out.
 * @member {Boolean} profanityFilter
 */
RecognitionConfig.prototype['profanityFilter'] = undefined;

/**
 * Sample rate in Hertz of the audio data sent in all `RecognitionAudio` messages. Valid values are: 8000-48000. 16000 is optimal. For best results, set the sampling rate of the audio source to 16000 Hz. If that's not possible, use the native sample rate of the audio source (instead of re-sampling). This field is optional for FLAC and WAV audio files, but is required for all other audio formats. For details, see AudioEncoding.
 * @member {Number} sampleRateHertz
 */
RecognitionConfig.prototype['sampleRateHertz'] = undefined;

/**
 * Array of SpeechContext. A means to provide context to assist the speech recognition. For more information, see [speech adaptation](https://cloud.google.com/speech-to-text/docs/adaptation).
 * @member {Array.<module:model/SpeechContext>} speechContexts
 */
RecognitionConfig.prototype['speechContexts'] = undefined;

/**
 * @member {module:model/TranscriptNormalization} transcriptNormalization
 */
RecognitionConfig.prototype['transcriptNormalization'] = undefined;

/**
 * Set to true to use an enhanced model for speech recognition. If `use_enhanced` is set to true and the `model` field is not set, then an appropriate enhanced model is chosen if an enhanced model exists for the audio. If `use_enhanced` is true and an enhanced version of the specified model does not exist, then the speech is recognized using the standard version of the specified model.
 * @member {Boolean} useEnhanced
 */
RecognitionConfig.prototype['useEnhanced'] = undefined;





/**
 * Allowed values for the <code>encoding</code> property.
 * @enum {String}
 * @readonly
 */
RecognitionConfig['EncodingEnum'] = {

    /**
     * value: "ENCODING_UNSPECIFIED"
     * @const
     */
    "ENCODING_UNSPECIFIED": "ENCODING_UNSPECIFIED",

    /**
     * value: "LINEAR16"
     * @const
     */
    "LINEAR16": "LINEAR16",

    /**
     * value: "FLAC"
     * @const
     */
    "FLAC": "FLAC",

    /**
     * value: "MULAW"
     * @const
     */
    "MULAW": "MULAW",

    /**
     * value: "AMR"
     * @const
     */
    "AMR": "AMR",

    /**
     * value: "AMR_WB"
     * @const
     */
    "AMR_WB": "AMR_WB",

    /**
     * value: "OGG_OPUS"
     * @const
     */
    "OGG_OPUS": "OGG_OPUS",

    /**
     * value: "SPEEX_WITH_HEADER_BYTE"
     * @const
     */
    "SPEEX_WITH_HEADER_BYTE": "SPEEX_WITH_HEADER_BYTE",

    /**
     * value: "MP3"
     * @const
     */
    "MP3": "MP3",

    /**
     * value: "WEBM_OPUS"
     * @const
     */
    "WEBM_OPUS": "WEBM_OPUS"
};



export default RecognitionConfig;

