/**
 * Access Approval API
 * An API for controlling access to data by Google personnel.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnrolledService from './EnrolledService';

/**
 * The AccessApprovalSettings model module.
 * @module model/AccessApprovalSettings
 * @version v1
 */
class AccessApprovalSettings {
    /**
     * Constructs a new <code>AccessApprovalSettings</code>.
     * Settings on a Project/Folder/Organization related to Access Approval.
     * @alias module:model/AccessApprovalSettings
     */
    constructor() { 
        
        AccessApprovalSettings.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AccessApprovalSettings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccessApprovalSettings} obj Optional instance to populate.
     * @return {module:model/AccessApprovalSettings} The populated <code>AccessApprovalSettings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccessApprovalSettings();

            if (data.hasOwnProperty('activeKeyVersion')) {
                obj['activeKeyVersion'] = ApiClient.convertToType(data['activeKeyVersion'], 'String');
            }
            if (data.hasOwnProperty('ancestorHasActiveKeyVersion')) {
                obj['ancestorHasActiveKeyVersion'] = ApiClient.convertToType(data['ancestorHasActiveKeyVersion'], 'Boolean');
            }
            if (data.hasOwnProperty('enrolledAncestor')) {
                obj['enrolledAncestor'] = ApiClient.convertToType(data['enrolledAncestor'], 'Boolean');
            }
            if (data.hasOwnProperty('enrolledServices')) {
                obj['enrolledServices'] = ApiClient.convertToType(data['enrolledServices'], [EnrolledService]);
            }
            if (data.hasOwnProperty('invalidKeyVersion')) {
                obj['invalidKeyVersion'] = ApiClient.convertToType(data['invalidKeyVersion'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('notificationEmails')) {
                obj['notificationEmails'] = ApiClient.convertToType(data['notificationEmails'], ['String']);
            }
            if (data.hasOwnProperty('notificationPubsubTopic')) {
                obj['notificationPubsubTopic'] = ApiClient.convertToType(data['notificationPubsubTopic'], 'String');
            }
            if (data.hasOwnProperty('preferNoBroadApprovalRequests')) {
                obj['preferNoBroadApprovalRequests'] = ApiClient.convertToType(data['preferNoBroadApprovalRequests'], 'Boolean');
            }
            if (data.hasOwnProperty('preferredRequestExpirationDays')) {
                obj['preferredRequestExpirationDays'] = ApiClient.convertToType(data['preferredRequestExpirationDays'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccessApprovalSettings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccessApprovalSettings</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['activeKeyVersion'] && !(typeof data['activeKeyVersion'] === 'string' || data['activeKeyVersion'] instanceof String)) {
            throw new Error("Expected the field `activeKeyVersion` to be a primitive type in the JSON string but got " + data['activeKeyVersion']);
        }
        if (data['enrolledServices']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['enrolledServices'])) {
                throw new Error("Expected the field `enrolledServices` to be an array in the JSON data but got " + data['enrolledServices']);
            }
            // validate the optional field `enrolledServices` (array)
            for (const item of data['enrolledServices']) {
                EnrolledService.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['notificationEmails'])) {
            throw new Error("Expected the field `notificationEmails` to be an array in the JSON data but got " + data['notificationEmails']);
        }
        // ensure the json data is a string
        if (data['notificationPubsubTopic'] && !(typeof data['notificationPubsubTopic'] === 'string' || data['notificationPubsubTopic'] instanceof String)) {
            throw new Error("Expected the field `notificationPubsubTopic` to be a primitive type in the JSON string but got " + data['notificationPubsubTopic']);
        }

        return true;
    }


}



/**
 * The asymmetric crypto key version to use for signing approval requests. Empty active_key_version indicates that a Google-managed key should be used for signing. This property will be ignored if set by an ancestor of this resource, and new non-empty values may not be set.
 * @member {String} activeKeyVersion
 */
AccessApprovalSettings.prototype['activeKeyVersion'] = undefined;

/**
 * Output only. This field is read only (not settable via UpdateAccessApprovalSettings method). If the field is true, that indicates that an ancestor of this Project or Folder has set active_key_version (this field will always be unset for the organization since organizations do not have ancestors).
 * @member {Boolean} ancestorHasActiveKeyVersion
 */
AccessApprovalSettings.prototype['ancestorHasActiveKeyVersion'] = undefined;

/**
 * Output only. This field is read only (not settable via UpdateAccessApprovalSettings method). If the field is true, that indicates that at least one service is enrolled for Access Approval in one or more ancestors of the Project or Folder (this field will always be unset for the organization since organizations do not have ancestors).
 * @member {Boolean} enrolledAncestor
 */
AccessApprovalSettings.prototype['enrolledAncestor'] = undefined;

/**
 * A list of Google Cloud Services for which the given resource has Access Approval enrolled. Access requests for the resource given by name against any of these services contained here will be required to have explicit approval. If name refers to an organization, enrollment can be done for individual services. If name refers to a folder or project, enrollment can only be done on an all or nothing basis. If a cloud_product is repeated in this list, the first entry will be honored and all following entries will be discarded. A maximum of 10 enrolled services will be enforced, to be expanded as the set of supported services is expanded.
 * @member {Array.<module:model/EnrolledService>} enrolledServices
 */
AccessApprovalSettings.prototype['enrolledServices'] = undefined;

/**
 * Output only. This field is read only (not settable via UpdateAccessApprovalSettings method). If the field is true, that indicates that there is some configuration issue with the active_key_version configured at this level in the resource hierarchy (e.g. it doesn't exist or the Access Approval service account doesn't have the correct permissions on it, etc.) This key version is not necessarily the effective key version at this level, as key versions are inherited top-down.
 * @member {Boolean} invalidKeyVersion
 */
AccessApprovalSettings.prototype['invalidKeyVersion'] = undefined;

/**
 * The resource name of the settings. Format is one of: * \"projects/{project}/accessApprovalSettings\" * \"folders/{folder}/accessApprovalSettings\" * \"organizations/{organization}/accessApprovalSettings\"
 * @member {String} name
 */
AccessApprovalSettings.prototype['name'] = undefined;

/**
 * A list of email addresses to which notifications relating to approval requests should be sent. Notifications relating to a resource will be sent to all emails in the settings of ancestor resources of that resource. A maximum of 50 email addresses are allowed.
 * @member {Array.<String>} notificationEmails
 */
AccessApprovalSettings.prototype['notificationEmails'] = undefined;

/**
 * Optional. A pubsub topic to which notifications relating to approval requests should be sent.
 * @member {String} notificationPubsubTopic
 */
AccessApprovalSettings.prototype['notificationPubsubTopic'] = undefined;

/**
 * This preference is communicated to Google personnel when sending an approval request but can be overridden if necessary.
 * @member {Boolean} preferNoBroadApprovalRequests
 */
AccessApprovalSettings.prototype['preferNoBroadApprovalRequests'] = undefined;

/**
 * This preference is shared with Google personnel, but can be overridden if said personnel deems necessary. The approver ultimately can set the expiration at approval time.
 * @member {Number} preferredRequestExpirationDays
 */
AccessApprovalSettings.prototype['preferredRequestExpirationDays'] = undefined;






export default AccessApprovalSettings;

