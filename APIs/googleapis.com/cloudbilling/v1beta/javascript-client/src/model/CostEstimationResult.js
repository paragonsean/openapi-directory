/**
 * Cloud Billing API
 * Allows developers to manage billing for their Google Cloud Platform projects programmatically.
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SegmentCostEstimate from './SegmentCostEstimate';
import Sku from './Sku';

/**
 * The CostEstimationResult model module.
 * @module model/CostEstimationResult
 * @version v1beta
 */
class CostEstimationResult {
    /**
     * Constructs a new <code>CostEstimationResult</code>.
     * The result of a estimating the costs of a &#x60;CostScenario&#x60;.
     * @alias module:model/CostEstimationResult
     */
    constructor() { 
        
        CostEstimationResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CostEstimationResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CostEstimationResult} obj Optional instance to populate.
     * @return {module:model/CostEstimationResult} The populated <code>CostEstimationResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CostEstimationResult();

            if (data.hasOwnProperty('currencyCode')) {
                obj['currencyCode'] = ApiClient.convertToType(data['currencyCode'], 'String');
            }
            if (data.hasOwnProperty('segmentCostEstimates')) {
                obj['segmentCostEstimates'] = ApiClient.convertToType(data['segmentCostEstimates'], [SegmentCostEstimate]);
            }
            if (data.hasOwnProperty('skus')) {
                obj['skus'] = ApiClient.convertToType(data['skus'], [Sku]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CostEstimationResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CostEstimationResult</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['currencyCode'] && !(typeof data['currencyCode'] === 'string' || data['currencyCode'] instanceof String)) {
            throw new Error("Expected the field `currencyCode` to be a primitive type in the JSON string but got " + data['currencyCode']);
        }
        if (data['segmentCostEstimates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['segmentCostEstimates'])) {
                throw new Error("Expected the field `segmentCostEstimates` to be an array in the JSON data but got " + data['segmentCostEstimates']);
            }
            // validate the optional field `segmentCostEstimates` (array)
            for (const item of data['segmentCostEstimates']) {
                SegmentCostEstimate.validateJSON(item);
            };
        }
        if (data['skus']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['skus'])) {
                throw new Error("Expected the field `skus` to be an array in the JSON data but got " + data['skus']);
            }
            // validate the optional field `skus` (array)
            for (const item of data['skus']) {
                Sku.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Required. The ISO 4217 currency code for the cost estimate.
 * @member {String} currencyCode
 */
CostEstimationResult.prototype['currencyCode'] = undefined;

/**
 * Required. Estimated costs for each idealized month of a `CostScenario`.
 * @member {Array.<module:model/SegmentCostEstimate>} segmentCostEstimates
 */
CostEstimationResult.prototype['segmentCostEstimates'] = undefined;

/**
 * Required. Information about SKUs used in the estimate.
 * @member {Array.<module:model/Sku>} skus
 */
CostEstimationResult.prototype['skus'] = undefined;






export default CostEstimationResult;

