/**
 * Cloud Billing API
 * Allows developers to manage billing for their Google Cloud Platform projects programmatically.
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CostEstimate from './CostEstimate';
import SkuCostEstimate from './SkuCostEstimate';

/**
 * The WorkloadCostEstimate model module.
 * @module model/WorkloadCostEstimate
 * @version v1beta
 */
class WorkloadCostEstimate {
    /**
     * Constructs a new <code>WorkloadCostEstimate</code>.
     * Estimated cost for a workload.
     * @alias module:model/WorkloadCostEstimate
     */
    constructor() { 
        
        WorkloadCostEstimate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WorkloadCostEstimate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkloadCostEstimate} obj Optional instance to populate.
     * @return {module:model/WorkloadCostEstimate} The populated <code>WorkloadCostEstimate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkloadCostEstimate();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('skuCostEstimates')) {
                obj['skuCostEstimates'] = ApiClient.convertToType(data['skuCostEstimates'], [SkuCostEstimate]);
            }
            if (data.hasOwnProperty('workloadTotalCostEstimate')) {
                obj['workloadTotalCostEstimate'] = CostEstimate.constructFromObject(data['workloadTotalCostEstimate']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkloadCostEstimate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkloadCostEstimate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['skuCostEstimates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['skuCostEstimates'])) {
                throw new Error("Expected the field `skuCostEstimates` to be an array in the JSON data but got " + data['skuCostEstimates']);
            }
            // validate the optional field `skuCostEstimates` (array)
            for (const item of data['skuCostEstimates']) {
                SkuCostEstimate.validateJSON(item);
            };
        }
        // validate the optional field `workloadTotalCostEstimate`
        if (data['workloadTotalCostEstimate']) { // data not null
          CostEstimate.validateJSON(data['workloadTotalCostEstimate']);
        }

        return true;
    }


}



/**
 * The name of the workload, as specified in the `CostScenario`.
 * @member {String} name
 */
WorkloadCostEstimate.prototype['name'] = undefined;

/**
 * Estimated costs for each SKU in the workload.
 * @member {Array.<module:model/SkuCostEstimate>} skuCostEstimates
 */
WorkloadCostEstimate.prototype['skuCostEstimates'] = undefined;

/**
 * @member {module:model/CostEstimate} workloadTotalCostEstimate
 */
WorkloadCostEstimate.prototype['workloadTotalCostEstimate'] = undefined;






export default WorkloadCostEstimate;

