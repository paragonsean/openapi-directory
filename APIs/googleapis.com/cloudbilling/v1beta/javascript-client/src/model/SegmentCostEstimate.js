/**
 * Cloud Billing API
 * Allows developers to manage billing for their Google Cloud Platform projects programmatically.
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CommitmentCostEstimate from './CommitmentCostEstimate';
import CostEstimate from './CostEstimate';
import EstimationTimePoint from './EstimationTimePoint';
import WorkloadCostEstimate from './WorkloadCostEstimate';

/**
 * The SegmentCostEstimate model module.
 * @module model/SegmentCostEstimate
 * @version v1beta
 */
class SegmentCostEstimate {
    /**
     * Constructs a new <code>SegmentCostEstimate</code>.
     * Workload cost estimates for a single time segment.
     * @alias module:model/SegmentCostEstimate
     */
    constructor() { 
        
        SegmentCostEstimate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SegmentCostEstimate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SegmentCostEstimate} obj Optional instance to populate.
     * @return {module:model/SegmentCostEstimate} The populated <code>SegmentCostEstimate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SegmentCostEstimate();

            if (data.hasOwnProperty('commitmentCostEstimates')) {
                obj['commitmentCostEstimates'] = ApiClient.convertToType(data['commitmentCostEstimates'], [CommitmentCostEstimate]);
            }
            if (data.hasOwnProperty('segmentStartTime')) {
                obj['segmentStartTime'] = EstimationTimePoint.constructFromObject(data['segmentStartTime']);
            }
            if (data.hasOwnProperty('segmentTotalCostEstimate')) {
                obj['segmentTotalCostEstimate'] = CostEstimate.constructFromObject(data['segmentTotalCostEstimate']);
            }
            if (data.hasOwnProperty('workloadCostEstimates')) {
                obj['workloadCostEstimates'] = ApiClient.convertToType(data['workloadCostEstimates'], [WorkloadCostEstimate]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SegmentCostEstimate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SegmentCostEstimate</code>.
     */
    static validateJSON(data) {
        if (data['commitmentCostEstimates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['commitmentCostEstimates'])) {
                throw new Error("Expected the field `commitmentCostEstimates` to be an array in the JSON data but got " + data['commitmentCostEstimates']);
            }
            // validate the optional field `commitmentCostEstimates` (array)
            for (const item of data['commitmentCostEstimates']) {
                CommitmentCostEstimate.validateJSON(item);
            };
        }
        // validate the optional field `segmentStartTime`
        if (data['segmentStartTime']) { // data not null
          EstimationTimePoint.validateJSON(data['segmentStartTime']);
        }
        // validate the optional field `segmentTotalCostEstimate`
        if (data['segmentTotalCostEstimate']) { // data not null
          CostEstimate.validateJSON(data['segmentTotalCostEstimate']);
        }
        if (data['workloadCostEstimates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['workloadCostEstimates'])) {
                throw new Error("Expected the field `workloadCostEstimates` to be an array in the JSON data but got " + data['workloadCostEstimates']);
            }
            // validate the optional field `workloadCostEstimates` (array)
            for (const item of data['workloadCostEstimates']) {
                WorkloadCostEstimate.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Estimated costs for each commitment.
 * @member {Array.<module:model/CommitmentCostEstimate>} commitmentCostEstimates
 */
SegmentCostEstimate.prototype['commitmentCostEstimates'] = undefined;

/**
 * @member {module:model/EstimationTimePoint} segmentStartTime
 */
SegmentCostEstimate.prototype['segmentStartTime'] = undefined;

/**
 * @member {module:model/CostEstimate} segmentTotalCostEstimate
 */
SegmentCostEstimate.prototype['segmentTotalCostEstimate'] = undefined;

/**
 * Estimated costs for each workload.
 * @member {Array.<module:model/WorkloadCostEstimate>} workloadCostEstimates
 */
SegmentCostEstimate.prototype['workloadCostEstimates'] = undefined;






export default SegmentCostEstimate;

