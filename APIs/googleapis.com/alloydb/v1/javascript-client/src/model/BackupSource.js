/**
 * AlloyDB API
 * AlloyDB for PostgreSQL is an open source-compatible database service that provides a powerful option for migrating, modernizing, or building commercial-grade applications. It offers full compatibility with standard PostgreSQL, and is more than 4x faster for transactional workloads and up to 100x faster for analytical queries than standard PostgreSQL in our performance tests. AlloyDB for PostgreSQL offers a 99.99 percent availability SLA inclusive of maintenance. AlloyDB is optimized for the most demanding use cases, allowing you to build new applications that require high transaction throughput, large database sizes, or multiple read resources; scale existing PostgreSQL workloads with no application changes; and modernize legacy proprietary databases. 
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BackupSource model module.
 * @module model/BackupSource
 * @version v1
 */
class BackupSource {
    /**
     * Constructs a new <code>BackupSource</code>.
     * Message describing a BackupSource.
     * @alias module:model/BackupSource
     */
    constructor() { 
        
        BackupSource.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BackupSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BackupSource} obj Optional instance to populate.
     * @return {module:model/BackupSource} The populated <code>BackupSource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BackupSource();

            if (data.hasOwnProperty('backupName')) {
                obj['backupName'] = ApiClient.convertToType(data['backupName'], 'String');
            }
            if (data.hasOwnProperty('backupUid')) {
                obj['backupUid'] = ApiClient.convertToType(data['backupUid'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BackupSource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BackupSource</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['backupName'] && !(typeof data['backupName'] === 'string' || data['backupName'] instanceof String)) {
            throw new Error("Expected the field `backupName` to be a primitive type in the JSON string but got " + data['backupName']);
        }
        // ensure the json data is a string
        if (data['backupUid'] && !(typeof data['backupUid'] === 'string' || data['backupUid'] instanceof String)) {
            throw new Error("Expected the field `backupUid` to be a primitive type in the JSON string but got " + data['backupUid']);
        }

        return true;
    }


}



/**
 * Required. The name of the backup resource with the format: * projects/{project}/locations/{region}/backups/{backup_id}
 * @member {String} backupName
 */
BackupSource.prototype['backupName'] = undefined;

/**
 * Output only. The system-generated UID of the backup which was used to create this resource. The UID is generated when the backup is created, and it is retained until the backup is deleted.
 * @member {String} backupUid
 */
BackupSource.prototype['backupUid'] = undefined;






export default BackupSource;

