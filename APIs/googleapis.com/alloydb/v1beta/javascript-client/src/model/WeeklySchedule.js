/**
 * AlloyDB API
 * AlloyDB for PostgreSQL is an open source-compatible database service that provides a powerful option for migrating, modernizing, or building commercial-grade applications. It offers full compatibility with standard PostgreSQL, and is more than 4x faster for transactional workloads and up to 100x faster for analytical queries than standard PostgreSQL in our performance tests. AlloyDB for PostgreSQL offers a 99.99 percent availability SLA inclusive of maintenance. AlloyDB is optimized for the most demanding use cases, allowing you to build new applications that require high transaction throughput, large database sizes, or multiple read resources; scale existing PostgreSQL workloads with no application changes; and modernize legacy proprietary databases. 
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GoogleTypeTimeOfDay from './GoogleTypeTimeOfDay';

/**
 * The WeeklySchedule model module.
 * @module model/WeeklySchedule
 * @version v1beta
 */
class WeeklySchedule {
    /**
     * Constructs a new <code>WeeklySchedule</code>.
     * A weekly schedule starts a backup at prescribed start times within a day, for the specified days of the week. The weekly schedule message is flexible and can be used to create many types of schedules. For example, to have a daily backup that starts at 22:00, configure the &#x60;start_times&#x60; field to have one element \&quot;22:00\&quot; and the &#x60;days_of_week&#x60; field to have all seven days of the week.
     * @alias module:model/WeeklySchedule
     */
    constructor() { 
        
        WeeklySchedule.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WeeklySchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WeeklySchedule} obj Optional instance to populate.
     * @return {module:model/WeeklySchedule} The populated <code>WeeklySchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WeeklySchedule();

            if (data.hasOwnProperty('daysOfWeek')) {
                obj['daysOfWeek'] = ApiClient.convertToType(data['daysOfWeek'], ['String']);
            }
            if (data.hasOwnProperty('startTimes')) {
                obj['startTimes'] = ApiClient.convertToType(data['startTimes'], [GoogleTypeTimeOfDay]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WeeklySchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WeeklySchedule</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['daysOfWeek'])) {
            throw new Error("Expected the field `daysOfWeek` to be an array in the JSON data but got " + data['daysOfWeek']);
        }
        if (data['startTimes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['startTimes'])) {
                throw new Error("Expected the field `startTimes` to be an array in the JSON data but got " + data['startTimes']);
            }
            // validate the optional field `startTimes` (array)
            for (const item of data['startTimes']) {
                GoogleTypeTimeOfDay.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The days of the week to perform a backup. If this field is left empty, the default of every day of the week is used.
 * @member {Array.<module:model/WeeklySchedule.DaysOfWeekEnum>} daysOfWeek
 */
WeeklySchedule.prototype['daysOfWeek'] = undefined;

/**
 * The times during the day to start a backup. The start times are assumed to be in UTC and to be an exact hour (e.g., 04:00:00). If no start times are provided, a single fixed start time is chosen arbitrarily.
 * @member {Array.<module:model/GoogleTypeTimeOfDay>} startTimes
 */
WeeklySchedule.prototype['startTimes'] = undefined;





/**
 * Allowed values for the <code>daysOfWeek</code> property.
 * @enum {String}
 * @readonly
 */
WeeklySchedule['DaysOfWeekEnum'] = {

    /**
     * value: "DAY_OF_WEEK_UNSPECIFIED"
     * @const
     */
    "DAY_OF_WEEK_UNSPECIFIED": "DAY_OF_WEEK_UNSPECIFIED",

    /**
     * value: "MONDAY"
     * @const
     */
    "MONDAY": "MONDAY",

    /**
     * value: "TUESDAY"
     * @const
     */
    "TUESDAY": "TUESDAY",

    /**
     * value: "WEDNESDAY"
     * @const
     */
    "WEDNESDAY": "WEDNESDAY",

    /**
     * value: "THURSDAY"
     * @const
     */
    "THURSDAY": "THURSDAY",

    /**
     * value: "FRIDAY"
     * @const
     */
    "FRIDAY": "FRIDAY",

    /**
     * value: "SATURDAY"
     * @const
     */
    "SATURDAY": "SATURDAY",

    /**
     * value: "SUNDAY"
     * @const
     */
    "SUNDAY": "SUNDAY"
};



export default WeeklySchedule;

