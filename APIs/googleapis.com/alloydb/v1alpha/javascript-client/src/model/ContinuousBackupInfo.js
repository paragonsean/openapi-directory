/**
 * AlloyDB API
 * AlloyDB for PostgreSQL is an open source-compatible database service that provides a powerful option for migrating, modernizing, or building commercial-grade applications. It offers full compatibility with standard PostgreSQL, and is more than 4x faster for transactional workloads and up to 100x faster for analytical queries than standard PostgreSQL in our performance tests. AlloyDB for PostgreSQL offers a 99.99 percent availability SLA inclusive of maintenance. AlloyDB is optimized for the most demanding use cases, allowing you to build new applications that require high transaction throughput, large database sizes, or multiple read resources; scale existing PostgreSQL workloads with no application changes; and modernize legacy proprietary databases. 
 *
 * The version of the OpenAPI document: v1alpha
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EncryptionInfo from './EncryptionInfo';

/**
 * The ContinuousBackupInfo model module.
 * @module model/ContinuousBackupInfo
 * @version v1alpha
 */
class ContinuousBackupInfo {
    /**
     * Constructs a new <code>ContinuousBackupInfo</code>.
     * ContinuousBackupInfo describes the continuous backup properties of a cluster.
     * @alias module:model/ContinuousBackupInfo
     */
    constructor() { 
        
        ContinuousBackupInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ContinuousBackupInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContinuousBackupInfo} obj Optional instance to populate.
     * @return {module:model/ContinuousBackupInfo} The populated <code>ContinuousBackupInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContinuousBackupInfo();

            if (data.hasOwnProperty('earliestRestorableTime')) {
                obj['earliestRestorableTime'] = ApiClient.convertToType(data['earliestRestorableTime'], 'String');
            }
            if (data.hasOwnProperty('enabledTime')) {
                obj['enabledTime'] = ApiClient.convertToType(data['enabledTime'], 'String');
            }
            if (data.hasOwnProperty('encryptionInfo')) {
                obj['encryptionInfo'] = EncryptionInfo.constructFromObject(data['encryptionInfo']);
            }
            if (data.hasOwnProperty('schedule')) {
                obj['schedule'] = ApiClient.convertToType(data['schedule'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContinuousBackupInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContinuousBackupInfo</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['earliestRestorableTime'] && !(typeof data['earliestRestorableTime'] === 'string' || data['earliestRestorableTime'] instanceof String)) {
            throw new Error("Expected the field `earliestRestorableTime` to be a primitive type in the JSON string but got " + data['earliestRestorableTime']);
        }
        // ensure the json data is a string
        if (data['enabledTime'] && !(typeof data['enabledTime'] === 'string' || data['enabledTime'] instanceof String)) {
            throw new Error("Expected the field `enabledTime` to be a primitive type in the JSON string but got " + data['enabledTime']);
        }
        // validate the optional field `encryptionInfo`
        if (data['encryptionInfo']) { // data not null
          EncryptionInfo.validateJSON(data['encryptionInfo']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['schedule'])) {
            throw new Error("Expected the field `schedule` to be an array in the JSON data but got " + data['schedule']);
        }

        return true;
    }


}



/**
 * Output only. The earliest restorable time that can be restored to. Output only field.
 * @member {String} earliestRestorableTime
 */
ContinuousBackupInfo.prototype['earliestRestorableTime'] = undefined;

/**
 * Output only. When ContinuousBackup was most recently enabled. Set to null if ContinuousBackup is not enabled.
 * @member {String} enabledTime
 */
ContinuousBackupInfo.prototype['enabledTime'] = undefined;

/**
 * @member {module:model/EncryptionInfo} encryptionInfo
 */
ContinuousBackupInfo.prototype['encryptionInfo'] = undefined;

/**
 * Output only. Days of the week on which a continuous backup is taken. Output only field. Ignored if passed into the request.
 * @member {Array.<module:model/ContinuousBackupInfo.ScheduleEnum>} schedule
 */
ContinuousBackupInfo.prototype['schedule'] = undefined;





/**
 * Allowed values for the <code>schedule</code> property.
 * @enum {String}
 * @readonly
 */
ContinuousBackupInfo['ScheduleEnum'] = {

    /**
     * value: "DAY_OF_WEEK_UNSPECIFIED"
     * @const
     */
    "DAY_OF_WEEK_UNSPECIFIED": "DAY_OF_WEEK_UNSPECIFIED",

    /**
     * value: "MONDAY"
     * @const
     */
    "MONDAY": "MONDAY",

    /**
     * value: "TUESDAY"
     * @const
     */
    "TUESDAY": "TUESDAY",

    /**
     * value: "WEDNESDAY"
     * @const
     */
    "WEDNESDAY": "WEDNESDAY",

    /**
     * value: "THURSDAY"
     * @const
     */
    "THURSDAY": "THURSDAY",

    /**
     * value: "FRIDAY"
     * @const
     */
    "FRIDAY": "FRIDAY",

    /**
     * value: "SATURDAY"
     * @const
     */
    "SATURDAY": "SATURDAY",

    /**
     * value: "SUNDAY"
     * @const
     */
    "SUNDAY": "SUNDAY"
};



export default ContinuousBackupInfo;

