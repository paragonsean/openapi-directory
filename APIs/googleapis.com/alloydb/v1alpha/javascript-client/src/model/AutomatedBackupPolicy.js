/**
 * AlloyDB API
 * AlloyDB for PostgreSQL is an open source-compatible database service that provides a powerful option for migrating, modernizing, or building commercial-grade applications. It offers full compatibility with standard PostgreSQL, and is more than 4x faster for transactional workloads and up to 100x faster for analytical queries than standard PostgreSQL in our performance tests. AlloyDB for PostgreSQL offers a 99.99 percent availability SLA inclusive of maintenance. AlloyDB is optimized for the most demanding use cases, allowing you to build new applications that require high transaction throughput, large database sizes, or multiple read resources; scale existing PostgreSQL workloads with no application changes; and modernize legacy proprietary databases. 
 *
 * The version of the OpenAPI document: v1alpha
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EncryptionConfig from './EncryptionConfig';
import QuantityBasedRetention from './QuantityBasedRetention';
import TimeBasedRetention from './TimeBasedRetention';
import WeeklySchedule from './WeeklySchedule';

/**
 * The AutomatedBackupPolicy model module.
 * @module model/AutomatedBackupPolicy
 * @version v1alpha
 */
class AutomatedBackupPolicy {
    /**
     * Constructs a new <code>AutomatedBackupPolicy</code>.
     * Message describing the user-specified automated backup policy. All fields in the automated backup policy are optional. Defaults for each field are provided if they are not set.
     * @alias module:model/AutomatedBackupPolicy
     */
    constructor() { 
        
        AutomatedBackupPolicy.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AutomatedBackupPolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AutomatedBackupPolicy} obj Optional instance to populate.
     * @return {module:model/AutomatedBackupPolicy} The populated <code>AutomatedBackupPolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AutomatedBackupPolicy();

            if (data.hasOwnProperty('backupWindow')) {
                obj['backupWindow'] = ApiClient.convertToType(data['backupWindow'], 'String');
            }
            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('encryptionConfig')) {
                obj['encryptionConfig'] = EncryptionConfig.constructFromObject(data['encryptionConfig']);
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('quantityBasedRetention')) {
                obj['quantityBasedRetention'] = QuantityBasedRetention.constructFromObject(data['quantityBasedRetention']);
            }
            if (data.hasOwnProperty('timeBasedRetention')) {
                obj['timeBasedRetention'] = TimeBasedRetention.constructFromObject(data['timeBasedRetention']);
            }
            if (data.hasOwnProperty('weeklySchedule')) {
                obj['weeklySchedule'] = WeeklySchedule.constructFromObject(data['weeklySchedule']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AutomatedBackupPolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AutomatedBackupPolicy</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['backupWindow'] && !(typeof data['backupWindow'] === 'string' || data['backupWindow'] instanceof String)) {
            throw new Error("Expected the field `backupWindow` to be a primitive type in the JSON string but got " + data['backupWindow']);
        }
        // validate the optional field `encryptionConfig`
        if (data['encryptionConfig']) { // data not null
          EncryptionConfig.validateJSON(data['encryptionConfig']);
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // validate the optional field `quantityBasedRetention`
        if (data['quantityBasedRetention']) { // data not null
          QuantityBasedRetention.validateJSON(data['quantityBasedRetention']);
        }
        // validate the optional field `timeBasedRetention`
        if (data['timeBasedRetention']) { // data not null
          TimeBasedRetention.validateJSON(data['timeBasedRetention']);
        }
        // validate the optional field `weeklySchedule`
        if (data['weeklySchedule']) { // data not null
          WeeklySchedule.validateJSON(data['weeklySchedule']);
        }

        return true;
    }


}



/**
 * The length of the time window during which a backup can be taken. If a backup does not succeed within this time window, it will be canceled and considered failed. The backup window must be at least 5 minutes long. There is no upper bound on the window. If not set, it defaults to 1 hour.
 * @member {String} backupWindow
 */
AutomatedBackupPolicy.prototype['backupWindow'] = undefined;

/**
 * Whether automated automated backups are enabled. If not set, defaults to true.
 * @member {Boolean} enabled
 */
AutomatedBackupPolicy.prototype['enabled'] = undefined;

/**
 * @member {module:model/EncryptionConfig} encryptionConfig
 */
AutomatedBackupPolicy.prototype['encryptionConfig'] = undefined;

/**
 * Labels to apply to backups created using this configuration.
 * @member {Object.<String, String>} labels
 */
AutomatedBackupPolicy.prototype['labels'] = undefined;

/**
 * The location where the backup will be stored. Currently, the only supported option is to store the backup in the same region as the cluster. If empty, defaults to the region of the cluster.
 * @member {String} location
 */
AutomatedBackupPolicy.prototype['location'] = undefined;

/**
 * @member {module:model/QuantityBasedRetention} quantityBasedRetention
 */
AutomatedBackupPolicy.prototype['quantityBasedRetention'] = undefined;

/**
 * @member {module:model/TimeBasedRetention} timeBasedRetention
 */
AutomatedBackupPolicy.prototype['timeBasedRetention'] = undefined;

/**
 * @member {module:model/WeeklySchedule} weeklySchedule
 */
AutomatedBackupPolicy.prototype['weeklySchedule'] = undefined;






export default AutomatedBackupPolicy;

