/**
 * Cloud Run Admin API
 * Deploy and manage user provided container images that scale automatically based on incoming requests. The Cloud Run Admin API v1 follows the Knative Serving API specification, while v2 is aligned with Google Cloud AIP-based API standards, as described in https://google.aip.dev/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Container from './Container';
import Volume from './Volume';

/**
 * The TaskSpec model module.
 * @module model/TaskSpec
 * @version v1
 */
class TaskSpec {
    /**
     * Constructs a new <code>TaskSpec</code>.
     * TaskSpec is a description of a task.
     * @alias module:model/TaskSpec
     */
    constructor() { 
        
        TaskSpec.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TaskSpec</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TaskSpec} obj Optional instance to populate.
     * @return {module:model/TaskSpec} The populated <code>TaskSpec</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TaskSpec();

            if (data.hasOwnProperty('containers')) {
                obj['containers'] = ApiClient.convertToType(data['containers'], [Container]);
            }
            if (data.hasOwnProperty('maxRetries')) {
                obj['maxRetries'] = ApiClient.convertToType(data['maxRetries'], 'Number');
            }
            if (data.hasOwnProperty('serviceAccountName')) {
                obj['serviceAccountName'] = ApiClient.convertToType(data['serviceAccountName'], 'String');
            }
            if (data.hasOwnProperty('timeoutSeconds')) {
                obj['timeoutSeconds'] = ApiClient.convertToType(data['timeoutSeconds'], 'String');
            }
            if (data.hasOwnProperty('volumes')) {
                obj['volumes'] = ApiClient.convertToType(data['volumes'], [Volume]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TaskSpec</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TaskSpec</code>.
     */
    static validateJSON(data) {
        if (data['containers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['containers'])) {
                throw new Error("Expected the field `containers` to be an array in the JSON data but got " + data['containers']);
            }
            // validate the optional field `containers` (array)
            for (const item of data['containers']) {
                Container.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['serviceAccountName'] && !(typeof data['serviceAccountName'] === 'string' || data['serviceAccountName'] instanceof String)) {
            throw new Error("Expected the field `serviceAccountName` to be a primitive type in the JSON string but got " + data['serviceAccountName']);
        }
        // ensure the json data is a string
        if (data['timeoutSeconds'] && !(typeof data['timeoutSeconds'] === 'string' || data['timeoutSeconds'] instanceof String)) {
            throw new Error("Expected the field `timeoutSeconds` to be a primitive type in the JSON string but got " + data['timeoutSeconds']);
        }
        if (data['volumes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['volumes'])) {
                throw new Error("Expected the field `volumes` to be an array in the JSON data but got " + data['volumes']);
            }
            // validate the optional field `volumes` (array)
            for (const item of data['volumes']) {
                Volume.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Optional. List of containers belonging to the task. We disallow a number of fields on this Container.
 * @member {Array.<module:model/Container>} containers
 */
TaskSpec.prototype['containers'] = undefined;

/**
 * Optional. Number of retries allowed per task, before marking this job failed. Defaults to 3.
 * @member {Number} maxRetries
 */
TaskSpec.prototype['maxRetries'] = undefined;

/**
 * Optional. Email address of the IAM service account associated with the task of a job execution. The service account represents the identity of the running task, and determines what permissions the task has. If not provided, the task will use the project's default service account.
 * @member {String} serviceAccountName
 */
TaskSpec.prototype['serviceAccountName'] = undefined;

/**
 * Optional. Duration in seconds the task may be active before the system will actively try to mark it failed and kill associated containers. This applies per attempt of a task, meaning each retry can run for the full timeout. Defaults to 600 seconds.
 * @member {String} timeoutSeconds
 */
TaskSpec.prototype['timeoutSeconds'] = undefined;

/**
 * Optional. List of volumes that can be mounted by containers belonging to the task.
 * @member {Array.<module:model/Volume>} volumes
 */
TaskSpec.prototype['volumes'] = undefined;






export default TaskSpec;

