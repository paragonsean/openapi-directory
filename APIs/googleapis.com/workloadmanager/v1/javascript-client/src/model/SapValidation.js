/**
 * Workload Manager API
 * Workload Manager is a service that provides tooling for enterprise workloads to automate the deployment and validation of your workloads against best practices and recommendations.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SapValidationValidationDetail from './SapValidationValidationDetail';

/**
 * The SapValidation model module.
 * @module model/SapValidation
 * @version v1
 */
class SapValidation {
    /**
     * Constructs a new <code>SapValidation</code>.
     * A presentation of SAP workload insight. The schema of SAP workloads validation related data.
     * @alias module:model/SapValidation
     */
    constructor() { 
        
        SapValidation.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SapValidation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SapValidation} obj Optional instance to populate.
     * @return {module:model/SapValidation} The populated <code>SapValidation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SapValidation();

            if (data.hasOwnProperty('projectId')) {
                obj['projectId'] = ApiClient.convertToType(data['projectId'], 'String');
            }
            if (data.hasOwnProperty('validationDetails')) {
                obj['validationDetails'] = ApiClient.convertToType(data['validationDetails'], [SapValidationValidationDetail]);
            }
            if (data.hasOwnProperty('zone')) {
                obj['zone'] = ApiClient.convertToType(data['zone'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SapValidation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SapValidation</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['projectId'] && !(typeof data['projectId'] === 'string' || data['projectId'] instanceof String)) {
            throw new Error("Expected the field `projectId` to be a primitive type in the JSON string but got " + data['projectId']);
        }
        if (data['validationDetails']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['validationDetails'])) {
                throw new Error("Expected the field `validationDetails` to be an array in the JSON data but got " + data['validationDetails']);
            }
            // validate the optional field `validationDetails` (array)
            for (const item of data['validationDetails']) {
                SapValidationValidationDetail.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['zone'] && !(typeof data['zone'] === 'string' || data['zone'] instanceof String)) {
            throw new Error("Expected the field `zone` to be a primitive type in the JSON string but got " + data['zone']);
        }

        return true;
    }


}



/**
 * Required. The project_id of the cloud project that the Insight data comes from.
 * @member {String} projectId
 */
SapValidation.prototype['projectId'] = undefined;

/**
 * Optional. A list of SAP validation metrics data.
 * @member {Array.<module:model/SapValidationValidationDetail>} validationDetails
 */
SapValidation.prototype['validationDetails'] = undefined;

/**
 * Optional. The zone of the instance that the Insight data comes from.
 * @member {String} zone
 */
SapValidation.prototype['zone'] = undefined;






export default SapValidation;

