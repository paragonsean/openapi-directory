/**
 * Workload Manager API
 * Workload Manager is a service that provides tooling for enterprise workloads to automate the deployment and validation of your workloads against best practices and recommendations.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ScannedResource from './ScannedResource';

/**
 * The ListScannedResourcesResponse model module.
 * @module model/ListScannedResourcesResponse
 * @version v1
 */
class ListScannedResourcesResponse {
    /**
     * Constructs a new <code>ListScannedResourcesResponse</code>.
     * Message for response to list scanned resources
     * @alias module:model/ListScannedResourcesResponse
     */
    constructor() { 
        
        ListScannedResourcesResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListScannedResourcesResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListScannedResourcesResponse} obj Optional instance to populate.
     * @return {module:model/ListScannedResourcesResponse} The populated <code>ListScannedResourcesResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListScannedResourcesResponse();

            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('scannedResources')) {
                obj['scannedResources'] = ApiClient.convertToType(data['scannedResources'], [ScannedResource]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListScannedResourcesResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListScannedResourcesResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        if (data['scannedResources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['scannedResources'])) {
                throw new Error("Expected the field `scannedResources` to be an array in the JSON data but got " + data['scannedResources']);
            }
            // validate the optional field `scannedResources` (array)
            for (const item of data['scannedResources']) {
                ScannedResource.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A token, which can be sent as `page_token` to retrieve the next page. If this field is omitted, there are no subsequent pages.
 * @member {String} nextPageToken
 */
ListScannedResourcesResponse.prototype['nextPageToken'] = undefined;

/**
 * All scanned resources in response
 * @member {Array.<module:model/ScannedResource>} scannedResources
 */
ListScannedResourcesResponse.prototype['scannedResources'] = undefined;






export default ListScannedResourcesResponse;

