/**
 * Cloud Channel API
 * The Cloud Channel API enables Google Cloud partners to have a single unified resale platform and APIs across all of Google Cloud including GCP, Workspace, Maps and Chrome.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GoogleCloudChannelV1alpha1Column from './GoogleCloudChannelV1alpha1Column';

/**
 * The GoogleCloudChannelV1alpha1Report model module.
 * @module model/GoogleCloudChannelV1alpha1Report
 * @version v1
 */
class GoogleCloudChannelV1alpha1Report {
    /**
     * Constructs a new <code>GoogleCloudChannelV1alpha1Report</code>.
     * The ID and description of a report that was used to generate report data. For example, \&quot;Google Cloud Daily Spend\&quot;, \&quot;Google Workspace License Activity\&quot;, etc.
     * @alias module:model/GoogleCloudChannelV1alpha1Report
     */
    constructor() { 
        
        GoogleCloudChannelV1alpha1Report.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoogleCloudChannelV1alpha1Report</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoogleCloudChannelV1alpha1Report} obj Optional instance to populate.
     * @return {module:model/GoogleCloudChannelV1alpha1Report} The populated <code>GoogleCloudChannelV1alpha1Report</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoogleCloudChannelV1alpha1Report();

            if (data.hasOwnProperty('columns')) {
                obj['columns'] = ApiClient.convertToType(data['columns'], [GoogleCloudChannelV1alpha1Column]);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoogleCloudChannelV1alpha1Report</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoogleCloudChannelV1alpha1Report</code>.
     */
    static validateJSON(data) {
        if (data['columns']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['columns'])) {
                throw new Error("Expected the field `columns` to be an array in the JSON data but got " + data['columns']);
            }
            // validate the optional field `columns` (array)
            for (const item of data['columns']) {
                GoogleCloudChannelV1alpha1Column.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}



/**
 * The list of columns included in the report. This defines the schema of the report results.
 * @member {Array.<module:model/GoogleCloudChannelV1alpha1Column>} columns
 */
GoogleCloudChannelV1alpha1Report.prototype['columns'] = undefined;

/**
 * A description of other aspects of the report, such as the products it supports.
 * @member {String} description
 */
GoogleCloudChannelV1alpha1Report.prototype['description'] = undefined;

/**
 * A human-readable name for this report.
 * @member {String} displayName
 */
GoogleCloudChannelV1alpha1Report.prototype['displayName'] = undefined;

/**
 * Required. The report's resource name. Specifies the account and report used to generate report data. The report_id identifier is a UID (for example, `613bf59q`). Name uses the format: accounts/{account_id}/reports/{report_id}
 * @member {String} name
 */
GoogleCloudChannelV1alpha1Report.prototype['name'] = undefined;






export default GoogleCloudChannelV1alpha1Report;

