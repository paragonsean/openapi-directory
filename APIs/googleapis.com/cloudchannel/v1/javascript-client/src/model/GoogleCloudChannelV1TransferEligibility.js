/**
 * Cloud Channel API
 * The Cloud Channel API enables Google Cloud partners to have a single unified resale platform and APIs across all of Google Cloud including GCP, Workspace, Maps and Chrome.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GoogleCloudChannelV1TransferEligibility model module.
 * @module model/GoogleCloudChannelV1TransferEligibility
 * @version v1
 */
class GoogleCloudChannelV1TransferEligibility {
    /**
     * Constructs a new <code>GoogleCloudChannelV1TransferEligibility</code>.
     * Specifies transfer eligibility of a SKU.
     * @alias module:model/GoogleCloudChannelV1TransferEligibility
     */
    constructor() { 
        
        GoogleCloudChannelV1TransferEligibility.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoogleCloudChannelV1TransferEligibility</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoogleCloudChannelV1TransferEligibility} obj Optional instance to populate.
     * @return {module:model/GoogleCloudChannelV1TransferEligibility} The populated <code>GoogleCloudChannelV1TransferEligibility</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoogleCloudChannelV1TransferEligibility();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('ineligibilityReason')) {
                obj['ineligibilityReason'] = ApiClient.convertToType(data['ineligibilityReason'], 'String');
            }
            if (data.hasOwnProperty('isEligible')) {
                obj['isEligible'] = ApiClient.convertToType(data['isEligible'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoogleCloudChannelV1TransferEligibility</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoogleCloudChannelV1TransferEligibility</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['ineligibilityReason'] && !(typeof data['ineligibilityReason'] === 'string' || data['ineligibilityReason'] instanceof String)) {
            throw new Error("Expected the field `ineligibilityReason` to be a primitive type in the JSON string but got " + data['ineligibilityReason']);
        }

        return true;
    }


}



/**
 * Localized description if reseller is not eligible to transfer the SKU.
 * @member {String} description
 */
GoogleCloudChannelV1TransferEligibility.prototype['description'] = undefined;

/**
 * Specified the reason for ineligibility.
 * @member {module:model/GoogleCloudChannelV1TransferEligibility.IneligibilityReasonEnum} ineligibilityReason
 */
GoogleCloudChannelV1TransferEligibility.prototype['ineligibilityReason'] = undefined;

/**
 * Whether reseller is eligible to transfer the SKU.
 * @member {Boolean} isEligible
 */
GoogleCloudChannelV1TransferEligibility.prototype['isEligible'] = undefined;





/**
 * Allowed values for the <code>ineligibilityReason</code> property.
 * @enum {String}
 * @readonly
 */
GoogleCloudChannelV1TransferEligibility['IneligibilityReasonEnum'] = {

    /**
     * value: "REASON_UNSPECIFIED"
     * @const
     */
    "REASON_UNSPECIFIED": "REASON_UNSPECIFIED",

    /**
     * value: "PENDING_TOS_ACCEPTANCE"
     * @const
     */
    "PENDING_TOS_ACCEPTANCE": "PENDING_TOS_ACCEPTANCE",

    /**
     * value: "SKU_NOT_ELIGIBLE"
     * @const
     */
    "SKU_NOT_ELIGIBLE": "SKU_NOT_ELIGIBLE",

    /**
     * value: "SKU_SUSPENDED"
     * @const
     */
    "SKU_SUSPENDED": "SKU_SUSPENDED",

    /**
     * value: "CHANNEL_PARTNER_NOT_AUTHORIZED_FOR_SKU"
     * @const
     */
    "CHANNEL_PARTNER_NOT_AUTHORIZED_FOR_SKU": "CHANNEL_PARTNER_NOT_AUTHORIZED_FOR_SKU"
};



export default GoogleCloudChannelV1TransferEligibility;

