/**
 * Cloud Channel API
 * The Cloud Channel API enables Google Cloud partners to have a single unified resale platform and APIs across all of Google Cloud including GCP, Workspace, Maps and Chrome.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GoogleCloudChannelV1Price from './GoogleCloudChannelV1Price';
import GoogleCloudChannelV1PriceTier from './GoogleCloudChannelV1PriceTier';

/**
 * The GoogleCloudChannelV1PricePhase model module.
 * @module model/GoogleCloudChannelV1PricePhase
 * @version v1
 */
class GoogleCloudChannelV1PricePhase {
    /**
     * Constructs a new <code>GoogleCloudChannelV1PricePhase</code>.
     * Specifies the price by the duration of months. For example, a 20% discount for the first six months, then a 10% discount starting on the seventh month.
     * @alias module:model/GoogleCloudChannelV1PricePhase
     */
    constructor() { 
        
        GoogleCloudChannelV1PricePhase.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoogleCloudChannelV1PricePhase</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoogleCloudChannelV1PricePhase} obj Optional instance to populate.
     * @return {module:model/GoogleCloudChannelV1PricePhase} The populated <code>GoogleCloudChannelV1PricePhase</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoogleCloudChannelV1PricePhase();

            if (data.hasOwnProperty('firstPeriod')) {
                obj['firstPeriod'] = ApiClient.convertToType(data['firstPeriod'], 'Number');
            }
            if (data.hasOwnProperty('lastPeriod')) {
                obj['lastPeriod'] = ApiClient.convertToType(data['lastPeriod'], 'Number');
            }
            if (data.hasOwnProperty('periodType')) {
                obj['periodType'] = ApiClient.convertToType(data['periodType'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = GoogleCloudChannelV1Price.constructFromObject(data['price']);
            }
            if (data.hasOwnProperty('priceTiers')) {
                obj['priceTiers'] = ApiClient.convertToType(data['priceTiers'], [GoogleCloudChannelV1PriceTier]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoogleCloudChannelV1PricePhase</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoogleCloudChannelV1PricePhase</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['periodType'] && !(typeof data['periodType'] === 'string' || data['periodType'] instanceof String)) {
            throw new Error("Expected the field `periodType` to be a primitive type in the JSON string but got " + data['periodType']);
        }
        // validate the optional field `price`
        if (data['price']) { // data not null
          GoogleCloudChannelV1Price.validateJSON(data['price']);
        }
        if (data['priceTiers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['priceTiers'])) {
                throw new Error("Expected the field `priceTiers` to be an array in the JSON data but got " + data['priceTiers']);
            }
            // validate the optional field `priceTiers` (array)
            for (const item of data['priceTiers']) {
                GoogleCloudChannelV1PriceTier.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Defines first period for the phase.
 * @member {Number} firstPeriod
 */
GoogleCloudChannelV1PricePhase.prototype['firstPeriod'] = undefined;

/**
 * Defines first period for the phase.
 * @member {Number} lastPeriod
 */
GoogleCloudChannelV1PricePhase.prototype['lastPeriod'] = undefined;

/**
 * Defines the phase period type.
 * @member {module:model/GoogleCloudChannelV1PricePhase.PeriodTypeEnum} periodType
 */
GoogleCloudChannelV1PricePhase.prototype['periodType'] = undefined;

/**
 * @member {module:model/GoogleCloudChannelV1Price} price
 */
GoogleCloudChannelV1PricePhase.prototype['price'] = undefined;

/**
 * Price by the resource tiers.
 * @member {Array.<module:model/GoogleCloudChannelV1PriceTier>} priceTiers
 */
GoogleCloudChannelV1PricePhase.prototype['priceTiers'] = undefined;





/**
 * Allowed values for the <code>periodType</code> property.
 * @enum {String}
 * @readonly
 */
GoogleCloudChannelV1PricePhase['PeriodTypeEnum'] = {

    /**
     * value: "PERIOD_TYPE_UNSPECIFIED"
     * @const
     */
    "PERIOD_TYPE_UNSPECIFIED": "PERIOD_TYPE_UNSPECIFIED",

    /**
     * value: "DAY"
     * @const
     */
    "DAY": "DAY",

    /**
     * value: "MONTH"
     * @const
     */
    "MONTH": "MONTH",

    /**
     * value: "YEAR"
     * @const
     */
    "YEAR": "YEAR"
};



export default GoogleCloudChannelV1PricePhase;

