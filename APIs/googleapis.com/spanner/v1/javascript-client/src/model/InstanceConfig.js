/**
 * Cloud Spanner API
 * Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReplicaInfo from './ReplicaInfo';

/**
 * The InstanceConfig model module.
 * @module model/InstanceConfig
 * @version v1
 */
class InstanceConfig {
    /**
     * Constructs a new <code>InstanceConfig</code>.
     * A possible configuration for a Cloud Spanner instance. Configurations define the geographic placement of nodes and their replication.
     * @alias module:model/InstanceConfig
     */
    constructor() { 
        
        InstanceConfig.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InstanceConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InstanceConfig} obj Optional instance to populate.
     * @return {module:model/InstanceConfig} The populated <code>InstanceConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InstanceConfig();

            if (data.hasOwnProperty('baseConfig')) {
                obj['baseConfig'] = ApiClient.convertToType(data['baseConfig'], 'String');
            }
            if (data.hasOwnProperty('configType')) {
                obj['configType'] = ApiClient.convertToType(data['configType'], 'String');
            }
            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('etag')) {
                obj['etag'] = ApiClient.convertToType(data['etag'], 'String');
            }
            if (data.hasOwnProperty('freeInstanceAvailability')) {
                obj['freeInstanceAvailability'] = ApiClient.convertToType(data['freeInstanceAvailability'], 'String');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('leaderOptions')) {
                obj['leaderOptions'] = ApiClient.convertToType(data['leaderOptions'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('optionalReplicas')) {
                obj['optionalReplicas'] = ApiClient.convertToType(data['optionalReplicas'], [ReplicaInfo]);
            }
            if (data.hasOwnProperty('reconciling')) {
                obj['reconciling'] = ApiClient.convertToType(data['reconciling'], 'Boolean');
            }
            if (data.hasOwnProperty('replicas')) {
                obj['replicas'] = ApiClient.convertToType(data['replicas'], [ReplicaInfo]);
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('storageLimitPerProcessingUnit')) {
                obj['storageLimitPerProcessingUnit'] = ApiClient.convertToType(data['storageLimitPerProcessingUnit'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InstanceConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InstanceConfig</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['baseConfig'] && !(typeof data['baseConfig'] === 'string' || data['baseConfig'] instanceof String)) {
            throw new Error("Expected the field `baseConfig` to be a primitive type in the JSON string but got " + data['baseConfig']);
        }
        // ensure the json data is a string
        if (data['configType'] && !(typeof data['configType'] === 'string' || data['configType'] instanceof String)) {
            throw new Error("Expected the field `configType` to be a primitive type in the JSON string but got " + data['configType']);
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['etag'] && !(typeof data['etag'] === 'string' || data['etag'] instanceof String)) {
            throw new Error("Expected the field `etag` to be a primitive type in the JSON string but got " + data['etag']);
        }
        // ensure the json data is a string
        if (data['freeInstanceAvailability'] && !(typeof data['freeInstanceAvailability'] === 'string' || data['freeInstanceAvailability'] instanceof String)) {
            throw new Error("Expected the field `freeInstanceAvailability` to be a primitive type in the JSON string but got " + data['freeInstanceAvailability']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['leaderOptions'])) {
            throw new Error("Expected the field `leaderOptions` to be an array in the JSON data but got " + data['leaderOptions']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['optionalReplicas']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['optionalReplicas'])) {
                throw new Error("Expected the field `optionalReplicas` to be an array in the JSON data but got " + data['optionalReplicas']);
            }
            // validate the optional field `optionalReplicas` (array)
            for (const item of data['optionalReplicas']) {
                ReplicaInfo.validateJSON(item);
            };
        }
        if (data['replicas']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['replicas'])) {
                throw new Error("Expected the field `replicas` to be an array in the JSON data but got " + data['replicas']);
            }
            // validate the optional field `replicas` (array)
            for (const item of data['replicas']) {
                ReplicaInfo.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['storageLimitPerProcessingUnit'] && !(typeof data['storageLimitPerProcessingUnit'] === 'string' || data['storageLimitPerProcessingUnit'] instanceof String)) {
            throw new Error("Expected the field `storageLimitPerProcessingUnit` to be a primitive type in the JSON string but got " + data['storageLimitPerProcessingUnit']);
        }

        return true;
    }


}



/**
 * Base configuration name, e.g. projects//instanceConfigs/nam3, based on which this configuration is created. Only set for user managed configurations. `base_config` must refer to a configuration of type GOOGLE_MANAGED in the same project as this configuration.
 * @member {String} baseConfig
 */
InstanceConfig.prototype['baseConfig'] = undefined;

/**
 * Output only. Whether this instance config is a Google or User Managed Configuration.
 * @member {module:model/InstanceConfig.ConfigTypeEnum} configType
 */
InstanceConfig.prototype['configType'] = undefined;

/**
 * The name of this instance configuration as it appears in UIs.
 * @member {String} displayName
 */
InstanceConfig.prototype['displayName'] = undefined;

/**
 * etag is used for optimistic concurrency control as a way to help prevent simultaneous updates of a instance config from overwriting each other. It is strongly suggested that systems make use of the etag in the read-modify-write cycle to perform instance config updates in order to avoid race conditions: An etag is returned in the response which contains instance configs, and systems are expected to put that etag in the request to update instance config to ensure that their change will be applied to the same version of the instance config. If no etag is provided in the call to update instance config, then the existing instance config is overwritten blindly.
 * @member {String} etag
 */
InstanceConfig.prototype['etag'] = undefined;

/**
 * Output only. Describes whether free instances are available to be created in this instance config.
 * @member {module:model/InstanceConfig.FreeInstanceAvailabilityEnum} freeInstanceAvailability
 */
InstanceConfig.prototype['freeInstanceAvailability'] = undefined;

/**
 * Cloud Labels are a flexible and lightweight mechanism for organizing cloud resources into groups that reflect a customer's organizational needs and deployment strategies. Cloud Labels can be used to filter collections of resources. They can be used to control how resource metrics are aggregated. And they can be used as arguments to policy management rules (e.g. route, firewall, load balancing, etc.). * Label keys must be between 1 and 63 characters long and must conform to the following regular expression: `a-z{0,62}`. * Label values must be between 0 and 63 characters long and must conform to the regular expression `[a-z0-9_-]{0,63}`. * No more than 64 labels can be associated with a given resource. See https://goo.gl/xmQnxf for more information on and examples of labels. If you plan to use labels in your own code, please note that additional characters may be allowed in the future. Therefore, you are advised to use an internal label representation, such as JSON, which doesn't rely upon specific characters being disallowed. For example, representing labels as the string: name + \"_\" + value would prove problematic if we were to allow \"_\" in a future release.
 * @member {Object.<String, String>} labels
 */
InstanceConfig.prototype['labels'] = undefined;

/**
 * Allowed values of the \"default_leader\" schema option for databases in instances that use this instance configuration.
 * @member {Array.<String>} leaderOptions
 */
InstanceConfig.prototype['leaderOptions'] = undefined;

/**
 * A unique identifier for the instance configuration. Values are of the form `projects//instanceConfigs/a-z*`.
 * @member {String} name
 */
InstanceConfig.prototype['name'] = undefined;

/**
 * Output only. The available optional replicas to choose from for user managed configurations. Populated for Google managed configurations.
 * @member {Array.<module:model/ReplicaInfo>} optionalReplicas
 */
InstanceConfig.prototype['optionalReplicas'] = undefined;

/**
 * Output only. If true, the instance config is being created or updated. If false, there are no ongoing operations for the instance config.
 * @member {Boolean} reconciling
 */
InstanceConfig.prototype['reconciling'] = undefined;

/**
 * The geographic placement of nodes in this instance configuration and their replication properties.
 * @member {Array.<module:model/ReplicaInfo>} replicas
 */
InstanceConfig.prototype['replicas'] = undefined;

/**
 * Output only. The current instance config state. Applicable only for USER_MANAGED configs.
 * @member {module:model/InstanceConfig.StateEnum} state
 */
InstanceConfig.prototype['state'] = undefined;

/**
 * Output only. The storage limit in bytes per processing unit.
 * @member {String} storageLimitPerProcessingUnit
 */
InstanceConfig.prototype['storageLimitPerProcessingUnit'] = undefined;





/**
 * Allowed values for the <code>configType</code> property.
 * @enum {String}
 * @readonly
 */
InstanceConfig['ConfigTypeEnum'] = {

    /**
     * value: "TYPE_UNSPECIFIED"
     * @const
     */
    "TYPE_UNSPECIFIED": "TYPE_UNSPECIFIED",

    /**
     * value: "GOOGLE_MANAGED"
     * @const
     */
    "GOOGLE_MANAGED": "GOOGLE_MANAGED",

    /**
     * value: "USER_MANAGED"
     * @const
     */
    "USER_MANAGED": "USER_MANAGED"
};


/**
 * Allowed values for the <code>freeInstanceAvailability</code> property.
 * @enum {String}
 * @readonly
 */
InstanceConfig['FreeInstanceAvailabilityEnum'] = {

    /**
     * value: "FREE_INSTANCE_AVAILABILITY_UNSPECIFIED"
     * @const
     */
    "FREE_INSTANCE_AVAILABILITY_UNSPECIFIED": "FREE_INSTANCE_AVAILABILITY_UNSPECIFIED",

    /**
     * value: "AVAILABLE"
     * @const
     */
    "AVAILABLE": "AVAILABLE",

    /**
     * value: "UNSUPPORTED"
     * @const
     */
    "UNSUPPORTED": "UNSUPPORTED",

    /**
     * value: "DISABLED"
     * @const
     */
    "DISABLED": "DISABLED",

    /**
     * value: "QUOTA_EXCEEDED"
     * @const
     */
    "QUOTA_EXCEEDED": "QUOTA_EXCEEDED"
};


/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
InstanceConfig['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "CREATING"
     * @const
     */
    "CREATING": "CREATING",

    /**
     * value: "READY"
     * @const
     */
    "READY": "READY"
};



export default InstanceConfig;

