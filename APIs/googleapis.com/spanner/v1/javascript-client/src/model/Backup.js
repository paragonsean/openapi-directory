/**
 * Cloud Spanner API
 * Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EncryptionInfo from './EncryptionInfo';

/**
 * The Backup model module.
 * @module model/Backup
 * @version v1
 */
class Backup {
    /**
     * Constructs a new <code>Backup</code>.
     * A backup of a Cloud Spanner database.
     * @alias module:model/Backup
     */
    constructor() { 
        
        Backup.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Backup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Backup} obj Optional instance to populate.
     * @return {module:model/Backup} The populated <code>Backup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Backup();

            if (data.hasOwnProperty('createTime')) {
                obj['createTime'] = ApiClient.convertToType(data['createTime'], 'String');
            }
            if (data.hasOwnProperty('database')) {
                obj['database'] = ApiClient.convertToType(data['database'], 'String');
            }
            if (data.hasOwnProperty('databaseDialect')) {
                obj['databaseDialect'] = ApiClient.convertToType(data['databaseDialect'], 'String');
            }
            if (data.hasOwnProperty('encryptionInfo')) {
                obj['encryptionInfo'] = EncryptionInfo.constructFromObject(data['encryptionInfo']);
            }
            if (data.hasOwnProperty('expireTime')) {
                obj['expireTime'] = ApiClient.convertToType(data['expireTime'], 'String');
            }
            if (data.hasOwnProperty('maxExpireTime')) {
                obj['maxExpireTime'] = ApiClient.convertToType(data['maxExpireTime'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('referencingBackups')) {
                obj['referencingBackups'] = ApiClient.convertToType(data['referencingBackups'], ['String']);
            }
            if (data.hasOwnProperty('referencingDatabases')) {
                obj['referencingDatabases'] = ApiClient.convertToType(data['referencingDatabases'], ['String']);
            }
            if (data.hasOwnProperty('sizeBytes')) {
                obj['sizeBytes'] = ApiClient.convertToType(data['sizeBytes'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('versionTime')) {
                obj['versionTime'] = ApiClient.convertToType(data['versionTime'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Backup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Backup</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['createTime'] && !(typeof data['createTime'] === 'string' || data['createTime'] instanceof String)) {
            throw new Error("Expected the field `createTime` to be a primitive type in the JSON string but got " + data['createTime']);
        }
        // ensure the json data is a string
        if (data['database'] && !(typeof data['database'] === 'string' || data['database'] instanceof String)) {
            throw new Error("Expected the field `database` to be a primitive type in the JSON string but got " + data['database']);
        }
        // ensure the json data is a string
        if (data['databaseDialect'] && !(typeof data['databaseDialect'] === 'string' || data['databaseDialect'] instanceof String)) {
            throw new Error("Expected the field `databaseDialect` to be a primitive type in the JSON string but got " + data['databaseDialect']);
        }
        // validate the optional field `encryptionInfo`
        if (data['encryptionInfo']) { // data not null
          EncryptionInfo.validateJSON(data['encryptionInfo']);
        }
        // ensure the json data is a string
        if (data['expireTime'] && !(typeof data['expireTime'] === 'string' || data['expireTime'] instanceof String)) {
            throw new Error("Expected the field `expireTime` to be a primitive type in the JSON string but got " + data['expireTime']);
        }
        // ensure the json data is a string
        if (data['maxExpireTime'] && !(typeof data['maxExpireTime'] === 'string' || data['maxExpireTime'] instanceof String)) {
            throw new Error("Expected the field `maxExpireTime` to be a primitive type in the JSON string but got " + data['maxExpireTime']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['referencingBackups'])) {
            throw new Error("Expected the field `referencingBackups` to be an array in the JSON data but got " + data['referencingBackups']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['referencingDatabases'])) {
            throw new Error("Expected the field `referencingDatabases` to be an array in the JSON data but got " + data['referencingDatabases']);
        }
        // ensure the json data is a string
        if (data['sizeBytes'] && !(typeof data['sizeBytes'] === 'string' || data['sizeBytes'] instanceof String)) {
            throw new Error("Expected the field `sizeBytes` to be a primitive type in the JSON string but got " + data['sizeBytes']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['versionTime'] && !(typeof data['versionTime'] === 'string' || data['versionTime'] instanceof String)) {
            throw new Error("Expected the field `versionTime` to be a primitive type in the JSON string but got " + data['versionTime']);
        }

        return true;
    }


}



/**
 * Output only. The time the CreateBackup request is received. If the request does not specify `version_time`, the `version_time` of the backup will be equivalent to the `create_time`.
 * @member {String} createTime
 */
Backup.prototype['createTime'] = undefined;

/**
 * Required for the CreateBackup operation. Name of the database from which this backup was created. This needs to be in the same instance as the backup. Values are of the form `projects//instances//databases/`.
 * @member {String} database
 */
Backup.prototype['database'] = undefined;

/**
 * Output only. The database dialect information for the backup.
 * @member {module:model/Backup.DatabaseDialectEnum} databaseDialect
 */
Backup.prototype['databaseDialect'] = undefined;

/**
 * @member {module:model/EncryptionInfo} encryptionInfo
 */
Backup.prototype['encryptionInfo'] = undefined;

/**
 * Required for the CreateBackup operation. The expiration time of the backup, with microseconds granularity that must be at least 6 hours and at most 366 days from the time the CreateBackup request is processed. Once the `expire_time` has passed, the backup is eligible to be automatically deleted by Cloud Spanner to free the resources used by the backup.
 * @member {String} expireTime
 */
Backup.prototype['expireTime'] = undefined;

/**
 * Output only. The max allowed expiration time of the backup, with microseconds granularity. A backup's expiration time can be configured in multiple APIs: CreateBackup, UpdateBackup, CopyBackup. When updating or copying an existing backup, the expiration time specified must be less than `Backup.max_expire_time`.
 * @member {String} maxExpireTime
 */
Backup.prototype['maxExpireTime'] = undefined;

/**
 * Output only for the CreateBackup operation. Required for the UpdateBackup operation. A globally unique identifier for the backup which cannot be changed. Values are of the form `projects//instances//backups/a-z*[a-z0-9]` The final segment of the name must be between 2 and 60 characters in length. The backup is stored in the location(s) specified in the instance configuration of the instance containing the backup, identified by the prefix of the backup name of the form `projects//instances/`.
 * @member {String} name
 */
Backup.prototype['name'] = undefined;

/**
 * Output only. The names of the destination backups being created by copying this source backup. The backup names are of the form `projects//instances//backups/`. Referencing backups may exist in different instances. The existence of any referencing backup prevents the backup from being deleted. When the copy operation is done (either successfully completed or cancelled or the destination backup is deleted), the reference to the backup is removed.
 * @member {Array.<String>} referencingBackups
 */
Backup.prototype['referencingBackups'] = undefined;

/**
 * Output only. The names of the restored databases that reference the backup. The database names are of the form `projects//instances//databases/`. Referencing databases may exist in different instances. The existence of any referencing database prevents the backup from being deleted. When a restored database from the backup enters the `READY` state, the reference to the backup is removed.
 * @member {Array.<String>} referencingDatabases
 */
Backup.prototype['referencingDatabases'] = undefined;

/**
 * Output only. Size of the backup in bytes.
 * @member {String} sizeBytes
 */
Backup.prototype['sizeBytes'] = undefined;

/**
 * Output only. The current state of the backup.
 * @member {module:model/Backup.StateEnum} state
 */
Backup.prototype['state'] = undefined;

/**
 * The backup will contain an externally consistent copy of the database at the timestamp specified by `version_time`. If `version_time` is not specified, the system will set `version_time` to the `create_time` of the backup.
 * @member {String} versionTime
 */
Backup.prototype['versionTime'] = undefined;





/**
 * Allowed values for the <code>databaseDialect</code> property.
 * @enum {String}
 * @readonly
 */
Backup['DatabaseDialectEnum'] = {

    /**
     * value: "DATABASE_DIALECT_UNSPECIFIED"
     * @const
     */
    "DATABASE_DIALECT_UNSPECIFIED": "DATABASE_DIALECT_UNSPECIFIED",

    /**
     * value: "GOOGLE_STANDARD_SQL"
     * @const
     */
    "GOOGLE_STANDARD_SQL": "GOOGLE_STANDARD_SQL",

    /**
     * value: "POSTGRESQL"
     * @const
     */
    "POSTGRESQL": "POSTGRESQL"
};


/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
Backup['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "CREATING"
     * @const
     */
    "CREATING": "CREATING",

    /**
     * value: "READY"
     * @const
     */
    "READY": "READY"
};



export default Backup;

