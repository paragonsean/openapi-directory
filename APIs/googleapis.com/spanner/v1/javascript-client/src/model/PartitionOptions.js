/**
 * Cloud Spanner API
 * Cloud Spanner is a managed, mission-critical, globally consistent and scalable relational database service.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PartitionOptions model module.
 * @module model/PartitionOptions
 * @version v1
 */
class PartitionOptions {
    /**
     * Constructs a new <code>PartitionOptions</code>.
     * Options for a PartitionQueryRequest and PartitionReadRequest.
     * @alias module:model/PartitionOptions
     */
    constructor() { 
        
        PartitionOptions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PartitionOptions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PartitionOptions} obj Optional instance to populate.
     * @return {module:model/PartitionOptions} The populated <code>PartitionOptions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PartitionOptions();

            if (data.hasOwnProperty('maxPartitions')) {
                obj['maxPartitions'] = ApiClient.convertToType(data['maxPartitions'], 'String');
            }
            if (data.hasOwnProperty('partitionSizeBytes')) {
                obj['partitionSizeBytes'] = ApiClient.convertToType(data['partitionSizeBytes'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PartitionOptions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PartitionOptions</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['maxPartitions'] && !(typeof data['maxPartitions'] === 'string' || data['maxPartitions'] instanceof String)) {
            throw new Error("Expected the field `maxPartitions` to be a primitive type in the JSON string but got " + data['maxPartitions']);
        }
        // ensure the json data is a string
        if (data['partitionSizeBytes'] && !(typeof data['partitionSizeBytes'] === 'string' || data['partitionSizeBytes'] instanceof String)) {
            throw new Error("Expected the field `partitionSizeBytes` to be a primitive type in the JSON string but got " + data['partitionSizeBytes']);
        }

        return true;
    }


}



/**
 * **Note:** This hint is currently ignored by PartitionQuery and PartitionRead requests. The desired maximum number of partitions to return. For example, this may be set to the number of workers available. The default for this option is currently 10,000. The maximum value is currently 200,000. This is only a hint. The actual number of partitions returned may be smaller or larger than this maximum count request.
 * @member {String} maxPartitions
 */
PartitionOptions.prototype['maxPartitions'] = undefined;

/**
 * **Note:** This hint is currently ignored by PartitionQuery and PartitionRead requests. The desired data size for each partition generated. The default for this option is currently 1 GiB. This is only a hint. The actual size of each partition may be smaller or larger than this size request.
 * @member {String} partitionSizeBytes
 */
PartitionOptions.prototype['partitionSizeBytes'] = undefined;






export default PartitionOptions;

