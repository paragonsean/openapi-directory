/**
 * Service Usage API
 * Enables services that service consumers want to use on Google Cloud Platform, lists the available or enabled services, or disables services that service consumers no longer use.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Field from './Field';
import Option from './Option';
import SourceContext from './SourceContext';

/**
 * The Type model module.
 * @module model/Type
 * @version v1beta1
 */
class Type {
    /**
     * Constructs a new <code>Type</code>.
     * A protocol buffer message type.
     * @alias module:model/Type
     */
    constructor() { 
        
        Type.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Type</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Type} obj Optional instance to populate.
     * @return {module:model/Type} The populated <code>Type</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Type();

            if (data.hasOwnProperty('edition')) {
                obj['edition'] = ApiClient.convertToType(data['edition'], 'String');
            }
            if (data.hasOwnProperty('fields')) {
                obj['fields'] = ApiClient.convertToType(data['fields'], [Field]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('oneofs')) {
                obj['oneofs'] = ApiClient.convertToType(data['oneofs'], ['String']);
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], [Option]);
            }
            if (data.hasOwnProperty('sourceContext')) {
                obj['sourceContext'] = SourceContext.constructFromObject(data['sourceContext']);
            }
            if (data.hasOwnProperty('syntax')) {
                obj['syntax'] = ApiClient.convertToType(data['syntax'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Type</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Type</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['edition'] && !(typeof data['edition'] === 'string' || data['edition'] instanceof String)) {
            throw new Error("Expected the field `edition` to be a primitive type in the JSON string but got " + data['edition']);
        }
        if (data['fields']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['fields'])) {
                throw new Error("Expected the field `fields` to be an array in the JSON data but got " + data['fields']);
            }
            // validate the optional field `fields` (array)
            for (const item of data['fields']) {
                Field.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['oneofs'])) {
            throw new Error("Expected the field `oneofs` to be an array in the JSON data but got " + data['oneofs']);
        }
        if (data['options']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['options'])) {
                throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
            }
            // validate the optional field `options` (array)
            for (const item of data['options']) {
                Option.validateJSON(item);
            };
        }
        // validate the optional field `sourceContext`
        if (data['sourceContext']) { // data not null
          SourceContext.validateJSON(data['sourceContext']);
        }
        // ensure the json data is a string
        if (data['syntax'] && !(typeof data['syntax'] === 'string' || data['syntax'] instanceof String)) {
            throw new Error("Expected the field `syntax` to be a primitive type in the JSON string but got " + data['syntax']);
        }

        return true;
    }


}



/**
 * The source edition string, only valid when syntax is SYNTAX_EDITIONS.
 * @member {String} edition
 */
Type.prototype['edition'] = undefined;

/**
 * The list of fields.
 * @member {Array.<module:model/Field>} fields
 */
Type.prototype['fields'] = undefined;

/**
 * The fully qualified message name.
 * @member {String} name
 */
Type.prototype['name'] = undefined;

/**
 * The list of types appearing in `oneof` definitions in this type.
 * @member {Array.<String>} oneofs
 */
Type.prototype['oneofs'] = undefined;

/**
 * The protocol buffer options.
 * @member {Array.<module:model/Option>} options
 */
Type.prototype['options'] = undefined;

/**
 * @member {module:model/SourceContext} sourceContext
 */
Type.prototype['sourceContext'] = undefined;

/**
 * The source syntax.
 * @member {module:model/Type.SyntaxEnum} syntax
 */
Type.prototype['syntax'] = undefined;





/**
 * Allowed values for the <code>syntax</code> property.
 * @enum {String}
 * @readonly
 */
Type['SyntaxEnum'] = {

    /**
     * value: "SYNTAX_PROTO2"
     * @const
     */
    "PROTO2": "SYNTAX_PROTO2",

    /**
     * value: "SYNTAX_PROTO3"
     * @const
     */
    "PROTO3": "SYNTAX_PROTO3",

    /**
     * value: "SYNTAX_EDITIONS"
     * @const
     */
    "EDITIONS": "SYNTAX_EDITIONS"
};



export default Type;

