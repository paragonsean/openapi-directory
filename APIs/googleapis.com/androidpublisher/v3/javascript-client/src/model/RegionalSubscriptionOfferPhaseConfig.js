/**
 * Google Play Android Developer API
 * Lets Android application developers access their Google Play accounts. At a high level, the expected workflow is to \"insert\" an Edit, make changes as necessary, and then \"commit\" it. 
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Money from './Money';

/**
 * The RegionalSubscriptionOfferPhaseConfig model module.
 * @module model/RegionalSubscriptionOfferPhaseConfig
 * @version v3
 */
class RegionalSubscriptionOfferPhaseConfig {
    /**
     * Constructs a new <code>RegionalSubscriptionOfferPhaseConfig</code>.
     * Configuration for a single phase of a subscription offer in a single region.
     * @alias module:model/RegionalSubscriptionOfferPhaseConfig
     */
    constructor() { 
        
        RegionalSubscriptionOfferPhaseConfig.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RegionalSubscriptionOfferPhaseConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RegionalSubscriptionOfferPhaseConfig} obj Optional instance to populate.
     * @return {module:model/RegionalSubscriptionOfferPhaseConfig} The populated <code>RegionalSubscriptionOfferPhaseConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RegionalSubscriptionOfferPhaseConfig();

            if (data.hasOwnProperty('absoluteDiscount')) {
                obj['absoluteDiscount'] = Money.constructFromObject(data['absoluteDiscount']);
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = Money.constructFromObject(data['price']);
            }
            if (data.hasOwnProperty('regionCode')) {
                obj['regionCode'] = ApiClient.convertToType(data['regionCode'], 'String');
            }
            if (data.hasOwnProperty('relativeDiscount')) {
                obj['relativeDiscount'] = ApiClient.convertToType(data['relativeDiscount'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RegionalSubscriptionOfferPhaseConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RegionalSubscriptionOfferPhaseConfig</code>.
     */
    static validateJSON(data) {
        // validate the optional field `absoluteDiscount`
        if (data['absoluteDiscount']) { // data not null
          Money.validateJSON(data['absoluteDiscount']);
        }
        // validate the optional field `price`
        if (data['price']) { // data not null
          Money.validateJSON(data['price']);
        }
        // ensure the json data is a string
        if (data['regionCode'] && !(typeof data['regionCode'] === 'string' || data['regionCode'] instanceof String)) {
            throw new Error("Expected the field `regionCode` to be a primitive type in the JSON string but got " + data['regionCode']);
        }

        return true;
    }


}



/**
 * @member {module:model/Money} absoluteDiscount
 */
RegionalSubscriptionOfferPhaseConfig.prototype['absoluteDiscount'] = undefined;

/**
 * @member {module:model/Money} price
 */
RegionalSubscriptionOfferPhaseConfig.prototype['price'] = undefined;

/**
 * Required. Immutable. The region to which this config applies.
 * @member {String} regionCode
 */
RegionalSubscriptionOfferPhaseConfig.prototype['regionCode'] = undefined;

/**
 * The fraction of the base plan price prorated over the phase duration that the user pays for this offer phase. For example, if the base plan price for this region is $12 for a period of 1 year, then a 50% discount for a phase of a duration of 3 months would correspond to a price of $1.50. The discount must be specified as a fraction strictly larger than 0 and strictly smaller than 1. The resulting price will be rounded to the nearest billable unit (e.g. cents for USD). The relative discount is considered invalid if the discounted price ends up being smaller than the minimum price allowed in this region.
 * @member {Number} relativeDiscount
 */
RegionalSubscriptionOfferPhaseConfig.prototype['relativeDiscount'] = undefined;






export default RegionalSubscriptionOfferPhaseConfig;

