/**
 * Dataform API
 * Service to develop, version control, and operationalize SQL pipelines in BigQuery.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CommitAuthor from './CommitAuthor';

/**
 * The CommitLogEntry model module.
 * @module model/CommitLogEntry
 * @version v1beta1
 */
class CommitLogEntry {
    /**
     * Constructs a new <code>CommitLogEntry</code>.
     * Represents a single commit log.
     * @alias module:model/CommitLogEntry
     */
    constructor() { 
        
        CommitLogEntry.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CommitLogEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CommitLogEntry} obj Optional instance to populate.
     * @return {module:model/CommitLogEntry} The populated <code>CommitLogEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CommitLogEntry();

            if (data.hasOwnProperty('author')) {
                obj['author'] = CommitAuthor.constructFromObject(data['author']);
            }
            if (data.hasOwnProperty('commitMessage')) {
                obj['commitMessage'] = ApiClient.convertToType(data['commitMessage'], 'String');
            }
            if (data.hasOwnProperty('commitSha')) {
                obj['commitSha'] = ApiClient.convertToType(data['commitSha'], 'String');
            }
            if (data.hasOwnProperty('commitTime')) {
                obj['commitTime'] = ApiClient.convertToType(data['commitTime'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CommitLogEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CommitLogEntry</code>.
     */
    static validateJSON(data) {
        // validate the optional field `author`
        if (data['author']) { // data not null
          CommitAuthor.validateJSON(data['author']);
        }
        // ensure the json data is a string
        if (data['commitMessage'] && !(typeof data['commitMessage'] === 'string' || data['commitMessage'] instanceof String)) {
            throw new Error("Expected the field `commitMessage` to be a primitive type in the JSON string but got " + data['commitMessage']);
        }
        // ensure the json data is a string
        if (data['commitSha'] && !(typeof data['commitSha'] === 'string' || data['commitSha'] instanceof String)) {
            throw new Error("Expected the field `commitSha` to be a primitive type in the JSON string but got " + data['commitSha']);
        }
        // ensure the json data is a string
        if (data['commitTime'] && !(typeof data['commitTime'] === 'string' || data['commitTime'] instanceof String)) {
            throw new Error("Expected the field `commitTime` to be a primitive type in the JSON string but got " + data['commitTime']);
        }

        return true;
    }


}



/**
 * @member {module:model/CommitAuthor} author
 */
CommitLogEntry.prototype['author'] = undefined;

/**
 * The commit message for this commit log entry.
 * @member {String} commitMessage
 */
CommitLogEntry.prototype['commitMessage'] = undefined;

/**
 * The commit SHA for this commit log entry.
 * @member {String} commitSha
 */
CommitLogEntry.prototype['commitSha'] = undefined;

/**
 * Commit timestamp.
 * @member {String} commitTime
 */
CommitLogEntry.prototype['commitTime'] = undefined;






export default CommitLogEntry;

