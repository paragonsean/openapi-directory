/**
 * Dataform API
 * Service to develop, version control, and operationalize SQL pipelines in BigQuery.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RelationDescriptor from './RelationDescriptor';
import Target from './Target';

/**
 * The Operations model module.
 * @module model/Operations
 * @version v1beta1
 */
class Operations {
    /**
     * Constructs a new <code>Operations</code>.
     * Represents a list of arbitrary database operations.
     * @alias module:model/Operations
     */
    constructor() { 
        
        Operations.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Operations</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Operations} obj Optional instance to populate.
     * @return {module:model/Operations} The populated <code>Operations</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Operations();

            if (data.hasOwnProperty('dependencyTargets')) {
                obj['dependencyTargets'] = ApiClient.convertToType(data['dependencyTargets'], [Target]);
            }
            if (data.hasOwnProperty('disabled')) {
                obj['disabled'] = ApiClient.convertToType(data['disabled'], 'Boolean');
            }
            if (data.hasOwnProperty('hasOutput')) {
                obj['hasOutput'] = ApiClient.convertToType(data['hasOutput'], 'Boolean');
            }
            if (data.hasOwnProperty('queries')) {
                obj['queries'] = ApiClient.convertToType(data['queries'], ['String']);
            }
            if (data.hasOwnProperty('relationDescriptor')) {
                obj['relationDescriptor'] = RelationDescriptor.constructFromObject(data['relationDescriptor']);
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Operations</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Operations</code>.
     */
    static validateJSON(data) {
        if (data['dependencyTargets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dependencyTargets'])) {
                throw new Error("Expected the field `dependencyTargets` to be an array in the JSON data but got " + data['dependencyTargets']);
            }
            // validate the optional field `dependencyTargets` (array)
            for (const item of data['dependencyTargets']) {
                Target.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['queries'])) {
            throw new Error("Expected the field `queries` to be an array in the JSON data but got " + data['queries']);
        }
        // validate the optional field `relationDescriptor`
        if (data['relationDescriptor']) { // data not null
          RelationDescriptor.validateJSON(data['relationDescriptor']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }

        return true;
    }


}



/**
 * A list of actions that this action depends on.
 * @member {Array.<module:model/Target>} dependencyTargets
 */
Operations.prototype['dependencyTargets'] = undefined;

/**
 * Whether this action is disabled (i.e. should not be run).
 * @member {Boolean} disabled
 */
Operations.prototype['disabled'] = undefined;

/**
 * Whether these operations produce an output relation.
 * @member {Boolean} hasOutput
 */
Operations.prototype['hasOutput'] = undefined;

/**
 * A list of arbitrary SQL statements that will be executed without alteration.
 * @member {Array.<String>} queries
 */
Operations.prototype['queries'] = undefined;

/**
 * @member {module:model/RelationDescriptor} relationDescriptor
 */
Operations.prototype['relationDescriptor'] = undefined;

/**
 * Arbitrary, user-defined tags on this action.
 * @member {Array.<String>} tags
 */
Operations.prototype['tags'] = undefined;






export default Operations;

