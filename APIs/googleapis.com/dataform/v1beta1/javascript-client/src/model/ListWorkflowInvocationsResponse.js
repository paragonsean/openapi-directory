/**
 * Dataform API
 * Service to develop, version control, and operationalize SQL pipelines in BigQuery.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkflowInvocation from './WorkflowInvocation';

/**
 * The ListWorkflowInvocationsResponse model module.
 * @module model/ListWorkflowInvocationsResponse
 * @version v1beta1
 */
class ListWorkflowInvocationsResponse {
    /**
     * Constructs a new <code>ListWorkflowInvocationsResponse</code>.
     * &#x60;ListWorkflowInvocations&#x60; response message.
     * @alias module:model/ListWorkflowInvocationsResponse
     */
    constructor() { 
        
        ListWorkflowInvocationsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListWorkflowInvocationsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListWorkflowInvocationsResponse} obj Optional instance to populate.
     * @return {module:model/ListWorkflowInvocationsResponse} The populated <code>ListWorkflowInvocationsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListWorkflowInvocationsResponse();

            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('unreachable')) {
                obj['unreachable'] = ApiClient.convertToType(data['unreachable'], ['String']);
            }
            if (data.hasOwnProperty('workflowInvocations')) {
                obj['workflowInvocations'] = ApiClient.convertToType(data['workflowInvocations'], [WorkflowInvocation]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListWorkflowInvocationsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListWorkflowInvocationsResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['unreachable'])) {
            throw new Error("Expected the field `unreachable` to be an array in the JSON data but got " + data['unreachable']);
        }
        if (data['workflowInvocations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['workflowInvocations'])) {
                throw new Error("Expected the field `workflowInvocations` to be an array in the JSON data but got " + data['workflowInvocations']);
            }
            // validate the optional field `workflowInvocations` (array)
            for (const item of data['workflowInvocations']) {
                WorkflowInvocation.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A token, which can be sent as `page_token` to retrieve the next page. If this field is omitted, there are no subsequent pages.
 * @member {String} nextPageToken
 */
ListWorkflowInvocationsResponse.prototype['nextPageToken'] = undefined;

/**
 * Locations which could not be reached.
 * @member {Array.<String>} unreachable
 */
ListWorkflowInvocationsResponse.prototype['unreachable'] = undefined;

/**
 * List of workflow invocations.
 * @member {Array.<module:model/WorkflowInvocation>} workflowInvocations
 */
ListWorkflowInvocationsResponse.prototype['workflowInvocations'] = undefined;






export default ListWorkflowInvocationsResponse;

