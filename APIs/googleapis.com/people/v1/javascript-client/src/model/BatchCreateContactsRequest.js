/**
 * People API
 * Provides access to information about profiles and contacts.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContactToCreate from './ContactToCreate';

/**
 * The BatchCreateContactsRequest model module.
 * @module model/BatchCreateContactsRequest
 * @version v1
 */
class BatchCreateContactsRequest {
    /**
     * Constructs a new <code>BatchCreateContactsRequest</code>.
     * A request to create a batch of contacts.
     * @alias module:model/BatchCreateContactsRequest
     */
    constructor() { 
        
        BatchCreateContactsRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BatchCreateContactsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BatchCreateContactsRequest} obj Optional instance to populate.
     * @return {module:model/BatchCreateContactsRequest} The populated <code>BatchCreateContactsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BatchCreateContactsRequest();

            if (data.hasOwnProperty('contacts')) {
                obj['contacts'] = ApiClient.convertToType(data['contacts'], [ContactToCreate]);
            }
            if (data.hasOwnProperty('readMask')) {
                obj['readMask'] = ApiClient.convertToType(data['readMask'], 'String');
            }
            if (data.hasOwnProperty('sources')) {
                obj['sources'] = ApiClient.convertToType(data['sources'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BatchCreateContactsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BatchCreateContactsRequest</code>.
     */
    static validateJSON(data) {
        if (data['contacts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['contacts'])) {
                throw new Error("Expected the field `contacts` to be an array in the JSON data but got " + data['contacts']);
            }
            // validate the optional field `contacts` (array)
            for (const item of data['contacts']) {
                ContactToCreate.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['readMask'] && !(typeof data['readMask'] === 'string' || data['readMask'] instanceof String)) {
            throw new Error("Expected the field `readMask` to be a primitive type in the JSON string but got " + data['readMask']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['sources'])) {
            throw new Error("Expected the field `sources` to be an array in the JSON data but got " + data['sources']);
        }

        return true;
    }


}



/**
 * Required. The contact to create. Allows up to 200 contacts in a single request.
 * @member {Array.<module:model/ContactToCreate>} contacts
 */
BatchCreateContactsRequest.prototype['contacts'] = undefined;

/**
 * Required. A field mask to restrict which fields on each person are returned in the response. Multiple fields can be specified by separating them with commas. If read mask is left empty, the post-mutate-get is skipped and no data will be returned in the response. Valid values are: * addresses * ageRanges * biographies * birthdays * calendarUrls * clientData * coverPhotos * emailAddresses * events * externalIds * genders * imClients * interests * locales * locations * memberships * metadata * miscKeywords * names * nicknames * occupations * organizations * phoneNumbers * photos * relations * sipAddresses * skills * urls * userDefined
 * @member {String} readMask
 */
BatchCreateContactsRequest.prototype['readMask'] = undefined;

/**
 * Optional. A mask of what source types to return in the post mutate read. Defaults to READ_SOURCE_TYPE_CONTACT and READ_SOURCE_TYPE_PROFILE if not set.
 * @member {Array.<module:model/BatchCreateContactsRequest.SourcesEnum>} sources
 */
BatchCreateContactsRequest.prototype['sources'] = undefined;





/**
 * Allowed values for the <code>sources</code> property.
 * @enum {String}
 * @readonly
 */
BatchCreateContactsRequest['SourcesEnum'] = {

    /**
     * value: "READ_SOURCE_TYPE_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "READ_SOURCE_TYPE_UNSPECIFIED",

    /**
     * value: "READ_SOURCE_TYPE_PROFILE"
     * @const
     */
    "PROFILE": "READ_SOURCE_TYPE_PROFILE",

    /**
     * value: "READ_SOURCE_TYPE_CONTACT"
     * @const
     */
    "CONTACT": "READ_SOURCE_TYPE_CONTACT",

    /**
     * value: "READ_SOURCE_TYPE_DOMAIN_CONTACT"
     * @const
     */
    "DOMAIN_CONTACT": "READ_SOURCE_TYPE_DOMAIN_CONTACT"
};



export default BatchCreateContactsRequest;

