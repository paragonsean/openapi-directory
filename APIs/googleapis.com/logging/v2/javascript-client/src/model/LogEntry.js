/**
 * Cloud Logging API
 * Writes log entries and manages your Cloud Logging configuration.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HttpRequest from './HttpRequest';
import LogEntryOperation from './LogEntryOperation';
import LogEntrySourceLocation from './LogEntrySourceLocation';
import LogErrorGroup from './LogErrorGroup';
import LogSplit from './LogSplit';
import MonitoredResource from './MonitoredResource';
import MonitoredResourceMetadata from './MonitoredResourceMetadata';

/**
 * The LogEntry model module.
 * @module model/LogEntry
 * @version v2
 */
class LogEntry {
    /**
     * Constructs a new <code>LogEntry</code>.
     * An individual entry in a log.
     * @alias module:model/LogEntry
     */
    constructor() { 
        
        LogEntry.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LogEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogEntry} obj Optional instance to populate.
     * @return {module:model/LogEntry} The populated <code>LogEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogEntry();

            if (data.hasOwnProperty('errorGroups')) {
                obj['errorGroups'] = ApiClient.convertToType(data['errorGroups'], [LogErrorGroup]);
            }
            if (data.hasOwnProperty('httpRequest')) {
                obj['httpRequest'] = HttpRequest.constructFromObject(data['httpRequest']);
            }
            if (data.hasOwnProperty('insertId')) {
                obj['insertId'] = ApiClient.convertToType(data['insertId'], 'String');
            }
            if (data.hasOwnProperty('jsonPayload')) {
                obj['jsonPayload'] = ApiClient.convertToType(data['jsonPayload'], {'String': Object});
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('logName')) {
                obj['logName'] = ApiClient.convertToType(data['logName'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = MonitoredResourceMetadata.constructFromObject(data['metadata']);
            }
            if (data.hasOwnProperty('operation')) {
                obj['operation'] = LogEntryOperation.constructFromObject(data['operation']);
            }
            if (data.hasOwnProperty('protoPayload')) {
                obj['protoPayload'] = ApiClient.convertToType(data['protoPayload'], {'String': Object});
            }
            if (data.hasOwnProperty('receiveTimestamp')) {
                obj['receiveTimestamp'] = ApiClient.convertToType(data['receiveTimestamp'], 'String');
            }
            if (data.hasOwnProperty('resource')) {
                obj['resource'] = MonitoredResource.constructFromObject(data['resource']);
            }
            if (data.hasOwnProperty('severity')) {
                obj['severity'] = ApiClient.convertToType(data['severity'], 'String');
            }
            if (data.hasOwnProperty('sourceLocation')) {
                obj['sourceLocation'] = LogEntrySourceLocation.constructFromObject(data['sourceLocation']);
            }
            if (data.hasOwnProperty('spanId')) {
                obj['spanId'] = ApiClient.convertToType(data['spanId'], 'String');
            }
            if (data.hasOwnProperty('split')) {
                obj['split'] = LogSplit.constructFromObject(data['split']);
            }
            if (data.hasOwnProperty('textPayload')) {
                obj['textPayload'] = ApiClient.convertToType(data['textPayload'], 'String');
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'String');
            }
            if (data.hasOwnProperty('trace')) {
                obj['trace'] = ApiClient.convertToType(data['trace'], 'String');
            }
            if (data.hasOwnProperty('traceSampled')) {
                obj['traceSampled'] = ApiClient.convertToType(data['traceSampled'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogEntry</code>.
     */
    static validateJSON(data) {
        if (data['errorGroups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errorGroups'])) {
                throw new Error("Expected the field `errorGroups` to be an array in the JSON data but got " + data['errorGroups']);
            }
            // validate the optional field `errorGroups` (array)
            for (const item of data['errorGroups']) {
                LogErrorGroup.validateJSON(item);
            };
        }
        // validate the optional field `httpRequest`
        if (data['httpRequest']) { // data not null
          HttpRequest.validateJSON(data['httpRequest']);
        }
        // ensure the json data is a string
        if (data['insertId'] && !(typeof data['insertId'] === 'string' || data['insertId'] instanceof String)) {
            throw new Error("Expected the field `insertId` to be a primitive type in the JSON string but got " + data['insertId']);
        }
        // ensure the json data is a string
        if (data['logName'] && !(typeof data['logName'] === 'string' || data['logName'] instanceof String)) {
            throw new Error("Expected the field `logName` to be a primitive type in the JSON string but got " + data['logName']);
        }
        // validate the optional field `metadata`
        if (data['metadata']) { // data not null
          MonitoredResourceMetadata.validateJSON(data['metadata']);
        }
        // validate the optional field `operation`
        if (data['operation']) { // data not null
          LogEntryOperation.validateJSON(data['operation']);
        }
        // ensure the json data is a string
        if (data['receiveTimestamp'] && !(typeof data['receiveTimestamp'] === 'string' || data['receiveTimestamp'] instanceof String)) {
            throw new Error("Expected the field `receiveTimestamp` to be a primitive type in the JSON string but got " + data['receiveTimestamp']);
        }
        // validate the optional field `resource`
        if (data['resource']) { // data not null
          MonitoredResource.validateJSON(data['resource']);
        }
        // ensure the json data is a string
        if (data['severity'] && !(typeof data['severity'] === 'string' || data['severity'] instanceof String)) {
            throw new Error("Expected the field `severity` to be a primitive type in the JSON string but got " + data['severity']);
        }
        // validate the optional field `sourceLocation`
        if (data['sourceLocation']) { // data not null
          LogEntrySourceLocation.validateJSON(data['sourceLocation']);
        }
        // ensure the json data is a string
        if (data['spanId'] && !(typeof data['spanId'] === 'string' || data['spanId'] instanceof String)) {
            throw new Error("Expected the field `spanId` to be a primitive type in the JSON string but got " + data['spanId']);
        }
        // validate the optional field `split`
        if (data['split']) { // data not null
          LogSplit.validateJSON(data['split']);
        }
        // ensure the json data is a string
        if (data['textPayload'] && !(typeof data['textPayload'] === 'string' || data['textPayload'] instanceof String)) {
            throw new Error("Expected the field `textPayload` to be a primitive type in the JSON string but got " + data['textPayload']);
        }
        // ensure the json data is a string
        if (data['timestamp'] && !(typeof data['timestamp'] === 'string' || data['timestamp'] instanceof String)) {
            throw new Error("Expected the field `timestamp` to be a primitive type in the JSON string but got " + data['timestamp']);
        }
        // ensure the json data is a string
        if (data['trace'] && !(typeof data['trace'] === 'string' || data['trace'] instanceof String)) {
            throw new Error("Expected the field `trace` to be a primitive type in the JSON string but got " + data['trace']);
        }

        return true;
    }


}



/**
 * Output only. The Error Reporting (https://cloud.google.com/error-reporting) error groups associated with this LogEntry. Error Reporting sets the values for this field during error group creation.For more information, see View error details( https://cloud.google.com/error-reporting/docs/viewing-errors#view_error_details)This field isn't available during log routing (https://cloud.google.com/logging/docs/routing/overview)
 * @member {Array.<module:model/LogErrorGroup>} errorGroups
 */
LogEntry.prototype['errorGroups'] = undefined;

/**
 * @member {module:model/HttpRequest} httpRequest
 */
LogEntry.prototype['httpRequest'] = undefined;

/**
 * Optional. A unique identifier for the log entry. If you provide a value, then Logging considers other log entries in the same project, with the same timestamp, and with the same insert_id to be duplicates which are removed in a single query result. However, there are no guarantees of de-duplication in the export of logs.If the insert_id is omitted when writing a log entry, the Logging API assigns its own unique identifier in this field.In queries, the insert_id is also used to order log entries that have the same log_name and timestamp values.
 * @member {String} insertId
 */
LogEntry.prototype['insertId'] = undefined;

/**
 * The log entry payload, represented as a structure that is expressed as a JSON object.
 * @member {Object.<String, Object>} jsonPayload
 */
LogEntry.prototype['jsonPayload'] = undefined;

/**
 * Optional. A map of key, value pairs that provides additional information about the log entry. The labels can be user-defined or system-defined.User-defined labels are arbitrary key, value pairs that you can use to classify logs.System-defined labels are defined by GCP services for platform logs. They have two components - a service namespace component and the attribute name. For example: compute.googleapis.com/resource_name.Cloud Logging truncates label keys that exceed 512 B and label values that exceed 64 KB upon their associated log entry being written. The truncation is indicated by an ellipsis at the end of the character string.
 * @member {Object.<String, String>} labels
 */
LogEntry.prototype['labels'] = undefined;

/**
 * Required. The resource name of the log to which this log entry belongs: \"projects/[PROJECT_ID]/logs/[LOG_ID]\" \"organizations/[ORGANIZATION_ID]/logs/[LOG_ID]\" \"billingAccounts/[BILLING_ACCOUNT_ID]/logs/[LOG_ID]\" \"folders/[FOLDER_ID]/logs/[LOG_ID]\" A project number may be used in place of PROJECT_ID. The project number is translated to its corresponding PROJECT_ID internally and the log_name field will contain PROJECT_ID in queries and exports.[LOG_ID] must be URL-encoded within log_name. Example: \"organizations/1234567890/logs/cloudresourcemanager.googleapis.com%2Factivity\".[LOG_ID] must be less than 512 characters long and can only include the following characters: upper and lower case alphanumeric characters, forward-slash, underscore, hyphen, and period.For backward compatibility, if log_name begins with a forward-slash, such as /projects/..., then the log entry is processed as usual, but the forward-slash is removed. Listing the log entry will not show the leading slash and filtering for a log name with a leading slash will never return any results.
 * @member {String} logName
 */
LogEntry.prototype['logName'] = undefined;

/**
 * @member {module:model/MonitoredResourceMetadata} metadata
 */
LogEntry.prototype['metadata'] = undefined;

/**
 * @member {module:model/LogEntryOperation} operation
 */
LogEntry.prototype['operation'] = undefined;

/**
 * The log entry payload, represented as a protocol buffer. Some Google Cloud Platform services use this field for their log entry payloads.The following protocol buffer types are supported; user-defined types are not supported:\"type.googleapis.com/google.cloud.audit.AuditLog\" \"type.googleapis.com/google.appengine.logging.v1.RequestLog\"
 * @member {Object.<String, Object>} protoPayload
 */
LogEntry.prototype['protoPayload'] = undefined;

/**
 * Output only. The time the log entry was received by Logging.
 * @member {String} receiveTimestamp
 */
LogEntry.prototype['receiveTimestamp'] = undefined;

/**
 * @member {module:model/MonitoredResource} resource
 */
LogEntry.prototype['resource'] = undefined;

/**
 * Optional. The severity of the log entry. The default value is LogSeverity.DEFAULT.
 * @member {module:model/LogEntry.SeverityEnum} severity
 */
LogEntry.prototype['severity'] = undefined;

/**
 * @member {module:model/LogEntrySourceLocation} sourceLocation
 */
LogEntry.prototype['sourceLocation'] = undefined;

/**
 * Optional. The ID of the Cloud Trace (https://cloud.google.com/trace) span associated with the current operation in which the log is being written. For example, if a span has the REST resource name of \"projects/some-project/traces/some-trace/spans/some-span-id\", then the span_id field is \"some-span-id\".A Span (https://cloud.google.com/trace/docs/reference/v2/rest/v2/projects.traces/batchWrite#Span) represents a single operation within a trace. Whereas a trace may involve multiple different microservices running on multiple different machines, a span generally corresponds to a single logical operation being performed in a single instance of a microservice on one specific machine. Spans are the nodes within the tree that is a trace.Applications that are instrumented for tracing (https://cloud.google.com/trace/docs/setup) will generally assign a new, unique span ID on each incoming request. It is also common to create and record additional spans corresponding to internal processing elements as well as issuing requests to dependencies.The span ID is expected to be a 16-character, hexadecimal encoding of an 8-byte array and should not be zero. It should be unique within the trace and should, ideally, be generated in a manner that is uniformly random.Example values: 000000000000004a 7a2190356c3fc94b 0000f00300090021 d39223e101960076
 * @member {String} spanId
 */
LogEntry.prototype['spanId'] = undefined;

/**
 * @member {module:model/LogSplit} split
 */
LogEntry.prototype['split'] = undefined;

/**
 * The log entry payload, represented as a Unicode string (UTF-8).
 * @member {String} textPayload
 */
LogEntry.prototype['textPayload'] = undefined;

/**
 * Optional. The time the event described by the log entry occurred. This time is used to compute the log entry's age and to enforce the logs retention period. If this field is omitted in a new log entry, then Logging assigns it the current time. Timestamps have nanosecond accuracy, but trailing zeros in the fractional seconds might be omitted when the timestamp is displayed.Incoming log entries must have timestamps that don't exceed the logs retention period (https://cloud.google.com/logging/quotas#logs_retention_periods) in the past, and that don't exceed 24 hours in the future. Log entries outside those time boundaries are rejected by Logging.
 * @member {String} timestamp
 */
LogEntry.prototype['timestamp'] = undefined;

/**
 * Optional. The REST resource name of the trace being written to Cloud Trace (https://cloud.google.com/trace) in association with this log entry. For example, if your trace data is stored in the Cloud project \"my-trace-project\" and if the service that is creating the log entry receives a trace header that includes the trace ID \"12345\", then the service should use \"projects/my-trace-project/traces/12345\".The trace field provides the link between logs and traces. By using this field, you can navigate from a log entry to a trace.
 * @member {String} trace
 */
LogEntry.prototype['trace'] = undefined;

/**
 * Optional. The sampling decision of the span associated with the log entry at the time the log entry was created. This field corresponds to the sampled flag in the W3C trace-context specification (https://www.w3.org/TR/trace-context/#sampled-flag). A non-sampled trace value is still useful as a request correlation identifier. The default is False.
 * @member {Boolean} traceSampled
 */
LogEntry.prototype['traceSampled'] = undefined;





/**
 * Allowed values for the <code>severity</code> property.
 * @enum {String}
 * @readonly
 */
LogEntry['SeverityEnum'] = {

    /**
     * value: "DEFAULT"
     * @const
     */
    "DEFAULT": "DEFAULT",

    /**
     * value: "DEBUG"
     * @const
     */
    "DEBUG": "DEBUG",

    /**
     * value: "INFO"
     * @const
     */
    "INFO": "INFO",

    /**
     * value: "NOTICE"
     * @const
     */
    "NOTICE": "NOTICE",

    /**
     * value: "WARNING"
     * @const
     */
    "WARNING": "WARNING",

    /**
     * value: "ERROR"
     * @const
     */
    "ERROR": "ERROR",

    /**
     * value: "CRITICAL"
     * @const
     */
    "CRITICAL": "CRITICAL",

    /**
     * value: "ALERT"
     * @const
     */
    "ALERT": "ALERT",

    /**
     * value: "EMERGENCY"
     * @const
     */
    "EMERGENCY": "EMERGENCY"
};



export default LogEntry;

