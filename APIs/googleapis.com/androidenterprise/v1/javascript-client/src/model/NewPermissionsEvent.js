/**
 * Google Play EMM API
 * Manages the deployment of apps to Android Enterprise devices.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NewPermissionsEvent model module.
 * @module model/NewPermissionsEvent
 * @version v1
 */
class NewPermissionsEvent {
    /**
     * Constructs a new <code>NewPermissionsEvent</code>.
     * An event generated when new permissions are added to an app.
     * @alias module:model/NewPermissionsEvent
     */
    constructor() { 
        
        NewPermissionsEvent.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NewPermissionsEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NewPermissionsEvent} obj Optional instance to populate.
     * @return {module:model/NewPermissionsEvent} The populated <code>NewPermissionsEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NewPermissionsEvent();

            if (data.hasOwnProperty('approvedPermissions')) {
                obj['approvedPermissions'] = ApiClient.convertToType(data['approvedPermissions'], ['String']);
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('requestedPermissions')) {
                obj['requestedPermissions'] = ApiClient.convertToType(data['requestedPermissions'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NewPermissionsEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NewPermissionsEvent</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['approvedPermissions'])) {
            throw new Error("Expected the field `approvedPermissions` to be an array in the JSON data but got " + data['approvedPermissions']);
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['requestedPermissions'])) {
            throw new Error("Expected the field `requestedPermissions` to be an array in the JSON data but got " + data['requestedPermissions']);
        }

        return true;
    }


}



/**
 * The set of permissions that the enterprise admin has already approved for this application. Use Permissions.Get on the EMM API to retrieve details about these permissions.
 * @member {Array.<String>} approvedPermissions
 */
NewPermissionsEvent.prototype['approvedPermissions'] = undefined;

/**
 * The id of the product (e.g. \"app:com.google.android.gm\") for which new permissions were added. This field will always be present.
 * @member {String} productId
 */
NewPermissionsEvent.prototype['productId'] = undefined;

/**
 * The set of permissions that the app is currently requesting. Use Permissions.Get on the EMM API to retrieve details about these permissions.
 * @member {Array.<String>} requestedPermissions
 */
NewPermissionsEvent.prototype['requestedPermissions'] = undefined;






export default NewPermissionsEvent;

