/**
 * Google Play EMM API
 * Manages the deployment of apps to Android Enterprise devices.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ProductVisibility model module.
 * @module model/ProductVisibility
 * @version v1
 */
class ProductVisibility {
    /**
     * Constructs a new <code>ProductVisibility</code>.
     * A product to be made visible to a user.
     * @alias module:model/ProductVisibility
     */
    constructor() { 
        
        ProductVisibility.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ProductVisibility</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductVisibility} obj Optional instance to populate.
     * @return {module:model/ProductVisibility} The populated <code>ProductVisibility</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductVisibility();

            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('trackIds')) {
                obj['trackIds'] = ApiClient.convertToType(data['trackIds'], ['String']);
            }
            if (data.hasOwnProperty('tracks')) {
                obj['tracks'] = ApiClient.convertToType(data['tracks'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductVisibility</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductVisibility</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['trackIds'])) {
            throw new Error("Expected the field `trackIds` to be an array in the JSON data but got " + data['trackIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tracks'])) {
            throw new Error("Expected the field `tracks` to be an array in the JSON data but got " + data['tracks']);
        }

        return true;
    }


}



/**
 * The product ID to make visible to the user. Required for each item in the productVisibility list.
 * @member {String} productId
 */
ProductVisibility.prototype['productId'] = undefined;

/**
 * Grants the user visibility to the specified product track(s), identified by trackIds.
 * @member {Array.<String>} trackIds
 */
ProductVisibility.prototype['trackIds'] = undefined;

/**
 * Deprecated. Use trackIds instead.
 * @member {Array.<module:model/ProductVisibility.TracksEnum>} tracks
 */
ProductVisibility.prototype['tracks'] = undefined;





/**
 * Allowed values for the <code>tracks</code> property.
 * @enum {String}
 * @readonly
 */
ProductVisibility['TracksEnum'] = {

    /**
     * value: "appTrackUnspecified"
     * @const
     */
    "appTrackUnspecified": "appTrackUnspecified",

    /**
     * value: "production"
     * @const
     */
    "production": "production",

    /**
     * value: "beta"
     * @const
     */
    "beta": "beta",

    /**
     * value: "alpha"
     * @const
     */
    "alpha": "alpha"
};



export default ProductVisibility;

