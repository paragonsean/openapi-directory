# coding: utf-8

from datetime import date, datetime

from typing import List, Dict, Type

from openapi_server.models.base_model import Model
from openapi_server import util


class GoogleCloudDiscoveryengineV1betaGcsSource(Model):
    """NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

    Do not edit the class manually.
    """

    def __init__(self, data_schema: str=None, input_uris: List[str]=None):
        """GoogleCloudDiscoveryengineV1betaGcsSource - a model defined in OpenAPI

        :param data_schema: The data_schema of this GoogleCloudDiscoveryengineV1betaGcsSource.
        :param input_uris: The input_uris of this GoogleCloudDiscoveryengineV1betaGcsSource.
        """
        self.openapi_types = {
            'data_schema': str,
            'input_uris': List[str]
        }

        self.attribute_map = {
            'data_schema': 'dataSchema',
            'input_uris': 'inputUris'
        }

        self._data_schema = data_schema
        self._input_uris = input_uris

    @classmethod
    def from_dict(cls, dikt: dict) -> 'GoogleCloudDiscoveryengineV1betaGcsSource':
        """Returns the dict as a model

        :param dikt: A dict.
        :return: The GoogleCloudDiscoveryengineV1betaGcsSource of this GoogleCloudDiscoveryengineV1betaGcsSource.
        """
        return util.deserialize_model(dikt, cls)

    @property
    def data_schema(self):
        """Gets the data_schema of this GoogleCloudDiscoveryengineV1betaGcsSource.

        The schema to use when parsing the data from the source. Supported values for document imports: * `document` (default): One JSON Document per line. Each document must have a valid Document.id. * `content`: Unstructured data (e.g. PDF, HTML). Each file matched by `input_uris` becomes a document, with the ID set to the first 128 bits of SHA256(URI) encoded as a hex string. * `custom`: One custom data JSON per row in arbitrary format that conforms to the defined Schema of the data store. This can only be used by Gen App Builder. * `csv`: A CSV file with header conforming to the defined Schema of the data store. Each entry after the header is imported as a Document. This can only be used by Gen App Builder. Supported values for user even imports: * `user_event` (default): One JSON UserEvent per line.

        :return: The data_schema of this GoogleCloudDiscoveryengineV1betaGcsSource.
        :rtype: str
        """
        return self._data_schema

    @data_schema.setter
    def data_schema(self, data_schema):
        """Sets the data_schema of this GoogleCloudDiscoveryengineV1betaGcsSource.

        The schema to use when parsing the data from the source. Supported values for document imports: * `document` (default): One JSON Document per line. Each document must have a valid Document.id. * `content`: Unstructured data (e.g. PDF, HTML). Each file matched by `input_uris` becomes a document, with the ID set to the first 128 bits of SHA256(URI) encoded as a hex string. * `custom`: One custom data JSON per row in arbitrary format that conforms to the defined Schema of the data store. This can only be used by Gen App Builder. * `csv`: A CSV file with header conforming to the defined Schema of the data store. Each entry after the header is imported as a Document. This can only be used by Gen App Builder. Supported values for user even imports: * `user_event` (default): One JSON UserEvent per line.

        :param data_schema: The data_schema of this GoogleCloudDiscoveryengineV1betaGcsSource.
        :type data_schema: str
        """

        self._data_schema = data_schema

    @property
    def input_uris(self):
        """Gets the input_uris of this GoogleCloudDiscoveryengineV1betaGcsSource.

        Required. Cloud Storage URIs to input files. URI can be up to 2000 characters long. URIs can match the full object path (for example, `gs://bucket/directory/object.json`) or a pattern matching one or more files, such as `gs://bucket/directory/*.json`. A request can contain at most 100 files (or 100,000 files if `data_schema` is `content`). Each file can be up to 2 GB (or 100 MB if `data_schema` is `content`).

        :return: The input_uris of this GoogleCloudDiscoveryengineV1betaGcsSource.
        :rtype: List[str]
        """
        return self._input_uris

    @input_uris.setter
    def input_uris(self, input_uris):
        """Sets the input_uris of this GoogleCloudDiscoveryengineV1betaGcsSource.

        Required. Cloud Storage URIs to input files. URI can be up to 2000 characters long. URIs can match the full object path (for example, `gs://bucket/directory/object.json`) or a pattern matching one or more files, such as `gs://bucket/directory/*.json`. A request can contain at most 100 files (or 100,000 files if `data_schema` is `content`). Each file can be up to 2 GB (or 100 MB if `data_schema` is `content`).

        :param input_uris: The input_uris of this GoogleCloudDiscoveryengineV1betaGcsSource.
        :type input_uris: List[str]
        """

        self._input_uris = input_uris
