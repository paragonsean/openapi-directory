/**
 * Discovery Engine API
 * Discovery Engine API.
 *
 * The version of the OpenAPI document: v1alpha
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GoogleCloudDiscoveryengineV1alphaGcsSource model module.
 * @module model/GoogleCloudDiscoveryengineV1alphaGcsSource
 * @version v1alpha
 */
class GoogleCloudDiscoveryengineV1alphaGcsSource {
    /**
     * Constructs a new <code>GoogleCloudDiscoveryengineV1alphaGcsSource</code>.
     * Cloud Storage location for input content.
     * @alias module:model/GoogleCloudDiscoveryengineV1alphaGcsSource
     */
    constructor() { 
        
        GoogleCloudDiscoveryengineV1alphaGcsSource.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoogleCloudDiscoveryengineV1alphaGcsSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoogleCloudDiscoveryengineV1alphaGcsSource} obj Optional instance to populate.
     * @return {module:model/GoogleCloudDiscoveryengineV1alphaGcsSource} The populated <code>GoogleCloudDiscoveryengineV1alphaGcsSource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoogleCloudDiscoveryengineV1alphaGcsSource();

            if (data.hasOwnProperty('dataSchema')) {
                obj['dataSchema'] = ApiClient.convertToType(data['dataSchema'], 'String');
            }
            if (data.hasOwnProperty('inputUris')) {
                obj['inputUris'] = ApiClient.convertToType(data['inputUris'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoogleCloudDiscoveryengineV1alphaGcsSource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoogleCloudDiscoveryengineV1alphaGcsSource</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['dataSchema'] && !(typeof data['dataSchema'] === 'string' || data['dataSchema'] instanceof String)) {
            throw new Error("Expected the field `dataSchema` to be a primitive type in the JSON string but got " + data['dataSchema']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['inputUris'])) {
            throw new Error("Expected the field `inputUris` to be an array in the JSON data but got " + data['inputUris']);
        }

        return true;
    }


}



/**
 * The schema to use when parsing the data from the source. Supported values for document imports: * `document` (default): One JSON Document per line. Each document must have a valid Document.id. * `content`: Unstructured data (e.g. PDF, HTML). Each file matched by `input_uris` becomes a document, with the ID set to the first 128 bits of SHA256(URI) encoded as a hex string. * `custom`: One custom data JSON per row in arbitrary format that conforms to the defined Schema of the data store. This can only be used by Gen App Builder. * `csv`: A CSV file with header conforming to the defined Schema of the data store. Each entry after the header is imported as a Document. This can only be used by Gen App Builder. Supported values for user even imports: * `user_event` (default): One JSON UserEvent per line.
 * @member {String} dataSchema
 */
GoogleCloudDiscoveryengineV1alphaGcsSource.prototype['dataSchema'] = undefined;

/**
 * Required. Cloud Storage URIs to input files. URI can be up to 2000 characters long. URIs can match the full object path (for example, `gs://bucket/directory/object.json`) or a pattern matching one or more files, such as `gs://bucket/directory/_*.json`. A request can contain at most 100 files (or 100,000 files if `data_schema` is `content`). Each file can be up to 2 GB (or 100 MB if `data_schema` is `content`).
 * @member {Array.<String>} inputUris
 */
GoogleCloudDiscoveryengineV1alphaGcsSource.prototype['inputUris'] = undefined;






export default GoogleCloudDiscoveryengineV1alphaGcsSource;

