/**
 * Access Context Manager API
 * An API for setting attribute based access control to requests to Google Cloud services. *Warning:* Do not mix *v1alpha* and *v1* API usage in the same access policy. The v1alpha API supports new Access Context Manager features, which may have different attributes or behaviors that are not supported by v1. The practice of mixed API usage within a policy may result in the inability to update that policy, including any access levels or service perimeters belonging to it. It is not recommended to use both v1 and v1alpha for modifying policies with critical service perimeters. Modifications using v1alpha should be limited to policies with non-production/non-critical service perimeters.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MethodSelector from './MethodSelector';

/**
 * The SupportedService model module.
 * @module model/SupportedService
 * @version v1
 */
class SupportedService {
    /**
     * Constructs a new <code>SupportedService</code>.
     * &#x60;SupportedService&#x60; specifies the VPC Service Controls and its properties.
     * @alias module:model/SupportedService
     */
    constructor() { 
        
        SupportedService.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SupportedService</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SupportedService} obj Optional instance to populate.
     * @return {module:model/SupportedService} The populated <code>SupportedService</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SupportedService();

            if (data.hasOwnProperty('availableOnRestrictedVip')) {
                obj['availableOnRestrictedVip'] = ApiClient.convertToType(data['availableOnRestrictedVip'], 'Boolean');
            }
            if (data.hasOwnProperty('knownLimitations')) {
                obj['knownLimitations'] = ApiClient.convertToType(data['knownLimitations'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('supportStage')) {
                obj['supportStage'] = ApiClient.convertToType(data['supportStage'], 'String');
            }
            if (data.hasOwnProperty('supportedMethods')) {
                obj['supportedMethods'] = ApiClient.convertToType(data['supportedMethods'], [MethodSelector]);
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SupportedService</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SupportedService</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['supportStage'] && !(typeof data['supportStage'] === 'string' || data['supportStage'] instanceof String)) {
            throw new Error("Expected the field `supportStage` to be a primitive type in the JSON string but got " + data['supportStage']);
        }
        if (data['supportedMethods']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['supportedMethods'])) {
                throw new Error("Expected the field `supportedMethods` to be an array in the JSON data but got " + data['supportedMethods']);
            }
            // validate the optional field `supportedMethods` (array)
            for (const item of data['supportedMethods']) {
                MethodSelector.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}



/**
 * True if the service is available on the restricted VIP. Services on the restricted VIP typically either support VPC Service Controls or are core infrastructure services required for the functioning of Google Cloud.
 * @member {Boolean} availableOnRestrictedVip
 */
SupportedService.prototype['availableOnRestrictedVip'] = undefined;

/**
 * True if the service is supported with some limitations. Check [documentation](https://cloud.google.com/vpc-service-controls/docs/supported-products) for details.
 * @member {Boolean} knownLimitations
 */
SupportedService.prototype['knownLimitations'] = undefined;

/**
 * The service name or address of the supported service, such as `service.googleapis.com`.
 * @member {String} name
 */
SupportedService.prototype['name'] = undefined;

/**
 * The support stage of the service.
 * @member {module:model/SupportedService.SupportStageEnum} supportStage
 */
SupportedService.prototype['supportStage'] = undefined;

/**
 * The list of the supported methods. This field exists only in response to GetSupportedService
 * @member {Array.<module:model/MethodSelector>} supportedMethods
 */
SupportedService.prototype['supportedMethods'] = undefined;

/**
 * The name of the supported product, such as 'Cloud Product API'.
 * @member {String} title
 */
SupportedService.prototype['title'] = undefined;





/**
 * Allowed values for the <code>supportStage</code> property.
 * @enum {String}
 * @readonly
 */
SupportedService['SupportStageEnum'] = {

    /**
     * value: "LAUNCH_STAGE_UNSPECIFIED"
     * @const
     */
    "LAUNCH_STAGE_UNSPECIFIED": "LAUNCH_STAGE_UNSPECIFIED",

    /**
     * value: "UNIMPLEMENTED"
     * @const
     */
    "UNIMPLEMENTED": "UNIMPLEMENTED",

    /**
     * value: "PRELAUNCH"
     * @const
     */
    "PRELAUNCH": "PRELAUNCH",

    /**
     * value: "EARLY_ACCESS"
     * @const
     */
    "EARLY_ACCESS": "EARLY_ACCESS",

    /**
     * value: "ALPHA"
     * @const
     */
    "ALPHA": "ALPHA",

    /**
     * value: "BETA"
     * @const
     */
    "BETA": "BETA",

    /**
     * value: "GA"
     * @const
     */
    "GA": "GA",

    /**
     * value: "DEPRECATED"
     * @const
     */
    "DEPRECATED": "DEPRECATED"
};



export default SupportedService;

