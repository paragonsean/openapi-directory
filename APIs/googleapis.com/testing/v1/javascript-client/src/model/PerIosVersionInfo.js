/**
 * Cloud Testing API
 * Allows developers to run automated tests for their mobile applications on Google infrastructure.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PerIosVersionInfo model module.
 * @module model/PerIosVersionInfo
 * @version v1
 */
class PerIosVersionInfo {
    /**
     * Constructs a new <code>PerIosVersionInfo</code>.
     * A version-specific information of an iOS model.
     * @alias module:model/PerIosVersionInfo
     */
    constructor() { 
        
        PerIosVersionInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PerIosVersionInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PerIosVersionInfo} obj Optional instance to populate.
     * @return {module:model/PerIosVersionInfo} The populated <code>PerIosVersionInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PerIosVersionInfo();

            if (data.hasOwnProperty('deviceCapacity')) {
                obj['deviceCapacity'] = ApiClient.convertToType(data['deviceCapacity'], 'String');
            }
            if (data.hasOwnProperty('versionId')) {
                obj['versionId'] = ApiClient.convertToType(data['versionId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PerIosVersionInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PerIosVersionInfo</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['deviceCapacity'] && !(typeof data['deviceCapacity'] === 'string' || data['deviceCapacity'] instanceof String)) {
            throw new Error("Expected the field `deviceCapacity` to be a primitive type in the JSON string but got " + data['deviceCapacity']);
        }
        // ensure the json data is a string
        if (data['versionId'] && !(typeof data['versionId'] === 'string' || data['versionId'] instanceof String)) {
            throw new Error("Expected the field `versionId` to be a primitive type in the JSON string but got " + data['versionId']);
        }

        return true;
    }


}



/**
 * The number of online devices for an iOS version.
 * @member {module:model/PerIosVersionInfo.DeviceCapacityEnum} deviceCapacity
 */
PerIosVersionInfo.prototype['deviceCapacity'] = undefined;

/**
 * An iOS version.
 * @member {String} versionId
 */
PerIosVersionInfo.prototype['versionId'] = undefined;





/**
 * Allowed values for the <code>deviceCapacity</code> property.
 * @enum {String}
 * @readonly
 */
PerIosVersionInfo['DeviceCapacityEnum'] = {

    /**
     * value: "DEVICE_CAPACITY_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "DEVICE_CAPACITY_UNSPECIFIED",

    /**
     * value: "DEVICE_CAPACITY_HIGH"
     * @const
     */
    "HIGH": "DEVICE_CAPACITY_HIGH",

    /**
     * value: "DEVICE_CAPACITY_MEDIUM"
     * @const
     */
    "MEDIUM": "DEVICE_CAPACITY_MEDIUM",

    /**
     * value: "DEVICE_CAPACITY_LOW"
     * @const
     */
    "LOW": "DEVICE_CAPACITY_LOW",

    /**
     * value: "DEVICE_CAPACITY_NONE"
     * @const
     */
    "NONE": "DEVICE_CAPACITY_NONE"
};



export default PerIosVersionInfo;

