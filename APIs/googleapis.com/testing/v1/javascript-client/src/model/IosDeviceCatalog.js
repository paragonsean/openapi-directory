/**
 * Cloud Testing API
 * Allows developers to run automated tests for their mobile applications on Google infrastructure.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IosModel from './IosModel';
import IosRuntimeConfiguration from './IosRuntimeConfiguration';
import IosVersion from './IosVersion';
import XcodeVersion from './XcodeVersion';

/**
 * The IosDeviceCatalog model module.
 * @module model/IosDeviceCatalog
 * @version v1
 */
class IosDeviceCatalog {
    /**
     * Constructs a new <code>IosDeviceCatalog</code>.
     * The currently supported iOS devices.
     * @alias module:model/IosDeviceCatalog
     */
    constructor() { 
        
        IosDeviceCatalog.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>IosDeviceCatalog</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IosDeviceCatalog} obj Optional instance to populate.
     * @return {module:model/IosDeviceCatalog} The populated <code>IosDeviceCatalog</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IosDeviceCatalog();

            if (data.hasOwnProperty('models')) {
                obj['models'] = ApiClient.convertToType(data['models'], [IosModel]);
            }
            if (data.hasOwnProperty('runtimeConfiguration')) {
                obj['runtimeConfiguration'] = IosRuntimeConfiguration.constructFromObject(data['runtimeConfiguration']);
            }
            if (data.hasOwnProperty('versions')) {
                obj['versions'] = ApiClient.convertToType(data['versions'], [IosVersion]);
            }
            if (data.hasOwnProperty('xcodeVersions')) {
                obj['xcodeVersions'] = ApiClient.convertToType(data['xcodeVersions'], [XcodeVersion]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IosDeviceCatalog</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IosDeviceCatalog</code>.
     */
    static validateJSON(data) {
        if (data['models']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['models'])) {
                throw new Error("Expected the field `models` to be an array in the JSON data but got " + data['models']);
            }
            // validate the optional field `models` (array)
            for (const item of data['models']) {
                IosModel.validateJSON(item);
            };
        }
        // validate the optional field `runtimeConfiguration`
        if (data['runtimeConfiguration']) { // data not null
          IosRuntimeConfiguration.validateJSON(data['runtimeConfiguration']);
        }
        if (data['versions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['versions'])) {
                throw new Error("Expected the field `versions` to be an array in the JSON data but got " + data['versions']);
            }
            // validate the optional field `versions` (array)
            for (const item of data['versions']) {
                IosVersion.validateJSON(item);
            };
        }
        if (data['xcodeVersions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['xcodeVersions'])) {
                throw new Error("Expected the field `xcodeVersions` to be an array in the JSON data but got " + data['xcodeVersions']);
            }
            // validate the optional field `xcodeVersions` (array)
            for (const item of data['xcodeVersions']) {
                XcodeVersion.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The set of supported iOS device models.
 * @member {Array.<module:model/IosModel>} models
 */
IosDeviceCatalog.prototype['models'] = undefined;

/**
 * @member {module:model/IosRuntimeConfiguration} runtimeConfiguration
 */
IosDeviceCatalog.prototype['runtimeConfiguration'] = undefined;

/**
 * The set of supported iOS software versions.
 * @member {Array.<module:model/IosVersion>} versions
 */
IosDeviceCatalog.prototype['versions'] = undefined;

/**
 * The set of supported Xcode versions.
 * @member {Array.<module:model/XcodeVersion>} xcodeVersions
 */
IosDeviceCatalog.prototype['xcodeVersions'] = undefined;






export default IosDeviceCatalog;

