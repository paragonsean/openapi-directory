/**
 * API Gateway API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ApigatewayGateway from './ApigatewayGateway';

/**
 * The ApigatewayListGatewaysResponse model module.
 * @module model/ApigatewayListGatewaysResponse
 * @version v1
 */
class ApigatewayListGatewaysResponse {
    /**
     * Constructs a new <code>ApigatewayListGatewaysResponse</code>.
     * Response message for ApiGatewayService.ListGateways
     * @alias module:model/ApigatewayListGatewaysResponse
     */
    constructor() { 
        
        ApigatewayListGatewaysResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ApigatewayListGatewaysResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApigatewayListGatewaysResponse} obj Optional instance to populate.
     * @return {module:model/ApigatewayListGatewaysResponse} The populated <code>ApigatewayListGatewaysResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApigatewayListGatewaysResponse();

            if (data.hasOwnProperty('gateways')) {
                obj['gateways'] = ApiClient.convertToType(data['gateways'], [ApigatewayGateway]);
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('unreachableLocations')) {
                obj['unreachableLocations'] = ApiClient.convertToType(data['unreachableLocations'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApigatewayListGatewaysResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApigatewayListGatewaysResponse</code>.
     */
    static validateJSON(data) {
        if (data['gateways']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['gateways'])) {
                throw new Error("Expected the field `gateways` to be an array in the JSON data but got " + data['gateways']);
            }
            // validate the optional field `gateways` (array)
            for (const item of data['gateways']) {
                ApigatewayGateway.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['unreachableLocations'])) {
            throw new Error("Expected the field `unreachableLocations` to be an array in the JSON data but got " + data['unreachableLocations']);
        }

        return true;
    }


}



/**
 * Gateways.
 * @member {Array.<module:model/ApigatewayGateway>} gateways
 */
ApigatewayListGatewaysResponse.prototype['gateways'] = undefined;

/**
 * Next page token.
 * @member {String} nextPageToken
 */
ApigatewayListGatewaysResponse.prototype['nextPageToken'] = undefined;

/**
 * Locations that could not be reached.
 * @member {Array.<String>} unreachableLocations
 */
ApigatewayListGatewaysResponse.prototype['unreachableLocations'] = undefined;






export default ApigatewayListGatewaysResponse;

