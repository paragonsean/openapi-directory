/**
 * My Business Business Calls API
 * The My Business Business Calls API manages business calls information of a location on Google and collect insights like the number of missed calls to their location. Additional information about Business calls can be found at https://support.google.com/business/answer/9688285?p=call_history. If the Google Business Profile links to a Google Ads account and call history is turned on, calls that last longer than a specific time, and that can be attributed to an ad interaction, will show in the linked Google Ads account under the \"Calls from Ads\" conversion. If smart bidding and call conversions are used in the optimization strategy, there could be a change in ad spend. Learn more about smart bidding. To view and perform actions on a location's calls, you need to be a `OWNER`, `CO_OWNER` or `MANAGER` of the location. Note - If you have a quota of 0 after enabling the API, please request for GBP API access.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HourlyMetrics from './HourlyMetrics';
import ModelDate from './ModelDate';
import WeekDayMetrics from './WeekDayMetrics';

/**
 * The AggregateMetrics model module.
 * @module model/AggregateMetrics
 * @version v1
 */
class AggregateMetrics {
    /**
     * Constructs a new <code>AggregateMetrics</code>.
     * Metrics aggregated over the input time range.
     * @alias module:model/AggregateMetrics
     */
    constructor() { 
        
        AggregateMetrics.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AggregateMetrics</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AggregateMetrics} obj Optional instance to populate.
     * @return {module:model/AggregateMetrics} The populated <code>AggregateMetrics</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AggregateMetrics();

            if (data.hasOwnProperty('answeredCallsCount')) {
                obj['answeredCallsCount'] = ApiClient.convertToType(data['answeredCallsCount'], 'Number');
            }
            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ModelDate.constructFromObject(data['endDate']);
            }
            if (data.hasOwnProperty('hourlyMetrics')) {
                obj['hourlyMetrics'] = ApiClient.convertToType(data['hourlyMetrics'], [HourlyMetrics]);
            }
            if (data.hasOwnProperty('missedCallsCount')) {
                obj['missedCallsCount'] = ApiClient.convertToType(data['missedCallsCount'], 'Number');
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ModelDate.constructFromObject(data['startDate']);
            }
            if (data.hasOwnProperty('weekdayMetrics')) {
                obj['weekdayMetrics'] = ApiClient.convertToType(data['weekdayMetrics'], [WeekDayMetrics]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AggregateMetrics</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AggregateMetrics</code>.
     */
    static validateJSON(data) {
        // validate the optional field `endDate`
        if (data['endDate']) { // data not null
          ModelDate.validateJSON(data['endDate']);
        }
        if (data['hourlyMetrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['hourlyMetrics'])) {
                throw new Error("Expected the field `hourlyMetrics` to be an array in the JSON data but got " + data['hourlyMetrics']);
            }
            // validate the optional field `hourlyMetrics` (array)
            for (const item of data['hourlyMetrics']) {
                HourlyMetrics.validateJSON(item);
            };
        }
        // validate the optional field `startDate`
        if (data['startDate']) { // data not null
          ModelDate.validateJSON(data['startDate']);
        }
        if (data['weekdayMetrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['weekdayMetrics'])) {
                throw new Error("Expected the field `weekdayMetrics` to be an array in the JSON data but got " + data['weekdayMetrics']);
            }
            // validate the optional field `weekdayMetrics` (array)
            for (const item of data['weekdayMetrics']) {
                WeekDayMetrics.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Total count of answered calls.
 * @member {Number} answeredCallsCount
 */
AggregateMetrics.prototype['answeredCallsCount'] = undefined;

/**
 * @member {module:model/ModelDate} endDate
 */
AggregateMetrics.prototype['endDate'] = undefined;

/**
 * A list of metrics by hour of day.
 * @member {Array.<module:model/HourlyMetrics>} hourlyMetrics
 */
AggregateMetrics.prototype['hourlyMetrics'] = undefined;

/**
 * Total count of missed calls.
 * @member {Number} missedCallsCount
 */
AggregateMetrics.prototype['missedCallsCount'] = undefined;

/**
 * @member {module:model/ModelDate} startDate
 */
AggregateMetrics.prototype['startDate'] = undefined;

/**
 * A list of metrics by day of week.
 * @member {Array.<module:model/WeekDayMetrics>} weekdayMetrics
 */
AggregateMetrics.prototype['weekdayMetrics'] = undefined;






export default AggregateMetrics;

