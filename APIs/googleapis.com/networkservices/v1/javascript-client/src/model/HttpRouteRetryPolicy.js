/**
 * Network Services API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The HttpRouteRetryPolicy model module.
 * @module model/HttpRouteRetryPolicy
 * @version v1
 */
class HttpRouteRetryPolicy {
    /**
     * Constructs a new <code>HttpRouteRetryPolicy</code>.
     * The specifications for retries.
     * @alias module:model/HttpRouteRetryPolicy
     */
    constructor() { 
        
        HttpRouteRetryPolicy.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>HttpRouteRetryPolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HttpRouteRetryPolicy} obj Optional instance to populate.
     * @return {module:model/HttpRouteRetryPolicy} The populated <code>HttpRouteRetryPolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HttpRouteRetryPolicy();

            if (data.hasOwnProperty('numRetries')) {
                obj['numRetries'] = ApiClient.convertToType(data['numRetries'], 'Number');
            }
            if (data.hasOwnProperty('perTryTimeout')) {
                obj['perTryTimeout'] = ApiClient.convertToType(data['perTryTimeout'], 'String');
            }
            if (data.hasOwnProperty('retryConditions')) {
                obj['retryConditions'] = ApiClient.convertToType(data['retryConditions'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HttpRouteRetryPolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HttpRouteRetryPolicy</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['perTryTimeout'] && !(typeof data['perTryTimeout'] === 'string' || data['perTryTimeout'] instanceof String)) {
            throw new Error("Expected the field `perTryTimeout` to be a primitive type in the JSON string but got " + data['perTryTimeout']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['retryConditions'])) {
            throw new Error("Expected the field `retryConditions` to be an array in the JSON data but got " + data['retryConditions']);
        }

        return true;
    }


}



/**
 * Specifies the allowed number of retries. This number must be > 0. If not specified, default to 1.
 * @member {Number} numRetries
 */
HttpRouteRetryPolicy.prototype['numRetries'] = undefined;

/**
 * Specifies a non-zero timeout per retry attempt.
 * @member {String} perTryTimeout
 */
HttpRouteRetryPolicy.prototype['perTryTimeout'] = undefined;

/**
 * Specifies one or more conditions when this retry policy applies. Valid values are: 5xx: Proxy will attempt a retry if the destination service responds with any 5xx response code, of if the destination service does not respond at all, example: disconnect, reset, read timeout, connection failure and refused streams. gateway-error: Similar to 5xx, but only applies to response codes 502, 503, 504. reset: Proxy will attempt a retry if the destination service does not respond at all (disconnect/reset/read timeout) connect-failure: Proxy will retry on failures connecting to destination for example due to connection timeouts. retriable-4xx: Proxy will retry fro retriable 4xx response codes. Currently the only retriable error supported is 409. refused-stream: Proxy will retry if the destination resets the stream with a REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
 * @member {Array.<String>} retryConditions
 */
HttpRouteRetryPolicy.prototype['retryConditions'] = undefined;






export default HttpRouteRetryPolicy;

