/**
 * Google Drive API
 * The Google Drive API allows clients to access resources from Google Drive.
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Drive from './Drive';

/**
 * The DriveList model module.
 * @module model/DriveList
 * @version v3
 */
class DriveList {
    /**
     * Constructs a new <code>DriveList</code>.
     * A list of shared drives.
     * @alias module:model/DriveList
     */
    constructor() { 
        
        DriveList.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['kind'] = 'drive#driveList';
    }

    /**
     * Constructs a <code>DriveList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DriveList} obj Optional instance to populate.
     * @return {module:model/DriveList} The populated <code>DriveList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DriveList();

            if (data.hasOwnProperty('drives')) {
                obj['drives'] = ApiClient.convertToType(data['drives'], [Drive]);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DriveList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DriveList</code>.
     */
    static validateJSON(data) {
        if (data['drives']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['drives'])) {
                throw new Error("Expected the field `drives` to be an array in the JSON data but got " + data['drives']);
            }
            // validate the optional field `drives` (array)
            for (const item of data['drives']) {
                Drive.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }

        return true;
    }


}



/**
 * The list of shared drives. If nextPageToken is populated, then this list may be incomplete and an additional page of results should be fetched.
 * @member {Array.<module:model/Drive>} drives
 */
DriveList.prototype['drives'] = undefined;

/**
 * Identifies what kind of resource this is. Value: the fixed string `\"drive#driveList\"`.
 * @member {String} kind
 * @default 'drive#driveList'
 */
DriveList.prototype['kind'] = 'drive#driveList';

/**
 * The page token for the next page of shared drives. This will be absent if the end of the list has been reached. If the token is rejected for any reason, it should be discarded, and pagination should be restarted from the first page of results. The page token is typically valid for several hours. However, if new items are added or removed, your expected results might differ.
 * @member {String} nextPageToken
 */
DriveList.prototype['nextPageToken'] = undefined;






export default DriveList;

