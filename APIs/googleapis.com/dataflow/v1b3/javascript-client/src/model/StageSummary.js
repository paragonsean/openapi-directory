/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MetricUpdate from './MetricUpdate';
import ProgressTimeseries from './ProgressTimeseries';
import StragglerSummary from './StragglerSummary';

/**
 * The StageSummary model module.
 * @module model/StageSummary
 * @version v1b3
 */
class StageSummary {
    /**
     * Constructs a new <code>StageSummary</code>.
     * Information about a particular execution stage of a job.
     * @alias module:model/StageSummary
     */
    constructor() { 
        
        StageSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>StageSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StageSummary} obj Optional instance to populate.
     * @return {module:model/StageSummary} The populated <code>StageSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StageSummary();

            if (data.hasOwnProperty('endTime')) {
                obj['endTime'] = ApiClient.convertToType(data['endTime'], 'String');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [MetricUpdate]);
            }
            if (data.hasOwnProperty('progress')) {
                obj['progress'] = ProgressTimeseries.constructFromObject(data['progress']);
            }
            if (data.hasOwnProperty('stageId')) {
                obj['stageId'] = ApiClient.convertToType(data['stageId'], 'String');
            }
            if (data.hasOwnProperty('startTime')) {
                obj['startTime'] = ApiClient.convertToType(data['startTime'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('stragglerSummary')) {
                obj['stragglerSummary'] = StragglerSummary.constructFromObject(data['stragglerSummary']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StageSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StageSummary</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['endTime'] && !(typeof data['endTime'] === 'string' || data['endTime'] instanceof String)) {
            throw new Error("Expected the field `endTime` to be a primitive type in the JSON string but got " + data['endTime']);
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                MetricUpdate.validateJSON(item);
            };
        }
        // validate the optional field `progress`
        if (data['progress']) { // data not null
          ProgressTimeseries.validateJSON(data['progress']);
        }
        // ensure the json data is a string
        if (data['stageId'] && !(typeof data['stageId'] === 'string' || data['stageId'] instanceof String)) {
            throw new Error("Expected the field `stageId` to be a primitive type in the JSON string but got " + data['stageId']);
        }
        // ensure the json data is a string
        if (data['startTime'] && !(typeof data['startTime'] === 'string' || data['startTime'] instanceof String)) {
            throw new Error("Expected the field `startTime` to be a primitive type in the JSON string but got " + data['startTime']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // validate the optional field `stragglerSummary`
        if (data['stragglerSummary']) { // data not null
          StragglerSummary.validateJSON(data['stragglerSummary']);
        }

        return true;
    }


}



/**
 * End time of this stage. If the work item is completed, this is the actual end time of the stage. Otherwise, it is the predicted end time.
 * @member {String} endTime
 */
StageSummary.prototype['endTime'] = undefined;

/**
 * Metrics for this stage.
 * @member {Array.<module:model/MetricUpdate>} metrics
 */
StageSummary.prototype['metrics'] = undefined;

/**
 * @member {module:model/ProgressTimeseries} progress
 */
StageSummary.prototype['progress'] = undefined;

/**
 * ID of this stage
 * @member {String} stageId
 */
StageSummary.prototype['stageId'] = undefined;

/**
 * Start time of this stage.
 * @member {String} startTime
 */
StageSummary.prototype['startTime'] = undefined;

/**
 * State of this stage.
 * @member {module:model/StageSummary.StateEnum} state
 */
StageSummary.prototype['state'] = undefined;

/**
 * @member {module:model/StragglerSummary} stragglerSummary
 */
StageSummary.prototype['stragglerSummary'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
StageSummary['StateEnum'] = {

    /**
     * value: "EXECUTION_STATE_UNKNOWN"
     * @const
     */
    "UNKNOWN": "EXECUTION_STATE_UNKNOWN",

    /**
     * value: "EXECUTION_STATE_NOT_STARTED"
     * @const
     */
    "NOT_STARTED": "EXECUTION_STATE_NOT_STARTED",

    /**
     * value: "EXECUTION_STATE_RUNNING"
     * @const
     */
    "RUNNING": "EXECUTION_STATE_RUNNING",

    /**
     * value: "EXECUTION_STATE_SUCCEEDED"
     * @const
     */
    "SUCCEEDED": "EXECUTION_STATE_SUCCEEDED",

    /**
     * value: "EXECUTION_STATE_FAILED"
     * @const
     */
    "FAILED": "EXECUTION_STATE_FAILED",

    /**
     * value: "EXECUTION_STATE_CANCELLED"
     * @const
     */
    "CANCELLED": "EXECUTION_STATE_CANCELLED"
};



export default StageSummary;

