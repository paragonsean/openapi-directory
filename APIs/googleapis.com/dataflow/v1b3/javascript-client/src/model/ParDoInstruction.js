/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import InstructionInput from './InstructionInput';
import MultiOutputInfo from './MultiOutputInfo';
import SideInputInfo from './SideInputInfo';

/**
 * The ParDoInstruction model module.
 * @module model/ParDoInstruction
 * @version v1b3
 */
class ParDoInstruction {
    /**
     * Constructs a new <code>ParDoInstruction</code>.
     * An instruction that does a ParDo operation. Takes one main input and zero or more side inputs, and produces zero or more outputs. Runs user code.
     * @alias module:model/ParDoInstruction
     */
    constructor() { 
        
        ParDoInstruction.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ParDoInstruction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ParDoInstruction} obj Optional instance to populate.
     * @return {module:model/ParDoInstruction} The populated <code>ParDoInstruction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ParDoInstruction();

            if (data.hasOwnProperty('input')) {
                obj['input'] = InstructionInput.constructFromObject(data['input']);
            }
            if (data.hasOwnProperty('multiOutputInfos')) {
                obj['multiOutputInfos'] = ApiClient.convertToType(data['multiOutputInfos'], [MultiOutputInfo]);
            }
            if (data.hasOwnProperty('numOutputs')) {
                obj['numOutputs'] = ApiClient.convertToType(data['numOutputs'], 'Number');
            }
            if (data.hasOwnProperty('sideInputs')) {
                obj['sideInputs'] = ApiClient.convertToType(data['sideInputs'], [SideInputInfo]);
            }
            if (data.hasOwnProperty('userFn')) {
                obj['userFn'] = ApiClient.convertToType(data['userFn'], {'String': Object});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ParDoInstruction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ParDoInstruction</code>.
     */
    static validateJSON(data) {
        // validate the optional field `input`
        if (data['input']) { // data not null
          InstructionInput.validateJSON(data['input']);
        }
        if (data['multiOutputInfos']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['multiOutputInfos'])) {
                throw new Error("Expected the field `multiOutputInfos` to be an array in the JSON data but got " + data['multiOutputInfos']);
            }
            // validate the optional field `multiOutputInfos` (array)
            for (const item of data['multiOutputInfos']) {
                MultiOutputInfo.validateJSON(item);
            };
        }
        if (data['sideInputs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sideInputs'])) {
                throw new Error("Expected the field `sideInputs` to be an array in the JSON data but got " + data['sideInputs']);
            }
            // validate the optional field `sideInputs` (array)
            for (const item of data['sideInputs']) {
                SideInputInfo.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/InstructionInput} input
 */
ParDoInstruction.prototype['input'] = undefined;

/**
 * Information about each of the outputs, if user_fn is a MultiDoFn.
 * @member {Array.<module:model/MultiOutputInfo>} multiOutputInfos
 */
ParDoInstruction.prototype['multiOutputInfos'] = undefined;

/**
 * The number of outputs.
 * @member {Number} numOutputs
 */
ParDoInstruction.prototype['numOutputs'] = undefined;

/**
 * Zero or more side inputs.
 * @member {Array.<module:model/SideInputInfo>} sideInputs
 */
ParDoInstruction.prototype['sideInputs'] = undefined;

/**
 * The user function to invoke.
 * @member {Object.<String, Object>} userFn
 */
ParDoInstruction.prototype['userFn'] = undefined;






export default ParDoInstruction;

