/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WorkerLifecycleEvent model module.
 * @module model/WorkerLifecycleEvent
 * @version v1b3
 */
class WorkerLifecycleEvent {
    /**
     * Constructs a new <code>WorkerLifecycleEvent</code>.
     * A report of an event in a worker&#39;s lifecycle. The proto contains one event, because the worker is expected to asynchronously send each message immediately after the event. Due to this asynchrony, messages may arrive out of order (or missing), and it is up to the consumer to interpret. The timestamp of the event is in the enclosing WorkerMessage proto.
     * @alias module:model/WorkerLifecycleEvent
     */
    constructor() { 
        
        WorkerLifecycleEvent.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WorkerLifecycleEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkerLifecycleEvent} obj Optional instance to populate.
     * @return {module:model/WorkerLifecycleEvent} The populated <code>WorkerLifecycleEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkerLifecycleEvent();

            if (data.hasOwnProperty('containerStartTime')) {
                obj['containerStartTime'] = ApiClient.convertToType(data['containerStartTime'], 'String');
            }
            if (data.hasOwnProperty('event')) {
                obj['event'] = ApiClient.convertToType(data['event'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], {'String': 'String'});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkerLifecycleEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkerLifecycleEvent</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['containerStartTime'] && !(typeof data['containerStartTime'] === 'string' || data['containerStartTime'] instanceof String)) {
            throw new Error("Expected the field `containerStartTime` to be a primitive type in the JSON string but got " + data['containerStartTime']);
        }
        // ensure the json data is a string
        if (data['event'] && !(typeof data['event'] === 'string' || data['event'] instanceof String)) {
            throw new Error("Expected the field `event` to be a primitive type in the JSON string but got " + data['event']);
        }

        return true;
    }


}



/**
 * The start time of this container. All events will report this so that events can be grouped together across container/VM restarts.
 * @member {String} containerStartTime
 */
WorkerLifecycleEvent.prototype['containerStartTime'] = undefined;

/**
 * The event being reported.
 * @member {module:model/WorkerLifecycleEvent.EventEnum} event
 */
WorkerLifecycleEvent.prototype['event'] = undefined;

/**
 * Other stats that can accompany an event. E.g. { \"downloaded_bytes\" : \"123456\" }
 * @member {Object.<String, String>} metadata
 */
WorkerLifecycleEvent.prototype['metadata'] = undefined;





/**
 * Allowed values for the <code>event</code> property.
 * @enum {String}
 * @readonly
 */
WorkerLifecycleEvent['EventEnum'] = {

    /**
     * value: "UNKNOWN_EVENT"
     * @const
     */
    "UNKNOWN_EVENT": "UNKNOWN_EVENT",

    /**
     * value: "OS_START"
     * @const
     */
    "OS_START": "OS_START",

    /**
     * value: "CONTAINER_START"
     * @const
     */
    "CONTAINER_START": "CONTAINER_START",

    /**
     * value: "NETWORK_UP"
     * @const
     */
    "NETWORK_UP": "NETWORK_UP",

    /**
     * value: "STAGING_FILES_DOWNLOAD_START"
     * @const
     */
    "STAGING_FILES_DOWNLOAD_START": "STAGING_FILES_DOWNLOAD_START",

    /**
     * value: "STAGING_FILES_DOWNLOAD_FINISH"
     * @const
     */
    "STAGING_FILES_DOWNLOAD_FINISH": "STAGING_FILES_DOWNLOAD_FINISH",

    /**
     * value: "SDK_INSTALL_START"
     * @const
     */
    "SDK_INSTALL_START": "SDK_INSTALL_START",

    /**
     * value: "SDK_INSTALL_FINISH"
     * @const
     */
    "SDK_INSTALL_FINISH": "SDK_INSTALL_FINISH"
};



export default WorkerLifecycleEvent;

