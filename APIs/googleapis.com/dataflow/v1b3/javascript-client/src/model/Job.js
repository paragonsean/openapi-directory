/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Environment from './Environment';
import ExecutionStageState from './ExecutionStageState';
import JobExecutionInfo from './JobExecutionInfo';
import JobMetadata from './JobMetadata';
import PipelineDescription from './PipelineDescription';
import RuntimeUpdatableParams from './RuntimeUpdatableParams';
import Step from './Step';

/**
 * The Job model module.
 * @module model/Job
 * @version v1b3
 */
class Job {
    /**
     * Constructs a new <code>Job</code>.
     * Defines a job to be run by the Cloud Dataflow service. Do not enter confidential information when you supply string values using the API.
     * @alias module:model/Job
     */
    constructor() { 
        
        Job.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Job</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Job} obj Optional instance to populate.
     * @return {module:model/Job} The populated <code>Job</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Job();

            if (data.hasOwnProperty('clientRequestId')) {
                obj['clientRequestId'] = ApiClient.convertToType(data['clientRequestId'], 'String');
            }
            if (data.hasOwnProperty('createTime')) {
                obj['createTime'] = ApiClient.convertToType(data['createTime'], 'String');
            }
            if (data.hasOwnProperty('createdFromSnapshotId')) {
                obj['createdFromSnapshotId'] = ApiClient.convertToType(data['createdFromSnapshotId'], 'String');
            }
            if (data.hasOwnProperty('currentState')) {
                obj['currentState'] = ApiClient.convertToType(data['currentState'], 'String');
            }
            if (data.hasOwnProperty('currentStateTime')) {
                obj['currentStateTime'] = ApiClient.convertToType(data['currentStateTime'], 'String');
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = Environment.constructFromObject(data['environment']);
            }
            if (data.hasOwnProperty('executionInfo')) {
                obj['executionInfo'] = JobExecutionInfo.constructFromObject(data['executionInfo']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('jobMetadata')) {
                obj['jobMetadata'] = JobMetadata.constructFromObject(data['jobMetadata']);
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('pipelineDescription')) {
                obj['pipelineDescription'] = PipelineDescription.constructFromObject(data['pipelineDescription']);
            }
            if (data.hasOwnProperty('projectId')) {
                obj['projectId'] = ApiClient.convertToType(data['projectId'], 'String');
            }
            if (data.hasOwnProperty('replaceJobId')) {
                obj['replaceJobId'] = ApiClient.convertToType(data['replaceJobId'], 'String');
            }
            if (data.hasOwnProperty('replacedByJobId')) {
                obj['replacedByJobId'] = ApiClient.convertToType(data['replacedByJobId'], 'String');
            }
            if (data.hasOwnProperty('requestedState')) {
                obj['requestedState'] = ApiClient.convertToType(data['requestedState'], 'String');
            }
            if (data.hasOwnProperty('runtimeUpdatableParams')) {
                obj['runtimeUpdatableParams'] = RuntimeUpdatableParams.constructFromObject(data['runtimeUpdatableParams']);
            }
            if (data.hasOwnProperty('satisfiesPzi')) {
                obj['satisfiesPzi'] = ApiClient.convertToType(data['satisfiesPzi'], 'Boolean');
            }
            if (data.hasOwnProperty('satisfiesPzs')) {
                obj['satisfiesPzs'] = ApiClient.convertToType(data['satisfiesPzs'], 'Boolean');
            }
            if (data.hasOwnProperty('stageStates')) {
                obj['stageStates'] = ApiClient.convertToType(data['stageStates'], [ExecutionStageState]);
            }
            if (data.hasOwnProperty('startTime')) {
                obj['startTime'] = ApiClient.convertToType(data['startTime'], 'String');
            }
            if (data.hasOwnProperty('steps')) {
                obj['steps'] = ApiClient.convertToType(data['steps'], [Step]);
            }
            if (data.hasOwnProperty('stepsLocation')) {
                obj['stepsLocation'] = ApiClient.convertToType(data['stepsLocation'], 'String');
            }
            if (data.hasOwnProperty('tempFiles')) {
                obj['tempFiles'] = ApiClient.convertToType(data['tempFiles'], ['String']);
            }
            if (data.hasOwnProperty('transformNameMapping')) {
                obj['transformNameMapping'] = ApiClient.convertToType(data['transformNameMapping'], {'String': 'String'});
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Job</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Job</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['clientRequestId'] && !(typeof data['clientRequestId'] === 'string' || data['clientRequestId'] instanceof String)) {
            throw new Error("Expected the field `clientRequestId` to be a primitive type in the JSON string but got " + data['clientRequestId']);
        }
        // ensure the json data is a string
        if (data['createTime'] && !(typeof data['createTime'] === 'string' || data['createTime'] instanceof String)) {
            throw new Error("Expected the field `createTime` to be a primitive type in the JSON string but got " + data['createTime']);
        }
        // ensure the json data is a string
        if (data['createdFromSnapshotId'] && !(typeof data['createdFromSnapshotId'] === 'string' || data['createdFromSnapshotId'] instanceof String)) {
            throw new Error("Expected the field `createdFromSnapshotId` to be a primitive type in the JSON string but got " + data['createdFromSnapshotId']);
        }
        // ensure the json data is a string
        if (data['currentState'] && !(typeof data['currentState'] === 'string' || data['currentState'] instanceof String)) {
            throw new Error("Expected the field `currentState` to be a primitive type in the JSON string but got " + data['currentState']);
        }
        // ensure the json data is a string
        if (data['currentStateTime'] && !(typeof data['currentStateTime'] === 'string' || data['currentStateTime'] instanceof String)) {
            throw new Error("Expected the field `currentStateTime` to be a primitive type in the JSON string but got " + data['currentStateTime']);
        }
        // validate the optional field `environment`
        if (data['environment']) { // data not null
          Environment.validateJSON(data['environment']);
        }
        // validate the optional field `executionInfo`
        if (data['executionInfo']) { // data not null
          JobExecutionInfo.validateJSON(data['executionInfo']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `jobMetadata`
        if (data['jobMetadata']) { // data not null
          JobMetadata.validateJSON(data['jobMetadata']);
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `pipelineDescription`
        if (data['pipelineDescription']) { // data not null
          PipelineDescription.validateJSON(data['pipelineDescription']);
        }
        // ensure the json data is a string
        if (data['projectId'] && !(typeof data['projectId'] === 'string' || data['projectId'] instanceof String)) {
            throw new Error("Expected the field `projectId` to be a primitive type in the JSON string but got " + data['projectId']);
        }
        // ensure the json data is a string
        if (data['replaceJobId'] && !(typeof data['replaceJobId'] === 'string' || data['replaceJobId'] instanceof String)) {
            throw new Error("Expected the field `replaceJobId` to be a primitive type in the JSON string but got " + data['replaceJobId']);
        }
        // ensure the json data is a string
        if (data['replacedByJobId'] && !(typeof data['replacedByJobId'] === 'string' || data['replacedByJobId'] instanceof String)) {
            throw new Error("Expected the field `replacedByJobId` to be a primitive type in the JSON string but got " + data['replacedByJobId']);
        }
        // ensure the json data is a string
        if (data['requestedState'] && !(typeof data['requestedState'] === 'string' || data['requestedState'] instanceof String)) {
            throw new Error("Expected the field `requestedState` to be a primitive type in the JSON string but got " + data['requestedState']);
        }
        // validate the optional field `runtimeUpdatableParams`
        if (data['runtimeUpdatableParams']) { // data not null
          RuntimeUpdatableParams.validateJSON(data['runtimeUpdatableParams']);
        }
        if (data['stageStates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stageStates'])) {
                throw new Error("Expected the field `stageStates` to be an array in the JSON data but got " + data['stageStates']);
            }
            // validate the optional field `stageStates` (array)
            for (const item of data['stageStates']) {
                ExecutionStageState.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['startTime'] && !(typeof data['startTime'] === 'string' || data['startTime'] instanceof String)) {
            throw new Error("Expected the field `startTime` to be a primitive type in the JSON string but got " + data['startTime']);
        }
        if (data['steps']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['steps'])) {
                throw new Error("Expected the field `steps` to be an array in the JSON data but got " + data['steps']);
            }
            // validate the optional field `steps` (array)
            for (const item of data['steps']) {
                Step.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['stepsLocation'] && !(typeof data['stepsLocation'] === 'string' || data['stepsLocation'] instanceof String)) {
            throw new Error("Expected the field `stepsLocation` to be a primitive type in the JSON string but got " + data['stepsLocation']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tempFiles'])) {
            throw new Error("Expected the field `tempFiles` to be an array in the JSON data but got " + data['tempFiles']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * The client's unique identifier of the job, re-used across retried attempts. If this field is set, the service will ensure its uniqueness. The request to create a job will fail if the service has knowledge of a previously submitted job with the same client's ID and job name. The caller may use this field to ensure idempotence of job creation across retried attempts to create a job. By default, the field is empty and, in that case, the service ignores it.
 * @member {String} clientRequestId
 */
Job.prototype['clientRequestId'] = undefined;

/**
 * The timestamp when the job was initially created. Immutable and set by the Cloud Dataflow service.
 * @member {String} createTime
 */
Job.prototype['createTime'] = undefined;

/**
 * If this is specified, the job's initial state is populated from the given snapshot.
 * @member {String} createdFromSnapshotId
 */
Job.prototype['createdFromSnapshotId'] = undefined;

/**
 * The current state of the job. Jobs are created in the `JOB_STATE_STOPPED` state unless otherwise specified. A job in the `JOB_STATE_RUNNING` state may asynchronously enter a terminal state. After a job has reached a terminal state, no further state updates may be made. This field might be mutated by the Dataflow service; callers cannot mutate it.
 * @member {module:model/Job.CurrentStateEnum} currentState
 */
Job.prototype['currentState'] = undefined;

/**
 * The timestamp associated with the current state.
 * @member {String} currentStateTime
 */
Job.prototype['currentStateTime'] = undefined;

/**
 * @member {module:model/Environment} environment
 */
Job.prototype['environment'] = undefined;

/**
 * @member {module:model/JobExecutionInfo} executionInfo
 */
Job.prototype['executionInfo'] = undefined;

/**
 * The unique ID of this job. This field is set by the Dataflow service when the job is created, and is immutable for the life of the job.
 * @member {String} id
 */
Job.prototype['id'] = undefined;

/**
 * @member {module:model/JobMetadata} jobMetadata
 */
Job.prototype['jobMetadata'] = undefined;

/**
 * User-defined labels for this job. The labels map can contain no more than 64 entries. Entries of the labels map are UTF8 strings that comply with the following restrictions: * Keys must conform to regexp: \\p{Ll}\\p{Lo}{0,62} * Values must conform to regexp: [\\p{Ll}\\p{Lo}\\p{N}_-]{0,63} * Both keys and values are additionally constrained to be <= 128 bytes in size.
 * @member {Object.<String, String>} labels
 */
Job.prototype['labels'] = undefined;

/**
 * The [regional endpoint] (https://cloud.google.com/dataflow/docs/concepts/regional-endpoints) that contains this job.
 * @member {String} location
 */
Job.prototype['location'] = undefined;

/**
 * The user-specified Dataflow job name. Only one active job with a given name can exist in a project within one region at any given time. Jobs in different regions can have the same name. If a caller attempts to create a job with the same name as an active job that already exists, the attempt returns the existing job. The name must match the regular expression `[a-z]([-a-z0-9]{0,1022}[a-z0-9])?`
 * @member {String} name
 */
Job.prototype['name'] = undefined;

/**
 * @member {module:model/PipelineDescription} pipelineDescription
 */
Job.prototype['pipelineDescription'] = undefined;

/**
 * The ID of the Google Cloud project that the job belongs to.
 * @member {String} projectId
 */
Job.prototype['projectId'] = undefined;

/**
 * If this job is an update of an existing job, this field is the job ID of the job it replaced. When sending a `CreateJobRequest`, you can update a job by specifying it here. The job named here is stopped, and its intermediate state is transferred to this job.
 * @member {String} replaceJobId
 */
Job.prototype['replaceJobId'] = undefined;

/**
 * If another job is an update of this job (and thus, this job is in `JOB_STATE_UPDATED`), this field contains the ID of that job.
 * @member {String} replacedByJobId
 */
Job.prototype['replacedByJobId'] = undefined;

/**
 * The job's requested state. Applies to `UpdateJob` requests. Set `requested_state` with `UpdateJob` requests to switch between the states `JOB_STATE_STOPPED` and `JOB_STATE_RUNNING`. You can also use `UpdateJob` requests to change a job's state from `JOB_STATE_RUNNING` to `JOB_STATE_CANCELLED`, `JOB_STATE_DONE`, or `JOB_STATE_DRAINED`. These states irrevocably terminate the job if it hasn't already reached a terminal state. This field has no effect on `CreateJob` requests.
 * @member {module:model/Job.RequestedStateEnum} requestedState
 */
Job.prototype['requestedState'] = undefined;

/**
 * @member {module:model/RuntimeUpdatableParams} runtimeUpdatableParams
 */
Job.prototype['runtimeUpdatableParams'] = undefined;

/**
 * Output only. Reserved for future use. This field is set only in responses from the server; it is ignored if it is set in any requests.
 * @member {Boolean} satisfiesPzi
 */
Job.prototype['satisfiesPzi'] = undefined;

/**
 * Reserved for future use. This field is set only in responses from the server; it is ignored if it is set in any requests.
 * @member {Boolean} satisfiesPzs
 */
Job.prototype['satisfiesPzs'] = undefined;

/**
 * This field may be mutated by the Cloud Dataflow service; callers cannot mutate it.
 * @member {Array.<module:model/ExecutionStageState>} stageStates
 */
Job.prototype['stageStates'] = undefined;

/**
 * The timestamp when the job was started (transitioned to JOB_STATE_PENDING). Flexible resource scheduling jobs are started with some delay after job creation, so start_time is unset before start and is updated when the job is started by the Cloud Dataflow service. For other jobs, start_time always equals to create_time and is immutable and set by the Cloud Dataflow service.
 * @member {String} startTime
 */
Job.prototype['startTime'] = undefined;

/**
 * Exactly one of step or steps_location should be specified. The top-level steps that constitute the entire job. Only retrieved with JOB_VIEW_ALL.
 * @member {Array.<module:model/Step>} steps
 */
Job.prototype['steps'] = undefined;

/**
 * The Cloud Storage location where the steps are stored.
 * @member {String} stepsLocation
 */
Job.prototype['stepsLocation'] = undefined;

/**
 * A set of files the system should be aware of that are used for temporary storage. These temporary files will be removed on job completion. No duplicates are allowed. No file patterns are supported. The supported files are: Google Cloud Storage: storage.googleapis.com/{bucket}/{object} bucket.storage.googleapis.com/{object}
 * @member {Array.<String>} tempFiles
 */
Job.prototype['tempFiles'] = undefined;

/**
 * The map of transform name prefixes of the job to be replaced to the corresponding name prefixes of the new job.
 * @member {Object.<String, String>} transformNameMapping
 */
Job.prototype['transformNameMapping'] = undefined;

/**
 * The type of Dataflow job.
 * @member {module:model/Job.TypeEnum} type
 */
Job.prototype['type'] = undefined;





/**
 * Allowed values for the <code>currentState</code> property.
 * @enum {String}
 * @readonly
 */
Job['CurrentStateEnum'] = {

    /**
     * value: "JOB_STATE_UNKNOWN"
     * @const
     */
    "UNKNOWN": "JOB_STATE_UNKNOWN",

    /**
     * value: "JOB_STATE_STOPPED"
     * @const
     */
    "STOPPED": "JOB_STATE_STOPPED",

    /**
     * value: "JOB_STATE_RUNNING"
     * @const
     */
    "RUNNING": "JOB_STATE_RUNNING",

    /**
     * value: "JOB_STATE_DONE"
     * @const
     */
    "DONE": "JOB_STATE_DONE",

    /**
     * value: "JOB_STATE_FAILED"
     * @const
     */
    "FAILED": "JOB_STATE_FAILED",

    /**
     * value: "JOB_STATE_CANCELLED"
     * @const
     */
    "CANCELLED": "JOB_STATE_CANCELLED",

    /**
     * value: "JOB_STATE_UPDATED"
     * @const
     */
    "UPDATED": "JOB_STATE_UPDATED",

    /**
     * value: "JOB_STATE_DRAINING"
     * @const
     */
    "DRAINING": "JOB_STATE_DRAINING",

    /**
     * value: "JOB_STATE_DRAINED"
     * @const
     */
    "DRAINED": "JOB_STATE_DRAINED",

    /**
     * value: "JOB_STATE_PENDING"
     * @const
     */
    "PENDING": "JOB_STATE_PENDING",

    /**
     * value: "JOB_STATE_CANCELLING"
     * @const
     */
    "CANCELLING": "JOB_STATE_CANCELLING",

    /**
     * value: "JOB_STATE_QUEUED"
     * @const
     */
    "QUEUED": "JOB_STATE_QUEUED",

    /**
     * value: "JOB_STATE_RESOURCE_CLEANING_UP"
     * @const
     */
    "RESOURCE_CLEANING_UP": "JOB_STATE_RESOURCE_CLEANING_UP"
};


/**
 * Allowed values for the <code>requestedState</code> property.
 * @enum {String}
 * @readonly
 */
Job['RequestedStateEnum'] = {

    /**
     * value: "JOB_STATE_UNKNOWN"
     * @const
     */
    "UNKNOWN": "JOB_STATE_UNKNOWN",

    /**
     * value: "JOB_STATE_STOPPED"
     * @const
     */
    "STOPPED": "JOB_STATE_STOPPED",

    /**
     * value: "JOB_STATE_RUNNING"
     * @const
     */
    "RUNNING": "JOB_STATE_RUNNING",

    /**
     * value: "JOB_STATE_DONE"
     * @const
     */
    "DONE": "JOB_STATE_DONE",

    /**
     * value: "JOB_STATE_FAILED"
     * @const
     */
    "FAILED": "JOB_STATE_FAILED",

    /**
     * value: "JOB_STATE_CANCELLED"
     * @const
     */
    "CANCELLED": "JOB_STATE_CANCELLED",

    /**
     * value: "JOB_STATE_UPDATED"
     * @const
     */
    "UPDATED": "JOB_STATE_UPDATED",

    /**
     * value: "JOB_STATE_DRAINING"
     * @const
     */
    "DRAINING": "JOB_STATE_DRAINING",

    /**
     * value: "JOB_STATE_DRAINED"
     * @const
     */
    "DRAINED": "JOB_STATE_DRAINED",

    /**
     * value: "JOB_STATE_PENDING"
     * @const
     */
    "PENDING": "JOB_STATE_PENDING",

    /**
     * value: "JOB_STATE_CANCELLING"
     * @const
     */
    "CANCELLING": "JOB_STATE_CANCELLING",

    /**
     * value: "JOB_STATE_QUEUED"
     * @const
     */
    "QUEUED": "JOB_STATE_QUEUED",

    /**
     * value: "JOB_STATE_RESOURCE_CLEANING_UP"
     * @const
     */
    "RESOURCE_CLEANING_UP": "JOB_STATE_RESOURCE_CLEANING_UP"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
Job['TypeEnum'] = {

    /**
     * value: "JOB_TYPE_UNKNOWN"
     * @const
     */
    "UNKNOWN": "JOB_TYPE_UNKNOWN",

    /**
     * value: "JOB_TYPE_BATCH"
     * @const
     */
    "BATCH": "JOB_TYPE_BATCH",

    /**
     * value: "JOB_TYPE_STREAMING"
     * @const
     */
    "STREAMING": "JOB_TYPE_STREAMING"
};



export default Job;

