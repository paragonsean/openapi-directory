/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DisplayData from './DisplayData';

/**
 * The TransformSummary model module.
 * @module model/TransformSummary
 * @version v1b3
 */
class TransformSummary {
    /**
     * Constructs a new <code>TransformSummary</code>.
     * Description of the type, names/ids, and input/outputs for a transform.
     * @alias module:model/TransformSummary
     */
    constructor() { 
        
        TransformSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TransformSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransformSummary} obj Optional instance to populate.
     * @return {module:model/TransformSummary} The populated <code>TransformSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransformSummary();

            if (data.hasOwnProperty('displayData')) {
                obj['displayData'] = ApiClient.convertToType(data['displayData'], [DisplayData]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('inputCollectionName')) {
                obj['inputCollectionName'] = ApiClient.convertToType(data['inputCollectionName'], ['String']);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('outputCollectionName')) {
                obj['outputCollectionName'] = ApiClient.convertToType(data['outputCollectionName'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransformSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransformSummary</code>.
     */
    static validateJSON(data) {
        if (data['displayData']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['displayData'])) {
                throw new Error("Expected the field `displayData` to be an array in the JSON data but got " + data['displayData']);
            }
            // validate the optional field `displayData` (array)
            for (const item of data['displayData']) {
                DisplayData.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['inputCollectionName'])) {
            throw new Error("Expected the field `inputCollectionName` to be an array in the JSON data but got " + data['inputCollectionName']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['outputCollectionName'])) {
            throw new Error("Expected the field `outputCollectionName` to be an array in the JSON data but got " + data['outputCollectionName']);
        }

        return true;
    }


}



/**
 * Transform-specific display data.
 * @member {Array.<module:model/DisplayData>} displayData
 */
TransformSummary.prototype['displayData'] = undefined;

/**
 * SDK generated id of this transform instance.
 * @member {String} id
 */
TransformSummary.prototype['id'] = undefined;

/**
 * User names for all collection inputs to this transform.
 * @member {Array.<String>} inputCollectionName
 */
TransformSummary.prototype['inputCollectionName'] = undefined;

/**
 * Type of transform.
 * @member {module:model/TransformSummary.KindEnum} kind
 */
TransformSummary.prototype['kind'] = undefined;

/**
 * User provided name for this transform instance.
 * @member {String} name
 */
TransformSummary.prototype['name'] = undefined;

/**
 * User names for all collection outputs to this transform.
 * @member {Array.<String>} outputCollectionName
 */
TransformSummary.prototype['outputCollectionName'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
TransformSummary['KindEnum'] = {

    /**
     * value: "UNKNOWN_KIND"
     * @const
     */
    "UNKNOWN_KIND": "UNKNOWN_KIND",

    /**
     * value: "PAR_DO_KIND"
     * @const
     */
    "PAR_DO_KIND": "PAR_DO_KIND",

    /**
     * value: "GROUP_BY_KEY_KIND"
     * @const
     */
    "GROUP_BY_KEY_KIND": "GROUP_BY_KEY_KIND",

    /**
     * value: "FLATTEN_KIND"
     * @const
     */
    "FLATTEN_KIND": "FLATTEN_KIND",

    /**
     * value: "READ_KIND"
     * @const
     */
    "READ_KIND": "READ_KIND",

    /**
     * value: "WRITE_KIND"
     * @const
     */
    "WRITE_KIND": "WRITE_KIND",

    /**
     * value: "CONSTANT_KIND"
     * @const
     */
    "CONSTANT_KIND": "CONSTANT_KIND",

    /**
     * value: "SINGLETON_KIND"
     * @const
     */
    "SINGLETON_KIND": "SINGLETON_KIND",

    /**
     * value: "SHUFFLE_KIND"
     * @const
     */
    "SHUFFLE_KIND": "SHUFFLE_KIND"
};



export default TransformSummary;

