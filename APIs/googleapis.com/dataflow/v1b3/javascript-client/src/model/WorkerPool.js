/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AutoscalingSettings from './AutoscalingSettings';
import Disk from './Disk';
import Package from './Package';
import SdkHarnessContainerImage from './SdkHarnessContainerImage';
import TaskRunnerSettings from './TaskRunnerSettings';

/**
 * The WorkerPool model module.
 * @module model/WorkerPool
 * @version v1b3
 */
class WorkerPool {
    /**
     * Constructs a new <code>WorkerPool</code>.
     * Describes one particular pool of Cloud Dataflow workers to be instantiated by the Cloud Dataflow service in order to perform the computations required by a job. Note that a workflow job may use multiple pools, in order to match the various computational requirements of the various stages of the job.
     * @alias module:model/WorkerPool
     */
    constructor() { 
        
        WorkerPool.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WorkerPool</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkerPool} obj Optional instance to populate.
     * @return {module:model/WorkerPool} The populated <code>WorkerPool</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkerPool();

            if (data.hasOwnProperty('autoscalingSettings')) {
                obj['autoscalingSettings'] = AutoscalingSettings.constructFromObject(data['autoscalingSettings']);
            }
            if (data.hasOwnProperty('dataDisks')) {
                obj['dataDisks'] = ApiClient.convertToType(data['dataDisks'], [Disk]);
            }
            if (data.hasOwnProperty('defaultPackageSet')) {
                obj['defaultPackageSet'] = ApiClient.convertToType(data['defaultPackageSet'], 'String');
            }
            if (data.hasOwnProperty('diskSizeGb')) {
                obj['diskSizeGb'] = ApiClient.convertToType(data['diskSizeGb'], 'Number');
            }
            if (data.hasOwnProperty('diskSourceImage')) {
                obj['diskSourceImage'] = ApiClient.convertToType(data['diskSourceImage'], 'String');
            }
            if (data.hasOwnProperty('diskType')) {
                obj['diskType'] = ApiClient.convertToType(data['diskType'], 'String');
            }
            if (data.hasOwnProperty('ipConfiguration')) {
                obj['ipConfiguration'] = ApiClient.convertToType(data['ipConfiguration'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('machineType')) {
                obj['machineType'] = ApiClient.convertToType(data['machineType'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], {'String': 'String'});
            }
            if (data.hasOwnProperty('network')) {
                obj['network'] = ApiClient.convertToType(data['network'], 'String');
            }
            if (data.hasOwnProperty('numThreadsPerWorker')) {
                obj['numThreadsPerWorker'] = ApiClient.convertToType(data['numThreadsPerWorker'], 'Number');
            }
            if (data.hasOwnProperty('numWorkers')) {
                obj['numWorkers'] = ApiClient.convertToType(data['numWorkers'], 'Number');
            }
            if (data.hasOwnProperty('onHostMaintenance')) {
                obj['onHostMaintenance'] = ApiClient.convertToType(data['onHostMaintenance'], 'String');
            }
            if (data.hasOwnProperty('packages')) {
                obj['packages'] = ApiClient.convertToType(data['packages'], [Package]);
            }
            if (data.hasOwnProperty('poolArgs')) {
                obj['poolArgs'] = ApiClient.convertToType(data['poolArgs'], {'String': Object});
            }
            if (data.hasOwnProperty('sdkHarnessContainerImages')) {
                obj['sdkHarnessContainerImages'] = ApiClient.convertToType(data['sdkHarnessContainerImages'], [SdkHarnessContainerImage]);
            }
            if (data.hasOwnProperty('subnetwork')) {
                obj['subnetwork'] = ApiClient.convertToType(data['subnetwork'], 'String');
            }
            if (data.hasOwnProperty('taskrunnerSettings')) {
                obj['taskrunnerSettings'] = TaskRunnerSettings.constructFromObject(data['taskrunnerSettings']);
            }
            if (data.hasOwnProperty('teardownPolicy')) {
                obj['teardownPolicy'] = ApiClient.convertToType(data['teardownPolicy'], 'String');
            }
            if (data.hasOwnProperty('workerHarnessContainerImage')) {
                obj['workerHarnessContainerImage'] = ApiClient.convertToType(data['workerHarnessContainerImage'], 'String');
            }
            if (data.hasOwnProperty('zone')) {
                obj['zone'] = ApiClient.convertToType(data['zone'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkerPool</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkerPool</code>.
     */
    static validateJSON(data) {
        // validate the optional field `autoscalingSettings`
        if (data['autoscalingSettings']) { // data not null
          AutoscalingSettings.validateJSON(data['autoscalingSettings']);
        }
        if (data['dataDisks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dataDisks'])) {
                throw new Error("Expected the field `dataDisks` to be an array in the JSON data but got " + data['dataDisks']);
            }
            // validate the optional field `dataDisks` (array)
            for (const item of data['dataDisks']) {
                Disk.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['defaultPackageSet'] && !(typeof data['defaultPackageSet'] === 'string' || data['defaultPackageSet'] instanceof String)) {
            throw new Error("Expected the field `defaultPackageSet` to be a primitive type in the JSON string but got " + data['defaultPackageSet']);
        }
        // ensure the json data is a string
        if (data['diskSourceImage'] && !(typeof data['diskSourceImage'] === 'string' || data['diskSourceImage'] instanceof String)) {
            throw new Error("Expected the field `diskSourceImage` to be a primitive type in the JSON string but got " + data['diskSourceImage']);
        }
        // ensure the json data is a string
        if (data['diskType'] && !(typeof data['diskType'] === 'string' || data['diskType'] instanceof String)) {
            throw new Error("Expected the field `diskType` to be a primitive type in the JSON string but got " + data['diskType']);
        }
        // ensure the json data is a string
        if (data['ipConfiguration'] && !(typeof data['ipConfiguration'] === 'string' || data['ipConfiguration'] instanceof String)) {
            throw new Error("Expected the field `ipConfiguration` to be a primitive type in the JSON string but got " + data['ipConfiguration']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['machineType'] && !(typeof data['machineType'] === 'string' || data['machineType'] instanceof String)) {
            throw new Error("Expected the field `machineType` to be a primitive type in the JSON string but got " + data['machineType']);
        }
        // ensure the json data is a string
        if (data['network'] && !(typeof data['network'] === 'string' || data['network'] instanceof String)) {
            throw new Error("Expected the field `network` to be a primitive type in the JSON string but got " + data['network']);
        }
        // ensure the json data is a string
        if (data['onHostMaintenance'] && !(typeof data['onHostMaintenance'] === 'string' || data['onHostMaintenance'] instanceof String)) {
            throw new Error("Expected the field `onHostMaintenance` to be a primitive type in the JSON string but got " + data['onHostMaintenance']);
        }
        if (data['packages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['packages'])) {
                throw new Error("Expected the field `packages` to be an array in the JSON data but got " + data['packages']);
            }
            // validate the optional field `packages` (array)
            for (const item of data['packages']) {
                Package.validateJSON(item);
            };
        }
        if (data['sdkHarnessContainerImages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sdkHarnessContainerImages'])) {
                throw new Error("Expected the field `sdkHarnessContainerImages` to be an array in the JSON data but got " + data['sdkHarnessContainerImages']);
            }
            // validate the optional field `sdkHarnessContainerImages` (array)
            for (const item of data['sdkHarnessContainerImages']) {
                SdkHarnessContainerImage.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['subnetwork'] && !(typeof data['subnetwork'] === 'string' || data['subnetwork'] instanceof String)) {
            throw new Error("Expected the field `subnetwork` to be a primitive type in the JSON string but got " + data['subnetwork']);
        }
        // validate the optional field `taskrunnerSettings`
        if (data['taskrunnerSettings']) { // data not null
          TaskRunnerSettings.validateJSON(data['taskrunnerSettings']);
        }
        // ensure the json data is a string
        if (data['teardownPolicy'] && !(typeof data['teardownPolicy'] === 'string' || data['teardownPolicy'] instanceof String)) {
            throw new Error("Expected the field `teardownPolicy` to be a primitive type in the JSON string but got " + data['teardownPolicy']);
        }
        // ensure the json data is a string
        if (data['workerHarnessContainerImage'] && !(typeof data['workerHarnessContainerImage'] === 'string' || data['workerHarnessContainerImage'] instanceof String)) {
            throw new Error("Expected the field `workerHarnessContainerImage` to be a primitive type in the JSON string but got " + data['workerHarnessContainerImage']);
        }
        // ensure the json data is a string
        if (data['zone'] && !(typeof data['zone'] === 'string' || data['zone'] instanceof String)) {
            throw new Error("Expected the field `zone` to be a primitive type in the JSON string but got " + data['zone']);
        }

        return true;
    }


}



/**
 * @member {module:model/AutoscalingSettings} autoscalingSettings
 */
WorkerPool.prototype['autoscalingSettings'] = undefined;

/**
 * Data disks that are used by a VM in this workflow.
 * @member {Array.<module:model/Disk>} dataDisks
 */
WorkerPool.prototype['dataDisks'] = undefined;

/**
 * The default package set to install. This allows the service to select a default set of packages which are useful to worker harnesses written in a particular language.
 * @member {module:model/WorkerPool.DefaultPackageSetEnum} defaultPackageSet
 */
WorkerPool.prototype['defaultPackageSet'] = undefined;

/**
 * Size of root disk for VMs, in GB. If zero or unspecified, the service will attempt to choose a reasonable default.
 * @member {Number} diskSizeGb
 */
WorkerPool.prototype['diskSizeGb'] = undefined;

/**
 * Fully qualified source image for disks.
 * @member {String} diskSourceImage
 */
WorkerPool.prototype['diskSourceImage'] = undefined;

/**
 * Type of root disk for VMs. If empty or unspecified, the service will attempt to choose a reasonable default.
 * @member {String} diskType
 */
WorkerPool.prototype['diskType'] = undefined;

/**
 * Configuration for VM IPs.
 * @member {module:model/WorkerPool.IpConfigurationEnum} ipConfiguration
 */
WorkerPool.prototype['ipConfiguration'] = undefined;

/**
 * The kind of the worker pool; currently only `harness` and `shuffle` are supported.
 * @member {String} kind
 */
WorkerPool.prototype['kind'] = undefined;

/**
 * Machine type (e.g. \"n1-standard-1\"). If empty or unspecified, the service will attempt to choose a reasonable default.
 * @member {String} machineType
 */
WorkerPool.prototype['machineType'] = undefined;

/**
 * Metadata to set on the Google Compute Engine VMs.
 * @member {Object.<String, String>} metadata
 */
WorkerPool.prototype['metadata'] = undefined;

/**
 * Network to which VMs will be assigned. If empty or unspecified, the service will use the network \"default\".
 * @member {String} network
 */
WorkerPool.prototype['network'] = undefined;

/**
 * The number of threads per worker harness. If empty or unspecified, the service will choose a number of threads (according to the number of cores on the selected machine type for batch, or 1 by convention for streaming).
 * @member {Number} numThreadsPerWorker
 */
WorkerPool.prototype['numThreadsPerWorker'] = undefined;

/**
 * Number of Google Compute Engine workers in this pool needed to execute the job. If zero or unspecified, the service will attempt to choose a reasonable default.
 * @member {Number} numWorkers
 */
WorkerPool.prototype['numWorkers'] = undefined;

/**
 * The action to take on host maintenance, as defined by the Google Compute Engine API.
 * @member {String} onHostMaintenance
 */
WorkerPool.prototype['onHostMaintenance'] = undefined;

/**
 * Packages to be installed on workers.
 * @member {Array.<module:model/Package>} packages
 */
WorkerPool.prototype['packages'] = undefined;

/**
 * Extra arguments for this worker pool.
 * @member {Object.<String, Object>} poolArgs
 */
WorkerPool.prototype['poolArgs'] = undefined;

/**
 * Set of SDK harness containers needed to execute this pipeline. This will only be set in the Fn API path. For non-cross-language pipelines this should have only one entry. Cross-language pipelines will have two or more entries.
 * @member {Array.<module:model/SdkHarnessContainerImage>} sdkHarnessContainerImages
 */
WorkerPool.prototype['sdkHarnessContainerImages'] = undefined;

/**
 * Subnetwork to which VMs will be assigned, if desired. Expected to be of the form \"regions/REGION/subnetworks/SUBNETWORK\".
 * @member {String} subnetwork
 */
WorkerPool.prototype['subnetwork'] = undefined;

/**
 * @member {module:model/TaskRunnerSettings} taskrunnerSettings
 */
WorkerPool.prototype['taskrunnerSettings'] = undefined;

/**
 * Sets the policy for determining when to turndown worker pool. Allowed values are: `TEARDOWN_ALWAYS`, `TEARDOWN_ON_SUCCESS`, and `TEARDOWN_NEVER`. `TEARDOWN_ALWAYS` means workers are always torn down regardless of whether the job succeeds. `TEARDOWN_ON_SUCCESS` means workers are torn down if the job succeeds. `TEARDOWN_NEVER` means the workers are never torn down. If the workers are not torn down by the service, they will continue to run and use Google Compute Engine VM resources in the user's project until they are explicitly terminated by the user. Because of this, Google recommends using the `TEARDOWN_ALWAYS` policy except for small, manually supervised test jobs. If unknown or unspecified, the service will attempt to choose a reasonable default.
 * @member {module:model/WorkerPool.TeardownPolicyEnum} teardownPolicy
 */
WorkerPool.prototype['teardownPolicy'] = undefined;

/**
 * Required. Docker container image that executes the Cloud Dataflow worker harness, residing in Google Container Registry. Deprecated for the Fn API path. Use sdk_harness_container_images instead.
 * @member {String} workerHarnessContainerImage
 */
WorkerPool.prototype['workerHarnessContainerImage'] = undefined;

/**
 * Zone to run the worker pools in. If empty or unspecified, the service will attempt to choose a reasonable default.
 * @member {String} zone
 */
WorkerPool.prototype['zone'] = undefined;





/**
 * Allowed values for the <code>defaultPackageSet</code> property.
 * @enum {String}
 * @readonly
 */
WorkerPool['DefaultPackageSetEnum'] = {

    /**
     * value: "DEFAULT_PACKAGE_SET_UNKNOWN"
     * @const
     */
    "UNKNOWN": "DEFAULT_PACKAGE_SET_UNKNOWN",

    /**
     * value: "DEFAULT_PACKAGE_SET_NONE"
     * @const
     */
    "NONE": "DEFAULT_PACKAGE_SET_NONE",

    /**
     * value: "DEFAULT_PACKAGE_SET_JAVA"
     * @const
     */
    "JAVA": "DEFAULT_PACKAGE_SET_JAVA",

    /**
     * value: "DEFAULT_PACKAGE_SET_PYTHON"
     * @const
     */
    "PYTHON": "DEFAULT_PACKAGE_SET_PYTHON"
};


/**
 * Allowed values for the <code>ipConfiguration</code> property.
 * @enum {String}
 * @readonly
 */
WorkerPool['IpConfigurationEnum'] = {

    /**
     * value: "WORKER_IP_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "WORKER_IP_UNSPECIFIED",

    /**
     * value: "WORKER_IP_PUBLIC"
     * @const
     */
    "PUBLIC": "WORKER_IP_PUBLIC",

    /**
     * value: "WORKER_IP_PRIVATE"
     * @const
     */
    "PRIVATE": "WORKER_IP_PRIVATE"
};


/**
 * Allowed values for the <code>teardownPolicy</code> property.
 * @enum {String}
 * @readonly
 */
WorkerPool['TeardownPolicyEnum'] = {

    /**
     * value: "TEARDOWN_POLICY_UNKNOWN"
     * @const
     */
    "POLICY_UNKNOWN": "TEARDOWN_POLICY_UNKNOWN",

    /**
     * value: "TEARDOWN_ALWAYS"
     * @const
     */
    "ALWAYS": "TEARDOWN_ALWAYS",

    /**
     * value: "TEARDOWN_ON_SUCCESS"
     * @const
     */
    "ON_SUCCESS": "TEARDOWN_ON_SUCCESS",

    /**
     * value: "TEARDOWN_NEVER"
     * @const
     */
    "NEVER": "TEARDOWN_NEVER"
};



export default WorkerPool;

