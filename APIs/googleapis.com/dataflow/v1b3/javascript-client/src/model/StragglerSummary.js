/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Straggler from './Straggler';

/**
 * The StragglerSummary model module.
 * @module model/StragglerSummary
 * @version v1b3
 */
class StragglerSummary {
    /**
     * Constructs a new <code>StragglerSummary</code>.
     * Summarized straggler identification details.
     * @alias module:model/StragglerSummary
     */
    constructor() { 
        
        StragglerSummary.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>StragglerSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StragglerSummary} obj Optional instance to populate.
     * @return {module:model/StragglerSummary} The populated <code>StragglerSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StragglerSummary();

            if (data.hasOwnProperty('recentStragglers')) {
                obj['recentStragglers'] = ApiClient.convertToType(data['recentStragglers'], [Straggler]);
            }
            if (data.hasOwnProperty('stragglerCauseCount')) {
                obj['stragglerCauseCount'] = ApiClient.convertToType(data['stragglerCauseCount'], {'String': 'String'});
            }
            if (data.hasOwnProperty('totalStragglerCount')) {
                obj['totalStragglerCount'] = ApiClient.convertToType(data['totalStragglerCount'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StragglerSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StragglerSummary</code>.
     */
    static validateJSON(data) {
        if (data['recentStragglers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['recentStragglers'])) {
                throw new Error("Expected the field `recentStragglers` to be an array in the JSON data but got " + data['recentStragglers']);
            }
            // validate the optional field `recentStragglers` (array)
            for (const item of data['recentStragglers']) {
                Straggler.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['totalStragglerCount'] && !(typeof data['totalStragglerCount'] === 'string' || data['totalStragglerCount'] instanceof String)) {
            throw new Error("Expected the field `totalStragglerCount` to be a primitive type in the JSON string but got " + data['totalStragglerCount']);
        }

        return true;
    }


}



/**
 * The most recent stragglers.
 * @member {Array.<module:model/Straggler>} recentStragglers
 */
StragglerSummary.prototype['recentStragglers'] = undefined;

/**
 * Aggregated counts of straggler causes, keyed by the string representation of the StragglerCause enum.
 * @member {Object.<String, String>} stragglerCauseCount
 */
StragglerSummary.prototype['stragglerCauseCount'] = undefined;

/**
 * The total count of stragglers.
 * @member {String} totalStragglerCount
 */
StragglerSummary.prototype['totalStragglerCount'] = undefined;






export default StragglerSummary;

