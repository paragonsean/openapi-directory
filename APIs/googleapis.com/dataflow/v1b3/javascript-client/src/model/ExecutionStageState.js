/**
 * Dataflow API
 * Manages Google Cloud Dataflow projects on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1b3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExecutionStageState model module.
 * @module model/ExecutionStageState
 * @version v1b3
 */
class ExecutionStageState {
    /**
     * Constructs a new <code>ExecutionStageState</code>.
     * A message describing the state of a particular execution stage.
     * @alias module:model/ExecutionStageState
     */
    constructor() { 
        
        ExecutionStageState.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExecutionStageState</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExecutionStageState} obj Optional instance to populate.
     * @return {module:model/ExecutionStageState} The populated <code>ExecutionStageState</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExecutionStageState();

            if (data.hasOwnProperty('currentStateTime')) {
                obj['currentStateTime'] = ApiClient.convertToType(data['currentStateTime'], 'String');
            }
            if (data.hasOwnProperty('executionStageName')) {
                obj['executionStageName'] = ApiClient.convertToType(data['executionStageName'], 'String');
            }
            if (data.hasOwnProperty('executionStageState')) {
                obj['executionStageState'] = ApiClient.convertToType(data['executionStageState'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExecutionStageState</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExecutionStageState</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['currentStateTime'] && !(typeof data['currentStateTime'] === 'string' || data['currentStateTime'] instanceof String)) {
            throw new Error("Expected the field `currentStateTime` to be a primitive type in the JSON string but got " + data['currentStateTime']);
        }
        // ensure the json data is a string
        if (data['executionStageName'] && !(typeof data['executionStageName'] === 'string' || data['executionStageName'] instanceof String)) {
            throw new Error("Expected the field `executionStageName` to be a primitive type in the JSON string but got " + data['executionStageName']);
        }
        // ensure the json data is a string
        if (data['executionStageState'] && !(typeof data['executionStageState'] === 'string' || data['executionStageState'] instanceof String)) {
            throw new Error("Expected the field `executionStageState` to be a primitive type in the JSON string but got " + data['executionStageState']);
        }

        return true;
    }


}



/**
 * The time at which the stage transitioned to this state.
 * @member {String} currentStateTime
 */
ExecutionStageState.prototype['currentStateTime'] = undefined;

/**
 * The name of the execution stage.
 * @member {String} executionStageName
 */
ExecutionStageState.prototype['executionStageName'] = undefined;

/**
 * Executions stage states allow the same set of values as JobState.
 * @member {module:model/ExecutionStageState.ExecutionStageStateEnum} executionStageState
 */
ExecutionStageState.prototype['executionStageState'] = undefined;





/**
 * Allowed values for the <code>executionStageState</code> property.
 * @enum {String}
 * @readonly
 */
ExecutionStageState['ExecutionStageStateEnum'] = {

    /**
     * value: "JOB_STATE_UNKNOWN"
     * @const
     */
    "UNKNOWN": "JOB_STATE_UNKNOWN",

    /**
     * value: "JOB_STATE_STOPPED"
     * @const
     */
    "STOPPED": "JOB_STATE_STOPPED",

    /**
     * value: "JOB_STATE_RUNNING"
     * @const
     */
    "RUNNING": "JOB_STATE_RUNNING",

    /**
     * value: "JOB_STATE_DONE"
     * @const
     */
    "DONE": "JOB_STATE_DONE",

    /**
     * value: "JOB_STATE_FAILED"
     * @const
     */
    "FAILED": "JOB_STATE_FAILED",

    /**
     * value: "JOB_STATE_CANCELLED"
     * @const
     */
    "CANCELLED": "JOB_STATE_CANCELLED",

    /**
     * value: "JOB_STATE_UPDATED"
     * @const
     */
    "UPDATED": "JOB_STATE_UPDATED",

    /**
     * value: "JOB_STATE_DRAINING"
     * @const
     */
    "DRAINING": "JOB_STATE_DRAINING",

    /**
     * value: "JOB_STATE_DRAINED"
     * @const
     */
    "DRAINED": "JOB_STATE_DRAINED",

    /**
     * value: "JOB_STATE_PENDING"
     * @const
     */
    "PENDING": "JOB_STATE_PENDING",

    /**
     * value: "JOB_STATE_CANCELLING"
     * @const
     */
    "CANCELLING": "JOB_STATE_CANCELLING",

    /**
     * value: "JOB_STATE_QUEUED"
     * @const
     */
    "QUEUED": "JOB_STATE_QUEUED",

    /**
     * value: "JOB_STATE_RESOURCE_CLEANING_UP"
     * @const
     */
    "RESOURCE_CLEANING_UP": "JOB_STATE_RESOURCE_CLEANING_UP"
};



export default ExecutionStageState;

