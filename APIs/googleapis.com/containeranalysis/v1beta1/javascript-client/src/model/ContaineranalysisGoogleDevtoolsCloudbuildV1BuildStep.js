/**
 * Container Analysis API
 * This API is a prerequisite for leveraging Artifact Analysis scanning capabilities in both Artifact Registry and with Advanced Vulnerability Insights (runtime scanning) in GKE. In addition, the Container Analysis API is an implementation of the Grafeas API, which enables storing, querying, and retrieval of critical metadata about all of your software artifacts.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan from './ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan';
import ContaineranalysisGoogleDevtoolsCloudbuildV1Volume from './ContaineranalysisGoogleDevtoolsCloudbuildV1Volume';

/**
 * The ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep model module.
 * @module model/ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep
 * @version v1beta1
 */
class ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep {
    /**
     * Constructs a new <code>ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep</code>.
     * A step in the build pipeline.
     * @alias module:model/ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep
     */
    constructor() { 
        
        ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep} obj Optional instance to populate.
     * @return {module:model/ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep} The populated <code>ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep();

            if (data.hasOwnProperty('allowExitCodes')) {
                obj['allowExitCodes'] = ApiClient.convertToType(data['allowExitCodes'], ['Number']);
            }
            if (data.hasOwnProperty('allowFailure')) {
                obj['allowFailure'] = ApiClient.convertToType(data['allowFailure'], 'Boolean');
            }
            if (data.hasOwnProperty('args')) {
                obj['args'] = ApiClient.convertToType(data['args'], ['String']);
            }
            if (data.hasOwnProperty('automapSubstitutions')) {
                obj['automapSubstitutions'] = ApiClient.convertToType(data['automapSubstitutions'], 'Boolean');
            }
            if (data.hasOwnProperty('dir')) {
                obj['dir'] = ApiClient.convertToType(data['dir'], 'String');
            }
            if (data.hasOwnProperty('entrypoint')) {
                obj['entrypoint'] = ApiClient.convertToType(data['entrypoint'], 'String');
            }
            if (data.hasOwnProperty('env')) {
                obj['env'] = ApiClient.convertToType(data['env'], ['String']);
            }
            if (data.hasOwnProperty('exitCode')) {
                obj['exitCode'] = ApiClient.convertToType(data['exitCode'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('pullTiming')) {
                obj['pullTiming'] = ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan.constructFromObject(data['pullTiming']);
            }
            if (data.hasOwnProperty('script')) {
                obj['script'] = ApiClient.convertToType(data['script'], 'String');
            }
            if (data.hasOwnProperty('secretEnv')) {
                obj['secretEnv'] = ApiClient.convertToType(data['secretEnv'], ['String']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('timeout')) {
                obj['timeout'] = ApiClient.convertToType(data['timeout'], 'String');
            }
            if (data.hasOwnProperty('timing')) {
                obj['timing'] = ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan.constructFromObject(data['timing']);
            }
            if (data.hasOwnProperty('volumes')) {
                obj['volumes'] = ApiClient.convertToType(data['volumes'], [ContaineranalysisGoogleDevtoolsCloudbuildV1Volume]);
            }
            if (data.hasOwnProperty('waitFor')) {
                obj['waitFor'] = ApiClient.convertToType(data['waitFor'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['allowExitCodes'])) {
            throw new Error("Expected the field `allowExitCodes` to be an array in the JSON data but got " + data['allowExitCodes']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['args'])) {
            throw new Error("Expected the field `args` to be an array in the JSON data but got " + data['args']);
        }
        // ensure the json data is a string
        if (data['dir'] && !(typeof data['dir'] === 'string' || data['dir'] instanceof String)) {
            throw new Error("Expected the field `dir` to be a primitive type in the JSON string but got " + data['dir']);
        }
        // ensure the json data is a string
        if (data['entrypoint'] && !(typeof data['entrypoint'] === 'string' || data['entrypoint'] instanceof String)) {
            throw new Error("Expected the field `entrypoint` to be a primitive type in the JSON string but got " + data['entrypoint']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['env'])) {
            throw new Error("Expected the field `env` to be an array in the JSON data but got " + data['env']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `pullTiming`
        if (data['pullTiming']) { // data not null
          ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan.validateJSON(data['pullTiming']);
        }
        // ensure the json data is a string
        if (data['script'] && !(typeof data['script'] === 'string' || data['script'] instanceof String)) {
            throw new Error("Expected the field `script` to be a primitive type in the JSON string but got " + data['script']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['secretEnv'])) {
            throw new Error("Expected the field `secretEnv` to be an array in the JSON data but got " + data['secretEnv']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['timeout'] && !(typeof data['timeout'] === 'string' || data['timeout'] instanceof String)) {
            throw new Error("Expected the field `timeout` to be a primitive type in the JSON string but got " + data['timeout']);
        }
        // validate the optional field `timing`
        if (data['timing']) { // data not null
          ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan.validateJSON(data['timing']);
        }
        if (data['volumes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['volumes'])) {
                throw new Error("Expected the field `volumes` to be an array in the JSON data but got " + data['volumes']);
            }
            // validate the optional field `volumes` (array)
            for (const item of data['volumes']) {
                ContaineranalysisGoogleDevtoolsCloudbuildV1Volume.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['waitFor'])) {
            throw new Error("Expected the field `waitFor` to be an array in the JSON data but got " + data['waitFor']);
        }

        return true;
    }


}



/**
 * Allow this build step to fail without failing the entire build if and only if the exit code is one of the specified codes. If allow_failure is also specified, this field will take precedence.
 * @member {Array.<Number>} allowExitCodes
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['allowExitCodes'] = undefined;

/**
 * Allow this build step to fail without failing the entire build. If false, the entire build will fail if this step fails. Otherwise, the build will succeed, but this step will still have a failure status. Error information will be reported in the failure_detail field.
 * @member {Boolean} allowFailure
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['allowFailure'] = undefined;

/**
 * A list of arguments that will be presented to the step when it is started. If the image used to run the step's container has an entrypoint, the `args` are used as arguments to that entrypoint. If the image does not define an entrypoint, the first element in args is used as the entrypoint, and the remainder will be used as arguments.
 * @member {Array.<String>} args
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['args'] = undefined;

/**
 * Option to include built-in and custom substitutions as env variables for this build step. This option will override the global option in BuildOption.
 * @member {Boolean} automapSubstitutions
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['automapSubstitutions'] = undefined;

/**
 * Working directory to use when running this step's container. If this value is a relative path, it is relative to the build's working directory. If this value is absolute, it may be outside the build's working directory, in which case the contents of the path may not be persisted across build step executions, unless a `volume` for that path is specified. If the build specifies a `RepoSource` with `dir` and a step with a `dir`, which specifies an absolute path, the `RepoSource` `dir` is ignored for the step's execution.
 * @member {String} dir
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['dir'] = undefined;

/**
 * Entrypoint to be used instead of the build step image's default entrypoint. If unset, the image's default entrypoint is used.
 * @member {String} entrypoint
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['entrypoint'] = undefined;

/**
 * A list of environment variable definitions to be used when running a step. The elements are of the form \"KEY=VALUE\" for the environment variable \"KEY\" being given the value \"VALUE\".
 * @member {Array.<String>} env
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['env'] = undefined;

/**
 * Output only. Return code from running the step.
 * @member {Number} exitCode
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['exitCode'] = undefined;

/**
 * Unique identifier for this build step, used in `wait_for` to reference this build step as a dependency.
 * @member {String} id
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['id'] = undefined;

/**
 * Required. The name of the container image that will run this particular build step. If the image is available in the host's Docker daemon's cache, it will be run directly. If not, the host will attempt to pull the image first, using the builder service account's credentials if necessary. The Docker daemon's cache will already have the latest versions of all of the officially supported build steps ([https://github.com/GoogleCloudPlatform/cloud-builders](https://github.com/GoogleCloudPlatform/cloud-builders)). The Docker daemon will also have cached many of the layers for some popular images, like \"ubuntu\", \"debian\", but they will be refreshed at the time you attempt to use them. If you built an image in a previous build step, it will be stored in the host's Docker daemon's cache and is available to use as the name for a later build step.
 * @member {String} name
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['name'] = undefined;

/**
 * @member {module:model/ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan} pullTiming
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['pullTiming'] = undefined;

/**
 * A shell script to be executed in the step. When script is provided, the user cannot specify the entrypoint or args.
 * @member {String} script
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['script'] = undefined;

/**
 * A list of environment variables which are encrypted using a Cloud Key Management Service crypto key. These values must be specified in the build's `Secret`.
 * @member {Array.<String>} secretEnv
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['secretEnv'] = undefined;

/**
 * Output only. Status of the build step. At this time, build step status is only updated on build completion; step status is not updated in real-time as the build progresses.
 * @member {module:model/ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.StatusEnum} status
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['status'] = undefined;

/**
 * Time limit for executing this build step. If not defined, the step has no time limit and will be allowed to continue to run until either it completes or the build itself times out.
 * @member {String} timeout
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['timeout'] = undefined;

/**
 * @member {module:model/ContaineranalysisGoogleDevtoolsCloudbuildV1TimeSpan} timing
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['timing'] = undefined;

/**
 * List of volumes to mount into the build step. Each volume is created as an empty volume prior to execution of the build step. Upon completion of the build, volumes and their contents are discarded. Using a named volume in only one step is not valid as it is indicative of a build request with an incorrect configuration.
 * @member {Array.<module:model/ContaineranalysisGoogleDevtoolsCloudbuildV1Volume>} volumes
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['volumes'] = undefined;

/**
 * The ID(s) of the step(s) that this build step depends on. This build step will not start until all the build steps in `wait_for` have completed successfully. If `wait_for` is empty, this build step will start when all previous build steps in the `Build.Steps` list have completed successfully.
 * @member {Array.<String>} waitFor
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep.prototype['waitFor'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep['StatusEnum'] = {

    /**
     * value: "STATUS_UNKNOWN"
     * @const
     */
    "STATUS_UNKNOWN": "STATUS_UNKNOWN",

    /**
     * value: "PENDING"
     * @const
     */
    "PENDING": "PENDING",

    /**
     * value: "QUEUED"
     * @const
     */
    "QUEUED": "QUEUED",

    /**
     * value: "WORKING"
     * @const
     */
    "WORKING": "WORKING",

    /**
     * value: "SUCCESS"
     * @const
     */
    "SUCCESS": "SUCCESS",

    /**
     * value: "FAILURE"
     * @const
     */
    "FAILURE": "FAILURE",

    /**
     * value: "INTERNAL_ERROR"
     * @const
     */
    "INTERNAL_ERROR": "INTERNAL_ERROR",

    /**
     * value: "TIMEOUT"
     * @const
     */
    "TIMEOUT": "TIMEOUT",

    /**
     * value: "CANCELLED"
     * @const
     */
    "CANCELLED": "CANCELLED",

    /**
     * value: "EXPIRED"
     * @const
     */
    "EXPIRED": "EXPIRED"
};



export default ContaineranalysisGoogleDevtoolsCloudbuildV1BuildStep;

