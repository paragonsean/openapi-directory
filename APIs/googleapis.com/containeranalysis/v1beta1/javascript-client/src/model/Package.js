/**
 * Container Analysis API
 * This API is a prerequisite for leveraging Artifact Analysis scanning capabilities in both Artifact Registry and with Advanced Vulnerability Insights (runtime scanning) in GKE. In addition, the Container Analysis API is an implementation of the Grafeas API, which enables storing, querying, and retrieval of critical metadata about all of your software artifacts.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Digest from './Digest';
import Distribution from './Distribution';
import License from './License';
import Version from './Version';

/**
 * The Package model module.
 * @module model/Package
 * @version v1beta1
 */
class Package {
    /**
     * Constructs a new <code>Package</code>.
     * Package represents a particular package version.
     * @alias module:model/Package
     */
    constructor() { 
        
        Package.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Package</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Package} obj Optional instance to populate.
     * @return {module:model/Package} The populated <code>Package</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Package();

            if (data.hasOwnProperty('architecture')) {
                obj['architecture'] = ApiClient.convertToType(data['architecture'], 'String');
            }
            if (data.hasOwnProperty('cpeUri')) {
                obj['cpeUri'] = ApiClient.convertToType(data['cpeUri'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('digest')) {
                obj['digest'] = ApiClient.convertToType(data['digest'], [Digest]);
            }
            if (data.hasOwnProperty('distribution')) {
                obj['distribution'] = ApiClient.convertToType(data['distribution'], [Distribution]);
            }
            if (data.hasOwnProperty('license')) {
                obj['license'] = License.constructFromObject(data['license']);
            }
            if (data.hasOwnProperty('maintainer')) {
                obj['maintainer'] = ApiClient.convertToType(data['maintainer'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('packageType')) {
                obj['packageType'] = ApiClient.convertToType(data['packageType'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = Version.constructFromObject(data['version']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Package</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Package</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['architecture'] && !(typeof data['architecture'] === 'string' || data['architecture'] instanceof String)) {
            throw new Error("Expected the field `architecture` to be a primitive type in the JSON string but got " + data['architecture']);
        }
        // ensure the json data is a string
        if (data['cpeUri'] && !(typeof data['cpeUri'] === 'string' || data['cpeUri'] instanceof String)) {
            throw new Error("Expected the field `cpeUri` to be a primitive type in the JSON string but got " + data['cpeUri']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['digest']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['digest'])) {
                throw new Error("Expected the field `digest` to be an array in the JSON data but got " + data['digest']);
            }
            // validate the optional field `digest` (array)
            for (const item of data['digest']) {
                Digest.validateJSON(item);
            };
        }
        if (data['distribution']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['distribution'])) {
                throw new Error("Expected the field `distribution` to be an array in the JSON data but got " + data['distribution']);
            }
            // validate the optional field `distribution` (array)
            for (const item of data['distribution']) {
                Distribution.validateJSON(item);
            };
        }
        // validate the optional field `license`
        if (data['license']) { // data not null
          License.validateJSON(data['license']);
        }
        // ensure the json data is a string
        if (data['maintainer'] && !(typeof data['maintainer'] === 'string' || data['maintainer'] instanceof String)) {
            throw new Error("Expected the field `maintainer` to be a primitive type in the JSON string but got " + data['maintainer']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['packageType'] && !(typeof data['packageType'] === 'string' || data['packageType'] instanceof String)) {
            throw new Error("Expected the field `packageType` to be a primitive type in the JSON string but got " + data['packageType']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // validate the optional field `version`
        if (data['version']) { // data not null
          Version.validateJSON(data['version']);
        }

        return true;
    }


}



/**
 * The CPU architecture for which packages in this distribution channel were built. Architecture will be blank for language packages.
 * @member {module:model/Package.ArchitectureEnum} architecture
 */
Package.prototype['architecture'] = undefined;

/**
 * The cpe_uri in [CPE format](https://cpe.mitre.org/specification/) denoting the package manager version distributing a package. The cpe_uri will be blank for language packages.
 * @member {String} cpeUri
 */
Package.prototype['cpeUri'] = undefined;

/**
 * The description of this package.
 * @member {String} description
 */
Package.prototype['description'] = undefined;

/**
 * Hash value, typically a file digest, that allows unique identification a specific package.
 * @member {Array.<module:model/Digest>} digest
 */
Package.prototype['digest'] = undefined;

/**
 * The various channels by which a package is distributed.
 * @member {Array.<module:model/Distribution>} distribution
 */
Package.prototype['distribution'] = undefined;

/**
 * @member {module:model/License} license
 */
Package.prototype['license'] = undefined;

/**
 * A freeform text denoting the maintainer of this package.
 * @member {String} maintainer
 */
Package.prototype['maintainer'] = undefined;

/**
 * Required. Immutable. The name of the package.
 * @member {String} name
 */
Package.prototype['name'] = undefined;

/**
 * The type of package; whether native or non native (e.g., ruby gems, node.js packages, etc.).
 * @member {String} packageType
 */
Package.prototype['packageType'] = undefined;

/**
 * The homepage for this package.
 * @member {String} url
 */
Package.prototype['url'] = undefined;

/**
 * @member {module:model/Version} version
 */
Package.prototype['version'] = undefined;





/**
 * Allowed values for the <code>architecture</code> property.
 * @enum {String}
 * @readonly
 */
Package['ArchitectureEnum'] = {

    /**
     * value: "ARCHITECTURE_UNSPECIFIED"
     * @const
     */
    "ARCHITECTURE_UNSPECIFIED": "ARCHITECTURE_UNSPECIFIED",

    /**
     * value: "X86"
     * @const
     */
    "X86": "X86",

    /**
     * value: "X64"
     * @const
     */
    "X64": "X64"
};



export default Package;

