/**
 * Container Analysis API
 * This API is a prerequisite for leveraging Artifact Analysis scanning capabilities in both Artifact Registry and with Advanced Vulnerability Insights (runtime scanning) in GKE. In addition, the Container Analysis API is an implementation of the Grafeas API, which enables storing, querying, and retrieval of critical metadata about all of your software artifacts.
 *
 * The version of the OpenAPI document: v1alpha1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExternalRef model module.
 * @module model/ExternalRef
 * @version v1alpha1
 */
class ExternalRef {
    /**
     * Constructs a new <code>ExternalRef</code>.
     * An External Reference allows a Package to reference an external source of additional information, metadata, enumerations, asset identifiers, or downloadable content believed to be relevant to the Package
     * @alias module:model/ExternalRef
     */
    constructor() { 
        
        ExternalRef.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExternalRef</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExternalRef} obj Optional instance to populate.
     * @return {module:model/ExternalRef} The populated <code>ExternalRef</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExternalRef();

            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('comment')) {
                obj['comment'] = ApiClient.convertToType(data['comment'], 'String');
            }
            if (data.hasOwnProperty('locator')) {
                obj['locator'] = ApiClient.convertToType(data['locator'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExternalRef</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExternalRef</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['category'] && !(typeof data['category'] === 'string' || data['category'] instanceof String)) {
            throw new Error("Expected the field `category` to be a primitive type in the JSON string but got " + data['category']);
        }
        // ensure the json data is a string
        if (data['comment'] && !(typeof data['comment'] === 'string' || data['comment'] instanceof String)) {
            throw new Error("Expected the field `comment` to be a primitive type in the JSON string but got " + data['comment']);
        }
        // ensure the json data is a string
        if (data['locator'] && !(typeof data['locator'] === 'string' || data['locator'] instanceof String)) {
            throw new Error("Expected the field `locator` to be a primitive type in the JSON string but got " + data['locator']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * An External Reference allows a Package to reference an external source of additional information, metadata, enumerations, asset identifiers, or downloadable content believed to be relevant to the Package
 * @member {module:model/ExternalRef.CategoryEnum} category
 */
ExternalRef.prototype['category'] = undefined;

/**
 * Human-readable information about the purpose and target of the reference
 * @member {String} comment
 */
ExternalRef.prototype['comment'] = undefined;

/**
 * The unique string with no spaces necessary to access the package-specific information, metadata, or content within the target location
 * @member {String} locator
 */
ExternalRef.prototype['locator'] = undefined;

/**
 * Type of category (e.g. 'npm' for the PACKAGE_MANAGER category)
 * @member {String} type
 */
ExternalRef.prototype['type'] = undefined;





/**
 * Allowed values for the <code>category</code> property.
 * @enum {String}
 * @readonly
 */
ExternalRef['CategoryEnum'] = {

    /**
     * value: "CATEGORY_UNSPECIFIED"
     * @const
     */
    "CATEGORY_UNSPECIFIED": "CATEGORY_UNSPECIFIED",

    /**
     * value: "SECURITY"
     * @const
     */
    "SECURITY": "SECURITY",

    /**
     * value: "PACKAGE_MANAGER"
     * @const
     */
    "PACKAGE_MANAGER": "PACKAGE_MANAGER",

    /**
     * value: "PERSISTENT_ID"
     * @const
     */
    "PERSISTENT_ID": "PERSISTENT_ID",

    /**
     * value: "OTHER"
     * @const
     */
    "OTHER": "OTHER"
};



export default ExternalRef;

