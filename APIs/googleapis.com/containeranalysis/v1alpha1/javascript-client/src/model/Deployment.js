/**
 * Container Analysis API
 * This API is a prerequisite for leveraging Artifact Analysis scanning capabilities in both Artifact Registry and with Advanced Vulnerability Insights (runtime scanning) in GKE. In addition, the Container Analysis API is an implementation of the Grafeas API, which enables storing, querying, and retrieval of critical metadata about all of your software artifacts.
 *
 * The version of the OpenAPI document: v1alpha1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Deployment model module.
 * @module model/Deployment
 * @version v1alpha1
 */
class Deployment {
    /**
     * Constructs a new <code>Deployment</code>.
     * The period during which some deployable was active in a runtime.
     * @alias module:model/Deployment
     */
    constructor() { 
        
        Deployment.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Deployment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Deployment} obj Optional instance to populate.
     * @return {module:model/Deployment} The populated <code>Deployment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Deployment();

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('config')) {
                obj['config'] = ApiClient.convertToType(data['config'], 'String');
            }
            if (data.hasOwnProperty('deployTime')) {
                obj['deployTime'] = ApiClient.convertToType(data['deployTime'], 'String');
            }
            if (data.hasOwnProperty('platform')) {
                obj['platform'] = ApiClient.convertToType(data['platform'], 'String');
            }
            if (data.hasOwnProperty('resourceUri')) {
                obj['resourceUri'] = ApiClient.convertToType(data['resourceUri'], ['String']);
            }
            if (data.hasOwnProperty('undeployTime')) {
                obj['undeployTime'] = ApiClient.convertToType(data['undeployTime'], 'String');
            }
            if (data.hasOwnProperty('userEmail')) {
                obj['userEmail'] = ApiClient.convertToType(data['userEmail'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Deployment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Deployment</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['config'] && !(typeof data['config'] === 'string' || data['config'] instanceof String)) {
            throw new Error("Expected the field `config` to be a primitive type in the JSON string but got " + data['config']);
        }
        // ensure the json data is a string
        if (data['deployTime'] && !(typeof data['deployTime'] === 'string' || data['deployTime'] instanceof String)) {
            throw new Error("Expected the field `deployTime` to be a primitive type in the JSON string but got " + data['deployTime']);
        }
        // ensure the json data is a string
        if (data['platform'] && !(typeof data['platform'] === 'string' || data['platform'] instanceof String)) {
            throw new Error("Expected the field `platform` to be a primitive type in the JSON string but got " + data['platform']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['resourceUri'])) {
            throw new Error("Expected the field `resourceUri` to be an array in the JSON data but got " + data['resourceUri']);
        }
        // ensure the json data is a string
        if (data['undeployTime'] && !(typeof data['undeployTime'] === 'string' || data['undeployTime'] instanceof String)) {
            throw new Error("Expected the field `undeployTime` to be a primitive type in the JSON string but got " + data['undeployTime']);
        }
        // ensure the json data is a string
        if (data['userEmail'] && !(typeof data['userEmail'] === 'string' || data['userEmail'] instanceof String)) {
            throw new Error("Expected the field `userEmail` to be a primitive type in the JSON string but got " + data['userEmail']);
        }

        return true;
    }


}



/**
 * Address of the runtime element hosting this deployment.
 * @member {String} address
 */
Deployment.prototype['address'] = undefined;

/**
 * Configuration used to create this deployment.
 * @member {String} config
 */
Deployment.prototype['config'] = undefined;

/**
 * Beginning of the lifetime of this deployment.
 * @member {String} deployTime
 */
Deployment.prototype['deployTime'] = undefined;

/**
 * Platform hosting this deployment.
 * @member {module:model/Deployment.PlatformEnum} platform
 */
Deployment.prototype['platform'] = undefined;

/**
 * Resource URI for the artifact being deployed taken from the deployable field with the same name.
 * @member {Array.<String>} resourceUri
 */
Deployment.prototype['resourceUri'] = undefined;

/**
 * End of the lifetime of this deployment.
 * @member {String} undeployTime
 */
Deployment.prototype['undeployTime'] = undefined;

/**
 * Identity of the user that triggered this deployment.
 * @member {String} userEmail
 */
Deployment.prototype['userEmail'] = undefined;





/**
 * Allowed values for the <code>platform</code> property.
 * @enum {String}
 * @readonly
 */
Deployment['PlatformEnum'] = {

    /**
     * value: "PLATFORM_UNSPECIFIED"
     * @const
     */
    "PLATFORM_UNSPECIFIED": "PLATFORM_UNSPECIFIED",

    /**
     * value: "GKE"
     * @const
     */
    "GKE": "GKE",

    /**
     * value: "FLEX"
     * @const
     */
    "FLEX": "FLEX",

    /**
     * value: "CUSTOM"
     * @const
     */
    "CUSTOM": "CUSTOM"
};



export default Deployment;

