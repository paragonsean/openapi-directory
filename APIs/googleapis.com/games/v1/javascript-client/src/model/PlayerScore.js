/**
 * Google Play Game Services
 * The Google Play games service allows developers to enhance games with social leaderboards, achievements, game state, sign-in with Google, and more.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PlayerScore model module.
 * @module model/PlayerScore
 * @version v1
 */
class PlayerScore {
    /**
     * Constructs a new <code>PlayerScore</code>.
     * A player score.
     * @alias module:model/PlayerScore
     */
    constructor() { 
        
        PlayerScore.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PlayerScore</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PlayerScore} obj Optional instance to populate.
     * @return {module:model/PlayerScore} The populated <code>PlayerScore</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PlayerScore();

            if (data.hasOwnProperty('formattedScore')) {
                obj['formattedScore'] = ApiClient.convertToType(data['formattedScore'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'String');
            }
            if (data.hasOwnProperty('scoreTag')) {
                obj['scoreTag'] = ApiClient.convertToType(data['scoreTag'], 'String');
            }
            if (data.hasOwnProperty('timeSpan')) {
                obj['timeSpan'] = ApiClient.convertToType(data['timeSpan'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PlayerScore</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PlayerScore</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['formattedScore'] && !(typeof data['formattedScore'] === 'string' || data['formattedScore'] instanceof String)) {
            throw new Error("Expected the field `formattedScore` to be a primitive type in the JSON string but got " + data['formattedScore']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['score'] && !(typeof data['score'] === 'string' || data['score'] instanceof String)) {
            throw new Error("Expected the field `score` to be a primitive type in the JSON string but got " + data['score']);
        }
        // ensure the json data is a string
        if (data['scoreTag'] && !(typeof data['scoreTag'] === 'string' || data['scoreTag'] instanceof String)) {
            throw new Error("Expected the field `scoreTag` to be a primitive type in the JSON string but got " + data['scoreTag']);
        }
        // ensure the json data is a string
        if (data['timeSpan'] && !(typeof data['timeSpan'] === 'string' || data['timeSpan'] instanceof String)) {
            throw new Error("Expected the field `timeSpan` to be a primitive type in the JSON string but got " + data['timeSpan']);
        }

        return true;
    }


}



/**
 * The formatted score for this player score.
 * @member {String} formattedScore
 */
PlayerScore.prototype['formattedScore'] = undefined;

/**
 * Uniquely identifies the type of this resource. Value is always the fixed string `games#playerScore`.
 * @member {String} kind
 */
PlayerScore.prototype['kind'] = undefined;

/**
 * The numerical value for this player score.
 * @member {String} score
 */
PlayerScore.prototype['score'] = undefined;

/**
 * Additional information about this score. Values will contain no more than 64 URI-safe characters as defined by section 2.3 of RFC 3986.
 * @member {String} scoreTag
 */
PlayerScore.prototype['scoreTag'] = undefined;

/**
 * The time span for this player score.
 * @member {module:model/PlayerScore.TimeSpanEnum} timeSpan
 */
PlayerScore.prototype['timeSpan'] = undefined;





/**
 * Allowed values for the <code>timeSpan</code> property.
 * @enum {String}
 * @readonly
 */
PlayerScore['TimeSpanEnum'] = {

    /**
     * value: "ALL_TIME"
     * @const
     */
    "ALL_TIME": "ALL_TIME",

    /**
     * value: "WEEKLY"
     * @const
     */
    "WEEKLY": "WEEKLY",

    /**
     * value: "DAILY"
     * @const
     */
    "DAILY": "DAILY"
};



export default PlayerScore;

