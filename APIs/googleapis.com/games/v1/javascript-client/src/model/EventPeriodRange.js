/**
 * Google Play Game Services
 * The Google Play games service allows developers to enhance games with social leaderboards, achievements, game state, sign-in with Google, and more.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EventPeriodRange model module.
 * @module model/EventPeriodRange
 * @version v1
 */
class EventPeriodRange {
    /**
     * Constructs a new <code>EventPeriodRange</code>.
     * An event period time range.
     * @alias module:model/EventPeriodRange
     */
    constructor() { 
        
        EventPeriodRange.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EventPeriodRange</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventPeriodRange} obj Optional instance to populate.
     * @return {module:model/EventPeriodRange} The populated <code>EventPeriodRange</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventPeriodRange();

            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('periodEndMillis')) {
                obj['periodEndMillis'] = ApiClient.convertToType(data['periodEndMillis'], 'String');
            }
            if (data.hasOwnProperty('periodStartMillis')) {
                obj['periodStartMillis'] = ApiClient.convertToType(data['periodStartMillis'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventPeriodRange</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventPeriodRange</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['periodEndMillis'] && !(typeof data['periodEndMillis'] === 'string' || data['periodEndMillis'] instanceof String)) {
            throw new Error("Expected the field `periodEndMillis` to be a primitive type in the JSON string but got " + data['periodEndMillis']);
        }
        // ensure the json data is a string
        if (data['periodStartMillis'] && !(typeof data['periodStartMillis'] === 'string' || data['periodStartMillis'] instanceof String)) {
            throw new Error("Expected the field `periodStartMillis` to be a primitive type in the JSON string but got " + data['periodStartMillis']);
        }

        return true;
    }


}



/**
 * Uniquely identifies the type of this resource. Value is always the fixed string `games#eventPeriodRange`.
 * @member {String} kind
 */
EventPeriodRange.prototype['kind'] = undefined;

/**
 * The time when this update period ends, in millis, since 1970 UTC (Unix Epoch).
 * @member {String} periodEndMillis
 */
EventPeriodRange.prototype['periodEndMillis'] = undefined;

/**
 * The time when this update period begins, in millis, since 1970 UTC (Unix Epoch).
 * @member {String} periodStartMillis
 */
EventPeriodRange.prototype['periodStartMillis'] = undefined;






export default EventPeriodRange;

