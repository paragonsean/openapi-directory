/**
 * Google Workspace Reseller API
 * Perform common functions that are available on the Channel Services console at scale, like placing orders and viewing customer information
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SubscriptionPlanCommitmentInterval from './SubscriptionPlanCommitmentInterval';

/**
 * The SubscriptionPlan model module.
 * @module model/SubscriptionPlan
 * @version v1
 */
class SubscriptionPlan {
    /**
     * Constructs a new <code>SubscriptionPlan</code>.
     * The &#x60;plan&#x60; property is required. In this version of the API, the G Suite plans are the flexible plan, annual commitment plan, and the 30-day free trial plan. For more information about the API\&quot;s payment plans, see the API concepts.
     * @alias module:model/SubscriptionPlan
     */
    constructor() { 
        
        SubscriptionPlan.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SubscriptionPlan</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SubscriptionPlan} obj Optional instance to populate.
     * @return {module:model/SubscriptionPlan} The populated <code>SubscriptionPlan</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SubscriptionPlan();

            if (data.hasOwnProperty('commitmentInterval')) {
                obj['commitmentInterval'] = SubscriptionPlanCommitmentInterval.constructFromObject(data['commitmentInterval']);
            }
            if (data.hasOwnProperty('isCommitmentPlan')) {
                obj['isCommitmentPlan'] = ApiClient.convertToType(data['isCommitmentPlan'], 'Boolean');
            }
            if (data.hasOwnProperty('planName')) {
                obj['planName'] = ApiClient.convertToType(data['planName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SubscriptionPlan</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SubscriptionPlan</code>.
     */
    static validateJSON(data) {
        // validate the optional field `commitmentInterval`
        if (data['commitmentInterval']) { // data not null
          SubscriptionPlanCommitmentInterval.validateJSON(data['commitmentInterval']);
        }
        // ensure the json data is a string
        if (data['planName'] && !(typeof data['planName'] === 'string' || data['planName'] instanceof String)) {
            throw new Error("Expected the field `planName` to be a primitive type in the JSON string but got " + data['planName']);
        }

        return true;
    }


}



/**
 * @member {module:model/SubscriptionPlanCommitmentInterval} commitmentInterval
 */
SubscriptionPlan.prototype['commitmentInterval'] = undefined;

/**
 * The `isCommitmentPlan` property's boolean value identifies the plan as an annual commitment plan: - `true` — The subscription's plan is an annual commitment plan. - `false` — The plan is not an annual commitment plan. 
 * @member {Boolean} isCommitmentPlan
 */
SubscriptionPlan.prototype['isCommitmentPlan'] = undefined;

/**
 * The `planName` property is required. This is the name of the subscription's plan. For more information about the Google payment plans, see the API concepts. Possible values are: - `ANNUAL_MONTHLY_PAY` — The annual commitment plan with monthly payments. *Caution: *`ANNUAL_MONTHLY_PAY` is returned as `ANNUAL` in all API responses. - `ANNUAL_YEARLY_PAY` — The annual commitment plan with yearly payments - `FLEXIBLE` — The flexible plan - `TRIAL` — The 30-day free trial plan. A subscription in trial will be suspended after the 30th free day if no payment plan is assigned. Calling `changePlan` will assign a payment plan to a trial but will not activate the plan. A trial will automatically begin its assigned payment plan after its 30th free day or immediately after calling `startPaidService`. - `FREE` — The free plan is exclusive to the Cloud Identity SKU and does not incur any billing. 
 * @member {String} planName
 */
SubscriptionPlan.prototype['planName'] = undefined;






export default SubscriptionPlan;

