/**
 * Authorized Buyers Marketplace API
 * The Authorized Buyers Marketplace API lets buyers programmatically discover inventory; propose, retrieve and negotiate deals with publishers.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RtbMetrics model module.
 * @module model/RtbMetrics
 * @version v1
 */
class RtbMetrics {
    /**
     * Constructs a new <code>RtbMetrics</code>.
     * Real-time bidding metrics. For what each metric means refer to [Report metrics](https://support.google.com/adxbuyer/answer/6115195#report-metrics)
     * @alias module:model/RtbMetrics
     */
    constructor() { 
        
        RtbMetrics.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RtbMetrics</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RtbMetrics} obj Optional instance to populate.
     * @return {module:model/RtbMetrics} The populated <code>RtbMetrics</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RtbMetrics();

            if (data.hasOwnProperty('adImpressions7Days')) {
                obj['adImpressions7Days'] = ApiClient.convertToType(data['adImpressions7Days'], 'String');
            }
            if (data.hasOwnProperty('bidRate7Days')) {
                obj['bidRate7Days'] = ApiClient.convertToType(data['bidRate7Days'], 'Number');
            }
            if (data.hasOwnProperty('bidRequests7Days')) {
                obj['bidRequests7Days'] = ApiClient.convertToType(data['bidRequests7Days'], 'String');
            }
            if (data.hasOwnProperty('bids7Days')) {
                obj['bids7Days'] = ApiClient.convertToType(data['bids7Days'], 'String');
            }
            if (data.hasOwnProperty('filteredBidRate7Days')) {
                obj['filteredBidRate7Days'] = ApiClient.convertToType(data['filteredBidRate7Days'], 'Number');
            }
            if (data.hasOwnProperty('mustBidRateCurrentMonth')) {
                obj['mustBidRateCurrentMonth'] = ApiClient.convertToType(data['mustBidRateCurrentMonth'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RtbMetrics</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RtbMetrics</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['adImpressions7Days'] && !(typeof data['adImpressions7Days'] === 'string' || data['adImpressions7Days'] instanceof String)) {
            throw new Error("Expected the field `adImpressions7Days` to be a primitive type in the JSON string but got " + data['adImpressions7Days']);
        }
        // ensure the json data is a string
        if (data['bidRequests7Days'] && !(typeof data['bidRequests7Days'] === 'string' || data['bidRequests7Days'] instanceof String)) {
            throw new Error("Expected the field `bidRequests7Days` to be a primitive type in the JSON string but got " + data['bidRequests7Days']);
        }
        // ensure the json data is a string
        if (data['bids7Days'] && !(typeof data['bids7Days'] === 'string' || data['bids7Days'] instanceof String)) {
            throw new Error("Expected the field `bids7Days` to be a primitive type in the JSON string but got " + data['bids7Days']);
        }

        return true;
    }


}



/**
 * Ad impressions in last 7 days.
 * @member {String} adImpressions7Days
 */
RtbMetrics.prototype['adImpressions7Days'] = undefined;

/**
 * Bid rate in last 7 days, calculated by (bids / bid requests).
 * @member {Number} bidRate7Days
 */
RtbMetrics.prototype['bidRate7Days'] = undefined;

/**
 * Bid requests in last 7 days.
 * @member {String} bidRequests7Days
 */
RtbMetrics.prototype['bidRequests7Days'] = undefined;

/**
 * Bids in last 7 days.
 * @member {String} bids7Days
 */
RtbMetrics.prototype['bids7Days'] = undefined;

/**
 * Filtered bid rate in last 7 days, calculated by (filtered bids / bids).
 * @member {Number} filteredBidRate7Days
 */
RtbMetrics.prototype['filteredBidRate7Days'] = undefined;

/**
 * Must bid rate for current month.
 * @member {Number} mustBidRateCurrentMonth
 */
RtbMetrics.prototype['mustBidRateCurrentMonth'] = undefined;






export default RtbMetrics;

