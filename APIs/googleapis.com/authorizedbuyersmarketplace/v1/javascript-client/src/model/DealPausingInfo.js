/**
 * Authorized Buyers Marketplace API
 * The Authorized Buyers Marketplace API lets buyers programmatically discover inventory; propose, retrieve and negotiate deals with publishers.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DealPausingInfo model module.
 * @module model/DealPausingInfo
 * @version v1
 */
class DealPausingInfo {
    /**
     * Constructs a new <code>DealPausingInfo</code>.
     * Information related to deal pausing.
     * @alias module:model/DealPausingInfo
     */
    constructor() { 
        
        DealPausingInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DealPausingInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DealPausingInfo} obj Optional instance to populate.
     * @return {module:model/DealPausingInfo} The populated <code>DealPausingInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DealPausingInfo();

            if (data.hasOwnProperty('pauseReason')) {
                obj['pauseReason'] = ApiClient.convertToType(data['pauseReason'], 'String');
            }
            if (data.hasOwnProperty('pauseRole')) {
                obj['pauseRole'] = ApiClient.convertToType(data['pauseRole'], 'String');
            }
            if (data.hasOwnProperty('pausingConsented')) {
                obj['pausingConsented'] = ApiClient.convertToType(data['pausingConsented'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DealPausingInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DealPausingInfo</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['pauseReason'] && !(typeof data['pauseReason'] === 'string' || data['pauseReason'] instanceof String)) {
            throw new Error("Expected the field `pauseReason` to be a primitive type in the JSON string but got " + data['pauseReason']);
        }
        // ensure the json data is a string
        if (data['pauseRole'] && !(typeof data['pauseRole'] === 'string' || data['pauseRole'] instanceof String)) {
            throw new Error("Expected the field `pauseRole` to be a primitive type in the JSON string but got " + data['pauseRole']);
        }

        return true;
    }


}



/**
 * The reason for the pausing of the deal; empty for active deals.
 * @member {String} pauseReason
 */
DealPausingInfo.prototype['pauseReason'] = undefined;

/**
 * The party that first paused the deal; unspecified for active deals.
 * @member {module:model/DealPausingInfo.PauseRoleEnum} pauseRole
 */
DealPausingInfo.prototype['pauseRole'] = undefined;

/**
 * Whether pausing is consented between buyer and seller for the deal.
 * @member {Boolean} pausingConsented
 */
DealPausingInfo.prototype['pausingConsented'] = undefined;





/**
 * Allowed values for the <code>pauseRole</code> property.
 * @enum {String}
 * @readonly
 */
DealPausingInfo['PauseRoleEnum'] = {

    /**
     * value: "BUYER_SELLER_ROLE_UNSPECIFIED"
     * @const
     */
    "BUYER_SELLER_ROLE_UNSPECIFIED": "BUYER_SELLER_ROLE_UNSPECIFIED",

    /**
     * value: "BUYER"
     * @const
     */
    "BUYER": "BUYER",

    /**
     * value: "SELLER"
     * @const
     */
    "SELLER": "SELLER"
};



export default DealPausingInfo;

