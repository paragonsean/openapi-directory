/**
 * Authorized Buyers Marketplace API
 * The Authorized Buyers Marketplace API lets buyers programmatically discover inventory; propose, retrieve and negotiate deals with publishers.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AdSize from './AdSize';

/**
 * The InventorySizeTargeting model module.
 * @module model/InventorySizeTargeting
 * @version v1
 */
class InventorySizeTargeting {
    /**
     * Constructs a new <code>InventorySizeTargeting</code>.
     * Represents the size of an ad unit that can be targeted on a bid request.
     * @alias module:model/InventorySizeTargeting
     */
    constructor() { 
        
        InventorySizeTargeting.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InventorySizeTargeting</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InventorySizeTargeting} obj Optional instance to populate.
     * @return {module:model/InventorySizeTargeting} The populated <code>InventorySizeTargeting</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InventorySizeTargeting();

            if (data.hasOwnProperty('excludedInventorySizes')) {
                obj['excludedInventorySizes'] = ApiClient.convertToType(data['excludedInventorySizes'], [AdSize]);
            }
            if (data.hasOwnProperty('targetedInventorySizes')) {
                obj['targetedInventorySizes'] = ApiClient.convertToType(data['targetedInventorySizes'], [AdSize]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InventorySizeTargeting</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InventorySizeTargeting</code>.
     */
    static validateJSON(data) {
        if (data['excludedInventorySizes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['excludedInventorySizes'])) {
                throw new Error("Expected the field `excludedInventorySizes` to be an array in the JSON data but got " + data['excludedInventorySizes']);
            }
            // validate the optional field `excludedInventorySizes` (array)
            for (const item of data['excludedInventorySizes']) {
                AdSize.validateJSON(item);
            };
        }
        if (data['targetedInventorySizes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['targetedInventorySizes'])) {
                throw new Error("Expected the field `targetedInventorySizes` to be an array in the JSON data but got " + data['targetedInventorySizes']);
            }
            // validate the optional field `targetedInventorySizes` (array)
            for (const item of data['targetedInventorySizes']) {
                AdSize.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A list of inventory sizes to be excluded.
 * @member {Array.<module:model/AdSize>} excludedInventorySizes
 */
InventorySizeTargeting.prototype['excludedInventorySizes'] = undefined;

/**
 * A list of inventory sizes to be included.
 * @member {Array.<module:model/AdSize>} targetedInventorySizes
 */
InventorySizeTargeting.prototype['targetedInventorySizes'] = undefined;






export default InventorySizeTargeting;

