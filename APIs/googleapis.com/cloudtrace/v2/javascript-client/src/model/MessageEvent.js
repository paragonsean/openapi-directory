/**
 * Cloud Trace API
 * Sends application trace data to Cloud Trace for viewing. Trace data is collected for all App Engine applications by default. Trace data from other applications can be provided using this API. This library is used to interact with the Cloud Trace API directly. If you are looking to instrument your application for Cloud Trace, we recommend using OpenTelemetry. 
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MessageEvent model module.
 * @module model/MessageEvent
 * @version v2
 */
class MessageEvent {
    /**
     * Constructs a new <code>MessageEvent</code>.
     * An event describing a message sent/received between Spans.
     * @alias module:model/MessageEvent
     */
    constructor() { 
        
        MessageEvent.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>MessageEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MessageEvent} obj Optional instance to populate.
     * @return {module:model/MessageEvent} The populated <code>MessageEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MessageEvent();

            if (data.hasOwnProperty('compressedSizeBytes')) {
                obj['compressedSizeBytes'] = ApiClient.convertToType(data['compressedSizeBytes'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('uncompressedSizeBytes')) {
                obj['uncompressedSizeBytes'] = ApiClient.convertToType(data['uncompressedSizeBytes'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MessageEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MessageEvent</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['compressedSizeBytes'] && !(typeof data['compressedSizeBytes'] === 'string' || data['compressedSizeBytes'] instanceof String)) {
            throw new Error("Expected the field `compressedSizeBytes` to be a primitive type in the JSON string but got " + data['compressedSizeBytes']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['uncompressedSizeBytes'] && !(typeof data['uncompressedSizeBytes'] === 'string' || data['uncompressedSizeBytes'] instanceof String)) {
            throw new Error("Expected the field `uncompressedSizeBytes` to be a primitive type in the JSON string but got " + data['uncompressedSizeBytes']);
        }

        return true;
    }


}



/**
 * The number of compressed bytes sent or received. If missing, the compressed size is assumed to be the same size as the uncompressed size.
 * @member {String} compressedSizeBytes
 */
MessageEvent.prototype['compressedSizeBytes'] = undefined;

/**
 * An identifier for the MessageEvent's message that can be used to match `SENT` and `RECEIVED` MessageEvents.
 * @member {String} id
 */
MessageEvent.prototype['id'] = undefined;

/**
 * Type of MessageEvent. Indicates whether the message was sent or received.
 * @member {module:model/MessageEvent.TypeEnum} type
 */
MessageEvent.prototype['type'] = undefined;

/**
 * The number of uncompressed bytes sent or received.
 * @member {String} uncompressedSizeBytes
 */
MessageEvent.prototype['uncompressedSizeBytes'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
MessageEvent['TypeEnum'] = {

    /**
     * value: "TYPE_UNSPECIFIED"
     * @const
     */
    "TYPE_UNSPECIFIED": "TYPE_UNSPECIFIED",

    /**
     * value: "SENT"
     * @const
     */
    "SENT": "SENT",

    /**
     * value: "RECEIVED"
     * @const
     */
    "RECEIVED": "RECEIVED"
};



export default MessageEvent;

