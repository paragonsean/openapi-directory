/**
 * Genomics API
 * Uploads, processes, queries, and searches Genomics data in the cloud.
 *
 * The version of the OpenAPI document: v2alpha1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Accelerator from './Accelerator';
import Disk from './Disk';
import Network from './Network';
import ServiceAccount from './ServiceAccount';
import Volume from './Volume';

/**
 * The VirtualMachine model module.
 * @module model/VirtualMachine
 * @version v2alpha1
 */
class VirtualMachine {
    /**
     * Constructs a new <code>VirtualMachine</code>.
     * Carries information about a Compute Engine VM resource.
     * @alias module:model/VirtualMachine
     */
    constructor() { 
        
        VirtualMachine.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>VirtualMachine</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VirtualMachine} obj Optional instance to populate.
     * @return {module:model/VirtualMachine} The populated <code>VirtualMachine</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VirtualMachine();

            if (data.hasOwnProperty('accelerators')) {
                obj['accelerators'] = ApiClient.convertToType(data['accelerators'], [Accelerator]);
            }
            if (data.hasOwnProperty('bootDiskSizeGb')) {
                obj['bootDiskSizeGb'] = ApiClient.convertToType(data['bootDiskSizeGb'], 'Number');
            }
            if (data.hasOwnProperty('bootImage')) {
                obj['bootImage'] = ApiClient.convertToType(data['bootImage'], 'String');
            }
            if (data.hasOwnProperty('cpuPlatform')) {
                obj['cpuPlatform'] = ApiClient.convertToType(data['cpuPlatform'], 'String');
            }
            if (data.hasOwnProperty('disks')) {
                obj['disks'] = ApiClient.convertToType(data['disks'], [Disk]);
            }
            if (data.hasOwnProperty('dockerCacheImages')) {
                obj['dockerCacheImages'] = ApiClient.convertToType(data['dockerCacheImages'], ['String']);
            }
            if (data.hasOwnProperty('enableStackdriverMonitoring')) {
                obj['enableStackdriverMonitoring'] = ApiClient.convertToType(data['enableStackdriverMonitoring'], 'Boolean');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('machineType')) {
                obj['machineType'] = ApiClient.convertToType(data['machineType'], 'String');
            }
            if (data.hasOwnProperty('network')) {
                obj['network'] = Network.constructFromObject(data['network']);
            }
            if (data.hasOwnProperty('nvidiaDriverVersion')) {
                obj['nvidiaDriverVersion'] = ApiClient.convertToType(data['nvidiaDriverVersion'], 'String');
            }
            if (data.hasOwnProperty('preemptible')) {
                obj['preemptible'] = ApiClient.convertToType(data['preemptible'], 'Boolean');
            }
            if (data.hasOwnProperty('reservation')) {
                obj['reservation'] = ApiClient.convertToType(data['reservation'], 'String');
            }
            if (data.hasOwnProperty('serviceAccount')) {
                obj['serviceAccount'] = ServiceAccount.constructFromObject(data['serviceAccount']);
            }
            if (data.hasOwnProperty('volumes')) {
                obj['volumes'] = ApiClient.convertToType(data['volumes'], [Volume]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VirtualMachine</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VirtualMachine</code>.
     */
    static validateJSON(data) {
        if (data['accelerators']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['accelerators'])) {
                throw new Error("Expected the field `accelerators` to be an array in the JSON data but got " + data['accelerators']);
            }
            // validate the optional field `accelerators` (array)
            for (const item of data['accelerators']) {
                Accelerator.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['bootImage'] && !(typeof data['bootImage'] === 'string' || data['bootImage'] instanceof String)) {
            throw new Error("Expected the field `bootImage` to be a primitive type in the JSON string but got " + data['bootImage']);
        }
        // ensure the json data is a string
        if (data['cpuPlatform'] && !(typeof data['cpuPlatform'] === 'string' || data['cpuPlatform'] instanceof String)) {
            throw new Error("Expected the field `cpuPlatform` to be a primitive type in the JSON string but got " + data['cpuPlatform']);
        }
        if (data['disks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['disks'])) {
                throw new Error("Expected the field `disks` to be an array in the JSON data but got " + data['disks']);
            }
            // validate the optional field `disks` (array)
            for (const item of data['disks']) {
                Disk.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['dockerCacheImages'])) {
            throw new Error("Expected the field `dockerCacheImages` to be an array in the JSON data but got " + data['dockerCacheImages']);
        }
        // ensure the json data is a string
        if (data['machineType'] && !(typeof data['machineType'] === 'string' || data['machineType'] instanceof String)) {
            throw new Error("Expected the field `machineType` to be a primitive type in the JSON string but got " + data['machineType']);
        }
        // validate the optional field `network`
        if (data['network']) { // data not null
          Network.validateJSON(data['network']);
        }
        // ensure the json data is a string
        if (data['nvidiaDriverVersion'] && !(typeof data['nvidiaDriverVersion'] === 'string' || data['nvidiaDriverVersion'] instanceof String)) {
            throw new Error("Expected the field `nvidiaDriverVersion` to be a primitive type in the JSON string but got " + data['nvidiaDriverVersion']);
        }
        // ensure the json data is a string
        if (data['reservation'] && !(typeof data['reservation'] === 'string' || data['reservation'] instanceof String)) {
            throw new Error("Expected the field `reservation` to be a primitive type in the JSON string but got " + data['reservation']);
        }
        // validate the optional field `serviceAccount`
        if (data['serviceAccount']) { // data not null
          ServiceAccount.validateJSON(data['serviceAccount']);
        }
        if (data['volumes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['volumes'])) {
                throw new Error("Expected the field `volumes` to be an array in the JSON data but got " + data['volumes']);
            }
            // validate the optional field `volumes` (array)
            for (const item of data['volumes']) {
                Volume.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The list of accelerators to attach to the VM.
 * @member {Array.<module:model/Accelerator>} accelerators
 */
VirtualMachine.prototype['accelerators'] = undefined;

/**
 * The size of the boot disk, in GB. The boot disk must be large enough to accommodate all of the Docker images from each action in the pipeline at the same time. If not specified, a small but reasonable default value is used.
 * @member {Number} bootDiskSizeGb
 */
VirtualMachine.prototype['bootDiskSizeGb'] = undefined;

/**
 * The host operating system image to use. Currently, only Container-Optimized OS images can be used. The default value is `projects/cos-cloud/global/images/family/cos-stable`, which selects the latest stable release of Container-Optimized OS. This option is provided to allow testing against the beta release of the operating system to ensure that the new version does not interact negatively with production pipelines. To test a pipeline against the beta release of Container-Optimized OS, use the value `projects/cos-cloud/global/images/family/cos-beta`.
 * @member {String} bootImage
 */
VirtualMachine.prototype['bootImage'] = undefined;

/**
 * The CPU platform to request. An instance based on a newer platform can be allocated, but never one with fewer capabilities. The value of this parameter must be a valid Compute Engine CPU platform name (such as \"Intel Skylake\"). This parameter is only useful for carefully optimized work loads where the CPU platform has a significant impact. For more information about the effect of this parameter, see https://cloud.google.com/compute/docs/instances/specify-min-cpu-platform.
 * @member {String} cpuPlatform
 */
VirtualMachine.prototype['cpuPlatform'] = undefined;

/**
 * The list of disks to create and attach to the VM. Specify either the `volumes[]` field or the `disks[]` field, but not both.
 * @member {Array.<module:model/Disk>} disks
 */
VirtualMachine.prototype['disks'] = undefined;

/**
 * The Compute Engine Disk Images to use as a Docker cache. The disks will be mounted into the Docker folder in a way that the images present in the cache will not need to be pulled. The digests of the cached images must match those of the tags used or the latest version will still be pulled. The root directory of the ext4 image must contain `image` and `overlay2` directories copied from the Docker directory of a VM where the desired Docker images have already been pulled. Any images pulled that are not cached will be stored on the first cache disk instead of the boot disk. Only a single image is supported.
 * @member {Array.<String>} dockerCacheImages
 */
VirtualMachine.prototype['dockerCacheImages'] = undefined;

/**
 * Whether Stackdriver monitoring should be enabled on the VM.
 * @member {Boolean} enableStackdriverMonitoring
 */
VirtualMachine.prototype['enableStackdriverMonitoring'] = undefined;

/**
 * Optional set of labels to apply to the VM and any attached disk resources. These labels must adhere to the [name and value restrictions](https://cloud.google.com/compute/docs/labeling-resources) on VM labels imposed by Compute Engine. Labels keys with the prefix 'google-' are reserved for use by Google. Labels applied at creation time to the VM. Applied on a best-effort basis to attached disk resources shortly after VM creation.
 * @member {Object.<String, String>} labels
 */
VirtualMachine.prototype['labels'] = undefined;

/**
 * Required. The machine type of the virtual machine to create. Must be the short name of a standard machine type (such as \"n1-standard-1\") or a custom machine type (such as \"custom-1-4096\", where \"1\" indicates the number of vCPUs and \"4096\" indicates the memory in MB). See [Creating an instance with a custom machine type](https://cloud.google.com/compute/docs/instances/creating-instance-with-custom-machine-type#create) for more specifications on creating a custom machine type.
 * @member {String} machineType
 */
VirtualMachine.prototype['machineType'] = undefined;

/**
 * @member {module:model/Network} network
 */
VirtualMachine.prototype['network'] = undefined;

/**
 * The NVIDIA driver version to use when attaching an NVIDIA GPU accelerator. The version specified here must be compatible with the GPU libraries contained in the container being executed, and must be one of the drivers hosted in the `nvidia-drivers-us-public` bucket on Google Cloud Storage.
 * @member {String} nvidiaDriverVersion
 */
VirtualMachine.prototype['nvidiaDriverVersion'] = undefined;

/**
 * If true, allocate a preemptible VM.
 * @member {Boolean} preemptible
 */
VirtualMachine.prototype['preemptible'] = undefined;

/**
 * If specified, the VM will only be allocated inside the matching reservation. It will fail if the VM parameters don't match the reservation.
 * @member {String} reservation
 */
VirtualMachine.prototype['reservation'] = undefined;

/**
 * @member {module:model/ServiceAccount} serviceAccount
 */
VirtualMachine.prototype['serviceAccount'] = undefined;

/**
 * The list of disks and other storage to create or attach to the VM. Specify either the `volumes[]` field or the `disks[]` field, but not both.
 * @member {Array.<module:model/Volume>} volumes
 */
VirtualMachine.prototype['volumes'] = undefined;






export default VirtualMachine;

