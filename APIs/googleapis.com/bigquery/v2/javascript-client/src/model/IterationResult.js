/**
 * BigQuery API
 * A data platform for customers to create, manage, share and query data.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ArimaResult from './ArimaResult';
import ClusterInfo from './ClusterInfo';
import PrincipalComponentInfo from './PrincipalComponentInfo';

/**
 * The IterationResult model module.
 * @module model/IterationResult
 * @version v2
 */
class IterationResult {
    /**
     * Constructs a new <code>IterationResult</code>.
     * Information about a single iteration of the training run.
     * @alias module:model/IterationResult
     */
    constructor() { 
        
        IterationResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>IterationResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IterationResult} obj Optional instance to populate.
     * @return {module:model/IterationResult} The populated <code>IterationResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IterationResult();

            if (data.hasOwnProperty('arimaResult')) {
                obj['arimaResult'] = ArimaResult.constructFromObject(data['arimaResult']);
            }
            if (data.hasOwnProperty('clusterInfos')) {
                obj['clusterInfos'] = ApiClient.convertToType(data['clusterInfos'], [ClusterInfo]);
            }
            if (data.hasOwnProperty('durationMs')) {
                obj['durationMs'] = ApiClient.convertToType(data['durationMs'], 'String');
            }
            if (data.hasOwnProperty('evalLoss')) {
                obj['evalLoss'] = ApiClient.convertToType(data['evalLoss'], 'Number');
            }
            if (data.hasOwnProperty('index')) {
                obj['index'] = ApiClient.convertToType(data['index'], 'Number');
            }
            if (data.hasOwnProperty('learnRate')) {
                obj['learnRate'] = ApiClient.convertToType(data['learnRate'], 'Number');
            }
            if (data.hasOwnProperty('principalComponentInfos')) {
                obj['principalComponentInfos'] = ApiClient.convertToType(data['principalComponentInfos'], [PrincipalComponentInfo]);
            }
            if (data.hasOwnProperty('trainingLoss')) {
                obj['trainingLoss'] = ApiClient.convertToType(data['trainingLoss'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IterationResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IterationResult</code>.
     */
    static validateJSON(data) {
        // validate the optional field `arimaResult`
        if (data['arimaResult']) { // data not null
          ArimaResult.validateJSON(data['arimaResult']);
        }
        if (data['clusterInfos']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['clusterInfos'])) {
                throw new Error("Expected the field `clusterInfos` to be an array in the JSON data but got " + data['clusterInfos']);
            }
            // validate the optional field `clusterInfos` (array)
            for (const item of data['clusterInfos']) {
                ClusterInfo.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['durationMs'] && !(typeof data['durationMs'] === 'string' || data['durationMs'] instanceof String)) {
            throw new Error("Expected the field `durationMs` to be a primitive type in the JSON string but got " + data['durationMs']);
        }
        if (data['principalComponentInfos']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['principalComponentInfos'])) {
                throw new Error("Expected the field `principalComponentInfos` to be an array in the JSON data but got " + data['principalComponentInfos']);
            }
            // validate the optional field `principalComponentInfos` (array)
            for (const item of data['principalComponentInfos']) {
                PrincipalComponentInfo.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/ArimaResult} arimaResult
 */
IterationResult.prototype['arimaResult'] = undefined;

/**
 * Information about top clusters for clustering models.
 * @member {Array.<module:model/ClusterInfo>} clusterInfos
 */
IterationResult.prototype['clusterInfos'] = undefined;

/**
 * Time taken to run the iteration in milliseconds.
 * @member {String} durationMs
 */
IterationResult.prototype['durationMs'] = undefined;

/**
 * Loss computed on the eval data at the end of iteration.
 * @member {Number} evalLoss
 */
IterationResult.prototype['evalLoss'] = undefined;

/**
 * Index of the iteration, 0 based.
 * @member {Number} index
 */
IterationResult.prototype['index'] = undefined;

/**
 * Learn rate used for this iteration.
 * @member {Number} learnRate
 */
IterationResult.prototype['learnRate'] = undefined;

/**
 * The information of the principal components.
 * @member {Array.<module:model/PrincipalComponentInfo>} principalComponentInfos
 */
IterationResult.prototype['principalComponentInfos'] = undefined;

/**
 * Loss computed on the training data at the end of iteration.
 * @member {Number} trainingLoss
 */
IterationResult.prototype['trainingLoss'] = undefined;






export default IterationResult;

