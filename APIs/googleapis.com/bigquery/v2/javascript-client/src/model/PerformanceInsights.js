/**
 * BigQuery API
 * A data platform for customers to create, manage, share and query data.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import StagePerformanceChangeInsight from './StagePerformanceChangeInsight';
import StagePerformanceStandaloneInsight from './StagePerformanceStandaloneInsight';

/**
 * The PerformanceInsights model module.
 * @module model/PerformanceInsights
 * @version v2
 */
class PerformanceInsights {
    /**
     * Constructs a new <code>PerformanceInsights</code>.
     * Performance insights for the job.
     * @alias module:model/PerformanceInsights
     */
    constructor() { 
        
        PerformanceInsights.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PerformanceInsights</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PerformanceInsights} obj Optional instance to populate.
     * @return {module:model/PerformanceInsights} The populated <code>PerformanceInsights</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PerformanceInsights();

            if (data.hasOwnProperty('avgPreviousExecutionMs')) {
                obj['avgPreviousExecutionMs'] = ApiClient.convertToType(data['avgPreviousExecutionMs'], 'String');
            }
            if (data.hasOwnProperty('stagePerformanceChangeInsights')) {
                obj['stagePerformanceChangeInsights'] = ApiClient.convertToType(data['stagePerformanceChangeInsights'], [StagePerformanceChangeInsight]);
            }
            if (data.hasOwnProperty('stagePerformanceStandaloneInsights')) {
                obj['stagePerformanceStandaloneInsights'] = ApiClient.convertToType(data['stagePerformanceStandaloneInsights'], [StagePerformanceStandaloneInsight]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PerformanceInsights</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PerformanceInsights</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['avgPreviousExecutionMs'] && !(typeof data['avgPreviousExecutionMs'] === 'string' || data['avgPreviousExecutionMs'] instanceof String)) {
            throw new Error("Expected the field `avgPreviousExecutionMs` to be a primitive type in the JSON string but got " + data['avgPreviousExecutionMs']);
        }
        if (data['stagePerformanceChangeInsights']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stagePerformanceChangeInsights'])) {
                throw new Error("Expected the field `stagePerformanceChangeInsights` to be an array in the JSON data but got " + data['stagePerformanceChangeInsights']);
            }
            // validate the optional field `stagePerformanceChangeInsights` (array)
            for (const item of data['stagePerformanceChangeInsights']) {
                StagePerformanceChangeInsight.validateJSON(item);
            };
        }
        if (data['stagePerformanceStandaloneInsights']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stagePerformanceStandaloneInsights'])) {
                throw new Error("Expected the field `stagePerformanceStandaloneInsights` to be an array in the JSON data but got " + data['stagePerformanceStandaloneInsights']);
            }
            // validate the optional field `stagePerformanceStandaloneInsights` (array)
            for (const item of data['stagePerformanceStandaloneInsights']) {
                StagePerformanceStandaloneInsight.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Output only. Average execution ms of previous runs. Indicates the job ran slow compared to previous executions. To find previous executions, use INFORMATION_SCHEMA tables and filter jobs with same query hash.
 * @member {String} avgPreviousExecutionMs
 */
PerformanceInsights.prototype['avgPreviousExecutionMs'] = undefined;

/**
 * Output only. Query stage performance insights compared to previous runs, for diagnosing performance regression.
 * @member {Array.<module:model/StagePerformanceChangeInsight>} stagePerformanceChangeInsights
 */
PerformanceInsights.prototype['stagePerformanceChangeInsights'] = undefined;

/**
 * Output only. Standalone query stage performance insights, for exploring potential improvements.
 * @member {Array.<module:model/StagePerformanceStandaloneInsight>} stagePerformanceStandaloneInsights
 */
PerformanceInsights.prototype['stagePerformanceStandaloneInsights'] = undefined;






export default PerformanceInsights;

