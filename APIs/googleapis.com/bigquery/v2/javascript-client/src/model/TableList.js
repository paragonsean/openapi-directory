/**
 * BigQuery API
 * A data platform for customers to create, manage, share and query data.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TableListTablesInner from './TableListTablesInner';

/**
 * The TableList model module.
 * @module model/TableList
 * @version v2
 */
class TableList {
    /**
     * Constructs a new <code>TableList</code>.
     * Partial projection of the metadata for a given table in a list response.
     * @alias module:model/TableList
     */
    constructor() { 
        
        TableList.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['kind'] = 'bigquery#tableList';
    }

    /**
     * Constructs a <code>TableList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TableList} obj Optional instance to populate.
     * @return {module:model/TableList} The populated <code>TableList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TableList();

            if (data.hasOwnProperty('etag')) {
                obj['etag'] = ApiClient.convertToType(data['etag'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('tables')) {
                obj['tables'] = ApiClient.convertToType(data['tables'], [TableListTablesInner]);
            }
            if (data.hasOwnProperty('totalItems')) {
                obj['totalItems'] = ApiClient.convertToType(data['totalItems'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TableList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TableList</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['etag'] && !(typeof data['etag'] === 'string' || data['etag'] instanceof String)) {
            throw new Error("Expected the field `etag` to be a primitive type in the JSON string but got " + data['etag']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        if (data['tables']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tables'])) {
                throw new Error("Expected the field `tables` to be an array in the JSON data but got " + data['tables']);
            }
            // validate the optional field `tables` (array)
            for (const item of data['tables']) {
                TableListTablesInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A hash of this page of results.
 * @member {String} etag
 */
TableList.prototype['etag'] = undefined;

/**
 * The type of list.
 * @member {String} kind
 * @default 'bigquery#tableList'
 */
TableList.prototype['kind'] = 'bigquery#tableList';

/**
 * A token to request the next page of results.
 * @member {String} nextPageToken
 */
TableList.prototype['nextPageToken'] = undefined;

/**
 * Tables in the requested dataset.
 * @member {Array.<module:model/TableListTablesInner>} tables
 */
TableList.prototype['tables'] = undefined;

/**
 * The total number of tables in the dataset.
 * @member {Number} totalItems
 */
TableList.prototype['totalItems'] = undefined;






export default TableList;

