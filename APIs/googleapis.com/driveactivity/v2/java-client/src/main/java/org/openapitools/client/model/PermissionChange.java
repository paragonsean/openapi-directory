/*
 * Drive Activity API
 * Provides a historical view of activity in Google Drive.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.Permission;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * A change of the permission setting on an item.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:35:06.299010-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class PermissionChange {
  public static final String SERIALIZED_NAME_ADDED_PERMISSIONS = "addedPermissions";
  @SerializedName(SERIALIZED_NAME_ADDED_PERMISSIONS)
  private List<Permission> addedPermissions = new ArrayList<>();

  public static final String SERIALIZED_NAME_REMOVED_PERMISSIONS = "removedPermissions";
  @SerializedName(SERIALIZED_NAME_REMOVED_PERMISSIONS)
  private List<Permission> removedPermissions = new ArrayList<>();

  public PermissionChange() {
  }

  public PermissionChange addedPermissions(List<Permission> addedPermissions) {
    this.addedPermissions = addedPermissions;
    return this;
  }

  public PermissionChange addAddedPermissionsItem(Permission addedPermissionsItem) {
    if (this.addedPermissions == null) {
      this.addedPermissions = new ArrayList<>();
    }
    this.addedPermissions.add(addedPermissionsItem);
    return this;
  }

  /**
   * The set of permissions added by this change.
   * @return addedPermissions
   */
  @javax.annotation.Nullable
  public List<Permission> getAddedPermissions() {
    return addedPermissions;
  }

  public void setAddedPermissions(List<Permission> addedPermissions) {
    this.addedPermissions = addedPermissions;
  }


  public PermissionChange removedPermissions(List<Permission> removedPermissions) {
    this.removedPermissions = removedPermissions;
    return this;
  }

  public PermissionChange addRemovedPermissionsItem(Permission removedPermissionsItem) {
    if (this.removedPermissions == null) {
      this.removedPermissions = new ArrayList<>();
    }
    this.removedPermissions.add(removedPermissionsItem);
    return this;
  }

  /**
   * The set of permissions removed by this change.
   * @return removedPermissions
   */
  @javax.annotation.Nullable
  public List<Permission> getRemovedPermissions() {
    return removedPermissions;
  }

  public void setRemovedPermissions(List<Permission> removedPermissions) {
    this.removedPermissions = removedPermissions;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    PermissionChange permissionChange = (PermissionChange) o;
    return Objects.equals(this.addedPermissions, permissionChange.addedPermissions) &&
        Objects.equals(this.removedPermissions, permissionChange.removedPermissions);
  }

  @Override
  public int hashCode() {
    return Objects.hash(addedPermissions, removedPermissions);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class PermissionChange {\n");
    sb.append("    addedPermissions: ").append(toIndentedString(addedPermissions)).append("\n");
    sb.append("    removedPermissions: ").append(toIndentedString(removedPermissions)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("addedPermissions");
    openapiFields.add("removedPermissions");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to PermissionChange
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!PermissionChange.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in PermissionChange is not found in the empty JSON string", PermissionChange.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!PermissionChange.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `PermissionChange` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("addedPermissions") != null && !jsonObj.get("addedPermissions").isJsonNull()) {
        JsonArray jsonArrayaddedPermissions = jsonObj.getAsJsonArray("addedPermissions");
        if (jsonArrayaddedPermissions != null) {
          // ensure the json data is an array
          if (!jsonObj.get("addedPermissions").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `addedPermissions` to be an array in the JSON string but got `%s`", jsonObj.get("addedPermissions").toString()));
          }

          // validate the optional field `addedPermissions` (array)
          for (int i = 0; i < jsonArrayaddedPermissions.size(); i++) {
            Permission.validateJsonElement(jsonArrayaddedPermissions.get(i));
          };
        }
      }
      if (jsonObj.get("removedPermissions") != null && !jsonObj.get("removedPermissions").isJsonNull()) {
        JsonArray jsonArrayremovedPermissions = jsonObj.getAsJsonArray("removedPermissions");
        if (jsonArrayremovedPermissions != null) {
          // ensure the json data is an array
          if (!jsonObj.get("removedPermissions").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `removedPermissions` to be an array in the JSON string but got `%s`", jsonObj.get("removedPermissions").toString()));
          }

          // validate the optional field `removedPermissions` (array)
          for (int i = 0; i < jsonArrayremovedPermissions.size(); i++) {
            Permission.validateJsonElement(jsonArrayremovedPermissions.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!PermissionChange.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'PermissionChange' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<PermissionChange> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(PermissionChange.class));

       return (TypeAdapter<T>) new TypeAdapter<PermissionChange>() {
           @Override
           public void write(JsonWriter out, PermissionChange value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public PermissionChange read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of PermissionChange given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of PermissionChange
   * @throws IOException if the JSON string is invalid with respect to PermissionChange
   */
  public static PermissionChange fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, PermissionChange.class);
  }

  /**
   * Convert an instance of PermissionChange to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

