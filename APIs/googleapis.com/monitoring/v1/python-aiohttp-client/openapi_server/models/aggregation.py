# coding: utf-8

from datetime import date, datetime

from typing import List, Dict, Type

from openapi_server.models.base_model import Model
from openapi_server import util


class Aggregation(Model):
    """NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

    Do not edit the class manually.
    """

    def __init__(self, alignment_period: str=None, cross_series_reducer: str=None, group_by_fields: List[str]=None, per_series_aligner: str=None):
        """Aggregation - a model defined in OpenAPI

        :param alignment_period: The alignment_period of this Aggregation.
        :param cross_series_reducer: The cross_series_reducer of this Aggregation.
        :param group_by_fields: The group_by_fields of this Aggregation.
        :param per_series_aligner: The per_series_aligner of this Aggregation.
        """
        self.openapi_types = {
            'alignment_period': str,
            'cross_series_reducer': str,
            'group_by_fields': List[str],
            'per_series_aligner': str
        }

        self.attribute_map = {
            'alignment_period': 'alignmentPeriod',
            'cross_series_reducer': 'crossSeriesReducer',
            'group_by_fields': 'groupByFields',
            'per_series_aligner': 'perSeriesAligner'
        }

        self._alignment_period = alignment_period
        self._cross_series_reducer = cross_series_reducer
        self._group_by_fields = group_by_fields
        self._per_series_aligner = per_series_aligner

    @classmethod
    def from_dict(cls, dikt: dict) -> 'Aggregation':
        """Returns the dict as a model

        :param dikt: A dict.
        :return: The Aggregation of this Aggregation.
        """
        return util.deserialize_model(dikt, cls)

    @property
    def alignment_period(self):
        """Gets the alignment_period of this Aggregation.

        The alignment_period specifies a time interval, in seconds, that is used to divide the data in all the time series into consistent blocks of time. This will be done before the per-series aligner can be applied to the data.The value must be at least 60 seconds. If a per-series aligner other than ALIGN_NONE is specified, this field is required or an error is returned. If no per-series aligner is specified, or the aligner ALIGN_NONE is specified, then this field is ignored.The maximum value of the alignment_period is 2 years, or 104 weeks.

        :return: The alignment_period of this Aggregation.
        :rtype: str
        """
        return self._alignment_period

    @alignment_period.setter
    def alignment_period(self, alignment_period):
        """Sets the alignment_period of this Aggregation.

        The alignment_period specifies a time interval, in seconds, that is used to divide the data in all the time series into consistent blocks of time. This will be done before the per-series aligner can be applied to the data.The value must be at least 60 seconds. If a per-series aligner other than ALIGN_NONE is specified, this field is required or an error is returned. If no per-series aligner is specified, or the aligner ALIGN_NONE is specified, then this field is ignored.The maximum value of the alignment_period is 2 years, or 104 weeks.

        :param alignment_period: The alignment_period of this Aggregation.
        :type alignment_period: str
        """

        self._alignment_period = alignment_period

    @property
    def cross_series_reducer(self):
        """Gets the cross_series_reducer of this Aggregation.

        The reduction operation to be used to combine time series into a single time series, where the value of each data point in the resulting series is a function of all the already aligned values in the input time series.Not all reducer operations can be applied to all time series. The valid choices depend on the metric_kind and the value_type of the original time series. Reduction can yield a time series with a different metric_kind or value_type than the input time series.Time series data must first be aligned (see per_series_aligner) in order to perform cross-time series reduction. If cross_series_reducer is specified, then per_series_aligner must be specified, and must not be ALIGN_NONE. An alignment_period must also be specified; otherwise, an error is returned.

        :return: The cross_series_reducer of this Aggregation.
        :rtype: str
        """
        return self._cross_series_reducer

    @cross_series_reducer.setter
    def cross_series_reducer(self, cross_series_reducer):
        """Sets the cross_series_reducer of this Aggregation.

        The reduction operation to be used to combine time series into a single time series, where the value of each data point in the resulting series is a function of all the already aligned values in the input time series.Not all reducer operations can be applied to all time series. The valid choices depend on the metric_kind and the value_type of the original time series. Reduction can yield a time series with a different metric_kind or value_type than the input time series.Time series data must first be aligned (see per_series_aligner) in order to perform cross-time series reduction. If cross_series_reducer is specified, then per_series_aligner must be specified, and must not be ALIGN_NONE. An alignment_period must also be specified; otherwise, an error is returned.

        :param cross_series_reducer: The cross_series_reducer of this Aggregation.
        :type cross_series_reducer: str
        """
        allowed_values = ["REDUCE_NONE", "REDUCE_MEAN", "REDUCE_MIN", "REDUCE_MAX", "REDUCE_SUM", "REDUCE_STDDEV", "REDUCE_COUNT", "REDUCE_COUNT_TRUE", "REDUCE_COUNT_FALSE", "REDUCE_FRACTION_TRUE", "REDUCE_PERCENTILE_99", "REDUCE_PERCENTILE_95", "REDUCE_PERCENTILE_50", "REDUCE_PERCENTILE_05"]  # noqa: E501
        if cross_series_reducer not in allowed_values:
            raise ValueError(
                "Invalid value for `cross_series_reducer` ({0}), must be one of {1}"
                .format(cross_series_reducer, allowed_values)
            )

        self._cross_series_reducer = cross_series_reducer

    @property
    def group_by_fields(self):
        """Gets the group_by_fields of this Aggregation.

        The set of fields to preserve when cross_series_reducer is specified. The group_by_fields determine how the time series are partitioned into subsets prior to applying the aggregation operation. Each subset contains time series that have the same value for each of the grouping fields. Each individual time series is a member of exactly one subset. The cross_series_reducer is applied to each subset of time series. It is not possible to reduce across different resource types, so this field implicitly contains resource.type. Fields not specified in group_by_fields are aggregated away. If group_by_fields is not specified and all the time series have the same resource type, then the time series are aggregated into a single output time series. If cross_series_reducer is not defined, this field is ignored.

        :return: The group_by_fields of this Aggregation.
        :rtype: List[str]
        """
        return self._group_by_fields

    @group_by_fields.setter
    def group_by_fields(self, group_by_fields):
        """Sets the group_by_fields of this Aggregation.

        The set of fields to preserve when cross_series_reducer is specified. The group_by_fields determine how the time series are partitioned into subsets prior to applying the aggregation operation. Each subset contains time series that have the same value for each of the grouping fields. Each individual time series is a member of exactly one subset. The cross_series_reducer is applied to each subset of time series. It is not possible to reduce across different resource types, so this field implicitly contains resource.type. Fields not specified in group_by_fields are aggregated away. If group_by_fields is not specified and all the time series have the same resource type, then the time series are aggregated into a single output time series. If cross_series_reducer is not defined, this field is ignored.

        :param group_by_fields: The group_by_fields of this Aggregation.
        :type group_by_fields: List[str]
        """

        self._group_by_fields = group_by_fields

    @property
    def per_series_aligner(self):
        """Gets the per_series_aligner of this Aggregation.

        An Aligner describes how to bring the data points in a single time series into temporal alignment. Except for ALIGN_NONE, all alignments cause all the data points in an alignment_period to be mathematically grouped together, resulting in a single data point for each alignment_period with end timestamp at the end of the period.Not all alignment operations may be applied to all time series. The valid choices depend on the metric_kind and value_type of the original time series. Alignment can change the metric_kind or the value_type of the time series.Time series data must be aligned in order to perform cross-time series reduction. If cross_series_reducer is specified, then per_series_aligner must be specified and not equal to ALIGN_NONE and alignment_period must be specified; otherwise, an error is returned.

        :return: The per_series_aligner of this Aggregation.
        :rtype: str
        """
        return self._per_series_aligner

    @per_series_aligner.setter
    def per_series_aligner(self, per_series_aligner):
        """Sets the per_series_aligner of this Aggregation.

        An Aligner describes how to bring the data points in a single time series into temporal alignment. Except for ALIGN_NONE, all alignments cause all the data points in an alignment_period to be mathematically grouped together, resulting in a single data point for each alignment_period with end timestamp at the end of the period.Not all alignment operations may be applied to all time series. The valid choices depend on the metric_kind and value_type of the original time series. Alignment can change the metric_kind or the value_type of the time series.Time series data must be aligned in order to perform cross-time series reduction. If cross_series_reducer is specified, then per_series_aligner must be specified and not equal to ALIGN_NONE and alignment_period must be specified; otherwise, an error is returned.

        :param per_series_aligner: The per_series_aligner of this Aggregation.
        :type per_series_aligner: str
        """
        allowed_values = ["ALIGN_NONE", "ALIGN_DELTA", "ALIGN_RATE", "ALIGN_INTERPOLATE", "ALIGN_NEXT_OLDER", "ALIGN_MIN", "ALIGN_MAX", "ALIGN_MEAN", "ALIGN_COUNT", "ALIGN_SUM", "ALIGN_STDDEV", "ALIGN_COUNT_TRUE", "ALIGN_COUNT_FALSE", "ALIGN_FRACTION_TRUE", "ALIGN_PERCENTILE_99", "ALIGN_PERCENTILE_95", "ALIGN_PERCENTILE_50", "ALIGN_PERCENTILE_05", "ALIGN_PERCENT_CHANGE"]  # noqa: E501
        if per_series_aligner not in allowed_values:
            raise ValueError(
                "Invalid value for `per_series_aligner` ({0}), must be one of {1}"
                .format(per_series_aligner, allowed_values)
            )

        self._per_series_aligner = per_series_aligner
