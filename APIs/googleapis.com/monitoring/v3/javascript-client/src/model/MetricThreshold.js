/**
 * Cloud Monitoring API
 * Manages your Cloud Monitoring data and configurations.
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Aggregation from './Aggregation';
import ForecastOptions from './ForecastOptions';
import Trigger from './Trigger';

/**
 * The MetricThreshold model module.
 * @module model/MetricThreshold
 * @version v3
 */
class MetricThreshold {
    /**
     * Constructs a new <code>MetricThreshold</code>.
     * A condition type that compares a collection of time series against a threshold.
     * @alias module:model/MetricThreshold
     */
    constructor() { 
        
        MetricThreshold.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>MetricThreshold</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MetricThreshold} obj Optional instance to populate.
     * @return {module:model/MetricThreshold} The populated <code>MetricThreshold</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MetricThreshold();

            if (data.hasOwnProperty('aggregations')) {
                obj['aggregations'] = ApiClient.convertToType(data['aggregations'], [Aggregation]);
            }
            if (data.hasOwnProperty('comparison')) {
                obj['comparison'] = ApiClient.convertToType(data['comparison'], 'String');
            }
            if (data.hasOwnProperty('denominatorAggregations')) {
                obj['denominatorAggregations'] = ApiClient.convertToType(data['denominatorAggregations'], [Aggregation]);
            }
            if (data.hasOwnProperty('denominatorFilter')) {
                obj['denominatorFilter'] = ApiClient.convertToType(data['denominatorFilter'], 'String');
            }
            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'String');
            }
            if (data.hasOwnProperty('evaluationMissingData')) {
                obj['evaluationMissingData'] = ApiClient.convertToType(data['evaluationMissingData'], 'String');
            }
            if (data.hasOwnProperty('filter')) {
                obj['filter'] = ApiClient.convertToType(data['filter'], 'String');
            }
            if (data.hasOwnProperty('forecastOptions')) {
                obj['forecastOptions'] = ForecastOptions.constructFromObject(data['forecastOptions']);
            }
            if (data.hasOwnProperty('thresholdValue')) {
                obj['thresholdValue'] = ApiClient.convertToType(data['thresholdValue'], 'Number');
            }
            if (data.hasOwnProperty('trigger')) {
                obj['trigger'] = Trigger.constructFromObject(data['trigger']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MetricThreshold</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MetricThreshold</code>.
     */
    static validateJSON(data) {
        if (data['aggregations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['aggregations'])) {
                throw new Error("Expected the field `aggregations` to be an array in the JSON data but got " + data['aggregations']);
            }
            // validate the optional field `aggregations` (array)
            for (const item of data['aggregations']) {
                Aggregation.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['comparison'] && !(typeof data['comparison'] === 'string' || data['comparison'] instanceof String)) {
            throw new Error("Expected the field `comparison` to be a primitive type in the JSON string but got " + data['comparison']);
        }
        if (data['denominatorAggregations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['denominatorAggregations'])) {
                throw new Error("Expected the field `denominatorAggregations` to be an array in the JSON data but got " + data['denominatorAggregations']);
            }
            // validate the optional field `denominatorAggregations` (array)
            for (const item of data['denominatorAggregations']) {
                Aggregation.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['denominatorFilter'] && !(typeof data['denominatorFilter'] === 'string' || data['denominatorFilter'] instanceof String)) {
            throw new Error("Expected the field `denominatorFilter` to be a primitive type in the JSON string but got " + data['denominatorFilter']);
        }
        // ensure the json data is a string
        if (data['duration'] && !(typeof data['duration'] === 'string' || data['duration'] instanceof String)) {
            throw new Error("Expected the field `duration` to be a primitive type in the JSON string but got " + data['duration']);
        }
        // ensure the json data is a string
        if (data['evaluationMissingData'] && !(typeof data['evaluationMissingData'] === 'string' || data['evaluationMissingData'] instanceof String)) {
            throw new Error("Expected the field `evaluationMissingData` to be a primitive type in the JSON string but got " + data['evaluationMissingData']);
        }
        // ensure the json data is a string
        if (data['filter'] && !(typeof data['filter'] === 'string' || data['filter'] instanceof String)) {
            throw new Error("Expected the field `filter` to be a primitive type in the JSON string but got " + data['filter']);
        }
        // validate the optional field `forecastOptions`
        if (data['forecastOptions']) { // data not null
          ForecastOptions.validateJSON(data['forecastOptions']);
        }
        // validate the optional field `trigger`
        if (data['trigger']) { // data not null
          Trigger.validateJSON(data['trigger']);
        }

        return true;
    }


}



/**
 * Specifies the alignment of data points in individual time series as well as how to combine the retrieved time series together (such as when aggregating multiple streams on each resource to a single stream for each resource or when aggregating streams across all members of a group of resources). Multiple aggregations are applied in the order specified.This field is similar to the one in the ListTimeSeries request (https://cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.timeSeries/list). It is advisable to use the ListTimeSeries method when debugging this field.
 * @member {Array.<module:model/Aggregation>} aggregations
 */
MetricThreshold.prototype['aggregations'] = undefined;

/**
 * The comparison to apply between the time series (indicated by filter and aggregation) and the threshold (indicated by threshold_value). The comparison is applied on each time series, with the time series on the left-hand side and the threshold on the right-hand side.Only COMPARISON_LT and COMPARISON_GT are supported currently.
 * @member {module:model/MetricThreshold.ComparisonEnum} comparison
 */
MetricThreshold.prototype['comparison'] = undefined;

/**
 * Specifies the alignment of data points in individual time series selected by denominatorFilter as well as how to combine the retrieved time series together (such as when aggregating multiple streams on each resource to a single stream for each resource or when aggregating streams across all members of a group of resources).When computing ratios, the aggregations and denominator_aggregations fields must use the same alignment period and produce time series that have the same periodicity and labels.
 * @member {Array.<module:model/Aggregation>} denominatorAggregations
 */
MetricThreshold.prototype['denominatorAggregations'] = undefined;

/**
 * A filter (https://cloud.google.com/monitoring/api/v3/filters) that identifies a time series that should be used as the denominator of a ratio that will be compared with the threshold. If a denominator_filter is specified, the time series specified by the filter field will be used as the numerator.The filter must specify the metric type and optionally may contain restrictions on resource type, resource labels, and metric labels. This field may not exceed 2048 Unicode characters in length.
 * @member {String} denominatorFilter
 */
MetricThreshold.prototype['denominatorFilter'] = undefined;

/**
 * The amount of time that a time series must violate the threshold to be considered failing. Currently, only values that are a multiple of a minute--e.g., 0, 60, 120, or 300 seconds--are supported. If an invalid value is given, an error will be returned. When choosing a duration, it is useful to keep in mind the frequency of the underlying time series data (which may also be affected by any alignments specified in the aggregations field); a good duration is long enough so that a single outlier does not generate spurious alerts, but short enough that unhealthy states are detected and alerted on quickly.
 * @member {String} duration
 */
MetricThreshold.prototype['duration'] = undefined;

/**
 * A condition control that determines how metric-threshold conditions are evaluated when data stops arriving. To use this control, the value of the duration field must be greater than or equal to 60 seconds.
 * @member {module:model/MetricThreshold.EvaluationMissingDataEnum} evaluationMissingData
 */
MetricThreshold.prototype['evaluationMissingData'] = undefined;

/**
 * Required. A filter (https://cloud.google.com/monitoring/api/v3/filters) that identifies which time series should be compared with the threshold.The filter is similar to the one that is specified in the ListTimeSeries request (https://cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.timeSeries/list) (that call is useful to verify the time series that will be retrieved / processed). The filter must specify the metric type and the resource type. Optionally, it can specify resource labels and metric labels. This field must not exceed 2048 Unicode characters in length.
 * @member {String} filter
 */
MetricThreshold.prototype['filter'] = undefined;

/**
 * @member {module:model/ForecastOptions} forecastOptions
 */
MetricThreshold.prototype['forecastOptions'] = undefined;

/**
 * A value against which to compare the time series.
 * @member {Number} thresholdValue
 */
MetricThreshold.prototype['thresholdValue'] = undefined;

/**
 * @member {module:model/Trigger} trigger
 */
MetricThreshold.prototype['trigger'] = undefined;





/**
 * Allowed values for the <code>comparison</code> property.
 * @enum {String}
 * @readonly
 */
MetricThreshold['ComparisonEnum'] = {

    /**
     * value: "COMPARISON_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "COMPARISON_UNSPECIFIED",

    /**
     * value: "COMPARISON_GT"
     * @const
     */
    "GT": "COMPARISON_GT",

    /**
     * value: "COMPARISON_GE"
     * @const
     */
    "GE": "COMPARISON_GE",

    /**
     * value: "COMPARISON_LT"
     * @const
     */
    "LT": "COMPARISON_LT",

    /**
     * value: "COMPARISON_LE"
     * @const
     */
    "LE": "COMPARISON_LE",

    /**
     * value: "COMPARISON_EQ"
     * @const
     */
    "EQ": "COMPARISON_EQ",

    /**
     * value: "COMPARISON_NE"
     * @const
     */
    "NE": "COMPARISON_NE"
};


/**
 * Allowed values for the <code>evaluationMissingData</code> property.
 * @enum {String}
 * @readonly
 */
MetricThreshold['EvaluationMissingDataEnum'] = {

    /**
     * value: "EVALUATION_MISSING_DATA_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "EVALUATION_MISSING_DATA_UNSPECIFIED",

    /**
     * value: "EVALUATION_MISSING_DATA_INACTIVE"
     * @const
     */
    "INACTIVE": "EVALUATION_MISSING_DATA_INACTIVE",

    /**
     * value: "EVALUATION_MISSING_DATA_ACTIVE"
     * @const
     */
    "ACTIVE": "EVALUATION_MISSING_DATA_ACTIVE",

    /**
     * value: "EVALUATION_MISSING_DATA_NO_OP"
     * @const
     */
    "NO_OP": "EVALUATION_MISSING_DATA_NO_OP"
};



export default MetricThreshold;

