/**
 * Cloud Monitoring API
 * Manages your Cloud Monitoring data and configurations.
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IstioCanonicalService model module.
 * @module model/IstioCanonicalService
 * @version v3
 */
class IstioCanonicalService {
    /**
     * Constructs a new <code>IstioCanonicalService</code>.
     * Canonical service scoped to an Istio mesh. Anthos clusters running ASM &gt;&#x3D; 1.6.8 will have their services ingested as this type.
     * @alias module:model/IstioCanonicalService
     */
    constructor() { 
        
        IstioCanonicalService.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>IstioCanonicalService</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IstioCanonicalService} obj Optional instance to populate.
     * @return {module:model/IstioCanonicalService} The populated <code>IstioCanonicalService</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IstioCanonicalService();

            if (data.hasOwnProperty('canonicalService')) {
                obj['canonicalService'] = ApiClient.convertToType(data['canonicalService'], 'String');
            }
            if (data.hasOwnProperty('canonicalServiceNamespace')) {
                obj['canonicalServiceNamespace'] = ApiClient.convertToType(data['canonicalServiceNamespace'], 'String');
            }
            if (data.hasOwnProperty('meshUid')) {
                obj['meshUid'] = ApiClient.convertToType(data['meshUid'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IstioCanonicalService</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IstioCanonicalService</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['canonicalService'] && !(typeof data['canonicalService'] === 'string' || data['canonicalService'] instanceof String)) {
            throw new Error("Expected the field `canonicalService` to be a primitive type in the JSON string but got " + data['canonicalService']);
        }
        // ensure the json data is a string
        if (data['canonicalServiceNamespace'] && !(typeof data['canonicalServiceNamespace'] === 'string' || data['canonicalServiceNamespace'] instanceof String)) {
            throw new Error("Expected the field `canonicalServiceNamespace` to be a primitive type in the JSON string but got " + data['canonicalServiceNamespace']);
        }
        // ensure the json data is a string
        if (data['meshUid'] && !(typeof data['meshUid'] === 'string' || data['meshUid'] instanceof String)) {
            throw new Error("Expected the field `meshUid` to be a primitive type in the JSON string but got " + data['meshUid']);
        }

        return true;
    }


}



/**
 * The name of the canonical service underlying this service. Corresponds to the destination_canonical_service_name metric label in label in Istio metrics (https://cloud.google.com/monitoring/api/metrics_istio).
 * @member {String} canonicalService
 */
IstioCanonicalService.prototype['canonicalService'] = undefined;

/**
 * The namespace of the canonical service underlying this service. Corresponds to the destination_canonical_service_namespace metric label in Istio metrics (https://cloud.google.com/monitoring/api/metrics_istio).
 * @member {String} canonicalServiceNamespace
 */
IstioCanonicalService.prototype['canonicalServiceNamespace'] = undefined;

/**
 * Identifier for the Istio mesh in which this canonical service is defined. Corresponds to the mesh_uid metric label in Istio metrics (https://cloud.google.com/monitoring/api/metrics_istio).
 * @member {String} meshUid
 */
IstioCanonicalService.prototype['meshUid'] = undefined;






export default IstioCanonicalService;

