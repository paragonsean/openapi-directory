/**
 * Cloud Vision API
 * Integrates Google Vision features, including image labeling, face, logo, and landmark detection, optical character recognition (OCR), and detection of explicit content, into applications.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BoundingPoly from './BoundingPoly';
import ObjectAnnotation from './ObjectAnnotation';
import Result from './Result';

/**
 * The GroupedResult model module.
 * @module model/GroupedResult
 * @version v1
 */
class GroupedResult {
    /**
     * Constructs a new <code>GroupedResult</code>.
     * Information about the products similar to a single product in a query image.
     * @alias module:model/GroupedResult
     */
    constructor() { 
        
        GroupedResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GroupedResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupedResult} obj Optional instance to populate.
     * @return {module:model/GroupedResult} The populated <code>GroupedResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupedResult();

            if (data.hasOwnProperty('boundingPoly')) {
                obj['boundingPoly'] = BoundingPoly.constructFromObject(data['boundingPoly']);
            }
            if (data.hasOwnProperty('objectAnnotations')) {
                obj['objectAnnotations'] = ApiClient.convertToType(data['objectAnnotations'], [ObjectAnnotation]);
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [Result]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupedResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupedResult</code>.
     */
    static validateJSON(data) {
        // validate the optional field `boundingPoly`
        if (data['boundingPoly']) { // data not null
          BoundingPoly.validateJSON(data['boundingPoly']);
        }
        if (data['objectAnnotations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['objectAnnotations'])) {
                throw new Error("Expected the field `objectAnnotations` to be an array in the JSON data but got " + data['objectAnnotations']);
            }
            // validate the optional field `objectAnnotations` (array)
            for (const item of data['objectAnnotations']) {
                ObjectAnnotation.validateJSON(item);
            };
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                Result.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/BoundingPoly} boundingPoly
 */
GroupedResult.prototype['boundingPoly'] = undefined;

/**
 * List of generic predictions for the object in the bounding box.
 * @member {Array.<module:model/ObjectAnnotation>} objectAnnotations
 */
GroupedResult.prototype['objectAnnotations'] = undefined;

/**
 * List of results, one for each product match.
 * @member {Array.<module:model/Result>} results
 */
GroupedResult.prototype['results'] = undefined;






export default GroupedResult;

