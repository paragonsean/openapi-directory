/*
 * Cloud Vision API
 * Integrates Google Vision features, including image labeling, face, logo, and landmark detection, optical character recognition (OCR), and detection of explicit content, into applications.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.NormalizedVertex;
import org.openapitools.client.model.Vertex;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * A bounding polygon for the detected image annotation.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:45:39.270009-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class BoundingPoly {
  public static final String SERIALIZED_NAME_NORMALIZED_VERTICES = "normalizedVertices";
  @SerializedName(SERIALIZED_NAME_NORMALIZED_VERTICES)
  private List<NormalizedVertex> normalizedVertices = new ArrayList<>();

  public static final String SERIALIZED_NAME_VERTICES = "vertices";
  @SerializedName(SERIALIZED_NAME_VERTICES)
  private List<Vertex> vertices = new ArrayList<>();

  public BoundingPoly() {
  }

  public BoundingPoly normalizedVertices(List<NormalizedVertex> normalizedVertices) {
    this.normalizedVertices = normalizedVertices;
    return this;
  }

  public BoundingPoly addNormalizedVerticesItem(NormalizedVertex normalizedVerticesItem) {
    if (this.normalizedVertices == null) {
      this.normalizedVertices = new ArrayList<>();
    }
    this.normalizedVertices.add(normalizedVerticesItem);
    return this;
  }

  /**
   * The bounding polygon normalized vertices.
   * @return normalizedVertices
   */
  @javax.annotation.Nullable
  public List<NormalizedVertex> getNormalizedVertices() {
    return normalizedVertices;
  }

  public void setNormalizedVertices(List<NormalizedVertex> normalizedVertices) {
    this.normalizedVertices = normalizedVertices;
  }


  public BoundingPoly vertices(List<Vertex> vertices) {
    this.vertices = vertices;
    return this;
  }

  public BoundingPoly addVerticesItem(Vertex verticesItem) {
    if (this.vertices == null) {
      this.vertices = new ArrayList<>();
    }
    this.vertices.add(verticesItem);
    return this;
  }

  /**
   * The bounding polygon vertices.
   * @return vertices
   */
  @javax.annotation.Nullable
  public List<Vertex> getVertices() {
    return vertices;
  }

  public void setVertices(List<Vertex> vertices) {
    this.vertices = vertices;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    BoundingPoly boundingPoly = (BoundingPoly) o;
    return Objects.equals(this.normalizedVertices, boundingPoly.normalizedVertices) &&
        Objects.equals(this.vertices, boundingPoly.vertices);
  }

  @Override
  public int hashCode() {
    return Objects.hash(normalizedVertices, vertices);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class BoundingPoly {\n");
    sb.append("    normalizedVertices: ").append(toIndentedString(normalizedVertices)).append("\n");
    sb.append("    vertices: ").append(toIndentedString(vertices)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("normalizedVertices");
    openapiFields.add("vertices");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to BoundingPoly
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!BoundingPoly.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in BoundingPoly is not found in the empty JSON string", BoundingPoly.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!BoundingPoly.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `BoundingPoly` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("normalizedVertices") != null && !jsonObj.get("normalizedVertices").isJsonNull()) {
        JsonArray jsonArraynormalizedVertices = jsonObj.getAsJsonArray("normalizedVertices");
        if (jsonArraynormalizedVertices != null) {
          // ensure the json data is an array
          if (!jsonObj.get("normalizedVertices").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `normalizedVertices` to be an array in the JSON string but got `%s`", jsonObj.get("normalizedVertices").toString()));
          }

          // validate the optional field `normalizedVertices` (array)
          for (int i = 0; i < jsonArraynormalizedVertices.size(); i++) {
            NormalizedVertex.validateJsonElement(jsonArraynormalizedVertices.get(i));
          };
        }
      }
      if (jsonObj.get("vertices") != null && !jsonObj.get("vertices").isJsonNull()) {
        JsonArray jsonArrayvertices = jsonObj.getAsJsonArray("vertices");
        if (jsonArrayvertices != null) {
          // ensure the json data is an array
          if (!jsonObj.get("vertices").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `vertices` to be an array in the JSON string but got `%s`", jsonObj.get("vertices").toString()));
          }

          // validate the optional field `vertices` (array)
          for (int i = 0; i < jsonArrayvertices.size(); i++) {
            Vertex.validateJsonElement(jsonArrayvertices.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!BoundingPoly.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'BoundingPoly' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<BoundingPoly> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(BoundingPoly.class));

       return (TypeAdapter<T>) new TypeAdapter<BoundingPoly>() {
           @Override
           public void write(JsonWriter out, BoundingPoly value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public BoundingPoly read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of BoundingPoly given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of BoundingPoly
   * @throws IOException if the JSON string is invalid with respect to BoundingPoly
   */
  public static BoundingPoly fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, BoundingPoly.class);
  }

  /**
   * Convert an instance of BoundingPoly to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

