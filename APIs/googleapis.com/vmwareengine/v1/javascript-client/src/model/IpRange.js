/**
 * VMware Engine API
 * The Google VMware Engine API lets you programmatically manage VMware environments.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IpRange model module.
 * @module model/IpRange
 * @version v1
 */
class IpRange {
    /**
     * Constructs a new <code>IpRange</code>.
     * An IP range provided in any one of the supported formats.
     * @alias module:model/IpRange
     */
    constructor() { 
        
        IpRange.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>IpRange</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IpRange} obj Optional instance to populate.
     * @return {module:model/IpRange} The populated <code>IpRange</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IpRange();

            if (data.hasOwnProperty('externalAddress')) {
                obj['externalAddress'] = ApiClient.convertToType(data['externalAddress'], 'String');
            }
            if (data.hasOwnProperty('ipAddress')) {
                obj['ipAddress'] = ApiClient.convertToType(data['ipAddress'], 'String');
            }
            if (data.hasOwnProperty('ipAddressRange')) {
                obj['ipAddressRange'] = ApiClient.convertToType(data['ipAddressRange'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IpRange</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IpRange</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['externalAddress'] && !(typeof data['externalAddress'] === 'string' || data['externalAddress'] instanceof String)) {
            throw new Error("Expected the field `externalAddress` to be a primitive type in the JSON string but got " + data['externalAddress']);
        }
        // ensure the json data is a string
        if (data['ipAddress'] && !(typeof data['ipAddress'] === 'string' || data['ipAddress'] instanceof String)) {
            throw new Error("Expected the field `ipAddress` to be a primitive type in the JSON string but got " + data['ipAddress']);
        }
        // ensure the json data is a string
        if (data['ipAddressRange'] && !(typeof data['ipAddressRange'] === 'string' || data['ipAddressRange'] instanceof String)) {
            throw new Error("Expected the field `ipAddressRange` to be a primitive type in the JSON string but got " + data['ipAddressRange']);
        }

        return true;
    }


}



/**
 * The name of an `ExternalAddress` resource. The external address must have been reserved in the scope of this external access rule's parent network policy. Provide the external address name in the form of `projects/{project}/locations/{location}/privateClouds/{private_cloud}/externalAddresses/{external_address}`. For example: `projects/my-project/locations/us-central1-a/privateClouds/my-cloud/externalAddresses/my-address`.
 * @member {String} externalAddress
 */
IpRange.prototype['externalAddress'] = undefined;

/**
 * A single IP address. For example: `10.0.0.5`.
 * @member {String} ipAddress
 */
IpRange.prototype['ipAddress'] = undefined;

/**
 * An IP address range in the CIDR format. For example: `10.0.0.0/24`.
 * @member {String} ipAddressRange
 */
IpRange.prototype['ipAddressRange'] = undefined;






export default IpRange;

