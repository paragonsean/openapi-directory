/**
 * VMware Engine API
 * The Google VMware Engine API lets you programmatically manage VMware environments.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IpRange from './IpRange';

/**
 * The ExternalAccessRule model module.
 * @module model/ExternalAccessRule
 * @version v1
 */
class ExternalAccessRule {
    /**
     * Constructs a new <code>ExternalAccessRule</code>.
     * External access firewall rules for filtering incoming traffic destined to &#x60;ExternalAddress&#x60; resources.
     * @alias module:model/ExternalAccessRule
     */
    constructor() { 
        
        ExternalAccessRule.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExternalAccessRule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExternalAccessRule} obj Optional instance to populate.
     * @return {module:model/ExternalAccessRule} The populated <code>ExternalAccessRule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExternalAccessRule();

            if (data.hasOwnProperty('action')) {
                obj['action'] = ApiClient.convertToType(data['action'], 'String');
            }
            if (data.hasOwnProperty('createTime')) {
                obj['createTime'] = ApiClient.convertToType(data['createTime'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('destinationIpRanges')) {
                obj['destinationIpRanges'] = ApiClient.convertToType(data['destinationIpRanges'], [IpRange]);
            }
            if (data.hasOwnProperty('destinationPorts')) {
                obj['destinationPorts'] = ApiClient.convertToType(data['destinationPorts'], ['String']);
            }
            if (data.hasOwnProperty('ipProtocol')) {
                obj['ipProtocol'] = ApiClient.convertToType(data['ipProtocol'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'Number');
            }
            if (data.hasOwnProperty('sourceIpRanges')) {
                obj['sourceIpRanges'] = ApiClient.convertToType(data['sourceIpRanges'], [IpRange]);
            }
            if (data.hasOwnProperty('sourcePorts')) {
                obj['sourcePorts'] = ApiClient.convertToType(data['sourcePorts'], ['String']);
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('uid')) {
                obj['uid'] = ApiClient.convertToType(data['uid'], 'String');
            }
            if (data.hasOwnProperty('updateTime')) {
                obj['updateTime'] = ApiClient.convertToType(data['updateTime'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExternalAccessRule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExternalAccessRule</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['action'] && !(typeof data['action'] === 'string' || data['action'] instanceof String)) {
            throw new Error("Expected the field `action` to be a primitive type in the JSON string but got " + data['action']);
        }
        // ensure the json data is a string
        if (data['createTime'] && !(typeof data['createTime'] === 'string' || data['createTime'] instanceof String)) {
            throw new Error("Expected the field `createTime` to be a primitive type in the JSON string but got " + data['createTime']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['destinationIpRanges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['destinationIpRanges'])) {
                throw new Error("Expected the field `destinationIpRanges` to be an array in the JSON data but got " + data['destinationIpRanges']);
            }
            // validate the optional field `destinationIpRanges` (array)
            for (const item of data['destinationIpRanges']) {
                IpRange.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['destinationPorts'])) {
            throw new Error("Expected the field `destinationPorts` to be an array in the JSON data but got " + data['destinationPorts']);
        }
        // ensure the json data is a string
        if (data['ipProtocol'] && !(typeof data['ipProtocol'] === 'string' || data['ipProtocol'] instanceof String)) {
            throw new Error("Expected the field `ipProtocol` to be a primitive type in the JSON string but got " + data['ipProtocol']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['sourceIpRanges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sourceIpRanges'])) {
                throw new Error("Expected the field `sourceIpRanges` to be an array in the JSON data but got " + data['sourceIpRanges']);
            }
            // validate the optional field `sourceIpRanges` (array)
            for (const item of data['sourceIpRanges']) {
                IpRange.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['sourcePorts'])) {
            throw new Error("Expected the field `sourcePorts` to be an array in the JSON data but got " + data['sourcePorts']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['uid'] && !(typeof data['uid'] === 'string' || data['uid'] instanceof String)) {
            throw new Error("Expected the field `uid` to be a primitive type in the JSON string but got " + data['uid']);
        }
        // ensure the json data is a string
        if (data['updateTime'] && !(typeof data['updateTime'] === 'string' || data['updateTime'] instanceof String)) {
            throw new Error("Expected the field `updateTime` to be a primitive type in the JSON string but got " + data['updateTime']);
        }

        return true;
    }


}



/**
 * The action that the external access rule performs.
 * @member {module:model/ExternalAccessRule.ActionEnum} action
 */
ExternalAccessRule.prototype['action'] = undefined;

/**
 * Output only. Creation time of this resource.
 * @member {String} createTime
 */
ExternalAccessRule.prototype['createTime'] = undefined;

/**
 * User-provided description for this external access rule.
 * @member {String} description
 */
ExternalAccessRule.prototype['description'] = undefined;

/**
 * If destination ranges are specified, the external access rule applies only to the traffic that has a destination IP address in these ranges. The specified IP addresses must have reserved external IP addresses in the scope of the parent network policy. To match all external IP addresses in the scope of the parent network policy, specify `0.0.0.0/0`. To match a specific external IP address, specify it using the `IpRange.external_address` property.
 * @member {Array.<module:model/IpRange>} destinationIpRanges
 */
ExternalAccessRule.prototype['destinationIpRanges'] = undefined;

/**
 * A list of destination ports to which the external access rule applies. This field is only applicable for the UDP or TCP protocol. Each entry must be either an integer or a range. For example: `[\"22\"]`, `[\"80\",\"443\"]`, or `[\"12345-12349\"]`. To match all destination ports, specify `[\"0-65535\"]`.
 * @member {Array.<String>} destinationPorts
 */
ExternalAccessRule.prototype['destinationPorts'] = undefined;

/**
 * The IP protocol to which the external access rule applies. This value can be one of the following three protocol strings (not case-sensitive): `tcp`, `udp`, or `icmp`.
 * @member {String} ipProtocol
 */
ExternalAccessRule.prototype['ipProtocol'] = undefined;

/**
 * Output only. The resource name of this external access rule. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. For example: `projects/my-project/locations/us-central1/networkPolicies/my-policy/externalAccessRules/my-rule`
 * @member {String} name
 */
ExternalAccessRule.prototype['name'] = undefined;

/**
 * External access rule priority, which determines the external access rule to use when multiple rules apply. If multiple rules have the same priority, their ordering is non-deterministic. If specific ordering is required, assign unique priorities to enforce such ordering. The external access rule priority is an integer from 100 to 4096, both inclusive. Lower integers indicate higher precedence. For example, a rule with priority `100` has higher precedence than a rule with priority `101`.
 * @member {Number} priority
 */
ExternalAccessRule.prototype['priority'] = undefined;

/**
 * If source ranges are specified, the external access rule applies only to traffic that has a source IP address in these ranges. These ranges can either be expressed in the CIDR format or as an IP address. As only inbound rules are supported, `ExternalAddress` resources cannot be the source IP addresses of an external access rule. To match all source addresses, specify `0.0.0.0/0`.
 * @member {Array.<module:model/IpRange>} sourceIpRanges
 */
ExternalAccessRule.prototype['sourceIpRanges'] = undefined;

/**
 * A list of source ports to which the external access rule applies. This field is only applicable for the UDP or TCP protocol. Each entry must be either an integer or a range. For example: `[\"22\"]`, `[\"80\",\"443\"]`, or `[\"12345-12349\"]`. To match all source ports, specify `[\"0-65535\"]`.
 * @member {Array.<String>} sourcePorts
 */
ExternalAccessRule.prototype['sourcePorts'] = undefined;

/**
 * Output only. The state of the resource.
 * @member {module:model/ExternalAccessRule.StateEnum} state
 */
ExternalAccessRule.prototype['state'] = undefined;

/**
 * Output only. System-generated unique identifier for the resource.
 * @member {String} uid
 */
ExternalAccessRule.prototype['uid'] = undefined;

/**
 * Output only. Last update time of this resource.
 * @member {String} updateTime
 */
ExternalAccessRule.prototype['updateTime'] = undefined;





/**
 * Allowed values for the <code>action</code> property.
 * @enum {String}
 * @readonly
 */
ExternalAccessRule['ActionEnum'] = {

    /**
     * value: "ACTION_UNSPECIFIED"
     * @const
     */
    "ACTION_UNSPECIFIED": "ACTION_UNSPECIFIED",

    /**
     * value: "ALLOW"
     * @const
     */
    "ALLOW": "ALLOW",

    /**
     * value: "DENY"
     * @const
     */
    "DENY": "DENY"
};


/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
ExternalAccessRule['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "ACTIVE"
     * @const
     */
    "ACTIVE": "ACTIVE",

    /**
     * value: "CREATING"
     * @const
     */
    "CREATING": "CREATING",

    /**
     * value: "UPDATING"
     * @const
     */
    "UPDATING": "UPDATING",

    /**
     * value: "DELETING"
     * @const
     */
    "DELETING": "DELETING"
};



export default ExternalAccessRule;

