/**
 * VMware Engine API
 * The Google VMware Engine API lets you programmatically manage VMware environments.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PrivateCloud from './PrivateCloud';

/**
 * The ListPrivateCloudsResponse model module.
 * @module model/ListPrivateCloudsResponse
 * @version v1
 */
class ListPrivateCloudsResponse {
    /**
     * Constructs a new <code>ListPrivateCloudsResponse</code>.
     * Response message for VmwareEngine.ListPrivateClouds
     * @alias module:model/ListPrivateCloudsResponse
     */
    constructor() { 
        
        ListPrivateCloudsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListPrivateCloudsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListPrivateCloudsResponse} obj Optional instance to populate.
     * @return {module:model/ListPrivateCloudsResponse} The populated <code>ListPrivateCloudsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListPrivateCloudsResponse();

            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('privateClouds')) {
                obj['privateClouds'] = ApiClient.convertToType(data['privateClouds'], [PrivateCloud]);
            }
            if (data.hasOwnProperty('unreachable')) {
                obj['unreachable'] = ApiClient.convertToType(data['unreachable'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListPrivateCloudsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListPrivateCloudsResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        if (data['privateClouds']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['privateClouds'])) {
                throw new Error("Expected the field `privateClouds` to be an array in the JSON data but got " + data['privateClouds']);
            }
            // validate the optional field `privateClouds` (array)
            for (const item of data['privateClouds']) {
                PrivateCloud.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['unreachable'])) {
            throw new Error("Expected the field `unreachable` to be an array in the JSON data but got " + data['unreachable']);
        }

        return true;
    }


}



/**
 * A token, which can be sent as `page_token` to retrieve the next page. If this field is omitted, there are no subsequent pages.
 * @member {String} nextPageToken
 */
ListPrivateCloudsResponse.prototype['nextPageToken'] = undefined;

/**
 * A list of private clouds.
 * @member {Array.<module:model/PrivateCloud>} privateClouds
 */
ListPrivateCloudsResponse.prototype['privateClouds'] = undefined;

/**
 * Locations that could not be reached when making an aggregated query using wildcards.
 * @member {Array.<String>} unreachable
 */
ListPrivateCloudsResponse.prototype['unreachable'] = undefined;






export default ListPrivateCloudsResponse;

