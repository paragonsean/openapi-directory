/**
 * VMware Engine API
 * The Google VMware Engine API lets you programmatically manage VMware environments.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PeeringRoute from './PeeringRoute';

/**
 * The ListPrivateConnectionPeeringRoutesResponse model module.
 * @module model/ListPrivateConnectionPeeringRoutesResponse
 * @version v1
 */
class ListPrivateConnectionPeeringRoutesResponse {
    /**
     * Constructs a new <code>ListPrivateConnectionPeeringRoutesResponse</code>.
     * Response message for VmwareEngine.ListPrivateConnectionPeeringRoutes
     * @alias module:model/ListPrivateConnectionPeeringRoutesResponse
     */
    constructor() { 
        
        ListPrivateConnectionPeeringRoutesResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListPrivateConnectionPeeringRoutesResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListPrivateConnectionPeeringRoutesResponse} obj Optional instance to populate.
     * @return {module:model/ListPrivateConnectionPeeringRoutesResponse} The populated <code>ListPrivateConnectionPeeringRoutesResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListPrivateConnectionPeeringRoutesResponse();

            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('peeringRoutes')) {
                obj['peeringRoutes'] = ApiClient.convertToType(data['peeringRoutes'], [PeeringRoute]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListPrivateConnectionPeeringRoutesResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListPrivateConnectionPeeringRoutesResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        if (data['peeringRoutes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['peeringRoutes'])) {
                throw new Error("Expected the field `peeringRoutes` to be an array in the JSON data but got " + data['peeringRoutes']);
            }
            // validate the optional field `peeringRoutes` (array)
            for (const item of data['peeringRoutes']) {
                PeeringRoute.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A token, which can be sent as `page_token` to retrieve the next page. If this field is omitted, there are no subsequent pages.
 * @member {String} nextPageToken
 */
ListPrivateConnectionPeeringRoutesResponse.prototype['nextPageToken'] = undefined;

/**
 * A list of peering routes.
 * @member {Array.<module:model/PeeringRoute>} peeringRoutes
 */
ListPrivateConnectionPeeringRoutesResponse.prototype['peeringRoutes'] = undefined;






export default ListPrivateConnectionPeeringRoutesResponse;

