/*
 * Dataproc Metastore API
 * The Dataproc Metastore API is used to manage the lifecycle and configuration of metastore services.
 *
 * The version of the OpenAPI document: v1alpha
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.MetadataExport;
import org.openapitools.client.model.Restore;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * The metadata management activities of the metastore service.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:50:14.340752-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class MetadataManagementActivity {
  public static final String SERIALIZED_NAME_METADATA_EXPORTS = "metadataExports";
  @SerializedName(SERIALIZED_NAME_METADATA_EXPORTS)
  private List<MetadataExport> metadataExports = new ArrayList<>();

  public static final String SERIALIZED_NAME_RESTORES = "restores";
  @SerializedName(SERIALIZED_NAME_RESTORES)
  private List<Restore> restores = new ArrayList<>();

  public MetadataManagementActivity() {
  }

  public MetadataManagementActivity(
     List<MetadataExport> metadataExports, 
     List<Restore> restores
  ) {
    this();
    this.metadataExports = metadataExports;
    this.restores = restores;
  }

  /**
   * Output only. The latest metadata exports of the metastore service.
   * @return metadataExports
   */
  @javax.annotation.Nullable
  public List<MetadataExport> getMetadataExports() {
    return metadataExports;
  }



  /**
   * Output only. The latest restores of the metastore service.
   * @return restores
   */
  @javax.annotation.Nullable
  public List<Restore> getRestores() {
    return restores;
  }




  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    MetadataManagementActivity metadataManagementActivity = (MetadataManagementActivity) o;
    return Objects.equals(this.metadataExports, metadataManagementActivity.metadataExports) &&
        Objects.equals(this.restores, metadataManagementActivity.restores);
  }

  @Override
  public int hashCode() {
    return Objects.hash(metadataExports, restores);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class MetadataManagementActivity {\n");
    sb.append("    metadataExports: ").append(toIndentedString(metadataExports)).append("\n");
    sb.append("    restores: ").append(toIndentedString(restores)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("metadataExports");
    openapiFields.add("restores");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to MetadataManagementActivity
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!MetadataManagementActivity.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in MetadataManagementActivity is not found in the empty JSON string", MetadataManagementActivity.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!MetadataManagementActivity.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `MetadataManagementActivity` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("metadataExports") != null && !jsonObj.get("metadataExports").isJsonNull()) {
        JsonArray jsonArraymetadataExports = jsonObj.getAsJsonArray("metadataExports");
        if (jsonArraymetadataExports != null) {
          // ensure the json data is an array
          if (!jsonObj.get("metadataExports").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `metadataExports` to be an array in the JSON string but got `%s`", jsonObj.get("metadataExports").toString()));
          }

          // validate the optional field `metadataExports` (array)
          for (int i = 0; i < jsonArraymetadataExports.size(); i++) {
            MetadataExport.validateJsonElement(jsonArraymetadataExports.get(i));
          };
        }
      }
      if (jsonObj.get("restores") != null && !jsonObj.get("restores").isJsonNull()) {
        JsonArray jsonArrayrestores = jsonObj.getAsJsonArray("restores");
        if (jsonArrayrestores != null) {
          // ensure the json data is an array
          if (!jsonObj.get("restores").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `restores` to be an array in the JSON string but got `%s`", jsonObj.get("restores").toString()));
          }

          // validate the optional field `restores` (array)
          for (int i = 0; i < jsonArrayrestores.size(); i++) {
            Restore.validateJsonElement(jsonArrayrestores.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!MetadataManagementActivity.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'MetadataManagementActivity' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<MetadataManagementActivity> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(MetadataManagementActivity.class));

       return (TypeAdapter<T>) new TypeAdapter<MetadataManagementActivity>() {
           @Override
           public void write(JsonWriter out, MetadataManagementActivity value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public MetadataManagementActivity read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of MetadataManagementActivity given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of MetadataManagementActivity
   * @throws IOException if the JSON string is invalid with respect to MetadataManagementActivity
   */
  public static MetadataManagementActivity fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, MetadataManagementActivity.class);
  }

  /**
   * Convert an instance of MetadataManagementActivity to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

