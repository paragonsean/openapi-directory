/**
 * Cloud DNS API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1beta2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DnsKey from './DnsKey';
import ResponseHeader from './ResponseHeader';

/**
 * The DnsKeysListResponse model module.
 * @module model/DnsKeysListResponse
 * @version v1beta2
 */
class DnsKeysListResponse {
    /**
     * Constructs a new <code>DnsKeysListResponse</code>.
     * The response to a request to enumerate DnsKeys in a ManagedZone.
     * @alias module:model/DnsKeysListResponse
     */
    constructor() { 
        
        DnsKeysListResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['kind'] = 'dns#dnsKeysListResponse';
    }

    /**
     * Constructs a <code>DnsKeysListResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DnsKeysListResponse} obj Optional instance to populate.
     * @return {module:model/DnsKeysListResponse} The populated <code>DnsKeysListResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DnsKeysListResponse();

            if (data.hasOwnProperty('dnsKeys')) {
                obj['dnsKeys'] = ApiClient.convertToType(data['dnsKeys'], [DnsKey]);
            }
            if (data.hasOwnProperty('header')) {
                obj['header'] = ResponseHeader.constructFromObject(data['header']);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DnsKeysListResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DnsKeysListResponse</code>.
     */
    static validateJSON(data) {
        if (data['dnsKeys']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dnsKeys'])) {
                throw new Error("Expected the field `dnsKeys` to be an array in the JSON data but got " + data['dnsKeys']);
            }
            // validate the optional field `dnsKeys` (array)
            for (const item of data['dnsKeys']) {
                DnsKey.validateJSON(item);
            };
        }
        // validate the optional field `header`
        if (data['header']) { // data not null
          ResponseHeader.validateJSON(data['header']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }

        return true;
    }


}



/**
 * The requested resources.
 * @member {Array.<module:model/DnsKey>} dnsKeys
 */
DnsKeysListResponse.prototype['dnsKeys'] = undefined;

/**
 * @member {module:model/ResponseHeader} header
 */
DnsKeysListResponse.prototype['header'] = undefined;

/**
 * Type of resource.
 * @member {String} kind
 * @default 'dns#dnsKeysListResponse'
 */
DnsKeysListResponse.prototype['kind'] = 'dns#dnsKeysListResponse';

/**
 * The presence of this field indicates that there exist more results following your last page of results in pagination order. To fetch them, make another list request using this value as your pagination token. In this way you can retrieve the complete contents of even very large collections one page at a time. However, if the contents of the collection change between the first and last paginated list request, the set of all elements returned are an inconsistent view of the collection. There is no way to retrieve a \"snapshot\" of collections larger than the maximum page size.
 * @member {String} nextPageToken
 */
DnsKeysListResponse.prototype['nextPageToken'] = undefined;






export default DnsKeysListResponse;

