/**
 * Cloud DNS API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1beta2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ResponsePolicyGKECluster from './ResponsePolicyGKECluster';
import ResponsePolicyNetwork from './ResponsePolicyNetwork';

/**
 * The ResponsePolicy model module.
 * @module model/ResponsePolicy
 * @version v1beta2
 */
class ResponsePolicy {
    /**
     * Constructs a new <code>ResponsePolicy</code>.
     * A Response Policy is a collection of selectors that apply to queries made against one or more Virtual Private Cloud networks.
     * @alias module:model/ResponsePolicy
     */
    constructor() { 
        
        ResponsePolicy.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['kind'] = 'dns#responsePolicy';
    }

    /**
     * Constructs a <code>ResponsePolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ResponsePolicy} obj Optional instance to populate.
     * @return {module:model/ResponsePolicy} The populated <code>ResponsePolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ResponsePolicy();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('gkeClusters')) {
                obj['gkeClusters'] = ApiClient.convertToType(data['gkeClusters'], [ResponsePolicyGKECluster]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('networks')) {
                obj['networks'] = ApiClient.convertToType(data['networks'], [ResponsePolicyNetwork]);
            }
            if (data.hasOwnProperty('responsePolicyName')) {
                obj['responsePolicyName'] = ApiClient.convertToType(data['responsePolicyName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ResponsePolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ResponsePolicy</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['gkeClusters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['gkeClusters'])) {
                throw new Error("Expected the field `gkeClusters` to be an array in the JSON data but got " + data['gkeClusters']);
            }
            // validate the optional field `gkeClusters` (array)
            for (const item of data['gkeClusters']) {
                ResponsePolicyGKECluster.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        if (data['networks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['networks'])) {
                throw new Error("Expected the field `networks` to be an array in the JSON data but got " + data['networks']);
            }
            // validate the optional field `networks` (array)
            for (const item of data['networks']) {
                ResponsePolicyNetwork.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['responsePolicyName'] && !(typeof data['responsePolicyName'] === 'string' || data['responsePolicyName'] instanceof String)) {
            throw new Error("Expected the field `responsePolicyName` to be a primitive type in the JSON string but got " + data['responsePolicyName']);
        }

        return true;
    }


}



/**
 * User-provided description for this Response Policy.
 * @member {String} description
 */
ResponsePolicy.prototype['description'] = undefined;

/**
 * The list of Google Kubernetes Engine clusters to which this response policy is applied.
 * @member {Array.<module:model/ResponsePolicyGKECluster>} gkeClusters
 */
ResponsePolicy.prototype['gkeClusters'] = undefined;

/**
 * Unique identifier for the resource; defined by the server (output only).
 * @member {String} id
 */
ResponsePolicy.prototype['id'] = undefined;

/**
 * @member {String} kind
 * @default 'dns#responsePolicy'
 */
ResponsePolicy.prototype['kind'] = 'dns#responsePolicy';

/**
 * User labels.
 * @member {Object.<String, String>} labels
 */
ResponsePolicy.prototype['labels'] = undefined;

/**
 * List of network names specifying networks to which this policy is applied.
 * @member {Array.<module:model/ResponsePolicyNetwork>} networks
 */
ResponsePolicy.prototype['networks'] = undefined;

/**
 * User assigned name for this Response Policy.
 * @member {String} responsePolicyName
 */
ResponsePolicy.prototype['responsePolicyName'] = undefined;






export default ResponsePolicy;

