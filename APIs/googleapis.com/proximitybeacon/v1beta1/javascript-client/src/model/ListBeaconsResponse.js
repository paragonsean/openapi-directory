/**
 * Proximity Beacon API
 * Registers, manages, indexes, and searches beacons.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Beacon from './Beacon';

/**
 * The ListBeaconsResponse model module.
 * @module model/ListBeaconsResponse
 * @version v1beta1
 */
class ListBeaconsResponse {
    /**
     * Constructs a new <code>ListBeaconsResponse</code>.
     * Response that contains list beacon results and pagination help.
     * @alias module:model/ListBeaconsResponse
     */
    constructor() { 
        
        ListBeaconsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListBeaconsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListBeaconsResponse} obj Optional instance to populate.
     * @return {module:model/ListBeaconsResponse} The populated <code>ListBeaconsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListBeaconsResponse();

            if (data.hasOwnProperty('beacons')) {
                obj['beacons'] = ApiClient.convertToType(data['beacons'], [Beacon]);
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('totalCount')) {
                obj['totalCount'] = ApiClient.convertToType(data['totalCount'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListBeaconsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListBeaconsResponse</code>.
     */
    static validateJSON(data) {
        if (data['beacons']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['beacons'])) {
                throw new Error("Expected the field `beacons` to be an array in the JSON data but got " + data['beacons']);
            }
            // validate the optional field `beacons` (array)
            for (const item of data['beacons']) {
                Beacon.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        // ensure the json data is a string
        if (data['totalCount'] && !(typeof data['totalCount'] === 'string' || data['totalCount'] instanceof String)) {
            throw new Error("Expected the field `totalCount` to be a primitive type in the JSON string but got " + data['totalCount']);
        }

        return true;
    }


}



/**
 * The beacons that matched the search criteria.
 * @member {Array.<module:model/Beacon>} beacons
 */
ListBeaconsResponse.prototype['beacons'] = undefined;

/**
 * An opaque pagination token that the client may provide in their next request to retrieve the next page of results.
 * @member {String} nextPageToken
 */
ListBeaconsResponse.prototype['nextPageToken'] = undefined;

/**
 * Estimate of the total number of beacons matched by the query. Higher values may be less accurate.
 * @member {String} totalCount
 */
ListBeaconsResponse.prototype['totalCount'] = undefined;






export default ListBeaconsResponse;

