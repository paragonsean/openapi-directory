/**
 * App Hub API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DiscoveredWorkload from './DiscoveredWorkload';

/**
 * The ListDiscoveredWorkloadsResponse model module.
 * @module model/ListDiscoveredWorkloadsResponse
 * @version v1
 */
class ListDiscoveredWorkloadsResponse {
    /**
     * Constructs a new <code>ListDiscoveredWorkloadsResponse</code>.
     * Response for ListDiscoveredWorkloads.
     * @alias module:model/ListDiscoveredWorkloadsResponse
     */
    constructor() { 
        
        ListDiscoveredWorkloadsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListDiscoveredWorkloadsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListDiscoveredWorkloadsResponse} obj Optional instance to populate.
     * @return {module:model/ListDiscoveredWorkloadsResponse} The populated <code>ListDiscoveredWorkloadsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListDiscoveredWorkloadsResponse();

            if (data.hasOwnProperty('discoveredWorkloads')) {
                obj['discoveredWorkloads'] = ApiClient.convertToType(data['discoveredWorkloads'], [DiscoveredWorkload]);
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('unreachable')) {
                obj['unreachable'] = ApiClient.convertToType(data['unreachable'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListDiscoveredWorkloadsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListDiscoveredWorkloadsResponse</code>.
     */
    static validateJSON(data) {
        if (data['discoveredWorkloads']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['discoveredWorkloads'])) {
                throw new Error("Expected the field `discoveredWorkloads` to be an array in the JSON data but got " + data['discoveredWorkloads']);
            }
            // validate the optional field `discoveredWorkloads` (array)
            for (const item of data['discoveredWorkloads']) {
                DiscoveredWorkload.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['unreachable'])) {
            throw new Error("Expected the field `unreachable` to be an array in the JSON data but got " + data['unreachable']);
        }

        return true;
    }


}



/**
 * List of discovered workloads.
 * @member {Array.<module:model/DiscoveredWorkload>} discoveredWorkloads
 */
ListDiscoveredWorkloadsResponse.prototype['discoveredWorkloads'] = undefined;

/**
 * A token identifying a page of results the server should return.
 * @member {String} nextPageToken
 */
ListDiscoveredWorkloadsResponse.prototype['nextPageToken'] = undefined;

/**
 * Locations that could not be reached.
 * @member {Array.<String>} unreachable
 */
ListDiscoveredWorkloadsResponse.prototype['unreachable'] = undefined;






export default ListDiscoveredWorkloadsResponse;

