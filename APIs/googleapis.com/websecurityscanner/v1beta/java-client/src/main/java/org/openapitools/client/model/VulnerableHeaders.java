/*
 * Web Security Scanner API
 * Scans your Compute and App Engine apps for common web vulnerabilities.
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.Header;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * Information about vulnerable or missing HTTP Headers.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:42:56.316990-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class VulnerableHeaders {
  public static final String SERIALIZED_NAME_HEADERS = "headers";
  @SerializedName(SERIALIZED_NAME_HEADERS)
  private List<Header> headers = new ArrayList<>();

  public static final String SERIALIZED_NAME_MISSING_HEADERS = "missingHeaders";
  @SerializedName(SERIALIZED_NAME_MISSING_HEADERS)
  private List<Header> missingHeaders = new ArrayList<>();

  public VulnerableHeaders() {
  }

  public VulnerableHeaders headers(List<Header> headers) {
    this.headers = headers;
    return this;
  }

  public VulnerableHeaders addHeadersItem(Header headersItem) {
    if (this.headers == null) {
      this.headers = new ArrayList<>();
    }
    this.headers.add(headersItem);
    return this;
  }

  /**
   * List of vulnerable headers.
   * @return headers
   */
  @javax.annotation.Nullable
  public List<Header> getHeaders() {
    return headers;
  }

  public void setHeaders(List<Header> headers) {
    this.headers = headers;
  }


  public VulnerableHeaders missingHeaders(List<Header> missingHeaders) {
    this.missingHeaders = missingHeaders;
    return this;
  }

  public VulnerableHeaders addMissingHeadersItem(Header missingHeadersItem) {
    if (this.missingHeaders == null) {
      this.missingHeaders = new ArrayList<>();
    }
    this.missingHeaders.add(missingHeadersItem);
    return this;
  }

  /**
   * List of missing headers.
   * @return missingHeaders
   */
  @javax.annotation.Nullable
  public List<Header> getMissingHeaders() {
    return missingHeaders;
  }

  public void setMissingHeaders(List<Header> missingHeaders) {
    this.missingHeaders = missingHeaders;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    VulnerableHeaders vulnerableHeaders = (VulnerableHeaders) o;
    return Objects.equals(this.headers, vulnerableHeaders.headers) &&
        Objects.equals(this.missingHeaders, vulnerableHeaders.missingHeaders);
  }

  @Override
  public int hashCode() {
    return Objects.hash(headers, missingHeaders);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class VulnerableHeaders {\n");
    sb.append("    headers: ").append(toIndentedString(headers)).append("\n");
    sb.append("    missingHeaders: ").append(toIndentedString(missingHeaders)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("headers");
    openapiFields.add("missingHeaders");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to VulnerableHeaders
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!VulnerableHeaders.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in VulnerableHeaders is not found in the empty JSON string", VulnerableHeaders.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!VulnerableHeaders.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `VulnerableHeaders` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("headers") != null && !jsonObj.get("headers").isJsonNull()) {
        JsonArray jsonArrayheaders = jsonObj.getAsJsonArray("headers");
        if (jsonArrayheaders != null) {
          // ensure the json data is an array
          if (!jsonObj.get("headers").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `headers` to be an array in the JSON string but got `%s`", jsonObj.get("headers").toString()));
          }

          // validate the optional field `headers` (array)
          for (int i = 0; i < jsonArrayheaders.size(); i++) {
            Header.validateJsonElement(jsonArrayheaders.get(i));
          };
        }
      }
      if (jsonObj.get("missingHeaders") != null && !jsonObj.get("missingHeaders").isJsonNull()) {
        JsonArray jsonArraymissingHeaders = jsonObj.getAsJsonArray("missingHeaders");
        if (jsonArraymissingHeaders != null) {
          // ensure the json data is an array
          if (!jsonObj.get("missingHeaders").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `missingHeaders` to be an array in the JSON string but got `%s`", jsonObj.get("missingHeaders").toString()));
          }

          // validate the optional field `missingHeaders` (array)
          for (int i = 0; i < jsonArraymissingHeaders.size(); i++) {
            Header.validateJsonElement(jsonArraymissingHeaders.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!VulnerableHeaders.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'VulnerableHeaders' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<VulnerableHeaders> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(VulnerableHeaders.class));

       return (TypeAdapter<T>) new TypeAdapter<VulnerableHeaders>() {
           @Override
           public void write(JsonWriter out, VulnerableHeaders value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public VulnerableHeaders read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of VulnerableHeaders given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of VulnerableHeaders
   * @throws IOException if the JSON string is invalid with respect to VulnerableHeaders
   */
  public static VulnerableHeaders fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, VulnerableHeaders.class);
  }

  /**
   * Convert an instance of VulnerableHeaders to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

