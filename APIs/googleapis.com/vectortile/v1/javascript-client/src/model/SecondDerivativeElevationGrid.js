/**
 * Semantic Tile API
 * Serves vector tiles containing geospatial data. 
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SecondDerivativeElevationGrid model module.
 * @module model/SecondDerivativeElevationGrid
 * @version v1
 */
class SecondDerivativeElevationGrid {
    /**
     * Constructs a new <code>SecondDerivativeElevationGrid</code>.
     * A packed representation of a 2D grid of uniformly spaced points containing elevation data. Each point within the grid represents the altitude in meters above average sea level at that location within the tile. Elevations provided are (generally) relative to the EGM96 geoid, however some areas will be relative to NAVD88. EGM96 and NAVD88 are off by no more than 2 meters. The grid is oriented north-west to south-east, as illustrated: rows[0].a[0] rows[0].a[m] +-----------------+ | | | N | | ^ | | | | | W &lt;-----&gt; E | | | | | v | | S | | | +-----------------+ rows[n].a[0] rows[n].a[m] Rather than storing the altitudes directly, we store the diffs of the diffs between them as integers at some requested level of precision to take advantage of integer packing. Note that the data is packed in such a way that is fast to decode in Unity and that further optimizes wire size.
     * @alias module:model/SecondDerivativeElevationGrid
     */
    constructor() { 
        
        SecondDerivativeElevationGrid.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SecondDerivativeElevationGrid</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SecondDerivativeElevationGrid} obj Optional instance to populate.
     * @return {module:model/SecondDerivativeElevationGrid} The populated <code>SecondDerivativeElevationGrid</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SecondDerivativeElevationGrid();

            if (data.hasOwnProperty('altitudeMultiplier')) {
                obj['altitudeMultiplier'] = ApiClient.convertToType(data['altitudeMultiplier'], 'Number');
            }
            if (data.hasOwnProperty('columnCount')) {
                obj['columnCount'] = ApiClient.convertToType(data['columnCount'], 'Number');
            }
            if (data.hasOwnProperty('encodedData')) {
                obj['encodedData'] = ApiClient.convertToType(data['encodedData'], 'Blob');
            }
            if (data.hasOwnProperty('rowCount')) {
                obj['rowCount'] = ApiClient.convertToType(data['rowCount'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SecondDerivativeElevationGrid</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SecondDerivativeElevationGrid</code>.
     */
    static validateJSON(data) {

        return true;
    }


}



/**
 * A multiplier applied to the elements in the encoded data to extract the actual altitudes in meters.
 * @member {Number} altitudeMultiplier
 */
SecondDerivativeElevationGrid.prototype['altitudeMultiplier'] = undefined;

/**
 * The number of columns included in the encoded elevation data (i.e. the horizontal resolution of the grid).
 * @member {Number} columnCount
 */
SecondDerivativeElevationGrid.prototype['columnCount'] = undefined;

/**
 * A stream of elements each representing a point on the tile running across each row from left to right, top to bottom. There will be precisely horizontal_resolution * vertical_resolution elements in the stream. The elements are not the heights, rather the second order derivative of the values one would expect in a stream of height data. Each element is a varint with the following encoding: ------------------------------------------------------------------------| | Head Nibble | ------------------------------------------------------------------------| | Bit 0 | Bit 1 | Bits 2-3 | | Terminator| Sign (1=neg) | Least significant 2 bits of absolute error | ------------------------------------------------------------------------| | Tail Nibble #1 | ------------------------------------------------------------------------| | Bit 0 | Bit 1-3 | | Terminator| Least significant 3 bits of absolute error | ------------------------------------------------------------------------| | ... | Tail Nibble #n | ------------------------------------------------------------------------| | Bit 0 | Bit 1-3 | | Terminator| Least significant 3 bits of absolute error | ------------------------------------------------------------------------|
 * @member {Blob} encodedData
 */
SecondDerivativeElevationGrid.prototype['encodedData'] = undefined;

/**
 * The number of rows included in the encoded elevation data (i.e. the vertical resolution of the grid).
 * @member {Number} rowCount
 */
SecondDerivativeElevationGrid.prototype['rowCount'] = undefined;






export default SecondDerivativeElevationGrid;

