/**
 * AI Platform Training & Prediction API
 * An API to enable creating and using machine learning models.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GoogleCloudMlV1MetricSpec from './GoogleCloudMlV1MetricSpec';

/**
 * The GoogleCloudMlV1AutoScaling model module.
 * @module model/GoogleCloudMlV1AutoScaling
 * @version v1
 */
class GoogleCloudMlV1AutoScaling {
    /**
     * Constructs a new <code>GoogleCloudMlV1AutoScaling</code>.
     * Options for automatically scaling a model.
     * @alias module:model/GoogleCloudMlV1AutoScaling
     */
    constructor() { 
        
        GoogleCloudMlV1AutoScaling.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoogleCloudMlV1AutoScaling</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoogleCloudMlV1AutoScaling} obj Optional instance to populate.
     * @return {module:model/GoogleCloudMlV1AutoScaling} The populated <code>GoogleCloudMlV1AutoScaling</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoogleCloudMlV1AutoScaling();

            if (data.hasOwnProperty('maxNodes')) {
                obj['maxNodes'] = ApiClient.convertToType(data['maxNodes'], 'Number');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [GoogleCloudMlV1MetricSpec]);
            }
            if (data.hasOwnProperty('minNodes')) {
                obj['minNodes'] = ApiClient.convertToType(data['minNodes'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoogleCloudMlV1AutoScaling</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoogleCloudMlV1AutoScaling</code>.
     */
    static validateJSON(data) {
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                GoogleCloudMlV1MetricSpec.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The maximum number of nodes to scale this model under load. The actual value will depend on resource quota and availability.
 * @member {Number} maxNodes
 */
GoogleCloudMlV1AutoScaling.prototype['maxNodes'] = undefined;

/**
 * MetricSpec contains the specifications to use to calculate the desired nodes count.
 * @member {Array.<module:model/GoogleCloudMlV1MetricSpec>} metrics
 */
GoogleCloudMlV1AutoScaling.prototype['metrics'] = undefined;

/**
 * Optional. The minimum number of nodes to allocate for this model. These nodes are always up, starting from the time the model is deployed. Therefore, the cost of operating this model will be at least `rate` * `min_nodes` * number of hours since last billing cycle, where `rate` is the cost per node-hour as documented in the [pricing guide](/ml-engine/docs/pricing), even if no predictions are performed. There is additional cost for each prediction performed. Unlike manual scaling, if the load gets too heavy for the nodes that are up, the service will automatically add nodes to handle the increased load as well as scale back as traffic drops, always maintaining at least `min_nodes`. You will be charged for the time in which additional nodes are used. If `min_nodes` is not specified and AutoScaling is used with a [legacy (MLS1) machine type](/ml-engine/docs/machine-types-online-prediction), `min_nodes` defaults to 0, in which case, when traffic to a model stops (and after a cool-down period), nodes will be shut down and no charges will be incurred until traffic to the model resumes. If `min_nodes` is not specified and AutoScaling is used with a [Compute Engine (N1) machine type](/ml-engine/docs/machine-types-online-prediction), `min_nodes` defaults to 1. `min_nodes` must be at least 1 for use with a Compute Engine machine type. You can set `min_nodes` when creating the model version, and you can also update `min_nodes` for an existing version: update_body.json: { 'autoScaling': { 'minNodes': 5 } } HTTP request: PATCH https://ml.googleapis.com/v1/{name=projects/_*_/models/_*_/versions/_*}?update_mask=autoScaling.minNodes -d @./update_body.json 
 * @member {Number} minNodes
 */
GoogleCloudMlV1AutoScaling.prototype['minNodes'] = undefined;






export default GoogleCloudMlV1AutoScaling;

