/**
 * Cloud TPU API
 * TPU API provides customers with access to Google TPU technology.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Interval from './Interval';

/**
 * The QueueingPolicy model module.
 * @module model/QueueingPolicy
 * @version v2
 */
class QueueingPolicy {
    /**
     * Constructs a new <code>QueueingPolicy</code>.
     * Defines the policy of the QueuedRequest.
     * @alias module:model/QueueingPolicy
     */
    constructor() { 
        
        QueueingPolicy.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>QueueingPolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QueueingPolicy} obj Optional instance to populate.
     * @return {module:model/QueueingPolicy} The populated <code>QueueingPolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QueueingPolicy();

            if (data.hasOwnProperty('validAfterDuration')) {
                obj['validAfterDuration'] = ApiClient.convertToType(data['validAfterDuration'], 'String');
            }
            if (data.hasOwnProperty('validAfterTime')) {
                obj['validAfterTime'] = ApiClient.convertToType(data['validAfterTime'], 'String');
            }
            if (data.hasOwnProperty('validInterval')) {
                obj['validInterval'] = Interval.constructFromObject(data['validInterval']);
            }
            if (data.hasOwnProperty('validUntilDuration')) {
                obj['validUntilDuration'] = ApiClient.convertToType(data['validUntilDuration'], 'String');
            }
            if (data.hasOwnProperty('validUntilTime')) {
                obj['validUntilTime'] = ApiClient.convertToType(data['validUntilTime'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QueueingPolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QueueingPolicy</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['validAfterDuration'] && !(typeof data['validAfterDuration'] === 'string' || data['validAfterDuration'] instanceof String)) {
            throw new Error("Expected the field `validAfterDuration` to be a primitive type in the JSON string but got " + data['validAfterDuration']);
        }
        // ensure the json data is a string
        if (data['validAfterTime'] && !(typeof data['validAfterTime'] === 'string' || data['validAfterTime'] instanceof String)) {
            throw new Error("Expected the field `validAfterTime` to be a primitive type in the JSON string but got " + data['validAfterTime']);
        }
        // validate the optional field `validInterval`
        if (data['validInterval']) { // data not null
          Interval.validateJSON(data['validInterval']);
        }
        // ensure the json data is a string
        if (data['validUntilDuration'] && !(typeof data['validUntilDuration'] === 'string' || data['validUntilDuration'] instanceof String)) {
            throw new Error("Expected the field `validUntilDuration` to be a primitive type in the JSON string but got " + data['validUntilDuration']);
        }
        // ensure the json data is a string
        if (data['validUntilTime'] && !(typeof data['validUntilTime'] === 'string' || data['validUntilTime'] instanceof String)) {
            throw new Error("Expected the field `validUntilTime` to be a primitive type in the JSON string but got " + data['validUntilTime']);
        }

        return true;
    }


}



/**
 * Optional. A relative time after which resources may be created.
 * @member {String} validAfterDuration
 */
QueueingPolicy.prototype['validAfterDuration'] = undefined;

/**
 * Optional. An absolute time after which resources may be created.
 * @member {String} validAfterTime
 */
QueueingPolicy.prototype['validAfterTime'] = undefined;

/**
 * @member {module:model/Interval} validInterval
 */
QueueingPolicy.prototype['validInterval'] = undefined;

/**
 * Optional. A relative time after which resources should not be created. If the request cannot be fulfilled by this time the request will be failed.
 * @member {String} validUntilDuration
 */
QueueingPolicy.prototype['validUntilDuration'] = undefined;

/**
 * Optional. An absolute time after which resources should not be created. If the request cannot be fulfilled by this time the request will be failed.
 * @member {String} validUntilTime
 */
QueueingPolicy.prototype['validUntilTime'] = undefined;






export default QueueingPolicy;

