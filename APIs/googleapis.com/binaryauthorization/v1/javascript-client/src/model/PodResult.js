/**
 * Binary Authorization API
 * The management interface for Binary Authorization, a service that provides policy-based deployment validation and control for images deployed to Google Kubernetes Engine (GKE), Anthos Service Mesh, Anthos Clusters, and Cloud Run. 
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ImageResult from './ImageResult';

/**
 * The PodResult model module.
 * @module model/PodResult
 * @version v1
 */
class PodResult {
    /**
     * Constructs a new <code>PodResult</code>.
     * Result of evaluating the whole GKE policy for one Pod.
     * @alias module:model/PodResult
     */
    constructor() { 
        
        PodResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PodResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PodResult} obj Optional instance to populate.
     * @return {module:model/PodResult} The populated <code>PodResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PodResult();

            if (data.hasOwnProperty('imageResults')) {
                obj['imageResults'] = ApiClient.convertToType(data['imageResults'], [ImageResult]);
            }
            if (data.hasOwnProperty('kubernetesNamespace')) {
                obj['kubernetesNamespace'] = ApiClient.convertToType(data['kubernetesNamespace'], 'String');
            }
            if (data.hasOwnProperty('kubernetesServiceAccount')) {
                obj['kubernetesServiceAccount'] = ApiClient.convertToType(data['kubernetesServiceAccount'], 'String');
            }
            if (data.hasOwnProperty('podName')) {
                obj['podName'] = ApiClient.convertToType(data['podName'], 'String');
            }
            if (data.hasOwnProperty('verdict')) {
                obj['verdict'] = ApiClient.convertToType(data['verdict'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PodResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PodResult</code>.
     */
    static validateJSON(data) {
        if (data['imageResults']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['imageResults'])) {
                throw new Error("Expected the field `imageResults` to be an array in the JSON data but got " + data['imageResults']);
            }
            // validate the optional field `imageResults` (array)
            for (const item of data['imageResults']) {
                ImageResult.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['kubernetesNamespace'] && !(typeof data['kubernetesNamespace'] === 'string' || data['kubernetesNamespace'] instanceof String)) {
            throw new Error("Expected the field `kubernetesNamespace` to be a primitive type in the JSON string but got " + data['kubernetesNamespace']);
        }
        // ensure the json data is a string
        if (data['kubernetesServiceAccount'] && !(typeof data['kubernetesServiceAccount'] === 'string' || data['kubernetesServiceAccount'] instanceof String)) {
            throw new Error("Expected the field `kubernetesServiceAccount` to be a primitive type in the JSON string but got " + data['kubernetesServiceAccount']);
        }
        // ensure the json data is a string
        if (data['podName'] && !(typeof data['podName'] === 'string' || data['podName'] instanceof String)) {
            throw new Error("Expected the field `podName` to be a primitive type in the JSON string but got " + data['podName']);
        }
        // ensure the json data is a string
        if (data['verdict'] && !(typeof data['verdict'] === 'string' || data['verdict'] instanceof String)) {
            throw new Error("Expected the field `verdict` to be a primitive type in the JSON string but got " + data['verdict']);
        }

        return true;
    }


}



/**
 * Per-image details.
 * @member {Array.<module:model/ImageResult>} imageResults
 */
PodResult.prototype['imageResults'] = undefined;

/**
 * The Kubernetes namespace of the Pod.
 * @member {String} kubernetesNamespace
 */
PodResult.prototype['kubernetesNamespace'] = undefined;

/**
 * The Kubernetes service account of the Pod.
 * @member {String} kubernetesServiceAccount
 */
PodResult.prototype['kubernetesServiceAccount'] = undefined;

/**
 * The name of the Pod.
 * @member {String} podName
 */
PodResult.prototype['podName'] = undefined;

/**
 * The result of evaluating this Pod.
 * @member {module:model/PodResult.VerdictEnum} verdict
 */
PodResult.prototype['verdict'] = undefined;





/**
 * Allowed values for the <code>verdict</code> property.
 * @enum {String}
 * @readonly
 */
PodResult['VerdictEnum'] = {

    /**
     * value: "POD_VERDICT_UNSPECIFIED"
     * @const
     */
    "POD_VERDICT_UNSPECIFIED": "POD_VERDICT_UNSPECIFIED",

    /**
     * value: "CONFORMANT"
     * @const
     */
    "CONFORMANT": "CONFORMANT",

    /**
     * value: "NON_CONFORMANT"
     * @const
     */
    "NON_CONFORMANT": "NON_CONFORMANT",

    /**
     * value: "ERROR"
     * @const
     */
    "ERROR": "ERROR"
};



export default PodResult;

