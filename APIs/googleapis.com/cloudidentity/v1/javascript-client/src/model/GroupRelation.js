/**
 * Cloud Identity API
 * API for provisioning and managing identity resources.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EntityKey from './EntityKey';
import TransitiveMembershipRole from './TransitiveMembershipRole';

/**
 * The GroupRelation model module.
 * @module model/GroupRelation
 * @version v1
 */
class GroupRelation {
    /**
     * Constructs a new <code>GroupRelation</code>.
     * Message representing a transitive group of a user or a group.
     * @alias module:model/GroupRelation
     */
    constructor() { 
        
        GroupRelation.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GroupRelation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupRelation} obj Optional instance to populate.
     * @return {module:model/GroupRelation} The populated <code>GroupRelation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupRelation();

            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('group')) {
                obj['group'] = ApiClient.convertToType(data['group'], 'String');
            }
            if (data.hasOwnProperty('groupKey')) {
                obj['groupKey'] = EntityKey.constructFromObject(data['groupKey']);
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('relationType')) {
                obj['relationType'] = ApiClient.convertToType(data['relationType'], 'String');
            }
            if (data.hasOwnProperty('roles')) {
                obj['roles'] = ApiClient.convertToType(data['roles'], [TransitiveMembershipRole]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupRelation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupRelation</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['group'] && !(typeof data['group'] === 'string' || data['group'] instanceof String)) {
            throw new Error("Expected the field `group` to be a primitive type in the JSON string but got " + data['group']);
        }
        // validate the optional field `groupKey`
        if (data['groupKey']) { // data not null
          EntityKey.validateJSON(data['groupKey']);
        }
        // ensure the json data is a string
        if (data['relationType'] && !(typeof data['relationType'] === 'string' || data['relationType'] instanceof String)) {
            throw new Error("Expected the field `relationType` to be a primitive type in the JSON string but got " + data['relationType']);
        }
        if (data['roles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['roles'])) {
                throw new Error("Expected the field `roles` to be an array in the JSON data but got " + data['roles']);
            }
            // validate the optional field `roles` (array)
            for (const item of data['roles']) {
                TransitiveMembershipRole.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Display name for this group.
 * @member {String} displayName
 */
GroupRelation.prototype['displayName'] = undefined;

/**
 * Resource name for this group.
 * @member {String} group
 */
GroupRelation.prototype['group'] = undefined;

/**
 * @member {module:model/EntityKey} groupKey
 */
GroupRelation.prototype['groupKey'] = undefined;

/**
 * Labels for Group resource.
 * @member {Object.<String, String>} labels
 */
GroupRelation.prototype['labels'] = undefined;

/**
 * The relation between the member and the transitive group.
 * @member {module:model/GroupRelation.RelationTypeEnum} relationType
 */
GroupRelation.prototype['relationType'] = undefined;

/**
 * Membership roles of the member for the group.
 * @member {Array.<module:model/TransitiveMembershipRole>} roles
 */
GroupRelation.prototype['roles'] = undefined;





/**
 * Allowed values for the <code>relationType</code> property.
 * @enum {String}
 * @readonly
 */
GroupRelation['RelationTypeEnum'] = {

    /**
     * value: "RELATION_TYPE_UNSPECIFIED"
     * @const
     */
    "RELATION_TYPE_UNSPECIFIED": "RELATION_TYPE_UNSPECIFIED",

    /**
     * value: "DIRECT"
     * @const
     */
    "DIRECT": "DIRECT",

    /**
     * value: "INDIRECT"
     * @const
     */
    "INDIRECT": "INDIRECT",

    /**
     * value: "DIRECT_AND_INDIRECT"
     * @const
     */
    "DIRECT_AND_INDIRECT": "DIRECT_AND_INDIRECT"
};



export default GroupRelation;

