/**
 * Cloud Identity API
 * API for provisioning and managing identity resources.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SamlSsoInfo from './SamlSsoInfo';
import SignInBehavior from './SignInBehavior';

/**
 * The InboundSsoAssignment model module.
 * @module model/InboundSsoAssignment
 * @version v1
 */
class InboundSsoAssignment {
    /**
     * Constructs a new <code>InboundSsoAssignment</code>.
     * Targets with \&quot;set\&quot; SSO assignments and their respective assignments.
     * @alias module:model/InboundSsoAssignment
     */
    constructor() { 
        
        InboundSsoAssignment.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InboundSsoAssignment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InboundSsoAssignment} obj Optional instance to populate.
     * @return {module:model/InboundSsoAssignment} The populated <code>InboundSsoAssignment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InboundSsoAssignment();

            if (data.hasOwnProperty('customer')) {
                obj['customer'] = ApiClient.convertToType(data['customer'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('rank')) {
                obj['rank'] = ApiClient.convertToType(data['rank'], 'Number');
            }
            if (data.hasOwnProperty('samlSsoInfo')) {
                obj['samlSsoInfo'] = SamlSsoInfo.constructFromObject(data['samlSsoInfo']);
            }
            if (data.hasOwnProperty('signInBehavior')) {
                obj['signInBehavior'] = SignInBehavior.constructFromObject(data['signInBehavior']);
            }
            if (data.hasOwnProperty('ssoMode')) {
                obj['ssoMode'] = ApiClient.convertToType(data['ssoMode'], 'String');
            }
            if (data.hasOwnProperty('targetGroup')) {
                obj['targetGroup'] = ApiClient.convertToType(data['targetGroup'], 'String');
            }
            if (data.hasOwnProperty('targetOrgUnit')) {
                obj['targetOrgUnit'] = ApiClient.convertToType(data['targetOrgUnit'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InboundSsoAssignment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InboundSsoAssignment</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['customer'] && !(typeof data['customer'] === 'string' || data['customer'] instanceof String)) {
            throw new Error("Expected the field `customer` to be a primitive type in the JSON string but got " + data['customer']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `samlSsoInfo`
        if (data['samlSsoInfo']) { // data not null
          SamlSsoInfo.validateJSON(data['samlSsoInfo']);
        }
        // validate the optional field `signInBehavior`
        if (data['signInBehavior']) { // data not null
          SignInBehavior.validateJSON(data['signInBehavior']);
        }
        // ensure the json data is a string
        if (data['ssoMode'] && !(typeof data['ssoMode'] === 'string' || data['ssoMode'] instanceof String)) {
            throw new Error("Expected the field `ssoMode` to be a primitive type in the JSON string but got " + data['ssoMode']);
        }
        // ensure the json data is a string
        if (data['targetGroup'] && !(typeof data['targetGroup'] === 'string' || data['targetGroup'] instanceof String)) {
            throw new Error("Expected the field `targetGroup` to be a primitive type in the JSON string but got " + data['targetGroup']);
        }
        // ensure the json data is a string
        if (data['targetOrgUnit'] && !(typeof data['targetOrgUnit'] === 'string' || data['targetOrgUnit'] instanceof String)) {
            throw new Error("Expected the field `targetOrgUnit` to be a primitive type in the JSON string but got " + data['targetOrgUnit']);
        }

        return true;
    }


}



/**
 * Immutable. The customer. For example: `customers/C0123abc`.
 * @member {String} customer
 */
InboundSsoAssignment.prototype['customer'] = undefined;

/**
 * Output only. [Resource name](https://cloud.google.com/apis/design/resource_names) of the Inbound SSO Assignment.
 * @member {String} name
 */
InboundSsoAssignment.prototype['name'] = undefined;

/**
 * Must be zero (which is the default value so it can be omitted) for assignments with `target_org_unit` set and must be greater-than-or-equal-to one for assignments with `target_group` set.
 * @member {Number} rank
 */
InboundSsoAssignment.prototype['rank'] = undefined;

/**
 * @member {module:model/SamlSsoInfo} samlSsoInfo
 */
InboundSsoAssignment.prototype['samlSsoInfo'] = undefined;

/**
 * @member {module:model/SignInBehavior} signInBehavior
 */
InboundSsoAssignment.prototype['signInBehavior'] = undefined;

/**
 * Inbound SSO behavior.
 * @member {module:model/InboundSsoAssignment.SsoModeEnum} ssoMode
 */
InboundSsoAssignment.prototype['ssoMode'] = undefined;

/**
 * Immutable. Must be of the form `groups/{group}`.
 * @member {String} targetGroup
 */
InboundSsoAssignment.prototype['targetGroup'] = undefined;

/**
 * Immutable. Must be of the form `orgUnits/{org_unit}`.
 * @member {String} targetOrgUnit
 */
InboundSsoAssignment.prototype['targetOrgUnit'] = undefined;





/**
 * Allowed values for the <code>ssoMode</code> property.
 * @enum {String}
 * @readonly
 */
InboundSsoAssignment['SsoModeEnum'] = {

    /**
     * value: "SSO_MODE_UNSPECIFIED"
     * @const
     */
    "SSO_MODE_UNSPECIFIED": "SSO_MODE_UNSPECIFIED",

    /**
     * value: "SSO_OFF"
     * @const
     */
    "SSO_OFF": "SSO_OFF",

    /**
     * value: "SAML_SSO"
     * @const
     */
    "SAML_SSO": "SAML_SSO",

    /**
     * value: "DOMAIN_WIDE_SAML_IF_ENABLED"
     * @const
     */
    "DOMAIN_WIDE_SAML_IF_ENABLED": "DOMAIN_WIDE_SAML_IF_ENABLED"
};



export default InboundSsoAssignment;

