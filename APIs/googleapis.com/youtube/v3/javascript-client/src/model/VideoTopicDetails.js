/**
 * YouTube Data API v3
 * The YouTube Data API v3 is an API that provides access to YouTube data, such as videos, playlists, and channels.
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VideoTopicDetails model module.
 * @module model/VideoTopicDetails
 * @version v3
 */
class VideoTopicDetails {
    /**
     * Constructs a new <code>VideoTopicDetails</code>.
     * Freebase topic information related to the video.
     * @alias module:model/VideoTopicDetails
     */
    constructor() { 
        
        VideoTopicDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>VideoTopicDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VideoTopicDetails} obj Optional instance to populate.
     * @return {module:model/VideoTopicDetails} The populated <code>VideoTopicDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VideoTopicDetails();

            if (data.hasOwnProperty('relevantTopicIds')) {
                obj['relevantTopicIds'] = ApiClient.convertToType(data['relevantTopicIds'], ['String']);
            }
            if (data.hasOwnProperty('topicCategories')) {
                obj['topicCategories'] = ApiClient.convertToType(data['topicCategories'], ['String']);
            }
            if (data.hasOwnProperty('topicIds')) {
                obj['topicIds'] = ApiClient.convertToType(data['topicIds'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VideoTopicDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VideoTopicDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['relevantTopicIds'])) {
            throw new Error("Expected the field `relevantTopicIds` to be an array in the JSON data but got " + data['relevantTopicIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['topicCategories'])) {
            throw new Error("Expected the field `topicCategories` to be an array in the JSON data but got " + data['topicCategories']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['topicIds'])) {
            throw new Error("Expected the field `topicIds` to be an array in the JSON data but got " + data['topicIds']);
        }

        return true;
    }


}



/**
 * Similar to topic_id, except that these topics are merely relevant to the video. These are topics that may be mentioned in, or appear in the video. You can retrieve information about each topic using Freebase Topic API.
 * @member {Array.<String>} relevantTopicIds
 */
VideoTopicDetails.prototype['relevantTopicIds'] = undefined;

/**
 * A list of Wikipedia URLs that provide a high-level description of the video's content.
 * @member {Array.<String>} topicCategories
 */
VideoTopicDetails.prototype['topicCategories'] = undefined;

/**
 * A list of Freebase topic IDs that are centrally associated with the video. These are topics that are centrally featured in the video, and it can be said that the video is mainly about each of these. You can retrieve information about each topic using the < a href=\"http://wiki.freebase.com/wiki/Topic_API\">Freebase Topic API.
 * @member {Array.<String>} topicIds
 */
VideoTopicDetails.prototype['topicIds'] = undefined;






export default VideoTopicDetails;

