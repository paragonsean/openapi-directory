/**
 * Google My Business API
 * The Google My Business API provides an interface for managing business location information on Google.
 *
 * The version of the OpenAPI document: v4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LocationDrivingDirectionMetrics from './LocationDrivingDirectionMetrics';
import LocationMetrics from './LocationMetrics';

/**
 * The ReportLocationInsightsResponse model module.
 * @module model/ReportLocationInsightsResponse
 * @version v4
 */
class ReportLocationInsightsResponse {
    /**
     * Constructs a new <code>ReportLocationInsightsResponse</code>.
     * Response message for &#x60;Insights.ReportLocationInsights&#x60;.
     * @alias module:model/ReportLocationInsightsResponse
     */
    constructor() { 
        
        ReportLocationInsightsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReportLocationInsightsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportLocationInsightsResponse} obj Optional instance to populate.
     * @return {module:model/ReportLocationInsightsResponse} The populated <code>ReportLocationInsightsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportLocationInsightsResponse();

            if (data.hasOwnProperty('locationDrivingDirectionMetrics')) {
                obj['locationDrivingDirectionMetrics'] = ApiClient.convertToType(data['locationDrivingDirectionMetrics'], [LocationDrivingDirectionMetrics]);
            }
            if (data.hasOwnProperty('locationMetrics')) {
                obj['locationMetrics'] = ApiClient.convertToType(data['locationMetrics'], [LocationMetrics]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportLocationInsightsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportLocationInsightsResponse</code>.
     */
    static validateJSON(data) {
        if (data['locationDrivingDirectionMetrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['locationDrivingDirectionMetrics'])) {
                throw new Error("Expected the field `locationDrivingDirectionMetrics` to be an array in the JSON data but got " + data['locationDrivingDirectionMetrics']);
            }
            // validate the optional field `locationDrivingDirectionMetrics` (array)
            for (const item of data['locationDrivingDirectionMetrics']) {
                LocationDrivingDirectionMetrics.validateJSON(item);
            };
        }
        if (data['locationMetrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['locationMetrics'])) {
                throw new Error("Expected the field `locationMetrics` to be an array in the JSON data but got " + data['locationMetrics']);
            }
            // validate the optional field `locationMetrics` (array)
            for (const item of data['locationMetrics']) {
                LocationMetrics.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A collection of values for driving direction-related metrics.
 * @member {Array.<module:model/LocationDrivingDirectionMetrics>} locationDrivingDirectionMetrics
 */
ReportLocationInsightsResponse.prototype['locationDrivingDirectionMetrics'] = undefined;

/**
 * A collection of metric values by location.
 * @member {Array.<module:model/LocationMetrics>} locationMetrics
 */
ReportLocationInsightsResponse.prototype['locationMetrics'] = undefined;






export default ReportLocationInsightsResponse;

