/**
 * Google My Business API
 * The Google My Business API provides an interface for managing business location information on Google.
 *
 * The version of the OpenAPI document: v4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TopDirectionSources from './TopDirectionSources';

/**
 * The LocationDrivingDirectionMetrics model module.
 * @module model/LocationDrivingDirectionMetrics
 * @version v4
 */
class LocationDrivingDirectionMetrics {
    /**
     * Constructs a new <code>LocationDrivingDirectionMetrics</code>.
     * A location indexed with the regions that people usually come from. This is captured by counting how many driving-direction requests to this location are from each region.
     * @alias module:model/LocationDrivingDirectionMetrics
     */
    constructor() { 
        
        LocationDrivingDirectionMetrics.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LocationDrivingDirectionMetrics</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LocationDrivingDirectionMetrics} obj Optional instance to populate.
     * @return {module:model/LocationDrivingDirectionMetrics} The populated <code>LocationDrivingDirectionMetrics</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LocationDrivingDirectionMetrics();

            if (data.hasOwnProperty('locationName')) {
                obj['locationName'] = ApiClient.convertToType(data['locationName'], 'String');
            }
            if (data.hasOwnProperty('timeZone')) {
                obj['timeZone'] = ApiClient.convertToType(data['timeZone'], 'String');
            }
            if (data.hasOwnProperty('topDirectionSources')) {
                obj['topDirectionSources'] = ApiClient.convertToType(data['topDirectionSources'], [TopDirectionSources]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LocationDrivingDirectionMetrics</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LocationDrivingDirectionMetrics</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['locationName'] && !(typeof data['locationName'] === 'string' || data['locationName'] instanceof String)) {
            throw new Error("Expected the field `locationName` to be a primitive type in the JSON string but got " + data['locationName']);
        }
        // ensure the json data is a string
        if (data['timeZone'] && !(typeof data['timeZone'] === 'string' || data['timeZone'] instanceof String)) {
            throw new Error("Expected the field `timeZone` to be a primitive type in the JSON string but got " + data['timeZone']);
        }
        if (data['topDirectionSources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['topDirectionSources'])) {
                throw new Error("Expected the field `topDirectionSources` to be an array in the JSON data but got " + data['topDirectionSources']);
            }
            // validate the optional field `topDirectionSources` (array)
            for (const item of data['topDirectionSources']) {
                TopDirectionSources.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The location resource name this metric value belongs to.
 * @member {String} locationName
 */
LocationDrivingDirectionMetrics.prototype['locationName'] = undefined;

/**
 * Time zone (IANA timezone IDs, for example, 'Europe/London') of the location.
 * @member {String} timeZone
 */
LocationDrivingDirectionMetrics.prototype['timeZone'] = undefined;

/**
 * Driving-direction requests by source region. By convention, these are sorted by count with at most 10 results.
 * @member {Array.<module:model/TopDirectionSources>} topDirectionSources
 */
LocationDrivingDirectionMetrics.prototype['topDirectionSources'] = undefined;






export default LocationDrivingDirectionMetrics;

