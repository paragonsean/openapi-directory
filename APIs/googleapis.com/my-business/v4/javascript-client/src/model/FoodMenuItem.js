/**
 * Google My Business API
 * The Google My Business API provides an interface for managing business location information on Google.
 *
 * The version of the OpenAPI document: v4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FoodMenuItemAttributes from './FoodMenuItemAttributes';
import FoodMenuItemOption from './FoodMenuItemOption';
import MenuLabel from './MenuLabel';

/**
 * The FoodMenuItem model module.
 * @module model/FoodMenuItem
 * @version v4
 */
class FoodMenuItem {
    /**
     * Constructs a new <code>FoodMenuItem</code>.
     * Item of a Section. It can be the dish itself, or can contain multiple FoodMenuItemOption.
     * @alias module:model/FoodMenuItem
     */
    constructor() { 
        
        FoodMenuItem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FoodMenuItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FoodMenuItem} obj Optional instance to populate.
     * @return {module:model/FoodMenuItem} The populated <code>FoodMenuItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FoodMenuItem();

            if (data.hasOwnProperty('attributes')) {
                obj['attributes'] = FoodMenuItemAttributes.constructFromObject(data['attributes']);
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], [MenuLabel]);
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], [FoodMenuItemOption]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FoodMenuItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FoodMenuItem</code>.
     */
    static validateJSON(data) {
        // validate the optional field `attributes`
        if (data['attributes']) { // data not null
          FoodMenuItemAttributes.validateJSON(data['attributes']);
        }
        if (data['labels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['labels'])) {
                throw new Error("Expected the field `labels` to be an array in the JSON data but got " + data['labels']);
            }
            // validate the optional field `labels` (array)
            for (const item of data['labels']) {
                MenuLabel.validateJSON(item);
            };
        }
        if (data['options']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['options'])) {
                throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
            }
            // validate the optional field `options` (array)
            for (const item of data['options']) {
                FoodMenuItemOption.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/FoodMenuItemAttributes} attributes
 */
FoodMenuItem.prototype['attributes'] = undefined;

/**
 * Required. Language tagged labels for this menu item. Display names should be 140 characters or less, with descriptions 1,000 characters or less. At least one set of labels is required.
 * @member {Array.<module:model/MenuLabel>} labels
 */
FoodMenuItem.prototype['labels'] = undefined;

/**
 * Optional. This is for an item that comes in multiple different options, and users are required to make choices. E.g. \"regular\" vs. \"large\" pizza. When options are specified, labels and attributes at item level will automatically become the first option's labels and attributes. Clients only need to specify other additional food options in this field.
 * @member {Array.<module:model/FoodMenuItemOption>} options
 */
FoodMenuItem.prototype['options'] = undefined;






export default FoodMenuItem;

