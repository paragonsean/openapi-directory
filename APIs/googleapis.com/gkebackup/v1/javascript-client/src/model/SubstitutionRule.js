/**
 * Backup for GKE API
 * Backup for GKE is a managed Kubernetes workload backup and restore service for GKE clusters.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GroupKind from './GroupKind';

/**
 * The SubstitutionRule model module.
 * @module model/SubstitutionRule
 * @version v1
 */
class SubstitutionRule {
    /**
     * Constructs a new <code>SubstitutionRule</code>.
     * A transformation rule to be applied against Kubernetes resources as they are selected for restoration from a Backup. A rule contains both filtering logic (which resources are subject to substitution) and substitution logic.
     * @alias module:model/SubstitutionRule
     */
    constructor() { 
        
        SubstitutionRule.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SubstitutionRule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SubstitutionRule} obj Optional instance to populate.
     * @return {module:model/SubstitutionRule} The populated <code>SubstitutionRule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SubstitutionRule();

            if (data.hasOwnProperty('newValue')) {
                obj['newValue'] = ApiClient.convertToType(data['newValue'], 'String');
            }
            if (data.hasOwnProperty('originalValuePattern')) {
                obj['originalValuePattern'] = ApiClient.convertToType(data['originalValuePattern'], 'String');
            }
            if (data.hasOwnProperty('targetGroupKinds')) {
                obj['targetGroupKinds'] = ApiClient.convertToType(data['targetGroupKinds'], [GroupKind]);
            }
            if (data.hasOwnProperty('targetJsonPath')) {
                obj['targetJsonPath'] = ApiClient.convertToType(data['targetJsonPath'], 'String');
            }
            if (data.hasOwnProperty('targetNamespaces')) {
                obj['targetNamespaces'] = ApiClient.convertToType(data['targetNamespaces'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SubstitutionRule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SubstitutionRule</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['newValue'] && !(typeof data['newValue'] === 'string' || data['newValue'] instanceof String)) {
            throw new Error("Expected the field `newValue` to be a primitive type in the JSON string but got " + data['newValue']);
        }
        // ensure the json data is a string
        if (data['originalValuePattern'] && !(typeof data['originalValuePattern'] === 'string' || data['originalValuePattern'] instanceof String)) {
            throw new Error("Expected the field `originalValuePattern` to be a primitive type in the JSON string but got " + data['originalValuePattern']);
        }
        if (data['targetGroupKinds']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['targetGroupKinds'])) {
                throw new Error("Expected the field `targetGroupKinds` to be an array in the JSON data but got " + data['targetGroupKinds']);
            }
            // validate the optional field `targetGroupKinds` (array)
            for (const item of data['targetGroupKinds']) {
                GroupKind.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['targetJsonPath'] && !(typeof data['targetJsonPath'] === 'string' || data['targetJsonPath'] instanceof String)) {
            throw new Error("Expected the field `targetJsonPath` to be a primitive type in the JSON string but got " + data['targetJsonPath']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['targetNamespaces'])) {
            throw new Error("Expected the field `targetNamespaces` to be an array in the JSON data but got " + data['targetNamespaces']);
        }

        return true;
    }


}



/**
 * Optional. This is the new value to set for any fields that pass the filtering and selection criteria. To remove a value from a Kubernetes resource, either leave this field unspecified, or set it to the empty string (\"\").
 * @member {String} newValue
 */
SubstitutionRule.prototype['newValue'] = undefined;

/**
 * Optional. (Filtering parameter) This is a [regular expression] (https://en.wikipedia.org/wiki/Regular_expression) that is compared against the fields matched by the target_json_path expression (and must also have passed the previous filters). Substitution will not be performed against fields whose value does not match this expression. If this field is NOT specified, then ALL fields matched by the target_json_path expression will undergo substitution. Note that an empty (e.g., \"\", rather than unspecified) value for this field will only match empty fields.
 * @member {String} originalValuePattern
 */
SubstitutionRule.prototype['originalValuePattern'] = undefined;

/**
 * Optional. (Filtering parameter) Any resource subject to substitution must belong to one of the listed \"types\". If this field is not provided, no type filtering will be performed (all resources of all types matching previous filtering parameters will be candidates for substitution).
 * @member {Array.<module:model/GroupKind>} targetGroupKinds
 */
SubstitutionRule.prototype['targetGroupKinds'] = undefined;

/**
 * Required. This is a [JSONPath] (https://kubernetes.io/docs/reference/kubectl/jsonpath/) expression that matches specific fields of candidate resources and it operates as both a filtering parameter (resources that are not matched with this expression will not be candidates for substitution) as well as a field identifier (identifies exactly which fields out of the candidate resources will be modified).
 * @member {String} targetJsonPath
 */
SubstitutionRule.prototype['targetJsonPath'] = undefined;

/**
 * Optional. (Filtering parameter) Any resource subject to substitution must be contained within one of the listed Kubernetes Namespace in the Backup. If this field is not provided, no namespace filtering will be performed (all resources in all Namespaces, including all cluster-scoped resources, will be candidates for substitution). To mix cluster-scoped and namespaced resources in the same rule, use an empty string (\"\") as one of the target namespaces.
 * @member {Array.<String>} targetNamespaces
 */
SubstitutionRule.prototype['targetNamespaces'] = undefined;






export default SubstitutionRule;

