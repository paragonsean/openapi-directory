/**
 * Backup for GKE API
 * Backup for GKE is a managed Kubernetes workload backup and restore service for GKE clusters.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GroupKind from './GroupKind';

/**
 * The ClusterResourceRestoreScope model module.
 * @module model/ClusterResourceRestoreScope
 * @version v1
 */
class ClusterResourceRestoreScope {
    /**
     * Constructs a new <code>ClusterResourceRestoreScope</code>.
     * Defines the scope of cluster-scoped resources to restore. Some group kinds are not reasonable choices for a restore, and will cause an error if selected here. Any scope selection that would restore \&quot;all valid\&quot; resources automatically excludes these group kinds. - gkebackup.gke.io/BackupJob - gkebackup.gke.io/RestoreJob - metrics.k8s.io/NodeMetrics - migration.k8s.io/StorageState - migration.k8s.io/StorageVersionMigration - Node - snapshot.storage.k8s.io/VolumeSnapshotContent - storage.k8s.io/CSINode Some group kinds are driven by restore configuration elsewhere, and will cause an error if selected here. - Namespace - PersistentVolume
     * @alias module:model/ClusterResourceRestoreScope
     */
    constructor() { 
        
        ClusterResourceRestoreScope.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ClusterResourceRestoreScope</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ClusterResourceRestoreScope} obj Optional instance to populate.
     * @return {module:model/ClusterResourceRestoreScope} The populated <code>ClusterResourceRestoreScope</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ClusterResourceRestoreScope();

            if (data.hasOwnProperty('allGroupKinds')) {
                obj['allGroupKinds'] = ApiClient.convertToType(data['allGroupKinds'], 'Boolean');
            }
            if (data.hasOwnProperty('excludedGroupKinds')) {
                obj['excludedGroupKinds'] = ApiClient.convertToType(data['excludedGroupKinds'], [GroupKind]);
            }
            if (data.hasOwnProperty('noGroupKinds')) {
                obj['noGroupKinds'] = ApiClient.convertToType(data['noGroupKinds'], 'Boolean');
            }
            if (data.hasOwnProperty('selectedGroupKinds')) {
                obj['selectedGroupKinds'] = ApiClient.convertToType(data['selectedGroupKinds'], [GroupKind]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ClusterResourceRestoreScope</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ClusterResourceRestoreScope</code>.
     */
    static validateJSON(data) {
        if (data['excludedGroupKinds']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['excludedGroupKinds'])) {
                throw new Error("Expected the field `excludedGroupKinds` to be an array in the JSON data but got " + data['excludedGroupKinds']);
            }
            // validate the optional field `excludedGroupKinds` (array)
            for (const item of data['excludedGroupKinds']) {
                GroupKind.validateJSON(item);
            };
        }
        if (data['selectedGroupKinds']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['selectedGroupKinds'])) {
                throw new Error("Expected the field `selectedGroupKinds` to be an array in the JSON data but got " + data['selectedGroupKinds']);
            }
            // validate the optional field `selectedGroupKinds` (array)
            for (const item of data['selectedGroupKinds']) {
                GroupKind.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Optional. If True, all valid cluster-scoped resources will be restored. Mutually exclusive to any other field in the message.
 * @member {Boolean} allGroupKinds
 */
ClusterResourceRestoreScope.prototype['allGroupKinds'] = undefined;

/**
 * Optional. A list of cluster-scoped resource group kinds to NOT restore from the backup. If specified, all valid cluster-scoped resources will be restored except for those specified in the list. Mutually exclusive to any other field in the message.
 * @member {Array.<module:model/GroupKind>} excludedGroupKinds
 */
ClusterResourceRestoreScope.prototype['excludedGroupKinds'] = undefined;

/**
 * Optional. If True, no cluster-scoped resources will be restored. This has the same restore scope as if the message is not defined. Mutually exclusive to any other field in the message.
 * @member {Boolean} noGroupKinds
 */
ClusterResourceRestoreScope.prototype['noGroupKinds'] = undefined;

/**
 * Optional. A list of cluster-scoped resource group kinds to restore from the backup. If specified, only the selected resources will be restored. Mutually exclusive to any other field in the message.
 * @member {Array.<module:model/GroupKind>} selectedGroupKinds
 */
ClusterResourceRestoreScope.prototype['selectedGroupKinds'] = undefined;






export default ClusterResourceRestoreScope;

