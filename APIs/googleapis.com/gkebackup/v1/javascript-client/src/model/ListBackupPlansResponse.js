/**
 * Backup for GKE API
 * Backup for GKE is a managed Kubernetes workload backup and restore service for GKE clusters.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BackupPlan from './BackupPlan';

/**
 * The ListBackupPlansResponse model module.
 * @module model/ListBackupPlansResponse
 * @version v1
 */
class ListBackupPlansResponse {
    /**
     * Constructs a new <code>ListBackupPlansResponse</code>.
     * Response message for ListBackupPlans.
     * @alias module:model/ListBackupPlansResponse
     */
    constructor() { 
        
        ListBackupPlansResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListBackupPlansResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListBackupPlansResponse} obj Optional instance to populate.
     * @return {module:model/ListBackupPlansResponse} The populated <code>ListBackupPlansResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListBackupPlansResponse();

            if (data.hasOwnProperty('backupPlans')) {
                obj['backupPlans'] = ApiClient.convertToType(data['backupPlans'], [BackupPlan]);
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('unreachable')) {
                obj['unreachable'] = ApiClient.convertToType(data['unreachable'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListBackupPlansResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListBackupPlansResponse</code>.
     */
    static validateJSON(data) {
        if (data['backupPlans']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['backupPlans'])) {
                throw new Error("Expected the field `backupPlans` to be an array in the JSON data but got " + data['backupPlans']);
            }
            // validate the optional field `backupPlans` (array)
            for (const item of data['backupPlans']) {
                BackupPlan.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['unreachable'])) {
            throw new Error("Expected the field `unreachable` to be an array in the JSON data but got " + data['unreachable']);
        }

        return true;
    }


}



/**
 * The list of BackupPlans matching the given criteria.
 * @member {Array.<module:model/BackupPlan>} backupPlans
 */
ListBackupPlansResponse.prototype['backupPlans'] = undefined;

/**
 * A token which may be sent as page_token in a subsequent `ListBackupPlans` call to retrieve the next page of results. If this field is omitted or empty, then there are no more results to return.
 * @member {String} nextPageToken
 */
ListBackupPlansResponse.prototype['nextPageToken'] = undefined;

/**
 * Locations that could not be reached.
 * @member {Array.<String>} unreachable
 */
ListBackupPlansResponse.prototype['unreachable'] = undefined;






export default ListBackupPlansResponse;

