/**
 * Kubernetes Engine API
 * Builds and manages container-based applications, powered by the open source Kubernetes technology.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PlacementPolicy model module.
 * @module model/PlacementPolicy
 * @version v1beta1
 */
class PlacementPolicy {
    /**
     * Constructs a new <code>PlacementPolicy</code>.
     * PlacementPolicy defines the placement policy used by the node pool.
     * @alias module:model/PlacementPolicy
     */
    constructor() { 
        
        PlacementPolicy.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PlacementPolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PlacementPolicy} obj Optional instance to populate.
     * @return {module:model/PlacementPolicy} The populated <code>PlacementPolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PlacementPolicy();

            if (data.hasOwnProperty('policyName')) {
                obj['policyName'] = ApiClient.convertToType(data['policyName'], 'String');
            }
            if (data.hasOwnProperty('tpuTopology')) {
                obj['tpuTopology'] = ApiClient.convertToType(data['tpuTopology'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PlacementPolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PlacementPolicy</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['policyName'] && !(typeof data['policyName'] === 'string' || data['policyName'] instanceof String)) {
            throw new Error("Expected the field `policyName` to be a primitive type in the JSON string but got " + data['policyName']);
        }
        // ensure the json data is a string
        if (data['tpuTopology'] && !(typeof data['tpuTopology'] === 'string' || data['tpuTopology'] instanceof String)) {
            throw new Error("Expected the field `tpuTopology` to be a primitive type in the JSON string but got " + data['tpuTopology']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * If set, refers to the name of a custom resource policy supplied by the user. The resource policy must be in the same project and region as the node pool. If not found, InvalidArgument error is returned.
 * @member {String} policyName
 */
PlacementPolicy.prototype['policyName'] = undefined;

/**
 * TPU placement topology for pod slice node pool. https://cloud.google.com/tpu/docs/types-topologies#tpu_topologies
 * @member {String} tpuTopology
 */
PlacementPolicy.prototype['tpuTopology'] = undefined;

/**
 * The type of placement.
 * @member {module:model/PlacementPolicy.TypeEnum} type
 */
PlacementPolicy.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
PlacementPolicy['TypeEnum'] = {

    /**
     * value: "TYPE_UNSPECIFIED"
     * @const
     */
    "TYPE_UNSPECIFIED": "TYPE_UNSPECIFIED",

    /**
     * value: "COMPACT"
     * @const
     */
    "COMPACT": "COMPACT"
};



export default PlacementPolicy;

