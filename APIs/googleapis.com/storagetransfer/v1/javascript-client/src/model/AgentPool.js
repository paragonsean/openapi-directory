/**
 * Storage Transfer API
 * Transfers data from external data sources to a Google Cloud Storage bucket or between Google Cloud Storage buckets. 
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BandwidthLimit from './BandwidthLimit';

/**
 * The AgentPool model module.
 * @module model/AgentPool
 * @version v1
 */
class AgentPool {
    /**
     * Constructs a new <code>AgentPool</code>.
     * Represents an On-Premises Agent pool.
     * @alias module:model/AgentPool
     */
    constructor() { 
        
        AgentPool.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AgentPool</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AgentPool} obj Optional instance to populate.
     * @return {module:model/AgentPool} The populated <code>AgentPool</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AgentPool();

            if (data.hasOwnProperty('bandwidthLimit')) {
                obj['bandwidthLimit'] = BandwidthLimit.constructFromObject(data['bandwidthLimit']);
            }
            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AgentPool</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AgentPool</code>.
     */
    static validateJSON(data) {
        // validate the optional field `bandwidthLimit`
        if (data['bandwidthLimit']) { // data not null
          BandwidthLimit.validateJSON(data['bandwidthLimit']);
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}



/**
 * @member {module:model/BandwidthLimit} bandwidthLimit
 */
AgentPool.prototype['bandwidthLimit'] = undefined;

/**
 * Specifies the client-specified AgentPool description.
 * @member {String} displayName
 */
AgentPool.prototype['displayName'] = undefined;

/**
 * Required. Specifies a unique string that identifies the agent pool. Format: `projects/{project_id}/agentPools/{agent_pool_id}`
 * @member {String} name
 */
AgentPool.prototype['name'] = undefined;

/**
 * Output only. Specifies the state of the AgentPool.
 * @member {module:model/AgentPool.StateEnum} state
 */
AgentPool.prototype['state'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
AgentPool['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "CREATING"
     * @const
     */
    "CREATING": "CREATING",

    /**
     * value: "CREATED"
     * @const
     */
    "CREATED": "CREATED",

    /**
     * value: "DELETING"
     * @const
     */
    "DELETING": "DELETING"
};



export default AgentPool;

