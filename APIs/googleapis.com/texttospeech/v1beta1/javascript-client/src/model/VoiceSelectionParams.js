/**
 * Cloud Text-to-Speech API
 * Synthesizes natural-sounding speech by applying powerful neural network models.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CustomVoiceParams from './CustomVoiceParams';

/**
 * The VoiceSelectionParams model module.
 * @module model/VoiceSelectionParams
 * @version v1beta1
 */
class VoiceSelectionParams {
    /**
     * Constructs a new <code>VoiceSelectionParams</code>.
     * Description of which voice to use for a synthesis request.
     * @alias module:model/VoiceSelectionParams
     */
    constructor() { 
        
        VoiceSelectionParams.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>VoiceSelectionParams</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VoiceSelectionParams} obj Optional instance to populate.
     * @return {module:model/VoiceSelectionParams} The populated <code>VoiceSelectionParams</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VoiceSelectionParams();

            if (data.hasOwnProperty('customVoice')) {
                obj['customVoice'] = CustomVoiceParams.constructFromObject(data['customVoice']);
            }
            if (data.hasOwnProperty('languageCode')) {
                obj['languageCode'] = ApiClient.convertToType(data['languageCode'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('ssmlGender')) {
                obj['ssmlGender'] = ApiClient.convertToType(data['ssmlGender'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VoiceSelectionParams</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VoiceSelectionParams</code>.
     */
    static validateJSON(data) {
        // validate the optional field `customVoice`
        if (data['customVoice']) { // data not null
          CustomVoiceParams.validateJSON(data['customVoice']);
        }
        // ensure the json data is a string
        if (data['languageCode'] && !(typeof data['languageCode'] === 'string' || data['languageCode'] instanceof String)) {
            throw new Error("Expected the field `languageCode` to be a primitive type in the JSON string but got " + data['languageCode']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['ssmlGender'] && !(typeof data['ssmlGender'] === 'string' || data['ssmlGender'] instanceof String)) {
            throw new Error("Expected the field `ssmlGender` to be a primitive type in the JSON string but got " + data['ssmlGender']);
        }

        return true;
    }


}



/**
 * @member {module:model/CustomVoiceParams} customVoice
 */
VoiceSelectionParams.prototype['customVoice'] = undefined;

/**
 * Required. The language (and potentially also the region) of the voice expressed as a [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt) language tag, e.g. \"en-US\". This should not include a script tag (e.g. use \"cmn-cn\" rather than \"cmn-Hant-cn\"), because the script will be inferred from the input provided in the SynthesisInput. The TTS service will use this parameter to help choose an appropriate voice. Note that the TTS service may choose a voice with a slightly different language code than the one selected; it may substitute a different region (e.g. using en-US rather than en-CA if there isn't a Canadian voice available), or even a different language, e.g. using \"nb\" (Norwegian Bokmal) instead of \"no\" (Norwegian)\".
 * @member {String} languageCode
 */
VoiceSelectionParams.prototype['languageCode'] = undefined;

/**
 * The name of the voice. If not set, the service will choose a voice based on the other parameters such as language_code and gender.
 * @member {String} name
 */
VoiceSelectionParams.prototype['name'] = undefined;

/**
 * The preferred gender of the voice. If not set, the service will choose a voice based on the other parameters such as language_code and name. Note that this is only a preference, not requirement; if a voice of the appropriate gender is not available, the synthesizer should substitute a voice with a different gender rather than failing the request.
 * @member {module:model/VoiceSelectionParams.SsmlGenderEnum} ssmlGender
 */
VoiceSelectionParams.prototype['ssmlGender'] = undefined;





/**
 * Allowed values for the <code>ssmlGender</code> property.
 * @enum {String}
 * @readonly
 */
VoiceSelectionParams['SsmlGenderEnum'] = {

    /**
     * value: "SSML_VOICE_GENDER_UNSPECIFIED"
     * @const
     */
    "SSML_VOICE_GENDER_UNSPECIFIED": "SSML_VOICE_GENDER_UNSPECIFIED",

    /**
     * value: "MALE"
     * @const
     */
    "MALE": "MALE",

    /**
     * value: "FEMALE"
     * @const
     */
    "FEMALE": "FEMALE",

    /**
     * value: "NEUTRAL"
     * @const
     */
    "NEUTRAL": "NEUTRAL"
};



export default VoiceSelectionParams;

