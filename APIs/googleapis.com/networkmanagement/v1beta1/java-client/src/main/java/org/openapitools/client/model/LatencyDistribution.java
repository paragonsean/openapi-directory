/*
 * Network Management API
 * The Network Management API provides a collection of network performance monitoring and diagnostic capabilities.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.LatencyPercentile;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * Describes measured latency distribution.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:38:01.182928-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class LatencyDistribution {
  public static final String SERIALIZED_NAME_LATENCY_PERCENTILES = "latencyPercentiles";
  @SerializedName(SERIALIZED_NAME_LATENCY_PERCENTILES)
  private List<LatencyPercentile> latencyPercentiles = new ArrayList<>();

  public LatencyDistribution() {
  }

  public LatencyDistribution latencyPercentiles(List<LatencyPercentile> latencyPercentiles) {
    this.latencyPercentiles = latencyPercentiles;
    return this;
  }

  public LatencyDistribution addLatencyPercentilesItem(LatencyPercentile latencyPercentilesItem) {
    if (this.latencyPercentiles == null) {
      this.latencyPercentiles = new ArrayList<>();
    }
    this.latencyPercentiles.add(latencyPercentilesItem);
    return this;
  }

  /**
   * Representative latency percentiles.
   * @return latencyPercentiles
   */
  @javax.annotation.Nullable
  public List<LatencyPercentile> getLatencyPercentiles() {
    return latencyPercentiles;
  }

  public void setLatencyPercentiles(List<LatencyPercentile> latencyPercentiles) {
    this.latencyPercentiles = latencyPercentiles;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    LatencyDistribution latencyDistribution = (LatencyDistribution) o;
    return Objects.equals(this.latencyPercentiles, latencyDistribution.latencyPercentiles);
  }

  @Override
  public int hashCode() {
    return Objects.hash(latencyPercentiles);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class LatencyDistribution {\n");
    sb.append("    latencyPercentiles: ").append(toIndentedString(latencyPercentiles)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("latencyPercentiles");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to LatencyDistribution
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!LatencyDistribution.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in LatencyDistribution is not found in the empty JSON string", LatencyDistribution.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!LatencyDistribution.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `LatencyDistribution` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("latencyPercentiles") != null && !jsonObj.get("latencyPercentiles").isJsonNull()) {
        JsonArray jsonArraylatencyPercentiles = jsonObj.getAsJsonArray("latencyPercentiles");
        if (jsonArraylatencyPercentiles != null) {
          // ensure the json data is an array
          if (!jsonObj.get("latencyPercentiles").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `latencyPercentiles` to be an array in the JSON string but got `%s`", jsonObj.get("latencyPercentiles").toString()));
          }

          // validate the optional field `latencyPercentiles` (array)
          for (int i = 0; i < jsonArraylatencyPercentiles.size(); i++) {
            LatencyPercentile.validateJsonElement(jsonArraylatencyPercentiles.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!LatencyDistribution.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'LatencyDistribution' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<LatencyDistribution> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(LatencyDistribution.class));

       return (TypeAdapter<T>) new TypeAdapter<LatencyDistribution>() {
           @Override
           public void write(JsonWriter out, LatencyDistribution value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public LatencyDistribution read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of LatencyDistribution given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of LatencyDistribution
   * @throws IOException if the JSON string is invalid with respect to LatencyDistribution
   */
  public static LatencyDistribution fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, LatencyDistribution.class);
  }

  /**
   * Convert an instance of LatencyDistribution to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

