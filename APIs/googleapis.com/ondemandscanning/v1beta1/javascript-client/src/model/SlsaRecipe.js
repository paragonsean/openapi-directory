/**
 * On-Demand Scanning API
 * A service to scan container images for vulnerabilities.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SlsaRecipe model module.
 * @module model/SlsaRecipe
 * @version v1beta1
 */
class SlsaRecipe {
    /**
     * Constructs a new <code>SlsaRecipe</code>.
     * Steps taken to build the artifact. For a TaskRun, typically each container corresponds to one step in the recipe.
     * @alias module:model/SlsaRecipe
     */
    constructor() { 
        
        SlsaRecipe.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SlsaRecipe</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SlsaRecipe} obj Optional instance to populate.
     * @return {module:model/SlsaRecipe} The populated <code>SlsaRecipe</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SlsaRecipe();

            if (data.hasOwnProperty('arguments')) {
                obj['arguments'] = ApiClient.convertToType(data['arguments'], {'String': Object});
            }
            if (data.hasOwnProperty('definedInMaterial')) {
                obj['definedInMaterial'] = ApiClient.convertToType(data['definedInMaterial'], 'String');
            }
            if (data.hasOwnProperty('entryPoint')) {
                obj['entryPoint'] = ApiClient.convertToType(data['entryPoint'], 'String');
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = ApiClient.convertToType(data['environment'], {'String': Object});
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SlsaRecipe</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SlsaRecipe</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['definedInMaterial'] && !(typeof data['definedInMaterial'] === 'string' || data['definedInMaterial'] instanceof String)) {
            throw new Error("Expected the field `definedInMaterial` to be a primitive type in the JSON string but got " + data['definedInMaterial']);
        }
        // ensure the json data is a string
        if (data['entryPoint'] && !(typeof data['entryPoint'] === 'string' || data['entryPoint'] instanceof String)) {
            throw new Error("Expected the field `entryPoint` to be a primitive type in the JSON string but got " + data['entryPoint']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * Collection of all external inputs that influenced the build on top of recipe.definedInMaterial and recipe.entryPoint. For example, if the recipe type were \"make\", then this might be the flags passed to make aside from the target, which is captured in recipe.entryPoint. Depending on the recipe Type, the structure may be different.
 * @member {Object.<String, Object>} arguments
 */
SlsaRecipe.prototype['arguments'] = undefined;

/**
 * Index in materials containing the recipe steps that are not implied by recipe.type. For example, if the recipe type were \"make\", then this would point to the source containing the Makefile, not the make program itself. Set to -1 if the recipe doesn't come from a material, as zero is default unset value for int64.
 * @member {String} definedInMaterial
 */
SlsaRecipe.prototype['definedInMaterial'] = undefined;

/**
 * String identifying the entry point into the build. This is often a path to a configuration file and/or a target label within that file. The syntax and meaning are defined by recipe.type. For example, if the recipe type were \"make\", then this would reference the directory in which to run make as well as which target to use.
 * @member {String} entryPoint
 */
SlsaRecipe.prototype['entryPoint'] = undefined;

/**
 * Any other builder-controlled inputs necessary for correctly evaluating the recipe. Usually only needed for reproducing the build but not evaluated as part of policy. Depending on the recipe Type, the structure may be different.
 * @member {Object.<String, Object>} environment
 */
SlsaRecipe.prototype['environment'] = undefined;

/**
 * URI indicating what type of recipe was performed. It determines the meaning of recipe.entryPoint, recipe.arguments, recipe.environment, and materials.
 * @member {String} type
 */
SlsaRecipe.prototype['type'] = undefined;






export default SlsaRecipe;

