/**
 * Google Wallet API
 * API for issuers to save and manage Google Wallet Objects.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LocalizedString from './LocalizedString';

/**
 * The LabelValue model module.
 * @module model/LabelValue
 * @version v1
 */
class LabelValue {
    /**
     * Constructs a new <code>LabelValue</code>.
     * A pair of text strings to be displayed in the details view. Note we no longer display LabelValue/LabelValueRow as a table, instead a list of items.
     * @alias module:model/LabelValue
     */
    constructor() { 
        
        LabelValue.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LabelValue</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LabelValue} obj Optional instance to populate.
     * @return {module:model/LabelValue} The populated <code>LabelValue</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LabelValue();

            if (data.hasOwnProperty('label')) {
                obj['label'] = ApiClient.convertToType(data['label'], 'String');
            }
            if (data.hasOwnProperty('localizedLabel')) {
                obj['localizedLabel'] = LocalizedString.constructFromObject(data['localizedLabel']);
            }
            if (data.hasOwnProperty('localizedValue')) {
                obj['localizedValue'] = LocalizedString.constructFromObject(data['localizedValue']);
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LabelValue</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LabelValue</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['label'] && !(typeof data['label'] === 'string' || data['label'] instanceof String)) {
            throw new Error("Expected the field `label` to be a primitive type in the JSON string but got " + data['label']);
        }
        // validate the optional field `localizedLabel`
        if (data['localizedLabel']) { // data not null
          LocalizedString.validateJSON(data['localizedLabel']);
        }
        // validate the optional field `localizedValue`
        if (data['localizedValue']) { // data not null
          LocalizedString.validateJSON(data['localizedValue']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}



/**
 * The label for a specific row and column. Recommended maximum is 15 characters for a two-column layout and 30 characters for a one-column layout.
 * @member {String} label
 */
LabelValue.prototype['label'] = undefined;

/**
 * @member {module:model/LocalizedString} localizedLabel
 */
LabelValue.prototype['localizedLabel'] = undefined;

/**
 * @member {module:model/LocalizedString} localizedValue
 */
LabelValue.prototype['localizedValue'] = undefined;

/**
 * The value for a specific row and column. Recommended maximum is 15 characters for a two-column layout and 30 characters for a one-column layout.
 * @member {String} value
 */
LabelValue.prototype['value'] = undefined;






export default LabelValue;

