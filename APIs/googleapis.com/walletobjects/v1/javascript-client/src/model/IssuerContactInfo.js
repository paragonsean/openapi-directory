/**
 * Google Wallet API
 * API for issuers to save and manage Google Wallet Objects.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IssuerContactInfo model module.
 * @module model/IssuerContactInfo
 * @version v1
 */
class IssuerContactInfo {
    /**
     * Constructs a new <code>IssuerContactInfo</code>.
     * @alias module:model/IssuerContactInfo
     */
    constructor() { 
        
        IssuerContactInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>IssuerContactInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IssuerContactInfo} obj Optional instance to populate.
     * @return {module:model/IssuerContactInfo} The populated <code>IssuerContactInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IssuerContactInfo();

            if (data.hasOwnProperty('alertsEmails')) {
                obj['alertsEmails'] = ApiClient.convertToType(data['alertsEmails'], ['String']);
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('phone')) {
                obj['phone'] = ApiClient.convertToType(data['phone'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IssuerContactInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IssuerContactInfo</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['alertsEmails'])) {
            throw new Error("Expected the field `alertsEmails` to be an array in the JSON data but got " + data['alertsEmails']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['phone'] && !(typeof data['phone'] === 'string' || data['phone'] instanceof String)) {
            throw new Error("Expected the field `phone` to be a primitive type in the JSON string but got " + data['phone']);
        }

        return true;
    }


}



/**
 * Email addresses which will receive alerts.
 * @member {Array.<String>} alertsEmails
 */
IssuerContactInfo.prototype['alertsEmails'] = undefined;

/**
 * The primary contact email address.
 * @member {String} email
 */
IssuerContactInfo.prototype['email'] = undefined;

/**
 * The primary contact name.
 * @member {String} name
 */
IssuerContactInfo.prototype['name'] = undefined;

/**
 * The primary contact phone number.
 * @member {String} phone
 */
IssuerContactInfo.prototype['phone'] = undefined;






export default IssuerContactInfo;

