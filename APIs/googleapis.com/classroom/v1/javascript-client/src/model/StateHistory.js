/**
 * Google Classroom API
 * Manages classes, rosters, and invitations in Google Classroom.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The StateHistory model module.
 * @module model/StateHistory
 * @version v1
 */
class StateHistory {
    /**
     * Constructs a new <code>StateHistory</code>.
     * The history of each state this submission has been in.
     * @alias module:model/StateHistory
     */
    constructor() { 
        
        StateHistory.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>StateHistory</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StateHistory} obj Optional instance to populate.
     * @return {module:model/StateHistory} The populated <code>StateHistory</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StateHistory();

            if (data.hasOwnProperty('actorUserId')) {
                obj['actorUserId'] = ApiClient.convertToType(data['actorUserId'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('stateTimestamp')) {
                obj['stateTimestamp'] = ApiClient.convertToType(data['stateTimestamp'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StateHistory</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StateHistory</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['actorUserId'] && !(typeof data['actorUserId'] === 'string' || data['actorUserId'] instanceof String)) {
            throw new Error("Expected the field `actorUserId` to be a primitive type in the JSON string but got " + data['actorUserId']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['stateTimestamp'] && !(typeof data['stateTimestamp'] === 'string' || data['stateTimestamp'] instanceof String)) {
            throw new Error("Expected the field `stateTimestamp` to be a primitive type in the JSON string but got " + data['stateTimestamp']);
        }

        return true;
    }


}



/**
 * The teacher or student who made the change.
 * @member {String} actorUserId
 */
StateHistory.prototype['actorUserId'] = undefined;

/**
 * The workflow pipeline stage.
 * @member {module:model/StateHistory.StateEnum} state
 */
StateHistory.prototype['state'] = undefined;

/**
 * When the submission entered this state.
 * @member {String} stateTimestamp
 */
StateHistory.prototype['stateTimestamp'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
StateHistory['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "CREATED"
     * @const
     */
    "CREATED": "CREATED",

    /**
     * value: "TURNED_IN"
     * @const
     */
    "TURNED_IN": "TURNED_IN",

    /**
     * value: "RETURNED"
     * @const
     */
    "RETURNED": "RETURNED",

    /**
     * value: "RECLAIMED_BY_STUDENT"
     * @const
     */
    "RECLAIMED_BY_STUDENT": "RECLAIMED_BY_STUDENT",

    /**
     * value: "STUDENT_EDITED_AFTER_TURN_IN"
     * @const
     */
    "STUDENT_EDITED_AFTER_TURN_IN": "STUDENT_EDITED_AFTER_TURN_IN"
};



export default StateHistory;

