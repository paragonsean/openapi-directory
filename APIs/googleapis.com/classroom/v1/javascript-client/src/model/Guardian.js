/**
 * Google Classroom API
 * Manages classes, rosters, and invitations in Google Classroom.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UserProfile from './UserProfile';

/**
 * The Guardian model module.
 * @module model/Guardian
 * @version v1
 */
class Guardian {
    /**
     * Constructs a new <code>Guardian</code>.
     * Association between a student and a guardian of that student. The guardian may receive information about the student&#39;s course work.
     * @alias module:model/Guardian
     */
    constructor() { 
        
        Guardian.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Guardian</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Guardian} obj Optional instance to populate.
     * @return {module:model/Guardian} The populated <code>Guardian</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Guardian();

            if (data.hasOwnProperty('guardianId')) {
                obj['guardianId'] = ApiClient.convertToType(data['guardianId'], 'String');
            }
            if (data.hasOwnProperty('guardianProfile')) {
                obj['guardianProfile'] = UserProfile.constructFromObject(data['guardianProfile']);
            }
            if (data.hasOwnProperty('invitedEmailAddress')) {
                obj['invitedEmailAddress'] = ApiClient.convertToType(data['invitedEmailAddress'], 'String');
            }
            if (data.hasOwnProperty('studentId')) {
                obj['studentId'] = ApiClient.convertToType(data['studentId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Guardian</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Guardian</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['guardianId'] && !(typeof data['guardianId'] === 'string' || data['guardianId'] instanceof String)) {
            throw new Error("Expected the field `guardianId` to be a primitive type in the JSON string but got " + data['guardianId']);
        }
        // validate the optional field `guardianProfile`
        if (data['guardianProfile']) { // data not null
          UserProfile.validateJSON(data['guardianProfile']);
        }
        // ensure the json data is a string
        if (data['invitedEmailAddress'] && !(typeof data['invitedEmailAddress'] === 'string' || data['invitedEmailAddress'] instanceof String)) {
            throw new Error("Expected the field `invitedEmailAddress` to be a primitive type in the JSON string but got " + data['invitedEmailAddress']);
        }
        // ensure the json data is a string
        if (data['studentId'] && !(typeof data['studentId'] === 'string' || data['studentId'] instanceof String)) {
            throw new Error("Expected the field `studentId` to be a primitive type in the JSON string but got " + data['studentId']);
        }

        return true;
    }


}



/**
 * Identifier for the guardian.
 * @member {String} guardianId
 */
Guardian.prototype['guardianId'] = undefined;

/**
 * @member {module:model/UserProfile} guardianProfile
 */
Guardian.prototype['guardianProfile'] = undefined;

/**
 * The email address to which the initial guardian invitation was sent. This field is only visible to domain administrators.
 * @member {String} invitedEmailAddress
 */
Guardian.prototype['invitedEmailAddress'] = undefined;

/**
 * Identifier for the student to whom the guardian relationship applies.
 * @member {String} studentId
 */
Guardian.prototype['studentId'] = undefined;






export default Guardian;

