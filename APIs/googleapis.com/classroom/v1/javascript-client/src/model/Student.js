/**
 * Google Classroom API
 * Manages classes, rosters, and invitations in Google Classroom.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DriveFolder from './DriveFolder';
import UserProfile from './UserProfile';

/**
 * The Student model module.
 * @module model/Student
 * @version v1
 */
class Student {
    /**
     * Constructs a new <code>Student</code>.
     * Student in a course.
     * @alias module:model/Student
     */
    constructor() { 
        
        Student.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Student</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Student} obj Optional instance to populate.
     * @return {module:model/Student} The populated <code>Student</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Student();

            if (data.hasOwnProperty('courseId')) {
                obj['courseId'] = ApiClient.convertToType(data['courseId'], 'String');
            }
            if (data.hasOwnProperty('profile')) {
                obj['profile'] = UserProfile.constructFromObject(data['profile']);
            }
            if (data.hasOwnProperty('studentWorkFolder')) {
                obj['studentWorkFolder'] = DriveFolder.constructFromObject(data['studentWorkFolder']);
            }
            if (data.hasOwnProperty('userId')) {
                obj['userId'] = ApiClient.convertToType(data['userId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Student</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Student</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['courseId'] && !(typeof data['courseId'] === 'string' || data['courseId'] instanceof String)) {
            throw new Error("Expected the field `courseId` to be a primitive type in the JSON string but got " + data['courseId']);
        }
        // validate the optional field `profile`
        if (data['profile']) { // data not null
          UserProfile.validateJSON(data['profile']);
        }
        // validate the optional field `studentWorkFolder`
        if (data['studentWorkFolder']) { // data not null
          DriveFolder.validateJSON(data['studentWorkFolder']);
        }
        // ensure the json data is a string
        if (data['userId'] && !(typeof data['userId'] === 'string' || data['userId'] instanceof String)) {
            throw new Error("Expected the field `userId` to be a primitive type in the JSON string but got " + data['userId']);
        }

        return true;
    }


}



/**
 * Identifier of the course. Read-only.
 * @member {String} courseId
 */
Student.prototype['courseId'] = undefined;

/**
 * @member {module:model/UserProfile} profile
 */
Student.prototype['profile'] = undefined;

/**
 * @member {module:model/DriveFolder} studentWorkFolder
 */
Student.prototype['studentWorkFolder'] = undefined;

/**
 * Identifier of the user. When specified as a parameter of a request, this identifier can be one of the following: * the numeric identifier for the user * the email address of the user * the string literal `\"me\"`, indicating the requesting user
 * @member {String} userId
 */
Student.prototype['userId'] = undefined;






export default Student;

