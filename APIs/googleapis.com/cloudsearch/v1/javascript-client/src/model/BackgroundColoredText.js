/**
 * Cloud Search API
 * Cloud Search provides cloud-based search capabilities over Google Workspace data. The Cloud Search API allows indexing of non-Google Workspace data into Cloud Search.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BackgroundColoredText model module.
 * @module model/BackgroundColoredText
 * @version v1
 */
class BackgroundColoredText {
    /**
     * Constructs a new <code>BackgroundColoredText</code>.
     * @alias module:model/BackgroundColoredText
     */
    constructor() { 
        
        BackgroundColoredText.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BackgroundColoredText</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BackgroundColoredText} obj Optional instance to populate.
     * @return {module:model/BackgroundColoredText} The populated <code>BackgroundColoredText</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BackgroundColoredText();

            if (data.hasOwnProperty('backgroundColor')) {
                obj['backgroundColor'] = ApiClient.convertToType(data['backgroundColor'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BackgroundColoredText</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BackgroundColoredText</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['backgroundColor'] && !(typeof data['backgroundColor'] === 'string' || data['backgroundColor'] instanceof String)) {
            throw new Error("Expected the field `backgroundColor` to be a primitive type in the JSON string but got " + data['backgroundColor']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }

        return true;
    }


}



/**
 * [Optional] Color of the background. The text color can change depending on the selected background color, and the client does not have control over this. If missing, the background will be WHITE.
 * @member {module:model/BackgroundColoredText.BackgroundColorEnum} backgroundColor
 */
BackgroundColoredText.prototype['backgroundColor'] = undefined;

/**
 * [Required] The text to display.
 * @member {String} text
 */
BackgroundColoredText.prototype['text'] = undefined;





/**
 * Allowed values for the <code>backgroundColor</code> property.
 * @enum {String}
 * @readonly
 */
BackgroundColoredText['BackgroundColorEnum'] = {

    /**
     * value: "UNKNOWN_COLOR"
     * @const
     */
    "UNKNOWN_COLOR": "UNKNOWN_COLOR",

    /**
     * value: "WHITE"
     * @const
     */
    "WHITE": "WHITE",

    /**
     * value: "YELLOW"
     * @const
     */
    "YELLOW": "YELLOW",

    /**
     * value: "ORANGE"
     * @const
     */
    "ORANGE": "ORANGE",

    /**
     * value: "GREEN"
     * @const
     */
    "GREEN": "GREEN",

    /**
     * value: "BLUE"
     * @const
     */
    "BLUE": "BLUE",

    /**
     * value: "GREY"
     * @const
     */
    "GREY": "GREY"
};



export default BackgroundColoredText;

