/**
 * Cloud Search API
 * Cloud Search provides cloud-based search capabilities over Google Workspace data. The Cloud Search API allows indexing of non-Google Workspace data into Cloud Search.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EnterpriseTopazSidekickRankingParams model module.
 * @module model/EnterpriseTopazSidekickRankingParams
 * @version v1
 */
class EnterpriseTopazSidekickRankingParams {
    /**
     * Constructs a new <code>EnterpriseTopazSidekickRankingParams</code>.
     * Ranking params.
     * @alias module:model/EnterpriseTopazSidekickRankingParams
     */
    constructor() { 
        
        EnterpriseTopazSidekickRankingParams.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EnterpriseTopazSidekickRankingParams</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EnterpriseTopazSidekickRankingParams} obj Optional instance to populate.
     * @return {module:model/EnterpriseTopazSidekickRankingParams} The populated <code>EnterpriseTopazSidekickRankingParams</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EnterpriseTopazSidekickRankingParams();

            if (data.hasOwnProperty('endTimeMs')) {
                obj['endTimeMs'] = ApiClient.convertToType(data['endTimeMs'], 'String');
            }
            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'String');
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'Number');
            }
            if (data.hasOwnProperty('spanMs')) {
                obj['spanMs'] = ApiClient.convertToType(data['spanMs'], 'String');
            }
            if (data.hasOwnProperty('startTimeMs')) {
                obj['startTimeMs'] = ApiClient.convertToType(data['startTimeMs'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EnterpriseTopazSidekickRankingParams</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EnterpriseTopazSidekickRankingParams</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['endTimeMs'] && !(typeof data['endTimeMs'] === 'string' || data['endTimeMs'] instanceof String)) {
            throw new Error("Expected the field `endTimeMs` to be a primitive type in the JSON string but got " + data['endTimeMs']);
        }
        // ensure the json data is a string
        if (data['priority'] && !(typeof data['priority'] === 'string' || data['priority'] instanceof String)) {
            throw new Error("Expected the field `priority` to be a primitive type in the JSON string but got " + data['priority']);
        }
        // ensure the json data is a string
        if (data['spanMs'] && !(typeof data['spanMs'] === 'string' || data['spanMs'] instanceof String)) {
            throw new Error("Expected the field `spanMs` to be a primitive type in the JSON string but got " + data['spanMs']);
        }
        // ensure the json data is a string
        if (data['startTimeMs'] && !(typeof data['startTimeMs'] === 'string' || data['startTimeMs'] instanceof String)) {
            throw new Error("Expected the field `startTimeMs` to be a primitive type in the JSON string but got " + data['startTimeMs']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * The end-time that this object will expect to occur. If the type is marked as FIXED, then this end-time will persist after bidding. If the type is marked as FLEXIBLE, this field is NOT expected to be filled and will be filled in after it has won a bid. Expected to be set when type is set to FIXED.
 * @member {String} endTimeMs
 */
EnterpriseTopazSidekickRankingParams.prototype['endTimeMs'] = undefined;

/**
 * The priority to determine between objects that have the same start_time_ms The lower-value of priority == ranked higher. Max-priority = 0. Expected to be set for all types.
 * @member {module:model/EnterpriseTopazSidekickRankingParams.PriorityEnum} priority
 */
EnterpriseTopazSidekickRankingParams.prototype['priority'] = undefined;

/**
 * The score of the card to be used to break priority-ties
 * @member {Number} score
 */
EnterpriseTopazSidekickRankingParams.prototype['score'] = undefined;

/**
 * The span that this card will take in the stream Expected to be set when type is set to FLEXIBLE.
 * @member {String} spanMs
 */
EnterpriseTopazSidekickRankingParams.prototype['spanMs'] = undefined;

/**
 * The start-time that this object will bid-for If the type is marked as FIXED, then this start-time will persist after bidding. If the type is marked as FLEXIBLE, then it will occur at the given time or sometime after the requested time. Expected to be set for all types.
 * @member {String} startTimeMs
 */
EnterpriseTopazSidekickRankingParams.prototype['startTimeMs'] = undefined;

/**
 * The packing type of this object.
 * @member {module:model/EnterpriseTopazSidekickRankingParams.TypeEnum} type
 */
EnterpriseTopazSidekickRankingParams.prototype['type'] = undefined;





/**
 * Allowed values for the <code>priority</code> property.
 * @enum {String}
 * @readonly
 */
EnterpriseTopazSidekickRankingParams['PriorityEnum'] = {

    /**
     * value: "UNKNOWN"
     * @const
     */
    "UNKNOWN": "UNKNOWN",

    /**
     * value: "CRITICAL"
     * @const
     */
    "CRITICAL": "CRITICAL",

    /**
     * value: "IMPORTANT"
     * @const
     */
    "IMPORTANT": "IMPORTANT",

    /**
     * value: "HIGH"
     * @const
     */
    "HIGH": "HIGH",

    /**
     * value: "NORMAL"
     * @const
     */
    "NORMAL": "NORMAL",

    /**
     * value: "BEST_EFFORT"
     * @const
     */
    "BEST_EFFORT": "BEST_EFFORT"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
EnterpriseTopazSidekickRankingParams['TypeEnum'] = {

    /**
     * value: "FIXED"
     * @const
     */
    "FIXED": "FIXED",

    /**
     * value: "FLEXIBLE"
     * @const
     */
    "FLEXIBLE": "FLEXIBLE"
};



export default EnterpriseTopazSidekickRankingParams;

