/*
 * Cloud Search API
 * Cloud Search provides cloud-based search capabilities over Google Workspace data. The Cloud Search API allows indexing of non-Google Workspace data into Cloud Search.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.Filter;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * CompositeFilter
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:35:46.641752-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class CompositeFilter {
  /**
   * The logic operator of the sub filter.
   */
  @JsonAdapter(LogicOperatorEnum.Adapter.class)
  public enum LogicOperatorEnum {
    AND("AND"),
    
    OR("OR"),
    
    NOT("NOT");

    private String value;

    LogicOperatorEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static LogicOperatorEnum fromValue(String value) {
      for (LogicOperatorEnum b : LogicOperatorEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<LogicOperatorEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final LogicOperatorEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public LogicOperatorEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return LogicOperatorEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      LogicOperatorEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_LOGIC_OPERATOR = "logicOperator";
  @SerializedName(SERIALIZED_NAME_LOGIC_OPERATOR)
  private LogicOperatorEnum logicOperator;

  public static final String SERIALIZED_NAME_SUB_FILTERS = "subFilters";
  @SerializedName(SERIALIZED_NAME_SUB_FILTERS)
  private List<Filter> subFilters = new ArrayList<>();

  public CompositeFilter() {
  }

  public CompositeFilter logicOperator(LogicOperatorEnum logicOperator) {
    this.logicOperator = logicOperator;
    return this;
  }

  /**
   * The logic operator of the sub filter.
   * @return logicOperator
   */
  @javax.annotation.Nullable
  public LogicOperatorEnum getLogicOperator() {
    return logicOperator;
  }

  public void setLogicOperator(LogicOperatorEnum logicOperator) {
    this.logicOperator = logicOperator;
  }


  public CompositeFilter subFilters(List<Filter> subFilters) {
    this.subFilters = subFilters;
    return this;
  }

  public CompositeFilter addSubFiltersItem(Filter subFiltersItem) {
    if (this.subFilters == null) {
      this.subFilters = new ArrayList<>();
    }
    this.subFilters.add(subFiltersItem);
    return this;
  }

  /**
   * Sub filters.
   * @return subFilters
   */
  @javax.annotation.Nullable
  public List<Filter> getSubFilters() {
    return subFilters;
  }

  public void setSubFilters(List<Filter> subFilters) {
    this.subFilters = subFilters;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CompositeFilter compositeFilter = (CompositeFilter) o;
    return Objects.equals(this.logicOperator, compositeFilter.logicOperator) &&
        Objects.equals(this.subFilters, compositeFilter.subFilters);
  }

  @Override
  public int hashCode() {
    return Objects.hash(logicOperator, subFilters);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class CompositeFilter {\n");
    sb.append("    logicOperator: ").append(toIndentedString(logicOperator)).append("\n");
    sb.append("    subFilters: ").append(toIndentedString(subFilters)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("logicOperator");
    openapiFields.add("subFilters");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to CompositeFilter
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!CompositeFilter.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in CompositeFilter is not found in the empty JSON string", CompositeFilter.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!CompositeFilter.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `CompositeFilter` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("logicOperator") != null && !jsonObj.get("logicOperator").isJsonNull()) && !jsonObj.get("logicOperator").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `logicOperator` to be a primitive type in the JSON string but got `%s`", jsonObj.get("logicOperator").toString()));
      }
      // validate the optional field `logicOperator`
      if (jsonObj.get("logicOperator") != null && !jsonObj.get("logicOperator").isJsonNull()) {
        LogicOperatorEnum.validateJsonElement(jsonObj.get("logicOperator"));
      }
      if (jsonObj.get("subFilters") != null && !jsonObj.get("subFilters").isJsonNull()) {
        JsonArray jsonArraysubFilters = jsonObj.getAsJsonArray("subFilters");
        if (jsonArraysubFilters != null) {
          // ensure the json data is an array
          if (!jsonObj.get("subFilters").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `subFilters` to be an array in the JSON string but got `%s`", jsonObj.get("subFilters").toString()));
          }

          // validate the optional field `subFilters` (array)
          for (int i = 0; i < jsonArraysubFilters.size(); i++) {
            Filter.validateJsonElement(jsonArraysubFilters.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!CompositeFilter.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'CompositeFilter' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<CompositeFilter> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(CompositeFilter.class));

       return (TypeAdapter<T>) new TypeAdapter<CompositeFilter>() {
           @Override
           public void write(JsonWriter out, CompositeFilter value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public CompositeFilter read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of CompositeFilter given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of CompositeFilter
   * @throws IOException if the JSON string is invalid with respect to CompositeFilter
   */
  public static CompositeFilter fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CompositeFilter.class);
  }

  /**
   * Convert an instance of CompositeFilter to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

