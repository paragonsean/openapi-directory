/*
 * Cloud Search API
 * Cloud Search provides cloud-based search capabilities over Google Workspace data. The Cloud Search API allows indexing of non-Google Workspace data into Cloud Search.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.SuggestResult;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * Response of the suggest API.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:35:46.641752-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class SuggestResponse {
  public static final String SERIALIZED_NAME_SUGGEST_RESULTS = "suggestResults";
  @SerializedName(SERIALIZED_NAME_SUGGEST_RESULTS)
  private List<SuggestResult> suggestResults = new ArrayList<>();

  public SuggestResponse() {
  }

  public SuggestResponse suggestResults(List<SuggestResult> suggestResults) {
    this.suggestResults = suggestResults;
    return this;
  }

  public SuggestResponse addSuggestResultsItem(SuggestResult suggestResultsItem) {
    if (this.suggestResults == null) {
      this.suggestResults = new ArrayList<>();
    }
    this.suggestResults.add(suggestResultsItem);
    return this;
  }

  /**
   * List of suggestions.
   * @return suggestResults
   */
  @javax.annotation.Nullable
  public List<SuggestResult> getSuggestResults() {
    return suggestResults;
  }

  public void setSuggestResults(List<SuggestResult> suggestResults) {
    this.suggestResults = suggestResults;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SuggestResponse suggestResponse = (SuggestResponse) o;
    return Objects.equals(this.suggestResults, suggestResponse.suggestResults);
  }

  @Override
  public int hashCode() {
    return Objects.hash(suggestResults);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SuggestResponse {\n");
    sb.append("    suggestResults: ").append(toIndentedString(suggestResults)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("suggestResults");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SuggestResponse
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SuggestResponse.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SuggestResponse is not found in the empty JSON string", SuggestResponse.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!SuggestResponse.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `SuggestResponse` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("suggestResults") != null && !jsonObj.get("suggestResults").isJsonNull()) {
        JsonArray jsonArraysuggestResults = jsonObj.getAsJsonArray("suggestResults");
        if (jsonArraysuggestResults != null) {
          // ensure the json data is an array
          if (!jsonObj.get("suggestResults").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `suggestResults` to be an array in the JSON string but got `%s`", jsonObj.get("suggestResults").toString()));
          }

          // validate the optional field `suggestResults` (array)
          for (int i = 0; i < jsonArraysuggestResults.size(); i++) {
            SuggestResult.validateJsonElement(jsonArraysuggestResults.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SuggestResponse.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SuggestResponse' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SuggestResponse> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SuggestResponse.class));

       return (TypeAdapter<T>) new TypeAdapter<SuggestResponse>() {
           @Override
           public void write(JsonWriter out, SuggestResponse value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SuggestResponse read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SuggestResponse given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SuggestResponse
   * @throws IOException if the JSON string is invalid with respect to SuggestResponse
   */
  public static SuggestResponse fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SuggestResponse.class);
  }

  /**
   * Convert an instance of SuggestResponse to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

