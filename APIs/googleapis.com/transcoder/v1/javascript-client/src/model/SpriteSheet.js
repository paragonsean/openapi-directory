/**
 * Transcoder API
 * This API converts video files into formats suitable for consumer distribution. For more information, see the Transcoder API overview. 
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SpriteSheet model module.
 * @module model/SpriteSheet
 * @version v1
 */
class SpriteSheet {
    /**
     * Constructs a new <code>SpriteSheet</code>.
     * Sprite sheet configuration.
     * @alias module:model/SpriteSheet
     */
    constructor() { 
        
        SpriteSheet.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SpriteSheet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SpriteSheet} obj Optional instance to populate.
     * @return {module:model/SpriteSheet} The populated <code>SpriteSheet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SpriteSheet();

            if (data.hasOwnProperty('columnCount')) {
                obj['columnCount'] = ApiClient.convertToType(data['columnCount'], 'Number');
            }
            if (data.hasOwnProperty('endTimeOffset')) {
                obj['endTimeOffset'] = ApiClient.convertToType(data['endTimeOffset'], 'String');
            }
            if (data.hasOwnProperty('filePrefix')) {
                obj['filePrefix'] = ApiClient.convertToType(data['filePrefix'], 'String');
            }
            if (data.hasOwnProperty('format')) {
                obj['format'] = ApiClient.convertToType(data['format'], 'String');
            }
            if (data.hasOwnProperty('interval')) {
                obj['interval'] = ApiClient.convertToType(data['interval'], 'String');
            }
            if (data.hasOwnProperty('quality')) {
                obj['quality'] = ApiClient.convertToType(data['quality'], 'Number');
            }
            if (data.hasOwnProperty('rowCount')) {
                obj['rowCount'] = ApiClient.convertToType(data['rowCount'], 'Number');
            }
            if (data.hasOwnProperty('spriteHeightPixels')) {
                obj['spriteHeightPixels'] = ApiClient.convertToType(data['spriteHeightPixels'], 'Number');
            }
            if (data.hasOwnProperty('spriteWidthPixels')) {
                obj['spriteWidthPixels'] = ApiClient.convertToType(data['spriteWidthPixels'], 'Number');
            }
            if (data.hasOwnProperty('startTimeOffset')) {
                obj['startTimeOffset'] = ApiClient.convertToType(data['startTimeOffset'], 'String');
            }
            if (data.hasOwnProperty('totalCount')) {
                obj['totalCount'] = ApiClient.convertToType(data['totalCount'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SpriteSheet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SpriteSheet</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['endTimeOffset'] && !(typeof data['endTimeOffset'] === 'string' || data['endTimeOffset'] instanceof String)) {
            throw new Error("Expected the field `endTimeOffset` to be a primitive type in the JSON string but got " + data['endTimeOffset']);
        }
        // ensure the json data is a string
        if (data['filePrefix'] && !(typeof data['filePrefix'] === 'string' || data['filePrefix'] instanceof String)) {
            throw new Error("Expected the field `filePrefix` to be a primitive type in the JSON string but got " + data['filePrefix']);
        }
        // ensure the json data is a string
        if (data['format'] && !(typeof data['format'] === 'string' || data['format'] instanceof String)) {
            throw new Error("Expected the field `format` to be a primitive type in the JSON string but got " + data['format']);
        }
        // ensure the json data is a string
        if (data['interval'] && !(typeof data['interval'] === 'string' || data['interval'] instanceof String)) {
            throw new Error("Expected the field `interval` to be a primitive type in the JSON string but got " + data['interval']);
        }
        // ensure the json data is a string
        if (data['startTimeOffset'] && !(typeof data['startTimeOffset'] === 'string' || data['startTimeOffset'] instanceof String)) {
            throw new Error("Expected the field `startTimeOffset` to be a primitive type in the JSON string but got " + data['startTimeOffset']);
        }

        return true;
    }


}



/**
 * The maximum number of sprites per row in a sprite sheet. The default is 0, which indicates no maximum limit.
 * @member {Number} columnCount
 */
SpriteSheet.prototype['columnCount'] = undefined;

/**
 * End time in seconds, relative to the output file timeline. When `end_time_offset` is not specified, the sprites are generated until the end of the output file.
 * @member {String} endTimeOffset
 */
SpriteSheet.prototype['endTimeOffset'] = undefined;

/**
 * Required. File name prefix for the generated sprite sheets. Each sprite sheet has an incremental 10-digit zero-padded suffix starting from 0 before the extension, such as `sprite_sheet0000000123.jpeg`.
 * @member {String} filePrefix
 */
SpriteSheet.prototype['filePrefix'] = undefined;

/**
 * Format type. The default is `jpeg`. Supported formats: - `jpeg`
 * @member {String} format
 */
SpriteSheet.prototype['format'] = undefined;

/**
 * Starting from `0s`, create sprites at regular intervals. Specify the interval value in seconds.
 * @member {String} interval
 */
SpriteSheet.prototype['interval'] = undefined;

/**
 * The quality of the generated sprite sheet. Enter a value between 1 and 100, where 1 is the lowest quality and 100 is the highest quality. The default is 100. A high quality value corresponds to a low image data compression ratio.
 * @member {Number} quality
 */
SpriteSheet.prototype['quality'] = undefined;

/**
 * The maximum number of rows per sprite sheet. When the sprite sheet is full, a new sprite sheet is created. The default is 0, which indicates no maximum limit.
 * @member {Number} rowCount
 */
SpriteSheet.prototype['rowCount'] = undefined;

/**
 * Required. The height of sprite in pixels. Must be an even integer. To preserve the source aspect ratio, set the SpriteSheet.sprite_height_pixels field or the SpriteSheet.sprite_width_pixels field, but not both (the API will automatically calculate the missing field). For portrait videos that contain horizontal ASR and rotation metadata, provide the height, in pixels, per the horizontal ASR. The API calculates the width per the horizontal ASR. The API detects any rotation metadata and swaps the requested height and width for the output.
 * @member {Number} spriteHeightPixels
 */
SpriteSheet.prototype['spriteHeightPixels'] = undefined;

/**
 * Required. The width of sprite in pixels. Must be an even integer. To preserve the source aspect ratio, set the SpriteSheet.sprite_width_pixels field or the SpriteSheet.sprite_height_pixels field, but not both (the API will automatically calculate the missing field). For portrait videos that contain horizontal ASR and rotation metadata, provide the width, in pixels, per the horizontal ASR. The API calculates the height per the horizontal ASR. The API detects any rotation metadata and swaps the requested height and width for the output.
 * @member {Number} spriteWidthPixels
 */
SpriteSheet.prototype['spriteWidthPixels'] = undefined;

/**
 * Start time in seconds, relative to the output file timeline. Determines the first sprite to pick. The default is `0s`.
 * @member {String} startTimeOffset
 */
SpriteSheet.prototype['startTimeOffset'] = undefined;

/**
 * Total number of sprites. Create the specified number of sprites distributed evenly across the timeline of the output media. The default is 100.
 * @member {Number} totalCount
 */
SpriteSheet.prototype['totalCount'] = undefined;






export default SpriteSheet;

