/**
 * Google Analytics Data API
 * Accesses report data in Google Analytics. Warning: Creating multiple Customer Applications, Accounts, or Projects to simulate or act as a single Customer Application, Account, or Project (respectively) or to circumvent Service-specific usage limits or quotas is a direct violation of Google Cloud Platform Terms of Service as well as Google APIs Terms of Service. These actions can result in immediate termination of your GCP project(s) without any warning. 
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ActiveMetricRestriction model module.
 * @module model/ActiveMetricRestriction
 * @version v1beta
 */
class ActiveMetricRestriction {
    /**
     * Constructs a new <code>ActiveMetricRestriction</code>.
     * A metric actively restricted in creating the report.
     * @alias module:model/ActiveMetricRestriction
     */
    constructor() { 
        
        ActiveMetricRestriction.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ActiveMetricRestriction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ActiveMetricRestriction} obj Optional instance to populate.
     * @return {module:model/ActiveMetricRestriction} The populated <code>ActiveMetricRestriction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ActiveMetricRestriction();

            if (data.hasOwnProperty('metricName')) {
                obj['metricName'] = ApiClient.convertToType(data['metricName'], 'String');
            }
            if (data.hasOwnProperty('restrictedMetricTypes')) {
                obj['restrictedMetricTypes'] = ApiClient.convertToType(data['restrictedMetricTypes'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ActiveMetricRestriction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ActiveMetricRestriction</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['metricName'] && !(typeof data['metricName'] === 'string' || data['metricName'] instanceof String)) {
            throw new Error("Expected the field `metricName` to be a primitive type in the JSON string but got " + data['metricName']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['restrictedMetricTypes'])) {
            throw new Error("Expected the field `restrictedMetricTypes` to be an array in the JSON data but got " + data['restrictedMetricTypes']);
        }

        return true;
    }


}



/**
 * The name of the restricted metric.
 * @member {String} metricName
 */
ActiveMetricRestriction.prototype['metricName'] = undefined;

/**
 * The reason for this metric's restriction.
 * @member {Array.<module:model/ActiveMetricRestriction.RestrictedMetricTypesEnum>} restrictedMetricTypes
 */
ActiveMetricRestriction.prototype['restrictedMetricTypes'] = undefined;





/**
 * Allowed values for the <code>restrictedMetricTypes</code> property.
 * @enum {String}
 * @readonly
 */
ActiveMetricRestriction['RestrictedMetricTypesEnum'] = {

    /**
     * value: "RESTRICTED_METRIC_TYPE_UNSPECIFIED"
     * @const
     */
    "RESTRICTED_METRIC_TYPE_UNSPECIFIED": "RESTRICTED_METRIC_TYPE_UNSPECIFIED",

    /**
     * value: "COST_DATA"
     * @const
     */
    "COST_DATA": "COST_DATA",

    /**
     * value: "REVENUE_DATA"
     * @const
     */
    "REVENUE_DATA": "REVENUE_DATA"
};



export default ActiveMetricRestriction;

