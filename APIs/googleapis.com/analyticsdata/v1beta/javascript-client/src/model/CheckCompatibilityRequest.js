/**
 * Google Analytics Data API
 * Accesses report data in Google Analytics. Warning: Creating multiple Customer Applications, Accounts, or Projects to simulate or act as a single Customer Application, Account, or Project (respectively) or to circumvent Service-specific usage limits or quotas is a direct violation of Google Cloud Platform Terms of Service as well as Google APIs Terms of Service. These actions can result in immediate termination of your GCP project(s) without any warning. 
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Dimension from './Dimension';
import FilterExpression from './FilterExpression';
import Metric from './Metric';

/**
 * The CheckCompatibilityRequest model module.
 * @module model/CheckCompatibilityRequest
 * @version v1beta
 */
class CheckCompatibilityRequest {
    /**
     * Constructs a new <code>CheckCompatibilityRequest</code>.
     * The request for compatibility information for a report&#39;s dimensions and metrics. Check compatibility provides a preview of the compatibility of a report; fields shared with the &#x60;runReport&#x60; request should be the same values as in your &#x60;runReport&#x60; request.
     * @alias module:model/CheckCompatibilityRequest
     */
    constructor() { 
        
        CheckCompatibilityRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CheckCompatibilityRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CheckCompatibilityRequest} obj Optional instance to populate.
     * @return {module:model/CheckCompatibilityRequest} The populated <code>CheckCompatibilityRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CheckCompatibilityRequest();

            if (data.hasOwnProperty('compatibilityFilter')) {
                obj['compatibilityFilter'] = ApiClient.convertToType(data['compatibilityFilter'], 'String');
            }
            if (data.hasOwnProperty('dimensionFilter')) {
                obj['dimensionFilter'] = FilterExpression.constructFromObject(data['dimensionFilter']);
            }
            if (data.hasOwnProperty('dimensions')) {
                obj['dimensions'] = ApiClient.convertToType(data['dimensions'], [Dimension]);
            }
            if (data.hasOwnProperty('metricFilter')) {
                obj['metricFilter'] = FilterExpression.constructFromObject(data['metricFilter']);
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [Metric]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CheckCompatibilityRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CheckCompatibilityRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['compatibilityFilter'] && !(typeof data['compatibilityFilter'] === 'string' || data['compatibilityFilter'] instanceof String)) {
            throw new Error("Expected the field `compatibilityFilter` to be a primitive type in the JSON string but got " + data['compatibilityFilter']);
        }
        // validate the optional field `dimensionFilter`
        if (data['dimensionFilter']) { // data not null
          FilterExpression.validateJSON(data['dimensionFilter']);
        }
        if (data['dimensions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensions'])) {
                throw new Error("Expected the field `dimensions` to be an array in the JSON data but got " + data['dimensions']);
            }
            // validate the optional field `dimensions` (array)
            for (const item of data['dimensions']) {
                Dimension.validateJSON(item);
            };
        }
        // validate the optional field `metricFilter`
        if (data['metricFilter']) { // data not null
          FilterExpression.validateJSON(data['metricFilter']);
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                Metric.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Filters the dimensions and metrics in the response to just this compatibility. Commonly used as `”compatibilityFilter”: “COMPATIBLE”` to only return compatible dimensions & metrics.
 * @member {module:model/CheckCompatibilityRequest.CompatibilityFilterEnum} compatibilityFilter
 */
CheckCompatibilityRequest.prototype['compatibilityFilter'] = undefined;

/**
 * @member {module:model/FilterExpression} dimensionFilter
 */
CheckCompatibilityRequest.prototype['dimensionFilter'] = undefined;

/**
 * The dimensions in this report. `dimensions` should be the same value as in your `runReport` request.
 * @member {Array.<module:model/Dimension>} dimensions
 */
CheckCompatibilityRequest.prototype['dimensions'] = undefined;

/**
 * @member {module:model/FilterExpression} metricFilter
 */
CheckCompatibilityRequest.prototype['metricFilter'] = undefined;

/**
 * The metrics in this report. `metrics` should be the same value as in your `runReport` request.
 * @member {Array.<module:model/Metric>} metrics
 */
CheckCompatibilityRequest.prototype['metrics'] = undefined;





/**
 * Allowed values for the <code>compatibilityFilter</code> property.
 * @enum {String}
 * @readonly
 */
CheckCompatibilityRequest['CompatibilityFilterEnum'] = {

    /**
     * value: "COMPATIBILITY_UNSPECIFIED"
     * @const
     */
    "COMPATIBILITY_UNSPECIFIED": "COMPATIBILITY_UNSPECIFIED",

    /**
     * value: "COMPATIBLE"
     * @const
     */
    "COMPATIBLE": "COMPATIBLE",

    /**
     * value: "INCOMPATIBLE"
     * @const
     */
    "INCOMPATIBLE": "INCOMPATIBLE"
};



export default CheckCompatibilityRequest;

