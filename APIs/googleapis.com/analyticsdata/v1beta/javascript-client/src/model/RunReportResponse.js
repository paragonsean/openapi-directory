/**
 * Google Analytics Data API
 * Accesses report data in Google Analytics. Warning: Creating multiple Customer Applications, Accounts, or Projects to simulate or act as a single Customer Application, Account, or Project (respectively) or to circumvent Service-specific usage limits or quotas is a direct violation of Google Cloud Platform Terms of Service as well as Google APIs Terms of Service. These actions can result in immediate termination of your GCP project(s) without any warning. 
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DimensionHeader from './DimensionHeader';
import MetricHeader from './MetricHeader';
import PropertyQuota from './PropertyQuota';
import ResponseMetaData from './ResponseMetaData';
import Row from './Row';

/**
 * The RunReportResponse model module.
 * @module model/RunReportResponse
 * @version v1beta
 */
class RunReportResponse {
    /**
     * Constructs a new <code>RunReportResponse</code>.
     * The response report table corresponding to a request.
     * @alias module:model/RunReportResponse
     */
    constructor() { 
        
        RunReportResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RunReportResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RunReportResponse} obj Optional instance to populate.
     * @return {module:model/RunReportResponse} The populated <code>RunReportResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RunReportResponse();

            if (data.hasOwnProperty('dimensionHeaders')) {
                obj['dimensionHeaders'] = ApiClient.convertToType(data['dimensionHeaders'], [DimensionHeader]);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('maximums')) {
                obj['maximums'] = ApiClient.convertToType(data['maximums'], [Row]);
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ResponseMetaData.constructFromObject(data['metadata']);
            }
            if (data.hasOwnProperty('metricHeaders')) {
                obj['metricHeaders'] = ApiClient.convertToType(data['metricHeaders'], [MetricHeader]);
            }
            if (data.hasOwnProperty('minimums')) {
                obj['minimums'] = ApiClient.convertToType(data['minimums'], [Row]);
            }
            if (data.hasOwnProperty('propertyQuota')) {
                obj['propertyQuota'] = PropertyQuota.constructFromObject(data['propertyQuota']);
            }
            if (data.hasOwnProperty('rowCount')) {
                obj['rowCount'] = ApiClient.convertToType(data['rowCount'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Row]);
            }
            if (data.hasOwnProperty('totals')) {
                obj['totals'] = ApiClient.convertToType(data['totals'], [Row]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RunReportResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RunReportResponse</code>.
     */
    static validateJSON(data) {
        if (data['dimensionHeaders']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensionHeaders'])) {
                throw new Error("Expected the field `dimensionHeaders` to be an array in the JSON data but got " + data['dimensionHeaders']);
            }
            // validate the optional field `dimensionHeaders` (array)
            for (const item of data['dimensionHeaders']) {
                DimensionHeader.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        if (data['maximums']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['maximums'])) {
                throw new Error("Expected the field `maximums` to be an array in the JSON data but got " + data['maximums']);
            }
            // validate the optional field `maximums` (array)
            for (const item of data['maximums']) {
                Row.validateJSON(item);
            };
        }
        // validate the optional field `metadata`
        if (data['metadata']) { // data not null
          ResponseMetaData.validateJSON(data['metadata']);
        }
        if (data['metricHeaders']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metricHeaders'])) {
                throw new Error("Expected the field `metricHeaders` to be an array in the JSON data but got " + data['metricHeaders']);
            }
            // validate the optional field `metricHeaders` (array)
            for (const item of data['metricHeaders']) {
                MetricHeader.validateJSON(item);
            };
        }
        if (data['minimums']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['minimums'])) {
                throw new Error("Expected the field `minimums` to be an array in the JSON data but got " + data['minimums']);
            }
            // validate the optional field `minimums` (array)
            for (const item of data['minimums']) {
                Row.validateJSON(item);
            };
        }
        // validate the optional field `propertyQuota`
        if (data['propertyQuota']) { // data not null
          PropertyQuota.validateJSON(data['propertyQuota']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Row.validateJSON(item);
            };
        }
        if (data['totals']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['totals'])) {
                throw new Error("Expected the field `totals` to be an array in the JSON data but got " + data['totals']);
            }
            // validate the optional field `totals` (array)
            for (const item of data['totals']) {
                Row.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Describes dimension columns. The number of DimensionHeaders and ordering of DimensionHeaders matches the dimensions present in rows.
 * @member {Array.<module:model/DimensionHeader>} dimensionHeaders
 */
RunReportResponse.prototype['dimensionHeaders'] = undefined;

/**
 * Identifies what kind of resource this message is. This `kind` is always the fixed string \"analyticsData#runReport\". Useful to distinguish between response types in JSON.
 * @member {String} kind
 */
RunReportResponse.prototype['kind'] = undefined;

/**
 * If requested, the maximum values of metrics.
 * @member {Array.<module:model/Row>} maximums
 */
RunReportResponse.prototype['maximums'] = undefined;

/**
 * @member {module:model/ResponseMetaData} metadata
 */
RunReportResponse.prototype['metadata'] = undefined;

/**
 * Describes metric columns. The number of MetricHeaders and ordering of MetricHeaders matches the metrics present in rows.
 * @member {Array.<module:model/MetricHeader>} metricHeaders
 */
RunReportResponse.prototype['metricHeaders'] = undefined;

/**
 * If requested, the minimum values of metrics.
 * @member {Array.<module:model/Row>} minimums
 */
RunReportResponse.prototype['minimums'] = undefined;

/**
 * @member {module:model/PropertyQuota} propertyQuota
 */
RunReportResponse.prototype['propertyQuota'] = undefined;

/**
 * The total number of rows in the query result. `rowCount` is independent of the number of rows returned in the response, the `limit` request parameter, and the `offset` request parameter. For example if a query returns 175 rows and includes `limit` of 50 in the API request, the response will contain `rowCount` of 175 but only 50 rows. To learn more about this pagination parameter, see [Pagination](https://developers.google.com/analytics/devguides/reporting/data/v1/basics#pagination).
 * @member {Number} rowCount
 */
RunReportResponse.prototype['rowCount'] = undefined;

/**
 * Rows of dimension value combinations and metric values in the report.
 * @member {Array.<module:model/Row>} rows
 */
RunReportResponse.prototype['rows'] = undefined;

/**
 * If requested, the totaled values of metrics.
 * @member {Array.<module:model/Row>} totals
 */
RunReportResponse.prototype['totals'] = undefined;






export default RunReportResponse;

