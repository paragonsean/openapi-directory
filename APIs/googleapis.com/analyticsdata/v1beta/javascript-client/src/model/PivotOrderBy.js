/**
 * Google Analytics Data API
 * Accesses report data in Google Analytics. Warning: Creating multiple Customer Applications, Accounts, or Projects to simulate or act as a single Customer Application, Account, or Project (respectively) or to circumvent Service-specific usage limits or quotas is a direct violation of Google Cloud Platform Terms of Service as well as Google APIs Terms of Service. These actions can result in immediate termination of your GCP project(s) without any warning. 
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PivotSelection from './PivotSelection';

/**
 * The PivotOrderBy model module.
 * @module model/PivotOrderBy
 * @version v1beta
 */
class PivotOrderBy {
    /**
     * Constructs a new <code>PivotOrderBy</code>.
     * Sorts by a pivot column group.
     * @alias module:model/PivotOrderBy
     */
    constructor() { 
        
        PivotOrderBy.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PivotOrderBy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PivotOrderBy} obj Optional instance to populate.
     * @return {module:model/PivotOrderBy} The populated <code>PivotOrderBy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PivotOrderBy();

            if (data.hasOwnProperty('metricName')) {
                obj['metricName'] = ApiClient.convertToType(data['metricName'], 'String');
            }
            if (data.hasOwnProperty('pivotSelections')) {
                obj['pivotSelections'] = ApiClient.convertToType(data['pivotSelections'], [PivotSelection]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PivotOrderBy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PivotOrderBy</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['metricName'] && !(typeof data['metricName'] === 'string' || data['metricName'] instanceof String)) {
            throw new Error("Expected the field `metricName` to be a primitive type in the JSON string but got " + data['metricName']);
        }
        if (data['pivotSelections']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pivotSelections'])) {
                throw new Error("Expected the field `pivotSelections` to be an array in the JSON data but got " + data['pivotSelections']);
            }
            // validate the optional field `pivotSelections` (array)
            for (const item of data['pivotSelections']) {
                PivotSelection.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * In the response to order by, order rows by this column. Must be a metric name from the request.
 * @member {String} metricName
 */
PivotOrderBy.prototype['metricName'] = undefined;

/**
 * Used to select a dimension name and value pivot. If multiple pivot selections are given, the sort occurs on rows where all pivot selection dimension name and value pairs match the row's dimension name and value pair.
 * @member {Array.<module:model/PivotSelection>} pivotSelections
 */
PivotOrderBy.prototype['pivotSelections'] = undefined;






export default PivotOrderBy;

