/**
 * Google Forms API
 * Reads and writes Google Forms and responses.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Option from './Option';

/**
 * The ChoiceQuestion model module.
 * @module model/ChoiceQuestion
 * @version v1
 */
class ChoiceQuestion {
    /**
     * Constructs a new <code>ChoiceQuestion</code>.
     * A radio/checkbox/dropdown question.
     * @alias module:model/ChoiceQuestion
     */
    constructor() { 
        
        ChoiceQuestion.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ChoiceQuestion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChoiceQuestion} obj Optional instance to populate.
     * @return {module:model/ChoiceQuestion} The populated <code>ChoiceQuestion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChoiceQuestion();

            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], [Option]);
            }
            if (data.hasOwnProperty('shuffle')) {
                obj['shuffle'] = ApiClient.convertToType(data['shuffle'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChoiceQuestion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChoiceQuestion</code>.
     */
    static validateJSON(data) {
        if (data['options']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['options'])) {
                throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
            }
            // validate the optional field `options` (array)
            for (const item of data['options']) {
                Option.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * Required. List of options that a respondent must choose from.
 * @member {Array.<module:model/Option>} options
 */
ChoiceQuestion.prototype['options'] = undefined;

/**
 * Whether the options should be displayed in random order for different instances of the quiz. This is often used to prevent cheating by respondents who might be looking at another respondent's screen, or to address bias in a survey that might be introduced by always putting the same options first or last.
 * @member {Boolean} shuffle
 */
ChoiceQuestion.prototype['shuffle'] = undefined;

/**
 * Required. The type of choice question.
 * @member {module:model/ChoiceQuestion.TypeEnum} type
 */
ChoiceQuestion.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ChoiceQuestion['TypeEnum'] = {

    /**
     * value: "CHOICE_TYPE_UNSPECIFIED"
     * @const
     */
    "CHOICE_TYPE_UNSPECIFIED": "CHOICE_TYPE_UNSPECIFIED",

    /**
     * value: "RADIO"
     * @const
     */
    "RADIO": "RADIO",

    /**
     * value: "CHECKBOX"
     * @const
     */
    "CHECKBOX": "CHECKBOX",

    /**
     * value: "DROP_DOWN"
     * @const
     */
    "DROP_DOWN": "DROP_DOWN"
};



export default ChoiceQuestion;

