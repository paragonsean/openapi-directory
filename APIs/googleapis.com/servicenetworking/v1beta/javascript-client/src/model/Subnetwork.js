/**
 * Service Networking API
 * Provides automatic management of network configurations necessary for certain services.
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SecondaryIpRange from './SecondaryIpRange';

/**
 * The Subnetwork model module.
 * @module model/Subnetwork
 * @version v1beta
 */
class Subnetwork {
    /**
     * Constructs a new <code>Subnetwork</code>.
     * Represents a subnet that was created or discovered by a private access management service.
     * @alias module:model/Subnetwork
     */
    constructor() { 
        
        Subnetwork.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Subnetwork</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Subnetwork} obj Optional instance to populate.
     * @return {module:model/Subnetwork} The populated <code>Subnetwork</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Subnetwork();

            if (data.hasOwnProperty('ipCidrRange')) {
                obj['ipCidrRange'] = ApiClient.convertToType(data['ipCidrRange'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('network')) {
                obj['network'] = ApiClient.convertToType(data['network'], 'String');
            }
            if (data.hasOwnProperty('outsideAllocation')) {
                obj['outsideAllocation'] = ApiClient.convertToType(data['outsideAllocation'], 'Boolean');
            }
            if (data.hasOwnProperty('region')) {
                obj['region'] = ApiClient.convertToType(data['region'], 'String');
            }
            if (data.hasOwnProperty('secondaryIpRanges')) {
                obj['secondaryIpRanges'] = ApiClient.convertToType(data['secondaryIpRanges'], [SecondaryIpRange]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Subnetwork</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Subnetwork</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['ipCidrRange'] && !(typeof data['ipCidrRange'] === 'string' || data['ipCidrRange'] instanceof String)) {
            throw new Error("Expected the field `ipCidrRange` to be a primitive type in the JSON string but got " + data['ipCidrRange']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['network'] && !(typeof data['network'] === 'string' || data['network'] instanceof String)) {
            throw new Error("Expected the field `network` to be a primitive type in the JSON string but got " + data['network']);
        }
        // ensure the json data is a string
        if (data['region'] && !(typeof data['region'] === 'string' || data['region'] instanceof String)) {
            throw new Error("Expected the field `region` to be a primitive type in the JSON string but got " + data['region']);
        }
        if (data['secondaryIpRanges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['secondaryIpRanges'])) {
                throw new Error("Expected the field `secondaryIpRanges` to be an array in the JSON data but got " + data['secondaryIpRanges']);
            }
            // validate the optional field `secondaryIpRanges` (array)
            for (const item of data['secondaryIpRanges']) {
                SecondaryIpRange.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Subnetwork CIDR range in `10.x.x.x/y` format.
 * @member {String} ipCidrRange
 */
Subnetwork.prototype['ipCidrRange'] = undefined;

/**
 * Subnetwork name. See https://cloud.google.com/compute/docs/vpc/
 * @member {String} name
 */
Subnetwork.prototype['name'] = undefined;

/**
 * In the Shared VPC host project, the VPC network that's peered with the consumer network. For example: `projects/1234321/global/networks/host-network`
 * @member {String} network
 */
Subnetwork.prototype['network'] = undefined;

/**
 * This is a discovered subnet that is not within the current consumer allocated ranges.
 * @member {Boolean} outsideAllocation
 */
Subnetwork.prototype['outsideAllocation'] = undefined;

/**
 * GCP region where the subnetwork is located.
 * @member {String} region
 */
Subnetwork.prototype['region'] = undefined;

/**
 * List of secondary IP ranges in this subnetwork.
 * @member {Array.<module:model/SecondaryIpRange>} secondaryIpRanges
 */
Subnetwork.prototype['secondaryIpRanges'] = undefined;






export default Subnetwork;

