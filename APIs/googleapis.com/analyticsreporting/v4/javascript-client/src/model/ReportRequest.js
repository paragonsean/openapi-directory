/**
 * Analytics Reporting API
 * Accesses Analytics report data.
 *
 * The version of the OpenAPI document: v4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CohortGroup from './CohortGroup';
import DateRange from './DateRange';
import Dimension from './Dimension';
import DimensionFilterClause from './DimensionFilterClause';
import Metric from './Metric';
import MetricFilterClause from './MetricFilterClause';
import OrderBy from './OrderBy';
import Pivot from './Pivot';
import Segment from './Segment';

/**
 * The ReportRequest model module.
 * @module model/ReportRequest
 * @version v4
 */
class ReportRequest {
    /**
     * Constructs a new <code>ReportRequest</code>.
     * The main request class which specifies the Reporting API request.
     * @alias module:model/ReportRequest
     */
    constructor() { 
        
        ReportRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReportRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportRequest} obj Optional instance to populate.
     * @return {module:model/ReportRequest} The populated <code>ReportRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportRequest();

            if (data.hasOwnProperty('cohortGroup')) {
                obj['cohortGroup'] = CohortGroup.constructFromObject(data['cohortGroup']);
            }
            if (data.hasOwnProperty('dateRanges')) {
                obj['dateRanges'] = ApiClient.convertToType(data['dateRanges'], [DateRange]);
            }
            if (data.hasOwnProperty('dimensionFilterClauses')) {
                obj['dimensionFilterClauses'] = ApiClient.convertToType(data['dimensionFilterClauses'], [DimensionFilterClause]);
            }
            if (data.hasOwnProperty('dimensions')) {
                obj['dimensions'] = ApiClient.convertToType(data['dimensions'], [Dimension]);
            }
            if (data.hasOwnProperty('filtersExpression')) {
                obj['filtersExpression'] = ApiClient.convertToType(data['filtersExpression'], 'String');
            }
            if (data.hasOwnProperty('hideTotals')) {
                obj['hideTotals'] = ApiClient.convertToType(data['hideTotals'], 'Boolean');
            }
            if (data.hasOwnProperty('hideValueRanges')) {
                obj['hideValueRanges'] = ApiClient.convertToType(data['hideValueRanges'], 'Boolean');
            }
            if (data.hasOwnProperty('includeEmptyRows')) {
                obj['includeEmptyRows'] = ApiClient.convertToType(data['includeEmptyRows'], 'Boolean');
            }
            if (data.hasOwnProperty('metricFilterClauses')) {
                obj['metricFilterClauses'] = ApiClient.convertToType(data['metricFilterClauses'], [MetricFilterClause]);
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [Metric]);
            }
            if (data.hasOwnProperty('orderBys')) {
                obj['orderBys'] = ApiClient.convertToType(data['orderBys'], [OrderBy]);
            }
            if (data.hasOwnProperty('pageSize')) {
                obj['pageSize'] = ApiClient.convertToType(data['pageSize'], 'Number');
            }
            if (data.hasOwnProperty('pageToken')) {
                obj['pageToken'] = ApiClient.convertToType(data['pageToken'], 'String');
            }
            if (data.hasOwnProperty('pivots')) {
                obj['pivots'] = ApiClient.convertToType(data['pivots'], [Pivot]);
            }
            if (data.hasOwnProperty('samplingLevel')) {
                obj['samplingLevel'] = ApiClient.convertToType(data['samplingLevel'], 'String');
            }
            if (data.hasOwnProperty('segments')) {
                obj['segments'] = ApiClient.convertToType(data['segments'], [Segment]);
            }
            if (data.hasOwnProperty('viewId')) {
                obj['viewId'] = ApiClient.convertToType(data['viewId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportRequest</code>.
     */
    static validateJSON(data) {
        // validate the optional field `cohortGroup`
        if (data['cohortGroup']) { // data not null
          CohortGroup.validateJSON(data['cohortGroup']);
        }
        if (data['dateRanges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dateRanges'])) {
                throw new Error("Expected the field `dateRanges` to be an array in the JSON data but got " + data['dateRanges']);
            }
            // validate the optional field `dateRanges` (array)
            for (const item of data['dateRanges']) {
                DateRange.validateJSON(item);
            };
        }
        if (data['dimensionFilterClauses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensionFilterClauses'])) {
                throw new Error("Expected the field `dimensionFilterClauses` to be an array in the JSON data but got " + data['dimensionFilterClauses']);
            }
            // validate the optional field `dimensionFilterClauses` (array)
            for (const item of data['dimensionFilterClauses']) {
                DimensionFilterClause.validateJSON(item);
            };
        }
        if (data['dimensions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensions'])) {
                throw new Error("Expected the field `dimensions` to be an array in the JSON data but got " + data['dimensions']);
            }
            // validate the optional field `dimensions` (array)
            for (const item of data['dimensions']) {
                Dimension.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['filtersExpression'] && !(typeof data['filtersExpression'] === 'string' || data['filtersExpression'] instanceof String)) {
            throw new Error("Expected the field `filtersExpression` to be a primitive type in the JSON string but got " + data['filtersExpression']);
        }
        if (data['metricFilterClauses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metricFilterClauses'])) {
                throw new Error("Expected the field `metricFilterClauses` to be an array in the JSON data but got " + data['metricFilterClauses']);
            }
            // validate the optional field `metricFilterClauses` (array)
            for (const item of data['metricFilterClauses']) {
                MetricFilterClause.validateJSON(item);
            };
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                Metric.validateJSON(item);
            };
        }
        if (data['orderBys']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['orderBys'])) {
                throw new Error("Expected the field `orderBys` to be an array in the JSON data but got " + data['orderBys']);
            }
            // validate the optional field `orderBys` (array)
            for (const item of data['orderBys']) {
                OrderBy.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['pageToken'] && !(typeof data['pageToken'] === 'string' || data['pageToken'] instanceof String)) {
            throw new Error("Expected the field `pageToken` to be a primitive type in the JSON string but got " + data['pageToken']);
        }
        if (data['pivots']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pivots'])) {
                throw new Error("Expected the field `pivots` to be an array in the JSON data but got " + data['pivots']);
            }
            // validate the optional field `pivots` (array)
            for (const item of data['pivots']) {
                Pivot.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['samplingLevel'] && !(typeof data['samplingLevel'] === 'string' || data['samplingLevel'] instanceof String)) {
            throw new Error("Expected the field `samplingLevel` to be a primitive type in the JSON string but got " + data['samplingLevel']);
        }
        if (data['segments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['segments'])) {
                throw new Error("Expected the field `segments` to be an array in the JSON data but got " + data['segments']);
            }
            // validate the optional field `segments` (array)
            for (const item of data['segments']) {
                Segment.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['viewId'] && !(typeof data['viewId'] === 'string' || data['viewId'] instanceof String)) {
            throw new Error("Expected the field `viewId` to be a primitive type in the JSON string but got " + data['viewId']);
        }

        return true;
    }


}



/**
 * @member {module:model/CohortGroup} cohortGroup
 */
ReportRequest.prototype['cohortGroup'] = undefined;

/**
 * Date ranges in the request. The request can have a maximum of 2 date ranges. The response will contain a set of metric values for each combination of the dimensions for each date range in the request. So, if there are two date ranges, there will be two set of metric values, one for the original date range and one for the second date range. The `reportRequest.dateRanges` field should not be specified for cohorts or Lifetime value requests. If a date range is not provided, the default date range is (startDate: current date - 7 days, endDate: current date - 1 day). Every [ReportRequest](#ReportRequest) within a `batchGet` method must contain the same `dateRanges` definition.
 * @member {Array.<module:model/DateRange>} dateRanges
 */
ReportRequest.prototype['dateRanges'] = undefined;

/**
 * The dimension filter clauses for filtering Dimension Values. They are logically combined with the `AND` operator. Note that filtering occurs before any dimensions are aggregated, so that the returned metrics represent the total for only the relevant dimensions.
 * @member {Array.<module:model/DimensionFilterClause>} dimensionFilterClauses
 */
ReportRequest.prototype['dimensionFilterClauses'] = undefined;

/**
 * The dimensions requested. Requests can have a total of 9 dimensions.
 * @member {Array.<module:model/Dimension>} dimensions
 */
ReportRequest.prototype['dimensions'] = undefined;

/**
 * Dimension or metric filters that restrict the data returned for your request. To use the `filtersExpression`, supply a dimension or metric on which to filter, followed by the filter expression. For example, the following expression selects `ga:browser` dimension which starts with Firefox; `ga:browser=~^Firefox`. For more information on dimensions and metric filters, see [Filters reference](https://developers.google.com/analytics/devguides/reporting/core/v3/reference#filters).
 * @member {String} filtersExpression
 */
ReportRequest.prototype['filtersExpression'] = undefined;

/**
 * If set to true, hides the total of all metrics for all the matching rows, for every date range. The default false and will return the totals.
 * @member {Boolean} hideTotals
 */
ReportRequest.prototype['hideTotals'] = undefined;

/**
 * If set to true, hides the minimum and maximum across all matching rows. The default is false and the value ranges are returned.
 * @member {Boolean} hideValueRanges
 */
ReportRequest.prototype['hideValueRanges'] = undefined;

/**
 * If set to false, the response does not include rows if all the retrieved metrics are equal to zero. The default is false which will exclude these rows.
 * @member {Boolean} includeEmptyRows
 */
ReportRequest.prototype['includeEmptyRows'] = undefined;

/**
 * The metric filter clauses. They are logically combined with the `AND` operator. Metric filters look at only the first date range and not the comparing date range. Note that filtering on metrics occurs after the metrics are aggregated.
 * @member {Array.<module:model/MetricFilterClause>} metricFilterClauses
 */
ReportRequest.prototype['metricFilterClauses'] = undefined;

/**
 * The metrics requested. Requests must specify at least one metric. Requests can have a total of 10 metrics.
 * @member {Array.<module:model/Metric>} metrics
 */
ReportRequest.prototype['metrics'] = undefined;

/**
 * Sort order on output rows. To compare two rows, the elements of the following are applied in order until a difference is found. All date ranges in the output get the same row order.
 * @member {Array.<module:model/OrderBy>} orderBys
 */
ReportRequest.prototype['orderBys'] = undefined;

/**
 * Page size is for paging and specifies the maximum number of returned rows. Page size should be >= 0. A query returns the default of 1,000 rows. The Analytics Core Reporting API returns a maximum of 100,000 rows per request, no matter how many you ask for. It can also return fewer rows than requested, if there aren't as many dimension segments as you expect. For instance, there are fewer than 300 possible values for `ga:country`, so when segmenting only by country, you can't get more than 300 rows, even if you set `pageSize` to a higher value.
 * @member {Number} pageSize
 */
ReportRequest.prototype['pageSize'] = undefined;

/**
 * A continuation token to get the next page of the results. Adding this to the request will return the rows after the pageToken. The pageToken should be the value returned in the nextPageToken parameter in the response to the GetReports request.
 * @member {String} pageToken
 */
ReportRequest.prototype['pageToken'] = undefined;

/**
 * The pivot definitions. Requests can have a maximum of 2 pivots.
 * @member {Array.<module:model/Pivot>} pivots
 */
ReportRequest.prototype['pivots'] = undefined;

/**
 * The desired report [sample](https://support.google.com/analytics/answer/2637192) size. If the the `samplingLevel` field is unspecified the `DEFAULT` sampling level is used. Every [ReportRequest](#ReportRequest) within a `batchGet` method must contain the same `samplingLevel` definition. See [developer guide](/analytics/devguides/reporting/core/v4/basics#sampling) for details.
 * @member {module:model/ReportRequest.SamplingLevelEnum} samplingLevel
 */
ReportRequest.prototype['samplingLevel'] = undefined;

/**
 * Segment the data returned for the request. A segment definition helps look at a subset of the segment request. A request can contain up to four segments. Every [ReportRequest](#ReportRequest) within a `batchGet` method must contain the same `segments` definition. Requests with segments must have the `ga:segment` dimension.
 * @member {Array.<module:model/Segment>} segments
 */
ReportRequest.prototype['segments'] = undefined;

/**
 * The Analytics [view ID](https://support.google.com/analytics/answer/1009618) from which to retrieve data. Every [ReportRequest](#ReportRequest) within a `batchGet` method must contain the same `viewId`.
 * @member {String} viewId
 */
ReportRequest.prototype['viewId'] = undefined;





/**
 * Allowed values for the <code>samplingLevel</code> property.
 * @enum {String}
 * @readonly
 */
ReportRequest['SamplingLevelEnum'] = {

    /**
     * value: "SAMPLING_UNSPECIFIED"
     * @const
     */
    "SAMPLING_UNSPECIFIED": "SAMPLING_UNSPECIFIED",

    /**
     * value: "DEFAULT"
     * @const
     */
    "DEFAULT": "DEFAULT",

    /**
     * value: "SMALL"
     * @const
     */
    "SMALL": "SMALL",

    /**
     * value: "LARGE"
     * @const
     */
    "LARGE": "LARGE"
};



export default ReportRequest;

