/**
 * BigQuery Reservation API
 * A service to modify your BigQuery flat-rate reservations.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Autoscale from './Autoscale';

/**
 * The Reservation model module.
 * @module model/Reservation
 * @version v1
 */
class Reservation {
    /**
     * Constructs a new <code>Reservation</code>.
     * A reservation is a mechanism used to guarantee slots to users.
     * @alias module:model/Reservation
     */
    constructor() { 
        
        Reservation.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Reservation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Reservation} obj Optional instance to populate.
     * @return {module:model/Reservation} The populated <code>Reservation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Reservation();

            if (data.hasOwnProperty('autoscale')) {
                obj['autoscale'] = Autoscale.constructFromObject(data['autoscale']);
            }
            if (data.hasOwnProperty('concurrency')) {
                obj['concurrency'] = ApiClient.convertToType(data['concurrency'], 'String');
            }
            if (data.hasOwnProperty('creationTime')) {
                obj['creationTime'] = ApiClient.convertToType(data['creationTime'], 'String');
            }
            if (data.hasOwnProperty('edition')) {
                obj['edition'] = ApiClient.convertToType(data['edition'], 'String');
            }
            if (data.hasOwnProperty('ignoreIdleSlots')) {
                obj['ignoreIdleSlots'] = ApiClient.convertToType(data['ignoreIdleSlots'], 'Boolean');
            }
            if (data.hasOwnProperty('multiRegionAuxiliary')) {
                obj['multiRegionAuxiliary'] = ApiClient.convertToType(data['multiRegionAuxiliary'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('slotCapacity')) {
                obj['slotCapacity'] = ApiClient.convertToType(data['slotCapacity'], 'String');
            }
            if (data.hasOwnProperty('updateTime')) {
                obj['updateTime'] = ApiClient.convertToType(data['updateTime'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Reservation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Reservation</code>.
     */
    static validateJSON(data) {
        // validate the optional field `autoscale`
        if (data['autoscale']) { // data not null
          Autoscale.validateJSON(data['autoscale']);
        }
        // ensure the json data is a string
        if (data['concurrency'] && !(typeof data['concurrency'] === 'string' || data['concurrency'] instanceof String)) {
            throw new Error("Expected the field `concurrency` to be a primitive type in the JSON string but got " + data['concurrency']);
        }
        // ensure the json data is a string
        if (data['creationTime'] && !(typeof data['creationTime'] === 'string' || data['creationTime'] instanceof String)) {
            throw new Error("Expected the field `creationTime` to be a primitive type in the JSON string but got " + data['creationTime']);
        }
        // ensure the json data is a string
        if (data['edition'] && !(typeof data['edition'] === 'string' || data['edition'] instanceof String)) {
            throw new Error("Expected the field `edition` to be a primitive type in the JSON string but got " + data['edition']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['slotCapacity'] && !(typeof data['slotCapacity'] === 'string' || data['slotCapacity'] instanceof String)) {
            throw new Error("Expected the field `slotCapacity` to be a primitive type in the JSON string but got " + data['slotCapacity']);
        }
        // ensure the json data is a string
        if (data['updateTime'] && !(typeof data['updateTime'] === 'string' || data['updateTime'] instanceof String)) {
            throw new Error("Expected the field `updateTime` to be a primitive type in the JSON string but got " + data['updateTime']);
        }

        return true;
    }


}



/**
 * @member {module:model/Autoscale} autoscale
 */
Reservation.prototype['autoscale'] = undefined;

/**
 * Job concurrency target which sets a soft upper bound on the number of jobs that can run concurrently in this reservation. This is a soft target due to asynchronous nature of the system and various optimizations for small queries. Default value is 0 which means that concurrency target will be automatically computed by the system. NOTE: this field is exposed as `target_job_concurrency` in the Information Schema, DDL and BQ CLI.
 * @member {String} concurrency
 */
Reservation.prototype['concurrency'] = undefined;

/**
 * Output only. Creation time of the reservation.
 * @member {String} creationTime
 */
Reservation.prototype['creationTime'] = undefined;

/**
 * Edition of the reservation.
 * @member {module:model/Reservation.EditionEnum} edition
 */
Reservation.prototype['edition'] = undefined;

/**
 * If false, any query or pipeline job using this reservation will use idle slots from other reservations within the same admin project. If true, a query or pipeline job using this reservation will execute with the slot capacity specified in the slot_capacity field at most.
 * @member {Boolean} ignoreIdleSlots
 */
Reservation.prototype['ignoreIdleSlots'] = undefined;

/**
 * Applicable only for reservations located within one of the BigQuery multi-regions (US or EU). If set to true, this reservation is placed in the organization's secondary region which is designated for disaster recovery purposes. If false, this reservation is placed in the organization's default region. NOTE: this is a preview feature. Project must be allow-listed in order to set this field.
 * @member {Boolean} multiRegionAuxiliary
 */
Reservation.prototype['multiRegionAuxiliary'] = undefined;

/**
 * The resource name of the reservation, e.g., `projects/_*_/locations/_*_/reservations/team1-prod`. The reservation_id must only contain lower case alphanumeric characters or dashes. It must start with a letter and must not end with a dash. Its maximum length is 64 characters.
 * @member {String} name
 */
Reservation.prototype['name'] = undefined;

/**
 * Baseline slots available to this reservation. A slot is a unit of computational power in BigQuery, and serves as the unit of parallelism. Queries using this reservation might use more slots during runtime if ignore_idle_slots is set to false, or autoscaling is enabled. If edition is EDITION_UNSPECIFIED and total slot_capacity of the reservation and its siblings exceeds the total slot_count of all capacity commitments, the request will fail with `google.rpc.Code.RESOURCE_EXHAUSTED`. If edition is any value but EDITION_UNSPECIFIED, then the above requirement is not needed. The total slot_capacity of the reservation and its siblings may exceed the total slot_count of capacity commitments. In that case, the exceeding slots will be charged with the autoscale SKU. You can increase the number of baseline slots in a reservation every few minutes. If you want to decrease your baseline slots, you are limited to once an hour if you have recently changed your baseline slot capacity and your baseline slots exceed your committed slots. Otherwise, you can decrease your baseline slots every few minutes.
 * @member {String} slotCapacity
 */
Reservation.prototype['slotCapacity'] = undefined;

/**
 * Output only. Last update time of the reservation.
 * @member {String} updateTime
 */
Reservation.prototype['updateTime'] = undefined;





/**
 * Allowed values for the <code>edition</code> property.
 * @enum {String}
 * @readonly
 */
Reservation['EditionEnum'] = {

    /**
     * value: "EDITION_UNSPECIFIED"
     * @const
     */
    "EDITION_UNSPECIFIED": "EDITION_UNSPECIFIED",

    /**
     * value: "STANDARD"
     * @const
     */
    "STANDARD": "STANDARD",

    /**
     * value: "ENTERPRISE"
     * @const
     */
    "ENTERPRISE": "ENTERPRISE",

    /**
     * value: "ENTERPRISE_PLUS"
     * @const
     */
    "ENTERPRISE_PLUS": "ENTERPRISE_PLUS"
};



export default Reservation;

