/**
 * Dialogflow API
 * Builds conversational interfaces (for example, chatbots, and voice-powered apps and devices).
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GoogleCloudDialogflowV2IntentTrainingPhrasePart from './GoogleCloudDialogflowV2IntentTrainingPhrasePart';

/**
 * The GoogleCloudDialogflowV2IntentTrainingPhrase model module.
 * @module model/GoogleCloudDialogflowV2IntentTrainingPhrase
 * @version v2
 */
class GoogleCloudDialogflowV2IntentTrainingPhrase {
    /**
     * Constructs a new <code>GoogleCloudDialogflowV2IntentTrainingPhrase</code>.
     * Represents an example that the agent is trained on.
     * @alias module:model/GoogleCloudDialogflowV2IntentTrainingPhrase
     */
    constructor() { 
        
        GoogleCloudDialogflowV2IntentTrainingPhrase.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoogleCloudDialogflowV2IntentTrainingPhrase</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoogleCloudDialogflowV2IntentTrainingPhrase} obj Optional instance to populate.
     * @return {module:model/GoogleCloudDialogflowV2IntentTrainingPhrase} The populated <code>GoogleCloudDialogflowV2IntentTrainingPhrase</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoogleCloudDialogflowV2IntentTrainingPhrase();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('parts')) {
                obj['parts'] = ApiClient.convertToType(data['parts'], [GoogleCloudDialogflowV2IntentTrainingPhrasePart]);
            }
            if (data.hasOwnProperty('timesAddedCount')) {
                obj['timesAddedCount'] = ApiClient.convertToType(data['timesAddedCount'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoogleCloudDialogflowV2IntentTrainingPhrase</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoogleCloudDialogflowV2IntentTrainingPhrase</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['parts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['parts'])) {
                throw new Error("Expected the field `parts` to be an array in the JSON data but got " + data['parts']);
            }
            // validate the optional field `parts` (array)
            for (const item of data['parts']) {
                GoogleCloudDialogflowV2IntentTrainingPhrasePart.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * Output only. The unique identifier of this training phrase.
 * @member {String} name
 */
GoogleCloudDialogflowV2IntentTrainingPhrase.prototype['name'] = undefined;

/**
 * Required. The ordered list of training phrase parts. The parts are concatenated in order to form the training phrase. Note: The API does not automatically annotate training phrases like the Dialogflow Console does. Note: Do not forget to include whitespace at part boundaries, so the training phrase is well formatted when the parts are concatenated. If the training phrase does not need to be annotated with parameters, you just need a single part with only the Part.text field set. If you want to annotate the training phrase, you must create multiple parts, where the fields of each part are populated in one of two ways: - `Part.text` is set to a part of the phrase that has no parameters. - `Part.text` is set to a part of the phrase that you want to annotate, and the `entity_type`, `alias`, and `user_defined` fields are all set.
 * @member {Array.<module:model/GoogleCloudDialogflowV2IntentTrainingPhrasePart>} parts
 */
GoogleCloudDialogflowV2IntentTrainingPhrase.prototype['parts'] = undefined;

/**
 * Optional. Indicates how many times this example was added to the intent. Each time a developer adds an existing sample by editing an intent or training, this counter is increased.
 * @member {Number} timesAddedCount
 */
GoogleCloudDialogflowV2IntentTrainingPhrase.prototype['timesAddedCount'] = undefined;

/**
 * Required. The type of the training phrase.
 * @member {module:model/GoogleCloudDialogflowV2IntentTrainingPhrase.TypeEnum} type
 */
GoogleCloudDialogflowV2IntentTrainingPhrase.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
GoogleCloudDialogflowV2IntentTrainingPhrase['TypeEnum'] = {

    /**
     * value: "TYPE_UNSPECIFIED"
     * @const
     */
    "TYPE_UNSPECIFIED": "TYPE_UNSPECIFIED",

    /**
     * value: "EXAMPLE"
     * @const
     */
    "EXAMPLE": "EXAMPLE",

    /**
     * value: "TEMPLATE"
     * @const
     */
    "TEMPLATE": "TEMPLATE"
};



export default GoogleCloudDialogflowV2IntentTrainingPhrase;

