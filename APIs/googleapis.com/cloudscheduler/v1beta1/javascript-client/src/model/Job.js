/**
 * Cloud Scheduler API
 * Creates and manages jobs run on a regular recurring schedule.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AppEngineHttpTarget from './AppEngineHttpTarget';
import HttpTarget from './HttpTarget';
import PubsubTarget from './PubsubTarget';
import RetryConfig from './RetryConfig';
import Status from './Status';

/**
 * The Job model module.
 * @module model/Job
 * @version v1beta1
 */
class Job {
    /**
     * Constructs a new <code>Job</code>.
     * Configuration for a job. The maximum allowed size for a job is 1MB.
     * @alias module:model/Job
     */
    constructor() { 
        
        Job.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Job</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Job} obj Optional instance to populate.
     * @return {module:model/Job} The populated <code>Job</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Job();

            if (data.hasOwnProperty('appEngineHttpTarget')) {
                obj['appEngineHttpTarget'] = AppEngineHttpTarget.constructFromObject(data['appEngineHttpTarget']);
            }
            if (data.hasOwnProperty('attemptDeadline')) {
                obj['attemptDeadline'] = ApiClient.convertToType(data['attemptDeadline'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('httpTarget')) {
                obj['httpTarget'] = HttpTarget.constructFromObject(data['httpTarget']);
            }
            if (data.hasOwnProperty('lastAttemptTime')) {
                obj['lastAttemptTime'] = ApiClient.convertToType(data['lastAttemptTime'], 'String');
            }
            if (data.hasOwnProperty('legacyAppEngineCron')) {
                obj['legacyAppEngineCron'] = ApiClient.convertToType(data['legacyAppEngineCron'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('pubsubTarget')) {
                obj['pubsubTarget'] = PubsubTarget.constructFromObject(data['pubsubTarget']);
            }
            if (data.hasOwnProperty('retryConfig')) {
                obj['retryConfig'] = RetryConfig.constructFromObject(data['retryConfig']);
            }
            if (data.hasOwnProperty('schedule')) {
                obj['schedule'] = ApiClient.convertToType(data['schedule'], 'String');
            }
            if (data.hasOwnProperty('scheduleTime')) {
                obj['scheduleTime'] = ApiClient.convertToType(data['scheduleTime'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = Status.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('timeZone')) {
                obj['timeZone'] = ApiClient.convertToType(data['timeZone'], 'String');
            }
            if (data.hasOwnProperty('userUpdateTime')) {
                obj['userUpdateTime'] = ApiClient.convertToType(data['userUpdateTime'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Job</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Job</code>.
     */
    static validateJSON(data) {
        // validate the optional field `appEngineHttpTarget`
        if (data['appEngineHttpTarget']) { // data not null
          AppEngineHttpTarget.validateJSON(data['appEngineHttpTarget']);
        }
        // ensure the json data is a string
        if (data['attemptDeadline'] && !(typeof data['attemptDeadline'] === 'string' || data['attemptDeadline'] instanceof String)) {
            throw new Error("Expected the field `attemptDeadline` to be a primitive type in the JSON string but got " + data['attemptDeadline']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // validate the optional field `httpTarget`
        if (data['httpTarget']) { // data not null
          HttpTarget.validateJSON(data['httpTarget']);
        }
        // ensure the json data is a string
        if (data['lastAttemptTime'] && !(typeof data['lastAttemptTime'] === 'string' || data['lastAttemptTime'] instanceof String)) {
            throw new Error("Expected the field `lastAttemptTime` to be a primitive type in the JSON string but got " + data['lastAttemptTime']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `pubsubTarget`
        if (data['pubsubTarget']) { // data not null
          PubsubTarget.validateJSON(data['pubsubTarget']);
        }
        // validate the optional field `retryConfig`
        if (data['retryConfig']) { // data not null
          RetryConfig.validateJSON(data['retryConfig']);
        }
        // ensure the json data is a string
        if (data['schedule'] && !(typeof data['schedule'] === 'string' || data['schedule'] instanceof String)) {
            throw new Error("Expected the field `schedule` to be a primitive type in the JSON string but got " + data['schedule']);
        }
        // ensure the json data is a string
        if (data['scheduleTime'] && !(typeof data['scheduleTime'] === 'string' || data['scheduleTime'] instanceof String)) {
            throw new Error("Expected the field `scheduleTime` to be a primitive type in the JSON string but got " + data['scheduleTime']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // validate the optional field `status`
        if (data['status']) { // data not null
          Status.validateJSON(data['status']);
        }
        // ensure the json data is a string
        if (data['timeZone'] && !(typeof data['timeZone'] === 'string' || data['timeZone'] instanceof String)) {
            throw new Error("Expected the field `timeZone` to be a primitive type in the JSON string but got " + data['timeZone']);
        }
        // ensure the json data is a string
        if (data['userUpdateTime'] && !(typeof data['userUpdateTime'] === 'string' || data['userUpdateTime'] instanceof String)) {
            throw new Error("Expected the field `userUpdateTime` to be a primitive type in the JSON string but got " + data['userUpdateTime']);
        }

        return true;
    }


}



/**
 * @member {module:model/AppEngineHttpTarget} appEngineHttpTarget
 */
Job.prototype['appEngineHttpTarget'] = undefined;

/**
 * The deadline for job attempts. If the request handler does not respond by this deadline then the request is cancelled and the attempt is marked as a `DEADLINE_EXCEEDED` failure. The failed attempt can be viewed in execution logs. Cloud Scheduler will retry the job according to the RetryConfig. The default and the allowed values depend on the type of target: * For HTTP targets, the default is 3 minutes. The deadline must be in the interval [15 seconds, 30 minutes]. * For App Engine HTTP targets, 0 indicates that the request has the default deadline. The default deadline depends on the scaling type of the service: 10 minutes for standard apps with automatic scaling, 24 hours for standard apps with manual and basic scaling, and 60 minutes for flex apps. If the request deadline is set, it must be in the interval [15 seconds, 24 hours 15 seconds]. * For Pub/Sub targets, this field is ignored.
 * @member {String} attemptDeadline
 */
Job.prototype['attemptDeadline'] = undefined;

/**
 * Optionally caller-specified in CreateJob or UpdateJob. A human-readable description for the job. This string must not contain more than 500 characters.
 * @member {String} description
 */
Job.prototype['description'] = undefined;

/**
 * @member {module:model/HttpTarget} httpTarget
 */
Job.prototype['httpTarget'] = undefined;

/**
 * Output only. The time the last job attempt started.
 * @member {String} lastAttemptTime
 */
Job.prototype['lastAttemptTime'] = undefined;

/**
 * Immutable. This field is used to manage the legacy App Engine Cron jobs using the Cloud Scheduler API. If the field is set to true, the job will be considered a legacy job. Note that App Engine Cron jobs have fewer features than Cloud Scheduler jobs, e.g., are only limited to App Engine targets.
 * @member {Boolean} legacyAppEngineCron
 */
Job.prototype['legacyAppEngineCron'] = undefined;

/**
 * Optionally caller-specified in CreateJob, after which it becomes output only. The job name. For example: `projects/PROJECT_ID/locations/LOCATION_ID/jobs/JOB_ID`. * `PROJECT_ID` can contain letters ([A-Za-z]), numbers ([0-9]), hyphens (-), colons (:), or periods (.). For more information, see [Identifying projects](https://cloud.google.com/resource-manager/docs/creating-managing-projects#identifying_projects) * `LOCATION_ID` is the canonical ID for the job's location. The list of available locations can be obtained by calling ListLocations. For more information, see https://cloud.google.com/about/locations/. * `JOB_ID` can contain only letters ([A-Za-z]), numbers ([0-9]), hyphens (-), or underscores (_). The maximum length is 500 characters.
 * @member {String} name
 */
Job.prototype['name'] = undefined;

/**
 * @member {module:model/PubsubTarget} pubsubTarget
 */
Job.prototype['pubsubTarget'] = undefined;

/**
 * @member {module:model/RetryConfig} retryConfig
 */
Job.prototype['retryConfig'] = undefined;

/**
 * Required, except when used with UpdateJob. Describes the schedule on which the job will be executed. The schedule can be either of the following types: * [Crontab](https://en.wikipedia.org/wiki/Cron#Overview) * English-like [schedule](https://cloud.google.com/scheduler/docs/configuring/cron-job-schedules) As a general rule, execution `n + 1` of a job will not begin until execution `n` has finished. Cloud Scheduler will never allow two simultaneously outstanding executions. For example, this implies that if the `n+1`th execution is scheduled to run at 16:00 but the `n`th execution takes until 16:15, the `n+1`th execution will not start until `16:15`. A scheduled start time will be delayed if the previous execution has not ended when its scheduled time occurs. If retry_count > 0 and a job attempt fails, the job will be tried a total of retry_count times, with exponential backoff, until the next scheduled start time. If retry_count is 0, a job attempt will not be retried if it fails. Instead the Cloud Scheduler system will wait for the next scheduled execution time. Setting retry_count to 0 does not prevent failed jobs from running according to schedule after the failure.
 * @member {String} schedule
 */
Job.prototype['schedule'] = undefined;

/**
 * Output only. The next time the job is scheduled. Note that this may be a retry of a previously failed attempt or the next execution time according to the schedule.
 * @member {String} scheduleTime
 */
Job.prototype['scheduleTime'] = undefined;

/**
 * Output only. State of the job.
 * @member {module:model/Job.StateEnum} state
 */
Job.prototype['state'] = undefined;

/**
 * @member {module:model/Status} status
 */
Job.prototype['status'] = undefined;

/**
 * Specifies the time zone to be used in interpreting schedule. The value of this field must be a time zone name from the [tz database](http://en.wikipedia.org/wiki/Tz_database). Note that some time zones include a provision for daylight savings time. The rules for daylight saving time are determined by the chosen tz. For UTC use the string \"utc\". If a time zone is not specified, the default will be in UTC (also known as GMT).
 * @member {String} timeZone
 */
Job.prototype['timeZone'] = undefined;

/**
 * Output only. The creation time of the job.
 * @member {String} userUpdateTime
 */
Job.prototype['userUpdateTime'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
Job['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "ENABLED"
     * @const
     */
    "ENABLED": "ENABLED",

    /**
     * value: "PAUSED"
     * @const
     */
    "PAUSED": "PAUSED",

    /**
     * value: "DISABLED"
     * @const
     */
    "DISABLED": "DISABLED",

    /**
     * value: "UPDATE_FAILED"
     * @const
     */
    "UPDATE_FAILED": "UPDATE_FAILED"
};



export default Job;

