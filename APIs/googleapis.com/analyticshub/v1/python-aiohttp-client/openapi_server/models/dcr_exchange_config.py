# coding: utf-8

from datetime import date, datetime

from typing import List, Dict, Type

from openapi_server.models.base_model import Model
from openapi_server import util


class DcrExchangeConfig(Model):
    """NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

    Do not edit the class manually.
    """

    def __init__(self, single_linked_dataset_per_cleanroom: bool=None, single_selected_resource_sharing_restriction: bool=None):
        """DcrExchangeConfig - a model defined in OpenAPI

        :param single_linked_dataset_per_cleanroom: The single_linked_dataset_per_cleanroom of this DcrExchangeConfig.
        :param single_selected_resource_sharing_restriction: The single_selected_resource_sharing_restriction of this DcrExchangeConfig.
        """
        self.openapi_types = {
            'single_linked_dataset_per_cleanroom': bool,
            'single_selected_resource_sharing_restriction': bool
        }

        self.attribute_map = {
            'single_linked_dataset_per_cleanroom': 'singleLinkedDatasetPerCleanroom',
            'single_selected_resource_sharing_restriction': 'singleSelectedResourceSharingRestriction'
        }

        self._single_linked_dataset_per_cleanroom = single_linked_dataset_per_cleanroom
        self._single_selected_resource_sharing_restriction = single_selected_resource_sharing_restriction

    @classmethod
    def from_dict(cls, dikt: dict) -> 'DcrExchangeConfig':
        """Returns the dict as a model

        :param dikt: A dict.
        :return: The DcrExchangeConfig of this DcrExchangeConfig.
        """
        return util.deserialize_model(dikt, cls)

    @property
    def single_linked_dataset_per_cleanroom(self):
        """Gets the single_linked_dataset_per_cleanroom of this DcrExchangeConfig.

        Output only. If True, when subscribing to this DCR, it will create only one linked dataset containing all resources shared within the cleanroom. If False, when subscribing to this DCR, it will create 1 linked dataset per listing. This is not configurable, and by default, all new DCRs will have the restriction set to True.

        :return: The single_linked_dataset_per_cleanroom of this DcrExchangeConfig.
        :rtype: bool
        """
        return self._single_linked_dataset_per_cleanroom

    @single_linked_dataset_per_cleanroom.setter
    def single_linked_dataset_per_cleanroom(self, single_linked_dataset_per_cleanroom):
        """Sets the single_linked_dataset_per_cleanroom of this DcrExchangeConfig.

        Output only. If True, when subscribing to this DCR, it will create only one linked dataset containing all resources shared within the cleanroom. If False, when subscribing to this DCR, it will create 1 linked dataset per listing. This is not configurable, and by default, all new DCRs will have the restriction set to True.

        :param single_linked_dataset_per_cleanroom: The single_linked_dataset_per_cleanroom of this DcrExchangeConfig.
        :type single_linked_dataset_per_cleanroom: bool
        """

        self._single_linked_dataset_per_cleanroom = single_linked_dataset_per_cleanroom

    @property
    def single_selected_resource_sharing_restriction(self):
        """Gets the single_selected_resource_sharing_restriction of this DcrExchangeConfig.

        Output only. If True, this DCR restricts the contributors to sharing only a single resource in a Listing. And no two resources should have the same IDs. So if a contributor adds a view with a conflicting name, the CreateListing API will reject the request. if False, the data contributor can publish an entire dataset (as before). This is not configurable, and by default, all new DCRs will have the restriction set to True.

        :return: The single_selected_resource_sharing_restriction of this DcrExchangeConfig.
        :rtype: bool
        """
        return self._single_selected_resource_sharing_restriction

    @single_selected_resource_sharing_restriction.setter
    def single_selected_resource_sharing_restriction(self, single_selected_resource_sharing_restriction):
        """Sets the single_selected_resource_sharing_restriction of this DcrExchangeConfig.

        Output only. If True, this DCR restricts the contributors to sharing only a single resource in a Listing. And no two resources should have the same IDs. So if a contributor adds a view with a conflicting name, the CreateListing API will reject the request. if False, the data contributor can publish an entire dataset (as before). This is not configurable, and by default, all new DCRs will have the restriction set to True.

        :param single_selected_resource_sharing_restriction: The single_selected_resource_sharing_restriction of this DcrExchangeConfig.
        :type single_selected_resource_sharing_restriction: bool
        """

        self._single_selected_resource_sharing_restriction = single_selected_resource_sharing_restriction
