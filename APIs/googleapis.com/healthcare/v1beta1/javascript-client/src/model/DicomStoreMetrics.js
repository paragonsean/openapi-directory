/**
 * Cloud Healthcare API
 * Manage, store, and access healthcare data in Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DicomStoreMetrics model module.
 * @module model/DicomStoreMetrics
 * @version v1beta1
 */
class DicomStoreMetrics {
    /**
     * Constructs a new <code>DicomStoreMetrics</code>.
     * DicomStoreMetrics contains metrics describing a DICOM store.
     * @alias module:model/DicomStoreMetrics
     */
    constructor() { 
        
        DicomStoreMetrics.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DicomStoreMetrics</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DicomStoreMetrics} obj Optional instance to populate.
     * @return {module:model/DicomStoreMetrics} The populated <code>DicomStoreMetrics</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DicomStoreMetrics();

            if (data.hasOwnProperty('blobStorageSizeBytes')) {
                obj['blobStorageSizeBytes'] = ApiClient.convertToType(data['blobStorageSizeBytes'], 'String');
            }
            if (data.hasOwnProperty('instanceCount')) {
                obj['instanceCount'] = ApiClient.convertToType(data['instanceCount'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('seriesCount')) {
                obj['seriesCount'] = ApiClient.convertToType(data['seriesCount'], 'String');
            }
            if (data.hasOwnProperty('structuredStorageSizeBytes')) {
                obj['structuredStorageSizeBytes'] = ApiClient.convertToType(data['structuredStorageSizeBytes'], 'String');
            }
            if (data.hasOwnProperty('studyCount')) {
                obj['studyCount'] = ApiClient.convertToType(data['studyCount'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DicomStoreMetrics</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DicomStoreMetrics</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['blobStorageSizeBytes'] && !(typeof data['blobStorageSizeBytes'] === 'string' || data['blobStorageSizeBytes'] instanceof String)) {
            throw new Error("Expected the field `blobStorageSizeBytes` to be a primitive type in the JSON string but got " + data['blobStorageSizeBytes']);
        }
        // ensure the json data is a string
        if (data['instanceCount'] && !(typeof data['instanceCount'] === 'string' || data['instanceCount'] instanceof String)) {
            throw new Error("Expected the field `instanceCount` to be a primitive type in the JSON string but got " + data['instanceCount']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['seriesCount'] && !(typeof data['seriesCount'] === 'string' || data['seriesCount'] instanceof String)) {
            throw new Error("Expected the field `seriesCount` to be a primitive type in the JSON string but got " + data['seriesCount']);
        }
        // ensure the json data is a string
        if (data['structuredStorageSizeBytes'] && !(typeof data['structuredStorageSizeBytes'] === 'string' || data['structuredStorageSizeBytes'] instanceof String)) {
            throw new Error("Expected the field `structuredStorageSizeBytes` to be a primitive type in the JSON string but got " + data['structuredStorageSizeBytes']);
        }
        // ensure the json data is a string
        if (data['studyCount'] && !(typeof data['studyCount'] === 'string' || data['studyCount'] instanceof String)) {
            throw new Error("Expected the field `studyCount` to be a primitive type in the JSON string but got " + data['studyCount']);
        }

        return true;
    }


}



/**
 * Total blob storage bytes for all instances in the store.
 * @member {String} blobStorageSizeBytes
 */
DicomStoreMetrics.prototype['blobStorageSizeBytes'] = undefined;

/**
 * Number of instances in the store.
 * @member {String} instanceCount
 */
DicomStoreMetrics.prototype['instanceCount'] = undefined;

/**
 * Resource name of the DICOM store, of the form `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/dicomStores/{dicom_store_id}`.
 * @member {String} name
 */
DicomStoreMetrics.prototype['name'] = undefined;

/**
 * Number of series in the store.
 * @member {String} seriesCount
 */
DicomStoreMetrics.prototype['seriesCount'] = undefined;

/**
 * Total structured storage bytes for all instances in the store.
 * @member {String} structuredStorageSizeBytes
 */
DicomStoreMetrics.prototype['structuredStorageSizeBytes'] = undefined;

/**
 * Number of studies in the store.
 * @member {String} studyCount
 */
DicomStoreMetrics.prototype['studyCount'] = undefined;






export default DicomStoreMetrics;

