/**
 * Cloud Healthcare API
 * Manage, store, and access healthcare data in Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FhirNotificationConfig model module.
 * @module model/FhirNotificationConfig
 * @version v1
 */
class FhirNotificationConfig {
    /**
     * Constructs a new <code>FhirNotificationConfig</code>.
     * Contains the configuration for FHIR notifications.
     * @alias module:model/FhirNotificationConfig
     */
    constructor() { 
        
        FhirNotificationConfig.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FhirNotificationConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FhirNotificationConfig} obj Optional instance to populate.
     * @return {module:model/FhirNotificationConfig} The populated <code>FhirNotificationConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FhirNotificationConfig();

            if (data.hasOwnProperty('pubsubTopic')) {
                obj['pubsubTopic'] = ApiClient.convertToType(data['pubsubTopic'], 'String');
            }
            if (data.hasOwnProperty('sendFullResource')) {
                obj['sendFullResource'] = ApiClient.convertToType(data['sendFullResource'], 'Boolean');
            }
            if (data.hasOwnProperty('sendPreviousResourceOnDelete')) {
                obj['sendPreviousResourceOnDelete'] = ApiClient.convertToType(data['sendPreviousResourceOnDelete'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FhirNotificationConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FhirNotificationConfig</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['pubsubTopic'] && !(typeof data['pubsubTopic'] === 'string' || data['pubsubTopic'] instanceof String)) {
            throw new Error("Expected the field `pubsubTopic` to be a primitive type in the JSON string but got " + data['pubsubTopic']);
        }

        return true;
    }


}



/**
 * The [Pub/Sub](https://cloud.google.com/pubsub/docs/) topic that notifications of changes are published on. Supplied by the client. The notification is a `PubsubMessage` with the following fields: * `PubsubMessage.Data` contains the resource name. * `PubsubMessage.MessageId` is the ID of this notification. It is guaranteed to be unique within the topic. * `PubsubMessage.PublishTime` is the time when the message was published. Note that notifications are only sent if the topic is non-empty. [Topic names](https://cloud.google.com/pubsub/docs/overview#names) must be scoped to a project. The Cloud Healthcare API service account, service-@gcp-sa-healthcare.iam.gserviceaccount.com, must have publisher permissions on the given Pub/Sub topic. Not having adequate permissions causes the calls that send notifications to fail (https://cloud.google.com/healthcare-api/docs/permissions-healthcare-api-gcp-products#dicom_fhir_and_hl7v2_store_cloud_pubsub_permissions). If a notification can't be published to Pub/Sub, errors are logged to Cloud Logging. For more information, see [Viewing error logs in Cloud Logging](https://cloud.google.com/healthcare-api/docs/how-tos/logging).
 * @member {String} pubsubTopic
 */
FhirNotificationConfig.prototype['pubsubTopic'] = undefined;

/**
 * Whether to send full FHIR resource to this Pub/Sub topic.
 * @member {Boolean} sendFullResource
 */
FhirNotificationConfig.prototype['sendFullResource'] = undefined;

/**
 * Whether to send full FHIR resource to this Pub/Sub topic for deleting FHIR resource. Note that setting this to true does not guarantee that all previous resources will be sent in the format of full FHIR resource. When a resource change is too large or during heavy traffic, only the resource name will be sent. Clients should always check the \"payloadType\" label from a Pub/Sub message to determine whether it needs to fetch the full previous resource as a separate operation.
 * @member {Boolean} sendPreviousResourceOnDelete
 */
FhirNotificationConfig.prototype['sendPreviousResourceOnDelete'] = undefined;






export default FhirNotificationConfig;

