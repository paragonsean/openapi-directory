/**
 * Remote Build Execution API
 * Supplies a Remote Execution API service for tools such as bazel.
 *
 * The version of the OpenAPI document: v1alpha
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GoogleDevtoolsRemoteworkersV1test2Digest from './GoogleDevtoolsRemoteworkersV1test2Digest';
import GoogleRpcStatus from './GoogleRpcStatus';

/**
 * The GoogleDevtoolsRemoteworkersV1test2CommandResult model module.
 * @module model/GoogleDevtoolsRemoteworkersV1test2CommandResult
 * @version v1alpha
 */
class GoogleDevtoolsRemoteworkersV1test2CommandResult {
    /**
     * Constructs a new <code>GoogleDevtoolsRemoteworkersV1test2CommandResult</code>.
     * All information about the execution of a command, suitable for providing as the Bots interface&#39;s &#x60;Lease.result&#x60; field.
     * @alias module:model/GoogleDevtoolsRemoteworkersV1test2CommandResult
     */
    constructor() { 
        
        GoogleDevtoolsRemoteworkersV1test2CommandResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoogleDevtoolsRemoteworkersV1test2CommandResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoogleDevtoolsRemoteworkersV1test2CommandResult} obj Optional instance to populate.
     * @return {module:model/GoogleDevtoolsRemoteworkersV1test2CommandResult} The populated <code>GoogleDevtoolsRemoteworkersV1test2CommandResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoogleDevtoolsRemoteworkersV1test2CommandResult();

            if (data.hasOwnProperty('duration')) {
                obj['duration'] = ApiClient.convertToType(data['duration'], 'String');
            }
            if (data.hasOwnProperty('exitCode')) {
                obj['exitCode'] = ApiClient.convertToType(data['exitCode'], 'Number');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], [{'String': Object}]);
            }
            if (data.hasOwnProperty('outputs')) {
                obj['outputs'] = GoogleDevtoolsRemoteworkersV1test2Digest.constructFromObject(data['outputs']);
            }
            if (data.hasOwnProperty('overhead')) {
                obj['overhead'] = ApiClient.convertToType(data['overhead'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = GoogleRpcStatus.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoogleDevtoolsRemoteworkersV1test2CommandResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoogleDevtoolsRemoteworkersV1test2CommandResult</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['duration'] && !(typeof data['duration'] === 'string' || data['duration'] instanceof String)) {
            throw new Error("Expected the field `duration` to be a primitive type in the JSON string but got " + data['duration']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['metadata'])) {
            throw new Error("Expected the field `metadata` to be an array in the JSON data but got " + data['metadata']);
        }
        // validate the optional field `outputs`
        if (data['outputs']) { // data not null
          GoogleDevtoolsRemoteworkersV1test2Digest.validateJSON(data['outputs']);
        }
        // ensure the json data is a string
        if (data['overhead'] && !(typeof data['overhead'] === 'string' || data['overhead'] instanceof String)) {
            throw new Error("Expected the field `overhead` to be a primitive type in the JSON string but got " + data['overhead']);
        }
        // validate the optional field `status`
        if (data['status']) { // data not null
          GoogleRpcStatus.validateJSON(data['status']);
        }

        return true;
    }


}



/**
 * The elapsed time between calling Accept and Complete. The server will also have its own idea of what this should be, but this excludes the overhead of the RPCs and the bot response time.
 * @member {String} duration
 */
GoogleDevtoolsRemoteworkersV1test2CommandResult.prototype['duration'] = undefined;

/**
 * The exit code of the process. An exit code of \"0\" should only be trusted if `status` has a code of OK (otherwise it may simply be unset).
 * @member {Number} exitCode
 */
GoogleDevtoolsRemoteworkersV1test2CommandResult.prototype['exitCode'] = undefined;

/**
 * Implementation-dependent metadata about the task. Both servers and bots may define messages which can be encoded here; bots are free to provide metadata in multiple formats, and servers are free to choose one or more of the values to process and ignore others. In particular, it is *not* considered an error for the bot to provide the server with a field that it doesn't know about.
 * @member {Array.<Object.<String, Object>>} metadata
 */
GoogleDevtoolsRemoteworkersV1test2CommandResult.prototype['metadata'] = undefined;

/**
 * @member {module:model/GoogleDevtoolsRemoteworkersV1test2Digest} outputs
 */
GoogleDevtoolsRemoteworkersV1test2CommandResult.prototype['outputs'] = undefined;

/**
 * The amount of time *not* spent executing the command (ie uploading/downloading files).
 * @member {String} overhead
 */
GoogleDevtoolsRemoteworkersV1test2CommandResult.prototype['overhead'] = undefined;

/**
 * @member {module:model/GoogleRpcStatus} status
 */
GoogleDevtoolsRemoteworkersV1test2CommandResult.prototype['status'] = undefined;






export default GoogleDevtoolsRemoteworkersV1test2CommandResult;

