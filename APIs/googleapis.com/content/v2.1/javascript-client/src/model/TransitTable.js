/**
 * Content API for Shopping
 * Manage your product listings and accounts for Google Shopping
 *
 * The version of the OpenAPI document: v2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransitTableTransitTimeRow from './TransitTableTransitTimeRow';

/**
 * The TransitTable model module.
 * @module model/TransitTable
 * @version v2.1
 */
class TransitTable {
    /**
     * Constructs a new <code>TransitTable</code>.
     * @alias module:model/TransitTable
     */
    constructor() { 
        
        TransitTable.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TransitTable</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransitTable} obj Optional instance to populate.
     * @return {module:model/TransitTable} The populated <code>TransitTable</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransitTable();

            if (data.hasOwnProperty('postalCodeGroupNames')) {
                obj['postalCodeGroupNames'] = ApiClient.convertToType(data['postalCodeGroupNames'], ['String']);
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [TransitTableTransitTimeRow]);
            }
            if (data.hasOwnProperty('transitTimeLabels')) {
                obj['transitTimeLabels'] = ApiClient.convertToType(data['transitTimeLabels'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransitTable</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransitTable</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['postalCodeGroupNames'])) {
            throw new Error("Expected the field `postalCodeGroupNames` to be an array in the JSON data but got " + data['postalCodeGroupNames']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                TransitTableTransitTimeRow.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['transitTimeLabels'])) {
            throw new Error("Expected the field `transitTimeLabels` to be an array in the JSON data but got " + data['transitTimeLabels']);
        }

        return true;
    }


}



/**
 * A list of postal group names. The last value can be `\"all other locations\"`. Example: `[\"zone 1\", \"zone 2\", \"all other locations\"]`. The referred postal code groups must match the delivery country of the service.
 * @member {Array.<String>} postalCodeGroupNames
 */
TransitTable.prototype['postalCodeGroupNames'] = undefined;

/**
 * @member {Array.<module:model/TransitTableTransitTimeRow>} rows
 */
TransitTable.prototype['rows'] = undefined;

/**
 * A list of transit time labels. The last value can be `\"all other labels\"`. Example: `[\"food\", \"electronics\", \"all other labels\"]`.
 * @member {Array.<String>} transitTimeLabels
 */
TransitTable.prototype['transitTimeLabels'] = undefined;






export default TransitTable;

