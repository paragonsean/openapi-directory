/**
 * Content API for Shopping
 * Manage your product listings and accounts for Google Shopping
 *
 * The version of the OpenAPI document: v2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccountCredentials model module.
 * @module model/AccountCredentials
 * @version v2.1
 */
class AccountCredentials {
    /**
     * Constructs a new <code>AccountCredentials</code>.
     * Credentials allowing Google to call a partner&#39;s API on behalf of a merchant.
     * @alias module:model/AccountCredentials
     */
    constructor() { 
        
        AccountCredentials.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AccountCredentials</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccountCredentials} obj Optional instance to populate.
     * @return {module:model/AccountCredentials} The populated <code>AccountCredentials</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccountCredentials();

            if (data.hasOwnProperty('accessToken')) {
                obj['accessToken'] = ApiClient.convertToType(data['accessToken'], 'String');
            }
            if (data.hasOwnProperty('expiresIn')) {
                obj['expiresIn'] = ApiClient.convertToType(data['expiresIn'], 'String');
            }
            if (data.hasOwnProperty('purpose')) {
                obj['purpose'] = ApiClient.convertToType(data['purpose'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccountCredentials</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccountCredentials</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['accessToken'] && !(typeof data['accessToken'] === 'string' || data['accessToken'] instanceof String)) {
            throw new Error("Expected the field `accessToken` to be a primitive type in the JSON string but got " + data['accessToken']);
        }
        // ensure the json data is a string
        if (data['expiresIn'] && !(typeof data['expiresIn'] === 'string' || data['expiresIn'] instanceof String)) {
            throw new Error("Expected the field `expiresIn` to be a primitive type in the JSON string but got " + data['expiresIn']);
        }
        // ensure the json data is a string
        if (data['purpose'] && !(typeof data['purpose'] === 'string' || data['purpose'] instanceof String)) {
            throw new Error("Expected the field `purpose` to be a primitive type in the JSON string but got " + data['purpose']);
        }

        return true;
    }


}



/**
 * An OAuth access token.
 * @member {String} accessToken
 */
AccountCredentials.prototype['accessToken'] = undefined;

/**
 * The amount of time, in seconds, after which the access token is no longer valid.
 * @member {String} expiresIn
 */
AccountCredentials.prototype['expiresIn'] = undefined;

/**
 * Indicates to Google how Google should use these OAuth tokens.
 * @member {module:model/AccountCredentials.PurposeEnum} purpose
 */
AccountCredentials.prototype['purpose'] = undefined;





/**
 * Allowed values for the <code>purpose</code> property.
 * @enum {String}
 * @readonly
 */
AccountCredentials['PurposeEnum'] = {

    /**
     * value: "ACCOUNT_CREDENTIALS_PURPOSE_UNSPECIFIED"
     * @const
     */
    "ACCOUNT_CREDENTIALS_PURPOSE_UNSPECIFIED": "ACCOUNT_CREDENTIALS_PURPOSE_UNSPECIFIED",

    /**
     * value: "SHOPIFY_ORDER_MANAGEMENT"
     * @const
     */
    "SHOPIFY_ORDER_MANAGEMENT": "SHOPIFY_ORDER_MANAGEMENT",

    /**
     * value: "SHOPIFY_INTEGRATION"
     * @const
     */
    "SHOPIFY_INTEGRATION": "SHOPIFY_INTEGRATION"
};



export default AccountCredentials;

