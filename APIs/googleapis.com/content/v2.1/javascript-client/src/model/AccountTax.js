/**
 * Content API for Shopping
 * Manage your product listings and accounts for Google Shopping
 *
 * The version of the OpenAPI document: v2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AccountTaxTaxRule from './AccountTaxTaxRule';

/**
 * The AccountTax model module.
 * @module model/AccountTax
 * @version v2.1
 */
class AccountTax {
    /**
     * Constructs a new <code>AccountTax</code>.
     * The tax settings of a merchant account. All methods require the admin role.
     * @alias module:model/AccountTax
     */
    constructor() { 
        
        AccountTax.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AccountTax</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccountTax} obj Optional instance to populate.
     * @return {module:model/AccountTax} The populated <code>AccountTax</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccountTax();

            if (data.hasOwnProperty('accountId')) {
                obj['accountId'] = ApiClient.convertToType(data['accountId'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('rules')) {
                obj['rules'] = ApiClient.convertToType(data['rules'], [AccountTaxTaxRule]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccountTax</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccountTax</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['accountId'] && !(typeof data['accountId'] === 'string' || data['accountId'] instanceof String)) {
            throw new Error("Expected the field `accountId` to be a primitive type in the JSON string but got " + data['accountId']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        if (data['rules']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rules'])) {
                throw new Error("Expected the field `rules` to be an array in the JSON data but got " + data['rules']);
            }
            // validate the optional field `rules` (array)
            for (const item of data['rules']) {
                AccountTaxTaxRule.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Required. The ID of the account to which these account tax settings belong.
 * @member {String} accountId
 */
AccountTax.prototype['accountId'] = undefined;

/**
 * Identifies what kind of resource this is. Value: the fixed string \"`content#accountTax`\".
 * @member {String} kind
 */
AccountTax.prototype['kind'] = undefined;

/**
 * Tax rules. Updating the tax rules will enable \"US\" taxes (not reversible). Defining no rules is equivalent to not charging tax at all.
 * @member {Array.<module:model/AccountTaxTaxRule>} rules
 */
AccountTax.prototype['rules'] = undefined;






export default AccountTax;

