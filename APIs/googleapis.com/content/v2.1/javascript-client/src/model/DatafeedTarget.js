/**
 * Content API for Shopping
 * Manage your product listings and accounts for Google Shopping
 *
 * The version of the OpenAPI document: v2.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DatafeedTarget model module.
 * @module model/DatafeedTarget
 * @version v2.1
 */
class DatafeedTarget {
    /**
     * Constructs a new <code>DatafeedTarget</code>.
     * @alias module:model/DatafeedTarget
     */
    constructor() { 
        
        DatafeedTarget.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DatafeedTarget</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DatafeedTarget} obj Optional instance to populate.
     * @return {module:model/DatafeedTarget} The populated <code>DatafeedTarget</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DatafeedTarget();

            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
            if (data.hasOwnProperty('excludedDestinations')) {
                obj['excludedDestinations'] = ApiClient.convertToType(data['excludedDestinations'], ['String']);
            }
            if (data.hasOwnProperty('feedLabel')) {
                obj['feedLabel'] = ApiClient.convertToType(data['feedLabel'], 'String');
            }
            if (data.hasOwnProperty('includedDestinations')) {
                obj['includedDestinations'] = ApiClient.convertToType(data['includedDestinations'], ['String']);
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], 'String');
            }
            if (data.hasOwnProperty('targetCountries')) {
                obj['targetCountries'] = ApiClient.convertToType(data['targetCountries'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DatafeedTarget</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DatafeedTarget</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['excludedDestinations'])) {
            throw new Error("Expected the field `excludedDestinations` to be an array in the JSON data but got " + data['excludedDestinations']);
        }
        // ensure the json data is a string
        if (data['feedLabel'] && !(typeof data['feedLabel'] === 'string' || data['feedLabel'] instanceof String)) {
            throw new Error("Expected the field `feedLabel` to be a primitive type in the JSON string but got " + data['feedLabel']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['includedDestinations'])) {
            throw new Error("Expected the field `includedDestinations` to be an array in the JSON data but got " + data['includedDestinations']);
        }
        // ensure the json data is a string
        if (data['language'] && !(typeof data['language'] === 'string' || data['language'] instanceof String)) {
            throw new Error("Expected the field `language` to be a primitive type in the JSON string but got " + data['language']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['targetCountries'])) {
            throw new Error("Expected the field `targetCountries` to be an array in the JSON data but got " + data['targetCountries']);
        }

        return true;
    }


}



/**
 * Deprecated. Use `feedLabel` instead. The country where the items in the feed will be included in the search index, represented as a CLDR territory code.
 * @member {String} country
 */
DatafeedTarget.prototype['country'] = undefined;

/**
 * The list of destinations to exclude for this target (corresponds to cleared check boxes in Merchant Center). Products that are excluded from all destinations for more than 7 days are automatically deleted.
 * @member {Array.<String>} excludedDestinations
 */
DatafeedTarget.prototype['excludedDestinations'] = undefined;

/**
 * Feed label for the DatafeedTarget. Either `country` or `feedLabel` is required. If both `feedLabel` and `country` is specified, the values must match. Must be less than or equal to 20 uppercase letters (A-Z), numbers (0-9), and dashes (-).
 * @member {String} feedLabel
 */
DatafeedTarget.prototype['feedLabel'] = undefined;

/**
 * The list of destinations to include for this target (corresponds to checked check boxes in Merchant Center). Default destinations are always included unless provided in `excludedDestinations`.
 * @member {Array.<String>} includedDestinations
 */
DatafeedTarget.prototype['includedDestinations'] = undefined;

/**
 * The two-letter ISO 639-1 language of the items in the feed. Must be a valid language for `targets[].country`.
 * @member {String} language
 */
DatafeedTarget.prototype['language'] = undefined;

/**
 * The countries where the items may be displayed. Represented as a CLDR territory code. Will be ignored for \"product inventory\" feeds.
 * @member {Array.<String>} targetCountries
 */
DatafeedTarget.prototype['targetCountries'] = undefined;






export default DatafeedTarget;

