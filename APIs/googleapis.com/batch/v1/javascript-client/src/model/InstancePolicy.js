/**
 * Batch API
 * An API to manage the running of batch resources on Google Cloud Platform.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Accelerator from './Accelerator';
import AttachedDisk from './AttachedDisk';
import Disk from './Disk';

/**
 * The InstancePolicy model module.
 * @module model/InstancePolicy
 * @version v1
 */
class InstancePolicy {
    /**
     * Constructs a new <code>InstancePolicy</code>.
     * InstancePolicy describes an instance type and resources attached to each VM created by this InstancePolicy.
     * @alias module:model/InstancePolicy
     */
    constructor() { 
        
        InstancePolicy.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InstancePolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InstancePolicy} obj Optional instance to populate.
     * @return {module:model/InstancePolicy} The populated <code>InstancePolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InstancePolicy();

            if (data.hasOwnProperty('accelerators')) {
                obj['accelerators'] = ApiClient.convertToType(data['accelerators'], [Accelerator]);
            }
            if (data.hasOwnProperty('bootDisk')) {
                obj['bootDisk'] = Disk.constructFromObject(data['bootDisk']);
            }
            if (data.hasOwnProperty('disks')) {
                obj['disks'] = ApiClient.convertToType(data['disks'], [AttachedDisk]);
            }
            if (data.hasOwnProperty('machineType')) {
                obj['machineType'] = ApiClient.convertToType(data['machineType'], 'String');
            }
            if (data.hasOwnProperty('minCpuPlatform')) {
                obj['minCpuPlatform'] = ApiClient.convertToType(data['minCpuPlatform'], 'String');
            }
            if (data.hasOwnProperty('provisioningModel')) {
                obj['provisioningModel'] = ApiClient.convertToType(data['provisioningModel'], 'String');
            }
            if (data.hasOwnProperty('reservation')) {
                obj['reservation'] = ApiClient.convertToType(data['reservation'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InstancePolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InstancePolicy</code>.
     */
    static validateJSON(data) {
        if (data['accelerators']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['accelerators'])) {
                throw new Error("Expected the field `accelerators` to be an array in the JSON data but got " + data['accelerators']);
            }
            // validate the optional field `accelerators` (array)
            for (const item of data['accelerators']) {
                Accelerator.validateJSON(item);
            };
        }
        // validate the optional field `bootDisk`
        if (data['bootDisk']) { // data not null
          Disk.validateJSON(data['bootDisk']);
        }
        if (data['disks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['disks'])) {
                throw new Error("Expected the field `disks` to be an array in the JSON data but got " + data['disks']);
            }
            // validate the optional field `disks` (array)
            for (const item of data['disks']) {
                AttachedDisk.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['machineType'] && !(typeof data['machineType'] === 'string' || data['machineType'] instanceof String)) {
            throw new Error("Expected the field `machineType` to be a primitive type in the JSON string but got " + data['machineType']);
        }
        // ensure the json data is a string
        if (data['minCpuPlatform'] && !(typeof data['minCpuPlatform'] === 'string' || data['minCpuPlatform'] instanceof String)) {
            throw new Error("Expected the field `minCpuPlatform` to be a primitive type in the JSON string but got " + data['minCpuPlatform']);
        }
        // ensure the json data is a string
        if (data['provisioningModel'] && !(typeof data['provisioningModel'] === 'string' || data['provisioningModel'] instanceof String)) {
            throw new Error("Expected the field `provisioningModel` to be a primitive type in the JSON string but got " + data['provisioningModel']);
        }
        // ensure the json data is a string
        if (data['reservation'] && !(typeof data['reservation'] === 'string' || data['reservation'] instanceof String)) {
            throw new Error("Expected the field `reservation` to be a primitive type in the JSON string but got " + data['reservation']);
        }

        return true;
    }


}



/**
 * The accelerators attached to each VM instance.
 * @member {Array.<module:model/Accelerator>} accelerators
 */
InstancePolicy.prototype['accelerators'] = undefined;

/**
 * @member {module:model/Disk} bootDisk
 */
InstancePolicy.prototype['bootDisk'] = undefined;

/**
 * Non-boot disks to be attached for each VM created by this InstancePolicy. New disks will be deleted when the VM is deleted. A non-boot disk is a disk that can be of a device with a file system or a raw storage drive that is not ready for data storage and accessing.
 * @member {Array.<module:model/AttachedDisk>} disks
 */
InstancePolicy.prototype['disks'] = undefined;

/**
 * The Compute Engine machine type.
 * @member {String} machineType
 */
InstancePolicy.prototype['machineType'] = undefined;

/**
 * The minimum CPU platform. See https://cloud.google.com/compute/docs/instances/specify-min-cpu-platform.
 * @member {String} minCpuPlatform
 */
InstancePolicy.prototype['minCpuPlatform'] = undefined;

/**
 * The provisioning model.
 * @member {module:model/InstancePolicy.ProvisioningModelEnum} provisioningModel
 */
InstancePolicy.prototype['provisioningModel'] = undefined;

/**
 * Optional. If specified, VMs will consume only the specified reservation. If not specified (default), VMs will consume any applicable reservation.
 * @member {String} reservation
 */
InstancePolicy.prototype['reservation'] = undefined;





/**
 * Allowed values for the <code>provisioningModel</code> property.
 * @enum {String}
 * @readonly
 */
InstancePolicy['ProvisioningModelEnum'] = {

    /**
     * value: "PROVISIONING_MODEL_UNSPECIFIED"
     * @const
     */
    "PROVISIONING_MODEL_UNSPECIFIED": "PROVISIONING_MODEL_UNSPECIFIED",

    /**
     * value: "STANDARD"
     * @const
     */
    "STANDARD": "STANDARD",

    /**
     * value: "SPOT"
     * @const
     */
    "SPOT": "SPOT",

    /**
     * value: "PREEMPTIBLE"
     * @const
     */
    "PREEMPTIBLE": "PREEMPTIBLE"
};



export default InstancePolicy;

