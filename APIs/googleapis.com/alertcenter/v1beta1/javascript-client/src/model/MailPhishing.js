/**
 * Google Workspace Alert Center API
 * Manages alerts on issues affecting your domain. Note: The current version of this API (v1beta1) is available to all Google Workspace customers. 
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DomainId from './DomainId';
import GmailMessageInfo from './GmailMessageInfo';
import MaliciousEntity from './MaliciousEntity';

/**
 * The MailPhishing model module.
 * @module model/MailPhishing
 * @version v1beta1
 */
class MailPhishing {
    /**
     * Constructs a new <code>MailPhishing</code>.
     * Proto for all phishing alerts with common payload. Supported types are any of the following: * User reported phishing * User reported spam spike * Suspicious message reported * Phishing reclassification * Malware reclassification * Gmail potential employee spoofing
     * @alias module:model/MailPhishing
     */
    constructor() { 
        
        MailPhishing.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>MailPhishing</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MailPhishing} obj Optional instance to populate.
     * @return {module:model/MailPhishing} The populated <code>MailPhishing</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MailPhishing();

            if (data.hasOwnProperty('domainId')) {
                obj['domainId'] = DomainId.constructFromObject(data['domainId']);
            }
            if (data.hasOwnProperty('isInternal')) {
                obj['isInternal'] = ApiClient.convertToType(data['isInternal'], 'Boolean');
            }
            if (data.hasOwnProperty('maliciousEntity')) {
                obj['maliciousEntity'] = MaliciousEntity.constructFromObject(data['maliciousEntity']);
            }
            if (data.hasOwnProperty('messages')) {
                obj['messages'] = ApiClient.convertToType(data['messages'], [GmailMessageInfo]);
            }
            if (data.hasOwnProperty('systemActionType')) {
                obj['systemActionType'] = ApiClient.convertToType(data['systemActionType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MailPhishing</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MailPhishing</code>.
     */
    static validateJSON(data) {
        // validate the optional field `domainId`
        if (data['domainId']) { // data not null
          DomainId.validateJSON(data['domainId']);
        }
        // validate the optional field `maliciousEntity`
        if (data['maliciousEntity']) { // data not null
          MaliciousEntity.validateJSON(data['maliciousEntity']);
        }
        if (data['messages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['messages'])) {
                throw new Error("Expected the field `messages` to be an array in the JSON data but got " + data['messages']);
            }
            // validate the optional field `messages` (array)
            for (const item of data['messages']) {
                GmailMessageInfo.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['systemActionType'] && !(typeof data['systemActionType'] === 'string' || data['systemActionType'] instanceof String)) {
            throw new Error("Expected the field `systemActionType` to be a primitive type in the JSON string but got " + data['systemActionType']);
        }

        return true;
    }


}



/**
 * @member {module:model/DomainId} domainId
 */
MailPhishing.prototype['domainId'] = undefined;

/**
 * If `true`, the email originated from within the organization.
 * @member {Boolean} isInternal
 */
MailPhishing.prototype['isInternal'] = undefined;

/**
 * @member {module:model/MaliciousEntity} maliciousEntity
 */
MailPhishing.prototype['maliciousEntity'] = undefined;

/**
 * The list of messages contained by this alert.
 * @member {Array.<module:model/GmailMessageInfo>} messages
 */
MailPhishing.prototype['messages'] = undefined;

/**
 * System actions on the messages.
 * @member {module:model/MailPhishing.SystemActionTypeEnum} systemActionType
 */
MailPhishing.prototype['systemActionType'] = undefined;





/**
 * Allowed values for the <code>systemActionType</code> property.
 * @enum {String}
 * @readonly
 */
MailPhishing['SystemActionTypeEnum'] = {

    /**
     * value: "SYSTEM_ACTION_TYPE_UNSPECIFIED"
     * @const
     */
    "SYSTEM_ACTION_TYPE_UNSPECIFIED": "SYSTEM_ACTION_TYPE_UNSPECIFIED",

    /**
     * value: "NO_OPERATION"
     * @const
     */
    "NO_OPERATION": "NO_OPERATION",

    /**
     * value: "REMOVED_FROM_INBOX"
     * @const
     */
    "REMOVED_FROM_INBOX": "REMOVED_FROM_INBOX"
};



export default MailPhishing;

