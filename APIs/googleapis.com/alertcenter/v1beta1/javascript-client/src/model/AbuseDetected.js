/**
 * Google Workspace Alert Center API
 * Manages alerts on issues affecting your domain. Note: The current version of this API (v1beta1) is available to all Google Workspace customers. 
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EntityList from './EntityList';

/**
 * The AbuseDetected model module.
 * @module model/AbuseDetected
 * @version v1beta1
 */
class AbuseDetected {
    /**
     * Constructs a new <code>AbuseDetected</code>.
     * A generic alert for abusive user activity occurring with a customer.
     * @alias module:model/AbuseDetected
     */
    constructor() { 
        
        AbuseDetected.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AbuseDetected</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AbuseDetected} obj Optional instance to populate.
     * @return {module:model/AbuseDetected} The populated <code>AbuseDetected</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AbuseDetected();

            if (data.hasOwnProperty('additionalDetails')) {
                obj['additionalDetails'] = EntityList.constructFromObject(data['additionalDetails']);
            }
            if (data.hasOwnProperty('product')) {
                obj['product'] = ApiClient.convertToType(data['product'], 'String');
            }
            if (data.hasOwnProperty('subAlertId')) {
                obj['subAlertId'] = ApiClient.convertToType(data['subAlertId'], 'String');
            }
            if (data.hasOwnProperty('variationType')) {
                obj['variationType'] = ApiClient.convertToType(data['variationType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AbuseDetected</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AbuseDetected</code>.
     */
    static validateJSON(data) {
        // validate the optional field `additionalDetails`
        if (data['additionalDetails']) { // data not null
          EntityList.validateJSON(data['additionalDetails']);
        }
        // ensure the json data is a string
        if (data['product'] && !(typeof data['product'] === 'string' || data['product'] instanceof String)) {
            throw new Error("Expected the field `product` to be a primitive type in the JSON string but got " + data['product']);
        }
        // ensure the json data is a string
        if (data['subAlertId'] && !(typeof data['subAlertId'] === 'string' || data['subAlertId'] instanceof String)) {
            throw new Error("Expected the field `subAlertId` to be a primitive type in the JSON string but got " + data['subAlertId']);
        }
        // ensure the json data is a string
        if (data['variationType'] && !(typeof data['variationType'] === 'string' || data['variationType'] instanceof String)) {
            throw new Error("Expected the field `variationType` to be a primitive type in the JSON string but got " + data['variationType']);
        }

        return true;
    }


}



/**
 * @member {module:model/EntityList} additionalDetails
 */
AbuseDetected.prototype['additionalDetails'] = undefined;

/**
 * Product that the abuse is originating from.
 * @member {String} product
 */
AbuseDetected.prototype['product'] = undefined;

/**
 * Unique identifier of each sub alert that is onboarded.
 * @member {String} subAlertId
 */
AbuseDetected.prototype['subAlertId'] = undefined;

/**
 * Variation of AbuseDetected alerts. The variation_type determines the texts displayed the alert details. This differs from sub_alert_id because each sub alert can have multiple variation_types, representing different stages of the alert.
 * @member {module:model/AbuseDetected.VariationTypeEnum} variationType
 */
AbuseDetected.prototype['variationType'] = undefined;





/**
 * Allowed values for the <code>variationType</code> property.
 * @enum {String}
 * @readonly
 */
AbuseDetected['VariationTypeEnum'] = {

    /**
     * value: "ABUSE_DETECTED_VARIATION_TYPE_UNSPECIFIED"
     * @const
     */
    "ABUSE_DETECTED_VARIATION_TYPE_UNSPECIFIED": "ABUSE_DETECTED_VARIATION_TYPE_UNSPECIFIED",

    /**
     * value: "DRIVE_ABUSIVE_CONTENT"
     * @const
     */
    "DRIVE_ABUSIVE_CONTENT": "DRIVE_ABUSIVE_CONTENT",

    /**
     * value: "LIMITED_DISABLE"
     * @const
     */
    "LIMITED_DISABLE": "LIMITED_DISABLE"
};



export default AbuseDetected;

