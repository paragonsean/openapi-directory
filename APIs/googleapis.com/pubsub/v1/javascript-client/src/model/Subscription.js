/**
 * Cloud Pub/Sub API
 * Provides reliable, many-to-many, asynchronous messaging between applications. 
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BigQueryConfig from './BigQueryConfig';
import CloudStorageConfig from './CloudStorageConfig';
import DeadLetterPolicy from './DeadLetterPolicy';
import ExpirationPolicy from './ExpirationPolicy';
import PushConfig from './PushConfig';
import RetryPolicy from './RetryPolicy';

/**
 * The Subscription model module.
 * @module model/Subscription
 * @version v1
 */
class Subscription {
    /**
     * Constructs a new <code>Subscription</code>.
     * A subscription resource. If none of &#x60;push_config&#x60;, &#x60;bigquery_config&#x60;, or &#x60;cloud_storage_config&#x60; is set, then the subscriber will pull and ack messages using API methods. At most one of these fields may be set.
     * @alias module:model/Subscription
     */
    constructor() { 
        
        Subscription.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Subscription</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Subscription} obj Optional instance to populate.
     * @return {module:model/Subscription} The populated <code>Subscription</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Subscription();

            if (data.hasOwnProperty('ackDeadlineSeconds')) {
                obj['ackDeadlineSeconds'] = ApiClient.convertToType(data['ackDeadlineSeconds'], 'Number');
            }
            if (data.hasOwnProperty('bigqueryConfig')) {
                obj['bigqueryConfig'] = BigQueryConfig.constructFromObject(data['bigqueryConfig']);
            }
            if (data.hasOwnProperty('cloudStorageConfig')) {
                obj['cloudStorageConfig'] = CloudStorageConfig.constructFromObject(data['cloudStorageConfig']);
            }
            if (data.hasOwnProperty('deadLetterPolicy')) {
                obj['deadLetterPolicy'] = DeadLetterPolicy.constructFromObject(data['deadLetterPolicy']);
            }
            if (data.hasOwnProperty('detached')) {
                obj['detached'] = ApiClient.convertToType(data['detached'], 'Boolean');
            }
            if (data.hasOwnProperty('enableExactlyOnceDelivery')) {
                obj['enableExactlyOnceDelivery'] = ApiClient.convertToType(data['enableExactlyOnceDelivery'], 'Boolean');
            }
            if (data.hasOwnProperty('enableMessageOrdering')) {
                obj['enableMessageOrdering'] = ApiClient.convertToType(data['enableMessageOrdering'], 'Boolean');
            }
            if (data.hasOwnProperty('expirationPolicy')) {
                obj['expirationPolicy'] = ExpirationPolicy.constructFromObject(data['expirationPolicy']);
            }
            if (data.hasOwnProperty('filter')) {
                obj['filter'] = ApiClient.convertToType(data['filter'], 'String');
            }
            if (data.hasOwnProperty('labels')) {
                obj['labels'] = ApiClient.convertToType(data['labels'], {'String': 'String'});
            }
            if (data.hasOwnProperty('messageRetentionDuration')) {
                obj['messageRetentionDuration'] = ApiClient.convertToType(data['messageRetentionDuration'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('pushConfig')) {
                obj['pushConfig'] = PushConfig.constructFromObject(data['pushConfig']);
            }
            if (data.hasOwnProperty('retainAckedMessages')) {
                obj['retainAckedMessages'] = ApiClient.convertToType(data['retainAckedMessages'], 'Boolean');
            }
            if (data.hasOwnProperty('retryPolicy')) {
                obj['retryPolicy'] = RetryPolicy.constructFromObject(data['retryPolicy']);
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('topic')) {
                obj['topic'] = ApiClient.convertToType(data['topic'], 'String');
            }
            if (data.hasOwnProperty('topicMessageRetentionDuration')) {
                obj['topicMessageRetentionDuration'] = ApiClient.convertToType(data['topicMessageRetentionDuration'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Subscription</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Subscription</code>.
     */
    static validateJSON(data) {
        // validate the optional field `bigqueryConfig`
        if (data['bigqueryConfig']) { // data not null
          BigQueryConfig.validateJSON(data['bigqueryConfig']);
        }
        // validate the optional field `cloudStorageConfig`
        if (data['cloudStorageConfig']) { // data not null
          CloudStorageConfig.validateJSON(data['cloudStorageConfig']);
        }
        // validate the optional field `deadLetterPolicy`
        if (data['deadLetterPolicy']) { // data not null
          DeadLetterPolicy.validateJSON(data['deadLetterPolicy']);
        }
        // validate the optional field `expirationPolicy`
        if (data['expirationPolicy']) { // data not null
          ExpirationPolicy.validateJSON(data['expirationPolicy']);
        }
        // ensure the json data is a string
        if (data['filter'] && !(typeof data['filter'] === 'string' || data['filter'] instanceof String)) {
            throw new Error("Expected the field `filter` to be a primitive type in the JSON string but got " + data['filter']);
        }
        // ensure the json data is a string
        if (data['messageRetentionDuration'] && !(typeof data['messageRetentionDuration'] === 'string' || data['messageRetentionDuration'] instanceof String)) {
            throw new Error("Expected the field `messageRetentionDuration` to be a primitive type in the JSON string but got " + data['messageRetentionDuration']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `pushConfig`
        if (data['pushConfig']) { // data not null
          PushConfig.validateJSON(data['pushConfig']);
        }
        // validate the optional field `retryPolicy`
        if (data['retryPolicy']) { // data not null
          RetryPolicy.validateJSON(data['retryPolicy']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['topic'] && !(typeof data['topic'] === 'string' || data['topic'] instanceof String)) {
            throw new Error("Expected the field `topic` to be a primitive type in the JSON string but got " + data['topic']);
        }
        // ensure the json data is a string
        if (data['topicMessageRetentionDuration'] && !(typeof data['topicMessageRetentionDuration'] === 'string' || data['topicMessageRetentionDuration'] instanceof String)) {
            throw new Error("Expected the field `topicMessageRetentionDuration` to be a primitive type in the JSON string but got " + data['topicMessageRetentionDuration']);
        }

        return true;
    }


}



/**
 * Optional. The approximate amount of time (on a best-effort basis) Pub/Sub waits for the subscriber to acknowledge receipt before resending the message. In the interval after the message is delivered and before it is acknowledged, it is considered to be _outstanding_. During that time period, the message will not be redelivered (on a best-effort basis). For pull subscriptions, this value is used as the initial value for the ack deadline. To override this value for a given message, call `ModifyAckDeadline` with the corresponding `ack_id` if using non-streaming pull or send the `ack_id` in a `StreamingModifyAckDeadlineRequest` if using streaming pull. The minimum custom deadline you can specify is 10 seconds. The maximum custom deadline you can specify is 600 seconds (10 minutes). If this parameter is 0, a default value of 10 seconds is used. For push delivery, this value is also used to set the request timeout for the call to the push endpoint. If the subscriber never acknowledges the message, the Pub/Sub system will eventually redeliver the message.
 * @member {Number} ackDeadlineSeconds
 */
Subscription.prototype['ackDeadlineSeconds'] = undefined;

/**
 * @member {module:model/BigQueryConfig} bigqueryConfig
 */
Subscription.prototype['bigqueryConfig'] = undefined;

/**
 * @member {module:model/CloudStorageConfig} cloudStorageConfig
 */
Subscription.prototype['cloudStorageConfig'] = undefined;

/**
 * @member {module:model/DeadLetterPolicy} deadLetterPolicy
 */
Subscription.prototype['deadLetterPolicy'] = undefined;

/**
 * Optional. Indicates whether the subscription is detached from its topic. Detached subscriptions don't receive messages from their topic and don't retain any backlog. `Pull` and `StreamingPull` requests will return FAILED_PRECONDITION. If the subscription is a push subscription, pushes to the endpoint will not be made.
 * @member {Boolean} detached
 */
Subscription.prototype['detached'] = undefined;

/**
 * Optional. If true, Pub/Sub provides the following guarantees for the delivery of a message with a given value of `message_id` on this subscription: * The message sent to a subscriber is guaranteed not to be resent before the message's acknowledgement deadline expires. * An acknowledged message will not be resent to a subscriber. Note that subscribers may still receive multiple copies of a message when `enable_exactly_once_delivery` is true if the message was published multiple times by a publisher client. These copies are considered distinct by Pub/Sub and have distinct `message_id` values.
 * @member {Boolean} enableExactlyOnceDelivery
 */
Subscription.prototype['enableExactlyOnceDelivery'] = undefined;

/**
 * Optional. If true, messages published with the same `ordering_key` in `PubsubMessage` will be delivered to the subscribers in the order in which they are received by the Pub/Sub system. Otherwise, they may be delivered in any order.
 * @member {Boolean} enableMessageOrdering
 */
Subscription.prototype['enableMessageOrdering'] = undefined;

/**
 * @member {module:model/ExpirationPolicy} expirationPolicy
 */
Subscription.prototype['expirationPolicy'] = undefined;

/**
 * Optional. An expression written in the Pub/Sub [filter language](https://cloud.google.com/pubsub/docs/filtering). If non-empty, then only `PubsubMessage`s whose `attributes` field matches the filter are delivered on this subscription. If empty, then no messages are filtered out.
 * @member {String} filter
 */
Subscription.prototype['filter'] = undefined;

/**
 * Optional. See [Creating and managing labels](https://cloud.google.com/pubsub/docs/labels).
 * @member {Object.<String, String>} labels
 */
Subscription.prototype['labels'] = undefined;

/**
 * Optional. How long to retain unacknowledged messages in the subscription's backlog, from the moment a message is published. If `retain_acked_messages` is true, then this also configures the retention of acknowledged messages, and thus configures how far back in time a `Seek` can be done. Defaults to 7 days. Cannot be more than 7 days or less than 10 minutes.
 * @member {String} messageRetentionDuration
 */
Subscription.prototype['messageRetentionDuration'] = undefined;

/**
 * Required. The name of the subscription. It must have the format `\"projects/{project}/subscriptions/{subscription}\"`. `{subscription}` must start with a letter, and contain only letters (`[A-Za-z]`), numbers (`[0-9]`), dashes (`-`), underscores (`_`), periods (`.`), tildes (`~`), plus (`+`) or percent signs (`%`). It must be between 3 and 255 characters in length, and it must not start with `\"goog\"`.
 * @member {String} name
 */
Subscription.prototype['name'] = undefined;

/**
 * @member {module:model/PushConfig} pushConfig
 */
Subscription.prototype['pushConfig'] = undefined;

/**
 * Optional. Indicates whether to retain acknowledged messages. If true, then messages are not expunged from the subscription's backlog, even if they are acknowledged, until they fall out of the `message_retention_duration` window. This must be true if you would like to [`Seek` to a timestamp] (https://cloud.google.com/pubsub/docs/replay-overview#seek_to_a_time) in the past to replay previously-acknowledged messages.
 * @member {Boolean} retainAckedMessages
 */
Subscription.prototype['retainAckedMessages'] = undefined;

/**
 * @member {module:model/RetryPolicy} retryPolicy
 */
Subscription.prototype['retryPolicy'] = undefined;

/**
 * Output only. An output-only field indicating whether or not the subscription can receive messages.
 * @member {module:model/Subscription.StateEnum} state
 */
Subscription.prototype['state'] = undefined;

/**
 * Required. The name of the topic from which this subscription is receiving messages. Format is `projects/{project}/topics/{topic}`. The value of this field will be `_deleted-topic_` if the topic has been deleted.
 * @member {String} topic
 */
Subscription.prototype['topic'] = undefined;

/**
 * Output only. Indicates the minimum duration for which a message is retained after it is published to the subscription's topic. If this field is set, messages published to the subscription's topic in the last `topic_message_retention_duration` are always available to subscribers. See the `message_retention_duration` field in `Topic`. This field is set only in responses from the server; it is ignored if it is set in any requests.
 * @member {String} topicMessageRetentionDuration
 */
Subscription.prototype['topicMessageRetentionDuration'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
Subscription['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "ACTIVE"
     * @const
     */
    "ACTIVE": "ACTIVE",

    /**
     * value: "RESOURCE_ERROR"
     * @const
     */
    "RESOURCE_ERROR": "RESOURCE_ERROR"
};



export default Subscription;

