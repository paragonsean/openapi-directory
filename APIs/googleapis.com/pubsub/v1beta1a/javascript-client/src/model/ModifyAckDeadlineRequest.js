/**
 * Cloud Pub/Sub API
 * Provides reliable, many-to-many, asynchronous messaging between applications. 
 *
 * The version of the OpenAPI document: v1beta1a
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ModifyAckDeadlineRequest model module.
 * @module model/ModifyAckDeadlineRequest
 * @version v1beta1a
 */
class ModifyAckDeadlineRequest {
    /**
     * Constructs a new <code>ModifyAckDeadlineRequest</code>.
     * Request for the ModifyAckDeadline method.
     * @alias module:model/ModifyAckDeadlineRequest
     */
    constructor() { 
        
        ModifyAckDeadlineRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ModifyAckDeadlineRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ModifyAckDeadlineRequest} obj Optional instance to populate.
     * @return {module:model/ModifyAckDeadlineRequest} The populated <code>ModifyAckDeadlineRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ModifyAckDeadlineRequest();

            if (data.hasOwnProperty('ackDeadlineSeconds')) {
                obj['ackDeadlineSeconds'] = ApiClient.convertToType(data['ackDeadlineSeconds'], 'Number');
            }
            if (data.hasOwnProperty('ackId')) {
                obj['ackId'] = ApiClient.convertToType(data['ackId'], 'String');
            }
            if (data.hasOwnProperty('ackIds')) {
                obj['ackIds'] = ApiClient.convertToType(data['ackIds'], ['String']);
            }
            if (data.hasOwnProperty('subscription')) {
                obj['subscription'] = ApiClient.convertToType(data['subscription'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ModifyAckDeadlineRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ModifyAckDeadlineRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['ackId'] && !(typeof data['ackId'] === 'string' || data['ackId'] instanceof String)) {
            throw new Error("Expected the field `ackId` to be a primitive type in the JSON string but got " + data['ackId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['ackIds'])) {
            throw new Error("Expected the field `ackIds` to be an array in the JSON data but got " + data['ackIds']);
        }
        // ensure the json data is a string
        if (data['subscription'] && !(typeof data['subscription'] === 'string' || data['subscription'] instanceof String)) {
            throw new Error("Expected the field `subscription` to be a primitive type in the JSON string but got " + data['subscription']);
        }

        return true;
    }


}



/**
 * The new ack deadline with respect to the time this request was sent to the Pub/Sub system. Must be >= 0. For example, if the value is 10, the new ack deadline will expire 10 seconds after the ModifyAckDeadline call was made. Specifying zero may immediately make the message available for another pull request.
 * @member {Number} ackDeadlineSeconds
 */
ModifyAckDeadlineRequest.prototype['ackDeadlineSeconds'] = undefined;

/**
 * The acknowledgment ID. Either this or ack_ids must be populated, not both.
 * @member {String} ackId
 */
ModifyAckDeadlineRequest.prototype['ackId'] = undefined;

/**
 * List of acknowledgment IDs. Either this field or ack_id should be populated, not both.
 * @member {Array.<String>} ackIds
 */
ModifyAckDeadlineRequest.prototype['ackIds'] = undefined;

/**
 * Next Index: 5 The name of the subscription from which messages are being pulled.
 * @member {String} subscription
 */
ModifyAckDeadlineRequest.prototype['subscription'] = undefined;






export default ModifyAckDeadlineRequest;

