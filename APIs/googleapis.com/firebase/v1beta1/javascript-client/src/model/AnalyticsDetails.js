/**
 * Firebase Management API
 * The Firebase Management API enables programmatic setup and management of Firebase projects, including a project's Firebase resources and Firebase apps.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AnalyticsProperty from './AnalyticsProperty';
import StreamMapping from './StreamMapping';

/**
 * The AnalyticsDetails model module.
 * @module model/AnalyticsDetails
 * @version v1beta1
 */
class AnalyticsDetails {
    /**
     * Constructs a new <code>AnalyticsDetails</code>.
     * @alias module:model/AnalyticsDetails
     */
    constructor() { 
        
        AnalyticsDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AnalyticsDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AnalyticsDetails} obj Optional instance to populate.
     * @return {module:model/AnalyticsDetails} The populated <code>AnalyticsDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AnalyticsDetails();

            if (data.hasOwnProperty('analyticsProperty')) {
                obj['analyticsProperty'] = AnalyticsProperty.constructFromObject(data['analyticsProperty']);
            }
            if (data.hasOwnProperty('streamMappings')) {
                obj['streamMappings'] = ApiClient.convertToType(data['streamMappings'], [StreamMapping]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AnalyticsDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AnalyticsDetails</code>.
     */
    static validateJSON(data) {
        // validate the optional field `analyticsProperty`
        if (data['analyticsProperty']) { // data not null
          AnalyticsProperty.validateJSON(data['analyticsProperty']);
        }
        if (data['streamMappings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['streamMappings'])) {
                throw new Error("Expected the field `streamMappings` to be an array in the JSON data but got " + data['streamMappings']);
            }
            // validate the optional field `streamMappings` (array)
            for (const item of data['streamMappings']) {
                StreamMapping.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/AnalyticsProperty} analyticsProperty
 */
AnalyticsDetails.prototype['analyticsProperty'] = undefined;

/**
 *  - For `AndroidApps` and `IosApps`: a map of `app` to `streamId` for each Firebase App in the specified `FirebaseProject`. Each `app` and `streamId` appears only once. - For `WebApps`: a map of `app` to `streamId` and `measurementId` for each `WebApp` in the specified `FirebaseProject`. Each `app`, `streamId`, and `measurementId` appears only once.
 * @member {Array.<module:model/StreamMapping>} streamMappings
 */
AnalyticsDetails.prototype['streamMappings'] = undefined;






export default AnalyticsDetails;

