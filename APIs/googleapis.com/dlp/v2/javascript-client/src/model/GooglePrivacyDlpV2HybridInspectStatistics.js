/**
 * Sensitive Data Protection (DLP)
 * Discover and protect your sensitive data. A fully managed service designed to help you discover, classify, and protect your valuable data assets with ease.
 *
 * The version of the OpenAPI document: v2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GooglePrivacyDlpV2HybridInspectStatistics model module.
 * @module model/GooglePrivacyDlpV2HybridInspectStatistics
 * @version v2
 */
class GooglePrivacyDlpV2HybridInspectStatistics {
    /**
     * Constructs a new <code>GooglePrivacyDlpV2HybridInspectStatistics</code>.
     * Statistics related to processing hybrid inspect requests.
     * @alias module:model/GooglePrivacyDlpV2HybridInspectStatistics
     */
    constructor() { 
        
        GooglePrivacyDlpV2HybridInspectStatistics.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GooglePrivacyDlpV2HybridInspectStatistics</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GooglePrivacyDlpV2HybridInspectStatistics} obj Optional instance to populate.
     * @return {module:model/GooglePrivacyDlpV2HybridInspectStatistics} The populated <code>GooglePrivacyDlpV2HybridInspectStatistics</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GooglePrivacyDlpV2HybridInspectStatistics();

            if (data.hasOwnProperty('abortedCount')) {
                obj['abortedCount'] = ApiClient.convertToType(data['abortedCount'], 'String');
            }
            if (data.hasOwnProperty('pendingCount')) {
                obj['pendingCount'] = ApiClient.convertToType(data['pendingCount'], 'String');
            }
            if (data.hasOwnProperty('processedCount')) {
                obj['processedCount'] = ApiClient.convertToType(data['processedCount'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GooglePrivacyDlpV2HybridInspectStatistics</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GooglePrivacyDlpV2HybridInspectStatistics</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['abortedCount'] && !(typeof data['abortedCount'] === 'string' || data['abortedCount'] instanceof String)) {
            throw new Error("Expected the field `abortedCount` to be a primitive type in the JSON string but got " + data['abortedCount']);
        }
        // ensure the json data is a string
        if (data['pendingCount'] && !(typeof data['pendingCount'] === 'string' || data['pendingCount'] instanceof String)) {
            throw new Error("Expected the field `pendingCount` to be a primitive type in the JSON string but got " + data['pendingCount']);
        }
        // ensure the json data is a string
        if (data['processedCount'] && !(typeof data['processedCount'] === 'string' || data['processedCount'] instanceof String)) {
            throw new Error("Expected the field `processedCount` to be a primitive type in the JSON string but got " + data['processedCount']);
        }

        return true;
    }


}



/**
 * The number of hybrid inspection requests aborted because the job ran out of quota or was ended before they could be processed.
 * @member {String} abortedCount
 */
GooglePrivacyDlpV2HybridInspectStatistics.prototype['abortedCount'] = undefined;

/**
 * The number of hybrid requests currently being processed. Only populated when called via method `getDlpJob`. A burst of traffic may cause hybrid inspect requests to be enqueued. Processing will take place as quickly as possible, but resource limitations may impact how long a request is enqueued for.
 * @member {String} pendingCount
 */
GooglePrivacyDlpV2HybridInspectStatistics.prototype['pendingCount'] = undefined;

/**
 * The number of hybrid inspection requests processed within this job.
 * @member {String} processedCount
 */
GooglePrivacyDlpV2HybridInspectStatistics.prototype['processedCount'] = undefined;






export default GooglePrivacyDlpV2HybridInspectStatistics;

