/**
 * GKE Hub API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1alpha2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ResourceManifest from './ResourceManifest';
import ResourceOptions from './ResourceOptions';

/**
 * The KubernetesResource model module.
 * @module model/KubernetesResource
 * @version v1alpha2
 */
class KubernetesResource {
    /**
     * Constructs a new <code>KubernetesResource</code>.
     * KubernetesResource contains the YAML manifests and configuration for Membership Kubernetes resources in the cluster. After CreateMembership or UpdateMembership, these resources should be re-applied in the cluster.
     * @alias module:model/KubernetesResource
     */
    constructor() { 
        
        KubernetesResource.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>KubernetesResource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KubernetesResource} obj Optional instance to populate.
     * @return {module:model/KubernetesResource} The populated <code>KubernetesResource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KubernetesResource();

            if (data.hasOwnProperty('connectResources')) {
                obj['connectResources'] = ApiClient.convertToType(data['connectResources'], [ResourceManifest]);
            }
            if (data.hasOwnProperty('membershipCrManifest')) {
                obj['membershipCrManifest'] = ApiClient.convertToType(data['membershipCrManifest'], 'String');
            }
            if (data.hasOwnProperty('membershipResources')) {
                obj['membershipResources'] = ApiClient.convertToType(data['membershipResources'], [ResourceManifest]);
            }
            if (data.hasOwnProperty('resourceOptions')) {
                obj['resourceOptions'] = ResourceOptions.constructFromObject(data['resourceOptions']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KubernetesResource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KubernetesResource</code>.
     */
    static validateJSON(data) {
        if (data['connectResources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['connectResources'])) {
                throw new Error("Expected the field `connectResources` to be an array in the JSON data but got " + data['connectResources']);
            }
            // validate the optional field `connectResources` (array)
            for (const item of data['connectResources']) {
                ResourceManifest.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['membershipCrManifest'] && !(typeof data['membershipCrManifest'] === 'string' || data['membershipCrManifest'] instanceof String)) {
            throw new Error("Expected the field `membershipCrManifest` to be a primitive type in the JSON string but got " + data['membershipCrManifest']);
        }
        if (data['membershipResources']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['membershipResources'])) {
                throw new Error("Expected the field `membershipResources` to be an array in the JSON data but got " + data['membershipResources']);
            }
            // validate the optional field `membershipResources` (array)
            for (const item of data['membershipResources']) {
                ResourceManifest.validateJSON(item);
            };
        }
        // validate the optional field `resourceOptions`
        if (data['resourceOptions']) { // data not null
          ResourceOptions.validateJSON(data['resourceOptions']);
        }

        return true;
    }


}



/**
 * Output only. The Kubernetes resources for installing the GKE Connect agent. This field is only populated in the Membership returned from a successful long-running operation from CreateMembership or UpdateMembership. It is not populated during normal GetMembership or ListMemberships requests. To get the resource manifest after the initial registration, the caller should make a UpdateMembership call with an empty field mask.
 * @member {Array.<module:model/ResourceManifest>} connectResources
 */
KubernetesResource.prototype['connectResources'] = undefined;

/**
 * Input only. The YAML representation of the Membership CR. This field is ignored for GKE clusters where Hub can read the CR directly. Callers should provide the CR that is currently present in the cluster during Create or Update, or leave this field empty if none exists. The CR manifest is used to validate the cluster has not been registered with another Membership.
 * @member {String} membershipCrManifest
 */
KubernetesResource.prototype['membershipCrManifest'] = undefined;

/**
 * Output only. Additional Kubernetes resources that need to be applied to the cluster after Membership creation, and after every update. This field is only populated in the Membership returned from a successful long-running operation from CreateMembership or UpdateMembership. It is not populated during normal GetMembership or ListMemberships requests. To get the resource manifest after the initial registration, the caller should make a UpdateMembership call with an empty field mask.
 * @member {Array.<module:model/ResourceManifest>} membershipResources
 */
KubernetesResource.prototype['membershipResources'] = undefined;

/**
 * @member {module:model/ResourceOptions} resourceOptions
 */
KubernetesResource.prototype['resourceOptions'] = undefined;






export default KubernetesResource;

