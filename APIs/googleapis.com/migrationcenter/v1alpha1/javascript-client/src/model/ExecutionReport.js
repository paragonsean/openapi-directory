/**
 * Migration Center API
 * A unified platform that helps you accelerate your end-to-end cloud journey from your current on-premises or cloud environments to Google Cloud.
 *
 * The version of the OpenAPI document: v1alpha1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ImportError from './ImportError';
import ValidationReport from './ValidationReport';

/**
 * The ExecutionReport model module.
 * @module model/ExecutionReport
 * @version v1alpha1
 */
class ExecutionReport {
    /**
     * Constructs a new <code>ExecutionReport</code>.
     * A resource that reports result of the import job execution.
     * @alias module:model/ExecutionReport
     */
    constructor() { 
        
        ExecutionReport.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExecutionReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExecutionReport} obj Optional instance to populate.
     * @return {module:model/ExecutionReport} The populated <code>ExecutionReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExecutionReport();

            if (data.hasOwnProperty('executionErrors')) {
                obj['executionErrors'] = ValidationReport.constructFromObject(data['executionErrors']);
            }
            if (data.hasOwnProperty('framesReported')) {
                obj['framesReported'] = ApiClient.convertToType(data['framesReported'], 'Number');
            }
            if (data.hasOwnProperty('jobErrors')) {
                obj['jobErrors'] = ApiClient.convertToType(data['jobErrors'], [ImportError]);
            }
            if (data.hasOwnProperty('totalRowsCount')) {
                obj['totalRowsCount'] = ApiClient.convertToType(data['totalRowsCount'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExecutionReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExecutionReport</code>.
     */
    static validateJSON(data) {
        // validate the optional field `executionErrors`
        if (data['executionErrors']) { // data not null
          ValidationReport.validateJSON(data['executionErrors']);
        }
        if (data['jobErrors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['jobErrors'])) {
                throw new Error("Expected the field `jobErrors` to be an array in the JSON data but got " + data['jobErrors']);
            }
            // validate the optional field `jobErrors` (array)
            for (const item of data['jobErrors']) {
                ImportError.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/ValidationReport} executionErrors
 */
ExecutionReport.prototype['executionErrors'] = undefined;

/**
 * Total number of asset frames reported for the import job.
 * @member {Number} framesReported
 */
ExecutionReport.prototype['framesReported'] = undefined;

/**
 * List of job-level errors. Deprecated, use the job errors under execution_errors instead.
 * @member {Array.<module:model/ImportError>} jobErrors
 */
ExecutionReport.prototype['jobErrors'] = undefined;

/**
 * Total number of rows in the import job.
 * @member {Number} totalRowsCount
 */
ExecutionReport.prototype['totalRowsCount'] = undefined;






export default ExecutionReport;

