/**
 * Cloud Talent Solution API
 * Cloud Talent Solution provides the capability to create, read, update, and delete job postings, as well as search jobs based on keywords and filters. 
 *
 * The version of the OpenAPI document: v4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NamespacedDebugInput model module.
 * @module model/NamespacedDebugInput
 * @version v4
 */
class NamespacedDebugInput {
    /**
     * Constructs a new <code>NamespacedDebugInput</code>.
     * Next ID: 16
     * @alias module:model/NamespacedDebugInput
     */
    constructor() { 
        
        NamespacedDebugInput.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NamespacedDebugInput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NamespacedDebugInput} obj Optional instance to populate.
     * @return {module:model/NamespacedDebugInput} The populated <code>NamespacedDebugInput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NamespacedDebugInput();

            if (data.hasOwnProperty('absolutelyForcedExpNames')) {
                obj['absolutelyForcedExpNames'] = ApiClient.convertToType(data['absolutelyForcedExpNames'], ['String']);
            }
            if (data.hasOwnProperty('absolutelyForcedExpTags')) {
                obj['absolutelyForcedExpTags'] = ApiClient.convertToType(data['absolutelyForcedExpTags'], ['String']);
            }
            if (data.hasOwnProperty('absolutelyForcedExps')) {
                obj['absolutelyForcedExps'] = ApiClient.convertToType(data['absolutelyForcedExps'], ['Number']);
            }
            if (data.hasOwnProperty('conditionallyForcedExpNames')) {
                obj['conditionallyForcedExpNames'] = ApiClient.convertToType(data['conditionallyForcedExpNames'], ['String']);
            }
            if (data.hasOwnProperty('conditionallyForcedExpTags')) {
                obj['conditionallyForcedExpTags'] = ApiClient.convertToType(data['conditionallyForcedExpTags'], ['String']);
            }
            if (data.hasOwnProperty('conditionallyForcedExps')) {
                obj['conditionallyForcedExps'] = ApiClient.convertToType(data['conditionallyForcedExps'], ['Number']);
            }
            if (data.hasOwnProperty('disableAutomaticEnrollmentSelection')) {
                obj['disableAutomaticEnrollmentSelection'] = ApiClient.convertToType(data['disableAutomaticEnrollmentSelection'], 'Boolean');
            }
            if (data.hasOwnProperty('disableExpNames')) {
                obj['disableExpNames'] = ApiClient.convertToType(data['disableExpNames'], ['String']);
            }
            if (data.hasOwnProperty('disableExpTags')) {
                obj['disableExpTags'] = ApiClient.convertToType(data['disableExpTags'], ['String']);
            }
            if (data.hasOwnProperty('disableExps')) {
                obj['disableExps'] = ApiClient.convertToType(data['disableExps'], ['Number']);
            }
            if (data.hasOwnProperty('disableManualEnrollmentSelection')) {
                obj['disableManualEnrollmentSelection'] = ApiClient.convertToType(data['disableManualEnrollmentSelection'], 'Boolean');
            }
            if (data.hasOwnProperty('disableOrganicSelection')) {
                obj['disableOrganicSelection'] = ApiClient.convertToType(data['disableOrganicSelection'], 'Boolean');
            }
            if (data.hasOwnProperty('forcedFlags')) {
                obj['forcedFlags'] = ApiClient.convertToType(data['forcedFlags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('forcedRollouts')) {
                obj['forcedRollouts'] = ApiClient.convertToType(data['forcedRollouts'], {'String': 'Boolean'});
            }
            if (data.hasOwnProperty('testingMode')) {
                obj['testingMode'] = ApiClient.convertToType(data['testingMode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NamespacedDebugInput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NamespacedDebugInput</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['absolutelyForcedExpNames'])) {
            throw new Error("Expected the field `absolutelyForcedExpNames` to be an array in the JSON data but got " + data['absolutelyForcedExpNames']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['absolutelyForcedExpTags'])) {
            throw new Error("Expected the field `absolutelyForcedExpTags` to be an array in the JSON data but got " + data['absolutelyForcedExpTags']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['absolutelyForcedExps'])) {
            throw new Error("Expected the field `absolutelyForcedExps` to be an array in the JSON data but got " + data['absolutelyForcedExps']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['conditionallyForcedExpNames'])) {
            throw new Error("Expected the field `conditionallyForcedExpNames` to be an array in the JSON data but got " + data['conditionallyForcedExpNames']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['conditionallyForcedExpTags'])) {
            throw new Error("Expected the field `conditionallyForcedExpTags` to be an array in the JSON data but got " + data['conditionallyForcedExpTags']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['conditionallyForcedExps'])) {
            throw new Error("Expected the field `conditionallyForcedExps` to be an array in the JSON data but got " + data['conditionallyForcedExps']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['disableExpNames'])) {
            throw new Error("Expected the field `disableExpNames` to be an array in the JSON data but got " + data['disableExpNames']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['disableExpTags'])) {
            throw new Error("Expected the field `disableExpTags` to be an array in the JSON data but got " + data['disableExpTags']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['disableExps'])) {
            throw new Error("Expected the field `disableExps` to be an array in the JSON data but got " + data['disableExps']);
        }
        // ensure the json data is a string
        if (data['testingMode'] && !(typeof data['testingMode'] === 'string' || data['testingMode'] instanceof String)) {
            throw new Error("Expected the field `testingMode` to be a primitive type in the JSON string but got " + data['testingMode']);
        }

        return true;
    }


}



/**
 * Set of experiment names to be absolutely forced. These experiments will be forced without evaluating the conditions.
 * @member {Array.<String>} absolutelyForcedExpNames
 */
NamespacedDebugInput.prototype['absolutelyForcedExpNames'] = undefined;

/**
 * Set of experiment tags to be absolutely forced. The experiments with these tags will be forced without evaluating the conditions.
 * @member {Array.<String>} absolutelyForcedExpTags
 */
NamespacedDebugInput.prototype['absolutelyForcedExpTags'] = undefined;

/**
 * Set of experiment ids to be absolutely forced. These ids will be forced without evaluating the conditions.
 * @member {Array.<Number>} absolutelyForcedExps
 */
NamespacedDebugInput.prototype['absolutelyForcedExps'] = undefined;

/**
 * Set of experiment names to be conditionally forced. These experiments will be forced only if their conditions and their parent domain's conditions are true.
 * @member {Array.<String>} conditionallyForcedExpNames
 */
NamespacedDebugInput.prototype['conditionallyForcedExpNames'] = undefined;

/**
 * Set of experiment tags to be conditionally forced. The experiments with these tags will be forced only if their conditions and their parent domain's conditions are true.
 * @member {Array.<String>} conditionallyForcedExpTags
 */
NamespacedDebugInput.prototype['conditionallyForcedExpTags'] = undefined;

/**
 * Set of experiment ids to be conditionally forced. These ids will be forced only if their conditions and their parent domain's conditions are true.
 * @member {Array.<Number>} conditionallyForcedExps
 */
NamespacedDebugInput.prototype['conditionallyForcedExps'] = undefined;

/**
 * If true, disable automatic enrollment selection (at all diversion points). Automatic enrollment selection means experiment selection process based on the experiment's automatic enrollment condition. This does not disable selection of forced experiments. Setting this field to false does not change anything in the experiment selection process.
 * @member {Boolean} disableAutomaticEnrollmentSelection
 */
NamespacedDebugInput.prototype['disableAutomaticEnrollmentSelection'] = undefined;

/**
 * Set of experiment names to be disabled. If an experiment is disabled, it is never selected nor forced. If an aggregate experiment is disabled, its partitions are disabled together. If an experiment with an enrollment is disabled, the enrollment is disabled together. If a name corresponds to a domain, the domain itself and all descendant experiments and domains are disabled together.
 * @member {Array.<String>} disableExpNames
 */
NamespacedDebugInput.prototype['disableExpNames'] = undefined;

/**
 * Set of experiment tags to be disabled. All experiments that are tagged with one or more of these tags are disabled. If an experiment is disabled, it is never selected nor forced. If an aggregate experiment is disabled, its partitions are disabled together. If an experiment with an enrollment is disabled, the enrollment is disabled together.
 * @member {Array.<String>} disableExpTags
 */
NamespacedDebugInput.prototype['disableExpTags'] = undefined;

/**
 * Set of experiment ids to be disabled. If an experiment is disabled, it is never selected nor forced. If an aggregate experiment is disabled, its partitions are disabled together. If an experiment with an enrollment is disabled, the enrollment is disabled together. If an ID corresponds to a domain, the domain itself and all descendant experiments and domains are disabled together.
 * @member {Array.<Number>} disableExps
 */
NamespacedDebugInput.prototype['disableExps'] = undefined;

/**
 * If true, disable manual enrollment selection (at all diversion points). Manual enrollment selection means experiment selection process based on the request's manual enrollment states (a.k.a. opt-in experiments). This does not disable selection of forced experiments. Setting this field to false does not change anything in the experiment selection process.
 * @member {Boolean} disableManualEnrollmentSelection
 */
NamespacedDebugInput.prototype['disableManualEnrollmentSelection'] = undefined;

/**
 * If true, disable organic experiment selection (at all diversion points). Organic selection means experiment selection process based on traffic allocation and diversion condition evaluation. This does not disable selection of forced experiments. This is useful in cases when it is not known whether experiment selection behavior is responsible for a error or breakage. Disabling organic selection may help to isolate the cause of a given problem. Setting this field to false does not change anything in the experiment selection process.
 * @member {Boolean} disableOrganicSelection
 */
NamespacedDebugInput.prototype['disableOrganicSelection'] = undefined;

/**
 * Flags to force in a particular experiment state. Map from flag name to flag value.
 * @member {Object.<String, String>} forcedFlags
 */
NamespacedDebugInput.prototype['forcedFlags'] = undefined;

/**
 * Rollouts to force in a particular experiment state. Map from rollout name to rollout value.
 * @member {Object.<String, Boolean>} forcedRollouts
 */
NamespacedDebugInput.prototype['forcedRollouts'] = undefined;

/**
 * Sets different testing modes. See the documentation in the TestingMode message for more information.
 * @member {module:model/NamespacedDebugInput.TestingModeEnum} testingMode
 */
NamespacedDebugInput.prototype['testingMode'] = undefined;





/**
 * Allowed values for the <code>testingMode</code> property.
 * @enum {String}
 * @readonly
 */
NamespacedDebugInput['TestingModeEnum'] = {

    /**
     * value: "TESTING_MODE_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "TESTING_MODE_UNSPECIFIED",

    /**
     * value: "TESTING_MODE_ALL_OFF"
     * @const
     */
    "ALL_OFF": "TESTING_MODE_ALL_OFF",

    /**
     * value: "TESTING_MODE_ALL_ON"
     * @const
     */
    "ALL_ON": "TESTING_MODE_ALL_ON"
};



export default NamespacedDebugInput;

