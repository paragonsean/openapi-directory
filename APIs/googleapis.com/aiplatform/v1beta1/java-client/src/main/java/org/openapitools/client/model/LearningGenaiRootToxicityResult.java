/*
 * Vertex AI API
 * Train high-quality custom machine learning models with minimal machine learning expertise and effort.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.LearningGenaiRootToxicitySignal;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * A model can generate multiple signals and this captures all the generated signals for a single message.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:33:30.041358-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class LearningGenaiRootToxicityResult {
  public static final String SERIALIZED_NAME_SIGNALS = "signals";
  @SerializedName(SERIALIZED_NAME_SIGNALS)
  private List<LearningGenaiRootToxicitySignal> signals = new ArrayList<>();

  public LearningGenaiRootToxicityResult() {
  }

  public LearningGenaiRootToxicityResult signals(List<LearningGenaiRootToxicitySignal> signals) {
    this.signals = signals;
    return this;
  }

  public LearningGenaiRootToxicityResult addSignalsItem(LearningGenaiRootToxicitySignal signalsItem) {
    if (this.signals == null) {
      this.signals = new ArrayList<>();
    }
    this.signals.add(signalsItem);
    return this;
  }

  /**
   * Get signals
   * @return signals
   */
  @javax.annotation.Nullable
  public List<LearningGenaiRootToxicitySignal> getSignals() {
    return signals;
  }

  public void setSignals(List<LearningGenaiRootToxicitySignal> signals) {
    this.signals = signals;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    LearningGenaiRootToxicityResult learningGenaiRootToxicityResult = (LearningGenaiRootToxicityResult) o;
    return Objects.equals(this.signals, learningGenaiRootToxicityResult.signals);
  }

  @Override
  public int hashCode() {
    return Objects.hash(signals);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class LearningGenaiRootToxicityResult {\n");
    sb.append("    signals: ").append(toIndentedString(signals)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("signals");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to LearningGenaiRootToxicityResult
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!LearningGenaiRootToxicityResult.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in LearningGenaiRootToxicityResult is not found in the empty JSON string", LearningGenaiRootToxicityResult.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!LearningGenaiRootToxicityResult.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `LearningGenaiRootToxicityResult` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("signals") != null && !jsonObj.get("signals").isJsonNull()) {
        JsonArray jsonArraysignals = jsonObj.getAsJsonArray("signals");
        if (jsonArraysignals != null) {
          // ensure the json data is an array
          if (!jsonObj.get("signals").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `signals` to be an array in the JSON string but got `%s`", jsonObj.get("signals").toString()));
          }

          // validate the optional field `signals` (array)
          for (int i = 0; i < jsonArraysignals.size(); i++) {
            LearningGenaiRootToxicitySignal.validateJsonElement(jsonArraysignals.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!LearningGenaiRootToxicityResult.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'LearningGenaiRootToxicityResult' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<LearningGenaiRootToxicityResult> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(LearningGenaiRootToxicityResult.class));

       return (TypeAdapter<T>) new TypeAdapter<LearningGenaiRootToxicityResult>() {
           @Override
           public void write(JsonWriter out, LearningGenaiRootToxicityResult value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public LearningGenaiRootToxicityResult read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of LearningGenaiRootToxicityResult given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of LearningGenaiRootToxicityResult
   * @throws IOException if the JSON string is invalid with respect to LearningGenaiRootToxicityResult
   */
  public static LearningGenaiRootToxicityResult fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, LearningGenaiRootToxicityResult.class);
  }

  /**
   * Convert an instance of LearningGenaiRootToxicityResult to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

