/**
 * Calendar API
 * Manipulates events and other calendar data.
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CalendarListEntry from './CalendarListEntry';

/**
 * The CalendarList model module.
 * @module model/CalendarList
 * @version v3
 */
class CalendarList {
    /**
     * Constructs a new <code>CalendarList</code>.
     * @alias module:model/CalendarList
     */
    constructor() { 
        
        CalendarList.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['kind'] = 'calendar#calendarList';
    }

    /**
     * Constructs a <code>CalendarList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CalendarList} obj Optional instance to populate.
     * @return {module:model/CalendarList} The populated <code>CalendarList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CalendarList();

            if (data.hasOwnProperty('etag')) {
                obj['etag'] = ApiClient.convertToType(data['etag'], 'String');
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [CalendarListEntry]);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('nextSyncToken')) {
                obj['nextSyncToken'] = ApiClient.convertToType(data['nextSyncToken'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CalendarList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CalendarList</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['etag'] && !(typeof data['etag'] === 'string' || data['etag'] instanceof String)) {
            throw new Error("Expected the field `etag` to be a primitive type in the JSON string but got " + data['etag']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                CalendarListEntry.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        // ensure the json data is a string
        if (data['nextSyncToken'] && !(typeof data['nextSyncToken'] === 'string' || data['nextSyncToken'] instanceof String)) {
            throw new Error("Expected the field `nextSyncToken` to be a primitive type in the JSON string but got " + data['nextSyncToken']);
        }

        return true;
    }


}



/**
 * ETag of the collection.
 * @member {String} etag
 */
CalendarList.prototype['etag'] = undefined;

/**
 * Calendars that are present on the user's calendar list.
 * @member {Array.<module:model/CalendarListEntry>} items
 */
CalendarList.prototype['items'] = undefined;

/**
 * Type of the collection (\"calendar#calendarList\").
 * @member {String} kind
 * @default 'calendar#calendarList'
 */
CalendarList.prototype['kind'] = 'calendar#calendarList';

/**
 * Token used to access the next page of this result. Omitted if no further results are available, in which case nextSyncToken is provided.
 * @member {String} nextPageToken
 */
CalendarList.prototype['nextPageToken'] = undefined;

/**
 * Token used at a later point in time to retrieve only the entries that have changed since this result was returned. Omitted if further results are available, in which case nextPageToken is provided.
 * @member {String} nextSyncToken
 */
CalendarList.prototype['nextSyncToken'] = undefined;






export default CalendarList;

