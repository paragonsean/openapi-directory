/**
 * Display & Video 360 API
 * Display & Video 360 API allows users to automate complex Display & Video 360 workflows, such as creating insertion orders and setting targeting options for individual line items.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExitEvent model module.
 * @module model/ExitEvent
 * @version v1
 */
class ExitEvent {
    /**
     * Constructs a new <code>ExitEvent</code>.
     * Exit event of the creative.
     * @alias module:model/ExitEvent
     */
    constructor() { 
        
        ExitEvent.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExitEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExitEvent} obj Optional instance to populate.
     * @return {module:model/ExitEvent} The populated <code>ExitEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExitEvent();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('reportingName')) {
                obj['reportingName'] = ApiClient.convertToType(data['reportingName'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExitEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExitEvent</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['reportingName'] && !(typeof data['reportingName'] === 'string' || data['reportingName'] instanceof String)) {
            throw new Error("Expected the field `reportingName` to be a primitive type in the JSON string but got " + data['reportingName']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}



/**
 * The name of the click tag of the exit event. The name must be unique within one creative. Leave it empty or unset for creatives containing image assets only.
 * @member {String} name
 */
ExitEvent.prototype['name'] = undefined;

/**
 * The name used to identify this event in reports. Leave it empty or unset for creatives containing image assets only.
 * @member {String} reportingName
 */
ExitEvent.prototype['reportingName'] = undefined;

/**
 * Required. The type of the exit event.
 * @member {module:model/ExitEvent.TypeEnum} type
 */
ExitEvent.prototype['type'] = undefined;

/**
 * Required. The click through URL of the exit event. This is required when type is: * `EXIT_EVENT_TYPE_DEFAULT` * `EXIT_EVENT_TYPE_BACKUP`
 * @member {String} url
 */
ExitEvent.prototype['url'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ExitEvent['TypeEnum'] = {

    /**
     * value: "EXIT_EVENT_TYPE_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "EXIT_EVENT_TYPE_UNSPECIFIED",

    /**
     * value: "EXIT_EVENT_TYPE_DEFAULT"
     * @const
     */
    "DEFAULT": "EXIT_EVENT_TYPE_DEFAULT",

    /**
     * value: "EXIT_EVENT_TYPE_BACKUP"
     * @const
     */
    "BACKUP": "EXIT_EVENT_TYPE_BACKUP"
};



export default ExitEvent;

