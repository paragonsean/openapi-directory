/**
 * Cloud Deploy API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CloudRunConfig model module.
 * @module model/CloudRunConfig
 * @version v1
 */
class CloudRunConfig {
    /**
     * Constructs a new <code>CloudRunConfig</code>.
     * CloudRunConfig contains the Cloud Run runtime configuration.
     * @alias module:model/CloudRunConfig
     */
    constructor() { 
        
        CloudRunConfig.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CloudRunConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CloudRunConfig} obj Optional instance to populate.
     * @return {module:model/CloudRunConfig} The populated <code>CloudRunConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CloudRunConfig();

            if (data.hasOwnProperty('automaticTrafficControl')) {
                obj['automaticTrafficControl'] = ApiClient.convertToType(data['automaticTrafficControl'], 'Boolean');
            }
            if (data.hasOwnProperty('canaryRevisionTags')) {
                obj['canaryRevisionTags'] = ApiClient.convertToType(data['canaryRevisionTags'], ['String']);
            }
            if (data.hasOwnProperty('priorRevisionTags')) {
                obj['priorRevisionTags'] = ApiClient.convertToType(data['priorRevisionTags'], ['String']);
            }
            if (data.hasOwnProperty('stableRevisionTags')) {
                obj['stableRevisionTags'] = ApiClient.convertToType(data['stableRevisionTags'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CloudRunConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CloudRunConfig</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['canaryRevisionTags'])) {
            throw new Error("Expected the field `canaryRevisionTags` to be an array in the JSON data but got " + data['canaryRevisionTags']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['priorRevisionTags'])) {
            throw new Error("Expected the field `priorRevisionTags` to be an array in the JSON data but got " + data['priorRevisionTags']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['stableRevisionTags'])) {
            throw new Error("Expected the field `stableRevisionTags` to be an array in the JSON data but got " + data['stableRevisionTags']);
        }

        return true;
    }


}



/**
 * Whether Cloud Deploy should update the traffic stanza in a Cloud Run Service on the user's behalf to facilitate traffic splitting. This is required to be true for CanaryDeployments, but optional for CustomCanaryDeployments.
 * @member {Boolean} automaticTrafficControl
 */
CloudRunConfig.prototype['automaticTrafficControl'] = undefined;

/**
 * Optional. A list of tags that are added to the canary revision while the canary phase is in progress.
 * @member {Array.<String>} canaryRevisionTags
 */
CloudRunConfig.prototype['canaryRevisionTags'] = undefined;

/**
 * Optional. A list of tags that are added to the prior revision while the canary phase is in progress.
 * @member {Array.<String>} priorRevisionTags
 */
CloudRunConfig.prototype['priorRevisionTags'] = undefined;

/**
 * Optional. A list of tags that are added to the final stable revision when the stable phase is applied.
 * @member {Array.<String>} stableRevisionTags
 */
CloudRunConfig.prototype['stableRevisionTags'] = undefined;






export default CloudRunConfig;

