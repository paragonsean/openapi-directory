/**
 * Cloud Deploy API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RepairPhase from './RepairPhase';

/**
 * The RepairRolloutOperation model module.
 * @module model/RepairRolloutOperation
 * @version v1
 */
class RepairRolloutOperation {
    /**
     * Constructs a new <code>RepairRolloutOperation</code>.
     * Contains the information for an automated &#x60;repair rollout&#x60; operation.
     * @alias module:model/RepairRolloutOperation
     */
    constructor() { 
        
        RepairRolloutOperation.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RepairRolloutOperation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RepairRolloutOperation} obj Optional instance to populate.
     * @return {module:model/RepairRolloutOperation} The populated <code>RepairRolloutOperation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RepairRolloutOperation();

            if (data.hasOwnProperty('currentRepairModeIndex')) {
                obj['currentRepairModeIndex'] = ApiClient.convertToType(data['currentRepairModeIndex'], 'String');
            }
            if (data.hasOwnProperty('jobId')) {
                obj['jobId'] = ApiClient.convertToType(data['jobId'], 'String');
            }
            if (data.hasOwnProperty('phaseId')) {
                obj['phaseId'] = ApiClient.convertToType(data['phaseId'], 'String');
            }
            if (data.hasOwnProperty('repairPhases')) {
                obj['repairPhases'] = ApiClient.convertToType(data['repairPhases'], [RepairPhase]);
            }
            if (data.hasOwnProperty('rollout')) {
                obj['rollout'] = ApiClient.convertToType(data['rollout'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RepairRolloutOperation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RepairRolloutOperation</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['currentRepairModeIndex'] && !(typeof data['currentRepairModeIndex'] === 'string' || data['currentRepairModeIndex'] instanceof String)) {
            throw new Error("Expected the field `currentRepairModeIndex` to be a primitive type in the JSON string but got " + data['currentRepairModeIndex']);
        }
        // ensure the json data is a string
        if (data['jobId'] && !(typeof data['jobId'] === 'string' || data['jobId'] instanceof String)) {
            throw new Error("Expected the field `jobId` to be a primitive type in the JSON string but got " + data['jobId']);
        }
        // ensure the json data is a string
        if (data['phaseId'] && !(typeof data['phaseId'] === 'string' || data['phaseId'] instanceof String)) {
            throw new Error("Expected the field `phaseId` to be a primitive type in the JSON string but got " + data['phaseId']);
        }
        if (data['repairPhases']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['repairPhases'])) {
                throw new Error("Expected the field `repairPhases` to be an array in the JSON data but got " + data['repairPhases']);
            }
            // validate the optional field `repairPhases` (array)
            for (const item of data['repairPhases']) {
                RepairPhase.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['rollout'] && !(typeof data['rollout'] === 'string' || data['rollout'] instanceof String)) {
            throw new Error("Expected the field `rollout` to be a primitive type in the JSON string but got " + data['rollout']);
        }

        return true;
    }


}



/**
 * Output only. The index of the current repair action in the repair sequence.
 * @member {String} currentRepairModeIndex
 */
RepairRolloutOperation.prototype['currentRepairModeIndex'] = undefined;

/**
 * Output only. The job ID for the Job to repair.
 * @member {String} jobId
 */
RepairRolloutOperation.prototype['jobId'] = undefined;

/**
 * Output only. The phase ID of the phase that includes the job being repaired.
 * @member {String} phaseId
 */
RepairRolloutOperation.prototype['phaseId'] = undefined;

/**
 * Output only. Records of the repair attempts. Each repair phase may have multiple retry attempts or single rollback attempt.
 * @member {Array.<module:model/RepairPhase>} repairPhases
 */
RepairRolloutOperation.prototype['repairPhases'] = undefined;

/**
 * Output only. The name of the rollout that initiates the `AutomationRun`.
 * @member {String} rollout
 */
RepairRolloutOperation.prototype['rollout'] = undefined;






export default RepairRolloutOperation;

