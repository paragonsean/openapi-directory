/**
 * Cloud Deploy API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChildRolloutJobs from './ChildRolloutJobs';
import DeploymentJobs from './DeploymentJobs';

/**
 * The Phase model module.
 * @module model/Phase
 * @version v1
 */
class Phase {
    /**
     * Constructs a new <code>Phase</code>.
     * Phase represents a collection of jobs that are logically grouped together for a &#x60;Rollout&#x60;.
     * @alias module:model/Phase
     */
    constructor() { 
        
        Phase.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Phase</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Phase} obj Optional instance to populate.
     * @return {module:model/Phase} The populated <code>Phase</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Phase();

            if (data.hasOwnProperty('childRolloutJobs')) {
                obj['childRolloutJobs'] = ChildRolloutJobs.constructFromObject(data['childRolloutJobs']);
            }
            if (data.hasOwnProperty('deploymentJobs')) {
                obj['deploymentJobs'] = DeploymentJobs.constructFromObject(data['deploymentJobs']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('skipMessage')) {
                obj['skipMessage'] = ApiClient.convertToType(data['skipMessage'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Phase</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Phase</code>.
     */
    static validateJSON(data) {
        // validate the optional field `childRolloutJobs`
        if (data['childRolloutJobs']) { // data not null
          ChildRolloutJobs.validateJSON(data['childRolloutJobs']);
        }
        // validate the optional field `deploymentJobs`
        if (data['deploymentJobs']) { // data not null
          DeploymentJobs.validateJSON(data['deploymentJobs']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['skipMessage'] && !(typeof data['skipMessage'] === 'string' || data['skipMessage'] instanceof String)) {
            throw new Error("Expected the field `skipMessage` to be a primitive type in the JSON string but got " + data['skipMessage']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}



/**
 * @member {module:model/ChildRolloutJobs} childRolloutJobs
 */
Phase.prototype['childRolloutJobs'] = undefined;

/**
 * @member {module:model/DeploymentJobs} deploymentJobs
 */
Phase.prototype['deploymentJobs'] = undefined;

/**
 * Output only. The ID of the Phase.
 * @member {String} id
 */
Phase.prototype['id'] = undefined;

/**
 * Output only. Additional information on why the Phase was skipped, if available.
 * @member {String} skipMessage
 */
Phase.prototype['skipMessage'] = undefined;

/**
 * Output only. Current state of the Phase.
 * @member {module:model/Phase.StateEnum} state
 */
Phase.prototype['state'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
Phase['StateEnum'] = {

    /**
     * value: "STATE_UNSPECIFIED"
     * @const
     */
    "STATE_UNSPECIFIED": "STATE_UNSPECIFIED",

    /**
     * value: "PENDING"
     * @const
     */
    "PENDING": "PENDING",

    /**
     * value: "IN_PROGRESS"
     * @const
     */
    "IN_PROGRESS": "IN_PROGRESS",

    /**
     * value: "SUCCEEDED"
     * @const
     */
    "SUCCEEDED": "SUCCEEDED",

    /**
     * value: "FAILED"
     * @const
     */
    "FAILED": "FAILED",

    /**
     * value: "ABORTED"
     * @const
     */
    "ABORTED": "ABORTED",

    /**
     * value: "SKIPPED"
     * @const
     */
    "SKIPPED": "SKIPPED"
};



export default Phase;

