/**
 * Cloud Deploy API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RollbackAttempt model module.
 * @module model/RollbackAttempt
 * @version v1
 */
class RollbackAttempt {
    /**
     * Constructs a new <code>RollbackAttempt</code>.
     * RollbackAttempt represents an action of rolling back a Cloud Deploy &#39;Target&#39;.
     * @alias module:model/RollbackAttempt
     */
    constructor() { 
        
        RollbackAttempt.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RollbackAttempt</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RollbackAttempt} obj Optional instance to populate.
     * @return {module:model/RollbackAttempt} The populated <code>RollbackAttempt</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RollbackAttempt();

            if (data.hasOwnProperty('destinationPhase')) {
                obj['destinationPhase'] = ApiClient.convertToType(data['destinationPhase'], 'String');
            }
            if (data.hasOwnProperty('rolloutId')) {
                obj['rolloutId'] = ApiClient.convertToType(data['rolloutId'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('stateDesc')) {
                obj['stateDesc'] = ApiClient.convertToType(data['stateDesc'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RollbackAttempt</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RollbackAttempt</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['destinationPhase'] && !(typeof data['destinationPhase'] === 'string' || data['destinationPhase'] instanceof String)) {
            throw new Error("Expected the field `destinationPhase` to be a primitive type in the JSON string but got " + data['destinationPhase']);
        }
        // ensure the json data is a string
        if (data['rolloutId'] && !(typeof data['rolloutId'] === 'string' || data['rolloutId'] instanceof String)) {
            throw new Error("Expected the field `rolloutId` to be a primitive type in the JSON string but got " + data['rolloutId']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['stateDesc'] && !(typeof data['stateDesc'] === 'string' || data['stateDesc'] instanceof String)) {
            throw new Error("Expected the field `stateDesc` to be a primitive type in the JSON string but got " + data['stateDesc']);
        }

        return true;
    }


}



/**
 * Output only. The phase to which the rollout will be rolled back to.
 * @member {String} destinationPhase
 */
RollbackAttempt.prototype['destinationPhase'] = undefined;

/**
 * Output only. ID of the rollback `Rollout` to create.
 * @member {String} rolloutId
 */
RollbackAttempt.prototype['rolloutId'] = undefined;

/**
 * Output only. Valid state of this rollback action.
 * @member {module:model/RollbackAttempt.StateEnum} state
 */
RollbackAttempt.prototype['state'] = undefined;

/**
 * Output only. Description of the state of the Rollback.
 * @member {String} stateDesc
 */
RollbackAttempt.prototype['stateDesc'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
RollbackAttempt['StateEnum'] = {

    /**
     * value: "REPAIR_STATE_UNSPECIFIED"
     * @const
     */
    "UNSPECIFIED": "REPAIR_STATE_UNSPECIFIED",

    /**
     * value: "REPAIR_STATE_SUCCEEDED"
     * @const
     */
    "SUCCEEDED": "REPAIR_STATE_SUCCEEDED",

    /**
     * value: "REPAIR_STATE_CANCELLED"
     * @const
     */
    "CANCELLED": "REPAIR_STATE_CANCELLED",

    /**
     * value: "REPAIR_STATE_FAILED"
     * @const
     */
    "FAILED": "REPAIR_STATE_FAILED",

    /**
     * value: "REPAIR_STATE_IN_PROGRESS"
     * @const
     */
    "IN_PROGRESS": "REPAIR_STATE_IN_PROGRESS",

    /**
     * value: "REPAIR_STATE_PENDING"
     * @const
     */
    "PENDING": "REPAIR_STATE_PENDING",

    /**
     * value: "REPAIR_STATE_SKIPPED"
     * @const
     */
    "SKIPPED": "REPAIR_STATE_SKIPPED",

    /**
     * value: "REPAIR_STATE_ABORTED"
     * @const
     */
    "ABORTED": "REPAIR_STATE_ABORTED"
};



export default RollbackAttempt;

