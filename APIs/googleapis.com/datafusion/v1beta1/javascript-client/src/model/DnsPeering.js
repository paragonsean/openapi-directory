/**
 * Cloud Data Fusion API
 * Cloud Data Fusion is a fully-managed, cloud native, enterprise data integration service for quickly building and managing data pipelines. It provides a graphical interface to increase time efficiency and reduce complexity, and allows business users, developers, and data scientists to easily and reliably build scalable data integration solutions to cleanse, prepare, blend, transfer and transform data without having to wrestle with infrastructure.
 *
 * The version of the OpenAPI document: v1beta1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DnsPeering model module.
 * @module model/DnsPeering
 * @version v1beta1
 */
class DnsPeering {
    /**
     * Constructs a new <code>DnsPeering</code>.
     * DNS peering configuration. These configurations are used to create DNS peering with the customer Cloud DNS.
     * @alias module:model/DnsPeering
     */
    constructor() { 
        
        DnsPeering.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DnsPeering</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DnsPeering} obj Optional instance to populate.
     * @return {module:model/DnsPeering} The populated <code>DnsPeering</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DnsPeering();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('domain')) {
                obj['domain'] = ApiClient.convertToType(data['domain'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('targetNetwork')) {
                obj['targetNetwork'] = ApiClient.convertToType(data['targetNetwork'], 'String');
            }
            if (data.hasOwnProperty('targetProject')) {
                obj['targetProject'] = ApiClient.convertToType(data['targetProject'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DnsPeering</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DnsPeering</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['domain'] && !(typeof data['domain'] === 'string' || data['domain'] instanceof String)) {
            throw new Error("Expected the field `domain` to be a primitive type in the JSON string but got " + data['domain']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['targetNetwork'] && !(typeof data['targetNetwork'] === 'string' || data['targetNetwork'] instanceof String)) {
            throw new Error("Expected the field `targetNetwork` to be a primitive type in the JSON string but got " + data['targetNetwork']);
        }
        // ensure the json data is a string
        if (data['targetProject'] && !(typeof data['targetProject'] === 'string' || data['targetProject'] instanceof String)) {
            throw new Error("Expected the field `targetProject` to be a primitive type in the JSON string but got " + data['targetProject']);
        }

        return true;
    }


}



/**
 * Optional. Optional description of the dns zone.
 * @member {String} description
 */
DnsPeering.prototype['description'] = undefined;

/**
 * Required. The dns name suffix of the zone.
 * @member {String} domain
 */
DnsPeering.prototype['domain'] = undefined;

/**
 * Required. The resource name of the dns peering zone. Format: projects/{project}/locations/{location}/instances/{instance}/dnsPeerings/{dns_peering}
 * @member {String} name
 */
DnsPeering.prototype['name'] = undefined;

/**
 * Optional. Optional target network to which dns peering should happen.
 * @member {String} targetNetwork
 */
DnsPeering.prototype['targetNetwork'] = undefined;

/**
 * Optional. Optional target project to which dns peering should happen.
 * @member {String} targetProject
 */
DnsPeering.prototype['targetProject'] = undefined;






export default DnsPeering;

