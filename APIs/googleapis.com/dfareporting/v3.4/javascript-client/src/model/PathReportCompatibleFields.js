/**
 * Campaign Manager 360 API
 * Build applications to efficiently manage large or complex trafficking, reporting, and attribution workflows for Campaign Manager 360.
 *
 * The version of the OpenAPI document: v3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Dimension from './Dimension';
import Metric from './Metric';

/**
 * The PathReportCompatibleFields model module.
 * @module model/PathReportCompatibleFields
 * @version v3.4
 */
class PathReportCompatibleFields {
    /**
     * Constructs a new <code>PathReportCompatibleFields</code>.
     * Represents fields that are compatible to be selected for a report of type \&quot;PATH\&quot;.
     * @alias module:model/PathReportCompatibleFields
     */
    constructor() { 
        
        PathReportCompatibleFields.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PathReportCompatibleFields</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PathReportCompatibleFields} obj Optional instance to populate.
     * @return {module:model/PathReportCompatibleFields} The populated <code>PathReportCompatibleFields</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PathReportCompatibleFields();

            if (data.hasOwnProperty('channelGroupings')) {
                obj['channelGroupings'] = ApiClient.convertToType(data['channelGroupings'], [Dimension]);
            }
            if (data.hasOwnProperty('dimensions')) {
                obj['dimensions'] = ApiClient.convertToType(data['dimensions'], [Dimension]);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [Metric]);
            }
            if (data.hasOwnProperty('pathFilters')) {
                obj['pathFilters'] = ApiClient.convertToType(data['pathFilters'], [Dimension]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PathReportCompatibleFields</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PathReportCompatibleFields</code>.
     */
    static validateJSON(data) {
        if (data['channelGroupings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['channelGroupings'])) {
                throw new Error("Expected the field `channelGroupings` to be an array in the JSON data but got " + data['channelGroupings']);
            }
            // validate the optional field `channelGroupings` (array)
            for (const item of data['channelGroupings']) {
                Dimension.validateJSON(item);
            };
        }
        if (data['dimensions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensions'])) {
                throw new Error("Expected the field `dimensions` to be an array in the JSON data but got " + data['dimensions']);
            }
            // validate the optional field `dimensions` (array)
            for (const item of data['dimensions']) {
                Dimension.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                Metric.validateJSON(item);
            };
        }
        if (data['pathFilters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pathFilters'])) {
                throw new Error("Expected the field `pathFilters` to be an array in the JSON data but got " + data['pathFilters']);
            }
            // validate the optional field `pathFilters` (array)
            for (const item of data['pathFilters']) {
                Dimension.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Dimensions which are compatible to be selected in the \"channelGroupings\" section of the report.
 * @member {Array.<module:model/Dimension>} channelGroupings
 */
PathReportCompatibleFields.prototype['channelGroupings'] = undefined;

/**
 * Dimensions which are compatible to be selected in the \"dimensions\" section of the report.
 * @member {Array.<module:model/Dimension>} dimensions
 */
PathReportCompatibleFields.prototype['dimensions'] = undefined;

/**
 * The kind of resource this is, in this case dfareporting#pathReportCompatibleFields.
 * @member {String} kind
 */
PathReportCompatibleFields.prototype['kind'] = undefined;

/**
 * Metrics which are compatible to be selected in the \"metricNames\" section of the report.
 * @member {Array.<module:model/Metric>} metrics
 */
PathReportCompatibleFields.prototype['metrics'] = undefined;

/**
 * Dimensions which are compatible to be selected in the \"pathFilters\" section of the report.
 * @member {Array.<module:model/Dimension>} pathFilters
 */
PathReportCompatibleFields.prototype['pathFilters'] = undefined;






export default PathReportCompatibleFields;

