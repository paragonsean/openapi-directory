/**
 * Campaign Manager 360 API
 * Build applications to efficiently manage large or complex trafficking, reporting, and attribution workflows for Campaign Manager 360.
 *
 * The version of the OpenAPI document: v3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DateRange from './DateRange';
import DimensionValue from './DimensionValue';
import ReportFloodlightCriteriaReportProperties from './ReportFloodlightCriteriaReportProperties';
import SortedDimension from './SortedDimension';

/**
 * The ReportFloodlightCriteria model module.
 * @module model/ReportFloodlightCriteria
 * @version v3.4
 */
class ReportFloodlightCriteria {
    /**
     * Constructs a new <code>ReportFloodlightCriteria</code>.
     * The report criteria for a report of type \&quot;FLOODLIGHT\&quot;.
     * @alias module:model/ReportFloodlightCriteria
     */
    constructor() { 
        
        ReportFloodlightCriteria.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReportFloodlightCriteria</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportFloodlightCriteria} obj Optional instance to populate.
     * @return {module:model/ReportFloodlightCriteria} The populated <code>ReportFloodlightCriteria</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportFloodlightCriteria();

            if (data.hasOwnProperty('customRichMediaEvents')) {
                obj['customRichMediaEvents'] = ApiClient.convertToType(data['customRichMediaEvents'], [DimensionValue]);
            }
            if (data.hasOwnProperty('dateRange')) {
                obj['dateRange'] = DateRange.constructFromObject(data['dateRange']);
            }
            if (data.hasOwnProperty('dimensionFilters')) {
                obj['dimensionFilters'] = ApiClient.convertToType(data['dimensionFilters'], [DimensionValue]);
            }
            if (data.hasOwnProperty('dimensions')) {
                obj['dimensions'] = ApiClient.convertToType(data['dimensions'], [SortedDimension]);
            }
            if (data.hasOwnProperty('floodlightConfigId')) {
                obj['floodlightConfigId'] = DimensionValue.constructFromObject(data['floodlightConfigId']);
            }
            if (data.hasOwnProperty('metricNames')) {
                obj['metricNames'] = ApiClient.convertToType(data['metricNames'], ['String']);
            }
            if (data.hasOwnProperty('reportProperties')) {
                obj['reportProperties'] = ReportFloodlightCriteriaReportProperties.constructFromObject(data['reportProperties']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportFloodlightCriteria</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportFloodlightCriteria</code>.
     */
    static validateJSON(data) {
        if (data['customRichMediaEvents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['customRichMediaEvents'])) {
                throw new Error("Expected the field `customRichMediaEvents` to be an array in the JSON data but got " + data['customRichMediaEvents']);
            }
            // validate the optional field `customRichMediaEvents` (array)
            for (const item of data['customRichMediaEvents']) {
                DimensionValue.validateJSON(item);
            };
        }
        // validate the optional field `dateRange`
        if (data['dateRange']) { // data not null
          DateRange.validateJSON(data['dateRange']);
        }
        if (data['dimensionFilters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensionFilters'])) {
                throw new Error("Expected the field `dimensionFilters` to be an array in the JSON data but got " + data['dimensionFilters']);
            }
            // validate the optional field `dimensionFilters` (array)
            for (const item of data['dimensionFilters']) {
                DimensionValue.validateJSON(item);
            };
        }
        if (data['dimensions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensions'])) {
                throw new Error("Expected the field `dimensions` to be an array in the JSON data but got " + data['dimensions']);
            }
            // validate the optional field `dimensions` (array)
            for (const item of data['dimensions']) {
                SortedDimension.validateJSON(item);
            };
        }
        // validate the optional field `floodlightConfigId`
        if (data['floodlightConfigId']) { // data not null
          DimensionValue.validateJSON(data['floodlightConfigId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['metricNames'])) {
            throw new Error("Expected the field `metricNames` to be an array in the JSON data but got " + data['metricNames']);
        }
        // validate the optional field `reportProperties`
        if (data['reportProperties']) { // data not null
          ReportFloodlightCriteriaReportProperties.validateJSON(data['reportProperties']);
        }

        return true;
    }


}



/**
 * The list of custom rich media events to include.
 * @member {Array.<module:model/DimensionValue>} customRichMediaEvents
 */
ReportFloodlightCriteria.prototype['customRichMediaEvents'] = undefined;

/**
 * @member {module:model/DateRange} dateRange
 */
ReportFloodlightCriteria.prototype['dateRange'] = undefined;

/**
 * The list of filters on which dimensions are filtered. Filters for different dimensions are ANDed, filters for the same dimension are grouped together and ORed.
 * @member {Array.<module:model/DimensionValue>} dimensionFilters
 */
ReportFloodlightCriteria.prototype['dimensionFilters'] = undefined;

/**
 * The list of dimensions the report should include.
 * @member {Array.<module:model/SortedDimension>} dimensions
 */
ReportFloodlightCriteria.prototype['dimensions'] = undefined;

/**
 * @member {module:model/DimensionValue} floodlightConfigId
 */
ReportFloodlightCriteria.prototype['floodlightConfigId'] = undefined;

/**
 * The list of names of metrics the report should include.
 * @member {Array.<String>} metricNames
 */
ReportFloodlightCriteria.prototype['metricNames'] = undefined;

/**
 * @member {module:model/ReportFloodlightCriteriaReportProperties} reportProperties
 */
ReportFloodlightCriteria.prototype['reportProperties'] = undefined;






export default ReportFloodlightCriteria;

