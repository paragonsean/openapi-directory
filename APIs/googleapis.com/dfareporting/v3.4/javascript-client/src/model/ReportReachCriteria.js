/**
 * Campaign Manager 360 API
 * Build applications to efficiently manage large or complex trafficking, reporting, and attribution workflows for Campaign Manager 360.
 *
 * The version of the OpenAPI document: v3.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Activities from './Activities';
import CustomRichMediaEvents from './CustomRichMediaEvents';
import DateRange from './DateRange';
import DimensionValue from './DimensionValue';
import SortedDimension from './SortedDimension';

/**
 * The ReportReachCriteria model module.
 * @module model/ReportReachCriteria
 * @version v3.4
 */
class ReportReachCriteria {
    /**
     * Constructs a new <code>ReportReachCriteria</code>.
     * The report criteria for a report of type \&quot;REACH\&quot;.
     * @alias module:model/ReportReachCriteria
     */
    constructor() { 
        
        ReportReachCriteria.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReportReachCriteria</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportReachCriteria} obj Optional instance to populate.
     * @return {module:model/ReportReachCriteria} The populated <code>ReportReachCriteria</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportReachCriteria();

            if (data.hasOwnProperty('activities')) {
                obj['activities'] = Activities.constructFromObject(data['activities']);
            }
            if (data.hasOwnProperty('customRichMediaEvents')) {
                obj['customRichMediaEvents'] = CustomRichMediaEvents.constructFromObject(data['customRichMediaEvents']);
            }
            if (data.hasOwnProperty('dateRange')) {
                obj['dateRange'] = DateRange.constructFromObject(data['dateRange']);
            }
            if (data.hasOwnProperty('dimensionFilters')) {
                obj['dimensionFilters'] = ApiClient.convertToType(data['dimensionFilters'], [DimensionValue]);
            }
            if (data.hasOwnProperty('dimensions')) {
                obj['dimensions'] = ApiClient.convertToType(data['dimensions'], [SortedDimension]);
            }
            if (data.hasOwnProperty('enableAllDimensionCombinations')) {
                obj['enableAllDimensionCombinations'] = ApiClient.convertToType(data['enableAllDimensionCombinations'], 'Boolean');
            }
            if (data.hasOwnProperty('metricNames')) {
                obj['metricNames'] = ApiClient.convertToType(data['metricNames'], ['String']);
            }
            if (data.hasOwnProperty('reachByFrequencyMetricNames')) {
                obj['reachByFrequencyMetricNames'] = ApiClient.convertToType(data['reachByFrequencyMetricNames'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportReachCriteria</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportReachCriteria</code>.
     */
    static validateJSON(data) {
        // validate the optional field `activities`
        if (data['activities']) { // data not null
          Activities.validateJSON(data['activities']);
        }
        // validate the optional field `customRichMediaEvents`
        if (data['customRichMediaEvents']) { // data not null
          CustomRichMediaEvents.validateJSON(data['customRichMediaEvents']);
        }
        // validate the optional field `dateRange`
        if (data['dateRange']) { // data not null
          DateRange.validateJSON(data['dateRange']);
        }
        if (data['dimensionFilters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensionFilters'])) {
                throw new Error("Expected the field `dimensionFilters` to be an array in the JSON data but got " + data['dimensionFilters']);
            }
            // validate the optional field `dimensionFilters` (array)
            for (const item of data['dimensionFilters']) {
                DimensionValue.validateJSON(item);
            };
        }
        if (data['dimensions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensions'])) {
                throw new Error("Expected the field `dimensions` to be an array in the JSON data but got " + data['dimensions']);
            }
            // validate the optional field `dimensions` (array)
            for (const item of data['dimensions']) {
                SortedDimension.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['metricNames'])) {
            throw new Error("Expected the field `metricNames` to be an array in the JSON data but got " + data['metricNames']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['reachByFrequencyMetricNames'])) {
            throw new Error("Expected the field `reachByFrequencyMetricNames` to be an array in the JSON data but got " + data['reachByFrequencyMetricNames']);
        }

        return true;
    }


}



/**
 * @member {module:model/Activities} activities
 */
ReportReachCriteria.prototype['activities'] = undefined;

/**
 * @member {module:model/CustomRichMediaEvents} customRichMediaEvents
 */
ReportReachCriteria.prototype['customRichMediaEvents'] = undefined;

/**
 * @member {module:model/DateRange} dateRange
 */
ReportReachCriteria.prototype['dateRange'] = undefined;

/**
 * The list of filters on which dimensions are filtered. Filters for different dimensions are ANDed, filters for the same dimension are grouped together and ORed.
 * @member {Array.<module:model/DimensionValue>} dimensionFilters
 */
ReportReachCriteria.prototype['dimensionFilters'] = undefined;

/**
 * The list of dimensions the report should include.
 * @member {Array.<module:model/SortedDimension>} dimensions
 */
ReportReachCriteria.prototype['dimensions'] = undefined;

/**
 * Whether to enable all reach dimension combinations in the report. Defaults to false. If enabled, the date range of the report should be within the last 42 days.
 * @member {Boolean} enableAllDimensionCombinations
 */
ReportReachCriteria.prototype['enableAllDimensionCombinations'] = undefined;

/**
 * The list of names of metrics the report should include.
 * @member {Array.<String>} metricNames
 */
ReportReachCriteria.prototype['metricNames'] = undefined;

/**
 * The list of names of Reach By Frequency metrics the report should include.
 * @member {Array.<String>} reachByFrequencyMetricNames
 */
ReportReachCriteria.prototype['reachByFrequencyMetricNames'] = undefined;






export default ReportReachCriteria;

