/**
 * Campaign Manager 360 API
 * Build applications to efficiently manage large or complex trafficking, reporting, and attribution workflows for Campaign Manager 360.
 *
 * The version of the OpenAPI document: v3.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Dimension from './Dimension';
import Metric from './Metric';

/**
 * The CrossDimensionReachReportCompatibleFields model module.
 * @module model/CrossDimensionReachReportCompatibleFields
 * @version v3.3
 */
class CrossDimensionReachReportCompatibleFields {
    /**
     * Constructs a new <code>CrossDimensionReachReportCompatibleFields</code>.
     * Represents fields that are compatible to be selected for a report of type \&quot;CROSS_DIMENSION_REACH\&quot;.
     * @alias module:model/CrossDimensionReachReportCompatibleFields
     */
    constructor() { 
        
        CrossDimensionReachReportCompatibleFields.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CrossDimensionReachReportCompatibleFields</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CrossDimensionReachReportCompatibleFields} obj Optional instance to populate.
     * @return {module:model/CrossDimensionReachReportCompatibleFields} The populated <code>CrossDimensionReachReportCompatibleFields</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CrossDimensionReachReportCompatibleFields();

            if (data.hasOwnProperty('breakdown')) {
                obj['breakdown'] = ApiClient.convertToType(data['breakdown'], [Dimension]);
            }
            if (data.hasOwnProperty('dimensionFilters')) {
                obj['dimensionFilters'] = ApiClient.convertToType(data['dimensionFilters'], [Dimension]);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [Metric]);
            }
            if (data.hasOwnProperty('overlapMetrics')) {
                obj['overlapMetrics'] = ApiClient.convertToType(data['overlapMetrics'], [Metric]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CrossDimensionReachReportCompatibleFields</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CrossDimensionReachReportCompatibleFields</code>.
     */
    static validateJSON(data) {
        if (data['breakdown']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['breakdown'])) {
                throw new Error("Expected the field `breakdown` to be an array in the JSON data but got " + data['breakdown']);
            }
            // validate the optional field `breakdown` (array)
            for (const item of data['breakdown']) {
                Dimension.validateJSON(item);
            };
        }
        if (data['dimensionFilters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dimensionFilters'])) {
                throw new Error("Expected the field `dimensionFilters` to be an array in the JSON data but got " + data['dimensionFilters']);
            }
            // validate the optional field `dimensionFilters` (array)
            for (const item of data['dimensionFilters']) {
                Dimension.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        if (data['metrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metrics'])) {
                throw new Error("Expected the field `metrics` to be an array in the JSON data but got " + data['metrics']);
            }
            // validate the optional field `metrics` (array)
            for (const item of data['metrics']) {
                Metric.validateJSON(item);
            };
        }
        if (data['overlapMetrics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['overlapMetrics'])) {
                throw new Error("Expected the field `overlapMetrics` to be an array in the JSON data but got " + data['overlapMetrics']);
            }
            // validate the optional field `overlapMetrics` (array)
            for (const item of data['overlapMetrics']) {
                Metric.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Dimensions which are compatible to be selected in the \"breakdown\" section of the report.
 * @member {Array.<module:model/Dimension>} breakdown
 */
CrossDimensionReachReportCompatibleFields.prototype['breakdown'] = undefined;

/**
 * Dimensions which are compatible to be selected in the \"dimensionFilters\" section of the report.
 * @member {Array.<module:model/Dimension>} dimensionFilters
 */
CrossDimensionReachReportCompatibleFields.prototype['dimensionFilters'] = undefined;

/**
 * The kind of resource this is, in this case dfareporting#crossDimensionReachReportCompatibleFields.
 * @member {String} kind
 */
CrossDimensionReachReportCompatibleFields.prototype['kind'] = undefined;

/**
 * Metrics which are compatible to be selected in the \"metricNames\" section of the report.
 * @member {Array.<module:model/Metric>} metrics
 */
CrossDimensionReachReportCompatibleFields.prototype['metrics'] = undefined;

/**
 * Metrics which are compatible to be selected in the \"overlapMetricNames\" section of the report.
 * @member {Array.<module:model/Metric>} overlapMetrics
 */
CrossDimensionReachReportCompatibleFields.prototype['overlapMetrics'] = undefined;






export default CrossDimensionReachReportCompatibleFields;

