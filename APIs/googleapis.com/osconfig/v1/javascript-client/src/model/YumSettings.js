/**
 * OS Config API
 * OS management tools that can be used for patch management, patch compliance, and configuration management on VM instances.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The YumSettings model module.
 * @module model/YumSettings
 * @version v1
 */
class YumSettings {
    /**
     * Constructs a new <code>YumSettings</code>.
     * Yum patching is performed by executing &#x60;yum update&#x60;. Additional options can be set to control how this is executed. Note that not all settings are supported on all platforms.
     * @alias module:model/YumSettings
     */
    constructor() { 
        
        YumSettings.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>YumSettings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/YumSettings} obj Optional instance to populate.
     * @return {module:model/YumSettings} The populated <code>YumSettings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new YumSettings();

            if (data.hasOwnProperty('excludes')) {
                obj['excludes'] = ApiClient.convertToType(data['excludes'], ['String']);
            }
            if (data.hasOwnProperty('exclusivePackages')) {
                obj['exclusivePackages'] = ApiClient.convertToType(data['exclusivePackages'], ['String']);
            }
            if (data.hasOwnProperty('minimal')) {
                obj['minimal'] = ApiClient.convertToType(data['minimal'], 'Boolean');
            }
            if (data.hasOwnProperty('security')) {
                obj['security'] = ApiClient.convertToType(data['security'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>YumSettings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>YumSettings</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['excludes'])) {
            throw new Error("Expected the field `excludes` to be an array in the JSON data but got " + data['excludes']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['exclusivePackages'])) {
            throw new Error("Expected the field `exclusivePackages` to be an array in the JSON data but got " + data['exclusivePackages']);
        }

        return true;
    }


}



/**
 * List of packages to exclude from update. These packages are excluded by using the yum `--exclude` flag.
 * @member {Array.<String>} excludes
 */
YumSettings.prototype['excludes'] = undefined;

/**
 * An exclusive list of packages to be updated. These are the only packages that will be updated. If these packages are not installed, they will be ignored. This field must not be specified with any other patch configuration fields.
 * @member {Array.<String>} exclusivePackages
 */
YumSettings.prototype['exclusivePackages'] = undefined;

/**
 * Will cause patch to run `yum update-minimal` instead.
 * @member {Boolean} minimal
 */
YumSettings.prototype['minimal'] = undefined;

/**
 * Adds the `--security` flag to `yum update`. Not supported on all platforms.
 * @member {Boolean} security
 */
YumSettings.prototype['security'] = undefined;






export default YumSettings;

