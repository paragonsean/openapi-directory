/**
 * OS Config API
 * OS management tools that can be used for patch management, patch compliance, and configuration management on VM instances.
 *
 * The version of the OpenAPI document: v1alpha
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OSPolicyResourceFile from './OSPolicyResourceFile';

/**
 * The OSPolicyResourceFileResource model module.
 * @module model/OSPolicyResourceFileResource
 * @version v1alpha
 */
class OSPolicyResourceFileResource {
    /**
     * Constructs a new <code>OSPolicyResourceFileResource</code>.
     * A resource that manages the state of a file.
     * @alias module:model/OSPolicyResourceFileResource
     */
    constructor() { 
        
        OSPolicyResourceFileResource.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OSPolicyResourceFileResource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OSPolicyResourceFileResource} obj Optional instance to populate.
     * @return {module:model/OSPolicyResourceFileResource} The populated <code>OSPolicyResourceFileResource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OSPolicyResourceFileResource();

            if (data.hasOwnProperty('content')) {
                obj['content'] = ApiClient.convertToType(data['content'], 'String');
            }
            if (data.hasOwnProperty('file')) {
                obj['file'] = OSPolicyResourceFile.constructFromObject(data['file']);
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OSPolicyResourceFileResource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OSPolicyResourceFileResource</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['content'] && !(typeof data['content'] === 'string' || data['content'] instanceof String)) {
            throw new Error("Expected the field `content` to be a primitive type in the JSON string but got " + data['content']);
        }
        // validate the optional field `file`
        if (data['file']) { // data not null
          OSPolicyResourceFile.validateJSON(data['file']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }
        // ensure the json data is a string
        if (data['permissions'] && !(typeof data['permissions'] === 'string' || data['permissions'] instanceof String)) {
            throw new Error("Expected the field `permissions` to be a primitive type in the JSON string but got " + data['permissions']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}



/**
 * A a file with this content. The size of the content is limited to 32KiB.
 * @member {String} content
 */
OSPolicyResourceFileResource.prototype['content'] = undefined;

/**
 * @member {module:model/OSPolicyResourceFile} file
 */
OSPolicyResourceFileResource.prototype['file'] = undefined;

/**
 * Required. The absolute path of the file within the VM.
 * @member {String} path
 */
OSPolicyResourceFileResource.prototype['path'] = undefined;

/**
 * Consists of three octal digits which represent, in order, the permissions of the owner, group, and other users for the file (similarly to the numeric mode used in the linux chmod utility). Each digit represents a three bit number with the 4 bit corresponding to the read permissions, the 2 bit corresponds to the write bit, and the one bit corresponds to the execute permission. Default behavior is 755. Below are some examples of permissions and their associated values: read, write, and execute: 7 read and execute: 5 read and write: 6 read only: 4
 * @member {String} permissions
 */
OSPolicyResourceFileResource.prototype['permissions'] = undefined;

/**
 * Required. Desired state of the file.
 * @member {module:model/OSPolicyResourceFileResource.StateEnum} state
 */
OSPolicyResourceFileResource.prototype['state'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
OSPolicyResourceFileResource['StateEnum'] = {

    /**
     * value: "DESIRED_STATE_UNSPECIFIED"
     * @const
     */
    "DESIRED_STATE_UNSPECIFIED": "DESIRED_STATE_UNSPECIFIED",

    /**
     * value: "PRESENT"
     * @const
     */
    "PRESENT": "PRESENT",

    /**
     * value: "ABSENT"
     * @const
     */
    "ABSENT": "ABSENT",

    /**
     * value: "CONTENTS_MATCH"
     * @const
     */
    "CONTENTS_MATCH": "CONTENTS_MATCH"
};



export default OSPolicyResourceFileResource;

