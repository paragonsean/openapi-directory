/**
 * OS Config API
 * OS management tools that can be used for patch management, patch compliance, and configuration management on VM instances.
 *
 * The version of the OpenAPI document: v1alpha
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OSPolicyResourceComplianceExecResourceOutput from './OSPolicyResourceComplianceExecResourceOutput';
import OSPolicyResourceConfigStep from './OSPolicyResourceConfigStep';

/**
 * The OSPolicyResourceCompliance model module.
 * @module model/OSPolicyResourceCompliance
 * @version v1alpha
 */
class OSPolicyResourceCompliance {
    /**
     * Constructs a new <code>OSPolicyResourceCompliance</code>.
     * Compliance data for an OS policy resource.
     * @alias module:model/OSPolicyResourceCompliance
     */
    constructor() { 
        
        OSPolicyResourceCompliance.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OSPolicyResourceCompliance</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OSPolicyResourceCompliance} obj Optional instance to populate.
     * @return {module:model/OSPolicyResourceCompliance} The populated <code>OSPolicyResourceCompliance</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OSPolicyResourceCompliance();

            if (data.hasOwnProperty('configSteps')) {
                obj['configSteps'] = ApiClient.convertToType(data['configSteps'], [OSPolicyResourceConfigStep]);
            }
            if (data.hasOwnProperty('execResourceOutput')) {
                obj['execResourceOutput'] = OSPolicyResourceComplianceExecResourceOutput.constructFromObject(data['execResourceOutput']);
            }
            if (data.hasOwnProperty('osPolicyResourceId')) {
                obj['osPolicyResourceId'] = ApiClient.convertToType(data['osPolicyResourceId'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OSPolicyResourceCompliance</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OSPolicyResourceCompliance</code>.
     */
    static validateJSON(data) {
        if (data['configSteps']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['configSteps'])) {
                throw new Error("Expected the field `configSteps` to be an array in the JSON data but got " + data['configSteps']);
            }
            // validate the optional field `configSteps` (array)
            for (const item of data['configSteps']) {
                OSPolicyResourceConfigStep.validateJSON(item);
            };
        }
        // validate the optional field `execResourceOutput`
        if (data['execResourceOutput']) { // data not null
          OSPolicyResourceComplianceExecResourceOutput.validateJSON(data['execResourceOutput']);
        }
        // ensure the json data is a string
        if (data['osPolicyResourceId'] && !(typeof data['osPolicyResourceId'] === 'string' || data['osPolicyResourceId'] instanceof String)) {
            throw new Error("Expected the field `osPolicyResourceId` to be a primitive type in the JSON string but got " + data['osPolicyResourceId']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}



/**
 * Ordered list of configuration steps taken by the agent for the OS policy resource.
 * @member {Array.<module:model/OSPolicyResourceConfigStep>} configSteps
 */
OSPolicyResourceCompliance.prototype['configSteps'] = undefined;

/**
 * @member {module:model/OSPolicyResourceComplianceExecResourceOutput} execResourceOutput
 */
OSPolicyResourceCompliance.prototype['execResourceOutput'] = undefined;

/**
 * The id of the OS policy resource.
 * @member {String} osPolicyResourceId
 */
OSPolicyResourceCompliance.prototype['osPolicyResourceId'] = undefined;

/**
 * Compliance state of the OS policy resource.
 * @member {module:model/OSPolicyResourceCompliance.StateEnum} state
 */
OSPolicyResourceCompliance.prototype['state'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
OSPolicyResourceCompliance['StateEnum'] = {

    /**
     * value: "OS_POLICY_COMPLIANCE_STATE_UNSPECIFIED"
     * @const
     */
    "OS_POLICY_COMPLIANCE_STATE_UNSPECIFIED": "OS_POLICY_COMPLIANCE_STATE_UNSPECIFIED",

    /**
     * value: "COMPLIANT"
     * @const
     */
    "COMPLIANT": "COMPLIANT",

    /**
     * value: "NON_COMPLIANT"
     * @const
     */
    "NON_COMPLIANT": "NON_COMPLIANT",

    /**
     * value: "UNKNOWN"
     * @const
     */
    "UNKNOWN": "UNKNOWN",

    /**
     * value: "NO_OS_POLICIES_APPLICABLE"
     * @const
     */
    "NO_OS_POLICIES_APPLICABLE": "NO_OS_POLICIES_APPLICABLE"
};



export default OSPolicyResourceCompliance;

