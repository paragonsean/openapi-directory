/**
 * OS Config API
 * OS management tools that can be used for patch management, patch compliance, and configuration management on VM instances.
 *
 * The version of the OpenAPI document: v1beta
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GcsObject from './GcsObject';

/**
 * The ExecStepConfig model module.
 * @module model/ExecStepConfig
 * @version v1beta
 */
class ExecStepConfig {
    /**
     * Constructs a new <code>ExecStepConfig</code>.
     * Common configurations for an ExecStep.
     * @alias module:model/ExecStepConfig
     */
    constructor() { 
        
        ExecStepConfig.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ExecStepConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExecStepConfig} obj Optional instance to populate.
     * @return {module:model/ExecStepConfig} The populated <code>ExecStepConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExecStepConfig();

            if (data.hasOwnProperty('allowedSuccessCodes')) {
                obj['allowedSuccessCodes'] = ApiClient.convertToType(data['allowedSuccessCodes'], ['Number']);
            }
            if (data.hasOwnProperty('gcsObject')) {
                obj['gcsObject'] = GcsObject.constructFromObject(data['gcsObject']);
            }
            if (data.hasOwnProperty('interpreter')) {
                obj['interpreter'] = ApiClient.convertToType(data['interpreter'], 'String');
            }
            if (data.hasOwnProperty('localPath')) {
                obj['localPath'] = ApiClient.convertToType(data['localPath'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExecStepConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExecStepConfig</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['allowedSuccessCodes'])) {
            throw new Error("Expected the field `allowedSuccessCodes` to be an array in the JSON data but got " + data['allowedSuccessCodes']);
        }
        // validate the optional field `gcsObject`
        if (data['gcsObject']) { // data not null
          GcsObject.validateJSON(data['gcsObject']);
        }
        // ensure the json data is a string
        if (data['interpreter'] && !(typeof data['interpreter'] === 'string' || data['interpreter'] instanceof String)) {
            throw new Error("Expected the field `interpreter` to be a primitive type in the JSON string but got " + data['interpreter']);
        }
        // ensure the json data is a string
        if (data['localPath'] && !(typeof data['localPath'] === 'string' || data['localPath'] instanceof String)) {
            throw new Error("Expected the field `localPath` to be a primitive type in the JSON string but got " + data['localPath']);
        }

        return true;
    }


}



/**
 * Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
 * @member {Array.<Number>} allowedSuccessCodes
 */
ExecStepConfig.prototype['allowedSuccessCodes'] = undefined;

/**
 * @member {module:model/GcsObject} gcsObject
 */
ExecStepConfig.prototype['gcsObject'] = undefined;

/**
 * The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with [shebang lines] (https://en.wikipedia.org/wiki/Shebang_\\(Unix\\)).
 * @member {module:model/ExecStepConfig.InterpreterEnum} interpreter
 */
ExecStepConfig.prototype['interpreter'] = undefined;

/**
 * An absolute path to the executable on the VM.
 * @member {String} localPath
 */
ExecStepConfig.prototype['localPath'] = undefined;





/**
 * Allowed values for the <code>interpreter</code> property.
 * @enum {String}
 * @readonly
 */
ExecStepConfig['InterpreterEnum'] = {

    /**
     * value: "INTERPRETER_UNSPECIFIED"
     * @const
     */
    "INTERPRETER_UNSPECIFIED": "INTERPRETER_UNSPECIFIED",

    /**
     * value: "NONE"
     * @const
     */
    "NONE": "NONE",

    /**
     * value: "SHELL"
     * @const
     */
    "SHELL": "SHELL",

    /**
     * value: "POWERSHELL"
     * @const
     */
    "POWERSHELL": "POWERSHELL"
};



export default ExecStepConfig;

