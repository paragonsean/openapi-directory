/**
 * Google Docs API
 * Reads and writes Google Docs documents.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SectionStyle from './SectionStyle';

/**
 * The SectionBreak model module.
 * @module model/SectionBreak
 * @version v1
 */
class SectionBreak {
    /**
     * Constructs a new <code>SectionBreak</code>.
     * A StructuralElement representing a section break. A section is a range of content that has the same SectionStyle. A section break represents the start of a new section, and the section style applies to the section after the section break. The document body always begins with a section break.
     * @alias module:model/SectionBreak
     */
    constructor() { 
        
        SectionBreak.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SectionBreak</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SectionBreak} obj Optional instance to populate.
     * @return {module:model/SectionBreak} The populated <code>SectionBreak</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SectionBreak();

            if (data.hasOwnProperty('sectionStyle')) {
                obj['sectionStyle'] = SectionStyle.constructFromObject(data['sectionStyle']);
            }
            if (data.hasOwnProperty('suggestedDeletionIds')) {
                obj['suggestedDeletionIds'] = ApiClient.convertToType(data['suggestedDeletionIds'], ['String']);
            }
            if (data.hasOwnProperty('suggestedInsertionIds')) {
                obj['suggestedInsertionIds'] = ApiClient.convertToType(data['suggestedInsertionIds'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SectionBreak</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SectionBreak</code>.
     */
    static validateJSON(data) {
        // validate the optional field `sectionStyle`
        if (data['sectionStyle']) { // data not null
          SectionStyle.validateJSON(data['sectionStyle']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedDeletionIds'])) {
            throw new Error("Expected the field `suggestedDeletionIds` to be an array in the JSON data but got " + data['suggestedDeletionIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedInsertionIds'])) {
            throw new Error("Expected the field `suggestedInsertionIds` to be an array in the JSON data but got " + data['suggestedInsertionIds']);
        }

        return true;
    }


}



/**
 * @member {module:model/SectionStyle} sectionStyle
 */
SectionBreak.prototype['sectionStyle'] = undefined;

/**
 * The suggested deletion IDs. If empty, then there are no suggested deletions of this content.
 * @member {Array.<String>} suggestedDeletionIds
 */
SectionBreak.prototype['suggestedDeletionIds'] = undefined;

/**
 * The suggested insertion IDs. A SectionBreak may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.
 * @member {Array.<String>} suggestedInsertionIds
 */
SectionBreak.prototype['suggestedInsertionIds'] = undefined;






export default SectionBreak;

