/**
 * Google Docs API
 * Reads and writes Google Docs documents.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import StructuralElement from './StructuralElement';
import SuggestedTableCellStyle from './SuggestedTableCellStyle';
import TableCellStyle from './TableCellStyle';

/**
 * The TableCell model module.
 * @module model/TableCell
 * @version v1
 */
class TableCell {
    /**
     * Constructs a new <code>TableCell</code>.
     * The contents and style of a cell in a Table.
     * @alias module:model/TableCell
     */
    constructor() { 
        
        TableCell.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TableCell</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TableCell} obj Optional instance to populate.
     * @return {module:model/TableCell} The populated <code>TableCell</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TableCell();

            if (data.hasOwnProperty('content')) {
                obj['content'] = ApiClient.convertToType(data['content'], [StructuralElement]);
            }
            if (data.hasOwnProperty('endIndex')) {
                obj['endIndex'] = ApiClient.convertToType(data['endIndex'], 'Number');
            }
            if (data.hasOwnProperty('startIndex')) {
                obj['startIndex'] = ApiClient.convertToType(data['startIndex'], 'Number');
            }
            if (data.hasOwnProperty('suggestedDeletionIds')) {
                obj['suggestedDeletionIds'] = ApiClient.convertToType(data['suggestedDeletionIds'], ['String']);
            }
            if (data.hasOwnProperty('suggestedInsertionIds')) {
                obj['suggestedInsertionIds'] = ApiClient.convertToType(data['suggestedInsertionIds'], ['String']);
            }
            if (data.hasOwnProperty('suggestedTableCellStyleChanges')) {
                obj['suggestedTableCellStyleChanges'] = ApiClient.convertToType(data['suggestedTableCellStyleChanges'], {'String': SuggestedTableCellStyle});
            }
            if (data.hasOwnProperty('tableCellStyle')) {
                obj['tableCellStyle'] = TableCellStyle.constructFromObject(data['tableCellStyle']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TableCell</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TableCell</code>.
     */
    static validateJSON(data) {
        if (data['content']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['content'])) {
                throw new Error("Expected the field `content` to be an array in the JSON data but got " + data['content']);
            }
            // validate the optional field `content` (array)
            for (const item of data['content']) {
                StructuralElement.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedDeletionIds'])) {
            throw new Error("Expected the field `suggestedDeletionIds` to be an array in the JSON data but got " + data['suggestedDeletionIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedInsertionIds'])) {
            throw new Error("Expected the field `suggestedInsertionIds` to be an array in the JSON data but got " + data['suggestedInsertionIds']);
        }
        // validate the optional field `tableCellStyle`
        if (data['tableCellStyle']) { // data not null
          TableCellStyle.validateJSON(data['tableCellStyle']);
        }

        return true;
    }


}



/**
 * The content of the cell.
 * @member {Array.<module:model/StructuralElement>} content
 */
TableCell.prototype['content'] = undefined;

/**
 * The zero-based end index of this cell, exclusive, in UTF-16 code units.
 * @member {Number} endIndex
 */
TableCell.prototype['endIndex'] = undefined;

/**
 * The zero-based start index of this cell, in UTF-16 code units.
 * @member {Number} startIndex
 */
TableCell.prototype['startIndex'] = undefined;

/**
 * The suggested deletion IDs. If empty, then there are no suggested deletions of this content.
 * @member {Array.<String>} suggestedDeletionIds
 */
TableCell.prototype['suggestedDeletionIds'] = undefined;

/**
 * The suggested insertion IDs. A TableCell may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.
 * @member {Array.<String>} suggestedInsertionIds
 */
TableCell.prototype['suggestedInsertionIds'] = undefined;

/**
 * The suggested changes to the table cell style, keyed by suggestion ID.
 * @member {Object.<String, module:model/SuggestedTableCellStyle>} suggestedTableCellStyleChanges
 */
TableCell.prototype['suggestedTableCellStyleChanges'] = undefined;

/**
 * @member {module:model/TableCellStyle} tableCellStyle
 */
TableCell.prototype['tableCellStyle'] = undefined;






export default TableCell;

