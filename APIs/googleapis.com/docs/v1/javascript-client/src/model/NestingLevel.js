/**
 * Google Docs API
 * Reads and writes Google Docs documents.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Dimension from './Dimension';
import TextStyle from './TextStyle';

/**
 * The NestingLevel model module.
 * @module model/NestingLevel
 * @version v1
 */
class NestingLevel {
    /**
     * Constructs a new <code>NestingLevel</code>.
     * Contains properties describing the look and feel of a list bullet at a given level of nesting.
     * @alias module:model/NestingLevel
     */
    constructor() { 
        
        NestingLevel.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NestingLevel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NestingLevel} obj Optional instance to populate.
     * @return {module:model/NestingLevel} The populated <code>NestingLevel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NestingLevel();

            if (data.hasOwnProperty('bulletAlignment')) {
                obj['bulletAlignment'] = ApiClient.convertToType(data['bulletAlignment'], 'String');
            }
            if (data.hasOwnProperty('glyphFormat')) {
                obj['glyphFormat'] = ApiClient.convertToType(data['glyphFormat'], 'String');
            }
            if (data.hasOwnProperty('glyphSymbol')) {
                obj['glyphSymbol'] = ApiClient.convertToType(data['glyphSymbol'], 'String');
            }
            if (data.hasOwnProperty('glyphType')) {
                obj['glyphType'] = ApiClient.convertToType(data['glyphType'], 'String');
            }
            if (data.hasOwnProperty('indentFirstLine')) {
                obj['indentFirstLine'] = Dimension.constructFromObject(data['indentFirstLine']);
            }
            if (data.hasOwnProperty('indentStart')) {
                obj['indentStart'] = Dimension.constructFromObject(data['indentStart']);
            }
            if (data.hasOwnProperty('startNumber')) {
                obj['startNumber'] = ApiClient.convertToType(data['startNumber'], 'Number');
            }
            if (data.hasOwnProperty('textStyle')) {
                obj['textStyle'] = TextStyle.constructFromObject(data['textStyle']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NestingLevel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NestingLevel</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['bulletAlignment'] && !(typeof data['bulletAlignment'] === 'string' || data['bulletAlignment'] instanceof String)) {
            throw new Error("Expected the field `bulletAlignment` to be a primitive type in the JSON string but got " + data['bulletAlignment']);
        }
        // ensure the json data is a string
        if (data['glyphFormat'] && !(typeof data['glyphFormat'] === 'string' || data['glyphFormat'] instanceof String)) {
            throw new Error("Expected the field `glyphFormat` to be a primitive type in the JSON string but got " + data['glyphFormat']);
        }
        // ensure the json data is a string
        if (data['glyphSymbol'] && !(typeof data['glyphSymbol'] === 'string' || data['glyphSymbol'] instanceof String)) {
            throw new Error("Expected the field `glyphSymbol` to be a primitive type in the JSON string but got " + data['glyphSymbol']);
        }
        // ensure the json data is a string
        if (data['glyphType'] && !(typeof data['glyphType'] === 'string' || data['glyphType'] instanceof String)) {
            throw new Error("Expected the field `glyphType` to be a primitive type in the JSON string but got " + data['glyphType']);
        }
        // validate the optional field `indentFirstLine`
        if (data['indentFirstLine']) { // data not null
          Dimension.validateJSON(data['indentFirstLine']);
        }
        // validate the optional field `indentStart`
        if (data['indentStart']) { // data not null
          Dimension.validateJSON(data['indentStart']);
        }
        // validate the optional field `textStyle`
        if (data['textStyle']) { // data not null
          TextStyle.validateJSON(data['textStyle']);
        }

        return true;
    }


}



/**
 * The alignment of the bullet within the space allotted for rendering the bullet.
 * @member {module:model/NestingLevel.BulletAlignmentEnum} bulletAlignment
 */
NestingLevel.prototype['bulletAlignment'] = undefined;

/**
 * The format string used by bullets at this level of nesting. The glyph format contains one or more placeholders, and these placeholders are replaced with the appropriate values depending on the glyph_type or glyph_symbol. The placeholders follow the pattern `%[nesting_level]`. Furthermore, placeholders can have prefixes and suffixes. Thus, the glyph format follows the pattern `%[nesting_level]`. Note that the prefix and suffix are optional and can be arbitrary strings. For example, the glyph format `%0.` indicates that the rendered glyph will replace the placeholder with the corresponding glyph for nesting level 0 followed by a period as the suffix. So a list with a glyph type of UPPER_ALPHA and glyph format `%0.` at nesting level 0 will result in a list with rendered glyphs `A.` `B.` `C.` The glyph format can contain placeholders for the current nesting level as well as placeholders for parent nesting levels. For example, a list can have a glyph format of `%0.` at nesting level 0 and a glyph format of `%0.%1.` at nesting level 1. Assuming both nesting levels have DECIMAL glyph types, this would result in a list with rendered glyphs `1.` `2.` ` 2.1.` ` 2.2.` `3.` For nesting levels that are ordered, the string that replaces a placeholder in the glyph format for a particular paragraph depends on the paragraph's order within the list.
 * @member {String} glyphFormat
 */
NestingLevel.prototype['glyphFormat'] = undefined;

/**
 * A custom glyph symbol used by bullets when paragraphs at this level of nesting are unordered. The glyph symbol replaces placeholders within the glyph_format. For example, if the glyph_symbol is the solid circle corresponding to Unicode U+25cf code point and the glyph_format is `%0`, the rendered glyph would be the solid circle.
 * @member {String} glyphSymbol
 */
NestingLevel.prototype['glyphSymbol'] = undefined;

/**
 * The type of glyph used by bullets when paragraphs at this level of nesting are ordered. The glyph type determines the type of glyph used to replace placeholders within the glyph_format when paragraphs at this level of nesting are ordered. For example, if the nesting level is 0, the glyph_format is `%0.` and the glyph type is DECIMAL, then the rendered glyph would replace the placeholder `%0` in the glyph format with a number corresponding to list item's order within the list.
 * @member {module:model/NestingLevel.GlyphTypeEnum} glyphType
 */
NestingLevel.prototype['glyphType'] = undefined;

/**
 * @member {module:model/Dimension} indentFirstLine
 */
NestingLevel.prototype['indentFirstLine'] = undefined;

/**
 * @member {module:model/Dimension} indentStart
 */
NestingLevel.prototype['indentStart'] = undefined;

/**
 * The number of the first list item at this nesting level. A value of 0 is treated as a value of 1 for lettered lists and Roman numeral lists. For values of both 0 and 1, lettered and Roman numeral lists will begin at `a` and `i` respectively. This value is ignored for nesting levels with unordered glyphs.
 * @member {Number} startNumber
 */
NestingLevel.prototype['startNumber'] = undefined;

/**
 * @member {module:model/TextStyle} textStyle
 */
NestingLevel.prototype['textStyle'] = undefined;





/**
 * Allowed values for the <code>bulletAlignment</code> property.
 * @enum {String}
 * @readonly
 */
NestingLevel['BulletAlignmentEnum'] = {

    /**
     * value: "BULLET_ALIGNMENT_UNSPECIFIED"
     * @const
     */
    "BULLET_ALIGNMENT_UNSPECIFIED": "BULLET_ALIGNMENT_UNSPECIFIED",

    /**
     * value: "START"
     * @const
     */
    "START": "START",

    /**
     * value: "CENTER"
     * @const
     */
    "CENTER": "CENTER",

    /**
     * value: "END"
     * @const
     */
    "END": "END"
};


/**
 * Allowed values for the <code>glyphType</code> property.
 * @enum {String}
 * @readonly
 */
NestingLevel['GlyphTypeEnum'] = {

    /**
     * value: "GLYPH_TYPE_UNSPECIFIED"
     * @const
     */
    "GLYPH_TYPE_UNSPECIFIED": "GLYPH_TYPE_UNSPECIFIED",

    /**
     * value: "NONE"
     * @const
     */
    "NONE": "NONE",

    /**
     * value: "DECIMAL"
     * @const
     */
    "DECIMAL": "DECIMAL",

    /**
     * value: "ZERO_DECIMAL"
     * @const
     */
    "ZERO_DECIMAL": "ZERO_DECIMAL",

    /**
     * value: "UPPER_ALPHA"
     * @const
     */
    "UPPER_ALPHA": "UPPER_ALPHA",

    /**
     * value: "ALPHA"
     * @const
     */
    "ALPHA": "ALPHA",

    /**
     * value: "UPPER_ROMAN"
     * @const
     */
    "UPPER_ROMAN": "UPPER_ROMAN",

    /**
     * value: "ROMAN"
     * @const
     */
    "ROMAN": "ROMAN"
};



export default NestingLevel;

