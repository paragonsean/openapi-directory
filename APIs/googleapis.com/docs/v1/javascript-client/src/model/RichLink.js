/**
 * Google Docs API
 * Reads and writes Google Docs documents.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RichLinkProperties from './RichLinkProperties';
import SuggestedTextStyle from './SuggestedTextStyle';
import TextStyle from './TextStyle';

/**
 * The RichLink model module.
 * @module model/RichLink
 * @version v1
 */
class RichLink {
    /**
     * Constructs a new <code>RichLink</code>.
     * A link to a Google resource (such as a file in Drive, a YouTube video, or a Calendar event).
     * @alias module:model/RichLink
     */
    constructor() { 
        
        RichLink.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RichLink</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RichLink} obj Optional instance to populate.
     * @return {module:model/RichLink} The populated <code>RichLink</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RichLink();

            if (data.hasOwnProperty('richLinkId')) {
                obj['richLinkId'] = ApiClient.convertToType(data['richLinkId'], 'String');
            }
            if (data.hasOwnProperty('richLinkProperties')) {
                obj['richLinkProperties'] = RichLinkProperties.constructFromObject(data['richLinkProperties']);
            }
            if (data.hasOwnProperty('suggestedDeletionIds')) {
                obj['suggestedDeletionIds'] = ApiClient.convertToType(data['suggestedDeletionIds'], ['String']);
            }
            if (data.hasOwnProperty('suggestedInsertionIds')) {
                obj['suggestedInsertionIds'] = ApiClient.convertToType(data['suggestedInsertionIds'], ['String']);
            }
            if (data.hasOwnProperty('suggestedTextStyleChanges')) {
                obj['suggestedTextStyleChanges'] = ApiClient.convertToType(data['suggestedTextStyleChanges'], {'String': SuggestedTextStyle});
            }
            if (data.hasOwnProperty('textStyle')) {
                obj['textStyle'] = TextStyle.constructFromObject(data['textStyle']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RichLink</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RichLink</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['richLinkId'] && !(typeof data['richLinkId'] === 'string' || data['richLinkId'] instanceof String)) {
            throw new Error("Expected the field `richLinkId` to be a primitive type in the JSON string but got " + data['richLinkId']);
        }
        // validate the optional field `richLinkProperties`
        if (data['richLinkProperties']) { // data not null
          RichLinkProperties.validateJSON(data['richLinkProperties']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedDeletionIds'])) {
            throw new Error("Expected the field `suggestedDeletionIds` to be an array in the JSON data but got " + data['suggestedDeletionIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedInsertionIds'])) {
            throw new Error("Expected the field `suggestedInsertionIds` to be an array in the JSON data but got " + data['suggestedInsertionIds']);
        }
        // validate the optional field `textStyle`
        if (data['textStyle']) { // data not null
          TextStyle.validateJSON(data['textStyle']);
        }

        return true;
    }


}



/**
 * Output only. The ID of this link.
 * @member {String} richLinkId
 */
RichLink.prototype['richLinkId'] = undefined;

/**
 * @member {module:model/RichLinkProperties} richLinkProperties
 */
RichLink.prototype['richLinkProperties'] = undefined;

/**
 * IDs for suggestions that remove this link from the document. A RichLink might have multiple deletion IDs if, for example, multiple users suggest deleting it. If empty, then this person link isn't suggested for deletion.
 * @member {Array.<String>} suggestedDeletionIds
 */
RichLink.prototype['suggestedDeletionIds'] = undefined;

/**
 * IDs for suggestions that insert this link into the document. A RichLink might have multiple insertion IDs if it's a nested suggested change (a suggestion within a suggestion made by a different user, for example). If empty, then this person link isn't a suggested insertion.
 * @member {Array.<String>} suggestedInsertionIds
 */
RichLink.prototype['suggestedInsertionIds'] = undefined;

/**
 * The suggested text style changes to this RichLink, keyed by suggestion ID.
 * @member {Object.<String, module:model/SuggestedTextStyle>} suggestedTextStyleChanges
 */
RichLink.prototype['suggestedTextStyleChanges'] = undefined;

/**
 * @member {module:model/TextStyle} textStyle
 */
RichLink.prototype['textStyle'] = undefined;






export default RichLink;

