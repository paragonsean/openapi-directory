/**
 * Google Docs API
 * Reads and writes Google Docs documents.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SuggestedTextStyle from './SuggestedTextStyle';
import TextStyle from './TextStyle';

/**
 * The AutoText model module.
 * @module model/AutoText
 * @version v1
 */
class AutoText {
    /**
     * Constructs a new <code>AutoText</code>.
     * A ParagraphElement representing a spot in the text that&#39;s dynamically replaced with content that can change over time, like a page number.
     * @alias module:model/AutoText
     */
    constructor() { 
        
        AutoText.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AutoText</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AutoText} obj Optional instance to populate.
     * @return {module:model/AutoText} The populated <code>AutoText</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AutoText();

            if (data.hasOwnProperty('suggestedDeletionIds')) {
                obj['suggestedDeletionIds'] = ApiClient.convertToType(data['suggestedDeletionIds'], ['String']);
            }
            if (data.hasOwnProperty('suggestedInsertionIds')) {
                obj['suggestedInsertionIds'] = ApiClient.convertToType(data['suggestedInsertionIds'], ['String']);
            }
            if (data.hasOwnProperty('suggestedTextStyleChanges')) {
                obj['suggestedTextStyleChanges'] = ApiClient.convertToType(data['suggestedTextStyleChanges'], {'String': SuggestedTextStyle});
            }
            if (data.hasOwnProperty('textStyle')) {
                obj['textStyle'] = TextStyle.constructFromObject(data['textStyle']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AutoText</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AutoText</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedDeletionIds'])) {
            throw new Error("Expected the field `suggestedDeletionIds` to be an array in the JSON data but got " + data['suggestedDeletionIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['suggestedInsertionIds'])) {
            throw new Error("Expected the field `suggestedInsertionIds` to be an array in the JSON data but got " + data['suggestedInsertionIds']);
        }
        // validate the optional field `textStyle`
        if (data['textStyle']) { // data not null
          TextStyle.validateJSON(data['textStyle']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * The suggested deletion IDs. If empty, then there are no suggested deletions of this content.
 * @member {Array.<String>} suggestedDeletionIds
 */
AutoText.prototype['suggestedDeletionIds'] = undefined;

/**
 * The suggested insertion IDs. An AutoText may have multiple insertion IDs if it's a nested suggested change. If empty, then this is not a suggested insertion.
 * @member {Array.<String>} suggestedInsertionIds
 */
AutoText.prototype['suggestedInsertionIds'] = undefined;

/**
 * The suggested text style changes to this AutoText, keyed by suggestion ID.
 * @member {Object.<String, module:model/SuggestedTextStyle>} suggestedTextStyleChanges
 */
AutoText.prototype['suggestedTextStyleChanges'] = undefined;

/**
 * @member {module:model/TextStyle} textStyle
 */
AutoText.prototype['textStyle'] = undefined;

/**
 * The type of this auto text.
 * @member {module:model/AutoText.TypeEnum} type
 */
AutoText.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
AutoText['TypeEnum'] = {

    /**
     * value: "TYPE_UNSPECIFIED"
     * @const
     */
    "TYPE_UNSPECIFIED": "TYPE_UNSPECIFIED",

    /**
     * value: "PAGE_NUMBER"
     * @const
     */
    "PAGE_NUMBER": "PAGE_NUMBER",

    /**
     * value: "PAGE_COUNT"
     * @const
     */
    "PAGE_COUNT": "PAGE_COUNT"
};



export default AutoText;

