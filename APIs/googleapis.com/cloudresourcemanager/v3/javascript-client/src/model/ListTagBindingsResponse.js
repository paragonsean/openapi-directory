/**
 * Cloud Resource Manager API
 * Creates, reads, and updates metadata for Google Cloud Platform resource containers.
 *
 * The version of the OpenAPI document: v3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TagBinding from './TagBinding';

/**
 * The ListTagBindingsResponse model module.
 * @module model/ListTagBindingsResponse
 * @version v3
 */
class ListTagBindingsResponse {
    /**
     * Constructs a new <code>ListTagBindingsResponse</code>.
     * The ListTagBindings response.
     * @alias module:model/ListTagBindingsResponse
     */
    constructor() { 
        
        ListTagBindingsResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ListTagBindingsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListTagBindingsResponse} obj Optional instance to populate.
     * @return {module:model/ListTagBindingsResponse} The populated <code>ListTagBindingsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListTagBindingsResponse();

            if (data.hasOwnProperty('nextPageToken')) {
                obj['nextPageToken'] = ApiClient.convertToType(data['nextPageToken'], 'String');
            }
            if (data.hasOwnProperty('tagBindings')) {
                obj['tagBindings'] = ApiClient.convertToType(data['tagBindings'], [TagBinding]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListTagBindingsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListTagBindingsResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['nextPageToken'] && !(typeof data['nextPageToken'] === 'string' || data['nextPageToken'] instanceof String)) {
            throw new Error("Expected the field `nextPageToken` to be a primitive type in the JSON string but got " + data['nextPageToken']);
        }
        if (data['tagBindings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tagBindings'])) {
                throw new Error("Expected the field `tagBindings` to be an array in the JSON data but got " + data['tagBindings']);
            }
            // validate the optional field `tagBindings` (array)
            for (const item of data['tagBindings']) {
                TagBinding.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Pagination token. If the result set is too large to fit in a single response, this token is returned. It encodes the position of the current result cursor. Feeding this value into a new list request with the `page_token` parameter gives the next page of the results. When `next_page_token` is not filled in, there is no next page and the list returned is the last page in the result set. Pagination tokens have a limited lifetime.
 * @member {String} nextPageToken
 */
ListTagBindingsResponse.prototype['nextPageToken'] = undefined;

/**
 * A possibly paginated list of TagBindings for the specified resource.
 * @member {Array.<module:model/TagBinding>} tagBindings
 */
ListTagBindingsResponse.prototype['tagBindings'] = undefined;






export default ListTagBindingsResponse;

