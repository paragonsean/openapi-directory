/**
 * Google Play Integrity API
 * The Play Integrity API helps you check that you're interacting with your genuine app on a genuine Android device powered by Google Play services. The Play Integrity API has replaced SafetyNet Attestation and Android Device Verification.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RequestDetails model module.
 * @module model/RequestDetails
 * @version v1
 */
class RequestDetails {
    /**
     * Constructs a new <code>RequestDetails</code>.
     * Contains the integrity request information.
     * @alias module:model/RequestDetails
     */
    constructor() { 
        
        RequestDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RequestDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RequestDetails} obj Optional instance to populate.
     * @return {module:model/RequestDetails} The populated <code>RequestDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RequestDetails();

            if (data.hasOwnProperty('nonce')) {
                obj['nonce'] = ApiClient.convertToType(data['nonce'], 'String');
            }
            if (data.hasOwnProperty('requestHash')) {
                obj['requestHash'] = ApiClient.convertToType(data['requestHash'], 'String');
            }
            if (data.hasOwnProperty('requestPackageName')) {
                obj['requestPackageName'] = ApiClient.convertToType(data['requestPackageName'], 'String');
            }
            if (data.hasOwnProperty('timestampMillis')) {
                obj['timestampMillis'] = ApiClient.convertToType(data['timestampMillis'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RequestDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RequestDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['nonce'] && !(typeof data['nonce'] === 'string' || data['nonce'] instanceof String)) {
            throw new Error("Expected the field `nonce` to be a primitive type in the JSON string but got " + data['nonce']);
        }
        // ensure the json data is a string
        if (data['requestHash'] && !(typeof data['requestHash'] === 'string' || data['requestHash'] instanceof String)) {
            throw new Error("Expected the field `requestHash` to be a primitive type in the JSON string but got " + data['requestHash']);
        }
        // ensure the json data is a string
        if (data['requestPackageName'] && !(typeof data['requestPackageName'] === 'string' || data['requestPackageName'] instanceof String)) {
            throw new Error("Expected the field `requestPackageName` to be a primitive type in the JSON string but got " + data['requestPackageName']);
        }
        // ensure the json data is a string
        if (data['timestampMillis'] && !(typeof data['timestampMillis'] === 'string' || data['timestampMillis'] instanceof String)) {
            throw new Error("Expected the field `timestampMillis` to be a primitive type in the JSON string but got " + data['timestampMillis']);
        }

        return true;
    }


}



/**
 * Nonce that was provided in the request (which is base64 web-safe no-wrap).
 * @member {String} nonce
 */
RequestDetails.prototype['nonce'] = undefined;

/**
 * Request hash that was provided in the request.
 * @member {String} requestHash
 */
RequestDetails.prototype['requestHash'] = undefined;

/**
 * Required. Application package name this attestation was requested for. Note: This field makes no guarantees or promises on the caller integrity. For details on application integrity, check application_integrity.
 * @member {String} requestPackageName
 */
RequestDetails.prototype['requestPackageName'] = undefined;

/**
 * Required. Timestamp, in milliseconds, of the integrity application request.
 * @member {String} timestampMillis
 */
RequestDetails.prototype['timestampMillis'] = undefined;






export default RequestDetails;

