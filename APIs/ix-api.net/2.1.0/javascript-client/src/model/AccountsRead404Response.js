/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProblemResponse from './ProblemResponse';

/**
 * The AccountsRead404Response model module.
 * @module model/AccountsRead404Response
 * @version 2.1.0
 */
class AccountsRead404Response {
    /**
     * Constructs a new <code>AccountsRead404Response</code>.
     * @alias module:model/AccountsRead404Response
     * @implements module:model/ProblemResponse
     * @param type {module:model/AccountsRead404Response.TypeEnum} 
     */
    constructor(type) { 
        ProblemResponse.initialize(this, type);
        AccountsRead404Response.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['type'] = type;
    }

    /**
     * Constructs a <code>AccountsRead404Response</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccountsRead404Response} obj Optional instance to populate.
     * @return {module:model/AccountsRead404Response} The populated <code>AccountsRead404Response</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccountsRead404Response();
            ProblemResponse.constructFromObject(data, obj);

            if (data.hasOwnProperty('detail')) {
                obj['detail'] = ApiClient.convertToType(data['detail'], 'String');
            }
            if (data.hasOwnProperty('instance')) {
                obj['instance'] = ApiClient.convertToType(data['instance'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], Object);
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], Object);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccountsRead404Response</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccountsRead404Response</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AccountsRead404Response.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['detail'] && !(typeof data['detail'] === 'string' || data['detail'] instanceof String)) {
            throw new Error("Expected the field `detail` to be a primitive type in the JSON string but got " + data['detail']);
        }
        // ensure the json data is a string
        if (data['instance'] && !(typeof data['instance'] === 'string' || data['instance'] instanceof String)) {
            throw new Error("Expected the field `instance` to be a primitive type in the JSON string but got " + data['instance']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

AccountsRead404Response.RequiredProperties = ["type"];

/**
 * A human-readable explanation specific to this occurrence of the problem.
 * @member {String} detail
 */
AccountsRead404Response.prototype['detail'] = undefined;

/**
 * A URI reference that identifies the specific occurrence of the problem.  It may or may not yield further information if dereferenced.
 * @member {String} instance
 */
AccountsRead404Response.prototype['instance'] = undefined;

/**
 * @member {Object} status
 */
AccountsRead404Response.prototype['status'] = undefined;

/**
 * @member {Object} title
 */
AccountsRead404Response.prototype['title'] = undefined;

/**
 * @member {module:model/AccountsRead404Response.TypeEnum} type
 */
AccountsRead404Response.prototype['type'] = undefined;


// Implement ProblemResponse interface:
/**
 * A human-readable explanation specific to this occurrence of the problem.
 * @member {String} detail
 */
ProblemResponse.prototype['detail'] = undefined;
/**
 * A URI reference that identifies the specific occurrence of the problem.  It may or may not yield further information if dereferenced.
 * @member {String} instance
 */
ProblemResponse.prototype['instance'] = undefined;
/**
 * The HTTP status code ([RFC7231], Section 6) generated by the origin server for this occurrence of the problem.
 * @member {Number} status
 */
ProblemResponse.prototype['status'] = undefined;
/**
 * A short, human-readable summary of the problem type.  It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization (e.g., using proactive content negotiation; see [RFC7231], Section 3.4). 
 * @member {String} title
 */
ProblemResponse.prototype['title'] = undefined;
/**
 * A URI reference (see RFC3986) that identifies the problem type.  This specification encourages that, when dereferenced, it provide human-readable documentation for the problem type (e.g., using HTML [W3C.REC-html5-20141028]).  When this member is not present, its value is assumed to be \"about:blank\". 
 * @member {String} type
 */
ProblemResponse.prototype['type'] = undefined;



/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
AccountsRead404Response['TypeEnum'] = {

    /**
     * value: "https://errors.ix-api.net/v2/not-found.html"
     * @const
     */
    "https://errors.ix-api.net/v2/not-found.html": "https://errors.ix-api.net/v2/not-found.html"
};



export default AccountsRead404Response;

