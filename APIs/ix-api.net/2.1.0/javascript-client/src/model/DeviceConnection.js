/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DeviceConnectionPartial from './DeviceConnectionPartial';

/**
 * The DeviceConnection model module.
 * @module model/DeviceConnection
 * @version 2.1.0
 */
class DeviceConnection {
    /**
     * Constructs a new <code>DeviceConnection</code>.
     * @alias module:model/DeviceConnection
     * @implements module:model/DeviceConnectionPartial
     * @param capacityMax {Number} 
     * @param connectedDevice {String} 
     * @param device {String} 
     * @param id {String} 
     */
    constructor(capacityMax, connectedDevice, device, id) { 
        DeviceConnectionPartial.initialize(this);
        DeviceConnection.initialize(this, capacityMax, connectedDevice, device, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, capacityMax, connectedDevice, device, id) { 
        obj['capacity_max'] = capacityMax;
        obj['connected_device'] = connectedDevice;
        obj['device'] = device;
        obj['id'] = id;
    }

    /**
     * Constructs a <code>DeviceConnection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeviceConnection} obj Optional instance to populate.
     * @return {module:model/DeviceConnection} The populated <code>DeviceConnection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeviceConnection();
            DeviceConnectionPartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('capacity_max')) {
                obj['capacity_max'] = ApiClient.convertToType(data['capacity_max'], 'Number');
            }
            if (data.hasOwnProperty('connected_device')) {
                obj['connected_device'] = ApiClient.convertToType(data['connected_device'], 'String');
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = ApiClient.convertToType(data['device'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeviceConnection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeviceConnection</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeviceConnection.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['connected_device'] && !(typeof data['connected_device'] === 'string' || data['connected_device'] instanceof String)) {
            throw new Error("Expected the field `connected_device` to be a primitive type in the JSON string but got " + data['connected_device']);
        }
        // ensure the json data is a string
        if (data['device'] && !(typeof data['device'] === 'string' || data['device'] instanceof String)) {
            throw new Error("Expected the field `device` to be a primitive type in the JSON string but got " + data['device']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }

        return true;
    }


}

DeviceConnection.RequiredProperties = ["capacity_max", "connected_device", "device", "id"];

/**
 * @member {Number} capacity_max
 */
DeviceConnection.prototype['capacity_max'] = undefined;

/**
 * @member {String} connected_device
 */
DeviceConnection.prototype['connected_device'] = undefined;

/**
 * @member {String} device
 */
DeviceConnection.prototype['device'] = undefined;

/**
 * @member {String} id
 */
DeviceConnection.prototype['id'] = undefined;


// Implement DeviceConnectionPartial interface:
/**
 * @member {Number} capacity_max
 */
DeviceConnectionPartial.prototype['capacity_max'] = undefined;
/**
 * @member {String} connected_device
 */
DeviceConnectionPartial.prototype['connected_device'] = undefined;
/**
 * @member {String} device
 */
DeviceConnectionPartial.prototype['device'] = undefined;
/**
 * @member {String} id
 */
DeviceConnectionPartial.prototype['id'] = undefined;




export default DeviceConnection;

