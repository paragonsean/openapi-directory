/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ManageablePartial from './ManageablePartial';

/**
 * The Manageable model module.
 * @module model/Manageable
 * @version 2.1.0
 */
class Manageable {
    /**
     * Constructs a new <code>Manageable</code>.
     * @alias module:model/Manageable
     * @implements module:model/ManageablePartial
     * @param managingAccount {String} The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
     */
    constructor(managingAccount) { 
        ManageablePartial.initialize(this);
        Manageable.initialize(this, managingAccount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, managingAccount) { 
        obj['managing_account'] = managingAccount;
    }

    /**
     * Constructs a <code>Manageable</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Manageable} obj Optional instance to populate.
     * @return {module:model/Manageable} The populated <code>Manageable</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Manageable();
            ManageablePartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('managing_account')) {
                obj['managing_account'] = ApiClient.convertToType(data['managing_account'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Manageable</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Manageable</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Manageable.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['managing_account'] && !(typeof data['managing_account'] === 'string' || data['managing_account'] instanceof String)) {
            throw new Error("Expected the field `managing_account` to be a primitive type in the JSON string but got " + data['managing_account']);
        }

        return true;
    }


}

Manageable.RequiredProperties = ["managing_account"];

/**
 * The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
 * @member {String} managing_account
 */
Manageable.prototype['managing_account'] = undefined;


// Implement ManageablePartial interface:
/**
 * The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
 * @member {String} managing_account
 */
ManageablePartial.prototype['managing_account'] = undefined;




export default Manageable;

