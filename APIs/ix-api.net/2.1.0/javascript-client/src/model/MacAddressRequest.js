/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MacAddressRequestPartial from './MacAddressRequestPartial';

/**
 * The MacAddressRequest model module.
 * @module model/MacAddressRequest
 * @version 2.1.0
 */
class MacAddressRequest {
    /**
     * Constructs a new <code>MacAddressRequest</code>.
     * @alias module:model/MacAddressRequest
     * @implements module:model/MacAddressRequestPartial
     * @param address {String} Unicast MAC address, formatted hexadecimal values with colons. 
     * @param consumingAccount {String} The `id` of the account consuming a service.  Used to be `owning_customer`. 
     * @param managingAccount {String} The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
     */
    constructor(address, consumingAccount, managingAccount) { 
        MacAddressRequestPartial.initialize(this);
        MacAddressRequest.initialize(this, address, consumingAccount, managingAccount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, address, consumingAccount, managingAccount) { 
        obj['address'] = address;
        obj['consuming_account'] = consumingAccount;
        obj['managing_account'] = managingAccount;
    }

    /**
     * Constructs a <code>MacAddressRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MacAddressRequest} obj Optional instance to populate.
     * @return {module:model/MacAddressRequest} The populated <code>MacAddressRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MacAddressRequest();
            MacAddressRequestPartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('consuming_account')) {
                obj['consuming_account'] = ApiClient.convertToType(data['consuming_account'], 'String');
            }
            if (data.hasOwnProperty('external_ref')) {
                obj['external_ref'] = ApiClient.convertToType(data['external_ref'], 'String');
            }
            if (data.hasOwnProperty('managing_account')) {
                obj['managing_account'] = ApiClient.convertToType(data['managing_account'], 'String');
            }
            if (data.hasOwnProperty('valid_not_after')) {
                obj['valid_not_after'] = ApiClient.convertToType(data['valid_not_after'], 'Date');
            }
            if (data.hasOwnProperty('valid_not_before')) {
                obj['valid_not_before'] = ApiClient.convertToType(data['valid_not_before'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MacAddressRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MacAddressRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MacAddressRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['consuming_account'] && !(typeof data['consuming_account'] === 'string' || data['consuming_account'] instanceof String)) {
            throw new Error("Expected the field `consuming_account` to be a primitive type in the JSON string but got " + data['consuming_account']);
        }
        // ensure the json data is a string
        if (data['external_ref'] && !(typeof data['external_ref'] === 'string' || data['external_ref'] instanceof String)) {
            throw new Error("Expected the field `external_ref` to be a primitive type in the JSON string but got " + data['external_ref']);
        }
        // ensure the json data is a string
        if (data['managing_account'] && !(typeof data['managing_account'] === 'string' || data['managing_account'] instanceof String)) {
            throw new Error("Expected the field `managing_account` to be a primitive type in the JSON string but got " + data['managing_account']);
        }

        return true;
    }


}

MacAddressRequest.RequiredProperties = ["address", "consuming_account", "managing_account"];

/**
 * Unicast MAC address, formatted hexadecimal values with colons. 
 * @member {String} address
 */
MacAddressRequest.prototype['address'] = undefined;

/**
 * The `id` of the account consuming a service.  Used to be `owning_customer`. 
 * @member {String} consuming_account
 */
MacAddressRequest.prototype['consuming_account'] = undefined;

/**
 * Reference field, free to use for the API user.
 * @member {String} external_ref
 */
MacAddressRequest.prototype['external_ref'] = undefined;

/**
 * The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
 * @member {String} managing_account
 */
MacAddressRequest.prototype['managing_account'] = undefined;

/**
 * @member {Date} valid_not_after
 */
MacAddressRequest.prototype['valid_not_after'] = undefined;

/**
 * @member {Date} valid_not_before
 */
MacAddressRequest.prototype['valid_not_before'] = undefined;


// Implement MacAddressRequestPartial interface:
/**
 * Unicast MAC address, formatted hexadecimal values with colons. 
 * @member {String} address
 */
MacAddressRequestPartial.prototype['address'] = undefined;
/**
 * The `id` of the account consuming a service.  Used to be `owning_customer`. 
 * @member {String} consuming_account
 */
MacAddressRequestPartial.prototype['consuming_account'] = undefined;
/**
 * Reference field, free to use for the API user.
 * @member {String} external_ref
 */
MacAddressRequestPartial.prototype['external_ref'] = undefined;
/**
 * The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
 * @member {String} managing_account
 */
MacAddressRequestPartial.prototype['managing_account'] = undefined;
/**
 * @member {Date} valid_not_after
 */
MacAddressRequestPartial.prototype['valid_not_after'] = undefined;
/**
 * @member {Date} valid_not_before
 */
MacAddressRequestPartial.prototype['valid_not_before'] = undefined;




export default MacAddressRequest;

