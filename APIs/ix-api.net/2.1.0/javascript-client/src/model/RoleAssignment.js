/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RoleAssignmentPartial from './RoleAssignmentPartial';

/**
 * The RoleAssignment model module.
 * @module model/RoleAssignment
 * @version 2.1.0
 */
class RoleAssignment {
    /**
     * Constructs a new <code>RoleAssignment</code>.
     * @alias module:model/RoleAssignment
     * @implements module:model/RoleAssignmentPartial
     * @param contact {String} The `id` of a contact the role is assigned to. 
     * @param id {String} 
     * @param role {String} The `id` of a role the contact is assigned to. 
     */
    constructor(contact, id, role) { 
        RoleAssignmentPartial.initialize(this);
        RoleAssignment.initialize(this, contact, id, role);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contact, id, role) { 
        obj['contact'] = contact;
        obj['id'] = id;
        obj['role'] = role;
    }

    /**
     * Constructs a <code>RoleAssignment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoleAssignment} obj Optional instance to populate.
     * @return {module:model/RoleAssignment} The populated <code>RoleAssignment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoleAssignment();
            RoleAssignmentPartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('contact')) {
                obj['contact'] = ApiClient.convertToType(data['contact'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoleAssignment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoleAssignment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoleAssignment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['contact'] && !(typeof data['contact'] === 'string' || data['contact'] instanceof String)) {
            throw new Error("Expected the field `contact` to be a primitive type in the JSON string but got " + data['contact']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }

        return true;
    }


}

RoleAssignment.RequiredProperties = ["contact", "id", "role"];

/**
 * The `id` of a contact the role is assigned to. 
 * @member {String} contact
 */
RoleAssignment.prototype['contact'] = undefined;

/**
 * @member {String} id
 */
RoleAssignment.prototype['id'] = undefined;

/**
 * The `id` of a role the contact is assigned to. 
 * @member {String} role
 */
RoleAssignment.prototype['role'] = undefined;


// Implement RoleAssignmentPartial interface:
/**
 * The `id` of a contact the role is assigned to. 
 * @member {String} contact
 */
RoleAssignmentPartial.prototype['contact'] = undefined;
/**
 * @member {String} id
 */
RoleAssignmentPartial.prototype['id'] = undefined;
/**
 * The `id` of a role the contact is assigned to. 
 * @member {String} role
 */
RoleAssignmentPartial.prototype['role'] = undefined;




export default RoleAssignment;

