/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RoleRequestPartial from './RoleRequestPartial';

/**
 * The RoleRequest model module.
 * @module model/RoleRequest
 * @version 2.1.0
 */
class RoleRequest {
    /**
     * Constructs a new <code>RoleRequest</code>.
     * @alias module:model/RoleRequest
     * @implements module:model/RoleRequestPartial
     * @param name {String} The name of the role. 
     * @param requiredFields {Array.<String>} A list of required field names. 
     */
    constructor(name, requiredFields) { 
        RoleRequestPartial.initialize(this);
        RoleRequest.initialize(this, name, requiredFields);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, requiredFields) { 
        obj['name'] = name;
        obj['required_fields'] = requiredFields;
    }

    /**
     * Constructs a <code>RoleRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoleRequest} obj Optional instance to populate.
     * @return {module:model/RoleRequest} The populated <code>RoleRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoleRequest();
            RoleRequestPartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('required_fields')) {
                obj['required_fields'] = ApiClient.convertToType(data['required_fields'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoleRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoleRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoleRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['required_fields'])) {
            throw new Error("Expected the field `required_fields` to be an array in the JSON data but got " + data['required_fields']);
        }

        return true;
    }


}

RoleRequest.RequiredProperties = ["name", "required_fields"];

/**
 * The name of the role. 
 * @member {String} name
 */
RoleRequest.prototype['name'] = undefined;

/**
 * A list of required field names. 
 * @member {Array.<String>} required_fields
 */
RoleRequest.prototype['required_fields'] = undefined;


// Implement RoleRequestPartial interface:
/**
 * The name of the role. 
 * @member {String} name
 */
RoleRequestPartial.prototype['name'] = undefined;
/**
 * A list of required field names. 
 * @member {Array.<String>} required_fields
 */
RoleRequestPartial.prototype['required_fields'] = undefined;




export default RoleRequest;

