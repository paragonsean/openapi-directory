/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContactablePartial from './ContactablePartial';

/**
 * The Contactable model module.
 * @module model/Contactable
 * @version 2.1.0
 */
class Contactable {
    /**
     * Constructs a new <code>Contactable</code>.
     * @alias module:model/Contactable
     * @implements module:model/ContactablePartial
     * @param roleAssignments {Array.<String>} A set of `RoleAssignment`s. See the documentation on the specific `required_contact_roles`, `nfc_required_contact_roles` or `nsc_required_contact_roles` on what `RoleAssignment`s to provide. 
     */
    constructor(roleAssignments) { 
        ContactablePartial.initialize(this);
        Contactable.initialize(this, roleAssignments);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, roleAssignments) { 
        obj['role_assignments'] = roleAssignments;
    }

    /**
     * Constructs a <code>Contactable</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Contactable} obj Optional instance to populate.
     * @return {module:model/Contactable} The populated <code>Contactable</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Contactable();
            ContactablePartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('role_assignments')) {
                obj['role_assignments'] = ApiClient.convertToType(data['role_assignments'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Contactable</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Contactable</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Contactable.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['role_assignments'])) {
            throw new Error("Expected the field `role_assignments` to be an array in the JSON data but got " + data['role_assignments']);
        }

        return true;
    }


}

Contactable.RequiredProperties = ["role_assignments"];

/**
 * A set of `RoleAssignment`s. See the documentation on the specific `required_contact_roles`, `nfc_required_contact_roles` or `nsc_required_contact_roles` on what `RoleAssignment`s to provide. 
 * @member {Array.<String>} role_assignments
 */
Contactable.prototype['role_assignments'] = undefined;


// Implement ContactablePartial interface:
/**
 * A set of `RoleAssignment`s. See the documentation on the specific `required_contact_roles`, `nfc_required_contact_roles` or `nsc_required_contact_roles` on what `RoleAssignment`s to provide. 
 * @member {Array.<String>} role_assignments
 */
ContactablePartial.prototype['role_assignments'] = undefined;




export default Contactable;

