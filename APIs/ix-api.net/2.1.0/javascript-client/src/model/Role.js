/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RolePartial from './RolePartial';

/**
 * The Role model module.
 * @module model/Role
 * @version 2.1.0
 */
class Role {
    /**
     * Constructs a new <code>Role</code>.
     * @alias module:model/Role
     * @implements module:model/RolePartial
     * @param id {String} 
     * @param name {String} The name of the role. 
     * @param requiredFields {Array.<String>} A list of required field names. 
     */
    constructor(id, name, requiredFields) { 
        RolePartial.initialize(this);
        Role.initialize(this, id, name, requiredFields);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, requiredFields) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['required_fields'] = requiredFields;
    }

    /**
     * Constructs a <code>Role</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Role} obj Optional instance to populate.
     * @return {module:model/Role} The populated <code>Role</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Role();
            RolePartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('required_fields')) {
                obj['required_fields'] = ApiClient.convertToType(data['required_fields'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Role</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Role</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Role.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['required_fields'])) {
            throw new Error("Expected the field `required_fields` to be an array in the JSON data but got " + data['required_fields']);
        }

        return true;
    }


}

Role.RequiredProperties = ["id", "name", "required_fields"];

/**
 * @member {String} id
 */
Role.prototype['id'] = undefined;

/**
 * The name of the role. 
 * @member {String} name
 */
Role.prototype['name'] = undefined;

/**
 * A list of required field names. 
 * @member {Array.<String>} required_fields
 */
Role.prototype['required_fields'] = undefined;


// Implement RolePartial interface:
/**
 * @member {String} id
 */
RolePartial.prototype['id'] = undefined;
/**
 * The name of the role. 
 * @member {String} name
 */
RolePartial.prototype['name'] = undefined;
/**
 * A list of required field names. 
 * @member {Array.<String>} required_fields
 */
RolePartial.prototype['required_fields'] = undefined;




export default Role;

