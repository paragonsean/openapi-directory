/**
 * IX-API
 *  This API allows to config/change/delete Internet Exchange services.  # Filters  When querying collections, the provided query parameters are validated. Unknown query parameters are ignored. Providing invalid filter values should yield a validation error. 
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DenyMemberJoiningRuleUpdatePartial from './DenyMemberJoiningRuleUpdatePartial';

/**
 * The DenyMemberJoiningRuleUpdate model module.
 * @module model/DenyMemberJoiningRuleUpdate
 * @version 2.1.0
 */
class DenyMemberJoiningRuleUpdate {
    /**
     * Constructs a new <code>DenyMemberJoiningRuleUpdate</code>.
     * @alias module:model/DenyMemberJoiningRuleUpdate
     * @implements module:model/DenyMemberJoiningRuleUpdatePartial
     * @param consumingAccount {String} The `id` of the account to which access to the network service should be granted or denied. 
     * @param managingAccount {String} The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
     * @param type {String} 
     */
    constructor(consumingAccount, managingAccount, type) { 
        DenyMemberJoiningRuleUpdatePartial.initialize(this, type);
        DenyMemberJoiningRuleUpdate.initialize(this, consumingAccount, managingAccount, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, consumingAccount, managingAccount, type) { 
        obj['consuming_account'] = consumingAccount;
        obj['managing_account'] = managingAccount;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>DenyMemberJoiningRuleUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DenyMemberJoiningRuleUpdate} obj Optional instance to populate.
     * @return {module:model/DenyMemberJoiningRuleUpdate} The populated <code>DenyMemberJoiningRuleUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DenyMemberJoiningRuleUpdate();
            DenyMemberJoiningRuleUpdatePartial.constructFromObject(data, obj);

            if (data.hasOwnProperty('consuming_account')) {
                obj['consuming_account'] = ApiClient.convertToType(data['consuming_account'], 'String');
            }
            if (data.hasOwnProperty('external_ref')) {
                obj['external_ref'] = ApiClient.convertToType(data['external_ref'], 'String');
            }
            if (data.hasOwnProperty('managing_account')) {
                obj['managing_account'] = ApiClient.convertToType(data['managing_account'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DenyMemberJoiningRuleUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DenyMemberJoiningRuleUpdate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DenyMemberJoiningRuleUpdate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['consuming_account'] && !(typeof data['consuming_account'] === 'string' || data['consuming_account'] instanceof String)) {
            throw new Error("Expected the field `consuming_account` to be a primitive type in the JSON string but got " + data['consuming_account']);
        }
        // ensure the json data is a string
        if (data['external_ref'] && !(typeof data['external_ref'] === 'string' || data['external_ref'] instanceof String)) {
            throw new Error("Expected the field `external_ref` to be a primitive type in the JSON string but got " + data['external_ref']);
        }
        // ensure the json data is a string
        if (data['managing_account'] && !(typeof data['managing_account'] === 'string' || data['managing_account'] instanceof String)) {
            throw new Error("Expected the field `managing_account` to be a primitive type in the JSON string but got " + data['managing_account']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

DenyMemberJoiningRuleUpdate.RequiredProperties = ["consuming_account", "managing_account", "type"];

/**
 * The `id` of the account to which access to the network service should be granted or denied. 
 * @member {String} consuming_account
 */
DenyMemberJoiningRuleUpdate.prototype['consuming_account'] = undefined;

/**
 * Reference field, free to use for the API user.
 * @member {String} external_ref
 */
DenyMemberJoiningRuleUpdate.prototype['external_ref'] = undefined;

/**
 * The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
 * @member {String} managing_account
 */
DenyMemberJoiningRuleUpdate.prototype['managing_account'] = undefined;

/**
 * @member {String} type
 */
DenyMemberJoiningRuleUpdate.prototype['type'] = undefined;


// Implement DenyMemberJoiningRuleUpdatePartial interface:
/**
 * The `id` of the account to which access to the network service should be granted or denied. 
 * @member {String} consuming_account
 */
DenyMemberJoiningRuleUpdatePartial.prototype['consuming_account'] = undefined;
/**
 * Reference field, free to use for the API user.
 * @member {String} external_ref
 */
DenyMemberJoiningRuleUpdatePartial.prototype['external_ref'] = undefined;
/**
 * The `id` of the account responsible for managing the service via the API. A manager can read and update the state of entities. 
 * @member {String} managing_account
 */
DenyMemberJoiningRuleUpdatePartial.prototype['managing_account'] = undefined;
/**
 * @member {String} type
 */
DenyMemberJoiningRuleUpdatePartial.prototype['type'] = undefined;




export default DenyMemberJoiningRuleUpdate;

