/**
 * PowerTools Developer
 * Apptigent PowerTools Developer Edition is a powerful suite of API endpoints for custom applications running on any stack. Manipulate text, modify collections, format dates and times, convert currency, perform advanced mathematical calculations, shorten URL's, encode strings, convert text to speech, translate content into multiple languages, process images, and more. PowerTools is the ultimate developer toolkit.
 *
 * The version of the OpenAPI document: 2021.1.01
 * Contact: support@apptigent.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The InputCollectionSearch model module.
 * @module model/InputCollectionSearch
 * @version 2021.1.01
 */
class InputCollectionSearch {
    /**
     * Constructs a new <code>InputCollectionSearch</code>.
     * @alias module:model/InputCollectionSearch
     * @param input {Array.<String>} Collection of strings to search
     * @param match {String} Text to match
     */
    constructor(input, match) { 
        
        InputCollectionSearch.initialize(this, input, match);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, input, match) { 
        obj['input'] = input;
        obj['match'] = match;
    }

    /**
     * Constructs a <code>InputCollectionSearch</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InputCollectionSearch} obj Optional instance to populate.
     * @return {module:model/InputCollectionSearch} The populated <code>InputCollectionSearch</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InputCollectionSearch();

            if (data.hasOwnProperty('ignorecase')) {
                obj['ignorecase'] = ApiClient.convertToType(data['ignorecase'], 'String');
            }
            if (data.hasOwnProperty('input')) {
                obj['input'] = ApiClient.convertToType(data['input'], ['String']);
            }
            if (data.hasOwnProperty('match')) {
                obj['match'] = ApiClient.convertToType(data['match'], 'String');
            }
            if (data.hasOwnProperty('trim')) {
                obj['trim'] = ApiClient.convertToType(data['trim'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InputCollectionSearch</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InputCollectionSearch</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InputCollectionSearch.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ignorecase'] && !(typeof data['ignorecase'] === 'string' || data['ignorecase'] instanceof String)) {
            throw new Error("Expected the field `ignorecase` to be a primitive type in the JSON string but got " + data['ignorecase']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['input'])) {
            throw new Error("Expected the field `input` to be an array in the JSON data but got " + data['input']);
        }
        // ensure the json data is a string
        if (data['match'] && !(typeof data['match'] === 'string' || data['match'] instanceof String)) {
            throw new Error("Expected the field `match` to be a primitive type in the JSON string but got " + data['match']);
        }
        // ensure the json data is a string
        if (data['trim'] && !(typeof data['trim'] === 'string' || data['trim'] instanceof String)) {
            throw new Error("Expected the field `trim` to be a primitive type in the JSON string but got " + data['trim']);
        }

        return true;
    }


}

InputCollectionSearch.RequiredProperties = ["input", "match"];

/**
 * Ignore case when performing comparison
 * @member {module:model/InputCollectionSearch.IgnorecaseEnum} ignorecase
 */
InputCollectionSearch.prototype['ignorecase'] = undefined;

/**
 * Collection of strings to search
 * @member {Array.<String>} input
 */
InputCollectionSearch.prototype['input'] = undefined;

/**
 * Text to match
 * @member {String} match
 */
InputCollectionSearch.prototype['match'] = undefined;

/**
 * Trim white space from comparison string
 * @member {module:model/InputCollectionSearch.TrimEnum} trim
 */
InputCollectionSearch.prototype['trim'] = undefined;





/**
 * Allowed values for the <code>ignorecase</code> property.
 * @enum {String}
 * @readonly
 */
InputCollectionSearch['IgnorecaseEnum'] = {

    /**
     * value: "true"
     * @const
     */
    "true": "true",

    /**
     * value: "false"
     * @const
     */
    "false": "false"
};


/**
 * Allowed values for the <code>trim</code> property.
 * @enum {String}
 * @readonly
 */
InputCollectionSearch['TrimEnum'] = {

    /**
     * value: "true"
     * @const
     */
    "true": "true",

    /**
     * value: "false"
     * @const
     */
    "false": "false"
};



export default InputCollectionSearch;

