/**
 * PowerTools Developer
 * Apptigent PowerTools Developer Edition is a powerful suite of API endpoints for custom applications running on any stack. Manipulate text, modify collections, format dates and times, convert currency, perform advanced mathematical calculations, shorten URL's, encode strings, convert text to speech, translate content into multiple languages, process images, and more. PowerTools is the ultimate developer toolkit.
 *
 * The version of the OpenAPI document: 2021.1.01
 * Contact: support@apptigent.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The InputStringComparison model module.
 * @module model/InputStringComparison
 * @version 2021.1.01
 */
class InputStringComparison {
    /**
     * Constructs a new <code>InputStringComparison</code>.
     * @alias module:model/InputStringComparison
     * @param compare {String} Comparison string
     * @param input {String} Original string
     * @param lower {module:model/InputStringComparison.LowerEnum} Convert strings to lowercase before comparison
     * @param trim {module:model/InputStringComparison.TrimEnum} Trim strings before comparison
     */
    constructor(compare, input, lower, trim) { 
        
        InputStringComparison.initialize(this, compare, input, lower, trim);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, compare, input, lower, trim) { 
        obj['compare'] = compare;
        obj['input'] = input;
        obj['lower'] = lower;
        obj['trim'] = trim;
    }

    /**
     * Constructs a <code>InputStringComparison</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InputStringComparison} obj Optional instance to populate.
     * @return {module:model/InputStringComparison} The populated <code>InputStringComparison</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InputStringComparison();

            if (data.hasOwnProperty('compare')) {
                obj['compare'] = ApiClient.convertToType(data['compare'], 'String');
            }
            if (data.hasOwnProperty('input')) {
                obj['input'] = ApiClient.convertToType(data['input'], 'String');
            }
            if (data.hasOwnProperty('lower')) {
                obj['lower'] = ApiClient.convertToType(data['lower'], 'String');
            }
            if (data.hasOwnProperty('trim')) {
                obj['trim'] = ApiClient.convertToType(data['trim'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InputStringComparison</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InputStringComparison</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InputStringComparison.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['compare'] && !(typeof data['compare'] === 'string' || data['compare'] instanceof String)) {
            throw new Error("Expected the field `compare` to be a primitive type in the JSON string but got " + data['compare']);
        }
        // ensure the json data is a string
        if (data['input'] && !(typeof data['input'] === 'string' || data['input'] instanceof String)) {
            throw new Error("Expected the field `input` to be a primitive type in the JSON string but got " + data['input']);
        }
        // ensure the json data is a string
        if (data['lower'] && !(typeof data['lower'] === 'string' || data['lower'] instanceof String)) {
            throw new Error("Expected the field `lower` to be a primitive type in the JSON string but got " + data['lower']);
        }
        // ensure the json data is a string
        if (data['trim'] && !(typeof data['trim'] === 'string' || data['trim'] instanceof String)) {
            throw new Error("Expected the field `trim` to be a primitive type in the JSON string but got " + data['trim']);
        }

        return true;
    }


}

InputStringComparison.RequiredProperties = ["compare", "input", "lower", "trim"];

/**
 * Comparison string
 * @member {String} compare
 */
InputStringComparison.prototype['compare'] = undefined;

/**
 * Original string
 * @member {String} input
 */
InputStringComparison.prototype['input'] = undefined;

/**
 * Convert strings to lowercase before comparison
 * @member {module:model/InputStringComparison.LowerEnum} lower
 */
InputStringComparison.prototype['lower'] = undefined;

/**
 * Trim strings before comparison
 * @member {module:model/InputStringComparison.TrimEnum} trim
 */
InputStringComparison.prototype['trim'] = undefined;





/**
 * Allowed values for the <code>lower</code> property.
 * @enum {String}
 * @readonly
 */
InputStringComparison['LowerEnum'] = {

    /**
     * value: "true"
     * @const
     */
    "true": "true",

    /**
     * value: "false"
     * @const
     */
    "false": "false"
};


/**
 * Allowed values for the <code>trim</code> property.
 * @enum {String}
 * @readonly
 */
InputStringComparison['TrimEnum'] = {

    /**
     * value: "true"
     * @const
     */
    "true": "true",

    /**
     * value: "false"
     * @const
     */
    "false": "false"
};



export default InputStringComparison;

