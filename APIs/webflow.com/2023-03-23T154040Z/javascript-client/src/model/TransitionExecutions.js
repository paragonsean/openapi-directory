/**
 * Lucidtech API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2023-03-23T15:40:40Z
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LogsTransitionId from './LogsTransitionId';
import TransitionExecutionsExecutionsInner from './TransitionExecutionsExecutionsInner';

/**
 * The TransitionExecutions model module.
 * @module model/TransitionExecutions
 * @version 2023-03-23T15:40:40Z
 */
class TransitionExecutions {
    /**
     * Constructs a new <code>TransitionExecutions</code>.
     * @alias module:model/TransitionExecutions
     * @param executions {Array.<module:model/TransitionExecutionsExecutionsInner>} 
     * @param nextToken {String} 
     * @param transitionId {module:model/LogsTransitionId} 
     */
    constructor(executions, nextToken, transitionId) { 
        
        TransitionExecutions.initialize(this, executions, nextToken, transitionId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, executions, nextToken, transitionId) { 
        obj['executions'] = executions;
        obj['nextToken'] = nextToken;
        obj['transitionId'] = transitionId;
    }

    /**
     * Constructs a <code>TransitionExecutions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransitionExecutions} obj Optional instance to populate.
     * @return {module:model/TransitionExecutions} The populated <code>TransitionExecutions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransitionExecutions();

            if (data.hasOwnProperty('executions')) {
                obj['executions'] = ApiClient.convertToType(data['executions'], [TransitionExecutionsExecutionsInner]);
            }
            if (data.hasOwnProperty('nextToken')) {
                obj['nextToken'] = ApiClient.convertToType(data['nextToken'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], ['String']);
            }
            if (data.hasOwnProperty('transitionId')) {
                obj['transitionId'] = LogsTransitionId.constructFromObject(data['transitionId']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransitionExecutions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransitionExecutions</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransitionExecutions.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['executions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['executions'])) {
                throw new Error("Expected the field `executions` to be an array in the JSON data but got " + data['executions']);
            }
            // validate the optional field `executions` (array)
            for (const item of data['executions']) {
                TransitionExecutionsExecutionsInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextToken'] && !(typeof data['nextToken'] === 'string' || data['nextToken'] instanceof String)) {
            throw new Error("Expected the field `nextToken` to be a primitive type in the JSON string but got " + data['nextToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['status'])) {
            throw new Error("Expected the field `status` to be an array in the JSON data but got " + data['status']);
        }
        // validate the optional field `transitionId`
        if (data['transitionId']) { // data not null
          LogsTransitionId.validateJSON(data['transitionId']);
        }

        return true;
    }


}

TransitionExecutions.RequiredProperties = ["executions", "nextToken", "transitionId"];

/**
 * @member {Array.<module:model/TransitionExecutionsExecutionsInner>} executions
 */
TransitionExecutions.prototype['executions'] = undefined;

/**
 * @member {String} nextToken
 */
TransitionExecutions.prototype['nextToken'] = undefined;

/**
 * @member {Array.<module:model/TransitionExecutions.StatusEnum>} status
 */
TransitionExecutions.prototype['status'] = undefined;

/**
 * @member {module:model/LogsTransitionId} transitionId
 */
TransitionExecutions.prototype['transitionId'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
TransitionExecutions['StatusEnum'] = {

    /**
     * value: "running"
     * @const
     */
    "running": "running",

    /**
     * value: "succeeded"
     * @const
     */
    "succeeded": "succeeded",

    /**
     * value: "failed"
     * @const
     */
    "failed": "failed",

    /**
     * value: "rejected"
     * @const
     */
    "rejected": "rejected",

    /**
     * value: "retry"
     * @const
     */
    "retry": "retry"
};



export default TransitionExecutions;

