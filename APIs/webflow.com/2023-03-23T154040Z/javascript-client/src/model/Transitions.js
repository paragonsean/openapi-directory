/**
 * Lucidtech API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2023-03-23T15:40:40Z
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransitionsTransitionsInner from './TransitionsTransitionsInner';

/**
 * The Transitions model module.
 * @module model/Transitions
 * @version 2023-03-23T15:40:40Z
 */
class Transitions {
    /**
     * Constructs a new <code>Transitions</code>.
     * @alias module:model/Transitions
     * @param nextToken {String} 
     * @param transitions {Array.<module:model/TransitionsTransitionsInner>} 
     */
    constructor(nextToken, transitions) { 
        
        Transitions.initialize(this, nextToken, transitions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, nextToken, transitions) { 
        obj['nextToken'] = nextToken;
        obj['transitions'] = transitions;
    }

    /**
     * Constructs a <code>Transitions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Transitions} obj Optional instance to populate.
     * @return {module:model/Transitions} The populated <code>Transitions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Transitions();

            if (data.hasOwnProperty('nextToken')) {
                obj['nextToken'] = ApiClient.convertToType(data['nextToken'], 'String');
            }
            if (data.hasOwnProperty('transitionType')) {
                obj['transitionType'] = ApiClient.convertToType(data['transitionType'], ['String']);
            }
            if (data.hasOwnProperty('transitions')) {
                obj['transitions'] = ApiClient.convertToType(data['transitions'], [TransitionsTransitionsInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Transitions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Transitions</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Transitions.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['nextToken'] && !(typeof data['nextToken'] === 'string' || data['nextToken'] instanceof String)) {
            throw new Error("Expected the field `nextToken` to be a primitive type in the JSON string but got " + data['nextToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['transitionType'])) {
            throw new Error("Expected the field `transitionType` to be an array in the JSON data but got " + data['transitionType']);
        }
        if (data['transitions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['transitions'])) {
                throw new Error("Expected the field `transitions` to be an array in the JSON data but got " + data['transitions']);
            }
            // validate the optional field `transitions` (array)
            for (const item of data['transitions']) {
                TransitionsTransitionsInner.validateJSON(item);
            };
        }

        return true;
    }


}

Transitions.RequiredProperties = ["nextToken", "transitions"];

/**
 * @member {String} nextToken
 */
Transitions.prototype['nextToken'] = undefined;

/**
 * @member {Array.<module:model/Transitions.TransitionTypeEnum>} transitionType
 */
Transitions.prototype['transitionType'] = undefined;

/**
 * @member {Array.<module:model/TransitionsTransitionsInner>} transitions
 */
Transitions.prototype['transitions'] = undefined;





/**
 * Allowed values for the <code>transitionType</code> property.
 * @enum {String}
 * @readonly
 */
Transitions['TransitionTypeEnum'] = {

    /**
     * value: "docker"
     * @const
     */
    "docker": "docker",

    /**
     * value: "manual"
     * @const
     */
    "manual": "manual"
};



export default Transitions;

