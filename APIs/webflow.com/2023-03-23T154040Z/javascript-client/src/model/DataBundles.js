/**
 * Lucidtech API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2023-03-23T15:40:40Z
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DataBundlesDataBundlesInner from './DataBundlesDataBundlesInner';

/**
 * The DataBundles model module.
 * @module model/DataBundles
 * @version 2023-03-23T15:40:40Z
 */
class DataBundles {
    /**
     * Constructs a new <code>DataBundles</code>.
     * @alias module:model/DataBundles
     * @param dataBundles {Array.<module:model/DataBundlesDataBundlesInner>} 
     * @param nextToken {String} 
     */
    constructor(dataBundles, nextToken) { 
        
        DataBundles.initialize(this, dataBundles, nextToken);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dataBundles, nextToken) { 
        obj['dataBundles'] = dataBundles;
        obj['nextToken'] = nextToken;
    }

    /**
     * Constructs a <code>DataBundles</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DataBundles} obj Optional instance to populate.
     * @return {module:model/DataBundles} The populated <code>DataBundles</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DataBundles();

            if (data.hasOwnProperty('dataBundles')) {
                obj['dataBundles'] = ApiClient.convertToType(data['dataBundles'], [DataBundlesDataBundlesInner]);
            }
            if (data.hasOwnProperty('nextToken')) {
                obj['nextToken'] = ApiClient.convertToType(data['nextToken'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DataBundles</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DataBundles</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DataBundles.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['dataBundles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dataBundles'])) {
                throw new Error("Expected the field `dataBundles` to be an array in the JSON data but got " + data['dataBundles']);
            }
            // validate the optional field `dataBundles` (array)
            for (const item of data['dataBundles']) {
                DataBundlesDataBundlesInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextToken'] && !(typeof data['nextToken'] === 'string' || data['nextToken'] instanceof String)) {
            throw new Error("Expected the field `nextToken` to be a primitive type in the JSON string but got " + data['nextToken']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['status'])) {
            throw new Error("Expected the field `status` to be an array in the JSON data but got " + data['status']);
        }

        return true;
    }


}

DataBundles.RequiredProperties = ["dataBundles", "nextToken"];

/**
 * @member {Array.<module:model/DataBundlesDataBundlesInner>} dataBundles
 */
DataBundles.prototype['dataBundles'] = undefined;

/**
 * @member {String} nextToken
 */
DataBundles.prototype['nextToken'] = undefined;

/**
 * @member {Array.<module:model/DataBundles.StatusEnum>} status
 */
DataBundles.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
DataBundles['StatusEnum'] = {

    /**
     * value: "running"
     * @const
     */
    "running": "running",

    /**
     * value: "succeeded"
     * @const
     */
    "succeeded": "succeeded",

    /**
     * value: "failed"
     * @const
     */
    "failed": "failed"
};



export default DataBundles;

