/**
 * TCGdex API
 * A Multilanguage Pokémon TCG Database with Cards Pictures and most of the informations contained on the cards. You can find out more about TCGdex at [https://www.tcgdex.net](https://www.tcgdex.net) or on [Discord](https://discord.gg/NehYTAhsZE).
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: contact@tcgdex.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SetResume from './SetResume';

/**
 * The Serie model module.
 * @module model/Serie
 * @version 2.0.0
 */
class Serie {
    /**
     * Constructs a new <code>Serie</code>.
     * @alias module:model/Serie
     * @param id {String} 
     * @param name {String} 
     * @param sets {Array.<module:model/SetResume>} 
     */
    constructor(id, name, sets) { 
        
        Serie.initialize(this, id, name, sets);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, sets) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['sets'] = sets;
    }

    /**
     * Constructs a <code>Serie</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Serie} obj Optional instance to populate.
     * @return {module:model/Serie} The populated <code>Serie</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Serie();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('logo')) {
                obj['logo'] = ApiClient.convertToType(data['logo'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('sets')) {
                obj['sets'] = ApiClient.convertToType(data['sets'], [SetResume]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Serie</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Serie</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Serie.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['logo'] && !(typeof data['logo'] === 'string' || data['logo'] instanceof String)) {
            throw new Error("Expected the field `logo` to be a primitive type in the JSON string but got " + data['logo']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['sets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sets'])) {
                throw new Error("Expected the field `sets` to be an array in the JSON data but got " + data['sets']);
            }
            // validate the optional field `sets` (array)
            for (const item of data['sets']) {
                SetResume.validateJSON(item);
            };
        }

        return true;
    }


}

Serie.RequiredProperties = ["id", "name", "sets"];

/**
 * @member {String} id
 */
Serie.prototype['id'] = undefined;

/**
 * @member {String} logo
 */
Serie.prototype['logo'] = undefined;

/**
 * @member {String} name
 */
Serie.prototype['name'] = undefined;

/**
 * @member {Array.<module:model/SetResume>} sets
 */
Serie.prototype['sets'] = undefined;






export default Serie;

