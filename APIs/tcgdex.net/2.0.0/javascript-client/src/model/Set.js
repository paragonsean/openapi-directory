/**
 * TCGdex API
 * A Multilanguage Pokémon TCG Database with Cards Pictures and most of the informations contained on the cards. You can find out more about TCGdex at [https://www.tcgdex.net](https://www.tcgdex.net) or on [Discord](https://discord.gg/NehYTAhsZE).
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: contact@tcgdex.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CardResume from './CardResume';
import SetCardCount from './SetCardCount';

/**
 * The Set model module.
 * @module model/Set
 * @version 2.0.0
 */
class Set {
    /**
     * Constructs a new <code>Set</code>.
     * @alias module:model/Set
     * @param cardCount {module:model/SetCardCount} 
     * @param cards {Array.<module:model/CardResume>} 
     * @param id {String} 
     * @param name {String} 
     */
    constructor(cardCount, cards, id, name) { 
        
        Set.initialize(this, cardCount, cards, id, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cardCount, cards, id, name) { 
        obj['cardCount'] = cardCount;
        obj['cards'] = cards;
        obj['id'] = id;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>Set</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Set} obj Optional instance to populate.
     * @return {module:model/Set} The populated <code>Set</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Set();

            if (data.hasOwnProperty('cardCount')) {
                obj['cardCount'] = SetCardCount.constructFromObject(data['cardCount']);
            }
            if (data.hasOwnProperty('cards')) {
                obj['cards'] = ApiClient.convertToType(data['cards'], [CardResume]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('logo')) {
                obj['logo'] = ApiClient.convertToType(data['logo'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('symbol')) {
                obj['symbol'] = ApiClient.convertToType(data['symbol'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Set</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Set</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Set.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `cardCount`
        if (data['cardCount']) { // data not null
          SetCardCount.validateJSON(data['cardCount']);
        }
        if (data['cards']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['cards'])) {
                throw new Error("Expected the field `cards` to be an array in the JSON data but got " + data['cards']);
            }
            // validate the optional field `cards` (array)
            for (const item of data['cards']) {
                CardResume.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['logo'] && !(typeof data['logo'] === 'string' || data['logo'] instanceof String)) {
            throw new Error("Expected the field `logo` to be a primitive type in the JSON string but got " + data['logo']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['symbol'] && !(typeof data['symbol'] === 'string' || data['symbol'] instanceof String)) {
            throw new Error("Expected the field `symbol` to be a primitive type in the JSON string but got " + data['symbol']);
        }

        return true;
    }


}

Set.RequiredProperties = ["cardCount", "cards", "id", "name"];

/**
 * @member {module:model/SetCardCount} cardCount
 */
Set.prototype['cardCount'] = undefined;

/**
 * @member {Array.<module:model/CardResume>} cards
 */
Set.prototype['cards'] = undefined;

/**
 * @member {String} id
 */
Set.prototype['id'] = undefined;

/**
 * @member {String} logo
 */
Set.prototype['logo'] = undefined;

/**
 * @member {String} name
 */
Set.prototype['name'] = undefined;

/**
 * @member {String} symbol
 */
Set.prototype['symbol'] = undefined;






export default Set;

