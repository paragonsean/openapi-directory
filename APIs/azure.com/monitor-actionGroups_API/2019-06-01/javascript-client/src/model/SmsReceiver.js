/**
 * Azure Action Groups
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-06-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReceiverStatus from './ReceiverStatus';

/**
 * The SmsReceiver model module.
 * @module model/SmsReceiver
 * @version 2019-06-01
 */
class SmsReceiver {
    /**
     * Constructs a new <code>SmsReceiver</code>.
     * An SMS receiver.
     * @alias module:model/SmsReceiver
     * @param countryCode {String} The country code of the SMS receiver.
     * @param name {String} The name of the SMS receiver. Names must be unique across all receivers within an action group.
     * @param phoneNumber {String} The phone number of the SMS receiver.
     */
    constructor(countryCode, name, phoneNumber) { 
        
        SmsReceiver.initialize(this, countryCode, name, phoneNumber);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, countryCode, name, phoneNumber) { 
        obj['countryCode'] = countryCode;
        obj['name'] = name;
        obj['phoneNumber'] = phoneNumber;
    }

    /**
     * Constructs a <code>SmsReceiver</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SmsReceiver} obj Optional instance to populate.
     * @return {module:model/SmsReceiver} The populated <code>SmsReceiver</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SmsReceiver();

            if (data.hasOwnProperty('countryCode')) {
                obj['countryCode'] = ApiClient.convertToType(data['countryCode'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('phoneNumber')) {
                obj['phoneNumber'] = ApiClient.convertToType(data['phoneNumber'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ReceiverStatus.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SmsReceiver</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SmsReceiver</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SmsReceiver.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['countryCode'] && !(typeof data['countryCode'] === 'string' || data['countryCode'] instanceof String)) {
            throw new Error("Expected the field `countryCode` to be a primitive type in the JSON string but got " + data['countryCode']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['phoneNumber'] && !(typeof data['phoneNumber'] === 'string' || data['phoneNumber'] instanceof String)) {
            throw new Error("Expected the field `phoneNumber` to be a primitive type in the JSON string but got " + data['phoneNumber']);
        }

        return true;
    }


}

SmsReceiver.RequiredProperties = ["countryCode", "name", "phoneNumber"];

/**
 * The country code of the SMS receiver.
 * @member {String} countryCode
 */
SmsReceiver.prototype['countryCode'] = undefined;

/**
 * The name of the SMS receiver. Names must be unique across all receivers within an action group.
 * @member {String} name
 */
SmsReceiver.prototype['name'] = undefined;

/**
 * The phone number of the SMS receiver.
 * @member {String} phoneNumber
 */
SmsReceiver.prototype['phoneNumber'] = undefined;

/**
 * @member {module:model/ReceiverStatus} status
 */
SmsReceiver.prototype['status'] = undefined;






export default SmsReceiver;

