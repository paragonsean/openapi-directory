/**
 * Azure Action Groups
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2017-04-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AutomationRunbookReceiver from './AutomationRunbookReceiver';
import AzureAppPushReceiver from './AzureAppPushReceiver';
import EmailReceiver from './EmailReceiver';
import ItsmReceiver from './ItsmReceiver';
import SmsReceiver from './SmsReceiver';
import WebhookReceiver from './WebhookReceiver';

/**
 * The ActionGroup model module.
 * @module model/ActionGroup
 * @version 2017-04-01
 */
class ActionGroup {
    /**
     * Constructs a new <code>ActionGroup</code>.
     * An Azure action group.
     * @alias module:model/ActionGroup
     * @param enabled {Boolean} Indicates whether this action group is enabled. If an action group is not enabled, then none of its receivers will receive communications.
     * @param groupShortName {String} The short name of the action group. This will be used in SMS messages.
     */
    constructor(enabled, groupShortName) { 
        
        ActionGroup.initialize(this, enabled, groupShortName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, enabled, groupShortName) { 
        obj['enabled'] = enabled || true;
        obj['groupShortName'] = groupShortName;
    }

    /**
     * Constructs a <code>ActionGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ActionGroup} obj Optional instance to populate.
     * @return {module:model/ActionGroup} The populated <code>ActionGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ActionGroup();

            if (data.hasOwnProperty('automationRunbookReceivers')) {
                obj['automationRunbookReceivers'] = ApiClient.convertToType(data['automationRunbookReceivers'], [AutomationRunbookReceiver]);
            }
            if (data.hasOwnProperty('azureAppPushReceivers')) {
                obj['azureAppPushReceivers'] = ApiClient.convertToType(data['azureAppPushReceivers'], [AzureAppPushReceiver]);
            }
            if (data.hasOwnProperty('emailReceivers')) {
                obj['emailReceivers'] = ApiClient.convertToType(data['emailReceivers'], [EmailReceiver]);
            }
            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('groupShortName')) {
                obj['groupShortName'] = ApiClient.convertToType(data['groupShortName'], 'String');
            }
            if (data.hasOwnProperty('itsmReceivers')) {
                obj['itsmReceivers'] = ApiClient.convertToType(data['itsmReceivers'], [ItsmReceiver]);
            }
            if (data.hasOwnProperty('smsReceivers')) {
                obj['smsReceivers'] = ApiClient.convertToType(data['smsReceivers'], [SmsReceiver]);
            }
            if (data.hasOwnProperty('webhookReceivers')) {
                obj['webhookReceivers'] = ApiClient.convertToType(data['webhookReceivers'], [WebhookReceiver]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ActionGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ActionGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ActionGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['automationRunbookReceivers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['automationRunbookReceivers'])) {
                throw new Error("Expected the field `automationRunbookReceivers` to be an array in the JSON data but got " + data['automationRunbookReceivers']);
            }
            // validate the optional field `automationRunbookReceivers` (array)
            for (const item of data['automationRunbookReceivers']) {
                AutomationRunbookReceiver.validateJSON(item);
            };
        }
        if (data['azureAppPushReceivers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['azureAppPushReceivers'])) {
                throw new Error("Expected the field `azureAppPushReceivers` to be an array in the JSON data but got " + data['azureAppPushReceivers']);
            }
            // validate the optional field `azureAppPushReceivers` (array)
            for (const item of data['azureAppPushReceivers']) {
                AzureAppPushReceiver.validateJSON(item);
            };
        }
        if (data['emailReceivers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['emailReceivers'])) {
                throw new Error("Expected the field `emailReceivers` to be an array in the JSON data but got " + data['emailReceivers']);
            }
            // validate the optional field `emailReceivers` (array)
            for (const item of data['emailReceivers']) {
                EmailReceiver.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['groupShortName'] && !(typeof data['groupShortName'] === 'string' || data['groupShortName'] instanceof String)) {
            throw new Error("Expected the field `groupShortName` to be a primitive type in the JSON string but got " + data['groupShortName']);
        }
        if (data['itsmReceivers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['itsmReceivers'])) {
                throw new Error("Expected the field `itsmReceivers` to be an array in the JSON data but got " + data['itsmReceivers']);
            }
            // validate the optional field `itsmReceivers` (array)
            for (const item of data['itsmReceivers']) {
                ItsmReceiver.validateJSON(item);
            };
        }
        if (data['smsReceivers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['smsReceivers'])) {
                throw new Error("Expected the field `smsReceivers` to be an array in the JSON data but got " + data['smsReceivers']);
            }
            // validate the optional field `smsReceivers` (array)
            for (const item of data['smsReceivers']) {
                SmsReceiver.validateJSON(item);
            };
        }
        if (data['webhookReceivers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['webhookReceivers'])) {
                throw new Error("Expected the field `webhookReceivers` to be an array in the JSON data but got " + data['webhookReceivers']);
            }
            // validate the optional field `webhookReceivers` (array)
            for (const item of data['webhookReceivers']) {
                WebhookReceiver.validateJSON(item);
            };
        }

        return true;
    }


}

ActionGroup.RequiredProperties = ["enabled", "groupShortName"];

/**
 * The list of AutomationRunbook receivers that are part of this action group.
 * @member {Array.<module:model/AutomationRunbookReceiver>} automationRunbookReceivers
 */
ActionGroup.prototype['automationRunbookReceivers'] = undefined;

/**
 * The list of AzureAppPush receivers that are part of this action group.
 * @member {Array.<module:model/AzureAppPushReceiver>} azureAppPushReceivers
 */
ActionGroup.prototype['azureAppPushReceivers'] = undefined;

/**
 * The list of email receivers that are part of this action group.
 * @member {Array.<module:model/EmailReceiver>} emailReceivers
 */
ActionGroup.prototype['emailReceivers'] = undefined;

/**
 * Indicates whether this action group is enabled. If an action group is not enabled, then none of its receivers will receive communications.
 * @member {Boolean} enabled
 * @default true
 */
ActionGroup.prototype['enabled'] = true;

/**
 * The short name of the action group. This will be used in SMS messages.
 * @member {String} groupShortName
 */
ActionGroup.prototype['groupShortName'] = undefined;

/**
 * The list of ITSM receivers that are part of this action group.
 * @member {Array.<module:model/ItsmReceiver>} itsmReceivers
 */
ActionGroup.prototype['itsmReceivers'] = undefined;

/**
 * The list of SMS receivers that are part of this action group.
 * @member {Array.<module:model/SmsReceiver>} smsReceivers
 */
ActionGroup.prototype['smsReceivers'] = undefined;

/**
 * The list of webhook receivers that are part of this action group.
 * @member {Array.<module:model/WebhookReceiver>} webhookReceivers
 */
ActionGroup.prototype['webhookReceivers'] = undefined;






export default ActionGroup;

