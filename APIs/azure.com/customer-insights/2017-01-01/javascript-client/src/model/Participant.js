/**
 * CustomerInsightsManagementClient
 * The Azure Customer Insights management API provides a RESTful set of web services that interact with Azure Customer Insights service to manage your resources. The API has entities that capture the relationship between an end user and the Azure Customer Insights service.
 *
 * The version of the OpenAPI document: 2017-01-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ParticipantPropertyReference from './ParticipantPropertyReference';

/**
 * The Participant model module.
 * @module model/Participant
 * @version 2017-01-01
 */
class Participant {
    /**
     * Constructs a new <code>Participant</code>.
     * Describes a profile type participating in an interaction.
     * @alias module:model/Participant
     * @param participantName {String} Participant name.
     * @param participantPropertyReferences {Array.<module:model/ParticipantPropertyReference>} The property references.
     * @param profileTypeName {String} Profile type name.
     */
    constructor(participantName, participantPropertyReferences, profileTypeName) { 
        
        Participant.initialize(this, participantName, participantPropertyReferences, profileTypeName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, participantName, participantPropertyReferences, profileTypeName) { 
        obj['participantName'] = participantName;
        obj['participantPropertyReferences'] = participantPropertyReferences;
        obj['profileTypeName'] = profileTypeName;
    }

    /**
     * Constructs a <code>Participant</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Participant} obj Optional instance to populate.
     * @return {module:model/Participant} The populated <code>Participant</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Participant();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], {'String': 'String'});
            }
            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], {'String': 'String'});
            }
            if (data.hasOwnProperty('participantName')) {
                obj['participantName'] = ApiClient.convertToType(data['participantName'], 'String');
            }
            if (data.hasOwnProperty('participantPropertyReferences')) {
                obj['participantPropertyReferences'] = ApiClient.convertToType(data['participantPropertyReferences'], [ParticipantPropertyReference]);
            }
            if (data.hasOwnProperty('profileTypeName')) {
                obj['profileTypeName'] = ApiClient.convertToType(data['profileTypeName'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Participant</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Participant</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Participant.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['participantName'] && !(typeof data['participantName'] === 'string' || data['participantName'] instanceof String)) {
            throw new Error("Expected the field `participantName` to be a primitive type in the JSON string but got " + data['participantName']);
        }
        if (data['participantPropertyReferences']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['participantPropertyReferences'])) {
                throw new Error("Expected the field `participantPropertyReferences` to be an array in the JSON data but got " + data['participantPropertyReferences']);
            }
            // validate the optional field `participantPropertyReferences` (array)
            for (const item of data['participantPropertyReferences']) {
                ParticipantPropertyReference.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['profileTypeName'] && !(typeof data['profileTypeName'] === 'string' || data['profileTypeName'] instanceof String)) {
            throw new Error("Expected the field `profileTypeName` to be a primitive type in the JSON string but got " + data['profileTypeName']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }

        return true;
    }


}

Participant.RequiredProperties = ["participantName", "participantPropertyReferences", "profileTypeName"];

/**
 * Localized descriptions.
 * @member {Object.<String, String>} description
 */
Participant.prototype['description'] = undefined;

/**
 * Localized display name.
 * @member {Object.<String, String>} displayName
 */
Participant.prototype['displayName'] = undefined;

/**
 * Participant name.
 * @member {String} participantName
 */
Participant.prototype['participantName'] = undefined;

/**
 * The property references.
 * @member {Array.<module:model/ParticipantPropertyReference>} participantPropertyReferences
 */
Participant.prototype['participantPropertyReferences'] = undefined;

/**
 * Profile type name.
 * @member {String} profileTypeName
 */
Participant.prototype['profileTypeName'] = undefined;

/**
 * The role that the participant is playing in the interaction.
 * @member {String} role
 */
Participant.prototype['role'] = undefined;






export default Participant;

