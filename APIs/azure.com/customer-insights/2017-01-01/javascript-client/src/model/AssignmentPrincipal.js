/**
 * CustomerInsightsManagementClient
 * The Azure Customer Insights management API provides a RESTful set of web services that interact with Azure Customer Insights service to manage your resources. The API has entities that capture the relationship between an end user and the Azure Customer Insights service.
 *
 * The version of the OpenAPI document: 2017-01-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AssignmentPrincipal model module.
 * @module model/AssignmentPrincipal
 * @version 2017-01-01
 */
class AssignmentPrincipal {
    /**
     * Constructs a new <code>AssignmentPrincipal</code>.
     * The AssignmentPrincipal
     * @alias module:model/AssignmentPrincipal
     * @param principalId {String} The principal id being assigned to.
     * @param principalType {String} The Type of the principal ID.
     */
    constructor(principalId, principalType) { 
        
        AssignmentPrincipal.initialize(this, principalId, principalType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, principalId, principalType) { 
        obj['principalId'] = principalId;
        obj['principalType'] = principalType;
    }

    /**
     * Constructs a <code>AssignmentPrincipal</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AssignmentPrincipal} obj Optional instance to populate.
     * @return {module:model/AssignmentPrincipal} The populated <code>AssignmentPrincipal</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AssignmentPrincipal();

            if (data.hasOwnProperty('principalId')) {
                obj['principalId'] = ApiClient.convertToType(data['principalId'], 'String');
            }
            if (data.hasOwnProperty('principalMetadata')) {
                obj['principalMetadata'] = ApiClient.convertToType(data['principalMetadata'], {'String': 'String'});
            }
            if (data.hasOwnProperty('principalType')) {
                obj['principalType'] = ApiClient.convertToType(data['principalType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AssignmentPrincipal</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AssignmentPrincipal</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AssignmentPrincipal.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['principalId'] && !(typeof data['principalId'] === 'string' || data['principalId'] instanceof String)) {
            throw new Error("Expected the field `principalId` to be a primitive type in the JSON string but got " + data['principalId']);
        }
        // ensure the json data is a string
        if (data['principalType'] && !(typeof data['principalType'] === 'string' || data['principalType'] instanceof String)) {
            throw new Error("Expected the field `principalType` to be a primitive type in the JSON string but got " + data['principalType']);
        }

        return true;
    }


}

AssignmentPrincipal.RequiredProperties = ["principalId", "principalType"];

/**
 * The principal id being assigned to.
 * @member {String} principalId
 */
AssignmentPrincipal.prototype['principalId'] = undefined;

/**
 * Other metadata for the principal.
 * @member {Object.<String, String>} principalMetadata
 */
AssignmentPrincipal.prototype['principalMetadata'] = undefined;

/**
 * The Type of the principal ID.
 * @member {String} principalType
 */
AssignmentPrincipal.prototype['principalType'] = undefined;






export default AssignmentPrincipal;

