/**
 * CustomerInsightsManagementClient
 * The Azure Customer Insights management API provides a RESTful set of web services that interact with Azure Customer Insights service to manage your resources. The API has entities that capture the relationship between an end user and the Azure Customer Insights service.
 *
 * The version of the OpenAPI document: 2017-04-26
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PredictionMappings model module.
 * @module model/PredictionMappings
 * @version 2017-04-26
 */
class PredictionMappings {
    /**
     * Constructs a new <code>PredictionMappings</code>.
     * Definition of the link mapping of prediction.
     * @alias module:model/PredictionMappings
     * @param grade {String} The grade of the link mapping.
     * @param reason {String} The reason of the link mapping.
     * @param score {String} The score of the link mapping.
     */
    constructor(grade, reason, score) { 
        
        PredictionMappings.initialize(this, grade, reason, score);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, grade, reason, score) { 
        obj['grade'] = grade;
        obj['reason'] = reason;
        obj['score'] = score;
    }

    /**
     * Constructs a <code>PredictionMappings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PredictionMappings} obj Optional instance to populate.
     * @return {module:model/PredictionMappings} The populated <code>PredictionMappings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PredictionMappings();

            if (data.hasOwnProperty('grade')) {
                obj['grade'] = ApiClient.convertToType(data['grade'], 'String');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PredictionMappings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PredictionMappings</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PredictionMappings.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['grade'] && !(typeof data['grade'] === 'string' || data['grade'] instanceof String)) {
            throw new Error("Expected the field `grade` to be a primitive type in the JSON string but got " + data['grade']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['score'] && !(typeof data['score'] === 'string' || data['score'] instanceof String)) {
            throw new Error("Expected the field `score` to be a primitive type in the JSON string but got " + data['score']);
        }

        return true;
    }


}

PredictionMappings.RequiredProperties = ["grade", "reason", "score"];

/**
 * The grade of the link mapping.
 * @member {String} grade
 */
PredictionMappings.prototype['grade'] = undefined;

/**
 * The reason of the link mapping.
 * @member {String} reason
 */
PredictionMappings.prototype['reason'] = undefined;

/**
 * The score of the link mapping.
 * @member {String} score
 */
PredictionMappings.prototype['score'] = undefined;






export default PredictionMappings;

