/**
 * BatchService
 * A client for issuing REST requests to the Azure Batch service.
 *
 * The version of the OpenAPI document: 2019-08-01.10.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NFSMountConfiguration model module.
 * @module model/NFSMountConfiguration
 * @version 2019-08-01.10.0
 */
class NFSMountConfiguration {
    /**
     * Constructs a new <code>NFSMountConfiguration</code>.
     * @alias module:model/NFSMountConfiguration
     * @param relativeMountPath {String} All file systems are mounted relative to the Batch mounts directory, accessible via the AZ_BATCH_NODE_MOUNTS_DIR environment variable.
     * @param source {String} 
     */
    constructor(relativeMountPath, source) { 
        
        NFSMountConfiguration.initialize(this, relativeMountPath, source);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, relativeMountPath, source) { 
        obj['relativeMountPath'] = relativeMountPath;
        obj['source'] = source;
    }

    /**
     * Constructs a <code>NFSMountConfiguration</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NFSMountConfiguration} obj Optional instance to populate.
     * @return {module:model/NFSMountConfiguration} The populated <code>NFSMountConfiguration</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NFSMountConfiguration();

            if (data.hasOwnProperty('mountOptions')) {
                obj['mountOptions'] = ApiClient.convertToType(data['mountOptions'], 'String');
            }
            if (data.hasOwnProperty('relativeMountPath')) {
                obj['relativeMountPath'] = ApiClient.convertToType(data['relativeMountPath'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NFSMountConfiguration</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NFSMountConfiguration</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NFSMountConfiguration.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['mountOptions'] && !(typeof data['mountOptions'] === 'string' || data['mountOptions'] instanceof String)) {
            throw new Error("Expected the field `mountOptions` to be a primitive type in the JSON string but got " + data['mountOptions']);
        }
        // ensure the json data is a string
        if (data['relativeMountPath'] && !(typeof data['relativeMountPath'] === 'string' || data['relativeMountPath'] instanceof String)) {
            throw new Error("Expected the field `relativeMountPath` to be a primitive type in the JSON string but got " + data['relativeMountPath']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

NFSMountConfiguration.RequiredProperties = ["relativeMountPath", "source"];

/**
 * These are 'net use' options in Windows and 'mount' options in Linux.
 * @member {String} mountOptions
 */
NFSMountConfiguration.prototype['mountOptions'] = undefined;

/**
 * All file systems are mounted relative to the Batch mounts directory, accessible via the AZ_BATCH_NODE_MOUNTS_DIR environment variable.
 * @member {String} relativeMountPath
 */
NFSMountConfiguration.prototype['relativeMountPath'] = undefined;

/**
 * @member {String} source
 */
NFSMountConfiguration.prototype['source'] = undefined;






export default NFSMountConfiguration;

