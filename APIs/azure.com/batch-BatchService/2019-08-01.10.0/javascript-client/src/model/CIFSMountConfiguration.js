/**
 * BatchService
 * A client for issuing REST requests to the Azure Batch service.
 *
 * The version of the OpenAPI document: 2019-08-01.10.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CIFSMountConfiguration model module.
 * @module model/CIFSMountConfiguration
 * @version 2019-08-01.10.0
 */
class CIFSMountConfiguration {
    /**
     * Constructs a new <code>CIFSMountConfiguration</code>.
     * @alias module:model/CIFSMountConfiguration
     * @param password {String} 
     * @param relativeMountPath {String} All file systems are mounted relative to the Batch mounts directory, accessible via the AZ_BATCH_NODE_MOUNTS_DIR environment variable.
     * @param source {String} 
     * @param username {String} 
     */
    constructor(password, relativeMountPath, source, username) { 
        
        CIFSMountConfiguration.initialize(this, password, relativeMountPath, source, username);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, password, relativeMountPath, source, username) { 
        obj['password'] = password;
        obj['relativeMountPath'] = relativeMountPath;
        obj['source'] = source;
        obj['username'] = username;
    }

    /**
     * Constructs a <code>CIFSMountConfiguration</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CIFSMountConfiguration} obj Optional instance to populate.
     * @return {module:model/CIFSMountConfiguration} The populated <code>CIFSMountConfiguration</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CIFSMountConfiguration();

            if (data.hasOwnProperty('mountOptions')) {
                obj['mountOptions'] = ApiClient.convertToType(data['mountOptions'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('relativeMountPath')) {
                obj['relativeMountPath'] = ApiClient.convertToType(data['relativeMountPath'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CIFSMountConfiguration</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CIFSMountConfiguration</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CIFSMountConfiguration.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['mountOptions'] && !(typeof data['mountOptions'] === 'string' || data['mountOptions'] instanceof String)) {
            throw new Error("Expected the field `mountOptions` to be a primitive type in the JSON string but got " + data['mountOptions']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['relativeMountPath'] && !(typeof data['relativeMountPath'] === 'string' || data['relativeMountPath'] instanceof String)) {
            throw new Error("Expected the field `relativeMountPath` to be a primitive type in the JSON string but got " + data['relativeMountPath']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }

        return true;
    }


}

CIFSMountConfiguration.RequiredProperties = ["password", "relativeMountPath", "source", "username"];

/**
 * These are 'net use' options in Windows and 'mount' options in Linux.
 * @member {String} mountOptions
 */
CIFSMountConfiguration.prototype['mountOptions'] = undefined;

/**
 * @member {String} password
 */
CIFSMountConfiguration.prototype['password'] = undefined;

/**
 * All file systems are mounted relative to the Batch mounts directory, accessible via the AZ_BATCH_NODE_MOUNTS_DIR environment variable.
 * @member {String} relativeMountPath
 */
CIFSMountConfiguration.prototype['relativeMountPath'] = undefined;

/**
 * @member {String} source
 */
CIFSMountConfiguration.prototype['source'] = undefined;

/**
 * @member {String} username
 */
CIFSMountConfiguration.prototype['username'] = undefined;






export default CIFSMountConfiguration;

