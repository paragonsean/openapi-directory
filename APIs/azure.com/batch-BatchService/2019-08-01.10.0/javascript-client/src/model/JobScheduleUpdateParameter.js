/**
 * BatchService
 * A client for issuing REST requests to the Azure Batch service.
 *
 * The version of the OpenAPI document: 2019-08-01.10.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JobSpecification from './JobSpecification';
import MetadataItem from './MetadataItem';
import Schedule from './Schedule';

/**
 * The JobScheduleUpdateParameter model module.
 * @module model/JobScheduleUpdateParameter
 * @version 2019-08-01.10.0
 */
class JobScheduleUpdateParameter {
    /**
     * Constructs a new <code>JobScheduleUpdateParameter</code>.
     * @alias module:model/JobScheduleUpdateParameter
     * @param jobSpecification {module:model/JobSpecification} 
     * @param schedule {module:model/Schedule} 
     */
    constructor(jobSpecification, schedule) { 
        
        JobScheduleUpdateParameter.initialize(this, jobSpecification, schedule);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, jobSpecification, schedule) { 
        obj['jobSpecification'] = jobSpecification;
        obj['schedule'] = schedule;
    }

    /**
     * Constructs a <code>JobScheduleUpdateParameter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobScheduleUpdateParameter} obj Optional instance to populate.
     * @return {module:model/JobScheduleUpdateParameter} The populated <code>JobScheduleUpdateParameter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobScheduleUpdateParameter();

            if (data.hasOwnProperty('jobSpecification')) {
                obj['jobSpecification'] = JobSpecification.constructFromObject(data['jobSpecification']);
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], [MetadataItem]);
            }
            if (data.hasOwnProperty('schedule')) {
                obj['schedule'] = Schedule.constructFromObject(data['schedule']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobScheduleUpdateParameter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobScheduleUpdateParameter</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobScheduleUpdateParameter.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `jobSpecification`
        if (data['jobSpecification']) { // data not null
          JobSpecification.validateJSON(data['jobSpecification']);
        }
        if (data['metadata']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metadata'])) {
                throw new Error("Expected the field `metadata` to be an array in the JSON data but got " + data['metadata']);
            }
            // validate the optional field `metadata` (array)
            for (const item of data['metadata']) {
                MetadataItem.validateJSON(item);
            };
        }
        // validate the optional field `schedule`
        if (data['schedule']) { // data not null
          Schedule.validateJSON(data['schedule']);
        }

        return true;
    }


}

JobScheduleUpdateParameter.RequiredProperties = ["jobSpecification", "schedule"];

/**
 * @member {module:model/JobSpecification} jobSpecification
 */
JobScheduleUpdateParameter.prototype['jobSpecification'] = undefined;

/**
 * If you do not specify this element, it takes the default value of an empty list; in effect, any existing metadata is deleted.
 * @member {Array.<module:model/MetadataItem>} metadata
 */
JobScheduleUpdateParameter.prototype['metadata'] = undefined;

/**
 * @member {module:model/Schedule} schedule
 */
JobScheduleUpdateParameter.prototype['schedule'] = undefined;






export default JobScheduleUpdateParameter;

