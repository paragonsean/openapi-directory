/**
 * NetworkManagementClient
 * The Microsoft Azure Network management API provides a RESTful set of web services that interact with Microsoft Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Microsoft Azure Networks service.
 *
 * The version of the OpenAPI document: 2019-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RouteFilterPropertiesFormatIpv6PeeringsInner from './RouteFilterPropertiesFormatIpv6PeeringsInner';
import RouteFilterRule from './RouteFilterRule';

/**
 * The RouteFilterPropertiesFormat model module.
 * @module model/RouteFilterPropertiesFormat
 * @version 2019-07-01
 */
class RouteFilterPropertiesFormat {
    /**
     * Constructs a new <code>RouteFilterPropertiesFormat</code>.
     * Route Filter Resource.
     * @alias module:model/RouteFilterPropertiesFormat
     */
    constructor() { 
        
        RouteFilterPropertiesFormat.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RouteFilterPropertiesFormat</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RouteFilterPropertiesFormat} obj Optional instance to populate.
     * @return {module:model/RouteFilterPropertiesFormat} The populated <code>RouteFilterPropertiesFormat</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RouteFilterPropertiesFormat();

            if (data.hasOwnProperty('ipv6Peerings')) {
                obj['ipv6Peerings'] = ApiClient.convertToType(data['ipv6Peerings'], [RouteFilterPropertiesFormatIpv6PeeringsInner]);
            }
            if (data.hasOwnProperty('peerings')) {
                obj['peerings'] = ApiClient.convertToType(data['peerings'], [RouteFilterPropertiesFormatIpv6PeeringsInner]);
            }
            if (data.hasOwnProperty('provisioningState')) {
                obj['provisioningState'] = ApiClient.convertToType(data['provisioningState'], 'String');
            }
            if (data.hasOwnProperty('rules')) {
                obj['rules'] = ApiClient.convertToType(data['rules'], [RouteFilterRule]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RouteFilterPropertiesFormat</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RouteFilterPropertiesFormat</code>.
     */
    static validateJSON(data) {
        if (data['ipv6Peerings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ipv6Peerings'])) {
                throw new Error("Expected the field `ipv6Peerings` to be an array in the JSON data but got " + data['ipv6Peerings']);
            }
            // validate the optional field `ipv6Peerings` (array)
            for (const item of data['ipv6Peerings']) {
                RouteFilterPropertiesFormatIpv6PeeringsInner.validateJSON(item);
            };
        }
        if (data['peerings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['peerings'])) {
                throw new Error("Expected the field `peerings` to be an array in the JSON data but got " + data['peerings']);
            }
            // validate the optional field `peerings` (array)
            for (const item of data['peerings']) {
                RouteFilterPropertiesFormatIpv6PeeringsInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['provisioningState'] && !(typeof data['provisioningState'] === 'string' || data['provisioningState'] instanceof String)) {
            throw new Error("Expected the field `provisioningState` to be a primitive type in the JSON string but got " + data['provisioningState']);
        }
        if (data['rules']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rules'])) {
                throw new Error("Expected the field `rules` to be an array in the JSON data but got " + data['rules']);
            }
            // validate the optional field `rules` (array)
            for (const item of data['rules']) {
                RouteFilterRule.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A collection of references to express route circuit ipv6 peerings.
 * @member {Array.<module:model/RouteFilterPropertiesFormatIpv6PeeringsInner>} ipv6Peerings
 */
RouteFilterPropertiesFormat.prototype['ipv6Peerings'] = undefined;

/**
 * A collection of references to express route circuit peerings.
 * @member {Array.<module:model/RouteFilterPropertiesFormatIpv6PeeringsInner>} peerings
 */
RouteFilterPropertiesFormat.prototype['peerings'] = undefined;

/**
 * The current provisioning state.
 * @member {module:model/RouteFilterPropertiesFormat.ProvisioningStateEnum} provisioningState
 */
RouteFilterPropertiesFormat.prototype['provisioningState'] = undefined;

/**
 * Collection of RouteFilterRules contained within a route filter.
 * @member {Array.<module:model/RouteFilterRule>} rules
 */
RouteFilterPropertiesFormat.prototype['rules'] = undefined;





/**
 * Allowed values for the <code>provisioningState</code> property.
 * @enum {String}
 * @readonly
 */
RouteFilterPropertiesFormat['ProvisioningStateEnum'] = {

    /**
     * value: "Succeeded"
     * @const
     */
    "Succeeded": "Succeeded",

    /**
     * value: "Updating"
     * @const
     */
    "Updating": "Updating",

    /**
     * value: "Deleting"
     * @const
     */
    "Deleting": "Deleting",

    /**
     * value: "Failed"
     * @const
     */
    "Failed": "Failed"
};



export default RouteFilterPropertiesFormat;

