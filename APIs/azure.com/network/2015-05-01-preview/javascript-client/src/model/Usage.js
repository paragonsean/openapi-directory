/**
 * NetworkResourceProviderClient
 * The Windows Azure Network management API provides a RESTful set of web services that interact with Windows Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Windows Azure Networks service.
 *
 * The version of the OpenAPI document: 2015-05-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UsageName from './UsageName';

/**
 * The Usage model module.
 * @module model/Usage
 * @version 2015-05-01-preview
 */
class Usage {
    /**
     * Constructs a new <code>Usage</code>.
     * Describes Network Resource Usage.
     * @alias module:model/Usage
     * @param currentValue {Number} Gets or sets the current value of the usage.
     * @param limit {Number} Gets or sets the limit of usage.
     * @param name {module:model/UsageName} 
     * @param unit {module:model/Usage.UnitEnum} Gets or sets an enum describing the unit of measurement.
     */
    constructor(currentValue, limit, name, unit) { 
        
        Usage.initialize(this, currentValue, limit, name, unit);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, currentValue, limit, name, unit) { 
        obj['currentValue'] = currentValue;
        obj['limit'] = limit;
        obj['name'] = name;
        obj['unit'] = unit;
    }

    /**
     * Constructs a <code>Usage</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Usage} obj Optional instance to populate.
     * @return {module:model/Usage} The populated <code>Usage</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Usage();

            if (data.hasOwnProperty('currentValue')) {
                obj['currentValue'] = ApiClient.convertToType(data['currentValue'], 'Number');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = UsageName.constructFromObject(data['name']);
            }
            if (data.hasOwnProperty('unit')) {
                obj['unit'] = ApiClient.convertToType(data['unit'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Usage</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Usage</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Usage.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `name`
        if (data['name']) { // data not null
          UsageName.validateJSON(data['name']);
        }
        // ensure the json data is a string
        if (data['unit'] && !(typeof data['unit'] === 'string' || data['unit'] instanceof String)) {
            throw new Error("Expected the field `unit` to be a primitive type in the JSON string but got " + data['unit']);
        }

        return true;
    }


}

Usage.RequiredProperties = ["currentValue", "limit", "name", "unit"];

/**
 * Gets or sets the current value of the usage.
 * @member {Number} currentValue
 */
Usage.prototype['currentValue'] = undefined;

/**
 * Gets or sets the limit of usage.
 * @member {Number} limit
 */
Usage.prototype['limit'] = undefined;

/**
 * @member {module:model/UsageName} name
 */
Usage.prototype['name'] = undefined;

/**
 * Gets or sets an enum describing the unit of measurement.
 * @member {module:model/Usage.UnitEnum} unit
 */
Usage.prototype['unit'] = undefined;





/**
 * Allowed values for the <code>unit</code> property.
 * @enum {String}
 * @readonly
 */
Usage['UnitEnum'] = {

    /**
     * value: "Count"
     * @const
     */
    "Count": "Count"
};



export default Usage;

