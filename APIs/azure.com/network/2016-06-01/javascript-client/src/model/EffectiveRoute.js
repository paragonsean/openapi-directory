/**
 * NetworkManagementClient
 * The Microsoft Azure Network management API provides a RESTful set of web services that interact with Microsoft Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Microsoft Azure Networks service.
 *
 * The version of the OpenAPI document: 2016-06-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EffectiveRoute model module.
 * @module model/EffectiveRoute
 * @version 2016-06-01
 */
class EffectiveRoute {
    /**
     * Constructs a new <code>EffectiveRoute</code>.
     * Effective Route
     * @alias module:model/EffectiveRoute
     */
    constructor() { 
        
        EffectiveRoute.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EffectiveRoute</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EffectiveRoute} obj Optional instance to populate.
     * @return {module:model/EffectiveRoute} The populated <code>EffectiveRoute</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EffectiveRoute();

            if (data.hasOwnProperty('addressPrefix')) {
                obj['addressPrefix'] = ApiClient.convertToType(data['addressPrefix'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('nextHopIpAddress')) {
                obj['nextHopIpAddress'] = ApiClient.convertToType(data['nextHopIpAddress'], ['String']);
            }
            if (data.hasOwnProperty('nextHopType')) {
                obj['nextHopType'] = ApiClient.convertToType(data['nextHopType'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EffectiveRoute</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EffectiveRoute</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['addressPrefix'])) {
            throw new Error("Expected the field `addressPrefix` to be an array in the JSON data but got " + data['addressPrefix']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['nextHopIpAddress'])) {
            throw new Error("Expected the field `nextHopIpAddress` to be an array in the JSON data but got " + data['nextHopIpAddress']);
        }
        // ensure the json data is a string
        if (data['nextHopType'] && !(typeof data['nextHopType'] === 'string' || data['nextHopType'] instanceof String)) {
            throw new Error("Expected the field `nextHopType` to be a primitive type in the JSON string but got " + data['nextHopType']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}



/**
 * Gets address prefixes of the effective routes in CIDR notation.
 * @member {Array.<String>} addressPrefix
 */
EffectiveRoute.prototype['addressPrefix'] = undefined;

/**
 * Gets the name of the user defined route. This is optional.
 * @member {String} name
 */
EffectiveRoute.prototype['name'] = undefined;

/**
 * Gets the IP address of the next hop of the effective route
 * @member {Array.<String>} nextHopIpAddress
 */
EffectiveRoute.prototype['nextHopIpAddress'] = undefined;

/**
 * Gets or sets the type of Azure hop the packet should be sent to.
 * @member {module:model/EffectiveRoute.NextHopTypeEnum} nextHopType
 */
EffectiveRoute.prototype['nextHopType'] = undefined;

/**
 * Gets who created the route
 * @member {module:model/EffectiveRoute.SourceEnum} source
 */
EffectiveRoute.prototype['source'] = undefined;

/**
 * Gets value of effective route
 * @member {module:model/EffectiveRoute.StateEnum} state
 */
EffectiveRoute.prototype['state'] = undefined;





/**
 * Allowed values for the <code>nextHopType</code> property.
 * @enum {String}
 * @readonly
 */
EffectiveRoute['NextHopTypeEnum'] = {

    /**
     * value: "VirtualNetworkGateway"
     * @const
     */
    "VirtualNetworkGateway": "VirtualNetworkGateway",

    /**
     * value: "VnetLocal"
     * @const
     */
    "VnetLocal": "VnetLocal",

    /**
     * value: "Internet"
     * @const
     */
    "Internet": "Internet",

    /**
     * value: "VirtualAppliance"
     * @const
     */
    "VirtualAppliance": "VirtualAppliance",

    /**
     * value: "None"
     * @const
     */
    "None": "None"
};


/**
 * Allowed values for the <code>source</code> property.
 * @enum {String}
 * @readonly
 */
EffectiveRoute['SourceEnum'] = {

    /**
     * value: "Unknown"
     * @const
     */
    "Unknown": "Unknown",

    /**
     * value: "User"
     * @const
     */
    "User": "User",

    /**
     * value: "VirtualNetworkGateway"
     * @const
     */
    "VirtualNetworkGateway": "VirtualNetworkGateway",

    /**
     * value: "Default"
     * @const
     */
    "Default": "Default"
};


/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
EffectiveRoute['StateEnum'] = {

    /**
     * value: "Active"
     * @const
     */
    "Active": "Active",

    /**
     * value: "Invalid"
     * @const
     */
    "Invalid": "Invalid"
};



export default EffectiveRoute;

