/**
 * Content Moderator Client
 * You use the API to scan your content as it is generated. Content Moderator then processes your content and sends the results along with relevant information either back to your systems or to the built-in review tool. You can use this information to take decisions e.g. take it down, send to human judge, etc.    When using the API, images need to have a minimum of 128 pixels and a maximum file size of 4MB.   Text can be at most 1024 characters long.   If the content passed to the text API or the image API exceeds the size limits, the API will return an error code that informs about the issue.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KeyValuePair from './KeyValuePair';
import Tag from './Tag';

/**
 * The Frame model module.
 * @module model/Frame
 * @version 1.0
 */
class Frame {
    /**
     * Constructs a new <code>Frame</code>.
     * Video frame property details.
     * @alias module:model/Frame
     */
    constructor() { 
        
        Frame.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Frame</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Frame} obj Optional instance to populate.
     * @return {module:model/Frame} The populated <code>Frame</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Frame();

            if (data.hasOwnProperty('FrameImage')) {
                obj['FrameImage'] = ApiClient.convertToType(data['FrameImage'], 'String');
            }
            if (data.hasOwnProperty('Metadata')) {
                obj['Metadata'] = ApiClient.convertToType(data['Metadata'], [KeyValuePair]);
            }
            if (data.hasOwnProperty('ReviewerResultTags')) {
                obj['ReviewerResultTags'] = ApiClient.convertToType(data['ReviewerResultTags'], [Tag]);
            }
            if (data.hasOwnProperty('Timestamp')) {
                obj['Timestamp'] = ApiClient.convertToType(data['Timestamp'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Frame</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Frame</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['FrameImage'] && !(typeof data['FrameImage'] === 'string' || data['FrameImage'] instanceof String)) {
            throw new Error("Expected the field `FrameImage` to be a primitive type in the JSON string but got " + data['FrameImage']);
        }
        if (data['Metadata']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Metadata'])) {
                throw new Error("Expected the field `Metadata` to be an array in the JSON data but got " + data['Metadata']);
            }
            // validate the optional field `Metadata` (array)
            for (const item of data['Metadata']) {
                KeyValuePair.validateJSON(item);
            };
        }
        if (data['ReviewerResultTags']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ReviewerResultTags'])) {
                throw new Error("Expected the field `ReviewerResultTags` to be an array in the JSON data but got " + data['ReviewerResultTags']);
            }
            // validate the optional field `ReviewerResultTags` (array)
            for (const item of data['ReviewerResultTags']) {
                Tag.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['Timestamp'] && !(typeof data['Timestamp'] === 'string' || data['Timestamp'] instanceof String)) {
            throw new Error("Expected the field `Timestamp` to be a primitive type in the JSON string but got " + data['Timestamp']);
        }

        return true;
    }


}



/**
 * Frame image.
 * @member {String} FrameImage
 */
Frame.prototype['FrameImage'] = undefined;

/**
 * Array of KeyValue.
 * @member {Array.<module:model/KeyValuePair>} Metadata
 */
Frame.prototype['Metadata'] = undefined;

/**
 * Reviewer result tags.
 * @member {Array.<module:model/Tag>} ReviewerResultTags
 */
Frame.prototype['ReviewerResultTags'] = undefined;

/**
 * Timestamp of the frame.
 * @member {String} Timestamp
 */
Frame.prototype['Timestamp'] = undefined;






export default Frame;

