/**
 * Content Moderator Client
 * You use the API to scan your content as it is generated. Content Moderator then processes your content and sends the results along with relevant information either back to your systems or to the built-in review tool. You can use this information to take decisions e.g. take it down, send to human judge, etc.    When using the API, images need to have a minimum of 128 pixels and a maximum file size of 4MB.   Text can be at most 1024 characters long.   If the content passed to the text API or the image API exceeds the size limits, the API will return an error code that informs about the issue.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Candidate from './Candidate';
import KeyValuePair from './KeyValuePair';
import Status from './Status';

/**
 * The OCR model module.
 * @module model/OCR
 * @version 1.0
 */
class OCR {
    /**
     * Constructs a new <code>OCR</code>.
     * Contains the text found in image for the language specified.
     * @alias module:model/OCR
     */
    constructor() { 
        
        OCR.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OCR</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OCR} obj Optional instance to populate.
     * @return {module:model/OCR} The populated <code>OCR</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OCR();

            if (data.hasOwnProperty('CacheId')) {
                obj['CacheId'] = ApiClient.convertToType(data['CacheId'], 'String');
            }
            if (data.hasOwnProperty('Candidates')) {
                obj['Candidates'] = ApiClient.convertToType(data['Candidates'], [Candidate]);
            }
            if (data.hasOwnProperty('Language')) {
                obj['Language'] = ApiClient.convertToType(data['Language'], 'String');
            }
            if (data.hasOwnProperty('Metadata')) {
                obj['Metadata'] = ApiClient.convertToType(data['Metadata'], [KeyValuePair]);
            }
            if (data.hasOwnProperty('Status')) {
                obj['Status'] = Status.constructFromObject(data['Status']);
            }
            if (data.hasOwnProperty('Text')) {
                obj['Text'] = ApiClient.convertToType(data['Text'], 'String');
            }
            if (data.hasOwnProperty('TrackingId')) {
                obj['TrackingId'] = ApiClient.convertToType(data['TrackingId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OCR</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OCR</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['CacheId'] && !(typeof data['CacheId'] === 'string' || data['CacheId'] instanceof String)) {
            throw new Error("Expected the field `CacheId` to be a primitive type in the JSON string but got " + data['CacheId']);
        }
        if (data['Candidates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Candidates'])) {
                throw new Error("Expected the field `Candidates` to be an array in the JSON data but got " + data['Candidates']);
            }
            // validate the optional field `Candidates` (array)
            for (const item of data['Candidates']) {
                Candidate.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['Language'] && !(typeof data['Language'] === 'string' || data['Language'] instanceof String)) {
            throw new Error("Expected the field `Language` to be a primitive type in the JSON string but got " + data['Language']);
        }
        if (data['Metadata']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Metadata'])) {
                throw new Error("Expected the field `Metadata` to be an array in the JSON data but got " + data['Metadata']);
            }
            // validate the optional field `Metadata` (array)
            for (const item of data['Metadata']) {
                KeyValuePair.validateJSON(item);
            };
        }
        // validate the optional field `Status`
        if (data['Status']) { // data not null
          Status.validateJSON(data['Status']);
        }
        // ensure the json data is a string
        if (data['Text'] && !(typeof data['Text'] === 'string' || data['Text'] instanceof String)) {
            throw new Error("Expected the field `Text` to be a primitive type in the JSON string but got " + data['Text']);
        }
        // ensure the json data is a string
        if (data['TrackingId'] && !(typeof data['TrackingId'] === 'string' || data['TrackingId'] instanceof String)) {
            throw new Error("Expected the field `TrackingId` to be a primitive type in the JSON string but got " + data['TrackingId']);
        }

        return true;
    }


}



/**
 * The cache id.
 * @member {String} CacheId
 */
OCR.prototype['CacheId'] = undefined;

/**
 * The list of candidate text.
 * @member {Array.<module:model/Candidate>} Candidates
 */
OCR.prototype['Candidates'] = undefined;

/**
 * The ISO 639-3 code.
 * @member {String} Language
 */
OCR.prototype['Language'] = undefined;

/**
 * Array of KeyValue.
 * @member {Array.<module:model/KeyValuePair>} Metadata
 */
OCR.prototype['Metadata'] = undefined;

/**
 * @member {module:model/Status} Status
 */
OCR.prototype['Status'] = undefined;

/**
 * The found text.
 * @member {String} Text
 */
OCR.prototype['Text'] = undefined;

/**
 * The tracking id.
 * @member {String} TrackingId
 */
OCR.prototype['TrackingId'] = undefined;






export default OCR;

