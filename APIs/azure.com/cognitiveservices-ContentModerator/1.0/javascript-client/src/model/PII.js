/**
 * Content Moderator Client
 * You use the API to scan your content as it is generated. Content Moderator then processes your content and sends the results along with relevant information either back to your systems or to the built-in review tool. You can use this information to take decisions e.g. take it down, send to human judge, etc.    When using the API, images need to have a minimum of 128 pixels and a maximum file size of 4MB.   Text can be at most 1024 characters long.   If the content passed to the text API or the image API exceeds the size limits, the API will return an error code that informs about the issue.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Address from './Address';
import Email from './Email';
import IPA from './IPA';
import Phone from './Phone';
import SSN from './SSN';

/**
 * The PII model module.
 * @module model/PII
 * @version 1.0
 */
class PII {
    /**
     * Constructs a new <code>PII</code>.
     * Personal Identifier Information details.
     * @alias module:model/PII
     */
    constructor() { 
        
        PII.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PII</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PII} obj Optional instance to populate.
     * @return {module:model/PII} The populated <code>PII</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PII();

            if (data.hasOwnProperty('Address')) {
                obj['Address'] = ApiClient.convertToType(data['Address'], [Address]);
            }
            if (data.hasOwnProperty('Email')) {
                obj['Email'] = ApiClient.convertToType(data['Email'], [Email]);
            }
            if (data.hasOwnProperty('IPA')) {
                obj['IPA'] = ApiClient.convertToType(data['IPA'], [IPA]);
            }
            if (data.hasOwnProperty('Phone')) {
                obj['Phone'] = ApiClient.convertToType(data['Phone'], [Phone]);
            }
            if (data.hasOwnProperty('SSN')) {
                obj['SSN'] = ApiClient.convertToType(data['SSN'], [SSN]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PII</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PII</code>.
     */
    static validateJSON(data) {
        if (data['Address']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Address'])) {
                throw new Error("Expected the field `Address` to be an array in the JSON data but got " + data['Address']);
            }
            // validate the optional field `Address` (array)
            for (const item of data['Address']) {
                Address.validateJSON(item);
            };
        }
        if (data['Email']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Email'])) {
                throw new Error("Expected the field `Email` to be an array in the JSON data but got " + data['Email']);
            }
            // validate the optional field `Email` (array)
            for (const item of data['Email']) {
                Email.validateJSON(item);
            };
        }
        if (data['IPA']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['IPA'])) {
                throw new Error("Expected the field `IPA` to be an array in the JSON data but got " + data['IPA']);
            }
            // validate the optional field `IPA` (array)
            for (const item of data['IPA']) {
                IPA.validateJSON(item);
            };
        }
        if (data['Phone']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Phone'])) {
                throw new Error("Expected the field `Phone` to be an array in the JSON data but got " + data['Phone']);
            }
            // validate the optional field `Phone` (array)
            for (const item of data['Phone']) {
                Phone.validateJSON(item);
            };
        }
        if (data['SSN']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['SSN'])) {
                throw new Error("Expected the field `SSN` to be an array in the JSON data but got " + data['SSN']);
            }
            // validate the optional field `SSN` (array)
            for (const item of data['SSN']) {
                SSN.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/Address>} Address
 */
PII.prototype['Address'] = undefined;

/**
 * @member {Array.<module:model/Email>} Email
 */
PII.prototype['Email'] = undefined;

/**
 * @member {Array.<module:model/IPA>} IPA
 */
PII.prototype['IPA'] = undefined;

/**
 * @member {Array.<module:model/Phone>} Phone
 */
PII.prototype['Phone'] = undefined;

/**
 * @member {Array.<module:model/SSN>} SSN
 */
PII.prototype['SSN'] = undefined;






export default PII;

