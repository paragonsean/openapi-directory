/**
 * SeaBreezeManagementClient
 * APIs to deploy and manage resources to SeaBreeze.
 *
 * The version of the OpenAPI document: 2018-07-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ContainerVolume model module.
 * @module model/ContainerVolume
 * @version 2018-07-01-preview
 */
class ContainerVolume {
    /**
     * Constructs a new <code>ContainerVolume</code>.
     * Describes how a volume is attached to a container.
     * @alias module:model/ContainerVolume
     * @param destinationPath {String} The path within the container at which the volume should be mounted. Only valid path characters are allowed.
     * @param name {String} Name of the volume.
     */
    constructor(destinationPath, name) { 
        
        ContainerVolume.initialize(this, destinationPath, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationPath, name) { 
        obj['destinationPath'] = destinationPath;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>ContainerVolume</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContainerVolume} obj Optional instance to populate.
     * @return {module:model/ContainerVolume} The populated <code>ContainerVolume</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContainerVolume();

            if (data.hasOwnProperty('destinationPath')) {
                obj['destinationPath'] = ApiClient.convertToType(data['destinationPath'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('readOnly')) {
                obj['readOnly'] = ApiClient.convertToType(data['readOnly'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContainerVolume</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContainerVolume</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContainerVolume.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['destinationPath'] && !(typeof data['destinationPath'] === 'string' || data['destinationPath'] instanceof String)) {
            throw new Error("Expected the field `destinationPath` to be a primitive type in the JSON string but got " + data['destinationPath']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

ContainerVolume.RequiredProperties = ["destinationPath", "name"];

/**
 * The path within the container at which the volume should be mounted. Only valid path characters are allowed.
 * @member {String} destinationPath
 */
ContainerVolume.prototype['destinationPath'] = undefined;

/**
 * Name of the volume.
 * @member {String} name
 */
ContainerVolume.prototype['name'] = undefined;

/**
 * The flag indicating whether the volume is read only. Default is 'false'.
 * @member {Boolean} readOnly
 */
ContainerVolume.prototype['readOnly'] = undefined;






export default ContainerVolume;

