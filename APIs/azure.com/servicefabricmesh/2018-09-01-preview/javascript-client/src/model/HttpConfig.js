/**
 * SeaBreezeManagementClient
 * APIs to deploy and manage resources to SeaBreeze.
 *
 * The version of the OpenAPI document: 2018-09-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HttpHostConfig from './HttpHostConfig';

/**
 * The HttpConfig model module.
 * @module model/HttpConfig
 * @version 2018-09-01-preview
 */
class HttpConfig {
    /**
     * Constructs a new <code>HttpConfig</code>.
     * Describes the http configuration for external connectivity for this network.
     * @alias module:model/HttpConfig
     * @param hosts {Array.<module:model/HttpHostConfig>} description for routing.
     * @param name {String} http gateway config name.
     * @param port {Number} Specifies the port at which the service endpoint below needs to be exposed.
     */
    constructor(hosts, name, port) { 
        
        HttpConfig.initialize(this, hosts, name, port);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hosts, name, port) { 
        obj['hosts'] = hosts;
        obj['name'] = name;
        obj['port'] = port;
    }

    /**
     * Constructs a <code>HttpConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HttpConfig} obj Optional instance to populate.
     * @return {module:model/HttpConfig} The populated <code>HttpConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HttpConfig();

            if (data.hasOwnProperty('hosts')) {
                obj['hosts'] = ApiClient.convertToType(data['hosts'], [HttpHostConfig]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('port')) {
                obj['port'] = ApiClient.convertToType(data['port'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HttpConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HttpConfig</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of HttpConfig.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['hosts']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['hosts'])) {
                throw new Error("Expected the field `hosts` to be an array in the JSON data but got " + data['hosts']);
            }
            // validate the optional field `hosts` (array)
            for (const item of data['hosts']) {
                HttpHostConfig.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

HttpConfig.RequiredProperties = ["hosts", "name", "port"];

/**
 * description for routing.
 * @member {Array.<module:model/HttpHostConfig>} hosts
 */
HttpConfig.prototype['hosts'] = undefined;

/**
 * http gateway config name.
 * @member {String} name
 */
HttpConfig.prototype['name'] = undefined;

/**
 * Specifies the port at which the service endpoint below needs to be exposed.
 * @member {Number} port
 */
HttpConfig.prototype['port'] = undefined;






export default HttpConfig;

