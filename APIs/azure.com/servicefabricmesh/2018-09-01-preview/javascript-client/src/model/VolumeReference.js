/**
 * SeaBreezeManagementClient
 * APIs to deploy and manage resources to SeaBreeze.
 *
 * The version of the OpenAPI document: 2018-09-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VolumeReference model module.
 * @module model/VolumeReference
 * @version 2018-09-01-preview
 */
class VolumeReference {
    /**
     * Constructs a new <code>VolumeReference</code>.
     * Describes a reference to a volume resource.
     * @alias module:model/VolumeReference
     * @param destinationPath {String} The path within the container at which the volume should be mounted. Only valid path characters are allowed.
     * @param name {String} Name of the volume being referenced.
     */
    constructor(destinationPath, name) { 
        
        VolumeReference.initialize(this, destinationPath, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationPath, name) { 
        obj['destinationPath'] = destinationPath;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>VolumeReference</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VolumeReference} obj Optional instance to populate.
     * @return {module:model/VolumeReference} The populated <code>VolumeReference</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VolumeReference();

            if (data.hasOwnProperty('destinationPath')) {
                obj['destinationPath'] = ApiClient.convertToType(data['destinationPath'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('readOnly')) {
                obj['readOnly'] = ApiClient.convertToType(data['readOnly'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VolumeReference</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VolumeReference</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VolumeReference.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['destinationPath'] && !(typeof data['destinationPath'] === 'string' || data['destinationPath'] instanceof String)) {
            throw new Error("Expected the field `destinationPath` to be a primitive type in the JSON string but got " + data['destinationPath']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

VolumeReference.RequiredProperties = ["destinationPath", "name"];

/**
 * The path within the container at which the volume should be mounted. Only valid path characters are allowed.
 * @member {String} destinationPath
 */
VolumeReference.prototype['destinationPath'] = undefined;

/**
 * Name of the volume being referenced.
 * @member {String} name
 */
VolumeReference.prototype['name'] = undefined;

/**
 * The flag indicating whether the volume is read only. Default is 'false'.
 * @member {Boolean} readOnly
 */
VolumeReference.prototype['readOnly'] = undefined;






export default VolumeReference;

