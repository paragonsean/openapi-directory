/**
 * Microsoft.ResourceHealth
 * The Resource Health Client.
 *
 * The version of the OpenAPI document: 2018-07-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Update from './Update';

/**
 * The ImpactedServiceRegion model module.
 * @module model/ImpactedServiceRegion
 * @version 2018-07-01-preview
 */
class ImpactedServiceRegion {
    /**
     * Constructs a new <code>ImpactedServiceRegion</code>.
     * Azure region impacted by the service health event.
     * @alias module:model/ImpactedServiceRegion
     */
    constructor() { 
        
        ImpactedServiceRegion.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ImpactedServiceRegion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImpactedServiceRegion} obj Optional instance to populate.
     * @return {module:model/ImpactedServiceRegion} The populated <code>ImpactedServiceRegion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImpactedServiceRegion();

            if (data.hasOwnProperty('impactedRegion')) {
                obj['impactedRegion'] = ApiClient.convertToType(data['impactedRegion'], 'String');
            }
            if (data.hasOwnProperty('impactedSubscriptions')) {
                obj['impactedSubscriptions'] = ApiClient.convertToType(data['impactedSubscriptions'], ['String']);
            }
            if (data.hasOwnProperty('lastUpdateTime')) {
                obj['lastUpdateTime'] = ApiClient.convertToType(data['lastUpdateTime'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('updates')) {
                obj['updates'] = ApiClient.convertToType(data['updates'], [Update]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImpactedServiceRegion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImpactedServiceRegion</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['impactedRegion'] && !(typeof data['impactedRegion'] === 'string' || data['impactedRegion'] instanceof String)) {
            throw new Error("Expected the field `impactedRegion` to be a primitive type in the JSON string but got " + data['impactedRegion']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['impactedSubscriptions'])) {
            throw new Error("Expected the field `impactedSubscriptions` to be an array in the JSON data but got " + data['impactedSubscriptions']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        if (data['updates']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['updates'])) {
                throw new Error("Expected the field `updates` to be an array in the JSON data but got " + data['updates']);
            }
            // validate the optional field `updates` (array)
            for (const item of data['updates']) {
                Update.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Impacted region name.
 * @member {String} impactedRegion
 */
ImpactedServiceRegion.prototype['impactedRegion'] = undefined;

/**
 * List subscription impacted by the service health event.
 * @member {Array.<String>} impactedSubscriptions
 */
ImpactedServiceRegion.prototype['impactedSubscriptions'] = undefined;

/**
 * It provides the Timestamp for when the last update for the service health event.
 * @member {Date} lastUpdateTime
 */
ImpactedServiceRegion.prototype['lastUpdateTime'] = undefined;

/**
 * Current status of event in the region.
 * @member {module:model/ImpactedServiceRegion.StatusEnum} status
 */
ImpactedServiceRegion.prototype['status'] = undefined;

/**
 * List of updates for given service health event.
 * @member {Array.<module:model/Update>} updates
 */
ImpactedServiceRegion.prototype['updates'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ImpactedServiceRegion['StatusEnum'] = {

    /**
     * value: "Active"
     * @const
     */
    "Active": "Active",

    /**
     * value: "Resolved"
     * @const
     */
    "Resolved": "Resolved"
};



export default ImpactedServiceRegion;

