/*
 * Microsoft.ResourceHealth
 * The Resource Health Client.
 *
 * The version of the OpenAPI document: 2018-07-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.StatusActiveEvent;
import org.openapitools.client.model.StatusBanner;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * On-going emerging issue from azure status.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T10:18:50.134798-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class EmergingIssue {
  public static final String SERIALIZED_NAME_REFRESH_TIMESTAMP = "refreshTimestamp";
  @SerializedName(SERIALIZED_NAME_REFRESH_TIMESTAMP)
  private OffsetDateTime refreshTimestamp;

  public static final String SERIALIZED_NAME_STATUS_ACTIVE_EVENTS = "statusActiveEvents";
  @SerializedName(SERIALIZED_NAME_STATUS_ACTIVE_EVENTS)
  private List<StatusActiveEvent> statusActiveEvents = new ArrayList<>();

  public static final String SERIALIZED_NAME_STATUS_BANNERS = "statusBanners";
  @SerializedName(SERIALIZED_NAME_STATUS_BANNERS)
  private List<StatusBanner> statusBanners = new ArrayList<>();

  public EmergingIssue() {
  }

  public EmergingIssue refreshTimestamp(OffsetDateTime refreshTimestamp) {
    this.refreshTimestamp = refreshTimestamp;
    return this;
  }

  /**
   * Timestamp for when last time refreshed for ongoing emerging issue.
   * @return refreshTimestamp
   */
  @javax.annotation.Nullable
  public OffsetDateTime getRefreshTimestamp() {
    return refreshTimestamp;
  }

  public void setRefreshTimestamp(OffsetDateTime refreshTimestamp) {
    this.refreshTimestamp = refreshTimestamp;
  }


  public EmergingIssue statusActiveEvents(List<StatusActiveEvent> statusActiveEvents) {
    this.statusActiveEvents = statusActiveEvents;
    return this;
  }

  public EmergingIssue addStatusActiveEventsItem(StatusActiveEvent statusActiveEventsItem) {
    if (this.statusActiveEvents == null) {
      this.statusActiveEvents = new ArrayList<>();
    }
    this.statusActiveEvents.add(statusActiveEventsItem);
    return this;
  }

  /**
   * The list of emerging issues of active event type.
   * @return statusActiveEvents
   */
  @javax.annotation.Nullable
  public List<StatusActiveEvent> getStatusActiveEvents() {
    return statusActiveEvents;
  }

  public void setStatusActiveEvents(List<StatusActiveEvent> statusActiveEvents) {
    this.statusActiveEvents = statusActiveEvents;
  }


  public EmergingIssue statusBanners(List<StatusBanner> statusBanners) {
    this.statusBanners = statusBanners;
    return this;
  }

  public EmergingIssue addStatusBannersItem(StatusBanner statusBannersItem) {
    if (this.statusBanners == null) {
      this.statusBanners = new ArrayList<>();
    }
    this.statusBanners.add(statusBannersItem);
    return this;
  }

  /**
   * The list of emerging issues of banner type.
   * @return statusBanners
   */
  @javax.annotation.Nullable
  public List<StatusBanner> getStatusBanners() {
    return statusBanners;
  }

  public void setStatusBanners(List<StatusBanner> statusBanners) {
    this.statusBanners = statusBanners;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    EmergingIssue emergingIssue = (EmergingIssue) o;
    return Objects.equals(this.refreshTimestamp, emergingIssue.refreshTimestamp) &&
        Objects.equals(this.statusActiveEvents, emergingIssue.statusActiveEvents) &&
        Objects.equals(this.statusBanners, emergingIssue.statusBanners);
  }

  @Override
  public int hashCode() {
    return Objects.hash(refreshTimestamp, statusActiveEvents, statusBanners);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class EmergingIssue {\n");
    sb.append("    refreshTimestamp: ").append(toIndentedString(refreshTimestamp)).append("\n");
    sb.append("    statusActiveEvents: ").append(toIndentedString(statusActiveEvents)).append("\n");
    sb.append("    statusBanners: ").append(toIndentedString(statusBanners)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("refreshTimestamp");
    openapiFields.add("statusActiveEvents");
    openapiFields.add("statusBanners");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to EmergingIssue
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!EmergingIssue.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in EmergingIssue is not found in the empty JSON string", EmergingIssue.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!EmergingIssue.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `EmergingIssue` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("statusActiveEvents") != null && !jsonObj.get("statusActiveEvents").isJsonNull()) {
        JsonArray jsonArraystatusActiveEvents = jsonObj.getAsJsonArray("statusActiveEvents");
        if (jsonArraystatusActiveEvents != null) {
          // ensure the json data is an array
          if (!jsonObj.get("statusActiveEvents").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `statusActiveEvents` to be an array in the JSON string but got `%s`", jsonObj.get("statusActiveEvents").toString()));
          }

          // validate the optional field `statusActiveEvents` (array)
          for (int i = 0; i < jsonArraystatusActiveEvents.size(); i++) {
            StatusActiveEvent.validateJsonElement(jsonArraystatusActiveEvents.get(i));
          };
        }
      }
      if (jsonObj.get("statusBanners") != null && !jsonObj.get("statusBanners").isJsonNull()) {
        JsonArray jsonArraystatusBanners = jsonObj.getAsJsonArray("statusBanners");
        if (jsonArraystatusBanners != null) {
          // ensure the json data is an array
          if (!jsonObj.get("statusBanners").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `statusBanners` to be an array in the JSON string but got `%s`", jsonObj.get("statusBanners").toString()));
          }

          // validate the optional field `statusBanners` (array)
          for (int i = 0; i < jsonArraystatusBanners.size(); i++) {
            StatusBanner.validateJsonElement(jsonArraystatusBanners.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!EmergingIssue.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'EmergingIssue' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<EmergingIssue> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(EmergingIssue.class));

       return (TypeAdapter<T>) new TypeAdapter<EmergingIssue>() {
           @Override
           public void write(JsonWriter out, EmergingIssue value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public EmergingIssue read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of EmergingIssue given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of EmergingIssue
   * @throws IOException if the JSON string is invalid with respect to EmergingIssue
   */
  public static EmergingIssue fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, EmergingIssue.class);
  }

  /**
   * Convert an instance of EmergingIssue to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

