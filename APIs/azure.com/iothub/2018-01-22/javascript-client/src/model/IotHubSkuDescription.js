/**
 * iotHubClient
 * Use this API to manage the IoT hubs in your Azure subscription.
 *
 * The version of the OpenAPI document: 2018-01-22
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IotHubCapacity from './IotHubCapacity';
import IotHubSkuInfo from './IotHubSkuInfo';

/**
 * The IotHubSkuDescription model module.
 * @module model/IotHubSkuDescription
 * @version 2018-01-22
 */
class IotHubSkuDescription {
    /**
     * Constructs a new <code>IotHubSkuDescription</code>.
     * SKU properties.
     * @alias module:model/IotHubSkuDescription
     * @param capacity {module:model/IotHubCapacity} 
     * @param sku {module:model/IotHubSkuInfo} 
     */
    constructor(capacity, sku) { 
        
        IotHubSkuDescription.initialize(this, capacity, sku);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, capacity, sku) { 
        obj['capacity'] = capacity;
        obj['sku'] = sku;
    }

    /**
     * Constructs a <code>IotHubSkuDescription</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IotHubSkuDescription} obj Optional instance to populate.
     * @return {module:model/IotHubSkuDescription} The populated <code>IotHubSkuDescription</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IotHubSkuDescription();

            if (data.hasOwnProperty('capacity')) {
                obj['capacity'] = IotHubCapacity.constructFromObject(data['capacity']);
            }
            if (data.hasOwnProperty('resourceType')) {
                obj['resourceType'] = ApiClient.convertToType(data['resourceType'], 'String');
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = IotHubSkuInfo.constructFromObject(data['sku']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IotHubSkuDescription</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IotHubSkuDescription</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IotHubSkuDescription.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `capacity`
        if (data['capacity']) { // data not null
          IotHubCapacity.validateJSON(data['capacity']);
        }
        // ensure the json data is a string
        if (data['resourceType'] && !(typeof data['resourceType'] === 'string' || data['resourceType'] instanceof String)) {
            throw new Error("Expected the field `resourceType` to be a primitive type in the JSON string but got " + data['resourceType']);
        }
        // validate the optional field `sku`
        if (data['sku']) { // data not null
          IotHubSkuInfo.validateJSON(data['sku']);
        }

        return true;
    }


}

IotHubSkuDescription.RequiredProperties = ["capacity", "sku"];

/**
 * @member {module:model/IotHubCapacity} capacity
 */
IotHubSkuDescription.prototype['capacity'] = undefined;

/**
 * The type of the resource.
 * @member {String} resourceType
 */
IotHubSkuDescription.prototype['resourceType'] = undefined;

/**
 * @member {module:model/IotHubSkuInfo} sku
 */
IotHubSkuDescription.prototype['sku'] = undefined;






export default IotHubSkuDescription;

