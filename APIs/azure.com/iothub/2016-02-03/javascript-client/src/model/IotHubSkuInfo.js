/**
 * iotHubClient
 * Use this API to manage the IoT hubs in your Azure subscription.
 *
 * The version of the OpenAPI document: 2016-02-03
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IotHubSkuInfo model module.
 * @module model/IotHubSkuInfo
 * @version 2016-02-03
 */
class IotHubSkuInfo {
    /**
     * Constructs a new <code>IotHubSkuInfo</code>.
     * Information about the SKU of the IoT hub.
     * @alias module:model/IotHubSkuInfo
     * @param capacity {Number} The number of provisioned IoT Hub units. See: https://docs.microsoft.com/azure/azure-subscription-service-limits#iot-hub-limits.
     * @param name {module:model/IotHubSkuInfo.NameEnum} The name of the SKU.
     */
    constructor(capacity, name) { 
        
        IotHubSkuInfo.initialize(this, capacity, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, capacity, name) { 
        obj['capacity'] = capacity;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>IotHubSkuInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IotHubSkuInfo} obj Optional instance to populate.
     * @return {module:model/IotHubSkuInfo} The populated <code>IotHubSkuInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IotHubSkuInfo();

            if (data.hasOwnProperty('capacity')) {
                obj['capacity'] = ApiClient.convertToType(data['capacity'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('tier')) {
                obj['tier'] = ApiClient.convertToType(data['tier'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IotHubSkuInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IotHubSkuInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IotHubSkuInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['tier'] && !(typeof data['tier'] === 'string' || data['tier'] instanceof String)) {
            throw new Error("Expected the field `tier` to be a primitive type in the JSON string but got " + data['tier']);
        }

        return true;
    }


}

IotHubSkuInfo.RequiredProperties = ["capacity", "name"];

/**
 * The number of provisioned IoT Hub units. See: https://docs.microsoft.com/azure/azure-subscription-service-limits#iot-hub-limits.
 * @member {Number} capacity
 */
IotHubSkuInfo.prototype['capacity'] = undefined;

/**
 * The name of the SKU.
 * @member {module:model/IotHubSkuInfo.NameEnum} name
 */
IotHubSkuInfo.prototype['name'] = undefined;

/**
 * The billing tier for the IoT hub.
 * @member {module:model/IotHubSkuInfo.TierEnum} tier
 */
IotHubSkuInfo.prototype['tier'] = undefined;





/**
 * Allowed values for the <code>name</code> property.
 * @enum {String}
 * @readonly
 */
IotHubSkuInfo['NameEnum'] = {

    /**
     * value: "F1"
     * @const
     */
    "F1": "F1",

    /**
     * value: "S1"
     * @const
     */
    "S1": "S1",

    /**
     * value: "S2"
     * @const
     */
    "S2": "S2",

    /**
     * value: "S3"
     * @const
     */
    "S3": "S3"
};


/**
 * Allowed values for the <code>tier</code> property.
 * @enum {String}
 * @readonly
 */
IotHubSkuInfo['TierEnum'] = {

    /**
     * value: "Free"
     * @const
     */
    "Free": "Free",

    /**
     * value: "Standard"
     * @const
     */
    "Standard": "Standard"
};



export default IotHubSkuInfo;

