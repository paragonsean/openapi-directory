/**
 * iotHubClient
 * Use this API to manage the IoT hubs in your Azure subscription.
 *
 * The version of the OpenAPI document: 2018-04-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FallbackRouteProperties model module.
 * @module model/FallbackRouteProperties
 * @version 2018-04-01
 */
class FallbackRouteProperties {
    /**
     * Constructs a new <code>FallbackRouteProperties</code>.
     * The properties of the fallback route. IoT Hub uses these properties when it routes messages to the fallback endpoint.
     * @alias module:model/FallbackRouteProperties
     * @param endpointNames {Array.<String>} The list of endpoints to which the messages that satisfy the condition are routed to. Currently only 1 endpoint is allowed.
     * @param isEnabled {Boolean} Used to specify whether the fallback route is enabled.
     * @param source {module:model/FallbackRouteProperties.SourceEnum} The source to which the routing rule is to be applied to. For example, DeviceMessages
     */
    constructor(endpointNames, isEnabled, source) { 
        
        FallbackRouteProperties.initialize(this, endpointNames, isEnabled, source);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endpointNames, isEnabled, source) { 
        obj['endpointNames'] = endpointNames;
        obj['isEnabled'] = isEnabled;
        obj['source'] = source;
    }

    /**
     * Constructs a <code>FallbackRouteProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FallbackRouteProperties} obj Optional instance to populate.
     * @return {module:model/FallbackRouteProperties} The populated <code>FallbackRouteProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FallbackRouteProperties();

            if (data.hasOwnProperty('condition')) {
                obj['condition'] = ApiClient.convertToType(data['condition'], 'String');
            }
            if (data.hasOwnProperty('endpointNames')) {
                obj['endpointNames'] = ApiClient.convertToType(data['endpointNames'], ['String']);
            }
            if (data.hasOwnProperty('isEnabled')) {
                obj['isEnabled'] = ApiClient.convertToType(data['isEnabled'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FallbackRouteProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FallbackRouteProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FallbackRouteProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['condition'] && !(typeof data['condition'] === 'string' || data['condition'] instanceof String)) {
            throw new Error("Expected the field `condition` to be a primitive type in the JSON string but got " + data['condition']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['endpointNames'])) {
            throw new Error("Expected the field `endpointNames` to be an array in the JSON data but got " + data['endpointNames']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

FallbackRouteProperties.RequiredProperties = ["endpointNames", "isEnabled", "source"];

/**
 * The condition which is evaluated in order to apply the fallback route. If the condition is not provided it will evaluate to true by default. For grammar, See: https://docs.microsoft.com/azure/iot-hub/iot-hub-devguide-query-language
 * @member {String} condition
 */
FallbackRouteProperties.prototype['condition'] = undefined;

/**
 * The list of endpoints to which the messages that satisfy the condition are routed to. Currently only 1 endpoint is allowed.
 * @member {Array.<String>} endpointNames
 */
FallbackRouteProperties.prototype['endpointNames'] = undefined;

/**
 * Used to specify whether the fallback route is enabled.
 * @member {Boolean} isEnabled
 */
FallbackRouteProperties.prototype['isEnabled'] = undefined;

/**
 * The name of the route. The name can only include alphanumeric characters, periods, underscores, hyphens, has a maximum length of 64 characters, and must be unique.
 * @member {String} name
 */
FallbackRouteProperties.prototype['name'] = undefined;

/**
 * The source to which the routing rule is to be applied to. For example, DeviceMessages
 * @member {module:model/FallbackRouteProperties.SourceEnum} source
 */
FallbackRouteProperties.prototype['source'] = undefined;





/**
 * Allowed values for the <code>source</code> property.
 * @enum {String}
 * @readonly
 */
FallbackRouteProperties['SourceEnum'] = {

    /**
     * value: "DeviceMessages"
     * @const
     */
    "DeviceMessages": "DeviceMessages"
};



export default FallbackRouteProperties;

