/**
 * iotHubClient
 * Use this API to manage the IoT hubs in your Azure subscription.
 *
 * The version of the OpenAPI document: 2019-07-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RoutingEventHubProperties from './RoutingEventHubProperties';
import RoutingServiceBusQueueEndpointProperties from './RoutingServiceBusQueueEndpointProperties';
import RoutingServiceBusTopicEndpointProperties from './RoutingServiceBusTopicEndpointProperties';
import RoutingStorageContainerProperties from './RoutingStorageContainerProperties';

/**
 * The RoutingEndpoints model module.
 * @module model/RoutingEndpoints
 * @version 2019-07-01-preview
 */
class RoutingEndpoints {
    /**
     * Constructs a new <code>RoutingEndpoints</code>.
     * The properties related to the custom endpoints to which your IoT hub routes messages based on the routing rules. A maximum of 10 custom endpoints are allowed across all endpoint types for paid hubs and only 1 custom endpoint is allowed across all endpoint types for free hubs.
     * @alias module:model/RoutingEndpoints
     */
    constructor() { 
        
        RoutingEndpoints.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RoutingEndpoints</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoutingEndpoints} obj Optional instance to populate.
     * @return {module:model/RoutingEndpoints} The populated <code>RoutingEndpoints</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoutingEndpoints();

            if (data.hasOwnProperty('eventHubs')) {
                obj['eventHubs'] = ApiClient.convertToType(data['eventHubs'], [RoutingEventHubProperties]);
            }
            if (data.hasOwnProperty('serviceBusQueues')) {
                obj['serviceBusQueues'] = ApiClient.convertToType(data['serviceBusQueues'], [RoutingServiceBusQueueEndpointProperties]);
            }
            if (data.hasOwnProperty('serviceBusTopics')) {
                obj['serviceBusTopics'] = ApiClient.convertToType(data['serviceBusTopics'], [RoutingServiceBusTopicEndpointProperties]);
            }
            if (data.hasOwnProperty('storageContainers')) {
                obj['storageContainers'] = ApiClient.convertToType(data['storageContainers'], [RoutingStorageContainerProperties]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoutingEndpoints</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoutingEndpoints</code>.
     */
    static validateJSON(data) {
        if (data['eventHubs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['eventHubs'])) {
                throw new Error("Expected the field `eventHubs` to be an array in the JSON data but got " + data['eventHubs']);
            }
            // validate the optional field `eventHubs` (array)
            for (const item of data['eventHubs']) {
                RoutingEventHubProperties.validateJSON(item);
            };
        }
        if (data['serviceBusQueues']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['serviceBusQueues'])) {
                throw new Error("Expected the field `serviceBusQueues` to be an array in the JSON data but got " + data['serviceBusQueues']);
            }
            // validate the optional field `serviceBusQueues` (array)
            for (const item of data['serviceBusQueues']) {
                RoutingServiceBusQueueEndpointProperties.validateJSON(item);
            };
        }
        if (data['serviceBusTopics']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['serviceBusTopics'])) {
                throw new Error("Expected the field `serviceBusTopics` to be an array in the JSON data but got " + data['serviceBusTopics']);
            }
            // validate the optional field `serviceBusTopics` (array)
            for (const item of data['serviceBusTopics']) {
                RoutingServiceBusTopicEndpointProperties.validateJSON(item);
            };
        }
        if (data['storageContainers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['storageContainers'])) {
                throw new Error("Expected the field `storageContainers` to be an array in the JSON data but got " + data['storageContainers']);
            }
            // validate the optional field `storageContainers` (array)
            for (const item of data['storageContainers']) {
                RoutingStorageContainerProperties.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The list of Event Hubs endpoints that IoT hub routes messages to, based on the routing rules. This list does not include the built-in Event Hubs endpoint.
 * @member {Array.<module:model/RoutingEventHubProperties>} eventHubs
 */
RoutingEndpoints.prototype['eventHubs'] = undefined;

/**
 * The list of Service Bus queue endpoints that IoT hub routes the messages to, based on the routing rules.
 * @member {Array.<module:model/RoutingServiceBusQueueEndpointProperties>} serviceBusQueues
 */
RoutingEndpoints.prototype['serviceBusQueues'] = undefined;

/**
 * The list of Service Bus topic endpoints that the IoT hub routes the messages to, based on the routing rules.
 * @member {Array.<module:model/RoutingServiceBusTopicEndpointProperties>} serviceBusTopics
 */
RoutingEndpoints.prototype['serviceBusTopics'] = undefined;

/**
 * The list of storage container endpoints that IoT hub routes messages to, based on the routing rules.
 * @member {Array.<module:model/RoutingStorageContainerProperties>} storageContainers
 */
RoutingEndpoints.prototype['storageContainers'] = undefined;






export default RoutingEndpoints;

