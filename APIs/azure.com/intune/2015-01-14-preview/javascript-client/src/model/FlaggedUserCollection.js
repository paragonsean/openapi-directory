/**
 * IntuneResourceManagementClient
 * Microsoft.Intune Resource provider Api features in the swagger-2.0 specification
 *
 * The version of the OpenAPI document: 2015-01-14-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FlaggedUser from './FlaggedUser';

/**
 * The FlaggedUserCollection model module.
 * @module model/FlaggedUserCollection
 * @version 2015-01-14-preview
 */
class FlaggedUserCollection {
    /**
     * Constructs a new <code>FlaggedUserCollection</code>.
     * Flagged user collection for the given tenant.
     * @alias module:model/FlaggedUserCollection
     * @param value {Array.<module:model/FlaggedUser>} 
     */
    constructor(value) { 
        
        FlaggedUserCollection.initialize(this, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, value) { 
        obj['value'] = value;
    }

    /**
     * Constructs a <code>FlaggedUserCollection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FlaggedUserCollection} obj Optional instance to populate.
     * @return {module:model/FlaggedUserCollection} The populated <code>FlaggedUserCollection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FlaggedUserCollection();

            if (data.hasOwnProperty('nextlink')) {
                obj['nextlink'] = ApiClient.convertToType(data['nextlink'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], [FlaggedUser]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FlaggedUserCollection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FlaggedUserCollection</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FlaggedUserCollection.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['nextlink'] && !(typeof data['nextlink'] === 'string' || data['nextlink'] instanceof String)) {
            throw new Error("Expected the field `nextlink` to be a primitive type in the JSON string but got " + data['nextlink']);
        }
        if (data['value']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['value'])) {
                throw new Error("Expected the field `value` to be an array in the JSON data but got " + data['value']);
            }
            // validate the optional field `value` (array)
            for (const item of data['value']) {
                FlaggedUser.validateJSON(item);
            };
        }

        return true;
    }


}

FlaggedUserCollection.RequiredProperties = ["value"];

/**
 * Gets the URL to get the next set of results.
 * @member {String} nextlink
 */
FlaggedUserCollection.prototype['nextlink'] = undefined;

/**
 * @member {Array.<module:model/FlaggedUser>} value
 */
FlaggedUserCollection.prototype['value'] = undefined;






export default FlaggedUserCollection;

