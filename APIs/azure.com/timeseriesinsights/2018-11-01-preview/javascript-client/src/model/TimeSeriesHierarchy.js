/**
 * TimeSeriesInsightsClient
 * Time Series Insights environment data plane client for PAYG (Preview L1 SKU) environments.
 *
 * The version of the OpenAPI document: 2018-11-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TimeSeriesHierarchySource from './TimeSeriesHierarchySource';

/**
 * The TimeSeriesHierarchy model module.
 * @module model/TimeSeriesHierarchy
 * @version 2018-11-01-preview
 */
class TimeSeriesHierarchy {
    /**
     * Constructs a new <code>TimeSeriesHierarchy</code>.
     * Time series hierarchy organizes time series instances into a tree.
     * @alias module:model/TimeSeriesHierarchy
     * @param name {String} User-given unique name for the type. It is mutable and not null.
     * @param source {module:model/TimeSeriesHierarchySource} 
     */
    constructor(name, source) { 
        
        TimeSeriesHierarchy.initialize(this, name, source);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, source) { 
        obj['name'] = name;
        obj['source'] = source;
    }

    /**
     * Constructs a <code>TimeSeriesHierarchy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TimeSeriesHierarchy} obj Optional instance to populate.
     * @return {module:model/TimeSeriesHierarchy} The populated <code>TimeSeriesHierarchy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TimeSeriesHierarchy();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = TimeSeriesHierarchySource.constructFromObject(data['source']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TimeSeriesHierarchy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TimeSeriesHierarchy</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TimeSeriesHierarchy.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `source`
        if (data['source']) { // data not null
          TimeSeriesHierarchySource.validateJSON(data['source']);
        }

        return true;
    }


}

TimeSeriesHierarchy.RequiredProperties = ["name", "source"];

/**
 * Unique hierarchy identifier that is a immutable GUID. Can be null while creating hierarchy objects and then server generates the id, not null on get and delete operations.
 * @member {String} id
 */
TimeSeriesHierarchy.prototype['id'] = undefined;

/**
 * User-given unique name for the type. It is mutable and not null.
 * @member {String} name
 */
TimeSeriesHierarchy.prototype['name'] = undefined;

/**
 * @member {module:model/TimeSeriesHierarchySource} source
 */
TimeSeriesHierarchy.prototype['source'] = undefined;






export default TimeSeriesHierarchy;

