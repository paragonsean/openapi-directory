/**
 * CostManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-08-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReportDataset from './ReportDataset';
import ReportTimePeriod from './ReportTimePeriod';

/**
 * The ReportDefinition model module.
 * @module model/ReportDefinition
 * @version 2018-08-01-preview
 */
class ReportDefinition {
    /**
     * Constructs a new <code>ReportDefinition</code>.
     * The definition of a report.
     * @alias module:model/ReportDefinition
     * @param timeframe {module:model/ReportDefinition.TimeframeEnum} The time frame for pulling data for the report. If custom, then a specific time period must be provided.
     * @param type {module:model/ReportDefinition.TypeEnum} The type of the report.
     */
    constructor(timeframe, type) { 
        
        ReportDefinition.initialize(this, timeframe, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, timeframe, type) { 
        obj['timeframe'] = timeframe;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ReportDefinition</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportDefinition} obj Optional instance to populate.
     * @return {module:model/ReportDefinition} The populated <code>ReportDefinition</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportDefinition();

            if (data.hasOwnProperty('dataset')) {
                obj['dataset'] = ReportDataset.constructFromObject(data['dataset']);
            }
            if (data.hasOwnProperty('timePeriod')) {
                obj['timePeriod'] = ReportTimePeriod.constructFromObject(data['timePeriod']);
            }
            if (data.hasOwnProperty('timeframe')) {
                obj['timeframe'] = ApiClient.convertToType(data['timeframe'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportDefinition</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportDefinition</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReportDefinition.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `dataset`
        if (data['dataset']) { // data not null
          ReportDataset.validateJSON(data['dataset']);
        }
        // validate the optional field `timePeriod`
        if (data['timePeriod']) { // data not null
          ReportTimePeriod.validateJSON(data['timePeriod']);
        }
        // ensure the json data is a string
        if (data['timeframe'] && !(typeof data['timeframe'] === 'string' || data['timeframe'] instanceof String)) {
            throw new Error("Expected the field `timeframe` to be a primitive type in the JSON string but got " + data['timeframe']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ReportDefinition.RequiredProperties = ["timeframe", "type"];

/**
 * @member {module:model/ReportDataset} dataset
 */
ReportDefinition.prototype['dataset'] = undefined;

/**
 * @member {module:model/ReportTimePeriod} timePeriod
 */
ReportDefinition.prototype['timePeriod'] = undefined;

/**
 * The time frame for pulling data for the report. If custom, then a specific time period must be provided.
 * @member {module:model/ReportDefinition.TimeframeEnum} timeframe
 */
ReportDefinition.prototype['timeframe'] = undefined;

/**
 * The type of the report.
 * @member {module:model/ReportDefinition.TypeEnum} type
 */
ReportDefinition.prototype['type'] = undefined;





/**
 * Allowed values for the <code>timeframe</code> property.
 * @enum {String}
 * @readonly
 */
ReportDefinition['TimeframeEnum'] = {

    /**
     * value: "WeekToDate"
     * @const
     */
    "WeekToDate": "WeekToDate",

    /**
     * value: "MonthToDate"
     * @const
     */
    "MonthToDate": "MonthToDate",

    /**
     * value: "Custom"
     * @const
     */
    "Custom": "Custom"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ReportDefinition['TypeEnum'] = {

    /**
     * value: "Usage"
     * @const
     */
    "Usage": "Usage"
};



export default ReportDefinition;

