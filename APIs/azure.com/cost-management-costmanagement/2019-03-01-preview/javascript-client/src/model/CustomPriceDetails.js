/**
 * CostManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-03-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Markup from './Markup';

/**
 * The CustomPriceDetails model module.
 * @module model/CustomPriceDetails
 * @version 2019-03-01-preview
 */
class CustomPriceDetails {
    /**
     * Constructs a new <code>CustomPriceDetails</code>.
     * Represents Custom price showback rule model
     * @alias module:model/CustomPriceDetails
     */
    constructor() { 
        
        CustomPriceDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CustomPriceDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomPriceDetails} obj Optional instance to populate.
     * @return {module:model/CustomPriceDetails} The populated <code>CustomPriceDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomPriceDetails();

            if (data.hasOwnProperty('benefits')) {
                obj['benefits'] = ApiClient.convertToType(data['benefits'], ['String']);
            }
            if (data.hasOwnProperty('markups')) {
                obj['markups'] = ApiClient.convertToType(data['markups'], [Markup]);
            }
            if (data.hasOwnProperty('pricesheet')) {
                obj['pricesheet'] = ApiClient.convertToType(data['pricesheet'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomPriceDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomPriceDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['benefits'])) {
            throw new Error("Expected the field `benefits` to be an array in the JSON data but got " + data['benefits']);
        }
        if (data['markups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['markups'])) {
                throw new Error("Expected the field `markups` to be an array in the JSON data but got " + data['markups']);
            }
            // validate the optional field `markups` (array)
            for (const item of data['markups']) {
                Markup.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['pricesheet'] && !(typeof data['pricesheet'] === 'string' || data['pricesheet'] instanceof String)) {
            throw new Error("Expected the field `pricesheet` to be a primitive type in the JSON string but got " + data['pricesheet']);
        }

        return true;
    }


}



/**
 * Array of benefits.
 * @member {Array.<module:model/CustomPriceDetails.BenefitsEnum>} benefits
 */
CustomPriceDetails.prototype['benefits'] = undefined;

/**
 * List of markups.
 * @member {Array.<module:model/Markup>} markups
 */
CustomPriceDetails.prototype['markups'] = undefined;

/**
 * @member {String} pricesheet
 */
CustomPriceDetails.prototype['pricesheet'] = undefined;





/**
 * Allowed values for the <code>benefits</code> property.
 * @enum {String}
 * @readonly
 */
CustomPriceDetails['BenefitsEnum'] = {

    /**
     * value: "None"
     * @const
     */
    "None": "None",

    /**
     * value: "All"
     * @const
     */
    "All": "All",

    /**
     * value: "AHUB"
     * @const
     */
    "AHUB": "AHUB",

    /**
     * value: "Reservations"
     * @const
     */
    "Reservations": "Reservations"
};



export default CustomPriceDetails;

