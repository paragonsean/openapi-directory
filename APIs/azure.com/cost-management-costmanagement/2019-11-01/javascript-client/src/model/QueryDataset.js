/**
 * CostManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-11-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import QueryAggregation from './QueryAggregation';
import QueryDatasetConfiguration from './QueryDatasetConfiguration';
import QueryFilter from './QueryFilter';
import QueryGrouping from './QueryGrouping';

/**
 * The QueryDataset model module.
 * @module model/QueryDataset
 * @version 2019-11-01
 */
class QueryDataset {
    /**
     * Constructs a new <code>QueryDataset</code>.
     * The definition of data present in the query.
     * @alias module:model/QueryDataset
     */
    constructor() { 
        
        QueryDataset.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>QueryDataset</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QueryDataset} obj Optional instance to populate.
     * @return {module:model/QueryDataset} The populated <code>QueryDataset</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QueryDataset();

            if (data.hasOwnProperty('aggregation')) {
                obj['aggregation'] = ApiClient.convertToType(data['aggregation'], {'String': QueryAggregation});
            }
            if (data.hasOwnProperty('configuration')) {
                obj['configuration'] = QueryDatasetConfiguration.constructFromObject(data['configuration']);
            }
            if (data.hasOwnProperty('filter')) {
                obj['filter'] = QueryFilter.constructFromObject(data['filter']);
            }
            if (data.hasOwnProperty('granularity')) {
                obj['granularity'] = ApiClient.convertToType(data['granularity'], 'String');
            }
            if (data.hasOwnProperty('grouping')) {
                obj['grouping'] = ApiClient.convertToType(data['grouping'], [QueryGrouping]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QueryDataset</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QueryDataset</code>.
     */
    static validateJSON(data) {
        // validate the optional field `configuration`
        if (data['configuration']) { // data not null
          QueryDatasetConfiguration.validateJSON(data['configuration']);
        }
        // validate the optional field `filter`
        if (data['filter']) { // data not null
          QueryFilter.validateJSON(data['filter']);
        }
        // ensure the json data is a string
        if (data['granularity'] && !(typeof data['granularity'] === 'string' || data['granularity'] instanceof String)) {
            throw new Error("Expected the field `granularity` to be a primitive type in the JSON string but got " + data['granularity']);
        }
        if (data['grouping']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['grouping'])) {
                throw new Error("Expected the field `grouping` to be an array in the JSON data but got " + data['grouping']);
            }
            // validate the optional field `grouping` (array)
            for (const item of data['grouping']) {
                QueryGrouping.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Dictionary of aggregation expression to use in the query. The key of each item in the dictionary is the alias for the aggregated column. Query can have up to 2 aggregation clauses.
 * @member {Object.<String, module:model/QueryAggregation>} aggregation
 */
QueryDataset.prototype['aggregation'] = undefined;

/**
 * @member {module:model/QueryDatasetConfiguration} configuration
 */
QueryDataset.prototype['configuration'] = undefined;

/**
 * @member {module:model/QueryFilter} filter
 */
QueryDataset.prototype['filter'] = undefined;

/**
 * The granularity of rows in the query.
 * @member {module:model/QueryDataset.GranularityEnum} granularity
 */
QueryDataset.prototype['granularity'] = undefined;

/**
 * Array of group by expression to use in the query. Query can have up to 2 group by clauses.
 * @member {Array.<module:model/QueryGrouping>} grouping
 */
QueryDataset.prototype['grouping'] = undefined;





/**
 * Allowed values for the <code>granularity</code> property.
 * @enum {String}
 * @readonly
 */
QueryDataset['GranularityEnum'] = {

    /**
     * value: "Daily"
     * @const
     */
    "Daily": "Daily"
};



export default QueryDataset;

