/**
 * CostManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-09-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExportRecurrencePeriod from './ExportRecurrencePeriod';

/**
 * The ExportSchedule model module.
 * @module model/ExportSchedule
 * @version 2019-09-01
 */
class ExportSchedule {
    /**
     * Constructs a new <code>ExportSchedule</code>.
     * The schedule associated with a export.
     * @alias module:model/ExportSchedule
     * @param recurrence {module:model/ExportSchedule.RecurrenceEnum} The schedule recurrence.
     */
    constructor(recurrence) { 
        
        ExportSchedule.initialize(this, recurrence);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, recurrence) { 
        obj['recurrence'] = recurrence;
    }

    /**
     * Constructs a <code>ExportSchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExportSchedule} obj Optional instance to populate.
     * @return {module:model/ExportSchedule} The populated <code>ExportSchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExportSchedule();

            if (data.hasOwnProperty('recurrence')) {
                obj['recurrence'] = ApiClient.convertToType(data['recurrence'], 'String');
            }
            if (data.hasOwnProperty('recurrencePeriod')) {
                obj['recurrencePeriod'] = ExportRecurrencePeriod.constructFromObject(data['recurrencePeriod']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExportSchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExportSchedule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExportSchedule.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['recurrence'] && !(typeof data['recurrence'] === 'string' || data['recurrence'] instanceof String)) {
            throw new Error("Expected the field `recurrence` to be a primitive type in the JSON string but got " + data['recurrence']);
        }
        // validate the optional field `recurrencePeriod`
        if (data['recurrencePeriod']) { // data not null
          ExportRecurrencePeriod.validateJSON(data['recurrencePeriod']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

ExportSchedule.RequiredProperties = ["recurrence"];

/**
 * The schedule recurrence.
 * @member {module:model/ExportSchedule.RecurrenceEnum} recurrence
 */
ExportSchedule.prototype['recurrence'] = undefined;

/**
 * @member {module:model/ExportRecurrencePeriod} recurrencePeriod
 */
ExportSchedule.prototype['recurrencePeriod'] = undefined;

/**
 * The status of the schedule. Whether active or not. If inactive, the export's scheduled execution is paused.
 * @member {module:model/ExportSchedule.StatusEnum} status
 */
ExportSchedule.prototype['status'] = undefined;





/**
 * Allowed values for the <code>recurrence</code> property.
 * @enum {String}
 * @readonly
 */
ExportSchedule['RecurrenceEnum'] = {

    /**
     * value: "Daily"
     * @const
     */
    "Daily": "Daily",

    /**
     * value: "Weekly"
     * @const
     */
    "Weekly": "Weekly",

    /**
     * value: "Monthly"
     * @const
     */
    "Monthly": "Monthly",

    /**
     * value: "Annually"
     * @const
     */
    "Annually": "Annually"
};


/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ExportSchedule['StatusEnum'] = {

    /**
     * value: "Active"
     * @const
     */
    "Active": "Active",

    /**
     * value: "Inactive"
     * @const
     */
    "Inactive": "Inactive"
};



export default ExportSchedule;

