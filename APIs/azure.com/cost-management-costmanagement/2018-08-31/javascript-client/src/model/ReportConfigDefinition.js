/**
 * CostManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-08-31
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReportConfigDataset from './ReportConfigDataset';
import ReportConfigTimePeriod from './ReportConfigTimePeriod';

/**
 * The ReportConfigDefinition model module.
 * @module model/ReportConfigDefinition
 * @version 2018-08-31
 */
class ReportConfigDefinition {
    /**
     * Constructs a new <code>ReportConfigDefinition</code>.
     * The definition of a report config.
     * @alias module:model/ReportConfigDefinition
     * @param timeframe {module:model/ReportConfigDefinition.TimeframeEnum} The time frame for pulling data for the report. If custom, then a specific time period must be provided.
     * @param type {module:model/ReportConfigDefinition.TypeEnum} The type of the report.
     */
    constructor(timeframe, type) { 
        
        ReportConfigDefinition.initialize(this, timeframe, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, timeframe, type) { 
        obj['timeframe'] = timeframe;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ReportConfigDefinition</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReportConfigDefinition} obj Optional instance to populate.
     * @return {module:model/ReportConfigDefinition} The populated <code>ReportConfigDefinition</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReportConfigDefinition();

            if (data.hasOwnProperty('dataset')) {
                obj['dataset'] = ReportConfigDataset.constructFromObject(data['dataset']);
            }
            if (data.hasOwnProperty('timePeriod')) {
                obj['timePeriod'] = ReportConfigTimePeriod.constructFromObject(data['timePeriod']);
            }
            if (data.hasOwnProperty('timeframe')) {
                obj['timeframe'] = ApiClient.convertToType(data['timeframe'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReportConfigDefinition</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReportConfigDefinition</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReportConfigDefinition.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `dataset`
        if (data['dataset']) { // data not null
          ReportConfigDataset.validateJSON(data['dataset']);
        }
        // validate the optional field `timePeriod`
        if (data['timePeriod']) { // data not null
          ReportConfigTimePeriod.validateJSON(data['timePeriod']);
        }
        // ensure the json data is a string
        if (data['timeframe'] && !(typeof data['timeframe'] === 'string' || data['timeframe'] instanceof String)) {
            throw new Error("Expected the field `timeframe` to be a primitive type in the JSON string but got " + data['timeframe']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ReportConfigDefinition.RequiredProperties = ["timeframe", "type"];

/**
 * @member {module:model/ReportConfigDataset} dataset
 */
ReportConfigDefinition.prototype['dataset'] = undefined;

/**
 * @member {module:model/ReportConfigTimePeriod} timePeriod
 */
ReportConfigDefinition.prototype['timePeriod'] = undefined;

/**
 * The time frame for pulling data for the report. If custom, then a specific time period must be provided.
 * @member {module:model/ReportConfigDefinition.TimeframeEnum} timeframe
 */
ReportConfigDefinition.prototype['timeframe'] = undefined;

/**
 * The type of the report.
 * @member {module:model/ReportConfigDefinition.TypeEnum} type
 */
ReportConfigDefinition.prototype['type'] = undefined;





/**
 * Allowed values for the <code>timeframe</code> property.
 * @enum {String}
 * @readonly
 */
ReportConfigDefinition['TimeframeEnum'] = {

    /**
     * value: "WeekToDate"
     * @const
     */
    "WeekToDate": "WeekToDate",

    /**
     * value: "MonthToDate"
     * @const
     */
    "MonthToDate": "MonthToDate",

    /**
     * value: "YearToDate"
     * @const
     */
    "YearToDate": "YearToDate",

    /**
     * value: "Custom"
     * @const
     */
    "Custom": "Custom"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ReportConfigDefinition['TypeEnum'] = {

    /**
     * value: "Usage"
     * @const
     */
    "Usage": "Usage"
};



export default ReportConfigDefinition;

