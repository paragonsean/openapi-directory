/**
 * NetworkManagementClient
 * The Microsoft Azure Network management API provides a RESTful set of web services that interact with Microsoft Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Microsoft Azure Networks service.
 *
 * The version of the OpenAPI document: 2019-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NatGatewayPropertiesFormatPublicIpAddressesInner from './NatGatewayPropertiesFormatPublicIpAddressesInner';

/**
 * The NatGatewayPropertiesFormat model module.
 * @module model/NatGatewayPropertiesFormat
 * @version 2019-07-01
 */
class NatGatewayPropertiesFormat {
    /**
     * Constructs a new <code>NatGatewayPropertiesFormat</code>.
     * Nat Gateway properties.
     * @alias module:model/NatGatewayPropertiesFormat
     */
    constructor() { 
        
        NatGatewayPropertiesFormat.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>NatGatewayPropertiesFormat</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NatGatewayPropertiesFormat} obj Optional instance to populate.
     * @return {module:model/NatGatewayPropertiesFormat} The populated <code>NatGatewayPropertiesFormat</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NatGatewayPropertiesFormat();

            if (data.hasOwnProperty('idleTimeoutInMinutes')) {
                obj['idleTimeoutInMinutes'] = ApiClient.convertToType(data['idleTimeoutInMinutes'], 'Number');
            }
            if (data.hasOwnProperty('provisioningState')) {
                obj['provisioningState'] = ApiClient.convertToType(data['provisioningState'], 'String');
            }
            if (data.hasOwnProperty('publicIpAddresses')) {
                obj['publicIpAddresses'] = ApiClient.convertToType(data['publicIpAddresses'], [NatGatewayPropertiesFormatPublicIpAddressesInner]);
            }
            if (data.hasOwnProperty('publicIpPrefixes')) {
                obj['publicIpPrefixes'] = ApiClient.convertToType(data['publicIpPrefixes'], [NatGatewayPropertiesFormatPublicIpAddressesInner]);
            }
            if (data.hasOwnProperty('resourceGuid')) {
                obj['resourceGuid'] = ApiClient.convertToType(data['resourceGuid'], 'String');
            }
            if (data.hasOwnProperty('subnets')) {
                obj['subnets'] = ApiClient.convertToType(data['subnets'], [NatGatewayPropertiesFormatPublicIpAddressesInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NatGatewayPropertiesFormat</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NatGatewayPropertiesFormat</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['provisioningState'] && !(typeof data['provisioningState'] === 'string' || data['provisioningState'] instanceof String)) {
            throw new Error("Expected the field `provisioningState` to be a primitive type in the JSON string but got " + data['provisioningState']);
        }
        if (data['publicIpAddresses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['publicIpAddresses'])) {
                throw new Error("Expected the field `publicIpAddresses` to be an array in the JSON data but got " + data['publicIpAddresses']);
            }
            // validate the optional field `publicIpAddresses` (array)
            for (const item of data['publicIpAddresses']) {
                NatGatewayPropertiesFormatPublicIpAddressesInner.validateJSON(item);
            };
        }
        if (data['publicIpPrefixes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['publicIpPrefixes'])) {
                throw new Error("Expected the field `publicIpPrefixes` to be an array in the JSON data but got " + data['publicIpPrefixes']);
            }
            // validate the optional field `publicIpPrefixes` (array)
            for (const item of data['publicIpPrefixes']) {
                NatGatewayPropertiesFormatPublicIpAddressesInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['resourceGuid'] && !(typeof data['resourceGuid'] === 'string' || data['resourceGuid'] instanceof String)) {
            throw new Error("Expected the field `resourceGuid` to be a primitive type in the JSON string but got " + data['resourceGuid']);
        }
        if (data['subnets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['subnets'])) {
                throw new Error("Expected the field `subnets` to be an array in the JSON data but got " + data['subnets']);
            }
            // validate the optional field `subnets` (array)
            for (const item of data['subnets']) {
                NatGatewayPropertiesFormatPublicIpAddressesInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The idle timeout of the nat gateway.
 * @member {Number} idleTimeoutInMinutes
 */
NatGatewayPropertiesFormat.prototype['idleTimeoutInMinutes'] = undefined;

/**
 * The current provisioning state.
 * @member {module:model/NatGatewayPropertiesFormat.ProvisioningStateEnum} provisioningState
 */
NatGatewayPropertiesFormat.prototype['provisioningState'] = undefined;

/**
 * An array of public ip addresses associated with the nat gateway resource.
 * @member {Array.<module:model/NatGatewayPropertiesFormatPublicIpAddressesInner>} publicIpAddresses
 */
NatGatewayPropertiesFormat.prototype['publicIpAddresses'] = undefined;

/**
 * An array of public ip prefixes associated with the nat gateway resource.
 * @member {Array.<module:model/NatGatewayPropertiesFormatPublicIpAddressesInner>} publicIpPrefixes
 */
NatGatewayPropertiesFormat.prototype['publicIpPrefixes'] = undefined;

/**
 * The resource GUID property of the NAT gateway resource.
 * @member {String} resourceGuid
 */
NatGatewayPropertiesFormat.prototype['resourceGuid'] = undefined;

/**
 * An array of references to the subnets using this nat gateway resource.
 * @member {Array.<module:model/NatGatewayPropertiesFormatPublicIpAddressesInner>} subnets
 */
NatGatewayPropertiesFormat.prototype['subnets'] = undefined;





/**
 * Allowed values for the <code>provisioningState</code> property.
 * @enum {String}
 * @readonly
 */
NatGatewayPropertiesFormat['ProvisioningStateEnum'] = {

    /**
     * value: "Succeeded"
     * @const
     */
    "Succeeded": "Succeeded",

    /**
     * value: "Updating"
     * @const
     */
    "Updating": "Updating",

    /**
     * value: "Deleting"
     * @const
     */
    "Deleting": "Deleting",

    /**
     * value: "Failed"
     * @const
     */
    "Failed": "Failed"
};



export default NatGatewayPropertiesFormat;

