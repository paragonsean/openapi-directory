/**
 * ApplicationInsightsManagementClient
 * Azure Application Insights client for web test based alerting.
 *
 * The version of the OpenAPI document: 2015-05-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WebTest from './WebTest';

/**
 * The WebTestListResult model module.
 * @module model/WebTestListResult
 * @version 2015-05-01
 */
class WebTestListResult {
    /**
     * Constructs a new <code>WebTestListResult</code>.
     * A list of 0 or more Application Insights web test definitions.
     * @alias module:model/WebTestListResult
     * @param value {Array.<module:model/WebTest>} Set of Application Insights web test definitions.
     */
    constructor(value) { 
        
        WebTestListResult.initialize(this, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, value) { 
        obj['value'] = value;
    }

    /**
     * Constructs a <code>WebTestListResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WebTestListResult} obj Optional instance to populate.
     * @return {module:model/WebTestListResult} The populated <code>WebTestListResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebTestListResult();

            if (data.hasOwnProperty('nextLink')) {
                obj['nextLink'] = ApiClient.convertToType(data['nextLink'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], [WebTest]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebTestListResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebTestListResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WebTestListResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['nextLink'] && !(typeof data['nextLink'] === 'string' || data['nextLink'] instanceof String)) {
            throw new Error("Expected the field `nextLink` to be a primitive type in the JSON string but got " + data['nextLink']);
        }
        if (data['value']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['value'])) {
                throw new Error("Expected the field `value` to be an array in the JSON data but got " + data['value']);
            }
            // validate the optional field `value` (array)
            for (const item of data['value']) {
                WebTest.validateJSON(item);
            };
        }

        return true;
    }


}

WebTestListResult.RequiredProperties = ["value"];

/**
 * The link to get the next part of the returned list of web tests, should the return set be too large for a single request. May be null.
 * @member {String} nextLink
 */
WebTestListResult.prototype['nextLink'] = undefined;

/**
 * Set of Application Insights web test definitions.
 * @member {Array.<module:model/WebTest>} value
 */
WebTestListResult.prototype['value'] = undefined;






export default WebTestListResult;

