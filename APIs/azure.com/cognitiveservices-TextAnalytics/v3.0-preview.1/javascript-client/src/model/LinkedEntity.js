/**
 * Text Analytics Client
 * The Text Analytics API is a suite of text analytics web services built with best-in-class Microsoft machine learning algorithms. The API can be used to analyze unstructured text for tasks such as sentiment analysis, key phrase extraction and language detection. No training data is needed to use this API; just bring your text data. This API uses advanced natural language processing techniques to deliver best in class predictions. Further documentation can be found in https://docs.microsoft.com/en-us/azure/cognitive-services/text-analytics/overview
 *
 * The version of the OpenAPI document: v3.0-preview.1
 * Contact: mlapi@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Match from './Match';

/**
 * The LinkedEntity model module.
 * @module model/LinkedEntity
 * @version v3.0-preview.1
 */
class LinkedEntity {
    /**
     * Constructs a new <code>LinkedEntity</code>.
     * @alias module:model/LinkedEntity
     * @param dataSource {String} Data source used to extract entity linking, such as Wiki/Bing etc.
     * @param language {String} Language used in the data source.
     * @param matches {Array.<module:model/Match>} List of instances this entity appears in the text.
     * @param name {String} Entity Linking formal name.
     * @param url {String} URL for the entity's page from the data source.
     */
    constructor(dataSource, language, matches, name, url) { 
        
        LinkedEntity.initialize(this, dataSource, language, matches, name, url);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dataSource, language, matches, name, url) { 
        obj['dataSource'] = dataSource;
        obj['language'] = language;
        obj['matches'] = matches;
        obj['name'] = name;
        obj['url'] = url;
    }

    /**
     * Constructs a <code>LinkedEntity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LinkedEntity} obj Optional instance to populate.
     * @return {module:model/LinkedEntity} The populated <code>LinkedEntity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LinkedEntity();

            if (data.hasOwnProperty('dataSource')) {
                obj['dataSource'] = ApiClient.convertToType(data['dataSource'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], 'String');
            }
            if (data.hasOwnProperty('matches')) {
                obj['matches'] = ApiClient.convertToType(data['matches'], [Match]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LinkedEntity</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LinkedEntity</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LinkedEntity.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['dataSource'] && !(typeof data['dataSource'] === 'string' || data['dataSource'] instanceof String)) {
            throw new Error("Expected the field `dataSource` to be a primitive type in the JSON string but got " + data['dataSource']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['language'] && !(typeof data['language'] === 'string' || data['language'] instanceof String)) {
            throw new Error("Expected the field `language` to be a primitive type in the JSON string but got " + data['language']);
        }
        if (data['matches']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['matches'])) {
                throw new Error("Expected the field `matches` to be an array in the JSON data but got " + data['matches']);
            }
            // validate the optional field `matches` (array)
            for (const item of data['matches']) {
                Match.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }

        return true;
    }


}

LinkedEntity.RequiredProperties = ["dataSource", "language", "matches", "name", "url"];

/**
 * Data source used to extract entity linking, such as Wiki/Bing etc.
 * @member {String} dataSource
 */
LinkedEntity.prototype['dataSource'] = undefined;

/**
 * Unique identifier of the recognized entity from the data source.
 * @member {String} id
 */
LinkedEntity.prototype['id'] = undefined;

/**
 * Language used in the data source.
 * @member {String} language
 */
LinkedEntity.prototype['language'] = undefined;

/**
 * List of instances this entity appears in the text.
 * @member {Array.<module:model/Match>} matches
 */
LinkedEntity.prototype['matches'] = undefined;

/**
 * Entity Linking formal name.
 * @member {String} name
 */
LinkedEntity.prototype['name'] = undefined;

/**
 * URL for the entity's page from the data source.
 * @member {String} url
 */
LinkedEntity.prototype['url'] = undefined;






export default LinkedEntity;

