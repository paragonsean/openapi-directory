/**
 * Text Analytics Client
 * The Text Analytics API is a suite of text analytics web services built with best-in-class Microsoft machine learning algorithms. The API can be used to analyze unstructured text for tasks such as sentiment analysis, key phrase extraction and language detection. No training data is needed to use this API; just bring your text data. This API uses advanced natural language processing techniques to deliver best in class predictions. Further documentation can be found in https://docs.microsoft.com/en-us/azure/cognitive-services/text-analytics/overview
 *
 * The version of the OpenAPI document: v3.0-preview.1
 * Contact: mlapi@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DocumentEntities from './DocumentEntities';
import DocumentError from './DocumentError';
import RequestStatistics from './RequestStatistics';

/**
 * The EntitiesResult model module.
 * @module model/EntitiesResult
 * @version v3.0-preview.1
 */
class EntitiesResult {
    /**
     * Constructs a new <code>EntitiesResult</code>.
     * @alias module:model/EntitiesResult
     * @param documents {Array.<module:model/DocumentEntities>} Response by document
     * @param errors {Array.<module:model/DocumentError>} Errors by document id.
     * @param modelVersion {String} This field indicates which model is used for scoring.
     */
    constructor(documents, errors, modelVersion) { 
        
        EntitiesResult.initialize(this, documents, errors, modelVersion);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, documents, errors, modelVersion) { 
        obj['documents'] = documents;
        obj['errors'] = errors;
        obj['modelVersion'] = modelVersion;
    }

    /**
     * Constructs a <code>EntitiesResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EntitiesResult} obj Optional instance to populate.
     * @return {module:model/EntitiesResult} The populated <code>EntitiesResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EntitiesResult();

            if (data.hasOwnProperty('documents')) {
                obj['documents'] = ApiClient.convertToType(data['documents'], [DocumentEntities]);
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [DocumentError]);
            }
            if (data.hasOwnProperty('modelVersion')) {
                obj['modelVersion'] = ApiClient.convertToType(data['modelVersion'], 'String');
            }
            if (data.hasOwnProperty('statistics')) {
                obj['statistics'] = RequestStatistics.constructFromObject(data['statistics']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EntitiesResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EntitiesResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EntitiesResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['documents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['documents'])) {
                throw new Error("Expected the field `documents` to be an array in the JSON data but got " + data['documents']);
            }
            // validate the optional field `documents` (array)
            for (const item of data['documents']) {
                DocumentEntities.validateJSON(item);
            };
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                DocumentError.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['modelVersion'] && !(typeof data['modelVersion'] === 'string' || data['modelVersion'] instanceof String)) {
            throw new Error("Expected the field `modelVersion` to be a primitive type in the JSON string but got " + data['modelVersion']);
        }
        // validate the optional field `statistics`
        if (data['statistics']) { // data not null
          RequestStatistics.validateJSON(data['statistics']);
        }

        return true;
    }


}

EntitiesResult.RequiredProperties = ["documents", "errors", "modelVersion"];

/**
 * Response by document
 * @member {Array.<module:model/DocumentEntities>} documents
 */
EntitiesResult.prototype['documents'] = undefined;

/**
 * Errors by document id.
 * @member {Array.<module:model/DocumentError>} errors
 */
EntitiesResult.prototype['errors'] = undefined;

/**
 * This field indicates which model is used for scoring.
 * @member {String} modelVersion
 */
EntitiesResult.prototype['modelVersion'] = undefined;

/**
 * @member {module:model/RequestStatistics} statistics
 */
EntitiesResult.prototype['statistics'] = undefined;






export default EntitiesResult;

