/**
 * Text Analytics Client
 * The Text Analytics API is a suite of text analytics web services built with best-in-class Microsoft machine learning algorithms. The API can be used to analyze unstructured text for tasks such as sentiment analysis, key phrase extraction and language detection. No training data is needed to use this API; just bring your text data. This API uses advanced natural language processing techniques to deliver best in class predictions. Further documentation can be found in https://docs.microsoft.com/en-us/azure/cognitive-services/text-analytics/overview
 *
 * The version of the OpenAPI document: v3.0-preview.1
 * Contact: mlapi@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DocumentStatistics from './DocumentStatistics';
import SentenceSentiment from './SentenceSentiment';
import SentimentConfidenceScorePerLabel from './SentimentConfidenceScorePerLabel';

/**
 * The DocumentSentiment model module.
 * @module model/DocumentSentiment
 * @version v3.0-preview.1
 */
class DocumentSentiment {
    /**
     * Constructs a new <code>DocumentSentiment</code>.
     * @alias module:model/DocumentSentiment
     * @param documentScores {module:model/SentimentConfidenceScorePerLabel} 
     * @param id {String} Unique, non-empty document identifier.
     * @param sentences {Array.<module:model/SentenceSentiment>} Sentence level sentiment analysis.
     * @param sentiment {module:model/DocumentSentiment.SentimentEnum} Predicted sentiment for document (Negative, Neutral, Positive, or Mixed).
     */
    constructor(documentScores, id, sentences, sentiment) { 
        
        DocumentSentiment.initialize(this, documentScores, id, sentences, sentiment);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, documentScores, id, sentences, sentiment) { 
        obj['documentScores'] = documentScores;
        obj['id'] = id;
        obj['sentences'] = sentences;
        obj['sentiment'] = sentiment;
    }

    /**
     * Constructs a <code>DocumentSentiment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DocumentSentiment} obj Optional instance to populate.
     * @return {module:model/DocumentSentiment} The populated <code>DocumentSentiment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DocumentSentiment();

            if (data.hasOwnProperty('documentScores')) {
                obj['documentScores'] = SentimentConfidenceScorePerLabel.constructFromObject(data['documentScores']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('sentences')) {
                obj['sentences'] = ApiClient.convertToType(data['sentences'], [SentenceSentiment]);
            }
            if (data.hasOwnProperty('sentiment')) {
                obj['sentiment'] = ApiClient.convertToType(data['sentiment'], 'String');
            }
            if (data.hasOwnProperty('statistics')) {
                obj['statistics'] = DocumentStatistics.constructFromObject(data['statistics']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DocumentSentiment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DocumentSentiment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DocumentSentiment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `documentScores`
        if (data['documentScores']) { // data not null
          SentimentConfidenceScorePerLabel.validateJSON(data['documentScores']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        if (data['sentences']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sentences'])) {
                throw new Error("Expected the field `sentences` to be an array in the JSON data but got " + data['sentences']);
            }
            // validate the optional field `sentences` (array)
            for (const item of data['sentences']) {
                SentenceSentiment.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['sentiment'] && !(typeof data['sentiment'] === 'string' || data['sentiment'] instanceof String)) {
            throw new Error("Expected the field `sentiment` to be a primitive type in the JSON string but got " + data['sentiment']);
        }
        // validate the optional field `statistics`
        if (data['statistics']) { // data not null
          DocumentStatistics.validateJSON(data['statistics']);
        }

        return true;
    }


}

DocumentSentiment.RequiredProperties = ["documentScores", "id", "sentences", "sentiment"];

/**
 * @member {module:model/SentimentConfidenceScorePerLabel} documentScores
 */
DocumentSentiment.prototype['documentScores'] = undefined;

/**
 * Unique, non-empty document identifier.
 * @member {String} id
 */
DocumentSentiment.prototype['id'] = undefined;

/**
 * Sentence level sentiment analysis.
 * @member {Array.<module:model/SentenceSentiment>} sentences
 */
DocumentSentiment.prototype['sentences'] = undefined;

/**
 * Predicted sentiment for document (Negative, Neutral, Positive, or Mixed).
 * @member {module:model/DocumentSentiment.SentimentEnum} sentiment
 */
DocumentSentiment.prototype['sentiment'] = undefined;

/**
 * @member {module:model/DocumentStatistics} statistics
 */
DocumentSentiment.prototype['statistics'] = undefined;





/**
 * Allowed values for the <code>sentiment</code> property.
 * @enum {String}
 * @readonly
 */
DocumentSentiment['SentimentEnum'] = {

    /**
     * value: "positive"
     * @const
     */
    "positive": "positive",

    /**
     * value: "neutral"
     * @const
     */
    "neutral": "neutral",

    /**
     * value: "negative"
     * @const
     */
    "negative": "negative",

    /**
     * value: "mixed"
     * @const
     */
    "mixed": "mixed"
};



export default DocumentSentiment;

