/**
 * MySQLManagementClient
 * The Microsoft Azure management API provides create, read, update, and delete functionality for Azure MySQL resources including servers, databases, firewall rules, VNET rules, log files and configurations with new business model.
 *
 * The version of the OpenAPI document: 2017-12-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ServerAdministratorProperties model module.
 * @module model/ServerAdministratorProperties
 * @version 2017-12-01
 */
class ServerAdministratorProperties {
    /**
     * Constructs a new <code>ServerAdministratorProperties</code>.
     * The properties of an server Administrator.
     * @alias module:model/ServerAdministratorProperties
     * @param administratorType {module:model/ServerAdministratorProperties.AdministratorTypeEnum} The type of administrator.
     * @param login {String} The server administrator login value.
     * @param sid {String} The server administrator Sid (Secure ID).
     * @param tenantId {String} The server Active Directory Administrator tenant id.
     */
    constructor(administratorType, login, sid, tenantId) { 
        
        ServerAdministratorProperties.initialize(this, administratorType, login, sid, tenantId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, administratorType, login, sid, tenantId) { 
        obj['administratorType'] = administratorType;
        obj['login'] = login;
        obj['sid'] = sid;
        obj['tenantId'] = tenantId;
    }

    /**
     * Constructs a <code>ServerAdministratorProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServerAdministratorProperties} obj Optional instance to populate.
     * @return {module:model/ServerAdministratorProperties} The populated <code>ServerAdministratorProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServerAdministratorProperties();

            if (data.hasOwnProperty('administratorType')) {
                obj['administratorType'] = ApiClient.convertToType(data['administratorType'], 'String');
            }
            if (data.hasOwnProperty('login')) {
                obj['login'] = ApiClient.convertToType(data['login'], 'String');
            }
            if (data.hasOwnProperty('sid')) {
                obj['sid'] = ApiClient.convertToType(data['sid'], 'String');
            }
            if (data.hasOwnProperty('tenantId')) {
                obj['tenantId'] = ApiClient.convertToType(data['tenantId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServerAdministratorProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServerAdministratorProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ServerAdministratorProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['administratorType'] && !(typeof data['administratorType'] === 'string' || data['administratorType'] instanceof String)) {
            throw new Error("Expected the field `administratorType` to be a primitive type in the JSON string but got " + data['administratorType']);
        }
        // ensure the json data is a string
        if (data['login'] && !(typeof data['login'] === 'string' || data['login'] instanceof String)) {
            throw new Error("Expected the field `login` to be a primitive type in the JSON string but got " + data['login']);
        }
        // ensure the json data is a string
        if (data['sid'] && !(typeof data['sid'] === 'string' || data['sid'] instanceof String)) {
            throw new Error("Expected the field `sid` to be a primitive type in the JSON string but got " + data['sid']);
        }
        // ensure the json data is a string
        if (data['tenantId'] && !(typeof data['tenantId'] === 'string' || data['tenantId'] instanceof String)) {
            throw new Error("Expected the field `tenantId` to be a primitive type in the JSON string but got " + data['tenantId']);
        }

        return true;
    }


}

ServerAdministratorProperties.RequiredProperties = ["administratorType", "login", "sid", "tenantId"];

/**
 * The type of administrator.
 * @member {module:model/ServerAdministratorProperties.AdministratorTypeEnum} administratorType
 */
ServerAdministratorProperties.prototype['administratorType'] = undefined;

/**
 * The server administrator login value.
 * @member {String} login
 */
ServerAdministratorProperties.prototype['login'] = undefined;

/**
 * The server administrator Sid (Secure ID).
 * @member {String} sid
 */
ServerAdministratorProperties.prototype['sid'] = undefined;

/**
 * The server Active Directory Administrator tenant id.
 * @member {String} tenantId
 */
ServerAdministratorProperties.prototype['tenantId'] = undefined;





/**
 * Allowed values for the <code>administratorType</code> property.
 * @enum {String}
 * @readonly
 */
ServerAdministratorProperties['AdministratorTypeEnum'] = {

    /**
     * value: "ActiveDirectory"
     * @const
     */
    "ActiveDirectory": "ActiveDirectory"
};



export default ServerAdministratorProperties;

