/**
 * RedisManagementClient
 * REST API for Azure Redis Cache Service.
 *
 * The version of the OpenAPI document: 2017-10-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RedisCreateProperties from './RedisCreateProperties';

/**
 * The RedisCreateParameters model module.
 * @module model/RedisCreateParameters
 * @version 2017-10-01
 */
class RedisCreateParameters {
    /**
     * Constructs a new <code>RedisCreateParameters</code>.
     * Parameters supplied to the Create Redis operation.
     * @alias module:model/RedisCreateParameters
     * @param location {String} The geo-location where the resource lives
     * @param properties {module:model/RedisCreateProperties} 
     */
    constructor(location, properties) { 
        
        RedisCreateParameters.initialize(this, location, properties);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, location, properties) { 
        obj['location'] = location;
        obj['properties'] = properties;
    }

    /**
     * Constructs a <code>RedisCreateParameters</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RedisCreateParameters} obj Optional instance to populate.
     * @return {module:model/RedisCreateParameters} The populated <code>RedisCreateParameters</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RedisCreateParameters();

            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('properties')) {
                obj['properties'] = RedisCreateProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('zones')) {
                obj['zones'] = ApiClient.convertToType(data['zones'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RedisCreateParameters</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RedisCreateParameters</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RedisCreateParameters.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          RedisCreateProperties.validateJSON(data['properties']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['zones'])) {
            throw new Error("Expected the field `zones` to be an array in the JSON data but got " + data['zones']);
        }

        return true;
    }


}

RedisCreateParameters.RequiredProperties = ["location", "properties"];

/**
 * The geo-location where the resource lives
 * @member {String} location
 */
RedisCreateParameters.prototype['location'] = undefined;

/**
 * @member {module:model/RedisCreateProperties} properties
 */
RedisCreateParameters.prototype['properties'] = undefined;

/**
 * Resource tags.
 * @member {Object.<String, String>} tags
 */
RedisCreateParameters.prototype['tags'] = undefined;

/**
 * A list of availability zones denoting where the resource needs to come from.
 * @member {Array.<String>} zones
 */
RedisCreateParameters.prototype['zones'] = undefined;






export default RedisCreateParameters;

