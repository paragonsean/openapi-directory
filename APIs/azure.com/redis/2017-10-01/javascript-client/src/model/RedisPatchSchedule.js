/**
 * RedisManagementClient
 * REST API for Azure Redis Cache Service.
 *
 * The version of the OpenAPI document: 2017-10-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProxyResource from './ProxyResource';
import ScheduleEntries from './ScheduleEntries';

/**
 * The RedisPatchSchedule model module.
 * @module model/RedisPatchSchedule
 * @version 2017-10-01
 */
class RedisPatchSchedule {
    /**
     * Constructs a new <code>RedisPatchSchedule</code>.
     * Response to put/get patch schedules for Redis cache.
     * @alias module:model/RedisPatchSchedule
     * @implements module:model/ProxyResource
     */
    constructor() { 
        ProxyResource.initialize(this);
        RedisPatchSchedule.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['properties'] = properties;
    }

    /**
     * Constructs a <code>RedisPatchSchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RedisPatchSchedule} obj Optional instance to populate.
     * @return {module:model/RedisPatchSchedule} The populated <code>RedisPatchSchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RedisPatchSchedule();
            ProxyResource.constructFromObject(data, obj);

            if (data.hasOwnProperty('properties')) {
                obj['properties'] = ScheduleEntries.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RedisPatchSchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RedisPatchSchedule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RedisPatchSchedule.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          ScheduleEntries.validateJSON(data['properties']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

RedisPatchSchedule.RequiredProperties = ["properties"];

/**
 * @member {module:model/ScheduleEntries} properties
 */
RedisPatchSchedule.prototype['properties'] = undefined;

/**
 * Resource ID.
 * @member {String} id
 */
RedisPatchSchedule.prototype['id'] = undefined;

/**
 * Resource name.
 * @member {String} name
 */
RedisPatchSchedule.prototype['name'] = undefined;

/**
 * Resource type.
 * @member {String} type
 */
RedisPatchSchedule.prototype['type'] = undefined;


// Implement ProxyResource interface:
/**
 * Resource ID.
 * @member {String} id
 */
ProxyResource.prototype['id'] = undefined;
/**
 * Resource name.
 * @member {String} name
 */
ProxyResource.prototype['name'] = undefined;
/**
 * Resource type.
 * @member {String} type
 */
ProxyResource.prototype['type'] = undefined;




export default RedisPatchSchedule;

