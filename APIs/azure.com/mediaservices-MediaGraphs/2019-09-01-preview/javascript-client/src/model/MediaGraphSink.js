/**
 * Azure Media Services
 * This Swagger was generated by the API Framework.
 *
 * The version of the OpenAPI document: 2019-09-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MediaGraphSink model module.
 * @module model/MediaGraphSink
 * @version 2019-09-01-preview
 */
class MediaGraphSink {
    /**
     * Constructs a new <code>MediaGraphSink</code>.
     * Media Sink
     * @alias module:model/MediaGraphSink
     * @param odataType {String} The discriminator for derived types.
     * @param inputs {Array.<String>} Sink inputs
     * @param name {String} Sink name
     */
    constructor(odataType, inputs, name) { 
        
        MediaGraphSink.initialize(this, odataType, inputs, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, odataType, inputs, name) { 
        obj['@odata.type'] = odataType;
        obj['inputs'] = inputs;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>MediaGraphSink</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MediaGraphSink} obj Optional instance to populate.
     * @return {module:model/MediaGraphSink} The populated <code>MediaGraphSink</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MediaGraphSink();

            if (data.hasOwnProperty('@odata.type')) {
                obj['@odata.type'] = ApiClient.convertToType(data['@odata.type'], 'String');
            }
            if (data.hasOwnProperty('inputs')) {
                obj['inputs'] = ApiClient.convertToType(data['inputs'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MediaGraphSink</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MediaGraphSink</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MediaGraphSink.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['@odata.type'] && !(typeof data['@odata.type'] === 'string' || data['@odata.type'] instanceof String)) {
            throw new Error("Expected the field `@odata.type` to be a primitive type in the JSON string but got " + data['@odata.type']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['inputs'])) {
            throw new Error("Expected the field `inputs` to be an array in the JSON data but got " + data['inputs']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

MediaGraphSink.RequiredProperties = ["@odata.type", "inputs", "name"];

/**
 * The discriminator for derived types.
 * @member {String} @odata.type
 */
MediaGraphSink.prototype['@odata.type'] = undefined;

/**
 * Sink inputs
 * @member {Array.<String>} inputs
 */
MediaGraphSink.prototype['inputs'] = undefined;

/**
 * Sink name
 * @member {String} name
 */
MediaGraphSink.prototype['name'] = undefined;






export default MediaGraphSink;

