/**
 * Microsoft.ResourceHealth
 * The Resource Health Client.
 *
 * The version of the OpenAPI document: 2017-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ImpactedRegion from './ImpactedRegion';

/**
 * The EmergingIssueImpact model module.
 * @module model/EmergingIssueImpact
 * @version 2017-07-01
 */
class EmergingIssueImpact {
    /**
     * Constructs a new <code>EmergingIssueImpact</code>.
     * Object of the emerging issue impact on services and regions.
     * @alias module:model/EmergingIssueImpact
     */
    constructor() { 
        
        EmergingIssueImpact.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EmergingIssueImpact</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmergingIssueImpact} obj Optional instance to populate.
     * @return {module:model/EmergingIssueImpact} The populated <code>EmergingIssueImpact</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmergingIssueImpact();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('regions')) {
                obj['regions'] = ApiClient.convertToType(data['regions'], [ImpactedRegion]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmergingIssueImpact</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmergingIssueImpact</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['regions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['regions'])) {
                throw new Error("Expected the field `regions` to be an array in the JSON data but got " + data['regions']);
            }
            // validate the optional field `regions` (array)
            for (const item of data['regions']) {
                ImpactedRegion.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The impacted service id.
 * @member {String} id
 */
EmergingIssueImpact.prototype['id'] = undefined;

/**
 * The impacted service name.
 * @member {String} name
 */
EmergingIssueImpact.prototype['name'] = undefined;

/**
 * The list of impacted regions for corresponding emerging issues.
 * @member {Array.<module:model/ImpactedRegion>} regions
 */
EmergingIssueImpact.prototype['regions'] = undefined;






export default EmergingIssueImpact;

