/**
 * ApiManagementClient
 * Use these REST APIs for performing operations on API entity and their Operations associated with your Azure API Management deployment.
 *
 * The version of the OpenAPI document: 2016-10-10
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RepresentationContract from './RepresentationContract';

/**
 * The ResultContract model module.
 * @module model/ResultContract
 * @version 2016-10-10
 */
class ResultContract {
    /**
     * Constructs a new <code>ResultContract</code>.
     * Operation response details.
     * @alias module:model/ResultContract
     * @param statusCode {Number} Operation response HTTP status code.
     */
    constructor(statusCode) { 
        
        ResultContract.initialize(this, statusCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, statusCode) { 
        obj['statusCode'] = statusCode;
    }

    /**
     * Constructs a <code>ResultContract</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ResultContract} obj Optional instance to populate.
     * @return {module:model/ResultContract} The populated <code>ResultContract</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ResultContract();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('representations')) {
                obj['representations'] = ApiClient.convertToType(data['representations'], [RepresentationContract]);
            }
            if (data.hasOwnProperty('statusCode')) {
                obj['statusCode'] = ApiClient.convertToType(data['statusCode'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ResultContract</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ResultContract</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ResultContract.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['representations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['representations'])) {
                throw new Error("Expected the field `representations` to be an array in the JSON data but got " + data['representations']);
            }
            // validate the optional field `representations` (array)
            for (const item of data['representations']) {
                RepresentationContract.validateJSON(item);
            };
        }

        return true;
    }


}

ResultContract.RequiredProperties = ["statusCode"];

/**
 * Operation response description.
 * @member {String} description
 */
ResultContract.prototype['description'] = undefined;

/**
 * Collection of operation response representations.
 * @member {Array.<module:model/RepresentationContract>} representations
 */
ResultContract.prototype['representations'] = undefined;

/**
 * Operation response HTTP status code.
 * @member {Number} statusCode
 */
ResultContract.prototype['statusCode'] = undefined;






export default ResultContract;

