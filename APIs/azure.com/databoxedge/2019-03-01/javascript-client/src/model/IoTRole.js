/**
 * DataBoxEdgeManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IoTRoleProperties from './IoTRoleProperties';
import Role from './Role';

/**
 * The IoTRole model module.
 * @module model/IoTRole
 * @version 2019-03-01
 */
class IoTRole {
    /**
     * Constructs a new <code>IoTRole</code>.
     * Compute role.
     * @alias module:model/IoTRole
     * @implements module:model/Role
     * @param kind {module:model/IoTRole.KindEnum} Role type.
     */
    constructor(kind) { 
        Role.initialize(this);
        IoTRole.initialize(this, kind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, kind) { 
        obj['kind'] = kind;
    }

    /**
     * Constructs a <code>IoTRole</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IoTRole} obj Optional instance to populate.
     * @return {module:model/IoTRole} The populated <code>IoTRole</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IoTRole();
            Role.constructFromObject(data, obj);

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('properties')) {
                obj['properties'] = IoTRoleProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IoTRole</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IoTRole</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IoTRole.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          IoTRoleProperties.validateJSON(data['properties']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }

        return true;
    }


}

IoTRole.RequiredProperties = ["kind"];

/**
 * The path ID that uniquely identifies the object.
 * @member {String} id
 */
IoTRole.prototype['id'] = undefined;

/**
 * The object name.
 * @member {String} name
 */
IoTRole.prototype['name'] = undefined;

/**
 * @member {module:model/IoTRoleProperties} properties
 */
IoTRole.prototype['properties'] = undefined;

/**
 * The hierarchical type of the object.
 * @member {String} type
 */
IoTRole.prototype['type'] = undefined;

/**
 * Role type.
 * @member {module:model/IoTRole.KindEnum} kind
 */
IoTRole.prototype['kind'] = undefined;


// Implement Role interface:
/**
 * The path ID that uniquely identifies the object.
 * @member {String} id
 */
Role.prototype['id'] = undefined;
/**
 * The object name.
 * @member {String} name
 */
Role.prototype['name'] = undefined;
/**
 * The hierarchical type of the object.
 * @member {String} type
 */
Role.prototype['type'] = undefined;



/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
IoTRole['KindEnum'] = {

    /**
     * value: "IOT"
     * @const
     */
    "IOT": "IOT",

    /**
     * value: "ASA"
     * @const
     */
    "ASA": "ASA",

    /**
     * value: "Functions"
     * @const
     */
    "Functions": "Functions",

    /**
     * value: "Cognitive"
     * @const
     */
    "Cognitive": "Cognitive"
};



export default IoTRole;

