/**
 * DataLakeAnalyticsCatalogManagementClient
 * Creates an Azure Data Lake Analytics catalog client.
 *
 * The version of the OpenAPI document: 2015-10-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CatalogItemList from './CatalogItemList';
import USqlExternalDataSource from './USqlExternalDataSource';

/**
 * The USqlExternalDataSourceList model module.
 * @module model/USqlExternalDataSourceList
 * @version 2015-10-01-preview
 */
class USqlExternalDataSourceList {
    /**
     * Constructs a new <code>USqlExternalDataSourceList</code>.
     * A Data Lake Analytics catalog U-SQL external datasource item list.
     * @alias module:model/USqlExternalDataSourceList
     * @implements module:model/CatalogItemList
     */
    constructor() { 
        CatalogItemList.initialize(this);
        USqlExternalDataSourceList.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>USqlExternalDataSourceList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/USqlExternalDataSourceList} obj Optional instance to populate.
     * @return {module:model/USqlExternalDataSourceList} The populated <code>USqlExternalDataSourceList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new USqlExternalDataSourceList();
            CatalogItemList.constructFromObject(data, obj);

            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], [USqlExternalDataSource]);
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('nextLink')) {
                obj['nextLink'] = ApiClient.convertToType(data['nextLink'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>USqlExternalDataSourceList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>USqlExternalDataSourceList</code>.
     */
    static validateJSON(data) {
        if (data['value']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['value'])) {
                throw new Error("Expected the field `value` to be an array in the JSON data but got " + data['value']);
            }
            // validate the optional field `value` (array)
            for (const item of data['value']) {
                USqlExternalDataSource.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['nextLink'] && !(typeof data['nextLink'] === 'string' || data['nextLink'] instanceof String)) {
            throw new Error("Expected the field `nextLink` to be a primitive type in the JSON string but got " + data['nextLink']);
        }

        return true;
    }


}



/**
 * the list of external data sources in the database
 * @member {Array.<module:model/USqlExternalDataSource>} value
 */
USqlExternalDataSourceList.prototype['value'] = undefined;

/**
 * the count of items in the list.
 * @member {Number} count
 */
USqlExternalDataSourceList.prototype['count'] = undefined;

/**
 * the link to the next page of results.
 * @member {String} nextLink
 */
USqlExternalDataSourceList.prototype['nextLink'] = undefined;


// Implement CatalogItemList interface:
/**
 * the count of items in the list.
 * @member {Number} count
 */
CatalogItemList.prototype['count'] = undefined;
/**
 * the link to the next page of results.
 * @member {String} nextLink
 */
CatalogItemList.prototype['nextLink'] = undefined;




export default USqlExternalDataSourceList;

