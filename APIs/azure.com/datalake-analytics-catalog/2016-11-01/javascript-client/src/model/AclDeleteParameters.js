/**
 * DataLakeAnalyticsCatalogManagementClient
 * Creates an Azure Data Lake Analytics catalog client.
 *
 * The version of the OpenAPI document: 2016-11-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AclDeleteParameters model module.
 * @module model/AclDeleteParameters
 * @version 2016-11-01
 */
class AclDeleteParameters {
    /**
     * Constructs a new <code>AclDeleteParameters</code>.
     * The parameters used to delete an access control list (ACL) entry.
     * @alias module:model/AclDeleteParameters
     * @param aceType {module:model/AclDeleteParameters.AceTypeEnum} the access control list (ACL) entry type. UserObj and GroupObj denote the owning user and group, respectively.
     * @param principalId {String} the Azure AD object ID of the user or group being specified in the access control list (ACL) entry.
     */
    constructor(aceType, principalId) { 
        
        AclDeleteParameters.initialize(this, aceType, principalId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, aceType, principalId) { 
        obj['aceType'] = aceType;
        obj['principalId'] = principalId;
    }

    /**
     * Constructs a <code>AclDeleteParameters</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AclDeleteParameters} obj Optional instance to populate.
     * @return {module:model/AclDeleteParameters} The populated <code>AclDeleteParameters</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AclDeleteParameters();

            if (data.hasOwnProperty('aceType')) {
                obj['aceType'] = ApiClient.convertToType(data['aceType'], 'String');
            }
            if (data.hasOwnProperty('principalId')) {
                obj['principalId'] = ApiClient.convertToType(data['principalId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AclDeleteParameters</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AclDeleteParameters</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AclDeleteParameters.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['aceType'] && !(typeof data['aceType'] === 'string' || data['aceType'] instanceof String)) {
            throw new Error("Expected the field `aceType` to be a primitive type in the JSON string but got " + data['aceType']);
        }
        // ensure the json data is a string
        if (data['principalId'] && !(typeof data['principalId'] === 'string' || data['principalId'] instanceof String)) {
            throw new Error("Expected the field `principalId` to be a primitive type in the JSON string but got " + data['principalId']);
        }

        return true;
    }


}

AclDeleteParameters.RequiredProperties = ["aceType", "principalId"];

/**
 * the access control list (ACL) entry type. UserObj and GroupObj denote the owning user and group, respectively.
 * @member {module:model/AclDeleteParameters.AceTypeEnum} aceType
 */
AclDeleteParameters.prototype['aceType'] = undefined;

/**
 * the Azure AD object ID of the user or group being specified in the access control list (ACL) entry.
 * @member {String} principalId
 */
AclDeleteParameters.prototype['principalId'] = undefined;





/**
 * Allowed values for the <code>aceType</code> property.
 * @enum {String}
 * @readonly
 */
AclDeleteParameters['AceTypeEnum'] = {

    /**
     * value: "UserObj"
     * @const
     */
    "UserObj": "UserObj",

    /**
     * value: "GroupObj"
     * @const
     */
    "GroupObj": "GroupObj",

    /**
     * value: "Other"
     * @const
     */
    "Other": "Other",

    /**
     * value: "User"
     * @const
     */
    "User": "User",

    /**
     * value: "Group"
     * @const
     */
    "Group": "Group"
};



export default AclDeleteParameters;

