/**
 * LogicManagementClient
 * REST API for Azure Logic Apps.
 *
 * The version of the OpenAPI document: 2016-06-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RecurrenceScheduleOccurrence from './RecurrenceScheduleOccurrence';

/**
 * The RecurrenceSchedule model module.
 * @module model/RecurrenceSchedule
 * @version 2016-06-01
 */
class RecurrenceSchedule {
    /**
     * Constructs a new <code>RecurrenceSchedule</code>.
     * The recurrence schedule.
     * @alias module:model/RecurrenceSchedule
     */
    constructor() { 
        
        RecurrenceSchedule.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RecurrenceSchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecurrenceSchedule} obj Optional instance to populate.
     * @return {module:model/RecurrenceSchedule} The populated <code>RecurrenceSchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecurrenceSchedule();

            if (data.hasOwnProperty('hours')) {
                obj['hours'] = ApiClient.convertToType(data['hours'], ['Number']);
            }
            if (data.hasOwnProperty('minutes')) {
                obj['minutes'] = ApiClient.convertToType(data['minutes'], ['Number']);
            }
            if (data.hasOwnProperty('monthDays')) {
                obj['monthDays'] = ApiClient.convertToType(data['monthDays'], ['Number']);
            }
            if (data.hasOwnProperty('monthlyOccurrences')) {
                obj['monthlyOccurrences'] = ApiClient.convertToType(data['monthlyOccurrences'], [RecurrenceScheduleOccurrence]);
            }
            if (data.hasOwnProperty('weekDays')) {
                obj['weekDays'] = ApiClient.convertToType(data['weekDays'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecurrenceSchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecurrenceSchedule</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['hours'])) {
            throw new Error("Expected the field `hours` to be an array in the JSON data but got " + data['hours']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['minutes'])) {
            throw new Error("Expected the field `minutes` to be an array in the JSON data but got " + data['minutes']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['monthDays'])) {
            throw new Error("Expected the field `monthDays` to be an array in the JSON data but got " + data['monthDays']);
        }
        if (data['monthlyOccurrences']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['monthlyOccurrences'])) {
                throw new Error("Expected the field `monthlyOccurrences` to be an array in the JSON data but got " + data['monthlyOccurrences']);
            }
            // validate the optional field `monthlyOccurrences` (array)
            for (const item of data['monthlyOccurrences']) {
                RecurrenceScheduleOccurrence.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['weekDays'])) {
            throw new Error("Expected the field `weekDays` to be an array in the JSON data but got " + data['weekDays']);
        }

        return true;
    }


}



/**
 * The hours.
 * @member {Array.<Number>} hours
 */
RecurrenceSchedule.prototype['hours'] = undefined;

/**
 * The minutes.
 * @member {Array.<Number>} minutes
 */
RecurrenceSchedule.prototype['minutes'] = undefined;

/**
 * The month days.
 * @member {Array.<Number>} monthDays
 */
RecurrenceSchedule.prototype['monthDays'] = undefined;

/**
 * The monthly occurrences.
 * @member {Array.<module:model/RecurrenceScheduleOccurrence>} monthlyOccurrences
 */
RecurrenceSchedule.prototype['monthlyOccurrences'] = undefined;

/**
 * The days of the week.
 * @member {Array.<module:model/RecurrenceSchedule.WeekDaysEnum>} weekDays
 */
RecurrenceSchedule.prototype['weekDays'] = undefined;





/**
 * Allowed values for the <code>weekDays</code> property.
 * @enum {String}
 * @readonly
 */
RecurrenceSchedule['WeekDaysEnum'] = {

    /**
     * value: "Sunday"
     * @const
     */
    "Sunday": "Sunday",

    /**
     * value: "Monday"
     * @const
     */
    "Monday": "Monday",

    /**
     * value: "Tuesday"
     * @const
     */
    "Tuesday": "Tuesday",

    /**
     * value: "Wednesday"
     * @const
     */
    "Wednesday": "Wednesday",

    /**
     * value: "Thursday"
     * @const
     */
    "Thursday": "Thursday",

    /**
     * value: "Friday"
     * @const
     */
    "Friday": "Friday",

    /**
     * value: "Saturday"
     * @const
     */
    "Saturday": "Saturday"
};



export default RecurrenceSchedule;

