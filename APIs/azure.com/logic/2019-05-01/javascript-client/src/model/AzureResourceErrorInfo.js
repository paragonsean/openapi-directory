/**
 * LogicManagementClient
 * REST API for Azure Logic Apps.
 *
 * The version of the OpenAPI document: 2019-05-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AzureResourceErrorInfo from './AzureResourceErrorInfo';
import ErrorInfo from './ErrorInfo';

/**
 * The AzureResourceErrorInfo model module.
 * @module model/AzureResourceErrorInfo
 * @version 2019-05-01
 */
class AzureResourceErrorInfo {
    /**
     * Constructs a new <code>AzureResourceErrorInfo</code>.
     * The azure resource error info.
     * @alias module:model/AzureResourceErrorInfo
     * @implements module:model/ErrorInfo
     * @param code {String} The error code.
     */
    constructor(code) { 
        ErrorInfo.initialize(this, code);
        AzureResourceErrorInfo.initialize(this, code);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, code) { 
        obj['message'] = message;
        obj['code'] = code;
    }

    /**
     * Constructs a <code>AzureResourceErrorInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AzureResourceErrorInfo} obj Optional instance to populate.
     * @return {module:model/AzureResourceErrorInfo} The populated <code>AzureResourceErrorInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AzureResourceErrorInfo();
            ErrorInfo.constructFromObject(data, obj);

            if (data.hasOwnProperty('details')) {
                obj['details'] = ApiClient.convertToType(data['details'], [AzureResourceErrorInfo]);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AzureResourceErrorInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AzureResourceErrorInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AzureResourceErrorInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['details']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['details'])) {
                throw new Error("Expected the field `details` to be an array in the JSON data but got " + data['details']);
            }
            // validate the optional field `details` (array)
            for (const item of data['details']) {
                AzureResourceErrorInfo.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }

        return true;
    }


}

AzureResourceErrorInfo.RequiredProperties = ["message", "code"];

/**
 * The error details.
 * @member {Array.<module:model/AzureResourceErrorInfo>} details
 */
AzureResourceErrorInfo.prototype['details'] = undefined;

/**
 * The error message.
 * @member {String} message
 */
AzureResourceErrorInfo.prototype['message'] = undefined;

/**
 * The error code.
 * @member {String} code
 */
AzureResourceErrorInfo.prototype['code'] = undefined;


// Implement ErrorInfo interface:
/**
 * The error code.
 * @member {String} code
 */
ErrorInfo.prototype['code'] = undefined;




export default AzureResourceErrorInfo;

