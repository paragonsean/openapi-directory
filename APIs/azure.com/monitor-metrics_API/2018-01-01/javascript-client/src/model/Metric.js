/**
 * MonitorManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-01-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LocalizableString from './LocalizableString';
import TimeSeriesElement from './TimeSeriesElement';
import Unit from './Unit';

/**
 * The Metric model module.
 * @module model/Metric
 * @version 2018-01-01
 */
class Metric {
    /**
     * Constructs a new <code>Metric</code>.
     * The result data of a query.
     * @alias module:model/Metric
     * @param id {String} the metric Id.
     * @param name {module:model/LocalizableString} 
     * @param timeseries {Array.<module:model/TimeSeriesElement>} the time series returned when a data query is performed.
     * @param type {String} the resource type of the metric resource.
     * @param unit {module:model/Unit} 
     */
    constructor(id, name, timeseries, type, unit) { 
        
        Metric.initialize(this, id, name, timeseries, type, unit);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, timeseries, type, unit) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['timeseries'] = timeseries;
        obj['type'] = type;
        obj['unit'] = unit;
    }

    /**
     * Constructs a <code>Metric</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Metric} obj Optional instance to populate.
     * @return {module:model/Metric} The populated <code>Metric</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Metric();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = LocalizableString.constructFromObject(data['name']);
            }
            if (data.hasOwnProperty('timeseries')) {
                obj['timeseries'] = ApiClient.convertToType(data['timeseries'], [TimeSeriesElement]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('unit')) {
                obj['unit'] = Unit.constructFromObject(data['unit']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Metric</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Metric</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Metric.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `name`
        if (data['name']) { // data not null
          LocalizableString.validateJSON(data['name']);
        }
        if (data['timeseries']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['timeseries'])) {
                throw new Error("Expected the field `timeseries` to be an array in the JSON data but got " + data['timeseries']);
            }
            // validate the optional field `timeseries` (array)
            for (const item of data['timeseries']) {
                TimeSeriesElement.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Metric.RequiredProperties = ["id", "name", "timeseries", "type", "unit"];

/**
 * the metric Id.
 * @member {String} id
 */
Metric.prototype['id'] = undefined;

/**
 * @member {module:model/LocalizableString} name
 */
Metric.prototype['name'] = undefined;

/**
 * the time series returned when a data query is performed.
 * @member {Array.<module:model/TimeSeriesElement>} timeseries
 */
Metric.prototype['timeseries'] = undefined;

/**
 * the resource type of the metric resource.
 * @member {String} type
 */
Metric.prototype['type'] = undefined;

/**
 * @member {module:model/Unit} unit
 */
Metric.prototype['unit'] = undefined;






export default Metric;

