/*
 * MonitorManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-01-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.client.model.MetadataValue;
import org.openapitools.client.model.MetricValue;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.openapitools.client.JSON;

/**
 * A time series result type. The discriminator value is always TimeSeries in this case.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-12T11:11:34.764477-04:00[America/New_York]", comments = "Generator version: 7.9.0")
public class TimeSeriesElement {
  public static final String SERIALIZED_NAME_DATA = "data";
  @SerializedName(SERIALIZED_NAME_DATA)
  private List<MetricValue> data = new ArrayList<>();

  public static final String SERIALIZED_NAME_METADATAVALUES = "metadatavalues";
  @SerializedName(SERIALIZED_NAME_METADATAVALUES)
  private List<MetadataValue> metadatavalues = new ArrayList<>();

  public TimeSeriesElement() {
  }

  public TimeSeriesElement data(List<MetricValue> data) {
    this.data = data;
    return this;
  }

  public TimeSeriesElement addDataItem(MetricValue dataItem) {
    if (this.data == null) {
      this.data = new ArrayList<>();
    }
    this.data.add(dataItem);
    return this;
  }

  /**
   * An array of data points representing the metric values.  This is only returned if a result type of data is specified.
   * @return data
   */
  @javax.annotation.Nullable
  public List<MetricValue> getData() {
    return data;
  }

  public void setData(List<MetricValue> data) {
    this.data = data;
  }


  public TimeSeriesElement metadatavalues(List<MetadataValue> metadatavalues) {
    this.metadatavalues = metadatavalues;
    return this;
  }

  public TimeSeriesElement addMetadatavaluesItem(MetadataValue metadatavaluesItem) {
    if (this.metadatavalues == null) {
      this.metadatavalues = new ArrayList<>();
    }
    this.metadatavalues.add(metadatavaluesItem);
    return this;
  }

  /**
   * the metadata values returned if $filter was specified in the call.
   * @return metadatavalues
   */
  @javax.annotation.Nullable
  public List<MetadataValue> getMetadatavalues() {
    return metadatavalues;
  }

  public void setMetadatavalues(List<MetadataValue> metadatavalues) {
    this.metadatavalues = metadatavalues;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TimeSeriesElement timeSeriesElement = (TimeSeriesElement) o;
    return Objects.equals(this.data, timeSeriesElement.data) &&
        Objects.equals(this.metadatavalues, timeSeriesElement.metadatavalues);
  }

  @Override
  public int hashCode() {
    return Objects.hash(data, metadatavalues);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TimeSeriesElement {\n");
    sb.append("    data: ").append(toIndentedString(data)).append("\n");
    sb.append("    metadatavalues: ").append(toIndentedString(metadatavalues)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("data");
    openapiFields.add("metadatavalues");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to TimeSeriesElement
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TimeSeriesElement.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TimeSeriesElement is not found in the empty JSON string", TimeSeriesElement.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!TimeSeriesElement.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `TimeSeriesElement` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("data") != null && !jsonObj.get("data").isJsonNull()) {
        JsonArray jsonArraydata = jsonObj.getAsJsonArray("data");
        if (jsonArraydata != null) {
          // ensure the json data is an array
          if (!jsonObj.get("data").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `data` to be an array in the JSON string but got `%s`", jsonObj.get("data").toString()));
          }

          // validate the optional field `data` (array)
          for (int i = 0; i < jsonArraydata.size(); i++) {
            MetricValue.validateJsonElement(jsonArraydata.get(i));
          };
        }
      }
      if (jsonObj.get("metadatavalues") != null && !jsonObj.get("metadatavalues").isJsonNull()) {
        JsonArray jsonArraymetadatavalues = jsonObj.getAsJsonArray("metadatavalues");
        if (jsonArraymetadatavalues != null) {
          // ensure the json data is an array
          if (!jsonObj.get("metadatavalues").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `metadatavalues` to be an array in the JSON string but got `%s`", jsonObj.get("metadatavalues").toString()));
          }

          // validate the optional field `metadatavalues` (array)
          for (int i = 0; i < jsonArraymetadatavalues.size(); i++) {
            MetadataValue.validateJsonElement(jsonArraymetadatavalues.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TimeSeriesElement.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TimeSeriesElement' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TimeSeriesElement> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TimeSeriesElement.class));

       return (TypeAdapter<T>) new TypeAdapter<TimeSeriesElement>() {
           @Override
           public void write(JsonWriter out, TimeSeriesElement value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public TimeSeriesElement read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of TimeSeriesElement given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of TimeSeriesElement
   * @throws IOException if the JSON string is invalid with respect to TimeSeriesElement
   */
  public static TimeSeriesElement fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TimeSeriesElement.class);
  }

  /**
   * Convert an instance of TimeSeriesElement to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

