/**
 * RecoveryServicesBackupClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-05-13
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AzureWorkloadRecoveryPoint from './AzureWorkloadRecoveryPoint';
import PointInTimeRange from './PointInTimeRange';

/**
 * The AzureWorkloadPointInTimeRecoveryPoint model module.
 * @module model/AzureWorkloadPointInTimeRecoveryPoint
 * @version 2019-05-13
 */
class AzureWorkloadPointInTimeRecoveryPoint {
    /**
     * Constructs a new <code>AzureWorkloadPointInTimeRecoveryPoint</code>.
     * Recovery point specific to PointInTime
     * @alias module:model/AzureWorkloadPointInTimeRecoveryPoint
     * @extends module:model/AzureWorkloadRecoveryPoint
     * @implements module:model/AzureWorkloadRecoveryPoint
     * @param objectType {String} This property will be used as the discriminator for deciding the specific types in the polymorphic chain of types.
     */
    constructor(objectType) { 
        AzureWorkloadRecoveryPoint.initialize(this, objectType);
        AzureWorkloadPointInTimeRecoveryPoint.initialize(this, objectType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, objectType) { 
    }

    /**
     * Constructs a <code>AzureWorkloadPointInTimeRecoveryPoint</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AzureWorkloadPointInTimeRecoveryPoint} obj Optional instance to populate.
     * @return {module:model/AzureWorkloadPointInTimeRecoveryPoint} The populated <code>AzureWorkloadPointInTimeRecoveryPoint</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AzureWorkloadPointInTimeRecoveryPoint();
            AzureWorkloadRecoveryPoint.constructFromObject(data, obj);
            AzureWorkloadRecoveryPoint.constructFromObject(data, obj);

            if (data.hasOwnProperty('timeRanges')) {
                obj['timeRanges'] = ApiClient.convertToType(data['timeRanges'], [PointInTimeRange]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AzureWorkloadPointInTimeRecoveryPoint</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AzureWorkloadPointInTimeRecoveryPoint</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AzureWorkloadPointInTimeRecoveryPoint.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['timeRanges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['timeRanges'])) {
                throw new Error("Expected the field `timeRanges` to be an array in the JSON data but got " + data['timeRanges']);
            }
            // validate the optional field `timeRanges` (array)
            for (const item of data['timeRanges']) {
                PointInTimeRange.validateJSON(item);
            };
        }

        return true;
    }


}

AzureWorkloadPointInTimeRecoveryPoint.RequiredProperties = ["objectType"];

/**
 * List of log ranges
 * @member {Array.<module:model/PointInTimeRange>} timeRanges
 */
AzureWorkloadPointInTimeRecoveryPoint.prototype['timeRanges'] = undefined;


// Implement AzureWorkloadRecoveryPoint interface:
/**
 * This property will be used as the discriminator for deciding the specific types in the polymorphic chain of types.
 * @member {String} objectType
 */
AzureWorkloadRecoveryPoint.prototype['objectType'] = undefined;




export default AzureWorkloadPointInTimeRecoveryPoint;

