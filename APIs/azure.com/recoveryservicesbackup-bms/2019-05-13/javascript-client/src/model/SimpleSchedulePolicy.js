/**
 * RecoveryServicesBackupClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-05-13
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SchedulePolicy from './SchedulePolicy';

/**
 * The SimpleSchedulePolicy model module.
 * @module model/SimpleSchedulePolicy
 * @version 2019-05-13
 */
class SimpleSchedulePolicy {
    /**
     * Constructs a new <code>SimpleSchedulePolicy</code>.
     * Simple policy schedule.
     * @alias module:model/SimpleSchedulePolicy
     * @extends module:model/SchedulePolicy
     * @implements module:model/SchedulePolicy
     * @param schedulePolicyType {String} This property will be used as the discriminator for deciding the specific types in the polymorphic chain of types.
     */
    constructor(schedulePolicyType) { 
        SchedulePolicy.initialize(this, schedulePolicyType);
        SimpleSchedulePolicy.initialize(this, schedulePolicyType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, schedulePolicyType) { 
    }

    /**
     * Constructs a <code>SimpleSchedulePolicy</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SimpleSchedulePolicy} obj Optional instance to populate.
     * @return {module:model/SimpleSchedulePolicy} The populated <code>SimpleSchedulePolicy</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SimpleSchedulePolicy();
            SchedulePolicy.constructFromObject(data, obj);
            SchedulePolicy.constructFromObject(data, obj);

            if (data.hasOwnProperty('scheduleRunDays')) {
                obj['scheduleRunDays'] = ApiClient.convertToType(data['scheduleRunDays'], ['String']);
            }
            if (data.hasOwnProperty('scheduleRunFrequency')) {
                obj['scheduleRunFrequency'] = ApiClient.convertToType(data['scheduleRunFrequency'], 'String');
            }
            if (data.hasOwnProperty('scheduleRunTimes')) {
                obj['scheduleRunTimes'] = ApiClient.convertToType(data['scheduleRunTimes'], ['Date']);
            }
            if (data.hasOwnProperty('scheduleWeeklyFrequency')) {
                obj['scheduleWeeklyFrequency'] = ApiClient.convertToType(data['scheduleWeeklyFrequency'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SimpleSchedulePolicy</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SimpleSchedulePolicy</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SimpleSchedulePolicy.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scheduleRunDays'])) {
            throw new Error("Expected the field `scheduleRunDays` to be an array in the JSON data but got " + data['scheduleRunDays']);
        }
        // ensure the json data is a string
        if (data['scheduleRunFrequency'] && !(typeof data['scheduleRunFrequency'] === 'string' || data['scheduleRunFrequency'] instanceof String)) {
            throw new Error("Expected the field `scheduleRunFrequency` to be a primitive type in the JSON string but got " + data['scheduleRunFrequency']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scheduleRunTimes'])) {
            throw new Error("Expected the field `scheduleRunTimes` to be an array in the JSON data but got " + data['scheduleRunTimes']);
        }

        return true;
    }


}

SimpleSchedulePolicy.RequiredProperties = ["schedulePolicyType"];

/**
 * List of days of week this schedule has to be run.
 * @member {Array.<module:model/SimpleSchedulePolicy.ScheduleRunDaysEnum>} scheduleRunDays
 */
SimpleSchedulePolicy.prototype['scheduleRunDays'] = undefined;

/**
 * Frequency of the schedule operation of this policy.
 * @member {module:model/SimpleSchedulePolicy.ScheduleRunFrequencyEnum} scheduleRunFrequency
 */
SimpleSchedulePolicy.prototype['scheduleRunFrequency'] = undefined;

/**
 * List of times of day this schedule has to be run.
 * @member {Array.<Date>} scheduleRunTimes
 */
SimpleSchedulePolicy.prototype['scheduleRunTimes'] = undefined;

/**
 * At every number weeks this schedule has to be run.
 * @member {Number} scheduleWeeklyFrequency
 */
SimpleSchedulePolicy.prototype['scheduleWeeklyFrequency'] = undefined;


// Implement SchedulePolicy interface:
/**
 * This property will be used as the discriminator for deciding the specific types in the polymorphic chain of types.
 * @member {String} schedulePolicyType
 */
SchedulePolicy.prototype['schedulePolicyType'] = undefined;



/**
 * Allowed values for the <code>scheduleRunDays</code> property.
 * @enum {String}
 * @readonly
 */
SimpleSchedulePolicy['ScheduleRunDaysEnum'] = {

    /**
     * value: "Sunday"
     * @const
     */
    "Sunday": "Sunday",

    /**
     * value: "Monday"
     * @const
     */
    "Monday": "Monday",

    /**
     * value: "Tuesday"
     * @const
     */
    "Tuesday": "Tuesday",

    /**
     * value: "Wednesday"
     * @const
     */
    "Wednesday": "Wednesday",

    /**
     * value: "Thursday"
     * @const
     */
    "Thursday": "Thursday",

    /**
     * value: "Friday"
     * @const
     */
    "Friday": "Friday",

    /**
     * value: "Saturday"
     * @const
     */
    "Saturday": "Saturday"
};


/**
 * Allowed values for the <code>scheduleRunFrequency</code> property.
 * @enum {String}
 * @readonly
 */
SimpleSchedulePolicy['ScheduleRunFrequencyEnum'] = {

    /**
     * value: "Invalid"
     * @const
     */
    "Invalid": "Invalid",

    /**
     * value: "Daily"
     * @const
     */
    "Daily": "Daily",

    /**
     * value: "Weekly"
     * @const
     */
    "Weekly": "Weekly"
};



export default SimpleSchedulePolicy;

