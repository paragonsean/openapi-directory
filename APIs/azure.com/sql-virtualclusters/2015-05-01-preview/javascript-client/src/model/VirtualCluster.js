/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2015-05-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TrackedResource from './TrackedResource';
import VirtualClusterProperties from './VirtualClusterProperties';

/**
 * The VirtualCluster model module.
 * @module model/VirtualCluster
 * @version 2015-05-01-preview
 */
class VirtualCluster {
    /**
     * Constructs a new <code>VirtualCluster</code>.
     * An Azure SQL virtual cluster.
     * @alias module:model/VirtualCluster
     * @implements module:model/TrackedResource
     * @param location {String} Resource location.
     */
    constructor(location) { 
        TrackedResource.initialize(this);
        VirtualCluster.initialize(this, location);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, location) { 
        obj['location'] = location;
    }

    /**
     * Constructs a <code>VirtualCluster</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VirtualCluster} obj Optional instance to populate.
     * @return {module:model/VirtualCluster} The populated <code>VirtualCluster</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VirtualCluster();
            TrackedResource.constructFromObject(data, obj);

            if (data.hasOwnProperty('properties')) {
                obj['properties'] = VirtualClusterProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VirtualCluster</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VirtualCluster</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VirtualCluster.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          VirtualClusterProperties.validateJSON(data['properties']);
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

VirtualCluster.RequiredProperties = ["location"];

/**
 * @member {module:model/VirtualClusterProperties} properties
 */
VirtualCluster.prototype['properties'] = undefined;

/**
 * Resource location.
 * @member {String} location
 */
VirtualCluster.prototype['location'] = undefined;

/**
 * Resource tags.
 * @member {Object.<String, String>} tags
 */
VirtualCluster.prototype['tags'] = undefined;

/**
 * Resource ID.
 * @member {String} id
 */
VirtualCluster.prototype['id'] = undefined;

/**
 * Resource name.
 * @member {String} name
 */
VirtualCluster.prototype['name'] = undefined;

/**
 * Resource type.
 * @member {String} type
 */
VirtualCluster.prototype['type'] = undefined;


// Implement TrackedResource interface:
/**
 * Resource ID.
 * @member {String} id
 */
TrackedResource.prototype['id'] = undefined;
/**
 * Resource name.
 * @member {String} name
 */
TrackedResource.prototype['name'] = undefined;
/**
 * Resource type.
 * @member {String} type
 */
TrackedResource.prototype['type'] = undefined;




export default VirtualCluster;

