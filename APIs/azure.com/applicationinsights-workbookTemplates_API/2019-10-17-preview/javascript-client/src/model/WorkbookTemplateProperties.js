/**
 * ApplicationInsightsManagementClient
 * Azure Application Insights workbook template type.
 *
 * The version of the OpenAPI document: 2019-10-17-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkbookTemplateGallery from './WorkbookTemplateGallery';
import WorkbookTemplateLocalizedGallery from './WorkbookTemplateLocalizedGallery';

/**
 * The WorkbookTemplateProperties model module.
 * @module model/WorkbookTemplateProperties
 * @version 2019-10-17-preview
 */
class WorkbookTemplateProperties {
    /**
     * Constructs a new <code>WorkbookTemplateProperties</code>.
     * Properties that contain a workbook template.
     * @alias module:model/WorkbookTemplateProperties
     * @param galleries {Array.<module:model/WorkbookTemplateGallery>} Workbook galleries supported by the template.
     * @param templateData {Object} Valid JSON object containing workbook template payload.
     */
    constructor(galleries, templateData) { 
        
        WorkbookTemplateProperties.initialize(this, galleries, templateData);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, galleries, templateData) { 
        obj['galleries'] = galleries;
        obj['templateData'] = templateData;
    }

    /**
     * Constructs a <code>WorkbookTemplateProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkbookTemplateProperties} obj Optional instance to populate.
     * @return {module:model/WorkbookTemplateProperties} The populated <code>WorkbookTemplateProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkbookTemplateProperties();

            if (data.hasOwnProperty('author')) {
                obj['author'] = ApiClient.convertToType(data['author'], 'String');
            }
            if (data.hasOwnProperty('galleries')) {
                obj['galleries'] = ApiClient.convertToType(data['galleries'], [WorkbookTemplateGallery]);
            }
            if (data.hasOwnProperty('localized')) {
                obj['localized'] = ApiClient.convertToType(data['localized'], {'String': [WorkbookTemplateLocalizedGallery]});
            }
            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'Number');
            }
            if (data.hasOwnProperty('templateData')) {
                obj['templateData'] = ApiClient.convertToType(data['templateData'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkbookTemplateProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkbookTemplateProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WorkbookTemplateProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['author'] && !(typeof data['author'] === 'string' || data['author'] instanceof String)) {
            throw new Error("Expected the field `author` to be a primitive type in the JSON string but got " + data['author']);
        }
        if (data['galleries']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['galleries'])) {
                throw new Error("Expected the field `galleries` to be an array in the JSON data but got " + data['galleries']);
            }
            // validate the optional field `galleries` (array)
            for (const item of data['galleries']) {
                WorkbookTemplateGallery.validateJSON(item);
            };
        }

        return true;
    }


}

WorkbookTemplateProperties.RequiredProperties = ["galleries", "templateData"];

/**
 * Information about the author of the workbook template.
 * @member {String} author
 */
WorkbookTemplateProperties.prototype['author'] = undefined;

/**
 * Workbook galleries supported by the template.
 * @member {Array.<module:model/WorkbookTemplateGallery>} galleries
 */
WorkbookTemplateProperties.prototype['galleries'] = undefined;

/**
 * Key value pair of localized gallery. Each key is the locale code of languages supported by the Azure portal.
 * @member {Object.<String, Array.<module:model/WorkbookTemplateLocalizedGallery>>} localized
 */
WorkbookTemplateProperties.prototype['localized'] = undefined;

/**
 * Priority of the template. Determines which template to open when a workbook gallery is opened in viewer mode.
 * @member {Number} priority
 */
WorkbookTemplateProperties.prototype['priority'] = undefined;

/**
 * Valid JSON object containing workbook template payload.
 * @member {Object} templateData
 */
WorkbookTemplateProperties.prototype['templateData'] = undefined;






export default WorkbookTemplateProperties;

