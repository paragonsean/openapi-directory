/**
 * ApiManagementClient
 * Use these REST APIs to manage Azure API Management deployment.
 *
 * The version of the OpenAPI document: 2019-12-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ResourceSkuResult from './ResourceSkuResult';

/**
 * The ResourceSkuResults model module.
 * @module model/ResourceSkuResults
 * @version 2019-12-01-preview
 */
class ResourceSkuResults {
    /**
     * Constructs a new <code>ResourceSkuResults</code>.
     * The API Management service SKUs operation response.
     * @alias module:model/ResourceSkuResults
     * @param value {Array.<module:model/ResourceSkuResult>} The list of skus available for the service.
     */
    constructor(value) { 
        
        ResourceSkuResults.initialize(this, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, value) { 
        obj['value'] = value;
    }

    /**
     * Constructs a <code>ResourceSkuResults</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ResourceSkuResults} obj Optional instance to populate.
     * @return {module:model/ResourceSkuResults} The populated <code>ResourceSkuResults</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ResourceSkuResults();

            if (data.hasOwnProperty('nextLink')) {
                obj['nextLink'] = ApiClient.convertToType(data['nextLink'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], [ResourceSkuResult]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ResourceSkuResults</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ResourceSkuResults</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ResourceSkuResults.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['nextLink'] && !(typeof data['nextLink'] === 'string' || data['nextLink'] instanceof String)) {
            throw new Error("Expected the field `nextLink` to be a primitive type in the JSON string but got " + data['nextLink']);
        }
        if (data['value']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['value'])) {
                throw new Error("Expected the field `value` to be an array in the JSON data but got " + data['value']);
            }
            // validate the optional field `value` (array)
            for (const item of data['value']) {
                ResourceSkuResult.validateJSON(item);
            };
        }

        return true;
    }


}

ResourceSkuResults.RequiredProperties = ["value"];

/**
 * The uri to fetch the next page of API Management service Skus.
 * @member {String} nextLink
 */
ResourceSkuResults.prototype['nextLink'] = undefined;

/**
 * The list of skus available for the service.
 * @member {Array.<module:model/ResourceSkuResult>} value
 */
ResourceSkuResults.prototype['value'] = undefined;






export default ResourceSkuResults;

