/**
 * CdnManagementClient
 * Use these APIs to manage Azure CDN resources through the Azure Resource Manager. You must make sure that requests made to these resources are secure.
 *
 * The version of the OpenAPI document: 2019-12-31
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OriginProperties from './OriginProperties';
import TrackedResource from './TrackedResource';

/**
 * The Origin model module.
 * @module model/Origin
 * @version 2019-12-31
 */
class Origin {
    /**
     * Constructs a new <code>Origin</code>.
     * CDN origin is the source of the content being delivered via CDN. When the edge nodes represented by an endpoint do not have the requested content cached, they attempt to fetch it from one or more of the configured origins.
     * @alias module:model/Origin
     * @implements module:model/TrackedResource
     * @param location {String} Resource location.
     */
    constructor(location) { 
        TrackedResource.initialize(this);
        Origin.initialize(this, location);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, location) { 
        obj['location'] = location;
    }

    /**
     * Constructs a <code>Origin</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Origin} obj Optional instance to populate.
     * @return {module:model/Origin} The populated <code>Origin</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Origin();
            TrackedResource.constructFromObject(data, obj);

            if (data.hasOwnProperty('properties')) {
                obj['properties'] = OriginProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Origin</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Origin</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Origin.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          OriginProperties.validateJSON(data['properties']);
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Origin.RequiredProperties = ["location"];

/**
 * @member {module:model/OriginProperties} properties
 */
Origin.prototype['properties'] = undefined;

/**
 * Resource location.
 * @member {String} location
 */
Origin.prototype['location'] = undefined;

/**
 * Resource tags.
 * @member {Object.<String, String>} tags
 */
Origin.prototype['tags'] = undefined;

/**
 * Resource ID.
 * @member {String} id
 */
Origin.prototype['id'] = undefined;

/**
 * Resource name.
 * @member {String} name
 */
Origin.prototype['name'] = undefined;

/**
 * Resource type.
 * @member {String} type
 */
Origin.prototype['type'] = undefined;


// Implement TrackedResource interface:
/**
 * Resource ID.
 * @member {String} id
 */
TrackedResource.prototype['id'] = undefined;
/**
 * Resource name.
 * @member {String} name
 */
TrackedResource.prototype['name'] = undefined;
/**
 * Resource type.
 * @member {String} type
 */
TrackedResource.prototype['type'] = undefined;




export default Origin;

