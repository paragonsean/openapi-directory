/**
 * Azure Media Services
 * This Swagger was generated by the API Framework.
 *
 * The version of the OpenAPI document: 2018-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Image from './Image';
import PngLayer from './PngLayer';

/**
 * The PngImage model module.
 * @module model/PngImage
 * @version 2018-07-01
 */
class PngImage {
    /**
     * Constructs a new <code>PngImage</code>.
     * Describes the properties for producing a series of PNG images from the input video.
     * @alias module:model/PngImage
     * @extends module:model/Image
     * @implements module:model/Image
     * @param start {String} The position in the input video from where to start generating thumbnails. The value can be in absolute timestamp (ISO 8601, e.g: PT05S), or a frame count (For example, 10 for the 10th frame), or a relative value (For example, 1%). Also supports a macro {Best}, which tells the encoder to select the best thumbnail from the first few seconds of the video.
     * @param odataType {String} The discriminator for derived types.
     */
    constructor(start, odataType) { 
        Image.initialize(this, odataType);
        PngImage.initialize(this, start, odataType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, start, odataType) { 
    }

    /**
     * Constructs a <code>PngImage</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PngImage} obj Optional instance to populate.
     * @return {module:model/PngImage} The populated <code>PngImage</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PngImage();
            Image.constructFromObject(data, obj);
            Image.constructFromObject(data, obj);

            if (data.hasOwnProperty('layers')) {
                obj['layers'] = ApiClient.convertToType(data['layers'], [PngLayer]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PngImage</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PngImage</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PngImage.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['layers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['layers'])) {
                throw new Error("Expected the field `layers` to be an array in the JSON data but got " + data['layers']);
            }
            // validate the optional field `layers` (array)
            for (const item of data['layers']) {
                PngLayer.validateJSON(item);
            };
        }

        return true;
    }


}

PngImage.RequiredProperties = ["start", "@odata.type"];

/**
 * A collection of output PNG image layers to be produced by the encoder.
 * @member {Array.<module:model/PngLayer>} layers
 */
PngImage.prototype['layers'] = undefined;


// Implement Image interface:
/**
 * The distance between two key frames, thereby defining a group of pictures (GOP). The value should be a non-zero integer in the range [1, 30] seconds, specified in ISO 8601 format. The default is 2 seconds (PT2S).
 * @member {String} keyFrameInterval
 */
Image.prototype['keyFrameInterval'] = undefined;
/**
 * The resizing mode - how the input video will be resized to fit the desired output resolution(s). Default is AutoSize
 * @member {module:model/Image.StretchModeEnum} stretchMode
 */
Image.prototype['stretchMode'] = undefined;
/**
 * The discriminator for derived types.
 * @member {String} @odata.type
 */
Image.prototype['@odata.type'] = undefined;
/**
 * An optional label for the codec. The label can be used to control muxing behavior.
 * @member {String} label
 */
Image.prototype['label'] = undefined;




export default PngImage;

