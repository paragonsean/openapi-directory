/**
 * Azure Media Services
 * This Swagger was generated by the API Framework.
 *
 * The version of the OpenAPI document: 2018-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransformOutput from './TransformOutput';

/**
 * The TransformProperties model module.
 * @module model/TransformProperties
 * @version 2018-06-01-preview
 */
class TransformProperties {
    /**
     * Constructs a new <code>TransformProperties</code>.
     * A Transform.
     * @alias module:model/TransformProperties
     * @param outputs {Array.<module:model/TransformOutput>} An array of one or more TransformOutputs that the Transform should generate.
     */
    constructor(outputs) { 
        
        TransformProperties.initialize(this, outputs);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, outputs) { 
        obj['outputs'] = outputs;
    }

    /**
     * Constructs a <code>TransformProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransformProperties} obj Optional instance to populate.
     * @return {module:model/TransformProperties} The populated <code>TransformProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransformProperties();

            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Date');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('lastModified')) {
                obj['lastModified'] = ApiClient.convertToType(data['lastModified'], 'Date');
            }
            if (data.hasOwnProperty('outputs')) {
                obj['outputs'] = ApiClient.convertToType(data['outputs'], [TransformOutput]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransformProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransformProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransformProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['outputs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['outputs'])) {
                throw new Error("Expected the field `outputs` to be an array in the JSON data but got " + data['outputs']);
            }
            // validate the optional field `outputs` (array)
            for (const item of data['outputs']) {
                TransformOutput.validateJSON(item);
            };
        }

        return true;
    }


}

TransformProperties.RequiredProperties = ["outputs"];

/**
 * The UTC date and time when the Transform was created, in 'YYYY-MM-DDThh:mm:ssZ' format.
 * @member {Date} created
 */
TransformProperties.prototype['created'] = undefined;

/**
 * An optional verbose description of the Transform.
 * @member {String} description
 */
TransformProperties.prototype['description'] = undefined;

/**
 * The UTC date and time when the Transform was last updated, in 'YYYY-MM-DDThh:mm:ssZ' format.
 * @member {Date} lastModified
 */
TransformProperties.prototype['lastModified'] = undefined;

/**
 * An array of one or more TransformOutputs that the Transform should generate.
 * @member {Array.<module:model/TransformOutput>} outputs
 */
TransformProperties.prototype['outputs'] = undefined;






export default TransformProperties;

