/**
 * Azure Media Services
 * This Swagger was generated by the API Framework.
 *
 * The version of the OpenAPI document: 2018-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JobInputClip from './JobInputClip';

/**
 * The JobInputAsset model module.
 * @module model/JobInputAsset
 * @version 2018-06-01-preview
 */
class JobInputAsset {
    /**
     * Constructs a new <code>JobInputAsset</code>.
     * Represents an Asset for input into a Job.
     * @alias module:model/JobInputAsset
     * @extends module:model/JobInputClip
     * @implements module:model/JobInputClip
     * @param odataType {String} The discriminator for derived types.
     */
    constructor(odataType) { 
        JobInputClip.initialize(this, odataType);
        JobInputAsset.initialize(this, odataType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, odataType) { 
        obj['assetName'] = assetName;
    }

    /**
     * Constructs a <code>JobInputAsset</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobInputAsset} obj Optional instance to populate.
     * @return {module:model/JobInputAsset} The populated <code>JobInputAsset</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobInputAsset();
            JobInputClip.constructFromObject(data, obj);
            JobInputClip.constructFromObject(data, obj);

            if (data.hasOwnProperty('assetName')) {
                obj['assetName'] = ApiClient.convertToType(data['assetName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobInputAsset</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobInputAsset</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobInputAsset.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['assetName'] && !(typeof data['assetName'] === 'string' || data['assetName'] instanceof String)) {
            throw new Error("Expected the field `assetName` to be a primitive type in the JSON string but got " + data['assetName']);
        }

        return true;
    }


}

JobInputAsset.RequiredProperties = ["assetName", "@odata.type"];

/**
 * The name of the input Asset.
 * @member {String} assetName
 */
JobInputAsset.prototype['assetName'] = undefined;


// Implement JobInputClip interface:
/**
 * The discriminator for derived types.
 * @member {String} @odata.type
 */
JobInputClip.prototype['@odata.type'] = undefined;
/**
 * A label that is assigned to a JobInput, that is used to satisfy a reference used in the Transform. For example, a Transform can be authored so as to take an image file with the label 'xyz' and apply it as an overlay onto the input video before it is encoded. When submitting a Job, exactly one of the JobInputs should be the image file, and it should have the label 'xyz'.
 * @member {String} label
 */
JobInputClip.prototype['label'] = undefined;




export default JobInputAsset;

