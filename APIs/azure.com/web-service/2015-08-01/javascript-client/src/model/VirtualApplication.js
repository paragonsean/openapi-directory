/**
 * WebSite Management Client
 * Use these APIs to manage Azure Websites resources through the Azure Resource Manager. All task operations conform to the HTTP/1.1 protocol specification and each operation returns an x-ms-request-id header that can be used to obtain information about the request. You must make sure that requests made to these resources are secure. For more information, see https://msdn.microsoft.com/en-us/library/azure/dn790557.aspx.
 *
 * The version of the OpenAPI document: 2015-08-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import VirtualDirectory from './VirtualDirectory';

/**
 * The VirtualApplication model module.
 * @module model/VirtualApplication
 * @version 2015-08-01
 */
class VirtualApplication {
    /**
     * Constructs a new <code>VirtualApplication</code>.
     * @alias module:model/VirtualApplication
     */
    constructor() { 
        
        VirtualApplication.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>VirtualApplication</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VirtualApplication} obj Optional instance to populate.
     * @return {module:model/VirtualApplication} The populated <code>VirtualApplication</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VirtualApplication();

            if (data.hasOwnProperty('physicalPath')) {
                obj['physicalPath'] = ApiClient.convertToType(data['physicalPath'], 'String');
            }
            if (data.hasOwnProperty('preloadEnabled')) {
                obj['preloadEnabled'] = ApiClient.convertToType(data['preloadEnabled'], 'Boolean');
            }
            if (data.hasOwnProperty('virtualDirectories')) {
                obj['virtualDirectories'] = ApiClient.convertToType(data['virtualDirectories'], [VirtualDirectory]);
            }
            if (data.hasOwnProperty('virtualPath')) {
                obj['virtualPath'] = ApiClient.convertToType(data['virtualPath'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VirtualApplication</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VirtualApplication</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['physicalPath'] && !(typeof data['physicalPath'] === 'string' || data['physicalPath'] instanceof String)) {
            throw new Error("Expected the field `physicalPath` to be a primitive type in the JSON string but got " + data['physicalPath']);
        }
        if (data['virtualDirectories']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['virtualDirectories'])) {
                throw new Error("Expected the field `virtualDirectories` to be an array in the JSON data but got " + data['virtualDirectories']);
            }
            // validate the optional field `virtualDirectories` (array)
            for (const item of data['virtualDirectories']) {
                VirtualDirectory.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['virtualPath'] && !(typeof data['virtualPath'] === 'string' || data['virtualPath'] instanceof String)) {
            throw new Error("Expected the field `virtualPath` to be a primitive type in the JSON string but got " + data['virtualPath']);
        }

        return true;
    }


}



/**
 * @member {String} physicalPath
 */
VirtualApplication.prototype['physicalPath'] = undefined;

/**
 * @member {Boolean} preloadEnabled
 */
VirtualApplication.prototype['preloadEnabled'] = undefined;

/**
 * @member {Array.<module:model/VirtualDirectory>} virtualDirectories
 */
VirtualApplication.prototype['virtualDirectories'] = undefined;

/**
 * @member {String} virtualPath
 */
VirtualApplication.prototype['virtualPath'] = undefined;






export default VirtualApplication;

