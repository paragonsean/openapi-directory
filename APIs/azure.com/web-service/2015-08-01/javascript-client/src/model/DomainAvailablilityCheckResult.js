/**
 * WebSite Management Client
 * Use these APIs to manage Azure Websites resources through the Azure Resource Manager. All task operations conform to the HTTP/1.1 protocol specification and each operation returns an x-ms-request-id header that can be used to obtain information about the request. You must make sure that requests made to these resources are secure. For more information, see https://msdn.microsoft.com/en-us/library/azure/dn790557.aspx.
 *
 * The version of the OpenAPI document: 2015-08-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DomainAvailablilityCheckResult model module.
 * @module model/DomainAvailablilityCheckResult
 * @version 2015-08-01
 */
class DomainAvailablilityCheckResult {
    /**
     * Constructs a new <code>DomainAvailablilityCheckResult</code>.
     * Domain availablility check result
     * @alias module:model/DomainAvailablilityCheckResult
     */
    constructor() { 
        
        DomainAvailablilityCheckResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DomainAvailablilityCheckResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DomainAvailablilityCheckResult} obj Optional instance to populate.
     * @return {module:model/DomainAvailablilityCheckResult} The populated <code>DomainAvailablilityCheckResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DomainAvailablilityCheckResult();

            if (data.hasOwnProperty('available')) {
                obj['available'] = ApiClient.convertToType(data['available'], 'Boolean');
            }
            if (data.hasOwnProperty('domainType')) {
                obj['domainType'] = ApiClient.convertToType(data['domainType'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DomainAvailablilityCheckResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DomainAvailablilityCheckResult</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['domainType'] && !(typeof data['domainType'] === 'string' || data['domainType'] instanceof String)) {
            throw new Error("Expected the field `domainType` to be a primitive type in the JSON string but got " + data['domainType']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}



/**
 * If true then domain can be purchased using CreateDomain Api
 * @member {Boolean} available
 */
DomainAvailablilityCheckResult.prototype['available'] = undefined;

/**
 * Domain type
 * @member {module:model/DomainAvailablilityCheckResult.DomainTypeEnum} domainType
 */
DomainAvailablilityCheckResult.prototype['domainType'] = undefined;

/**
 * Name of the domain
 * @member {String} name
 */
DomainAvailablilityCheckResult.prototype['name'] = undefined;





/**
 * Allowed values for the <code>domainType</code> property.
 * @enum {String}
 * @readonly
 */
DomainAvailablilityCheckResult['DomainTypeEnum'] = {

    /**
     * value: "Regular"
     * @const
     */
    "Regular": "Regular",

    /**
     * value: "SoftDeleted"
     * @const
     */
    "SoftDeleted": "SoftDeleted"
};



export default DomainAvailablilityCheckResult;

