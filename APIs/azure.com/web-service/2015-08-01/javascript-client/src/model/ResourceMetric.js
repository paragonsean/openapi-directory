/**
 * WebSite Management Client
 * Use these APIs to manage Azure Websites resources through the Azure Resource Manager. All task operations conform to the HTTP/1.1 protocol specification and each operation returns an x-ms-request-id header that can be used to obtain information about the request. You must make sure that requests made to these resources are secure. For more information, see https://msdn.microsoft.com/en-us/library/azure/dn790557.aspx.
 *
 * The version of the OpenAPI document: 2015-08-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KeyValuePairStringString from './KeyValuePairStringString';
import ResourceMetricName from './ResourceMetricName';
import ResourceMetricValue from './ResourceMetricValue';

/**
 * The ResourceMetric model module.
 * @module model/ResourceMetric
 * @version 2015-08-01
 */
class ResourceMetric {
    /**
     * Constructs a new <code>ResourceMetric</code>.
     * Object representing a metric for any resource
     * @alias module:model/ResourceMetric
     */
    constructor() { 
        
        ResourceMetric.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ResourceMetric</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ResourceMetric} obj Optional instance to populate.
     * @return {module:model/ResourceMetric} The populated <code>ResourceMetric</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ResourceMetric();

            if (data.hasOwnProperty('endTime')) {
                obj['endTime'] = ApiClient.convertToType(data['endTime'], 'Date');
            }
            if (data.hasOwnProperty('metricValues')) {
                obj['metricValues'] = ApiClient.convertToType(data['metricValues'], [ResourceMetricValue]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ResourceMetricName.constructFromObject(data['name']);
            }
            if (data.hasOwnProperty('properties')) {
                obj['properties'] = ApiClient.convertToType(data['properties'], [KeyValuePairStringString]);
            }
            if (data.hasOwnProperty('resourceId')) {
                obj['resourceId'] = ApiClient.convertToType(data['resourceId'], 'String');
            }
            if (data.hasOwnProperty('startTime')) {
                obj['startTime'] = ApiClient.convertToType(data['startTime'], 'Date');
            }
            if (data.hasOwnProperty('timeGrain')) {
                obj['timeGrain'] = ApiClient.convertToType(data['timeGrain'], 'String');
            }
            if (data.hasOwnProperty('unit')) {
                obj['unit'] = ApiClient.convertToType(data['unit'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ResourceMetric</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ResourceMetric</code>.
     */
    static validateJSON(data) {
        if (data['metricValues']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metricValues'])) {
                throw new Error("Expected the field `metricValues` to be an array in the JSON data but got " + data['metricValues']);
            }
            // validate the optional field `metricValues` (array)
            for (const item of data['metricValues']) {
                ResourceMetricValue.validateJSON(item);
            };
        }
        // validate the optional field `name`
        if (data['name']) { // data not null
          ResourceMetricName.validateJSON(data['name']);
        }
        if (data['properties']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['properties'])) {
                throw new Error("Expected the field `properties` to be an array in the JSON data but got " + data['properties']);
            }
            // validate the optional field `properties` (array)
            for (const item of data['properties']) {
                KeyValuePairStringString.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['resourceId'] && !(typeof data['resourceId'] === 'string' || data['resourceId'] instanceof String)) {
            throw new Error("Expected the field `resourceId` to be a primitive type in the JSON string but got " + data['resourceId']);
        }
        // ensure the json data is a string
        if (data['timeGrain'] && !(typeof data['timeGrain'] === 'string' || data['timeGrain'] instanceof String)) {
            throw new Error("Expected the field `timeGrain` to be a primitive type in the JSON string but got " + data['timeGrain']);
        }
        // ensure the json data is a string
        if (data['unit'] && !(typeof data['unit'] === 'string' || data['unit'] instanceof String)) {
            throw new Error("Expected the field `unit` to be a primitive type in the JSON string but got " + data['unit']);
        }

        return true;
    }


}



/**
 * Metric end time
 * @member {Date} endTime
 */
ResourceMetric.prototype['endTime'] = undefined;

/**
 * Metric values
 * @member {Array.<module:model/ResourceMetricValue>} metricValues
 */
ResourceMetric.prototype['metricValues'] = undefined;

/**
 * @member {module:model/ResourceMetricName} name
 */
ResourceMetric.prototype['name'] = undefined;

/**
 * Properties
 * @member {Array.<module:model/KeyValuePairStringString>} properties
 */
ResourceMetric.prototype['properties'] = undefined;

/**
 * Metric resource Id
 * @member {String} resourceId
 */
ResourceMetric.prototype['resourceId'] = undefined;

/**
 * Metric start time
 * @member {Date} startTime
 */
ResourceMetric.prototype['startTime'] = undefined;

/**
 * Metric granularity. E.g PT1H, PT5M, P1D
 * @member {String} timeGrain
 */
ResourceMetric.prototype['timeGrain'] = undefined;

/**
 * Metric unit
 * @member {String} unit
 */
ResourceMetric.prototype['unit'] = undefined;






export default ResourceMetric;

