# C++ Qt API client

# 

WebSite Management Client

- API version: 2015-08-01
- Generator version: 7.9.0

Use these APIs to manage Azure Websites resources through the Azure Resource Manager. All task operations conform to the HTTP/1.1 protocol specification and each operation returns an x-ms-request-id header that can be used to obtain information about the request. You must make sure that requests made to these resources are secure. For more information, see https://msdn.microsoft.com/en-us/library/azure/dn790557.aspx.


*Automatically generated by the [OpenAPI Generator](https://openapi-generator.tech)*


## Requirements

Building the API client library requires:

1. CMake 3.2+
2. Qt
3. C++ Compiler

## Getting Started

example.h:
```c++

#include <iostream>
#include "../client/OAICertificateOrdersApi.h"

using namespace test_namespace;

class Example : public QObject {
    Q_OBJECT
    QString create();
    QString create();
    QString create();
    QString create();
    QString create();
    OAICertificateOrderCertificate create();
public Q_SLOTS:
   void exampleFunction1();
};

```

example.cpp:
```c++

#include "../client/OAICertificateOrdersApi.h"
#include "example.h"
#include <QTimer>
#include <QEventLoop>

QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
OAICertificateOrderCertificate Example::create(){
    OAICertificateOrderCertificate obj;
 return obj;
}

void Example::exampleFunction1(){
     OAICertificateOrdersApi apiInstance;
     
      //OAuth Authentication supported right now

      QEventLoop loop;
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString resource_group_name = create(); // QString | Azure resource group name

      QEventLoop loop;
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString certificate_order_name = create(); // QString | Certificate name

      QEventLoop loop;
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString name = create(); // QString | Certificate name

      QEventLoop loop;
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString subscription_id = create(); // QString | Subscription Id

      QEventLoop loop;
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString api_version = create(); // QString | API Version

      QEventLoop loop;
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAICertificateOrdersApi::certificateOrdersCreateOrUpdateCertificateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      OAICertificateOrderCertificate key_vault_certificate = create(); // OAICertificateOrderCertificate | Key Vault secret csm Id
      apiInstance.certificateOrdersCreateOrUpdateCertificate(resource_group_namecertificate_order_namenamesubscription_idapi_versionkey_vault_certificate);
      QTimer::singleShot(5000, &loop, &QEventLoop::quit);
      loop.exec();
  }

```

## Documentation for Servers

Parameterized Servers are supported. Define a server in the API for each endpoint with arbitrary numbers of variables:

```yaml
servers:
- url: http://{server}:{port}/{basePath}
  description: Description of the Server
  variables:
    server:
        enum:
          - 'petstore'
          - 'qa-petstore'
          - 'dev-petstore'
        default: 'petstore'
    port:
      enum:
        - '3000'
        - '1000'
      default: '3000'
    basePath:
      default: v1
```
To change the default variable, use this function in each Api:
```c++
int setDefaultServerValue(int serverIndex,const QString &operation, const QString &variable,const QString &val);
```
The parameter "serverIndex" will choose a server from the server list for each endpoint. There is always at least one server with index 0. The Parameter "operation" should be the desired endpoint operationid.
Variable is the name of the variable you wish to change and the value is the new default Value.
The function will return -1 when the variable does not exists, -2 if value is not defined in the variable enum and -3 if the operation is not found.

If your endpoint has multiple server objects in the servers array, you can set the server that will be used with this function:
```c++
void setServerIndex(const QString &operation, int serverIndex);
```
Parameter "operation" should be your operationid. "serverIndex" is the index you want to set as your default server. The function will check if there is a server with your index.
Here is an example of multiple servers in the servers array. The first server will have index 0 and the second will have index 1.
```yaml
servers:
- url: http://{server}:8080/
  description: Description of the Server
  variables:
    server:
        enum:
          - 'petstore'
          - 'qa-petstore'
          - 'dev-petstore'
        default: 'petstore'
- url: https://localhost:8080/v1
```

## Documentation for Authorization

Authentication schemes defined for the API:
### azure_auth


- **Type**: OAuth
- **Flow**: implicit
- **Authorization URL**: https://login.microsoftonline.com/common/oauth2/authorize
- **Scopes**: 
  - user_impersonation: impersonate your user account


## Author




## License

 for more information visit []()