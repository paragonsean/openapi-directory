/**
 * VirtualMachineImageTemplate
 * Virtual Machine Image Template
 *
 * The version of the OpenAPI document: 2018-02-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ImageTemplateSource from './ImageTemplateSource';

/**
 * The ImageTemplatePlatformImageSource model module.
 * @module model/ImageTemplatePlatformImageSource
 * @version 2018-02-01-preview
 */
class ImageTemplatePlatformImageSource {
    /**
     * Constructs a new <code>ImageTemplatePlatformImageSource</code>.
     * Describes an image source from [Azure Gallery Images](https://docs.microsoft.com/en-us/rest/api/compute/virtualmachineimages).
     * @alias module:model/ImageTemplatePlatformImageSource
     * @extends module:model/ImageTemplateSource
     * @implements module:model/ImageTemplateSource
     * @param type {String} Specifies the type of source image you want to start with.
     */
    constructor(type) { 
        ImageTemplateSource.initialize(this, type);
        ImageTemplatePlatformImageSource.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
    }

    /**
     * Constructs a <code>ImageTemplatePlatformImageSource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImageTemplatePlatformImageSource} obj Optional instance to populate.
     * @return {module:model/ImageTemplatePlatformImageSource} The populated <code>ImageTemplatePlatformImageSource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImageTemplatePlatformImageSource();
            ImageTemplateSource.constructFromObject(data, obj);
            ImageTemplateSource.constructFromObject(data, obj);

            if (data.hasOwnProperty('offer')) {
                obj['offer'] = ApiClient.convertToType(data['offer'], 'String');
            }
            if (data.hasOwnProperty('publisher')) {
                obj['publisher'] = ApiClient.convertToType(data['publisher'], 'String');
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImageTemplatePlatformImageSource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImageTemplatePlatformImageSource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ImageTemplatePlatformImageSource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['offer'] && !(typeof data['offer'] === 'string' || data['offer'] instanceof String)) {
            throw new Error("Expected the field `offer` to be a primitive type in the JSON string but got " + data['offer']);
        }
        // ensure the json data is a string
        if (data['publisher'] && !(typeof data['publisher'] === 'string' || data['publisher'] instanceof String)) {
            throw new Error("Expected the field `publisher` to be a primitive type in the JSON string but got " + data['publisher']);
        }
        // ensure the json data is a string
        if (data['sku'] && !(typeof data['sku'] === 'string' || data['sku'] instanceof String)) {
            throw new Error("Expected the field `sku` to be a primitive type in the JSON string but got " + data['sku']);
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }

        return true;
    }


}

ImageTemplatePlatformImageSource.RequiredProperties = ["type"];

/**
 * Image offer from the [Azure Gallery Images](https://docs.microsoft.com/en-us/rest/api/compute/virtualmachineimages).
 * @member {String} offer
 */
ImageTemplatePlatformImageSource.prototype['offer'] = undefined;

/**
 * Image Publisher in [Azure Gallery Images](https://docs.microsoft.com/en-us/rest/api/compute/virtualmachineimages).
 * @member {String} publisher
 */
ImageTemplatePlatformImageSource.prototype['publisher'] = undefined;

/**
 * Image sku from the [Azure Gallery Images](https://docs.microsoft.com/en-us/rest/api/compute/virtualmachineimages).
 * @member {String} sku
 */
ImageTemplatePlatformImageSource.prototype['sku'] = undefined;

/**
 * Image version from the [Azure Gallery Images](https://docs.microsoft.com/en-us/rest/api/compute/virtualmachineimages).
 * @member {String} version
 */
ImageTemplatePlatformImageSource.prototype['version'] = undefined;


// Implement ImageTemplateSource interface:
/**
 * Specifies the type of source image you want to start with.
 * @member {String} type
 */
ImageTemplateSource.prototype['type'] = undefined;




export default ImageTemplatePlatformImageSource;

