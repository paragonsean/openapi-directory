/**
 * DevSpacesManagement
 * Dev Spaces REST API
 *
 * The version of the OpenAPI document: 2019-04-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ControllerProperties from './ControllerProperties';
import Sku from './Sku';
import TrackedResource from './TrackedResource';

/**
 * The Controller model module.
 * @module model/Controller
 * @version 2019-04-01
 */
class Controller {
    /**
     * Constructs a new <code>Controller</code>.
     * @alias module:model/Controller
     * @implements module:model/TrackedResource
     */
    constructor() { 
        TrackedResource.initialize(this);
        Controller.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['properties'] = properties;
        obj['sku'] = sku;
    }

    /**
     * Constructs a <code>Controller</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Controller} obj Optional instance to populate.
     * @return {module:model/Controller} The populated <code>Controller</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Controller();
            TrackedResource.constructFromObject(data, obj);

            if (data.hasOwnProperty('properties')) {
                obj['properties'] = ControllerProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = Sku.constructFromObject(data['sku']);
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Controller</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Controller</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Controller.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          ControllerProperties.validateJSON(data['properties']);
        }
        // validate the optional field `sku`
        if (data['sku']) { // data not null
          Sku.validateJSON(data['sku']);
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

Controller.RequiredProperties = ["properties", "sku"];

/**
 * @member {module:model/ControllerProperties} properties
 */
Controller.prototype['properties'] = undefined;

/**
 * @member {module:model/Sku} sku
 */
Controller.prototype['sku'] = undefined;

/**
 * Region where the Azure resource is located.
 * @member {String} location
 */
Controller.prototype['location'] = undefined;

/**
 * Tags for the Azure resource.
 * @member {Object.<String, String>} tags
 */
Controller.prototype['tags'] = undefined;

/**
 * Fully qualified resource Id for the resource.
 * @member {String} id
 */
Controller.prototype['id'] = undefined;

/**
 * The name of the resource.
 * @member {String} name
 */
Controller.prototype['name'] = undefined;

/**
 * The type of the resource.
 * @member {String} type
 */
Controller.prototype['type'] = undefined;


// Implement TrackedResource interface:
/**
 * Fully qualified resource Id for the resource.
 * @member {String} id
 */
TrackedResource.prototype['id'] = undefined;
/**
 * The name of the resource.
 * @member {String} name
 */
TrackedResource.prototype['name'] = undefined;
/**
 * The type of the resource.
 * @member {String} type
 */
TrackedResource.prototype['type'] = undefined;




export default Controller;

