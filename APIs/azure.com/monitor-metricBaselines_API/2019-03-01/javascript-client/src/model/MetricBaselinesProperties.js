/**
 * MonitorManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TimeSeriesBaseline from './TimeSeriesBaseline';

/**
 * The MetricBaselinesProperties model module.
 * @module model/MetricBaselinesProperties
 * @version 2019-03-01
 */
class MetricBaselinesProperties {
    /**
     * Constructs a new <code>MetricBaselinesProperties</code>.
     * The response to a metric baselines query.
     * @alias module:model/MetricBaselinesProperties
     * @param baselines {Array.<module:model/TimeSeriesBaseline>} The baseline for each time series that was queried.
     * @param interval {String} The interval (window size) for which the metric data was returned in.  This may be adjusted in the future and returned back from what was originally requested.  This is not present if a metadata request was made.
     * @param timespan {String} The timespan for which the data was retrieved. Its value consists of two datetimes concatenated, separated by '/'.  This may be adjusted in the future and returned back from what was originally requested.
     */
    constructor(baselines, interval, timespan) { 
        
        MetricBaselinesProperties.initialize(this, baselines, interval, timespan);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, baselines, interval, timespan) { 
        obj['baselines'] = baselines;
        obj['interval'] = interval;
        obj['timespan'] = timespan;
    }

    /**
     * Constructs a <code>MetricBaselinesProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MetricBaselinesProperties} obj Optional instance to populate.
     * @return {module:model/MetricBaselinesProperties} The populated <code>MetricBaselinesProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MetricBaselinesProperties();

            if (data.hasOwnProperty('baselines')) {
                obj['baselines'] = ApiClient.convertToType(data['baselines'], [TimeSeriesBaseline]);
            }
            if (data.hasOwnProperty('interval')) {
                obj['interval'] = ApiClient.convertToType(data['interval'], 'String');
            }
            if (data.hasOwnProperty('namespace')) {
                obj['namespace'] = ApiClient.convertToType(data['namespace'], 'String');
            }
            if (data.hasOwnProperty('timespan')) {
                obj['timespan'] = ApiClient.convertToType(data['timespan'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MetricBaselinesProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MetricBaselinesProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MetricBaselinesProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['baselines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['baselines'])) {
                throw new Error("Expected the field `baselines` to be an array in the JSON data but got " + data['baselines']);
            }
            // validate the optional field `baselines` (array)
            for (const item of data['baselines']) {
                TimeSeriesBaseline.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['interval'] && !(typeof data['interval'] === 'string' || data['interval'] instanceof String)) {
            throw new Error("Expected the field `interval` to be a primitive type in the JSON string but got " + data['interval']);
        }
        // ensure the json data is a string
        if (data['namespace'] && !(typeof data['namespace'] === 'string' || data['namespace'] instanceof String)) {
            throw new Error("Expected the field `namespace` to be a primitive type in the JSON string but got " + data['namespace']);
        }
        // ensure the json data is a string
        if (data['timespan'] && !(typeof data['timespan'] === 'string' || data['timespan'] instanceof String)) {
            throw new Error("Expected the field `timespan` to be a primitive type in the JSON string but got " + data['timespan']);
        }

        return true;
    }


}

MetricBaselinesProperties.RequiredProperties = ["baselines", "interval", "timespan"];

/**
 * The baseline for each time series that was queried.
 * @member {Array.<module:model/TimeSeriesBaseline>} baselines
 */
MetricBaselinesProperties.prototype['baselines'] = undefined;

/**
 * The interval (window size) for which the metric data was returned in.  This may be adjusted in the future and returned back from what was originally requested.  This is not present if a metadata request was made.
 * @member {String} interval
 */
MetricBaselinesProperties.prototype['interval'] = undefined;

/**
 * The namespace of the metrics been queried.
 * @member {String} namespace
 */
MetricBaselinesProperties.prototype['namespace'] = undefined;

/**
 * The timespan for which the data was retrieved. Its value consists of two datetimes concatenated, separated by '/'.  This may be adjusted in the future and returned back from what was originally requested.
 * @member {String} timespan
 */
MetricBaselinesProperties.prototype['timespan'] = undefined;






export default MetricBaselinesProperties;

