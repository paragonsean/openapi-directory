/**
 * Azure Log Analytics
 * This API exposes Azure Log Analytics query capabilities
 *
 * The version of the OpenAPI document: 2017-10-01
 * Contact: aiapi@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The QueryBody model module.
 * @module model/QueryBody
 * @version 2017-10-01
 */
class QueryBody {
    /**
     * Constructs a new <code>QueryBody</code>.
     * The Analytics query. Learn more about the [Analytics query syntax](https://azure.microsoft.com/documentation/articles/app-insights-analytics-reference/)
     * @alias module:model/QueryBody
     * @param query {String} The Analytics query. Learn more about the [Analytics query syntax](https://azure.microsoft.com/documentation/articles/app-insights-analytics-reference/)
     */
    constructor(query) { 
        
        QueryBody.initialize(this, query);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, query) { 
        obj['query'] = query;
    }

    /**
     * Constructs a <code>QueryBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QueryBody} obj Optional instance to populate.
     * @return {module:model/QueryBody} The populated <code>QueryBody</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QueryBody();

            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('timespan')) {
                obj['timespan'] = ApiClient.convertToType(data['timespan'], 'String');
            }
            if (data.hasOwnProperty('workspaces')) {
                obj['workspaces'] = ApiClient.convertToType(data['workspaces'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QueryBody</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QueryBody</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of QueryBody.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['timespan'] && !(typeof data['timespan'] === 'string' || data['timespan'] instanceof String)) {
            throw new Error("Expected the field `timespan` to be a primitive type in the JSON string but got " + data['timespan']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['workspaces'])) {
            throw new Error("Expected the field `workspaces` to be an array in the JSON data but got " + data['workspaces']);
        }

        return true;
    }


}

QueryBody.RequiredProperties = ["query"];

/**
 * The Analytics query. Learn more about the [Analytics query syntax](https://azure.microsoft.com/documentation/articles/app-insights-analytics-reference/)
 * @member {String} query
 */
QueryBody.prototype['query'] = undefined;

/**
 * Optional. The timespan over which to query data. This is an ISO8601 time period value.  This timespan is applied in addition to any that are specified in the query expression.
 * @member {String} timespan
 */
QueryBody.prototype['timespan'] = undefined;

/**
 * Workspace IDs to include in cross-workspace queries.
 * @member {Array.<String>} workspaces
 */
QueryBody.prototype['workspaces'] = undefined;






export default QueryBody;

