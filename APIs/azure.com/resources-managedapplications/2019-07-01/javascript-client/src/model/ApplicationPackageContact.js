/**
 * ApplicationClient
 * ARM applications
 *
 * The version of the OpenAPI document: 2019-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApplicationPackageContact model module.
 * @module model/ApplicationPackageContact
 * @version 2019-07-01
 */
class ApplicationPackageContact {
    /**
     * Constructs a new <code>ApplicationPackageContact</code>.
     * The application package contact information.
     * @alias module:model/ApplicationPackageContact
     * @param email {String} The contact email.
     * @param phone {String} The contact phone number.
     */
    constructor(email, phone) { 
        
        ApplicationPackageContact.initialize(this, email, phone);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, phone) { 
        obj['email'] = email;
        obj['phone'] = phone;
    }

    /**
     * Constructs a <code>ApplicationPackageContact</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApplicationPackageContact} obj Optional instance to populate.
     * @return {module:model/ApplicationPackageContact} The populated <code>ApplicationPackageContact</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApplicationPackageContact();

            if (data.hasOwnProperty('contactName')) {
                obj['contactName'] = ApiClient.convertToType(data['contactName'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('phone')) {
                obj['phone'] = ApiClient.convertToType(data['phone'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApplicationPackageContact</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApplicationPackageContact</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApplicationPackageContact.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['contactName'] && !(typeof data['contactName'] === 'string' || data['contactName'] instanceof String)) {
            throw new Error("Expected the field `contactName` to be a primitive type in the JSON string but got " + data['contactName']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['phone'] && !(typeof data['phone'] === 'string' || data['phone'] instanceof String)) {
            throw new Error("Expected the field `phone` to be a primitive type in the JSON string but got " + data['phone']);
        }

        return true;
    }


}

ApplicationPackageContact.RequiredProperties = ["email", "phone"];

/**
 * The contact name.
 * @member {String} contactName
 */
ApplicationPackageContact.prototype['contactName'] = undefined;

/**
 * The contact email.
 * @member {String} email
 */
ApplicationPackageContact.prototype['email'] = undefined;

/**
 * The contact phone number.
 * @member {String} phone
 */
ApplicationPackageContact.prototype['phone'] = undefined;






export default ApplicationPackageContact;

