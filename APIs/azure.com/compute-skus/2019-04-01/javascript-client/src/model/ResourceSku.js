/**
 * ComputeManagementClient
 * The Compute Management Client.
 *
 * The version of the OpenAPI document: 2019-04-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ResourceSkuCapabilities from './ResourceSkuCapabilities';
import ResourceSkuCapacity from './ResourceSkuCapacity';
import ResourceSkuCosts from './ResourceSkuCosts';
import ResourceSkuLocationInfo from './ResourceSkuLocationInfo';
import ResourceSkuRestrictions from './ResourceSkuRestrictions';

/**
 * The ResourceSku model module.
 * @module model/ResourceSku
 * @version 2019-04-01
 */
class ResourceSku {
    /**
     * Constructs a new <code>ResourceSku</code>.
     * Describes an available Compute SKU.
     * @alias module:model/ResourceSku
     */
    constructor() { 
        
        ResourceSku.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ResourceSku</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ResourceSku} obj Optional instance to populate.
     * @return {module:model/ResourceSku} The populated <code>ResourceSku</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ResourceSku();

            if (data.hasOwnProperty('apiVersions')) {
                obj['apiVersions'] = ApiClient.convertToType(data['apiVersions'], ['String']);
            }
            if (data.hasOwnProperty('capabilities')) {
                obj['capabilities'] = ApiClient.convertToType(data['capabilities'], [ResourceSkuCapabilities]);
            }
            if (data.hasOwnProperty('capacity')) {
                obj['capacity'] = ResourceSkuCapacity.constructFromObject(data['capacity']);
            }
            if (data.hasOwnProperty('costs')) {
                obj['costs'] = ApiClient.convertToType(data['costs'], [ResourceSkuCosts]);
            }
            if (data.hasOwnProperty('family')) {
                obj['family'] = ApiClient.convertToType(data['family'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('locationInfo')) {
                obj['locationInfo'] = ApiClient.convertToType(data['locationInfo'], [ResourceSkuLocationInfo]);
            }
            if (data.hasOwnProperty('locations')) {
                obj['locations'] = ApiClient.convertToType(data['locations'], ['String']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('resourceType')) {
                obj['resourceType'] = ApiClient.convertToType(data['resourceType'], 'String');
            }
            if (data.hasOwnProperty('restrictions')) {
                obj['restrictions'] = ApiClient.convertToType(data['restrictions'], [ResourceSkuRestrictions]);
            }
            if (data.hasOwnProperty('size')) {
                obj['size'] = ApiClient.convertToType(data['size'], 'String');
            }
            if (data.hasOwnProperty('tier')) {
                obj['tier'] = ApiClient.convertToType(data['tier'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ResourceSku</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ResourceSku</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['apiVersions'])) {
            throw new Error("Expected the field `apiVersions` to be an array in the JSON data but got " + data['apiVersions']);
        }
        if (data['capabilities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['capabilities'])) {
                throw new Error("Expected the field `capabilities` to be an array in the JSON data but got " + data['capabilities']);
            }
            // validate the optional field `capabilities` (array)
            for (const item of data['capabilities']) {
                ResourceSkuCapabilities.validateJSON(item);
            };
        }
        // validate the optional field `capacity`
        if (data['capacity']) { // data not null
          ResourceSkuCapacity.validateJSON(data['capacity']);
        }
        if (data['costs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['costs'])) {
                throw new Error("Expected the field `costs` to be an array in the JSON data but got " + data['costs']);
            }
            // validate the optional field `costs` (array)
            for (const item of data['costs']) {
                ResourceSkuCosts.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['family'] && !(typeof data['family'] === 'string' || data['family'] instanceof String)) {
            throw new Error("Expected the field `family` to be a primitive type in the JSON string but got " + data['family']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        if (data['locationInfo']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['locationInfo'])) {
                throw new Error("Expected the field `locationInfo` to be an array in the JSON data but got " + data['locationInfo']);
            }
            // validate the optional field `locationInfo` (array)
            for (const item of data['locationInfo']) {
                ResourceSkuLocationInfo.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['locations'])) {
            throw new Error("Expected the field `locations` to be an array in the JSON data but got " + data['locations']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['resourceType'] && !(typeof data['resourceType'] === 'string' || data['resourceType'] instanceof String)) {
            throw new Error("Expected the field `resourceType` to be a primitive type in the JSON string but got " + data['resourceType']);
        }
        if (data['restrictions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['restrictions'])) {
                throw new Error("Expected the field `restrictions` to be an array in the JSON data but got " + data['restrictions']);
            }
            // validate the optional field `restrictions` (array)
            for (const item of data['restrictions']) {
                ResourceSkuRestrictions.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['size'] && !(typeof data['size'] === 'string' || data['size'] instanceof String)) {
            throw new Error("Expected the field `size` to be a primitive type in the JSON string but got " + data['size']);
        }
        // ensure the json data is a string
        if (data['tier'] && !(typeof data['tier'] === 'string' || data['tier'] instanceof String)) {
            throw new Error("Expected the field `tier` to be a primitive type in the JSON string but got " + data['tier']);
        }

        return true;
    }


}



/**
 * The api versions that support this SKU.
 * @member {Array.<String>} apiVersions
 */
ResourceSku.prototype['apiVersions'] = undefined;

/**
 * A name value pair to describe the capability.
 * @member {Array.<module:model/ResourceSkuCapabilities>} capabilities
 */
ResourceSku.prototype['capabilities'] = undefined;

/**
 * @member {module:model/ResourceSkuCapacity} capacity
 */
ResourceSku.prototype['capacity'] = undefined;

/**
 * Metadata for retrieving price info.
 * @member {Array.<module:model/ResourceSkuCosts>} costs
 */
ResourceSku.prototype['costs'] = undefined;

/**
 * The Family of this particular SKU.
 * @member {String} family
 */
ResourceSku.prototype['family'] = undefined;

/**
 * The Kind of resources that are supported in this SKU.
 * @member {String} kind
 */
ResourceSku.prototype['kind'] = undefined;

/**
 * A list of locations and availability zones in those locations where the SKU is available.
 * @member {Array.<module:model/ResourceSkuLocationInfo>} locationInfo
 */
ResourceSku.prototype['locationInfo'] = undefined;

/**
 * The set of locations that the SKU is available.
 * @member {Array.<String>} locations
 */
ResourceSku.prototype['locations'] = undefined;

/**
 * The name of SKU.
 * @member {String} name
 */
ResourceSku.prototype['name'] = undefined;

/**
 * The type of resource the SKU applies to.
 * @member {String} resourceType
 */
ResourceSku.prototype['resourceType'] = undefined;

/**
 * The restrictions because of which SKU cannot be used. This is empty if there are no restrictions.
 * @member {Array.<module:model/ResourceSkuRestrictions>} restrictions
 */
ResourceSku.prototype['restrictions'] = undefined;

/**
 * The Size of the SKU.
 * @member {String} size
 */
ResourceSku.prototype['size'] = undefined;

/**
 * Specifies the tier of virtual machines in a scale set.<br /><br /> Possible Values:<br /><br /> **Standard**<br /><br /> **Basic**
 * @member {String} tier
 */
ResourceSku.prototype['tier'] = undefined;






export default ResourceSku;

