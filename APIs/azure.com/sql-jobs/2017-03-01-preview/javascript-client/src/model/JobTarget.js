/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2017-03-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The JobTarget model module.
 * @module model/JobTarget
 * @version 2017-03-01-preview
 */
class JobTarget {
    /**
     * Constructs a new <code>JobTarget</code>.
     * A job target, for example a specific database or a container of databases that is evaluated during job execution.
     * @alias module:model/JobTarget
     * @param type {module:model/JobTarget.TypeEnum} The target type.
     */
    constructor(type) { 
        
        JobTarget.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['membershipType'] = 'Include';
        obj['type'] = type;
    }

    /**
     * Constructs a <code>JobTarget</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobTarget} obj Optional instance to populate.
     * @return {module:model/JobTarget} The populated <code>JobTarget</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobTarget();

            if (data.hasOwnProperty('databaseName')) {
                obj['databaseName'] = ApiClient.convertToType(data['databaseName'], 'String');
            }
            if (data.hasOwnProperty('elasticPoolName')) {
                obj['elasticPoolName'] = ApiClient.convertToType(data['elasticPoolName'], 'String');
            }
            if (data.hasOwnProperty('membershipType')) {
                obj['membershipType'] = ApiClient.convertToType(data['membershipType'], 'String');
            }
            if (data.hasOwnProperty('refreshCredential')) {
                obj['refreshCredential'] = ApiClient.convertToType(data['refreshCredential'], 'String');
            }
            if (data.hasOwnProperty('serverName')) {
                obj['serverName'] = ApiClient.convertToType(data['serverName'], 'String');
            }
            if (data.hasOwnProperty('shardMapName')) {
                obj['shardMapName'] = ApiClient.convertToType(data['shardMapName'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobTarget</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobTarget</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobTarget.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['databaseName'] && !(typeof data['databaseName'] === 'string' || data['databaseName'] instanceof String)) {
            throw new Error("Expected the field `databaseName` to be a primitive type in the JSON string but got " + data['databaseName']);
        }
        // ensure the json data is a string
        if (data['elasticPoolName'] && !(typeof data['elasticPoolName'] === 'string' || data['elasticPoolName'] instanceof String)) {
            throw new Error("Expected the field `elasticPoolName` to be a primitive type in the JSON string but got " + data['elasticPoolName']);
        }
        // ensure the json data is a string
        if (data['membershipType'] && !(typeof data['membershipType'] === 'string' || data['membershipType'] instanceof String)) {
            throw new Error("Expected the field `membershipType` to be a primitive type in the JSON string but got " + data['membershipType']);
        }
        // ensure the json data is a string
        if (data['refreshCredential'] && !(typeof data['refreshCredential'] === 'string' || data['refreshCredential'] instanceof String)) {
            throw new Error("Expected the field `refreshCredential` to be a primitive type in the JSON string but got " + data['refreshCredential']);
        }
        // ensure the json data is a string
        if (data['serverName'] && !(typeof data['serverName'] === 'string' || data['serverName'] instanceof String)) {
            throw new Error("Expected the field `serverName` to be a primitive type in the JSON string but got " + data['serverName']);
        }
        // ensure the json data is a string
        if (data['shardMapName'] && !(typeof data['shardMapName'] === 'string' || data['shardMapName'] instanceof String)) {
            throw new Error("Expected the field `shardMapName` to be a primitive type in the JSON string but got " + data['shardMapName']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

JobTarget.RequiredProperties = ["type"];

/**
 * The target database name.
 * @member {String} databaseName
 */
JobTarget.prototype['databaseName'] = undefined;

/**
 * The target elastic pool name.
 * @member {String} elasticPoolName
 */
JobTarget.prototype['elasticPoolName'] = undefined;

/**
 * Whether the target is included or excluded from the group.
 * @member {module:model/JobTarget.MembershipTypeEnum} membershipType
 * @default 'Include'
 */
JobTarget.prototype['membershipType'] = 'Include';

/**
 * The resource ID of the credential that is used during job execution to connect to the target and determine the list of databases inside the target.
 * @member {String} refreshCredential
 */
JobTarget.prototype['refreshCredential'] = undefined;

/**
 * The target server name.
 * @member {String} serverName
 */
JobTarget.prototype['serverName'] = undefined;

/**
 * The target shard map.
 * @member {String} shardMapName
 */
JobTarget.prototype['shardMapName'] = undefined;

/**
 * The target type.
 * @member {module:model/JobTarget.TypeEnum} type
 */
JobTarget.prototype['type'] = undefined;





/**
 * Allowed values for the <code>membershipType</code> property.
 * @enum {String}
 * @readonly
 */
JobTarget['MembershipTypeEnum'] = {

    /**
     * value: "Include"
     * @const
     */
    "Include": "Include",

    /**
     * value: "Exclude"
     * @const
     */
    "Exclude": "Exclude"
};


/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
JobTarget['TypeEnum'] = {

    /**
     * value: "TargetGroup"
     * @const
     */
    "TargetGroup": "TargetGroup",

    /**
     * value: "SqlDatabase"
     * @const
     */
    "SqlDatabase": "SqlDatabase",

    /**
     * value: "SqlElasticPool"
     * @const
     */
    "SqlElasticPool": "SqlElasticPool",

    /**
     * value: "SqlShardMap"
     * @const
     */
    "SqlShardMap": "SqlShardMap",

    /**
     * value: "SqlServer"
     * @const
     */
    "SqlServer": "SqlServer"
};



export default JobTarget;

