/**
 * ServiceFabricManagementClient
 * Azure Service Fabric Resource Provider API Client
 *
 * The version of the OpenAPI document: 2019-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PartitionScheme from './PartitionScheme';
import PartitionSchemeDescription from './PartitionSchemeDescription';

/**
 * The UniformInt64RangePartitionSchemeDescription model module.
 * @module model/UniformInt64RangePartitionSchemeDescription
 * @version 2019-03-01
 */
class UniformInt64RangePartitionSchemeDescription {
    /**
     * Constructs a new <code>UniformInt64RangePartitionSchemeDescription</code>.
     * Describes a partitioning scheme where an integer range is allocated evenly across a number of partitions.
     * @alias module:model/UniformInt64RangePartitionSchemeDescription
     * @extends module:model/PartitionSchemeDescription
     * @implements module:model/PartitionSchemeDescription
     * @param partitionScheme {module:model/PartitionScheme} 
     */
    constructor(partitionScheme) { 
        PartitionSchemeDescription.initialize(this, partitionScheme);
        UniformInt64RangePartitionSchemeDescription.initialize(this, partitionScheme);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, partitionScheme) { 
        obj['Count'] = count;
        obj['HighKey'] = highKey;
        obj['LowKey'] = lowKey;
    }

    /**
     * Constructs a <code>UniformInt64RangePartitionSchemeDescription</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UniformInt64RangePartitionSchemeDescription} obj Optional instance to populate.
     * @return {module:model/UniformInt64RangePartitionSchemeDescription} The populated <code>UniformInt64RangePartitionSchemeDescription</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UniformInt64RangePartitionSchemeDescription();
            PartitionSchemeDescription.constructFromObject(data, obj);
            PartitionSchemeDescription.constructFromObject(data, obj);

            if (data.hasOwnProperty('Count')) {
                obj['Count'] = ApiClient.convertToType(data['Count'], 'Number');
            }
            if (data.hasOwnProperty('HighKey')) {
                obj['HighKey'] = ApiClient.convertToType(data['HighKey'], 'String');
            }
            if (data.hasOwnProperty('LowKey')) {
                obj['LowKey'] = ApiClient.convertToType(data['LowKey'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UniformInt64RangePartitionSchemeDescription</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UniformInt64RangePartitionSchemeDescription</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UniformInt64RangePartitionSchemeDescription.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['HighKey'] && !(typeof data['HighKey'] === 'string' || data['HighKey'] instanceof String)) {
            throw new Error("Expected the field `HighKey` to be a primitive type in the JSON string but got " + data['HighKey']);
        }
        // ensure the json data is a string
        if (data['LowKey'] && !(typeof data['LowKey'] === 'string' || data['LowKey'] instanceof String)) {
            throw new Error("Expected the field `LowKey` to be a primitive type in the JSON string but got " + data['LowKey']);
        }

        return true;
    }


}

UniformInt64RangePartitionSchemeDescription.RequiredProperties = ["Count", "HighKey", "LowKey", "partitionScheme"];

/**
 * The number of partitions.
 * @member {Number} Count
 */
UniformInt64RangePartitionSchemeDescription.prototype['Count'] = undefined;

/**
 * String indicating the upper bound of the partition key range that should be split between the partition ‘Count’ 
 * @member {String} HighKey
 */
UniformInt64RangePartitionSchemeDescription.prototype['HighKey'] = undefined;

/**
 * String indicating the lower bound of the partition key range that should be split between the partition ‘Count’ 
 * @member {String} LowKey
 */
UniformInt64RangePartitionSchemeDescription.prototype['LowKey'] = undefined;


// Implement PartitionSchemeDescription interface:
/**
 * @member {module:model/PartitionScheme} partitionScheme
 */
PartitionSchemeDescription.prototype['partitionScheme'] = undefined;




export default UniformInt64RangePartitionSchemeDescription;

