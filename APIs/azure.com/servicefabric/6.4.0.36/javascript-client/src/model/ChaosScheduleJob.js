/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 6.4.0.36
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChaosScheduleJobActiveDaysOfWeek from './ChaosScheduleJobActiveDaysOfWeek';
import TimeRange from './TimeRange';

/**
 * The ChaosScheduleJob model module.
 * @module model/ChaosScheduleJob
 * @version 6.4.0.36
 */
class ChaosScheduleJob {
    /**
     * Constructs a new <code>ChaosScheduleJob</code>.
     * Defines a repetition rule and parameters of Chaos to be used with the Chaos Schedule.
     * @alias module:model/ChaosScheduleJob
     */
    constructor() { 
        
        ChaosScheduleJob.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ChaosScheduleJob</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChaosScheduleJob} obj Optional instance to populate.
     * @return {module:model/ChaosScheduleJob} The populated <code>ChaosScheduleJob</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChaosScheduleJob();

            if (data.hasOwnProperty('ChaosParameters')) {
                obj['ChaosParameters'] = ApiClient.convertToType(data['ChaosParameters'], 'String');
            }
            if (data.hasOwnProperty('Days')) {
                obj['Days'] = ChaosScheduleJobActiveDaysOfWeek.constructFromObject(data['Days']);
            }
            if (data.hasOwnProperty('Times')) {
                obj['Times'] = ApiClient.convertToType(data['Times'], [TimeRange]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChaosScheduleJob</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChaosScheduleJob</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['ChaosParameters'] && !(typeof data['ChaosParameters'] === 'string' || data['ChaosParameters'] instanceof String)) {
            throw new Error("Expected the field `ChaosParameters` to be a primitive type in the JSON string but got " + data['ChaosParameters']);
        }
        // validate the optional field `Days`
        if (data['Days']) { // data not null
          ChaosScheduleJobActiveDaysOfWeek.validateJSON(data['Days']);
        }
        if (data['Times']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Times'])) {
                throw new Error("Expected the field `Times` to be an array in the JSON data but got " + data['Times']);
            }
            // validate the optional field `Times` (array)
            for (const item of data['Times']) {
                TimeRange.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * A reference to which Chaos Parameters of the Chaos Schedule to use.
 * @member {String} ChaosParameters
 */
ChaosScheduleJob.prototype['ChaosParameters'] = undefined;

/**
 * @member {module:model/ChaosScheduleJobActiveDaysOfWeek} Days
 */
ChaosScheduleJob.prototype['Days'] = undefined;

/**
 * A list of Time Ranges that specify when during active days that this job will run. The times are interpreted as UTC.
 * @member {Array.<module:model/TimeRange>} Times
 */
ChaosScheduleJob.prototype['Times'] = undefined;






export default ChaosScheduleJob;

