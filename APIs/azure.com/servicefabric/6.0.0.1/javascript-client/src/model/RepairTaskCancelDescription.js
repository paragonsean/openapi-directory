/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 6.0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RepairTaskCancelDescription model module.
 * @module model/RepairTaskCancelDescription
 * @version 6.0.0.1
 */
class RepairTaskCancelDescription {
    /**
     * Constructs a new <code>RepairTaskCancelDescription</code>.
     * Describes a request to cancel a repair task.  This type supports the Service Fabric platform; it is not meant to be used directly from your code. 
     * @alias module:model/RepairTaskCancelDescription
     * @param taskId {String} The ID of the repair task.
     */
    constructor(taskId) { 
        
        RepairTaskCancelDescription.initialize(this, taskId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, taskId) { 
        obj['TaskId'] = taskId;
    }

    /**
     * Constructs a <code>RepairTaskCancelDescription</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RepairTaskCancelDescription} obj Optional instance to populate.
     * @return {module:model/RepairTaskCancelDescription} The populated <code>RepairTaskCancelDescription</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RepairTaskCancelDescription();

            if (data.hasOwnProperty('RequestAbort')) {
                obj['RequestAbort'] = ApiClient.convertToType(data['RequestAbort'], 'Boolean');
            }
            if (data.hasOwnProperty('TaskId')) {
                obj['TaskId'] = ApiClient.convertToType(data['TaskId'], 'String');
            }
            if (data.hasOwnProperty('Version')) {
                obj['Version'] = ApiClient.convertToType(data['Version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RepairTaskCancelDescription</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RepairTaskCancelDescription</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RepairTaskCancelDescription.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['TaskId'] && !(typeof data['TaskId'] === 'string' || data['TaskId'] instanceof String)) {
            throw new Error("Expected the field `TaskId` to be a primitive type in the JSON string but got " + data['TaskId']);
        }
        // ensure the json data is a string
        if (data['Version'] && !(typeof data['Version'] === 'string' || data['Version'] instanceof String)) {
            throw new Error("Expected the field `Version` to be a primitive type in the JSON string but got " + data['Version']);
        }

        return true;
    }


}

RepairTaskCancelDescription.RequiredProperties = ["TaskId"];

/**
 * _True_ if the repair should be stopped as soon as possible even if it has already started executing. _False_ if the repair should be cancelled only if execution has not yet started.</para>
 * @member {Boolean} RequestAbort
 */
RepairTaskCancelDescription.prototype['RequestAbort'] = undefined;

/**
 * The ID of the repair task.
 * @member {String} TaskId
 */
RepairTaskCancelDescription.prototype['TaskId'] = undefined;

/**
 * The current version number of the repair task. If non-zero, then the request will only succeed if this value matches the actual current version of the repair task. If zero, then no version check is performed.</para>
 * @member {String} Version
 */
RepairTaskCancelDescription.prototype['Version'] = undefined;






export default RepairTaskCancelDescription;

