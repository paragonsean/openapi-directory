/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 6.0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChaosEventWrapper from './ChaosEventWrapper';
import ChaosParameters from './ChaosParameters';

/**
 * The ChaosReport model module.
 * @module model/ChaosReport
 * @version 6.0.0.1
 */
class ChaosReport {
    /**
     * Constructs a new <code>ChaosReport</code>.
     * Contains detailed Chaos report. 
     * @alias module:model/ChaosReport
     */
    constructor() { 
        
        ChaosReport.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ChaosReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChaosReport} obj Optional instance to populate.
     * @return {module:model/ChaosReport} The populated <code>ChaosReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChaosReport();

            if (data.hasOwnProperty('ChaosParameters')) {
                obj['ChaosParameters'] = ChaosParameters.constructFromObject(data['ChaosParameters']);
            }
            if (data.hasOwnProperty('ContinuationToken')) {
                obj['ContinuationToken'] = ApiClient.convertToType(data['ContinuationToken'], 'String');
            }
            if (data.hasOwnProperty('History')) {
                obj['History'] = ApiClient.convertToType(data['History'], [ChaosEventWrapper]);
            }
            if (data.hasOwnProperty('Status')) {
                obj['Status'] = ApiClient.convertToType(data['Status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChaosReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChaosReport</code>.
     */
    static validateJSON(data) {
        // validate the optional field `ChaosParameters`
        if (data['ChaosParameters']) { // data not null
          ChaosParameters.validateJSON(data['ChaosParameters']);
        }
        // ensure the json data is a string
        if (data['ContinuationToken'] && !(typeof data['ContinuationToken'] === 'string' || data['ContinuationToken'] instanceof String)) {
            throw new Error("Expected the field `ContinuationToken` to be a primitive type in the JSON string but got " + data['ContinuationToken']);
        }
        if (data['History']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['History'])) {
                throw new Error("Expected the field `History` to be an array in the JSON data but got " + data['History']);
            }
            // validate the optional field `History` (array)
            for (const item of data['History']) {
                ChaosEventWrapper.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['Status'] && !(typeof data['Status'] === 'string' || data['Status'] instanceof String)) {
            throw new Error("Expected the field `Status` to be a primitive type in the JSON string but got " + data['Status']);
        }

        return true;
    }


}



/**
 * @member {module:model/ChaosParameters} ChaosParameters
 */
ChaosReport.prototype['ChaosParameters'] = undefined;

/**
 * The continuation token parameter is used to obtain next set of results. The continuation token is included in the response of the API when the results from the system do not fit in a single response. When this value is passed to the next API call, the API returns next set of results. If there are no further results then the continuation token is not included in the response.
 * @member {String} ContinuationToken
 */
ChaosReport.prototype['ContinuationToken'] = undefined;

/**
 * @member {Array.<module:model/ChaosEventWrapper>} History
 */
ChaosReport.prototype['History'] = undefined;

/**
 * Current status of the Chaos run.  - Invalid - Indicates an invalid Chaos status. All Service Fabric enumerations have the invalid type.   The valus is zero. - Running - Indicates that Chaos is not stopped. - Stopped - Indicates that Chaos is not scheduling futher faults.
 * @member {module:model/ChaosReport.StatusEnum} Status
 */
ChaosReport.prototype['Status'] = undefined;





/**
 * Allowed values for the <code>Status</code> property.
 * @enum {String}
 * @readonly
 */
ChaosReport['StatusEnum'] = {

    /**
     * value: "Invalid"
     * @const
     */
    "Invalid": "Invalid",

    /**
     * value: "Running"
     * @const
     */
    "Running": "Running",

    /**
     * value: "Stopped"
     * @const
     */
    "Stopped": "Stopped"
};



export default ChaosReport;

