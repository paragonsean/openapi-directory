/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 6.0.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PartitionInformation from './PartitionInformation';
import ServicePartitionKind from './ServicePartitionKind';

/**
 * The Int64RangePartitionInformation model module.
 * @module model/Int64RangePartitionInformation
 * @version 6.0.0.1
 */
class Int64RangePartitionInformation {
    /**
     * Constructs a new <code>Int64RangePartitionInformation</code>.
     * Describes the partition information for the integer range that is based on partition schemes.
     * @alias module:model/Int64RangePartitionInformation
     * @extends module:model/PartitionInformation
     * @implements module:model/PartitionInformation
     * @param servicePartitionKind {module:model/ServicePartitionKind} 
     */
    constructor(servicePartitionKind) { 
        PartitionInformation.initialize(this, servicePartitionKind);
        Int64RangePartitionInformation.initialize(this, servicePartitionKind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, servicePartitionKind) { 
    }

    /**
     * Constructs a <code>Int64RangePartitionInformation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Int64RangePartitionInformation} obj Optional instance to populate.
     * @return {module:model/Int64RangePartitionInformation} The populated <code>Int64RangePartitionInformation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Int64RangePartitionInformation();
            PartitionInformation.constructFromObject(data, obj);
            PartitionInformation.constructFromObject(data, obj);

            if (data.hasOwnProperty('HighKey')) {
                obj['HighKey'] = ApiClient.convertToType(data['HighKey'], 'String');
            }
            if (data.hasOwnProperty('LowKey')) {
                obj['LowKey'] = ApiClient.convertToType(data['LowKey'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Int64RangePartitionInformation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Int64RangePartitionInformation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Int64RangePartitionInformation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['HighKey'] && !(typeof data['HighKey'] === 'string' || data['HighKey'] instanceof String)) {
            throw new Error("Expected the field `HighKey` to be a primitive type in the JSON string but got " + data['HighKey']);
        }
        // ensure the json data is a string
        if (data['LowKey'] && !(typeof data['LowKey'] === 'string' || data['LowKey'] instanceof String)) {
            throw new Error("Expected the field `LowKey` to be a primitive type in the JSON string but got " + data['LowKey']);
        }

        return true;
    }


}

Int64RangePartitionInformation.RequiredProperties = ["ServicePartitionKind"];

/**
 * Specifies the maximum key value handled by this partition.
 * @member {String} HighKey
 */
Int64RangePartitionInformation.prototype['HighKey'] = undefined;

/**
 * Specifies the minimum key value handled by this partition.
 * @member {String} LowKey
 */
Int64RangePartitionInformation.prototype['LowKey'] = undefined;


// Implement PartitionInformation interface:
/**
 * An internal ID used by Service Fabric to uniquely identify a partition. This is a randomly generated GUID when the service was created. The partition id is unique and does not change for the lifetime of the service. If the same service was deleted and recreated the ids of its partitions would be different.
 * @member {String} Id
 */
PartitionInformation.prototype['Id'] = undefined;
/**
 * @member {module:model/ServicePartitionKind} ServicePartitionKind
 */
PartitionInformation.prototype['ServicePartitionKind'] = undefined;




export default Int64RangePartitionInformation;

