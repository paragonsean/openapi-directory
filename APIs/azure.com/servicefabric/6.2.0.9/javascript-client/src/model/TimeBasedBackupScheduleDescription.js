/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 6.2.0.9
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BackupScheduleDescription from './BackupScheduleDescription';
import BackupScheduleFrequencyType from './BackupScheduleFrequencyType';
import BackupScheduleKind from './BackupScheduleKind';
import DayOfWeek from './DayOfWeek';

/**
 * The TimeBasedBackupScheduleDescription model module.
 * @module model/TimeBasedBackupScheduleDescription
 * @version 6.2.0.9
 */
class TimeBasedBackupScheduleDescription {
    /**
     * Constructs a new <code>TimeBasedBackupScheduleDescription</code>.
     * Describes the time based backup schedule.
     * @alias module:model/TimeBasedBackupScheduleDescription
     * @extends module:model/BackupScheduleDescription
     * @implements module:model/BackupScheduleDescription
     * @param scheduleKind {module:model/BackupScheduleKind} 
     */
    constructor(scheduleKind) { 
        BackupScheduleDescription.initialize(this, scheduleKind);
        TimeBasedBackupScheduleDescription.initialize(this, scheduleKind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, scheduleKind) { 
        obj['RunTimes'] = runTimes;
        obj['ScheduleFrequencyType'] = scheduleFrequencyType;
    }

    /**
     * Constructs a <code>TimeBasedBackupScheduleDescription</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TimeBasedBackupScheduleDescription} obj Optional instance to populate.
     * @return {module:model/TimeBasedBackupScheduleDescription} The populated <code>TimeBasedBackupScheduleDescription</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TimeBasedBackupScheduleDescription();
            BackupScheduleDescription.constructFromObject(data, obj);
            BackupScheduleDescription.constructFromObject(data, obj);

            if (data.hasOwnProperty('RunDays')) {
                obj['RunDays'] = ApiClient.convertToType(data['RunDays'], [DayOfWeek]);
            }
            if (data.hasOwnProperty('RunTimes')) {
                obj['RunTimes'] = ApiClient.convertToType(data['RunTimes'], ['Date']);
            }
            if (data.hasOwnProperty('ScheduleFrequencyType')) {
                obj['ScheduleFrequencyType'] = BackupScheduleFrequencyType.constructFromObject(data['ScheduleFrequencyType']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TimeBasedBackupScheduleDescription</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TimeBasedBackupScheduleDescription</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TimeBasedBackupScheduleDescription.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['RunDays'])) {
            throw new Error("Expected the field `RunDays` to be an array in the JSON data but got " + data['RunDays']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['RunTimes'])) {
            throw new Error("Expected the field `RunTimes` to be an array in the JSON data but got " + data['RunTimes']);
        }

        return true;
    }


}

TimeBasedBackupScheduleDescription.RequiredProperties = ["RunTimes", "ScheduleFrequencyType", "ScheduleKind"];

/**
 * List of days of a week when to trigger the periodic backup. This is valid only when the backup schedule frequency type is weekly.
 * @member {Array.<module:model/DayOfWeek>} RunDays
 */
TimeBasedBackupScheduleDescription.prototype['RunDays'] = undefined;

/**
 * Represents the list of exact time during the day in ISO8601 format. Like '19:00:00' will represent '7PM' during the day. Date specified along with time will be ignored.
 * @member {Array.<Date>} RunTimes
 */
TimeBasedBackupScheduleDescription.prototype['RunTimes'] = undefined;

/**
 * @member {module:model/BackupScheduleFrequencyType} ScheduleFrequencyType
 */
TimeBasedBackupScheduleDescription.prototype['ScheduleFrequencyType'] = undefined;


// Implement BackupScheduleDescription interface:
/**
 * @member {module:model/BackupScheduleKind} ScheduleKind
 */
BackupScheduleDescription.prototype['ScheduleKind'] = undefined;




export default TimeBasedBackupScheduleDescription;

