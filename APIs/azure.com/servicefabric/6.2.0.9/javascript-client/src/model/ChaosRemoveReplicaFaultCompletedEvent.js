/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 6.2.0.9
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FabricEventKind from './FabricEventKind';
import ReplicaEvent from './ReplicaEvent';

/**
 * The ChaosRemoveReplicaFaultCompletedEvent model module.
 * @module model/ChaosRemoveReplicaFaultCompletedEvent
 * @version 6.2.0.9
 */
class ChaosRemoveReplicaFaultCompletedEvent {
    /**
     * Constructs a new <code>ChaosRemoveReplicaFaultCompletedEvent</code>.
     * Chaos Remove Replica Fault Completed event.
     * @alias module:model/ChaosRemoveReplicaFaultCompletedEvent
     * @extends module:model/ReplicaEvent
     * @implements module:model/ReplicaEvent
     * @param eventInstanceId {String} The identifier for the FabricEvent instance.
     * @param kind {module:model/FabricEventKind} 
     * @param timeStamp {Date} The time event was logged.
     * @param partitionId {String} An internal ID used by Service Fabric to uniquely identify a partition. This is a randomly generated GUID when the service was created. The partition ID is unique and does not change for the lifetime of the service. If the same service was deleted and recreated the IDs of its partitions would be different.
     * @param replicaId {Number} Id of a stateful service replica. ReplicaId is used by Service Fabric to uniquely identify a replica of a partition. It is unique within a partition and does not change for the lifetime of the replica. If a replica gets dropped and another replica gets created on the same node for the same partition, it will get a different value for the id. Sometimes the id of a stateless service instance is also referred as a replica id.
     * @param faultGroupId {String} Id of fault group.
     * @param faultId {String} Id of fault.
     * @param serviceUri {String} Service name.
     */
    constructor(eventInstanceId, kind, timeStamp, partitionId, replicaId, faultGroupId, faultId, serviceUri) { 
        ReplicaEvent.initialize(this, eventInstanceId, kind, timeStamp, partitionId, replicaId);
        ChaosRemoveReplicaFaultCompletedEvent.initialize(this, eventInstanceId, kind, timeStamp, partitionId, replicaId, faultGroupId, faultId, serviceUri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, eventInstanceId, kind, timeStamp, partitionId, replicaId, faultGroupId, faultId, serviceUri) { 
        obj['FaultGroupId'] = faultGroupId;
        obj['FaultId'] = faultId;
        obj['ServiceUri'] = serviceUri;
    }

    /**
     * Constructs a <code>ChaosRemoveReplicaFaultCompletedEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChaosRemoveReplicaFaultCompletedEvent} obj Optional instance to populate.
     * @return {module:model/ChaosRemoveReplicaFaultCompletedEvent} The populated <code>ChaosRemoveReplicaFaultCompletedEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChaosRemoveReplicaFaultCompletedEvent();
            ReplicaEvent.constructFromObject(data, obj);
            ReplicaEvent.constructFromObject(data, obj);

            if (data.hasOwnProperty('FaultGroupId')) {
                obj['FaultGroupId'] = ApiClient.convertToType(data['FaultGroupId'], 'String');
            }
            if (data.hasOwnProperty('FaultId')) {
                obj['FaultId'] = ApiClient.convertToType(data['FaultId'], 'String');
            }
            if (data.hasOwnProperty('ServiceUri')) {
                obj['ServiceUri'] = ApiClient.convertToType(data['ServiceUri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChaosRemoveReplicaFaultCompletedEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChaosRemoveReplicaFaultCompletedEvent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChaosRemoveReplicaFaultCompletedEvent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['FaultGroupId'] && !(typeof data['FaultGroupId'] === 'string' || data['FaultGroupId'] instanceof String)) {
            throw new Error("Expected the field `FaultGroupId` to be a primitive type in the JSON string but got " + data['FaultGroupId']);
        }
        // ensure the json data is a string
        if (data['FaultId'] && !(typeof data['FaultId'] === 'string' || data['FaultId'] instanceof String)) {
            throw new Error("Expected the field `FaultId` to be a primitive type in the JSON string but got " + data['FaultId']);
        }
        // ensure the json data is a string
        if (data['ServiceUri'] && !(typeof data['ServiceUri'] === 'string' || data['ServiceUri'] instanceof String)) {
            throw new Error("Expected the field `ServiceUri` to be a primitive type in the JSON string but got " + data['ServiceUri']);
        }

        return true;
    }


}

ChaosRemoveReplicaFaultCompletedEvent.RequiredProperties = ["FaultGroupId", "FaultId", "ServiceUri", "EventInstanceId", "Kind", "TimeStamp", "PartitionId", "ReplicaId"];

/**
 * Id of fault group.
 * @member {String} FaultGroupId
 */
ChaosRemoveReplicaFaultCompletedEvent.prototype['FaultGroupId'] = undefined;

/**
 * Id of fault.
 * @member {String} FaultId
 */
ChaosRemoveReplicaFaultCompletedEvent.prototype['FaultId'] = undefined;

/**
 * Service name.
 * @member {String} ServiceUri
 */
ChaosRemoveReplicaFaultCompletedEvent.prototype['ServiceUri'] = undefined;


// Implement ReplicaEvent interface:
/**
 * The identifier for the FabricEvent instance.
 * @member {String} EventInstanceId
 */
ReplicaEvent.prototype['EventInstanceId'] = undefined;
/**
 * Shows there is existing related events available.
 * @member {Boolean} HasCorrelatedEvents
 */
ReplicaEvent.prototype['HasCorrelatedEvents'] = undefined;
/**
 * @member {module:model/FabricEventKind} Kind
 */
ReplicaEvent.prototype['Kind'] = undefined;
/**
 * The time event was logged.
 * @member {Date} TimeStamp
 */
ReplicaEvent.prototype['TimeStamp'] = undefined;
/**
 * An internal ID used by Service Fabric to uniquely identify a partition. This is a randomly generated GUID when the service was created. The partition ID is unique and does not change for the lifetime of the service. If the same service was deleted and recreated the IDs of its partitions would be different.
 * @member {String} PartitionId
 */
ReplicaEvent.prototype['PartitionId'] = undefined;
/**
 * Id of a stateful service replica. ReplicaId is used by Service Fabric to uniquely identify a replica of a partition. It is unique within a partition and does not change for the lifetime of the replica. If a replica gets dropped and another replica gets created on the same node for the same partition, it will get a different value for the id. Sometimes the id of a stateless service instance is also referred as a replica id.
 * @member {Number} ReplicaId
 */
ReplicaEvent.prototype['ReplicaId'] = undefined;




export default ChaosRemoveReplicaFaultCompletedEvent;

