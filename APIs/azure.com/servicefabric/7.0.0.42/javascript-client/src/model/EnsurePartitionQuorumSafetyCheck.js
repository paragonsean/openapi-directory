/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PartitionSafetyCheck from './PartitionSafetyCheck';
import SafetyCheckKind from './SafetyCheckKind';

/**
 * The EnsurePartitionQuorumSafetyCheck model module.
 * @module model/EnsurePartitionQuorumSafetyCheck
 * @version 7.0.0.42
 */
class EnsurePartitionQuorumSafetyCheck {
    /**
     * Constructs a new <code>EnsurePartitionQuorumSafetyCheck</code>.
     * Safety check that ensures that a quorum of replicas are not lost for a partition.
     * @alias module:model/EnsurePartitionQuorumSafetyCheck
     * @extends module:model/PartitionSafetyCheck
     * @implements module:model/PartitionSafetyCheck
     * @param kind {module:model/SafetyCheckKind} 
     */
    constructor(kind) { 
        PartitionSafetyCheck.initialize(this, kind);
        EnsurePartitionQuorumSafetyCheck.initialize(this, kind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, kind) { 
    }

    /**
     * Constructs a <code>EnsurePartitionQuorumSafetyCheck</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EnsurePartitionQuorumSafetyCheck} obj Optional instance to populate.
     * @return {module:model/EnsurePartitionQuorumSafetyCheck} The populated <code>EnsurePartitionQuorumSafetyCheck</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EnsurePartitionQuorumSafetyCheck();
            PartitionSafetyCheck.constructFromObject(data, obj);
            PartitionSafetyCheck.constructFromObject(data, obj);

        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EnsurePartitionQuorumSafetyCheck</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EnsurePartitionQuorumSafetyCheck</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EnsurePartitionQuorumSafetyCheck.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

EnsurePartitionQuorumSafetyCheck.RequiredProperties = ["Kind"];


// Implement PartitionSafetyCheck interface:
/**
 * @member {module:model/SafetyCheckKind} Kind
 */
PartitionSafetyCheck.prototype['Kind'] = undefined;




export default EnsurePartitionQuorumSafetyCheck;

