/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProbeHttpGetHeaders from './ProbeHttpGetHeaders';

/**
 * The ProbeHttpGet model module.
 * @module model/ProbeHttpGet
 * @version 7.0.0.42
 */
class ProbeHttpGet {
    /**
     * Constructs a new <code>ProbeHttpGet</code>.
     * Http probe for the container.
     * @alias module:model/ProbeHttpGet
     * @param port {Number} Port to access for probe.
     */
    constructor(port) { 
        
        ProbeHttpGet.initialize(this, port);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, port) { 
        obj['port'] = port;
    }

    /**
     * Constructs a <code>ProbeHttpGet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProbeHttpGet} obj Optional instance to populate.
     * @return {module:model/ProbeHttpGet} The populated <code>ProbeHttpGet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProbeHttpGet();

            if (data.hasOwnProperty('host')) {
                obj['host'] = ApiClient.convertToType(data['host'], 'String');
            }
            if (data.hasOwnProperty('httpHeaders')) {
                obj['httpHeaders'] = ApiClient.convertToType(data['httpHeaders'], [ProbeHttpGetHeaders]);
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('port')) {
                obj['port'] = ApiClient.convertToType(data['port'], 'Number');
            }
            if (data.hasOwnProperty('scheme')) {
                obj['scheme'] = ApiClient.convertToType(data['scheme'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProbeHttpGet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProbeHttpGet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProbeHttpGet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['host'] && !(typeof data['host'] === 'string' || data['host'] instanceof String)) {
            throw new Error("Expected the field `host` to be a primitive type in the JSON string but got " + data['host']);
        }
        if (data['httpHeaders']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['httpHeaders'])) {
                throw new Error("Expected the field `httpHeaders` to be an array in the JSON data but got " + data['httpHeaders']);
            }
            // validate the optional field `httpHeaders` (array)
            for (const item of data['httpHeaders']) {
                ProbeHttpGetHeaders.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }
        // ensure the json data is a string
        if (data['scheme'] && !(typeof data['scheme'] === 'string' || data['scheme'] instanceof String)) {
            throw new Error("Expected the field `scheme` to be a primitive type in the JSON string but got " + data['scheme']);
        }

        return true;
    }


}

ProbeHttpGet.RequiredProperties = ["port"];

/**
 * Host IP to connect to.
 * @member {String} host
 */
ProbeHttpGet.prototype['host'] = undefined;

/**
 * Headers to set in the request.
 * @member {Array.<module:model/ProbeHttpGetHeaders>} httpHeaders
 */
ProbeHttpGet.prototype['httpHeaders'] = undefined;

/**
 * Path to access on the HTTP request.
 * @member {String} path
 */
ProbeHttpGet.prototype['path'] = undefined;

/**
 * Port to access for probe.
 * @member {Number} port
 */
ProbeHttpGet.prototype['port'] = undefined;

/**
 * Scheme for the http probe. Can be Http or Https.
 * @member {module:model/ProbeHttpGet.SchemeEnum} scheme
 */
ProbeHttpGet.prototype['scheme'] = undefined;





/**
 * Allowed values for the <code>scheme</code> property.
 * @enum {String}
 * @readonly
 */
ProbeHttpGet['SchemeEnum'] = {

    /**
     * value: "http"
     * @const
     */
    "http": "http",

    /**
     * value: "https"
     * @const
     */
    "https": "https"
};



export default ProbeHttpGet;

