/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HealthEvaluation from './HealthEvaluation';
import HealthEvaluationKind from './HealthEvaluationKind';
import HealthEvaluationWrapper from './HealthEvaluationWrapper';
import HealthState from './HealthState';

/**
 * The ReplicasHealthEvaluation model module.
 * @module model/ReplicasHealthEvaluation
 * @version 7.0.0.42
 */
class ReplicasHealthEvaluation {
    /**
     * Constructs a new <code>ReplicasHealthEvaluation</code>.
     * Represents health evaluation for replicas, containing health evaluations for each unhealthy replica that impacted current aggregated health state. Can be returned when evaluating partition health and the aggregated health state is either Error or Warning.
     * @alias module:model/ReplicasHealthEvaluation
     * @extends module:model/HealthEvaluation
     * @implements module:model/HealthEvaluation
     * @param kind {module:model/HealthEvaluationKind} 
     */
    constructor(kind) { 
        HealthEvaluation.initialize(this, kind);
        ReplicasHealthEvaluation.initialize(this, kind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, kind) { 
    }

    /**
     * Constructs a <code>ReplicasHealthEvaluation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReplicasHealthEvaluation} obj Optional instance to populate.
     * @return {module:model/ReplicasHealthEvaluation} The populated <code>ReplicasHealthEvaluation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReplicasHealthEvaluation();
            HealthEvaluation.constructFromObject(data, obj);
            HealthEvaluation.constructFromObject(data, obj);

            if (data.hasOwnProperty('MaxPercentUnhealthyReplicasPerPartition')) {
                obj['MaxPercentUnhealthyReplicasPerPartition'] = ApiClient.convertToType(data['MaxPercentUnhealthyReplicasPerPartition'], 'Number');
            }
            if (data.hasOwnProperty('TotalCount')) {
                obj['TotalCount'] = ApiClient.convertToType(data['TotalCount'], 'Number');
            }
            if (data.hasOwnProperty('UnhealthyEvaluations')) {
                obj['UnhealthyEvaluations'] = ApiClient.convertToType(data['UnhealthyEvaluations'], [HealthEvaluationWrapper]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReplicasHealthEvaluation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReplicasHealthEvaluation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReplicasHealthEvaluation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['UnhealthyEvaluations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['UnhealthyEvaluations'])) {
                throw new Error("Expected the field `UnhealthyEvaluations` to be an array in the JSON data but got " + data['UnhealthyEvaluations']);
            }
            // validate the optional field `UnhealthyEvaluations` (array)
            for (const item of data['UnhealthyEvaluations']) {
                HealthEvaluationWrapper.validateJSON(item);
            };
        }

        return true;
    }


}

ReplicasHealthEvaluation.RequiredProperties = ["Kind"];

/**
 * Maximum allowed percentage of unhealthy replicas per partition from the ApplicationHealthPolicy.
 * @member {Number} MaxPercentUnhealthyReplicasPerPartition
 */
ReplicasHealthEvaluation.prototype['MaxPercentUnhealthyReplicasPerPartition'] = undefined;

/**
 * Total number of replicas in the partition from the health store.
 * @member {Number} TotalCount
 */
ReplicasHealthEvaluation.prototype['TotalCount'] = undefined;

/**
 * List of health evaluations that resulted in the current aggregated health state.
 * @member {Array.<module:model/HealthEvaluationWrapper>} UnhealthyEvaluations
 */
ReplicasHealthEvaluation.prototype['UnhealthyEvaluations'] = undefined;


// Implement HealthEvaluation interface:
/**
 * @member {module:model/HealthState} AggregatedHealthState
 */
HealthEvaluation.prototype['AggregatedHealthState'] = undefined;
/**
 * Description of the health evaluation, which represents a summary of the evaluation process.
 * @member {String} Description
 */
HealthEvaluation.prototype['Description'] = undefined;
/**
 * @member {module:model/HealthEvaluationKind} Kind
 */
HealthEvaluation.prototype['Kind'] = undefined;




export default ReplicasHealthEvaluation;

