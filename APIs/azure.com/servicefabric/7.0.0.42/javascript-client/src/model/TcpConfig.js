/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GatewayDestination from './GatewayDestination';

/**
 * The TcpConfig model module.
 * @module model/TcpConfig
 * @version 7.0.0.42
 */
class TcpConfig {
    /**
     * Constructs a new <code>TcpConfig</code>.
     * Describes the tcp configuration for external connectivity for this network.
     * @alias module:model/TcpConfig
     * @param destination {module:model/GatewayDestination} 
     * @param name {String} tcp gateway config name.
     * @param port {Number} Specifies the port at which the service endpoint below needs to be exposed.
     */
    constructor(destination, name, port) { 
        
        TcpConfig.initialize(this, destination, name, port);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destination, name, port) { 
        obj['destination'] = destination;
        obj['name'] = name;
        obj['port'] = port;
    }

    /**
     * Constructs a <code>TcpConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TcpConfig} obj Optional instance to populate.
     * @return {module:model/TcpConfig} The populated <code>TcpConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TcpConfig();

            if (data.hasOwnProperty('destination')) {
                obj['destination'] = GatewayDestination.constructFromObject(data['destination']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('port')) {
                obj['port'] = ApiClient.convertToType(data['port'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TcpConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TcpConfig</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TcpConfig.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `destination`
        if (data['destination']) { // data not null
          GatewayDestination.validateJSON(data['destination']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

TcpConfig.RequiredProperties = ["destination", "name", "port"];

/**
 * @member {module:model/GatewayDestination} destination
 */
TcpConfig.prototype['destination'] = undefined;

/**
 * tcp gateway config name.
 * @member {String} name
 */
TcpConfig.prototype['name'] = undefined;

/**
 * Specifies the port at which the service endpoint below needs to be exposed.
 * @member {Number} port
 */
TcpConfig.prototype['port'] = undefined;






export default TcpConfig;

