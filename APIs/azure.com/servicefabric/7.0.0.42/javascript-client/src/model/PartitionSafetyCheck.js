/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SafetyCheck from './SafetyCheck';
import SafetyCheckKind from './SafetyCheckKind';

/**
 * The PartitionSafetyCheck model module.
 * @module model/PartitionSafetyCheck
 * @version 7.0.0.42
 */
class PartitionSafetyCheck {
    /**
     * Constructs a new <code>PartitionSafetyCheck</code>.
     * Represents a safety check for the service partition being performed by service fabric before continuing with operations.
     * @alias module:model/PartitionSafetyCheck
     * @extends module:model/SafetyCheck
     * @implements module:model/SafetyCheck
     * @param kind {module:model/SafetyCheckKind} 
     */
    constructor(kind) { 
        SafetyCheck.initialize(this, kind);
        PartitionSafetyCheck.initialize(this, kind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, kind) { 
    }

    /**
     * Constructs a <code>PartitionSafetyCheck</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PartitionSafetyCheck} obj Optional instance to populate.
     * @return {module:model/PartitionSafetyCheck} The populated <code>PartitionSafetyCheck</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PartitionSafetyCheck();
            SafetyCheck.constructFromObject(data, obj);
            SafetyCheck.constructFromObject(data, obj);

            if (data.hasOwnProperty('PartitionId')) {
                obj['PartitionId'] = ApiClient.convertToType(data['PartitionId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PartitionSafetyCheck</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PartitionSafetyCheck</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PartitionSafetyCheck.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['PartitionId'] && !(typeof data['PartitionId'] === 'string' || data['PartitionId'] instanceof String)) {
            throw new Error("Expected the field `PartitionId` to be a primitive type in the JSON string but got " + data['PartitionId']);
        }

        return true;
    }


}

PartitionSafetyCheck.RequiredProperties = ["Kind"];

/**
 * An internal ID used by Service Fabric to uniquely identify a partition. This is a randomly generated GUID when the service was created. The partition ID is unique and does not change for the lifetime of the service. If the same service was deleted and recreated the IDs of its partitions would be different.
 * @member {String} PartitionId
 */
PartitionSafetyCheck.prototype['PartitionId'] = undefined;


// Implement SafetyCheck interface:
/**
 * @member {module:model/SafetyCheckKind} Kind
 */
SafetyCheck.prototype['Kind'] = undefined;




export default PartitionSafetyCheck;

