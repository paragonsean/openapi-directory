/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HealthState from './HealthState';
import ReplicaHealthState from './ReplicaHealthState';
import ServiceKind from './ServiceKind';

/**
 * The StatelessServiceInstanceHealthState model module.
 * @module model/StatelessServiceInstanceHealthState
 * @version 7.0.0.42
 */
class StatelessServiceInstanceHealthState {
    /**
     * Constructs a new <code>StatelessServiceInstanceHealthState</code>.
     * Represents the health state of the stateless service instance, which contains the instance ID and the aggregated health state.
     * @alias module:model/StatelessServiceInstanceHealthState
     * @implements module:model/ReplicaHealthState
     * @param serviceKind {module:model/ServiceKind} 
     */
    constructor(serviceKind) { 
        ReplicaHealthState.initialize(this);
        StatelessServiceInstanceHealthState.initialize(this, serviceKind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, serviceKind) { 
        obj['ServiceKind'] = serviceKind;
    }

    /**
     * Constructs a <code>StatelessServiceInstanceHealthState</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StatelessServiceInstanceHealthState} obj Optional instance to populate.
     * @return {module:model/StatelessServiceInstanceHealthState} The populated <code>StatelessServiceInstanceHealthState</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StatelessServiceInstanceHealthState();
            ReplicaHealthState.constructFromObject(data, obj);

            if (data.hasOwnProperty('ReplicaId')) {
                obj['ReplicaId'] = ApiClient.convertToType(data['ReplicaId'], 'String');
            }
            if (data.hasOwnProperty('PartitionId')) {
                obj['PartitionId'] = ApiClient.convertToType(data['PartitionId'], 'String');
            }
            if (data.hasOwnProperty('ServiceKind')) {
                obj['ServiceKind'] = ServiceKind.constructFromObject(data['ServiceKind']);
            }
            if (data.hasOwnProperty('AggregatedHealthState')) {
                obj['AggregatedHealthState'] = HealthState.constructFromObject(data['AggregatedHealthState']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StatelessServiceInstanceHealthState</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StatelessServiceInstanceHealthState</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StatelessServiceInstanceHealthState.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ReplicaId'] && !(typeof data['ReplicaId'] === 'string' || data['ReplicaId'] instanceof String)) {
            throw new Error("Expected the field `ReplicaId` to be a primitive type in the JSON string but got " + data['ReplicaId']);
        }
        // ensure the json data is a string
        if (data['PartitionId'] && !(typeof data['PartitionId'] === 'string' || data['PartitionId'] instanceof String)) {
            throw new Error("Expected the field `PartitionId` to be a primitive type in the JSON string but got " + data['PartitionId']);
        }

        return true;
    }


}

StatelessServiceInstanceHealthState.RequiredProperties = ["ServiceKind"];

/**
 * Id of a stateful service replica. ReplicaId is used by Service Fabric to uniquely identify a replica of a partition. It is unique within a partition and does not change for the lifetime of the replica. If a replica gets dropped and another replica gets created on the same node for the same partition, it will get a different value for the id. Sometimes the id of a stateless service instance is also referred as a replica id.
 * @member {String} ReplicaId
 */
StatelessServiceInstanceHealthState.prototype['ReplicaId'] = undefined;

/**
 * An internal ID used by Service Fabric to uniquely identify a partition. This is a randomly generated GUID when the service was created. The partition ID is unique and does not change for the lifetime of the service. If the same service was deleted and recreated the IDs of its partitions would be different.
 * @member {String} PartitionId
 */
StatelessServiceInstanceHealthState.prototype['PartitionId'] = undefined;

/**
 * @member {module:model/ServiceKind} ServiceKind
 */
StatelessServiceInstanceHealthState.prototype['ServiceKind'] = undefined;

/**
 * @member {module:model/HealthState} AggregatedHealthState
 */
StatelessServiceInstanceHealthState.prototype['AggregatedHealthState'] = undefined;


// Implement ReplicaHealthState interface:
/**
 * @member {module:model/HealthState} AggregatedHealthState
 */
ReplicaHealthState.prototype['AggregatedHealthState'] = undefined;




export default StatelessServiceInstanceHealthState;

