/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BackupEntity from './BackupEntity';
import BackupEntityKind from './BackupEntityKind';

/**
 * The PartitionBackupEntity model module.
 * @module model/PartitionBackupEntity
 * @version 7.0.0.42
 */
class PartitionBackupEntity {
    /**
     * Constructs a new <code>PartitionBackupEntity</code>.
     * Identifies the Service Fabric stateful partition which is being backed up.
     * @alias module:model/PartitionBackupEntity
     * @extends module:model/BackupEntity
     * @implements module:model/BackupEntity
     * @param entityKind {module:model/BackupEntityKind} 
     */
    constructor(entityKind) { 
        BackupEntity.initialize(this, entityKind);
        PartitionBackupEntity.initialize(this, entityKind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, entityKind) { 
    }

    /**
     * Constructs a <code>PartitionBackupEntity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PartitionBackupEntity} obj Optional instance to populate.
     * @return {module:model/PartitionBackupEntity} The populated <code>PartitionBackupEntity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PartitionBackupEntity();
            BackupEntity.constructFromObject(data, obj);
            BackupEntity.constructFromObject(data, obj);

            if (data.hasOwnProperty('PartitionId')) {
                obj['PartitionId'] = ApiClient.convertToType(data['PartitionId'], 'String');
            }
            if (data.hasOwnProperty('ServiceName')) {
                obj['ServiceName'] = ApiClient.convertToType(data['ServiceName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PartitionBackupEntity</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PartitionBackupEntity</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PartitionBackupEntity.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['PartitionId'] && !(typeof data['PartitionId'] === 'string' || data['PartitionId'] instanceof String)) {
            throw new Error("Expected the field `PartitionId` to be a primitive type in the JSON string but got " + data['PartitionId']);
        }
        // ensure the json data is a string
        if (data['ServiceName'] && !(typeof data['ServiceName'] === 'string' || data['ServiceName'] instanceof String)) {
            throw new Error("Expected the field `ServiceName` to be a primitive type in the JSON string but got " + data['ServiceName']);
        }

        return true;
    }


}

PartitionBackupEntity.RequiredProperties = ["EntityKind"];

/**
 * An internal ID used by Service Fabric to uniquely identify a partition. This is a randomly generated GUID when the service was created. The partition ID is unique and does not change for the lifetime of the service. If the same service was deleted and recreated the IDs of its partitions would be different.
 * @member {String} PartitionId
 */
PartitionBackupEntity.prototype['PartitionId'] = undefined;

/**
 * The full name of the service with 'fabric:' URI scheme.
 * @member {String} ServiceName
 */
PartitionBackupEntity.prototype['ServiceName'] = undefined;


// Implement BackupEntity interface:
/**
 * @member {module:model/BackupEntityKind} EntityKind
 */
BackupEntity.prototype['EntityKind'] = undefined;




export default PartitionBackupEntity;

