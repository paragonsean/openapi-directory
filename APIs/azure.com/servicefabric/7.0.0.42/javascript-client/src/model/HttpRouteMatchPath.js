/**
 * Service Fabric Client APIs
 * Service Fabric REST Client APIs allows management of Service Fabric clusters, applications and services.
 *
 * The version of the OpenAPI document: 7.0.0.42
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The HttpRouteMatchPath model module.
 * @module model/HttpRouteMatchPath
 * @version 7.0.0.42
 */
class HttpRouteMatchPath {
    /**
     * Constructs a new <code>HttpRouteMatchPath</code>.
     * Path to match for routing.
     * @alias module:model/HttpRouteMatchPath
     * @param type {module:model/HttpRouteMatchPath.TypeEnum} how to match value in the Uri
     * @param value {String} Uri path to match for request.
     */
    constructor(type, value) { 
        
        HttpRouteMatchPath.initialize(this, type, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, value) { 
        obj['type'] = type;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>HttpRouteMatchPath</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HttpRouteMatchPath} obj Optional instance to populate.
     * @return {module:model/HttpRouteMatchPath} The populated <code>HttpRouteMatchPath</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HttpRouteMatchPath();

            if (data.hasOwnProperty('rewrite')) {
                obj['rewrite'] = ApiClient.convertToType(data['rewrite'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HttpRouteMatchPath</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HttpRouteMatchPath</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of HttpRouteMatchPath.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['rewrite'] && !(typeof data['rewrite'] === 'string' || data['rewrite'] instanceof String)) {
            throw new Error("Expected the field `rewrite` to be a primitive type in the JSON string but got " + data['rewrite']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

HttpRouteMatchPath.RequiredProperties = ["type", "value"];

/**
 * replacement string for matched part of the Uri.
 * @member {String} rewrite
 */
HttpRouteMatchPath.prototype['rewrite'] = undefined;

/**
 * how to match value in the Uri
 * @member {module:model/HttpRouteMatchPath.TypeEnum} type
 */
HttpRouteMatchPath.prototype['type'] = undefined;

/**
 * Uri path to match for request.
 * @member {String} value
 */
HttpRouteMatchPath.prototype['value'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
HttpRouteMatchPath['TypeEnum'] = {

    /**
     * value: "prefix"
     * @const
     */
    "prefix": "prefix"
};



export default HttpRouteMatchPath;

