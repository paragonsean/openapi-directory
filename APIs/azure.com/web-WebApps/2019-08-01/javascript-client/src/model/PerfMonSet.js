/**
 * WebApps API Client
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2019-08-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PerfMonSample from './PerfMonSample';

/**
 * The PerfMonSet model module.
 * @module model/PerfMonSet
 * @version 2019-08-01
 */
class PerfMonSet {
    /**
     * Constructs a new <code>PerfMonSet</code>.
     * Metric information.
     * @alias module:model/PerfMonSet
     */
    constructor() { 
        
        PerfMonSet.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PerfMonSet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PerfMonSet} obj Optional instance to populate.
     * @return {module:model/PerfMonSet} The populated <code>PerfMonSet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PerfMonSet();

            if (data.hasOwnProperty('endTime')) {
                obj['endTime'] = ApiClient.convertToType(data['endTime'], 'Date');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('startTime')) {
                obj['startTime'] = ApiClient.convertToType(data['startTime'], 'Date');
            }
            if (data.hasOwnProperty('timeGrain')) {
                obj['timeGrain'] = ApiClient.convertToType(data['timeGrain'], 'String');
            }
            if (data.hasOwnProperty('values')) {
                obj['values'] = ApiClient.convertToType(data['values'], [PerfMonSample]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PerfMonSet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PerfMonSet</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['timeGrain'] && !(typeof data['timeGrain'] === 'string' || data['timeGrain'] instanceof String)) {
            throw new Error("Expected the field `timeGrain` to be a primitive type in the JSON string but got " + data['timeGrain']);
        }
        if (data['values']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['values'])) {
                throw new Error("Expected the field `values` to be an array in the JSON data but got " + data['values']);
            }
            // validate the optional field `values` (array)
            for (const item of data['values']) {
                PerfMonSample.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * End time of the period.
 * @member {Date} endTime
 */
PerfMonSet.prototype['endTime'] = undefined;

/**
 * Unique key name of the counter.
 * @member {String} name
 */
PerfMonSet.prototype['name'] = undefined;

/**
 * Start time of the period.
 * @member {Date} startTime
 */
PerfMonSet.prototype['startTime'] = undefined;

/**
 * Presented time grain.
 * @member {String} timeGrain
 */
PerfMonSet.prototype['timeGrain'] = undefined;

/**
 * Collection of workers that are active during this time.
 * @member {Array.<module:model/PerfMonSample>} values
 */
PerfMonSet.prototype['values'] = undefined;






export default PerfMonSet;

