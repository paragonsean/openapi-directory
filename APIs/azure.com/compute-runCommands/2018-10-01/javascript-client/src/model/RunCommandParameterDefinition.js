/**
 * RunCommandsClient
 * The Run Commands Client.
 *
 * The version of the OpenAPI document: 2018-10-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RunCommandParameterDefinition model module.
 * @module model/RunCommandParameterDefinition
 * @version 2018-10-01
 */
class RunCommandParameterDefinition {
    /**
     * Constructs a new <code>RunCommandParameterDefinition</code>.
     * Describes the properties of a run command parameter.
     * @alias module:model/RunCommandParameterDefinition
     * @param name {String} The run command parameter name.
     * @param type {String} The run command parameter type.
     */
    constructor(name, type) { 
        
        RunCommandParameterDefinition.initialize(this, name, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, type) { 
        obj['name'] = name;
        obj['required'] = false;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>RunCommandParameterDefinition</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RunCommandParameterDefinition} obj Optional instance to populate.
     * @return {module:model/RunCommandParameterDefinition} The populated <code>RunCommandParameterDefinition</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RunCommandParameterDefinition();

            if (data.hasOwnProperty('defaultValue')) {
                obj['defaultValue'] = ApiClient.convertToType(data['defaultValue'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RunCommandParameterDefinition</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RunCommandParameterDefinition</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RunCommandParameterDefinition.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['defaultValue'] && !(typeof data['defaultValue'] === 'string' || data['defaultValue'] instanceof String)) {
            throw new Error("Expected the field `defaultValue` to be a primitive type in the JSON string but got " + data['defaultValue']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

RunCommandParameterDefinition.RequiredProperties = ["name", "type"];

/**
 * The run command parameter default value.
 * @member {String} defaultValue
 */
RunCommandParameterDefinition.prototype['defaultValue'] = undefined;

/**
 * The run command parameter name.
 * @member {String} name
 */
RunCommandParameterDefinition.prototype['name'] = undefined;

/**
 * The run command parameter required.
 * @member {Boolean} required
 * @default false
 */
RunCommandParameterDefinition.prototype['required'] = false;

/**
 * The run command parameter type.
 * @member {String} type
 */
RunCommandParameterDefinition.prototype['type'] = undefined;






export default RunCommandParameterDefinition;

