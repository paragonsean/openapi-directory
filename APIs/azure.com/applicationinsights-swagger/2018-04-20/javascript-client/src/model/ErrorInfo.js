/**
 * Application Insights Data Plane
 * This API exposes AI metric & event information and associated metadata
 *
 * The version of the OpenAPI document: 2018-04-20
 * Contact: aiapi@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ErrorDetail from './ErrorDetail';

/**
 * The ErrorInfo model module.
 * @module model/ErrorInfo
 * @version 2018-04-20
 */
class ErrorInfo {
    /**
     * Constructs a new <code>ErrorInfo</code>.
     * @alias module:model/ErrorInfo
     * @param code {String} A machine readable error code.
     * @param message {String} A human readable error message.
     */
    constructor(code, message) { 
        
        ErrorInfo.initialize(this, code, message);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, code, message) { 
        obj['code'] = code;
        obj['message'] = message;
    }

    /**
     * Constructs a <code>ErrorInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ErrorInfo} obj Optional instance to populate.
     * @return {module:model/ErrorInfo} The populated <code>ErrorInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ErrorInfo();

            if (data.hasOwnProperty('additionalProperties')) {
                obj['additionalProperties'] = ApiClient.convertToType(data['additionalProperties'], Object);
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('details')) {
                obj['details'] = ApiClient.convertToType(data['details'], [ErrorDetail]);
            }
            if (data.hasOwnProperty('innererror')) {
                obj['innererror'] = ErrorInfo.constructFromObject(data['innererror']);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ErrorInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ErrorInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ErrorInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        if (data['details']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['details'])) {
                throw new Error("Expected the field `details` to be an array in the JSON data but got " + data['details']);
            }
            // validate the optional field `details` (array)
            for (const item of data['details']) {
                ErrorDetail.validateJSON(item);
            };
        }
        // validate the optional field `innererror`
        if (data['innererror']) { // data not null
          ErrorInfo.validateJSON(data['innererror']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }

        return true;
    }


}

ErrorInfo.RequiredProperties = ["code", "message"];

/**
 * @member {Object} additionalProperties
 */
ErrorInfo.prototype['additionalProperties'] = undefined;

/**
 * A machine readable error code.
 * @member {String} code
 */
ErrorInfo.prototype['code'] = undefined;

/**
 * error details.
 * @member {Array.<module:model/ErrorDetail>} details
 */
ErrorInfo.prototype['details'] = undefined;

/**
 * @member {module:model/ErrorInfo} innererror
 */
ErrorInfo.prototype['innererror'] = undefined;

/**
 * A human readable error message.
 * @member {String} message
 */
ErrorInfo.prototype['message'] = undefined;






export default ErrorInfo;

