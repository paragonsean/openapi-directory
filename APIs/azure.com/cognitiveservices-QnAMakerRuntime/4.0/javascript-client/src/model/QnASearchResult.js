/**
 * QnAMaker Runtime Client
 * An API for QnAMaker runtime
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContextDTO from './ContextDTO';
import MetadataDTO from './MetadataDTO';

/**
 * The QnASearchResult model module.
 * @module model/QnASearchResult
 * @version 4.0
 */
class QnASearchResult {
    /**
     * Constructs a new <code>QnASearchResult</code>.
     * Represents Search Result.
     * @alias module:model/QnASearchResult
     */
    constructor() { 
        
        QnASearchResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>QnASearchResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QnASearchResult} obj Optional instance to populate.
     * @return {module:model/QnASearchResult} The populated <code>QnASearchResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QnASearchResult();

            if (data.hasOwnProperty('answer')) {
                obj['answer'] = ApiClient.convertToType(data['answer'], 'String');
            }
            if (data.hasOwnProperty('context')) {
                obj['context'] = ApiClient.convertToType(data['context'], ContextDTO);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], [MetadataDTO]);
            }
            if (data.hasOwnProperty('questions')) {
                obj['questions'] = ApiClient.convertToType(data['questions'], ['String']);
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'Number');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QnASearchResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QnASearchResult</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['answer'] && !(typeof data['answer'] === 'string' || data['answer'] instanceof String)) {
            throw new Error("Expected the field `answer` to be a primitive type in the JSON string but got " + data['answer']);
        }
        // validate the optional field `context`
        if (data['context']) { // data not null
          ContextDTO.validateJSON(data['context']);
        }
        if (data['metadata']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metadata'])) {
                throw new Error("Expected the field `metadata` to be an array in the JSON data but got " + data['metadata']);
            }
            // validate the optional field `metadata` (array)
            for (const item of data['metadata']) {
                MetadataDTO.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['questions'])) {
            throw new Error("Expected the field `questions` to be an array in the JSON data but got " + data['questions']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}



/**
 * Answer.
 * @member {String} answer
 */
QnASearchResult.prototype['answer'] = undefined;

/**
 * Context object of the QnA
 * @member {module:model/ContextDTO} context
 */
QnASearchResult.prototype['context'] = undefined;

/**
 * Id of the QnA result.
 * @member {Number} id
 */
QnASearchResult.prototype['id'] = undefined;

/**
 * List of metadata.
 * @member {Array.<module:model/MetadataDTO>} metadata
 */
QnASearchResult.prototype['metadata'] = undefined;

/**
 * List of questions.
 * @member {Array.<String>} questions
 */
QnASearchResult.prototype['questions'] = undefined;

/**
 * Search result score.
 * @member {Number} score
 */
QnASearchResult.prototype['score'] = undefined;

/**
 * Source of QnA result.
 * @member {String} source
 */
QnASearchResult.prototype['source'] = undefined;






export default QnASearchResult;

