/**
 * HDInsightManagementClient
 * The HDInsight Management Client.
 *
 * The version of the OpenAPI document: 2015-03-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Autoscale from './Autoscale';
import DataDisksGroups from './DataDisksGroups';
import HardwareProfile from './HardwareProfile';
import OsProfile from './OsProfile';
import RoleScriptActionsInner from './RoleScriptActionsInner';
import VirtualNetworkProfile from './VirtualNetworkProfile';

/**
 * The Role model module.
 * @module model/Role
 * @version 2015-03-01-preview
 */
class Role {
    /**
     * Constructs a new <code>Role</code>.
     * Describes a role on the cluster.
     * @alias module:model/Role
     */
    constructor() { 
        
        Role.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Role</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Role} obj Optional instance to populate.
     * @return {module:model/Role} The populated <code>Role</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Role();

            if (data.hasOwnProperty('autoscale')) {
                obj['autoscale'] = Autoscale.constructFromObject(data['autoscale']);
            }
            if (data.hasOwnProperty('dataDisksGroups')) {
                obj['dataDisksGroups'] = ApiClient.convertToType(data['dataDisksGroups'], [DataDisksGroups]);
            }
            if (data.hasOwnProperty('hardwareProfile')) {
                obj['hardwareProfile'] = HardwareProfile.constructFromObject(data['hardwareProfile']);
            }
            if (data.hasOwnProperty('minInstanceCount')) {
                obj['minInstanceCount'] = ApiClient.convertToType(data['minInstanceCount'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('osProfile')) {
                obj['osProfile'] = OsProfile.constructFromObject(data['osProfile']);
            }
            if (data.hasOwnProperty('scriptActions')) {
                obj['scriptActions'] = ApiClient.convertToType(data['scriptActions'], [RoleScriptActionsInner]);
            }
            if (data.hasOwnProperty('targetInstanceCount')) {
                obj['targetInstanceCount'] = ApiClient.convertToType(data['targetInstanceCount'], 'Number');
            }
            if (data.hasOwnProperty('virtualNetworkProfile')) {
                obj['virtualNetworkProfile'] = VirtualNetworkProfile.constructFromObject(data['virtualNetworkProfile']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Role</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Role</code>.
     */
    static validateJSON(data) {
        // validate the optional field `autoscale`
        if (data['autoscale']) { // data not null
          Autoscale.validateJSON(data['autoscale']);
        }
        if (data['dataDisksGroups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dataDisksGroups'])) {
                throw new Error("Expected the field `dataDisksGroups` to be an array in the JSON data but got " + data['dataDisksGroups']);
            }
            // validate the optional field `dataDisksGroups` (array)
            for (const item of data['dataDisksGroups']) {
                DataDisksGroups.validateJSON(item);
            };
        }
        // validate the optional field `hardwareProfile`
        if (data['hardwareProfile']) { // data not null
          HardwareProfile.validateJSON(data['hardwareProfile']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `osProfile`
        if (data['osProfile']) { // data not null
          OsProfile.validateJSON(data['osProfile']);
        }
        if (data['scriptActions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['scriptActions'])) {
                throw new Error("Expected the field `scriptActions` to be an array in the JSON data but got " + data['scriptActions']);
            }
            // validate the optional field `scriptActions` (array)
            for (const item of data['scriptActions']) {
                RoleScriptActionsInner.validateJSON(item);
            };
        }
        // validate the optional field `virtualNetworkProfile`
        if (data['virtualNetworkProfile']) { // data not null
          VirtualNetworkProfile.validateJSON(data['virtualNetworkProfile']);
        }

        return true;
    }


}



/**
 * @member {module:model/Autoscale} autoscale
 */
Role.prototype['autoscale'] = undefined;

/**
 * The data disks groups for the role.
 * @member {Array.<module:model/DataDisksGroups>} dataDisksGroups
 */
Role.prototype['dataDisksGroups'] = undefined;

/**
 * @member {module:model/HardwareProfile} hardwareProfile
 */
Role.prototype['hardwareProfile'] = undefined;

/**
 * The minimum instance count of the cluster.
 * @member {Number} minInstanceCount
 */
Role.prototype['minInstanceCount'] = undefined;

/**
 * The name of the role.
 * @member {String} name
 */
Role.prototype['name'] = undefined;

/**
 * @member {module:model/OsProfile} osProfile
 */
Role.prototype['osProfile'] = undefined;

/**
 * The list of script actions on the role.
 * @member {Array.<module:model/RoleScriptActionsInner>} scriptActions
 */
Role.prototype['scriptActions'] = undefined;

/**
 * The instance count of the cluster.
 * @member {Number} targetInstanceCount
 */
Role.prototype['targetInstanceCount'] = undefined;

/**
 * @member {module:model/VirtualNetworkProfile} virtualNetworkProfile
 */
Role.prototype['virtualNetworkProfile'] = undefined;






export default Role;

