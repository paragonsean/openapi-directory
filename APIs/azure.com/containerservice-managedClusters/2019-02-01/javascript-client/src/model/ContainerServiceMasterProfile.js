/**
 * ContainerServiceClient
 * The Container Service Client.
 *
 * The version of the OpenAPI document: 2019-02-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContainerServiceStorageProfile from './ContainerServiceStorageProfile';
import ContainerServiceVMSize from './ContainerServiceVMSize';

/**
 * The ContainerServiceMasterProfile model module.
 * @module model/ContainerServiceMasterProfile
 * @version 2019-02-01
 */
class ContainerServiceMasterProfile {
    /**
     * Constructs a new <code>ContainerServiceMasterProfile</code>.
     * Profile for the container service master.
     * @alias module:model/ContainerServiceMasterProfile
     * @param dnsPrefix {String} DNS prefix to be used to create the FQDN for the master pool.
     * @param vmSize {module:model/ContainerServiceVMSize} 
     */
    constructor(dnsPrefix, vmSize) { 
        
        ContainerServiceMasterProfile.initialize(this, dnsPrefix, vmSize);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dnsPrefix, vmSize) { 
        obj['count'] = CountEnum.1;
        obj['dnsPrefix'] = dnsPrefix;
        obj['firstConsecutiveStaticIP'] = '10.240.255.5';
        obj['vmSize'] = vmSize;
    }

    /**
     * Constructs a <code>ContainerServiceMasterProfile</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContainerServiceMasterProfile} obj Optional instance to populate.
     * @return {module:model/ContainerServiceMasterProfile} The populated <code>ContainerServiceMasterProfile</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContainerServiceMasterProfile();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('dnsPrefix')) {
                obj['dnsPrefix'] = ApiClient.convertToType(data['dnsPrefix'], 'String');
            }
            if (data.hasOwnProperty('firstConsecutiveStaticIP')) {
                obj['firstConsecutiveStaticIP'] = ApiClient.convertToType(data['firstConsecutiveStaticIP'], 'String');
            }
            if (data.hasOwnProperty('fqdn')) {
                obj['fqdn'] = ApiClient.convertToType(data['fqdn'], 'String');
            }
            if (data.hasOwnProperty('osDiskSizeGB')) {
                obj['osDiskSizeGB'] = ApiClient.convertToType(data['osDiskSizeGB'], 'Number');
            }
            if (data.hasOwnProperty('storageProfile')) {
                obj['storageProfile'] = ContainerServiceStorageProfile.constructFromObject(data['storageProfile']);
            }
            if (data.hasOwnProperty('vmSize')) {
                obj['vmSize'] = ContainerServiceVMSize.constructFromObject(data['vmSize']);
            }
            if (data.hasOwnProperty('vnetSubnetID')) {
                obj['vnetSubnetID'] = ApiClient.convertToType(data['vnetSubnetID'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContainerServiceMasterProfile</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContainerServiceMasterProfile</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContainerServiceMasterProfile.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['dnsPrefix'] && !(typeof data['dnsPrefix'] === 'string' || data['dnsPrefix'] instanceof String)) {
            throw new Error("Expected the field `dnsPrefix` to be a primitive type in the JSON string but got " + data['dnsPrefix']);
        }
        // ensure the json data is a string
        if (data['firstConsecutiveStaticIP'] && !(typeof data['firstConsecutiveStaticIP'] === 'string' || data['firstConsecutiveStaticIP'] instanceof String)) {
            throw new Error("Expected the field `firstConsecutiveStaticIP` to be a primitive type in the JSON string but got " + data['firstConsecutiveStaticIP']);
        }
        // ensure the json data is a string
        if (data['fqdn'] && !(typeof data['fqdn'] === 'string' || data['fqdn'] instanceof String)) {
            throw new Error("Expected the field `fqdn` to be a primitive type in the JSON string but got " + data['fqdn']);
        }
        // ensure the json data is a string
        if (data['vnetSubnetID'] && !(typeof data['vnetSubnetID'] === 'string' || data['vnetSubnetID'] instanceof String)) {
            throw new Error("Expected the field `vnetSubnetID` to be a primitive type in the JSON string but got " + data['vnetSubnetID']);
        }

        return true;
    }


}

ContainerServiceMasterProfile.RequiredProperties = ["dnsPrefix", "vmSize"];

/**
 * Number of masters (VMs) in the container service cluster. Allowed values are 1, 3, and 5. The default value is 1.
 * @member {module:model/ContainerServiceMasterProfile.CountEnum} count
 * @default CountEnum.1
 */
ContainerServiceMasterProfile.prototype['count'] = CountEnum.1;

/**
 * DNS prefix to be used to create the FQDN for the master pool.
 * @member {String} dnsPrefix
 */
ContainerServiceMasterProfile.prototype['dnsPrefix'] = undefined;

/**
 * FirstConsecutiveStaticIP used to specify the first static ip of masters.
 * @member {String} firstConsecutiveStaticIP
 * @default '10.240.255.5'
 */
ContainerServiceMasterProfile.prototype['firstConsecutiveStaticIP'] = '10.240.255.5';

/**
 * FQDN for the master pool.
 * @member {String} fqdn
 */
ContainerServiceMasterProfile.prototype['fqdn'] = undefined;

/**
 * OS Disk Size in GB to be used to specify the disk size for every machine in this master/agent pool. If you specify 0, it will apply the default osDisk size according to the vmSize specified.
 * @member {Number} osDiskSizeGB
 */
ContainerServiceMasterProfile.prototype['osDiskSizeGB'] = undefined;

/**
 * @member {module:model/ContainerServiceStorageProfile} storageProfile
 */
ContainerServiceMasterProfile.prototype['storageProfile'] = undefined;

/**
 * @member {module:model/ContainerServiceVMSize} vmSize
 */
ContainerServiceMasterProfile.prototype['vmSize'] = undefined;

/**
 * VNet SubnetID specifies the VNet's subnet identifier.
 * @member {String} vnetSubnetID
 */
ContainerServiceMasterProfile.prototype['vnetSubnetID'] = undefined;





/**
 * Allowed values for the <code>count</code> property.
 * @enum {Number}
 * @readonly
 */
ContainerServiceMasterProfile['CountEnum'] = {

    /**
     * value: 1
     * @const
     */
    "1": 1,

    /**
     * value: 3
     * @const
     */
    "3": 3,

    /**
     * value: 5
     * @const
     */
    "5": 5
};



export default ContainerServiceMasterProfile;

