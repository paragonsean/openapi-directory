/**
 * ContainerServiceClient
 * The Container Service Client.
 *
 * The version of the OpenAPI document: 2018-08-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AgentPoolType from './AgentPoolType';
import ContainerServiceVMSize from './ContainerServiceVMSize';
import OSType from './OSType';

/**
 * The ManagedClusterAgentPoolProfile model module.
 * @module model/ManagedClusterAgentPoolProfile
 * @version 2018-08-01-preview
 */
class ManagedClusterAgentPoolProfile {
    /**
     * Constructs a new <code>ManagedClusterAgentPoolProfile</code>.
     * Profile for the container service agent pool.
     * @alias module:model/ManagedClusterAgentPoolProfile
     * @param count {Number} Number of agents (VMs) to host docker containers. Allowed values must be in the range of 1 to 100 (inclusive). The default value is 1. 
     * @param name {String} Unique name of the agent pool profile in the context of the subscription and resource group.
     * @param vmSize {module:model/ContainerServiceVMSize} 
     */
    constructor(count, name, vmSize) { 
        
        ManagedClusterAgentPoolProfile.initialize(this, count, name, vmSize);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count, name, vmSize) { 
        obj['count'] = count || 1;
        obj['name'] = name;
        obj['vmSize'] = vmSize;
    }

    /**
     * Constructs a <code>ManagedClusterAgentPoolProfile</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ManagedClusterAgentPoolProfile} obj Optional instance to populate.
     * @return {module:model/ManagedClusterAgentPoolProfile} The populated <code>ManagedClusterAgentPoolProfile</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ManagedClusterAgentPoolProfile();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('enableAutoScaling')) {
                obj['enableAutoScaling'] = ApiClient.convertToType(data['enableAutoScaling'], 'Boolean');
            }
            if (data.hasOwnProperty('maxCount')) {
                obj['maxCount'] = ApiClient.convertToType(data['maxCount'], 'Number');
            }
            if (data.hasOwnProperty('maxPods')) {
                obj['maxPods'] = ApiClient.convertToType(data['maxPods'], 'Number');
            }
            if (data.hasOwnProperty('minCount')) {
                obj['minCount'] = ApiClient.convertToType(data['minCount'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('osDiskSizeGB')) {
                obj['osDiskSizeGB'] = ApiClient.convertToType(data['osDiskSizeGB'], 'Number');
            }
            if (data.hasOwnProperty('osType')) {
                obj['osType'] = OSType.constructFromObject(data['osType']);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = AgentPoolType.constructFromObject(data['type']);
            }
            if (data.hasOwnProperty('vmSize')) {
                obj['vmSize'] = ContainerServiceVMSize.constructFromObject(data['vmSize']);
            }
            if (data.hasOwnProperty('vnetSubnetID')) {
                obj['vnetSubnetID'] = ApiClient.convertToType(data['vnetSubnetID'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ManagedClusterAgentPoolProfile</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ManagedClusterAgentPoolProfile</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ManagedClusterAgentPoolProfile.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['vnetSubnetID'] && !(typeof data['vnetSubnetID'] === 'string' || data['vnetSubnetID'] instanceof String)) {
            throw new Error("Expected the field `vnetSubnetID` to be a primitive type in the JSON string but got " + data['vnetSubnetID']);
        }

        return true;
    }


}

ManagedClusterAgentPoolProfile.RequiredProperties = ["count", "name", "vmSize"];

/**
 * Number of agents (VMs) to host docker containers. Allowed values must be in the range of 1 to 100 (inclusive). The default value is 1. 
 * @member {Number} count
 * @default 1
 */
ManagedClusterAgentPoolProfile.prototype['count'] = 1;

/**
 * Whether to enable auto-scaler
 * @member {Boolean} enableAutoScaling
 */
ManagedClusterAgentPoolProfile.prototype['enableAutoScaling'] = undefined;

/**
 * Maximum number of nodes for auto-scaling
 * @member {Number} maxCount
 */
ManagedClusterAgentPoolProfile.prototype['maxCount'] = undefined;

/**
 * Maximum number of pods that can run on a node.
 * @member {Number} maxPods
 */
ManagedClusterAgentPoolProfile.prototype['maxPods'] = undefined;

/**
 * Minimum number of nodes for auto-scaling
 * @member {Number} minCount
 */
ManagedClusterAgentPoolProfile.prototype['minCount'] = undefined;

/**
 * Unique name of the agent pool profile in the context of the subscription and resource group.
 * @member {String} name
 */
ManagedClusterAgentPoolProfile.prototype['name'] = undefined;

/**
 * OS Disk Size in GB to be used to specify the disk size for every machine in this master/agent pool. If you specify 0, it will apply the default osDisk size according to the vmSize specified.
 * @member {Number} osDiskSizeGB
 */
ManagedClusterAgentPoolProfile.prototype['osDiskSizeGB'] = undefined;

/**
 * @member {module:model/OSType} osType
 */
ManagedClusterAgentPoolProfile.prototype['osType'] = undefined;

/**
 * @member {module:model/AgentPoolType} type
 */
ManagedClusterAgentPoolProfile.prototype['type'] = undefined;

/**
 * @member {module:model/ContainerServiceVMSize} vmSize
 */
ManagedClusterAgentPoolProfile.prototype['vmSize'] = undefined;

/**
 * VNet SubnetID specifies the VNet's subnet identifier.
 * @member {String} vnetSubnetID
 */
ManagedClusterAgentPoolProfile.prototype['vnetSubnetID'] = undefined;






export default ManagedClusterAgentPoolProfile;

