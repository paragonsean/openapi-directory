/**
 * ContainerServiceClient
 * The Container Service Client.
 *
 * The version of the OpenAPI document: 2017-08-31
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KeyVaultSecretRef from './KeyVaultSecretRef';

/**
 * The ContainerServiceServicePrincipalProfile model module.
 * @module model/ContainerServiceServicePrincipalProfile
 * @version 2017-08-31
 */
class ContainerServiceServicePrincipalProfile {
    /**
     * Constructs a new <code>ContainerServiceServicePrincipalProfile</code>.
     * Information about a service principal identity for the cluster to use for manipulating Azure APIs. Either secret or keyVaultSecretRef must be specified.
     * @alias module:model/ContainerServiceServicePrincipalProfile
     * @param clientId {String} The ID for the service principal.
     */
    constructor(clientId) { 
        
        ContainerServiceServicePrincipalProfile.initialize(this, clientId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, clientId) { 
        obj['clientId'] = clientId;
    }

    /**
     * Constructs a <code>ContainerServiceServicePrincipalProfile</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContainerServiceServicePrincipalProfile} obj Optional instance to populate.
     * @return {module:model/ContainerServiceServicePrincipalProfile} The populated <code>ContainerServiceServicePrincipalProfile</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContainerServiceServicePrincipalProfile();

            if (data.hasOwnProperty('clientId')) {
                obj['clientId'] = ApiClient.convertToType(data['clientId'], 'String');
            }
            if (data.hasOwnProperty('keyVaultSecretRef')) {
                obj['keyVaultSecretRef'] = KeyVaultSecretRef.constructFromObject(data['keyVaultSecretRef']);
            }
            if (data.hasOwnProperty('secret')) {
                obj['secret'] = ApiClient.convertToType(data['secret'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContainerServiceServicePrincipalProfile</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContainerServiceServicePrincipalProfile</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContainerServiceServicePrincipalProfile.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['clientId'] && !(typeof data['clientId'] === 'string' || data['clientId'] instanceof String)) {
            throw new Error("Expected the field `clientId` to be a primitive type in the JSON string but got " + data['clientId']);
        }
        // validate the optional field `keyVaultSecretRef`
        if (data['keyVaultSecretRef']) { // data not null
          KeyVaultSecretRef.validateJSON(data['keyVaultSecretRef']);
        }
        // ensure the json data is a string
        if (data['secret'] && !(typeof data['secret'] === 'string' || data['secret'] instanceof String)) {
            throw new Error("Expected the field `secret` to be a primitive type in the JSON string but got " + data['secret']);
        }

        return true;
    }


}

ContainerServiceServicePrincipalProfile.RequiredProperties = ["clientId"];

/**
 * The ID for the service principal.
 * @member {String} clientId
 */
ContainerServiceServicePrincipalProfile.prototype['clientId'] = undefined;

/**
 * @member {module:model/KeyVaultSecretRef} keyVaultSecretRef
 */
ContainerServiceServicePrincipalProfile.prototype['keyVaultSecretRef'] = undefined;

/**
 * The secret password associated with the service principal in plain text.
 * @member {String} secret
 */
ContainerServiceServicePrincipalProfile.prototype['secret'] = undefined;






export default ContainerServiceServicePrincipalProfile;

