/**
 * ContainerServiceClient
 * The Container Service Client.
 *
 * The version of the OpenAPI document: 2019-04-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContainerServiceSshConfiguration from './ContainerServiceSshConfiguration';

/**
 * The ContainerServiceLinuxProfile model module.
 * @module model/ContainerServiceLinuxProfile
 * @version 2019-04-01
 */
class ContainerServiceLinuxProfile {
    /**
     * Constructs a new <code>ContainerServiceLinuxProfile</code>.
     * Profile for Linux VMs in the container service cluster.
     * @alias module:model/ContainerServiceLinuxProfile
     * @param adminUsername {String} The administrator username to use for Linux VMs.
     * @param ssh {module:model/ContainerServiceSshConfiguration} 
     */
    constructor(adminUsername, ssh) { 
        
        ContainerServiceLinuxProfile.initialize(this, adminUsername, ssh);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, adminUsername, ssh) { 
        obj['adminUsername'] = adminUsername;
        obj['ssh'] = ssh;
    }

    /**
     * Constructs a <code>ContainerServiceLinuxProfile</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContainerServiceLinuxProfile} obj Optional instance to populate.
     * @return {module:model/ContainerServiceLinuxProfile} The populated <code>ContainerServiceLinuxProfile</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContainerServiceLinuxProfile();

            if (data.hasOwnProperty('adminUsername')) {
                obj['adminUsername'] = ApiClient.convertToType(data['adminUsername'], 'String');
            }
            if (data.hasOwnProperty('ssh')) {
                obj['ssh'] = ContainerServiceSshConfiguration.constructFromObject(data['ssh']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContainerServiceLinuxProfile</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContainerServiceLinuxProfile</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContainerServiceLinuxProfile.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['adminUsername'] && !(typeof data['adminUsername'] === 'string' || data['adminUsername'] instanceof String)) {
            throw new Error("Expected the field `adminUsername` to be a primitive type in the JSON string but got " + data['adminUsername']);
        }
        // validate the optional field `ssh`
        if (data['ssh']) { // data not null
          ContainerServiceSshConfiguration.validateJSON(data['ssh']);
        }

        return true;
    }


}

ContainerServiceLinuxProfile.RequiredProperties = ["adminUsername", "ssh"];

/**
 * The administrator username to use for Linux VMs.
 * @member {String} adminUsername
 */
ContainerServiceLinuxProfile.prototype['adminUsername'] = undefined;

/**
 * @member {module:model/ContainerServiceSshConfiguration} ssh
 */
ContainerServiceLinuxProfile.prototype['ssh'] = undefined;






export default ContainerServiceLinuxProfile;

