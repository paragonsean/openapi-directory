/**
 * DataShareManagementClient
 * Creates a Microsoft.DataShare management client.
 *
 * The version of the OpenAPI document: 2019-11-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ShareSubscriptionSynchronization model module.
 * @module model/ShareSubscriptionSynchronization
 * @version 2019-11-01
 */
class ShareSubscriptionSynchronization {
    /**
     * Constructs a new <code>ShareSubscriptionSynchronization</code>.
     * A ShareSubscriptionSynchronization data transfer object.
     * @alias module:model/ShareSubscriptionSynchronization
     * @param synchronizationId {String} Synchronization id
     */
    constructor(synchronizationId) { 
        
        ShareSubscriptionSynchronization.initialize(this, synchronizationId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, synchronizationId) { 
        obj['synchronizationId'] = synchronizationId;
    }

    /**
     * Constructs a <code>ShareSubscriptionSynchronization</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ShareSubscriptionSynchronization} obj Optional instance to populate.
     * @return {module:model/ShareSubscriptionSynchronization} The populated <code>ShareSubscriptionSynchronization</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ShareSubscriptionSynchronization();

            if (data.hasOwnProperty('durationMs')) {
                obj['durationMs'] = ApiClient.convertToType(data['durationMs'], 'Number');
            }
            if (data.hasOwnProperty('endTime')) {
                obj['endTime'] = ApiClient.convertToType(data['endTime'], 'Date');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('startTime')) {
                obj['startTime'] = ApiClient.convertToType(data['startTime'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('synchronizationId')) {
                obj['synchronizationId'] = ApiClient.convertToType(data['synchronizationId'], 'String');
            }
            if (data.hasOwnProperty('synchronizationMode')) {
                obj['synchronizationMode'] = ApiClient.convertToType(data['synchronizationMode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ShareSubscriptionSynchronization</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ShareSubscriptionSynchronization</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ShareSubscriptionSynchronization.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['synchronizationId'] && !(typeof data['synchronizationId'] === 'string' || data['synchronizationId'] instanceof String)) {
            throw new Error("Expected the field `synchronizationId` to be a primitive type in the JSON string but got " + data['synchronizationId']);
        }
        // ensure the json data is a string
        if (data['synchronizationMode'] && !(typeof data['synchronizationMode'] === 'string' || data['synchronizationMode'] instanceof String)) {
            throw new Error("Expected the field `synchronizationMode` to be a primitive type in the JSON string but got " + data['synchronizationMode']);
        }

        return true;
    }


}

ShareSubscriptionSynchronization.RequiredProperties = ["synchronizationId"];

/**
 * Synchronization duration
 * @member {Number} durationMs
 */
ShareSubscriptionSynchronization.prototype['durationMs'] = undefined;

/**
 * End time of synchronization
 * @member {Date} endTime
 */
ShareSubscriptionSynchronization.prototype['endTime'] = undefined;

/**
 * message of Synchronization
 * @member {String} message
 */
ShareSubscriptionSynchronization.prototype['message'] = undefined;

/**
 * start time of synchronization
 * @member {Date} startTime
 */
ShareSubscriptionSynchronization.prototype['startTime'] = undefined;

/**
 * Raw Status
 * @member {String} status
 */
ShareSubscriptionSynchronization.prototype['status'] = undefined;

/**
 * Synchronization id
 * @member {String} synchronizationId
 */
ShareSubscriptionSynchronization.prototype['synchronizationId'] = undefined;

/**
 * Synchronization Mode
 * @member {module:model/ShareSubscriptionSynchronization.SynchronizationModeEnum} synchronizationMode
 */
ShareSubscriptionSynchronization.prototype['synchronizationMode'] = undefined;





/**
 * Allowed values for the <code>synchronizationMode</code> property.
 * @enum {String}
 * @readonly
 */
ShareSubscriptionSynchronization['SynchronizationModeEnum'] = {

    /**
     * value: "Incremental"
     * @const
     */
    "Incremental": "Incremental",

    /**
     * value: "FullSync"
     * @const
     */
    "FullSync": "FullSync"
};



export default ShareSubscriptionSynchronization;

