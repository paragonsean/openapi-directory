/**
 * ApplicationInsightsManagementClient
 * Azure Application Insights workbook type.
 *
 * The version of the OpenAPI document: 2015-05-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WorkbookProperties model module.
 * @module model/WorkbookProperties
 * @version 2015-05-01
 */
class WorkbookProperties {
    /**
     * Constructs a new <code>WorkbookProperties</code>.
     * Properties that contain a workbook.
     * @alias module:model/WorkbookProperties
     * @param category {String} Workbook category, as defined by the user at creation time.
     * @param kind {module:model/WorkbookProperties.KindEnum} Enum indicating if this workbook definition is owned by a specific user or is shared between all users with access to the Application Insights component.
     * @param name {String} The user-defined name of the workbook.
     * @param serializedData {String} Configuration of this particular workbook. Configuration data is a string containing valid JSON
     * @param userId {String} Unique user id of the specific user that owns this workbook.
     * @param workbookId {String} Internally assigned unique id of the workbook definition.
     */
    constructor(category, kind, name, serializedData, userId, workbookId) { 
        
        WorkbookProperties.initialize(this, category, kind, name, serializedData, userId, workbookId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, category, kind, name, serializedData, userId, workbookId) { 
        obj['category'] = category;
        obj['kind'] = kind || 'shared';
        obj['name'] = name;
        obj['serializedData'] = serializedData;
        obj['userId'] = userId;
        obj['workbookId'] = workbookId;
    }

    /**
     * Constructs a <code>WorkbookProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkbookProperties} obj Optional instance to populate.
     * @return {module:model/WorkbookProperties} The populated <code>WorkbookProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkbookProperties();

            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('serializedData')) {
                obj['serializedData'] = ApiClient.convertToType(data['serializedData'], 'String');
            }
            if (data.hasOwnProperty('sourceResourceId')) {
                obj['sourceResourceId'] = ApiClient.convertToType(data['sourceResourceId'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], ['String']);
            }
            if (data.hasOwnProperty('timeModified')) {
                obj['timeModified'] = ApiClient.convertToType(data['timeModified'], 'String');
            }
            if (data.hasOwnProperty('userId')) {
                obj['userId'] = ApiClient.convertToType(data['userId'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
            if (data.hasOwnProperty('workbookId')) {
                obj['workbookId'] = ApiClient.convertToType(data['workbookId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkbookProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkbookProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WorkbookProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['category'] && !(typeof data['category'] === 'string' || data['category'] instanceof String)) {
            throw new Error("Expected the field `category` to be a primitive type in the JSON string but got " + data['category']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['serializedData'] && !(typeof data['serializedData'] === 'string' || data['serializedData'] instanceof String)) {
            throw new Error("Expected the field `serializedData` to be a primitive type in the JSON string but got " + data['serializedData']);
        }
        // ensure the json data is a string
        if (data['sourceResourceId'] && !(typeof data['sourceResourceId'] === 'string' || data['sourceResourceId'] instanceof String)) {
            throw new Error("Expected the field `sourceResourceId` to be a primitive type in the JSON string but got " + data['sourceResourceId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }
        // ensure the json data is a string
        if (data['timeModified'] && !(typeof data['timeModified'] === 'string' || data['timeModified'] instanceof String)) {
            throw new Error("Expected the field `timeModified` to be a primitive type in the JSON string but got " + data['timeModified']);
        }
        // ensure the json data is a string
        if (data['userId'] && !(typeof data['userId'] === 'string' || data['userId'] instanceof String)) {
            throw new Error("Expected the field `userId` to be a primitive type in the JSON string but got " + data['userId']);
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }
        // ensure the json data is a string
        if (data['workbookId'] && !(typeof data['workbookId'] === 'string' || data['workbookId'] instanceof String)) {
            throw new Error("Expected the field `workbookId` to be a primitive type in the JSON string but got " + data['workbookId']);
        }

        return true;
    }


}

WorkbookProperties.RequiredProperties = ["category", "kind", "name", "serializedData", "userId", "workbookId"];

/**
 * Workbook category, as defined by the user at creation time.
 * @member {String} category
 */
WorkbookProperties.prototype['category'] = undefined;

/**
 * Enum indicating if this workbook definition is owned by a specific user or is shared between all users with access to the Application Insights component.
 * @member {module:model/WorkbookProperties.KindEnum} kind
 * @default 'shared'
 */
WorkbookProperties.prototype['kind'] = 'shared';

/**
 * The user-defined name of the workbook.
 * @member {String} name
 */
WorkbookProperties.prototype['name'] = undefined;

/**
 * Configuration of this particular workbook. Configuration data is a string containing valid JSON
 * @member {String} serializedData
 */
WorkbookProperties.prototype['serializedData'] = undefined;

/**
 * Optional resourceId for a source resource.
 * @member {String} sourceResourceId
 */
WorkbookProperties.prototype['sourceResourceId'] = undefined;

/**
 * A list of 0 or more tags that are associated with this workbook definition
 * @member {Array.<String>} tags
 */
WorkbookProperties.prototype['tags'] = undefined;

/**
 * Date and time in UTC of the last modification that was made to this workbook definition.
 * @member {String} timeModified
 */
WorkbookProperties.prototype['timeModified'] = undefined;

/**
 * Unique user id of the specific user that owns this workbook.
 * @member {String} userId
 */
WorkbookProperties.prototype['userId'] = undefined;

/**
 * This instance's version of the data model. This can change as new features are added that can be marked workbook.
 * @member {String} version
 */
WorkbookProperties.prototype['version'] = undefined;

/**
 * Internally assigned unique id of the workbook definition.
 * @member {String} workbookId
 */
WorkbookProperties.prototype['workbookId'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
WorkbookProperties['KindEnum'] = {

    /**
     * value: "shared"
     * @const
     */
    "shared": "shared",

    /**
     * value: "user"
     * @const
     */
    "user": "user"
};



export default WorkbookProperties;

