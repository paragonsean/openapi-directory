/**
 * KeyVaultManagementClient
 * The Azure management API provides a RESTful set of web services that interact with Azure Key Vault.
 *
 * The version of the OpenAPI document: 2018-02-14-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Sku model module.
 * @module model/Sku
 * @version 2018-02-14-preview
 */
class Sku {
    /**
     * Constructs a new <code>Sku</code>.
     * SKU details
     * @alias module:model/Sku
     * @param family {module:model/Sku.FamilyEnum} SKU family name
     * @param name {module:model/Sku.NameEnum} SKU name to specify whether the key vault is a standard vault or a premium vault.
     */
    constructor(family, name) { 
        
        Sku.initialize(this, family, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, family, name) { 
        obj['family'] = family;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>Sku</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Sku} obj Optional instance to populate.
     * @return {module:model/Sku} The populated <code>Sku</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Sku();

            if (data.hasOwnProperty('family')) {
                obj['family'] = ApiClient.convertToType(data['family'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Sku</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Sku</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Sku.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['family'] && !(typeof data['family'] === 'string' || data['family'] instanceof String)) {
            throw new Error("Expected the field `family` to be a primitive type in the JSON string but got " + data['family']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

Sku.RequiredProperties = ["family", "name"];

/**
 * SKU family name
 * @member {module:model/Sku.FamilyEnum} family
 */
Sku.prototype['family'] = undefined;

/**
 * SKU name to specify whether the key vault is a standard vault or a premium vault.
 * @member {module:model/Sku.NameEnum} name
 */
Sku.prototype['name'] = undefined;





/**
 * Allowed values for the <code>family</code> property.
 * @enum {String}
 * @readonly
 */
Sku['FamilyEnum'] = {

    /**
     * value: "A"
     * @const
     */
    "A": "A"
};


/**
 * Allowed values for the <code>name</code> property.
 * @enum {String}
 * @readonly
 */
Sku['NameEnum'] = {

    /**
     * value: "standard"
     * @const
     */
    "standard": "standard",

    /**
     * value: "premium"
     * @const
     */
    "premium": "premium"
};



export default Sku;

