/**
 * Form Recognizer Client
 * Extracts information from forms and images into structured data.
 *
 * The version of the OpenAPI document: 2.0-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Language from './Language';
import TextWord from './TextWord';

/**
 * The TextLine model module.
 * @module model/TextLine
 * @version 2.0-preview
 */
class TextLine {
    /**
     * Constructs a new <code>TextLine</code>.
     * An object representing an extracted text line.
     * @alias module:model/TextLine
     * @param boundingBox {Array.<Number>} Quadrangle bounding box, with coordinates specified relative to the top-left of the original image. The eight numbers represent the four points, clockwise from the top-left corner relative to the text orientation. For image, the (x, y) coordinates are measured in pixels. For PDF, the (x, y) coordinates are measured in inches.
     * @param text {String} The text content of the line.
     * @param words {Array.<module:model/TextWord>} List of words in the text line.
     */
    constructor(boundingBox, text, words) { 
        
        TextLine.initialize(this, boundingBox, text, words);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, boundingBox, text, words) { 
        obj['boundingBox'] = boundingBox;
        obj['text'] = text;
        obj['words'] = words;
    }

    /**
     * Constructs a <code>TextLine</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TextLine} obj Optional instance to populate.
     * @return {module:model/TextLine} The populated <code>TextLine</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TextLine();

            if (data.hasOwnProperty('boundingBox')) {
                obj['boundingBox'] = ApiClient.convertToType(data['boundingBox'], ['Number']);
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = Language.constructFromObject(data['language']);
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('words')) {
                obj['words'] = ApiClient.convertToType(data['words'], [TextWord]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TextLine</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TextLine</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TextLine.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['boundingBox'])) {
            throw new Error("Expected the field `boundingBox` to be an array in the JSON data but got " + data['boundingBox']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        if (data['words']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['words'])) {
                throw new Error("Expected the field `words` to be an array in the JSON data but got " + data['words']);
            }
            // validate the optional field `words` (array)
            for (const item of data['words']) {
                TextWord.validateJSON(item);
            };
        }

        return true;
    }


}

TextLine.RequiredProperties = ["boundingBox", "text", "words"];

/**
 * Quadrangle bounding box, with coordinates specified relative to the top-left of the original image. The eight numbers represent the four points, clockwise from the top-left corner relative to the text orientation. For image, the (x, y) coordinates are measured in pixels. For PDF, the (x, y) coordinates are measured in inches.
 * @member {Array.<Number>} boundingBox
 */
TextLine.prototype['boundingBox'] = undefined;

/**
 * @member {module:model/Language} language
 */
TextLine.prototype['language'] = undefined;

/**
 * The text content of the line.
 * @member {String} text
 */
TextLine.prototype['text'] = undefined;

/**
 * List of words in the text line.
 * @member {Array.<module:model/TextWord>} words
 */
TextLine.prototype['words'] = undefined;






export default TextLine;

