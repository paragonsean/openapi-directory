/**
 * Form Recognizer Client
 * Extracts information from forms and images into structured data.
 *
 * The version of the OpenAPI document: 2.0-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DataTableCell model module.
 * @module model/DataTableCell
 * @version 2.0-preview
 */
class DataTableCell {
    /**
     * Constructs a new <code>DataTableCell</code>.
     * Information about the extracted cell in a table.
     * @alias module:model/DataTableCell
     * @param boundingBox {Array.<Number>} Quadrangle bounding box, with coordinates specified relative to the top-left of the original image. The eight numbers represent the four points, clockwise from the top-left corner relative to the text orientation. For image, the (x, y) coordinates are measured in pixels. For PDF, the (x, y) coordinates are measured in inches.
     * @param columnIndex {Number} Column index of the cell.
     * @param confidence {Number} Confidence value.
     * @param rowIndex {Number} Row index of the cell.
     * @param text {String} Text content of the cell.
     */
    constructor(boundingBox, columnIndex, confidence, rowIndex, text) { 
        
        DataTableCell.initialize(this, boundingBox, columnIndex, confidence, rowIndex, text);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, boundingBox, columnIndex, confidence, rowIndex, text) { 
        obj['boundingBox'] = boundingBox;
        obj['columnIndex'] = columnIndex;
        obj['confidence'] = confidence;
        obj['isFooter'] = false;
        obj['isHeader'] = false;
        obj['rowIndex'] = rowIndex;
        obj['text'] = text;
    }

    /**
     * Constructs a <code>DataTableCell</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DataTableCell} obj Optional instance to populate.
     * @return {module:model/DataTableCell} The populated <code>DataTableCell</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DataTableCell();

            if (data.hasOwnProperty('boundingBox')) {
                obj['boundingBox'] = ApiClient.convertToType(data['boundingBox'], ['Number']);
            }
            if (data.hasOwnProperty('columnIndex')) {
                obj['columnIndex'] = ApiClient.convertToType(data['columnIndex'], 'Number');
            }
            if (data.hasOwnProperty('columnSpan')) {
                obj['columnSpan'] = ApiClient.convertToType(data['columnSpan'], 'Number');
            }
            if (data.hasOwnProperty('confidence')) {
                obj['confidence'] = ApiClient.convertToType(data['confidence'], 'Number');
            }
            if (data.hasOwnProperty('elements')) {
                obj['elements'] = ApiClient.convertToType(data['elements'], ['String']);
            }
            if (data.hasOwnProperty('isFooter')) {
                obj['isFooter'] = ApiClient.convertToType(data['isFooter'], 'Boolean');
            }
            if (data.hasOwnProperty('isHeader')) {
                obj['isHeader'] = ApiClient.convertToType(data['isHeader'], 'Boolean');
            }
            if (data.hasOwnProperty('rowIndex')) {
                obj['rowIndex'] = ApiClient.convertToType(data['rowIndex'], 'Number');
            }
            if (data.hasOwnProperty('rowSpan')) {
                obj['rowSpan'] = ApiClient.convertToType(data['rowSpan'], 'Number');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DataTableCell</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DataTableCell</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DataTableCell.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['boundingBox'])) {
            throw new Error("Expected the field `boundingBox` to be an array in the JSON data but got " + data['boundingBox']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['elements'])) {
            throw new Error("Expected the field `elements` to be an array in the JSON data but got " + data['elements']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }

        return true;
    }


}

DataTableCell.RequiredProperties = ["boundingBox", "columnIndex", "confidence", "rowIndex", "text"];

/**
 * Quadrangle bounding box, with coordinates specified relative to the top-left of the original image. The eight numbers represent the four points, clockwise from the top-left corner relative to the text orientation. For image, the (x, y) coordinates are measured in pixels. For PDF, the (x, y) coordinates are measured in inches.
 * @member {Array.<Number>} boundingBox
 */
DataTableCell.prototype['boundingBox'] = undefined;

/**
 * Column index of the cell.
 * @member {Number} columnIndex
 */
DataTableCell.prototype['columnIndex'] = undefined;

/**
 * Number of columns spanned by this cell.
 * @member {Number} columnSpan
 */
DataTableCell.prototype['columnSpan'] = undefined;

/**
 * Confidence value.
 * @member {Number} confidence
 */
DataTableCell.prototype['confidence'] = undefined;

/**
 * When includeTextDetails is set to true, a list of references to the text elements constituting this table cell.
 * @member {Array.<String>} elements
 */
DataTableCell.prototype['elements'] = undefined;

/**
 * Is the current cell a footer cell?
 * @member {Boolean} isFooter
 * @default false
 */
DataTableCell.prototype['isFooter'] = false;

/**
 * Is the current cell a header cell?
 * @member {Boolean} isHeader
 * @default false
 */
DataTableCell.prototype['isHeader'] = false;

/**
 * Row index of the cell.
 * @member {Number} rowIndex
 */
DataTableCell.prototype['rowIndex'] = undefined;

/**
 * Number of rows spanned by this cell.
 * @member {Number} rowSpan
 */
DataTableCell.prototype['rowSpan'] = undefined;

/**
 * Text content of the cell.
 * @member {String} text
 */
DataTableCell.prototype['text'] = undefined;






export default DataTableCell;

