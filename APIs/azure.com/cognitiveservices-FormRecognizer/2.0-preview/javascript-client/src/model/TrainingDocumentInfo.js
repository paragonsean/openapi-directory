/**
 * Form Recognizer Client
 * Extracts information from forms and images into structured data.
 *
 * The version of the OpenAPI document: 2.0-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ErrorInformation from './ErrorInformation';

/**
 * The TrainingDocumentInfo model module.
 * @module model/TrainingDocumentInfo
 * @version 2.0-preview
 */
class TrainingDocumentInfo {
    /**
     * Constructs a new <code>TrainingDocumentInfo</code>.
     * Report for a custom model training document.
     * @alias module:model/TrainingDocumentInfo
     * @param documentName {String} Training document name.
     * @param errors {Array.<module:model/ErrorInformation>} List of errors.
     * @param pages {Number} Total number of pages trained.
     * @param status {module:model/TrainingDocumentInfo.StatusEnum} Status of the training operation.
     */
    constructor(documentName, errors, pages, status) { 
        
        TrainingDocumentInfo.initialize(this, documentName, errors, pages, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, documentName, errors, pages, status) { 
        obj['documentName'] = documentName;
        obj['errors'] = errors;
        obj['pages'] = pages;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>TrainingDocumentInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrainingDocumentInfo} obj Optional instance to populate.
     * @return {module:model/TrainingDocumentInfo} The populated <code>TrainingDocumentInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrainingDocumentInfo();

            if (data.hasOwnProperty('documentName')) {
                obj['documentName'] = ApiClient.convertToType(data['documentName'], 'String');
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [ErrorInformation]);
            }
            if (data.hasOwnProperty('pages')) {
                obj['pages'] = ApiClient.convertToType(data['pages'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrainingDocumentInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrainingDocumentInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrainingDocumentInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['documentName'] && !(typeof data['documentName'] === 'string' || data['documentName'] instanceof String)) {
            throw new Error("Expected the field `documentName` to be a primitive type in the JSON string but got " + data['documentName']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                ErrorInformation.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

TrainingDocumentInfo.RequiredProperties = ["documentName", "errors", "pages", "status"];

/**
 * Training document name.
 * @member {String} documentName
 */
TrainingDocumentInfo.prototype['documentName'] = undefined;

/**
 * List of errors.
 * @member {Array.<module:model/ErrorInformation>} errors
 */
TrainingDocumentInfo.prototype['errors'] = undefined;

/**
 * Total number of pages trained.
 * @member {Number} pages
 */
TrainingDocumentInfo.prototype['pages'] = undefined;

/**
 * Status of the training operation.
 * @member {module:model/TrainingDocumentInfo.StatusEnum} status
 */
TrainingDocumentInfo.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
TrainingDocumentInfo['StatusEnum'] = {

    /**
     * value: "succeeded"
     * @const
     */
    "succeeded": "succeeded",

    /**
     * value: "partiallySucceeded"
     * @const
     */
    "partiallySucceeded": "partiallySucceeded",

    /**
     * value: "failed"
     * @const
     */
    "failed": "failed"
};



export default TrainingDocumentInfo;

