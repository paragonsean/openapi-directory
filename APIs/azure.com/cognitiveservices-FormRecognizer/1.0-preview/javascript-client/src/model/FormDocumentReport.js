/**
 * Form Recognizer Client
 * Extracts information from forms and images into structured data based on a model created by a set of representative training forms.
 *
 * The version of the OpenAPI document: 1.0-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FormDocumentReport model module.
 * @module model/FormDocumentReport
 * @version 1.0-preview
 */
class FormDocumentReport {
    /**
     * Constructs a new <code>FormDocumentReport</code>.
     * @alias module:model/FormDocumentReport
     */
    constructor() { 
        
        FormDocumentReport.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FormDocumentReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FormDocumentReport} obj Optional instance to populate.
     * @return {module:model/FormDocumentReport} The populated <code>FormDocumentReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FormDocumentReport();

            if (data.hasOwnProperty('documentName')) {
                obj['documentName'] = ApiClient.convertToType(data['documentName'], 'String');
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], ['String']);
            }
            if (data.hasOwnProperty('pages')) {
                obj['pages'] = ApiClient.convertToType(data['pages'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FormDocumentReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FormDocumentReport</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['documentName'] && !(typeof data['documentName'] === 'string' || data['documentName'] instanceof String)) {
            throw new Error("Expected the field `documentName` to be a primitive type in the JSON string but got " + data['documentName']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['errors'])) {
            throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}



/**
 * Reference to the data that the report is for.
 * @member {String} documentName
 */
FormDocumentReport.prototype['documentName'] = undefined;

/**
 * List of errors per page.
 * @member {Array.<String>} errors
 */
FormDocumentReport.prototype['errors'] = undefined;

/**
 * Total number of pages trained on.
 * @member {Number} pages
 */
FormDocumentReport.prototype['pages'] = undefined;

/**
 * Status of the training operation.
 * @member {module:model/FormDocumentReport.StatusEnum} status
 */
FormDocumentReport.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
FormDocumentReport['StatusEnum'] = {

    /**
     * value: "success"
     * @const
     */
    "success": "success",

    /**
     * value: "partialSuccess"
     * @const
     */
    "partialSuccess": "partialSuccess",

    /**
     * value: "failure"
     * @const
     */
    "failure": "failure"
};



export default FormDocumentReport;

