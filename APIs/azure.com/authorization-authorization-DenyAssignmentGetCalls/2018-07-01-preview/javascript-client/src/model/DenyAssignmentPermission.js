/**
 * AuthorizationManagementClient
 * Role based access control provides you a way to apply granular level policy administration down to individual resources or resource groups. These operations enable you to get deny assignments. A deny assignment describes the set of actions on resources that are denied for Azure Active Directory users.
 *
 * The version of the OpenAPI document: 2018-07-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DenyAssignmentPermission model module.
 * @module model/DenyAssignmentPermission
 * @version 2018-07-01-preview
 */
class DenyAssignmentPermission {
    /**
     * Constructs a new <code>DenyAssignmentPermission</code>.
     * Deny assignment permissions.
     * @alias module:model/DenyAssignmentPermission
     */
    constructor() { 
        
        DenyAssignmentPermission.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DenyAssignmentPermission</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DenyAssignmentPermission} obj Optional instance to populate.
     * @return {module:model/DenyAssignmentPermission} The populated <code>DenyAssignmentPermission</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DenyAssignmentPermission();

            if (data.hasOwnProperty('actions')) {
                obj['actions'] = ApiClient.convertToType(data['actions'], ['String']);
            }
            if (data.hasOwnProperty('dataActions')) {
                obj['dataActions'] = ApiClient.convertToType(data['dataActions'], ['String']);
            }
            if (data.hasOwnProperty('notActions')) {
                obj['notActions'] = ApiClient.convertToType(data['notActions'], ['String']);
            }
            if (data.hasOwnProperty('notDataActions')) {
                obj['notDataActions'] = ApiClient.convertToType(data['notDataActions'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DenyAssignmentPermission</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DenyAssignmentPermission</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['actions'])) {
            throw new Error("Expected the field `actions` to be an array in the JSON data but got " + data['actions']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['dataActions'])) {
            throw new Error("Expected the field `dataActions` to be an array in the JSON data but got " + data['dataActions']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['notActions'])) {
            throw new Error("Expected the field `notActions` to be an array in the JSON data but got " + data['notActions']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['notDataActions'])) {
            throw new Error("Expected the field `notDataActions` to be an array in the JSON data but got " + data['notDataActions']);
        }

        return true;
    }


}



/**
 * Actions to which the deny assignment does not grant access.
 * @member {Array.<String>} actions
 */
DenyAssignmentPermission.prototype['actions'] = undefined;

/**
 * Data actions to which the deny assignment does not grant access.
 * @member {Array.<String>} dataActions
 */
DenyAssignmentPermission.prototype['dataActions'] = undefined;

/**
 * Actions to exclude from that the deny assignment does not grant access.
 * @member {Array.<String>} notActions
 */
DenyAssignmentPermission.prototype['notActions'] = undefined;

/**
 * Data actions to exclude from that the deny assignment does not grant access.
 * @member {Array.<String>} notDataActions
 */
DenyAssignmentPermission.prototype['notDataActions'] = undefined;






export default DenyAssignmentPermission;

