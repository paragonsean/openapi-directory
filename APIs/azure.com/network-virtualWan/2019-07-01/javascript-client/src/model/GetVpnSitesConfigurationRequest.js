/**
 * VirtualWANAsAServiceManagementClient
 * REST API for Azure VirtualWAN As a Service.
 *
 * The version of the OpenAPI document: 2019-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetVpnSitesConfigurationRequest model module.
 * @module model/GetVpnSitesConfigurationRequest
 * @version 2019-07-01
 */
class GetVpnSitesConfigurationRequest {
    /**
     * Constructs a new <code>GetVpnSitesConfigurationRequest</code>.
     * List of Vpn-Sites.
     * @alias module:model/GetVpnSitesConfigurationRequest
     * @param outputBlobSasUrl {String} The sas-url to download the configurations for vpn-sites.
     */
    constructor(outputBlobSasUrl) { 
        
        GetVpnSitesConfigurationRequest.initialize(this, outputBlobSasUrl);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, outputBlobSasUrl) { 
        obj['outputBlobSasUrl'] = outputBlobSasUrl;
    }

    /**
     * Constructs a <code>GetVpnSitesConfigurationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetVpnSitesConfigurationRequest} obj Optional instance to populate.
     * @return {module:model/GetVpnSitesConfigurationRequest} The populated <code>GetVpnSitesConfigurationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetVpnSitesConfigurationRequest();

            if (data.hasOwnProperty('outputBlobSasUrl')) {
                obj['outputBlobSasUrl'] = ApiClient.convertToType(data['outputBlobSasUrl'], 'String');
            }
            if (data.hasOwnProperty('vpnSites')) {
                obj['vpnSites'] = ApiClient.convertToType(data['vpnSites'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetVpnSitesConfigurationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetVpnSitesConfigurationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetVpnSitesConfigurationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['outputBlobSasUrl'] && !(typeof data['outputBlobSasUrl'] === 'string' || data['outputBlobSasUrl'] instanceof String)) {
            throw new Error("Expected the field `outputBlobSasUrl` to be a primitive type in the JSON string but got " + data['outputBlobSasUrl']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['vpnSites'])) {
            throw new Error("Expected the field `vpnSites` to be an array in the JSON data but got " + data['vpnSites']);
        }

        return true;
    }


}

GetVpnSitesConfigurationRequest.RequiredProperties = ["outputBlobSasUrl"];

/**
 * The sas-url to download the configurations for vpn-sites.
 * @member {String} outputBlobSasUrl
 */
GetVpnSitesConfigurationRequest.prototype['outputBlobSasUrl'] = undefined;

/**
 * List of resource-ids of the vpn-sites for which config is to be downloaded.
 * @member {Array.<String>} vpnSites
 */
GetVpnSitesConfigurationRequest.prototype['vpnSites'] = undefined;






export default GetVpnSitesConfigurationRequest;

