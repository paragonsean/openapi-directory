/**
 * Azure Migrate Hub
 * Migrate your workloads to Azure.
 *
 * The version of the OpenAPI document: 2018-09-01-preview
 * Contact: azuremigratehub@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AssessmentDetails from './AssessmentDetails';
import DiscoveryDetails from './DiscoveryDetails';
import MigrationDetails from './MigrationDetails';

/**
 * The MachineProperties model module.
 * @module model/MachineProperties
 * @version 2018-09-01-preview
 */
class MachineProperties {
    /**
     * Constructs a new <code>MachineProperties</code>.
     * Properties of the machine resource.
     * @alias module:model/MachineProperties
     */
    constructor() { 
        
        MachineProperties.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>MachineProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MachineProperties} obj Optional instance to populate.
     * @return {module:model/MachineProperties} The populated <code>MachineProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MachineProperties();

            if (data.hasOwnProperty('assessmentData')) {
                obj['assessmentData'] = ApiClient.convertToType(data['assessmentData'], [AssessmentDetails]);
            }
            if (data.hasOwnProperty('discoveryData')) {
                obj['discoveryData'] = ApiClient.convertToType(data['discoveryData'], [DiscoveryDetails]);
            }
            if (data.hasOwnProperty('lastUpdatedTime')) {
                obj['lastUpdatedTime'] = ApiClient.convertToType(data['lastUpdatedTime'], 'Date');
            }
            if (data.hasOwnProperty('migrationData')) {
                obj['migrationData'] = ApiClient.convertToType(data['migrationData'], [MigrationDetails]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MachineProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MachineProperties</code>.
     */
    static validateJSON(data) {
        if (data['assessmentData']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assessmentData'])) {
                throw new Error("Expected the field `assessmentData` to be an array in the JSON data but got " + data['assessmentData']);
            }
            // validate the optional field `assessmentData` (array)
            for (const item of data['assessmentData']) {
                AssessmentDetails.validateJSON(item);
            };
        }
        if (data['discoveryData']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['discoveryData'])) {
                throw new Error("Expected the field `discoveryData` to be an array in the JSON data but got " + data['discoveryData']);
            }
            // validate the optional field `discoveryData` (array)
            for (const item of data['discoveryData']) {
                DiscoveryDetails.validateJSON(item);
            };
        }
        if (data['migrationData']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['migrationData'])) {
                throw new Error("Expected the field `migrationData` to be an array in the JSON data but got " + data['migrationData']);
            }
            // validate the optional field `migrationData` (array)
            for (const item of data['migrationData']) {
                MigrationDetails.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Gets or sets the assessment details of the machine published by various sources.
 * @member {Array.<module:model/AssessmentDetails>} assessmentData
 */
MachineProperties.prototype['assessmentData'] = undefined;

/**
 * Gets or sets the discovery details of the machine published by various sources.
 * @member {Array.<module:model/DiscoveryDetails>} discoveryData
 */
MachineProperties.prototype['discoveryData'] = undefined;

/**
 * Gets or sets the time of the last modification of the machine.
 * @member {Date} lastUpdatedTime
 */
MachineProperties.prototype['lastUpdatedTime'] = undefined;

/**
 * Gets or sets the migration details of the machine published by various sources.
 * @member {Array.<module:model/MigrationDetails>} migrationData
 */
MachineProperties.prototype['migrationData'] = undefined;






export default MachineProperties;

