/**
 * Face Client
 * An API for face detection, verification, and identification.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TakeSnapshotRequest model module.
 * @module model/TakeSnapshotRequest
 * @version 1.0
 */
class TakeSnapshotRequest {
    /**
     * Constructs a new <code>TakeSnapshotRequest</code>.
     * Request body for taking snapshot operation.
     * @alias module:model/TakeSnapshotRequest
     * @param applyScope {Array.<String>} Array of the target Face subscription ids for the snapshot, specified by the user who created the snapshot when calling Snapshot - Take. For each snapshot, only subscriptions included in the applyScope of Snapshot - Take can apply it.
     * @param objectId {String} User specified source object id to take snapshot from.
     * @param type {module:model/TakeSnapshotRequest.TypeEnum} User specified type for the source object to take snapshot from. Currently FaceList, PersonGroup, LargeFaceList and LargePersonGroup are supported.
     */
    constructor(applyScope, objectId, type) { 
        
        TakeSnapshotRequest.initialize(this, applyScope, objectId, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, applyScope, objectId, type) { 
        obj['applyScope'] = applyScope;
        obj['objectId'] = objectId;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>TakeSnapshotRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TakeSnapshotRequest} obj Optional instance to populate.
     * @return {module:model/TakeSnapshotRequest} The populated <code>TakeSnapshotRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TakeSnapshotRequest();

            if (data.hasOwnProperty('applyScope')) {
                obj['applyScope'] = ApiClient.convertToType(data['applyScope'], ['String']);
            }
            if (data.hasOwnProperty('objectId')) {
                obj['objectId'] = ApiClient.convertToType(data['objectId'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('userData')) {
                obj['userData'] = ApiClient.convertToType(data['userData'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TakeSnapshotRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TakeSnapshotRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TakeSnapshotRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['applyScope'])) {
            throw new Error("Expected the field `applyScope` to be an array in the JSON data but got " + data['applyScope']);
        }
        // ensure the json data is a string
        if (data['objectId'] && !(typeof data['objectId'] === 'string' || data['objectId'] instanceof String)) {
            throw new Error("Expected the field `objectId` to be a primitive type in the JSON string but got " + data['objectId']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['userData'] && !(typeof data['userData'] === 'string' || data['userData'] instanceof String)) {
            throw new Error("Expected the field `userData` to be a primitive type in the JSON string but got " + data['userData']);
        }

        return true;
    }


}

TakeSnapshotRequest.RequiredProperties = ["applyScope", "objectId", "type"];

/**
 * Array of the target Face subscription ids for the snapshot, specified by the user who created the snapshot when calling Snapshot - Take. For each snapshot, only subscriptions included in the applyScope of Snapshot - Take can apply it.
 * @member {Array.<String>} applyScope
 */
TakeSnapshotRequest.prototype['applyScope'] = undefined;

/**
 * User specified source object id to take snapshot from.
 * @member {String} objectId
 */
TakeSnapshotRequest.prototype['objectId'] = undefined;

/**
 * User specified type for the source object to take snapshot from. Currently FaceList, PersonGroup, LargeFaceList and LargePersonGroup are supported.
 * @member {module:model/TakeSnapshotRequest.TypeEnum} type
 */
TakeSnapshotRequest.prototype['type'] = undefined;

/**
 * User specified data about the snapshot for any purpose. Length should not exceed 16KB.
 * @member {String} userData
 */
TakeSnapshotRequest.prototype['userData'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
TakeSnapshotRequest['TypeEnum'] = {

    /**
     * value: "FaceList"
     * @const
     */
    "FaceList": "FaceList",

    /**
     * value: "LargeFaceList"
     * @const
     */
    "LargeFaceList": "LargeFaceList",

    /**
     * value: "LargePersonGroup"
     * @const
     */
    "LargePersonGroup": "LargePersonGroup",

    /**
     * value: "PersonGroup"
     * @const
     */
    "PersonGroup": "PersonGroup"
};



export default TakeSnapshotRequest;

