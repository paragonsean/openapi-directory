/**
 * Face Client
 * An API for face detection, verification, and identification.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PersistedFace model module.
 * @module model/PersistedFace
 * @version 1.0
 */
class PersistedFace {
    /**
     * Constructs a new <code>PersistedFace</code>.
     * PersonFace object.
     * @alias module:model/PersistedFace
     * @param persistedFaceId {String} The persistedFaceId of the target face, which is persisted and will not expire. Different from faceId created by Face - Detect and will expire in 24 hours after the detection call.
     */
    constructor(persistedFaceId) { 
        
        PersistedFace.initialize(this, persistedFaceId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, persistedFaceId) { 
        obj['persistedFaceId'] = persistedFaceId;
    }

    /**
     * Constructs a <code>PersistedFace</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PersistedFace} obj Optional instance to populate.
     * @return {module:model/PersistedFace} The populated <code>PersistedFace</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PersistedFace();

            if (data.hasOwnProperty('persistedFaceId')) {
                obj['persistedFaceId'] = ApiClient.convertToType(data['persistedFaceId'], 'String');
            }
            if (data.hasOwnProperty('userData')) {
                obj['userData'] = ApiClient.convertToType(data['userData'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PersistedFace</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PersistedFace</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PersistedFace.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['persistedFaceId'] && !(typeof data['persistedFaceId'] === 'string' || data['persistedFaceId'] instanceof String)) {
            throw new Error("Expected the field `persistedFaceId` to be a primitive type in the JSON string but got " + data['persistedFaceId']);
        }
        // ensure the json data is a string
        if (data['userData'] && !(typeof data['userData'] === 'string' || data['userData'] instanceof String)) {
            throw new Error("Expected the field `userData` to be a primitive type in the JSON string but got " + data['userData']);
        }

        return true;
    }


}

PersistedFace.RequiredProperties = ["persistedFaceId"];

/**
 * The persistedFaceId of the target face, which is persisted and will not expire. Different from faceId created by Face - Detect and will expire in 24 hours after the detection call.
 * @member {String} persistedFaceId
 */
PersistedFace.prototype['persistedFaceId'] = undefined;

/**
 * User-provided data attached to the face. The size limit is 1KB.
 * @member {String} userData
 */
PersistedFace.prototype['userData'] = undefined;






export default PersistedFace;

