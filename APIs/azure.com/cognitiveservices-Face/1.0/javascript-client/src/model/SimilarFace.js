/**
 * Face Client
 * An API for face detection, verification, and identification.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SimilarFace model module.
 * @module model/SimilarFace
 * @version 1.0
 */
class SimilarFace {
    /**
     * Constructs a new <code>SimilarFace</code>.
     * Response body for find similar face operation.
     * @alias module:model/SimilarFace
     * @param confidence {Number} A number ranging from 0 to 1 indicating a level of confidence associated with a property.
     */
    constructor(confidence) { 
        
        SimilarFace.initialize(this, confidence);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, confidence) { 
        obj['confidence'] = confidence;
    }

    /**
     * Constructs a <code>SimilarFace</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SimilarFace} obj Optional instance to populate.
     * @return {module:model/SimilarFace} The populated <code>SimilarFace</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SimilarFace();

            if (data.hasOwnProperty('confidence')) {
                obj['confidence'] = ApiClient.convertToType(data['confidence'], 'Number');
            }
            if (data.hasOwnProperty('faceId')) {
                obj['faceId'] = ApiClient.convertToType(data['faceId'], 'String');
            }
            if (data.hasOwnProperty('persistedFaceId')) {
                obj['persistedFaceId'] = ApiClient.convertToType(data['persistedFaceId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SimilarFace</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SimilarFace</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SimilarFace.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['faceId'] && !(typeof data['faceId'] === 'string' || data['faceId'] instanceof String)) {
            throw new Error("Expected the field `faceId` to be a primitive type in the JSON string but got " + data['faceId']);
        }
        // ensure the json data is a string
        if (data['persistedFaceId'] && !(typeof data['persistedFaceId'] === 'string' || data['persistedFaceId'] instanceof String)) {
            throw new Error("Expected the field `persistedFaceId` to be a primitive type in the JSON string but got " + data['persistedFaceId']);
        }

        return true;
    }


}

SimilarFace.RequiredProperties = ["confidence"];

/**
 * A number ranging from 0 to 1 indicating a level of confidence associated with a property.
 * @member {Number} confidence
 */
SimilarFace.prototype['confidence'] = undefined;

/**
 * FaceId of candidate face when find by faceIds. faceId is created by Face - Detect and will expire 24 hours after the detection call
 * @member {String} faceId
 */
SimilarFace.prototype['faceId'] = undefined;

/**
 * PersistedFaceId of candidate face when find by faceListId. persistedFaceId in face list is persisted and will not expire. As showed in below response
 * @member {String} persistedFaceId
 */
SimilarFace.prototype['persistedFaceId'] = undefined;






export default SimilarFace;

