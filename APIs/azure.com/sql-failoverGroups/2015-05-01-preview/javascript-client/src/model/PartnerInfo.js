/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2015-05-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PartnerInfo model module.
 * @module model/PartnerInfo
 * @version 2015-05-01-preview
 */
class PartnerInfo {
    /**
     * Constructs a new <code>PartnerInfo</code>.
     * Partner server information for the failover group.
     * @alias module:model/PartnerInfo
     * @param id {String} Resource identifier of the partner server.
     */
    constructor(id) { 
        
        PartnerInfo.initialize(this, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id) { 
        obj['id'] = id;
    }

    /**
     * Constructs a <code>PartnerInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PartnerInfo} obj Optional instance to populate.
     * @return {module:model/PartnerInfo} The populated <code>PartnerInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PartnerInfo();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('replicationRole')) {
                obj['replicationRole'] = ApiClient.convertToType(data['replicationRole'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PartnerInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PartnerInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PartnerInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // ensure the json data is a string
        if (data['replicationRole'] && !(typeof data['replicationRole'] === 'string' || data['replicationRole'] instanceof String)) {
            throw new Error("Expected the field `replicationRole` to be a primitive type in the JSON string but got " + data['replicationRole']);
        }

        return true;
    }


}

PartnerInfo.RequiredProperties = ["id"];

/**
 * Resource identifier of the partner server.
 * @member {String} id
 */
PartnerInfo.prototype['id'] = undefined;

/**
 * Geo location of the partner server.
 * @member {String} location
 */
PartnerInfo.prototype['location'] = undefined;

/**
 * Replication role of the partner server.
 * @member {module:model/PartnerInfo.ReplicationRoleEnum} replicationRole
 */
PartnerInfo.prototype['replicationRole'] = undefined;





/**
 * Allowed values for the <code>replicationRole</code> property.
 * @enum {String}
 * @readonly
 */
PartnerInfo['ReplicationRoleEnum'] = {

    /**
     * value: "Primary"
     * @const
     */
    "Primary": "Primary",

    /**
     * value: "Secondary"
     * @const
     */
    "Secondary": "Secondary"
};



export default PartnerInfo;

