/**
 * ComputeManagementClient
 * The Compute Management Client.
 *
 * The version of the OpenAPI document: 2019-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VaultCertificate model module.
 * @module model/VaultCertificate
 * @version 2019-07-01
 */
class VaultCertificate {
    /**
     * Constructs a new <code>VaultCertificate</code>.
     * Describes a single certificate reference in a Key Vault, and where the certificate should reside on the VM.
     * @alias module:model/VaultCertificate
     */
    constructor() { 
        
        VaultCertificate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>VaultCertificate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VaultCertificate} obj Optional instance to populate.
     * @return {module:model/VaultCertificate} The populated <code>VaultCertificate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VaultCertificate();

            if (data.hasOwnProperty('certificateStore')) {
                obj['certificateStore'] = ApiClient.convertToType(data['certificateStore'], 'String');
            }
            if (data.hasOwnProperty('certificateUrl')) {
                obj['certificateUrl'] = ApiClient.convertToType(data['certificateUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VaultCertificate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VaultCertificate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['certificateStore'] && !(typeof data['certificateStore'] === 'string' || data['certificateStore'] instanceof String)) {
            throw new Error("Expected the field `certificateStore` to be a primitive type in the JSON string but got " + data['certificateStore']);
        }
        // ensure the json data is a string
        if (data['certificateUrl'] && !(typeof data['certificateUrl'] === 'string' || data['certificateUrl'] instanceof String)) {
            throw new Error("Expected the field `certificateUrl` to be a primitive type in the JSON string but got " + data['certificateUrl']);
        }

        return true;
    }


}



/**
 * For Windows VMs, specifies the certificate store on the Virtual Machine to which the certificate should be added. The specified certificate store is implicitly in the LocalMachine account. <br><br>For Linux VMs, the certificate file is placed under the /var/lib/waagent directory, with the file name &lt;UppercaseThumbprint&gt;.crt for the X509 certificate file and &lt;UppercaseThumbprint&gt;.prv for private key. Both of these files are .pem formatted.
 * @member {String} certificateStore
 */
VaultCertificate.prototype['certificateStore'] = undefined;

/**
 * This is the URL of a certificate that has been uploaded to Key Vault as a secret. For adding a secret to the Key Vault, see [Add a key or secret to the key vault](https://docs.microsoft.com/azure/key-vault/key-vault-get-started/#add). In this case, your certificate needs to be It is the Base64 encoding of the following JSON Object which is encoded in UTF-8: <br><br> {<br>  \"data\":\"<Base64-encoded-certificate>\",<br>  \"dataType\":\"pfx\",<br>  \"password\":\"<pfx-file-password>\"<br>}
 * @member {String} certificateUrl
 */
VaultCertificate.prototype['certificateUrl'] = undefined;






export default VaultCertificate;

