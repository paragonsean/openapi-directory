/**
 * ComputeManagementClient
 * The Compute Management Client.
 *
 * The version of the OpenAPI document: 2019-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VirtualMachineCaptureParameters model module.
 * @module model/VirtualMachineCaptureParameters
 * @version 2019-03-01
 */
class VirtualMachineCaptureParameters {
    /**
     * Constructs a new <code>VirtualMachineCaptureParameters</code>.
     * Capture Virtual Machine parameters.
     * @alias module:model/VirtualMachineCaptureParameters
     * @param destinationContainerName {String} The destination container name.
     * @param overwriteVhds {Boolean} Specifies whether to overwrite the destination virtual hard disk, in case of conflict.
     * @param vhdPrefix {String} The captured virtual hard disk's name prefix.
     */
    constructor(destinationContainerName, overwriteVhds, vhdPrefix) { 
        
        VirtualMachineCaptureParameters.initialize(this, destinationContainerName, overwriteVhds, vhdPrefix);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destinationContainerName, overwriteVhds, vhdPrefix) { 
        obj['destinationContainerName'] = destinationContainerName;
        obj['overwriteVhds'] = overwriteVhds;
        obj['vhdPrefix'] = vhdPrefix;
    }

    /**
     * Constructs a <code>VirtualMachineCaptureParameters</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VirtualMachineCaptureParameters} obj Optional instance to populate.
     * @return {module:model/VirtualMachineCaptureParameters} The populated <code>VirtualMachineCaptureParameters</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VirtualMachineCaptureParameters();

            if (data.hasOwnProperty('destinationContainerName')) {
                obj['destinationContainerName'] = ApiClient.convertToType(data['destinationContainerName'], 'String');
            }
            if (data.hasOwnProperty('overwriteVhds')) {
                obj['overwriteVhds'] = ApiClient.convertToType(data['overwriteVhds'], 'Boolean');
            }
            if (data.hasOwnProperty('vhdPrefix')) {
                obj['vhdPrefix'] = ApiClient.convertToType(data['vhdPrefix'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VirtualMachineCaptureParameters</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VirtualMachineCaptureParameters</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VirtualMachineCaptureParameters.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['destinationContainerName'] && !(typeof data['destinationContainerName'] === 'string' || data['destinationContainerName'] instanceof String)) {
            throw new Error("Expected the field `destinationContainerName` to be a primitive type in the JSON string but got " + data['destinationContainerName']);
        }
        // ensure the json data is a string
        if (data['vhdPrefix'] && !(typeof data['vhdPrefix'] === 'string' || data['vhdPrefix'] instanceof String)) {
            throw new Error("Expected the field `vhdPrefix` to be a primitive type in the JSON string but got " + data['vhdPrefix']);
        }

        return true;
    }


}

VirtualMachineCaptureParameters.RequiredProperties = ["destinationContainerName", "overwriteVhds", "vhdPrefix"];

/**
 * The destination container name.
 * @member {String} destinationContainerName
 */
VirtualMachineCaptureParameters.prototype['destinationContainerName'] = undefined;

/**
 * Specifies whether to overwrite the destination virtual hard disk, in case of conflict.
 * @member {Boolean} overwriteVhds
 */
VirtualMachineCaptureParameters.prototype['overwriteVhds'] = undefined;

/**
 * The captured virtual hard disk's name prefix.
 * @member {String} vhdPrefix
 */
VirtualMachineCaptureParameters.prototype['vhdPrefix'] = undefined;






export default VirtualMachineCaptureParameters;

