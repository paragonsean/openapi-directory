/**
 * StorSimpleManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2016-10-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BackupScheduleGroupProperties from './BackupScheduleGroupProperties';
import BaseModel from './BaseModel';

/**
 * The BackupScheduleGroup model module.
 * @module model/BackupScheduleGroup
 * @version 2016-10-01
 */
class BackupScheduleGroup {
    /**
     * Constructs a new <code>BackupScheduleGroup</code>.
     * The Backup Schedule Group
     * @alias module:model/BackupScheduleGroup
     * @implements module:model/BaseModel
     */
    constructor() { 
        BaseModel.initialize(this);
        BackupScheduleGroup.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['properties'] = properties;
    }

    /**
     * Constructs a <code>BackupScheduleGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BackupScheduleGroup} obj Optional instance to populate.
     * @return {module:model/BackupScheduleGroup} The populated <code>BackupScheduleGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BackupScheduleGroup();
            BaseModel.constructFromObject(data, obj);

            if (data.hasOwnProperty('properties')) {
                obj['properties'] = BackupScheduleGroupProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BackupScheduleGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BackupScheduleGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BackupScheduleGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          BackupScheduleGroupProperties.validateJSON(data['properties']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

BackupScheduleGroup.RequiredProperties = ["properties"];

/**
 * @member {module:model/BackupScheduleGroupProperties} properties
 */
BackupScheduleGroup.prototype['properties'] = undefined;

/**
 * The identifier.
 * @member {String} id
 */
BackupScheduleGroup.prototype['id'] = undefined;

/**
 * The name.
 * @member {String} name
 */
BackupScheduleGroup.prototype['name'] = undefined;

/**
 * The type.
 * @member {String} type
 */
BackupScheduleGroup.prototype['type'] = undefined;


// Implement BaseModel interface:
/**
 * The identifier.
 * @member {String} id
 */
BaseModel.prototype['id'] = undefined;
/**
 * The name.
 * @member {String} name
 */
BaseModel.prototype['name'] = undefined;
/**
 * The type.
 * @member {String} type
 */
BaseModel.prototype['type'] = undefined;




export default BackupScheduleGroup;

