/**
 * StorSimpleManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2016-10-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BackupElementProperties model module.
 * @module model/BackupElementProperties
 * @version 2016-10-01
 */
class BackupElementProperties {
    /**
     * Constructs a new <code>BackupElementProperties</code>.
     * The backup element properties
     * @alias module:model/BackupElementProperties
     * @param dataPolicy {module:model/BackupElementProperties.DataPolicyEnum} The data policy of backed up endpoint.
     * @param endpointName {String} The name of the endpoint.
     * @param sizeInBytes {Number} The size in bytes.
     */
    constructor(dataPolicy, endpointName, sizeInBytes) { 
        
        BackupElementProperties.initialize(this, dataPolicy, endpointName, sizeInBytes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dataPolicy, endpointName, sizeInBytes) { 
        obj['dataPolicy'] = dataPolicy;
        obj['endpointName'] = endpointName;
        obj['sizeInBytes'] = sizeInBytes;
    }

    /**
     * Constructs a <code>BackupElementProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BackupElementProperties} obj Optional instance to populate.
     * @return {module:model/BackupElementProperties} The populated <code>BackupElementProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BackupElementProperties();

            if (data.hasOwnProperty('dataPolicy')) {
                obj['dataPolicy'] = ApiClient.convertToType(data['dataPolicy'], 'String');
            }
            if (data.hasOwnProperty('endpointName')) {
                obj['endpointName'] = ApiClient.convertToType(data['endpointName'], 'String');
            }
            if (data.hasOwnProperty('sizeInBytes')) {
                obj['sizeInBytes'] = ApiClient.convertToType(data['sizeInBytes'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BackupElementProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BackupElementProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BackupElementProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['dataPolicy'] && !(typeof data['dataPolicy'] === 'string' || data['dataPolicy'] instanceof String)) {
            throw new Error("Expected the field `dataPolicy` to be a primitive type in the JSON string but got " + data['dataPolicy']);
        }
        // ensure the json data is a string
        if (data['endpointName'] && !(typeof data['endpointName'] === 'string' || data['endpointName'] instanceof String)) {
            throw new Error("Expected the field `endpointName` to be a primitive type in the JSON string but got " + data['endpointName']);
        }

        return true;
    }


}

BackupElementProperties.RequiredProperties = ["dataPolicy", "endpointName", "sizeInBytes"];

/**
 * The data policy of backed up endpoint.
 * @member {module:model/BackupElementProperties.DataPolicyEnum} dataPolicy
 */
BackupElementProperties.prototype['dataPolicy'] = undefined;

/**
 * The name of the endpoint.
 * @member {String} endpointName
 */
BackupElementProperties.prototype['endpointName'] = undefined;

/**
 * The size in bytes.
 * @member {Number} sizeInBytes
 */
BackupElementProperties.prototype['sizeInBytes'] = undefined;





/**
 * Allowed values for the <code>dataPolicy</code> property.
 * @enum {String}
 * @readonly
 */
BackupElementProperties['DataPolicyEnum'] = {

    /**
     * value: "Invalid"
     * @const
     */
    "Invalid": "Invalid",

    /**
     * value: "Local"
     * @const
     */
    "Local": "Local",

    /**
     * value: "Tiered"
     * @const
     */
    "Tiered": "Tiered",

    /**
     * value: "Cloud"
     * @const
     */
    "Cloud": "Cloud"
};



export default BackupElementProperties;

