/**
 * Azure IoT Central
 * Azure IoT Central is a service that makes it easy to connect, monitor, and manage your IoT devices at scale.
 *
 * The version of the OpenAPI document: preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DeviceTemplate from './DeviceTemplate';

/**
 * The DeviceTemplateCollection model module.
 * @module model/DeviceTemplateCollection
 * @version preview
 */
class DeviceTemplateCollection {
    /**
     * Constructs a new <code>DeviceTemplateCollection</code>.
     * @alias module:model/DeviceTemplateCollection
     * @param value {Array.<module:model/DeviceTemplate>} The collection of device templates.
     */
    constructor(value) { 
        
        DeviceTemplateCollection.initialize(this, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, value) { 
        obj['value'] = value;
    }

    /**
     * Constructs a <code>DeviceTemplateCollection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeviceTemplateCollection} obj Optional instance to populate.
     * @return {module:model/DeviceTemplateCollection} The populated <code>DeviceTemplateCollection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeviceTemplateCollection();

            if (data.hasOwnProperty('nextLink')) {
                obj['nextLink'] = ApiClient.convertToType(data['nextLink'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], [DeviceTemplate]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeviceTemplateCollection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeviceTemplateCollection</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeviceTemplateCollection.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['nextLink'] && !(typeof data['nextLink'] === 'string' || data['nextLink'] instanceof String)) {
            throw new Error("Expected the field `nextLink` to be a primitive type in the JSON string but got " + data['nextLink']);
        }
        if (data['value']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['value'])) {
                throw new Error("Expected the field `value` to be an array in the JSON data but got " + data['value']);
            }
            // validate the optional field `value` (array)
            for (const item of data['value']) {
                DeviceTemplate.validateJSON(item);
            };
        }

        return true;
    }


}

DeviceTemplateCollection.RequiredProperties = ["value"];

/**
 * URL to get the next page of device templates.
 * @member {String} nextLink
 */
DeviceTemplateCollection.prototype['nextLink'] = undefined;

/**
 * The collection of device templates.
 * @member {Array.<module:model/DeviceTemplate>} value
 */
DeviceTemplateCollection.prototype['value'] = undefined;






export default DeviceTemplateCollection;

