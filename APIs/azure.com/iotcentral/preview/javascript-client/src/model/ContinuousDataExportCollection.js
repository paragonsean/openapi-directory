/**
 * Azure IoT Central
 * Azure IoT Central is a service that makes it easy to connect, monitor, and manage your IoT devices at scale.
 *
 * The version of the OpenAPI document: preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContinuousDataExport from './ContinuousDataExport';

/**
 * The ContinuousDataExportCollection model module.
 * @module model/ContinuousDataExportCollection
 * @version preview
 */
class ContinuousDataExportCollection {
    /**
     * Constructs a new <code>ContinuousDataExportCollection</code>.
     * @alias module:model/ContinuousDataExportCollection
     * @param value {Array.<module:model/ContinuousDataExport>} The collection of continuous data exports.
     */
    constructor(value) { 
        
        ContinuousDataExportCollection.initialize(this, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, value) { 
        obj['value'] = value;
    }

    /**
     * Constructs a <code>ContinuousDataExportCollection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContinuousDataExportCollection} obj Optional instance to populate.
     * @return {module:model/ContinuousDataExportCollection} The populated <code>ContinuousDataExportCollection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContinuousDataExportCollection();

            if (data.hasOwnProperty('nextLink')) {
                obj['nextLink'] = ApiClient.convertToType(data['nextLink'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], [ContinuousDataExport]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContinuousDataExportCollection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContinuousDataExportCollection</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContinuousDataExportCollection.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['nextLink'] && !(typeof data['nextLink'] === 'string' || data['nextLink'] instanceof String)) {
            throw new Error("Expected the field `nextLink` to be a primitive type in the JSON string but got " + data['nextLink']);
        }
        if (data['value']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['value'])) {
                throw new Error("Expected the field `value` to be an array in the JSON data but got " + data['value']);
            }
            // validate the optional field `value` (array)
            for (const item of data['value']) {
                ContinuousDataExport.validateJSON(item);
            };
        }

        return true;
    }


}

ContinuousDataExportCollection.RequiredProperties = ["value"];

/**
 * URL to get the next page of continuous data exports.
 * @member {String} nextLink
 */
ContinuousDataExportCollection.prototype['nextLink'] = undefined;

/**
 * The collection of continuous data exports.
 * @member {Array.<module:model/ContinuousDataExport>} value
 */
ContinuousDataExportCollection.prototype['value'] = undefined;






export default ContinuousDataExportCollection;

