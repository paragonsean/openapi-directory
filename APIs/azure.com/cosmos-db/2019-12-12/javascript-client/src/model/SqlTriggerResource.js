/**
 * Cosmos DB
 * Azure Cosmos DB Database Service Resource Provider REST API
 *
 * The version of the OpenAPI document: 2019-12-12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SqlTriggerResource model module.
 * @module model/SqlTriggerResource
 * @version 2019-12-12
 */
class SqlTriggerResource {
    /**
     * Constructs a new <code>SqlTriggerResource</code>.
     * Cosmos DB SQL trigger resource object
     * @alias module:model/SqlTriggerResource
     * @param id {String} Name of the Cosmos DB SQL trigger
     */
    constructor(id) { 
        
        SqlTriggerResource.initialize(this, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id) { 
        obj['id'] = id;
    }

    /**
     * Constructs a <code>SqlTriggerResource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SqlTriggerResource} obj Optional instance to populate.
     * @return {module:model/SqlTriggerResource} The populated <code>SqlTriggerResource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SqlTriggerResource();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('triggerOperation')) {
                obj['triggerOperation'] = ApiClient.convertToType(data['triggerOperation'], 'String');
            }
            if (data.hasOwnProperty('triggerType')) {
                obj['triggerType'] = ApiClient.convertToType(data['triggerType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SqlTriggerResource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SqlTriggerResource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SqlTriggerResource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['triggerOperation'] && !(typeof data['triggerOperation'] === 'string' || data['triggerOperation'] instanceof String)) {
            throw new Error("Expected the field `triggerOperation` to be a primitive type in the JSON string but got " + data['triggerOperation']);
        }
        // ensure the json data is a string
        if (data['triggerType'] && !(typeof data['triggerType'] === 'string' || data['triggerType'] instanceof String)) {
            throw new Error("Expected the field `triggerType` to be a primitive type in the JSON string but got " + data['triggerType']);
        }

        return true;
    }


}

SqlTriggerResource.RequiredProperties = ["id"];

/**
 * Body of the Trigger
 * @member {String} body
 */
SqlTriggerResource.prototype['body'] = undefined;

/**
 * Name of the Cosmos DB SQL trigger
 * @member {String} id
 */
SqlTriggerResource.prototype['id'] = undefined;

/**
 * The operation the trigger is associated with
 * @member {module:model/SqlTriggerResource.TriggerOperationEnum} triggerOperation
 */
SqlTriggerResource.prototype['triggerOperation'] = undefined;

/**
 * Type of the Trigger
 * @member {module:model/SqlTriggerResource.TriggerTypeEnum} triggerType
 */
SqlTriggerResource.prototype['triggerType'] = undefined;





/**
 * Allowed values for the <code>triggerOperation</code> property.
 * @enum {String}
 * @readonly
 */
SqlTriggerResource['TriggerOperationEnum'] = {

    /**
     * value: "All"
     * @const
     */
    "All": "All",

    /**
     * value: "Create"
     * @const
     */
    "Create": "Create",

    /**
     * value: "Update"
     * @const
     */
    "Update": "Update",

    /**
     * value: "Delete"
     * @const
     */
    "Delete": "Delete",

    /**
     * value: "Replace"
     * @const
     */
    "Replace": "Replace"
};


/**
 * Allowed values for the <code>triggerType</code> property.
 * @enum {String}
 * @readonly
 */
SqlTriggerResource['TriggerTypeEnum'] = {

    /**
     * value: "Pre"
     * @const
     */
    "Pre": "Pre",

    /**
     * value: "Post"
     * @const
     */
    "Post": "Post"
};



export default SqlTriggerResource;

