/**
 * ContainerRegistryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-02-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SourceControlAuthInfo model module.
 * @module model/SourceControlAuthInfo
 * @version 2018-02-01-preview
 */
class SourceControlAuthInfo {
    /**
     * Constructs a new <code>SourceControlAuthInfo</code>.
     * The authorization properties for accessing the source code repository.
     * @alias module:model/SourceControlAuthInfo
     * @param token {String} The access token used to access the source control provider.
     */
    constructor(token) { 
        
        SourceControlAuthInfo.initialize(this, token);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, token) { 
        obj['token'] = token;
    }

    /**
     * Constructs a <code>SourceControlAuthInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SourceControlAuthInfo} obj Optional instance to populate.
     * @return {module:model/SourceControlAuthInfo} The populated <code>SourceControlAuthInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SourceControlAuthInfo();

            if (data.hasOwnProperty('expiresIn')) {
                obj['expiresIn'] = ApiClient.convertToType(data['expiresIn'], 'Number');
            }
            if (data.hasOwnProperty('refreshToken')) {
                obj['refreshToken'] = ApiClient.convertToType(data['refreshToken'], 'String');
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('tokenType')) {
                obj['tokenType'] = ApiClient.convertToType(data['tokenType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SourceControlAuthInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SourceControlAuthInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SourceControlAuthInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['refreshToken'] && !(typeof data['refreshToken'] === 'string' || data['refreshToken'] instanceof String)) {
            throw new Error("Expected the field `refreshToken` to be a primitive type in the JSON string but got " + data['refreshToken']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['tokenType'] && !(typeof data['tokenType'] === 'string' || data['tokenType'] instanceof String)) {
            throw new Error("Expected the field `tokenType` to be a primitive type in the JSON string but got " + data['tokenType']);
        }

        return true;
    }


}

SourceControlAuthInfo.RequiredProperties = ["token"];

/**
 * Time in seconds that the token remains valid
 * @member {Number} expiresIn
 */
SourceControlAuthInfo.prototype['expiresIn'] = undefined;

/**
 * The refresh token used to refresh the access token.
 * @member {String} refreshToken
 */
SourceControlAuthInfo.prototype['refreshToken'] = undefined;

/**
 * The scope of the access token.
 * @member {String} scope
 */
SourceControlAuthInfo.prototype['scope'] = undefined;

/**
 * The access token used to access the source control provider.
 * @member {String} token
 */
SourceControlAuthInfo.prototype['token'] = undefined;

/**
 * The type of Auth token.
 * @member {module:model/SourceControlAuthInfo.TokenTypeEnum} tokenType
 */
SourceControlAuthInfo.prototype['tokenType'] = undefined;





/**
 * Allowed values for the <code>tokenType</code> property.
 * @enum {String}
 * @readonly
 */
SourceControlAuthInfo['TokenTypeEnum'] = {

    /**
     * value: "PAT"
     * @const
     */
    "PAT": "PAT",

    /**
     * value: "OAuth"
     * @const
     */
    "OAuth": "OAuth"
};



export default SourceControlAuthInfo;

