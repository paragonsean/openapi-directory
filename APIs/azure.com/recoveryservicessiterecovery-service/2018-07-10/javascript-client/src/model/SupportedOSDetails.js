/**
 * SiteRecoveryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-07-10
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OSVersionWrapper from './OSVersionWrapper';

/**
 * The SupportedOSDetails model module.
 * @module model/SupportedOSDetails
 * @version 2018-07-10
 */
class SupportedOSDetails {
    /**
     * Constructs a new <code>SupportedOSDetails</code>.
     * Supported Operating system details.
     * @alias module:model/SupportedOSDetails
     */
    constructor() { 
        
        SupportedOSDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SupportedOSDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SupportedOSDetails} obj Optional instance to populate.
     * @return {module:model/SupportedOSDetails} The populated <code>SupportedOSDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SupportedOSDetails();

            if (data.hasOwnProperty('osName')) {
                obj['osName'] = ApiClient.convertToType(data['osName'], 'String');
            }
            if (data.hasOwnProperty('osType')) {
                obj['osType'] = ApiClient.convertToType(data['osType'], 'String');
            }
            if (data.hasOwnProperty('osVersions')) {
                obj['osVersions'] = ApiClient.convertToType(data['osVersions'], [OSVersionWrapper]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SupportedOSDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SupportedOSDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['osName'] && !(typeof data['osName'] === 'string' || data['osName'] instanceof String)) {
            throw new Error("Expected the field `osName` to be a primitive type in the JSON string but got " + data['osName']);
        }
        // ensure the json data is a string
        if (data['osType'] && !(typeof data['osType'] === 'string' || data['osType'] instanceof String)) {
            throw new Error("Expected the field `osType` to be a primitive type in the JSON string but got " + data['osType']);
        }
        if (data['osVersions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['osVersions'])) {
                throw new Error("Expected the field `osVersions` to be an array in the JSON data but got " + data['osVersions']);
            }
            // validate the optional field `osVersions` (array)
            for (const item of data['osVersions']) {
                OSVersionWrapper.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The name.
 * @member {String} osName
 */
SupportedOSDetails.prototype['osName'] = undefined;

/**
 * The type.
 * @member {String} osType
 */
SupportedOSDetails.prototype['osType'] = undefined;

/**
 * List of version for OS.
 * @member {Array.<module:model/OSVersionWrapper>} osVersions
 */
SupportedOSDetails.prototype['osVersions'] = undefined;






export default SupportedOSDetails;

