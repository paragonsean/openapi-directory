/**
 * SiteRecoveryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-01-10
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RecoveryPlanProviderSpecificFailoverInput from './RecoveryPlanProviderSpecificFailoverInput';

/**
 * The RecoveryPlanTestFailoverInputProperties model module.
 * @module model/RecoveryPlanTestFailoverInputProperties
 * @version 2018-01-10
 */
class RecoveryPlanTestFailoverInputProperties {
    /**
     * Constructs a new <code>RecoveryPlanTestFailoverInputProperties</code>.
     * Recovery plan test failover input properties.
     * @alias module:model/RecoveryPlanTestFailoverInputProperties
     * @param failoverDirection {module:model/RecoveryPlanTestFailoverInputProperties.FailoverDirectionEnum} The failover direction.
     * @param networkType {String} The network type to be used for test failover.
     */
    constructor(failoverDirection, networkType) { 
        
        RecoveryPlanTestFailoverInputProperties.initialize(this, failoverDirection, networkType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, failoverDirection, networkType) { 
        obj['failoverDirection'] = failoverDirection;
        obj['networkType'] = networkType;
    }

    /**
     * Constructs a <code>RecoveryPlanTestFailoverInputProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RecoveryPlanTestFailoverInputProperties} obj Optional instance to populate.
     * @return {module:model/RecoveryPlanTestFailoverInputProperties} The populated <code>RecoveryPlanTestFailoverInputProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecoveryPlanTestFailoverInputProperties();

            if (data.hasOwnProperty('failoverDirection')) {
                obj['failoverDirection'] = ApiClient.convertToType(data['failoverDirection'], 'String');
            }
            if (data.hasOwnProperty('networkId')) {
                obj['networkId'] = ApiClient.convertToType(data['networkId'], 'String');
            }
            if (data.hasOwnProperty('networkType')) {
                obj['networkType'] = ApiClient.convertToType(data['networkType'], 'String');
            }
            if (data.hasOwnProperty('providerSpecificDetails')) {
                obj['providerSpecificDetails'] = ApiClient.convertToType(data['providerSpecificDetails'], [RecoveryPlanProviderSpecificFailoverInput]);
            }
            if (data.hasOwnProperty('skipTestFailoverCleanup')) {
                obj['skipTestFailoverCleanup'] = ApiClient.convertToType(data['skipTestFailoverCleanup'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecoveryPlanTestFailoverInputProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecoveryPlanTestFailoverInputProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecoveryPlanTestFailoverInputProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['failoverDirection'] && !(typeof data['failoverDirection'] === 'string' || data['failoverDirection'] instanceof String)) {
            throw new Error("Expected the field `failoverDirection` to be a primitive type in the JSON string but got " + data['failoverDirection']);
        }
        // ensure the json data is a string
        if (data['networkId'] && !(typeof data['networkId'] === 'string' || data['networkId'] instanceof String)) {
            throw new Error("Expected the field `networkId` to be a primitive type in the JSON string but got " + data['networkId']);
        }
        // ensure the json data is a string
        if (data['networkType'] && !(typeof data['networkType'] === 'string' || data['networkType'] instanceof String)) {
            throw new Error("Expected the field `networkType` to be a primitive type in the JSON string but got " + data['networkType']);
        }
        if (data['providerSpecificDetails']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['providerSpecificDetails'])) {
                throw new Error("Expected the field `providerSpecificDetails` to be an array in the JSON data but got " + data['providerSpecificDetails']);
            }
            // validate the optional field `providerSpecificDetails` (array)
            for (const item of data['providerSpecificDetails']) {
                RecoveryPlanProviderSpecificFailoverInput.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['skipTestFailoverCleanup'] && !(typeof data['skipTestFailoverCleanup'] === 'string' || data['skipTestFailoverCleanup'] instanceof String)) {
            throw new Error("Expected the field `skipTestFailoverCleanup` to be a primitive type in the JSON string but got " + data['skipTestFailoverCleanup']);
        }

        return true;
    }


}

RecoveryPlanTestFailoverInputProperties.RequiredProperties = ["failoverDirection", "networkType"];

/**
 * The failover direction.
 * @member {module:model/RecoveryPlanTestFailoverInputProperties.FailoverDirectionEnum} failoverDirection
 */
RecoveryPlanTestFailoverInputProperties.prototype['failoverDirection'] = undefined;

/**
 * The Id of the network to be used for test failover.
 * @member {String} networkId
 */
RecoveryPlanTestFailoverInputProperties.prototype['networkId'] = undefined;

/**
 * The network type to be used for test failover.
 * @member {String} networkType
 */
RecoveryPlanTestFailoverInputProperties.prototype['networkType'] = undefined;

/**
 * The provider specific properties.
 * @member {Array.<module:model/RecoveryPlanProviderSpecificFailoverInput>} providerSpecificDetails
 */
RecoveryPlanTestFailoverInputProperties.prototype['providerSpecificDetails'] = undefined;

/**
 * A value indicating whether the test failover cleanup is to be skipped.
 * @member {String} skipTestFailoverCleanup
 */
RecoveryPlanTestFailoverInputProperties.prototype['skipTestFailoverCleanup'] = undefined;





/**
 * Allowed values for the <code>failoverDirection</code> property.
 * @enum {String}
 * @readonly
 */
RecoveryPlanTestFailoverInputProperties['FailoverDirectionEnum'] = {

    /**
     * value: "PrimaryToRecovery"
     * @const
     */
    "PrimaryToRecovery": "PrimaryToRecovery",

    /**
     * value: "RecoveryToPrimary"
     * @const
     */
    "RecoveryToPrimary": "RecoveryToPrimary"
};



export default RecoveryPlanTestFailoverInputProperties;

