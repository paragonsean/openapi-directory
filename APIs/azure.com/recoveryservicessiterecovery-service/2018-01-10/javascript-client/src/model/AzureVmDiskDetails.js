/**
 * SiteRecoveryManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2018-01-10
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AzureVmDiskDetails model module.
 * @module model/AzureVmDiskDetails
 * @version 2018-01-10
 */
class AzureVmDiskDetails {
    /**
     * Constructs a new <code>AzureVmDiskDetails</code>.
     * Disk details for E2A provider.
     * @alias module:model/AzureVmDiskDetails
     */
    constructor() { 
        
        AzureVmDiskDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AzureVmDiskDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AzureVmDiskDetails} obj Optional instance to populate.
     * @return {module:model/AzureVmDiskDetails} The populated <code>AzureVmDiskDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AzureVmDiskDetails();

            if (data.hasOwnProperty('lunId')) {
                obj['lunId'] = ApiClient.convertToType(data['lunId'], 'String');
            }
            if (data.hasOwnProperty('maxSizeMB')) {
                obj['maxSizeMB'] = ApiClient.convertToType(data['maxSizeMB'], 'String');
            }
            if (data.hasOwnProperty('targetDiskLocation')) {
                obj['targetDiskLocation'] = ApiClient.convertToType(data['targetDiskLocation'], 'String');
            }
            if (data.hasOwnProperty('targetDiskName')) {
                obj['targetDiskName'] = ApiClient.convertToType(data['targetDiskName'], 'String');
            }
            if (data.hasOwnProperty('vhdId')) {
                obj['vhdId'] = ApiClient.convertToType(data['vhdId'], 'String');
            }
            if (data.hasOwnProperty('vhdName')) {
                obj['vhdName'] = ApiClient.convertToType(data['vhdName'], 'String');
            }
            if (data.hasOwnProperty('vhdType')) {
                obj['vhdType'] = ApiClient.convertToType(data['vhdType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AzureVmDiskDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AzureVmDiskDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['lunId'] && !(typeof data['lunId'] === 'string' || data['lunId'] instanceof String)) {
            throw new Error("Expected the field `lunId` to be a primitive type in the JSON string but got " + data['lunId']);
        }
        // ensure the json data is a string
        if (data['maxSizeMB'] && !(typeof data['maxSizeMB'] === 'string' || data['maxSizeMB'] instanceof String)) {
            throw new Error("Expected the field `maxSizeMB` to be a primitive type in the JSON string but got " + data['maxSizeMB']);
        }
        // ensure the json data is a string
        if (data['targetDiskLocation'] && !(typeof data['targetDiskLocation'] === 'string' || data['targetDiskLocation'] instanceof String)) {
            throw new Error("Expected the field `targetDiskLocation` to be a primitive type in the JSON string but got " + data['targetDiskLocation']);
        }
        // ensure the json data is a string
        if (data['targetDiskName'] && !(typeof data['targetDiskName'] === 'string' || data['targetDiskName'] instanceof String)) {
            throw new Error("Expected the field `targetDiskName` to be a primitive type in the JSON string but got " + data['targetDiskName']);
        }
        // ensure the json data is a string
        if (data['vhdId'] && !(typeof data['vhdId'] === 'string' || data['vhdId'] instanceof String)) {
            throw new Error("Expected the field `vhdId` to be a primitive type in the JSON string but got " + data['vhdId']);
        }
        // ensure the json data is a string
        if (data['vhdName'] && !(typeof data['vhdName'] === 'string' || data['vhdName'] instanceof String)) {
            throw new Error("Expected the field `vhdName` to be a primitive type in the JSON string but got " + data['vhdName']);
        }
        // ensure the json data is a string
        if (data['vhdType'] && !(typeof data['vhdType'] === 'string' || data['vhdType'] instanceof String)) {
            throw new Error("Expected the field `vhdType` to be a primitive type in the JSON string but got " + data['vhdType']);
        }

        return true;
    }


}



/**
 * Ordinal\\LunId of the disk for the Azure VM.
 * @member {String} lunId
 */
AzureVmDiskDetails.prototype['lunId'] = undefined;

/**
 * Max side in MB.
 * @member {String} maxSizeMB
 */
AzureVmDiskDetails.prototype['maxSizeMB'] = undefined;

/**
 * Blob uri of the Azure disk.
 * @member {String} targetDiskLocation
 */
AzureVmDiskDetails.prototype['targetDiskLocation'] = undefined;

/**
 * The target Azure disk name.
 * @member {String} targetDiskName
 */
AzureVmDiskDetails.prototype['targetDiskName'] = undefined;

/**
 * The VHD id.
 * @member {String} vhdId
 */
AzureVmDiskDetails.prototype['vhdId'] = undefined;

/**
 * VHD name.
 * @member {String} vhdName
 */
AzureVmDiskDetails.prototype['vhdName'] = undefined;

/**
 * VHD type.
 * @member {String} vhdType
 */
AzureVmDiskDetails.prototype['vhdType'] = undefined;






export default AzureVmDiskDetails;

