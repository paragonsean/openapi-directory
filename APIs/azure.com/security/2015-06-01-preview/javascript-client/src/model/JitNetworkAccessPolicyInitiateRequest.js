/**
 * Security Center
 * API spec for Microsoft.Security (Azure Security Center) resource provider
 *
 * The version of the OpenAPI document: 2015-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JitNetworkAccessPolicyInitiateVirtualMachine from './JitNetworkAccessPolicyInitiateVirtualMachine';

/**
 * The JitNetworkAccessPolicyInitiateRequest model module.
 * @module model/JitNetworkAccessPolicyInitiateRequest
 * @version 2015-06-01-preview
 */
class JitNetworkAccessPolicyInitiateRequest {
    /**
     * Constructs a new <code>JitNetworkAccessPolicyInitiateRequest</code>.
     * @alias module:model/JitNetworkAccessPolicyInitiateRequest
     * @param virtualMachines {Array.<module:model/JitNetworkAccessPolicyInitiateVirtualMachine>} A list of virtual machines & ports to open access for
     */
    constructor(virtualMachines) { 
        
        JitNetworkAccessPolicyInitiateRequest.initialize(this, virtualMachines);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, virtualMachines) { 
        obj['virtualMachines'] = virtualMachines;
    }

    /**
     * Constructs a <code>JitNetworkAccessPolicyInitiateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JitNetworkAccessPolicyInitiateRequest} obj Optional instance to populate.
     * @return {module:model/JitNetworkAccessPolicyInitiateRequest} The populated <code>JitNetworkAccessPolicyInitiateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JitNetworkAccessPolicyInitiateRequest();

            if (data.hasOwnProperty('virtualMachines')) {
                obj['virtualMachines'] = ApiClient.convertToType(data['virtualMachines'], [JitNetworkAccessPolicyInitiateVirtualMachine]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JitNetworkAccessPolicyInitiateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JitNetworkAccessPolicyInitiateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JitNetworkAccessPolicyInitiateRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['virtualMachines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['virtualMachines'])) {
                throw new Error("Expected the field `virtualMachines` to be an array in the JSON data but got " + data['virtualMachines']);
            }
            // validate the optional field `virtualMachines` (array)
            for (const item of data['virtualMachines']) {
                JitNetworkAccessPolicyInitiateVirtualMachine.validateJSON(item);
            };
        }

        return true;
    }


}

JitNetworkAccessPolicyInitiateRequest.RequiredProperties = ["virtualMachines"];

/**
 * A list of virtual machines & ports to open access for
 * @member {Array.<module:model/JitNetworkAccessPolicyInitiateVirtualMachine>} virtualMachines
 */
JitNetworkAccessPolicyInitiateRequest.prototype['virtualMachines'] = undefined;






export default JitNetworkAccessPolicyInitiateRequest;

