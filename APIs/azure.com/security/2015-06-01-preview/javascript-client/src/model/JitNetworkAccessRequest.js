/**
 * Security Center
 * API spec for Microsoft.Security (Azure Security Center) resource provider
 *
 * The version of the OpenAPI document: 2015-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JitNetworkAccessRequestVirtualMachine from './JitNetworkAccessRequestVirtualMachine';

/**
 * The JitNetworkAccessRequest model module.
 * @module model/JitNetworkAccessRequest
 * @version 2015-06-01-preview
 */
class JitNetworkAccessRequest {
    /**
     * Constructs a new <code>JitNetworkAccessRequest</code>.
     * @alias module:model/JitNetworkAccessRequest
     * @param requestor {String} The identity of the person who made the request
     * @param startTimeUtc {Date} The start time of the request in UTC
     * @param virtualMachines {Array.<module:model/JitNetworkAccessRequestVirtualMachine>} 
     */
    constructor(requestor, startTimeUtc, virtualMachines) { 
        
        JitNetworkAccessRequest.initialize(this, requestor, startTimeUtc, virtualMachines);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, requestor, startTimeUtc, virtualMachines) { 
        obj['requestor'] = requestor;
        obj['startTimeUtc'] = startTimeUtc;
        obj['virtualMachines'] = virtualMachines;
    }

    /**
     * Constructs a <code>JitNetworkAccessRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JitNetworkAccessRequest} obj Optional instance to populate.
     * @return {module:model/JitNetworkAccessRequest} The populated <code>JitNetworkAccessRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JitNetworkAccessRequest();

            if (data.hasOwnProperty('requestor')) {
                obj['requestor'] = ApiClient.convertToType(data['requestor'], 'String');
            }
            if (data.hasOwnProperty('startTimeUtc')) {
                obj['startTimeUtc'] = ApiClient.convertToType(data['startTimeUtc'], 'Date');
            }
            if (data.hasOwnProperty('virtualMachines')) {
                obj['virtualMachines'] = ApiClient.convertToType(data['virtualMachines'], [JitNetworkAccessRequestVirtualMachine]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JitNetworkAccessRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JitNetworkAccessRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JitNetworkAccessRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['requestor'] && !(typeof data['requestor'] === 'string' || data['requestor'] instanceof String)) {
            throw new Error("Expected the field `requestor` to be a primitive type in the JSON string but got " + data['requestor']);
        }
        if (data['virtualMachines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['virtualMachines'])) {
                throw new Error("Expected the field `virtualMachines` to be an array in the JSON data but got " + data['virtualMachines']);
            }
            // validate the optional field `virtualMachines` (array)
            for (const item of data['virtualMachines']) {
                JitNetworkAccessRequestVirtualMachine.validateJSON(item);
            };
        }

        return true;
    }


}

JitNetworkAccessRequest.RequiredProperties = ["requestor", "startTimeUtc", "virtualMachines"];

/**
 * The identity of the person who made the request
 * @member {String} requestor
 */
JitNetworkAccessRequest.prototype['requestor'] = undefined;

/**
 * The start time of the request in UTC
 * @member {Date} startTimeUtc
 */
JitNetworkAccessRequest.prototype['startTimeUtc'] = undefined;

/**
 * @member {Array.<module:model/JitNetworkAccessRequestVirtualMachine>} virtualMachines
 */
JitNetworkAccessRequest.prototype['virtualMachines'] = undefined;






export default JitNetworkAccessRequest;

