/**
 * Security Center
 * API spec for Microsoft.Security (Azure Security Center) resource provider
 *
 * The version of the OpenAPI document: 2015-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JitNetworkAccessPolicyVirtualMachine from './JitNetworkAccessPolicyVirtualMachine';
import JitNetworkAccessRequest from './JitNetworkAccessRequest';

/**
 * The JitNetworkAccessPolicyProperties model module.
 * @module model/JitNetworkAccessPolicyProperties
 * @version 2015-06-01-preview
 */
class JitNetworkAccessPolicyProperties {
    /**
     * Constructs a new <code>JitNetworkAccessPolicyProperties</code>.
     * @alias module:model/JitNetworkAccessPolicyProperties
     * @param virtualMachines {Array.<module:model/JitNetworkAccessPolicyVirtualMachine>} Configurations for Microsoft.Compute/virtualMachines resource type.
     */
    constructor(virtualMachines) { 
        
        JitNetworkAccessPolicyProperties.initialize(this, virtualMachines);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, virtualMachines) { 
        obj['virtualMachines'] = virtualMachines;
    }

    /**
     * Constructs a <code>JitNetworkAccessPolicyProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JitNetworkAccessPolicyProperties} obj Optional instance to populate.
     * @return {module:model/JitNetworkAccessPolicyProperties} The populated <code>JitNetworkAccessPolicyProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JitNetworkAccessPolicyProperties();

            if (data.hasOwnProperty('provisioningState')) {
                obj['provisioningState'] = ApiClient.convertToType(data['provisioningState'], 'String');
            }
            if (data.hasOwnProperty('requests')) {
                obj['requests'] = ApiClient.convertToType(data['requests'], [JitNetworkAccessRequest]);
            }
            if (data.hasOwnProperty('virtualMachines')) {
                obj['virtualMachines'] = ApiClient.convertToType(data['virtualMachines'], [JitNetworkAccessPolicyVirtualMachine]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JitNetworkAccessPolicyProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JitNetworkAccessPolicyProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JitNetworkAccessPolicyProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['provisioningState'] && !(typeof data['provisioningState'] === 'string' || data['provisioningState'] instanceof String)) {
            throw new Error("Expected the field `provisioningState` to be a primitive type in the JSON string but got " + data['provisioningState']);
        }
        if (data['requests']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['requests'])) {
                throw new Error("Expected the field `requests` to be an array in the JSON data but got " + data['requests']);
            }
            // validate the optional field `requests` (array)
            for (const item of data['requests']) {
                JitNetworkAccessRequest.validateJSON(item);
            };
        }
        if (data['virtualMachines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['virtualMachines'])) {
                throw new Error("Expected the field `virtualMachines` to be an array in the JSON data but got " + data['virtualMachines']);
            }
            // validate the optional field `virtualMachines` (array)
            for (const item of data['virtualMachines']) {
                JitNetworkAccessPolicyVirtualMachine.validateJSON(item);
            };
        }

        return true;
    }


}

JitNetworkAccessPolicyProperties.RequiredProperties = ["virtualMachines"];

/**
 * Gets the provisioning state of the Just-in-Time policy.
 * @member {String} provisioningState
 */
JitNetworkAccessPolicyProperties.prototype['provisioningState'] = undefined;

/**
 * @member {Array.<module:model/JitNetworkAccessRequest>} requests
 */
JitNetworkAccessPolicyProperties.prototype['requests'] = undefined;

/**
 * Configurations for Microsoft.Compute/virtualMachines resource type.
 * @member {Array.<module:model/JitNetworkAccessPolicyVirtualMachine>} virtualMachines
 */
JitNetworkAccessPolicyProperties.prototype['virtualMachines'] = undefined;






export default JitNetworkAccessPolicyProperties;

