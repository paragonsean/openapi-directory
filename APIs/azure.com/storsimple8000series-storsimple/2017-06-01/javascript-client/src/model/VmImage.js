/**
 * StorSimple8000SeriesManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2017-06-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VmImage model module.
 * @module model/VmImage
 * @version 2017-06-01
 */
class VmImage {
    /**
     * Constructs a new <code>VmImage</code>.
     * The virtual machine image.
     * @alias module:model/VmImage
     * @param name {String} The name.
     * @param offer {String} The offer.
     * @param publisher {String} The publisher.
     * @param sku {String} The SKU.
     * @param version {String} The version.
     */
    constructor(name, offer, publisher, sku, version) { 
        
        VmImage.initialize(this, name, offer, publisher, sku, version);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, offer, publisher, sku, version) { 
        obj['name'] = name;
        obj['offer'] = offer;
        obj['publisher'] = publisher;
        obj['sku'] = sku;
        obj['version'] = version;
    }

    /**
     * Constructs a <code>VmImage</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VmImage} obj Optional instance to populate.
     * @return {module:model/VmImage} The populated <code>VmImage</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VmImage();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('offer')) {
                obj['offer'] = ApiClient.convertToType(data['offer'], 'String');
            }
            if (data.hasOwnProperty('publisher')) {
                obj['publisher'] = ApiClient.convertToType(data['publisher'], 'String');
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VmImage</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VmImage</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VmImage.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['offer'] && !(typeof data['offer'] === 'string' || data['offer'] instanceof String)) {
            throw new Error("Expected the field `offer` to be a primitive type in the JSON string but got " + data['offer']);
        }
        // ensure the json data is a string
        if (data['publisher'] && !(typeof data['publisher'] === 'string' || data['publisher'] instanceof String)) {
            throw new Error("Expected the field `publisher` to be a primitive type in the JSON string but got " + data['publisher']);
        }
        // ensure the json data is a string
        if (data['sku'] && !(typeof data['sku'] === 'string' || data['sku'] instanceof String)) {
            throw new Error("Expected the field `sku` to be a primitive type in the JSON string but got " + data['sku']);
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }

        return true;
    }


}

VmImage.RequiredProperties = ["name", "offer", "publisher", "sku", "version"];

/**
 * The name.
 * @member {String} name
 */
VmImage.prototype['name'] = undefined;

/**
 * The offer.
 * @member {String} offer
 */
VmImage.prototype['offer'] = undefined;

/**
 * The publisher.
 * @member {String} publisher
 */
VmImage.prototype['publisher'] = undefined;

/**
 * The SKU.
 * @member {String} sku
 */
VmImage.prototype['sku'] = undefined;

/**
 * The version.
 * @member {String} version
 */
VmImage.prototype['version'] = undefined;






export default VmImage;

