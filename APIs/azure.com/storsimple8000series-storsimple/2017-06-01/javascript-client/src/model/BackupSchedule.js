/**
 * StorSimple8000SeriesManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2017-06-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BackupScheduleProperties from './BackupScheduleProperties';
import BaseModel from './BaseModel';

/**
 * The BackupSchedule model module.
 * @module model/BackupSchedule
 * @version 2017-06-01
 */
class BackupSchedule {
    /**
     * Constructs a new <code>BackupSchedule</code>.
     * The backup schedule.
     * @alias module:model/BackupSchedule
     * @implements module:model/BaseModel
     */
    constructor() { 
        BaseModel.initialize(this);
        BackupSchedule.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['properties'] = properties;
    }

    /**
     * Constructs a <code>BackupSchedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BackupSchedule} obj Optional instance to populate.
     * @return {module:model/BackupSchedule} The populated <code>BackupSchedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BackupSchedule();
            BaseModel.constructFromObject(data, obj);

            if (data.hasOwnProperty('properties')) {
                obj['properties'] = BackupScheduleProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BackupSchedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BackupSchedule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BackupSchedule.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          BackupScheduleProperties.validateJSON(data['properties']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

BackupSchedule.RequiredProperties = ["properties"];

/**
 * @member {module:model/BackupScheduleProperties} properties
 */
BackupSchedule.prototype['properties'] = undefined;

/**
 * The path ID that uniquely identifies the object.
 * @member {String} id
 */
BackupSchedule.prototype['id'] = undefined;

/**
 * The Kind of the object. Currently only Series8000 is supported
 * @member {module:model/BackupSchedule.KindEnum} kind
 */
BackupSchedule.prototype['kind'] = undefined;

/**
 * The name of the object.
 * @member {String} name
 */
BackupSchedule.prototype['name'] = undefined;

/**
 * The hierarchical type of the object.
 * @member {String} type
 */
BackupSchedule.prototype['type'] = undefined;


// Implement BaseModel interface:
/**
 * The path ID that uniquely identifies the object.
 * @member {String} id
 */
BaseModel.prototype['id'] = undefined;
/**
 * The Kind of the object. Currently only Series8000 is supported
 * @member {module:model/BaseModel.KindEnum} kind
 */
BaseModel.prototype['kind'] = undefined;
/**
 * The name of the object.
 * @member {String} name
 */
BaseModel.prototype['name'] = undefined;
/**
 * The hierarchical type of the object.
 * @member {String} type
 */
BaseModel.prototype['type'] = undefined;



/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
BackupSchedule['KindEnum'] = {

    /**
     * value: "Series8000"
     * @const
     */
    "Series8000": "Series8000"
};



export default BackupSchedule;

