/**
 * StorSimple8000SeriesManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2017-06-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BandwidthSchedule from './BandwidthSchedule';

/**
 * The BandwidthRateSettingProperties model module.
 * @module model/BandwidthRateSettingProperties
 * @version 2017-06-01
 */
class BandwidthRateSettingProperties {
    /**
     * Constructs a new <code>BandwidthRateSettingProperties</code>.
     * The properties of the bandwidth setting.
     * @alias module:model/BandwidthRateSettingProperties
     * @param schedules {Array.<module:model/BandwidthSchedule>} The schedules.
     */
    constructor(schedules) { 
        
        BandwidthRateSettingProperties.initialize(this, schedules);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, schedules) { 
        obj['schedules'] = schedules;
    }

    /**
     * Constructs a <code>BandwidthRateSettingProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BandwidthRateSettingProperties} obj Optional instance to populate.
     * @return {module:model/BandwidthRateSettingProperties} The populated <code>BandwidthRateSettingProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BandwidthRateSettingProperties();

            if (data.hasOwnProperty('schedules')) {
                obj['schedules'] = ApiClient.convertToType(data['schedules'], [BandwidthSchedule]);
            }
            if (data.hasOwnProperty('volumeCount')) {
                obj['volumeCount'] = ApiClient.convertToType(data['volumeCount'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BandwidthRateSettingProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BandwidthRateSettingProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BandwidthRateSettingProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['schedules']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['schedules'])) {
                throw new Error("Expected the field `schedules` to be an array in the JSON data but got " + data['schedules']);
            }
            // validate the optional field `schedules` (array)
            for (const item of data['schedules']) {
                BandwidthSchedule.validateJSON(item);
            };
        }

        return true;
    }


}

BandwidthRateSettingProperties.RequiredProperties = ["schedules"];

/**
 * The schedules.
 * @member {Array.<module:model/BandwidthSchedule>} schedules
 */
BandwidthRateSettingProperties.prototype['schedules'] = undefined;

/**
 * The number of volumes that uses the bandwidth setting.
 * @member {Number} volumeCount
 */
BandwidthRateSettingProperties.prototype['volumeCount'] = undefined;






export default BandwidthRateSettingProperties;

