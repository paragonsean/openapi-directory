/**
 * MonitorManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2015-04-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EmailNotification from './EmailNotification';
import WebhookNotification from './WebhookNotification';

/**
 * The AutoscaleNotification model module.
 * @module model/AutoscaleNotification
 * @version 2015-04-01
 */
class AutoscaleNotification {
    /**
     * Constructs a new <code>AutoscaleNotification</code>.
     * Autoscale notification.
     * @alias module:model/AutoscaleNotification
     * @param operation {module:model/AutoscaleNotification.OperationEnum} the operation associated with the notification and its value must be \"scale\"
     */
    constructor(operation) { 
        
        AutoscaleNotification.initialize(this, operation);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, operation) { 
        obj['operation'] = operation;
    }

    /**
     * Constructs a <code>AutoscaleNotification</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AutoscaleNotification} obj Optional instance to populate.
     * @return {module:model/AutoscaleNotification} The populated <code>AutoscaleNotification</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AutoscaleNotification();

            if (data.hasOwnProperty('email')) {
                obj['email'] = EmailNotification.constructFromObject(data['email']);
            }
            if (data.hasOwnProperty('operation')) {
                obj['operation'] = ApiClient.convertToType(data['operation'], 'String');
            }
            if (data.hasOwnProperty('webhooks')) {
                obj['webhooks'] = ApiClient.convertToType(data['webhooks'], [WebhookNotification]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AutoscaleNotification</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AutoscaleNotification</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AutoscaleNotification.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `email`
        if (data['email']) { // data not null
          EmailNotification.validateJSON(data['email']);
        }
        // ensure the json data is a string
        if (data['operation'] && !(typeof data['operation'] === 'string' || data['operation'] instanceof String)) {
            throw new Error("Expected the field `operation` to be a primitive type in the JSON string but got " + data['operation']);
        }
        if (data['webhooks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['webhooks'])) {
                throw new Error("Expected the field `webhooks` to be an array in the JSON data but got " + data['webhooks']);
            }
            // validate the optional field `webhooks` (array)
            for (const item of data['webhooks']) {
                WebhookNotification.validateJSON(item);
            };
        }

        return true;
    }


}

AutoscaleNotification.RequiredProperties = ["operation"];

/**
 * @member {module:model/EmailNotification} email
 */
AutoscaleNotification.prototype['email'] = undefined;

/**
 * the operation associated with the notification and its value must be \"scale\"
 * @member {module:model/AutoscaleNotification.OperationEnum} operation
 */
AutoscaleNotification.prototype['operation'] = undefined;

/**
 * the collection of webhook notifications.
 * @member {Array.<module:model/WebhookNotification>} webhooks
 */
AutoscaleNotification.prototype['webhooks'] = undefined;





/**
 * Allowed values for the <code>operation</code> property.
 * @enum {String}
 * @readonly
 */
AutoscaleNotification['OperationEnum'] = {

    /**
     * value: "Scale"
     * @const
     */
    "Scale": "Scale"
};



export default AutoscaleNotification;

