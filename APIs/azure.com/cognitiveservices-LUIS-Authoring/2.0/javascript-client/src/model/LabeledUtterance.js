/**
 * LUIS Authoring Client
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EntityLabel from './EntityLabel';
import EntityPrediction from './EntityPrediction';
import IntentPrediction from './IntentPrediction';

/**
 * The LabeledUtterance model module.
 * @module model/LabeledUtterance
 * @version 2.0
 */
class LabeledUtterance {
    /**
     * Constructs a new <code>LabeledUtterance</code>.
     * A prediction and label pair of an example.
     * @alias module:model/LabeledUtterance
     */
    constructor() { 
        
        LabeledUtterance.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LabeledUtterance</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LabeledUtterance} obj Optional instance to populate.
     * @return {module:model/LabeledUtterance} The populated <code>LabeledUtterance</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LabeledUtterance();

            if (data.hasOwnProperty('entityLabels')) {
                obj['entityLabels'] = ApiClient.convertToType(data['entityLabels'], [EntityLabel]);
            }
            if (data.hasOwnProperty('entityPredictions')) {
                obj['entityPredictions'] = ApiClient.convertToType(data['entityPredictions'], [EntityPrediction]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('intentLabel')) {
                obj['intentLabel'] = ApiClient.convertToType(data['intentLabel'], 'String');
            }
            if (data.hasOwnProperty('intentPredictions')) {
                obj['intentPredictions'] = ApiClient.convertToType(data['intentPredictions'], [IntentPrediction]);
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('tokenizedText')) {
                obj['tokenizedText'] = ApiClient.convertToType(data['tokenizedText'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LabeledUtterance</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LabeledUtterance</code>.
     */
    static validateJSON(data) {
        if (data['entityLabels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['entityLabels'])) {
                throw new Error("Expected the field `entityLabels` to be an array in the JSON data but got " + data['entityLabels']);
            }
            // validate the optional field `entityLabels` (array)
            for (const item of data['entityLabels']) {
                EntityLabel.validateJSON(item);
            };
        }
        if (data['entityPredictions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['entityPredictions'])) {
                throw new Error("Expected the field `entityPredictions` to be an array in the JSON data but got " + data['entityPredictions']);
            }
            // validate the optional field `entityPredictions` (array)
            for (const item of data['entityPredictions']) {
                EntityPrediction.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['intentLabel'] && !(typeof data['intentLabel'] === 'string' || data['intentLabel'] instanceof String)) {
            throw new Error("Expected the field `intentLabel` to be a primitive type in the JSON string but got " + data['intentLabel']);
        }
        if (data['intentPredictions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['intentPredictions'])) {
                throw new Error("Expected the field `intentPredictions` to be an array in the JSON data but got " + data['intentPredictions']);
            }
            // validate the optional field `intentPredictions` (array)
            for (const item of data['intentPredictions']) {
                IntentPrediction.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tokenizedText'])) {
            throw new Error("Expected the field `tokenizedText` to be an array in the JSON data but got " + data['tokenizedText']);
        }

        return true;
    }


}



/**
 * The entities matching the example.
 * @member {Array.<module:model/EntityLabel>} entityLabels
 */
LabeledUtterance.prototype['entityLabels'] = undefined;

/**
 * List of suggested entities.
 * @member {Array.<module:model/EntityPrediction>} entityPredictions
 */
LabeledUtterance.prototype['entityPredictions'] = undefined;

/**
 * ID of Labeled Utterance.
 * @member {Number} id
 */
LabeledUtterance.prototype['id'] = undefined;

/**
 * The intent matching the example.
 * @member {String} intentLabel
 */
LabeledUtterance.prototype['intentLabel'] = undefined;

/**
 * List of suggested intents.
 * @member {Array.<module:model/IntentPrediction>} intentPredictions
 */
LabeledUtterance.prototype['intentPredictions'] = undefined;

/**
 * The utterance. For example, \"What's the weather like in seattle?\"
 * @member {String} text
 */
LabeledUtterance.prototype['text'] = undefined;

/**
 * The utterance tokenized.
 * @member {Array.<String>} tokenizedText
 */
LabeledUtterance.prototype['tokenizedText'] = undefined;






export default LabeledUtterance;

