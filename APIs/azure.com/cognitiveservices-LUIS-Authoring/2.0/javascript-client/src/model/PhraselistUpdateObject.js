/**
 * LUIS Authoring Client
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PhraselistUpdateObject model module.
 * @module model/PhraselistUpdateObject
 * @version 2.0
 */
class PhraselistUpdateObject {
    /**
     * Constructs a new <code>PhraselistUpdateObject</code>.
     * Object model for updating a Phraselist.
     * @alias module:model/PhraselistUpdateObject
     */
    constructor() { 
        
        PhraselistUpdateObject.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['isActive'] = true;
        obj['isExchangeable'] = true;
    }

    /**
     * Constructs a <code>PhraselistUpdateObject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PhraselistUpdateObject} obj Optional instance to populate.
     * @return {module:model/PhraselistUpdateObject} The populated <code>PhraselistUpdateObject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PhraselistUpdateObject();

            if (data.hasOwnProperty('isActive')) {
                obj['isActive'] = ApiClient.convertToType(data['isActive'], 'Boolean');
            }
            if (data.hasOwnProperty('isExchangeable')) {
                obj['isExchangeable'] = ApiClient.convertToType(data['isExchangeable'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('phrases')) {
                obj['phrases'] = ApiClient.convertToType(data['phrases'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PhraselistUpdateObject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PhraselistUpdateObject</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['phrases'] && !(typeof data['phrases'] === 'string' || data['phrases'] instanceof String)) {
            throw new Error("Expected the field `phrases` to be a primitive type in the JSON string but got " + data['phrases']);
        }

        return true;
    }


}



/**
 * Indicates if the Phraselist is enabled.
 * @member {Boolean} isActive
 * @default true
 */
PhraselistUpdateObject.prototype['isActive'] = true;

/**
 * An exchangeable phrase list feature are serves as single feature to the LUIS underlying training algorithm. It is used as a lexicon lookup feature where its value is 1 if the lexicon contains a given word or 0 if it doesn’t. Think of an exchangeable as a synonyms list. A non-exchangeable phrase list feature has all the phrases in the list serve as separate features to the underlying training algorithm. So, if you your phrase list feature contains 5 phrases, they will be mapped to 5 separate features. You can think of the non-exchangeable phrase list feature as an additional bag of words that you are willing to add to LUIS existing vocabulary features. Think of a non-exchangeable as set of different words. Default value is true.
 * @member {Boolean} isExchangeable
 * @default true
 */
PhraselistUpdateObject.prototype['isExchangeable'] = true;

/**
 * The Phraselist name.
 * @member {String} name
 */
PhraselistUpdateObject.prototype['name'] = undefined;

/**
 * List of comma-separated phrases that represent the Phraselist.
 * @member {String} phrases
 */
PhraselistUpdateObject.prototype['phrases'] = undefined;






export default PhraselistUpdateObject;

