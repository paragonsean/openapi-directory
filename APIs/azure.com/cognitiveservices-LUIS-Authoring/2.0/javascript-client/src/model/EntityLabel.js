/**
 * LUIS Authoring Client
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EntityLabel model module.
 * @module model/EntityLabel
 * @version 2.0
 */
class EntityLabel {
    /**
     * Constructs a new <code>EntityLabel</code>.
     * Defines the entity type and position of the extracted entity within the example.
     * @alias module:model/EntityLabel
     * @param endTokenIndex {Number} The index within the utterance where the extracted entity ends.
     * @param entityName {String} The entity type.
     * @param startTokenIndex {Number} The index within the utterance where the extracted entity starts.
     */
    constructor(endTokenIndex, entityName, startTokenIndex) { 
        
        EntityLabel.initialize(this, endTokenIndex, entityName, startTokenIndex);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endTokenIndex, entityName, startTokenIndex) { 
        obj['endTokenIndex'] = endTokenIndex;
        obj['entityName'] = entityName;
        obj['startTokenIndex'] = startTokenIndex;
    }

    /**
     * Constructs a <code>EntityLabel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EntityLabel} obj Optional instance to populate.
     * @return {module:model/EntityLabel} The populated <code>EntityLabel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EntityLabel();

            if (data.hasOwnProperty('endTokenIndex')) {
                obj['endTokenIndex'] = ApiClient.convertToType(data['endTokenIndex'], 'Number');
            }
            if (data.hasOwnProperty('entityName')) {
                obj['entityName'] = ApiClient.convertToType(data['entityName'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('roleId')) {
                obj['roleId'] = ApiClient.convertToType(data['roleId'], 'String');
            }
            if (data.hasOwnProperty('startTokenIndex')) {
                obj['startTokenIndex'] = ApiClient.convertToType(data['startTokenIndex'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EntityLabel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EntityLabel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EntityLabel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['entityName'] && !(typeof data['entityName'] === 'string' || data['entityName'] instanceof String)) {
            throw new Error("Expected the field `entityName` to be a primitive type in the JSON string but got " + data['entityName']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }
        // ensure the json data is a string
        if (data['roleId'] && !(typeof data['roleId'] === 'string' || data['roleId'] instanceof String)) {
            throw new Error("Expected the field `roleId` to be a primitive type in the JSON string but got " + data['roleId']);
        }

        return true;
    }


}

EntityLabel.RequiredProperties = ["endTokenIndex", "entityName", "startTokenIndex"];

/**
 * The index within the utterance where the extracted entity ends.
 * @member {Number} endTokenIndex
 */
EntityLabel.prototype['endTokenIndex'] = undefined;

/**
 * The entity type.
 * @member {String} entityName
 */
EntityLabel.prototype['entityName'] = undefined;

/**
 * The role of the entity within the utterance.
 * @member {String} role
 */
EntityLabel.prototype['role'] = undefined;

/**
 * The role Id.
 * @member {String} roleId
 */
EntityLabel.prototype['roleId'] = undefined;

/**
 * The index within the utterance where the extracted entity starts.
 * @member {Number} startTokenIndex
 */
EntityLabel.prototype['startTokenIndex'] = undefined;






export default EntityLabel;

