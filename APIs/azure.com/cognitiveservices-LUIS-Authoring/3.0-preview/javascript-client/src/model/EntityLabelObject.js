/**
 * LUIS Authoring Client
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.0-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EntityLabelObject model module.
 * @module model/EntityLabelObject
 * @version 3.0-preview
 */
class EntityLabelObject {
    /**
     * Constructs a new <code>EntityLabelObject</code>.
     * Defines the entity type and position of the extracted entity within the example.
     * @alias module:model/EntityLabelObject
     * @param endCharIndex {Number} The index within the utterance where the extracted entity ends.
     * @param entityName {String} The entity type.
     * @param startCharIndex {Number} The index within the utterance where the extracted entity starts.
     */
    constructor(endCharIndex, entityName, startCharIndex) { 
        
        EntityLabelObject.initialize(this, endCharIndex, entityName, startCharIndex);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endCharIndex, entityName, startCharIndex) { 
        obj['endCharIndex'] = endCharIndex;
        obj['entityName'] = entityName;
        obj['startCharIndex'] = startCharIndex;
    }

    /**
     * Constructs a <code>EntityLabelObject</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EntityLabelObject} obj Optional instance to populate.
     * @return {module:model/EntityLabelObject} The populated <code>EntityLabelObject</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EntityLabelObject();

            if (data.hasOwnProperty('endCharIndex')) {
                obj['endCharIndex'] = ApiClient.convertToType(data['endCharIndex'], 'Number');
            }
            if (data.hasOwnProperty('entityName')) {
                obj['entityName'] = ApiClient.convertToType(data['entityName'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('startCharIndex')) {
                obj['startCharIndex'] = ApiClient.convertToType(data['startCharIndex'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EntityLabelObject</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EntityLabelObject</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EntityLabelObject.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['entityName'] && !(typeof data['entityName'] === 'string' || data['entityName'] instanceof String)) {
            throw new Error("Expected the field `entityName` to be a primitive type in the JSON string but got " + data['entityName']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }

        return true;
    }


}

EntityLabelObject.RequiredProperties = ["endCharIndex", "entityName", "startCharIndex"];

/**
 * The index within the utterance where the extracted entity ends.
 * @member {Number} endCharIndex
 */
EntityLabelObject.prototype['endCharIndex'] = undefined;

/**
 * The entity type.
 * @member {String} entityName
 */
EntityLabelObject.prototype['entityName'] = undefined;

/**
 * The role the entity plays in the utterance.
 * @member {String} role
 */
EntityLabelObject.prototype['role'] = undefined;

/**
 * The index within the utterance where the extracted entity starts.
 * @member {Number} startCharIndex
 */
EntityLabelObject.prototype['startCharIndex'] = undefined;






export default EntityLabelObject;

