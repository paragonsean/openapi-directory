/**
 * NetworkManagementClient
 * The Microsoft Azure Network management API provides a RESTful set of web services that interact with Microsoft Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Microsoft Azure Networks service.
 *
 * The version of the OpenAPI document: 2018-07-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TrafficQuery model module.
 * @module model/TrafficQuery
 * @version 2018-07-01
 */
class TrafficQuery {
    /**
     * Constructs a new <code>TrafficQuery</code>.
     * Parameters to compare with network configuration.
     * @alias module:model/TrafficQuery
     * @param destination {String} Traffic destination. Accepted values are: '*', IP Address/CIDR, Service Tag.
     * @param destinationPort {String} Traffic destination port. Accepted values are '*', port (for example, 3389) and port range (for example, 80-100).
     * @param direction {module:model/TrafficQuery.DirectionEnum} The direction of the traffic. Accepted values are 'Inbound' and 'Outbound'.
     * @param protocol {String} Protocol to be verified on. Accepted values are '*', TCP, UDP.
     * @param source {String} Traffic source. Accepted values are '*', IP Address/CIDR, Service Tag.
     */
    constructor(destination, destinationPort, direction, protocol, source) { 
        
        TrafficQuery.initialize(this, destination, destinationPort, direction, protocol, source);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, destination, destinationPort, direction, protocol, source) { 
        obj['destination'] = destination;
        obj['destinationPort'] = destinationPort;
        obj['direction'] = direction;
        obj['protocol'] = protocol;
        obj['source'] = source;
    }

    /**
     * Constructs a <code>TrafficQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrafficQuery} obj Optional instance to populate.
     * @return {module:model/TrafficQuery} The populated <code>TrafficQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrafficQuery();

            if (data.hasOwnProperty('destination')) {
                obj['destination'] = ApiClient.convertToType(data['destination'], 'String');
            }
            if (data.hasOwnProperty('destinationPort')) {
                obj['destinationPort'] = ApiClient.convertToType(data['destinationPort'], 'String');
            }
            if (data.hasOwnProperty('direction')) {
                obj['direction'] = ApiClient.convertToType(data['direction'], 'String');
            }
            if (data.hasOwnProperty('protocol')) {
                obj['protocol'] = ApiClient.convertToType(data['protocol'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrafficQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrafficQuery</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrafficQuery.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['destination'] && !(typeof data['destination'] === 'string' || data['destination'] instanceof String)) {
            throw new Error("Expected the field `destination` to be a primitive type in the JSON string but got " + data['destination']);
        }
        // ensure the json data is a string
        if (data['destinationPort'] && !(typeof data['destinationPort'] === 'string' || data['destinationPort'] instanceof String)) {
            throw new Error("Expected the field `destinationPort` to be a primitive type in the JSON string but got " + data['destinationPort']);
        }
        // ensure the json data is a string
        if (data['direction'] && !(typeof data['direction'] === 'string' || data['direction'] instanceof String)) {
            throw new Error("Expected the field `direction` to be a primitive type in the JSON string but got " + data['direction']);
        }
        // ensure the json data is a string
        if (data['protocol'] && !(typeof data['protocol'] === 'string' || data['protocol'] instanceof String)) {
            throw new Error("Expected the field `protocol` to be a primitive type in the JSON string but got " + data['protocol']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

TrafficQuery.RequiredProperties = ["destination", "destinationPort", "direction", "protocol", "source"];

/**
 * Traffic destination. Accepted values are: '*', IP Address/CIDR, Service Tag.
 * @member {String} destination
 */
TrafficQuery.prototype['destination'] = undefined;

/**
 * Traffic destination port. Accepted values are '*', port (for example, 3389) and port range (for example, 80-100).
 * @member {String} destinationPort
 */
TrafficQuery.prototype['destinationPort'] = undefined;

/**
 * The direction of the traffic. Accepted values are 'Inbound' and 'Outbound'.
 * @member {module:model/TrafficQuery.DirectionEnum} direction
 */
TrafficQuery.prototype['direction'] = undefined;

/**
 * Protocol to be verified on. Accepted values are '*', TCP, UDP.
 * @member {String} protocol
 */
TrafficQuery.prototype['protocol'] = undefined;

/**
 * Traffic source. Accepted values are '*', IP Address/CIDR, Service Tag.
 * @member {String} source
 */
TrafficQuery.prototype['source'] = undefined;





/**
 * Allowed values for the <code>direction</code> property.
 * @enum {String}
 * @readonly
 */
TrafficQuery['DirectionEnum'] = {

    /**
     * value: "Inbound"
     * @const
     */
    "Inbound": "Inbound",

    /**
     * value: "Outbound"
     * @const
     */
    "Outbound": "Outbound"
};



export default TrafficQuery;

