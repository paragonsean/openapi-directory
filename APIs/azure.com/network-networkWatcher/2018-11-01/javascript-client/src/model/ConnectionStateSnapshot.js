/**
 * NetworkManagementClient
 * The Microsoft Azure Network management API provides a RESTful set of web services that interact with Microsoft Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Microsoft Azure Networks service.
 *
 * The version of the OpenAPI document: 2018-11-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ConnectivityHop from './ConnectivityHop';

/**
 * The ConnectionStateSnapshot model module.
 * @module model/ConnectionStateSnapshot
 * @version 2018-11-01
 */
class ConnectionStateSnapshot {
    /**
     * Constructs a new <code>ConnectionStateSnapshot</code>.
     * Connection state snapshot.
     * @alias module:model/ConnectionStateSnapshot
     */
    constructor() { 
        
        ConnectionStateSnapshot.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ConnectionStateSnapshot</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ConnectionStateSnapshot} obj Optional instance to populate.
     * @return {module:model/ConnectionStateSnapshot} The populated <code>ConnectionStateSnapshot</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ConnectionStateSnapshot();

            if (data.hasOwnProperty('avgLatencyInMs')) {
                obj['avgLatencyInMs'] = ApiClient.convertToType(data['avgLatencyInMs'], 'Number');
            }
            if (data.hasOwnProperty('connectionState')) {
                obj['connectionState'] = ApiClient.convertToType(data['connectionState'], 'String');
            }
            if (data.hasOwnProperty('endTime')) {
                obj['endTime'] = ApiClient.convertToType(data['endTime'], 'Date');
            }
            if (data.hasOwnProperty('evaluationState')) {
                obj['evaluationState'] = ApiClient.convertToType(data['evaluationState'], 'String');
            }
            if (data.hasOwnProperty('hops')) {
                obj['hops'] = ApiClient.convertToType(data['hops'], [ConnectivityHop]);
            }
            if (data.hasOwnProperty('maxLatencyInMs')) {
                obj['maxLatencyInMs'] = ApiClient.convertToType(data['maxLatencyInMs'], 'Number');
            }
            if (data.hasOwnProperty('minLatencyInMs')) {
                obj['minLatencyInMs'] = ApiClient.convertToType(data['minLatencyInMs'], 'Number');
            }
            if (data.hasOwnProperty('probesFailed')) {
                obj['probesFailed'] = ApiClient.convertToType(data['probesFailed'], 'Number');
            }
            if (data.hasOwnProperty('probesSent')) {
                obj['probesSent'] = ApiClient.convertToType(data['probesSent'], 'Number');
            }
            if (data.hasOwnProperty('startTime')) {
                obj['startTime'] = ApiClient.convertToType(data['startTime'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ConnectionStateSnapshot</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ConnectionStateSnapshot</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['connectionState'] && !(typeof data['connectionState'] === 'string' || data['connectionState'] instanceof String)) {
            throw new Error("Expected the field `connectionState` to be a primitive type in the JSON string but got " + data['connectionState']);
        }
        // ensure the json data is a string
        if (data['evaluationState'] && !(typeof data['evaluationState'] === 'string' || data['evaluationState'] instanceof String)) {
            throw new Error("Expected the field `evaluationState` to be a primitive type in the JSON string but got " + data['evaluationState']);
        }
        if (data['hops']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['hops'])) {
                throw new Error("Expected the field `hops` to be an array in the JSON data but got " + data['hops']);
            }
            // validate the optional field `hops` (array)
            for (const item of data['hops']) {
                ConnectivityHop.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Average latency in ms.
 * @member {Number} avgLatencyInMs
 */
ConnectionStateSnapshot.prototype['avgLatencyInMs'] = undefined;

/**
 * The connection state.
 * @member {module:model/ConnectionStateSnapshot.ConnectionStateEnum} connectionState
 */
ConnectionStateSnapshot.prototype['connectionState'] = undefined;

/**
 * The end time of the connection snapshot.
 * @member {Date} endTime
 */
ConnectionStateSnapshot.prototype['endTime'] = undefined;

/**
 * Connectivity analysis evaluation state.
 * @member {module:model/ConnectionStateSnapshot.EvaluationStateEnum} evaluationState
 */
ConnectionStateSnapshot.prototype['evaluationState'] = undefined;

/**
 * List of hops between the source and the destination.
 * @member {Array.<module:model/ConnectivityHop>} hops
 */
ConnectionStateSnapshot.prototype['hops'] = undefined;

/**
 * Maximum latency in ms.
 * @member {Number} maxLatencyInMs
 */
ConnectionStateSnapshot.prototype['maxLatencyInMs'] = undefined;

/**
 * Minimum latency in ms.
 * @member {Number} minLatencyInMs
 */
ConnectionStateSnapshot.prototype['minLatencyInMs'] = undefined;

/**
 * The number of failed probes.
 * @member {Number} probesFailed
 */
ConnectionStateSnapshot.prototype['probesFailed'] = undefined;

/**
 * The number of sent probes.
 * @member {Number} probesSent
 */
ConnectionStateSnapshot.prototype['probesSent'] = undefined;

/**
 * The start time of the connection snapshot.
 * @member {Date} startTime
 */
ConnectionStateSnapshot.prototype['startTime'] = undefined;





/**
 * Allowed values for the <code>connectionState</code> property.
 * @enum {String}
 * @readonly
 */
ConnectionStateSnapshot['ConnectionStateEnum'] = {

    /**
     * value: "Reachable"
     * @const
     */
    "Reachable": "Reachable",

    /**
     * value: "Unreachable"
     * @const
     */
    "Unreachable": "Unreachable",

    /**
     * value: "Unknown"
     * @const
     */
    "Unknown": "Unknown"
};


/**
 * Allowed values for the <code>evaluationState</code> property.
 * @enum {String}
 * @readonly
 */
ConnectionStateSnapshot['EvaluationStateEnum'] = {

    /**
     * value: "NotStarted"
     * @const
     */
    "NotStarted": "NotStarted",

    /**
     * value: "InProgress"
     * @const
     */
    "InProgress": "InProgress",

    /**
     * value: "Completed"
     * @const
     */
    "Completed": "Completed"
};



export default ConnectionStateSnapshot;

