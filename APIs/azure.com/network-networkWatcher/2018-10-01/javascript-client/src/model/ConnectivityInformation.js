/**
 * NetworkManagementClient
 * The Microsoft Azure Network management API provides a RESTful set of web services that interact with Microsoft Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Microsoft Azure Networks service.
 *
 * The version of the OpenAPI document: 2018-10-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ConnectivityHop from './ConnectivityHop';

/**
 * The ConnectivityInformation model module.
 * @module model/ConnectivityInformation
 * @version 2018-10-01
 */
class ConnectivityInformation {
    /**
     * Constructs a new <code>ConnectivityInformation</code>.
     * Information on the connectivity status.
     * @alias module:model/ConnectivityInformation
     */
    constructor() { 
        
        ConnectivityInformation.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ConnectivityInformation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ConnectivityInformation} obj Optional instance to populate.
     * @return {module:model/ConnectivityInformation} The populated <code>ConnectivityInformation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ConnectivityInformation();

            if (data.hasOwnProperty('avgLatencyInMs')) {
                obj['avgLatencyInMs'] = ApiClient.convertToType(data['avgLatencyInMs'], 'Number');
            }
            if (data.hasOwnProperty('connectionStatus')) {
                obj['connectionStatus'] = ApiClient.convertToType(data['connectionStatus'], 'String');
            }
            if (data.hasOwnProperty('hops')) {
                obj['hops'] = ApiClient.convertToType(data['hops'], [ConnectivityHop]);
            }
            if (data.hasOwnProperty('maxLatencyInMs')) {
                obj['maxLatencyInMs'] = ApiClient.convertToType(data['maxLatencyInMs'], 'Number');
            }
            if (data.hasOwnProperty('minLatencyInMs')) {
                obj['minLatencyInMs'] = ApiClient.convertToType(data['minLatencyInMs'], 'Number');
            }
            if (data.hasOwnProperty('probesFailed')) {
                obj['probesFailed'] = ApiClient.convertToType(data['probesFailed'], 'Number');
            }
            if (data.hasOwnProperty('probesSent')) {
                obj['probesSent'] = ApiClient.convertToType(data['probesSent'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ConnectivityInformation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ConnectivityInformation</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['connectionStatus'] && !(typeof data['connectionStatus'] === 'string' || data['connectionStatus'] instanceof String)) {
            throw new Error("Expected the field `connectionStatus` to be a primitive type in the JSON string but got " + data['connectionStatus']);
        }
        if (data['hops']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['hops'])) {
                throw new Error("Expected the field `hops` to be an array in the JSON data but got " + data['hops']);
            }
            // validate the optional field `hops` (array)
            for (const item of data['hops']) {
                ConnectivityHop.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Average latency in milliseconds.
 * @member {Number} avgLatencyInMs
 */
ConnectivityInformation.prototype['avgLatencyInMs'] = undefined;

/**
 * The connection status.
 * @member {module:model/ConnectivityInformation.ConnectionStatusEnum} connectionStatus
 */
ConnectivityInformation.prototype['connectionStatus'] = undefined;

/**
 * List of hops between the source and the destination.
 * @member {Array.<module:model/ConnectivityHop>} hops
 */
ConnectivityInformation.prototype['hops'] = undefined;

/**
 * Maximum latency in milliseconds.
 * @member {Number} maxLatencyInMs
 */
ConnectivityInformation.prototype['maxLatencyInMs'] = undefined;

/**
 * Minimum latency in milliseconds.
 * @member {Number} minLatencyInMs
 */
ConnectivityInformation.prototype['minLatencyInMs'] = undefined;

/**
 * Number of failed probes.
 * @member {Number} probesFailed
 */
ConnectivityInformation.prototype['probesFailed'] = undefined;

/**
 * Total number of probes sent.
 * @member {Number} probesSent
 */
ConnectivityInformation.prototype['probesSent'] = undefined;





/**
 * Allowed values for the <code>connectionStatus</code> property.
 * @enum {String}
 * @readonly
 */
ConnectivityInformation['ConnectionStatusEnum'] = {

    /**
     * value: "Unknown"
     * @const
     */
    "Unknown": "Unknown",

    /**
     * value: "Connected"
     * @const
     */
    "Connected": "Connected",

    /**
     * value: "Disconnected"
     * @const
     */
    "Disconnected": "Disconnected",

    /**
     * value: "Degraded"
     * @const
     */
    "Degraded": "Degraded"
};



export default ConnectivityInformation;

