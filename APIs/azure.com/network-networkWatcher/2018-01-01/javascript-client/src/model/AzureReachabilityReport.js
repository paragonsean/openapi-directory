/**
 * NetworkManagementClient
 * The Microsoft Azure Network management API provides a RESTful set of web services that interact with Microsoft Azure Networks service to manage your network resources. The API has entities that capture the relationship between an end user and the Microsoft Azure Networks service.
 *
 * The version of the OpenAPI document: 2018-01-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AzureReachabilityReportItem from './AzureReachabilityReportItem';
import AzureReachabilityReportLocation from './AzureReachabilityReportLocation';

/**
 * The AzureReachabilityReport model module.
 * @module model/AzureReachabilityReport
 * @version 2018-01-01
 */
class AzureReachabilityReport {
    /**
     * Constructs a new <code>AzureReachabilityReport</code>.
     * Azure reachability report details.
     * @alias module:model/AzureReachabilityReport
     * @param aggregationLevel {String} The aggregation level of Azure reachability report. Can be Country, State or City.
     * @param providerLocation {module:model/AzureReachabilityReportLocation} 
     * @param reachabilityReport {Array.<module:model/AzureReachabilityReportItem>} List of Azure reachability report items.
     */
    constructor(aggregationLevel, providerLocation, reachabilityReport) { 
        
        AzureReachabilityReport.initialize(this, aggregationLevel, providerLocation, reachabilityReport);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, aggregationLevel, providerLocation, reachabilityReport) { 
        obj['aggregationLevel'] = aggregationLevel;
        obj['providerLocation'] = providerLocation;
        obj['reachabilityReport'] = reachabilityReport;
    }

    /**
     * Constructs a <code>AzureReachabilityReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AzureReachabilityReport} obj Optional instance to populate.
     * @return {module:model/AzureReachabilityReport} The populated <code>AzureReachabilityReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AzureReachabilityReport();

            if (data.hasOwnProperty('aggregationLevel')) {
                obj['aggregationLevel'] = ApiClient.convertToType(data['aggregationLevel'], 'String');
            }
            if (data.hasOwnProperty('providerLocation')) {
                obj['providerLocation'] = AzureReachabilityReportLocation.constructFromObject(data['providerLocation']);
            }
            if (data.hasOwnProperty('reachabilityReport')) {
                obj['reachabilityReport'] = ApiClient.convertToType(data['reachabilityReport'], [AzureReachabilityReportItem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AzureReachabilityReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AzureReachabilityReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AzureReachabilityReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['aggregationLevel'] && !(typeof data['aggregationLevel'] === 'string' || data['aggregationLevel'] instanceof String)) {
            throw new Error("Expected the field `aggregationLevel` to be a primitive type in the JSON string but got " + data['aggregationLevel']);
        }
        // validate the optional field `providerLocation`
        if (data['providerLocation']) { // data not null
          AzureReachabilityReportLocation.validateJSON(data['providerLocation']);
        }
        if (data['reachabilityReport']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['reachabilityReport'])) {
                throw new Error("Expected the field `reachabilityReport` to be an array in the JSON data but got " + data['reachabilityReport']);
            }
            // validate the optional field `reachabilityReport` (array)
            for (const item of data['reachabilityReport']) {
                AzureReachabilityReportItem.validateJSON(item);
            };
        }

        return true;
    }


}

AzureReachabilityReport.RequiredProperties = ["aggregationLevel", "providerLocation", "reachabilityReport"];

/**
 * The aggregation level of Azure reachability report. Can be Country, State or City.
 * @member {String} aggregationLevel
 */
AzureReachabilityReport.prototype['aggregationLevel'] = undefined;

/**
 * @member {module:model/AzureReachabilityReportLocation} providerLocation
 */
AzureReachabilityReport.prototype['providerLocation'] = undefined;

/**
 * List of Azure reachability report items.
 * @member {Array.<module:model/AzureReachabilityReportItem>} reachabilityReport
 */
AzureReachabilityReport.prototype['reachabilityReport'] = undefined;






export default AzureReachabilityReport;

