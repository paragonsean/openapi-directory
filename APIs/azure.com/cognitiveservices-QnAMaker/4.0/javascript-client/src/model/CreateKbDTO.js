/**
 * QnAMaker Client
 * An API for QnAMaker Service
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FileDTO from './FileDTO';
import QnADTO from './QnADTO';

/**
 * The CreateKbDTO model module.
 * @module model/CreateKbDTO
 * @version 4.0
 */
class CreateKbDTO {
    /**
     * Constructs a new <code>CreateKbDTO</code>.
     * Post body schema for CreateKb operation.
     * @alias module:model/CreateKbDTO
     * @param name {String} Friendly name for the knowledgebase.
     */
    constructor(name) { 
        
        CreateKbDTO.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>CreateKbDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateKbDTO} obj Optional instance to populate.
     * @return {module:model/CreateKbDTO} The populated <code>CreateKbDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateKbDTO();

            if (data.hasOwnProperty('defaultAnswerUsedForExtraction')) {
                obj['defaultAnswerUsedForExtraction'] = ApiClient.convertToType(data['defaultAnswerUsedForExtraction'], 'String');
            }
            if (data.hasOwnProperty('enableHierarchicalExtraction')) {
                obj['enableHierarchicalExtraction'] = ApiClient.convertToType(data['enableHierarchicalExtraction'], 'Boolean');
            }
            if (data.hasOwnProperty('files')) {
                obj['files'] = ApiClient.convertToType(data['files'], [FileDTO]);
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('qnaList')) {
                obj['qnaList'] = ApiClient.convertToType(data['qnaList'], [QnADTO]);
            }
            if (data.hasOwnProperty('urls')) {
                obj['urls'] = ApiClient.convertToType(data['urls'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateKbDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateKbDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateKbDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['defaultAnswerUsedForExtraction'] && !(typeof data['defaultAnswerUsedForExtraction'] === 'string' || data['defaultAnswerUsedForExtraction'] instanceof String)) {
            throw new Error("Expected the field `defaultAnswerUsedForExtraction` to be a primitive type in the JSON string but got " + data['defaultAnswerUsedForExtraction']);
        }
        if (data['files']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['files'])) {
                throw new Error("Expected the field `files` to be an array in the JSON data but got " + data['files']);
            }
            // validate the optional field `files` (array)
            for (const item of data['files']) {
                FileDTO.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['language'] && !(typeof data['language'] === 'string' || data['language'] instanceof String)) {
            throw new Error("Expected the field `language` to be a primitive type in the JSON string but got " + data['language']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['qnaList']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['qnaList'])) {
                throw new Error("Expected the field `qnaList` to be an array in the JSON data but got " + data['qnaList']);
            }
            // validate the optional field `qnaList` (array)
            for (const item of data['qnaList']) {
                QnADTO.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['urls'])) {
            throw new Error("Expected the field `urls` to be an array in the JSON data but got " + data['urls']);
        }

        return true;
    }


}

CreateKbDTO.RequiredProperties = ["name"];

/**
 * Text string to be used as the answer in any Q-A which has no extracted answer from the document but has a hierarchy. Required when EnableHierarchicalExtraction field is set to True.
 * @member {String} defaultAnswerUsedForExtraction
 */
CreateKbDTO.prototype['defaultAnswerUsedForExtraction'] = undefined;

/**
 * Enable hierarchical extraction of Q-A from files and urls. Value to be considered False if this field is not present.
 * @member {Boolean} enableHierarchicalExtraction
 */
CreateKbDTO.prototype['enableHierarchicalExtraction'] = undefined;

/**
 * List of files from which to Extract Q-A.
 * @member {Array.<module:model/FileDTO>} files
 */
CreateKbDTO.prototype['files'] = undefined;

/**
 * Language of the knowledgebase.
 * @member {String} language
 */
CreateKbDTO.prototype['language'] = undefined;

/**
 * Friendly name for the knowledgebase.
 * @member {String} name
 */
CreateKbDTO.prototype['name'] = undefined;

/**
 * List of Q-A (QnADTO) to be added to the knowledgebase. Q-A Ids are assigned by the service and should be omitted.
 * @member {Array.<module:model/QnADTO>} qnaList
 */
CreateKbDTO.prototype['qnaList'] = undefined;

/**
 * List of URLs to be used for extracting Q-A.
 * @member {Array.<String>} urls
 */
CreateKbDTO.prototype['urls'] = undefined;






export default CreateKbDTO;

