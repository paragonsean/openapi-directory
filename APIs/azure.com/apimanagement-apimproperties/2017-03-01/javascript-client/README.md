# api_management_client

ApiManagementClient - JavaScript client for api_management_client
Use these REST APIs for performing operations on Property entity associated with your Azure API Management deployment. API Management policies are a powerful capability of the system that allow the publisher to change the behavior of the API through configuration. Policies are a collection of statements that are executed sequentially on the request or response of an API. Policy statements can be constructed using literal text values, policy expressions, and properties. Each API Management service instance has a properties collection of key/value pairs that are global to the service instance. These properties can be used to manage constant string values across all API configuration and policies.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 2017-03-01
- Package version: 2017-03-01
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install api_management_client --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your api_management_client from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var ApiManagementClient = require('api_management_client');

var defaultClient = ApiManagementClient.ApiClient.instance;
// Configure API key authorization: apim_key
var apim_key = defaultClient.authentications['apim_key'];
apim_key.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//apim_key.apiKeyPrefix['Authorization'] = "Token"

var api = new ApiManagementClient.PropertyApi()
var propId = "propId_example"; // {String} Identifier of the property.
var apiVersion = "apiVersion_example"; // {String} Version of the API to be used with the client request.
var parameters = new ApiManagementClient.PropertyContract(); // {PropertyContract} Create parameters.
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.propertyCreateOrUpdate(propId, apiVersion, parameters, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://azure.local*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*ApiManagementClient.PropertyApi* | [**propertyCreateOrUpdate**](docs/PropertyApi.md#propertyCreateOrUpdate) | **PUT** /properties/{propId} | 
*ApiManagementClient.PropertyApi* | [**propertyDelete**](docs/PropertyApi.md#propertyDelete) | **DELETE** /properties/{propId} | 
*ApiManagementClient.PropertyApi* | [**propertyGet**](docs/PropertyApi.md#propertyGet) | **GET** /properties/{propId} | 
*ApiManagementClient.PropertyApi* | [**propertyList**](docs/PropertyApi.md#propertyList) | **GET** /properties | 
*ApiManagementClient.PropertyApi* | [**propertyUpdate**](docs/PropertyApi.md#propertyUpdate) | **PATCH** /properties/{propId} | 


## Documentation for Models

 - [ApiManagementClient.PropertyCollection](docs/PropertyCollection.md)
 - [ApiManagementClient.PropertyContract](docs/PropertyContract.md)
 - [ApiManagementClient.PropertyContractProperties](docs/PropertyContractProperties.md)
 - [ApiManagementClient.PropertyEntityBaseParameters](docs/PropertyEntityBaseParameters.md)
 - [ApiManagementClient.PropertyGetDefaultResponse](docs/PropertyGetDefaultResponse.md)
 - [ApiManagementClient.PropertyGetDefaultResponseDetailsInner](docs/PropertyGetDefaultResponseDetailsInner.md)
 - [ApiManagementClient.PropertyUpdateParameters](docs/PropertyUpdateParameters.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### apim_key


- **Type**: API key
- **API key parameter name**: Authorization
- **Location**: HTTP header

