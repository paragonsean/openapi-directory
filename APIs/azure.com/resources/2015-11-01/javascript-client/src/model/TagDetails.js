/**
 * ResourceManagementClient
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2015-11-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TagCount from './TagCount';
import TagValue from './TagValue';

/**
 * The TagDetails model module.
 * @module model/TagDetails
 * @version 2015-11-01
 */
class TagDetails {
    /**
     * Constructs a new <code>TagDetails</code>.
     * Tag details.
     * @alias module:model/TagDetails
     */
    constructor() { 
        
        TagDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TagDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TagDetails} obj Optional instance to populate.
     * @return {module:model/TagDetails} The populated <code>TagDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TagDetails();

            if (data.hasOwnProperty('count')) {
                obj['count'] = TagCount.constructFromObject(data['count']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('tagName')) {
                obj['tagName'] = ApiClient.convertToType(data['tagName'], 'String');
            }
            if (data.hasOwnProperty('values')) {
                obj['values'] = ApiClient.convertToType(data['values'], [TagValue]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TagDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TagDetails</code>.
     */
    static validateJSON(data) {
        // validate the optional field `count`
        if (data['count']) { // data not null
          TagCount.validateJSON(data['count']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['tagName'] && !(typeof data['tagName'] === 'string' || data['tagName'] instanceof String)) {
            throw new Error("Expected the field `tagName` to be a primitive type in the JSON string but got " + data['tagName']);
        }
        if (data['values']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['values'])) {
                throw new Error("Expected the field `values` to be an array in the JSON data but got " + data['values']);
            }
            // validate the optional field `values` (array)
            for (const item of data['values']) {
                TagValue.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/TagCount} count
 */
TagDetails.prototype['count'] = undefined;

/**
 * Gets or sets the tag ID.
 * @member {String} id
 */
TagDetails.prototype['id'] = undefined;

/**
 * Gets or sets the tag name.
 * @member {String} tagName
 */
TagDetails.prototype['tagName'] = undefined;

/**
 * Gets or sets the list of tag values.
 * @member {Array.<module:model/TagValue>} values
 */
TagDetails.prototype['values'] = undefined;






export default TagDetails;

