/**
 * SharedImageGalleryServiceClient
 * Shared Image Gallery Service Client.
 *
 * The version of the OpenAPI document: 2019-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GalleryImageIdentifier model module.
 * @module model/GalleryImageIdentifier
 * @version 2019-03-01
 */
class GalleryImageIdentifier {
    /**
     * Constructs a new <code>GalleryImageIdentifier</code>.
     * This is the gallery Image Definition identifier.
     * @alias module:model/GalleryImageIdentifier
     * @param offer {String} The name of the gallery Image Definition offer.
     * @param publisher {String} The name of the gallery Image Definition publisher.
     * @param sku {String} The name of the gallery Image Definition SKU.
     */
    constructor(offer, publisher, sku) { 
        
        GalleryImageIdentifier.initialize(this, offer, publisher, sku);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, offer, publisher, sku) { 
        obj['offer'] = offer;
        obj['publisher'] = publisher;
        obj['sku'] = sku;
    }

    /**
     * Constructs a <code>GalleryImageIdentifier</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GalleryImageIdentifier} obj Optional instance to populate.
     * @return {module:model/GalleryImageIdentifier} The populated <code>GalleryImageIdentifier</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GalleryImageIdentifier();

            if (data.hasOwnProperty('offer')) {
                obj['offer'] = ApiClient.convertToType(data['offer'], 'String');
            }
            if (data.hasOwnProperty('publisher')) {
                obj['publisher'] = ApiClient.convertToType(data['publisher'], 'String');
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GalleryImageIdentifier</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GalleryImageIdentifier</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GalleryImageIdentifier.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['offer'] && !(typeof data['offer'] === 'string' || data['offer'] instanceof String)) {
            throw new Error("Expected the field `offer` to be a primitive type in the JSON string but got " + data['offer']);
        }
        // ensure the json data is a string
        if (data['publisher'] && !(typeof data['publisher'] === 'string' || data['publisher'] instanceof String)) {
            throw new Error("Expected the field `publisher` to be a primitive type in the JSON string but got " + data['publisher']);
        }
        // ensure the json data is a string
        if (data['sku'] && !(typeof data['sku'] === 'string' || data['sku'] instanceof String)) {
            throw new Error("Expected the field `sku` to be a primitive type in the JSON string but got " + data['sku']);
        }

        return true;
    }


}

GalleryImageIdentifier.RequiredProperties = ["offer", "publisher", "sku"];

/**
 * The name of the gallery Image Definition offer.
 * @member {String} offer
 */
GalleryImageIdentifier.prototype['offer'] = undefined;

/**
 * The name of the gallery Image Definition publisher.
 * @member {String} publisher
 */
GalleryImageIdentifier.prototype['publisher'] = undefined;

/**
 * The name of the gallery Image Definition SKU.
 * @member {String} sku
 */
GalleryImageIdentifier.prototype['sku'] = undefined;






export default GalleryImageIdentifier;

