# ink_recognizer_client

InkRecognizerClient - JavaScript client for ink_recognizer_client
The service is used to perform ink layout and recognition of written words and shapes. Ink strokes passed to the service are recognized and organized into recognition results in the response
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0
- Package version: 1.0
- Generator version: 7.9.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install ink_recognizer_client --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your ink_recognizer_client from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var InkRecognizerClient = require('ink_recognizer_client');

var defaultClient = InkRecognizerClient.ApiClient.instance;
// Configure API key authorization: apim_key
var apim_key = defaultClient.authentications['apim_key'];
apim_key.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//apim_key.apiKeyPrefix['Ocp-Apim-Subscription-Key'] = "Token"

var api = new InkRecognizerClient.InkRecognizerApi()
var body = new InkRecognizerClient.AnalysisRequest(); // {AnalysisRequest} The collection of stroke objects to send for analysis
var opts = {
  'xMsClientRequestId': "xMsClientRequestId_example" // {String} The request id used to uniquely identify each request during troubleshooting. This is an optional parameter useful for correlating logs and other artifacts.
};
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.inkRecognizerRecognize(body, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://azure.local*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*InkRecognizerClient.InkRecognizerApi* | [**inkRecognizerRecognize**](docs/InkRecognizerApi.md#inkRecognizerRecognize) | **PUT** /recognize | 


## Documentation for Models

 - [InkRecognizerClient.AlternatePatternInner](docs/AlternatePatternInner.md)
 - [InkRecognizerClient.AnalysisRequest](docs/AnalysisRequest.md)
 - [InkRecognizerClient.AnalysisResponse](docs/AnalysisResponse.md)
 - [InkRecognizerClient.CategoryPattern](docs/CategoryPattern.md)
 - [InkRecognizerClient.ClassPattern](docs/ClassPattern.md)
 - [InkRecognizerClient.ContainerCategoryPattern](docs/ContainerCategoryPattern.md)
 - [InkRecognizerClient.DrawingAttributesPattern](docs/DrawingAttributesPattern.md)
 - [InkRecognizerClient.DrawingAttributesPatternColor](docs/DrawingAttributesPatternColor.md)
 - [InkRecognizerClient.ErrorModel](docs/ErrorModel.md)
 - [InkRecognizerClient.ErrorModelDetailsInner](docs/ErrorModelDetailsInner.md)
 - [InkRecognizerClient.InkPoint](docs/InkPoint.md)
 - [InkRecognizerClient.InkPointValueAttribute](docs/InkPointValueAttribute.md)
 - [InkRecognizerClient.LeafCategoryPattern](docs/LeafCategoryPattern.md)
 - [InkRecognizerClient.PointDetailsPattern](docs/PointDetailsPattern.md)
 - [InkRecognizerClient.RecognitionUnitInner](docs/RecognitionUnitInner.md)
 - [InkRecognizerClient.RecognitionUnitInnerBoundingRectangle](docs/RecognitionUnitInnerBoundingRectangle.md)
 - [InkRecognizerClient.ShapePattern](docs/ShapePattern.md)
 - [InkRecognizerClient.Stroke](docs/Stroke.md)


## Documentation for Authorization


Authentication schemes defined for the API:
### apim_key


- **Type**: API key
- **API key parameter name**: Ocp-Apim-Subscription-Key
- **Location**: HTTP header

