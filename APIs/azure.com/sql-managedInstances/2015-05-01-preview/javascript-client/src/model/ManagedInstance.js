/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2015-05-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ManagedInstanceProperties from './ManagedInstanceProperties';

/**
 * The ManagedInstance model module.
 * @module model/ManagedInstance
 * @version 2015-05-01-preview
 */
class ManagedInstance {
    /**
     * Constructs a new <code>ManagedInstance</code>.
     * An Azure SQL managed instance.
     * @alias module:model/ManagedInstance
     * @param location {String} Resource location.
     */
    constructor(location) { 
        
        ManagedInstance.initialize(this, location);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, location) { 
        obj['location'] = location;
    }

    /**
     * Constructs a <code>ManagedInstance</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ManagedInstance} obj Optional instance to populate.
     * @return {module:model/ManagedInstance} The populated <code>ManagedInstance</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ManagedInstance();

            if (data.hasOwnProperty('identity')) {
                obj['identity'] = ApiClient.convertToType(data['identity'], Object);
            }
            if (data.hasOwnProperty('properties')) {
                obj['properties'] = ManagedInstanceProperties.constructFromObject(data['properties']);
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], Object);
            }
            if (data.hasOwnProperty('location')) {
                obj['location'] = ApiClient.convertToType(data['location'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], {'String': 'String'});
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ManagedInstance</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ManagedInstance</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ManagedInstance.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `identity`
        if (data['identity']) { // data not null
          Object.validateJSON(data['identity']);
        }
        // validate the optional field `properties`
        if (data['properties']) { // data not null
          ManagedInstanceProperties.validateJSON(data['properties']);
        }
        // validate the optional field `sku`
        if (data['sku']) { // data not null
          Object.validateJSON(data['sku']);
        }
        // ensure the json data is a string
        if (data['location'] && !(typeof data['location'] === 'string' || data['location'] instanceof String)) {
            throw new Error("Expected the field `location` to be a primitive type in the JSON string but got " + data['location']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ManagedInstance.RequiredProperties = ["location"];

/**
 * Azure Active Directory identity configuration for a resource.
 * @member {Object} identity
 */
ManagedInstance.prototype['identity'] = undefined;

/**
 * @member {module:model/ManagedInstanceProperties} properties
 */
ManagedInstance.prototype['properties'] = undefined;

/**
 * An ARM Resource SKU.
 * @member {Object} sku
 */
ManagedInstance.prototype['sku'] = undefined;

/**
 * Resource location.
 * @member {String} location
 */
ManagedInstance.prototype['location'] = undefined;

/**
 * Resource tags.
 * @member {Object.<String, String>} tags
 */
ManagedInstance.prototype['tags'] = undefined;

/**
 * Resource ID.
 * @member {String} id
 */
ManagedInstance.prototype['id'] = undefined;

/**
 * Resource name.
 * @member {String} name
 */
ManagedInstance.prototype['name'] = undefined;

/**
 * Resource type.
 * @member {String} type
 */
ManagedInstance.prototype['type'] = undefined;






export default ManagedInstance;

