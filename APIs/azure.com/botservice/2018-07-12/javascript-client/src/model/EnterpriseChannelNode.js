/**
 * Azure Bot Service
 * Azure Bot Service is a platform for creating smart conversational agents.
 *
 * The version of the OpenAPI document: 2018-07-12
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EnterpriseChannelNode model module.
 * @module model/EnterpriseChannelNode
 * @version 2018-07-12
 */
class EnterpriseChannelNode {
    /**
     * Constructs a new <code>EnterpriseChannelNode</code>.
     * The properties specific to an Enterprise Channel Node.
     * @alias module:model/EnterpriseChannelNode
     * @param azureLocation {String} The location of the Enterprise Channel Node.
     * @param azureSku {String} The sku of the Enterprise Channel Node.
     * @param name {String} The name of the Enterprise Channel Node.
     */
    constructor(azureLocation, azureSku, name) { 
        
        EnterpriseChannelNode.initialize(this, azureLocation, azureSku, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, azureLocation, azureSku, name) { 
        obj['azureLocation'] = azureLocation;
        obj['azureSku'] = azureSku;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>EnterpriseChannelNode</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EnterpriseChannelNode} obj Optional instance to populate.
     * @return {module:model/EnterpriseChannelNode} The populated <code>EnterpriseChannelNode</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EnterpriseChannelNode();

            if (data.hasOwnProperty('azureLocation')) {
                obj['azureLocation'] = ApiClient.convertToType(data['azureLocation'], 'String');
            }
            if (data.hasOwnProperty('azureSku')) {
                obj['azureSku'] = ApiClient.convertToType(data['azureSku'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EnterpriseChannelNode</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EnterpriseChannelNode</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EnterpriseChannelNode.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['azureLocation'] && !(typeof data['azureLocation'] === 'string' || data['azureLocation'] instanceof String)) {
            throw new Error("Expected the field `azureLocation` to be a primitive type in the JSON string but got " + data['azureLocation']);
        }
        // ensure the json data is a string
        if (data['azureSku'] && !(typeof data['azureSku'] === 'string' || data['azureSku'] instanceof String)) {
            throw new Error("Expected the field `azureSku` to be a primitive type in the JSON string but got " + data['azureSku']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }

        return true;
    }


}

EnterpriseChannelNode.RequiredProperties = ["azureLocation", "azureSku", "name"];

/**
 * The location of the Enterprise Channel Node.
 * @member {String} azureLocation
 */
EnterpriseChannelNode.prototype['azureLocation'] = undefined;

/**
 * The sku of the Enterprise Channel Node.
 * @member {String} azureSku
 */
EnterpriseChannelNode.prototype['azureSku'] = undefined;

/**
 * Id of Enterprise Channel Node. This is generated by the Bot Framework.
 * @member {String} id
 */
EnterpriseChannelNode.prototype['id'] = undefined;

/**
 * The name of the Enterprise Channel Node.
 * @member {String} name
 */
EnterpriseChannelNode.prototype['name'] = undefined;

/**
 * The current state of the Enterprise Channel Node.
 * @member {module:model/EnterpriseChannelNode.StateEnum} state
 */
EnterpriseChannelNode.prototype['state'] = undefined;





/**
 * Allowed values for the <code>state</code> property.
 * @enum {String}
 * @readonly
 */
EnterpriseChannelNode['StateEnum'] = {

    /**
     * value: "Creating"
     * @const
     */
    "Creating": "Creating",

    /**
     * value: "CreateFailed"
     * @const
     */
    "CreateFailed": "CreateFailed",

    /**
     * value: "Started"
     * @const
     */
    "Started": "Started",

    /**
     * value: "Starting"
     * @const
     */
    "Starting": "Starting",

    /**
     * value: "StartFailed"
     * @const
     */
    "StartFailed": "StartFailed",

    /**
     * value: "Stopped"
     * @const
     */
    "Stopped": "Stopped",

    /**
     * value: "Stopping"
     * @const
     */
    "Stopping": "Stopping",

    /**
     * value: "StopFailed"
     * @const
     */
    "StopFailed": "StopFailed",

    /**
     * value: "Deleting"
     * @const
     */
    "Deleting": "Deleting",

    /**
     * value: "DeleteFailed"
     * @const
     */
    "DeleteFailed": "DeleteFailed"
};



export default EnterpriseChannelNode;

