/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2018-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import InstancePoolVcoresCapability from './InstancePoolVcoresCapability';
import LicenseTypeCapability from './LicenseTypeCapability';

/**
 * The InstancePoolFamilyCapability model module.
 * @module model/InstancePoolFamilyCapability
 * @version 2018-06-01-preview
 */
class InstancePoolFamilyCapability {
    /**
     * Constructs a new <code>InstancePoolFamilyCapability</code>.
     * The instance pool family capability.
     * @alias module:model/InstancePoolFamilyCapability
     */
    constructor() { 
        
        InstancePoolFamilyCapability.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InstancePoolFamilyCapability</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InstancePoolFamilyCapability} obj Optional instance to populate.
     * @return {module:model/InstancePoolFamilyCapability} The populated <code>InstancePoolFamilyCapability</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InstancePoolFamilyCapability();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('supportedLicenseTypes')) {
                obj['supportedLicenseTypes'] = ApiClient.convertToType(data['supportedLicenseTypes'], [LicenseTypeCapability]);
            }
            if (data.hasOwnProperty('supportedVcoresValues')) {
                obj['supportedVcoresValues'] = ApiClient.convertToType(data['supportedVcoresValues'], [InstancePoolVcoresCapability]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InstancePoolFamilyCapability</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InstancePoolFamilyCapability</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        if (data['supportedLicenseTypes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['supportedLicenseTypes'])) {
                throw new Error("Expected the field `supportedLicenseTypes` to be an array in the JSON data but got " + data['supportedLicenseTypes']);
            }
            // validate the optional field `supportedLicenseTypes` (array)
            for (const item of data['supportedLicenseTypes']) {
                LicenseTypeCapability.validateJSON(item);
            };
        }
        if (data['supportedVcoresValues']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['supportedVcoresValues'])) {
                throw new Error("Expected the field `supportedVcoresValues` to be an array in the JSON data but got " + data['supportedVcoresValues']);
            }
            // validate the optional field `supportedVcoresValues` (array)
            for (const item of data['supportedVcoresValues']) {
                InstancePoolVcoresCapability.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Family name.
 * @member {String} name
 */
InstancePoolFamilyCapability.prototype['name'] = undefined;

/**
 * The reason for the capability not being available.
 * @member {String} reason
 */
InstancePoolFamilyCapability.prototype['reason'] = undefined;

/**
 * The status of the capability.
 * @member {module:model/InstancePoolFamilyCapability.StatusEnum} status
 */
InstancePoolFamilyCapability.prototype['status'] = undefined;

/**
 * List of supported license types.
 * @member {Array.<module:model/LicenseTypeCapability>} supportedLicenseTypes
 */
InstancePoolFamilyCapability.prototype['supportedLicenseTypes'] = undefined;

/**
 * List of supported virtual cores values.
 * @member {Array.<module:model/InstancePoolVcoresCapability>} supportedVcoresValues
 */
InstancePoolFamilyCapability.prototype['supportedVcoresValues'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
InstancePoolFamilyCapability['StatusEnum'] = {

    /**
     * value: "Visible"
     * @const
     */
    "Visible": "Visible",

    /**
     * value: "Available"
     * @const
     */
    "Available": "Available",

    /**
     * value: "Default"
     * @const
     */
    "Default": "Default",

    /**
     * value: "Disabled"
     * @const
     */
    "Disabled": "Disabled"
};



export default InstancePoolFamilyCapability;

