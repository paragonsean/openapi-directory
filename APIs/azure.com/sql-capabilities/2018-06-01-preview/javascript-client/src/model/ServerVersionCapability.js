/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2018-06-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EditionCapability from './EditionCapability';
import ElasticPoolEditionCapability from './ElasticPoolEditionCapability';

/**
 * The ServerVersionCapability model module.
 * @module model/ServerVersionCapability
 * @version 2018-06-01-preview
 */
class ServerVersionCapability {
    /**
     * Constructs a new <code>ServerVersionCapability</code>.
     * The server capability
     * @alias module:model/ServerVersionCapability
     */
    constructor() { 
        
        ServerVersionCapability.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ServerVersionCapability</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServerVersionCapability} obj Optional instance to populate.
     * @return {module:model/ServerVersionCapability} The populated <code>ServerVersionCapability</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServerVersionCapability();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('supportedEditions')) {
                obj['supportedEditions'] = ApiClient.convertToType(data['supportedEditions'], [EditionCapability]);
            }
            if (data.hasOwnProperty('supportedElasticPoolEditions')) {
                obj['supportedElasticPoolEditions'] = ApiClient.convertToType(data['supportedElasticPoolEditions'], [ElasticPoolEditionCapability]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServerVersionCapability</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServerVersionCapability</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        if (data['supportedEditions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['supportedEditions'])) {
                throw new Error("Expected the field `supportedEditions` to be an array in the JSON data but got " + data['supportedEditions']);
            }
            // validate the optional field `supportedEditions` (array)
            for (const item of data['supportedEditions']) {
                EditionCapability.validateJSON(item);
            };
        }
        if (data['supportedElasticPoolEditions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['supportedElasticPoolEditions'])) {
                throw new Error("Expected the field `supportedElasticPoolEditions` to be an array in the JSON data but got " + data['supportedElasticPoolEditions']);
            }
            // validate the optional field `supportedElasticPoolEditions` (array)
            for (const item of data['supportedElasticPoolEditions']) {
                ElasticPoolEditionCapability.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The server version name.
 * @member {String} name
 */
ServerVersionCapability.prototype['name'] = undefined;

/**
 * The reason for the capability not being available.
 * @member {String} reason
 */
ServerVersionCapability.prototype['reason'] = undefined;

/**
 * The status of the capability.
 * @member {module:model/ServerVersionCapability.StatusEnum} status
 */
ServerVersionCapability.prototype['status'] = undefined;

/**
 * The list of supported database editions.
 * @member {Array.<module:model/EditionCapability>} supportedEditions
 */
ServerVersionCapability.prototype['supportedEditions'] = undefined;

/**
 * The list of supported elastic pool editions.
 * @member {Array.<module:model/ElasticPoolEditionCapability>} supportedElasticPoolEditions
 */
ServerVersionCapability.prototype['supportedElasticPoolEditions'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ServerVersionCapability['StatusEnum'] = {

    /**
     * value: "Visible"
     * @const
     */
    "Visible": "Visible",

    /**
     * value: "Available"
     * @const
     */
    "Available": "Available",

    /**
     * value: "Default"
     * @const
     */
    "Default": "Default",

    /**
     * value: "Disabled"
     * @const
     */
    "Disabled": "Disabled"
};



export default ServerVersionCapability;

