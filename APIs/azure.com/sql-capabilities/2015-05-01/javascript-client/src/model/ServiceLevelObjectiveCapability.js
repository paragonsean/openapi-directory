/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2015-05-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MaxSizeCapability from './MaxSizeCapability';
import PerformanceLevelCapability from './PerformanceLevelCapability';

/**
 * The ServiceLevelObjectiveCapability model module.
 * @module model/ServiceLevelObjectiveCapability
 * @version 2015-05-01
 */
class ServiceLevelObjectiveCapability {
    /**
     * Constructs a new <code>ServiceLevelObjectiveCapability</code>.
     * The service objectives capability.
     * @alias module:model/ServiceLevelObjectiveCapability
     */
    constructor() { 
        
        ServiceLevelObjectiveCapability.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ServiceLevelObjectiveCapability</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServiceLevelObjectiveCapability} obj Optional instance to populate.
     * @return {module:model/ServiceLevelObjectiveCapability} The populated <code>ServiceLevelObjectiveCapability</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServiceLevelObjectiveCapability();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('includedMaxSize')) {
                obj['includedMaxSize'] = MaxSizeCapability.constructFromObject(data['includedMaxSize']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('performanceLevel')) {
                obj['performanceLevel'] = PerformanceLevelCapability.constructFromObject(data['performanceLevel']);
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('supportedMaxSizes')) {
                obj['supportedMaxSizes'] = ApiClient.convertToType(data['supportedMaxSizes'], [MaxSizeCapability]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServiceLevelObjectiveCapability</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServiceLevelObjectiveCapability</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `includedMaxSize`
        if (data['includedMaxSize']) { // data not null
          MaxSizeCapability.validateJSON(data['includedMaxSize']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `performanceLevel`
        if (data['performanceLevel']) { // data not null
          PerformanceLevelCapability.validateJSON(data['performanceLevel']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        if (data['supportedMaxSizes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['supportedMaxSizes'])) {
                throw new Error("Expected the field `supportedMaxSizes` to be an array in the JSON data but got " + data['supportedMaxSizes']);
            }
            // validate the optional field `supportedMaxSizes` (array)
            for (const item of data['supportedMaxSizes']) {
                MaxSizeCapability.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The unique ID of the service objective.
 * @member {String} id
 */
ServiceLevelObjectiveCapability.prototype['id'] = undefined;

/**
 * @member {module:model/MaxSizeCapability} includedMaxSize
 */
ServiceLevelObjectiveCapability.prototype['includedMaxSize'] = undefined;

/**
 * The service objective name.
 * @member {String} name
 */
ServiceLevelObjectiveCapability.prototype['name'] = undefined;

/**
 * @member {module:model/PerformanceLevelCapability} performanceLevel
 */
ServiceLevelObjectiveCapability.prototype['performanceLevel'] = undefined;

/**
 * The reason for the capability not being available.
 * @member {String} reason
 */
ServiceLevelObjectiveCapability.prototype['reason'] = undefined;

/**
 * The status of the capability.
 * @member {module:model/ServiceLevelObjectiveCapability.StatusEnum} status
 */
ServiceLevelObjectiveCapability.prototype['status'] = undefined;

/**
 * The list of supported maximum database sizes for this service objective.
 * @member {Array.<module:model/MaxSizeCapability>} supportedMaxSizes
 */
ServiceLevelObjectiveCapability.prototype['supportedMaxSizes'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
ServiceLevelObjectiveCapability['StatusEnum'] = {

    /**
     * value: "Visible"
     * @const
     */
    "Visible": "Visible",

    /**
     * value: "Available"
     * @const
     */
    "Available": "Available",

    /**
     * value: "Default"
     * @const
     */
    "Default": "Default",

    /**
     * value: "Disabled"
     * @const
     */
    "Disabled": "Disabled"
};



export default ServiceLevelObjectiveCapability;

