/**
 * SqlManagementClient
 * The Azure SQL Database management API provides a RESTful set of web APIs that interact with Azure SQL Database services to manage your databases. The API enables users to create, retrieve, update, and delete databases, servers, and other entities.
 *
 * The version of the OpenAPI document: 2017-10-01-preview
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ElasticPoolPerformanceLevelCapabilitySku model module.
 * @module model/ElasticPoolPerformanceLevelCapabilitySku
 * @version 2017-10-01-preview
 */
class ElasticPoolPerformanceLevelCapabilitySku {
    /**
     * Constructs a new <code>ElasticPoolPerformanceLevelCapabilitySku</code>.
     * An ARM Resource SKU.
     * @alias module:model/ElasticPoolPerformanceLevelCapabilitySku
     * @param name {String} The name of the SKU, typically, a letter + Number code, e.g. P3.
     */
    constructor(name) { 
        
        ElasticPoolPerformanceLevelCapabilitySku.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>ElasticPoolPerformanceLevelCapabilitySku</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ElasticPoolPerformanceLevelCapabilitySku} obj Optional instance to populate.
     * @return {module:model/ElasticPoolPerformanceLevelCapabilitySku} The populated <code>ElasticPoolPerformanceLevelCapabilitySku</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ElasticPoolPerformanceLevelCapabilitySku();

            if (data.hasOwnProperty('capacity')) {
                obj['capacity'] = ApiClient.convertToType(data['capacity'], 'Number');
            }
            if (data.hasOwnProperty('family')) {
                obj['family'] = ApiClient.convertToType(data['family'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('size')) {
                obj['size'] = ApiClient.convertToType(data['size'], 'String');
            }
            if (data.hasOwnProperty('tier')) {
                obj['tier'] = ApiClient.convertToType(data['tier'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ElasticPoolPerformanceLevelCapabilitySku</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ElasticPoolPerformanceLevelCapabilitySku</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ElasticPoolPerformanceLevelCapabilitySku.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['family'] && !(typeof data['family'] === 'string' || data['family'] instanceof String)) {
            throw new Error("Expected the field `family` to be a primitive type in the JSON string but got " + data['family']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['size'] && !(typeof data['size'] === 'string' || data['size'] instanceof String)) {
            throw new Error("Expected the field `size` to be a primitive type in the JSON string but got " + data['size']);
        }
        // ensure the json data is a string
        if (data['tier'] && !(typeof data['tier'] === 'string' || data['tier'] instanceof String)) {
            throw new Error("Expected the field `tier` to be a primitive type in the JSON string but got " + data['tier']);
        }

        return true;
    }


}

ElasticPoolPerformanceLevelCapabilitySku.RequiredProperties = ["name"];

/**
 * Capacity of the particular SKU.
 * @member {Number} capacity
 */
ElasticPoolPerformanceLevelCapabilitySku.prototype['capacity'] = undefined;

/**
 * If the service has different generations of hardware, for the same SKU, then that can be captured here.
 * @member {String} family
 */
ElasticPoolPerformanceLevelCapabilitySku.prototype['family'] = undefined;

/**
 * The name of the SKU, typically, a letter + Number code, e.g. P3.
 * @member {String} name
 */
ElasticPoolPerformanceLevelCapabilitySku.prototype['name'] = undefined;

/**
 * Size of the particular SKU
 * @member {String} size
 */
ElasticPoolPerformanceLevelCapabilitySku.prototype['size'] = undefined;

/**
 * The tier or edition of the particular SKU, e.g. Basic, Premium.
 * @member {String} tier
 */
ElasticPoolPerformanceLevelCapabilitySku.prototype['tier'] = undefined;






export default ElasticPoolPerformanceLevelCapabilitySku;

