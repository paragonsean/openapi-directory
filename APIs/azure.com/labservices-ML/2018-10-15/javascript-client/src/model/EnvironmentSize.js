/**
 * ManagedLabsClient
 * The Managed Labs Client.
 *
 * The version of the OpenAPI document: 2018-10-15
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SizeInfo from './SizeInfo';

/**
 * The EnvironmentSize model module.
 * @module model/EnvironmentSize
 * @version 2018-10-15
 */
class EnvironmentSize {
    /**
     * Constructs a new <code>EnvironmentSize</code>.
     * Represents a size category supported by this Lab Account (small, medium or large)
     * @alias module:model/EnvironmentSize
     */
    constructor() { 
        
        EnvironmentSize.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EnvironmentSize</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EnvironmentSize} obj Optional instance to populate.
     * @return {module:model/EnvironmentSize} The populated <code>EnvironmentSize</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EnvironmentSize();

            if (data.hasOwnProperty('maxPrice')) {
                obj['maxPrice'] = ApiClient.convertToType(data['maxPrice'], 'Number');
            }
            if (data.hasOwnProperty('minMemory')) {
                obj['minMemory'] = ApiClient.convertToType(data['minMemory'], 'Number');
            }
            if (data.hasOwnProperty('minNumberOfCores')) {
                obj['minNumberOfCores'] = ApiClient.convertToType(data['minNumberOfCores'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('vmSizes')) {
                obj['vmSizes'] = ApiClient.convertToType(data['vmSizes'], [SizeInfo]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EnvironmentSize</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EnvironmentSize</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['vmSizes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['vmSizes'])) {
                throw new Error("Expected the field `vmSizes` to be an array in the JSON data but got " + data['vmSizes']);
            }
            // validate the optional field `vmSizes` (array)
            for (const item of data['vmSizes']) {
                SizeInfo.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The pay-as-you-go dollar price per hour this size will cost. It does not include discounts and may not reflect the actual price the size will cost. This is the maximum price of all prices within this tier.
 * @member {Number} maxPrice
 */
EnvironmentSize.prototype['maxPrice'] = undefined;

/**
 * The amount of memory available (in GB). This is the minimum amount of memory within this tier.
 * @member {Number} minMemory
 */
EnvironmentSize.prototype['minMemory'] = undefined;

/**
 * The number of cores a VM of this size has. This is the minimum number of cores within this tier.
 * @member {Number} minNumberOfCores
 */
EnvironmentSize.prototype['minNumberOfCores'] = undefined;

/**
 * The size category
 * @member {module:model/EnvironmentSize.NameEnum} name
 */
EnvironmentSize.prototype['name'] = undefined;

/**
 * Represents a set of compute sizes that can serve this given size type
 * @member {Array.<module:model/SizeInfo>} vmSizes
 */
EnvironmentSize.prototype['vmSizes'] = undefined;





/**
 * Allowed values for the <code>name</code> property.
 * @enum {String}
 * @readonly
 */
EnvironmentSize['NameEnum'] = {

    /**
     * value: "Basic"
     * @const
     */
    "Basic": "Basic",

    /**
     * value: "Standard"
     * @const
     */
    "Standard": "Standard",

    /**
     * value: "Performance"
     * @const
     */
    "Performance": "Performance"
};



export default EnvironmentSize;

