# C++ Qt API client

# 

ApiManagementClient

- API version: 2019-12-01-preview
- Generator version: 7.9.0

Use these REST APIs for performing operations on Tag entity in your Azure API Management deployment. Tags can be assigned to APIs, Operations and Products.


*Automatically generated by the [OpenAPI Generator](https://openapi-generator.tech)*


## Requirements

Building the API client library requires:

1. CMake 3.2+
2. Qt
3. C++ Compiler

## Getting Started

example.h:
```c++

#include <iostream>
#include "../client/OAITagApi.h"

using namespace test_namespace;

class Example : public QObject {
    Q_OBJECT
    QString create();
    QString create();
    QString create();
    QString create();
    QString create();
    OAITag_CreateOrUpdate_request create();
    QString create();
public Q_SLOTS:
   void exampleFunction1();
};

```

example.cpp:
```c++

#include "../client/OAITagApi.h"
#include "example.h"
#include <QTimer>
#include <QEventLoop>

QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
QString Example::create(){
    QString obj;
OAITag_CreateOrUpdate_request Example::create(){
    OAITag_CreateOrUpdate_request obj;
QString Example::create(){
    QString obj;
 return obj;
}

void Example::exampleFunction1(){
     OAITagApi apiInstance;
     
      //OAuth Authentication supported right now

      QEventLoop loop;
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString resource_group_name = create(); // QString | The name of the resource group.

      QEventLoop loop;
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString service_name = create(); // QString | The name of the API Management service.

      QEventLoop loop;
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString tag_id = create(); // QString | Tag identifier. Must be unique in the current API Management service instance.

      QEventLoop loop;
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString api_version = create(); // QString | Version of the API to be used with the client request.

      QEventLoop loop;
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString subscription_id = create(); // QString | Subscription credentials which uniquely identify Microsoft Azure subscription. The subscription ID forms part of the URI for every service call.

      QEventLoop loop;
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      OAITag_CreateOrUpdate_request parameters = create(); // OAITag_CreateOrUpdate_request | Create parameters.

      QEventLoop loop;
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignal, [&]() {
          loop.quit();
      });
      connect(&apiInstance, &OAITagApi::tagCreateOrUpdateSignalE, [&](QNetworkReply::NetworkError, QString error_str) {
          qDebug() << "Error happened while issuing request : " << error_str;
          loop.quit();
      });

      QString if_match = create(); // QString | ETag of the Entity. Not required when creating an entity, but required when updating an entity.
      apiInstance.tagCreateOrUpdate(resource_group_nameservice_nametag_idapi_versionsubscription_idparametersif_match);
      QTimer::singleShot(5000, &loop, &QEventLoop::quit);
      loop.exec();
  }

```

## Documentation for Servers

Parameterized Servers are supported. Define a server in the API for each endpoint with arbitrary numbers of variables:

```yaml
servers:
- url: http://{server}:{port}/{basePath}
  description: Description of the Server
  variables:
    server:
        enum:
          - 'petstore'
          - 'qa-petstore'
          - 'dev-petstore'
        default: 'petstore'
    port:
      enum:
        - '3000'
        - '1000'
      default: '3000'
    basePath:
      default: v1
```
To change the default variable, use this function in each Api:
```c++
int setDefaultServerValue(int serverIndex,const QString &operation, const QString &variable,const QString &val);
```
The parameter "serverIndex" will choose a server from the server list for each endpoint. There is always at least one server with index 0. The Parameter "operation" should be the desired endpoint operationid.
Variable is the name of the variable you wish to change and the value is the new default Value.
The function will return -1 when the variable does not exists, -2 if value is not defined in the variable enum and -3 if the operation is not found.

If your endpoint has multiple server objects in the servers array, you can set the server that will be used with this function:
```c++
void setServerIndex(const QString &operation, int serverIndex);
```
Parameter "operation" should be your operationid. "serverIndex" is the index you want to set as your default server. The function will check if there is a server with your index.
Here is an example of multiple servers in the servers array. The first server will have index 0 and the second will have index 1.
```yaml
servers:
- url: http://{server}:8080/
  description: Description of the Server
  variables:
    server:
        enum:
          - 'petstore'
          - 'qa-petstore'
          - 'dev-petstore'
        default: 'petstore'
- url: https://localhost:8080/v1
```

## Documentation for Authorization

Authentication schemes defined for the API:
### azure_auth


- **Type**: OAuth
- **Flow**: implicit
- **Authorization URL**: https://login.microsoftonline.com/common/oauth2/authorize
- **Scopes**: 
  - user_impersonation: impersonate your user account


## Author




## License

 for more information visit []()