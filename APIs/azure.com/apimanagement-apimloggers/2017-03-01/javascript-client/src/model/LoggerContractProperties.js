/**
 * ApiManagementClient
 * Use these REST APIs for performing operations on logger entity Azure API Management deployment.The Logger entity in API Management represents an event sink that you can use to log API Management events. Currently the Logger entity supports logging API Management events to Azure EventHub.
 *
 * The version of the OpenAPI document: 2017-03-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LoggerSamplingContract from './LoggerSamplingContract';

/**
 * The LoggerContractProperties model module.
 * @module model/LoggerContractProperties
 * @version 2017-03-01
 */
class LoggerContractProperties {
    /**
     * Constructs a new <code>LoggerContractProperties</code>.
     * The Logger entity in API Management represents an event sink that you can use to log API Management events. Currently the Logger entity supports logging API Management events to Azure Event Hubs.
     * @alias module:model/LoggerContractProperties
     * @param credentials {Object.<String, String>} The name and SendRule connection string of the event hub for azureEventHub logger. Instrumentation key for applicationInsights logger.
     * @param loggerType {module:model/LoggerContractProperties.LoggerTypeEnum} Logger type.
     */
    constructor(credentials, loggerType) { 
        
        LoggerContractProperties.initialize(this, credentials, loggerType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, credentials, loggerType) { 
        obj['credentials'] = credentials;
        obj['loggerType'] = loggerType;
    }

    /**
     * Constructs a <code>LoggerContractProperties</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LoggerContractProperties} obj Optional instance to populate.
     * @return {module:model/LoggerContractProperties} The populated <code>LoggerContractProperties</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LoggerContractProperties();

            if (data.hasOwnProperty('credentials')) {
                obj['credentials'] = ApiClient.convertToType(data['credentials'], {'String': 'String'});
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('isBuffered')) {
                obj['isBuffered'] = ApiClient.convertToType(data['isBuffered'], 'Boolean');
            }
            if (data.hasOwnProperty('loggerType')) {
                obj['loggerType'] = ApiClient.convertToType(data['loggerType'], 'String');
            }
            if (data.hasOwnProperty('sampling')) {
                obj['sampling'] = LoggerSamplingContract.constructFromObject(data['sampling']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LoggerContractProperties</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LoggerContractProperties</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LoggerContractProperties.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['loggerType'] && !(typeof data['loggerType'] === 'string' || data['loggerType'] instanceof String)) {
            throw new Error("Expected the field `loggerType` to be a primitive type in the JSON string but got " + data['loggerType']);
        }
        // validate the optional field `sampling`
        if (data['sampling']) { // data not null
          LoggerSamplingContract.validateJSON(data['sampling']);
        }

        return true;
    }


}

LoggerContractProperties.RequiredProperties = ["credentials", "loggerType"];

/**
 * The name and SendRule connection string of the event hub for azureEventHub logger. Instrumentation key for applicationInsights logger.
 * @member {Object.<String, String>} credentials
 */
LoggerContractProperties.prototype['credentials'] = undefined;

/**
 * Logger description.
 * @member {String} description
 */
LoggerContractProperties.prototype['description'] = undefined;

/**
 * Whether records are buffered in the logger before publishing. Default is assumed to be true.
 * @member {Boolean} isBuffered
 */
LoggerContractProperties.prototype['isBuffered'] = undefined;

/**
 * Logger type.
 * @member {module:model/LoggerContractProperties.LoggerTypeEnum} loggerType
 */
LoggerContractProperties.prototype['loggerType'] = undefined;

/**
 * @member {module:model/LoggerSamplingContract} sampling
 */
LoggerContractProperties.prototype['sampling'] = undefined;





/**
 * Allowed values for the <code>loggerType</code> property.
 * @enum {String}
 * @readonly
 */
LoggerContractProperties['LoggerTypeEnum'] = {

    /**
     * value: "azureEventHub"
     * @const
     */
    "azureEventHub": "azureEventHub",

    /**
     * value: "applicationInsights"
     * @const
     */
    "applicationInsights": "applicationInsights"
};



export default LoggerContractProperties;

