/**
 * doqs.dev | PDF filling API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BoundingBox from './BoundingBox';
import Eval from './Eval';

/**
 * The CheckBoxField model module.
 * @module model/CheckBoxField
 * @version 1.0
 */
class CheckBoxField {
    /**
     * Constructs a new <code>CheckBoxField</code>.
     * @alias module:model/CheckBoxField
     * @param bbox {module:model/BoundingBox} 
     * @param name {String} 
     * @param page {Number} 
     * @param type {module:model/CheckBoxField.TypeEnum} 
     */
    constructor(bbox, name, page, type) { 
        
        CheckBoxField.initialize(this, bbox, name, page, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bbox, name, page, type) { 
        obj['bbox'] = bbox;
        obj['name'] = name;
        obj['page'] = page;
        obj['required'] = true;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>CheckBoxField</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CheckBoxField} obj Optional instance to populate.
     * @return {module:model/CheckBoxField} The populated <code>CheckBoxField</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CheckBoxField();

            if (data.hasOwnProperty('bbox')) {
                obj['bbox'] = BoundingBox.constructFromObject(data['bbox']);
            }
            if (data.hasOwnProperty('eval')) {
                obj['eval'] = Eval.constructFromObject(data['eval']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('page')) {
                obj['page'] = ApiClient.convertToType(data['page'], 'Number');
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CheckBoxField</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CheckBoxField</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CheckBoxField.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `bbox`
        if (data['bbox']) { // data not null
          BoundingBox.validateJSON(data['bbox']);
        }
        // validate the optional field `eval`
        if (data['eval']) { // data not null
          Eval.validateJSON(data['eval']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

CheckBoxField.RequiredProperties = ["bbox", "name", "page", "type"];

/**
 * @member {module:model/BoundingBox} bbox
 */
CheckBoxField.prototype['bbox'] = undefined;

/**
 * @member {module:model/Eval} eval
 */
CheckBoxField.prototype['eval'] = undefined;

/**
 * @member {String} name
 */
CheckBoxField.prototype['name'] = undefined;

/**
 * @member {Number} page
 */
CheckBoxField.prototype['page'] = undefined;

/**
 * @member {Boolean} required
 * @default true
 */
CheckBoxField.prototype['required'] = true;

/**
 * @member {module:model/CheckBoxField.TypeEnum} type
 */
CheckBoxField.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
CheckBoxField['TypeEnum'] = {

    /**
     * value: "check"
     * @const
     */
    "check": "check"
};



export default CheckBoxField;

