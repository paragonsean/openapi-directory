/**
 * doqs.dev | PDF filling API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BoundingBox from './BoundingBox';
import ImageAnchor from './ImageAnchor';

/**
 * The ImageField model module.
 * @module model/ImageField
 * @version 1.0
 */
class ImageField {
    /**
     * Constructs a new <code>ImageField</code>.
     * @alias module:model/ImageField
     * @param bbox {module:model/BoundingBox} 
     * @param name {String} 
     * @param page {Number} 
     */
    constructor(bbox, name, page) { 
        
        ImageField.initialize(this, bbox, name, page);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bbox, name, page) { 
        obj['bbox'] = bbox;
        obj['name'] = name;
        obj['page'] = page;
        obj['required'] = true;
        obj['type'] = 'image';
    }

    /**
     * Constructs a <code>ImageField</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImageField} obj Optional instance to populate.
     * @return {module:model/ImageField} The populated <code>ImageField</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImageField();

            if (data.hasOwnProperty('anchor')) {
                obj['anchor'] = ApiClient.convertToType(data['anchor'], ImageAnchor);
            }
            if (data.hasOwnProperty('bbox')) {
                obj['bbox'] = BoundingBox.constructFromObject(data['bbox']);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('page')) {
                obj['page'] = ApiClient.convertToType(data['page'], 'Number');
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImageField</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImageField</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ImageField.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `bbox`
        if (data['bbox']) { // data not null
          BoundingBox.validateJSON(data['bbox']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ImageField.RequiredProperties = ["bbox", "name", "page"];

/**
 * @member {module:model/ImageAnchor} anchor
 */
ImageField.prototype['anchor'] = undefined;

/**
 * @member {module:model/BoundingBox} bbox
 */
ImageField.prototype['bbox'] = undefined;

/**
 * @member {String} name
 */
ImageField.prototype['name'] = undefined;

/**
 * @member {Number} page
 */
ImageField.prototype['page'] = undefined;

/**
 * @member {Boolean} required
 * @default true
 */
ImageField.prototype['required'] = true;

/**
 * @member {module:model/ImageField.TypeEnum} type
 * @default 'image'
 */
ImageField.prototype['type'] = 'image';





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ImageField['TypeEnum'] = {

    /**
     * value: "image"
     * @const
     */
    "image": "image"
};



export default ImageField;

