/**
 * Corrently.io
 * *Corrently - from italian corrente, which is energy* # Introduction The Corrently ecosystem gets maintained by [STROMDAO GmbH](https://www.stromdao.de/) to support green energy services for prosumers, grid operators, regulators, integrators or any other party with an emerging need of consensus driven management. As the [energy product Corrently](https://www.corrently.de/) got first launched in Germany parts of this documentation provide simple translations for better understanding. [Released SKDs for Download](https://github.com/energychain/corrently-api/releases) 
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: dev@stromdao.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PrepareTransactionRequest model module.
 * @module model/PrepareTransactionRequest
 * @version 2.0.0
 */
class PrepareTransactionRequest {
    /**
     * Constructs a new <code>PrepareTransactionRequest</code>.
     * @alias module:model/PrepareTransactionRequest
     */
    constructor() { 
        
        PrepareTransactionRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PrepareTransactionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PrepareTransactionRequest} obj Optional instance to populate.
     * @return {module:model/PrepareTransactionRequest} The populated <code>PrepareTransactionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PrepareTransactionRequest();

            if (data.hasOwnProperty('account')) {
                obj['account'] = ApiClient.convertToType(data['account'], 'String');
            }
            if (data.hasOwnProperty('signature')) {
                obj['signature'] = ApiClient.convertToType(data['signature'], 'String');
            }
            if (data.hasOwnProperty('to')) {
                obj['to'] = ApiClient.convertToType(data['to'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'Number');
            }
            if (data.hasOwnProperty('variation')) {
                obj['variation'] = ApiClient.convertToType(data['variation'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PrepareTransactionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PrepareTransactionRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['account'] && !(typeof data['account'] === 'string' || data['account'] instanceof String)) {
            throw new Error("Expected the field `account` to be a primitive type in the JSON string but got " + data['account']);
        }
        // ensure the json data is a string
        if (data['signature'] && !(typeof data['signature'] === 'string' || data['signature'] instanceof String)) {
            throw new Error("Expected the field `signature` to be a primitive type in the JSON string but got " + data['signature']);
        }
        // ensure the json data is a string
        if (data['to'] && !(typeof data['to'] === 'string' || data['to'] instanceof String)) {
            throw new Error("Expected the field `to` to be a primitive type in the JSON string but got " + data['to']);
        }
        // ensure the json data is a string
        if (data['variation'] && !(typeof data['variation'] === 'string' || data['variation'] instanceof String)) {
            throw new Error("Expected the field `variation` to be a primitive type in the JSON string but got " + data['variation']);
        }

        return true;
    }


}



/**
 * Stromkonto account address of sender
 * @member {String} account
 */
PrepareTransactionRequest.prototype['account'] = undefined;

/**
 * Signature per Stromkonto setting (might be simple email confirmation link)
 * @member {String} signature
 */
PrepareTransactionRequest.prototype['signature'] = undefined;

/**
 * Stromkonto account address of reciever
 * @member {String} to
 */
PrepareTransactionRequest.prototype['to'] = undefined;

/**
 * Amount to transfer (in Watthours for electricity, or pcs for trees)
 * @member {Number} value
 */
PrepareTransactionRequest.prototype['value'] = undefined;

/**
 * @member {module:model/PrepareTransactionRequest.VariationEnum} variation
 */
PrepareTransactionRequest.prototype['variation'] = undefined;





/**
 * Allowed values for the <code>variation</code> property.
 * @enum {String}
 * @readonly
 */
PrepareTransactionRequest['VariationEnum'] = {

    /**
     * value: "gsb"
     * @const
     */
    "gsb": "gsb",

    /**
     * value: "erzeugung"
     * @const
     */
    "erzeugung": "erzeugung",

    /**
     * value: "eigenstrom"
     * @const
     */
    "eigenstrom": "eigenstrom",

    /**
     * value: "co2"
     * @const
     */
    "co2": "co2",

    /**
     * value: "baeume"
     * @const
     */
    "baeume": "baeume"
};



export default PrepareTransactionRequest;

