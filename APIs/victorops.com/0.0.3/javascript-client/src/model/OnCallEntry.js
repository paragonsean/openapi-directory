/**
 * VictorOps
 * This API allows you to interact with the VictorOps platform in various ways. Your account may be limited to a total number of API calls per month. Also, some of these API calls have rate limits.  NOTE: In this documentation when creating a sample curl request (clicking the TRY IT OUT! button), in some API viewing interfaces, the '@' in an email address may be encoded. Please note that the REST endpoints will not process the encoded version. Make sure that the encoded character '%40' is changed to its unencoded form before submitting the curl request. 
 *
 * The version of the OpenAPI document: 0.0.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OnCallRoll from './OnCallRoll';
import OnCallType from './OnCallType';
import User from './User';

/**
 * The OnCallEntry model module.
 * @module model/OnCallEntry
 * @version 0.0.3
 */
class OnCallEntry {
    /**
     * Constructs a new <code>OnCallEntry</code>.
     * @alias module:model/OnCallEntry
     * @param onCallType {module:model/OnCallType} 
     * @param rolls {Array.<module:model/OnCallRoll>} 
     */
    constructor(onCallType, rolls) { 
        
        OnCallEntry.initialize(this, onCallType, rolls);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, onCallType, rolls) { 
        obj['onCallType'] = onCallType;
        obj['rolls'] = rolls;
    }

    /**
     * Constructs a <code>OnCallEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OnCallEntry} obj Optional instance to populate.
     * @return {module:model/OnCallEntry} The populated <code>OnCallEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OnCallEntry();

            if (data.hasOwnProperty('onCallType')) {
                obj['onCallType'] = OnCallType.constructFromObject(data['onCallType']);
            }
            if (data.hasOwnProperty('onCallUser')) {
                obj['onCallUser'] = User.constructFromObject(data['onCallUser']);
            }
            if (data.hasOwnProperty('overrideOnCallUser')) {
                obj['overrideOnCallUser'] = User.constructFromObject(data['overrideOnCallUser']);
            }
            if (data.hasOwnProperty('rolls')) {
                obj['rolls'] = ApiClient.convertToType(data['rolls'], [OnCallRoll]);
            }
            if (data.hasOwnProperty('rotationName')) {
                obj['rotationName'] = ApiClient.convertToType(data['rotationName'], 'String');
            }
            if (data.hasOwnProperty('shiftName')) {
                obj['shiftName'] = ApiClient.convertToType(data['shiftName'], 'String');
            }
            if (data.hasOwnProperty('shiftRoll')) {
                obj['shiftRoll'] = ApiClient.convertToType(data['shiftRoll'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OnCallEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OnCallEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OnCallEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `onCallUser`
        if (data['onCallUser']) { // data not null
          User.validateJSON(data['onCallUser']);
        }
        // validate the optional field `overrideOnCallUser`
        if (data['overrideOnCallUser']) { // data not null
          User.validateJSON(data['overrideOnCallUser']);
        }
        if (data['rolls']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rolls'])) {
                throw new Error("Expected the field `rolls` to be an array in the JSON data but got " + data['rolls']);
            }
            // validate the optional field `rolls` (array)
            for (const item of data['rolls']) {
                OnCallRoll.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['rotationName'] && !(typeof data['rotationName'] === 'string' || data['rotationName'] instanceof String)) {
            throw new Error("Expected the field `rotationName` to be a primitive type in the JSON string but got " + data['rotationName']);
        }
        // ensure the json data is a string
        if (data['shiftName'] && !(typeof data['shiftName'] === 'string' || data['shiftName'] instanceof String)) {
            throw new Error("Expected the field `shiftName` to be a primitive type in the JSON string but got " + data['shiftName']);
        }
        // ensure the json data is a string
        if (data['shiftRoll'] && !(typeof data['shiftRoll'] === 'string' || data['shiftRoll'] instanceof String)) {
            throw new Error("Expected the field `shiftRoll` to be a primitive type in the JSON string but got " + data['shiftRoll']);
        }

        return true;
    }


}

OnCallEntry.RequiredProperties = ["onCallType", "rolls"];

/**
 * @member {module:model/OnCallType} onCallType
 */
OnCallEntry.prototype['onCallType'] = undefined;

/**
 * @member {module:model/User} onCallUser
 */
OnCallEntry.prototype['onCallUser'] = undefined;

/**
 * @member {module:model/User} overrideOnCallUser
 */
OnCallEntry.prototype['overrideOnCallUser'] = undefined;

/**
 * @member {Array.<module:model/OnCallRoll>} rolls
 */
OnCallEntry.prototype['rolls'] = undefined;

/**
 * @member {String} rotationName
 */
OnCallEntry.prototype['rotationName'] = undefined;

/**
 * @member {String} shiftName
 */
OnCallEntry.prototype['shiftName'] = undefined;

/**
 * The shift roll time (ISO 8601)
 * @member {String} shiftRoll
 */
OnCallEntry.prototype['shiftRoll'] = undefined;






export default OnCallEntry;

