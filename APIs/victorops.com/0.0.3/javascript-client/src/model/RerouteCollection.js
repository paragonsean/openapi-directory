/**
 * VictorOps
 * This API allows you to interact with the VictorOps platform in various ways. Your account may be limited to a total number of API calls per month. Also, some of these API calls have rate limits.  NOTE: In this documentation when creating a sample curl request (clicking the TRY IT OUT! button), in some API viewing interfaces, the '@' in an email address may be encoded. Please note that the REST endpoints will not process the encoded version. Make sure that the encoded character '%40' is changed to its unencoded form before submitting the curl request. 
 *
 * The version of the OpenAPI document: 0.0.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Reroute from './Reroute';

/**
 * The RerouteCollection model module.
 * @module model/RerouteCollection
 * @version 0.0.3
 */
class RerouteCollection {
    /**
     * Constructs a new <code>RerouteCollection</code>.
     * @alias module:model/RerouteCollection
     * @param reroutes {Array.<module:model/Reroute>} 
     * @param userName {String} 
     */
    constructor(reroutes, userName) { 
        
        RerouteCollection.initialize(this, reroutes, userName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, reroutes, userName) { 
        obj['reroutes'] = reroutes;
        obj['userName'] = userName;
    }

    /**
     * Constructs a <code>RerouteCollection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RerouteCollection} obj Optional instance to populate.
     * @return {module:model/RerouteCollection} The populated <code>RerouteCollection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RerouteCollection();

            if (data.hasOwnProperty('reroutes')) {
                obj['reroutes'] = ApiClient.convertToType(data['reroutes'], [Reroute]);
            }
            if (data.hasOwnProperty('userName')) {
                obj['userName'] = ApiClient.convertToType(data['userName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RerouteCollection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RerouteCollection</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RerouteCollection.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['reroutes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['reroutes'])) {
                throw new Error("Expected the field `reroutes` to be an array in the JSON data but got " + data['reroutes']);
            }
            // validate the optional field `reroutes` (array)
            for (const item of data['reroutes']) {
                Reroute.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['userName'] && !(typeof data['userName'] === 'string' || data['userName'] instanceof String)) {
            throw new Error("Expected the field `userName` to be a primitive type in the JSON string but got " + data['userName']);
        }

        return true;
    }


}

RerouteCollection.RequiredProperties = ["reroutes", "userName"];

/**
 * @member {Array.<module:model/Reroute>} reroutes
 */
RerouteCollection.prototype['reroutes'] = undefined;

/**
 * @member {String} userName
 */
RerouteCollection.prototype['userName'] = undefined;






export default RerouteCollection;

