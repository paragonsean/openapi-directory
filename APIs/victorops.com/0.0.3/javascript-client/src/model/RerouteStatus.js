/**
 * VictorOps
 * This API allows you to interact with the VictorOps platform in various ways. Your account may be limited to a total number of API calls per month. Also, some of these API calls have rate limits.  NOTE: In this documentation when creating a sample curl request (clicking the TRY IT OUT! button), in some API viewing interfaces, the '@' in an email address may be encoded. Please note that the REST endpoints will not process the encoded version. Make sure that the encoded character '%40' is changed to its unencoded form before submitting the curl request. 
 *
 * The version of the OpenAPI document: 0.0.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RerouteTargetStatus from './RerouteTargetStatus';

/**
 * The RerouteStatus model module.
 * @module model/RerouteStatus
 * @version 0.0.3
 */
class RerouteStatus {
    /**
     * Constructs a new <code>RerouteStatus</code>.
     * @alias module:model/RerouteStatus
     * @param incidentNumber {String} 
     * @param success {Boolean} 
     * @param targetStatus {Array.<module:model/RerouteTargetStatus>} 
     */
    constructor(incidentNumber, success, targetStatus) { 
        
        RerouteStatus.initialize(this, incidentNumber, success, targetStatus);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, incidentNumber, success, targetStatus) { 
        obj['incidentNumber'] = incidentNumber;
        obj['success'] = success;
        obj['targetStatus'] = targetStatus;
    }

    /**
     * Constructs a <code>RerouteStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RerouteStatus} obj Optional instance to populate.
     * @return {module:model/RerouteStatus} The populated <code>RerouteStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RerouteStatus();

            if (data.hasOwnProperty('incidentNumber')) {
                obj['incidentNumber'] = ApiClient.convertToType(data['incidentNumber'], 'String');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('success')) {
                obj['success'] = ApiClient.convertToType(data['success'], 'Boolean');
            }
            if (data.hasOwnProperty('targetStatus')) {
                obj['targetStatus'] = ApiClient.convertToType(data['targetStatus'], [RerouteTargetStatus]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RerouteStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RerouteStatus</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RerouteStatus.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['incidentNumber'] && !(typeof data['incidentNumber'] === 'string' || data['incidentNumber'] instanceof String)) {
            throw new Error("Expected the field `incidentNumber` to be a primitive type in the JSON string but got " + data['incidentNumber']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        if (data['targetStatus']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['targetStatus'])) {
                throw new Error("Expected the field `targetStatus` to be an array in the JSON data but got " + data['targetStatus']);
            }
            // validate the optional field `targetStatus` (array)
            for (const item of data['targetStatus']) {
                RerouteTargetStatus.validateJSON(item);
            };
        }

        return true;
    }


}

RerouteStatus.RequiredProperties = ["incidentNumber", "success", "targetStatus"];

/**
 * @member {String} incidentNumber
 */
RerouteStatus.prototype['incidentNumber'] = undefined;

/**
 * @member {String} message
 */
RerouteStatus.prototype['message'] = undefined;

/**
 * @member {Boolean} success
 */
RerouteStatus.prototype['success'] = undefined;

/**
 * @member {Array.<module:model/RerouteTargetStatus>} targetStatus
 */
RerouteStatus.prototype['targetStatus'] = undefined;






export default RerouteStatus;

