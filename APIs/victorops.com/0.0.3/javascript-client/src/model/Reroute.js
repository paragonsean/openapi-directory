/**
 * VictorOps
 * This API allows you to interact with the VictorOps platform in various ways. Your account may be limited to a total number of API calls per month. Also, some of these API calls have rate limits.  NOTE: In this documentation when creating a sample curl request (clicking the TRY IT OUT! button), in some API viewing interfaces, the '@' in an email address may be encoded. Please note that the REST endpoints will not process the encoded version. Make sure that the encoded character '%40' is changed to its unencoded form before submitting the curl request. 
 *
 * The version of the OpenAPI document: 0.0.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IncidentTarget from './IncidentTarget';

/**
 * The Reroute model module.
 * @module model/Reroute
 * @version 0.0.3
 */
class Reroute {
    /**
     * Constructs a new <code>Reroute</code>.
     * @alias module:model/Reroute
     * @param incidentNumber {String} 
     * @param targets {Array.<module:model/IncidentTarget>} 
     */
    constructor(incidentNumber, targets) { 
        
        Reroute.initialize(this, incidentNumber, targets);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, incidentNumber, targets) { 
        obj['incidentNumber'] = incidentNumber;
        obj['targets'] = targets;
    }

    /**
     * Constructs a <code>Reroute</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Reroute} obj Optional instance to populate.
     * @return {module:model/Reroute} The populated <code>Reroute</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Reroute();

            if (data.hasOwnProperty('incidentNumber')) {
                obj['incidentNumber'] = ApiClient.convertToType(data['incidentNumber'], 'String');
            }
            if (data.hasOwnProperty('targets')) {
                obj['targets'] = ApiClient.convertToType(data['targets'], [IncidentTarget]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Reroute</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Reroute</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Reroute.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['incidentNumber'] && !(typeof data['incidentNumber'] === 'string' || data['incidentNumber'] instanceof String)) {
            throw new Error("Expected the field `incidentNumber` to be a primitive type in the JSON string but got " + data['incidentNumber']);
        }
        if (data['targets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['targets'])) {
                throw new Error("Expected the field `targets` to be an array in the JSON data but got " + data['targets']);
            }
            // validate the optional field `targets` (array)
            for (const item of data['targets']) {
                IncidentTarget.validateJSON(item);
            };
        }

        return true;
    }


}

Reroute.RequiredProperties = ["incidentNumber", "targets"];

/**
 * @member {String} incidentNumber
 */
Reroute.prototype['incidentNumber'] = undefined;

/**
 * @member {Array.<module:model/IncidentTarget>} targets
 */
Reroute.prototype['targets'] = undefined;






export default Reroute;

