/**
 * VictorOps
 * This API allows you to interact with the VictorOps platform in various ways. Your account may be limited to a total number of API calls per month. Also, some of these API calls have rate limits.  NOTE: In this documentation when creating a sample curl request (clicking the TRY IT OUT! button), in some API viewing interfaces, the '@' in an email address may be encoded. Please note that the REST endpoints will not process the encoded version. Make sure that the encoded character '%40' is changed to its unencoded form before submitting the curl request. 
 *
 * The version of the OpenAPI document: 0.0.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TeamScheduleRollResource model module.
 * @module model/TeamScheduleRollResource
 * @version 0.0.3
 */
class TeamScheduleRollResource {
    /**
     * Constructs a new <code>TeamScheduleRollResource</code>.
     * @alias module:model/TeamScheduleRollResource
     * @param change {Number} The time the period is scheduled to start
     * @param isRoll {Boolean} 
     * @param until {Number} The time the period is scheduled to end
     */
    constructor(change, isRoll, until) { 
        
        TeamScheduleRollResource.initialize(this, change, isRoll, until);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, change, isRoll, until) { 
        obj['change'] = change;
        obj['isRoll'] = isRoll;
        obj['until'] = until;
    }

    /**
     * Constructs a <code>TeamScheduleRollResource</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TeamScheduleRollResource} obj Optional instance to populate.
     * @return {module:model/TeamScheduleRollResource} The populated <code>TeamScheduleRollResource</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TeamScheduleRollResource();

            if (data.hasOwnProperty('change')) {
                obj['change'] = ApiClient.convertToType(data['change'], 'Number');
            }
            if (data.hasOwnProperty('isRoll')) {
                obj['isRoll'] = ApiClient.convertToType(data['isRoll'], 'Boolean');
            }
            if (data.hasOwnProperty('oncall')) {
                obj['oncall'] = ApiClient.convertToType(data['oncall'], 'String');
            }
            if (data.hasOwnProperty('until')) {
                obj['until'] = ApiClient.convertToType(data['until'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TeamScheduleRollResource</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TeamScheduleRollResource</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TeamScheduleRollResource.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['oncall'] && !(typeof data['oncall'] === 'string' || data['oncall'] instanceof String)) {
            throw new Error("Expected the field `oncall` to be a primitive type in the JSON string but got " + data['oncall']);
        }

        return true;
    }


}

TeamScheduleRollResource.RequiredProperties = ["change", "isRoll", "until"];

/**
 * The time the period is scheduled to start
 * @member {Number} change
 */
TeamScheduleRollResource.prototype['change'] = undefined;

/**
 * @member {Boolean} isRoll
 */
TeamScheduleRollResource.prototype['isRoll'] = undefined;

/**
 * The user scheduled on call (if any)
 * @member {String} oncall
 */
TeamScheduleRollResource.prototype['oncall'] = undefined;

/**
 * The time the period is scheduled to end
 * @member {Number} until
 */
TeamScheduleRollResource.prototype['until'] = undefined;






export default TeamScheduleRollResource;

