/**
 * NFL v3 Scores
 * NFL schedules, scores, odds, weather, and news API.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DepthChart from './DepthChart';

/**
 * The TeamDepthChart model module.
 * @module model/TeamDepthChart
 * @version 1.0
 */
class TeamDepthChart {
    /**
     * Constructs a new <code>TeamDepthChart</code>.
     * @alias module:model/TeamDepthChart
     */
    constructor() { 
        
        TeamDepthChart.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TeamDepthChart</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TeamDepthChart} obj Optional instance to populate.
     * @return {module:model/TeamDepthChart} The populated <code>TeamDepthChart</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TeamDepthChart();

            if (data.hasOwnProperty('Defense')) {
                obj['Defense'] = ApiClient.convertToType(data['Defense'], [DepthChart]);
            }
            if (data.hasOwnProperty('Offense')) {
                obj['Offense'] = ApiClient.convertToType(data['Offense'], [DepthChart]);
            }
            if (data.hasOwnProperty('SpecialTeams')) {
                obj['SpecialTeams'] = ApiClient.convertToType(data['SpecialTeams'], [DepthChart]);
            }
            if (data.hasOwnProperty('TeamID')) {
                obj['TeamID'] = ApiClient.convertToType(data['TeamID'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TeamDepthChart</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TeamDepthChart</code>.
     */
    static validateJSON(data) {
        if (data['Defense']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Defense'])) {
                throw new Error("Expected the field `Defense` to be an array in the JSON data but got " + data['Defense']);
            }
            // validate the optional field `Defense` (array)
            for (const item of data['Defense']) {
                DepthChart.validateJSON(item);
            };
        }
        if (data['Offense']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['Offense'])) {
                throw new Error("Expected the field `Offense` to be an array in the JSON data but got " + data['Offense']);
            }
            // validate the optional field `Offense` (array)
            for (const item of data['Offense']) {
                DepthChart.validateJSON(item);
            };
        }
        if (data['SpecialTeams']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['SpecialTeams'])) {
                throw new Error("Expected the field `SpecialTeams` to be an array in the JSON data but got " + data['SpecialTeams']);
            }
            // validate the optional field `SpecialTeams` (array)
            for (const item of data['SpecialTeams']) {
                DepthChart.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/DepthChart>} Defense
 */
TeamDepthChart.prototype['Defense'] = undefined;

/**
 * @member {Array.<module:model/DepthChart>} Offense
 */
TeamDepthChart.prototype['Offense'] = undefined;

/**
 * @member {Array.<module:model/DepthChart>} SpecialTeams
 */
TeamDepthChart.prototype['SpecialTeams'] = undefined;

/**
 * @member {Number} TeamID
 */
TeamDepthChart.prototype['TeamID'] = undefined;






export default TeamDepthChart;

