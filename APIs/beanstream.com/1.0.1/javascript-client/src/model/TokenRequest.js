/**
 * Beanstream Payments
 * https://www.beanstream.com/api/v1
 *
 * The version of the OpenAPI document: 1.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TokenRequest model module.
 * @module model/TokenRequest
 * @version 1.0.1
 */
class TokenRequest {
    /**
     * Constructs a new <code>TokenRequest</code>.
     * @alias module:model/TokenRequest
     * @param cvd {String} a 3 to 4 digit representation of CVD/CDD. This is the number usually found on the back of the credit card.
     * @param expiryMonth {String} a 2 digit representation of the expiry month. For example March is 03.
     * @param expiryYear {String} a 2 digit representation of the expiry year. For example 2016 is 16.
     * @param number {String} The credit card number
     */
    constructor(cvd, expiryMonth, expiryYear, number) { 
        
        TokenRequest.initialize(this, cvd, expiryMonth, expiryYear, number);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cvd, expiryMonth, expiryYear, number) { 
        obj['cvd'] = cvd;
        obj['expiry_month'] = expiryMonth;
        obj['expiry_year'] = expiryYear;
        obj['number'] = number;
    }

    /**
     * Constructs a <code>TokenRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokenRequest} obj Optional instance to populate.
     * @return {module:model/TokenRequest} The populated <code>TokenRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokenRequest();

            if (data.hasOwnProperty('cvd')) {
                obj['cvd'] = ApiClient.convertToType(data['cvd'], 'String');
            }
            if (data.hasOwnProperty('expiry_month')) {
                obj['expiry_month'] = ApiClient.convertToType(data['expiry_month'], 'String');
            }
            if (data.hasOwnProperty('expiry_year')) {
                obj['expiry_year'] = ApiClient.convertToType(data['expiry_year'], 'String');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokenRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokenRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TokenRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cvd'] && !(typeof data['cvd'] === 'string' || data['cvd'] instanceof String)) {
            throw new Error("Expected the field `cvd` to be a primitive type in the JSON string but got " + data['cvd']);
        }
        // ensure the json data is a string
        if (data['expiry_month'] && !(typeof data['expiry_month'] === 'string' || data['expiry_month'] instanceof String)) {
            throw new Error("Expected the field `expiry_month` to be a primitive type in the JSON string but got " + data['expiry_month']);
        }
        // ensure the json data is a string
        if (data['expiry_year'] && !(typeof data['expiry_year'] === 'string' || data['expiry_year'] instanceof String)) {
            throw new Error("Expected the field `expiry_year` to be a primitive type in the JSON string but got " + data['expiry_year']);
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }

        return true;
    }


}

TokenRequest.RequiredProperties = ["cvd", "expiry_month", "expiry_year", "number"];

/**
 * a 3 to 4 digit representation of CVD/CDD. This is the number usually found on the back of the credit card.
 * @member {String} cvd
 */
TokenRequest.prototype['cvd'] = undefined;

/**
 * a 2 digit representation of the expiry month. For example March is 03.
 * @member {String} expiry_month
 */
TokenRequest.prototype['expiry_month'] = undefined;

/**
 * a 2 digit representation of the expiry year. For example 2016 is 16.
 * @member {String} expiry_year
 */
TokenRequest.prototype['expiry_year'] = undefined;

/**
 * The credit card number
 * @member {String} number
 */
TokenRequest.prototype['number'] = undefined;






export default TokenRequest;

