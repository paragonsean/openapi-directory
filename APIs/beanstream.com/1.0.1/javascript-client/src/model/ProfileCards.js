/**
 * Beanstream Payments
 * https://www.beanstream.com/api/v1
 *
 * The version of the OpenAPI document: 1.0.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ProfileCards model module.
 * @module model/ProfileCards
 * @version 1.0.1
 */
class ProfileCards {
    /**
     * Constructs a new <code>ProfileCards</code>.
     * @alias module:model/ProfileCards
     */
    constructor() { 
        
        ProfileCards.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ProfileCards</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProfileCards} obj Optional instance to populate.
     * @return {module:model/ProfileCards} The populated <code>ProfileCards</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProfileCards();

            if (data.hasOwnProperty('card_id')) {
                obj['card_id'] = ApiClient.convertToType(data['card_id'], 'String');
            }
            if (data.hasOwnProperty('card_type')) {
                obj['card_type'] = ApiClient.convertToType(data['card_type'], 'String');
            }
            if (data.hasOwnProperty('expiry_month')) {
                obj['expiry_month'] = ApiClient.convertToType(data['expiry_month'], 'String');
            }
            if (data.hasOwnProperty('expiry_year')) {
                obj['expiry_year'] = ApiClient.convertToType(data['expiry_year'], 'String');
            }
            if (data.hasOwnProperty('function')) {
                obj['function'] = ApiClient.convertToType(data['function'], 'String');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProfileCards</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProfileCards</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['card_id'] && !(typeof data['card_id'] === 'string' || data['card_id'] instanceof String)) {
            throw new Error("Expected the field `card_id` to be a primitive type in the JSON string but got " + data['card_id']);
        }
        // ensure the json data is a string
        if (data['card_type'] && !(typeof data['card_type'] === 'string' || data['card_type'] instanceof String)) {
            throw new Error("Expected the field `card_type` to be a primitive type in the JSON string but got " + data['card_type']);
        }
        // ensure the json data is a string
        if (data['expiry_month'] && !(typeof data['expiry_month'] === 'string' || data['expiry_month'] instanceof String)) {
            throw new Error("Expected the field `expiry_month` to be a primitive type in the JSON string but got " + data['expiry_month']);
        }
        // ensure the json data is a string
        if (data['expiry_year'] && !(typeof data['expiry_year'] === 'string' || data['expiry_year'] instanceof String)) {
            throw new Error("Expected the field `expiry_year` to be a primitive type in the JSON string but got " + data['expiry_year']);
        }
        // ensure the json data is a string
        if (data['function'] && !(typeof data['function'] === 'string' || data['function'] instanceof String)) {
            throw new Error("Expected the field `function` to be a primitive type in the JSON string but got " + data['function']);
        }
        // ensure the json data is a string
        if (data['number'] && !(typeof data['number'] === 'string' || data['number'] instanceof String)) {
            throw new Error("Expected the field `number` to be a primitive type in the JSON string but got " + data['number']);
        }

        return true;
    }


}



/**
 * 1-indexed card ID. Total number of cards allowed is configured in your merchant account.
 * @member {String} card_id
 */
ProfileCards.prototype['card_id'] = undefined;

/**
 * MC, VI etc. characters(2)
 * @member {String} card_type
 */
ProfileCards.prototype['card_type'] = undefined;

/**
 * digits(2)
 * @member {String} expiry_month
 */
ProfileCards.prototype['expiry_month'] = undefined;

/**
 * digits(2)
 * @member {String} expiry_year
 */
ProfileCards.prototype['expiry_year'] = undefined;

/**
 * Will say DEF for the default card
 * @member {String} function
 */
ProfileCards.prototype['function'] = undefined;

/**
 * digits(20)
 * @member {String} number
 */
ProfileCards.prototype['number'] = undefined;






export default ProfileCards;

